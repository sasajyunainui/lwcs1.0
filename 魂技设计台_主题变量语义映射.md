# 魂技设计台主题变量语义映射

> 版本：2026-07-16
> 用途：约束魂技设计台只消费状态栏主题，不在组件内复制主题判断或固定色板。
## 1. 语义令牌

| 设计台令牌 | 来源 | 用途 | 禁止用途 |
| --- | --- | --- | --- |
| `shell` | `--暗场底板` 与 `--主题底色` | 设计台外壳、上下文栏 | 输入控件正文背景 |
| `surface` | `--暗场卡片` | 当前页编辑表面 | 大面积渐变 |
| `control` | `--暗场底板强` | 输入、按钮、分段控件 | 透明玻璃 |
| `popover` | `--暗场浮层` | Teleport 浮层、帮助层 | 页面主体 |
| `text` | `--文字主色` 或 `--主题高光` | 正文、输入、标题 | 低对比装饰 |
| `muted` | `--文字弱色` 与 `--文字主色` 的高对比混合 | 辅助说明、单位 | 必填标签和错误文字 |
| `accentPrimary` | `--主题主色` | 当前页、激活边线、关系线 | 大面积正文背景 |
| `accentSecondary` | `--主题辅色` 或主题主色的语义混合 | 次级关系、轻量提示 | 正文文字 |
| `accentText` | `--主题高光` | 激活标题和关键数字 | 所有普通文本 |
| `focus` | `--主题高光`，必要时与 `--主题主色` 混合 | 键盘焦点环 | 唯一错误提示 |
| `border` | `--边线颜色` 或状态栏弱边线 | 分隔、控件边界 | 代替焦点 |
| `danger` | `--状态危险色` | 错误、删除、超预算 | 普通装饰 |
| `warning` | `--状态警告色` 或主题高光 | 警告、自动规整 | 正文 |
| `success` | `--状态成功色` 或主题高光 | 校验通过、保存成功 | 未验证状态 |
| `fontFamily` | `--主题字体` | 所有设计台文本 | 组件内另引 Google Font |
| `headingWeight` | `--主题字重` | 标题、总计、页签 | 所有正文 |
| `bodyWeight` | `--主题字重` 的可读回退 | 正文和输入 | 过轻导致失真 |
| `radius` | `--卡片圆角` | 控件和区块圆角 | 组件内硬编码主题分支 |
| `cut` | `--面板裁切` | 仅外壳或主题允许的边角 | 影响文字可读性的切角 |

## 2. 绑定规则

- Vue 模板和组件 CSS 只使用 `.skill-designer-host` 内的语义令牌。
- 组件内禁止出现 `theme === ...`、主题名称选择器和五套 DOM 分支。
- 主题适配只改变外壳材质、字体、圆角/切角、激活态、焦点态、关系线和少量强调色。
- 普通文字必须使用 `text`，不能直接使用 `accentPrimary`。
- `accentPrimary`、`accentText`、`focus`、`danger`、`warning`、`success` 必须保持独立。
- 输入和正文表面使用实体高不透明度背景，不使用透明玻璃、文字辉光或大面积渐变。
- Teleport 浮层打开时复制宿主的完整语义令牌和当前主题标识。

## 3. 可读性门槛

- 普通文字与输入文字：至少 4.5:1。
- 控件边界、焦点环、图标状态：至少 3:1。
- 颜色不是唯一状态信息；错误、警告、成功都必须同时有文字或图标语义。
- 金龙王的暗红只用于关系线、填充和危险背景，正文与焦点使用象牙白/金色。
- 银龙王的轻字重只用于标题和辅助层，输入内容保持稳定字重。
