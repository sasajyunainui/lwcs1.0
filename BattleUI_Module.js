/* BattleUI_Module.js - 战斗终端系统 (JS 模块版) */

(() => {

class BattleUIComponent {
  constructor(container, snapshot, options = {}) {
    this.container = container;
    this.snapshot = snapshot;
    this.options = options;
    this.recordPortalNode = null;
    this.syncRecordPortalPosition = null;
    this.cleanupRecordPortalPosition = null;
    this.initDOM();
    this.initEngine();
  }

  initDOM() {
    if (!this.container.querySelector('.battle-module-scope')) {
      throw new Error('battle_ui_markup_missing');
    }
  }

  updateData(newSnapshot) {
    this.snapshot = newSnapshot;
    if (this.syncFromBattleEngine) this.syncFromBattleEngine();
    if (this.syncRecordPortalPosition) this.syncRecordPortalPosition();
  }

  destroy() {
    try {
      const 状态 = window.BattleUI?.state;
      if (状态?.智能警报弱化计时器) {
        clearTimeout(状态.智能警报弱化计时器);
        状态.智能警报弱化计时器 = null;
      }
    } catch (错误) {}
    try {
      if (typeof this.cleanupRecordPortalPosition === 'function') this.cleanupRecordPortalPosition();
      if (this.recordPortalNode?.remove) this.recordPortalNode.remove();
      this.recordPortalNode = null;
      this.syncRecordPortalPosition = null;
      this.cleanupRecordPortalPosition = null;
    } catch (错误) {}
    this.container.innerHTML = '';
  }

  initEngine() {
    const component = this;
    const snapshot = this.snapshot;
    const _options = this.options;
    const wrapperElement = this.container.querySelector('.battle-module-scope');
    if (!wrapperElement) throw new Error('battle_ui_markup_missing');
    const root = typeof globalThis !== 'undefined' ? globalThis : window;
    const globalDocument = root.document && typeof root.document.querySelector === 'function' ? root.document : null;
    const document = wrapperElement;
    const byId = id => wrapperElement.querySelector(`#${id}`);
    function 同步战斗记录终端主题(node = component.recordPortalNode) {
      if (!node || !component.container || typeof root.getComputedStyle !== 'function') return;
      const 样式 = root.getComputedStyle(component.container);
      [
        '--主题主色',
        '--资源亮色',
        '--文字主色',
        '--文字弱色',
        '--战斗主色',
        '--战斗强调色',
        '--战斗危险色',
        '--战斗警戒色',
        '--战斗玻璃底',
        '--战斗玻璃浮层',
        '--战斗边缘高光',
        '--战斗暗侧阴影',
        '--战斗扫描线',
        '--战斗环境光',
        '--战斗圆角',
      ].forEach(变量名 => {
        const 值 = 样式.getPropertyValue(变量名).trim();
        if (值) node.style.setProperty(变量名, 值);
      });
    }
    function 同步战斗记录终端位置() {
      const node = component.recordPortalNode;
      if (!node?.isConnected || !component.container?.getBoundingClientRect) return;
      同步战斗记录终端主题(node);
      const rect = component.container.getBoundingClientRect();
      const viewportWidth = Math.max(Number(root.innerWidth) || 0, Number(globalDocument?.documentElement?.clientWidth) || 0);
      const viewportHeight = Math.max(Number(root.innerHeight) || 0, Number(globalDocument?.documentElement?.clientHeight) || 0);
      if (!rect.width || !rect.height || !viewportWidth || !viewportHeight) return;
      const gap = 12;
      const margin = 10;
      const top = Math.max(margin, rect.top);
      const maxHeight = Math.max(120, viewportHeight - top - margin);
      const height = Math.min(rect.height, maxHeight);
      const sideSpace = viewportWidth - rect.right - gap - margin;
      const viewportSafeWidth = Math.max(300, viewportWidth - margin * 2);
      const width = Math.min(860, viewportSafeWidth, Math.max(380, sideSpace));
      const left = sideSpace >= 340 ? rect.right + gap : Math.max(margin, viewportWidth - margin - width);
      node.style.setProperty('--战斗记录外置-top', `${Math.round(top)}px`);
      node.style.setProperty('--战斗记录外置-left', `${Math.round(left)}px`);
      node.style.setProperty('--战斗记录外置-width', `${Math.round(width)}px`);
      node.style.setProperty('--战斗记录外置-height', `${Math.round(height)}px`);
    }
    component.syncRecordPortalPosition = 同步战斗记录终端位置;
    function 读取战斗记录终端节点() {
      if (component.recordPortalNode?.isConnected) return component.recordPortalNode;
      const 本地节点 = wrapperElement.querySelector('#ui-battle-record-terminal');
      if (!本地节点) return null;
      if (globalDocument?.body) {
        globalDocument.querySelectorAll('#ui-battle-record-terminal.battle-record-terminal--portal').forEach(node => {
          if (node !== 本地节点) node.remove();
        });
        本地节点.classList.add('battle-record-terminal--portal');
        本地节点.setAttribute('tabindex', '0');
        本地节点.setAttribute('aria-label', '战斗记录');
        globalDocument.body.appendChild(本地节点);
        component.recordPortalNode = 本地节点;
        同步战斗记录终端主题(本地节点);
        同步战斗记录终端位置();
        if (!component.cleanupRecordPortalPosition) {
          const 处理位置变化 = () => 同步战斗记录终端位置();
          let 尺寸观察器 = null;
          root.addEventListener?.('resize', 处理位置变化);
          globalDocument.addEventListener?.('scroll', 处理位置变化, true);
          if (typeof root.ResizeObserver === 'function') {
            尺寸观察器 = new root.ResizeObserver(处理位置变化);
            尺寸观察器.observe(component.container);
          }
          component.cleanupRecordPortalPosition = () => {
            root.removeEventListener?.('resize', 处理位置变化);
            globalDocument.removeEventListener?.('scroll', 处理位置变化, true);
            尺寸观察器?.disconnect();
          };
        }
      }
      return 本地节点;
    }
    const 战斗提交模式存储键 = 'lwcs_battle_submit_mode';
    const 战斗提交模式列表 = ['auto', 'manual', 'free_narrative'];
    const 战斗提交模式标签 = {
      auto: '自动',
      manual: '手动',
      free_narrative: '自由',
    };
    const 自动续推默认设置 = Object.freeze({
      maxRounds: 4,
      stopDamagePercent: 25,
      continueChancePercent: 100,
    });
    function 夹取自动续推数值(值, 回退, 下限, 上限) {
      const 数值 = Number(值);
      if (!Number.isFinite(数值)) return 回退;
      return Math.min(上限, Math.max(下限, 数值));
    }
    function 规范化自动续推设置(配置 = {}, mode = 'multi_round') {
      const 来源 = 配置 && typeof 配置 === 'object' ? 配置 : {};
      const 阈值原始值 =
        来源.stopDamagePercent ??
        来源.autoContinueStopDamagePercent ??
        来源.停推阈值百分比 ??
        (Number.isFinite(Number(来源.stopDamageRatio)) ? Number(来源.stopDamageRatio) * 100 : undefined);
      const 概率原始值 =
        来源.continueChancePercent ??
        来源.autoContinueChancePercent ??
        来源.低烈度续推概率百分比 ??
        (Number.isFinite(Number(来源.continueChance)) ? Number(来源.continueChance) * 100 : undefined);
      const 最大回合 = mode === 'multi_round'
        ? Math.round(夹取自动续推数值(
            来源.maxRounds ?? 来源.autoContinueMaxRounds ?? 来源.最大回合,
            自动续推默认设置.maxRounds,
            1,
            20,
          ))
        : 1;
      const 停推阈值百分比 = 夹取自动续推数值(阈值原始值, 自动续推默认设置.stopDamagePercent, 0, 100);
      const 低烈度续推概率百分比 = 夹取自动续推数值(概率原始值, 自动续推默认设置.continueChancePercent, 0, 100);
      return {
        maxRounds: 最大回合,
        stopDamagePercent: 停推阈值百分比,
        stopDamageRatio: 停推阈值百分比 / 100,
        continueChancePercent: 低烈度续推概率百分比,
        continueChance: 低烈度续推概率百分比 / 100,
      };
    }
    function 规范化战斗提交模式(值) {
      const 文本 = String(值 || '').trim();
      return 战斗提交模式列表.includes(文本) ? 文本 : 'manual';
    }
    function 读取战斗提交模式() {
      try {
        return 规范化战斗提交模式(root.localStorage?.getItem(战斗提交模式存储键));
      } catch (error) {
        return 'manual';
      }
    }
    function 写入战斗提交模式(模式) {
      const 下个模式 = 规范化战斗提交模式(模式);
      try {
        root.localStorage?.setItem(战斗提交模式存储键, 下个模式);
      } catch (error) {}
      return 下个模式;
    }
    function 同步战斗提交模式控件() {
      const 当前模式 = 读取战斗提交模式();
      const 标签节点 = byId('ui-battle-submit-mode-label');
      if (标签节点) 标签节点.textContent = 战斗提交模式标签[当前模式] || '手动';
      document.querySelectorAll('#ui-battle-submit-mode-group [data-submit-mode]').forEach(button => {
        button.classList.toggle('active', button.dataset.submitMode === 当前模式);
        button.setAttribute('aria-selected', button.dataset.submitMode === 当前模式 ? 'true' : 'false');
      });
      if (typeof 同步战斗下拉文本 === 'function') 同步战斗下拉文本(byId('ui-battle-submit-mode-group'), 当前模式, 'submit-mode');
      return 当前模式;
    }
    const resolveSharedSkillMechanismRegistry = () => {
      if (root.__LWCS_SKILL_MECHANISM_REGISTRY__ && typeof root.__LWCS_SKILL_MECHANISM_REGISTRY__ === 'object') {
        return root.__LWCS_SKILL_MECHANISM_REGISTRY__;
      }
      try {
        if (window.parent && window.parent !== window && window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__ && typeof window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__ === 'object') {
          return window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__;
        }
      } catch (error) {}
      try {
        if (window.top && window.top !== window && window.top.__LWCS_SKILL_MECHANISM_REGISTRY__ && typeof window.top.__LWCS_SKILL_MECHANISM_REGISTRY__ === 'object') {
          return window.top.__LWCS_SKILL_MECHANISM_REGISTRY__;
        }
      } catch (error) {}
      return null;
    };
    const SHARED_SKILL_MECHANISM_REGISTRY = resolveSharedSkillMechanismRegistry();
    if (!root.__LWCS_SKILL_MECHANISM_REGISTRY__ && SHARED_SKILL_MECHANISM_REGISTRY) {
      root.__LWCS_SKILL_MECHANISM_REGISTRY__ = SHARED_SKILL_MECHANISM_REGISTRY;
    }
    root.__LWCS_BATTLE_RUNTIME_REGISTRY_SOURCE__ = SHARED_SKILL_MECHANISM_REGISTRY ? 'shared' : 'fallback';
    root.__LWCS_BATTLE_RUNTIME_REGISTRY_SIZE__ =
      SHARED_SKILL_MECHANISM_REGISTRY?.机制定义 && typeof SHARED_SKILL_MECHANISM_REGISTRY.机制定义 === 'object'
        ? Object.keys(SHARED_SKILL_MECHANISM_REGISTRY.机制定义).length
        : 0;
    const 战斗来源类别上下文表 = new WeakMap();
    const 写入战斗来源类别上下文 = (skill, 上下文 = {}) => {
      if (!skill || typeof skill !== 'object') return skill;
      const 来源类别 = String(上下文?.来源类别 || 上下文?.category || 上下文?.来源分类 || '').trim();
      const 来源明细 = String(上下文?.来源明细 || 上下文?.source_detail || 上下文?.detail || 来源类别 || '').trim();
      const 魂环路径 = Array.isArray(上下文?.魂环路径) ? 上下文.魂环路径.map(片段 => String(片段)) : [];
      const 魂技槽位 = String(上下文?.魂技槽位 || 上下文?.技能键 || '').trim();
      const 当前 = 战斗来源类别上下文表.get(skill) || {};
      战斗来源类别上下文表.set(skill, {
        ...当前,
        ...(来源类别 ? { 来源类别 } : {}),
        ...(来源明细 ? { 来源明细 } : {}),
        ...(魂环路径.length ? { 魂环路径 } : {}),
        ...(魂技槽位 ? { 魂技槽位 } : {}),
      });
      if (来源类别) skill.__战斗来源类别 = 来源类别;
      if (来源明细) skill.__战斗来源明细 = 来源明细;
      if (魂环路径.length) skill.__魂环路径 = [...魂环路径];
      if (魂技槽位) skill.__魂技槽位 = 魂技槽位;
      return skill;
    };
    const 读取战斗来源类别上下文 = (skill = {}, 回退类别 = '魂技') => {
      const 运行态 = skill && typeof skill === 'object' ? 战斗来源类别上下文表.get(skill) || {} : {};
      const 来源类别 = String(运行态.来源类别 || skill?.__战斗来源类别 || 回退类别 || '魂技').trim() || '魂技';
      const 来源明细 = String(运行态.来源明细 || skill?.__战斗来源明细 || 来源类别).trim() || 来源类别;
      const 魂环路径 = Array.isArray(运行态.魂环路径)
        ? 运行态.魂环路径
        : Array.isArray(skill?.__魂环路径)
          ? skill.__魂环路径
          : [];
      const 魂技槽位 = String(运行态.魂技槽位 || skill?.__魂技槽位 || '').trim();
      return { 来源类别, 来源明细, 魂环路径: 魂环路径.map(片段 => String(片段)), 魂技槽位 };
    };
    const 规范化战斗来源类别 = (来源 = '', 回退 = '魂技') => {
      const 文本 = String(来源 || '').trim();
      if (['武魂融合技', '自创魂技', '血脉技能', '气血魂技', '魂骨技能', '装备技能', '物品技能', '精神领域', '复制技能', '状态授予技能', '战术'].includes(文本)) return 文本;
      return String(回退 || '魂技').trim() || '魂技';
    };
    const 读取战斗动作来源上下文 = (动作 = {}, skill = null, 回退类别 = '魂技') => {
      const 来源类别 = String(动作?.category || 动作?.来源类别 || 读取战斗来源类别上下文(skill, 回退类别).来源类别 || 回退类别 || '魂技').trim() || '魂技';
      const 来源明细 = String(动作?.source_detail || 动作?.来源明细 || 读取战斗来源类别上下文(skill, 来源类别).来源明细 || 来源类别).trim() || 来源类别;
      return { 来源类别, 来源明细 };
    };
    const 战斗核心原型兜底表 = {
      伤害结算: { 原型: '伤害结算', 默认方向: '敌方', 允许字段: ['目标', '生效方式', '威力倍率', '伤害类型', '防御穿透', '攻击段数'] },
      护盾变化: { 原型: '护盾变化', 默认方向: '友方', 允许字段: ['目标', '生效方式', '数值', '护盾值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度'] } } },
      属性修正: { 原型: '属性修正', 默认方向: '友方', 允许字段: ['目标', '生效方式', '属性', '数值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度'] } } },
      判定修正: { 原型: '判定修正', 默认方向: '敌方', 允许字段: ['目标', '生效方式', '判定', '数值', '持续回合', '打断效果', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['成功率'] } } },
      状态施加: { 原型: '状态施加', 默认方向: '敌方', 允许字段: ['目标', '生效方式', '状态', '数值', '副数值', '持续回合', '触发限制', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['成功率'] } } },
      状态移除: { 原型: '状态移除', 允许字段: ['目标', '生效方式', '状态', '数量', '数值', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['成功率'] } } },
      资源变化: { 原型: '资源变化', 允许字段: ['目标', '生效方式', '资源', '数值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度'] } } },
      资源转移: { 原型: '资源转移', 允许字段: ['目标', '生效方式', '资源', '数值', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度'] } } },
      资源锁定: { 原型: '资源锁定', 允许字段: ['目标', '生效方式', '资源', '数值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['成功率'] } } },
      结算修正: { 原型: '结算修正', 允许字段: ['目标', '生效方式', '结算', '数值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度', '消耗', '前摇'] } } },
      时窗修正: { 原型: '时窗修正', 允许字段: ['目标', '生效方式', '调整方式', '调整字段', '调整回合', '调整tick', '结算倍率', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['持续时间', '效果强度'] } } },
      机制抹消: { 原型: '机制抹消', 允许字段: ['目标', '生效方式', '抹消对象', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['成功率'] } } },
      决策干扰: { 原型: '决策干扰', 允许字段: ['目标', '生效方式', '干扰', '数值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['成功率'] } } },
      召唤生成: { 原型: '召唤生成', 允许字段: ['目标', '生效方式', '召唤物名称', '召唤数量', '数量'] },
      规则防御: { 原型: '规则防御', 允许字段: ['目标', '生效方式', '规则', '数值'] },
      位移执行: { 原型: '位移执行', 允许字段: ['目标', '生效方式', '位移类型', '位移对象', '距离', '数值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度'] } } },
      复制执行: { 原型: '复制执行', 允许字段: ['目标', '生效方式', '复制类型', '数值', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度'] } } },
      机制授予: { 原型: '机制授予', 允许字段: ['目标', '生效方式', '授予效果', '持续回合', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['效果强度'] } } },
      时光回溯: { 原型: '时光回溯', 允许字段: ['目标', '生效方式', '发动方式', '驱动属性', '影响方向'], 字段定义: { 影响方向: { 选项: ['成功率'] } } },
    };
    const BATTLE_PROTOTYPE_REGISTRY = Object.freeze({ ...战斗核心原型兜底表, ...(SHARED_SKILL_MECHANISM_REGISTRY?.原型定义 || {}) });
    const 战斗延迟回合原型集合 = new Set(['伤害结算', '护盾变化', '属性修正', '状态施加']);
    const 战斗原型允许延迟回合 = 原型 => 战斗延迟回合原型集合.has(String(原型 || '').trim());
    const BATTLE_PROTOTYPE_ARRAY_FIELDS = Object.freeze(['原型', '属性', '资源', '判定', '结算', '状态', '类型']);
    const lodashGet =
      root._ && typeof root._.get === 'function'
        ? root._.get.bind(root._)
        : (obj, path, fallback) => {
            const normalized = String(path || '')
              .split('.')
              .filter(Boolean);
            let cursor = obj;
            for (const seg of normalized) {
              if (cursor == null || typeof cursor !== 'object' || !(seg in cursor)) return fallback;
              cursor = cursor[seg];
            }
            return cursor === undefined ? fallback : cursor;
          };

    function normalizeStatPath(path) {
      return String(path || '')
        .replace(/^stat_data\./, '')
        .replace(/^\.+/, '');
    }

    function getTavernHelperRuntime() {
      const host = getHostWindow();
      if (root.TavernHelper && typeof root.TavernHelper.getVariables === 'function') return root.TavernHelper;
      if (host?.TavernHelper && typeof host.TavernHelper.getVariables === 'function') return host.TavernHelper;
      return null;
    }

    function normalizeVariablesEnvelope(rawVars) {
      if (!rawVars || typeof rawVars !== 'object') return {};
      const candidates = [
        rawVars.stat_data,
        rawVars.data?.stat_data,
        rawVars.variables?.stat_data,
        rawVars.payload?.stat_data,
        rawVars.root?.stat_data,
        rawVars.data,
        rawVars.variables,
        rawVars.payload,
        rawVars.root,
        rawVars,
      ];
      const statData = candidates.find(item => item && typeof item === 'object' && (item.char || item.world || item.sys));
      return statData ? { ...rawVars, stat_data: statData } : rawVars;
    }

    const SOUL_TOWER_MAX_AGE = 30;
    const SOUL_TOWER_TEAM_LIMIT = 7;
    const SOUL_TOWER_MAX_AGE_GAP = 3;
    const SOUL_TOWER_TOTAL_FLOORS = 108;
    const 试炼地点前缀 = '试炼-';
    const 升灵台退出地点 = '传灵塔入口';
    const 魂灵塔退出地点 = '史莱克城传灵塔总部';
    const SOUL_TOWER_LAYER_RULES = Object.freeze([
      Object.freeze({
        key: 'thousand',
        label: '千年魂灵区',
        rewardTier: '千年',
        gateStart: 1,
        gateEnd: 18,
        minAge: 1000,
        maxAge: 9999,
        qualitySteps: Object.freeze(['C', 'B', 'A']),
      }),
      Object.freeze({
        key: 'ten_thousand',
        label: '万年魂灵区',
        rewardTier: '万年',
        gateStart: 19,
        gateEnd: 36,
        minAge: 10000,
        maxAge: 99999,
        qualitySteps: Object.freeze(['B', 'A', 'S']),
      }),
      Object.freeze({
        key: 'pre_beast',
        label: '万年以上魂灵区',
        rewardTier: '万年以上',
        gateStart: 37,
        gateEnd: 99,
        minAge: 10000,
        maxAge: 99999,
        qualitySteps: Object.freeze(['A', 'S']),
      }),
      Object.freeze({
        key: 'beast',
        label: '凶兽魂灵区',
        rewardTier: '凶兽级',
        gateStart: 100,
        gateEnd: 108,
        minAge: 100000,
        maxAge: 200000,
        qualitySteps: Object.freeze(['S+']),
      }),
    ]);
    const SOUL_TOWER_GUARDIAN_SPECIES_POOL = Object.freeze([
      '龙类',
      '蛛类',
      '熊类',
      '植物系',
      '海魂兽',
      '鸟类',
      '猫科',
      '蛇类',
    ]);
    const SOUL_SPIRIT_QUALITY_VALUES = Object.freeze(['F', 'D', 'C', 'B', 'A', 'S', 'S+']);
    const SOUL_SPIRIT_QUALITY_MULTIPLIER_MAP = Object.freeze({
      F: 0.82,
      D: 0.9,
      C: 1.0,
      B: 1.08,
      A: 1.18,
      S: 1.32,
      'S+': 1.48,
    });
    const SOUL_SPIRIT_QUALITY_LEVEL_OFFSET_MAP = Object.freeze({
      F: -6,
      D: -3,
      C: 0,
      B: 2,
      A: 5,
      S: 8,
      'S+': 12,
    });

    function normalizeSoulSpiritQuality(value = '') {
      const text = String(value || '')
        .trim()
        .toUpperCase()
        .replace('＋', '+')
        .replace(/\s+/g, '');
      return SOUL_SPIRIT_QUALITY_VALUES.includes(text) ? text : '';
    }

    function 构建魂灵塔试炼地点(层数 = 1) {
      const floor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(Number(层数) || 1)));
      return `${试炼地点前缀}魂灵塔-第${floor}层`;
    }

    function 解析角色键(角色名 = '') {
      const safeName = String(角色名 || '').trim();
      if (!safeName) return '';
      const 角色表 = getMvuValue('char', {}) || {};
      if (角色表 && typeof 角色表 === 'object' && Object.prototype.hasOwnProperty.call(角色表, safeName)) {
        return safeName;
      }
      const 匹配项 = Object.entries(角色表).find(([键, 数据]) => {
        const 显示名 = String(数据?.name || 数据?.base?.name || 键 || '').trim();
        return 显示名 === safeName;
      });
      return 匹配项 ? String(匹配项[0] || '').trim() : safeName;
    }

    function 构建角色位置补丁(角色名 = '', 位置 = '') {
      const safeName = String(角色名 || '').trim();
      const safeLocation = String(位置 || '').trim();
      if (!safeName || !safeLocation) return null;
      const charKey = 解析角色键(safeName);
      if (!charKey) return null;
      return {
        op: 'add',
        path: `/char/${escapeJsonPointerSegment(charKey)}/状态/位置`,
        value: safeLocation,
      };
    }

    function 构建试炼状态补丁(试炼状态 = '') {
      return {
        op: 'add',
        path: '/world/战斗/试炼状态',
        value: String(试炼状态 || '').trim(),
      };
    }

    function createEmptySoulTowerDiscountSpiritRecord() {
      return {
        层数: 0,
        名称: '',
        标准物种: '',
        年限: 0,
        品质: '',
        已使用: false,
      };
    }

    function normalizeSoulTowerDiscountSpiritRecord(record = {}) {
      if (!record || typeof record !== 'object' || Array.isArray(record)) return createEmptySoulTowerDiscountSpiritRecord();
      const next = createEmptySoulTowerDiscountSpiritRecord();
      next.层数 = Math.max(0, Math.floor(Number(record.层数 || 0)));
      next.名称 = String(record.名称 || '').trim();
      next.标准物种 = String(record.标准物种 || '').trim();
      next.年限 = Math.max(0, Math.floor(Number(record.年限 || 0)));
      next.品质 = normalizeSoulSpiritQuality(record.品质 || '');
      next.已使用 = record.已使用 === true;
      if (!(next.层数 > 0 && next.标准物种 && next.年限 > 0 && next.品质 && next.已使用 === false)) {
        return createEmptySoulTowerDiscountSpiritRecord();
      }
      if (!next.名称) next.名称 = `${next.标准物种}魂灵`;
      return next;
    }

    function buildSoulTowerDiscountSpiritDisplay(record = {}) {
      const normalized = normalizeSoulTowerDiscountSpiritRecord(record);
      if (!(normalized.层数 > 0)) return '暂无';
      return `第${normalized.层数}层 · ${normalized.标准物种} · ${normalized.年限}年 · ${normalized.品质}`;
    }

    function getCombatUnitAgeValue(unit = {}) {
      const rawAge = unit?.属性?.年龄 ?? unit?.年龄 ?? unit?.age;
      if (typeof rawAge === 'number') return Number.isFinite(rawAge) ? rawAge : NaN;
      const text = String(rawAge == null ? '' : rawAge).trim();
      if (!text) return NaN;
      const directNumber = Number(text);
      if (Number.isFinite(directNumber)) return directNumber;
      const numericText = text.match(/-?\d+(?:\.\d+)?/);
      return numericText ? Number(numericText[0]) : NaN;
    }

    function isSoulTowerEligibleUnit(unit = {}) {
      const ageValue = getCombatUnitAgeValue(unit);
      return Number.isFinite(ageValue) && ageValue > 0 && ageValue <= SOUL_TOWER_MAX_AGE;
    }

    function isSoulTowerCombatTypeValue(combatType = '') {
      return String(combatType || '').trim() === '魂灵塔冲塔';
    }

    function getSoulTowerGateMeta(floor = 0) {
      const safeFloor = Math.max(1, Math.floor(Number(floor) || 1));
      const rule =
        SOUL_TOWER_LAYER_RULES.find(item => safeFloor >= item.gateStart && safeFloor <= item.gateEnd) ||
        SOUL_TOWER_LAYER_RULES[SOUL_TOWER_LAYER_RULES.length - 1];
      const gateIndex = SOUL_TOWER_LAYER_RULES.findIndex(item => item.key === rule.key) + 1;
      const layerInGate = safeFloor - rule.gateStart + 1;
      const isGateBoss = safeFloor === rule.gateEnd;
      const layerProgress = rule.gateEnd > rule.gateStart ? (safeFloor - rule.gateStart) / (rule.gateEnd - rule.gateStart) : 1;
      return {
        floor: safeFloor,
        gateIndex,
        gateStart: rule.gateStart,
        gateEnd: rule.gateEnd,
        layerInGate,
        isGateBoss,
        gateLabel: rule.label,
        gateRangeLabel: `${rule.gateStart}-${rule.gateEnd}层`,
        rewardTier: rule.rewardTier,
        layerProgress,
        minAge: rule.minAge,
        maxAge: rule.maxAge,
        qualitySteps: rule.qualitySteps,
        isTopNine: safeFloor >= 100,
        totalFloors: SOUL_TOWER_TOTAL_FLOORS,
        key: rule.key,
      };
    }

    function getSoulTowerGuardianQualityForFloor(floor = 1) {
      const meta = getSoulTowerGateMeta(floor);
      const steps = Array.isArray(meta.qualitySteps) && meta.qualitySteps.length ? meta.qualitySteps : ['C'];
      if (steps.length === 1) return steps[0];
      const progress = Math.max(0, Math.min(1, Number(meta.layerProgress || 0)));
      const index = Math.min(steps.length - 1, Math.floor(progress * steps.length));
      return steps[index] || steps[steps.length - 1];
    }

    function pickBattleSeedInt(min = 0, max = min) {
      const lo = Math.floor(Number(min));
      const hi = Math.floor(Number(max));
      if (!Number.isFinite(lo) || !Number.isFinite(hi)) return 0;
      if (hi <= lo) return lo;
      return lo + Math.floor(Math.random() * (hi - lo + 1));
    }

    function buildSoulTowerGuardianAgeForFloor(floor = 1) {
      const meta = getSoulTowerGateMeta(floor);
      const minAge = Math.max(1, Math.floor(Number(meta.minAge || 1)));
      const maxAge = Math.max(minAge, Math.floor(Number(meta.maxAge || minAge)));
      if (meta.isGateBoss) return maxAge;
      const gateLength = Math.max(1, meta.gateEnd - meta.gateStart);
      const progress = Math.max(0, Math.min(1, Number(meta.layerProgress || 0)));
      const anchor = minAge + Math.round((maxAge - minAge) * progress);
      const bucket = Math.max(1, Math.floor((maxAge - minAge) / Math.max(2, gateLength + 1)));
      const variance = Math.max(0, Math.floor(bucket * 0.45));
      const offset = variance > 0 ? pickBattleSeedInt(-variance, variance) : 0;
      return Math.max(minAge, Math.min(maxAge, anchor + offset));
    }

    function buildSoulTowerGuardianSeed(floor = 1) {
      const meta = getSoulTowerGateMeta(floor);
      const species =
        SOUL_TOWER_GUARDIAN_SPECIES_POOL[Math.max(0, Math.min(SOUL_TOWER_GUARDIAN_SPECIES_POOL.length - 1, Math.floor(Math.random() * SOUL_TOWER_GUARDIAN_SPECIES_POOL.length)))] ||
        '龙类';
      const age = buildSoulTowerGuardianAgeForFloor(meta.floor);
      const quality = normalizeSoulSpiritQuality(getSoulTowerGuardianQualityForFloor(meta.floor)) || 'C';
      return {
        name: '',
        来源: '临时单位',
        单位性质: '魂兽',
        数量: 1,
        标准物种: species,
        年限: age,
        品质: quality,
      };
    }

    function buildSoulTowerDiscountSpiritRecordFromGuardian(unit = {}, floor = 1) {
      const species = String(unit?.标准物种 || '').trim();
      const age = Math.max(0, Math.floor(Number(unit?.年限 || 0)));
      const quality = normalizeSoulSpiritQuality(unit?.品质 || '');
      const 名称 = String(unit?.name || unit?.名称 || '').trim();
      if (!(species && age > 0 && quality)) return createEmptySoulTowerDiscountSpiritRecord();
      return {
        层数: Math.max(1, Math.floor(Number(floor) || 1)),
        名称: 名称 || `${species}魂灵`,
        标准物种: species,
        年限: age,
        品质: quality,
        已使用: false,
      };
    }

    function getSoulTowerPlayerRosterUnits(combatData = {}) {
      return (combatData?.参战者?.team_player || []).filter(Boolean);
    }

    function 获取升灵台结算队伍(combatData = {}, 默认角色名 = '') {
      const 队伍种子 = (combatData?.参战者?.team_player || []).filter(Boolean);
      const 队伍成员 = [];
      const 已收录 = new Set();
      const 加入成员 = 成员名 => {
        const safeName = String(成员名 || '').trim();
        if (!safeName) return;
        const 角色键 = 解析角色键(safeName);
        if (!角色键 || 已收录.has(角色键)) return;
        const 角色数据 = getMvuValue(`char.${角色键}`, null);
        if (!角色数据 || typeof 角色数据 !== 'object') return;
        已收录.add(角色键);
        队伍成员.push({
          角色键,
          角色名: String(角色数据.name || safeName || 角色键).trim() || 角色键,
          角色数据,
        });
      };
      队伍种子.forEach(成员 => {
        const 成员名 = String(成员?.name || 成员?.名称 || '').trim();
        if (!成员名) return;
        加入成员(成员名);
      });
      if (!队伍成员.length && 默认角色名) {
        加入成员(默认角色名);
      }
      return 队伍成员;
    }

    function validateSoulTowerCombatRoster(combatData = {}) {
      const roster = getSoulTowerPlayerRosterUnits(combatData);
      if (!roster.length) return { ok: false, message: '魂灵塔队伍为空。' };
      if (roster.length > SOUL_TOWER_TEAM_LIMIT) {
        return { ok: false, message: `魂灵塔队伍最多 ${SOUL_TOWER_TEAM_LIMIT} 人。` };
      }
      const invalidMember = roster.find(unit => !isSoulTowerEligibleUnit(unit));
      if (invalidMember) {
        return {
          ok: false,
          message: `${String(invalidMember?.name || invalidMember?.名称 || '队员').trim() || '队员'} 已超过 ${SOUL_TOWER_MAX_AGE} 岁，无法参与魂灵塔试炼。`,
        };
      }
      const ages = roster.map(unit => getCombatUnitAgeValue(unit)).filter(age => Number.isFinite(age) && age > 0);
      const minAge = Math.min(...ages);
      const maxAge = Math.max(...ages);
      if (maxAge - minAge > SOUL_TOWER_MAX_AGE_GAP) {
        return { ok: false, message: `魂灵塔队伍成员年龄差不能超过 ${SOUL_TOWER_MAX_AGE_GAP} 岁。` };
      }
      return { ok: true, rosterCount: roster.length, minAge, maxAge };
    }

    function hasMvuRuntime() {
      return !!getTavernHelperRuntime();
    }

    function getAllVariablesSafe() {
      try {
        const helper = getTavernHelperRuntime();
        if (!helper) return {};
        const vars = helper.getVariables({ type: 'message', message_id: 'latest' });
        if (vars && typeof vars.then === 'function') {
          console.warn('BattleUIBridge: TavernHelper.getVariables returned Promise; synchronous battle read skipped');
          return {};
        }
        return normalizeVariablesEnvelope(vars);
      } catch (error) {
        console.warn('BattleUIBridge: TavernHelper.getVariables failed', error);
        return {};
      }
    }

    function getMvuValue(path, fallback) {
      const normalized = normalizeStatPath(path);
      if (normalized === 'world.战斗' || normalized.startsWith('world.战斗.')) {
        const snapshotRoot = component?.snapshot?.rootData || snapshot?.rootData;
        const snapshotValue = lodashGet(snapshotRoot, normalized, undefined);
        if (snapshotValue !== undefined) return snapshotValue;
      }
      if (!normalized) return lodashGet(getAllVariablesSafe(), 'stat_data', fallback);
      return lodashGet(getAllVariablesSafe(), `stat_data.${normalized}`, fallback);
    }

    async function waitForMvuReady() {
      try {
        const host = getHostWindow();
        const waiter =
          typeof root.waitGlobalInitialized === 'function'
            ? root.waitGlobalInitialized.bind(root)
            : typeof host?.waitGlobalInitialized === 'function'
              ? host.waitGlobalInitialized.bind(host)
              : null;
        if (waiter) {
          await waiter('Mvu');
        }
      } catch (error) {
        console.warn('BattleUIBridge: waitForMvuReady failed', error);
      }
      return hasMvuRuntime();
    }

    function subscribeMvuUpdates(handler) {
      if (typeof handler !== 'function') return false;
      const host = getHostWindow();
      const eventName = root.Mvu?.events?.VARIABLE_UPDATE_ENDED || host?.Mvu?.events?.VARIABLE_UPDATE_ENDED;
      const eventOnFn =
        typeof root.eventOn === 'function'
          ? root.eventOn.bind(root)
          : typeof host?.eventOn === 'function'
            ? host.eventOn.bind(host)
            : null;
      if (!eventName || !eventOnFn) return false;
      eventOnFn(eventName, handler);
      return true;
    }

    function deepClonePlain(value) {
      return value == null ? value : JSON.parse(JSON.stringify(value));
    }

    function escapeJsonPointerSegment(segment) {
      return String(segment).replace(/~/g, '~0').replace(/\//g, '~1');
    }

    const COMBAT_STAT_KEYS = [
      '年龄',
      '等级',
      '天赋梯队',
      '邪魂师',
      'HP',
      'HP上限',
      '魂力',
      '魂力上限',
      '精神力',
      '精神力上限',
      '力量',
      '防御',
      '敏捷',
      '体力',
      '体力上限',
      '状态效果',
    ];
    const COMBAT_STATUS_KEYS = ['存活', '死亡tick', '死亡类型', '受伤部位', '行动', '当前领域', '位置', '横坐标', '纵坐标'];
    const COMBAT_PROCESS_SYNC_STAT_KEYS = COMBAT_STAT_KEYS.filter(key => !['HP', 'HP上限'].includes(key));
    const COMBAT_PROCESS_SYNC_STATUS_KEYS = COMBAT_STATUS_KEYS.filter(key => !['存活', '受伤部位'].includes(key));

    function sanitizeCombatPersistenceData(value) {
      if (Array.isArray(value)) return value.map(item => sanitizeCombatPersistenceData(item));
      if (!value || typeof value !== 'object') return value;
      const cleaned = {};
      Object.entries(value).forEach(([key, item]) => {
        if (key === 'is_controlled' || key === 'action_declared' || key === '_current_cast_time' || /^temp_/.test(key))
          return;
        cleaned[key] = sanitizeCombatPersistenceData(item);
      });
      return cleaned;
    }

    function isPlainRecord(value) {
      return !!value && typeof value === 'object' && !Array.isArray(value);
    }

    function isDeepEqual(left, right) {
      if (left === right) return true;
      if (Number.isNaN(left) && Number.isNaN(right)) return true;

      if (Array.isArray(left) || Array.isArray(right)) {
        if (!Array.isArray(left) || !Array.isArray(right) || left.length !== right.length) return false;
        for (let index = 0; index < left.length; index += 1) {
          if (!isDeepEqual(left[index], right[index])) return false;
        }
        return true;
      }

      if (isPlainRecord(left) || isPlainRecord(right)) {
        if (!isPlainRecord(left) || !isPlainRecord(right)) return false;
        const leftKeys = Object.keys(left);
        const rightKeys = Object.keys(right);
        if (leftKeys.length !== rightKeys.length) return false;
        for (let index = 0; index < leftKeys.length; index += 1) {
          const key = leftKeys[index];
          if (!Object.prototype.hasOwnProperty.call(right, key)) return false;
          if (!isDeepEqual(left[key], right[key])) return false;
        }
        return true;
      }

      return false;
    }

    function clonePersistedCombatValue(value) {
      return sanitizeCombatPersistenceData(deepClonePlain(value));
    }

    const COMBAT_STAT_RATIO_RUNTIME_TO_SCHEMA_KEY = {
      str: '力量',
      def: '防御',
      agi: '敏捷',
      sp_max: '魂力上限',
    };
    const COMBAT_STAT_RATIO_SCHEMA_TO_RUNTIME_KEY = {
      力量: 'str',
      防御: 'def',
      敏捷: 'agi',
      魂力上限: 'sp_max',
    };
    const 战斗属性固定修正运行键到结构键 = {
      str: '力量',
      def: '防御',
      agi: '敏捷',
      vit_max: '体力上限',
      sp_max: '魂力上限',
      men_max: '精神力上限',
    };
    const 战斗属性固定修正结构键到运行键 = {
      力量: 'str',
      防御: 'def',
      敏捷: 'agi',
      体力上限: 'vit_max',
      魂力上限: 'sp_max',
      精神力上限: 'men_max',
    };
    const COMBAT_EFFECT_RUNTIME_TO_SCHEMA_KEY = {
      dot_damage: '持续伤害',
      skip_turn: '跳过回合',
      armor_pen: '破防比例',
    };
    const COMBAT_EFFECT_SCHEMA_TO_RUNTIME_KEY = {
      持续伤害: 'dot_damage',
      跳过回合: 'skip_turn',
      破防比例: 'armor_pen',
    };
    const COMBAT_EFFECT_BOOLEAN_KEYS = new Set([
      'skip_turn',
      'cannot_react',
      'silence',
      'disarm',
      'blind',
      'super_armor',
      '探查屏蔽',
    ]);
    const COMBAT_EFFECT_RUNTIME_DEFAULTS = {
      skip_turn: false,
      dot_damage: 0,
      armor_pen: 0,
    };
    const 时光回溯反应压制值 = 0.35;

    const 战斗阶段枚举_V1 = Object.freeze({
      无: '无',
      宣告: '宣告阶段',
      对轰判定: '对轰判定阶段',
      回合结算: '回合结算阶段',
    });
    const 战斗阶段合法值集合_V1 = new Set(Object.values(战斗阶段枚举_V1));

    // 系别 → 基础战略意图加权表 (数据驱动,与 推断战略意图() 的态势加权解耦)
    // 改了平衡只动这张表;新增系别在这里加一行即可。
    const 系别基础战略意图权重_V1 = Object.freeze({
      强攻系: Object.freeze({ 击杀: 34, 集火: 28, 破防: 12 }),
      敏攻系: Object.freeze({ 击杀: 26, 终局爆发: 24, 控制: 12 }),
      控制系: Object.freeze({ 控制: 38, 资源消耗: 18, 集火: 12 }),
      防御系: Object.freeze({ 保核: 36, 拖回合: 16, 破防: 10 }),
      治疗系: Object.freeze({ 保留资源: 22, 控制: 12, 保核: 8 }),
      辅助系: Object.freeze({ 控制: 18, 保留资源: 14, 集火: 12 }),
      食物系: Object.freeze({ 保留资源: 28, 拖回合: 12, 控制: 6 }),
    });
    const 系别基础战略意图权重默认_V1 = Object.freeze({ 集火: 18, 保留资源: 12 });

    // 态势加权表 — 简单阈值触发的意图增益(原 推断战略意图() 11022-11028 散落 if)
    // 仅收纳"单一数值条件 + 阈值方向"可数据化的项。复杂谓词(对象检查 / 函数调用)留在代码里。
    const 态势战略意图加权表_V1 = Object.freeze([
      { 取值: ctx => ctx.敌方最低血,  方向: '小于', 阈值: 0.38, 加权: Object.freeze({ 击杀: 34, 终局爆发: 28 }) },
      { 取值: ctx => ctx.我方残血触发, 方向: '为真', 加权: Object.freeze({ 保核: 36, 治疗: 34 }) },
      { 取值: ctx => ctx.战局画像?.召唤压力, 方向: '大于', 阈值: 0.18, 加权: Object.freeze({ 破召唤: 32 }) },
      { 取值: ctx => ctx.战局画像?.维持压力, 方向: '大于', 阈值: 0.10, 加权: Object.freeze({ 维持压制: 30 }) },
      { 取值: ctx => ctx.战局画像?.资源压力, 方向: '大于', 阈值: 0.55, 加权: Object.freeze({ 保留资源: 36 }) },
      { 取值: ctx => Number(ctx.behaviorState?.round || 0), 方向: '大于等于', 阈值: 4, 加权: Object.freeze({ 资源消耗: 14 }) },
    ]);

    // 技能战术修正阈值表 — 集中战术评分的散落系数 (Phase 2 会调动态多样性扣减)
    const 技能战术修正阈值_V1 = Object.freeze({
      行为多样性惩罚峰值: 72,         // 原 11127 hard-coded min(72, ...)
      行为多样性单次重复加成: 22,     // 原 11127 重复次数*22
      行为多样性压力倍增: 30,         // 原 11127 行为多样性压力*30
      行为多样性同技额外: 12,         // 原 11127 上次同技 ? 12 : 0
    });

    // 时机决策阈值表 — Phase 2/3 用,先占位以稳定数据契约
    const 时机决策阈值_V1 = Object.freeze({
      集火窗口_目标残血阈值: 0.30,    // Phase 2.A
      节奏感_领先差: 0.30,            // Phase 2.B
      节奏感_落后差: 0.30,            // Phase 2.B
      多样性早期回合: 3,              // Phase 2.C
      多样性中期回合: 6,              // Phase 2.C
      多样性早期缩放: 0.50,           // Phase 2.C
      多样性中期缩放: 0.85,           // Phase 2.C
      融合技释放_敌方残血阈值: 0.40,  // Phase 3.A
      大招释放_敌方残血阈值: 0.35,    // Phase 3.B
      大招释放_我方残血阈值: 0.40,    // Phase 3.B
      大招释放_最早回合: 5,           // Phase 3.B
      召唤_数量劣势触发: 1,           // Phase 3.C
      召唤_我方残血触发: 0.50,        // Phase 3.C
      召唤_过载阈值: 2,               // Phase 3.C
      反击意图_伤害阈值: 0.15,        // Phase 3.D
      稀缺技能_敌方残血阈值: 0.45,    // Phase 3.E (血脉/气血/魂骨)
      稀缺技能_终局压力阈值: 0.50,    // Phase 3.E
      装备_早期克制回合: 2,           // Phase 3.F (回合<2 不该浪费资源穿戴)
      装备_紧急释放_我方残血阈值: 0.60, // Phase 3.F
      装备_紧急释放_终局压力阈值: 0.50, // Phase 3.F
      真身领域_紧急释放_我方残血阈值: 0.50,  // Phase 3.H
      真身领域_决战_敌方残血阈值: 0.40,      // Phase 3.H
      遗忘机制_重启回合阈值: 5,       // Phase 3.I (距上次使用 ≥ 5 回合)
      遗忘机制_重启加成: 8,           // Phase 3.I
      防守优先_对方下回合威胁: 0.60,  // Phase 4
    });

    // Phase C: 战术修正层级量级表 — 5 层显式 cap, 防止跨层量级失衡
    const 战术修正层级量级_V1 = Object.freeze({
      基础层:     { 最小: 0,    最大: 100 },  // 12 项 加意图() + 目标优先级,允许累加
      情境修正层: { 最小: -80,  最大: 80  },  // 节奏 / 切换 / 收割 / 救急 / 多样性 / 重启
      机制专属层: { 最小: -90,  最大: 90  },  // 融合 / 大招 / 召唤 / 稀缺 / 装备
      资源约束层: { 最小: -100, 最大: 30  },  // 不允许加分(终局放行 +28 给 30 缓冲)
      一票否决层: { 最小: -150, 最大: 150 },  // 留给可斩杀类强信号
      总修正层:   { 最小: -170, 最大: 180 },  // 跨层总 cap,避免五层危机信号暴叠
    });
    function 应用层级clamp_V1(delta, 范围) {
      return Math.max(范围.最小, Math.min(范围.最大, delta));
    }

    const 对手威胁权重表_V1 = Object.freeze({
      基线: 0.18,
      蓄力即将完成: 0.42,
      蓄力中长期: 0.26,
      高血量: 0.14,
      中血量: 0.08,
      危险系别: 0.16,
      续航系别: 0.10,
      目标锁定: 0.08,
      反应防御: 0.05,
      维持压力: 0.10,
      召唤宿主: 0.08,
      全局蓄力: 0.10,
    });

    const 对手意图预测权重表_V1 = Object.freeze({
      默认: Object.freeze({ 输出: 16, 控制: 8, 治疗: 0, 保核: 4, 召唤: 0, 爆发: 8, 资源保留: 4 }),
      强攻系: Object.freeze({ 输出: 38, 控制: 4, 治疗: 0, 保核: 2, 召唤: 0, 爆发: 28, 资源保留: 4 }),
      敏攻系: Object.freeze({ 输出: 32, 控制: 10, 治疗: 0, 保核: 2, 召唤: 0, 爆发: 24, 资源保留: 4 }),
      控制系: Object.freeze({ 输出: 14, 控制: 40, 治疗: 0, 保核: 8, 召唤: 0, 爆发: 12, 资源保留: 6 }),
      防御系: Object.freeze({ 输出: 10, 控制: 12, 治疗: 0, 保核: 34, 召唤: 4, 爆发: 8, 资源保留: 10 }),
      治疗系: Object.freeze({ 输出: 4, 控制: 8, 治疗: 6, 保核: 14, 召唤: 0, 爆发: 4, 资源保留: 18 }),
      辅助系: Object.freeze({ 输出: 6, 控制: 16, 治疗: 4, 保核: 16, 召唤: 6, 爆发: 6, 资源保留: 14 }),
      食物系: Object.freeze({ 输出: 4, 控制: 6, 治疗: 6, 保核: 12, 召唤: 0, 爆发: 4, 资源保留: 24 }),
    });

    const 危机层级阈值_V1 = Object.freeze({
      轻度我方血量: 0.62,
      轻度威胁: 0.42,
      重度我方血量: 0.42,
      重度威胁: 0.62,
      致命我方血量: 0.28,
      致命威胁: 0.82,
      多人残血血量: 0.50,
    });

    const 反击窗口阈值_V1 = Object.freeze({
      我方血量下限: 0.30,
      我方血量上限: 0.60,
      敌方血量下限: 0.30,
      敌方血量上限: 0.50,
      可斩杀血量: 0.30,
      致命威胁: 0.82,
    });

    const 一阶前瞻权重表_V1 = Object.freeze({
      控制打断风险下降: -0.46,
      斩杀高威胁风险下降: -0.52,
      治疗致命风险下降: -0.48,
      防护致命风险下降: -0.36,
      防护错失反击: 0.24,
      高耗无斩杀风险上升: 0.28,
      召唤缓压: -0.16,
      破防后爆发风险下降: -0.22,
      控制后输出风险下降: -0.18,
    });

    const 目标克制权重表_V1 = Object.freeze({
      强攻系: Object.freeze({ 治疗系: 0.24, 辅助系: 0.22, 食物系: 0.22, 控制系: 0.18, 防御系: -0.08 }),
      敏攻系: Object.freeze({ 治疗系: 0.25, 辅助系: 0.22, 食物系: 0.22, 控制系: 0.16, 防御系: -0.12 }),
      控制系: Object.freeze({ 强攻系: 0.18, 敏攻系: 0.16, 控制系: 0.12, 治疗系: 0.10 }),
      防御系: Object.freeze({ 强攻系: 0.16, 敏攻系: 0.14, 控制系: 0.10 }),
      治疗系: Object.freeze({ 强攻系: 0.06, 控制系: 0.06 }),
      辅助系: Object.freeze({ 强攻系: 0.08, 控制系: 0.08 }),
      食物系: Object.freeze({ 强攻系: 0.04, 控制系: 0.04 }),
    });

    const 技能时机约束表_V1 = Object.freeze({
      融合技重复回合: 5,
      大招重复回合: 4,
      稀缺技能重复回合: 4,
      装备技能重复回合: 3,
      普通技能重复回合: 2,
      高风险重复惩罚: 46,
      普通重复惩罚: 16,
      控制后输出加成: 22,
      破防后爆发加成: 26,
      治疗后拖回合加成: 26,
      治疗后资源重整加成: 28,
      召唤后护宿主加成: 18,
      资源断档惩罚: 34,
    });

    const 友方保护权重表_V1 = Object.freeze({
      核心残血: 34,
      队友残血: 36,
      致命来袭: 28,
      被锁定: 22,
      异常控制: 36,
      我方蓄力: 14,
      支援职责: 28,
      低收益治疗: -22,
    });

    const 阶段节奏权重表_V1 = Object.freeze({
      开局高耗: -42,
      开局准备: 14,
      中盘竞争: 8,
      终局爆发: 30,
      终局控制: 10,
      资源枯竭恢复: 24,
      资源枯竭高耗: -36,
      残局收割: 34,
      残局防御: -18,
    });

    const 危机恢复权重表_V1 = Object.freeze({
      输出恢复: 22,
      控制压制: 16,
      资源恢复: 18,
      召唤恢复: 14,
      防御惯性: -24,
      低压纯防御: -20,
      反击窗口输出: 20,
    });

    const 空间收益权重表_V1 = Object.freeze({
      群体残血: 24,
      群体蓄力: 28,
      群体破防: 20,
      后排核心: 18,
      孤立收割: 22,
      孤立压制: 10,
      前排保护破口: 16,
      保护链断裂: 20,
      远距普通输出: -14,
      远距蓄力控制: 12,
    });

    const 短期战术记忆权重表_V1 = Object.freeze({
      控制后输出: 18,
      破防后爆发: 22,
      未击杀收割: 24,
      治疗后防护: 20,
      治疗后资源重整: 28,
      无效同类: -18,
      无理由连续切换: -14,
    });

    const 支援职责权重表_V1 = Object.freeze({
      治疗系保护: 32,
      辅助系驱散: 40,
      辅助系先手控制: 24,
      食物系资源支援: 44,
      低收益支援: -18,
      非职责普通进攻: -28,
    });

    const 资源节奏权重表_V1 = Object.freeze({
      高耗后恢复: 38,
      高耗后低耗防护: 20,
      高耗后连续高耗: -48,
      资源枯竭恢复: 36,
      资源枯竭低耗: 16,
      资源枯竭高耗: -40,
    });

    const 对手适应权重表_V1 = Object.freeze({
      连续治疗压制: 30,
      连续控制驱散: 32,
      连续控制先手: 28,
      连续控制保护: 22,
      连续高耗截断: 24,
      连续高耗反打: 18,
      对方低资源压节奏: 18,
      对方低资源恢复: 14,
    });

    const 二回合前瞻权重表_V1 = Object.freeze({
      高耗断档: -36,
      资源恢复: 26,
      下回合保命断档: -28,
      打断爆发: 28,
      阻止治疗救场: 24,
      先手防控: 18,
      破防后窗口: 16,
      控制后窗口: 18,
      治疗后重整: 18,
      保留斩杀资源: 16,
    });

    const 职责画像权重表_V1 = Object.freeze({
      主职输出: 14,
      主职收割: 18,
      主职打断: 18,
      主职破防: 12,
      主职保护: 20,
      主职驱散: 22,
      主职资源: 24,
      支援窗口压偏离: -16,
      非关键防御降温: -8,
    });

    // 应用 态势战略意图加权表 — Phase 1 用,封装比较 + 加权
    function 应用态势战略意图加权_V1(ctx, 加权) {
      态势战略意图加权表_V1.forEach(项 => {
        const 实际值 = 项.取值(ctx);
        let 命中 = false;
        if (项.方向 === '小于') 命中 = Number(实际值) < 项.阈值;
        else if (项.方向 === '大于') 命中 = Number(实际值) > 项.阈值;
        else if (项.方向 === '大于等于') 命中 = Number(实际值) >= 项.阈值;
        else if (项.方向 === '为真') 命中 = !!实际值;
        if (命中) Object.entries(项.加权).forEach(([意图, 分值]) => 加权(意图, 分值));
      });
    }

    function 读取规划距离衰减_V1(单位 = {}) {
      const 距离 = String(单位?.距离 || 单位?.站位距离 || 单位?.属性?.距离 || '').trim();
      if (/远|远距|后排/.test(距离)) return 0.6;
      if (/中|中距/.test(距离)) return 0.8;
      if (/近|近距|贴身|前排/.test(距离)) return 1;
      const 数值距离 = Number(单位?.distance ?? 单位?.range_distance);
      if (Number.isFinite(数值距离)) {
        if (数值距离 >= 8) return 0.6;
        if (数值距离 >= 4) return 0.8;
      }
      return 1;
    }

    function 读取规划距离优先系数_V1(单位 = {}) {
      const 衰减 = 读取规划距离衰减_V1(单位);
      if (衰减 >= 0.98) return 1;
      if (衰减 >= 0.78) return 0.90;
      return 0.78;
    }

    function 读取规划距离标签_V1(单位 = {}) {
      const 距离 = String(单位?.距离 || 单位?.站位距离 || 单位?.属性?.距离 || '').trim();
      if (/远|远距|后排/.test(距离)) return '远距';
      if (/中|中距/.test(距离)) return '中距';
      if (/近|近距|贴身|前排/.test(距离)) return '近距';
      const 数值距离 = Number(单位?.distance ?? 单位?.range_distance);
      if (Number.isFinite(数值距离)) {
        if (数值距离 >= 8) return '远距';
        if (数值距离 >= 4) return '中距';
        return '近距';
      }
      return '';
    }

    function 读取规划站位标签_V1(单位 = {}) {
      const 文本 = String(单位?.站位 || 单位?.阵位 || 单位?.阵型位置 || 单位?.位置 || 单位?.距离 || 单位?.站位距离 || 单位?.属性?.站位 || 单位?.属性?.位置 || '').trim();
      if (/后排|远程|后卫|后置/.test(文本)) return '后排';
      if (/前排|近战|前卫|前置/.test(文本)) return '前排';
      if (/中排|中距|中卫/.test(文本)) return '中排';
      return '';
    }

    function 判定规划核心单位_V1(单位 = {}) {
      return 单位?.是否核心 === true || 单位?.核心 === true || 单位?.isCore === true || 单位?.role === 'core' || /核心|主力/.test(String(单位?.标签 || 单位?.职责 || ''));
    }

    function 读取规划综合资源比例_V1(单位 = {}) {
      const 魂力 = 读取规划资源比例(单位, '魂力');
      const 精神力 = 读取规划资源比例(单位, '精神力');
      const 体力 = 读取规划资源比例(单位, '体力');
      return 限制行为概率(魂力 * 0.42 + 精神力 * 0.30 + 体力 * 0.28, 0, 1);
    }

    function 读取对手状态衰减_V1(单位 = {}) {
      const 快照 = buildConditionTacticalSnapshot(单位);
      if (快照.isLockedOrControlled) return 0.25;
      const 状态效果 = Object.values(单位?.状态效果 || 单位?.属性?.状态效果 || {});
      if (状态效果.some(项 => 项?.战斗效果?.skill_seal === true || 项?.战斗效果?.silence === true)) return 0.55;
      if (状态效果.some(项 => 项?.战斗效果?.disarm === true || 项?.战斗效果?.blind === true)) return 0.70;
      return 1;
    }

    function 预测对手下回合意图_V1(单位 = {}, 战局画像 = {}, behaviorState = {}) {
      const 系别 = 读取规划单位系别(单位);
      const 预测 = { ...(对手意图预测权重表_V1[系别] || 对手意图预测权重表_V1.默认) };
      const hpRatio = getCombatHpRatio(单位);
      const 蓄力剩余 = Number(单位?.蓄力剩余 ?? 单位?.cast_time_left ?? 0);
      const 资源比例 = 读取规划综合资源比例_V1(单位);
      const 快照 = buildConditionTacticalSnapshot(单位);
      const 状态衰减 = 读取对手状态衰减_V1(单位);
      const 召唤数量 = 读取召唤单位列表(behaviorState?.combatData || {}, { 宿主: 单位 }).length;
      const 维持压力 = 读取规划维持压力(单位);
      const 我方最低血 = Math.min(1, ...(战局画像.己方列表 || []).map(getCombatHpRatio));
      const 敌方最低血 = Math.min(1, ...(战局画像.敌方列表 || []).map(getCombatHpRatio));
      const 同侧队友残血待救 = (战局画像.敌方列表 || [])
        .some(队友 => 队友 && 队友 !== 单位 && getCombatHpRatio(队友) > 0 && getCombatHpRatio(队友) < 0.42);
      if (单位?.蓄力技能 || 蓄力剩余 > 0) {
        预测.爆发 += 蓄力剩余 <= 1 ? 34 : 20;
        预测.控制 += 8;
      }
      if (我方最低血 < 0.42) {
        预测.输出 += 10;
        预测.爆发 += 12;
      }
      if (敌方最低血 < 0.48) {
        预测.治疗 += ['治疗系', '辅助系', '食物系'].includes(系别) ? 22 : 6;
        预测.保核 += 16;
      }
      if (同侧队友残血待救 && ['治疗系', '辅助系', '食物系'].includes(系别)) {
        预测.治疗 += 34;
        预测.保核 += 12;
      }
      if (hpRatio < 0.35) {
        预测.保核 += 12;
        预测.资源保留 += 8;
        预测.爆发 -= 8;
      }
      if (资源比例 < 0.35) {
        预测.资源保留 += 28;
        预测.输出 -= 14;
        预测.爆发 -= 18;
      } else if (资源比例 > 0.72) {
        预测.输出 += 8;
        预测.爆发 += 8;
      }
      if (状态衰减 < 0.6) {
        预测.输出 -= 16;
        预测.爆发 -= 16;
        预测.控制 -= 10;
        预测.资源保留 += 10;
      }
      if (快照.hasReactiveDefense || 快照.hasShielded) 预测.保核 += 8;
      if (召唤数量 > 0) {
        预测.召唤 += Math.min(18, 6 + 召唤数量 * 4);
        预测.保核 += 6;
      }
      if (维持压力 > 0.05) {
        预测.控制 += 8;
        预测.资源保留 += 6;
      }
      const 最近行动 = 单位?.决策记忆?.recent_actions || {};
      const 最高重复 = Math.max(0, ...Object.values(最近行动).map(v => Number(v || 0)));
      if (最高重复 >= 2) {
        预测.输出 -= 6;
        预测.爆发 -= 6;
        预测.控制 -= 4;
        预测.资源保留 += 6;
      }
      if (behaviorState?.isChargingHighThreat) 预测.爆发 += 10;
      Object.keys(预测).forEach(key => { 预测[key] = Math.max(0, Math.round(预测[key])); });
      return 预测;
    }

    function 计算对手单体威胁_V1(单位 = {}, 战局画像 = {}, behaviorState = {}) {
      if (!单位 || typeof 单位 !== 'object' || 单位?.状态?.存活 === false || getCombatHpRatio(单位) <= 0) return 0;
      const 权重 = 对手威胁权重表_V1;
      const 快照 = buildConditionTacticalSnapshot(单位);
      const 系别 = 读取规划单位系别(单位);
      const hpRatio = getCombatHpRatio(单位);
      const 蓄力剩余 = Number(单位?.蓄力剩余 ?? 单位?.cast_time_left ?? 0);
      const 意图 = 预测对手下回合意图_V1(单位, 战局画像, behaviorState);
      let 基础威胁 = 权重.基线;
      if (单位?.蓄力技能 || 蓄力剩余 > 0) 基础威胁 += 蓄力剩余 <= 1 ? 权重.蓄力即将完成 : 权重.蓄力中长期;
      if (hpRatio > 0.70) 基础威胁 += 权重.高血量;
      else if (hpRatio >= 0.30) 基础威胁 += 权重.中血量;
      if (['强攻系', '控制系', '敏攻系'].includes(系别)) 基础威胁 += 权重.危险系别;
      if (['治疗系', '辅助系', '食物系'].includes(系别)) 基础威胁 += 权重.续航系别;
      if (快照.hasTargetLock) 基础威胁 += 权重.目标锁定;
      if (快照.hasReactiveDefense) 基础威胁 += 权重.反应防御;
      if (读取规划维持压力(单位) > 0) 基础威胁 += 权重.维持压力;
      if (读取召唤单位列表(behaviorState?.combatData || {}, { 宿主: 单位 }).length > 0) 基础威胁 += 权重.召唤宿主;
      if (behaviorState?.isChargingHighThreat) 基础威胁 += 权重.全局蓄力;
      const 意图压力 = (意图.输出 * 0.004 + 意图.控制 * 0.004 + 意图.爆发 * 0.005 + Math.max(意图.治疗, 意图.保核) * 0.002);
      const 资源系数 = Math.max(0.45, Math.min(1, 0.45 + 读取规划综合资源比例_V1(单位) * 0.55));
      return Math.max(0, 基础威胁 + 意图压力) * 资源系数 * 读取对手状态衰减_V1(单位) * 读取规划距离衰减_V1(单位);
    }

    // Phase 4: 预估对方下回合累计威胁 — 不递归、不枚举技能列表,
    // 用"多敌累计 + 对手意图 + 资源/状态/距离衰减"替代单体 max。
    function 预估对方下回合最大威胁_V1(战局画像 = {}, behaviorState = {}) {
      const 敌方 = Array.isArray(战局画像.敌方列表) ? 战局画像.敌方列表 : [];
      if (!敌方.length) return 0;
      const 总威胁 = 敌方.reduce((总和, 单位) => 总和 + 计算对手单体威胁_V1(单位, 战局画像, behaviorState), 0);
      return 限制行为概率(总威胁, 0, 1);
    }

    function 汇总对手意图预测_V1(战局画像 = {}, behaviorState = {}) {
      const 汇总 = { 输出: 0, 控制: 0, 治疗: 0, 保核: 0, 召唤: 0, 爆发: 0, 资源保留: 0 };
      (战局画像.敌方列表 || []).forEach(单位 => {
        if (!单位 || 单位?.状态?.存活 === false || getCombatHpRatio(单位) <= 0) return;
        const 单体威胁 = Math.max(0.15, 计算对手单体威胁_V1(单位, 战局画像, behaviorState));
        const 预测 = 预测对手下回合意图_V1(单位, 战局画像, behaviorState);
        Object.keys(汇总).forEach(key => { 汇总[key] += 预测[key] * 单体威胁; });
      });
      Object.keys(汇总).forEach(key => { 汇总[key] = Math.round(汇总[key]); });
      return 汇总;
    }

    function 判定危机层级_V1(战局画像 = {}, behaviorState = {}, actor = {}) {
      const 下回合威胁 = 预估对方下回合最大威胁_V1(战局画像, behaviorState);
      const 我方最低血 = Math.min(1, ...(战局画像.己方列表 || [actor]).filter(Boolean).map(getCombatHpRatio));
      const 多人残血 = (战局画像.己方列表 || []).filter(单位 => getCombatHpRatio(单位) < 危机层级阈值_V1.多人残血血量 && 单位?.状态?.存活 !== false).length >= 2;
      const 对方蓄力即将爆发 = (战局画像.敌方列表 || []).some(单位 => 单位?.状态?.存活 !== false && 单位?.蓄力技能 && Number(单位?.蓄力剩余 ?? 99) <= 1);
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const 撤退窗口 = 战局画像.允许撤离 === true && 我方最低血 < 0.34 && 下回合威胁 > 0.58;
      if (来袭.lethalRisk === true || 下回合威胁 >= 危机层级阈值_V1.致命威胁 || (对方蓄力即将爆发 && 我方最低血 < 危机层级阈值_V1.致命我方血量)) {
        return { 层级: '致命危机', 威胁: 下回合威胁, 加权: { 保核: 46, 治疗: 38, 控制: 34 } };
      }
      if (下回合威胁 >= 危机层级阈值_V1.重度威胁 || 多人残血 || 撤退窗口 || 我方最低血 < 危机层级阈值_V1.重度我方血量) {
        return { 层级: '重度危机', 威胁: 下回合威胁, 加权: { 控制: 30, 治疗: 26, 保核: 24 } };
      }
      if (下回合威胁 >= 危机层级阈值_V1.轻度威胁 || 我方最低血 < 危机层级阈值_V1.轻度我方血量 || Number(战局画像.资源压力 || 0) > 0.55) {
        return { 层级: '轻度危机', 威胁: 下回合威胁, 加权: { 控制: 14, 保核: 12, 治疗: 10 } };
      }
      return { 层级: '无危机', 威胁: 下回合威胁, 加权: {} };
    }

    function 判定反击窗口_V1(actor = {}, 战局画像 = {}, behaviorState = {}) {
      const 我方血量 = getCombatHpRatio(actor || 战局画像.我方核心 || {});
      const 敌方列表 = 战局画像.敌方列表 || [];
      const 下回合威胁 = 预估对方下回合最大威胁_V1(战局画像, behaviorState);
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const 有敌方反击血量 = 敌方列表.some(单位 => {
        const 血量 = getCombatHpRatio(单位);
        return 单位?.状态?.存活 !== false && 血量 >= 反击窗口阈值_V1.敌方血量下限 && 血量 <= 反击窗口阈值_V1.敌方血量上限;
      });
      const 有可斩杀目标 = 敌方列表.some(单位 => 单位?.状态?.存活 !== false && getCombatHpRatio(单位) > 0 && getCombatHpRatio(单位) <= 反击窗口阈值_V1.可斩杀血量);
      return 我方血量 >= 反击窗口阈值_V1.我方血量下限
        && 我方血量 <= 反击窗口阈值_V1.我方血量上限
        && (有敌方反击血量 || 有可斩杀目标)
        &&下回合威胁 < 反击窗口阈值_V1.致命威胁
        && 来袭.lethalRisk !== true;
    }

    function 推断行动类别_V1(指标 = {}) {
      if (指标.是治疗) return '治疗';
      if (指标.是防护) return '防护';
      if (指标.是控制) return '控制';
      if (指标.是召唤) return '召唤';
      if (指标.可斩杀 || 指标.是融合技 || 指标.是大招 || 指标.是稀缺技能) return '终局爆发';
      if (指标.是输出 || 指标.是破防) return '输出';
      return '资源保留';
    }

    function 评估我方行动后一阶风险_V1(行动类别, actor = {}, target = {}, 战局画像 = {}, behaviorState = {}) {
      const 当前威胁 = 预估对方下回合最大威胁_V1(战局画像, behaviorState);
      const 目标威胁 = target ? 计算对手单体威胁_V1(target, 战局画像, behaviorState) : 0;
      const 目标血量 = getCombatHpRatio(target || {});
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      let 风险变化 = 0;
      let 推荐意图 = '集火';
      let 理由 = '常规';
      if (行动类别 === '控制' && (target?.蓄力技能 || 目标威胁 > 0.45)) {
        风险变化 += 一阶前瞻权重表_V1.控制打断风险下降;
        推荐意图 = '控制';
        理由 = '控制高威胁';
      }
      if (['输出', '终局爆发'].includes(行动类别) && target && 目标血量 > 0 && (目标血量 <= 0.32 || 目标威胁 > 0.48)) {
        风险变化 += 一阶前瞻权重表_V1.斩杀高威胁风险下降 * Math.max(0.45, Math.min(1, 目标威胁 + (1 - 目标血量)));
        推荐意图 = '击杀';
        理由 = '斩杀高威胁';
      }
      if (行动类别 === '治疗' && 来袭.lethalRisk === true) {
        风险变化 += 一阶前瞻权重表_V1.治疗致命风险下降;
        推荐意图 = '治疗';
        理由 = '致命治疗';
      }
      if (行动类别 === '防护') {
        if (来袭.lethalRisk === true || 当前威胁 > 0.75) {
          风险变化 += 一阶前瞻权重表_V1.防护致命风险下降;
          推荐意图 = '保核';
          理由 = '致命防护';
        } else if (判定反击窗口_V1(actor, 战局画像, behaviorState)) {
          风险变化 += 一阶前瞻权重表_V1.防护错失反击;
          推荐意图 = '反击';
          理由 = '错失反击';
        }
      }
      if (行动类别 === '召唤' && 当前威胁 > 0.45) {
        风险变化 += 一阶前瞻权重表_V1.召唤缓压;
        推荐意图 = '保核';
        理由 = '召唤缓压';
      }
      return { 风险变化: 限制行为概率(风险变化, -1, 1), 推荐意图, 理由 };
    }

    function 计算目标克制调整_V1(actor = {}, target = {}) {
      const 攻方系别 = 读取规划单位系别(actor);
      const 目标系别 = 读取规划单位系别(target);
      const 表 = 目标克制权重表_V1[攻方系别] || {};
      let 调整 = Number(表[目标系别] || 0);
      const 血量 = getCombatHpRatio(target);
      if (攻方系别 === '敏攻系' && 血量 > 0 && 血量 < 0.38) 调整 += 0.12;
      if (攻方系别 === '控制系' && (target?.蓄力技能 || 读取规划维持压力(target) > 0)) 调整 += 0.18;
      if (['治疗系', '辅助系', '食物系'].includes(攻方系别) && ['防御系', '强攻系'].includes(目标系别) && !target?.蓄力技能) 调整 -= 0.10;
      return 限制行为概率(调整, -0.25, 0.25);
    }

    function 读取标准规划行为状态_V1(规划上下文 = {}) {
      return {
        ...规划上下文,
        combatData: 规划上下文.combatData || {},
        threatProfile: {
          ...(规划上下文.threatProfile || {}),
          ...(规划上下文.来袭窗口摘要 || {}),
        },
        来袭窗口摘要: {
          ...(规划上下文.来袭窗口摘要 || {}),
          ...(规划上下文.threatProfile || {}),
        },
      };
    }

    function 规范化行为记忆键_V1(值 = '') {
      return String(值 || '').trim().replace(/\s+/g, '').toLowerCase();
    }

    function 读取行为记忆候选键_V1(skill = {}, candidate = {}) {
      return [
        skill?.name,
        skill?.魂技名,
        skill?.技能名称,
        candidate?.name,
        candidate?.行动名,
        candidate?.分支名,
      ].map(规范化行为记忆键_V1).filter(Boolean);
    }

    function 读取归一行为记忆值_V1(记忆表 = {}, 候选键 = [], 默认值 = 0) {
      if (!记忆表 || typeof 记忆表 !== 'object') return 默认值;
      const 目标键 = new Set((候选键 || []).map(规范化行为记忆键_V1).filter(Boolean));
      if (!目标键.size) return 默认值;
      let 命中值 = 默认值;
      Object.entries(记忆表).forEach(([键, 值]) => {
        if (目标键.has(规范化行为记忆键_V1(键))) 命中值 = Math.max(Number(命中值 || 0), Number(值 || 0));
      });
      return 命中值;
    }

    function 写入归一行为记忆值_V1(记忆表 = {}, 原始键 = '', 值 = 0) {
      const 目标键 = String(原始键 || '').trim();
      if (!目标键 || !记忆表 || typeof 记忆表 !== 'object') return;
      const 归一键 = 规范化行为记忆键_V1(目标键);
      const 旧键 = Object.keys(记忆表).find(键 => 规范化行为记忆键_V1(键) === 归一键) || 目标键;
      记忆表[旧键] = 值;
    }

    function 判定防御动作类型_V1(skill = {}, candidate = {}) {
      const 文本 = [
        skill?.name, skill?.魂技名, skill?.技能名称, skill?.描述, skill?.效果,
        candidate?.name, candidate?.行动名, candidate?.分支名,
      ].map(值 => String(值 || '')).join('|');
      if (/治疗|恢复|回春|回血|疗愈/.test(文本)) return '治疗';
      if (/闪避|规避|躲避|身法|避让/.test(文本)) return '闪避';
      if (/免死|免伤|无敌|不死|抵死|伤害减免|减伤/.test(文本)) return '免伤';
      if (/反击|格挡反打|以守为攻/.test(文本)) return '反击';
      if (/打断|封技|沉默|眩晕|定身|控制/.test(文本)) return '控制截断';
      if (/防护|护盾|守护|格挡|防御|庇护|保核/.test(文本)) return '防护';
      if (/自保|兜底|拖回合/.test(文本)) return '纯防御';
      return '非防御';
    }

    function 评分防御动作质量_V1(防御类型 = '非防御', actor = {}, target = {}, behaviorState = {}) {
      if (防御类型 === '非防御') return { 修正: 0, 理由: '' };
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const 战局画像 = behaviorState?.战局画像 || {};
      const 威胁 = 预估对方下回合最大威胁_V1(战局画像, behaviorState);
      const 低威胁 = (威胁 < 0.35 || 来袭.低威胁 === true || 来袭.lowThreat === true) && 来袭.lethalRisk !== true && 来袭.severeDamage !== true;
      if (低威胁) {
        const 表 = { 纯防御: -28, 防护: -18, 闪避: -14, 免伤: -20, 治疗: getCombatHpRatio(target || actor) > 0.75 ? -18 : 4, 反击: 10, 控制截断: 8 };
        return { 修正: 表[防御类型] || 0, 理由: '低压防御降权' };
      }
      if (来袭.lethalRisk === true || 威胁 >= 危机层级阈值_V1.致命威胁) {
        const 表 = { 免伤: 34, 治疗: 30, 控制截断: 28, 防护: 20, 闪避: 16, 反击: 4, 纯防御: 8 };
        return { 修正: 表[防御类型] || 0, 理由: '致命防御质量' };
      }
      if (来袭.severeDamage === true || 威胁 >= 危机层级阈值_V1.重度威胁) {
        const 表 = { 闪避: 22, 防护: 20, 反击: 18, 控制截断: 24, 治疗: 18, 免伤: 20, 纯防御: 6 };
        return { 修正: 表[防御类型] || 0, 理由: '高压防御质量' };
      }
      const 表 = { 反击: 14, 控制截断: 12, 闪避: 8, 防护: 6, 治疗: 6, 免伤: 4, 纯防御: -4 };
      return { 修正: 表[防御类型] || 0, 理由: '常规防御质量' };
    }

    function 计算友方保护排序修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, skill = {}, 候选 = {}) {
      if (!['治疗', '防护'].includes(行动类别) && !['治疗', '防护'].includes(技能类别.主类别)) {
        return { 修正: 0, 理由: [], 治疗目标收益: 0 };
      }
      const 权重 = 友方保护权重表_V1;
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const 目标 = target || actor;
      const 目标血量 = getCombatHpRatio(目标);
      const 快照 = buildConditionTacticalSnapshot(目标);
      const actor系别 = 读取规划单位系别(actor);
      let 修正 = 0;
      const 理由 = [];
      if (目标血量 <= 0.28) { 修正 += 权重.核心残血; 理由.push(`核心/队友残血 +${权重.核心残血}`); }
      else if (目标血量 <= 0.45) { 修正 += 权重.队友残血; 理由.push(`保护低血队友 +${权重.队友残血}`); }
      else if (目标血量 >= 0.78 && !快照.hasBadCondition && !快照.isLockedOrControlled && 来袭.lethalRisk !== true) {
        修正 += 权重.低收益治疗;
        理由.push(`低收益治疗 ${权重.低收益治疗}`);
      }
      if (来袭.lethalRisk === true || 来袭.severeDamage === true || 来袭.targetHitsDefender === true) { 修正 += 权重.致命来袭; 理由.push(`来袭保护 +${权重.致命来袭}`); }
      if (快照.hasTargetLock || 目标?.hasTargetLock === true) { 修正 += 权重.被锁定; 理由.push(`被锁定保护 +${权重.被锁定}`); }
      if (快照.hasBadCondition || 快照.isLockedOrControlled) { 修正 += 权重.异常控制; 理由.push(`异常/控制保护 +${权重.异常控制}`); }
      if (/驱散|净化|解除|清除/.test(String(skill?.name || skill?.魂技名 || skill?.技能名称 || 候选?.name || '')) && (快照.hasBadCondition || 快照.isLockedOrControlled)) {
        修正 += 24;
        理由.push('驱散异常 +24');
      }
      if (目标?.蓄力技能) { 修正 += 权重.我方蓄力; 理由.push(`保护蓄力单位 +${权重.我方蓄力}`); }
      if (['治疗系', '辅助系', '食物系'].includes(actor系别) && (目标血量 <= 0.55 || 快照.hasBadCondition || 来袭.lethalRisk === true)) {
        修正 += 权重.支援职责;
        理由.push(`支援职责 +${权重.支援职责}`);
      }
      return { 修正: Math.round(修正), 理由, 治疗目标收益: Math.round(Math.max(0, (1 - 目标血量) * 100)) };
    }

    function 读取友方保护压力_V1(战局画像 = {}, behaviorState = {}) {
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      let 强度 = 0;
      const 理由 = [];
      (战局画像.己方列表 || []).forEach(单位 => {
        const 血量 = getCombatHpRatio(单位);
        const 快照 = buildConditionTacticalSnapshot(单位);
        if (血量 > 0 && 血量 <= 0.32) { 强度 += 34; 理由.push('友方濒危'); }
        else if (血量 > 0 && 血量 <= 0.45) { 强度 += 20; 理由.push('友方低血'); }
        if (快照.hasBadCondition || 快照.isLockedOrControlled) { 强度 += 28; 理由.push('友方异常'); }
        if (快照.hasTargetLock || 单位?.hasTargetLock === true) { 强度 += 18; 理由.push('友方被锁定'); }
      });
      if (来袭.lethalRisk === true || 来袭.severeDamage === true || 来袭.targetHitsDefender === true) { 强度 += 22; 理由.push('来袭保护窗口'); }
      return { 强度, 理由: [...new Set(理由)] };
    }

    function 判定资源恢复规划动作_V1(技能类别 = {}, skill = {}, 候选 = {}) {
      if (技能类别?.是资源恢复 === true || 技能类别?.效果画像?.是否资源恢复 === true) return true;
      const 文本 = [skill?.name, skill?.魂技名, skill?.技能名称, 候选?.name].map(值 => String(值 || '')).join('|');
      if (/资源锁定|封锁资源|封魂|耗竭|增耗|锁定治疗|破盾资源锁定/.test(文本)) return false;
      return /资源恢复|补给|回气|回魂|食物|续航|魂力恢复|精神力恢复|体力恢复/.test(文本);
    }

    function 计算支援职责排序修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, skill = {}, 候选 = {}) {
      const 系别 = 读取规划单位系别(actor);
      const 保护压力 = 读取友方保护压力_V1(战局画像, behaviorState);
      const actor资源 = 读取规划综合资源比例_V1(actor);
      const 文本 = [skill?.name, skill?.魂技名, skill?.技能名称, 候选?.name].map(值 => String(值 || '')).join('|');
      const 是资源恢复动作 = 判定资源恢复规划动作_V1(技能类别, skill, 候选);
      const 是支援动作 = ['治疗', '防护', '资源', '召唤'].includes(技能类别.主类别) || ['治疗', '防护'].includes(行动类别);
      let 修正 = 0;
      const 理由 = [];
      if (系别 === '治疗系') {
        if (保护压力.强度 >= 34 && ['治疗', '防护'].includes(行动类别)) { 修正 += 支援职责权重表_V1.治疗系保护; 理由.push(`治疗系保护职责 +${支援职责权重表_V1.治疗系保护}`); }
        else if (保护压力.强度 < 20 && 行动类别 === '治疗') { 修正 += 支援职责权重表_V1.低收益支援; 理由.push(`低收益治疗 ${支援职责权重表_V1.低收益支援}`); }
      }
      if (系别 === '辅助系') {
        const 有异常 = (战局画像.己方列表 || []).some(单位 => {
          const 快照 = buildConditionTacticalSnapshot(单位);
          return 快照.hasBadCondition || 快照.isLockedOrControlled;
        });
        if (有异常 && /驱散|净化|解除|清除|庇护/.test(文本)) { 修正 += 支援职责权重表_V1.辅助系驱散; 理由.push(`辅助驱散职责 +${支援职责权重表_V1.辅助系驱散}`); }
        if (有异常 && 行动类别 === '控制') { 修正 += 支援职责权重表_V1.辅助系先手控制; 理由.push(`辅助先手控制 +${支援职责权重表_V1.辅助系先手控制}`); }
      }
      if (系别 === '食物系') {
        if ((actor资源 < 0.46 || Number(战局画像.资源压力 || 0) > 0.55) && 是资源恢复动作) {
          修正 += 支援职责权重表_V1.食物系资源支援;
          理由.push(`食物系资源支援 +${支援职责权重表_V1.食物系资源支援}`);
        }
        if ((actor资源 < 0.46 || Number(战局画像.资源压力 || 0) > 0.55) && !是资源恢复动作 && !候选?.可斩杀 && !候选?.可打断) {
          修正 += 支援职责权重表_V1.非职责普通进攻;
          理由.push(`食物低资源压普通动作 ${支援职责权重表_V1.非职责普通进攻}`);
        }
      }
      if (['治疗系', '辅助系', '食物系'].includes(系别) && 保护压力.强度 >= 42 && !是支援动作 && !候选?.可斩杀 && !候选?.可打断) {
       修正 += 支援职责权重表_V1.非职责普通进攻;
        理由.push(`支援窗口压普通进攻 ${支援职责权重表_V1.非职责普通进攻}`);
      }
      return { 修正: Math.round(修正), 理由 };
    }

    function 读取技能规划效果数组_V1(skill = {}, context = {}) {
      if (Array.isArray(skill?._效果数组)) return skill._效果数组;
      try {
        if (typeof getSkillEffects === 'function') {
          return getSkillEffects(skill, {
            行为规划: true,
            actor: context.actor,
            caster: context.actor,
            target: context.target,
            defender: context.target,
            skill,
          }) || [];
        }
      } catch (_) {
        return [];
      }
      return [];
    }

    function 解析技能效果原型画像_V1(skill = {}, candidate = {}, context = {}) {
      const 显式类别 = String(candidate?.行动类别 || skill?.__规划主类别 || '').trim();
      const 效果列表 = 读取技能规划效果数组_V1(skill, context);
      const 命中 = {
        输出: false,
        控制: false,
        治疗: false,
        防护: false,
        驱散: false,
        破防: false,
        资源: false,
        召唤: false,
        群体: false,
      };
      const 来源 = [];
      (效果列表 || []).forEach(效果 => {
        const 原型 = String(效果?.原型 || 效果?.类型 || '').trim();
        const 文本 = JSON.stringify(效果 || {});
        if (原型 === '伤害结算' || /伤害结算|威力倍率|damage/i.test(文本)) 命中.输出 = true;
        if (原型 === '状态施加' && /控制|眩晕|沉默|封技|定身|束缚|禁锢|skip_turn|cannot_react|silence/i.test(文本)) 命中.控制 = true;
        if (原型 === '属性修改' && /HP|生命|治疗|回血|恢复|heal|recover/i.test(文本) && !/扣除|伤害|damage/i.test(文本)) 命中.治疗 = true;
        if (原型 === '规则防御' || /护盾|免伤|减伤|闪避|格挡|庇护|shield|dodge|damage_reduce/i.test(文本)) 命中.防护 = true;
        if (/驱散|净化|解除|清除异常|dispell|dispel|cleanse/i.test(原型 + 文本)) 命中.驱散 = true;
        if (/破防|破盾|防御降低|护盾削减|穿透|defense_break|shield_break/i.test(原型 + 文本)) 命中.破防 = true;
        if (/资源修改|资源锁定|封锁资源|增耗|封魂|耗竭|恢复魂力|魂力恢复|精神力恢复|体力恢复|resource|cost_increase/i.test(原型 + 文本)) 命中.资源 = true;
        if (原型 === '召唤生成' || 原型 === '机制授予' || /召唤|维持|宿主|summon|maintain/i.test(文本)) 命中.召唤 = true;
        if (/群体|范围|全体|aoe|targets|目标数|波及/i.test(文本)) 命中.群体 = true;
      });
      if (Object.values(命中).some(Boolean)) 来源.push('效果数组');
      const 文本 = [
        skill?.name, skill?.魂技名, skill?.技能名称, skill?.描述, skill?.效果,
        candidate?.name, candidate?.行动名, candidate?.分支名,
      ].map(值 => String(值 || '')).join('|');
      if (!来源.length) {
        命中.输出 = /伤害|攻击|斩击|爆发|强攻|群攻/.test(文本);
        命中.控制 = /控制|打断|封技|沉默|眩晕|定身|束缚|禁锢/.test(文本);
        命中.治疗 = /治疗|恢复|回春|回血|疗愈/.test(文本);
        命中.防护 = /防护|护盾|守护|格挡|免伤|减伤|闪避|庇护/.test(文本);
        命中.驱散 = /驱散|净化|解除|清除/.test(文本);
        命中.破防 = /破防|破盾|穿透|防御剥夺|精神抗性剥夺/.test(文本);
        命中.资源 = /资源锁定|资源转移|资源恢复|补给|回气|回魂|食物|续航|封魂|耗竭|魂力消耗|精神力消耗/.test(文本);
        命中.召唤 = /召唤|宿主|维持/.test(文本);
        命中.群体 = /群体|范围|全体|横扫|波及/.test(文本);
        if (Object.values(命中).some(Boolean)) 来源.push('文本兜底');
      }
      if (显式类别) {
        if (显式类别 === '输出' || 显式类别 === '终局爆发') 命中.输出 = true;
        if (显式类别 === '控制') 命中.控制 = true;
        if (显式类别 === '治疗') 命中.治疗 = true;
        if (显式类别 === '防护') 命中.防护 = true;
        if (显式类别 === '破防') 命中.破防 = true;
        if (/资源/.test(显式类别)) 命中.资源 = true;
        if (显式类别 === '召唤') 命中.召唤 = true;
        来源.push('候选显式类别');
      }
      const 是恢复资源 = 命中.资源 && /资源恢复|补给|回气|回魂|食物|续航/.test(JSON.stringify(效果列表 || []) + 文本);
      const 主类别 = 是恢复资源 ? '资源'
        : 命中.治疗 ? '治疗'
          : (命中.驱散 || 命中.防护) && !命中.输出 ? '防护'
            : 命中.控制 ? '控制'
              : 命中.破防 ? '破防'
                : 命中.召唤 ? '召唤'
                  : 命中.资源 ? '资源'
                    : 命中.输出 ? '输出'
                      : '输出';
      const 次要效果类型列表 = Object.entries({
        输出: 命中.输出,
        控制: 命中.控制,
        治疗: 命中.治疗,
        防护: 命中.防护,
        驱散: 命中.驱散,
        破防: 命中.破防,
        资源锁定: 命中.资源,
        召唤维持: 命中.召唤,
        群体: 命中.群体,
      }).filter(([, 值]) => 值).map(([键]) => 键);
      return {
        主要效果类型: 主类别,
        次要效果类型列表,
        目标阵营: ['治疗', '防护'].includes(主类别) ? '友方' : '敌方',
        是否高耗: candidate?.高耗 === true || /高耗|大招|融合|真身/.test(文本),
        是否终局爆发: 命中.输出 && /爆发|大招|终局|斩杀|融合/.test(文本),
        是否控制截断: 命中.控制,
        是否治疗: 命中.治疗,
        是否防护: 命中.防护,
        是否驱散: 命中.驱散,
        是否破防: 命中.破防,
        是否资源锁定: 命中.资源,
        是否资源恢复: 是恢复资源,
        是否召唤维持: 命中.召唤,
        是否群体: 命中.群体,
        分类来源: 来源[0] || '默认输出',
        分类来源列表: [...new Set(来源)],
        效果列表,
      };
    }

    function 推断技能规划类别_V1(skill = {}, candidate = {}, context = {}) {
      const 效果画像 = 解析技能效果原型画像_V1(skill, candidate, context);
      const 文本 = [
        skill?.name, skill?.魂技名, skill?.技能名称, skill?.描述, skill?.效果,
        candidate?.name, candidate?.行动名, candidate?.分支名,
      ].map(值 => String(值 || '')).join('|');
      const 类别 = {
        是输出: 效果画像.主要效果类型 === '输出' || 效果画像.次要效果类型列表.includes('输出'),
        是控制: 效果画像.是否控制截断,
        是治疗: 效果画像.是否治疗,
        是防护: 效果画像.是否防护 || 效果画像.是否驱散,
        是破防: 效果画像.是否破防,
        是召唤: 效果画像.是否召唤维持,
        是资源: 效果画像.是否资源锁定,
        是资源恢复: 效果画像.是否资源恢复,
        是群体: 效果画像.是否群体,
      };
      const 主类别 = 效果画像.主要效果类型 || (/爆发|大招|终局/.test(文本) ? '输出' : '输出');
      return { ...类别, 主类别, 效果列表: 效果画像.效果列表, 效果画像 };
    }

    function 计算目标战术价值_V1(actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, 行动类别 = '输出', 候选 = {}) {
      if (!target) {
        return { 修正: 0, 分解: {}, 目标切换收益: 0, 切换成本: 0, 保护链理由: [], 仇恨压力理由: [], 协同理由: [], 理由: [] };
      }
      const 快照 = buildConditionTacticalSnapshot(target);
      const 目标血量 = getCombatHpRatio(target);
      const 目标威胁 = 计算对手单体威胁_V1(target, 战局画像, behaviorState);
      const 系别 = 读取规划单位系别(target);
      const 站位 = 读取规划站位标签_V1(target);
      const 距离系数 = 读取规划距离优先系数_V1(target);
      const 距离标签 = 读取规划距离标签_V1(target);
      const 敌方残血待救 = (战局画像.敌方列表 || []).some(单位 => 单位 !== target && getCombatHpRatio(单位) > 0 && getCombatHpRatio(单位) < 0.42);
      const 群体窗口 = 候选?.群体 === true || 技能类别?.是群体 === true;
      const 多目标收益 = 群体窗口
        ? (战局画像.敌方列表 || []).filter(单位 => getCombatHpRatio(单位) > 0 && (getCombatHpRatio(单位) <= 0.45 || 单位?.蓄力技能 || buildConditionTacticalSnapshot(单位).hasShielded)).length
        : 0;
      const 分解 = {
        可斩杀价值: 0,
        蓄力打断价值: 0,
        治疗救场价值: 0,
        控制核心价值: 0,
        续航核心价值: 0,
        输出核心价值: 0,
        保护链价值: 0,
        仇恨压力价值: 0,
        队友协同价值: 0,
        距离可达价值: 0,
        孤立暴露价值: 0,
        群体收益价值: 0,
        切换成本: 0,
      };
      const 理由 = [];
      const 保护链理由 = [];
      const 仇恨压力理由 = [];
      const 协同理由 = [];
      if (目标血量 > 0 && 目标血量 <= 0.32 && ['输出', '终局爆发'].includes(行动类别)) {
        分解.可斩杀价值 = 候选?.可斩杀 ? 32 : 24;
        理由.push(`可斩杀价值 +${分解.可斩杀价值}`);
      }
      if (target?.蓄力技能 && Number(target?.蓄力剩余 ?? 99) <= 1 && ['控制', '终局爆发'].includes(行动类别)) {
        分解.蓄力打断价值 = 行动类别 === '控制' ? 34 : 22;
        理由.push(`蓄力打断价值 +${分解.蓄力打断价值}`);
      }
      if (['治疗系', '辅助系', '食物系'].includes(系别) && 敌方残血待救 && ['输出', '控制', '终局爆发'].includes(行动类别)) {
        分解.治疗救场价值 = 行动类别 === '控制' ? 34 : 28;
        理由.push(`治疗救场价值 +${分解.治疗救场价值}`);
      } else if (['治疗系', '辅助系', '食物系'].includes(系别) && ['输出', '控制', '终局爆发'].includes(行动类别)) {
        分解.续航核心价值 = 行动类别 === '控制' ? 22 : 22;
        理由.push(`续航核心价值 +${分解.续航核心价值}`);
      }
      if (系别 === '控制系' && ['控制', '输出', '终局爆发'].includes(行动类别)) {
        分解.控制核心价值 = 12;
        理由.push('控制核心价值 +12');
      }
      if (['强攻系', '敏攻系'].includes(系别) && 目标威胁 > 0.46 && ['控制', '输出', '终局爆发'].includes(行动类别)) {
        分解.输出核心价值 = Math.round(Math.min(18, 目标威胁 * 28));
        理由.push(`输出核心价值 +${分解.输出核心价值}`);
      }
      if ((站位 === '前排' || target?.被保护 === true || target?.hasGuard === true || 快照.hasShielded || 快照.hasReactiveDefense) && ['破防', '资源', '控制'].includes(技能类别.主类别)) {
        分解.保护链价值 = 18;
        保护链理由.push('破前排/护盾保护链');
      } else if ((target?.保护链断裂 === true || target?.guardBroken === true || target?.防护者被控 === true) && ['输出', '终局爆发'].includes(行动类别)) {
        分解.保护链价值 = 22;
        保护链理由.push('保护链断裂后核心暴露');
      }
      if (快照.hasTargetLock || target?.hasTargetLock === true || 目标威胁 > 0.58) {
        分解.仇恨压力价值 = Math.round(Math.min(18, 8 + 目标威胁 * 16));
        仇恨压力理由.push('目标正在制造压力');
      }
      if (actor?.决策记忆?.last_action_target && String(actor.决策记忆.last_action_target) === String(target?.name || target?.名称 || '')) {
        分解.队友协同价值 = ['输出', '终局爆发', '控制'].includes(行动类别) ? 10 : 4;
        协同理由.push('延续上回合目标协同');
      }
      if (['输出', '终局爆发'].includes(行动类别)) {
        分解.距离可达价值 = 距离系数 >= 0.98 ? 10 : 距离系数 >= 0.9 ? 0 : -12;
        if (分解.距离可达价值) 理由.push(`${距离标签 || '未知距离'}可达价值 ${分解.距离可达价值 > 0 ? '+' : ''}${分解.距离可达价值}`);
      } else if (行动类别 === '控制' && target?.蓄力技能 && 距离标签 === '远距') {
        分解.距离可达价值 = 8;
        理由.push('控制越距截断 +8');
      }
      if ((target?.孤立 === true || target?.被保护 === false || target?.hasGuard === false) && 目标血量 > 0 && 目标血量 <= 0.45 && ['输出', '终局爆发'].includes(行动类别)) {
        分解.孤立暴露价值 = 16;
        理由.push('孤立暴露收割 +16');
      }
      if (多目标收益 >= 2) {
        分解.群体收益价值 = Math.min(28, 多目标收益 * 10);
        理由.push(`群体目标价值 +${分解.群体收益价值}`);
      }
      const 目标切换 = 计算目标切换排序修正_V1(候选, target, 行动类别, 战局画像, behaviorState, 技能类别);
      分解.切换成本 = Math.min(0, Number(目标切换.修正 || 0));
      const 修正 = Object.values(分解).reduce((总和, 值) => 总和 + Number(值 || 0), 0);
      return {
        修正: Math.round(修正),
        分解,
        目标切换收益: Math.max(0, Number(目标切换.修正 || 0)),
        切换成本: Math.abs(Math.min(0, Number(目标切换.修正 || 0))),
        保护链理由,
        仇恨压力理由,
        协同理由,
        理由: [...理由, ...保护链理由, ...仇恨压力理由, ...协同理由],
      };
    }

    function 判定候选固定自身目标_V1(skill = {}, candidate = {}) {
      if (skill?.__固定自身目标 === true || candidate?.固定自身目标 === true) return true;
      const 文本 = [
        skill?.name, skill?.魂技名, skill?.技能名称, skill?.目标, skill?.描述,
        candidate?.name, candidate?.行动名, candidate?.分支名,
      ].map(值 => String(值 || '')).join('|');
      const 防御类型 = 判定防御动作类型_V1(skill, candidate);
      if (['闪避', '免伤', '纯防御'].includes(防御类型)) return true;
      if (/危机自保|承伤硬抗|肉体兜底|伺机闪避|收招转防|借力守势|坚壁反制|撤离|待机|观察|守势维持|守势对峙/.test(文本)) return true;
      if (/自身/.test(String(skill?.目标 || '')) && !/友方/.test(String(skill?.目标 || ''))) return true;
      return false;
    }

    function 选择技能规划目标_V1(skill = {}, actor = {}, 规划上下文 = {}, 默认目标 = null) {
      const 目标表 = 规划上下文?.目标优先表 || {};
      const 当前敌方 = new Set((规划上下文?.战局画像?.敌方列表 || []).map(单位 => String(单位?.name || 单位?.名称 || '').trim()).filter(Boolean));
      const 当前友方 = new Set((规划上下文?.战局画像?.己方列表 || []).map(单位 => String(单位?.name || 单位?.名称 || '').trim()).filter(Boolean));
      const 敌方 = (目标表.敌方 || []).filter(项 => !当前敌方.size || 当前敌方.has(String(项?.单位?.name || 项?.单位?.名称 || 项?.名称 || '').trim()));
      const 友方 = (目标表.友方 || []).filter(项 => !当前友方.size || 当前友方.has(String(项?.单位?.name || 项?.单位?.名称 || 项?.名称 || '').trim()));
      const 技能类别 = 推断技能规划类别_V1(skill, {}, { actor, target: 默认目标 });
      const 类型 = skill?.__规划主类别 || 技能类别.主类别;
      const 锁定敌对目标 = 规划上下文?.锁定敌对目标 && typeof 规划上下文.锁定敌对目标 === 'object'
        ? 规划上下文.锁定敌对目标
        : null;
      const 锁定目标有效 =
        !!锁定敌对目标 &&
        (typeof isCombatUnitAlive !== 'function' || isCombatUnitAlive(锁定敌对目标)) &&
        (!当前敌方.size || 当前敌方.has(String(锁定敌对目标?.name || 锁定敌对目标?.名称 || '').trim()));
      if (判定候选固定自身目标_V1(skill, { name: skill?.__规划候选名 || skill?.name || skill?.魂技名 || skill?.技能名称 || '' })) {
        return actor || 默认目标;
      }
      if (
        锁定目标有效 &&
        !['治疗', '防护'].includes(类型) &&
        !技能偏向友方目标(skill || {}) &&
        inferSkillPrimaryTargetKind(skill || {}) !== '自身'
      ) {
        return 锁定敌对目标;
      }
      if (['治疗', '防护'].includes(类型)) {
        return 友方.slice().sort((左, 右) => {
          const 友方评分 = 项 => {
            const 单位 = 项?.单位 || {};
            const 快照 = buildConditionTacticalSnapshot(单位);
            let 分数 = Number(项?.优先级 || 0) + (1 - getCombatHpRatio(单位)) * 92;
            if (规划上下文?.战局画像?.我方核心 && typeof isCombatUnitIdentityMatch === 'function' && isCombatUnitIdentityMatch(单位, 规划上下文.战局画像.我方核心?.name || 规划上下文.战局画像.我方核心)) 分数 += 24;
            if (快照.hasBadCondition || 快照.isLockedOrControlled) 分数 += 28;
            if (单位?.蓄力技能) 分数 += 16;
            if (规划上下文?.来袭窗口摘要?.lethalRisk || 规划上下文?.threatProfile?.lethalRisk) 分数 += 18;
            return 分数;
          };
          return 友方评分(右) - 友方评分(左);
        })[0]?.单位 || 默认目标;
      }
      if (类型 === '控制' && 默认目标) {
        const 默认目标名 = String(默认目标?.name || 默认目标?.名称 || '').trim();
        const 默认目标仍在场 = !当前敌方.size || 当前敌方.has(默认目标名);
        const 默认目标项 = 敌方.find(项 => isCombatUnitIdentityMatch(项?.单位, 默认目标名) || String(项?.名称 || '').trim() === 默认目标名);
        const 默认目标理由 = (默认目标项?.理由 || []).map(理由 => String(理由 || '')).join('|');
        const 默认目标系别 = 读取规划单位系别(默认目标);
        const 默认目标快照 = buildConditionTacticalSnapshot(默认目标);
        const 同侧残血待救 = (规划上下文?.战局画像?.敌方列表 || []).some(单位 => (
          单位 !== 默认目标 &&
          getCombatHpRatio(单位) > 0 &&
          getCombatHpRatio(单位) < 0.42
        ));
        const 默认目标救场窗口 = 同侧残血待救 || getCombatHpRatio(默认目标) <= 0.5 || /待救|残血|治疗核心|后排核心|资源核心/.test(默认目标理由);
        if (
          默认目标仍在场 &&
          !默认目标快照.isLockedOrControlled &&
          默认目标救场窗口 &&
          ['治疗系', '辅助系', '食物系', '控制系'].includes(默认目标系别)
        ) {
          return 默认目标;
        }
      }
      const 评分 = 项 => {
        const 单位 = 项?.单位 || {};
        const 快照 = buildConditionTacticalSnapshot(单位);
        const 血量 = getCombatHpRatio(单位);
        const 单体威胁 = 计算对手单体威胁_V1(单位, 规划上下文.战局画像, 规划上下文);
        const 同侧残血待救 = (规划上下文?.战局画像?.敌方列表 || []).some(其他 => (
          其他 !== 单位 &&
          getCombatHpRatio(其他) > 0 &&
          getCombatHpRatio(其他) < 0.42
        ));
        let 分数 = Number(项?.优先级 || 0);
        if (类型 === '控制') {
          if (单位?.蓄力技能 && Number(单位?.蓄力剩余 ?? 1) <= 1) 分数 += 74;
          else if (单位?.蓄力技能) 分数 += 48;
          if (读取规划维持压力(单位) > 0) 分数 += 38;
          if (单体威胁 > 0.48) 分数 += 26;
          if (['治疗系', '控制系'].includes(读取规划单位系别(单位))) 分数 += 16;
        } else if (类型 === '破防') {
          if (快照.hasShielded || 快照.hasDefenseBuffed || 快照.hasReactiveDefense) 分数 += 54;
          if (读取规划单位系别(单位) === '防御系') 分数 += 22;
        } else if (类型 === '资源') {
          if (['治疗系', '辅助系', '食物系'].includes(读取规划单位系别(单位))) 分数 += 28;
          if (读取规划综合资源比例_V1(单位) > 0.55) 分数 += 18;
          if (读取规划维持压力(单位) > 0) 分数 += 20;
        } else {
          if (血量 > 0 && 血量 < 0.36) 分数 += 42;
          if (单体威胁 > 0.52 && 血量 < 0.55) 分数 += 26;
          if (['治疗系', '辅助系', '食物系'].includes(读取规划单位系别(单位))) 分数 += 18;
          if (同侧残血待救 && ['治疗系', '辅助系', '食物系'].includes(读取规划单位系别(单位))) 分数 += 34;
          if (读取规划距离优先系数_V1(单位) >= 0.98) 分数 += 10;
        }
          const 目标价值 = 计算目标战术价值_V1(actor, 单位, 规划上下文.战局画像, 规划上下文, { ...技能类别, 主类别: 类型 }, 类型 === '控制' ? '控制' : 类型 === '资源' ? '控制' : '输出', { target: 默认目标 });
          分数 += Math.round(Number(目标价值.修正 || 0) * 0.45);
          if (读取规划距离优先系数_V1(单位) < 0.9 && !(类型 === '控制' && 单位?.蓄力技能)) 分数 -= 18;
        return 分数;
      };
      return 敌方.slice().sort((左, 右) => 评分(右) - 评分(左))[0]?.单位 || 默认目标;
    }

    function 推断上回合效果标签_V1(actor = {}, target = {}, 规划上下文 = {}) {
      const 标签 = new Set();
      const 上次行动 = String(规划上下文?.行为历史摘要?.上次行动 || actor?.决策记忆?.last_action || '');
      if (/控制|打断|封技|眩晕|定身|沉默/.test(上次行动)) 标签.add('控制');
      if (/破防|破盾|防御剥夺|穿透/.test(上次行动)) 标签.add('破防');
      if (/治疗|恢复|回春|回血/.test(上次行动)) 标签.add('治疗');
      if (/召唤|宿主/.test(上次行动)) 标签.add('召唤');
      if (/资源锁定|封魂|耗竭/.test(上次行动)) 标签.add('资源锁定');
      const 目标快照 = buildConditionTacticalSnapshot(target || {});
      if (目标快照.isLockedOrControlled) 标签.add('控制');
      if (目标快照.hasDefenseBreak || target?.防御破除 || target?.破防) 标签.add('破防');
      if (目标快照.hasShielded || 目标快照.hasReactiveDefense) 标签.add('召唤');
      return 标签;
    }

    function 计算对手意图排序修正_V1(行动类别 = '输出', target = null, 战局画像 = {}, behaviorState = {}, 技能类别 = {}) {
      const 对手意图 = 汇总对手意图预测_V1(战局画像, behaviorState);
      const 系别 = 读取规划单位系别(target || {});
      const 目标威胁 = target ? 计算对手单体威胁_V1(target, 战局画像, behaviorState) : 0;
      let 修正 = 0;
      const 理由 = [];
      if ((对手意图.爆发 >= 28 || 对手意图.输出 >= 34) && ['控制', '防护', '终局爆发'].includes(行动类别)) {
        const 增量 = 行动类别 === '控制' ? 8 : 行动类别 === '防护' ? 14 : 18;
        修正 += 增量; 理由.push(`应对爆发 +${增量}`);
      }
      if ((对手意图.治疗 >= 24 || 对手意图.保核 >= 28) && ['治疗系', '辅助系', '食物系'].includes(系别) && ['输出', '控制', '终局爆发'].includes(行动类别)) {
        const 敌方有残血待救 = (战局画像.敌方列表 || []).some(单位 => 单位 !== target && 单位?.状态?.存活 !== false && getCombatHpRatio(单位) > 0 && getCombatHpRatio(单位) < 0.42);
        const 增量 = 行动类别 === '控制' ? (敌方有残血待救 ? 68 : 24) : (敌方有残血待救 ? 48 : 22);
        修正 += 增量; 理由.push(`压治疗核心 +${增量}`);
      }
      if (对手意图.控制 >= 28 && ['控制', '防护', '治疗'].includes(行动类别)) {
        const 增量 = 行动类别 === '控制' ? 6 : 12;
        修正 += 增量; 理由.push(`应对控制 +${增量}`);
      }
      if (对手意图.资源保留 >= 26 && ['召唤', '资源'].includes(技能类别.主类别)) {
        修正 += 12; 理由.push('压节奏/资源恢复 +12');
      }
      if (对手意图.召唤 >= 18 && (技能类别.是输出 || 技能类别.是破防 || /宿主|召唤/.test(String(target?.name || target?.名称 || '')))) {
        修正 += 14; 理由.push('破召唤压力 +14');
      }
      if (target?.蓄力技能 && Number(target?.蓄力剩余 ?? 1) <= 1 && ['控制', '终局爆发'].includes(行动类别)) {
        修正 += 行动类别 === '控制' ? 30 : 18; 理由.push('蓄力截断窗口');
      }
      if (目标威胁 > 0.52 && ['控制', '输出', '终局爆发'].includes(行动类别)) {
        const 增量 = 行动类别 === '控制' ? Math.round(Math.min(4, 目标威胁 * 8)) : Math.round(Math.min(22, 目标威胁 * 26));
        修正 += 增量; 理由.push('高威胁目标响应');
      }
      return { 修正: Math.round(修正), 理由, 对手意图 };
    }

    function 读取对手近期行为摘要_V1(actor = {}, behaviorState = {}) {
      const 来源 = [
        ...(Array.isArray(behaviorState?.对手近期行动) ? behaviorState.对手近期行动 : []),
        ...(Array.isArray(behaviorState?.规划上下文?.行为历史摘要?.对手近期行动) ? behaviorState.规划上下文.行为历史摘要.对手近期行动 : []),
        ...(Array.isArray(actor?.决策记忆?.opponent_recent_actions) ? actor.决策记忆.opponent_recent_actions : []),
      ].map(值 => String(值 || '')).filter(Boolean).slice(-3);
      const 文本 = 来源.join('|');
      return {
        连续治疗: 来源.filter(值 => /治疗|回春|恢复|保核/.test(值)).length >= 2 || /连续治疗/.test(文本),
        连续控制: 来源.filter(值 => /控制|封技|打断|沉默|眩晕/.test(值)).length >= 2 || /连续控制/.test(文本),
        连续高耗: 来源.filter(值 => /高耗|大招|融合|爆发|真身/.test(值)).length >= 2 || /连续高耗/.test(文本),
        防守低资源: /低资源防守|资源保留|防守/.test(文本),
        来源,
      };
    }

    function 计算对手适应排序修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, skill = {}, 候选 = {}) {
      const 摘要 = 读取对手近期行为摘要_V1(actor, behaviorState);
      const target系别 = 读取规划单位系别(target || {});
      const 文本 = [skill?.name, skill?.魂技名, skill?.技能名称, 候选?.name].map(值 => String(值 || '')).join('|');
      const 目标快照 = buildConditionTacticalSnapshot(target || {});
      let 修正 = 0;
      const 理由 = [];
      if (摘要.连续治疗 && ['治疗系', '辅助系', '食物系'].includes(target系别) && ['输出', '控制', '终局爆发'].includes(行动类别)) {
        修正 += 对手适应权重表_V1.连续治疗压制;
        理由.push(`对方连续治疗压制 +${对手适应权重表_V1.连续治疗压制}`);
      }
      if (摘要.连续治疗 && 技能类别.主类别 === '资源') {
        修正 += 对手适应权重表_V1.连续治疗压制;
        理由.push(`资源锁定治疗节奏 +${对手适应权重表_V1.连续治疗压制}`);
      }
      if (摘要.连续控制 && /驱散|净化|解除|清除|庇护/.test(文本)) {
        修正 += 对手适应权重表_V1.连续控制驱散;
        理由.push(`对方连续控制驱散 +${对手适应权重表_V1.连续控制驱散}`);
      }
      if (摘要.连续控制 && 行动类别 === '控制') {
        修正 += 对手适应权重表_V1.连续控制先手;
        理由.push(`对方连续控制先手 +${对手适应权重表_V1.连续控制先手}`);
      }
      if (摘要.连续控制 && ['治疗', '防护'].includes(行动类别) && (目标快照.isLockedOrControlled || 目标快照.hasAbnormalState || /驱散|净化|解除|清除|庇护/.test(文本))) {
        修正 += 对手适应权重表_V1.连续控制保护;
        理由.push(`对方连续控制保护 +${对手适应权重表_V1.连续控制保护}`);
      }
      if (摘要.连续高耗 && ['控制', '防护'].includes(行动类别)) {
        修正 += 对手适应权重表_V1.连续高耗截断;
        理由.push(`对方连续高耗截断 +${对手适应权重表_V1.连续高耗截断}`);
      }
      if (摘要.连续高耗 && ['输出', '终局爆发'].includes(行动类别) && getCombatHpRatio(target || {}) <= 0.55) {
        修正 += 对手适应权重表_V1.连续高耗反打;
        理由.push(`对方连续高耗反打 +${对手适应权重表_V1.连续高耗反打}`);
      }
      if (摘要.防守低资源 || (战局画像.敌方列表 || []).some(单位 => 读取规划综合资源比例_V1(单位) < 0.30)) {
        if (['输出', '控制'].includes(行动类别)) { 修正 += 对手适应权重表_V1.对方低资源压节奏; 理由.push(`对方低资源压节奏 +${对手适应权重表_V1.对方低资源压节奏}`); }
        if (['资源', '召唤'].includes(技能类别.主类别)) { 修正 += 对手适应权重表_V1.对方低资源恢复; 理由.push(`对方低资源我方恢复 +${对手适应权重表_V1.对方低资源恢复}`); }
      }
      return { 修正: Math.round(修正), 理由, 摘要 };
    }

    function 判定候选池压力档位_V1(actor = {}, 战局画像 = {}, behaviorState = {}) {
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const actorHp = getCombatHpRatio(actor);
      const 敌方 = 战局画像.敌方列表 || [];
      const 下回合威胁 = 预估对方下回合最大威胁_V1(战局画像, behaviorState);
      const 有蓄力 = 敌方.some(单位 => 单位?.蓄力技能 && Number(单位?.蓄力剩余 ?? 99) <= 1);
      const 敌方最低血 = Math.min(1, ...敌方.map(getCombatHpRatio));
      if (来袭.lethalRisk === true || actorHp < 0.28 || 下回合威胁 >= 0.82) return '致命';
      if (敌方最低血 > 0 && 敌方最低血 <= 0.32) return '终局';
      if (有蓄力 || 来袭.severeDamage === true || 下回合威胁 >= 0.62 || actorHp < 0.42) return '高压';
      if (下回合威胁 >= 0.38 || actorHp < 0.65) return '中压';
      return '低压';
    }

    function 判定战斗阶段节奏_V1(actor = {}, 战局画像 = {}, behaviorState = {}) {
      const 回合 = Number(behaviorState?.round ?? behaviorState?.回合 ?? behaviorState?.当前回合 ?? 1);
      const actor资源 = 读取规划综合资源比例_V1(actor);
      const 敌方 = 战局画像.敌方列表 || [];
      const 敌方存活 = 敌方.filter(单位 => 单位?.状态?.存活 !== false && getCombatHpRatio(单位) > 0);
      const 敌方最低血 = Math.min(1, ...敌方存活.map(getCombatHpRatio));
      const 威胁 = 预估对方下回合最大威胁_V1(战局画像, behaviorState);
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      if (actor资源 < 0.32 || Number(战局画像.资源压力 || 0) > 0.70) return { 阶段: '资源枯竭', 理由: '资源低或资源压力高' };
      if (敌方存活.length <= 1 && 敌方最低血 > 0 && 敌方最低血 <= 0.45) return { 阶段: '残局收割', 理由: '敌方少且低血' };
      if (敌方最低血 > 0 && 敌方最低血 <= 0.32) return { 阶段: '终局', 理由: '存在终结窗口' };
      if (回合 <= 2 && 威胁 < 0.62 && 来袭.lethalRisk !== true) return { 阶段: '开局', 理由: '前两回合且未进入重压' };
      return { 阶段: '中盘', 理由: '常规竞争阶段' };
    }

    function 计算阶段节奏排序修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, 候选 = {}) {
      const 阶段判断 = 判定战斗阶段节奏_V1(actor, 战局画像, behaviorState);
      const 阶段 = 阶段判断.阶段;
      const 高耗 = 候选?.高耗 === true || Number(候选?.断档压力 || 0) >= 0.34;
      const 目标血量 = getCombatHpRatio(target || {});
      let 修正 = 0;
      const 理由 = [阶段判断.理由];
      if (阶段 === '开局') {
        if (高耗 && !候选?.可打断 && !候选?.可斩杀) { 修正 += 阶段节奏权重表_V1.开局高耗; 理由.push(`开局压高耗 ${阶段节奏权重表_V1.开局高耗}`); }
        if (['召唤', '资源'].includes(技能类别.主类别) || (行动类别 === '控制' && !高耗) || (行动类别 === '输出' && !高耗)) { 修正 += 阶段节奏权重表_V1.开局准备; 理由.push(`开局准备 +${阶段节奏权重表_V1.开局准备}`); }
      } else if (阶段 === '中盘') {
        if (['输出', '控制', '防护', '治疗'].includes(行动类别)) { 修正 += 阶段节奏权重表_V1.中盘竞争; 理由.push(`中盘竞争 +${阶段节奏权重表_V1.中盘竞争}`); }
      } else if (阶段 === '终局') {
        if (['输出', '终局爆发'].includes(行动类别)) { 修正 += 阶段节奏权重表_V1.终局爆发; 理由.push(`终局爆发 +${阶段节奏权重表_V1.终局爆发}`); }
        if (行动类别 === '控制' && target?.蓄力技能) { 修正 += 阶段节奏权重表_V1.终局控制; 理由.push(`终局截断 +${阶段节奏权重表_V1.终局控制}`); }
      } else if (阶段 === '资源枯竭') {
        if (['资源', '召唤'].includes(技能类别.主类别)) { 修正 += 阶段节奏权重表_V1.资源枯竭恢复; 理由.push(`资源枯竭恢复 +${阶段节奏权重表_V1.资源枯竭恢复}`); }
        else if (!高耗 && ['输出', '防护'].includes(行动类别)) { 修正 += 12; 理由.push('资源枯竭低耗竞争 +12'); }
        else if (!高耗 && 行动类别 === '控制') { 修正 += 10; 理由.push('资源枯竭功能控制 +10'); }
        if (高耗 && !候选?.可斩杀 && !候选?.可打断) { 修正 += 阶段节奏权重表_V1.资源枯竭高耗; 理由.push(`资源枯竭压高耗 ${阶段节奏权重表_V1.资源枯竭高耗}`); }
      } else if (阶段 === '残局收割') {
        if (['输出', '终局爆发'].includes(行动类别) && 目标血量 > 0 && 目标血量 <= 0.50) { 修正 += 阶段节奏权重表_V1.残局收割; 理由.push(`残局收割 +${阶段节奏权重表_V1.残局收割}`); }
        if (行动类别 === '防护') { 修正 += 阶段节奏权重表_V1.残局防御; 理由.push(`残局防御惯性 ${阶段节奏权重表_V1.残局防御}`); }
      }
      return { 修正: Math.round(修正), 阶段, 理由 };
    }

    function 计算候选类别预算修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, 候选 = {}) {
      const 压力档位 = 判定候选池压力档位_V1(actor, 战局画像, behaviorState);
      const actor系别 = 读取规划单位系别(actor);
      const targetHp = getCombatHpRatio(target || {});
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const 保护压力 = 读取友方保护压力_V1(战局画像, behaviorState);
      let 修正 = 0;
      const 理由 = [];
      if (压力档位 === '低压') {
        if (['输出', '终局爆发'].includes(行动类别)) { 修正 += 20; 理由.push('低压输出预算 +20'); }
        if (['资源', '召唤'].includes(技能类别.主类别)) { 修正 += 12; 理由.push('低压资源/召唤预算 +12'); }
        if (行动类别 === '控制') { 修正 -= 24; 理由.push('低压控制降温 -24'); }
        if (行动类别 === '防护') { 修正 -= 16; 理由.push('低压防御预算 -16'); }
      } else if (压力档位 === '中压') {
        if (行动类别 === '输出') { 修正 += 18; 理由.push('中压输出竞争 +18'); }
        if (行动类别 === '控制') { 修正 += 14; 理由.push('中压控制竞争 +14'); }
      } else if (压力档位 === '高压') {
        if (行动类别 === '控制') { 修正 += 8; 理由.push('高压控制预算 +8'); }
        if (行动类别 === '防护' || 行动类别 === '治疗') { 修正 += 10; 理由.push('高压保命预算 +10'); }
      } else if (压力档位 === '致命') {
        if (['控制', '防护', '治疗'].includes(行动类别)) { 修正 += 18; 理由.push('致命保命预算 +18'); }
      } else if (压力档位 === '终局') {
        if (['输出', '终局爆发'].includes(行动类别)) { 修正 += 20; 理由.push('终局输出预算 +20'); }
      }
      if (['强攻系', '敏攻系'].includes(actor系别) && ['输出', '终局爆发'].includes(行动类别) && 压力档位 !== '致命') {
        修正 += 10;
        理由.push('攻敏输出职责 +10');
      }
      if (targetHp >= 0.25 && targetHp <= 0.45 && 来袭.lethalRisk !== true && ['输出', '终局爆发'].includes(行动类别)) {
        const 系别加成 = ['强攻系', '敏攻系'].includes(actor系别) ? 8 : 0;
        修正 += 28 + 系别加成;
        理由.push(`收割竞争 +${28 + 系别加成}`);
      }
      if (保护压力.强度 >= 42) {
        if (['治疗', '防护'].includes(行动类别)) { 修正 += 26; 理由.push('友方保护窗口 +26'); }
        else if (!候选?.可斩杀 && !候选?.可打断) { 修正 -= 34; 理由.push('保护窗口压普通进攻 -34'); }
      } else if (保护压力.强度 >= 28) {
        if (['治疗', '防护'].includes(行动类别)) { 修正 += 24; 理由.push('友方异常保护 +24'); }
        else if (!候选?.可斩杀 && !候选?.可打断) { 修正 -= 34; 理由.push('异常保护压普通进攻 -34'); }
      }
      return { 修正: Math.round(修正), 理由, 压力档位 };
    }

    function 计算资源断档排序修正_V1(候选 = {}, 行动类别 = '输出', behaviorState = {}) {
      if (候选.资源可行 === false) return { 修正: -999, 硬否决: true, 理由: ['资源不可释放'] };
      const 断档 = Math.max(Number(候选.断档压力 || 0), Number(候选.资源压力 || 0));
      const 高耗 = 候选.高耗 === true || 断档 >= 0.34;
      const 高耗关键收益 = (候选.可斩杀 ? 28 : 0)
        + (候选.可打断 ? 26 : 0)
        + (候选.阻止治疗救场 ? 22 : 0)
        + ((behaviorState?.threatProfile?.lethalRisk === true || behaviorState?.来袭窗口摘要?.lethalRisk === true) ? 24 : 0)
        + (Number(候选.终局压力 || 0) > 0.58 ? 20 : 0);
      if (!高耗) return { 修正: 0, 硬否决: false, 理由: [], 高耗关键收益: 0, 资源断档惩罚: 0, 高价复读惩罚: 0 };
      if (高耗关键收益 > 0) return { 修正: 高耗关键收益, 硬否决: false, 理由: [`高耗关键收益 +${高耗关键收益}`], 高耗关键收益, 资源断档惩罚: 0 };
      const 惩罚 = 高耗 ? Math.round(Math.min(72, 34 + 断档 * 46 + (Number(候选.原始净收益 || 0) < 45 ? 16 : 0))) : 0;
      const 理由 = [];
      if (惩罚 > 0) 理由.push(`资源断档惩罚 -${惩罚}`);
      return { 修正: -惩罚, 硬否决: false, 理由, 高耗关键收益: 0, 资源断档惩罚: 惩罚, 高价复读惩罚: 0 };
    }

    function 计算资源节奏排序修正_V1(行动类别 = '输出', actor = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, skill = {}, 候选 = {}) {
      const 记忆 = actor?.决策记忆 || {};
      const 上次行动 = String(behaviorState?.规划上下文?.行为历史摘要?.上次行动 || 记忆.last_action || '');
      const 最近高耗 = /高耗|大招|融合|真身|终局/.test(上次行动) || 记忆.last_action_high_cost === true;
      const actor资源 = 读取规划综合资源比例_V1(actor);
      const 高耗 = 候选?.高耗 === true || Number(候选?.断档压力 || 0) >= 0.34 || 行动类别 === '终局爆发';
      const 文本 = [skill?.name, skill?.魂技名, skill?.技能名称, 候选?.name].map(值 => String(值 || '')).join('|');
      const 资源动作 = 判定资源恢复规划动作_V1(技能类别, skill, 候选);
      const 低耗动作 = !高耗 && ['输出', '防护', '治疗'].includes(行动类别);
      const 关键控制窗口 = 候选?.可打断 || 候选?.阻止治疗救场 || 候选?.可斩杀 || 候选?.target?.蓄力技能;
      const 终局窗口 = 候选?.可斩杀 || 候选?.可打断 || Number(候选?.终局压力 || 0) > 0.58 || behaviorState?.threatProfile?.lethalRisk === true || behaviorState?.来袭窗口摘要?.lethalRisk === true;
      let 修正 = 0;
      const 理由 = [];
      if (最近高耗 && !终局窗口) {
        if (资源动作) { 修正 += 资源节奏权重表_V1.高耗后恢复; 理由.push(`高耗后恢复 +${资源节奏权重表_V1.高耗后恢复}`); }
        if (低耗动作) { 修正 += 资源节奏权重表_V1.高耗后低耗防护; 理由.push(`高耗后低耗节奏 +${资源节奏权重表_V1.高耗后低耗防护}`); }
        if (高耗) { 修正 += 资源节奏权重表_V1.高耗后连续高耗; 理由.push(`非终局连续高耗 ${资源节奏权重表_V1.高耗后连续高耗}`); }
      }
      if (actor资源 < 0.34 || Number(战局画像.资源压力 || 0) > 0.70) {
        if (资源动作) { 修正 += 资源节奏权重表_V1.资源枯竭恢复; 理由.push(`资源枯竭恢复 +${资源节奏权重表_V1.资源枯竭恢复}`); }
        if (低耗动作) { 修正 += 资源节奏权重表_V1.资源枯竭低耗; 理由.push(`资源枯竭低耗 +${资源节奏权重表_V1.资源枯竭低耗}`); }
        if (行动类别 === '控制' && !高耗 && 关键控制窗口) { 修正 += 14; 理由.push('资源枯竭关键控制 +14'); }
        else if (行动类别 === '控制' && !高耗 && !关键控制窗口) { 修正 -= 18; 理由.push('资源枯竭非关键控制 -18'); }
        if (高耗 && !终局窗口) { 修正 += 资源节奏权重表_V1.资源枯竭高耗; 理由.push(`资源枯竭压高耗 ${资源节奏权重表_V1.资源枯竭高耗}`); }
      }
      return { 修正: Math.round(修正), 理由 };
    }

    function 计算距离可达排序修正_V1(行动类别 = '输出', target = {}, 技能类别 = {}) {
      if (!target || ['治疗', '防护'].includes(行动类别)) return { 修正: 0, 理由: [], 距离标签: '' };
      const 距离标签 = 读取规划距离标签_V1(target);
      if (!距离标签) return { 修正: 0, 理由: [], 距离标签: '' };
      if (距离标签 === '近距') return { 修正: ['输出', '终局爆发'].includes(行动类别) ? 10 : 4, 理由: ['近距可达'], 距离标签 };
      if (距离标签 === '中距') return { 修正: ['输出', '终局爆发'].includes(行动类别) ? -4 : 0, 理由: ['中距可达'], 距离标签 };
      if (行动类别 === '控制' && target?.蓄力技能) return { 修正: 8, 理由: ['远距蓄力仍需截断'], 距离标签 };
      if (技能类别.主类别 === '破防' || 技能类别.主类别 === '资源') return { 修正: -6, 理由: ['远距功能技能轻衰减'], 距离标签 };
      return { 修正: -18, 理由: ['远距可达衰减'], 距离标签 };
    }

    function 计算目标切换排序修正_V1(候选 = {}, target = null, 行动类别 = '输出', 战局画像 = {}, behaviorState = {}, 技能类别 = {}) {
      const 原目标 = 候选.target || null;
      const 目标变更 = 原目标 && target && (
        typeof isCombatUnitIdentityMatch === 'function'
          ? !isCombatUnitIdentityMatch(原目标, target?.name || target?.名称 || target)
          : String(原目标?.name || 原目标?.名称 || 原目标) !== String(target?.name || target?.名称 || target)
      );
      if (!目标变更) return { 修正: 0, 理由: [] };
      const targetHp = getCombatHpRatio(target || {});
      const 快照 = buildConditionTacticalSnapshot(target || {});
      const 治疗救场 = ['治疗系', '辅助系', '食物系'].includes(读取规划单位系别(target || {}))
        && (战局画像.敌方列表 || []).some(单位 => 单位 !== target && getCombatHpRatio(单位) > 0 && getCombatHpRatio(单位) < 0.42);
      if (target?.蓄力技能 && Number(target?.蓄力剩余 ?? 99) <= 1 && 行动类别 === '控制') return { 修正: 14, 理由: ['切换打断蓄力 +14'] };
      if (治疗救场 && ['输出', '控制', '终局爆发'].includes(行动类别)) return { 修正: 12, 理由: ['切换压治疗核心 +12'] };
      if (targetHp > 0 && targetHp <= 0.34 && ['输出', '终局爆发'].includes(行动类别)) return { 修正: 10, 理由: ['切换收割高威胁 +10'] };
      if ((快照.hasShielded || 快照.hasDefenseBuffed || 读取规划维持压力(target || {}) > 0) && (技能类别.是破防 || 技能类别.主类别 === '资源')) return { 修正: 10, 理由: ['切换命中护盾/维持 +10'] };
      return { 修正: -6, 理由: ['普通目标切换 -6'] };
    }

    function 计算危机恢复排序修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}) {
      const 危机 = 判定危机层级_V1(战局画像, behaviorState, actor);
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const 敌方蓄力 = (战局画像.敌方列表 || []).some(单位 => 单位?.蓄力技能 && Number(单位?.蓄力剩余 ?? 99) <= 1);
      const 低压恢复 = 危机.层级 === '无危机' && 来袭.lethalRisk !== true && 来袭.severeDamage !== true && !敌方蓄力;
      const 恢复标记 = behaviorState?.危机刚解除 === true || behaviorState?.危机恢复 === true || 来袭.危机刚解除 === true || 来袭.crisisRecovered === true || 低压恢复;
      let 修正 = 0;
      const 理由 = [];
      if (恢复标记) {
        if (['输出', '终局爆发'].includes(行动类别)) { 修正 += 危机恢复权重表_V1.输出恢复; 理由.push(`危机下降后输出恢复 +${危机恢复权重表_V1.输出恢复}`); }
        if (行动类别 === '控制') { 修正 += 危机恢复权重表_V1.控制压制; 理由.push(`危机下降后控制压制 +${危机恢复权重表_V1.控制压制}`); }
        if (['资源', '召唤'].includes(技能类别.主类别)) { 修正 += 危机恢复权重表_V1.资源恢复; 理由.push(`危机下降后资源重整 +${危机恢复权重表_V1.资源恢复}`); }
        if (行动类别 === '防护') { 修正 += 危机恢复权重表_V1.防御惯性; 理由.push(`危机后防御惯性 ${危机恢复权重表_V1.防御惯性}`); }
      }
      if (低压恢复 && 行动类别 === '防护') { 修正 += 危机恢复权重表_V1.低压纯防御; 理由.push(`低压纯防御 ${危机恢复权重表_V1.低压纯防御}`); }
      if (判定反击窗口_V1(actor, 战局画像, behaviorState) && ['输出', '控制', '终局爆发'].includes(行动类别)) {
        修正 += 危机恢复权重表_V1.反击窗口输出;
        理由.push(`反击窗口恢复 +${危机恢复权重表_V1.反击窗口输出}`);
      }
      return { 修正: Math.round(修正), 理由, 危机层级: 危机.层级 };
    }

    function 计算空间收益排序修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, skill = {}, 候选 = {}) {
      if (!target || ['治疗', '防护'].includes(行动类别)) return { 阵型修正: 0, 范围收益修正: 0, 群体收益修正: 0, 理由: [] };
      const 敌方 = 战局画像.敌方列表 || [];
      const 文本 = [skill?.name, skill?.魂技名, skill?.技能名称, skill?.描述, 候选?.name].map(值 => String(值 || '')).join('|');
      const 是群体 = 候选?.群体 === true || 技能类别?.是群体 === true || /群体|范围|全体|横扫|波及|aoe/i.test(文本);
      const 多残血 = 敌方.filter(单位 => 单位?.状态?.存活 !== false && getCombatHpRatio(单位) > 0 && getCombatHpRatio(单位) <= 0.45).length;
      const 多蓄力 = 敌方.filter(单位 => 单位?.状态?.存活 !== false && 单位?.蓄力技能 && Number(单位?.蓄力剩余 ?? 99) <= 1).length;
      const 多破防 = 敌方.filter(单位 => {
        const 快照 = buildConditionTacticalSnapshot(单位);
        return 单位?.状态?.存活 !== false && (快照.hasDefenseBreak || 单位?.破防 || 单位?.防御破除);
      }).length;
      const 目标快照 = buildConditionTacticalSnapshot(target);
      const 站位 = 读取规划站位标签_V1(target);
      const 距离标签 = 读取规划距离标签_V1(target);
      const 目标系别 = 读取规划单位系别(target);
      const 目标血量 = getCombatHpRatio(target);
      const 被保护 = target?.被保护 === true || target?.hasGuard === true || target?.守护者 || target?.保护者 || 目标快照.hasShielded || 目标快照.hasReactiveDefense;
      const 前排破口 = 站位 === '前排' && (目标快照.hasShielded || 目标快照.hasDefenseBuffed || 被保护) && ['控制', '破防', '资源'].includes(技能类别.主类别);
      const 后排核心 = 站位 === '后排' && ['治疗系', '辅助系', '食物系', '控制系'].includes(目标系别);
      const 孤立 = (target?.孤立 === true || target?.被保护 === false || target?.hasGuard === false || 距离标签 === '近距') && 目标血量 > 0 && 目标血量 <= 0.45 && !被保护;
      const 保护链断裂 = target?.保护链断裂 === true || target?.防护者被控 === true || target?.guardBroken === true;
      let 阵型修正 = 0;
      let 范围收益修正 = 0;
      let 群体收益修正 = 0;
      const 理由 = [];
      if (是群体 && 多残血 >= 2) { 群体收益修正 += 空间收益权重表_V1.群体残血; 理由.push(`群体残血 +${空间收益权重表_V1.群体残血}`); }
      if (是群体 && 多蓄力 >= 2) { 群体收益修正 += 空间收益权重表_V1.群体蓄力; 理由.push(`群体蓄力 +${空间收益权重表_V1.群体蓄力}`); }
      if (是群体 && 多破防 >= 2) { 群体收益修正 += 空间收益权重表_V1.群体破防; 理由.push(`群体破防 +${空间收益权重表_V1.群体破防}`); }
      if (后排核心 && ['控制', '输出', '终局爆发'].includes(行动类别)) { 阵型修正 += 空间收益权重表_V1.后排核心; 理由.push(`后排核心 +${空间收益权重表_V1.后排核心}`); }
      if (孤立 && ['输出', '终局爆发'].includes(行动类别)) { 阵型修正 += 空间收益权重表_V1.孤立收割; 理由.push(`孤立收割 +${空间收益权重表_V1.孤立收割}`); }
      if (孤立 && 行动类别 === '控制') { 阵型修正 += 空间收益权重表_V1.孤立压制; 理由.push(`孤立压制 +${空间收益权重表_V1.孤立压制}`); }
      if (前排破口) { 阵型修正 += 空间收益权重表_V1.前排保护破口; 理由.push(`前排保护破口 +${空间收益权重表_V1.前排保护破口}`); }
      if (保护链断裂 && ['输出', '终局爆发'].includes(行动类别)) { 阵型修正 += 空间收益权重表_V1.保护链断裂; 理由.push(`保护链断裂 +${空间收益权重表_V1.保护链断裂}`); }
      if (距离标签 === '远距' && ['输出', '终局爆发'].includes(行动类别) && !target?.蓄力技能 && !后排核心) { 范围收益修正 += 空间收益权重表_V1.远距普通输出; 理由.push(`远距普通输出 ${空间收益权重表_V1.远距普通输出}`); }
      if (距离标签 === '远距' && 行动类别 === '控制' && target?.蓄力技能) { 范围收益修正 += 空间收益权重表_V1.远距蓄力控制; 理由.push(`远距蓄力控制 +${空间收益权重表_V1.远距蓄力控制}`); }
      return { 阵型修正: Math.round(阵型修正), 范围收益修正: Math.round(范围收益修正), 群体收益修正: Math.round(群体收益修正), 理由 };
    }

    function 计算短期战术记忆排序修正_V1(actor = {}, target = {}, 行动类别 = '输出', 技能类别 = {}, 候选 = {}, behaviorState = {}) {
      const 记忆 = actor?.决策记忆 || {};
      const 上次行动 = String(behaviorState?.规划上下文?.行为历史摘要?.上次行动 || 记忆.last_action || '');
      const 上次目标 = String(behaviorState?.规划上下文?.行为历史摘要?.上次目标 || 记忆.last_action_target || '');
      const 目标名 = String(target?.name || target?.名称 || '');
      const 目标快照 = buildConditionTacticalSnapshot(target || {});
      const 标签 = [];
      let 修正 = 0;
      if ((/控制|打断|封技|眩晕|定身|沉默/.test(上次行动) || 目标快照.isLockedOrControlled) && ['输出', '终局爆发'].includes(行动类别) && (!上次目标 || !目标名 || 上次目标 === 目标名 || 目标快照.isLockedOrControlled)) {
        修正 += 短期战术记忆权重表_V1.控制后输出;
        标签.push('控制后输出');
      }
      if ((/破防|破盾|穿透|防御剥夺/.test(上次行动) || 目标快照.hasDefenseBreak || target?.破防 === true || target?.保护链断裂 === true) && ['输出', '终局爆发'].includes(行动类别) && (!上次目标 || !目标名 || 上次目标 === 目标名 || 目标快照.hasDefenseBreak || target?.破防 === true || target?.保护链断裂 === true)) {
        修正 += 短期战术记忆权重表_V1.破防后爆发;
        标签.push('破防后爆发');
      }
      if ((记忆.last_action_failed_to_kill === true || /未击杀|未斩杀/.test(上次行动)) && getCombatHpRatio(target || {}) > 0 && getCombatHpRatio(target || {}) <= 0.42 && ['输出', '终局爆发'].includes(行动类别)) {
        修正 += 短期战术记忆权重表_V1.未击杀收割;
        标签.push('未击杀收割');
      }
      if (/治疗|回春|庇护|保护/.test(上次行动) && ['防护', '资源', '召唤'].includes(技能类别.主类别)) {
        修正 += 短期战术记忆权重表_V1.治疗后防护;
        标签.push('治疗后拖回合');
        if (判定资源恢复规划动作_V1(技能类别, 候选?.skill || {}, 候选)) {
          修正 += 短期战术记忆权重表_V1.治疗后资源重整;
          标签.push('治疗后资源重整');
        }
      }
      if ((记忆.last_action_countered === true || 记忆.last_action_failed === true) && 上次行动 && new RegExp(上次行动.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')).test(String(候选?.name || ''))) {
        修正 += 短期战术记忆权重表_V1.无效同类;
        标签.push('刚被反制');
      }
      return { 修正: Math.round(修正), 标签, 理由: 标签 };
    }

    function 计算重复效用衰减_V1(选项 = {}) {
      const 重复次数 = Math.max(0, Number(选项.重复次数 ?? 选项.repeatCount ?? 0));
      const 同类重复次数 = Math.max(0, Number(选项.同类重复次数 ?? 选项.sameCategoryRepeatCount ?? 0));
      const 是高影响 = 选项.是高影响 === true || /终局爆发|控制|输出|压制/.test(String(选项.行动类别 || '')) || 选项.是高耗 === true;
      const 豁免 = (() => {
        if (选项.玩家锁定动作 === true) return 'PLAYER_LOCKED_ACTION';
        if (选项.只有一个合法动作 === true) return 'ONLY_LEGAL_ACTION';
        if (选项.可斩杀 === true) return 'LETHAL_WINDOW';
        if (选项.紧急治疗 === true) return 'EMERGENCY_HEAL';
        if (选项.致命截断 === true) return 'LETHAL_INTERRUPT';
        return '';
      })();
      let 倍率 = 1;
      let 原因码 = '';
      let 类别 = 'none';
      if (重复次数 >= 2) {
        倍率 = 0.10;
        原因码 = 'SAME_ACTION_REPEAT_HEAVY';
        类别 = 'same_action';
      } else if (重复次数 === 1) {
        倍率 = 0.35;
        原因码 = 'SAME_ACTION_REPEAT';
        类别 = 'same_action';
      } else if (是高影响 && 同类重复次数 > 0) {
        倍率 = 0.65;
        原因码 = 'SAME_CATEGORY_HIGH_IMPACT';
        类别 = 'same_category';
      }
      return {
        repeatCount: 重复次数,
        sameCategoryRepeatCount: 同类重复次数,
        repeatCategory: 类别,
        repeatDecayMultiplier: 倍率,
        repeatDecayReasonCode: 原因码,
        repeatExceptionCode: 豁免,
        repeatDecayApplied: 倍率 < 0.999,
      };
    }

    function 格式化重复效用衰减说明_V1(衰减 = {}) {
      const 倍率 = Number(衰减?.repeatDecayMultiplier || 1);
      if (!Number.isFinite(倍率) || 倍率 >= 0.999) return '';
      const 标签 = 衰减.repeatDecayReasonCode === 'SAME_CATEGORY_HIGH_IMPACT' ? '同类高影响动作复读衰减' : '重复释放衰减';
      const 豁免映射 = {
        LETHAL_WINDOW: '终结窗口豁免：目标濒死',
        EMERGENCY_HEAL: '救急窗口豁免：友方濒危',
        LETHAL_INTERRUPT: '截断窗口豁免：对方致命动作',
        ONLY_LEGAL_ACTION: '保底豁免：无其他合法动作',
        PLAYER_LOCKED_ACTION: '玩家锁定豁免：手动指定',
      };
      const 豁免 = 豁免映射[衰减.repeatExceptionCode] || '';
      return `${标签} x${倍率.toFixed(2)}${豁免 ? `；${豁免}` : ''}`;
    }

    function 计算连招排序修正_V1(actor = {}, target = {}, 规划上下文 = {}, 行动类别 = '输出', 技能类别 = {}) {
      const 标签 = 推断上回合效果标签_V1(actor, target, 规划上下文);
      let 修正 = 0;
      const 理由 = [];
      if (标签.has('控制') && (行动类别 === '输出' || 行动类别 === '终局爆发')) {
        修正 += 技能时机约束表_V1.控制后输出加成;
        理由.push(`控制后输出 +${技能时机约束表_V1.控制后输出加成}`);
      }
      if (标签.has('破防') && (行动类别 === '输出' || 行动类别 === '终局爆发')) {
        修正 += 技能时机约束表_V1.破防后爆发加成;
        理由.push(`破防后爆发 +${技能时机约束表_V1.破防后爆发加成}`);
      }
      if (标签.has('治疗') && ['防护', '资源', '召唤'].includes(技能类别.主类别)) {
        修正 += 技能时机约束表_V1.治疗后拖回合加成;
        理由.push(`治疗后拖回合 +${技能时机约束表_V1.治疗后拖回合加成}`);
        if (技能类别.是资源恢复 === true || 技能类别.效果画像?.是否资源恢复 === true || 行动类别 === '资源保留') {
          修正 += 技能时机约束表_V1.治疗后资源重整加成;
          理由.push(`治疗后资源重整 +${技能时机约束表_V1.治疗后资源重整加成}`);
        }
      }
      if (标签.has('召唤') && ['防护', '控制'].includes(行动类别)) {
        修正 += 技能时机约束表_V1.召唤后护宿主加成;
        理由.push(`召唤后护宿主 +${技能时机约束表_V1.召唤后护宿主加成}`);
      }
      if (标签.has('资源锁定') && ['输出', '控制', '终局爆发'].includes(行动类别)) {
        修正 += 14; 理由.push('资源锁定后压制 +14');
      }
      return { 修正, 理由, 标签: [...标签] };
    }

    function 推断行动者战斗职责_V1(actor = {}) {
      const 系别 = 读取规划单位系别(actor);
      const 职责表 = {
        强攻系: ['输出', '破防', '终局爆发', '压核心'],
        敏攻系: ['收割', '切后排', '打断', '追残血'],
        控制系: ['打断', '封技', '控治疗', '控爆发'],
        防御系: ['保核心', '承压', '反击', '压高输出'],
        辅助系: ['驱散', '保护', '资源节奏', '先手控制'],
        治疗系: ['治疗低血', '保核心', '清异常'],
        食物系: ['资源支援', '持续支援', '低压准备', '保队友'],
      };
      return { 系别, 职责: 职责表[系别] || ['均衡'], 职责文本: (职责表[系别] || ['均衡']).join('|') };
    }

    function 计算职责画像排序修正_V1(行动类别 = '输出', actor = {}, target = {}, 战局画像 = {}, behaviorState = {}, 技能类别 = {}, skill = {}, 候选 = {}) {
      const 画像 = 推断行动者战斗职责_V1(actor);
      const 职责文本 = 画像.职责文本;
      const targetHp = getCombatHpRatio(target || {});
      const 保护压力 = 读取友方保护压力_V1(战局画像, behaviorState);
      const 压力档位 = 判定候选池压力档位_V1(actor, 战局画像, behaviorState);
      const 文本 = [skill?.name, skill?.魂技名, skill?.技能名称, 候选?.name].map(值 => String(值 || '')).join('|');
      let 修正 = 0;
      const 理由 = [];
      if (/输出|压核心/.test(职责文本) && ['输出', '终局爆发'].includes(行动类别)) {
        修正 += 职责画像权重表_V1.主职输出;
        理由.push(`输出职责 +${职责画像权重表_V1.主职输出}`);
      }
      if (/收割|追残血/.test(职责文本) && ['输出', '终局爆发'].includes(行动类别) && targetHp > 0 && targetHp <= 0.45) {
        修正 += 职责画像权重表_V1.主职收割;
        理由.push(`收割职责 +${职责画像权重表_V1.主职收割}`);
      }
      if (/打断|封技|控治疗|控爆发|先手控制/.test(职责文本) && 行动类别 === '控制' && (target?.蓄力技能 || ['治疗系', '强攻系', '控制系'].includes(读取规划单位系别(target || {})))) {
       修正 += 职责画像权重表_V1.主职打断;
        理由.push(`控制职责 +${职责画像权重表_V1.主职打断}`);
      }
      if (/破防/.test(职责文本) && 技能类别.主类别 === '破防') {
        修正 += 职责画像权重表_V1.主职破防;
        理由.push(`破防职责 +${职责画像权重表_V1.主职破防}`);
      }
      if (/保核心|承压|保护|清异常/.test(职责文本) && ['治疗', '防护'].includes(行动类别) && 保护压力.强度 >= 28) {
        修正 += 职责画像权重表_V1.主职保护;
        理由.push(`保护职责 +${职责画像权重表_V1.主职保护}`);
      }
      if (/驱散|清异常/.test(职责文本) && /驱散|净化|解除|清除|庇护/.test(文本)) {
        修正 += 职责画像权重表_V1.主职驱散;
        理由.push(`驱散职责 +${职责画像权重表_V1.主职驱散}`);
      }
      if (/资源支援|持续支援|低压准备|资源节奏/.test(职责文本) && 判定资源恢复规划动作_V1(技能类别, skill, 候选)) {
        修正 += 职责画像权重表_V1.主职资源;
        理由.push(`资源职责 +${职责画像权重表_V1.主职资源}`);
      }
      if (['治疗系', '辅助系', '食物系'].includes(画像.系别) && 保护压力.强度 >= 34 && !['治疗', '防护'].includes(行动类别) && 技能类别.主类别 !== '资源' && !候选?.可斩杀 && !候选?.可打断) {
        修正 += 职责画像权重表_V1.支援窗口压偏离;
        理由.push(`支援窗口偏离 ${职责画像权重表_V1.支援窗口压偏离}`);
      }
      if (压力档位 === '低压' && 行动类别 === '防护' && !/保核心|承压/.test(职责文本)) {
        修正 += 职责画像权重表_V1.非关键防御降温;
        理由.push(`低压非职责防御 ${职责画像权重表_V1.非关键防御降温}`);
      }
      return { 修正: Math.round(修正), 画像, 理由 };
    }

    function 评估二回合前瞻_V1(候选 = {}, actor = {}, target = {}, 候选列表 = [], 战局画像 = {}, 对手意图 = {}, behaviorState = {}, 行动类别 = '输出', 技能类别 = {}) {
      const 高耗 = 候选?.高耗 === true || Number(候选?.断档压力 || 0) >= 0.34 || 行动类别 === '终局爆发';
      const actor资源 = 读取规划综合资源比例_V1(actor);
      const 下回合威胁 = 预估对方下回合最大威胁_V1(战局画像, behaviorState);
      const 来袭 = { ...(behaviorState?.来袭窗口摘要 || {}), ...(behaviorState?.threatProfile || {}) };
      const 终局放行 = 候选?.可斩杀 || 候选?.可打断 || 候选?.阻止治疗救场 || 来袭.lethalRisk === true || Number(候选?.终局压力 || 0) > 0.58;
      const 目标系别 = 读取规划单位系别(target || {});
      const 目标血量 = getCombatHpRatio(target || {});
      const 有下回合保命候选 = (候选列表 || []).some(项 => {
        const 名称 = String(项?.name || 项?.skill?.name || 项?.skill?.魂技名 || '');
        return /治疗|回春|免伤|防护|护盾|驱散|庇护|封技|控制|打断/.test(名称) && 项 !== 候选;
      });
      let 修正 = 0;
      const 前瞻理由 = [];
      const 资源后果 = [];
      const 敌方下回合风险 = [];
      const 己方下回合窗口 = [];
      if (高耗 && !终局放行 && (actor资源 < 0.48 || Number(候选?.断档压力 || 0) > 0.55)) {
        修正 += 二回合前瞻权重表_V1.高耗断档;
        资源后果.push('高耗后下回合资源断档');
      }
      if (高耗 && !终局放行 && 下回合威胁 > 0.62 && 有下回合保命候选) {
        修正 += 二回合前瞻权重表_V1.下回合保命断档;
        资源后果.push('保命候选可能断档');
      }
      if (判定资源恢复规划动作_V1(技能类别, 候选?.skill || {}, 候选) && (actor资源 < 0.42 || Number(战局画像.资源压力 || 0) > 0.62)) {
        修正 += 二回合前瞻权重表_V1.资源恢复;
        资源后果.push('本回合恢复保障下回合动作');
      }
      if ((Number(对手意图.爆发 || 0) >= 28 || Number(对手意图.输出 || 0) >= 34 || target?.蓄力技能) && ['控制', '防护'].includes(行动类别)) {
        const 增量 = 行动类别 === '控制' ? 二回合前瞻权重表_V1.打断爆发 : 18;
        修正 += 增量;
        敌方下回合风险.push(`敌方爆发风险，当前${行动类别} +${增量}`);
      }
      if ((Number(对手意图.治疗 || 0) >= 24 || Number(对手意图.保核 || 0) >= 28) && ['治疗系', '辅助系', '食物系'].includes(目标系别) && ['输出', '控制', '终局爆发'].includes(行动类别)) {
        修正 += 二回合前瞻权重表_V1.阻止治疗救场;
        敌方下回合风险.push(`阻止治疗救场 +${二回合前瞻权重表_V1.阻止治疗救场}`);
      }
      if (Number(对手意图.控制 || 0) >= 28 && ['防护', '治疗', '控制'].includes(行动类别)) {
        修正 += 二回合前瞻权重表_V1.先手防控;
        敌方下回合风险.push(`先手防控 +${二回合前瞻权重表_V1.先手防控}`);
      }
      if (技能类别.主类别 === '破防' && target && (buildConditionTacticalSnapshot(target).hasShielded || buildConditionTacticalSnapshot(target).hasDefenseBuffed || target?.被保护 === true)) {
        修正 += 二回合前瞻权重表_V1.破防后窗口;
        己方下回合窗口.push(`破防后爆发窗口 +${二回合前瞻权重表_V1.破防后窗口}`);
      }
      if (行动类别 === '控制' && target && (target?.蓄力技能 || 候选?.阻止治疗救场 || Number(对手意图.爆发 || 0) >= 28 || Number(对手意图.控制 || 0) >= 28)) {
        修正 += 二回合前瞻权重表_V1.控制后窗口;
        己方下回合窗口.push(`控制后输出窗口 +${二回合前瞻权重表_V1.控制后窗口}`);
      }
      if (行动类别 === '治疗' && getCombatHpRatio(target || actor) <= 0.50) {
        修正 += 二回合前瞻权重表_V1.治疗后重整;
        己方下回合窗口.push(`治疗后资源/防护重整 +${二回合前瞻权重表_V1.治疗后重整}`);
      }
      if (!候选?.可斩杀 && ['输出', '终局爆发'].includes(行动类别) && 目标血量 > 0.32 && 目标血量 <= 0.48 && actor资源 < 0.46 && 高耗) {
        修正 += 二回合前瞻权重表_V1.保留斩杀资源;
        资源后果.push('当前不斩杀，保留资源等下回合收割');
      }
      前瞻理由.push(...资源后果, ...敌方下回合风险, ...己方下回合窗口);
      return {
        二回合前瞻修正: Math.round(修正),
        资源后果,
        敌方下回合风险,
        己方下回合窗口,
        前瞻理由,
      };
    }

    function 排序行为规划候选_V1(actor = {}, 战局画像 = {}, 候选列表 = [], behaviorState = {}) {
      const 上下文 = {
        ...(behaviorState || {}),
        战局画像,
        threatProfile: behaviorState?.threatProfile || behaviorState?.来袭窗口摘要 || {},
        来袭窗口摘要: behaviorState?.来袭窗口摘要 || behaviorState?.threatProfile || {},
      };
      return (候选列表 || []).map(候选 => {
        const skill = 候选.skill || 候选;
        const 防御类型 = 判定防御动作类型_V1(skill, 候选);
        const 固定自身目标 = 判定候选固定自身目标_V1(skill, 候选);
        const 原始技能类别 = 推断技能规划类别_V1(skill, 候选, { actor, target: 候选.target });
        const 指定主类别 = 候选.行动类别 === '控制' ? '控制'
          : 候选.行动类别 === '治疗' ? '治疗'
            : 候选.行动类别 === '防护' ? '防护'
              : 固定自身目标 ? '防护'
              : 候选.行动类别 === '终局爆发' ? '输出'
                : '';
        const 技能类别 = {
          ...原始技能类别,
          主类别: 指定主类别 || 原始技能类别.主类别,
          是控制: 原始技能类别.是控制 || 候选.行动类别 === '控制',
          是治疗: 原始技能类别.是治疗 || 候选.行动类别 === '治疗',
          是防护: 原始技能类别.是防护 || 候选.行动类别 === '防护',
        };
        const 目标上下文 = { ...上下文, 目标优先表: 候选.目标优先表 || behaviorState?.目标优先表 || 上下文?.目标优先表 || { 敌方: [], 友方: [] } };
        const 目标 = 固定自身目标
          ? actor
          : 选择技能规划目标_V1({ ...skill, __规划主类别: 技能类别.主类别, __固定自身目标: 固定自身目标, __规划候选名: 候选?.name || '' }, actor, 目标上下文, 候选.target || null);
        const 行动类别 = 候选.行动类别 || (技能类别.主类别 === '治疗' ? '治疗'
          : 技能类别.主类别 === '防护' ? '防护'
              : 技能类别.主类别 === '控制' ? '控制'
                : 技能类别.主类别 === '资源' ? (技能类别.是资源恢复 ? '资源保留' : '控制')
                  : /爆发|大招|终局/.test(String(候选.name || skill?.name || skill?.魂技名 || '')) ? '终局爆发'
                    : '输出');
        const 风险 = 评估我方行动后一阶风险_V1(行动类别, actor, 目标, 战局画像, 上下文);
        const 防御质量 = 评分防御动作质量_V1(防御类型, actor, 目标 || actor, 上下文);
        const 对手意图响应 = 计算对手意图排序修正_V1(行动类别, 目标, 战局画像, 上下文, 技能类别);
        const 连招 = 计算连招排序修正_V1(actor, 目标, 上下文?.规划上下文 || 上下文, 行动类别, 技能类别);
        const 类别预算 = 计算候选类别预算修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别, 候选);
        const 友方保护 = 计算友方保护排序修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别, skill, 候选);
        const 距离可达 = 计算距离可达排序修正_V1(行动类别, 目标, 技能类别);
        const 阶段节奏 = 计算阶段节奏排序修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别, 候选);
        const 危机恢复 = 计算危机恢复排序修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别);
        const 空间收益 = 计算空间收益排序修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别, skill, 候选);
        const 短期记忆 = 计算短期战术记忆排序修正_V1(actor, 目标, 行动类别, 技能类别, 候选, 上下文);
        const 支援职责 = 计算支援职责排序修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别, skill, 候选);
        const 资源节奏 = 计算资源节奏排序修正_V1(行动类别, actor, 战局画像, 上下文, 技能类别, skill, 候选);
        const 对手适应 = 计算对手适应排序修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别, skill, 候选);
        const 目标价值 = 计算目标战术价值_V1(actor, 目标, 战局画像, 上下文, 技能类别, 行动类别, 候选);
        const 职责画像 = 计算职责画像排序修正_V1(行动类别, actor, 目标, 战局画像, 上下文, 技能类别, skill, 候选);
        const 二回合前瞻 = 评估二回合前瞻_V1(候选, actor, 目标, 候选列表, 战局画像, 对手意图响应.对手意图 || {}, 上下文, 行动类别, 技能类别);
        const 记忆键 = 读取行为记忆候选键_V1(skill, 候选);
        const 重复 = 读取归一行为记忆值_V1(actor?.决策记忆?.recent_actions || {}, 记忆键, 0);
        const 技能名 = String(skill?.name || skill?.魂技名 || skill?.技能名称 || 候选?.name || '').trim();
        const 闪避克制次数 = 读取闪避克制记忆(actor, 目标);
        const 目标敏攻拉扯 = 闪避克制次数 > 0 && (
          读取规划单位系别(目标) === '敏攻系' ||
          Number(目标?.final?.agi ?? 目标?.agi ?? 0) > Number(actor?.final?.agi ?? actor?.agi ?? 0)
        );
        const 闪避未中惩罚 = getActorSkillDodgeWhiffPenalty(actor, 技能名, 目标);
        const 技能目标文本 = String(getSkillTarget(skill || {}) || '').trim();
        const 效果列表 = 读取技能规划效果数组_V1(skill || {}, { actor, target: 目标 });
        const 是单体前压 =
          !固定自身目标 &&
          !技能偏向友方目标(skill || {}, 候选) &&
          !/群体|全场/.test(技能目标文本) &&
          (
            技能类别.主类别 === '输出' ||
            技能类别.主类别 === '控制' ||
            技能类别.主类别 === '削弱'
          );
        const 技能具备反敏攻价值 =
          !!skill && (
            (typeof isBattleSkillControlProfile === 'function' && isBattleSkillControlProfile(skill)) ||
            (typeof hasBattleSkillRuntimeConsumer === 'function' && hasBattleSkillRuntimeConsumer(skill, ['interrupt', 'hard_control', 'skill_seal', 'self_shift', 'disengage_shift', 'position_exchange', 'pursuit_shift'])) ||
            /群体|全场/.test(技能目标文本) ||
            效果列表.some(effect => {
              const 原型 = String(effect?.原型 || '').trim();
              const 战斗效果 = effect?.计算层效果 || {};
              if (['资源锁定', '规则改写', '决策干扰', '判定修正', '时窗修正'].includes(原型)) return true;
              if (原型 !== '状态施加') return false;
              return Number(战斗效果.hit_bonus || 0) > 0 ||
                Number(战斗效果.lock_level || 0) > 0 ||
                Number(战斗效果.dodge_penalty || 0) > 0 ||
                Number(战斗效果.cast_speed_penalty || 0) > 0 ||
                战斗效果.skip_turn === true ||
                战斗效果.cannot_react === true ||
                战斗效果.silence === true ||
                战斗效果.skill_seal === true ||
                战斗效果.blind === true;
            })
          );
        const 敏攻硬追惩罚 = 目标敏攻拉扯 && 是单体前压
          ? (
              技能具备反敏攻价值
                ? Math.max(0, 8 + 闪避克制次数 * 4)
                : 36 + 闪避克制次数 * 16 + Math.max(0, 重复 - 1) * 18 + (闪避未中惩罚 > 0 ? 12 : 0)
            )
          : 0;
        const 反敏攻价值得分 = 目标敏攻拉扯 && 技能具备反敏攻价值 ? 12 + 闪避克制次数 * 6 : 0;
        const 关键控制窗口 = 候选?.可打断 || 候选?.阻止治疗救场 || 目标?.蓄力技能 || 对手意图响应.理由.some(理由 => /蓄力|治疗核心|致命|爆发/.test(String(理由)));
        const 资源判定 = 计算资源断档排序修正_V1({ ...候选, 重复, 关键控制窗口 }, 行动类别, 上下文);
        const 资源修正 = 资源判定.修正;
        const 斩杀修正 = 候选.可斩杀 ? 36 : 0;
        const 目标切换 = 计算目标切换排序修正_V1(候选, 目标, 行动类别, 战局画像, 上下文, 技能类别);
        const 目标修正 = 目标切换.修正;
        const 关键理由 = 候选.可斩杀 || 候选.可打断 || 上下文?.threatProfile?.lethalRisk === true || 上下文?.来袭窗口摘要?.lethalRisk === true
          || 关键控制窗口;
        const 记忆惩罚 = 0;
        const 原始权重 = Number(候选.原始权重 ?? 候选.weight ?? 候选.基础收益 ?? 0);
        const 原始净收益 = Number(候选.原始净收益 ?? 候选.净收益 ?? 原始权重);
        const 战术修正 = Number(候选.战术修正 || 0);
        const 审计效果分类 = 固定自身目标
          ? {
              主要效果类型: '防护',
              次要效果类型列表: ['防护'],
              目标阵营: '友方',
              是否群体: false,
              是否资源恢复: false,
            }
          : {
              主要效果类型: 技能类别.效果画像?.主要效果类型 || 技能类别.主类别,
              次要效果类型列表: 技能类别.效果画像?.次要效果类型列表 || [],
              目标阵营: 技能类别.效果画像?.目标阵营 || (['治疗', '防护'].includes(技能类别.主类别) ? '友方' : '敌方'),
              是否群体: 技能类别.效果画像?.是否群体 === true,
              是否资源恢复: 技能类别.效果画像?.是否资源恢复 === true,
            };
        const 审计分类来源 = 固定自身目标 ? '防御动作语义' : (技能类别.效果画像?.分类来源 || '默认输出');
        const 同类重复次数 = 读取归一行为记忆值_V1(actor?.决策记忆?.recent_actions || {}, [行动类别, 技能类别.主类别], 0);
        const 重复衰减 = 计算重复效用衰减_V1({
          重复次数: 重复,
          同类重复次数: Math.max(0, 同类重复次数 - 重复),
          行动类别,
          是高耗: 候选.高耗 === true,
          可斩杀: 候选.可斩杀 === true,
          紧急治疗: 行动类别 === '治疗' && getCombatHpRatio(目标 || actor) < 0.45,
          致命截断: 关键控制窗口 && (上下文?.threatProfile?.lethalRisk === true || 上下文?.来袭窗口摘要?.lethalRisk === true),
        });
        const 衰减说明 = 格式化重复效用衰减说明_V1(重复衰减);
        const 衰减前分数 = 资源判定.硬否决 ? 0 : Math.max(0, Math.round(
          原始权重 + 原始净收益 * 0.35 + 战术修正 + 目标修正 - 风险.风险变化 * 80
          + 防御质量.修正 + 对手意图响应.修正 + 类别预算.修正 + 友方保护.修正 + 距离可达.修正
          + 阶段节奏.修正 + 危机恢复.修正 + 空间收益.阵型修正 + 空间收益.范围收益修正 + 空间收益.群体收益修正 + 短期记忆.修正
          + 支援职责.修正 + 资源节奏.修正 + 对手适应.修正 + 目标价值.修正 + 职责画像.修正 + 二回合前瞻.二回合前瞻修正
          + 连招.修正 - 记忆惩罚 - 闪避未中惩罚 - 敏攻硬追惩罚 + 反敏攻价值得分 + 资源修正 + 斩杀修正,
        ));
        const 分数 = Math.round(衰减前分数 * Number(重复衰减.repeatDecayMultiplier || 1));
        return {
          ...候选,
          target: 目标,
          weight: Math.max(0, 分数),
          __候选排序审计: {
            候选来源: 候选?.__候选来源 || 候选?.候选来源 || '',
            技能: skill?.name || skill?.魂技名 || skill?.技能名称 || 候选?.name || '',
            原始权重,
            原始净收益,
            战术修正,
            目标修正,
            对手意图修正: 对手意图响应.修正,
            类别预算修正: 类别预算.修正,
            友方保护修正: 友方保护.修正,
            距离修正: 距离可达.修正,
            阶段修正: 阶段节奏.修正,
            危机恢复修正: 危机恢复.修正,
            阵型修正: 空间收益.阵型修正,
            范围收益修正: 空间收益.范围收益修正,
            群体收益修正: 空间收益.群体收益修正,
            短期记忆修正: 短期记忆.修正,
            支援职责修正: 支援职责.修正,
            资源节奏修正: 资源节奏.修正,
            对手适应修正: 对手适应.修正,
            目标价值修正: 目标价值.修正,
            职责修正: 职责画像.修正,
            二回合前瞻修正: 二回合前瞻.二回合前瞻修正,
            防御质量修正: 防御质量.修正,
            资源修正,
            高耗关键收益: Number(资源判定.高耗关键收益 || 0),
            资源断档惩罚: Number(资源判定.资源断档惩罚 || 0),
            高价复读惩罚: Number(资源判定.高价复读惩罚 || 0),
            连招修正: 连招.修正,
            记忆惩罚,
            闪避未中惩罚,
            敏攻硬追惩罚,
            反敏攻价值得分,
            斩杀修正,
            repeatCount: 重复衰减.repeatCount,
            repeatCategory: 重复衰减.repeatCategory,
            repeatDecayMultiplier: 重复衰减.repeatDecayMultiplier,
            repeatDecayReasonCode: 重复衰减.repeatDecayReasonCode,
            repeatExceptionCode: 重复衰减.repeatExceptionCode,
            repeatDecayReasonText: 衰减说明,
            scoreBeforeRepeatDecay: 衰减前分数,
            scoreAfterRepeatDecay: Math.max(0, 分数),
            最终权重: Math.max(0, 分数),
            行动类别,
            技能类别: 技能类别.主类别,
            效果原型分类: 审计效果分类,
            分类来源: 审计分类来源,
            目标: (typeof 读取规划单位名称 === 'function' ? 读取规划单位名称(目标) : '') || 目标?.name || 目标?.名称 || '',
            风险,
            防御类型,
            防御质量,
            阶段判断: 阶段节奏.阶段,
            阶段理由: 阶段节奏.理由,
            危机恢复理由: 危机恢复.理由,
            危机层级: 危机恢复.危机层级,
            对手意图理由: 对手意图响应.理由,
            类别预算理由: 类别预算.理由,
            保护理由: 友方保护.理由,
            治疗目标收益: 友方保护.治疗目标收益,
            可达性理由: 距离可达.理由,
            空间理由: 空间收益.理由,
            记忆命中标签: 短期记忆.标签,
            支援职责理由: 支援职责.理由,
            高耗后理由: 资源节奏.理由,
            对手适应理由: 对手适应.理由,
            对手适应摘要: 对手适应.摘要,
            目标价值分解: 目标价值.分解,
            目标切换收益: 目标价值.目标切换收益,
            切换成本: 目标价值.切换成本,
            保护链理由: 目标价值.保护链理由,
            仇恨压力理由: 目标价值.仇恨压力理由,
            协同理由: 目标价值.协同理由,
            职责画像: 职责画像.画像,
            职责理由: 职责画像.理由,
            前瞻理由: 二回合前瞻.前瞻理由,
            资源后果: 二回合前瞻.资源后果,
            敌方下回合风险: 二回合前瞻.敌方下回合风险,
            己方下回合窗口: 二回合前瞻.己方下回合窗口,
            连续目标理由: 目标切换.理由,
            距离标签: 距离可达.距离标签,
            压力档位: 类别预算.压力档位,
            连招理由: 连招.理由,
            资源理由: 资源判定.理由,
            目标修正理由: 目标切换.理由,
            重复,
            选择原因: 资源判定.硬否决 ? '资源不可释放' : '候选池统一排序',
          },
        };
      }).sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0));
    }

    // ===== Phase D: 战场情境表 — 10 个独立情境,数据驱动加权 =====
    // 不再依赖"残血/压力"两条触发线,显式枚举常见战场情境。每条情境独立检查 + 加权。
    // 数据信号都来自现有 战局画像 / behaviorState / actor.属性.状态效果, 无需新加 schema。
    const 战场情境表_V1 = Object.freeze([
      {
        名称: '对方蓄力即将爆发(剩 ≤ 1)',
        检查: (战局画像) => (战局画像.敌方列表 || []).some(u =>
          (u?.状态?.存活 !== false) && u?.蓄力技能 && Number(u?.蓄力剩余 || 99) <= 1),
        加权: { 控制: 28, 保核: 22, 击杀: 16 },
      },
      {
        名称: '对方蓄力 ≥ 2 回合 (中长期布局)',
        检查: (战局画像) => (战局画像.敌方列表 || []).some(u =>
          (u?.状态?.存活 !== false) && u?.蓄力技能 && Number(u?.蓄力剩余 || 0) >= 2),
        加权: { 控制: 18, 资源消耗: 14 },
      },
      {
        名称: '我方人数劣势 ≥ 2',
        检查: (战局画像) => {
          const 我方 = (战局画像.己方列表 || []).filter(u => u?.状态?.存活 !== false).length;
          const 敌方 = (战局画像.敌方列表 || []).filter(u => u?.状态?.存活 !== false).length;
          return 敌方 - 我方 >= 2;
        },
        加权: { 拖回合: 22, 控制: 18, 保留资源: 12 },
      },
      {
        名称: '我方多人残血(≥ 2 人 < 50%)',
        检查: (战局画像) => (战局画像.己方列表 || []).filter(u =>
          getCombatHpRatio(u) < 0.5 && u?.状态?.存活 !== false).length >= 2,
        加权: { 治疗: 30, 保核: 24 },
      },
      {
        名称: '距集火完成 1 击 (敌方残血 < 15%)',
        检查: (战局画像) => (战局画像.敌方列表 || []).some(u => {
          const r = getCombatHpRatio(u);
          return r > 0 && r < 0.15 && (u?.状态?.存活 !== false);
        }),
        加权: { 击杀: 32, 终局爆发: 28, 集火: 24 },
      },
      {
        名称: '我方核心增益即将解除(剩 1 回合)',
        检查: (战局画像, behaviorState, actor) => {
          const 核心 = 战局画像.我方核心 || actor;
          const 状态效果 = 核心?.属性?.状态效果 || {};
          return Object.values(状态效果).some(s =>
            Number(s?.持续回合 || 0) === 1 &&
            (s?.类型 === '增益' || s?.类型 === 'buff'));
        },
        加权: { 维持压制: 16, 保核: 12 },
      },
      {
        名称: '敌方控制/减益即将解除',
        检查: (战局画像) => {
          const 状态效果 = 战局画像.敌方核心?.属性?.状态效果 || {};
          return Object.values(状态效果).some(s =>
            Number(s?.持续回合 || 0) === 1 &&
            (s?.类型 === '减益' || s?.类型 === 'debuff' || s?.类型 === '控制'));
        },
        加权: { 控制: 22, 击杀: 16 },
      },
      {
        名称: '撤退窗口成熟(允许撤离 + 我方明显劣势)',
        检查: (战局画像) => {
          if (!战局画像.允许撤离) return false;
          const 己方 = 战局画像.己方列表 || [];
          const 敌方 = 战局画像.敌方列表 || [];
          if (!己方.length || !敌方.length) return false;
          const 我方平均 = 己方.reduce((s, u) => s + getCombatHpRatio(u), 0) / 己方.length;
          const 敌方平均 = 敌方.reduce((s, u) => s + getCombatHpRatio(u), 0) / 敌方.length;
          return 我方平均 < 0.30 && 敌方平均 > 0.70;
        },
        加权: { 拖回合: 28, 保核: 18 },
      },
      {
        名称: '我方体力濒崩(actor 体力 < 30%)',
        检查: (战局画像, behaviorState, actor) => {
          const 体力上限 = Math.max(1, Number(actor?.属性?.体力上限 ?? actor?.vit_max ?? 1));
          const 当前体力 = Math.max(0, Number(actor?.属性?.体力 ?? actor?.vit ?? 0));
          return (当前体力 / 体力上限) < 0.30;
        },
        加权: { 保留资源: 24, 拖回合: 18 },  // 体力濒崩时减少高耗动作 + 拖时间回体力
      },
      {
        名称: '敌方体力濒崩(任一敌方 体力 < 25%)',
        检查: (战局画像) => (战局画像.敌方列表 || []).some(u => {
          if (u?.状态?.存活 === false) return false;
          const 体力上限 = Math.max(1, Number(u?.属性?.体力上限 ?? u?.vit_max ?? 1));
          const 当前体力 = Math.max(0, Number(u?.属性?.体力 ?? u?.vit ?? 0));
          return (当前体力 / 体力上限) < 0.25;
        }),
        加权: { 击杀: 22, 集火: 16 },  // 敌方力竭即收割窗口
      },
    ]);

    function 应用战场情境加权_V1(战局画像, behaviorState, actor, 加权, 选项 = {}) {
      const 命中情境 = [];
      for (const 项 of 战场情境表_V1) {
        try {
          if (选项.跳过危机情境 && /残血|蓄力即将爆发|撤退窗口|体力濒崩/.test(String(项.名称 || ''))) continue;
          if (项.检查(战局画像, behaviorState, actor)) {
            Object.entries(项.加权).forEach(([意图, 分值]) => 加权(意图, 分值));
            命中情境.push(项.名称);
          }
        } catch (error) {
          console.warn('[Phase D] 情境检查异常:', 项.名称, error?.message);
        }
      }
      return 命中情境;
    }

    function normalizeCombatStageValue(value, fallback = 战斗阶段枚举_V1.宣告) {
      const stage = String(value ?? '').trim();
      if (!stage) return fallback;
      if (战斗阶段合法值集合_V1.has(stage)) return stage;
      return fallback;
    }

    function coerceRuntimeCombatEffectValue(key, value, fallback) {
      if (COMBAT_EFFECT_BOOLEAN_KEYS.has(key)) return value === true;
      const parsed = Number(value);
      if (!Number.isFinite(parsed)) return fallback;
      return parsed;
    }

    function buildRuntimeStatRatioFromCondition(condition) {
      const runtimeMods = {};
      const rawRuntimeMods =
        condition?.面板修改比例 && typeof condition.面板修改比例 === 'object' && !Array.isArray(condition.面板修改比例)
          ? condition.面板修改比例
          : {};
      Object.entries(rawRuntimeMods).forEach(([key, value]) => {
        const runtimeKey = COMBAT_STAT_RATIO_SCHEMA_TO_RUNTIME_KEY[key] || key;
        const parsed = Number(value);
        if (!Number.isFinite(parsed)) return;
        runtimeMods[runtimeKey] = parsed;
      });

      const rawSchemaMods =
        condition?.面板倍率 && typeof condition.面板倍率 === 'object' && !Array.isArray(condition.面板倍率)
          ? condition.面板倍率
          : {};
      Object.entries(COMBAT_STAT_RATIO_SCHEMA_TO_RUNTIME_KEY).forEach(([schemaKey, runtimeKey]) => {
        const parsed = Number(rawSchemaMods[schemaKey]);
        if (!Number.isFinite(parsed)) return;
        runtimeMods[runtimeKey] = parsed;
      });

      return runtimeMods;
    }

    function 构建运行态属性固定修正(condition) {
      const 运行固定修正 = {};
      const 原始运行固定修正 =
        condition?.面板固定修正 && typeof condition.面板固定修正 === 'object' && !Array.isArray(condition.面板固定修正)
          ? condition.面板固定修正
          : {};
      Object.entries(原始运行固定修正).forEach(([键, 数值]) => {
        const 运行键 = 战斗属性固定修正结构键到运行键[键] || 键;
        const 解析值 = Number(数值);
        if (!Number.isFinite(解析值)) return;
        运行固定修正[运行键] = 解析值;
      });

      const 原始结构固定修正 =
        condition?.面板固定值 && typeof condition.面板固定值 === 'object' && !Array.isArray(condition.面板固定值)
          ? condition.面板固定值
          : {};
      Object.entries(战斗属性固定修正结构键到运行键).forEach(([结构键, 运行键]) => {
        const 解析值 = Number(原始结构固定修正[结构键]);
        if (!Number.isFinite(解析值)) return;
        运行固定修正[运行键] = 解析值;
      });

      return 运行固定修正;
    }

    function buildRuntimeCombatEffectsFromCondition(condition) {
      const normalized = createEmptyCombatEffectMap();
      const rawEffects =
        condition?.战斗效果 && typeof condition.战斗效果 === 'object' && !Array.isArray(condition.战斗效果)
          ? condition.战斗效果
          : {};

      Object.keys(normalized).forEach(runtimeKey => {
        if (rawEffects[runtimeKey] === undefined) return;
        normalized[runtimeKey] = coerceRuntimeCombatEffectValue(
          runtimeKey,
          rawEffects[runtimeKey],
          normalized[runtimeKey],
        );
      });

      Object.entries(COMBAT_EFFECT_SCHEMA_TO_RUNTIME_KEY).forEach(([schemaKey, runtimeKey]) => {
        if (rawEffects[schemaKey] === undefined) return;
        normalized[runtimeKey] = coerceRuntimeCombatEffectValue(
          runtimeKey,
          rawEffects[schemaKey],
          COMBAT_EFFECT_RUNTIME_DEFAULTS[runtimeKey] ?? normalized[runtimeKey],
        );
      });

      return normalized;
    }

    function normalizeCombatConditionForRuntime(condition) {
      const source = sanitizeCombatPersistenceData(deepClonePlain(condition || {}));
      const durationCandidate = source.duration !== undefined ? source.duration : source.持续回合;
      const durationParsed = Number(durationCandidate);
      const duration = Number.isFinite(durationParsed) ? Math.max(0, durationParsed) : 0;
      const layerParsed = Number(source.层数);
      return {
        ...source,
        类型: String(source.类型 || 'buff'),
        层数: Number.isFinite(layerParsed) ? layerParsed : 1,
        描述: String(source.描述 || '无'),
        duration,
        面板修改比例: buildRuntimeStatRatioFromCondition(source),
        面板固定修正: 构建运行态属性固定修正(source),
        战斗效果: buildRuntimeCombatEffectsFromCondition(source),
      };
    }

    function normalizeCombatConditionMapForRuntime(conditionMap) {
      if (!conditionMap || typeof conditionMap !== 'object' || Array.isArray(conditionMap)) return {};
      const normalized = {};
      Object.entries(conditionMap).forEach(([name, condition]) => {
        if (!name) return;
        normalized[name] = normalizeCombatConditionForRuntime(condition);
      });
      return normalized;
    }

    function buildSchemaStatRatioFromCondition(condition) {
      const schemaMods = {};
      const runtimeMods =
        condition?.面板修改比例 && typeof condition.面板修改比例 === 'object' && !Array.isArray(condition.面板修改比例)
          ? condition.面板修改比例
          : {};
      Object.entries(COMBAT_STAT_RATIO_RUNTIME_TO_SCHEMA_KEY).forEach(([runtimeKey, schemaKey]) => {
        const parsed = Number(runtimeMods[runtimeKey]);
        if (!Number.isFinite(parsed)) return;
        schemaMods[schemaKey] = parsed;
      });

      const rawSchemaMods =
        condition?.面板倍率 && typeof condition.面板倍率 === 'object' && !Array.isArray(condition.面板倍率)
          ? condition.面板倍率
          : {};
      Object.keys(COMBAT_STAT_RATIO_SCHEMA_TO_RUNTIME_KEY).forEach(schemaKey => {
        const parsed = Number(rawSchemaMods[schemaKey]);
        if (!Number.isFinite(parsed)) return;
        schemaMods[schemaKey] = parsed;
      });

      return schemaMods;
    }

    function 构建落盘属性固定值(condition) {
      const 结构固定值 = {};
      const 运行固定修正 =
        condition?.面板固定修正 && typeof condition.面板固定修正 === 'object' && !Array.isArray(condition.面板固定修正)
          ? condition.面板固定修正
          : {};
      Object.entries(战斗属性固定修正运行键到结构键).forEach(([运行键, 结构键]) => {
        const 解析值 = Number(运行固定修正[运行键]);
        if (!Number.isFinite(解析值) || Math.abs(解析值) < 0.0001) return;
        结构固定值[结构键] = 解析值;
      });

      const 原始结构固定值 =
        condition?.面板固定值 && typeof condition.面板固定值 === 'object' && !Array.isArray(condition.面板固定值)
          ? condition.面板固定值
          : {};
      Object.keys(战斗属性固定修正结构键到运行键).forEach(结构键 => {
        const 解析值 = Number(原始结构固定值[结构键]);
        if (!Number.isFinite(解析值) || Math.abs(解析值) < 0.0001) return;
        结构固定值[结构键] = 解析值;
      });

      return 结构固定值;
    }

    function buildSchemaCombatEffectsFromCondition(condition) {
      const schemaEffects = {};
      const rawEffects =
        condition?.战斗效果 && typeof condition.战斗效果 === 'object' && !Array.isArray(condition.战斗效果)
          ? condition.战斗效果
          : {};

      Object.entries(COMBAT_EFFECT_RUNTIME_TO_SCHEMA_KEY).forEach(([runtimeKey, schemaKey]) => {
        let value = rawEffects[runtimeKey];
        if (value === undefined && rawEffects[schemaKey] !== undefined) value = rawEffects[schemaKey];
        if (value === undefined) value = COMBAT_EFFECT_RUNTIME_DEFAULTS[runtimeKey];
        const normalized =
          runtimeKey === 'skip_turn' ? value === true : Number.isFinite(Number(value)) ? Number(value) : 0;
        schemaEffects[schemaKey] = normalized;
      });

      return schemaEffects;
    }

    function buildCombatConditionPersistenceSnapshot(condition) {
      const source = sanitizeCombatPersistenceData(deepClonePlain(condition || {}));
      const durationCandidate = source.持续回合 !== undefined ? source.持续回合 : source.duration;
      const durationParsed = Number(durationCandidate);
      const duration = Number.isFinite(durationParsed) ? Math.max(0, durationParsed) : 0;
      const layerParsed = Number(source.层数);
      const snapshot = {
        ...source,
        类型: String(source.类型 || 'buff'),
        层数: Number.isFinite(layerParsed) ? layerParsed : 1,
        描述: String(source.描述 || '无'),
        持续回合: duration,
        面板倍率: buildSchemaStatRatioFromCondition(source),
        面板固定值: 构建落盘属性固定值(source),
        战斗效果: buildSchemaCombatEffectsFromCondition(source),
      };
      delete snapshot.duration;
      delete snapshot.面板修改比例;
      delete snapshot.面板固定修正;
      return snapshot;
    }

    function buildCombatConditionMapPersistenceSnapshot(conditionMap) {
      if (!conditionMap || typeof conditionMap !== 'object' || Array.isArray(conditionMap)) return {};
      const snapshot = {};
      Object.entries(conditionMap).forEach(([name, condition]) => {
        if (!name) return;
        snapshot[name] = buildCombatConditionPersistenceSnapshot(condition);
      });
      return snapshot;
    }

    function buildCanonicalCombatStatSnapshot(participant, statKeys = COMBAT_PROCESS_SYNC_STAT_KEYS) {
      if (!participant || typeof participant !== 'object') return undefined;
      const sourceStat = participant.属性 && typeof participant.属性 === 'object' ? participant.属性 : participant;
      const snapshot = {};
      statKeys.forEach(key => {
        if (sourceStat[key] === undefined) return;
        if (key === '状态效果') {
          snapshot[key] = buildCombatConditionMapPersistenceSnapshot(sourceStat[key]);
          return;
        }
        snapshot[key] = clonePersistedCombatValue(sourceStat[key]);
      });
      return Object.keys(snapshot).length ? snapshot : undefined;
    }

    function buildCanonicalCombatStatusSnapshot(participant, statusKeys = COMBAT_PROCESS_SYNC_STATUS_KEYS) {
      if (!participant || typeof participant !== 'object') return undefined;
      const sourceStatus = participant.状态 && typeof participant.状态 === 'object' ? participant.状态 : participant;
      const snapshot = {};
      statusKeys.forEach(key => {
        if (sourceStatus[key] !== undefined) snapshot[key] = clonePersistedCombatValue(sourceStatus[key]);
      });
      return Object.keys(snapshot).length ? snapshot : undefined;
    }

    function buildCanonicalParticipantPersistenceSnapshot(participant, options = {}) {
      if (!participant || typeof participant !== 'object') return undefined;
      const source = sanitizeCombatPersistenceData(participant);
      const snapshot = {};
      const statSnapshot = buildCanonicalCombatStatSnapshot(source);
      const statusSnapshot = buildCanonicalCombatStatusSnapshot(source);
      if (statSnapshot) snapshot.属性 = statSnapshot;
      if (statusSnapshot) snapshot.状态 = statusSnapshot;
      if (options.includeHpFields) {
        const hpSnapshot = buildCanonicalCombatStatSnapshot(source, ['HP', 'HP上限']);
        if (hpSnapshot) snapshot.属性 = { ...(snapshot.属性 || {}), ...hpSnapshot };
      }
      ['决策记忆', '血脉之力', '复制效果'].forEach(key => {
        if (source[key] !== undefined) snapshot[key] = clonePersistedCombatValue(source[key]);
      });
      const equipmentSnapshot = source.装备;
      if (equipmentSnapshot !== undefined) snapshot.装备 = clonePersistedCombatValue(equipmentSnapshot);
      return Object.keys(snapshot).length ? snapshot : undefined;
    }

    function appendJsonPatchDiff(ops, basePath, previousValue, nextValue) {
      if (!basePath) return;

      if (nextValue === undefined) {
        if (previousValue !== undefined) ops.push({ op: 'remove', path: basePath });
        return;
      }

      if (previousValue === undefined) {
        ops.push({ op: 'add', path: basePath, value: deepClonePlain(nextValue) });
        return;
      }

      if (isDeepEqual(previousValue, nextValue)) return;

      const prevIsArray = Array.isArray(previousValue);
      const nextIsArray = Array.isArray(nextValue);
      if (prevIsArray || nextIsArray) {
        ops.push({ op: 'replace', path: basePath, value: deepClonePlain(nextValue) });
        return;
      }

      const prevIsObject = isPlainRecord(previousValue);
      const nextIsObject = isPlainRecord(nextValue);
      if (prevIsObject && nextIsObject) {
        const keys = new Set([...Object.keys(previousValue), ...Object.keys(nextValue)]);
        keys.forEach(key => {
          appendJsonPatchDiff(
            ops,
            `${basePath}/${escapeJsonPointerSegment(key)}`,
            previousValue[key],
            nextValue[key],
          );
        });
        return;
      }

      ops.push({ op: 'replace', path: basePath, value: deepClonePlain(nextValue) });
    }

    function appendLegacyParticipantCleanupOps(ops, participantPath, currentCharData) {
      if (!participantPath || !currentCharData || typeof currentCharData !== 'object') return;
      const legacyKeys = new Set([...COMBAT_STAT_KEYS, ...COMBAT_STATUS_KEYS]);
      legacyKeys.forEach(key => {
        if (!Object.prototype.hasOwnProperty.call(currentCharData, key)) return;
        ops.push({ op: 'remove', path: `${participantPath}/${escapeJsonPointerSegment(key)}` });
      });
    }

    function appendParticipantHpRecoverySnapshot(nextSnapshot, previousSnapshot, participant) {
      const sourceStat = participant?.属性 && typeof participant.属性 === 'object' ? participant.属性 : participant;
      const previousStat = previousSnapshot?.属性 && typeof previousSnapshot.属性 === 'object' ? previousSnapshot.属性 : {};
      const nextHp = Number(sourceStat?.HP);
      const prevHp = Number(previousStat?.HP);
      if (!Number.isFinite(nextHp)) return nextSnapshot;
      if (Number.isFinite(prevHp) && nextHp <= prevHp) return nextSnapshot;
      const hpFields = buildCanonicalCombatStatSnapshot(participant, ['HP', 'HP上限']);
      if (!hpFields) return nextSnapshot;
      nextSnapshot.属性 = { ...(nextSnapshot.属性 || {}), ...hpFields };
      return nextSnapshot;
    }

    function isTemporaryCombatParticipant(participant) {
      return !!(
        participant &&
        typeof participant === 'object' &&
        String(participant.来源 || '').trim() === '临时单位' &&
        String(participant.单位性质 || '').trim()
      );
    }

    function buildTemporaryCombatParticipantPersistenceSnapshot(participant) {
      if (!participant || typeof participant !== 'object') return undefined;
      const source = sanitizeCombatPersistenceData(deepClonePlain(participant));
      const snapshot = {
        name: String(source.name || '').trim() || '未知',
        来源: '临时单位',
        单位性质: String(source.单位性质 || '').trim(),
      };
      ['身份', '数量', '等级', '年限', '品质', '标准物种', '级别', '标准种族'].forEach(key => {
        if (source[key] !== undefined) snapshot[key] = clonePersistedCombatValue(source[key]);
      });
      const canonical = buildCanonicalParticipantPersistenceSnapshot(source) || {};
      Object.assign(snapshot, canonical);
      const hpFields = buildCanonicalCombatStatSnapshot(source, ['HP', 'HP上限']) || {};
      if (Object.keys(hpFields).length) {
        snapshot.属性 = { ...(snapshot.属性 || {}), ...hpFields };
      }
      if (source.状态 && typeof source.状态 === 'object') {
        snapshot.状态 = {
          ...(snapshot.状态 || {}),
          存活: source.状态.存活 !== false,
          ...(source.状态.受伤部位 !== undefined ? { 受伤部位: clonePersistedCombatValue(source.状态.受伤部位) } : {}),
        };
      }
      if (source.自创魂技 && typeof source.自创魂技 === 'object' && !Array.isArray(source.自创魂技)) {
        snapshot.自创魂技 = clonePersistedCombatValue(source.自创魂技);
      }
      return snapshot;
    }

    function pushParticipantSyncPatch(ops, participant, options = {}) {
      if (!participant || typeof participant !== 'object') return;
      if (isTemporaryCombatParticipant(participant)) return;
      if (participant.实力压制 && participant.实力压制.刻意压制等级 === true) return;
      const participantName = String(participant.name || '').trim();
      if (!participantName) return;

      const participantPath = `/char/${escapeJsonPointerSegment(participantName)}`;
      const currentCharData = getMvuValue(`char.${participantName}`, undefined);
      const 包含生命字段 = options.syncHpRecoveryOnly !== true;
      const previousSnapshot =
        buildCanonicalParticipantPersistenceSnapshot(currentCharData, { includeHpFields: 包含生命字段 }) || {};
      const nextSnapshot = buildCanonicalParticipantPersistenceSnapshot(participant, { includeHpFields: 包含生命字段 });
      if (!nextSnapshot || typeof nextSnapshot !== 'object' || Object.keys(nextSnapshot).length === 0) return;
      if (options.syncHpRecoveryOnly) appendParticipantHpRecoverySnapshot(nextSnapshot, previousSnapshot, participant);
      const fieldKeys = new Set([...Object.keys(previousSnapshot), ...Object.keys(nextSnapshot)]);

      fieldKeys.forEach(key => {
        appendJsonPatchDiff(
          ops,
          `${participantPath}/${escapeJsonPointerSegment(key)}`,
          previousSnapshot[key],
          nextSnapshot[key],
        );
      });

      appendLegacyParticipantCleanupOps(ops, participantPath, currentCharData);
    }

    const COMBAT_WORLD_PERSIST_KEYS = [
      '进行中',
      '战斗类型',
      '战斗意图',
      '先攻',
      '允许撤离',
      '环境',
      '裁断结果',
      'floor',
      '大关卡',
      '大关标签',
      '关卡范围',
      '关底战',
      '魂灵塔待结算',
      '试炼状态',
      '虚拟裁断数据',
    ];

    function compactCombatParticipantForPersistence(participant) {
      if (participant === null || participant === undefined) return undefined;
      if (typeof participant === 'string' || typeof participant === 'number') {
        return { name: String(participant) };
      }
      if (typeof participant !== 'object' || Array.isArray(participant)) return undefined;
      const source = sanitizeCombatPersistenceData(participant);
      if (isTemporaryCombatParticipant(source)) {
        return buildTemporaryCombatParticipantPersistenceSnapshot(source);
      }
      const compact = {};
      const participantName = String(source.name || '').trim();
      if (participantName) compact.name = participantName;
      if (source.势力 !== undefined) compact.势力 = String(source.势力 || '');
      if (source.实力压制 && typeof source.实力压制 === 'object') {
        compact.实力压制 = clonePersistedCombatValue(source.实力压制);
      }
      const aliveCandidate =
        source.存活 !== undefined
          ? source.存活
          : source.状态 && typeof source.状态 === 'object'
            ? source.状态.存活
            : undefined;
      if (aliveCandidate !== undefined) compact.存活 = aliveCandidate !== false;
      return Object.keys(compact).length ? compact : undefined;
    }

    function compactCombatDataForPersistence(combatData) {
      const source = sanitizeCombatPersistenceData(deepClonePlain(combatData || {}));
      const compact = {};

      COMBAT_WORLD_PERSIST_KEYS.forEach(key => {
        if (source[key] !== undefined) compact[key] = source[key];
      });

      const roundCandidate = source.回合;
      if (roundCandidate !== undefined) {
        const roundParsed = Number(roundCandidate);
        compact.回合 = Number.isFinite(roundParsed) ? Math.max(0, roundParsed) : 0;
      }

      const participants = source.参战者 && typeof source.参战者 === 'object' ? source.参战者 : undefined;
      if (participants) {
        const compactParticipants = {};
        if (Array.isArray(participants.team_player)) {
          compactParticipants.team_player = participants.team_player
            .map(compactCombatParticipantForPersistence)
            .filter(Boolean);
        }
        if (Array.isArray(participants.team_enemy)) {
          compactParticipants.team_enemy = participants.team_enemy
            .map(compactCombatParticipantForPersistence)
            .filter(Boolean);
        }
        if (Object.keys(compactParticipants).length) compact.参战者 = compactParticipants;
      }

      return compact;
    }

    function buildCombatJsonPatch(combatData, options = {}) {
      const fullCombatData = sanitizeCombatPersistenceData(deepClonePlain(combatData || {}));
      const safeCombatData = compactCombatDataForPersistence(fullCombatData);
      const ops = [];
      const previousRawCombatData = sanitizeCombatPersistenceData(getMvuValue('world.战斗', undefined));
      const previousCombatData = compactCombatDataForPersistence(previousRawCombatData);
      appendJsonPatchDiff(ops, '/world/战斗', previousCombatData, safeCombatData);

      const participants = fullCombatData?.参战者;
      if (!participants) return ops;
      if (options.skipParticipantSync === true) return ops;

      (participants.team_player || []).forEach(unit => pushParticipantSyncPatch(ops, unit, options));
      (participants.team_enemy || []).forEach(unit => pushParticipantSyncPatch(ops, unit, options));

      return ops;
    }

    function buildUpdateVariableText(patchOps, options = {}) {
      const analysis = String(
        options.analysis ||
          'Frontend battle arbitration already produced the exact combat result. Apply the following JSONPatch exactly as given.',
      ).trim();
      return `<UpdateVariable>\n<Analysis>${analysis}</Analysis>\n<JSONPatch>\n${JSON.stringify(patchOps || [], null, 2)}\n</JSONPatch>\n</UpdateVariable>`;
    }

    function persistCombatData(combatData, options = {}) {
      const safeCombatData = compactCombatDataForPersistence(combatData);
      const patchOps = buildCombatJsonPatch(combatData, {
        syncHpRecoveryOnly: options.syncHpRecoveryOnly !== false,
        skipParticipantSync: options.skipParticipantSync === true,
      });
      if (Array.isArray(options.extraPatchOps)) {
        patchOps.push(...options.extraPatchOps);
      }
      const updateVariableText = buildUpdateVariableText(patchOps, options);
      const detail = {
        combatData: safeCombatData,
        patchOps,
        updateVariableText,
        rootPath: '/world/战斗',
      };

      root.__lastBattleMvuUpdateRequest = detail;

      const bridge = root.BattleUIBridge || {};
      const adapter = bridge.hostAdapter || root.__battleUIHostAdapter;
      if (typeof bridge.onCombatDataChanged === 'function') {
        try {
          detail.delivery = bridge.onCombatDataChanged(detail);
        } catch (error) {
          console.warn('BattleUIBridge.onCombatDataChanged failed', error);
        }
      } else if (adapter && typeof adapter.onCombatDataChanged === 'function') {
        try {
          detail.delivery = adapter.onCombatDataChanged(detail);
        } catch (error) {
          console.warn('BattleUI hostAdapter.onCombatDataChanged failed', error);
        }
      }

      try {
        root.dispatchEvent(new CustomEvent('battle-ui-mvu-update-request', { detail }));
      } catch (error) {
        console.warn('battle-ui-mvu-update-request dispatch failed', error);
      }

      try {
        root.dispatchEvent(new CustomEvent('battle-ui-combat-data-changed', { detail }));
      } catch (error) {
        console.warn('battle-ui-combat-data-changed dispatch failed', error);
      }

      return detail;
    }

    function getHostWindow() {
      try {
        if (root.parent && root.parent !== root && root.parent.document) return root.parent;
      } catch (error) {
        console.warn('BattleUIBridge: parent window unavailable, fallback current window', error);
      }
      return root;
    }

    function getHostDocument() {
      return getHostWindow().document || document;
    }

    function findFirstElement(selectors, doc = getHostDocument()) {
      for (const selector of selectors) {
        const node = doc.querySelector(selector);
        if (node) return node;
      }
      return null;
    }

    function createInputEvent(type) {
      try {
        return new InputEvent(type, { bubbles: true, cancelable: true, composed: true });
      } catch (error) {
        return new Event(type, { bubbles: true, cancelable: true, composed: true });
      }
    }

    function setElementValue(element, value) {
      if (!element) return false;
      const nextValue = String(value ?? '');
      if (element.isContentEditable) {
        if ('textContent' in element) element.textContent = nextValue;
        if ('innerText' in element && element.innerText !== nextValue) element.innerText = nextValue;
        element.dispatchEvent(createInputEvent('input'));
        element.dispatchEvent(createInputEvent('change'));
        return true;
      }
      const prototype = element.tagName === 'TEXTAREA' ? HTMLTextAreaElement.prototype : HTMLInputElement.prototype;
      const descriptor = Object.getOwnPropertyDescriptor(prototype, 'value');
      if (descriptor?.set) descriptor.set.call(element, nextValue);
      else element.value = nextValue;
      element.dispatchEvent(createInputEvent('input'));
      element.dispatchEvent(createInputEvent('change'));
      return true;
    }

    function findChatInput(doc = getHostDocument()) {
      return findFirstElement(
        [
          '#send_textarea',
          '#chat_input',
          '#user-input',
          '#user_input',
          'textarea[data-testid="chat-input"]',
          'textarea[placeholder*="发送"]',
          'textarea[placeholder*="Send"]',
          'textarea[name="chat_input"]',
          'textarea[name="user_input"]',
          '[contenteditable="true"][data-testid="chat-input"]',
          'form [contenteditable="true"][role="textbox"]',
          '[contenteditable="true"][aria-label*="Send"]',
          '[contenteditable="true"][aria-label*="发送"]',
          'form textarea',
          'textarea',
        ],
        doc,
      );
    }

    function findSendButton(doc = getHostDocument()) {
      return findFirstElement(
        [
          '#send_but',
          '#send-button',
          'button[data-testid="send-button"]',
          'button[title*="Send"]',
          'button[aria-label*="Send"]',
          'button[aria-label*="发送"]',
          'button[title*="发送"]',
          'form button[type="submit"]',
        ],
        doc,
      );
    }

    function fillChatInput(text, options = {}) {
      const doc = options.document || getHostDocument();
      const input = findChatInput(doc);
      const ok = setElementValue(input, text);
      if (ok && typeof input?.focus === 'function') input.focus();
      return { ok, input };
    }

    function clickSendButton(options = {}) {
      const doc = options.document || getHostDocument();
      const button = findSendButton(doc);
      if (!button) return { ok: false, button: null };
      button.click();
      return { ok: true, button };
    }

    function queueSystemPrompt(text) {
      const prompt = String(text || '');
      root.__battlePendingSystemPrompt = prompt;
      try {
        root.dispatchEvent(new CustomEvent('battle-ui-system-prompt-ready', { detail: { systemPrompt: prompt } }));
      } catch (error) {
        console.warn('battle-ui-system-prompt-ready dispatch failed', error);
      }
      return prompt;
    }

    function deliverBattleRequest(detail, options = {}) {
      const bridge = root.BattleUIBridge || {};
      if (typeof bridge.hostSend === 'function') {
        return bridge.hostSend({ ...detail, options });
      }
      const adapter = bridge.hostAdapter || root.__battleUIHostAdapter;
      if (adapter && typeof adapter.sendUIRequest === 'function') {
        return adapter.sendUIRequest({ ...detail, options });
      }
      if (adapter && typeof adapter.sendBattleRequest === 'function') {
        return adapter.sendBattleRequest({ ...detail, options });
      }

      return {
        ok: false,
        mode: 'host-unavailable',
        reason: 'battle_host_send_unavailable',
      };
    }

    function looksLikeGenerationUrl(url) {
      return /(generate|completion|chat-completions|text-completions|api\/backends|v1\/chat\/completions|v1\/completions)/i.test(
        String(url || ''),
      );
    }

    function appendPromptText(base, extra) {
      const baseText = String(base || '').trim();
      const extraText = String(extra || '').trim();
      if (!extraText) return base;
      if (!baseText) return extraText;
      return `${baseText}\n\n${extraText}`;
    }

    function consumeQueuedSystemPromptForInjection(meta = {}) {
      const prompt = String(root.__battlePendingSystemPrompt || '').trim();
      if (!prompt) return '';
      root.__battlePendingSystemPrompt = '';
      root.__battleLastInjectedSystemPrompt = {
        prompt,
        ...meta,
        at: Date.now(),
      };
      try {
        root.dispatchEvent(
          new CustomEvent('battle-ui-system-prompt-consumed', {
            detail: root.__battleLastInjectedSystemPrompt,
          }),
        );
      } catch (error) {
        console.warn('battle-ui-system-prompt-consumed dispatch failed', error);
      }
      return prompt;
    }

    function injectSystemPromptIntoPayload(payload, prompt) {
      if (!payload || typeof payload !== 'object' || !prompt) {
        return { payload, injected: false, channel: null };
      }

      let injected = false;
      let channel = null;

      if (Array.isArray(payload.messages)) {
        const messages = payload.messages.map(msg => ({ ...msg }));
        const firstSystemIndex = messages.findIndex(msg => msg?.role === 'system');
        if (firstSystemIndex >= 0) {
          const target = { ...messages[firstSystemIndex] };
          if (typeof target.content === 'string' || target.content == null) {
            target.content = appendPromptText(target.content || '', prompt);
            messages[firstSystemIndex] = target;
          } else {
            messages.splice(firstSystemIndex, 0, { role: 'system', content: prompt });
          }
        } else {
          messages.unshift({ role: 'system', content: prompt });
        }
        payload = { ...payload, messages };
        injected = true;
        channel = 'messages.system';
      } else if (typeof payload.system === 'string' || payload.system == null) {
        payload = { ...payload, system: appendPromptText(payload.system || '', prompt) };
        injected = true;
        channel = 'system';
      } else if (typeof payload.systemPrompt === 'string' || payload.systemPrompt == null) {
        payload = { ...payload, systemPrompt: appendPromptText(payload.systemPrompt || '', prompt) };
        injected = true;
        channel = 'systemPrompt';
      } else if (typeof payload.instruction === 'string' || payload.instruction == null) {
        payload = { ...payload, instruction: appendPromptText(payload.instruction || '', prompt) };
        injected = true;
        channel = 'instruction';
      } else if (typeof payload.instructions === 'string' || payload.instructions == null) {
        payload = { ...payload, instructions: appendPromptText(payload.instructions || '', prompt) };
        injected = true;
        channel = 'instructions';
      } else if (typeof payload.prompt === 'string') {
        payload = { ...payload, prompt: `${prompt}\n\n${payload.prompt}` };
        injected = true;
        channel = 'prompt';
      }

      return { payload, injected, channel };
    }

    function patchRequestBodyIfNeeded(bodyText, url, transport) {
      const pendingPrompt = String(root.__battlePendingSystemPrompt || '').trim();
      if (!pendingPrompt || !looksLikeGenerationUrl(url) || typeof bodyText !== 'string' || !bodyText.trim()) {
        return { bodyText, injected: false, channel: null };
      }

      try {
        const payload = JSON.parse(bodyText);
        const promptToInject = consumeQueuedSystemPromptForInjection({ url: String(url || ''), transport });
        if (!promptToInject) return { bodyText, injected: false, channel: null };
        const result = injectSystemPromptIntoPayload(payload, promptToInject);
        if (!result.injected) {
          root.__battlePendingSystemPrompt = promptToInject;
          return { bodyText, injected: false, channel: null };
        }
        return {
          bodyText: JSON.stringify(result.payload),
          injected: true,
          channel: result.channel,
        };
      } catch (error) {
        return { bodyText, injected: false, channel: null };
      }
    }

    function installFetchHook() {
      const host = getHostWindow();
      if (!host || host.__battleUIFetchHookInstalled || typeof host.fetch !== 'function') return;
      const nativeFetch = host.fetch.bind(host);
      const HostRequest = host.Request || Request;

      host.fetch = async function (input, init) {
        let nextInput = input;
        let nextInit = init;
        const url = typeof input === 'string' ? input : input?.url || init?.url || '';

        if (init?.body && typeof init.body === 'string') {
          const patched = patchRequestBodyIfNeeded(init.body, url, 'fetch:init');
          if (patched.injected) {
            nextInit = { ...init, body: patched.bodyText };
          }
        } else if (typeof HostRequest !== 'undefined' && input instanceof HostRequest && !init?.body) {
          try {
            const cloned = input.clone();
            const bodyText = await cloned.text();
            const patched = patchRequestBodyIfNeeded(bodyText, url, 'fetch:request');
            if (patched.injected) {
              nextInput = new HostRequest(input, { body: patched.bodyText });
            }
          } catch (error) {
            // ignore request body patch failure
          }
        }

        return nativeFetch(nextInput, nextInit);
      };

      host.__battleUIFetchHookInstalled = true;
    }

    function installXHRHook() {
      const host = getHostWindow();
      const XHR = host?.XMLHttpRequest;
      if (!XHR || XHR.prototype.__battleUIXHRHookInstalled) return;
      const nativeOpen = XHR.prototype.open;
      const nativeSend = XHR.prototype.send;

      XHR.prototype.open = function (method, url, ...rest) {
        this.__battleUIRequestUrl = url;
        return nativeOpen.call(this, method, url, ...rest);
      };

      XHR.prototype.send = function (body) {
        if (typeof body === 'string') {
          const patched = patchRequestBodyIfNeeded(body, this.__battleUIRequestUrl, 'xhr');
          if (patched.injected) {
            body = patched.bodyText;
          }
        }
        return nativeSend.call(this, body);
      };

      XHR.prototype.__battleUIXHRHookInstalled = true;
    }

    function installHostHooks() {
      installFetchHook();
      installXHRHook();
    }

    if (typeof root.sendToAI !== 'function') {
      root.sendToAI = function (playerInput, systemPrompt, meta = {}) {
        const requestKind = String(meta?.requestKind || 'battle_settlement_plot');
        const detail = {
          requestSource: 'Battle_UI',
          requestKind,
          playerInput: String(playerInput || ''),
          systemPrompt: String(systemPrompt || ''),
          mvuUpdate: meta?.mvuUpdate || null,
          channels: {
            userInput: String(playerInput || ''),
            hiddenSystemPrompt: String(systemPrompt || ''),
            updateVariableText: String(meta?.mvuUpdate?.updateVariableText || ''),
            requestKind,
          },
        };
        detail.delivery = deliverBattleRequest(detail, { autoSend: meta?.autoSend !== false });
        root.__lastBattleAIRequest = detail;
        try {
          root.dispatchEvent(new CustomEvent('battle-ui-ai-request', { detail }));
        } catch (error) {
          console.warn('battle-ui-ai-request dispatch failed', error);
        }
        return detail;
      };
    }

    function normalizeBattleSkillNameMarks(text = '') {
      return String(text || '')
        .replace(/\[([^\[\]\n]{1,40})\]/g, (match, name) => {
          const trimmed = String(name || '').trim();
          if (!trimmed || /^(第\d+回合|状态|战损|主动|命中|未完全|战前|续推|单回合|行为|规划|候选|权重|前端|暗箱|防御|危机|实战|团战|连招|应招|起招|再判定|换招|脱离|撤离|护卫|缴械|元素|造物|召唤|资源|物品|状态结算|状态施加)/.test(trimmed)) return match;
          return `【${trimmed}】`;
        });
    }

    function normalizeBattleReportNameForMatch(text = '') {
      return String(text || '')
        .replace(/[【】\[\]\s]/g, '')
        .replace(/^(我方|敌方|玩家|NPC|同窗|目标)/, '')
        .trim();
    }

    function isSameBattleReportName(left = '', right = '') {
      const a = normalizeBattleReportNameForMatch(left);
      const b = normalizeBattleReportNameForMatch(right);
      return !!a && !!b && a === b;
    }

    function normalizeBattleActionDisplayName(name = '') {
      const text = String(name || '').replace(/^【|】$/g, '').trim();
      if (!text) return '';
      if (text === '常规攻击') return '普通攻击';
      if (text === '主动压迫') return '普通攻击';
      if (text === '肉体兜底') return '承伤硬抗';
      if (text === '硬抗') return '承伤硬抗';
      if (text === '系统反击') return '借势反打';
      if (/系统反击$/.test(text)) return text.replace(/系统反击$/, '借势反打');
      return text;
    }

    function isBattleTacticalFallbackAction(actionName = '') {
      return /承伤硬抗|肉体兜底|硬抗|防御|危机自保|收招转防|借力守势|坚壁反制|伺机闪避|闪避|撤离/.test(String(actionName || ''));
    }

    function 判定守势待机动作(actionName = '') {
      return /战术待机|待机|观察|防御|收招转防|守势维持|守势对峙|暂缓出手|pass|observe|defend|stance/i.test(String(actionName || ''));
    }

    function 构建守势待机短句(actorName = '行动者', actionName = '行动') {
      const actor = String(actorName || '行动者').trim();
      const action = normalizeBattleActionDisplayName(actionName || '');
      if (/防御|收招转防|守势|defend|stance/i.test(action)) return `${actor}收住攻势，稳住身位`;
      if (/观察|observe/i.test(action)) return `${actor}保持距离观察，稳住节奏`;
      return `${actor}暂缓出手，观察战局变化`;
    }

    function 映射玩家可见失败原因(reasonCode = '', rawReason = '') {
      const code = String(reasonCode || '').trim().toUpperCase();
      const reason = String(rawReason || '').trim();
      const byCode = {
        CAP_REACHED: '场面已满，造物已达上限',
        TARGET_LOST: '目标已失去有效落点',
        OUT_OF_MANA: '魂力不足',
        RESOURCE_NOT_ENOUGH: '魂力不足',
        RANGE_INVALID: '距离不满足',
        TARGET_INVALID: '目标非法',
        CONTROLLED: '行动被控制限制',
        SILENCED: '魂技被封锁',
        DISARMED: '出手条件受限',
        INTERRUPTED: '被对手截断',
        BLOCKED: '被对手压住节奏',
        NO_EFFECTIVE_OPENING: '战机已经消散',
      };
      if (byCode[code]) return byCode[code];
      if (/CAP_REACHED|达到上限|造物已达上限|场面已满/.test(reason)) return byCode.CAP_REACHED;
      if (/target_lost|TARGET_LOST|目标丢失|失去有效落点/i.test(reason)) return byCode.TARGET_LOST;
      if (/魂力不足|资源不足|OUT_OF_MANA|RESOURCE_NOT_ENOUGH/i.test(reason)) return byCode.OUT_OF_MANA;
      if (/距离不够|距离不足|RANGE_INVALID/i.test(reason)) return byCode.RANGE_INVALID;
      if (/目标非法|TARGET_INVALID/i.test(reason)) return byCode.TARGET_INVALID;
      if (/抢先压制|先手压制|行动受阻|被打断|interrupted|INTERRUPTED/i.test(reason)) return byCode.INTERRUPTED;
      if (/本回合对抗结束，未能形成有效出手|未能形成有效出手|被对手压住节奏|BLOCKED/i.test(reason)) return byCode.BLOCKED;
      if (/缺少可结算效果|未形成主动结算效果|没有形成主动结算效果|NO_EFFECTIVE_OPENING|no_effective_opening/i.test(reason)) return byCode.NO_EFFECTIVE_OPENING;
      return code ? `未知规则限制(CODE: ${code})` : '';
    }

    function 解析公开战报状态来源(rawLine = '', target = '', state = '', context = {}) {
      const raw = String(rawLine || '');
      const targetName = String(target || '').trim();
      const stateName = String(state || '').trim();
      const hits = Array.isArray(context?.hits) ? context.hits : [];
      const actionDeclarations = Array.isArray(context?.actionDeclarations) ? context.actionDeclarations : [];
      const openings = Array.isArray(context?.openings) ? context.openings : [];
      const registeredSource = 查找状态来源登记(context?.combatData || {}, {
        stateName,
        targetName,
        maxRound: Number(context?.round || 0),
      });
      if (registeredSource) {
        return {
          sourceRound: Math.max(0, Number(registeredSource.sourceRound || registeredSource.round || 0)),
          sourceActorName: String(registeredSource.sourceActorName || '').trim(),
          sourceActionName: normalizeBattleActionDisplayName(registeredSource.sourceActionName || ''),
          duration: Math.max(0, Number(registeredSource.duration || 0)),
          effectSummary: String(registeredSource.effectSummary || '').trim(),
          driverAttr: String(registeredSource.driverAttr || '').trim(),
        };
      }
      const sourceMatch = raw.match(/该状态由第(\d+)回合([^施。！？\s]+)施展【([^】]+)】附加/);
      if (sourceMatch) {
        return {
          sourceRound: Math.max(0, Number(sourceMatch[1] || 0)),
          sourceActorName: String(sourceMatch[2] || '').trim(),
          sourceActionName: normalizeBattleActionDisplayName(sourceMatch[3] || ''),
        };
      }
      const hit = hits.find(item => targetName && isSameBattleReportName(item?.target || '', targetName));
      if (hit?.actor || hit?.action) {
        return {
          sourceRound: Math.max(0, Number(context?.round || 0)),
          sourceActorName: String(hit?.actor || '').trim(),
          sourceActionName: normalizeBattleActionDisplayName(hit?.action || ''),
        };
      }
      const declaration = actionDeclarations.find(item =>
        item?.kind !== 'counter' &&
        targetName &&
        item?.target &&
        isSameBattleReportName(item.target, targetName)
      );
      if (declaration?.actor || declaration?.action) {
        return {
          sourceRound: Math.max(0, Number(context?.round || 0)),
          sourceActorName: String(declaration?.actor || '').trim(),
          sourceActionName: normalizeBattleActionDisplayName(declaration?.action || ''),
        };
      }
      if (openings.length === 1) {
        return {
          sourceRound: Math.max(0, Number(context?.round || 0)),
          sourceActorName: String(openings[0]?.actor || '').trim(),
          sourceActionName: normalizeBattleActionDisplayName(openings[0]?.action || ''),
        };
      }
      return { sourceActorName: '', sourceActionName: '' };
    }

    function 选择稳定战报短语(候选 = [], seedText = '') {
      const list = (Array.isArray(候选) ? 候选 : []).map(item => String(item || '').trim()).filter(Boolean);
      if (!list.length) return '';
      const seed = String(seedText || '').trim();
      let hash = 0;
      for (let i = 0; i < seed.length; i += 1) hash = ((hash << 5) - hash + seed.charCodeAt(i)) | 0;
      return list[Math.abs(hash) % list.length] || list[0];
    }

    const BATTLE_PUBLIC_FLAVOR_TEMPLATES = {
      blockedAction: [
        '{actor}刚欲催动【{skill}】，便被对手压住节奏，未能完成出手',
        '{actor}的【{skill}】尚未铺开，便被这轮交锋截断',
      ],
      hitDodgeFailed: [
        '{attacker}施展【{skill}】压向{target}，{target}试图闪身避让却未能摆脱攻势，承受了 {damage} 点伤害',
        '{attacker}以【{skill}】逼近{target}，{target}仓促闪避未果，仍受了 {damage} 点伤害',
      ],
      hitPlain: [
        '{attacker}施展【{skill}】指向{target}，造成了 {damage} 点伤害',
        '{attacker}以【{skill}】压出攻势，令{target}承受了 {damage} 点伤害',
      ],
      counterHit: [
        '{counterActor}抓住{target}露出的破绽，以【{counterSkill}】进行反击，造成了 {damage} 点伤害',
        '{counterActor}趁{target}攻势露隙，以【{counterSkill}】反击，造成了 {damage} 点伤害',
      ],
      counterZero: [
        '{counterActor}抓住{target}露出的破绽，以【{counterSkill}】进行反击，但未能造成实质伤害',
        '{counterActor}尝试以【{counterSkill}】反打{target}，但这次没有打出实质伤害',
      ],
      stateResisted: [
        '{target}抵住了【{state}】的附着',
        '【{state}】试图侵入{target}，却未能附着',
      ],
      stateTickDamage: [
        '{target}随后受【{state}】影响，损失了 {amount} 点{resource}{sourceText}',
        '回合流转间，【{state}】在{target}身上发作，令其损失 {amount} 点{resource}{sourceText}',
      ],
      stateTickHeal: [
        '{target}随后受【{state}】影响，恢复了 {amount} 点{resource}{sourceText}',
        '回合流转间，【{state}】为{target}带来恢复，使其回复 {amount} 点{resource}{sourceText}',
      ],
      create: [
        '{actor}趁隙催动【{skill}】{detail}',
        '{actor}催动【{skill}】在战场上凝结造物{detail}',
      ],
      hitGraze: [
        '{target}避开要害，却仍被【{skill}】余波擦中，受到 {damage} 点擦伤伤害',
        '{target}勉强卸开【{skill}】正锋，仍被余劲扫中，受到 {damage} 点擦伤伤害',
      ],
    };

    function 填充战报润色模板(template = '', data = {}) {
      return String(template || '').replace(/\{([a-zA-Z]+)\}/g, (_, key) => String(data?.[key] ?? '').trim()).replace(/\s+/g, ' ').trim();
    }

    function 选择战报润色模板(scene = '', data = {}, seedText = '') {
      const templates = BATTLE_PUBLIC_FLAVOR_TEMPLATES[scene] || [];
      const required = Array.isArray(data?.__required) ? data.__required : [];
      if (!templates.length || required.some(key => !String(data?.[key] ?? '').trim())) return '';
      return 填充战报润色模板(选择稳定战报短语(templates, seedText), data);
    }

    function 构建公开战报状态抵抗短句(event = {}, seedText = '') {
      const target = String(event?.targetName || '目标').trim();
      const state = 读取事件账本状态名(event);
      if (事件账本状态被免疫(event)) return `${target}免疫了【${state || '状态'}】`;
      return 选择战报润色模板('stateResisted', {
        target,
        state,
        __required: ['target', 'state'],
      }, seedText || `${event?.round || event?.sourceRound || 0}|stateResisted|${event?.actionId || event?.sourceActionId || ''}|${target}|${state}`);
    }

    function 构建公开战报失败短句(event = {}, round = 0) {
      const actor = String(event?.actorName || '行动者').trim();
      const action = normalizeBattleActionDisplayName(event?.finalActionName || event?.actionName || event?.sourceActionName || '');
      const reason = String(event?.failReason || event?.failureReason || event?.meta?.failureReason || '').trim();
      const outcome = String(event?.primaryOutcome || event?.meta?.primaryOutcome || '').trim();
      const reasonCode = String(event?.reasonCode || event?.meta?.reasonCode || '').trim();
      const actionText = `${action} ${event?.actionType || ''} ${event?.type || ''}`;
      const 是守势空过动作 = 判定守势待机动作(actionText);
      if (
        是守势空过动作 &&
        (
          outcome === 'no_effect' ||
          reasonCode === 'NO_EFFECTIVE_OPENING' ||
          /缺少可结算效果|未形成主动结算效果|没有形成主动结算效果|no_effective_opening|NO_EFFECTIVE_OPENING/.test(reason)
        )
      ) {
        return 构建守势待机短句(actor, actionText);
      }
      const mappedReason = 映射玩家可见失败原因(reasonCode, reason);
      if (outcome === 'cap_reached' || reasonCode === 'CAP_REACHED' || mappedReason === '场面已满，造物已达上限') {
        return `${actor}${action ? `的【${action}】` : ''}未能展开：${mappedReason || '场面已满，造物已达上限'}`;
      }
      if (String(event?.actionType || '').trim() === 'summon_control') {
        const summonName = String(event?.meta?.summonName || event?.actorName || '召唤物').trim();
        const hostName = String(event?.meta?.summonHostName || event?.targetName || '').trim();
        const restriction = String(event?.meta?.restriction || event?.result || '').trim();
        const prefix = hostName ? `${hostName}精神负载不足，` : '精神负载不足，';
        if (/dissipated|消散/.test(restriction)) return `${prefix}${summonName}的控制链断裂，被迫消散`;
        if (/recalled|回收/.test(restriction)) return `${prefix}${summonName}的控制链收紧，被强制回收`;
        if (/overload_compressed|compressed|压缩|超载/.test(restriction)) return `${prefix}${summonName}的行动被压缩，暂时无法稳定执行指令`;
        if (/skill_limited|limited|受限/.test(restriction)) return `${prefix}${summonName}的行动受限，只能维持最低限度响应`;
        return `${prefix}${summonName}的控制链受限，未能稳定响应指令`;
      }
      if (mappedReason === '目标已失去有效落点') {
        const target = String(event?.targetName || event?.meta?.declaredTargetName || '原目标').trim();
        return `${actor}${action ? `的【${action}】` : ''}原本锁定${target}，但目标已失去有效落点，本次行动取消`;
      }
      if (mappedReason === '被对手压住节奏') {
        return 选择战报润色模板('blockedAction', {
          actor,
          skill: action,
          __required: ['actor', 'skill'],
        }, `${round || event?.round || event?.sourceRound || 0}|blocked|${event?.actionId || event?.sourceActionId || ''}|${actor}|${action}`) || `${actor}${action ? `的【${action}】` : ''}被对手压住节奏，未能完成出手`;
      }
      if (mappedReason === '被对手截断') {
        return `${actor}${action ? `的【${action}】` : ''}被对手截断，未能完成出手`;
      }
      if (mappedReason === '战机已经消散') {
        return `${actor}${action ? `的【${action}】` : ''}战机已经消散，动作收束`;
      }
      if (mappedReason) {
        return `${actor}${action ? `的【${action}】` : ''}未能完成出手：${mappedReason}`;
      }
      return `${actor}${action ? `的【${action}】` : ''}未能完成出手`;
    }

    function 判定公开战报事件是内部兜底(event = {}) {
      const kind = String(event?.eventKind || '').trim();
      const action = normalizeBattleActionDisplayName(event?.finalActionName || event?.actionName || event?.sourceActionName || '');
      const actionType = String(event?.actionType || event?.type || '').trim();
      const reason = String(event?.failReason || event?.failureReason || event?.reasonText || event?.meta?.reasonText || event?.meta?.failureReason || '').trim();
      const source = String(event?.meta?.source || event?.source || '').trim();
      if (event?.playerAction === true || event?.meta?.playerAction === true || event?.meta?.source === 'player') return false;
      if (String(event?.actorSide || event?.meta?.actorSide || '').trim() === 'player') return false;
      if (kind === 'pass' && /observe|stance_hold/.test(String(event?.result || ''))) return false;
      if (['blocked_action', 'failed_action', 'target_fail'].includes(kind)) {
        if (/CAP_REACHED|达到上限|造物已达上限|场面已满/.test(`${reason} ${event?.reasonCode || event?.meta?.reasonCode || ''}`)) return false;
        if (/战术待机|待机|观察|守势维持|守势对峙|收招转防|防御/.test(action) && /未形成主动结算效果|NO_EFFECTIVE_OPENING|no_effective_opening|没有形成主动结算效果|缺少可结算效果|稳住身位/.test(reason)) return false;
        if (/auto_actor|ai_fallback|internal|system/i.test(source)) return true;
        if (/缺少可结算效果/.test(reason) && /auto|fallback|战术待机|观察|收招转防/.test(`${source} ${actionType} ${action}`)) return true;
      }
      return false;
    }

    function 读取战报事件Outcome(event = {}) {
      const explicit = String(event?.primaryOutcome || event?.meta?.primaryOutcome || '').trim();
      if (explicit) return explicit;
      const kind = String(event?.eventKind || '').trim();
      const result = String(event?.result || event?.meta?.result || '').trim();
      if (kind === 'hit_result') {
        if (/graze|chip|擦伤/.test(result)) return 'graze';
        if (/critical|暴击/.test(result)) return 'critical';
        if (/miss|evade|dodge|未命中|闪避/.test(result)) return 'dodged';
        return 读取事件账本数值(event, 'damage') > 0 ? 'full_hit' : 'no_effect';
      }
      if (kind === 'state_apply') {
        if (事件账本状态被免疫(event)) return 'state_immune';
        if (事件账本状态被抵抗(event)) return 'state_resisted';
        return 'state_applied';
      }
      if (kind === 'state_tick') return 'state_tick';
      if (kind === 'summon_assist') return 'summon_action';
      if (kind === 'create') return 'item_created';
      if (kind === 'summon_create') return 'summon_created';
      if (kind === 'resource_change' || kind === 'round_recover') return 'resource_recovered';
      if (kind === 'blocked_action') return 'interrupted';
      if (kind === 'failed_action' || kind === 'target_fail') return /CAP_REACHED|达到上限|造物已达上限|场面已满/.test(`${event?.reasonCode || ''} ${event?.failReason || ''}`) ? 'cap_reached' : 'interrupted';
      return 'no_effect';
    }

    function 事件账本状态已附着(event = {}) {
      const result = String(event?.result || event?.meta?.result || '').trim();
      return !result || /applied|success|生效|附着|施加/.test(result);
    }

    function 事件账本状态被抵抗(event = {}) {
      return /resist|resisted|抵抗|豁免|未附着/.test(String(event?.result || event?.meta?.result || '').trim());
    }

    function 事件账本状态被免疫(event = {}) {
      return /immune|immunity|免疫|无视异常/.test(String(event?.result || event?.meta?.result || '').trim());
    }

    function formatBattleReactionPhrase(actor = '', action = '') {
      const actorName = String(actor || '防守方').trim();
      const actionName = normalizeBattleActionDisplayName(action);
      if (
        !actionName ||
        actionName === '无' ||
        /无暇反应|无法反应|无反应|来不及反应/.test(actionName)
      ) {
        return 选择稳定战报短语([
          `令${actorName}猝不及防`,
          `令${actorName}无暇招架`,
          `${actorName}一时难以规避`,
        ], `${actorName}|${actionName}|no-reaction`) || `令${actorName}猝不及防`;
      }
      if (/伺机闪避|闪避/.test(actionName)) {
        return 选择稳定战报短语([
          `${actorName}以【${actionName}】闪身避让`,
          `${actorName}借【${actionName}】迅速拉开步点`,
          `${actorName}借【${actionName}】抢先侧身周旋`,
        ], `${actorName}|${actionName}|dodge`) || `${actorName}以【${actionName}】闪身避让`;
      }
      if (/肉体兜底/.test(actionName)) return `${actorName}兜住攻势，承压寻找反打窗口`;
      if (/承伤硬抗|硬抗/.test(actionName)) return `${actorName}收缩防线，顶住这轮攻势`;
      if (/防御|危机自保|收招转防|借力守势|坚壁反制/.test(actionName)) return `${actorName}转入防御，稳住防线`;
      if (actionName) {
        return 选择稳定战报短语([
          `${actorName}以一招【${actionName}】强硬应对`,
          `${actorName}以【${actionName}】从容周旋`,
          `${actorName}借【${actionName}】稳住局面`,
        ], `${actorName}|${actionName}|generic-action`) || `${actorName}以【${actionName}】从容周旋`;
      }
      return 选择稳定战报短语([
        `${actorName}见状迅速稳住阵脚`,
        `${actorName}急忙调动魂力护体`,
        `${actorName}在千钧一发之际做出反应`,
      ], `${actorName}|generic-reaction`) || `${actorName}见状迅速稳住阵脚`;
    }

    function 读取战报日志回合号(raw = '', fallbackRound = 0) {
      const match = String(raw || '').match(/[【\[]第(\d+)回合[】\]]|第(\d+)回合[：:]|[【\[]团战第(\d+)回合开始[】\]]/);
      return Math.max(0, Number(match?.[1] || match?.[2] || match?.[3] || fallbackRound || 0));
    }

    function 查找战报上下文单位(context = {}, name = '') {
      const targetName = String(name || '').trim();
      if (!targetName) return null;
      const combatData = context?.combatData && typeof context.combatData === 'object' ? context.combatData : context;
      const participants = combatData?.参战者 && typeof combatData.参战者 === 'object' ? combatData.参战者 : {};
      const pools = [
        ...(Array.isArray(participants.team_player) ? participants.team_player : []),
        ...(Array.isArray(participants.team_enemy) ? participants.team_enemy : []),
        ...(Array.isArray(participants.player_team) ? participants.player_team : []),
        ...(Array.isArray(participants.enemy_team) ? participants.enemy_team : []),
        ...(Array.isArray(combatData?.units) ? combatData.units : []),
        ...(Array.isArray(context?.units) ? context.units : []),
      ];
      return pools.find(unit => isSameBattleReportName(getCombatReportUnitName(unit), targetName)) || null;
    }

    function 按名称解析角色战斗魂技_战报(unit = {}, name = '') {
      const query = normalizeBattleActionDisplayName(name || '');
      if (!unit || typeof unit !== 'object' || !query) return null;
      const actions = fallbackCollectActions(unit);
      const matched = actions.find(action => {
        const names = [
          action?.name,
          action?.skill?.name,
          action?.skill?.魂技名,
          action?.raw_skill?.name,
          action?.raw_skill?.魂技名,
          action?.skill?.__魂技槽位,
          action?.raw_skill?.__魂技槽位,
        ].map(item => normalizeBattleActionDisplayName(item || '')).filter(Boolean);
        return names.some(item => item === query || isSameBattleReportName(item, query));
      });
      return matched?.raw_skill || matched?.skill || null;
    }

    function 读取战报伤害烈度文本(hit = {}, totalDamage = 0, context = {}) {
      const targetName = String(hit?.target || '').trim();
      const target = 查找战报上下文单位(context, targetName);
      if (!target) return '';
      const maxHp = Math.max(1, Number(getCombatHpMaxValue(target) || 0));
      if (!(maxHp > 1)) return '';
      const ratio = Math.max(0, Number(totalDamage || 0)) / maxHp;
      if (ratio >= 0.6) return '，几乎将其逼入濒危边缘';
      if (ratio >= 0.3) return '，瞬间撕开其防线，令其遭到重创';
      if (ratio >= 0.12) return '，令其闷哼一声，身形明显受创';
      if (ratio > 0 && ratio < 0.05) return '，只在其身上留下一道轻微擦伤';
      return '';
    }

    function 构建公开战报状态附加短句(items = [], options = {}) {
      const text = String(格式化状态附着补充文本(items, options) || '').trim();
      if (!text) return '';
      return text;
    }

    function 构建公开战报状态来源短句(item = {}, options = {}) {
      const sourceActor = String(item.sourceActorName || item.actorName || '').trim();
      const sourceAction = normalizeBattleActionDisplayName(item.sourceActionName || item.actionName || '');
      const sourceRound = Math.max(0, Number(item.sourceRound || item.round || 0));
      const duration = Math.max(0, Number(item.duration || item.持续回合 || 0));
      const sourceParts = [];
      if (sourceActor && sourceAction && sourceRound > 0) sourceParts.push(`该状态由第${sourceRound}回合${sourceActor}施展【${sourceAction}】附加`);
      if (duration > 0) sourceParts.push(`持续${duration}回合`);
      if (!sourceParts.length) return '';
      if (options?.plainText === true) return '';
      const detailParts = [];
      const effectSummary = String(item.effectSummary || '').trim();
      const driverAttr = String(item.driverAttr || '').trim();
      if (effectSummary) detailParts.push(`效果：${effectSummary}`);
      if (driverAttr) detailParts.push(`驱动属性：${driverAttr}`);
      const title = detailParts.join(' | ');
      return `<span class="combat-subtext"${title ? ` title="${htmlEscapeText(title)}"` : ''}>（${htmlEscapeText(sourceParts.join('，'))}）</span>`;
    }

    function 构建公开战报反击短句(actorName = '', actionName = '', targetName = '', damage = 0) {
      const actor = String(actorName || '反击方').trim();
      const action = normalizeBattleActionDisplayName(actionName || '') || '反击';
      const target = String(targetName || '对手').trim();
      const damageValue = Math.max(0, Number(damage || 0));
      return damageValue > 0
        ? `${actor}抓住${target}露出的破绽，以【${action}】进行反击，造成了 ${damageValue} 点伤害`
        : `${actor}抓住${target}露出的破绽，以【${action}】进行反击，但未能造成实质伤害`;
    }

    function 合并回合伤害文本(hits = [], context = {}) {
      const list = (Array.isArray(hits) ? hits : []).filter(item => item?.damage > 0);
      if (!list.length) return '';
      const total = list.reduce((sum, item) => sum + Math.max(0, Number(item.damage || 0)), 0);
      const actor = String(list[0]?.actor || '攻击方').trim();
      const target = String(list[0]?.target || '目标').trim();
      const severity = 读取战报伤害烈度文本(list[0], total, context);
      if (list.length > 1) return `${actor}连续${list.length}次命中${target}，共造成 ${total} 点伤害${severity}`;
      return `${actor}对${target}造成了 ${total} 点伤害${severity}`;
    }

    function 判定战报动作是待机守势(action = '') {
      return /战术待机|待机|观察|守势维持|守势对峙|防御|危机自保|承伤硬抗|硬抗|肉体兜底|闪避|伺机闪避|撤离/.test(normalizeBattleActionDisplayName(action));
    }

    function 构建起招战报开头(opening = {}, target = '') {
      const actor = String(opening?.actor || '行动者').trim();
      const action = normalizeBattleActionDisplayName(opening?.action || '');
      const targetName = String(target || '敌方目标').trim();
      if (/战术待机|待机|观察|守势维持|守势对峙/.test(action)) return `${actor}稳住阵脚，观察${targetName}的动向`;
      if (/承伤硬抗|硬抗|肉体兜底/.test(action)) return `${actor}收缩自身防线，稳住身位`;
      if (/防御|危机自保/.test(action)) return `${actor}转入防御，收缩自身防线`;
      if (/闪避|伺机闪避/.test(action)) return `${actor}以【${action}】拉开身位，避开正面碰撞`;
      if (/撤离/.test(action)) return `${actor}尝试【${action}】，寻找脱离战场的窗口`;
      return `${actor}施展【${action || '行动'}】指向${targetName}`;
    }

    function 格式化位移战报文本(items = []) {
      const list = (Array.isArray(items) ? items : []).map(item => String(item?.text || '').trim()).filter(Boolean);
      if (!list.length) return '';
      return list
        .slice(0, 2)
        .map(text => {
          if (/换位/.test(text)) return `位置变化生效，${text.replace(/^换位/, '换位')}`;
          if (/未生效|未成立|缺少/.test(text)) return `位移尝试未能完全成立，${text}`;
          return `身位变化生效，${text}`;
        })
        .join('；');
    }

    function 格式化造物战报文本(items = []) {
      const list = (Array.isArray(items) ? items : []).map(item => String(item?.text || '').trim()).filter(Boolean);
      if (!list.length) return '';
      return list.slice(0, 2).map(text => (/^[^，。！？\s]+以【[^】]+】/.test(text) ? text : `造物凝成：${text}`)).join('；');
    }

    function 格式化状态标签HTML(state = '', extras = '', options = {}) {
      const 状态名 = String(state || '').trim();
      if (!状态名) return '';
      const 提示 = String(extras || '').trim();
      if (options?.plainText === true) return `【${状态名}】`;
      return `<span class="combat-state-tag"${提示 ? ` title="${htmlEscapeText(提示)}"` : ''}>【${htmlEscapeText(状态名)}】</span>`;
    }

    function 格式化状态附着补充文本(items = [], options = {}) {
      const list = (Array.isArray(items) ? items : []).filter(item => item && item.state && item.target);
      if (!list.length) return '';
      const seen = new Set();
      const groups = new Map();
      list.filter(item => {
        const key = `${normalizeBattleReportNameForMatch(item.target)}|${String(item.state || '').trim()}`;
        if (seen.has(key)) return false;
        seen.add(key);
        return true;
      }).slice(0, 3).forEach(item => {
        const target = String(item.target || '').trim();
        if (!groups.has(target)) groups.set(target, []);
        groups.get(target).push(item);
      });
      return Array.from(groups.entries()).map(([target, group]) => {
        const states = group.map(item => {
          const state = String(item.state || '').trim();
          const duration = Math.max(0, Number(item.duration || item.持续回合 || 0));
          const effectSummary = String(item.effectSummary || '').trim();
          const driverAttr = String(item.driverAttr || '').trim();
          const extras = [
            duration > 0 ? `持续${duration}回合` : '',
            effectSummary ? `效果：${effectSummary}` : '',
            driverAttr ? `驱动属性：${driverAttr}` : '',
          ].filter(Boolean).join('，');
          const 状态标签 = 格式化状态标签HTML(state, extras, { plainText: options?.plainText === true });
          const 次级说明 = options?.plainText === true && duration > 0 ? `（持续${duration}回合）` : '';
          return `${状态标签}${options?.plainText === true ? 次级说明 : ''}`;
        }).filter(Boolean);
        if (!states.length) return '';
        return `${group.length > 1 ? '这一击同时令' : '这一击令'}${target}陷入${states.join('与')}`;
      }).filter(Boolean).join('；');
    }

    function 规范化状态附着断句(line = '') {
      return String(line || '')
        .replace(/，(这一击(?:同时)?令)/g, '。$1')
        .replace(/；(这一击(?:同时)?令)/g, '。$1')
        .replace(/。+/g, '。')
        .replace(/。；/g, '。');
    }

    function 规范化公开战报连续回合(lines = []) {
      return (Array.isArray(lines) ? lines : [])
        .map(line => String(line || '').trim())
        .filter(Boolean);
    }

    function 归一战斗事件记录(item = {}, patch = {}) {
      const eventKind = String(patch.eventKind || item.eventKind || '').trim();
      const normalized = {
        eventId: String(patch.eventId || item.eventId || '').trim(),
        eventKind,
        round: Number(patch.round || item.round || 0),
        actorName: String(patch.actorName || item.actorName || item.actor || '').trim(),
        actorSide: String(patch.actorSide || item.actorSide || item.side || item.meta?.actorSide || item.meta?.side || '').trim(),
        targetName: String(patch.targetName || item.targetName || item.target || '').trim(),
        targetSide: String(patch.targetSide || item.targetSide || item.meta?.targetSide || '').trim(),
        targetId: String(patch.targetId || item.targetId || item.targetKey || item.target_id || '').trim(),
        targetScope: String(patch.targetScope || item.targetScope || item.meta?.targetScope || '').trim(),
        actionName: normalizeBattleActionDisplayName(patch.actionName || item.actionName || item.meta?.actionName || item.skillName || item.meta?.skillName || item.action || ''),
        initialActionName: normalizeBattleActionDisplayName(patch.initialActionName || item.initialActionName || item.meta?.initialActionName || item.actionName || item.meta?.actionName || item.skillName || item.meta?.skillName || item.action || ''),
        finalActionName: normalizeBattleActionDisplayName(patch.finalActionName || item.finalActionName || item.meta?.finalActionName || item.actionName || item.meta?.actionName || item.skillName || item.meta?.skillName || item.action || ''),
        discardedActionName: normalizeBattleActionDisplayName(patch.discardedActionName || item.discardedActionName || item.meta?.discardedActionName || ''),
        actionType: String(patch.actionType || item.actionType || '').trim(),
        actionId: String(patch.actionId || item.actionId || '').trim(),
        sourceActionName: normalizeBattleActionDisplayName(patch.sourceActionName || item.sourceActionName || item.meta?.sourceActionName || ''),
        sourceActionId: String(patch.sourceActionId || item.sourceActionId || '').trim(),
        sourceRound: Number(patch.sourceRound || item.sourceRound || 0),
        chainNodeId: String(patch.chainNodeId || item.chainNodeId || '').trim(),
        parentNodeId: String(patch.parentNodeId || item.parentNodeId || '').trim(),
        sourceNodeId: String(patch.sourceNodeId || item.sourceNodeId || '').trim(),
        result: String(patch.result || item.result || '').trim(),
        failReason: String(patch.failReason || item.failReason || '').trim(),
        primaryOutcome: String(patch.primaryOutcome || item.primaryOutcome || item.meta?.primaryOutcome || '').trim(),
        failureReason: String(patch.failureReason || item.failureReason || item.failReason || item.meta?.failureReason || '').trim(),
        reasonCode: String(patch.reasonCode || item.reasonCode || item.meta?.reasonCode || '').trim(),
        reasonText: String(patch.reasonText || item.reasonText || item.meta?.reasonText || '').trim(),
        replanReasonCode: String(patch.replanReasonCode || item.replanReasonCode || item.meta?.replanReasonCode || '').trim(),
        replanReasonText: String(patch.replanReasonText || item.replanReasonText || item.meta?.replanReasonText || '').trim(),
        targetPoolSide: String(patch.targetPoolSide || item.targetPoolSide || '').trim(),
        applicationId: String(patch.applicationId || item.applicationId || '').trim(),
        duration: Math.max(0, Number(patch.duration || item.duration || 0)),
        effectSummary: String(patch.effectSummary || item.effectSummary || '').trim(),
        driverAttr: String(patch.driverAttr || item.driverAttr || '').trim(),
        meta: patch.meta && typeof patch.meta === 'object' ? patch.meta : (item.meta && typeof item.meta === 'object' ? item.meta : {}),
      };
      const actionStatus = String(patch.actionStatus || item.actionStatus || item.meta?.actionStatus || '').trim();
      if (actionStatus && /action|hit_result|state_apply|resource_change|create|summon|shield|blocked|failed|target_fail/.test(eventKind)) {
        normalized.actionStatus = actionStatus;
      }
      if (['hit_result', 'counter', 'state_tick'].includes(eventKind)) {
        normalized.appliedDamage = Math.max(0, Number(patch.appliedDamage ?? item.appliedDamage ?? patch.damage ?? item.damage ?? patch.meta?.appliedDamage ?? item.meta?.appliedDamage ?? patch.meta?.damage ?? item.meta?.damage ?? 0) || 0);
        normalized.damage = Math.max(0, Number(patch.damage ?? item.damage ?? patch.appliedDamage ?? item.appliedDamage ?? patch.meta?.damage ?? item.meta?.damage ?? patch.meta?.appliedDamage ?? item.meta?.appliedDamage ?? 0) || 0);
      }
      if (eventKind === 'hit_result') {
        normalized.effectCapability = (() => {
          const capability = patch.effectCapability && typeof patch.effectCapability === 'object'
            ? patch.effectCapability
            : (item.effectCapability && typeof item.effectCapability === 'object'
              ? item.effectCapability
              : (patch.meta?.effectCapability && typeof patch.meta.effectCapability === 'object'
                ? patch.meta.effectCapability
                : (item.meta?.effectCapability && typeof item.meta.effectCapability === 'object' ? item.meta.effectCapability : null)));
          if (!capability) return null;
          return {
            hasDamageEffect: capability.hasDamageEffect === true,
            effectKinds: Array.isArray(capability.effectKinds)
              ? capability.effectKinds.map(kind => String(kind || '').trim()).filter(Boolean).slice(0, 12)
              : [],
          };
        })();
      }
      Object.keys(normalized).forEach(key => {
        if (normalized[key] === '' || normalized[key] === null) delete normalized[key];
      });
      if (normalized.meta && typeof normalized.meta === 'object' && !Object.keys(normalized.meta).length) {
        delete normalized.meta;
      }
      return normalized;
    }

    function 读取事件账本状态名(event = {}) {
      return String(event?.stateName || event?.meta?.stateName || '').trim();
    }

    function 读取事件账本数值(event = {}, key = '') {
      if (['damage', 'finalDamage', 'appliedDamage'].includes(String(key || '').trim())) {
        const value = Number(event?.appliedDamage ?? event?.meta?.appliedDamage ?? event?.[key] ?? event?.meta?.[key] ?? 0);
        return Number.isFinite(value) ? value : 0;
      }
      return Number(event?.[key] ?? event?.meta?.[key] ?? 0);
    }

    function 格式化事件账本防御短语(event = {}) {
      const actor = String(event?.actorName || '防守方').trim();
      const action = normalizeBattleActionDisplayName(event?.actionName || '');
      if (!actor) return '';
      if (!action || action === '无' || /无暇反应|无法反应|无反应|来不及反应/.test(action)) {
        return 选择稳定战报短语([
          `令${actor}猝不及防`,
          `令${actor}无暇招架`,
          `${actor}一时难以规避`,
        ], `${actor}|${action}|ledger-no-reaction`) || `令${actor}猝不及防`;
      }
      if (/闪避|伺机闪避/.test(action) || event?.eventKind === 'dodge') {
        return /evaded|miss|dodge_success|闪避成功|未命中/.test(String(event?.result || '').trim())
          ? `${actor}闪身避让`
          : `${actor}试图闪身避让`;
      }
      if (/肉体兜底/.test(action)) return `${actor}兜住攻势，承压寻找反打窗口`;
      if (/承伤硬抗|硬抗/.test(action)) return `${actor}收缩防线，顶住这轮攻势`;
      if (/防御|危机自保|收招转防|借力守势|坚壁反制/.test(action) || event?.eventKind === 'defend') return `${actor}转入防御，稳住防线`;
      if (action) return `${actor}以【${action}】从容周旋`;
      return `${actor}见状迅速稳住阵脚`;
    }

    function 构建公开战报文本块(content = '', source = {}) {
      const text = String(content || '').trim();
      if (!text) return null;
      const block = { type: 'text', content: text };
      const sourceEventId = String(source?.eventId || source?.sourceEventId || '').trim();
      const sourceNodeId = String(source?.chainNodeId || source?.nodeId || source?.sourceNodeId || '').trim();
      if (sourceEventId) block.sourceEventId = sourceEventId;
      if (sourceNodeId) block.sourceNodeId = sourceNodeId;
      const sourceEventIds = Array.isArray(source?.sourceEventIds)
        ? source.sourceEventIds.map(id => String(id || '').trim()).filter(Boolean)
        : [];
      const sourceNodeIds = Array.isArray(source?.sourceNodeIds)
        ? source.sourceNodeIds.map(id => String(id || '').trim()).filter(Boolean)
        : [];
      if (sourceEventIds.length) block.sourceEventIds = [...new Set(sourceEventIds)];
      if (sourceNodeIds.length) block.sourceNodeIds = [...new Set(sourceNodeIds)];
      return block;
    }

    function 构建公开战报Badge块(kind = '', payload = {}) {
      const badgeKind = String(kind || '').trim();
      if (!badgeKind) return null;
      const block = {
        type: 'badge',
        kind: badgeKind,
        targetId: String(payload.targetId || '').trim(),
        targetName: String(payload.targetName || '').trim(),
        isSelf: payload.isSelf === true,
        sourceEventId: String(payload.sourceEventId || '').trim(),
        sourceNodeId: String(payload.sourceNodeId || '').trim(),
      };
      if (payload.value !== undefined) block.value = Number(payload.value || 0);
      if (payload.unit) block.unit = String(payload.unit || '').trim();
      if (payload.name) block.name = String(payload.name || '').trim();
      return block;
    }

    function 应用公开战报Badge错落(entry = {}) {
      const blocks = Array.isArray(entry?.blocks) ? entry.blocks : [];
      const animatedBadges = blocks.filter(block =>
        block &&
        block.type === 'badge' &&
        ['damage', 'heal', 'resource'].includes(String(block.kind || '').trim())
      );
      if (animatedBadges.length < 2) return entry;
      animatedBadges.forEach((block, index) => {
        block.delayMs = Math.min(600, index * 120);
        block.delayQueue = 'battle_badge_stagger';
      });
      entry.badgeDelayQueue = 'battle_badge_stagger';
      return entry;
    }

    function 序列化公开战报Blocks(blocks = []) {
      return (Array.isArray(blocks) ? blocks : [])
        .map(block => {
          if (!block || typeof block !== 'object') return '';
          if (block.type === 'text') return String(block.content || '').trim();
          if (block.type === 'badge') {
            if (block.kind === 'damage') return `${Number(block.value || 0)} ${block.unit || 'HP'}`;
            if (block.kind === 'heal') return `+${Math.max(0, Number(block.value || 0))} ${block.unit || 'HP'}`;
            if (block.kind === 'resource') return `${Number(block.value || 0) > 0 ? '+' : ''}${Math.round(Number(block.value || 0))} ${block.unit || block.name || '资源'}`;
            if (['item_created', 'summon_created', 'creation', 'cap_reached'].includes(block.kind)) {
              const name = String(block.name || (block.kind === 'cap_reached' ? '上限' : '造物生成')).trim();
              const value = Math.max(0, Math.round(Number(block.value || 0)));
              return block.kind === 'cap_reached' ? `【${name}】` : `${value > 0 ? `+${value} ` : ''}${name}`;
            }
            if (block.name) return `【${block.name}】`;
          }
          return '';
        })
        .filter(Boolean)
        .join(' ')
        .trim();
    }

    function 归一公开战报Block条目(item = {}) {
      const blocks = Array.isArray(item?.blocks) ? item.blocks : (Array.isArray(item) ? item : []);
      if (!blocks.length) return null;
      const text = 序列化公开战报Blocks(blocks);
      if (!text) return null;
      return {
        ...item,
        blocks,
        text,
      };
    }

    function 构建状态Tick公开战报Block条目(eventLedger = []) {
      const events = (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(event => String(event?.eventKind || '').trim() === 'state_tick');
      if (!events.length) return [];
      const 读取状态Tick来源文本 = event => {
        const meta = event?.meta && typeof event.meta === 'object' ? event.meta : {};
        const sourceActor = String(meta.sourceActorName || event?.actorName || '').trim();
        const sourceAction = String(meta.sourceActionName || event?.sourceActionName || event?.actionName || '').trim();
        const sourceRound = Number(meta.sourceRound || event?.sourceRound || 0);
        if (!sourceActor || !sourceAction || !(sourceRound > 0)) return '';
        return `（该状态由第${sourceRound}回合${sourceActor}施展【${sourceAction}】附加）`;
      };
      const byRound = new Map();
      events.forEach(event => {
        const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
        if (!byRound.has(round)) byRound.set(round, []);
        byRound.get(round).push(event);
      });
      const entries = [];
      [...byRound.entries()].sort((left, right) => Number(left[0] || 0) - Number(right[0] || 0)).forEach(([round, roundEvents]) => {
        const prefix = round > 0 ? `第${round}回合：` : '';
        const groups = new Map();
        roundEvents.forEach(item => {
          const state = 读取事件账本状态名(item) || '持续状态';
          const resource = String(item?.meta?.resource || '生命值').trim();
          const isHeal = String(item?.result || '').includes('恢复');
          const key = `${state}|${resource}|${isHeal ? 'heal' : 'damage'}`;
          if (!groups.has(key)) groups.set(key, { state, resource, isHeal, sourceText: '', items: [] });
          groups.get(key).items.push(item);
        });
        groups.forEach(grouped => {
          const sourceTexts = [...new Set(grouped.items.map(读取状态Tick来源文本).filter(Boolean))];
          grouped.sourceText = sourceTexts.length === 1 ? sourceTexts[0] : '';
          let text = '';
          if (grouped.items.length >= 2) {
            const total = grouped.items.reduce((sum, item) => sum + Math.max(0, 读取事件账本数值(item, 'amount')), 0);
            const targets = grouped.items.map(item => String(item.targetName || '目标').trim()).filter(Boolean);
            const targetText = targets.length >= 2 ? `${targets.length} 个目标` : targets.join('、');
            text = `${prefix}回合收束，${targetText || '场上目标'}共${grouped.isHeal ? '恢复' : '损失'} ${total} 点${grouped.resource}，来源为【${grouped.state}】${grouped.sourceText}。`;
          } else {
            const item = grouped.items[0];
            const target = String(item?.targetName || '目标').trim();
            const amount = Math.max(0, 读取事件账本数值(item, 'amount'));
            const flavored = 选择战报润色模板(grouped.isHeal ? 'stateTickHeal' : 'stateTickDamage', {
              target,
              state: grouped.state,
              amount,
              resource: grouped.resource,
              sourceText: grouped.sourceText,
              __required: ['target', 'state', 'amount', 'resource'],
            }, `${round}|stateTick|${target}|${grouped.state}|${amount}|${grouped.resource}|${grouped.isHeal ? 'heal' : 'damage'}`);
            text = `${prefix}${flavored || `${target}随后受【${grouped.state}】影响，${grouped.isHeal ? '恢复' : '损失'}了 ${amount} 点${grouped.resource}${grouped.sourceText}`}。`;
          }
          const sourceEventIds = grouped.items.map(item => String(item?.eventId || '').trim()).filter(Boolean);
          const sourceNodeIds = grouped.items.map(item => String(item?.chainNodeId || '').trim()).filter(Boolean);
          const textBlock = 构建公开战报文本块(text, {
            ...(grouped.items[0] || {}),
            sourceEventIds,
            sourceNodeIds,
          });
          if (textBlock) textBlock.projectionSource = 'state_tick_ast';
          const blocks = [textBlock].filter(Boolean);
          grouped.items.forEach(item => {
            const amount = Math.max(0, 读取事件账本数值(item, 'amount'));
            if (amount <= 0) return;
            const isHpResource = /生命值|HP|气血|血量/.test(grouped.resource);
            blocks.push(构建公开战报Badge块(isHpResource ? (grouped.isHeal ? 'heal' : 'damage') : 'resource', {
              value: isHpResource ? (grouped.isHeal ? amount : -amount) : (grouped.isHeal ? amount : -amount),
              unit: isHpResource ? 'HP' : grouped.resource,
              name: grouped.resource,
              targetName: String(item?.targetName || '').trim(),
              targetId: String(item?.targetId || '').trim(),
              sourceEventId: String(item?.eventId || '').trim(),
              sourceNodeId: String(item?.chainNodeId || '').trim(),
            }));
          });
          entries.push({ round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'state_tick_ast' });
        });
      });
      return entries;
    }

    function 构建召唤生成公开战报Block条目(eventLedger = []) {
      const events = (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(event => String(event?.eventKind || '').trim() === 'summon_create');
      if (!events.length) return [];
      const groups = new Map();
      events.forEach(event => {
        const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
        const actor = String(event?.actorName || '行动者').trim();
        const action = normalizeBattleActionDisplayName(event?.actionName || event?.sourceActionName || '召唤');
        const key = `${round}|${actor}|${action}`;
        if (!groups.has(key)) groups.set(key, { round, actor, action, items: [] });
        groups.get(key).items.push(event);
      });
      return [...groups.values()].sort((left, right) => Number(left.round || 0) - Number(right.round || 0)).map(group => {
        const prefix = group.round > 0 ? `第${group.round}回合：` : '';
        const summonText = group.items.map(item => {
          const summonName = String(item?.meta?.summonName || item?.summonName || '召唤物').trim();
          const summonType = String(item?.meta?.summonType || item?.summonType || '召唤单位').trim();
          const summonMode = String(item?.meta?.summonMode || item?.summonMode || '协同攻击').trim();
          const mentalLoad = Math.max(0, Math.round(Number(item?.meta?.mentalLoad ?? item?.mentalLoad ?? 0)));
          const loadText = mentalLoad > 0 ? `，精神负载 ${mentalLoad}` : '';
          return `召出${summonType}【${summonName}】。行动模式：${summonMode}${loadText}，将从下一回合开始参战`;
        }).filter(Boolean).join('；');
        const text = `${prefix}${group.actor}施展【${group.action || '召唤'}】，${summonText}。`;
        const sourceEventIds = group.items.map(item => String(item?.eventId || '').trim()).filter(Boolean);
        const sourceNodeIds = group.items.map(item => String(item?.chainNodeId || '').trim()).filter(Boolean);
        const textBlock = 构建公开战报文本块(text, {
          ...(group.items[0] || {}),
          sourceEventIds,
          sourceNodeIds,
        });
        if (textBlock) textBlock.projectionSource = 'summon_create_ast';
        const blocks = [textBlock].filter(Boolean);
        return { round: group.round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'summon_create_ast' };
      });
    }

    function 构建护盾生成公开战报Block条目(eventLedger = []) {
      const events = (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(event => String(event?.eventKind || '').trim() === 'shield_create');
      if (!events.length) return [];
      const groups = new Map();
      events.forEach(event => {
        const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
        const actor = String(event?.actorName || '行动者').trim();
        const action = normalizeBattleActionDisplayName(event?.actionName || event?.sourceActionName || '护盾');
        const key = `${round}|${actor}|${action}`;
        if (!groups.has(key)) groups.set(key, { round, actor, action, items: [] });
        groups.get(key).items.push(event);
      });
      return [...groups.values()].sort((left, right) => Number(left.round || 0) - Number(right.round || 0)).map(group => {
        const prefix = group.round > 0 ? `第${group.round}回合：` : '';
        const shieldText = group.items.map(item => {
          const amount = Math.max(0, 读取事件账本数值(item, 'amount'));
          const targetName = String(item?.targetName || group.actor).trim() || group.actor;
          if (isSameBattleReportName(targetName, group.actor)) return amount > 0 ? `为自身张开 ${amount} 点护盾` : '稳住自身防线';
          return amount > 0 ? `为${targetName}张开 ${amount} 点护盾` : `为${targetName}稳住防线`;
        }).filter(Boolean).join('；');
        const text = `${prefix}${group.actor}${shieldText ? `施展【${group.action || '护盾'}】，${shieldText}` : '转入防御，稳住防线'}。`;
        const sourceEventIds = group.items.map(item => String(item?.eventId || '').trim()).filter(Boolean);
        const sourceNodeIds = group.items.map(item => String(item?.chainNodeId || '').trim()).filter(Boolean);
        const textBlock = 构建公开战报文本块(text, {
          ...(group.items[0] || {}),
          sourceEventIds,
          sourceNodeIds,
        });
        if (textBlock) textBlock.projectionSource = 'shield_create_ast';
        const blocks = [textBlock].filter(Boolean);
        group.items.forEach(item => {
          const amount = Math.max(0, 读取事件账本数值(item, 'amount'));
          if (amount <= 0) return;
          const targetName = String(item?.targetName || group.actor).trim() || group.actor;
          blocks.push(构建公开战报Badge块('shield', {
            value: amount,
            targetName,
            targetId: String(item?.targetId || '').trim(),
            sourceEventId: String(item?.eventId || '').trim(),
            sourceNodeId: String(item?.chainNodeId || '').trim(),
          }));
        });
        return { round: group.round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'shield_create_ast' };
      });
    }

    function 构建命中结算公开战报Block条目(eventLedger = []) {
      const events = (Array.isArray(eventLedger) ? eventLedger : [])
        .map(item => item && typeof item === 'object' ? item : null)
        .filter(Boolean);
      if (!events.length) return [];
      const byRound = new Map();
      events.forEach(event => {
        const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
        if (!byRound.has(round)) byRound.set(round, []);
        byRound.get(round).push(event);
      });
      const entries = [];
      const sameActionFact = (event = {}, actionEvent = {}) => {
        const actionId = String(actionEvent?.actionId || '').trim();
        if (!actionId) return false;
        return String(event?.actionId || '').trim() === actionId ||
          String(event?.sourceActionId || '').trim() === actionId;
      };
      [...byRound.entries()].sort((left, right) => Number(left[0] || 0) - Number(right[0] || 0)).forEach(([round, roundEvents]) => {
        const prefix = round > 0 ? `第${round}回合：` : '';
        const starts = roundEvents.filter(item => item.eventKind === 'action_start');
        const hits = roundEvents.filter(item =>
          item.eventKind === 'hit_result' &&
          String(item.actionType || '').trim() !== 'counter' &&
          !/行为防反|系统反击|counter/i.test(String(item.actionName || ''))
        );
        const defenses = roundEvents.filter(item => ['dodge', 'defend', 'pass'].includes(String(item.eventKind || '').trim()));
        const counters = roundEvents.filter(item => item.eventKind === 'counter');
        const appliedStates = roundEvents.filter(item => item.eventKind === 'state_apply' && 事件账本状态已附着(item));
        const resistedStates = roundEvents.filter(item => item.eventKind === 'state_apply' && (事件账本状态被抵抗(item) || 事件账本状态被免疫(item)));
        hits.forEach(hit => {
          const actor = String(hit.actorName || '攻击方').trim();
          const target = String(hit.targetName || '目标').trim();
          const action = normalizeBattleActionDisplayName(hit.finalActionName || hit.actionName || starts.find(item => isSameBattleReportName(item.actorName, actor))?.actionName || '');
          const start = starts.find(item =>
            sameActionFact(hit, item) ||
            (isSameBattleReportName(item.actorName, actor) &&
              (!item.targetName || !target || isSameBattleReportName(item.targetName, target)) &&
              (!action || normalizeBattleActionDisplayName(item.actionName || '') === action))
          );
          const defense = defenses.find(item =>
            target &&
            isSameBattleReportName(item.actorName, target) &&
            (!start?.actionId || String(item.sourceActionId || '').trim() === String(start.actionId || '').trim())
          ) || defenses.find(item => target && isSameBattleReportName(item.actorName, target));
          const damage = Math.max(0, 读取事件账本数值(hit, 'damage'));
          const relatedStates = appliedStates.filter(item =>
            sameActionFact(item, start || hit) ||
            (isSameBattleReportName(item.targetName, target) &&
              (!item.actorName || !actor || isSameBattleReportName(item.actorName, actor)) &&
              (!item.sourceActionName || !action || normalizeBattleActionDisplayName(item.sourceActionName) === action))
          );
          const stateText = 构建公开战报状态附加短句(relatedStates.map(item => ({
            target: item.targetName,
            state: 读取事件账本状态名(item),
            duration: item.duration,
            effectSummary: item.effectSummary,
            driverAttr: item.driverAttr,
          })), { plainText: true });
          const resistedStateText = resistedStates.filter(item =>
            sameActionFact(item, start || hit) ||
            (isSameBattleReportName(item.targetName, target) &&
              (!item.actorName || !actor || isSameBattleReportName(item.actorName, actor)) &&
              (!item.sourceActionName || !action || normalizeBattleActionDisplayName(item.sourceActionName) === action))
          ).map(item => 构建公开战报状态抵抗短句(item, `${round}|stateResisted|${item.actionId || item.sourceActionId || ''}|${target}|${读取事件账本状态名(item)}`))
            .filter(Boolean)
            .filter((text, index, list) => list.indexOf(text) === index)
            .join('；');
          const hitActionId = String(hit.actionId || hit.sourceActionId || start?.actionId || '').trim();
          const relatedCounters = counters.filter(item => {
            if (String(item.result || '').trim() === 'fail') return false;
            if (!hitActionId || String(item.sourceActionId || '').trim() !== hitActionId) return false;
            return (
              isSameBattleReportName(item.actorName, target) &&
              isSameBattleReportName(item.targetName, actor)
            ) || String(item?.meta?.counterWindowNodeId || '').trim();
          });
          const relatedSecondCounters = relatedCounters.flatMap(counter => counters.filter(item =>
            Number(item?.meta?.counterDepth || 0) >= 2 &&
            isSameBattleReportName(item.actorName, actor) &&
            isSameBattleReportName(item.targetName, counter.actorName) &&
            (
              String(item.sourceNodeId || '').trim() === String(counter.chainNodeId || '').trim() ||
              String(item.parentNodeId || '').trim() === String(counter.chainNodeId || '').trim() ||
              String(item.sourceActionId || '').trim() === String(counter.actionId || '').trim()
            )
          ));
          const counterText = relatedCounters
            .map(item => {
              const counterAction = normalizeBattleActionDisplayName(item.actionName || '反击');
              const counterDamage = Math.max(0, 读取事件账本数值(item, 'damage'));
              return 选择战报润色模板(counterDamage > 0 ? 'counterHit' : 'counterZero', {
                counterActor: String(item.actorName || '').trim(),
                counterSkill: counterAction,
                target: String(item.targetName || '').trim(),
                damage: counterDamage,
                __required: ['counterActor', 'counterSkill', 'target'],
              }, `${round}|counter|${item.actionId || item.sourceActionId || ''}|${item.actorName}|${item.targetName}|${counterAction}|${counterDamage}`) || 构建公开战报反击短句(item.actorName, counterAction, item.targetName, counterDamage);
            })
            .filter((text, index, list) => list.indexOf(text) === index)
            .join('；');
          const secondCounterText = relatedSecondCounters
            .map(item => {
              const counterAction = normalizeBattleActionDisplayName(item.actionName || '反防反');
              const counterDamage = Math.max(0, 读取事件账本数值(item, 'damage'));
              return `${String(item.actorName || '攻方').trim()}顺势反压，以【${counterAction}】反防反${String(item.targetName || '目标').trim()}${counterDamage > 0 ? `，造成了 ${counterDamage} 点伤害` : '，但未能造成实质伤害'}`;
            })
            .filter((text, index, list) => list.indexOf(text) === index)
            .join('；');
          const pieces = [];
          const assistEvent = String(hit.actionType || '').trim() === 'summon_assist'
            ? roundEvents.find(item =>
                String(item?.eventKind || '').trim() === 'summon_assist' &&
                isSameBattleReportName(item.actorName || '', actor) &&
                isSameBattleReportName(item.targetName || '', target) &&
                normalizeBattleActionDisplayName(item.actionName || '') === action &&
                (!item.meta?.summonHitEventId || String(item.meta.summonHitEventId || '').trim() === String(hit.eventId || '').trim())
              )
            : null;
          const dodgeFullySucceeded = defense?.eventKind === 'dodge' &&
            (/evaded|miss|dodge_success|闪避成功|未命中/.test(String(defense.result || '').trim()) || /miss|evade|dodge|闪避|未命中/.test(String(hit.result || '')));
          const dodgeTriedAndFailed = defense?.eventKind === 'dodge' && !dodgeFullySucceeded && damage > 0;
          const outcome = 读取战报事件Outcome(hit);
          const seed = `${round}|hit|${hit.actionId || hit.sourceActionId || ''}|${actor}|${target}|${action}|${damage}`;
          const flavoredHit = assistEvent && damage > 0
            ? `${actor}承接${String(assistEvent.meta?.hostName || assistEvent.meta?.summonHostName || '宿主').trim()}的攻势，以【${action || '协同追击'}】协同追击${target}，造成了 ${damage} 点伤害`
            : outcome === 'graze' && damage > 0
              ? 选择战报润色模板('hitGraze', {
                  target,
                  skill: action || '行动',
                  damage,
                  __required: ['target', 'skill', 'damage'],
                }, seed) || `${target}避开要害，却仍被【${action || '行动'}】余波擦中，受到 ${damage} 点擦伤伤害`
            : damage > 0
              ? 选择战报润色模板(dodgeTriedAndFailed ? 'hitDodgeFailed' : 'hitPlain', {
                  attacker: actor,
                  target,
                  skill: action || '行动',
                  damage,
                  __required: ['attacker', 'target', 'skill', 'damage'],
                }, seed)
              : '';
          if (flavoredHit) pieces.push(flavoredHit);
          else pieces.push(`${actor}施展【${action || '行动'}】指向${target}`);
          if (defense) {
            const phrase = 格式化事件账本防御短语(defense);
            if (phrase && !flavoredHit) pieces.push(phrase);
            if (defense.eventKind === 'dodge') {
              if (dodgeFullySucceeded) pieces.push('闪避成功，这一击没有命中');
              else if ((damage > 0 || stateText) && !flavoredHit) pieces.push('未能摆脱这轮压制');
            }
          }
          if (damage > 0 && !flavoredHit) pieces.push(`${actor}对${target}造成了 ${damage} 点伤害`);
          else if (!(damage > 0) && !pieces.some(text => /没有命中|未能造成/.test(text))) pieces.push('这次交锋未造成实质伤害');
          if (stateText) pieces.push(stateText);
          if (resistedStateText) pieces.push(resistedStateText);
          if (counterText) pieces.push(`随后${counterText}`);
          if (secondCounterText) pieces.push(`紧接着${secondCounterText}`);
          const text = 规范化状态附着断句(`${prefix}${pieces.join('，').replace(/，随后/g, '；随后')}。`);
          const sourceItems = [start, hit, defense, assistEvent, ...relatedStates, ...relatedCounters, ...relatedSecondCounters].filter(Boolean);
          const sourceEventIds = sourceItems.map(item => String(item?.eventId || '').trim()).filter(Boolean);
          const sourceNodeIds = sourceItems.map(item => String(item?.chainNodeId || '').trim()).filter(Boolean);
          const textBlock = 构建公开战报文本块(text, {
            ...(hit || {}),
            sourceEventIds,
            sourceNodeIds,
          });
          if (textBlock) textBlock.projectionSource = 'hit_result_ast';
          const blocks = [textBlock].filter(Boolean);
          if (damage > 0) {
            blocks.push(构建公开战报Badge块('damage', {
              value: -damage,
              unit: 'HP',
              targetName: target,
              targetId: String(hit?.targetId || '').trim(),
              sourceEventId: String(hit?.eventId || '').trim(),
              sourceNodeId: String(hit?.chainNodeId || '').trim(),
            }));
          }
          relatedStates.forEach(item => {
            const stateName = 读取事件账本状态名(item);
            if (!stateName) return;
            blocks.push(构建公开战报Badge块('state', {
              name: stateName,
              targetName: String(item?.targetName || target).trim(),
              targetId: String(item?.targetId || '').trim(),
              sourceEventId: String(item?.eventId || '').trim(),
              sourceNodeId: String(item?.chainNodeId || '').trim(),
            }));
          });
          relatedCounters.forEach(item => {
            const counterDamage = Math.max(0, 读取事件账本数值(item, 'damage'));
            if (counterDamage <= 0) return;
            blocks.push(构建公开战报Badge块('damage', {
              value: -counterDamage,
              unit: 'HP',
              targetName: String(item?.targetName || actor).trim(),
              targetId: String(item?.targetId || '').trim(),
              sourceEventId: String(item?.eventId || '').trim(),
              sourceNodeId: String(item?.chainNodeId || '').trim(),
            }));
          });
          relatedSecondCounters.forEach(item => {
            const counterDamage = Math.max(0, 读取事件账本数值(item, 'damage'));
            if (counterDamage <= 0) return;
            blocks.push(构建公开战报Badge块('damage', {
              value: -counterDamage,
              unit: 'HP',
              targetName: String(item?.targetName || '').trim(),
              targetId: String(item?.targetId || '').trim(),
              sourceEventId: String(item?.eventId || '').trim(),
              sourceNodeId: String(item?.chainNodeId || '').trim(),
            }));
          });
          entries.push({ round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'hit_result_ast' });
        });
      });
      return entries;
    }

    function 构建状态附着公开战报Block条目(eventLedger = [], context = {}) {
      const events = (Array.isArray(eventLedger) ? eventLedger : [])
        .map(item => item && typeof item === 'object' ? item : null)
        .filter(Boolean);
      if (!events.length) return [];
      const hits = events.filter(item => String(item?.eventKind || '').trim() === 'hit_result');
      const starts = events.filter(item => String(item?.eventKind || '').trim() === 'action_start');
      const defenses = events.filter(item => ['dodge', 'defend', 'pass'].includes(String(item?.eventKind || '').trim()));
      const sameActionFact = (event = {}, actionEvent = {}) => {
        const actionId = String(actionEvent?.actionId || '').trim();
        if (!actionId) return false;
        return String(event?.actionId || '').trim() === actionId ||
          String(event?.sourceActionId || '').trim() === actionId;
      };
      const hasHitForState = (stateEvent = {}) => hits.some(hit =>
        sameActionFact(stateEvent, hit) ||
        (isSameBattleReportName(hit.actorName || '', stateEvent.actorName || '') &&
          isSameBattleReportName(hit.targetName || '', stateEvent.targetName || '') &&
          (!stateEvent.sourceActionName || normalizeBattleActionDisplayName(hit.actionName || '') === normalizeBattleActionDisplayName(stateEvent.sourceActionName || '')))
      );
      const stateEvents = events.filter(item =>
        String(item?.eventKind || '').trim() === 'state_apply' &&
        !hasHitForState(item)
      );
      if (!stateEvents.length) return [];
      const groups = new Map();
      stateEvents.forEach(event => {
        const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
        const actor = String(event?.actorName || '行动者').trim();
        const action = normalizeBattleActionDisplayName(event?.actionName || event?.sourceActionName || '行动');
        const target = String(event?.targetName || '').trim();
        const key = `${round}|${actor}|${action}|${target}`;
        if (!groups.has(key)) groups.set(key, { round, actor, action, target, items: [] });
        groups.get(key).items.push(event);
      });
      const entries = [];
      [...groups.values()].sort((left, right) => Number(left.round || 0) - Number(right.round || 0)).forEach(group => {
        const start = starts.find(item =>
          isSameBattleReportName(item.actorName || '', group.actor) &&
          (!group.action || normalizeBattleActionDisplayName(item.actionName || '') === group.action)
        );
        const target = group.target || String(start?.targetName || 读取战报默认敌对名(context, group.actor)).trim();
        const stateList = group.items.filter(item => 事件账本状态已附着(item));
        const resistedList = group.items.filter(item => 事件账本状态被抵抗(item) || 事件账本状态被免疫(item));
        const defense = defenses.find(item =>
          target &&
          isSameBattleReportName(item.actorName, target) &&
          (!start?.actionId || String(item.sourceActionId || '').trim() === String(start.actionId || '').trim())
        ) || defenses.find(item => target && isSameBattleReportName(item.actorName, target));
        const stateText = 构建公开战报状态附加短句(stateList.map(item => ({
          target: item.targetName,
          state: 读取事件账本状态名(item),
          duration: item.duration,
          effectSummary: item.effectSummary,
          driverAttr: item.driverAttr,
        })), { plainText: true });
        const stateOnlyText = stateText
          .replace(/^这一击同时令/, '这股魂力同时令')
          .replace(/^这一击令/, '这股魂力令')
          .replace(/造成/g, '带来');
        const resistedStateText = resistedList
          .map(item => 构建公开战报状态抵抗短句(item, `${group.round}|stateResisted|${item.actionId || item.sourceActionId || ''}|${item.targetName}|${读取事件账本状态名(item)}`))
          .filter(Boolean)
          .filter((text, index, list) => list.indexOf(text) === index)
          .join('；');
        if (!stateText && !resistedStateText) return;
        const prefix = group.round > 0 ? `第${group.round}回合：` : '';
        const parts = [`${group.actor}施展【${group.action || '行动'}】笼向${target}`];
        if (defense) {
          const phrase = 格式化事件账本防御短语(defense);
          if (phrase) parts.push(phrase);
          if (/闪避/.test(String(defense.actionName || ''))) {
            if (/evaded|miss|dodge_success|闪避成功|未命中/.test(String(defense.result || '').trim())) parts.push('闪避成功，这一击没有命中');
            else if (stateText || resistedStateText) parts.push('未能摆脱这轮压制');
          } else if (/承伤硬抗|肉体兜底|硬抗/.test(String(defense.actionName || ''))) {
            parts.push((stateText || resistedStateText) ? '强运魂力冲撞压制' : '收缩防线，勉强扛住了这轮压制');
          } else {
            parts.push((stateText || resistedStateText) ? '在无形压迫下调整身位' : '稳住身位，暂未被压垮');
          }
        }
        const actionText = parts.join('，');
        const outcomeText = [stateOnlyText, resistedStateText]
          .filter(Boolean)
          .join('；');
        const text = 规范化状态附着断句(`${prefix}${actionText}${outcomeText ? `。${outcomeText}` : ''}。`);
        const sourceItems = [start, defense, ...group.items].filter(Boolean);
        const sourceEventIds = sourceItems.map(item => String(item?.eventId || '').trim()).filter(Boolean);
        const sourceNodeIds = sourceItems.map(item => String(item?.chainNodeId || '').trim()).filter(Boolean);
        const textBlock = 构建公开战报文本块(text, {
          ...(group.items[0] || {}),
          sourceEventIds,
          sourceNodeIds,
        });
        if (textBlock) textBlock.projectionSource = 'state_apply_ast';
        const blocks = [textBlock].filter(Boolean);
        stateList.forEach(item => {
          const stateName = 读取事件账本状态名(item);
          if (!stateName) return;
          blocks.push(构建公开战报Badge块('state', {
            name: stateName,
            targetName: String(item?.targetName || target).trim(),
            targetId: String(item?.targetId || '').trim(),
            sourceEventId: String(item?.eventId || '').trim(),
            sourceNodeId: String(item?.chainNodeId || '').trim(),
          }));
        });
        entries.push({ round: group.round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'state_apply_ast' });
      });
      return entries;
    }

    function 构建失败动作公开战报Block条目(eventLedger = []) {
      const entries = [];
      (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(item => item && typeof item === 'object')
        .filter(item => ['target_fail', 'failed_action', 'blocked_action'].includes(String(item?.eventKind || '').trim()))
        .filter(item => !判定公开战报事件是内部兜底(item))
        .forEach(event => {
          const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
          const prefix = round > 0 ? `第${round}回合：` : '';
          const text = `${prefix}${构建公开战报失败短句(event, round).replace(/[。！？\s]+$/g, '')}。`;
          const textBlock = 构建公开战报文本块(text, event);
          if (textBlock) textBlock.projectionSource = 'failure_ast';
          const blocks = [textBlock].filter(Boolean);
          if (读取战报事件Outcome(event) === 'cap_reached') {
            blocks.push(构建公开战报Badge块('cap_reached', {
              name: '造物上限',
              targetName: String(event?.actorName || '').trim(),
              sourceEventId: String(event?.eventId || '').trim(),
              sourceNodeId: String(event?.chainNodeId || '').trim(),
            }));
          }
          if (!blocks.length) return;
          entries.push({ round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'failure_ast' });
      });
      return entries;
    }

    function 构建资源变化公开战报Block条目(eventLedger = []) {
      const entries = [];
      (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(item => item && typeof item === 'object')
        .filter(item => String(item?.eventKind || '').trim() === 'resource_change')
        .forEach(event => {
          const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
          const prefix = round > 0 ? `第${round}回合：` : '';
          const resource = String(event?.meta?.resource || event?.resource || '资源').trim();
          const delta = Number(event?.meta?.delta ?? event?.delta ?? event?.amount ?? event?.meta?.amount ?? 0);
          if (!resource || !delta) return;
          const targetName = String(event?.targetName || event?.actorName || '目标').trim();
          const mode = String(event?.meta?.transferMode || event?.meta?.reason || '').trim();
          const modeText = mode ? `（${mode}）` : '';
          const verb = delta > 0 ? '恢复' : '消耗';
          const text = `${prefix}${targetName}的${resource}${verb} ${Math.abs(Math.round(delta))} 点${modeText}。`;
          const textBlock = 构建公开战报文本块(text, event);
          if (textBlock) textBlock.projectionSource = 'resource_change_ast';
          const blocks = [textBlock].filter(Boolean);
          blocks.push(构建公开战报Badge块(/生命值|HP|气血|血量/.test(resource) ? (delta > 0 ? 'heal' : 'damage') : 'resource', {
            value: /生命值|HP|气血|血量/.test(resource)
              ? (delta > 0 ? Math.abs(Math.round(delta)) : -Math.abs(Math.round(delta)))
              : Math.round(delta),
            unit: /生命值|HP|气血|血量/.test(resource) ? 'HP' : resource,
            name: resource,
            targetName,
            targetId: String(event?.targetId || '').trim(),
            sourceEventId: String(event?.eventId || '').trim(),
            sourceNodeId: String(event?.chainNodeId || '').trim(),
          }));
          if (textBlock) {
            const eventId = String(event?.eventId || '').trim();
            const nodeId = String(event?.chainNodeId || '').trim();
            if (eventId) textBlock.sourceEventIds = [...new Set([...(Array.isArray(textBlock.sourceEventIds) ? textBlock.sourceEventIds : []), eventId])];
            if (nodeId) textBlock.sourceNodeIds = [...new Set([...(Array.isArray(textBlock.sourceNodeIds) ? textBlock.sourceNodeIds : []), nodeId])];
          }
          if (!blocks.length) return;
          entries.push({ round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'resource_change_ast' });
        });
      return entries;
    }

    function 构建回合流转公开战报Block条目(eventLedger = []) {
      const entries = [];
      (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(item => item && typeof item === 'object')
        .filter(item => ['round_recover', 'round_stop', 'round_continue'].includes(String(item?.eventKind || '').trim()))
        .forEach(event => {
          const kind = String(event?.eventKind || '').trim();
          const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
          const prefix = round > 0 ? `第${round}回合：` : '';
          let text = '';
          if (kind === 'round_recover') {
            const actor = String(event?.actorName || '单位').trim();
            const resource = String(event?.meta?.resource || event?.resource || '').trim();
            const amount = Math.max(0, Math.round(读取事件账本数值(event, 'amount')));
            if (actor && resource && amount > 0) text = `${prefix}${actor}回合末恢复 ${amount} 点${resource}。`;
          } else {
            const reason = String(event?.failReason || event?.failureReason || event?.meta?.reason || event?.reasonText || '').trim();
            if (reason) text = `${prefix}${reason.replace(/[。！？\s]+$/g, '')}。`;
          }
          if (!text) return;
          const textBlock = 构建公开战报文本块(text, event);
          if (textBlock) textBlock.projectionSource = kind === 'round_recover' ? 'round_recover_ast' : 'round_flow_ast';
          const blocks = [textBlock].filter(Boolean);
          if (kind === 'round_recover') {
            const actor = String(event?.actorName || '单位').trim();
            const resource = String(event?.meta?.resource || event?.resource || '').trim();
            const amount = Math.max(0, Math.round(读取事件账本数值(event, 'amount')));
            if (actor && resource && amount > 0) {
              blocks.push(构建公开战报Badge块(/生命值|HP|气血|血量/.test(resource) ? 'heal' : 'resource', {
                value: amount,
                unit: /生命值|HP|气血|血量/.test(resource) ? 'HP' : resource,
                name: resource,
                targetName: actor,
                targetId: String(event?.targetId || '').trim(),
                sourceEventId: String(event?.eventId || '').trim(),
                sourceNodeId: String(event?.chainNodeId || '').trim(),
              }));
              if (textBlock) {
                const eventId = String(event?.eventId || '').trim();
                const nodeId = String(event?.chainNodeId || '').trim();
                if (eventId) textBlock.sourceEventIds = [...new Set([...(Array.isArray(textBlock.sourceEventIds) ? textBlock.sourceEventIds : []), eventId])];
                if (nodeId) textBlock.sourceNodeIds = [...new Set([...(Array.isArray(textBlock.sourceNodeIds) ? textBlock.sourceNodeIds : []), nodeId])];
              }
            }
          }
          if (!blocks.length) return;
          entries.push({ round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: kind === 'round_recover' ? 'round_recover_ast' : 'round_flow_ast' });
        });
      return entries;
    }

    function 构建造物创建公开战报Block条目(eventLedger = []) {
      const groups = new Map();
      (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(item => item && typeof item === 'object')
        .filter(item => String(item?.eventKind || '').trim() === 'create')
        .forEach(event => {
          const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
          const actor = String(event?.actorName || '行动者').trim();
          const action = normalizeBattleActionDisplayName(event?.finalActionName || event?.actionName || event?.sourceActionName || '造物');
          const key = `${round}|${actor}|${action}`;
          if (!groups.has(key)) groups.set(key, { round, actor, action, items: [] });
          groups.get(key).items.push(event);
        });
      const entries = [];
      [...groups.values()].sort((left, right) => Number(left.round || 0) - Number(right.round || 0)).forEach(group => {
        const detailText = group.items
          .map(item => String(item?.meta?.text || '').trim())
          .filter(Boolean)
          .map(text => text
            .replace(/^\[造物承载\]\s*/, '')
            .replace(new RegExp(`^${group.actor}(?:以【[^】]+】)?`), '')
            .replace(/^，?/, '')
            .replace(/^完成造物[:：]\s*/, '')
            .replace(/^生成了造物/, '生成造物')
            .replace(/\s+/g, ' ')
            .trim())
          .filter(Boolean)
          .filter((text, index, list) =>
            list.findIndex(item => item.replace(/[。；\s]+$/g, '') === text.replace(/[。；\s]+$/g, '')) === index
          )
          .join('；');
        const detail = detailText ? `，${detailText.replace(/[。！？\s]+$/g, '')}` : '';
        const prefix = group.round > 0 ? `第${group.round}回合：` : '';
        const flavoredCreate = 选择战报润色模板('create', {
          actor: group.actor,
          skill: group.action || '造物',
          detail,
          __required: ['actor', 'skill'],
        }, `${group.round}|create|${group.actor}|${group.action}|${detailText}`);
        const text = `${prefix}${flavoredCreate || `${group.actor}完成【${group.action || '造物'}】${detail}`}。`;
        const sourceEventIds = group.items.map(item => String(item?.eventId || '').trim()).filter(Boolean);
        const sourceNodeIds = group.items.map(item => String(item?.chainNodeId || '').trim()).filter(Boolean);
          const textBlock = 构建公开战报文本块(text, {
          ...(group.items[0] || {}),
          sourceEventIds,
          sourceNodeIds,
        });
        if (textBlock) textBlock.projectionSource = 'create_ast';
        const blocks = [textBlock].filter(Boolean);
        group.items.forEach(item => {
          const name = String(item?.createdName || item?.meta?.createdName || item?.meta?.itemName || item?.targetName || '').trim();
          const count = Math.max(1, Number(item?.count || item?.amount || item?.meta?.count || item?.meta?.amount || 1));
          blocks.push(构建公开战报Badge块('item_created', {
            value: count,
            name: name || '造物生成',
            targetName: group.actor,
            sourceEventId: String(item?.eventId || '').trim(),
            sourceNodeId: String(item?.chainNodeId || '').trim(),
          }));
        });
        if (!blocks.length) return;
        entries.push({ round: group.round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'create_ast' });
      });
      return entries;
    }

    function 构建无伤害回合公开战报Block条目(eventLedger = []) {
      const groups = new Map();
      (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(item => item && typeof item === 'object')
        .forEach(event => {
          const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
          if (!(round > 0)) return;
          if (!groups.has(round)) groups.set(round, []);
          groups.get(round).push(event);
        });
      const entries = [];
      groups.forEach((roundEvents, round) => {
        const hasHostileResult = roundEvents.some(item => ['hit_result', 'counter', 'state_apply', 'state_tick', 'create', 'summon_create', 'shield_create', 'failed_action', 'target_fail'].includes(String(item?.eventKind || '').trim()));
        if (hasHostileResult) return;
        const actorTexts = new Map();
        const sourceEventIds = [];
        const sourceNodeIds = [];
        const pushActorText = (event = {}, text = '') => {
          const actor = String(event?.actorName || '').trim();
          const line = String(text || '').trim();
          if (!actor || !line) return;
          if (!actorTexts.has(actor)) actorTexts.set(actor, []);
          const list = actorTexts.get(actor);
          if (!list.includes(line)) list.push(line);
          const eventId = String(event?.eventId || '').trim();
          const nodeId = String(event?.chainNodeId || '').trim();
          if (eventId) sourceEventIds.push(eventId);
          if (nodeId) sourceNodeIds.push(nodeId);
        };
        roundEvents.forEach(item => {
          const action = normalizeBattleActionDisplayName(item?.actionName || '');
          const kind = String(item?.eventKind || '').trim();
          const outcome = String(item?.result || '').trim();
          if (kind === 'defend') {
            if (/收招转防/.test(action) || outcome === 'stance_hold') pushActorText(item, '收招转防，稳住自身防线');
            else if (/防御|承伤硬抗|借力守势|坚壁反制|危机自保/.test(action)) pushActorText(item, '转入防御，稳住自身防线');
            return;
          }
          if (kind === 'dodge' && outcome === 'evaded') {
            pushActorText(item, '闪身避让，稳住了身位');
            return;
          }
          if (kind === 'pass') {
            if (outcome === 'observe' || /观察/.test(action)) pushActorText(item, '保持距离观察，没有贸然追击');
            else if (outcome === 'stance_hold') pushActorText(item, '收住攻势，稳住身位');
            else pushActorText(item, '暂缓出手，观察战局变化');
            return;
          }
          if (kind === 'action_start' && 判定守势待机动作(action)) {
            if (/观察/.test(action)) pushActorText(item, '保持距离观察，没有贸然追击');
            else if (/防御|守势|收招转防/.test(action)) pushActorText(item, '转入防御，稳住自身防线');
            else pushActorText(item, '暂缓出手，观察战局变化');
            return;
          }
          if (kind === 'complete' && action) pushActorText(item, `完成【${action}】`);
        });
        const roundTexts = Array.from(actorTexts.entries())
          .map(([actor, textList]) => `${actor}${textList.join('，')}`)
          .filter(Boolean);
        if (!roundTexts.length) return;
        const text = `第${round}回合：${roundTexts.join('；')}。`;
        const textBlock = 构建公开战报文本块(text, {
          ...(roundEvents[0] || {}),
          sourceEventIds: [...new Set(sourceEventIds)],
          sourceNodeIds: [...new Set(sourceNodeIds)],
        });
        if (!textBlock) return;
        textBlock.projectionSource = 'quiet_round_ast';
        const blocks = [textBlock];
        entries.push({ round, blocks, text: 序列化公开战报Blocks(blocks) || text, projectionSource: 'quiet_round_ast' });
      });
      let lastQuietText = '';
      let quietRun = 0;
      return entries.filter(entry => {
        if (String(entry?.projectionSource || '').trim() !== 'quiet_round_ast') return true;
        const normalized = String(entry?.text || '').replace(/^第\d+回合：/, '').replace(/\s+/g, ' ').trim();
        if (normalized && normalized === lastQuietText) quietRun += 1;
        else {
          lastQuietText = normalized;
          quietRun = 1;
        }
        return quietRun <= 2;
      });
    }

    function 构建事件账本公开战报Blocks(eventLedger = [], limit = 8, context = {}) {
      const allEvents = (Array.isArray(eventLedger) ? eventLedger : []).filter(event => event && typeof event === 'object');
      const events = allEvents.filter(event => !判定公开战报事件是内部兜底(event));
      const astFirstEntries = [];
      const astFirstContentSet = new Set();
      const ledgerOrderByEventId = new Map();
      allEvents.forEach((event, index) => {
        const eventId = String(event?.eventId || '').trim();
        if (eventId && !ledgerOrderByEventId.has(eventId)) ledgerOrderByEventId.set(eventId, index);
      });
      const registerAstEntry = (entry = {}) => {
        应用公开战报Badge错落(entry);
        const blocks = Array.isArray(entry?.blocks) ? entry.blocks : [];
        const content = String(blocks.find(block => block?.type === 'text')?.content || '').trim();
        if (!content || astFirstContentSet.has(content)) return;
        astFirstContentSet.add(content);
        astFirstEntries.push(entry);
      };
      构建状态Tick公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建召唤生成公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建护盾生成公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建命中结算公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建状态附着公开战报Block条目(events, context).forEach(entry => {
        registerAstEntry(entry);
      });
      构建失败动作公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建资源变化公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建回合流转公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建造物创建公开战报Block条目(events).forEach(entry => {
        registerAstEntry(entry);
      });
      构建无伤害回合公开战报Block条目(allEvents).forEach(entry => {
        registerAstEntry(entry);
      });
      构建玩家起手反馈公开战报Block条目(allEvents, astFirstEntries).forEach(entry => {
        registerAstEntry(entry);
      });
      const entryOrder = (entry = {}) => {
        const sourceIds = [];
        const blocks = Array.isArray(entry?.blocks) ? entry.blocks : [];
        blocks.forEach(block => {
          if (Array.isArray(block?.sourceEventIds)) block.sourceEventIds.forEach(id => sourceIds.push(String(id || '').trim()));
          sourceIds.push(String(block?.sourceEventId || '').trim());
        });
        const order = sourceIds
          .filter(Boolean)
          .map(id => ledgerOrderByEventId.has(id) ? ledgerOrderByEventId.get(id) : Number.MAX_SAFE_INTEGER)
          .reduce((min, value) => Math.min(min, value), Number.MAX_SAFE_INTEGER);
        return Number.isFinite(order) ? order : Number.MAX_SAFE_INTEGER;
      };
      return astFirstEntries
        .sort((left, right) => {
          const roundDiff = Number(left?.round || 0) - Number(right?.round || 0);
          if (roundDiff) return roundDiff;
          return entryOrder(left) - entryOrder(right);
        })
        .slice(0, Math.max(0, Number(limit || 8)));
    }

    function 构建玩家起手反馈公开战报Block条目(eventLedger = [], existingEntries = []) {
      const entries = [];
      const usedEventIds = new Set();
      const usedNodeIds = new Set();
      const visibleTexts = [];
      (Array.isArray(existingEntries) ? existingEntries : []).forEach(entry => {
        const blocks = Array.isArray(entry?.blocks) ? entry.blocks : [];
        const text = String(entry?.text || 序列化公开战报Blocks(blocks) || '').trim();
        if (text) visibleTexts.push(text);
        blocks.forEach(block => {
          const eventId = String(block?.sourceEventId || '').trim();
          const nodeId = String(block?.sourceNodeId || '').trim();
          if (eventId) usedEventIds.add(eventId);
          if (nodeId) usedNodeIds.add(nodeId);
          if (Array.isArray(block?.sourceEventIds)) block.sourceEventIds.forEach(id => {
            const value = String(id || '').trim();
            if (value) usedEventIds.add(value);
          });
          if (Array.isArray(block?.sourceNodeIds)) block.sourceNodeIds.forEach(id => {
            const value = String(id || '').trim();
            if (value) usedNodeIds.add(value);
          });
        });
      });
      (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(event => event && typeof event === 'object')
        .filter(event => String(event?.eventKind || '').trim() === 'action_start')
        .filter(event => String(event?.actorSide || '').trim() === 'player')
        .filter(event => event?.meta?.source !== 'summon' && event?.source !== 'summon' && !/召唤自主/.test(String(event?.actionType || '')))
        .filter(event => !判定公开战报事件是内部兜底(event))
        .forEach(event => {
          const eventId = String(event?.eventId || '').trim();
          const nodeId = String(event?.chainNodeId || '').trim();
          const actor = String(event?.actorName || '玩家').trim();
          const action = normalizeBattleActionDisplayName(event?.finalActionName || event?.actionName || '行动');
          if ((eventId && usedEventIds.has(eventId)) || (nodeId && usedNodeIds.has(nodeId))) return;
          if (visibleTexts.some(text => actor && action && text.includes(actor) && text.includes(`【${action}】`))) return;
          const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
          const prefix = round > 0 ? `第${round}回合：` : '';
          const text = `${prefix}${actor}亮出【${action}】的起手，稳住战斗节奏。`;
          const textBlock = 构建公开战报文本块(text, event);
          if (!textBlock) return;
          textBlock.projectionSource = 'player_action_start_ast';
          if (eventId) textBlock.sourceEventIds = [...new Set([...(Array.isArray(textBlock.sourceEventIds) ? textBlock.sourceEventIds : []), eventId])];
          if (nodeId) textBlock.sourceNodeIds = [...new Set([...(Array.isArray(textBlock.sourceNodeIds) ? textBlock.sourceNodeIds : []), nodeId])];
          entries.push({ round, blocks: [textBlock], text: 序列化公开战报Blocks([textBlock]) || text, projectionSource: 'player_action_start_ast' });
        });
      return entries;
    }

    function 读取战报上下文单位(context = {}) {
      const combatData = context?.combatData && typeof context.combatData === 'object' ? context.combatData : {};
      const participants = combatData?.参战者 && typeof combatData.参战者 === 'object' ? combatData.参战者 : {};
      const playerUnits = [
        context.player,
        context.attacker,
        ...(Array.isArray(participants.team_player) ? participants.team_player : []),
      ].filter(Boolean);
      const enemyUnits = [
        context.enemy,
        context.defender,
        context.target,
        ...(Array.isArray(participants.team_enemy) ? participants.team_enemy : []),
      ].filter(Boolean);
      return { playerUnits, enemyUnits };
    }

    function 读取战报回退名称(units = [], fallback = '') {
      return String((Array.isArray(units) ? units : [])
        .map(unit => unit?.name || unit?.名称 || unit?.charKey || unit?.char_key || unit?.key || '')
        .find(Boolean) || fallback || '').trim();
    }

    function getCombatReportUnitName(unit = null, fallback = '') {
      return String(unit?.name || unit?.名称 || unit?.charKey || unit?.char_key || unit?.key || fallback || '').trim();
    }

    function 读取战报单位阵营(context = {}, actorName = '') {
      const name = String(actorName || '').trim();
      if (!name) return '';
      const { playerUnits, enemyUnits } = 读取战报上下文单位(context);
      if (playerUnits.some(unit => isSameBattleReportName(unit?.name || unit?.名称 || unit?.charKey || unit?.key || '', name))) return 'player';
      if (enemyUnits.some(unit => isSameBattleReportName(unit?.name || unit?.名称 || unit?.charKey || unit?.key || '', name))) return 'enemy';
      return '';
    }

    function 读取战报默认敌对名(context = {}, actorName = '') {
      const { playerUnits, enemyUnits } = 读取战报上下文单位(context);
      const actorSide = 读取战报单位阵营(context, actorName);
      if (actorSide === 'enemy') return 读取战报回退名称(playerUnits, '我方角色');
      return 读取战报回退名称(enemyUnits, enemyUnits.length > 1 ? '敌方目标' : '敌方角色');
    }

    function replaceBattleReportGenericNames(line = '', context = {}) {
      const { playerUnits, enemyUnits } = 读取战报上下文单位(context);
      const playerName = 读取战报回退名称(playerUnits, '我方角色');
      const enemyName = 读取战报回退名称(enemyUnits, enemyUnits.length > 1 ? '敌方目标' : '敌方角色');
      return String(line || '')
        .replace(/玩家方/g, '我方')
        .replace(/(^|[^\p{L}\p{N}_])玩家(?=$|[^\p{L}\p{N}_]|[\p{Script=Han}])/gu, `$1${playerName}`)
        .replace(/(^|[^\p{L}\p{N}_])NPC(?=$|[^\p{L}\p{N}_]|[\p{Script=Han}])/gu, `$1${enemyName}`);
    }

    function 规范化公开战报文本(line = '', context = {}) {
      return String(line || '')
        .replace(/，(这一击(?:同时)?令)/g, '。$1')
        .replace(/([。！？])\1+/g, '$1')
        .trim();
    }

    function 解析战报魂技引用(line = '', refName = '', offset = 0, context = {}) {
      const name = normalizeBattleActionDisplayName(refName || '');
      if (!name) return null;
      const ledger = Array.isArray(context?.eventLedger)
        ? context.eventLedger
        : (Array.isArray(context?.combatData?.__battleEventLedger) ? context.combatData.__battleEventLedger : []);
      const candidates = ledger.filter(event => {
        const kind = String(event?.eventKind || '').trim();
        if (!['action_start', 'hit_result', 'state_apply', 'counter', 'summon_assist', 'shield_create', 'create'].includes(kind)) return false;
        const actionName = normalizeBattleActionDisplayName(event?.finalActionName || event?.actionName || event?.sourceActionName || event?.meta?.actionName || '');
        return actionName && actionName === name;
      });
      if (candidates.length) {
        const textBefore = String(line || '').slice(0, Math.max(0, Number(offset || 0)));
        const byActor = candidates.find(event => {
          const actor = String(event?.actorName || '').trim();
          return actor && textBefore.includes(actor);
        }) || candidates[0];
        return {
          action: byActor,
          displayName: normalizeBattleActionDisplayName(byActor?.finalActionName || byActor?.actionName || byActor?.sourceActionName || name),
          actorName: String(byActor?.actorName || '').trim(),
          skillSlot: String(byActor?.skillSlot || byActor?.meta?.skillSlot || byActor?.meta?.魂技槽位 || byActor?.魂技槽位 || '').trim(),
        };
      }
      const unitContext = 读取战报上下文单位(context);
      const units = Array.isArray(context?.units)
        ? context.units
        : [...unitContext.playerUnits, ...unitContext.enemyUnits];
      const findSkillInValue = (value, depth = 0) => {
        if (!value || typeof value !== 'object' || depth > 5) return null;
        const skillName = normalizeBattleActionDisplayName(value?.name || value?.魂技名 || '');
        if (skillName && skillName === name) return value;
        if (Array.isArray(value)) {
          for (const item of value) {
            const found = findSkillInValue(item, depth + 1);
            if (found) return found;
          }
          return null;
        }
        for (const child of Object.values(value)) {
          const found = findSkillInValue(child, depth + 1);
          if (found) return found;
        }
        return null;
      };
      for (const unit of units) {
        const skill = findSkillInValue(unit);
        if (!skill) continue;
        return {
          action: skill,
          displayName: normalizeBattleActionDisplayName(skill?.name || skill?.魂技名 || name),
          actorName: getCombatReportUnitName(unit, ''),
          skillSlot: String(skill?.__魂技槽位 || skill?.魂技槽位 || '').trim(),
        };
      }
      return null;
    }

    function 渲染公开战报HTML(line = '', context = {}) {
      const text = String(line || '');
      const ledger = Array.isArray(context?.eventLedger)
        ? context.eventLedger
        : (Array.isArray(context?.combatData?.__battleEventLedger) ? context.combatData.__battleEventLedger : []);
      const stateHints = new Map();
      ledger.forEach(item => {
        const name = 读取事件账本状态名(item);
        if (!name || stateHints.has(name)) return;
        const parts = [];
        const duration = Math.max(0, Number(item?.duration || item?.meta?.duration || 0));
        const effectSummary = String(item?.effectSummary || item?.meta?.effectSummary || '').trim();
        const driverAttr = String(item?.driverAttr || item?.meta?.driverAttr || '').trim();
        if (duration > 0) parts.push(`持续${duration}回合`);
        if (effectSummary) parts.push(`效果：${effectSummary}`);
        if (driverAttr) parts.push(`驱动属性：${driverAttr}`);
        stateHints.set(name, parts.join('，'));
      });
      let html = '';
      let cursor = 0;
      let changed = false;
      Array.from(text.matchAll(/【([^】]+)】/g)).forEach(match => {
        const raw = String(match?.[0] || '');
        const refName = String(match?.[1] || '').trim();
        const index = Number(match?.index || 0);
        const resolved = 解析战报魂技引用(text, refName, index, context);
        const displayName = resolved?.displayName || refName;
        html += htmlEscapeText(text.slice(cursor, index));
        if (resolved?.action) {
          changed = true;
          html += `<button class="battle-preview-report-skill" type="button" data-battle-report-skill="1" data-skill-name="${htmlEscapeText(displayName)}" data-actor-name="${htmlEscapeText(resolved.actorName || '')}" data-skill-slot="${htmlEscapeText(resolved.skillSlot || '')}">【${htmlEscapeText(displayName)}】</button>`;
        } else if (stateHints.has(refName)) {
          changed = true;
          html += `<span class="combat-state-tag"${stateHints.get(refName) ? ` title="${htmlEscapeText(stateHints.get(refName))}"` : ''}>【${htmlEscapeText(refName)}】</span>`;
          const restText = text.slice(index + raw.length);
          const detailMatch = restText.match(/^（[^）]+）/);
          if (detailMatch) {
            html += `<span class="combat-subtext">${htmlEscapeText(detailMatch[0])}</span>`;
            cursor = index + raw.length + detailMatch[0].length;
            return;
          }
        } else {
          html += htmlEscapeText(raw);
        }
        cursor = index + raw.length;
      });
      html += htmlEscapeText(text.slice(cursor));
      html = html.replace(/（该状态由[^）]+）/g, match => `<span class="combat-subtext">${match}</span>`);
      return { html, changed };
    }

    function 渲染公开战报BadgeHTML(block = {}) {
      const kind = String(block?.kind || '').trim();
      const targetName = String(block?.targetName || '').trim();
      const sourceEventId = String(block?.sourceEventId || '').trim();
      const sourceNodeId = String(block?.sourceNodeId || '').trim();
      const attrs = [
        'data-report-badge="1"',
        kind ? `data-badge-kind="${htmlEscapeText(kind)}"` : '',
        targetName ? `data-target-name="${htmlEscapeText(targetName)}"` : '',
        sourceEventId ? `data-source-event-id="${htmlEscapeText(sourceEventId)}"` : '',
        sourceNodeId ? `data-source-node-id="${htmlEscapeText(sourceNodeId)}"` : '',
        Number(block?.delayMs || 0) > 0 ? `data-badge-delay-ms="${Math.round(Number(block.delayMs || 0))}"` : '',
        Number(block?.delayMs || 0) > 0 ? `style="--battle-badge-delay:${Math.round(Number(block.delayMs || 0))}ms"` : '',
      ].filter(Boolean).join(' ');
      if (kind === 'damage') return `<span class="battle-preview-report-badge battle-preview-report-badge--damage" ${attrs}>${htmlEscapeText(`${Number(block.value || 0)} ${block.unit || 'HP'}`)}</span>`;
      if (kind === 'heal') return `<span class="battle-preview-report-badge battle-preview-report-badge--heal" ${attrs}>${htmlEscapeText(`+${Math.max(0, Number(block.value || 0))} ${block.unit || 'HP'}`)}</span>`;
      if (kind === 'shield') return `<span class="battle-preview-report-badge battle-preview-report-badge--shield" ${attrs}>${htmlEscapeText(`+${Math.max(0, Number(block.value || 0))} 护盾`)}</span>`;
      if (kind === 'resource') {
        const value = Number(block.value || 0);
        const sign = value > 0 ? '+' : '';
        return `<span class="battle-preview-report-badge battle-preview-report-badge--resource" ${attrs}>${htmlEscapeText(`${sign}${value} ${block.unit || block.name || '资源'}`)}</span>`;
      }
      if (kind === 'item_created' || kind === 'creation') {
        const value = Math.max(0, Math.round(Number(block.value || 0)));
        const name = String(block?.name || '造物生成').trim();
        return `<span class="battle-preview-report-badge battle-preview-report-badge--creation" ${attrs}>${htmlEscapeText(`${value > 0 ? `+${value} ` : ''}${name}`)}</span>`;
      }
      if (kind === 'summon_created') {
        const name = String(block?.name || '召唤入场').trim();
        return `<span class="battle-preview-report-badge battle-preview-report-badge--summon" ${attrs}>${htmlEscapeText(name)}</span>`;
      }
      if (kind === 'cap_reached') return `<span class="battle-preview-report-badge battle-preview-report-badge--cap" ${attrs}>${htmlEscapeText(block?.name || '上限')}</span>`;
      const name = String(block?.name || kind || '').trim();
      return name ? `<span class="battle-preview-report-badge battle-preview-report-badge--state" ${attrs}>${htmlEscapeText(name)}</span>` : '';
    }

    function 构建公开战报Source属性(block = {}) {
      const sourceEventId = String(block?.sourceEventId || '').trim();
      const sourceNodeId = String(block?.sourceNodeId || '').trim();
      const sourceEventIds = Array.isArray(block?.sourceEventIds)
        ? block.sourceEventIds.map(id => String(id || '').trim()).filter(Boolean)
        : [];
      const sourceNodeIds = Array.isArray(block?.sourceNodeIds)
        ? block.sourceNodeIds.map(id => String(id || '').trim()).filter(Boolean)
        : [];
      return [
        sourceEventId ? `data-source-event-id="${htmlEscapeText(sourceEventId)}"` : '',
        sourceNodeId ? `data-source-node-id="${htmlEscapeText(sourceNodeId)}"` : '',
        sourceEventIds.length ? `data-source-event-ids="${htmlEscapeText([...new Set(sourceEventIds)].join(','))}"` : '',
        sourceNodeIds.length ? `data-source-node-ids="${htmlEscapeText([...new Set(sourceNodeIds)].join(','))}"` : '',
      ].filter(Boolean).join(' ');
    }

    function 渲染公开战报BlocksHTML(blocks = [], context = {}) {
      const textParts = [];
      const badgeParts = [];
      (Array.isArray(blocks) ? blocks : []).forEach(block => {
        if (!block || typeof block !== 'object') return;
        if (block.type === 'text') {
          const textHtml = 渲染公开战报HTML(block.content || '', context).html;
          if (textHtml) {
            const sourceAttrs = 构建公开战报Source属性(block);
            textParts.push(`<span class="battle-preview-report-text-block"${sourceAttrs ? ` ${sourceAttrs}` : ''}>${textHtml}</span>`);
          }
        } else if (block.type === 'badge') {
          const badgeHtml = 渲染公开战报BadgeHTML(block);
          if (badgeHtml) badgeParts.push(badgeHtml);
        }
      });
      const textHtml = textParts.join(' ');
      const badgeHtml = badgeParts.join(' ');
      const html = badgeParts.length
        ? `<span class="battle-preview-report-line"><span class="battle-preview-report-text">${textHtml}</span><span class="battle-preview-report-badges">${badgeHtml}</span></span>`
        : textHtml;
      return { html, changed: Boolean(html) };
    }

    function 构建事件账本公开战报Block文本行(eventLedger = [], limit = 8, context = {}) {
      return 构建事件账本公开战报Blocks(eventLedger, limit, context)
        .map(entry => 序列化公开战报Blocks(entry?.blocks))
        .map(line => 规范化公开战报文本(line, context))
        .filter(Boolean);
    }

    function 序列化公开战报条目文本行(publicReportBlocks = [], context = {}) {
      return (Array.isArray(publicReportBlocks) ? publicReportBlocks : [])
        .map(entry => 序列化公开战报Blocks(entry?.blocks))
        .map(line => 规范化公开战报文本(line, context))
        .filter(Boolean);
    }

    function 格式化战斗模式显示文本(modeLabel = '', battleMode = '', mode = '') {
      const raw = String(modeLabel || battleMode || mode || '战斗').trim();
      if (!raw) return '战斗';
      if (/自动续推|multi_round/i.test(raw)) return '多回合';
      if (/single_round/i.test(raw)) return '单回合';
      return raw;
    }

    function buildPublicBattleReportBlock({ battleLog = [], combatData = {}, battleOutcome = {}, modeLabel = '', roundCount = 0, eventLedger = null } = {}) {
      const ledger = eventLedger || combatData?.__battleEventLedger || [];
      const lines = 序列化公开战报条目文本行(构建事件账本公开战报Blocks(ledger, 8, { combatData }), { combatData });
      const header = `本轮战斗已由战斗模块完成结算：${格式化战斗模式显示文本(modeLabel)}，共推进 ${Math.max(0, Number(roundCount || 0))} 回合。`;
      const outcome = `当前结果：${battleOutcome?.label || battleOutcome?.type || combatData?.裁断结果 || '未分胜负'}。`;
      const intent = combatData?.战斗意图 ? `战斗意图：${combatData.战斗意图}。` : '';
      return [
        '<战斗公开战报>',
        header,
        ...lines,
        outcome,
        intent,
        '</战斗公开战报>',
      ].filter(Boolean).join('\n');
    }

    function 构建LLM战斗语义摘要(eventLedger = [], context = {}, options = {}) {
      const ledger = (Array.isArray(eventLedger) ? eventLedger : [])
        .filter(event => event && typeof event === 'object')
        .filter(event => !判定公开战报事件是内部兜底(event));
      const maxRounds = Math.max(1, Number(options.maxRounds || 3));
      const tokenBudget = Math.max(240, Number(options.tokenBudget || 1400));
      const rounds = [...new Set(ledger.map(event => Math.max(0, Number(event.round || event.sourceRound || 0))).filter(round => round > 0))]
        .sort((a, b) => a - b);
      const visibleRounds = new Set(rounds.slice(-maxRounds));
      const lines = [];
      const pushLine = line => {
        const clean = String(line || '')
          .replace(/\s+/g, ' ')
          .trim();
        if (clean) lines.push(clean);
      };
      ledger.filter(event => visibleRounds.has(Math.max(0, Number(event.round || event.sourceRound || 0)))).forEach(event => {
        const round = Math.max(0, Number(event.round || event.sourceRound || 0));
        const actor = String(event.actorName || '行动者').trim();
        const target = String(event.targetName || actor || '目标').trim();
        const skill = normalizeBattleActionDisplayName(event.finalActionName || event.actionName || (event.eventKind === 'state_tick' ? event.sourceActionName : '') || '行动');
        const outcome = 读取战报事件Outcome(event);
        const damage = Math.max(0, Math.round(读取事件账本数值(event, 'damage') || 读取事件账本数值(event, 'amount')));
        const severity = (() => {
          const targetUnit = 查找战报上下文单位(context, target);
          const maxHp = Math.max(1, Number(targetUnit ? getCombatHpMaxValue(targetUnit) : 0));
          const ratio = maxHp > 1 ? damage / maxHp : 0;
          if (outcome === 'dodged') return '规避成功';
          if (outcome === 'graze') return `擦伤(${damage}点)`;
          if (ratio >= 0.45) return `致命重创(${damage}点)`;
          if (ratio >= 0.18) return `重伤(${damage}点)`;
          if (damage > 0) return `轻伤(${damage}点)`;
          return '无伤害';
        })();
        if (event.eventKind === 'hit_result' || event.eventKind === 'counter') {
          const desc = outcome === 'graze'
            ? `${target}避开要害，但仍被余波擦伤。`
            : outcome === 'dodged'
              ? `${target}完成规避，未被命中。`
              : damage > 0
                ? `${target}承受了本次攻势。`
                : `${target}没有受到实质伤害。`;
          pushLine(`[回合${round}][${actor}] 使用【${skill}】 -> [${target}]。[结果: ${outcome}] [烈度: ${severity}] ${desc}`);
        } else if (event.eventKind === 'state_apply') {
          const stateName = 读取事件账本状态名(event) || '状态';
          const desc = outcome === 'state_resisted' || outcome === 'state_immune'
            ? `${target}化解了【${stateName}】。`
            : `${target}陷入【${stateName}】。`;
          pushLine(`[回合${round}][${actor}] 使用【${skill}】 -> [${target}]。[结果: ${outcome}] [烈度: 状态] ${desc}`);
        } else if (event.eventKind === 'create' || event.eventKind === 'summon_create') {
          const name = String(event.createdName || event.summonName || event.meta?.createdName || event.meta?.summonName || target || '造物').trim();
          pushLine(`[回合${round}][${actor}] 使用【${skill}】 -> [${name}]。[结果: ${outcome}] [烈度: 战术资源] ${actor}完成了${event.eventKind === 'summon_create' ? '召唤' : '造物'}。`);
        } else if (event.eventKind === 'resource_change' || event.eventKind === 'round_recover') {
          const resource = String(event.meta?.resource || event.resource || '资源').trim();
          const amount = Math.round(Number(event.meta?.delta ?? event.amount ?? event.meta?.amount ?? 0));
          pushLine(`[回合${round}][${actor}] 使用【${skill}】 -> [${target}]。[结果: resource_recovered] [烈度: 资源${amount >= 0 ? '+' : ''}${amount}] ${target}的${resource}发生变化。`);
        }
      });
      let text = lines.join('\n');
      if (text.length > tokenBudget) {
        const kept = [];
        for (let index = lines.length - 1; index >= 0; index -= 1) {
          const next = [lines[index], ...kept].join('\n');
          if (next.length > tokenBudget - 20) break;
          kept.unshift(lines[index]);
        }
        text = `[前序战斗过于激烈，已折叠]\n${kept.join('\n')}`.trim();
      }
      return text;
    }

    function buildBattleAdjudicationDossier({ combatData = {}, battleOutcome = {}, 压制战规则文本 = '', 虚拟战规则文本 = '', 裁断约束文本 = '' } = {}) {
      const participants = combatData?.参战者 && typeof combatData.参战者 === 'object' ? combatData.参战者 : {};
      const formatNames = list => (Array.isArray(list) ? list : [])
        .map(unit => String(unit?.name || unit?.名称 || '').trim())
        .filter(Boolean)
        .join('、') || '无';
      return [
        `[前端战果类型]\n${battleOutcome?.type || 'unknown'}`,
        `[前端战果说明]\n${battleOutcome?.label || ''}`,
        `[战斗类型]\n${combatData?.战斗类型 || ''}`,
        `[战斗意图]\n${combatData?.战斗意图 || ''}`,
        `[参战者]\n我方：${formatNames(participants.team_player)}\n敌方：${formatNames(participants.team_enemy)}`,
        `[前端建议结果]\n${combatData?.前端建议结果 || ''}`,
        `[建议终点HP区间]\n${combatData?.建议终点HP区间 || ''}`,
        `[前端推荐终点HP]\n${combatData?.前端推荐终点HP ?? ''}`,
        `[预计HP伤害]\n${combatData?.预计HP伤害 ?? ''}`,
        `[裁断约束]\n${裁断约束文本 || '无'}${压制战规则文本 || ''}${虚拟战规则文本 || ''}`,
      ].join('\n');
    }

    function collectBattleDecisionTrace(combatData = {}) {
      const trace = combatData?.__行动闭环诊断?.审计轨迹;
      if (!Array.isArray(trace)) return [];
      return trace.slice(-160).map(item => cloneBattleRuntimeAuditSnapshot(item));
    }

    function collectBattleResolutionTrace(combatData = {}) {
      const trace = combatData?.__battleResolutionTrace;
      if (!Array.isArray(trace)) return [];
      return trace.slice(-240).map(item => cloneBattleRuntimeAuditSnapshot(item));
    }

    function cloneBattleRuntimeAuditSnapshot(value, depth = 0) {
      if (value == null || typeof value !== 'object') return value;
      if (depth >= 6) return '[snapshot-depth-truncated]';
      if (Array.isArray(value)) return value.slice(0, 120).map(item => cloneBattleRuntimeAuditSnapshot(item, depth + 1));
      const blockedKeys = new Set([
        'combatData', '__父级战斗数据', '__battleEventLedger', '__battleResolutionTrace',
        '参战者', '完整战斗数据', '完整角色', '角色对象', 'actor', 'target', 'sourceActor', 'sourceTarget',
        'sourceSkill', 'originalSkill', '_效果数组', '效果数组', '完整效果数组',
      ]);
      const result = {};
      Object.entries(value).slice(0, 120).forEach(([key, item]) => {
        if (blockedKeys.has(key)) return;
        if ((key === 'skill' || key === '技能') && item && typeof item === 'object') return;
        if (typeof item === 'function' || typeof item === 'undefined') return;
        result[key] = cloneBattleRuntimeAuditSnapshot(item, depth + 1);
      });
      return result;
    }

    function buildBattlePreviewResult({
      intentText = '',
      mode = 'single_round',
      modeLabel = '',
      battleLog = [],
      combatData = {},
      battleOutcome = {},
      publicReport = '',
      publicReportBlocks = null,
      dossier = '',
      roundCount = 0,
      pendingSettlement = null,
      intentMode = '',
      eventLedger = null,
    } = {}) {
      const resolvedPublicReportBlocks = Array.isArray(publicReportBlocks)
        ? publicReportBlocks.map(item => cloneBattleRuntimeAuditSnapshot(item))
        : 构建事件账本公开战报Blocks(eventLedger || combatData?.__battleEventLedger || [], 8, { combatData }).map(item => cloneBattleRuntimeAuditSnapshot(item));
      const resolvedEventLedger = Array.isArray(eventLedger)
        ? eventLedger
        : (Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger : []);
      补水战斗运行态(combatData, resolvedEventLedger, { source: 'battle_preview' });
      const publicReportText = 序列化公开战报条目文本行(resolvedPublicReportBlocks, { combatData }).join('\n');
      const llmBattleSummary = 构建LLM战斗语义摘要(resolvedEventLedger, { combatData }, { maxRounds: 3 });
      const result = {
        preview: true,
        intentText: String(intentText || ''),
        mode: pendingSettlement ? 'tower_pending_preview' : 'battle_preview',
        battleMode: mode,
        modeLabel,
        intentMode: normalizeBattleIntentMode(intentMode || combatData?.战斗意图 || '点到为止'),
        logs: Array.isArray(battleLog) ? [...battleLog] : [],
        roundsExecuted: Math.max(0, Number(roundCount || 0)),
        battleOutcome: deepClonePlain(battleOutcome || {}),
        publicReport: publicReportText,
        publicReportBlocks: resolvedPublicReportBlocks,
        llmBattleSummary,
        dossier: String(dossier || ''),
        combatData,
        decisionTrace: collectBattleDecisionTrace(combatData),
        resolutionTrace: collectBattleResolutionTrace(combatData),
        eventLedger: resolvedEventLedger.map(item => cloneBattleRuntimeAuditSnapshot(item)),
        closedLoopLedger: cloneBattleRuntimeAuditSnapshot(combatData?.__行动闭环诊断?.事实账本 || null),
        snapshot: ui_getBattleSnapshot(combatData),
      };
      if (pendingSettlement) result.pendingSettlement = deepClonePlain(pendingSettlement);
      return result;
    }

    function registerBattleSettlementContext(payload = {}) {
      let api = null;
      try { api = root.__LWCS_REGISTER_BATTLE_SETTLEMENT_CONTEXT__; } catch (_) {}
      try { if (typeof api !== 'function') api = root.parent?.__LWCS_REGISTER_BATTLE_SETTLEMENT_CONTEXT__; } catch (_) {}
      try { if (typeof api !== 'function') api = root.top?.__LWCS_REGISTER_BATTLE_SETTLEMENT_CONTEXT__; } catch (_) {}
      if (typeof api !== 'function') return { ok: false, reason: 'battle_settlement_context_api_missing' };
      try {
        return api(payload);
      } catch (error) {
        console.warn('[battle] register battle settlement context failed', error);
        return { ok: false, reason: error && error.message ? error.message : 'battle_settlement_context_register_failed' };
      }
    }

    root.BattleUIBridge = Object.assign(root.BattleUIBridge || {}, {
      hasMvu() {
        return hasMvuRuntime();
      },
      async waitForMvuReady() {
        return waitForMvuReady();
      },
      getAllVariables() {
        return getAllVariablesSafe();
      },
      getStatData() {
        return getMvuValue('', {});
      },
      getMVU(path) {
        return getMvuValue(path);
      },
      setMVU(path, value) {
        console.warn(
          'BattleUIBridge.setMVU 未启用：当前按明月秋青规范仅从 getAllVariables()/stat_data 读取 MVU 变量。',
          path,
          value,
        );
        return false;
      },
      initCombatContext() {
        console.warn('BattleUIBridge.initCombatContext 已停用：战斗上下文应由 MVU 系统维护在 stat_data.* 下。');
        return getMvuValue('world.战斗');
      },
      getBattleContext() {
        return getMvuValue('world.战斗');
      },
      subscribeMvuUpdates(handler) {
        return subscribeMvuUpdates(handler);
      },
      getBattleSubmitMode() {
        return 读取战斗提交模式();
      },
      setBattleSubmitMode(mode) {
        const 下个模式 = 写入战斗提交模式(mode);
        同步战斗提交模式控件();
        try {
          root.dispatchEvent(new CustomEvent('battle-submit-mode-changed', { detail: { mode: 下个模式 } }));
        } catch (error) {}
        return 下个模式;
      },
      persistCombatData(combatData, options = {}) {
        return persistCombatData(combatData, options);
      },
      executePlayerBattleIntent(playerInput, options = {}) {
        const impl = root.BattleUIBridge?.__executePlayerBattleIntentImpl;
        if (typeof impl === 'function') return impl(playerInput, options);
        throw new Error('battle_player_intent_engine_not_ready');
      },
      executeBattleFlow(combatData, options = {}) {
        const impl = root.BattleUIBridge?.__executeBattleFlowImpl;
        if (typeof impl === 'function') return impl(combatData, options);
        throw new Error('battle_flow_engine_not_ready');
      },
      getBattleSnapshot(combatData) {
        const impl = root.BattleUIBridge?.__getBattleSnapshotImpl;
        if (typeof impl === 'function') return impl(combatData);
        return null;
      },
      getAvailableActions(charData, combatData) {
        const impl = root.BattleUIBridge?.__getAvailableActionsImpl;
        if (typeof impl === 'function') return impl(charData, combatData);
        return [];
      },
      buildCombatJsonPatch(combatData) {
        return buildCombatJsonPatch(combatData);
      },
      buildUpdateVariableTextFromCombat(combatData, options = {}) {
        return buildUpdateVariableText(buildCombatJsonPatch(combatData), options);
      },
      getLastMvuUpdateRequest() {
        return root.__lastBattleMvuUpdateRequest || null;
      },
      setCombatContext() {
        console.warn('BattleUIBridge.setCombatContext 已停用：请通过 MVU 系统更新 stat_data.*。');
        return getMvuValue('world.战斗');
      },
      findChatInput() {
        return findChatInput();
      },
      findSendButton() {
        return findSendButton();
      },
      pushUserInput(text, options = {}) {
        const payload = String(text || '');
        const fillResult = fillChatInput(payload, options);
        const sendResult = options.autoSend === false ? { ok: false, button: null } : clickSendButton(options);
        return {
          text: payload,
          filled: !!fillResult.ok,
          sent: !!sendResult.ok,
          inputFound: !!fillResult.input,
          sendButtonFound: !!sendResult.button,
        };
      },
      setPendingSystemPrompt(text) {
        return queueSystemPrompt(text);
      },
      getPendingSystemPrompt() {
        return root.__battlePendingSystemPrompt || '';
      },
      consumePendingSystemPrompt() {
        const prompt = root.__battlePendingSystemPrompt || '';
        root.__battlePendingSystemPrompt = '';
        return prompt;
      },
      clearPendingSystemPrompt() {
        root.__battlePendingSystemPrompt = '';
      },
      setHostAdapter(adapter) {
        root.__battleUIHostAdapter = adapter || null;
        return root.__battleUIHostAdapter;
      },
      getHostAdapter() {
        return root.__battleUIHostAdapter || null;
      },
      installHostHooks() {
        installHostHooks();
      },
      getLastInjectedSystemPrompt() {
        return root.__battleLastInjectedSystemPrompt || null;
      },
      deliverBattleRequest(detail, options = {}) {
        return deliverBattleRequest(detail, options);
      },
      getLastAIRequest() {
        return root.__lastBattleAIRequest || null;
      },
    });

    root.getBattleUiMvuValue = getMvuValue;
    root.getBattleUiAllVariables = getAllVariablesSafe;
    root.waitBattleUiMvuReady = waitForMvuReady;

    function fallbackNumber(value, fallback = 0) {
      const parsed = Number(value);
      return Number.isFinite(parsed) ? parsed : fallback;
    }

    function fallbackEscape(value) {
      return String(value ?? '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
    }

    function fallbackUnit(unit) {
      const safeUnit = unit && typeof unit === 'object' ? unit : {};
      const stat = safeUnit.属性 && typeof safeUnit.属性 === 'object' ? safeUnit.属性 : {};
      const merged = { ...stat, ...safeUnit };
      COMBAT_STAT_KEYS.forEach(key => {
        if (stat[key] !== undefined) merged[key] = stat[key];
      });
      merged.name = safeUnit.name || stat.name || safeUnit.base?.name || '未知';
      merged.等级 = fallbackNumber(merged.等级, 0);
      merged.系别 = safeUnit.系别 || safeUnit.type || merged.系别 || '未知系';
      merged.HP上限 = Math.max(
        1,
        fallbackNumber(merged.HP上限, fallbackNumber(stat.HP上限, fallbackNumber(merged.体力上限, fallbackNumber(stat.体力上限, 1)))),
      );
      merged.HP = Math.max(
        0,
        fallbackNumber(merged.HP, fallbackNumber(stat.HP, fallbackNumber(merged.体力, fallbackNumber(stat.体力, merged.HP上限)))),
      );
      merged.体力上限 = Math.max(1, fallbackNumber(merged.体力上限, fallbackNumber(stat.体力上限, 1)));
      merged.体力 = Math.max(0, fallbackNumber(merged.体力, fallbackNumber(stat.体力, merged.体力上限)));
      merged.魂力上限 = Math.max(1, fallbackNumber(merged.魂力上限, fallbackNumber(stat.魂力上限, 1)));
      merged.魂力 = Math.max(0, fallbackNumber(merged.魂力, fallbackNumber(stat.魂力, merged.魂力上限)));
      merged.精神力上限 = Math.max(1, fallbackNumber(merged.精神力上限, fallbackNumber(stat.精神力上限, 1)));
      merged.精神力 = Math.max(0, fallbackNumber(merged.精神力, fallbackNumber(stat.精神力, merged.精神力上限)));
      merged.力量 = fallbackNumber(merged.力量, fallbackNumber(stat.力量, 0));
      merged.防御 = fallbackNumber(merged.防御, fallbackNumber(stat.防御, 0));
      merged.敏捷 = fallbackNumber(merged.敏捷, fallbackNumber(stat.敏捷, 0));
      merged.状态效果 = merged.状态效果 || stat.状态效果 || {};
      merged.持续效果 = merged.持续效果 || {};
      merged.vit_max = merged.体力上限;
      merged.vit = merged.体力;
      merged.sta_max = merged.体力上限;
      merged.sta = merged.体力;
      merged.hp_max = merged.HP上限;
      merged.hp = merged.HP;
      merged.sp_max = merged.魂力上限;
      merged.sp = merged.魂力;
      merged.men_max = merged.精神力上限;
      merged.men = merged.精神力;
      merged.str = merged.力量;
      merged.def = merged.防御;
      merged.agi = merged.敏捷;
      return merged;
    }

    function 单位是魂师单位(单位 = {}) {
      const 属性 = 单位?.属性 && typeof 单位.属性 === 'object' ? 单位.属性 : {};
      const 有武魂结构 = ['第1武魂', '第2武魂', '武魂', '自创魂技', '武魂融合技'].some(字段 => {
        const 值 = 单位?.[字段] ?? 属性?.[字段];
        return !!(值 && typeof 值 === 'object' && Object.keys(值).length);
      });
      if (有武魂结构) return true;
      const 文本 = [
        单位?.单位性质,
        单位?.类型,
        单位?.身份,
        单位?.主身份,
        单位?.职业,
        单位?.来源,
        属性?.单位性质,
        属性?.类型,
        属性?.身份,
        属性?.主身份,
        属性?.职业,
      ]
        .map(条目 => String(条目 || '').trim())
        .filter(Boolean)
        .join(' ');
      if (/非魂师|不是魂师/.test(文本)) return false;
      return /魂师/.test(文本);
    }

    function 单位是魂兽单位(单位 = {}) {
      const 属性 = 单位?.属性 && typeof 单位.属性 === 'object' ? 单位.属性 : {};
      const 文本 = [单位?.单位性质, 单位?.类型, 单位?.身份, 单位?.标准物种, 单位?.具体物种, 属性?.单位性质, 属性?.类型]
        .map(条目 => String(条目 || '').trim())
        .filter(Boolean)
        .join(' ');
      return /魂兽/.test(文本) || Math.max(0, fallbackNumber(单位?.年限 ?? 属性?.年限 ?? getCombatUnitAgeValue(单位), 0)) > 0;
    }

    function 读取战斗单位徽标文本(单位 = {}) {
      const 属性 = 单位?.属性 && typeof 单位.属性 === 'object' ? 单位.属性 : {};
      if (单位是魂师单位(单位)) return `Lv.${单位?.lv ?? 单位?.等级 ?? 属性?.等级 ?? 0}`;
      if (!单位是魂兽单位(单位)) return '';
      const 年限 = Math.max(0, Math.floor(fallbackNumber(单位?.年限 ?? 属性?.年限 ?? getCombatUnitAgeValue(单位), 0)));
      return 年限 > 0 ? `${年限}年` : '';
    }

    function fallbackText(id, value) {
      const node = byId(id);
      if (node) node.textContent = String(value ?? '');
    }

    function 切换回退文本(节点标识, 文本值, 是否显示 = true) {
      const 节点 = byId(节点标识);
      if (!节点) return;
      节点.hidden = !是否显示;
      节点.textContent = 是否显示 ? String(文本值 ?? '') : '';
    }

    function fallbackBar(id, value, max) {
      const node = byId(id);
      if (!node) return;
      const ratio = Math.max(0, Math.min(100, (fallbackNumber(value, 0) / Math.max(1, fallbackNumber(max, 1))) * 100));
      node.style.width = `${ratio}%`;
    }

    function fallbackRenderStats(id, unit) {
      const node = byId(id);
      if (!node) return;
      node.innerHTML = [
        ['系别', unit.系别 || '未知'],
        ['力', Math.round(unit.力量 || 0)],
        ['防', Math.round(unit.防御 || 0)],
        ['速', Math.round(unit.敏捷 || 0)],
      ]
        .map(([label, value]) => `<div class="stat-item"><div class="stat-label">${fallbackEscape(label)}</div><div class="stat-value">${fallbackEscape(value)}</div></div>`)
        .join('');
    }

    function fallbackRenderBuffs(id, unit) {
      const node = byId(id);
      if (!node) return;
      const conditionChips = Object.entries(unit.状态效果 || {}).slice(0, 8).map(([name, condition]) => {
        const typeText = String(condition?.类型 || condition?.type || '').toLowerCase();
        const kind = /debuff|负面|伤|弱/.test(typeText) ? 'debuff' : 'buff';
        return `<span class="tag-chip ${kind}">${fallbackEscape(name)}</span>`;
      });
      const sustainChips = Object.keys(unit.持续效果 || {}).slice(0, 4).map(name => `<span class="tag-chip sustain">${fallbackEscape(name)}</span>`);
      node.innerHTML = [...conditionChips, ...sustainChips].join('');
    }

    function fallbackRenderUnit(prefix, rawUnit) {
      const unit = fallbackUnit(rawUnit);
      const 徽标文本 = 读取战斗单位徽标文本(unit);
      切换回退文本(`ui-${prefix}-lv`, 徽标文本, !!徽标文本);
      fallbackText(`ui-${prefix}-name`, unit.name || (prefix === 'player' ? '玩家' : '对手'));
      fallbackText(`ui-${prefix}-hp-text`, `${Math.round(unit.HP)} / ${Math.round(unit.HP上限)}`);
      fallbackText(`ui-${prefix}-sta-text`, `${Math.round(unit.体力)} / ${Math.round(unit.体力上限)}`);
      fallbackText(`ui-${prefix}-sp-text`, `${Math.round(unit.魂力)} / ${Math.round(unit.魂力上限)}`);
      fallbackText(`ui-${prefix}-men-text`, `${Math.round(unit.精神力)} / ${Math.round(unit.精神力上限)}`);
      fallbackBar(`ui-${prefix}-hp-bar`, unit.HP, unit.HP上限);
      fallbackBar(`ui-${prefix}-sta-bar`, unit.体力, unit.体力上限);
      fallbackBar(`ui-${prefix}-sp-bar`, unit.魂力, unit.魂力上限);
      fallbackBar(`ui-${prefix}-men-bar`, unit.精神力, unit.精神力上限);
      fallbackRenderStats(`ui-${prefix}-stats`, unit);
      fallbackRenderBuffs(`ui-${prefix}-buffs`, unit);
      return unit;
    }

    function 读取当前世界tick_V1() {
      return Math.max(0, Number(window.BattleUIBridge?.getMVU('world.时间.tick') || 0));
    }

    function 读取魂环路径恢复标记_V1(charData, 魂环路径 = []) {
      if (!charData || typeof charData !== 'object') return { 恢复中: false, 剩余tick: 0, 恢复tick: 0 };
      if (!Array.isArray(魂环路径) || !魂环路径.length) return { 恢复中: false, 剩余tick: 0, 恢复tick: 0 };
      const 魂环数据 = 按路径读取对象_V1(charData, 魂环路径);
      const 恢复tick = Math.max(0, Number(魂环数据?.炸环恢复tick || 0));
      if (!(恢复tick > 0)) return { 恢复中: false, 剩余tick: 0, 恢复tick: 0 };
      const 当前tick = 读取当前世界tick_V1();
      if (恢复tick <= 当前tick) return { 恢复中: false, 剩余tick: 0, 恢复tick };
      return { 恢复中: true, 剩余tick: 恢复tick - 当前tick, 恢复tick };
    }

    function 读取技能魂环恢复标记_V1(skill, charData) {
      if (!skill || !charData || typeof charData !== 'object') return { 恢复中: false, 剩余tick: 0, 恢复tick: 0 };
      const 魂环路径 = Array.isArray(skill.__魂环路径) ? skill.__魂环路径 : [];
      if (!魂环路径.length) return { 恢复中: false, 剩余tick: 0, 恢复tick: 0 };
      return 读取魂环路径恢复标记_V1(charData, 魂环路径);
    }

    function 判定魂环档位类型_V1(年限 = 0) {
      const 数值 = Math.max(0, Number(年限 || 0));
      if (数值 >= 100000) return '红';
      if (数值 >= 10000) return '黑';
      if (数值 >= 1000) return '紫';
      if (数值 >= 100) return '黄';
      return '白';
    }

    function 获取炸环百级校准倍率_V1(属性键 = 'str') {
      const 取基础属性 = typeof root.__LWCS_GET_BASE_STATS__ === 'function' ? root.__LWCS_GET_BASE_STATS__ : null;
      const 九十七级 = 取基础属性 ? 取基础属性(97) : null;
      const 百级 = 取基础属性 ? 取基础属性(100) : null;
      const 基准 = Number(九十七级?.[属性键] || 0);
      const 目标 = Number(百级?.[属性键] || 0);
      if (基准 > 0 && 目标 > 基准) return 目标 / 基准;
      return {
        str: 5.511348148148148,
        def: 5.511348148148148,
        agi: 5.511348148148148,
        vit_max: 5.511348148148148,
        men_max: 4.166666666666667,
        sp_max: 16,
      }[属性键] || 5.511348148148148;
    }

    function 计算炸环属性倍率_V1(属性键 = 'str', 总年限 = 0, 强化倍率 = 1) {
      const 年限比例 = Math.max(0, Number(总年限 || 0)) / 400000;
      const 基础倍率 = Math.max(0.01, Number(强化倍率 || 1));
      const 百级倍率 = 获取炸环百级校准倍率_V1(属性键);
      return Math.max(1.005, Number((1 + 基础倍率 * 年限比例 * (百级倍率 - 1)).toFixed(4)));
    }

    function 估算炸环属性等效等级_V1(属性键 = 'str', 属性值 = 0) {
      const 取基础属性 = typeof root.__LWCS_GET_BASE_STATS__ === 'function' ? root.__LWCS_GET_BASE_STATS__ : null;
      const 数值 = Number(属性值 || 0);
      if (!取基础属性 || !(数值 > 0)) return '';
      let 最接近等级 = 1;
      let 最小差值 = Infinity;
      for (let 等级 = 1; 等级 <= 100; 等级 += 0.5) {
        const 基准 = Number(取基础属性(等级)?.[属性键] || 0);
        const 差值 = Math.abs(基准 - 数值);
        if (差值 < 最小差值) {
          最小差值 = 差值;
          最接近等级 = 等级;
        }
      }
      return Number.isInteger(最接近等级) ? String(最接近等级) : 最接近等级.toFixed(1);
    }

    function 构建炸环自身属性增幅_V1(总年限 = 0, 强化倍率 = 1) {
      return {
        str: 计算炸环属性倍率_V1('str', 总年限, 强化倍率),
        def: 计算炸环属性倍率_V1('def', 总年限, 强化倍率),
        agi: 计算炸环属性倍率_V1('agi', 总年限, 强化倍率),
        sp_max: 计算炸环属性倍率_V1('sp_max', 总年限, 强化倍率),
        men_max: 计算炸环属性倍率_V1('men_max', 总年限, 强化倍率),
        vit_max: 计算炸环属性倍率_V1('vit_max', 总年限, 强化倍率),
      };
    }

    function 构建炸环十万年基准类比文本_V1(强化倍率 = 1) {
      const 基准 = typeof root.__LWCS_GET_BASE_STATS__ === 'function' ? root.__LWCS_GET_BASE_STATS__(97) : null;
      if (!基准) return '';
      const 面板倍率 = 构建炸环自身属性增幅_V1(100000, 强化倍率);
      const 项目 = [
        ['力', 'str'],
        ['防', 'def'],
        ['速', 'agi'],
        ['魂', 'sp_max'],
        ['精', 'men_max'],
        ['体', 'vit_max'],
      ];
      return 项目
        .map(([标签, 属性]) => {
          const 等效等级 = 估算炸环属性等效等级_V1(属性, Number(基准[属性] || 0) * Number(面板倍率[属性] || 1));
          return 等效等级 ? `${标签}≈${等效等级}级` : '';
        })
        .filter(Boolean)
        .join('、');
    }

    function 计算炸环恢复时长tick_V1(已炸魂环列表 = []) {
      const 最高年限 = 已炸魂环列表.reduce((最大值, 项) => Math.max(最大值, Number(项?.年限 || 0)), 0);
      const 年限系数 = Math.max(0.5, Math.min(3, Math.log10(Math.max(100, 最高年限)) - 1));
      return Math.max(1440, Math.round(4320 * 年限系数));
    }

    function 路径相同_V1(路径甲 = [], 路径乙 = []) {
      if (!Array.isArray(路径甲) || !Array.isArray(路径乙)) return false;
      if (路径甲.length !== 路径乙.length) return false;
      for (let 索引 = 0; 索引 < 路径甲.length; 索引 += 1) {
        if (String(路径甲[索引]) !== String(路径乙[索引])) return false;
      }
      return true;
    }

    function 收集角色可炸魂环列表_V1(角色数据 = {}) {
      if (!角色数据 || typeof 角色数据 !== 'object') return [];
      const 结果 = [];
      取角色武魂条目_战斗(角色数据).forEach(([武魂键, 武魂数据]) => {
        取武魂魂灵条目_战斗(武魂数据).forEach(([魂灵键, 魂灵数据]) => {
          取魂灵魂环条目_战斗(魂灵数据).forEach(([魂环键, 魂环数据]) => {
            if (!魂环数据 || typeof 魂环数据 !== 'object') return;
            const 年限 = Math.max(100, Number(魂环数据?.年限 || 100));
            结果.push({
              路径: [武魂键, 魂灵键, 魂环键],
              年限,
              档位类型: 判定魂环档位类型_V1(年限),
              魂环键,
            });
          });
        });
        取武魂直接魂环条目_战斗(武魂数据).forEach(([魂环键, 魂环数据]) => {
          if (!魂环数据 || typeof 魂环数据 !== 'object') return;
          const 年限 = Math.max(100, Number(魂环数据?.年限 || 100));
          结果.push({
            路径: [武魂键, 魂环键],
            年限,
            档位类型: 判定魂环档位类型_V1(年限),
            魂环键,
          });
        });
      });
      return 结果;
    }

    function 写入角色魂环恢复标记_V1(角色数据, 魂环路径 = [], 恢复tick = 0, 恢复时间文本 = '') {
      if (!角色数据 || typeof 角色数据 !== 'object') return false;
      if (!Array.isArray(魂环路径) || !魂环路径.length) return false;
      let 当前 = 角色数据;
      for (let 索引 = 0; 索引 < 魂环路径.length; 索引 += 1) {
        const 片段 = 魂环路径[索引];
        if (!当前 || typeof 当前 !== 'object' || !(片段 in 当前)) return false;
        if (索引 === 魂环路径.length - 1) {
          const 魂环数据 = 当前[片段];
          if (!魂环数据 || typeof 魂环数据 !== 'object') return false;
          魂环数据.炸环恢复tick = Math.max(0, Math.floor(Number(恢复tick || 0)));
          if (恢复时间文本) 魂环数据.炸环恢复时间 = String(恢复时间文本).trim();
          return true;
        }
        当前 = 当前[片段];
      }
      return false;
    }

    function 按路径读取对象_V1(根对象 = {}, 路径 = []) {
      let 当前 = 根对象;
      for (const 片段 of 路径) {
        if (!当前 || typeof 当前 !== 'object') return undefined;
        当前 = 当前[片段];
      }
      return 当前;
    }

    function 构建魂环恢复标记补丁_V1(角色名 = '', 魂环路径 = [], 恢复tick = 0, 恢复时间文本 = '') {
      const 角色文本 = String(角色名 || '').trim();
      if (!角色文本 || !Array.isArray(魂环路径) || !魂环路径.length) return [];
      const 基础路径 =
        `/char/${escapeJsonPointerSegment(角色文本)}/` +
        魂环路径.map(片段 => escapeJsonPointerSegment(片段)).join('/');
      const 补丁 = [
        { op: 'add', path: `${基础路径}/炸环恢复tick`, value: Math.max(0, Math.floor(Number(恢复tick || 0))) },
      ];
      if (恢复时间文本) 补丁.push({ op: 'add', path: `${基础路径}/炸环恢复时间`, value: String(恢复时间文本).trim() });
      return 补丁;
    }

    function 查找战斗物品定义_战斗(物品名 = '') {
      const 名称 = String(物品名 || '').trim();
      if (!名称 || 名称 === '无') return null;
      const 物品表 = window.BattleUIBridge?.getMVU?.('物品') || {};
      for (const 分类表 of Object.values(物品表 || {})) {
        if (分类表 && typeof 分类表 === 'object' && !Array.isArray(分类表) && 分类表[名称]) return 分类表[名称];
      }
      return null;
    }

    function 合并战斗物品定义_战斗(记录 = {}) {
      if (!记录 || typeof 记录 !== 'object' || Array.isArray(记录)) return {};
      const 名称 = String(记录.名称 || 记录.name || '').trim();
      const 定义 = 查找战斗物品定义_战斗(名称) || {};
      return { ...deepClonePlain(定义), ...记录, 名称 };
    }

    function 构建魂导器技能表_战斗(魂导器装备 = {}) {
      const 输出 = {};
      const 装配 = 魂导器装备?.装配 && typeof 魂导器装备.装配 === 'object' && !Array.isArray(魂导器装备.装配) ? 魂导器装备.装配 : {};
      const 魂导器默认魂力消耗表 = Object.freeze({ 1: 160, 2: 260, 3: 400, 4: 900, 5: 1600, 6: 2600, 7: 4500, 8: 6500, 9: 11000, 10: 16000, 11: 24000, 12: 36000 });
      Object.entries(装配).forEach(([槽位, 魂导器]) => {
        if (!魂导器 || typeof 魂导器 !== 'object' || Array.isArray(魂导器)) return;
        const 合并魂导器 = 合并战斗物品定义_战斗(魂导器);
        const 名称 = String(合并魂导器.名称 || 合并魂导器.name || '').trim();
        const 魂导等级 = Math.max(0, Math.min(12, Math.floor(Number(合并魂导器.魂导等级 || 0))));
        if (!名称 || 名称 === '无' || !(魂导等级 > 0)) return;
        const 技能表 = 合并魂导器.装备技能 && typeof 合并魂导器.装备技能 === 'object' && !Array.isArray(合并魂导器.装备技能) ? 合并魂导器.装备技能 : {};
        Object.entries(技能表).forEach(([技能名, 技能数据]) => {
          if (!技能数据 || typeof 技能数据 !== 'object' || Array.isArray(技能数据)) return;
          const 技能 = deepClonePlain(技能数据);
          技能.魂导等级 = 魂导等级;
          技能.__魂导等级 = 魂导等级;
          技能.__魂导器名称 = 名称;
          技能.__魂导器槽位 = 槽位;
          if (!String(技能.消耗 || '').trim() || String(技能.消耗 || '').trim() === '无') 技能.消耗 = `魂力:${魂导器默认魂力消耗表[魂导等级] || 160}`;
          技能.使用条件 = 技能.使用条件 && typeof 技能.使用条件 === 'object' && !Array.isArray(技能.使用条件) ? deepClonePlain(技能.使用条件) : {};
          if (魂导等级 >= 10) 技能.使用条件.最低等级 = Math.max(95, Number(技能.使用条件.最低等级 || 0));
          输出[`${槽位}:${技能名}`] = 技能;
        });
      });
      return 输出;
    }

    function fallbackPushSkill(actions, skill, fallbackName, category, 魂环路径 = null, 魂技槽位 = '') {
      if (!skill || typeof skill !== 'object') return;
      const name = String(skill.魂技名 || skill.name || fallbackName || '').trim();
      if (!name) return;
      const runtimeSkill = normalizeSkillData(skill, name);
      if (Array.isArray(魂环路径) && 魂环路径.length) runtimeSkill.__魂环路径 = [...魂环路径];
      const 技能槽位 = String(魂技槽位 || fallbackName || name).trim();
      if (技能槽位) runtimeSkill.__魂技槽位 = 技能槽位;
      const 来源明细 = String(category || '魂技').trim() || '魂技';
      const 来源类别 = 规范化战斗来源类别(来源明细, '魂技');
      写入战斗来源类别上下文(runtimeSkill, { 来源类别, 来源明细, 魂环路径, 魂技槽位: 技能槽位 });
      const 来源键 = [
        来源类别,
        ...(Array.isArray(魂环路径) ? 魂环路径 : []),
        技能槽位,
        name,
        actions.length,
      ].map(片段 => String(片段 || '').replace(/[^\w\u4e00-\u9fa5-]+/g, '_')).filter(Boolean).join('_');
      actions.push({
        id: `skill_${来源键 || actions.length}`,
        name,
        type: 'skill',
        action_type: '释放魂技',
        category: 来源类别,
        source_detail: 来源明细,
        skill: runtimeSkill,
        raw_skill: runtimeSkill,
        cast_time: fallbackNumber(skill.前摇, 10),
        cost_text: skill?.__fusion_display_cost_text ? String(skill.__fusion_display_cost_text) : String(skill.消耗 || ''),
        enabled: true,
      });
    }

    function fallbackCollectActions(charData) {
      const actions = [];
      const char = charData && typeof charData === 'object' ? charData : {};
      取角色武魂条目_战斗(char).forEach(([spiritName, spirit]) => {
        取武魂魂灵条目_战斗(spirit).forEach(([soulSpiritName, soulSpirit]) => {
          取魂灵魂环条目_战斗(soulSpirit).forEach(([ringIndex, ring]) => {
            取魂环魂技条目_战斗(ring).forEach(([skillName, skill]) => {
              fallbackPushSkill(
                actions,
                skill,
                skillName,
                spirit?.表象名称 || spiritName || `第${ringIndex}魂环`,
                [spiritName, soulSpiritName, ringIndex],
                skillName,
              );
            });
          });
        });
        取武魂直接魂环条目_战斗(spirit).forEach(([ringIndex, ring]) => {
          取魂环魂技条目_战斗(ring).forEach(([skillName, skill]) => {
            fallbackPushSkill(actions, skill, skillName, spirit?.表象名称 || spiritName || `第${ringIndex}魂环`, [spiritName, ringIndex], skillName);
          });
        });
      });
      Object.entries(char.自创魂技 || {}).forEach(([name, skill]) => fallbackPushSkill(actions, skill, name, '自创魂技'));
      Object.entries(char.装备 || {}).forEach(([装备键, equip]) => {
        if (装备键 === '魂导器') return;
        if (!equip || typeof equip !== 'object' || Array.isArray(equip)) return;
        const 合并装备 = 合并战斗物品定义_战斗(equip);
        if (Number(合并装备.魂导等级 || 0) > 0) return;
        Object.entries(合并装备.装备技能 || {}).forEach(([name, skill]) => fallbackPushSkill(actions, skill, name, '装备技能'));
      });
      Object.values(构建魂导器技能表_战斗(char.装备?.魂导器)).forEach(魂导技能 =>
        fallbackPushSkill(actions, 魂导技能, 魂导技能?.魂技名 || 魂导技能?.name || '魂导器技能', '装备技能'),
      );
      Object.entries(char.武魂融合技 || {}).forEach(([name, fusion]) =>
        fallbackPushSkill(actions, buildFusionCombatSkill(fusion, name, char), `武魂融合技·${name}`, '武魂融合技'),
      );
      actions.push(
        { id: 'basic_attack', name: '普通攻击', type: 'tactical', action_type: '常规攻击', category: '战术', cast_time: 10, cost_text: '无', enabled: true, skill: { name: '普通攻击' } },
        { id: 'guard', name: '防御', type: 'tactical', action_type: '防御', category: '战术', cast_time: 10, cost_text: '无', enabled: true, skill: { name: '防御' } },
        { id: 'evade', name: '闪避', type: 'tactical', action_type: '闪避', category: '战术', cast_time: 12, cost_text: '体力:5%', enabled: true, skill: { name: '闪避', 消耗: '体力:5%' } },
        { id: 'flee', name: '撤离', type: 'tactical', action_type: '撤离', category: '战术', cast_time: 20, cost_text: '无', enabled: true, skill: { name: '撤离' } },
      );
      return actions;
    }

    function resolveIntentTargetNameFromAction(action, combatData) {
      const safeAction = action || {};
      const skill = safeAction.raw_skill || safeAction.skill || {};
      if (safeAction.target_name) return String(safeAction.target_name || '').trim() || null;
      if (String(skill?.承载方式 || '').trim() === '造物承载') return null;
      const playerName = String(combatData?.参战者?.team_player?.[0]?.name || '').trim() || null;
      const enemyName = String(combatData?.参战者?.team_enemy?.[0]?.name || '').trim() || null;
      const targetKind = inferSkillPrimaryTargetKind(skill);
      if (['自身', '友方单体', '友方群体'].includes(targetKind)) return playerName;
      if (targetKind === '全场') return playerName || enemyName;
      return enemyName || playerName;
    }

    function fallbackBuildIntent(action, combatData) {
      const safeAction = action || {};
      const resolvedTargetName = resolveIntentTargetNameFromAction(safeAction, combatData);
      const skill = safeAction.raw_skill || safeAction.skill || { name: safeAction.name || '普通攻击' };
      const 来源上下文 = 读取战斗来源类别上下文(skill, safeAction.category || '魂技');
      const 魂环路径 = Array.isArray(skill?.__魂环路径) ? skill.__魂环路径.map(片段 => String(片段)) : [];
      const 魂技槽位 = String(skill?.__魂技槽位 || '').trim();
      const queue = [{
        type: safeAction.action_type || safeAction.type || '常规攻击',
        action_type: safeAction.action_type || safeAction.type || '常规攻击',
        category: safeAction.category || 来源上下文.来源类别,
        source_detail: safeAction.source_detail || 来源上下文.来源明细,
        skill,
        前摇: fallbackNumber(safeAction.cast_time, 10),
        target_name: resolvedTargetName,
        前摇已结算: safeAction.前摇已结算 === true,
      }];
      if (魂环路径.length) queue[0].__魂环路径 = 魂环路径;
      if (魂技槽位) queue[0].__魂技槽位 = 魂技槽位;
      const ownerName = String(root.BattleUI?.state?.player?.name || combatData?.参战者?.team_player?.[0]?.name || '').trim();
      if (ownerName) queue[0].actor_name = ownerName;
      if (safeAction.造物处理) queue[0].造物处理 = String(safeAction.造物处理 || '').trim();
      if (safeAction.物品接收者) queue[0].物品接收者 = String(safeAction.物品接收者 || '').trim();
      if (safeAction.立即使用 === true) queue[0].立即使用 = true;
      if (safeAction.即食 === true) queue[0].即食 = true;
      if (safeAction.食用目标) queue[0].食用目标 = String(safeAction.食用目标 || '').trim();
      return `${safeAction.name || '普通攻击'}\n[动作队列]${JSON.stringify(queue)}[/动作队列]\n[目标]${resolvedTargetName || '对手'}[/目标]`;
    }

    function fallbackRenderActions(actions, selectedId) {
      const node = byId('ui-action-grid');
      if (!node) return;
      node.innerHTML = actions
        .map(action => {
          const selected = action.id === selectedId ? ' is-selected' : '';
          const disabled = action.enabled === false ? ' disabled' : '';
          const meta = [action.cost_text, action.cast_time ? `${action.cast_time}` : ''].filter(Boolean).join(' / ');
          return `<button class="action-btn${selected}" type="button" data-action-id="${fallbackEscape(action.id)}"${disabled}><span class="action-name">${fallbackEscape(action.name)}</span><span class="action-meta"><span>${fallbackEscape(action.category || '战术')}</span><span class="action-cost">${fallbackEscape(meta)}</span></span></button>`;
        })
        .join('');
      node.querySelectorAll('[data-action-id]').forEach(button => {
        button.addEventListener('click', () => {
          const state = root.BattleUI?.state || {};
          const action = (state.availableActions || []).find(item => item.id === button.dataset.actionId);
          if (!action || action.enabled === false) return;
          state.selectedAction = action;
          state.selectedSkillActions = [action];
          const output = byId('ui-intent-output');
          if (output) output.value = fallbackBuildIntent(action, state.combatData);
          fallbackRenderActions(state.availableActions || [], action.id);
        });
      });
    }

    component.syncFromBattleEngine = function syncCurrentBattlePanel() {
      const combatData = deepClonePlain(getMvuValue('world.战斗') || _options.combatData || {});
      const participants = combatData && combatData.参战者 && typeof combatData.参战者 === 'object' ? combatData.参战者 : null;
      if (!participants) return;
      const 玩家队伍 = Array.isArray(participants.team_player) ? participants.team_player : [];
      const 敌方队伍 = Array.isArray(participants.team_enemy) ? participants.team_enemy : [];
      const player = fallbackRenderUnit('player', 玩家队伍[0]);
      const enemy = fallbackRenderUnit('enemy', 敌方队伍[0]);
      const chipNode = byId('ui-combat-chips');
      if (chipNode) {
        chipNode.innerHTML = [`回合 ${Number(combatData.回合 || 0)}`, combatData.战斗类型 || '战斗', combatData.阶段 || 战斗阶段枚举_V1.宣告, `${player.name} → ${enemy.name}`]
          .map(item => `<span class="intent-pill">${fallbackEscape(item)}</span>`)
          .join('');
      }
      const charData = getMvuValue(`char.${player.name}`) || 玩家队伍[0];
      const availableActions = fallbackCollectActions(charData);
      const previousState = root.BattleUI?.state || {};
      const currentIntentMode = previousState.currentIntentMode || combatData.战斗意图 || '点到为止';
      const autoContinueConfig = 规范化自动续推设置(previousState.autoContinueConfig || {}, 'multi_round');
      const matchedPreviousAction = previousState.selectedAction
        ? availableActions.find(action => action.id === previousState.selectedAction.id)
        : null;
      const selectedAction = matchedPreviousAction || availableActions.find(action => action.enabled !== false) || availableActions[0] || null;
      root.BattleUI = Object.assign(root.BattleUI || {}, {
        state: {
          ...previousState,
          combatData,
          player,
          enemy,
          availableActions,
          selectedAction,
          selectedSkillActions: selectedAction ? [selectedAction] : [],
          selectedPreActions: [],
          currentMode: previousState.currentMode || 'single_round',
          currentIntentMode,
          autoContinueConfig,
        },
        buildIntentText(actions = []) {
          return fallbackBuildIntent(actions[0] || root.BattleUI?.state?.selectedAction, root.BattleUI?.state?.combatData);
        },
        submitBattleIntent() {
          const state = root.BattleUI?.state || {};
          const action = state.selectedAction?.enabled !== false
            ? state.selectedAction
            : (state.availableActions || []).find(item => item.enabled !== false) || state.availableActions?.[0] || null;
          const intentText = fallbackBuildIntent(action, state.combatData);
          const output = byId('ui-intent-output');
          if (output) output.value = intentText;
          const battleMode = state.currentMode === 'multi_round' ? 'multi_round' : 'single_round';
          state.combatData.战斗意图 = state.currentIntentMode || '点到为止';
          try {
            const result = root.BattleUIBridge?.executePlayerBattleIntent?.(intentText, {
              mode: battleMode,
              intentMode: state.currentIntentMode || '点到为止',
              autoContinueConfig: 规范化自动续推设置(state.autoContinueConfig || {}, battleMode),
            });
            if (typeof component.syncFromBattleEngine === 'function') component.syncFromBattleEngine();
            root.dispatchEvent(new CustomEvent('battle-ui-submit-finished', { detail: result || { intentText } }));
            return result || { intentText };
          } catch (error) {
            const result = { intentText, mode: 'engine_error', battleMode, error };
            root.dispatchEvent(new CustomEvent('battle-ui-submit-finished', { detail: result }));
            return result;
          }
        },
      });
      fallbackRenderActions(availableActions, selectedAction?.id || '');
      渲染自动续推设置控件();
      const output = byId('ui-intent-output');
      if (output && selectedAction) output.value = fallbackBuildIntent(selectedAction, combatData);
      const intentModeInput = byId('ui-intent-mode');
      if (intentModeInput && intentModeInput.value !== currentIntentMode) intentModeInput.value = currentIntentMode;
      const arbitrateBtn = byId('ui-arbitrate');
      if (arbitrateBtn && !arbitrateBtn.__fallbackBattleBound) {
        arbitrateBtn.addEventListener('click', () => root.BattleUI?.submitBattleIntent?.());
        arbitrateBtn.__fallbackBattleBound = true;
        arbitrateBtn.__battleSubmitBound = true;
      }
      document.querySelectorAll('#ui-mode-group [data-mode]').forEach(btn => {
        if (btn.__battleModeBound) return;
        btn.addEventListener('click', () => {
          const normalized = btn.dataset.mode === 'multi_round' ? 'multi_round' : 'single_round';
          if (root.BattleUI && root.BattleUI.state) root.BattleUI.state.currentMode = normalized;
          document.querySelectorAll('#ui-mode-group [data-mode]').forEach(item => {
            item.classList.toggle('active', item.dataset.mode === normalized);
          });
        });
        btn.__battleModeBound = true;
      });
      document.querySelectorAll('#ui-battle-submit-mode-group [data-submit-mode]').forEach(btn => {
        if (btn.__battleSubmitModeBound) return;
        btn.addEventListener('click', () => {
          root.BattleUIBridge?.setBattleSubmitMode?.(btn.dataset.submitMode || 'manual');
        });
        btn.__battleSubmitModeBound = true;
      });
      同步战斗提交模式控件();
      if (intentModeInput && !intentModeInput.__battleIntentBound) {
        intentModeInput.addEventListener('change', () => {
          if (root.BattleUI && root.BattleUI.state) root.BattleUI.state.currentIntentMode = intentModeInput.value || '点到为止';
        });
        intentModeInput.__battleIntentBound = true;
      }
      const closeBtn = byId('ui-battle-close');
      if (closeBtn && !closeBtn.__battleCloseBound) {
        closeBtn.addEventListener('click', () => {
          root.dispatchEvent(new CustomEvent('battle-ui-close-request', { detail: { source: 'battle_ui' } }));
        });
        closeBtn.__battleCloseBound = true;
      }
    };

    const 延后同步战斗面板 = () => {
      if (typeof component.syncFromBattleEngine === 'function') component.syncFromBattleEngine();
    };
    if (typeof root.queueMicrotask === 'function') root.queueMicrotask(延后同步战斗面板);
    else root.setTimeout(延后同步战斗面板, 0);

    installHostHooks();
    /* __BATTLE_ENGINE_INLINE__ */
    function createEmptySkillSemantics() {
      return {
        战斗定位: '无',
        作用目标: '敌方单体',
        优先条件: [],
        风险等级: '中',
        保留倾向: 0,
        目标偏好: [],
        友方偏好: [],
        是否持续: false,
        是否可打断: false,
        是否可被霸体免疫: true,
      };
    }

    function createEmptyBattleSummary() {
      return {
        目标规模: '单体',
        生效方式: '直接',
        爆发级别: '中',
        持续性: '无',
        风险等级: '中',
        属性建模: '无',
        极性层级: '无',
        控制强度: '无',
        回复性质: '无',
        防御性质: '无',
        协同性: '低',
        保留倾向: 0,
      };
    }

    function mapSemanticTargetToCombatTarget(target) {
      const mapping = {
        敌方单体: '敌方/单体',
        敌方群体: '敌方/群体',
        自身: '自身',
        友方单体: '己方/单体',
        友方群体: '己方/群体',
        全场: '全场',
      };
      return mapping[target] || target || '敌方/单体';
    }

    const BATTLE_SKILL_TARGET_KINDS = new Set(['自身', '友方单体', '友方群体', '敌方单体', '敌方群体', '全场', '召唤物', '分身']);
    const BATTLE_SKILL_TARGET_SCALES = new Set(['自身', '单体', '群体', '全场', '召唤物', '分身']);
    const BATTLE_SKILL_TARGET_MODIFIERS = new Set([
      '受隐身筛选',
      '可被破隐',
      '可被嘲讽',
      '可被护卫重定向',
      '可被锁定强化',
    ]);

    function normalizeBattleEffectTargetKind(value = '', fallback = '敌方单体') {
      const text = String(value || '').trim();
      if (BATTLE_SKILL_TARGET_KINDS.has(text)) return text;
      const derived = (() => {
        if (/分身/.test(text)) return '分身';
        if (/造物/.test(text)) return '召唤物';
        if (/召唤物|召唤/.test(text)) return '召唤物';
        if (/全场敌方|敌方全场|全体敌|所有敌|全部敌/.test(text)) return '敌方群体';
        if (/友方群体|己方\/群体|全员/.test(text)) return '友方群体';
        if (/友方单体|己方\/单体/.test(text)) return '友方单体';
        if (/敌方群体/.test(text)) return '敌方群体';
        if (/敌方单体/.test(text)) return '敌方单体';
        if (/全场/.test(text)) return '全场';
        if (/自身/.test(text)) return '自身';
        return '';
      })();
      return BATTLE_SKILL_TARGET_KINDS.has(derived) ? derived : fallback;
    }

    function mapBattleTargetKindToCombatTarget(targetKind = '敌方单体') {
      return {
        自身: '自身',
        友方单体: '己方/单体',
        友方群体: '己方/群体',
        敌方单体: '敌方/单体',
        敌方群体: '敌方/群体',
        全场: '全场',
        召唤物: '召唤物',
        分身: '分身',
      }[normalizeBattleEffectTargetKind(targetKind)] || '敌方/单体';
    }

    function normalizeBattleSkillTargetModifiers(value = []) {
      const source = Array.isArray(value) ? value : [value];
      return Array.from(
        new Set(
          source
            .map(item => String(item || '').trim())
            .filter(item => BATTLE_SKILL_TARGET_MODIFIERS.has(item)),
        ),
      );
    }

    function inferBattleTargetKindFromTargetText(text = '') {
      return normalizeBattleEffectTargetKind(text, '敌方单体');
    }

    function deriveBattleTargetResolutionStrategy(targetKind = '敌方单体') {
      return ['敌方群体', '友方群体', '全场'].includes(normalizeBattleEffectTargetKind(targetKind))
        ? '全目标独立'
        : '单目标独立';
    }

    function deriveBattleSkillTargetModifiers(skill = {}, targetKind = '敌方单体') {
      const normalizedTargetKind = normalizeBattleEffectTargetKind(targetKind, '敌方单体');
      const effects = getSkillEffects(skill);
      const mechanisms = effects
        .map(effect => String(effect?.原型 || '').trim())
        .filter(Boolean);
      const modifiers = [];
      if (normalizedTargetKind === '敌方单体') {
        modifiers.push('受隐身筛选', '可被嘲讽', '可被护卫重定向', '可被锁定强化');
      }
      if (mechanisms.includes('破隐')) modifiers.push('可被破隐');
      return normalizeBattleSkillTargetModifiers(modifiers);
    }

    function normalizeBattleSkillTargetScale(value = '', fallback = '单体') {
      const text = String(value || '').trim();
      if (BATTLE_SKILL_TARGET_SCALES.has(text)) return text;
      if (text === '自身') return '自身';
      if (text === '全场') return '全场';
      if (/分身/.test(text)) return '分身';
      if (/召唤物|召唤/.test(text)) return '召唤物';
      if (/群体/.test(text)) return '群体';
      if (/单体/.test(text)) return '单体';
      return BATTLE_SKILL_TARGET_SCALES.has(fallback) ? fallback : '单体';
    }

    function normalizeBattleEffectTargetScale(value = '', fallback = '单体') {
      const text = String(value || '').trim();
      return normalizeBattleSkillTargetScale(text, fallback);
    }

    function 读取战斗数值正负(value) {
      const text = String(value ?? '').trim();
      if (!text) return 0;
      if (/%$/.test(text)) {
        const percent = Number(text.replace('%', ''));
        return Number.isFinite(percent) ? percent / 100 : 0;
      }
      const num = Number(text);
      return Number.isFinite(num) ? num : 0;
    }

    function 读取战斗数值是否百分比(value) {
      return /%$/.test(String(value ?? '').trim());
    }

    function 当前技能元素匹配结算修正(skill = {}, effect = {}) {
      const 限定元素列表 = normalizeBattleSkillAttributeTokens(effect?.限定元素);
      if (!限定元素列表.length) return true;
      const 当前技能元素集合 = new Set(normalizeBattleSkillAttributeTokens([
        ...(Array.isArray(skill?.附带属性) ? skill.附带属性 : [skill?.附带属性]),
        getBattleSkillAttributeSummary(skill || {}).显示元素,
        ...((getBattleSkillElementStructure(skill || {}).核心元素) || []),
        ...((getBattleSkillElementStructure(skill || {}).驱动元素) || []),
        ...((getBattleSkillElementStructure(skill || {}).触发元素) || []),
      ]));
      return 限定元素列表.some(元素 => 当前技能元素集合.has(元素));
    }

    function 读取技能当前元素集合(skill = {}) {
      return new Set(normalizeBattleSkillAttributeTokens([
        ...(Array.isArray(skill?.附带属性) ? skill.附带属性 : [skill?.附带属性]),
        getBattleSkillAttributeSummary(skill || {}).显示元素,
        ...((getBattleSkillElementStructure(skill || {}).核心元素) || []),
        ...((getBattleSkillElementStructure(skill || {}).驱动元素) || []),
        ...((getBattleSkillElementStructure(skill || {}).触发元素) || []),
      ]));
    }

    function 构建状态元素承伤修正(effect = {}) {
      const 状态 = String(effect?.状态 || '').trim();
      const 已有修正 = effect?.元素承伤修正 && typeof effect.元素承伤修正 === 'object' ? deepClonePlain(effect.元素承伤修正) : {};
      const 副数值 = 读取战斗数值正负(effect?.副数值);
      const 倍率 = Number((1 + Math.abs(副数值 || 0.2)).toFixed(4));
      if (状态 === '灼烧') 已有修正.火 = Math.max(Number(已有修正.火 || 1), 倍率);
      if (状态 === '冻伤') 已有修正.冰 = Math.max(Number(已有修正.冰 || 1), 倍率);
      return Object.keys(已有修正).length ? 已有修正 : null;
    }

    function 读取目标元素承伤倍率(target = {}, skill = {}) {
      const 当前元素集合 = 读取技能当前元素集合(skill);
      if (!当前元素集合.size) return 1;
      return Object.values(target?.状态效果 || {}).reduce((倍率, 状态) => {
        const 修正 = 状态?.元素承伤修正;
        if (!修正 || typeof 修正 !== 'object') return 倍率;
        const 本状态倍率 = Object.entries(修正).reduce((最大值, [元素, 值]) => {
          const 元素列表 = normalizeBattleSkillAttributeTokens(元素);
          if (!元素列表.some(item => 当前元素集合.has(item))) return 最大值;
          const 数值 = Number(值 || 1);
          return Number.isFinite(数值) ? Math.max(最大值, 数值) : 最大值;
        }, 1);
        return 倍率 * 本状态倍率;
      }, 1);
    }

    function 读取技能自身结算修正(skill = {}, 结算名 = '', context = {}) {
      const 技能 = skill && typeof skill === 'object' ? skill : {};
      return getSkillEffects(技能, { ...context, skill: 技能, 排除跟随主原型: true }).reduce((结果, effect) => {
        if (String(effect?.原型 || '').trim() !== '结算修正') return 结果;
        if (String(effect?.结算 || '').trim() !== 结算名) return 结果;
        if (!当前技能元素匹配结算修正(技能, effect)) return 结果;
        const 数值 = 读取战斗数值正负(effect?.数值);
        if (!Number.isFinite(数值) || 数值 === 0) return 结果;
        if (读取战斗数值是否百分比(effect?.数值)) 结果.倍率 *= Math.max(0, 1 + 数值);
        else 结果.绝对 += 数值;
        return 结果;
      }, { 倍率: 1, 绝对: 0 });
    }

    function 推断战斗机制默认方向(effect = {}) {
      const 原型 = String(effect?.原型 || '').trim();
      const 原型默认方向 = String(BATTLE_PROTOTYPE_REGISTRY[原型]?.默认方向 || '').trim();
      if (String(effect?.目标 || '').trim() === '召唤物') return '载体';
      if (String(effect?.目标 || '').trim() === '分身') return '载体';
      if (原型 === '召唤生成' || 原型默认方向 === '召唤物') return '无目标';
      if (原型默认方向 === '敌对' || 原型默认方向 === '敌方') return '敌方';
      if (原型默认方向 === '可赋予' || 原型默认方向 === '友方') return '友方';
      if (原型默认方向 === '自身') return '友方';
      if (原型 === '资源转移') {
        const 转移方式 = String(effect?.资源转移方式 || '').trim();
        if (转移方式 === '共享') return '友方';
        if (转移方式 === '均分') return '敌方';
        return '敌方';
      }
      if (['资源变化', '护盾变化', '属性修正'].includes(原型)) {
        return 读取战斗数值正负(effect?.数值) < 0 ? '敌方' : '友方';
      }
      if (原型 === '结算修正') {
        const 结算 = String(effect?.结算 || '').trim();
        if (
          (结算 === '造成伤害' && 读取战斗数值正负(effect?.数值) < 0) ||
          (结算 === '受到伤害' && 读取战斗数值正负(effect?.数值) > 0) ||
          ['反伤', '伤害转移', '治疗转伤害', '防御穿透', '反击', '持续伤害引爆'].includes(结算)
        )
          return '敌方';
        if (
          (结算 === '造成伤害' && 读取战斗数值正负(effect?.数值) > 0) ||
          (结算 === '受到伤害' && 读取战斗数值正负(effect?.数值) < 0) ||
          ['治疗', '技能效果', '伤害吸收', '伤害转治疗', '伤害分摊', '消耗分摊'].includes(结算)
        )
          return '友方';
        if (['消耗', '前摇'].includes(结算)) return 读取战斗数值正负(effect?.数值) > 0 ? '敌方' : '友方';
      }
      return '敌方';
    }

    function 推断战斗效果目标类型(effect = {}, fallbackTargetKind = '敌方单体') {
      const rawTarget = String(effect?.目标 ?? '').trim();
      const scale = normalizeBattleEffectTargetScale(rawTarget, deriveBattleSkillTargetScaleFromKind(fallbackTargetKind));
      if (scale === '自身') return '自身';
      if (scale === '全场') return '全场';
      if (scale === '召唤物') return '召唤物';
      if (scale === '分身') return '分身';
      if (/敌方|敌人|对手/.test(rawTarget)) return scale === '群体' ? '敌方群体' : '敌方单体';
      if (/友方|己方|队友/.test(rawTarget)) return scale === '群体' ? '友方群体' : '友方单体';
      const direction = 推断战斗机制默认方向(effect);
      if (direction === '友方') return scale === '群体' ? '友方群体' : '友方单体';
      return scale === '群体' ? '敌方群体' : '敌方单体';
    }

    function inferSkillPrimaryTargetKind(skill = {}) {
      const explicitTarget = String(skill?.目标 || skill?.target || '').trim();
      if (explicitTarget) return normalizeBattleExecutionEffectTargetKind(explicitTarget, '敌方单体');
      const effects = getSkillEffects(skill);
      const primaryEffect =
        effects.find(effect => effect && typeof effect === 'object' && String(effect.原型 || '').trim() === '伤害结算') ||
        effects.find(effect => effect && typeof effect === 'object' && String(effect.原型 || '').trim());
      return primaryEffect ? 推断战斗效果目标类型(primaryEffect) : '敌方单体';
    }

    function deriveBattleSkillTargetScaleFromKind(targetKind = '敌方单体') {
      const normalized = normalizeBattleEffectTargetKind(targetKind, '敌方单体');
      if (normalized === '自身') return '自身';
      if (normalized === '全场') return '全场';
      if (normalized === '召唤物') return '召唤物';
      if (normalized === '分身') return '分身';
      if (normalized.includes('群体')) return '群体';
      return '单体';
    }

    function normalizeBattleExecutionEffectTargetKind(value = '', fallback = '敌方单体') {
      const text = String(value || '').trim();
      if (!text) return normalizeBattleEffectTargetKind(fallback, '敌方单体');
      const aliasMap = {
        '己方/单体': '友方单体',
        '己方/群体': '友方群体',
        '敌方/单体': '敌方单体',
        '敌方/群体': '敌方群体',
        造物: '召唤物',
        召唤物: '召唤物',
        分身: '分身',
      };
      return normalizeBattleEffectTargetKind(aliasMap[text] || text, normalizeBattleEffectTargetKind(fallback, '敌方单体'));
    }

    const 战斗机制中文键英文名 = Object.freeze({
      增加命中: 'hit_bonus',
      降低命中: 'hit_penalty',
      增加闪避: 'dodge_bonus',
      降低闪避: 'dodge_penalty',
      增加反应: 'reaction_bonus',
      降低反应: 'reaction_penalty',
      增加攻速: 'attacker_speed_bonus',
      加快施放: 'cast_speed_bonus',
      减慢施放: 'cast_speed_penalty',
      锁定强度: 'lock_level',
      跳过回合: 'skip_turn',
      无法反应: 'cannot_react',
      每回合伤害: 'dot_damage',
      造成伤害修正: 'final_damage_mult',
      受到伤害修正: 'received_damage_mult',
      防御剥夺: 'defense_strip',
      精神抗性剥夺: 'spirit_resist_strip',
      治疗修正: 'final_heal_mult',
      技能效果倍率: 'skill_effect_mult',
      承伤修正: 'damage_reduction',
      反射比例: 'damage_reflect_ratio',
      分摊比例: 'damage_share_ratio',
      分摊人数: 'damage_share_count',
      消耗分摊比例: 'cost_share_ratio',
      消耗分摊人数: 'cost_share_count',
      免伤次数: 'block_count',
      禁疗比例: 'heal_block_ratio',
      反转比例: 'heal_inversion_ratio',
      反击倍率: 'counter_attack_ratio',
      吸收比例: 'damage_absorb_ratio',
      中断概率: 'interrupt_bonus',
      破甲比例: 'armor_pen',
      敏捷倍率: 'agi_ratio',
      隐蔽度: 'stealth_level',
      免疫等级阈值: 'invincible_tier_threshold',
      免死次数: 'death_save_count',
      最低生命保留: 'min_hp_floor',
      霸体: 'super_armor',
      无视异常: '无视异常',
      封技: 'skill_seal',
      缴械: 'disarm',
      沉默: 'silence',
      致盲: 'blind',
      探查屏蔽: '探查屏蔽',
      面板修改比例: 'stat_mods',
      力量: 'str',
      防御: 'def',
      敏捷: 'agi',
      体力: 'vit',
      体力上限: 'vit_max',
      生命: 'hp',
      生命比例: 'hp_ratio',
      魂力: 'sp',
      魂力上限: 'sp_max',
      魂力比例: 'sp_ratio',
      精神力: 'men',
      精神力上限: 'men_max',
      精神力比例: 'men_ratio',
    });

    function 补齐战斗机制英文键(数据) {
      if (Array.isArray(数据)) {
        数据.forEach(item => 补齐战斗机制英文键(item));
        return 数据;
      }
      if (!数据 || typeof 数据 !== 'object') return 数据;
      Object.entries(战斗机制中文键英文名).forEach(([中文键, 英文键]) => {
        if (数据[中文键] !== undefined && 数据[英文键] === undefined) 数据[英文键] = 数据[中文键];
      });
      Object.values(数据).forEach(value => 补齐战斗机制英文键(value));
      return 数据;
    }

    function 转换战斗机制运行值(数据) {
      if (Array.isArray(数据)) {
        数据.forEach(item => 转换战斗机制运行值(item));
        return 数据;
      }
      if (!数据 || typeof 数据 !== 'object') return 数据;
      ['属性', '驱动属性', '资源类型'].forEach(key => {
        if (typeof 数据[key] === 'string') 数据[key] = 战斗机制中文键英文名[数据[key]] || 数据[key];
      });
      Object.values(数据).forEach(value => 转换战斗机制运行值(value));
      return 数据;
    }

    function 读取原型字段数值(字段 = {}, fallback = 0) {
      const 候选 = [
        字段?.数值,
        字段?.倍率,
        字段?.比例,
        字段?.强度,
        字段?.次数,
        字段?.数量,
      ];
      for (const item of 候选) {
        if (item === undefined || item === null || item === '') continue;
        const text = String(item).trim();
        if (/^x/i.test(text)) {
          const parsed = Number(text.replace(/^x/i, ''));
          if (Number.isFinite(parsed)) return parsed;
        }
        const parsed = 读取战斗效果数值(text, fallback);
        if (Number.isFinite(parsed)) return parsed;
      }
      return fallback;
    }

    function 展开战斗原型数组字段(effect = {}) {
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return [];
      let entries = [deepClonePlain(effect)];
      BATTLE_PROTOTYPE_ARRAY_FIELDS.forEach(字段名 => {
        entries = entries.flatMap(entry => {
          const raw = entry && entry[字段名];
          if (!Array.isArray(raw)) return [entry];
          const values = raw.map(item => String(item || '').trim()).filter(Boolean);
          if (!values.length) return [entry];
          return values.map(value => ({ ...entry, [字段名]: value }));
        });
      });
      return entries;
    }

    function 写入原型战斗字段(计算层效果, 字段名, 数值, 模式 = '增量', 原始文本 = '') {
      if (!字段名) return;
      if (模式 === '布尔') {
        计算层效果[字段名] = true;
        return;
      }
      if (模式 === '次数') {
        计算层效果[字段名] = Math.max(1, Math.round(Number(数值 || 1)));
        return;
      }
      if (模式 === '倍率') {
        const raw = Number(数值);
        计算层效果[字段名] = raw > -1 && Math.abs(raw) < 0.5 && /^[+-]/.test(String(原始文本 || '')) ? 1 + raw : raw;
        return;
      }
      计算层效果[字段名] = Number(数值 || 0);
    }

    function 写入状态施加内置战斗效果(计算层效果, effect = {}, hydrated = {}) {
      const 状态 = String(effect?.状态 || '').trim();
      const 数值 = 读取原型字段数值(effect, 0);
      const 副数值 = 读取战斗数值正负(effect?.副数值);
      const 锁定强度 = Math.max(1, Math.round(Math.abs(Number(数值 || effect?.强度 || 1))));
      const 取比例 = (值, 默认值 = 0, 上限 = 1) => Math.min(上限, Math.abs(Number(值 || 默认值 || 0)));
      const 写比例字段 = (字段名, 值, 默认值 = 0) => {
        if (!字段名) return;
        计算层效果[字段名] = Math.max(Number(计算层效果[字段名] || 0), Math.abs(Number(值 || 默认值 || 0)));
      };
      const 写倍率字段 = (字段名, 值, 默认值 = 0) => {
        const 增量 = Number(值 || 默认值 || 0);
        计算层效果[字段名] = Math.max(Number(计算层效果[字段名] || 1), 1 + Math.abs(增量));
      };
      const 写面板倍率 = (字段名, 倍率) => {
        if (!字段名) return;
        hydrated.面板修改比例 = { ...(hydrated.面板修改比例 || {}), [字段名]: Number(倍率 || 1) };
      };
      if (状态 === '中毒') {
        写比例字段('dot_damage_ratio', 取比例(数值, 0.03, 0.03));
        写比例字段('hit_penalty', 副数值, 0.05);
      } else if (状态 === '流血') {
        写比例字段('dot_damage_ratio', 取比例(数值, 0.03, 0.03));
        写倍率字段('received_damage_mult', 副数值, 0.05);
      } else if (状态 === '灼烧') {
        写比例字段('dot_damage_ratio', 取比例(数值, 0.03, 0.03));
        写面板倍率('def', 1 - 取比例(副数值, 0.05));
      } else if (状态 === '冻伤') {
        写比例字段('dot_damage_ratio', 取比例(数值, 0.02, 0.03));
        写面板倍率('agi', 1 - 取比例(副数值, 0.05));
      } else if (状态 === '持续创伤') {
        写比例字段('dot_damage_ratio', 取比例(数值, 0.03, 0.03));
        写比例字段('reaction_penalty', 副数值, 0.05);
      } else if (状态 === '持续恢复') {
        写比例字段('hot_heal_ratio', 取比例(数值, 0.05, 0.05));
      } else if (状态 === '资源燃烧') {
        计算层效果.sp_gain_ratio = Math.min(Number(计算层效果.sp_gain_ratio || 0), -取比例(数值, 0.03, 0.03));
        计算层效果.cost_delta_ratio = Math.max(Number(计算层效果.cost_delta_ratio || 0), 取比例(副数值, 0.05));
      } else if (状态 === '迟缓') {
        写面板倍率('agi', 1 - 取比例(数值, 0.1));
        写比例字段('reaction_penalty', 副数值, 0.08);
      } else if (状态 === '眩晕') {
        计算层效果.skip_turn = true;
        计算层效果.cannot_react = true;
      } else if (状态 === '沉默') {
        计算层效果.silence = true;
      } else if (状态 === '致盲') {
        计算层效果.blind = true;
      } else if (状态 === '封技') {
        计算层效果.skill_seal = true;
      } else if (状态 === '禁疗') {
        计算层效果.heal_block_ratio = Math.max(计算层效果.heal_block_ratio || 0, 数值 || 0.4);
      } else if (状态 === '治疗反转') {
        计算层效果.heal_inversion_ratio = Math.max(计算层效果.heal_inversion_ratio || 0, 数值 || 1);
      } else if (状态 === '防御剥夺') {
        写比例字段('defense_strip', 数值, 0.2);
      } else if (状态 === '精神抗性剥夺') {
        写比例字段('spirit_resist_strip', 数值, 0.2);
      } else if (状态 === '隐匿') {
        计算层效果.stealth_level = 1;
      } else if (状态 === '探查屏蔽') {
        计算层效果.探查屏蔽 = true;
      } else if (状态 === '共享视野') {
        写比例字段('hit_bonus', 数值, 0.1);
        写比例字段('reaction_bonus', 副数值, 0.1);
      } else if (状态 === '标记') {
        计算层效果.dodge_penalty = Math.max(计算层效果.dodge_penalty || 0, 取比例(数值, 1));
      } else if (状态 === '护卫') {
        计算层效果.damage_reduction = Math.max(计算层效果.damage_reduction || 0, 取比例(数值, 0.1));
      } else if (状态 === '嘲讽') {
        计算层效果.lock_level = Math.max(计算层效果.lock_level || 0, 1);
      } else if (状态 === '位移限制') {
        计算层效果.lock_level = Math.max(计算层效果.lock_level || 0, 锁定强度);
        计算层效果.reaction_penalty = Math.max(计算层效果.reaction_penalty || 0, 取比例(数值, 0.15));
        计算层效果.dodge_penalty = Math.max(计算层效果.dodge_penalty || 0, 取比例(副数值, 0.1));
      } else if (状态 === '护盾') {
        写比例字段('shield_gain_bonus', 数值, 0.1);
      } else if (状态 === '无视异常') {
        计算层效果.无视异常 = true;
      } else if (状态 === '霸体') {
        计算层效果.super_armor = true;
      } else if (状态 === '虚弱') {
        const 倍率 = 1 - 取比例(数值, 0.08);
        写面板倍率('str', 倍率);
        写面板倍率('def', 倍率);
        写面板倍率('agi', 倍率);
      } else if (状态 === '失控') {
        计算层效果.random_target_rate = Math.max(计算层效果.random_target_rate || 0, 取比例(数值, 0.35));
        计算层效果.hit_penalty = Math.max(计算层效果.hit_penalty || 0, 取比例(副数值, 0.1));
      } else if (状态 === '反噬') {
        计算层效果.misfortune_backlash_ratio = Math.max(计算层效果.misfortune_backlash_ratio || 0, 取比例(数值, 0.03));
      } else if (状态 === '精神紊乱') {
        计算层效果.random_target_rate = Math.max(计算层效果.random_target_rate || 0, 取比例(数值, 0.25));
        计算层效果.reaction_penalty = Math.max(计算层效果.reaction_penalty || 0, 取比例(副数值, 0.08));
      } else if (状态 === '僵直') {
        计算层效果.cannot_react = true;
        计算层效果.reaction_penalty = Math.max(计算层效果.reaction_penalty || 0, 取比例(数值, 0.5));
        计算层效果.cast_speed_penalty = Math.max(计算层效果.cast_speed_penalty || 0, 0.35);
        计算层效果.dodge_penalty = Math.max(计算层效果.dodge_penalty || 0, 取比例(副数值, 0.2));
      } else if (状态 === '麻痹') {
        计算层效果.reaction_penalty = Math.max(计算层效果.reaction_penalty || 0, 取比例(数值, 0.5));
        计算层效果.dodge_penalty = Math.max(计算层效果.dodge_penalty || 0, 取比例(副数值, 0.2));
      } else if (状态 === '混乱') {
        计算层效果.random_target_rate = Math.max(计算层效果.random_target_rate || 0, 取比例(数值, 0.4));
        计算层效果.hit_penalty = Math.max(计算层效果.hit_penalty || 0, 取比例(副数值, 0.1));
      } else if (状态 === '魂力枯竭') {
        计算层效果.sp_gain_ratio = Math.min(Number(计算层效果.sp_gain_ratio || 0), -取比例(数值, 0.03, 0.03));
        计算层效果.cost_delta_ratio = Math.max(Number(计算层效果.cost_delta_ratio || 0), 取比例(副数值, 0.05));
      }
    }

    function 编译原型战斗效果(effect = {}, context = {}) {
      const 原型 = String(effect?.原型 || '').trim();
      if (!原型) return null;
      const 原型定义 = BATTLE_PROTOTYPE_REGISTRY[原型] || {};
      if (!原型定义.原型) return null;
      const hydrated = {
        ...deepClonePlain(effect || {}),
        运行机制: 原型,
        原型,
      };
      if (原型 === '机制抹消') {
        if (!String(hydrated.状态名称 || '').trim()) hydrated.状态名称 = '机制抹消';
        hydrated.抹消对象 = 规范化战斗机制抹消对象(hydrated.抹消对象);
        if (!(Number(hydrated.持续回合 || 0) > 0)) hydrated.持续回合 = 2;
      }
      if (原型 === '召唤生成' && hydrated.召唤数量 === undefined && effect?.数量 !== undefined) {
        hydrated.召唤数量 = effect.数量;
      }
      const 计算层效果 = createEmptyCombatEffectMap();
      const 数值 = 读取原型字段数值(effect, 0);
      const 原始数值 = String(effect?.数值 ?? effect?.倍率 ?? '');
      const 写字段 = (字段名, 模式 = '增量', 值 = 数值) => 写入原型战斗字段(计算层效果, 字段名, 值, 模式, 原始数值);
      if (原型 === '伤害结算') {
        hydrated.运行机制 = '伤害结算';
        const 攻击段数 = Math.max(1, Math.round(Number(effect?.攻击段数 || 1)));
        const 伤害绑定 = 读取战斗伤害类型绑定(effect?.伤害类型, context?.skill || context?.技能 || {});
        hydrated.伤害类型 = 伤害绑定.伤害类型;
        hydrated.攻击段数 = 攻击段数;
        hydrated.结算标签 = 伤害绑定.结算标签;
        hydrated.抗性类型 = 伤害绑定.抗性类型;
        if (Number(effect?.防御穿透 || 0) > 0) {
          hydrated.防御穿透 = Math.max(0, Number(effect.防御穿透 || 0));
          写字段('armor_pen', '增量', hydrated.防御穿透);
        }
        if (攻击段数 > 1) {
          写字段('multi_hit_count', '次数', 攻击段数);
          写字段('segment_damage_ratio', '增量', 1 / 攻击段数);
        }
      } else if (原型 === '判定修正') {
        const 判定 = String(effect?.判定 || '').trim();
        const 驱动属性 = String(effect?.驱动属性 || '').trim();
        const 驱动系数 = 驱动属性
          ? 计算原型驱动缩放系数(effect, effect?.__actor || {}, effect?.__actor?.final || {}, effect?.__target || {}, effect?.__target?.final || {})
          : 1;
        const 缩放数值 = 数值 * (Number.isFinite(驱动系数) ? 驱动系数 : 1);
        const 正向 = 缩放数值 >= 0;
        const 判定字段表 = {
          命中: 正向 ? 'hit_bonus' : 'hit_penalty',
          闪避: 正向 ? 'dodge_bonus' : 'dodge_penalty',
          反应: 正向 ? 'reaction_bonus' : 'reaction_penalty',
        };
        写字段(判定字段表[判定], '增量', Math.abs(缩放数值));
        if (effect?.打断效果 === true && !正向) {
          hydrated.运行机制 = '打断';
          hydrated.中断概率 = Math.abs(缩放数值);
          写字段('interrupt_bonus', '增量', Math.abs(缩放数值));
        }
      } else if (原型 === '结算修正') {
        const 结算 = String(effect?.结算 || '').trim();
        if (结算 === '造成伤害') {
          写字段('final_damage_mult', '倍率', 数值);
        } else if (结算 === '受到伤害') {
          if (数值 >= 0) 写字段('received_damage_mult', '倍率', 数值);
          else 写字段('damage_reduction', '增量', Math.abs(数值));
        } else if (结算 === '治疗') {
          写字段('final_heal_mult', '倍率', 数值);
        } else if (结算 === '消耗') {
          if (读取战斗数值是否百分比(原始数值) && 数值 > 0) 计算层效果.cost_delta_ratio = Math.max(Number(计算层效果.cost_delta_ratio || 0), 数值);
          if (读取战斗数值是否百分比(原始数值)) 写字段('cost_ratio', '倍率', 数值);
          else 写字段('cost_delta', '增量', 数值);
        } else if (结算 === '前摇') {
          if (读取战斗数值是否百分比(原始数值)) 写字段('windup_ratio', '倍率', 数值);
          else 写字段('windup_delta', '增量', 数值);
        } else {
          const 结算字段表 = {
            反伤: ['damage_reflect_ratio', '增量'],
            伤害转移: ['damage_transfer_ratio', '增量'],
            伤害吸收: ['damage_absorb_ratio', '增量'],
            伤害转治疗: ['damage_to_heal_ratio', '增量'],
            治疗转伤害: ['heal_to_damage_ratio', '增量'],
            伤害分摊: ['damage_share_ratio', '增量'],
            消耗分摊: ['cost_share_ratio', '增量'],
            防御穿透: ['armor_pen', '增量'],
            防御剥夺: ['defense_strip', '增量'],
            精神抗性剥夺: ['spirit_resist_strip', '增量'],
            反击: ['counter_attack_ratio', '增量'],
            持续伤害引爆: ['dot_detonate_ratio', '增量'],
            技能效果: ['skill_effect_mult', '倍率'],
          };
          const [字段名, 模式] = 结算字段表[结算] || [];
          写字段(字段名, 模式 || '增量', ['反伤', '伤害转移', '伤害吸收', '伤害转治疗', '治疗转伤害', '伤害分摊', '消耗分摊', '防御穿透', '防御剥夺', '精神抗性剥夺', '反击', '持续伤害引爆'].includes(结算) ? Math.abs(数值) : 数值);
          if (结算 === '伤害转移') 计算层效果.damage_transfer_target = String(effect?.转移对象 || '攻击者').trim() || '攻击者';
          if (结算 === '伤害吸收') {
            计算层效果.吸收来源 = ['造成伤害', '受到伤害'].includes(String(effect?.吸收来源 || '').trim()) ? String(effect?.吸收来源 || '').trim() : '造成伤害';
            计算层效果.吸收资源 = ['生命', '体力', '魂力', '精神力'].includes(String(effect?.吸收资源 || '').trim()) ? String(effect?.吸收资源 || '').trim() : '生命';
            计算层效果.吸收转化效果 = ['恢复资源', '下次造成伤害'].includes(String(effect?.转化效果 || '').trim()) ? String(effect?.转化效果 || '').trim() : '恢复资源';
            计算层效果.伤害吸收增幅上限 = Math.max(0, Math.min(1, Math.abs(读取战斗数值正负(effect?.增幅上限 || '+50%'))));
          }
          if (结算 === '伤害分摊') {
            计算层效果.damage_share_count = String(effect?.目标 || '').trim() === '群体' ? Math.max(1, Math.round(Number(effect?.数量 || 1))) : 1;
          }
          if (结算 === '消耗分摊') {
            计算层效果.cost_share_count = String(effect?.目标 || '').trim() === '群体' ? Math.max(1, Math.round(Number(effect?.数量 || 1))) : 1;
          }
        }
        if (结算 === '持续伤害引爆') hydrated.引爆倍率 = 数值 > 0 && 数值 < 1 && /^\+/.test(原始数值) ? 1 + 数值 : Math.max(0.1, 数值 || 1);
      } else if (原型 === '属性修正') {
        const 属性列表 = (Array.isArray(effect?.属性) ? effect.属性 : [effect?.属性])
          .map(属性 => 战斗机制中文键英文名[String(属性 || '').trim()] || String(属性 || '').trim())
          .filter(Boolean);
        属性列表.forEach(属性 => {
          if (读取战斗数值是否百分比(原始数值)) {
            const 面板倍率 = Number(Math.max(0.1, 1 + 数值).toFixed(4));
            hydrated.面板修改比例 = { ...(hydrated.面板修改比例 || {}), [属性]: 面板倍率 };
          } else {
            hydrated.面板固定修正 = { ...(hydrated.面板固定修正 || {}), [属性]: 数值 };
          }
        });
      } else if (原型 === '状态施加') {
        const 状态 = String(effect?.状态 || '').trim();
        hydrated.运行机制 = '状态施加';
        hydrated.状态名称 = 状态 || '状态';
        const 元素承伤修正 = 构建状态元素承伤修正(effect);
        if (元素承伤修正) hydrated.元素承伤修正 = 元素承伤修正;
        写入状态施加内置战斗效果(计算层效果, effect, hydrated);
        const 触发限制 = effect?.触发限制 && typeof effect.触发限制 === 'object' ? effect.触发限制 : null;
        if (状态 === '无视异常' && 触发限制 && String(触发限制.周期 || '').trim() === '每日') {
          写字段('每日触发次数上限', '次数', 触发限制.次数);
        }
        if (hasMeaningfulCombatEffect(计算层效果)) {
          hydrated.计算层效果 = mergeCombatEffectMaps(hydrated.计算层效果 || createEmptyCombatEffectMap(), 计算层效果);
        }
      } else if (原型 === '规则防御') {
        const 规则 = String(effect?.规则 || '').trim();
        const 规则字段表 = {
          免伤: 'block_count',
          免死: 'death_save_count',
        };
        写字段(规则字段表[规则], '次数', 读取原型字段数值(effect, 1));
      } else if (原型 === '决策干扰') {
        const 干扰 = String(effect?.干扰 || '').trim();
        if (干扰 === '判断干扰') 写字段('判断干扰强度', '增量', Math.abs(数值));
        if (干扰 === '索敌干扰') 写字段('索敌干扰强度', '增量', Math.abs(数值));
      } else if (原型 === '资源变化') {
        const 资源 = String(effect?.资源 || '').trim();
        const 持续回合 = Math.max(0, Math.round(Number(effect?.持续回合 || 0)));
        if (资源 === '魂力') {
          hydrated.属性 = 'sp';
          hydrated.资源类型 = '魂力';
          写字段('sp_gain_ratio', '增量');
        } else if (资源 === '精神力') {
          hydrated.属性 = 'men';
          hydrated.资源类型 = '精神力';
          写字段('men_gain_ratio', '增量');
        } else if (资源 === '体力') {
          hydrated.属性 = 'vit';
          hydrated.资源类型 = '体力';
          if (持续回合 > 0) 写字段('vit_gain_ratio', '增量');
          else 写字段('final_heal_bonus', '增量');
        } else if (资源 === '生命') {
          hydrated.属性 = 'hp';
          hydrated.资源类型 = '生命';
          if (持续回合 > 0 && 数值 < 0 && !(读取战斗数值是否百分比(原始数值) || Math.abs(数值) <= 1)) 写字段('dot_damage', '增量', Math.abs(数值));
          else if (持续回合 > 0 && 数值 < 0) 写字段('dot_damage_ratio', '增量', Math.abs(数值));
          else if (持续回合 > 0) 写字段('hot_heal_ratio', '增量');
          else 写字段('final_heal_bonus', '增量');
        }
        if (持续回合 > 0) {
          hydrated.持续回合 = Math.min(10, 持续回合);
          hydrated.运行机制 = '资源持续变化';
          hydrated.状态名称 = `${hydrated.资源类型 || 资源 || '资源'}${数值 >= 0 ? '持续恢复' : '持续流失'}`;
        }
      } else if (原型 === '资源转移') {
        hydrated.运行机制 = '资源转移';
      } else if (原型 === '护盾变化') {
        if (effect?.数值 !== undefined && hydrated.护盾值 === undefined) hydrated.护盾值 = effect.数值;
        写字段('shield_gain_bonus', '增量');
      } else if (原型 === '时窗修正') {
        const 调整方式 = String(effect?.调整方式 || '').trim();
        hydrated.运行机制 = '时窗修正';
        hydrated.运行态调整方式 = 调整方式 === '压缩' ? '压缩' : '延长';
        hydrated.调整字段 = String(effect?.调整字段 || '持续回合').trim() || '持续回合';
        hydrated.单日使用次数上限 = Math.max(1, Math.min(3, Math.floor(Number(effect?.单日使用次数上限 || 1))));
        if (hydrated.调整字段 === '持续回合') {
          hydrated.持续回合 = Math.max(1, Math.min(3, Number(effect?.调整回合 || effect?.持续回合 || 1)));
          hydrated.压缩回合 = Math.max(1, Math.min(3, Number(effect?.调整回合 || 1)));
          if (调整方式 === '压缩' && effect?.结算倍率 !== undefined) {
            const 时窗倍率 = 读取战斗数值正负(effect?.结算倍率);
            hydrated.引爆倍率 = 时窗倍率 || 1;
          }
        } else if (hydrated.调整字段 === '有效期tick') {
          hydrated.调整tick = Math.max(1, Math.min(1008, Number(effect?.调整tick || effect?.有效期tick || 6)));
        } else if (['触发次数', '使用次数', '可用次数'].includes(hydrated.调整字段)) {
          hydrated.调整次数 = Math.max(1, Math.min(3, Number(effect?.调整次数 || 1)));
        } else if (hydrated.调整字段 === '冷却回合') {
          hydrated.调整回合 = Math.max(1, Math.min(3, Number(effect?.调整回合 || 1)));
        }
      } else if (原型 === '时光回溯') {
        hydrated.运行机制 = '时光回溯';
        hydrated.状态名称 = '时光回溯';
        hydrated.持续回合 = 1;
        hydrated.发动方式 = ['主动', '被动'].includes(String(effect?.发动方式 || '').trim()) ? String(effect.发动方式).trim() : '被动';
        hydrated.时光回溯成功率 = 1;
        hydrated.时光回溯反应压制值 = 时光回溯反应压制值;
      } else if (原型 === '位移执行') {
        const 位移 = String(effect?.位移类型 || effect?.位移 || effect?.方向 || effect?.类型 || '').trim();
        const 位移对象 = String(effect?.位移对象 || '').trim();
        const 位移幅度 = Math.max(1, Math.min(30, Number(effect?.距离 || 1)));
        const 位移强度 = Math.max(0.04, Math.min(0.45, Math.abs(数值) || 位移幅度 * 0.04));
        const 近身反击风险 = Math.max(0, Math.min(0.3, (6 - 位移幅度) * 0.05));
        const 写自身位移收益 = () => {
          写字段('dodge_bonus', '增量', 位移强度);
          写字段('reaction_bonus', '增量', 位移强度);
          if (位移 === '脱离') 写字段('random_target_rate', '增量', Math.min(1, 位移强度));
        };
        const 写目标位移压制 = () => {
          if (位移 === '拉近') {
            写字段('hit_bonus', '增量', 位移强度);
            写字段('lock_level', '次数', 1);
            写字段('reaction_penalty', '增量', 位移强度);
            写字段('dodge_penalty', '增量', Math.max(0.04, 位移强度 * 0.5));
          } else if (位移 === '击退') {
            写字段('reaction_penalty', '增量', 位移强度);
            写字段('dodge_penalty', '增量', Math.max(0.04, 位移强度 * 0.5));
            写字段('hit_penalty', '增量', Math.max(0.04, 位移强度 * 0.5));
          } else if (位移 === '换位') {
            写字段('random_target_rate', '增量', Math.min(1, 位移强度));
            写字段('reaction_penalty', '增量', 位移强度);
            写字段('dodge_penalty', '增量', Math.max(0.04, 位移强度 * 0.5));
          }
        };
        if (位移对象 === '自身') {
          写自身位移收益();
        } else if (位移对象 === '目标') {
          写目标位移压制();
        } else if (位移对象 === '自身与目标') {
          写自身位移收益();
          写目标位移压制();
        }
        hydrated.持续回合 = Math.max(1, Math.min(10, Math.round(Number(effect?.持续回合 || 1))));
        hydrated.位移成功率 = 1;
      }
      if (原型 !== '状态施加' && hasMeaningfulCombatEffect(计算层效果)) {
        hydrated.计算层效果 = mergeCombatEffectMaps(hydrated.计算层效果 || createEmptyCombatEffectMap(), 计算层效果);
      }
      delete hydrated.机制;
      delete hydrated.参数;
      return hydrated;
    }

    function hydrateBattleExecutionEffectEntry(effect = {}, context = {}) {
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return null;
      const sourceEffect = effect?.原型 ? 编译原型战斗效果(effect, context) : null;
      if (!sourceEffect || typeof sourceEffect !== 'object' || Array.isArray(sourceEffect)) return null;
      const 运行机制 = String(sourceEffect?.原型 || '').trim();
      if (!运行机制 || 运行机制 === '系统基础') return null;
      const targetScale = normalizeBattleEffectTargetScale(sourceEffect?.目标 || effect?.目标 || '单体', '单体');
      const duration = Math.max(0, Math.round(Number(sourceEffect?.持续回合 ?? 0)));
      const hydrated = {
        ...deepClonePlain(sourceEffect || {}),
        目标: targetScale,
      };
      hydrated.运行时消费器 = String(sourceEffect?.运行时消费器 || 读取战斗原型运行消费器(sourceEffect)).trim();
      if (!String(hydrated.目标 || '').trim()) hydrated.目标 = String(effect?.目标 || targetScale || '').trim();
      delete hydrated.机制;
      delete hydrated.参数;
      if (duration > 0) hydrated.持续回合 = duration;
      return 转换战斗机制运行值(补齐战斗机制英文键(hydrated));
    }

    const 驱动属性中文运行键表 = Object.freeze({
      力量: 'str',
      防御: 'def',
      敏捷: 'agi',
      体力: 'vit',
      体力上限: 'vit_max',
      魂力: 'sp_max',
      魂力上限: 'sp_max',
      精神力: 'men_max',
      精神力上限: 'men_max',
      体力比例: 'vit_ratio',
      生命比例: 'hp_ratio',
      HP比例: 'hp_ratio',
      防御比例: 'def_ratio',
      魂力比例: 'sp_ratio',
      精神力比例: 'men_ratio',
    });

    function 规范驱动属性运行键(attributeKey) {
      const key = String(attributeKey || '').trim();
      return 驱动属性中文运行键表[key] || key;
    }

    function 选择战斗原型默认影响方向(原型 = '', effect = {}, fallback = '效果强度') {
      const 原型名 = String(原型 || '').trim();
      const 结算 = String(effect?.结算 || '').trim();
      const 调整字段 = String(effect?.调整字段 || '').trim();
      let 默认方向 = fallback;
      if (原型名 === '位移执行') 默认方向 = '效果强度';
      else if (原型名 === '复制执行') 默认方向 = '效果强度';
      else if (['判定修正', '状态移除', '决策干扰', '机制抹消', '资源锁定', '规则改写'].includes(原型名)) 默认方向 = '成功率';
      else if (原型名 === '结算修正' && 结算 === '消耗分摊') 默认方向 = '效果强度';
      else if (原型名 === '结算修正' && 结算 === '消耗') 默认方向 = '消耗';
      else if (原型名 === '结算修正' && 结算 === '前摇') 默认方向 = '前摇';
      else if (原型名 === '时窗修正') 默认方向 = ['持续回合', '有效期tick'].includes(调整字段) ? '持续时间' : '效果强度';
      else 默认方向 = '效果强度';
      const 原型定义 = BATTLE_PROTOTYPE_REGISTRY[原型名] || {};
      const 方向选项 = Array.isArray(原型定义?.字段定义?.影响方向?.选项) ? 原型定义.字段定义.影响方向.选项 : [];
      return 方向选项.includes(默认方向) ? 默认方向 : 方向选项[0] || fallback;
    }

    function 战斗原型命中绝对值默认无驱动(effect = {}) {
      if (!effect || typeof effect !== 'object') return false;
      const 数值列表 = ['数值', '结算倍率', '转化比例', '概率'].flatMap(key => {
        const 原值 = effect?.[key];
        if (原值 === undefined || 原值 === null) return [];
        return Array.isArray(原值) ? 原值 : [原值];
      });
      if (!数值列表.length) return false;
      return 数值列表.every(原值 => {
        if (typeof 原值 === 'number') return Number.isFinite(原值);
        const 文本 = String(原值 ?? '').trim();
        return !!文本 && /^[-+]?\d+(?:\.\d+)?%?$/.test(文本) && !/%$/.test(文本);
      });
    }

    function 读取战斗效果数值(value, fallback = 0) {
      const text = String(value ?? '').trim();
      if (!text) return fallback;
      if (/%$/.test(text)) {
        const percent = Number(text.replace('%', ''));
        return Number.isFinite(percent) ? percent / 100 : fallback;
      }
      const num = Number(text);
      return Number.isFinite(num) ? num : fallback;
    }

    function 读取战斗属性倍率(effect = {}) {
      const action = String(effect?.动作 || '').trim();
      const rawValue = effect?.数值;
      const value = 读取战斗效果数值(rawValue, 0);
      if (['倍率提升', '倍率压制'].includes(action)) {
        const num = Number(rawValue);
        if (Number.isFinite(num) && num > 0) return num;
      }
      if (!action || ['倍率提升', '倍率压制'].includes(action)) return Math.max(0, 1 + value);
      return value;
    }

    function 格式化战斗效果数值(value, digits = 2) {
      const num = Number(value || 0);
      if (!Number.isFinite(num)) return String(value ?? '');
      return Number(num.toFixed(digits));
    }

    function 格式化战斗百分比变化(value, digits = 2) {
      const num = Number(value || 0);
      if (!Number.isFinite(num)) return '+0%';
      const percent = Number((num * 100).toFixed(digits));
      return `${percent >= 0 ? '+' : ''}${percent}%`;
    }

    function 缩放战斗属性变化数值(rawValue, factor = 1) {
      const delta = 读取战斗效果数值(rawValue, 0);
      const multiplier = Math.max(0, 1 + delta);
      const scaled = delta < 0 ? scaleBattleDebuffRatio(multiplier, factor, 1) : scaleBattleFactor(multiplier, factor, 1);
      return 格式化战斗百分比变化(scaled - 1);
    }

    function getMechanismDriveValue(entity, finalEntity, attributeKey) {
      const 运行键 = 规范驱动属性运行键(attributeKey);
      const stats = finalEntity || entity || {};
      switch (运行键) {
        case 'men_max':
          return 读取战斗资源上限值(entity, stats, 'men');
        case 'sp_max':
          return 读取战斗资源上限值(entity, stats, 'sp');
        case 'vit_max':
          return 读取战斗资源上限值(entity, stats, 'vit');
        case 'agi':
        case 'str':
        case 'def':
        case 'vit':
          if (运行键 === 'vit') {
            return Math.max(0, Number(entity?.sta ?? stats?.sta ?? entity?.体力 ?? stats?.体力 ?? entity?.vit ?? stats?.vit ?? 0));
          }
          return Number(stats[运行键] || 0);
        case 'vit_ratio':
          return (
            Math.max(0, Number(entity?.sta || stats?.sta || entity?.体力 || stats?.体力 || 0)) /
            Math.max(1, Number(entity?.sta_max || stats?.sta_max || entity?.体力上限 || stats?.体力上限 || 1))
          );
        case 'hp_ratio':
          return (
            Math.max(0, Number(entity?.hp ?? stats?.hp ?? entity?.HP ?? stats?.HP ?? 0)) /
            Math.max(1, Number(entity?.hp_max ?? stats?.hp_max ?? entity?.HP上限 ?? stats?.HP上限 ?? 1))
          );
        case 'def_ratio':
          return Number(stats.def || entity?.def || 0);
        case 'sp_ratio':
          return (
            Math.max(0, Number(entity?.sp || stats?.sp || 0)) /
            Math.max(1, Number(entity?.sp_max || stats?.sp_max || 1))
          );
        case 'men_ratio':
          return (
            Math.max(0, Number(entity?.men || stats?.men || 0)) /
            Math.max(1, Number(entity?.men_max || stats?.men_max || 1))
          );
        default:
          return 0;
      }
    }

    function 计算原型驱动缩放系数(effect = {}, attacker = {}, attackerFinalStat = {}, defender = {}, defenderFinalStat = {}) {
      const 驱动属性 = String(effect?.驱动属性 || '').trim();
      if (!驱动属性) return 1;
      const 运行键 = 规范驱动属性运行键(驱动属性);
      if (运行键 === 'sp_max') {
        if (String(effect?.影响方向 || '').trim() === '消耗') return getSupportCostScale({ ...attacker, ...attackerFinalStat }, { ...defender, ...defenderFinalStat });
        return getSupportEffectScale({ ...attacker, ...attackerFinalStat }, { ...defender, ...defenderFinalStat });
      }
      if (运行键 === 'men_max') return 计算精神机制缩放系数(attacker, attackerFinalStat, defender, defenderFinalStat, 1);
      const 攻方 = Math.max(1, getMechanismDriveValue(attacker, attackerFinalStat, 运行键));
      const 守方 = Math.max(1, getMechanismDriveValue(defender, defenderFinalStat, 运行键));
      const 比值 = 攻方 / 守方;
      if (!Number.isFinite(比值) || 比值 <= 0) return 1;
      return 限制战斗倍率(Math.pow(比值, 0.45), 0.35, 1.85);
    }

    function 缩放原型面板修改比例(面板修改比例 = {}, 缩放系数 = 1) {
      const next = {};
      Object.entries(面板修改比例 || {}).forEach(([key, raw]) => {
        const value = Number(raw);
        if (!Number.isFinite(value)) return;
        next[key] = value >= 1 ? scaleBattleFactor(value, 缩放系数, 1) : scaleBattleDebuffRatio(value, 缩放系数, 1);
      });
      return next;
    }

    function 缩放原型面板固定修正(面板固定修正 = {}, 缩放系数 = 1) {
      const next = {};
      Object.entries(面板固定修正 || {}).forEach(([key, raw]) => {
        const value = Number(raw);
        if (!Number.isFinite(value)) return;
        next[key] = Number((value * 缩放系数).toFixed(4));
      });
      return next;
    }

    const 状态施加默认驱动属性表_战斗 = Object.freeze({
      中毒: '魂力上限',
      流血: '魂力上限',
      灼烧: '魂力上限',
      冻伤: '魂力上限',
      持续创伤: '魂力上限',
      持续恢复: '魂力上限',
      护盾: '魂力上限',
      无视异常: '魂力上限',
      位移限制: '魂力上限',
      麻痹: '魂力上限',
      僵直: '魂力上限',
      迟缓: '魂力上限',
      虚弱: '魂力上限',
      防御剥夺: '魂力上限',
      资源燃烧: '魂力上限',
      魂力枯竭: '魂力上限',
      眩晕: '精神力上限',
      沉默: '精神力上限',
      致盲: '精神力上限',
      封技: '精神力上限',
      混乱: '精神力上限',
      失控: '精神力上限',
      精神紊乱: '精神力上限',
      隐匿: '精神力上限',
      探查屏蔽: '精神力上限',
      共享视野: '精神力上限',
      标记: '精神力上限',
      嘲讽: '精神力上限',
      精神抗性剥夺: '精神力上限',
      禁疗: '精神力上限',
      治疗反转: '精神力上限',
      反噬: '精神力上限',
      霸体: '体力上限',
      护卫: '体力上限',
    });

    function 读取状态施加默认驱动属性_战斗(effect = {}) {
      const 状态名 = String(effect?.状态 || '').trim();
      return 状态施加默认驱动属性表_战斗[状态名] || '魂力上限';
    }

    function 缩放状态施加计算层效果(计算层效果 = {}, hydrated = {}, 缩放系数 = 1) {
      const ratio = Number(缩放系数 || 1);
      if (!Number.isFinite(ratio) || Math.abs(ratio - 1) < 0.0001) return;
      const 正向数值字段 = [
        'dot_damage',
        'dot_damage_ratio',
        'hot_heal_ratio',
        'reaction_penalty',
        'hit_penalty',
        'dodge_penalty',
        'cast_speed_penalty',
        'random_target_rate',
        'shield_gain_bonus',
        'damage_reduction',
        'heal_block_ratio',
        'heal_inversion_ratio',
        'defense_strip',
        'spirit_resist_strip',
        'misfortune_backlash_ratio',
        'reaction_bonus',
        'hit_bonus',
        'dodge_bonus',
        'cast_speed_bonus',
        'interrupt_bonus',
      ];
      正向数值字段.forEach(key => {
        if (计算层效果[key] !== undefined) 计算层效果[key] = scaleBattleValue(计算层效果[key], ratio, { min: 0, digits: 4 });
      });
      ['sp_gain_ratio', 'men_gain_ratio', 'vit_gain_ratio'].forEach(key => {
        if (计算层效果[key] !== undefined) 计算层效果[key] = scaleBattleValue(计算层效果[key], ratio, { digits: 4 });
      });
      if (计算层效果.lock_level !== undefined) 计算层效果.lock_level = scaleBattleLockLevel(计算层效果.lock_level, ratio);
      ['received_damage_mult', 'final_damage_mult', 'final_heal_mult', 'shield_gain_mult'].forEach(key => {
        if (计算层效果[key] === undefined) return;
        const value = Number(计算层效果[key]);
        if (!Number.isFinite(value)) return;
        计算层效果[key] = value >= 1
          ? scaleBattleFactor(value, ratio, 1)
          : scaleBattleDebuffRatio(value, ratio, 1);
      });
      if (hydrated.面板修改比例) hydrated.面板修改比例 = 缩放原型面板修改比例(hydrated.面板修改比例, ratio);
      if (hydrated.面板固定修正) hydrated.面板固定修正 = 缩放原型面板固定修正(hydrated.面板固定修正, ratio);

      const 有数值强度 = 正向数值字段.some(key => Number(计算层效果[key] || 0) !== 0) ||
        ['sp_gain_ratio', 'men_gain_ratio', 'vit_gain_ratio'].some(key => Number(计算层效果[key] || 0) !== 0) ||
        Number(计算层效果.lock_level || 0) > 0 ||
        ['received_damage_mult', 'final_damage_mult', 'final_heal_mult', 'shield_gain_mult'].some(key => Math.abs(Number(计算层效果[key] || 1) - 1) > 0.0001);
      const 是布尔状态 = [
        计算层效果.skip_turn,
        计算层效果.cannot_react,
        计算层效果.silence,
        计算层效果.blind,
        计算层效果.skill_seal,
        计算层效果.super_armor,
        计算层效果.invincible,
        计算层效果.无视异常,
        计算层效果.探查屏蔽,
        Number(计算层效果.stealth_level || 0) > 0,
      ].some(Boolean);
      if (!有数值强度 && 是布尔状态 && hydrated.持续回合 !== undefined) {
        const 原持续 = Math.max(1, Math.round(Number(hydrated.持续回合 || 1)));
        let 下一持续 = Math.max(1, Math.round(原持续 * ratio));
        const 是群体硬控 = ['群体', '全场'].includes(String(hydrated.目标 || '').trim()) &&
          (计算层效果.skip_turn === true ||
            计算层效果.cannot_react === true ||
            计算层效果.silence === true ||
            计算层效果.blind === true ||
            计算层效果.skill_seal === true);
        if (是群体硬控) 下一持续 = Math.min(1, 下一持续);
        hydrated.持续回合 = 下一持续;
      }
    }

    function 原型驱动缩放(effect = {}, directPayload = {}, hydrated = {}, attacker = {}, attackerFinalStat = {}, defender = {}, defenderFinalStat = {}) {
      const 影响方向 = String(effect?.影响方向 || '').trim();
      if (!String(effect?.驱动属性 || '').trim() || !影响方向) return;
      const 缩放系数 = 计算原型驱动缩放系数(effect, attacker, attackerFinalStat, defender, defenderFinalStat);
      if (!Number.isFinite(缩放系数) || Math.abs(缩放系数 - 1) < 0.0001) return;
      if (影响方向 === '效果强度') {
        if (String(effect?.原型 || '').trim() === '状态施加') {
          缩放状态施加计算层效果(directPayload, hydrated, 缩放系数);
          return;
        }
        const scaled = scaleBattleSupportBuffCalc(directPayload, 缩放系数);
        Object.keys(directPayload).forEach(key => delete directPayload[key]);
        Object.assign(directPayload, scaled);
        if (directPayload.面板修改比例) directPayload.面板修改比例 = 缩放原型面板修改比例(directPayload.面板修改比例, 缩放系数);
        if (directPayload.面板固定修正) directPayload.面板固定修正 = 缩放原型面板固定修正(directPayload.面板固定修正, 缩放系数);
        if (hydrated.威力倍率 !== undefined && !(读取对应等级(hydrated) > 0)) hydrated.威力倍率 = scaleBattleValue(hydrated.威力倍率, 缩放系数, { min: 0, digits: 2 });
        if (hydrated.防御穿透 !== undefined) hydrated.防御穿透 = scaleBattleValue(hydrated.防御穿透, 缩放系数, { min: 0, max: 100, digits: 0 });
        if (String(effect?.原型 || '').trim() === '资源锁定' && hydrated.数值 !== undefined) {
          const 原比例 = Math.abs(读取战斗数值正负(hydrated.数值));
          if (原比例 > 0) hydrated.数值 = `${Math.round(Math.min(1, 原比例 * 缩放系数) * 100)}%`;
        }
        if (hydrated.时光回溯反应压制值 !== undefined)
          hydrated.时光回溯反应压制值 = scaleBattleValue(hydrated.时光回溯反应压制值, 缩放系数, { min: 0.05, max: 0.85, digits: 4 });
        return;
      }
      if (影响方向 === '成功率') {
        ['interrupt_bonus', 'random_target_rate'].forEach(key => {
          if (directPayload[key] !== undefined) directPayload[key] = scaleBattleValue(directPayload[key], 缩放系数, { min: 0, digits: 4 });
        });
        if (directPayload.lock_level !== undefined) directPayload.lock_level = scaleBattleLockLevel(directPayload.lock_level, 缩放系数);
        if (directPayload.hit_bonus === undefined) directPayload.hit_bonus = Math.max(0, Number((缩放系数 - 1).toFixed(4)));
        if (hydrated.时光回溯成功率 !== undefined) hydrated.时光回溯成功率 = scaleBattleValue(hydrated.时光回溯成功率, 缩放系数, { min: 0, max: 1, digits: 4 });
        if (hydrated.位移成功率 !== undefined) hydrated.位移成功率 = scaleBattleValue(hydrated.位移成功率, 缩放系数, { min: 0, max: 1, digits: 4 });
        return;
      }
      if (影响方向 === '持续时间') {
        if (hydrated.持续回合 !== undefined) hydrated.持续回合 = Math.max(1, Math.round(Number(hydrated.持续回合 || 1) * 缩放系数));
        if (hydrated.持续tick !== undefined) hydrated.持续tick = Math.max(1, Math.round(Number(hydrated.持续tick || 1) * 缩放系数));
        return;
      }
      if (影响方向 === '消耗') {
        directPayload.cost_ratio = Math.max(Number(directPayload.cost_ratio || 1), 缩放系数);
        return;
      }
      if (影响方向 === '前摇') {
        directPayload.windup_ratio = Math.max(0.25, Number(directPayload.windup_ratio || 1) * 缩放系数);
      }
    }

    function 应用战斗原型驱动判定(effect = {}, context = {}) {
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return effect;
      if (String(effect?.原型 || '').trim() === '伤害结算') return effect;
      const 原型名 = String(effect?.原型 || '').trim();
      if (当前效果来自物品使用(context) && 读取对应等级(effect) > 0) return effect;
      if (原型名 === '复制执行' && !/属性|全部/.test(String(effect?.复制类型 || '').trim())) return effect;
      if (!['机制授予', '时光回溯', '位移执行', '复制执行'].includes(原型名) && String(effect?.目标 || '').trim() === '自身') return effect;
      if (战斗原型命中绝对值默认无驱动(effect)) return effect;
      const 原型定义 = BATTLE_PROTOTYPE_REGISTRY[原型名] || {};
      const 字段列表 = Array.isArray(原型定义?.允许字段) ? 原型定义.允许字段 : [];
      if (字段列表.includes('驱动属性') && 字段列表.includes('影响方向')) {
        if (Object.prototype.hasOwnProperty.call(effect, '驱动属性') && String(effect?.驱动属性 || '').trim() === '无') return effect;
        if (!String(effect?.驱动属性 || '').trim() || !String(effect?.影响方向 || '').trim()) {
          if (原型名 === '复制执行' && !/属性|全部/.test(String(effect?.复制类型 || '').trim())) return effect;
          const 结果 = deepClonePlain(effect);
          结果.驱动属性 = 原型名 === '状态施加'
            ? 读取状态施加默认驱动属性_战斗(结果)
            : 原型名 === '状态移除' || 原型名 === '时光回溯' || 原型名 === '复制执行'
              ? '精神力上限'
              : '魂力上限';
          const 方向选项 = Array.isArray(原型定义?.字段定义?.影响方向?.选项) ? 原型定义.字段定义.影响方向.选项 : [];
          结果.影响方向 = 选择战斗原型默认影响方向(原型名, 结果, 方向选项[0] || '效果强度');
          effect = 结果;
        }
      }
      if (!String(effect?.驱动属性 || '').trim() || !String(effect?.影响方向 || '').trim()) return effect;
      const 攻方 = context?.造物产出者属性 || context?.actor || context?.caster || context?.attacker;
      const 守方 = context?.target || context?.defender;
      if (!攻方 || typeof 攻方 !== 'object' || !守方 || typeof 守方 !== 'object') return effect;
      const 结果 = deepClonePlain(effect);
      const 计算层效果 = 结果.计算层效果 && typeof 结果.计算层效果 === 'object'
        ? deepClonePlain(结果.计算层效果)
        : {};
      const 攻方终值 = context?.造物产出者属性
        ? 攻方
        : context?.attackerFinalStat || 攻方.final || buildCombatFinalStats(攻方);
      原型驱动缩放(
        结果,
        计算层效果,
        结果,
        攻方,
        攻方终值,
        守方,
        context?.defenderFinalStat || 守方.final || buildCombatFinalStats(守方),
      );
      if (Object.keys(计算层效果).length) 结果.计算层效果 = 计算层效果;
      return 结果;
    }

    function mergeRuntimePayloadToState(payload, pState) {
      if (!payload || typeof payload !== 'object') return;
      const calc = pState.计算层效果 || (pState.计算层效果 = createEmptyCombatEffectMap());
      const stateMods = pState.面板修改比例 || (pState.面板修改比例 = {});
      const statMods = payload.面板修改比例 || payload.面板修改 || {};
      const stateDeltas = pState.面板固定修正 || (pState.面板固定修正 = {});
      const statDeltas = payload.面板固定修正 || payload.面板固定 || {};
      ['str', 'def', 'agi', 'vit_max', 'men_max', 'sp_max'].forEach(k => {
        if (statMods[k] !== undefined) stateMods[k] = statMods[k];
        if (statDeltas[k] !== undefined) stateDeltas[k] = Number(stateDeltas[k] || 0) + Number(statDeltas[k] || 0);
      });
      [
        'skip_turn',
        'cannot_react',
        'dot_damage',
        'dot_damage_ratio',
        'silence',
        'disarm',
        'blind',
        'invincible',
        'skill_seal',
        '探查屏蔽',
        'counter_attack_ratio',
        'damage_reflect_ratio',
        'damage_transfer_ratio',
        'damage_transfer_target',
        'damage_absorb_ratio',
        '吸收来源',
        '吸收资源',
        '吸收转化效果',
        '伤害吸收增幅上限',
        'damage_share_ratio',
        'cost_share_ratio',
        'damage_reduction',
        'block_count',
        'super_armor',
        'death_save_count',
        'revive_count',
        'revive_heal_ratio',
        'armor_pen',
        'reaction_bonus',
        'reaction_penalty',
        'attacker_speed_bonus',
        'cast_speed_bonus',
        'cast_speed_penalty',
        'hit_bonus',
        'hit_penalty',
        'dodge_bonus',
        'dodge_penalty',
        'lock_level',
        'interrupt_bonus',
        'final_damage_mult',
        'received_damage_mult',
        'defense_strip',
        'spirit_resist_strip',
        'final_damage_bonus',
        'final_heal_mult',
        'final_heal_bonus',
        'shield_gain_mult',
        'shield_gain_bonus',
        'skill_effect_mult',
        'vit_gain_ratio',
        'sp_gain_ratio',
        'men_gain_ratio',
        'heal_block_ratio',
        'cost_ratio',
        'cost_delta',
        'cost_delta_ratio',
        'windup_ratio',
        'windup_delta',
        'random_target_rate',
        'heal_inversion_ratio',
        'stealth_level',
        'min_hp_floor',
        'hot_heal_ratio',
        'damage_share_count',
        'cost_share_count',
        'invincible_tier_threshold',
        '每日触发次数上限',
        'bonus_true_damage_ratio',
        'element_seal_ratio',
        'misfortune_check_rate',
        'misfortune_backlash_ratio',
        '判断干扰强度',
        '索敌干扰强度',
        'action_lock_rounds',
        'interrupt_window',
        'multi_hit_count',
        'segment_damage_ratio',
      ].forEach(k => {
        if (payload[k] !== undefined) calc[k] = payload[k];
      });
    }

    function 限制战斗倍率(数值 = 1, 下限 = 0.05, 上限 = 1.8) {
      const 数值结果 = Number(数值);
      if (!Number.isFinite(数值结果)) return 1;
      return Math.max(下限, Math.min(上限, 数值结果));
    }

    function 读取战斗资源当前值(单位 = {}, 最终属性 = {}, 资源键 = 'sp') {
      const 属性 = 单位?.属性 && typeof 单位.属性 === 'object' ? 单位.属性 : {};
      const 当前字段 = 资源键 === 'men'
        ? ['men', '精神力']
        : 资源键 === 'vit'
          ? ['vit', 'sta', '体力']
          : ['sp', '魂力'];
      for (const 字段 of 当前字段) {
        const 候选 = 最终属性?.[字段] ?? 单位?.[字段] ?? 属性?.[字段];
        const 数值 = Number(候选);
        if (Number.isFinite(数值)) return Math.max(0, 数值);
      }
      return 0;
    }

    function 读取战斗资源上限值(单位 = {}, 最终属性 = {}, 资源键 = 'sp') {
      const 属性 = 单位?.属性 && typeof 单位.属性 === 'object' ? 单位.属性 : {};
      const 上限字段 = 资源键 === 'men'
        ? ['men_max', '精神力上限']
        : 资源键 === 'vit'
          ? ['vit_max', 'sta_max', '体力上限']
          : ['sp_max', '魂力上限'];
      for (const 字段 of 上限字段) {
        const 候选 = 最终属性?.[字段] ?? 单位?.[字段] ?? 属性?.[字段];
        const 数值 = Number(候选);
        if (Number.isFinite(数值) && 数值 > 0) return 数值;
      }
      return 1;
    }

    function 计算当前资源压制倍率(攻击方 = {}, 攻击方最终 = {}, 防御方 = {}, 防御方最终 = {}, 资源键 = 'sp', 选项 = {}) {
      const 攻方当前 = 读取战斗资源当前值(攻击方, 攻击方最终, 资源键);
      const 守方当前 = 读取战斗资源当前值(防御方, 防御方最终, 资源键);
      const 攻方上限 = 读取战斗资源上限值(攻击方, 攻击方最终, 资源键);
      const 攻方比例 = Math.max(0, Math.min(1, 攻方当前 / Math.max(1, 攻方上限)));
      const 当前压制 = 攻方当前 / Math.max(1, 守方当前);
      const 当前柔化 = Math.pow(Math.max(0.01, 当前压制), Number(选项.压制指数 || 0.45));
      const 余量修正 = 0.45 + 0.55 * 攻方比例;
      const 下限 = Number(选项.下限 ?? 0.35);
      const 上限 = Number(选项.上限 ?? 1.85);
      return 限制战斗倍率(当前柔化 * 余量修正, 下限, 上限);
    }

    function 计算魂力机制缩放系数(攻击方 = {}, 攻击方最终 = {}, 防御方 = {}, 防御方最终 = {}, 基础 = 1, 选项 = {}) {
      return 限制战斗倍率(
        Number(基础 || 1) * 计算当前资源压制倍率(攻击方, 攻击方最终, 防御方, 防御方最终, 'sp', 选项),
        Number(选项.下限 ?? 0.35),
        Number(选项.上限 ?? 1.85),
      );
    }

    function 计算精神机制缩放系数(攻击方 = {}, 攻击方最终 = {}, 防御方 = {}, 防御方最终 = {}, 基础 = 1, 选项 = {}) {
      const 临时掌控 = 限制战斗倍率(Number(攻击方最终?.mastery_ratio || 攻击方?.mastery_ratio || 1), 0.85, 1.15);
      return 限制战斗倍率(
        Number(基础 || 1) * 计算当前资源压制倍率(攻击方, 攻击方最终, 防御方, 防御方最终, 'men', 选项) * 临时掌控,
        Number(选项.下限 ?? 0.2),
        Number(选项.上限 ?? 1.8),
      );
    }

    function 计算规则机制缩放系数(攻击方 = {}, 攻击方最终 = {}, 防御方 = {}, 防御方最终 = {}, 基础 = 1) {
      return 计算精神机制缩放系数(攻击方, 攻击方最终, 防御方, 防御方最终, 基础, {
        下限: 0.2,
        上限: 1.8,
        压制指数: 0.42,
      });
    }

    function 计算精神伤害攻势值(单位 = {}, 最终属性 = {}) {
      const 当前精神力 = 读取战斗资源当前值(单位, 最终属性, 'men');
      const 精神上限 = 读取战斗资源上限值(单位, 最终属性, 'men');
      return Math.max(当前精神力, 精神上限 * 0.25);
    }

    function 计算物理伤害攻势值(单位 = {}, 最终属性 = {}, 伤害类型 = '') {
      const 力量 = Math.max(1, Number(最终属性?.str || 单位?.str || 1));
      if (规范化战斗伤害类型(伤害类型) !== '远程攻击') return 力量;
      const 敏捷折算 = Math.max(0, Number(最终属性?.agi || 单位?.agi || 0)) * 0.55;
      return Math.max(1, 力量 * 0.9, 敏捷折算);
    }

    const 战斗伤害类型列表 = Object.freeze(['近身攻击', '远程攻击', '精神攻击', '真实攻击']);

    function 规范化战斗伤害类型(伤害类型 = '', 技能 = {}) {
      const 文本 = String(伤害类型 || '').trim();
      if (战斗伤害类型列表.includes(文本)) return 文本;
      const 技能文本 = `${文本} ${技能?.name || ''} ${技能?.魂技名 || ''} ${技能?.技能名称 || ''} ${技能?.效果描述 || ''}`;
      if (/真实|规则|法则|无视|穿透本源|毁灭|时光/.test(技能文本)) return '真实攻击';
      if (/精神|识海|幻境|灵魂|眩晕|混乱|封技|恐惧/.test(技能文本)) return '精神攻击';
      if (/远程|射线|光束|飞刃|投掷|炮|波|箭|弹|雷|电|冰|火|光|暗|星|空间|元素|爆炸/.test(技能文本)) return '远程攻击';
      return '近身攻击';
    }

    function 读取战斗伤害类型绑定(伤害类型 = '', 技能 = {}) {
      const 类型 = 规范化战斗伤害类型(伤害类型, 技能);
      return {
        伤害类型: 类型,
        结算标签: 类型 === '真实攻击' ? '真实伤害' : '标准伤害',
        抗性类型: 类型 === '真实攻击' ? '真实无视' : 类型 === '精神攻击' ? '精神抗性' : 类型 === '远程攻击' ? '元素抗性' : '物理抗性',
      };
    }

    const 战斗伤害是近身攻击 = 伤害类型 => 规范化战斗伤害类型(伤害类型) === '近身攻击';
    const 战斗伤害是远程攻击 = 伤害类型 => 规范化战斗伤害类型(伤害类型) === '远程攻击';
    const 战斗伤害是精神攻击 = 伤害类型 => 规范化战斗伤害类型(伤害类型) === '精神攻击';
    const 战斗伤害是真实攻击 = 伤害类型 => 规范化战斗伤害类型(伤害类型) === '真实攻击';

    const 紫极魔瞳防守境界集合_战斗 = new Set(['芥子', '浩瀚']);

    function 读取战斗单位功法表_战斗(单位 = {}) {
      if (单位?.功法 && typeof 单位.功法 === 'object' && !Array.isArray(单位.功法)) return 单位.功法;
      if (单位?.角色数据?.功法 && typeof 单位.角色数据.功法 === 'object' && !Array.isArray(单位.角色数据.功法)) return 单位.角色数据.功法;
      if (单位?.raw?.功法 && typeof 单位.raw.功法 === 'object' && !Array.isArray(单位.raw.功法)) return 单位.raw.功法;
      const 角色名 = String(单位?.name || 单位?.名称 || '').trim();
      if (!角色名) return {};
      const 功法表 = window.BattleUIBridge?.getMVU?.(`char.${角色名}.功法`);
      return 功法表 && typeof 功法表 === 'object' && !Array.isArray(功法表) ? 功法表 : {};
    }

    function 掌握唐门功法_战斗(单位 = {}, 功法名 = '') {
      const 名称 = String(功法名 || '').trim();
      return !!名称 && !!读取战斗单位功法表_战斗(单位)[名称];
    }

    function 读取功法记录_战斗(单位 = {}, 功法名 = '') {
      const 名称 = String(功法名 || '').trim();
      const 记录 = 读取战斗单位功法表_战斗(单位)[名称];
      return 记录 && typeof 记录 === 'object' && !Array.isArray(记录) ? 记录 : {};
    }

    function 判断精神力驱动攻击_战斗(效果 = {}, 技能 = {}) {
      const 文本 = [
        效果?.伤害类型,
        效果?.驱动属性,
        效果?.状态,
        效果?.结算,
        技能?.魂技名,
        技能?.name,
        技能?.效果描述,
      ].map(值 => String(值 || '').trim()).join('|');
      return /精神攻击|精神力|精神控制|精神压制|识海|幻境|眩晕|混乱|封技|恐惧/.test(文本);
    }

    function 计算紫极魔瞳防守精神攻势值_战斗(单位 = {}, 最终属性 = {}, 效果 = {}, 技能 = {}) {
      const 基础 = 计算精神伤害攻势值(单位, 最终属性);
      const 记录 = 读取功法记录_战斗(单位, '紫极魔瞳');
      return 紫极魔瞳防守境界集合_战斗.has(String(记录.境界 || '').trim()) && 判断精神力驱动攻击_战斗(效果, 技能)
        ? 基础 * 1.3
        : 基础;
    }

    function 读取对应等级(effect = {}) {
      const 等级 = Math.round(Number(effect?.对应等级 || 0));
      if (!Number.isFinite(等级) || 等级 <= 0) return 0;
      return Math.max(1, Math.min(180, 等级));
    }

    function 当前效果来自物品使用(context = {}) {
      return !!String(context?.skill?.__物品名 || context?.action?.物品名 || context?.action?.skill?.__物品名 || '').trim();
    }

    function 读取物品非伤害效果目标(effect = {}, context = {}) {
      const 目标 = String(effect?.目标 || '').trim();
      if (!目标 || /自身|召唤物/.test(目标)) return context?.actor || context?.caster || context?.attacker || {};
      return context?.target || context?.defender || {};
    }

    function 读取物品效果目标等级(目标 = {}) {
      const 候选列表 = [
        目标?.final?.lv,
        目标?.lv,
        目标?.等级,
        目标?.对标等级,
        目标?.属性?.对标等级,
        目标?.属性?.等级,
      ];
      for (const 候选 of 候选列表) {
        const 等级 = Number(候选);
        if (Number.isFinite(等级)) return 等级;
      }
      const 文本 = [目标?.境界, 目标?.属性?.境界, 目标?.战力对标, 目标?.属性?.战力对标, 目标?.称号, 目标?.属性?.称号]
        .map(value => String(value || '').trim())
        .filter(Boolean)
        .join(' ');
      if (/准神/.test(文本)) return 99.5;
      if (/神级|百级|真神|一级神|二级神|三级神/.test(文本)) return 100;
      return 0;
    }

    function 物品非伤害效果超过对应等级(effect = {}, context = {}) {
      if (!当前效果来自物品使用(context)) return false;
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return false;
      if (String(effect?.原型 || '').trim() === '伤害结算') return false;
      const 等级 = 读取对应等级(effect);
      if (!(等级 > 0)) return false;
      return 读取物品效果目标等级(读取物品非伤害效果目标(effect, context)) > 等级;
    }

    function 构建对应等级数据(effect = {}) {
      const 等级 = 读取对应等级(effect);
      if (!(等级 > 0)) return null;
      const 读取基础属性 = typeof root.__LWCS_GET_BASE_STATS__ === 'function' ? root.__LWCS_GET_BASE_STATS__ : null;
      if (!读取基础属性) return null;
      const 基础属性 = 读取基础属性(等级) || {};
      const 最终属性 = {
        level: 等级,
        等级,
        str: Math.max(1, Number(基础属性.str || 1)),
        def: Math.max(1, Number(基础属性.def || 1)),
        agi: Math.max(1, Number(基础属性.agi || 1)),
        vit_max: Math.max(1, Number(基础属性.vit_max || 1)),
        sp_max: Math.max(0, Number(基础属性.sp_max || 0)),
        men_max: Math.max(1, Number(基础属性.men_max || 1)),
      };
      const 单位 = {
        name: `对应等级${等级}级`,
        level: 等级,
        等级,
        str: 最终属性.str,
        def: 最终属性.def,
        agi: 最终属性.agi,
        vit: 最终属性.vit_max,
        vit_max: 最终属性.vit_max,
        sp: 最终属性.sp_max,
        sp_max: 最终属性.sp_max,
        men: 最终属性.men_max,
        men_max: 最终属性.men_max,
        final: 最终属性,
      };
      return { 等级, 单位, 最终属性 };
    }

    function 读取技能启动消耗(skill = {}) {
      const rawCost = normalizeSkillData(skill).消耗 || getSkillCostText(skill);
      if (rawCost && typeof rawCost === 'object' && !Array.isArray(rawCost)) {
        return formatCostObjectToString(rawCost.启动 || rawCost.upfront || rawCost);
      }
      return String(rawCost || '无').split(/\s+维持[:：]/)[0].trim() || '无';
    }

    function 读取技能魂环位_战斗(skill = {}, 攻击方 = {}) {
      const 路径 = Array.isArray(skill?.__魂环路径) ? skill.__魂环路径 : [];
      const 命中 = 路径.map(项 => String(项 || '').match(/第(\d+)魂环/)).find(Boolean);
      if (命中) return Math.max(1, Math.floor(Number(命中[1] || 1)));
      const 名称 = String(skill?.魂技名 || skill?.name || '').trim();
      const 名称命中 = 名称.match(/第(\d+)魂技/);
      if (名称命中) return Math.max(1, Math.floor(Number(名称命中[1] || 1)));
      return Math.max(1, Math.min(9, Math.ceil(Math.max(1, Number(攻击方?.等级 || 攻击方?.level || 1)) / 10)));
    }

    function 读取战斗伤害固定百分比消耗(skill = {}, 攻击方 = {}) {
      const effects = getSkillEffects(skill, { actor: 攻击方, caster: 攻击方, attacker: 攻击方, 主原型成立: true, hit: true, 命中: true });
      if (!effects.some(effect => String(effect?.原型 || '').trim() === '伤害结算')) return 0;
      const 魂环位 = 读取技能魂环位_战斗(skill, 攻击方);
      if (魂环位 === 7) return 0;
      return Math.max(0, Math.min(0.27, 魂环位 * 0.03));
    }

    function 普通魂技含非法百分比消耗(action = {}, skill = {}, 攻击方 = {}) {
      if (!skill || typeof skill !== 'object') return false;
      const 动作类型 = String(action?.action_type || action?.type || '').trim();
      if (动作类型 !== '释放魂技') return false;
      const 明确魂技来源 =
        !!String(action?.category || action?.source_detail || action?.来源类别 || action?.来源明细 || '').trim() ||
        !!String(skill?.__战斗来源类别 || skill?.__战斗来源明细 || skill?.__魂技槽位 || '').trim() ||
        (Array.isArray(skill?.__魂环路径) && skill.__魂环路径.length > 0);
      if (!明确魂技来源) return false;
      const 来源类别 = 读取战斗动作来源上下文(action, skill, getBattleSkillSourceCategory(skill)).来源类别;
      if (!['魂技', '自创魂技'].includes(来源类别)) return false;
      if (来源类别 === '魂技' && 读取技能魂环位_战斗(skill, 攻击方) === 7) return false;
      const 标识 = [
        action?.action_type,
        action?.type,
        skill?.name,
        skill?.魂技名,
        读取战斗来源类别上下文(skill, 来源类别).来源明细,
      ].map(value => String(value || '').trim()).filter(Boolean).join(' ');
      if (/武魂融合技|武魂真身|真身/.test(标识)) return false;
      const 启动消耗 = 读取技能启动消耗(skill);
      return /%/.test(String(启动消耗 || ''));
    }

    function 检查普通魂技百分比启动消耗(skill = {}, 攻击方 = {}, action = {}) {
      if (!普通魂技含非法百分比消耗({ action_type: '释放魂技', ...action }, skill, 攻击方)) {
        return { 可用: true, 原因: '' };
      }
      return {
        可用: false,
        原因: '普通魂技启动消耗仍为百分比，请先重新生成固定数值消耗',
      };
    }

    function 计算伤害消耗加成系数(skill = {}, 攻击方 = {}) {
      const 承载消耗文本 = String(skill?.__维持释放伤害承载消耗 || '').trim();
      const 消耗 = parseSkillCostForChar({ ...skill, 消耗: 读取技能启动消耗(skill) }, 攻击方, {
        actor: 攻击方,
        caster: 攻击方,
        attacker: 攻击方,
        skill,
        当前行动: String(skill?.name || skill?.魂技名 || '释放魂技').trim(),
      });
      if (承载消耗文本) {
        const 维持消耗 = parseSkillCostForChar({ ...skill, 消耗: 承载消耗文本 }, 攻击方, {
          actor: 攻击方,
          caster: 攻击方,
          attacker: 攻击方,
          skill,
          当前行动: String(skill?.name || skill?.魂技名 || '维持释放').trim(),
        });
        消耗.reqSp = 维持消耗.reqSp;
        消耗.reqVit = 维持消耗.reqVit;
        消耗.reqMen = 维持消耗.reqMen;
      }
      const 魂力比例 = Math.max(0, Number(消耗.reqSp || 0)) / Math.max(1, 读取战斗资源上限值(攻击方, 攻击方.final || {}, 'sp'));
      const 体力比例 = Math.max(0, Number(消耗.reqVit || 0)) / Math.max(1, 读取战斗资源上限值(攻击方, 攻击方.final || {}, 'vit'));
      const 精神比例 = Math.max(0, Number(消耗.reqMen || 0)) / Math.max(1, 读取战斗资源上限值(攻击方, 攻击方.final || {}, 'men'));
      const 承载比例 = 魂力比例 + 体力比例 + 精神比例;
      const 固定比例 = 读取战斗伤害固定百分比消耗(skill, 攻击方);
      if (!(固定比例 > 0) || 承载比例 <= 固定比例) return 1;
      return Number((承载比例 / 固定比例).toFixed(4));
    }

    const BATTLE_MECHANISM_CONSUMERS = Object.freeze({
      stealth(ctx) {
        ctx.directPayload.stealth_level = Number(ctx.effect.隐蔽度 || ctx.effect.stealth_level || 0);
        ctx.directPayload.dodge_bonus = Number(ctx.effect.dodge_bonus || 0);
        ctx.directPayload.reaction_bonus = Number(ctx.effect.reaction_bonus || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '隐身', ['隐身']);
      },
      reveal(ctx) {
        ctx.directPayload.hit_bonus = Number(ctx.effect.hit_bonus || 0);
        ctx.directPayload.lock_level = Number(ctx.effect.lock_level || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '破隐', ['破隐']);
      },
      disarm(ctx) {
        ctx.directPayload.disarm = true;
        ctx.ensureStateShell('缴械', ['缴械']);
      },
      silence(ctx) {
        ctx.directPayload.silence = true;
        ctx.ensureStateShell('沉默', ['沉默']);
      },
      slow(ctx) {
        ctx.directPayload.面板修改比例 = { agi: ctx.effect.agi_ratio || 0.8 };
        ctx.ensureStateShell('减速', ['减速']);
      },
      blind(ctx) {
        ctx.directPayload.blind = true;
        ctx.ensureStateShell('致盲', ['致盲']);
      },
      mechanism_suppress(ctx) {
        const state = ctx.state || {};
        const 缩放 = 计算规则机制缩放系数(ctx.attacker, ctx.attackerFinalStat, ctx.defender, ctx.defenderFinalStat, 1);
        const 抹消对象 = 规范化战斗机制抹消对象(ctx.effect?.抹消对象);
        state.抹消规则 = [{ 抹消对象, 持续回合: Math.max(1, Number(ctx.effect?.持续回合 || 1)) }];
        ctx.ensureStateShell(ctx.effect?.状态名称 || '机制抹消', ['机制抹消']);
        if (!(Number(state.持续回合 || 0) > 0)) state.持续回合 = Math.max(1, Number(ctx.effect?.持续回合 || 1));
        if (Number(state.持续回合 || 0) > 0) state.持续回合 = Math.max(1, Math.round(Number(state.持续回合 || 1) * 缩放));
      },
      power_amplify(ctx) {
        const 缩放 = 计算魂力机制缩放系数(ctx.attacker, ctx.attackerFinalStat, ctx.defender, ctx.defenderFinalStat, 1, {
          下限: 0.45,
          上限: 1.65,
          压制指数: 0.38,
        });
        const 基础倍率 = Number(ctx.effect?.威力倍率 || ctx.effect?.final_damage_mult || 1.15);
        ctx.directPayload.final_damage_mult = Number((1 + (基础倍率 - 1) * 缩放).toFixed(4));
        ctx.ensureStateShell(ctx.effect?.状态名称 || '威力增幅', ['威力增幅']);
      },
      element_seal(ctx) {
        const 缩放 = 计算规则机制缩放系数(ctx.attacker, ctx.attackerFinalStat, ctx.defender, ctx.defenderFinalStat, 1);
        const 比例 = Math.max(0, Math.min(1, Number(ctx.effect?.封禁强度 || ctx.effect?.element_seal_ratio || 0.3) * 缩放));
        ctx.directPayload.element_seal_ratio = 比例;
        ctx.directPayload.final_damage_mult = Number(ctx.effect?.final_damage_mult || Math.max(0.2, 1 - 比例));
        ctx.directPayload.cost_ratio = Number(ctx.effect?.cost_ratio || 1 + 比例 * 0.8);
        ctx.directPayload.windup_ratio = Number(ctx.effect?.windup_ratio || 1 + 比例 * 0.6);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '元素封禁', ['元素封禁']);
      },
      rule_rewrite(ctx) {
        ctx.directPayload.__规则改写已转运行态 = true;
      },
      luck_interference(ctx) {
        const 缩放 = 计算规则机制缩放系数(ctx.attacker, ctx.attackerFinalStat, ctx.defender, ctx.defenderFinalStat, 1);
        ctx.directPayload.判断干扰强度 = Number((Math.abs(读取战斗数值正负(ctx.effect?.数值 || '+10%')) * 缩放).toFixed(4));
        ctx.ensureStateShell(ctx.effect?.状态名称 || '气运干涉', ['气运干涉']);
      },
      time_rewind(ctx) {
        原型驱动缩放(ctx.effect, {}, ctx.effect, ctx.attacker, ctx.attackerFinalStat, ctx.defender, ctx.defenderFinalStat);
        const 发动方式 = 解析时光回溯发动方式(ctx.effect);
        ctx.effect.发动方式 = 发动方式;
        if (发动方式 === '主动') {
          ctx.directPayload.reaction_penalty = Math.max(
            Number(ctx.directPayload.reaction_penalty || 0),
            Number(ctx.effect?.时光回溯反应压制值 || 时光回溯反应压制值),
          );
          return;
        }
        ctx.ensureStateShell(ctx.effect?.状态名称 || '时光回溯', ['时光回溯']);
        ctx.state.持续回合 = 1;
        ctx.state.发动方式 = '被动';
        ctx.state.时光回溯消耗文本 = String(getSkillCostText(ctx.skill || {}) || '无').trim() || '无';
        ctx.state.时光回溯成功率 = Math.max(0, Math.min(1, Number(ctx.effect?.时光回溯成功率 ?? 1)));
        ctx.state.时光回溯反应压制值 = Math.max(0, Math.min(0.95, Number(ctx.effect?.时光回溯反应压制值 || 时光回溯反应压制值)));
      },
      skill_effect_amplify(ctx) {
        const 缩放 = 计算魂力机制缩放系数(ctx.attacker, ctx.attackerFinalStat, ctx.defender, ctx.defenderFinalStat, 1, {
          下限: 0.45,
          上限: 1.65,
          压制指数: 0.38,
        });
        const 效果倍率 = Math.max(1, Number(ctx.effect?.效果倍率 || ctx.effect?.skill_effect_mult || 1));
        ctx.directPayload.skill_effect_mult = Number((1 + (效果倍率 - 1) * 缩放).toFixed(4));
        ctx.ensureStateShell(ctx.effect?.状态名称 || '技能效果增幅', ['技能效果增幅']);
      },
      soft_control(ctx) {
        ctx.directPayload.reaction_penalty = Number(ctx.effect.reaction_penalty || 0);
        ctx.directPayload.cast_speed_penalty = Number(ctx.effect.cast_speed_penalty || 0);
        ctx.directPayload.dodge_penalty = Number(ctx.effect.dodge_penalty || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '软控', ['软控']);
      },
      position_lock(ctx) {
        ctx.directPayload.reaction_penalty = Number(ctx.effect.reaction_penalty || 0);
        ctx.directPayload.dodge_penalty = Number(ctx.effect.dodge_penalty || 0);
        ctx.directPayload.lock_level = Number(ctx.effect.lock_level || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '位移限制', ['位移限制']);
      },
      self_shift(ctx) {
        ctx.directPayload.dodge_bonus = Number(ctx.effect.dodge_bonus || 0);
        ctx.directPayload.attacker_speed_bonus = Number(ctx.effect.attacker_speed_bonus || 0);
        ctx.directPayload.reaction_bonus = Number(ctx.effect.reaction_bonus || 0);
      },
      hostile_shift(ctx) {
        ctx.directPayload.dodge_penalty = Number(ctx.effect.dodge_penalty || 0);
        ctx.directPayload.reaction_penalty = Number(ctx.effect.reaction_penalty || 0);
        ctx.directPayload.lock_level = Number(ctx.effect.lock_level || 0);
      },
      position_exchange(ctx) {
        ctx.directPayload.dodge_penalty = Number(ctx.effect.dodge_penalty || 0);
        ctx.directPayload.reaction_penalty = Number(ctx.effect.reaction_penalty || 0);
        ctx.directPayload.lock_level = Number(ctx.effect.lock_level || 0);
      },
      hard_lock(ctx) {
        ctx.directPayload.dodge_penalty = Number(ctx.effect.dodge_penalty || 0);
        ctx.directPayload.reaction_penalty = Number(ctx.effect.reaction_penalty || 0);
        ctx.directPayload.lock_level = Number(ctx.effect.lock_level || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '强制绑定/锁定', ['强制绑定/锁定']);
      },
      guard(ctx) {
        ctx.directPayload.damage_reduction = Number(ctx.effect.damage_reduction || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '护卫', ['护卫']);
      },
      invincible(ctx) {
        ctx.directPayload.invincible = true;
        ctx.directPayload.super_armor = true;
        ctx.directPayload.damage_reduction = Math.max(Number(ctx.directPayload.damage_reduction || 0), Number(ctx.effect.damage_reduction || 0));
        ctx.directPayload.invincible_tier_threshold = Number(ctx.effect.免疫等级阈值 || ctx.effect.invincible_tier_threshold || 100);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '无敌金身', ['无敌金身']);
      },
      damage_reflect(ctx) {
        ctx.directPayload.damage_reflect_ratio = Number(ctx.effect.反射比例 || ctx.effect.damage_reflect_ratio || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '伤害反射', ['伤害反射']);
      },
      damage_transfer(ctx) {
        ctx.directPayload.damage_transfer_ratio = Number(ctx.effect.转移比例 || ctx.effect.damage_transfer_ratio || 0);
        ctx.directPayload.damage_transfer_target = String(ctx.effect.转移对象 || ctx.effect.damage_transfer_target || '攻击者').trim() || '攻击者';
        ctx.ensureStateShell(ctx.effect?.状态名称 || '伤害转移', ['伤害转移']);
      },
      damage_share(ctx) {
        ctx.directPayload.damage_share_ratio = Number(ctx.effect.分摊比例 || ctx.effect.damage_share_ratio || 0);
        ctx.directPayload.damage_share_count = Math.max(1, Number(ctx.effect.分摊人数 || ctx.effect.数量 || ctx.effect.damage_share_count || 1));
        ctx.ensureStateShell(ctx.effect?.状态名称 || '伤害分摊', ['伤害分摊']);
      },
      cost_share(ctx) {
        ctx.directPayload.cost_share_ratio = Number(ctx.effect.分摊比例 || ctx.effect.cost_share_ratio || 0);
        ctx.directPayload.cost_share_count = Math.max(1, Number(ctx.effect.分摊人数 || ctx.effect.数量 || ctx.effect.cost_share_count || 1));
        ctx.ensureStateShell(ctx.effect?.状态名称 || '消耗分摊', ['消耗分摊']);
      },
      skill_seal(ctx) {
        ctx.directPayload.skill_seal = true;
        ctx.ensureStateShell(ctx.effect?.状态名称 || '封技', ['封技']);
      },
      heal_inversion(ctx) {
        ctx.directPayload.heal_inversion_ratio = Number(ctx.effect.反转比例 || ctx.effect.heal_inversion_ratio || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '治疗反转', ['治疗反转']);
      },
      target_lock(ctx) {
        ctx.directPayload.hit_bonus = Number(ctx.effect.hit_bonus || 0);
        ctx.directPayload.lock_level = Number(ctx.effect.lock_level || 0);
        ctx.ensureStateShell('目标锁定', ['目标锁定']);
      },
      sense_block(ctx) {
        ctx.directPayload.探查屏蔽 = true;
        ctx.ensureStateShell(ctx.effect?.状态名称 || '探查屏蔽', ['探查屏蔽']);
      },
      expose_weakness(ctx) {
        const 结算 = String(ctx.effect?.结算 || '').trim();
        if (结算 === '防御剥夺') {
          ctx.directPayload.defense_strip = Number(ctx.effect?.计算层效果?.defense_strip ?? ctx.effect.defense_strip ?? 0.2);
        } else if (结算 === '精神抗性剥夺') {
          ctx.directPayload.spirit_resist_strip = Number(ctx.effect?.计算层效果?.spirit_resist_strip ?? ctx.effect.spirit_resist_strip ?? 0.2);
        } else {
          ctx.directPayload.defense_strip = Number(ctx.effect?.计算层效果?.defense_strip ?? ctx.effect.defense_strip ?? 0.2);
          ctx.directPayload.spirit_resist_strip = Number(ctx.effect?.计算层效果?.spirit_resist_strip ?? ctx.effect.spirit_resist_strip ?? 0.2);
        }
        ctx.ensureStateShell('标记弱点', ['标记弱点']);
      },
      pursuit_mark(ctx) {
        ctx.directPayload.attacker_speed_bonus = Number(ctx.effect.attacker_speed_bonus || 0);
        ctx.directPayload.hit_bonus = Number(ctx.effect.hit_bonus || 0);
        ctx.directPayload.final_damage_mult = Number(ctx.effect.final_damage_mult || 1.0);
        ctx.ensureStateShell('追击', ['追击']);
      },
      pursuit_shift(ctx) {
        ctx.directPayload.attacker_speed_bonus = Number(ctx.effect.attacker_speed_bonus || 0);
        ctx.directPayload.hit_bonus = Number(ctx.effect.hit_bonus || 0);
        ctx.directPayload.final_damage_mult = Number(ctx.effect.final_damage_mult || 1.0);
      },
      disengage_shift(ctx) {
        ctx.directPayload.dodge_bonus = Number(ctx.effect.dodge_bonus || 0);
        ctx.directPayload.cast_speed_bonus = Number(ctx.effect.cast_speed_bonus || 0);
        ctx.directPayload.reaction_bonus = Number(ctx.effect.reaction_bonus || 0);
      },
      counter(ctx) {
        ctx.directPayload.counter_attack_ratio = Number(ctx.effect.反击倍率 || ctx.effect.counter_attack_ratio || 0);
        ctx.directPayload.damage_reduction = Number(ctx.effect.damage_reduction || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '反制', ['反制']);
      },
      on_hit_counter(ctx) {
        ctx.directPayload.counter_attack_ratio = ctx.effect.反击倍率 || 0.5;
        ctx.ensureStateShell('受击反击', ['反击']);
      },
      damage_reduce(ctx) {
        ctx.directPayload.damage_reduction = 0.15;
        ctx.ensureStateShell('承伤修正', ['承伤修正']);
      },
      block(ctx) {
        ctx.directPayload.block_count = ctx.effect.抵消次数 || 1;
        ctx.ensureStateShell('免伤', ['免伤']);
      },
      super_armor(ctx) {
        ctx.directPayload.super_armor = true;
        ctx.directPayload.damage_reduction = Number(ctx.effect.damage_reduction || 0);
        ctx.ensureStateShell('霸体', ['霸体']);
      },
      anti_heal(ctx) {
        ctx.directPayload.heal_block_ratio = Number(ctx.effect.heal_block_ratio || 0);
        ctx.ensureStateShell('禁疗', ['禁疗']);
      },
      shared_vision(ctx) {
        ctx.directPayload.reaction_bonus = Number(ctx.effect.reaction_bonus || 0);
        ctx.directPayload.hit_bonus = Number(ctx.effect.hit_bonus || 0);
        ctx.directPayload.lock_level = Number(ctx.effect.lock_level || 0);
        ctx.ensureStateShell('共享视野', ['共享视野']);
      },
      summon(ctx) {
        const count = Math.max(1, Number(ctx.effect.召唤数量 || ctx.effect.数量 || 1));
        const 继承比例 = Math.max(0, Math.min(1, Number(ctx.effect.继承属性比例 || 0)));
        const 召唤强度 = 继承比例 > 0 ? 继承比例 : Math.max(0.01, Number(ctx.effect.强度 || 1));
        const mode = String(ctx.effect.行动模式 || '').trim();
        ctx.directPayload.final_damage_mult = Math.max(
          Number(ctx.directPayload.final_damage_mult || 1),
          Number(ctx.effect?.计算层效果?.final_damage_mult || Math.min(1.5, 1 + 召唤强度 * 0.12 + count * 0.03)),
        );
        if (/护卫|承伤/.test(mode + String(ctx.effect.承伤规则 || ''))) {
          ctx.directPayload.damage_reduction = Math.max(
            Number(ctx.directPayload.damage_reduction || 0),
            Number(ctx.effect?.计算层效果?.damage_reduction || Math.min(0.3, 召唤强度 * 0.08)),
          );
        }
        ctx.ensureStateShell(ctx.effect?.状态名称 || ctx.effect?.召唤物名称 || '召唤物', ['召唤']);
      },
      clone(ctx) {
        const cloneType = String(ctx.effect.分身类型 || '').trim();
        const cloneCount = Math.max(1, Number(ctx.effect.分身数量 || 1));
        const stealth = Math.max(0, Math.min(1, Number(ctx.effect.隐蔽度 || 0)));
        const inheritRatio = Math.max(0, Math.min(1, Number(ctx.effect.实力继承比例 || 0)));
        if (cloneType === '精神力分身') {
          ctx.directPayload.reaction_bonus = Number(
            ctx.effect.reaction_bonus || Math.min(0.28, 0.04 + stealth * 0.16 + inheritRatio * 0.08),
          );
          ctx.directPayload.hit_bonus = Number(
            ctx.effect.hit_bonus || Math.min(0.3, 0.04 + inheritRatio * 0.15 + cloneCount * 0.03),
          );
          ctx.directPayload.lock_level = Number(
            ctx.effect.lock_level || Math.min(3, Math.max(1, Math.round(1 + inheritRatio * 1.2 + stealth * 0.8))),
          );
          ctx.directPayload.damage_reduction = Number(
            ctx.effect.damage_reduction || Math.min(0.18, 0.02 + stealth * 0.05 + cloneCount * 0.01),
          );
        } else {
          ctx.directPayload.dodge_bonus = Number(
            ctx.effect.dodge_bonus || Math.min(0.35, 0.05 + stealth * 0.18 + inheritRatio * 0.08 + cloneCount * 0.03),
          );
          ctx.directPayload.attacker_speed_bonus = Number(
            ctx.effect.attacker_speed_bonus || Math.min(0.24, 0.03 + inheritRatio * 0.12 + cloneCount * 0.02),
          );
          ctx.directPayload.damage_reduction = Number(
            ctx.effect.damage_reduction || Math.min(0.22, 0.02 + stealth * 0.08 + cloneCount * 0.015),
          );
          ctx.directPayload.final_damage_mult = Number(
            ctx.effect.final_damage_mult || Math.min(1.28, 1 + inheritRatio * 0.12 + Math.max(0, cloneCount - 1) * 0.04),
          );
        }
        ctx.ensureStateShell(ctx.effect?.状态名称 || ctx.effect?.分身类型 || '分身', ['分身']);
      },
      death_save(ctx) {
        ctx.directPayload.super_armor = true;
        ctx.directPayload.min_hp_floor = 1;
        ctx.directPayload.death_save_count = ctx.effect.触发次数 || 1;
        ctx.ensureStateShell('免死', ['免死']);
      },
      hard_control(ctx) {
        ctx.directPayload.skip_turn = true;
        ctx.directPayload.cannot_react = true;
        ctx.ensureStateShell(ctx.effect?.状态名称 || '硬控', ['硬控']);
      },
      cost_increase(ctx) {
        const ratio = Number(ctx.effect?.计算层效果?.cost_ratio || ctx.effect?.cost_ratio || 1);
        ctx.directPayload.cost_ratio = ratio > 0 ? ratio : 1;
        ctx.directPayload.cost_delta_ratio = Number(ctx.effect?.计算层效果?.cost_delta_ratio || ctx.effect?.cost_delta_ratio || 0);
        ctx.ensureStateShell(ctx.effect?.状态名称 || '消耗', ['消耗']);
      },
      cost_reduce(ctx) {
        const ratio = Number(ctx.effect?.计算层效果?.cost_ratio || ctx.effect?.cost_ratio || 1);
        ctx.directPayload.cost_ratio = ratio > 0 ? ratio : 1;
        ctx.directPayload.cost_delta_ratio = Number(ctx.effect?.计算层效果?.cost_delta_ratio || ctx.effect?.cost_delta_ratio || 0);
        if (ctx.effect?.计算层效果?.cost_delta !== undefined || ctx.effect?.cost_delta !== undefined) {
          ctx.directPayload.cost_delta = Number(ctx.effect?.计算层效果?.cost_delta ?? ctx.effect?.cost_delta ?? 0);
        }
        ctx.ensureStateShell(ctx.effect?.状态名称 || '消耗', ['消耗']);
      },
      windup_increase(ctx) {
        const ratio = Number(ctx.effect?.计算层效果?.windup_ratio || ctx.effect?.windup_ratio || 1);
        ctx.directPayload.windup_ratio = ratio > 0 ? ratio : 1;
        if (ctx.effect?.计算层效果?.windup_delta !== undefined || ctx.effect?.windup_delta !== undefined) {
          ctx.directPayload.windup_delta = Number(ctx.effect?.计算层效果?.windup_delta ?? ctx.effect?.windup_delta ?? 0);
        }
        ctx.ensureStateShell(ctx.effect?.状态名称 || '前摇', ['前摇']);
      },
      windup_reduce(ctx) {
        const ratio = Number(ctx.effect?.计算层效果?.windup_ratio || ctx.effect?.windup_ratio || 1);
        ctx.directPayload.windup_ratio = ratio > 0 ? ratio : 1;
        if (ctx.effect?.计算层效果?.windup_delta !== undefined || ctx.effect?.windup_delta !== undefined) {
          ctx.directPayload.windup_delta = Number(ctx.effect?.计算层效果?.windup_delta ?? ctx.effect?.windup_delta ?? 0);
        }
        ctx.ensureStateShell(ctx.effect?.状态名称 || '前摇', ['前摇']);
      },
    });

    function applyRuntimeMechanismEffects(skill, attacker, attackerFinalStat, defender, defenderFinalStat, pState, context = {}) {
      const effects = getSkillEffects(skill, context);
      effects.forEach(effect => {
        const 运行效果 = effect;
        const 原型名 = String(运行效果?.原型 || '').trim();
        if (原型名 === '时光回溯' && 解析时光回溯发动方式(运行效果) === '主动') return;
        if (Number(运行效果?.延迟回合 || 0) > 0 && 战斗原型允许延迟回合(原型名)) return;
        const mechanism = 原型名 || '';
        const targetKind = 推断战斗效果目标类型(运行效果, String(运行效果?.目标 || effect?.目标 || '敌方单体'));
        const 允许生成状态壳 = ['状态施加', '规则防御', '时窗修正', '决策干扰', '结算修正', '属性修正', '判定修正', '时光回溯'].includes(原型名);
          const ensureStateShell = (fallbackName, extraFlags = []) => {
            if (!允许生成状态壳) return;
            const nextName = String(fallbackName || mechanism || '无');
            if (!nextName || nextName === '无') return;
            if (!pState.状态名称 || pState.状态名称 === '无') {
            pState.状态名称 = nextName;
            const nextTarget = String(运行效果?.目标 || '').trim();
            if (nextTarget) pState.目标 = nextTarget;
            }
            const duration = Math.max(0, Number(运行效果?.持续回合 || effect?.持续回合 || 0));
            if (duration > 0) pState.持续回合 = Math.max(Number(pState.持续回合 || 0), duration);
            [
              '攻击段数',
              '结算标签',
              '抗性类型',
            ].forEach(字段名 => {
              if (运行效果?.[字段名] !== undefined && 运行效果?.[字段名] !== '') pState[字段名] = deepClonePlain(运行效果[字段名]);
            });
            pState.特殊机制标识 = mergeSpecialFlags(pState.特殊机制标识 || '无', extraFlags);
          };
          const directPayload = {};
          if (运行效果?.计算层效果 && typeof 运行效果.计算层效果 === 'object') {
            Object.assign(directPayload, 运行效果.计算层效果);
          }
          if (运行效果?.面板修改比例 && typeof 运行效果.面板修改比例 === 'object') {
            directPayload.面板修改比例 = { ...(directPayload.面板修改比例 || {}), ...运行效果.面板修改比例 };
          }
          if (运行效果?.面板固定修正 && typeof 运行效果.面板固定修正 === 'object') {
            directPayload.面板固定修正 = { ...(directPayload.面板固定修正 || {}), ...运行效果.面板固定修正 };
          }
          if (原型名 === '属性修正' && (directPayload.面板修改比例 || directPayload.面板固定修正)) {
            const 属性名 = String(运行效果?.属性 || '属性修正').trim() || '属性修正';
            ensureStateShell(`${属性名}修正`, [
              Object.values(directPayload.面板固定修正 || {}).some(value => Number(value || 0) < 0) ||
              Object.values(directPayload.面板修改比例 || {}).some(value => Number(value || 1) < 1)
                ? '属性削弱'
                : '属性增益',
            ]);
          }
          const 运行时消费器 = String(读取战斗原型运行消费器(运行效果) || '').trim();
          const runtimeConsumer = 运行时消费器 ? BATTLE_MECHANISM_CONSUMERS[运行时消费器] : null;
          if (mechanism === '属性变化') {
            const property = String(运行效果?.属性 || '').trim();
            const action = String(运行效果?.动作 || '').trim();
            const value = action === '加值' || action === '减值' ? 读取战斗效果数值(运行效果?.数值, 0) : 读取战斗属性倍率(运行效果);
            if (['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max'].includes(property) && Number.isFinite(value)) {
              if (action === '加值' || action === '减值') directPayload.面板固定修正 = { [property]: value };
              else directPayload.面板修改比例 = { [property]: value };
              ensureStateShell('属性变化', [action || '属性变化']);
            }
            if (property === '威力' && Number.isFinite(value)) {
              directPayload.final_damage_mult = value;
              ensureStateShell('威力变化', [action || '威力变化']);
            }
          }
          if (mechanism === '持续恢复') {
            const property = String(effect?.属性 || '').trim();
            const value = 读取战斗效果数值(effect?.数值, 0);
            if (property === 'vit') directPayload.vit_gain_ratio = value;
            if (property === 'sp') directPayload.sp_gain_ratio = value;
            if (property === 'men') directPayload.men_gain_ratio = value;
            ensureStateShell('持续恢复', ['持续恢复']);
          }
          if (mechanism === '掌控修正') {
            const value = 读取战斗属性倍率(effect);
            const action = String(effect?.动作 || '').trim();
            if (action === '倍率压制' || (!action && value < 1)) directPayload.hit_penalty = Math.max(0, 1 - value);
            else directPayload.hit_bonus = Math.max(0, value - 1);
            ensureStateShell('掌控修正', ['掌控修正']);
          }
          if (mechanism === '速度修正') {
            const value = 读取战斗属性倍率(effect);
            const action = String(effect?.动作 || '').trim();
            if (action === '倍率压制' || (!action && value < 1)) {
              directPayload.reaction_penalty = Math.max(0, 1 - value);
              directPayload.dodge_penalty = Math.max(0, 1 - value);
            } else {
              directPayload.reaction_bonus = Math.max(0, value - 1);
              directPayload.attacker_speed_bonus = Math.max(0, value - 1);
              directPayload.dodge_bonus = Math.max(0, Math.max(0, value - 1) * 0.75);
            }
            ensureStateShell('速度修正', ['速度修正']);
          }
          if (mechanism === '打断') directPayload.interrupt_bonus = effect.中断概率 || 1.0;
          if (mechanism === '感知干扰') {
            directPayload.hit_penalty = Number(effect.hit_penalty || 0);
            directPayload.reaction_penalty = Number(effect.reaction_penalty || 0);
            directPayload.cast_speed_penalty = Number(effect.cast_speed_penalty || 0);
            ensureStateShell('感知干扰', ['感知干扰']);
          }
          if (runtimeConsumer)
            runtimeConsumer({
              effect,
              mechanism,
              directPayload,
              ensureStateShell,
              state: pState,
              attacker,
              attackerFinalStat,
              defender,
              defenderFinalStat,
              skill,
            });
          if (mechanism === '嘲讽') ensureStateShell('嘲讽', ['嘲讽']);
          if (原型名 && 原型名 !== '时光回溯') {
            原型驱动缩放(effect, directPayload, 运行效果, attacker, attackerFinalStat, defender, defenderFinalStat);
            if (原型名 === '判定修正' && Number(运行效果?.持续回合 || effect?.持续回合 || 0) > 0) {
              const 判定名 = String(运行效果?.判定 || effect?.判定 || '判定').trim() || '判定';
              const 是削弱 = Object.keys(directPayload).some(key => /penalty$/.test(key) && Number(directPayload[key] || 0) > 0);
              ensureStateShell(`${判定名}判定修正`, [是削弱 ? '判定削弱' : '判定增益']);
            }
            if (原型名 === '状态施加') ensureStateShell(String(运行效果?.状态 || '').trim() || mechanism, [mechanism]);
          }
          if (Object.keys(directPayload).length > 0) {
            mergeRuntimePayloadToState(directPayload, pState);
          }
      });
    }

    function mapPrimaryRoleToSkillType(role) {
      const mapping = {
        输出: '输出',
        控制: '控制',
        防御: '防御',
        辅助: '辅助',
        特殊: '辅助',
      };
      return mapping[role] || '输出';
    }

    function mergeSpecialFlags(existing, additions = []) {
      const set = new Set(
        String(existing || '无')
          .split(/[\/、,，\s]+/)
          .filter(Boolean)
          .filter(flag => flag !== '无'),
      );
      (additions || []).forEach(flag => {
        if (flag && flag !== '无') set.add(flag);
      });
      return set.size > 0 ? Array.from(set).join('/') : '无';
    }

    function formatCostObjectToString(costObj) {
      if (!costObj || typeof costObj !== 'object') return '无';
      const buildPart = obj =>
        [
          obj?.魂力 ? `魂力:${obj.魂力}` : '',
          obj?.体力 ? `体力:${obj.体力}` : '',
          obj?.精神力 ? `精神力:${obj.精神力}` : '',
        ]
          .filter(Boolean)
          .join(' ') || '无';
      const upfront = buildPart(costObj.启动 || costObj.upfront || costObj);
      const sustain = buildPart(costObj.维持 || costObj.sustain || {});
      return sustain !== '无' ? `${upfront} 维持:${sustain}` : upfront;
    }

    function deepClone(data) {
      return data == null ? data : JSON.parse(JSON.stringify(data));
    }

    function 读取槽位序号_战斗(槽位名 = '', 默认值 = 1) {
      const 匹配 = String(槽位名 || '').match(/第(\d+)/);
      return Math.max(1, Math.floor(Number(匹配 ? 匹配[1] : 默认值) || 默认值 || 1));
    }

    function 是武魂槽位键_战斗(键 = '') {
      return /^第\d+武魂$/.test(String(键 || '').trim());
    }

    function 是魂灵槽位键_战斗(键 = '') {
      return /^第\d+魂灵$/.test(String(键 || '').trim());
    }

    function 是魂环槽位键_战斗(键 = '') {
      return /^第\d+魂环$/.test(String(键 || '').trim());
    }

    function 是魂技槽位键_战斗(键 = '') {
      return /^第\d+魂技(?:_2)?$/.test(String(键 || '').trim());
    }

    function 是气血魂环槽位键_战斗(键 = '') {
      return /^第\d+气血魂环$/.test(String(键 || '').trim());
    }

    function 是血脉魂技槽位键_战斗(键 = '') {
      return /^第\d+血脉魂技(?:_2)?$/.test(String(键 || '').trim());
    }

    function 取槽位条目_战斗(对象 = {}, 判断函数 = () => false) {
      return Object.entries(对象 || {}).filter(([键, 值]) => 判断函数(键) && 值 && typeof 值 === 'object' && !Array.isArray(值));
    }

    function 取角色武魂条目_战斗(角色 = {}) {
      return 取槽位条目_战斗(角色, 是武魂槽位键_战斗);
    }

    function 取武魂魂灵条目_战斗(武魂 = {}) {
      return 取槽位条目_战斗(武魂, 是魂灵槽位键_战斗);
    }

    function 取武魂直接魂环条目_战斗(武魂 = {}) {
      return 取槽位条目_战斗(武魂, 是魂环槽位键_战斗);
    }

    function 取魂灵魂环条目_战斗(魂灵 = {}) {
      return 取槽位条目_战斗(魂灵, 是魂环槽位键_战斗);
    }

    function 取魂环魂技条目_战斗(魂环 = {}) {
      return 取槽位条目_战斗(魂环, 是魂技槽位键_战斗);
    }

    function 按魂环路径读取当前角色魂技_战斗(角色 = {}, 魂环路径 = [], 魂技槽位 = '') {
      if (!角色 || typeof 角色 !== 'object') return null;
      if (!Array.isArray(魂环路径) || !魂环路径.length) return null;
      const 魂环 = 按路径读取对象_V1(角色, 魂环路径);
      if (!魂环 || typeof 魂环 !== 'object' || Array.isArray(魂环)) return null;
      const 槽位 = String(魂技槽位 || '').trim();
      if (槽位) {
        const 技能 = 魂环[槽位];
        return 技能 && typeof 技能 === 'object' && !Array.isArray(技能) ? { 技能, 槽位 } : null;
      }
      const [默认槽位, 默认技能] = 取魂环魂技条目_战斗(魂环)[0] || [];
      return 默认技能 ? { 技能: 默认技能, 槽位: 默认槽位 } : null;
    }

    function 取血脉气血魂环条目_战斗(血脉 = {}) {
      return 取槽位条目_战斗(血脉, 是气血魂环槽位键_战斗);
    }

    function 取气血魂环魂技条目_战斗(魂环 = {}) {
      return 取槽位条目_战斗(魂环, 是血脉魂技槽位键_战斗);
    }

    function formatBattleCultivationLevelText(value, fallback = '0') {
      const parsed = Number(value);
      if (Number.isFinite(parsed)) {
        if (Math.abs(parsed - 99.5) < 0.001) return '准神';
        return String(parsed);
      }
      const text = String(value ?? '').trim();
      return text || fallback;
    }

    function getSpiritNameList(char) {
      return 取角色武魂条目_战斗(char)
        .map(([, sp]) => sp)
        .map(sp => sp?.表象名称 || '')
        .filter(Boolean);
    }

    function 读取角色系别列表(char) {
      const 系别集合 = new Set();
      const 属性系别 = String(char?.type || char?.系别 || '').trim();
      if (属性系别) 系别集合.add(属性系别);
      取角色武魂条目_战斗(char).forEach(([, 武魂数据]) => {
        const 武魂系别 = String(武魂数据?.type || 武魂数据?.系别 || '').trim();
        if (武魂系别) 系别集合.add(武魂系别);
      });
      return Array.from(系别集合);
    }

    function 是否辅助系角色(char) {
      return 读取角色系别列表(char).includes('辅助系');
    }

    function 是否七九武魂角色(char) {
      return getSpiritNameList(char).some(名称 => /[七九]/.test(String(名称 || '')));
    }

    function isSpecialSupportMartialSoul(char) {
      return 是否辅助系角色(char) && 是否七九武魂角色(char);
    }

    function 是否七九辅助单体语义(char, skill) {
      return isSpecialSupportMartialSoul(char) && isSupportLikeSkill(skill);
    }

    function 战斗机甲可召唤(unit = {}) {
      const 机甲 = unit?.装备?.机甲;
      if (!机甲 || typeof 机甲 !== 'object') return false;
      const 等级 = String(机甲.等级 || '').trim();
      return !!等级 && 等级 !== '无' && 机甲.状态 !== '重创' && 机甲.装备状态 !== '已装备';
    }

    function 战斗机甲已装备(unit = {}) {
      const 机甲 = unit?.装备?.机甲;
      if (!机甲 || typeof 机甲 !== 'object') return false;
      const 等级 = String(机甲.等级 || '').trim();
      return !!等级 && 等级 !== '无' && 机甲.状态 !== '重创' && 机甲.装备状态 === '已装备';
    }

    function getSupportEffectScale(caster, target) {
      if (isSpecialSupportMartialSoul(caster)) return 1;
      const casterSp = Math.max(1, caster?.sp_max || 1);
      const targetSp = Math.max(1, target?.sp_max || 1);
      const ratio = casterSp / targetSp;
      if (!Number.isFinite(ratio) || ratio <= 0) return 1;
      if (ratio >= 1) return Math.min(1.35, 1 + (Math.sqrt(ratio) - 1) * 0.35);
      return Math.max(0.72, 0.72 + 0.28 * Math.sqrt(ratio));
    }

    function getSupportCostScale(caster, target) {
      if (isSpecialSupportMartialSoul(caster)) {
        const 施术者魂力 = Math.max(1, Number(caster?.sp_max || caster?.属性?.魂力上限 || 1));
        const 目标魂力 = Math.max(1, Number(target?.sp_max || target?.属性?.魂力上限 || 1));
        const 越级倍率 = 目标魂力 / 施术者魂力;
        if (!Number.isFinite(越级倍率) || 越级倍率 <= 1) return 1;
        return Math.min(8, Math.max(1, Math.pow(越级倍率, 0.65)));
      }
      const effectScale = getSupportEffectScale(caster, target);
      if (!(effectScale > 0)) return 1;
      if (effectScale >= 1) return Math.min(1.05, 1 + (effectScale - 1) * 0.2);
      return Math.max(0.78, 1 - (1 - effectScale) * 0.65);
    }

    function getSoulDriveScale(attacker, defender) {
      return 计算魂力机制缩放系数(attacker, attacker?.final || {}, defender, defender?.final || {}, 1, {
        下限: 0.35,
        上限: 1.85,
        压制指数: 0.45,
      });
    }

    function 计算定位伤害倍率(攻击方 = {}, 防御方 = {}, 伤害类型 = '') {
      const 攻方系别列表 = 读取角色系别列表(攻击方);
      const 守方系别列表 = 读取角色系别列表(防御方);
      const 类型 = 规范化战斗伤害类型(伤害类型);
      if (攻方系别列表.includes('强攻系') && 守方系别列表.some(系别 => ['辅助系', '治疗系', '食物系', '召唤系'].includes(系别))) {
        return 类型 === '近身攻击' || 类型 === '远程攻击' ? 1.9 : 1.45;
      }
      if (攻方系别列表.includes('强攻系') && 守方系别列表.includes('防御系')) return 0.92;
      return 1;
    }

    function getSpiritDriveScale(attacker, defender) {
      return 计算精神机制缩放系数(attacker, attacker?.final || {}, defender, defender?.final || {}, 1, {
        下限: 0.25,
        上限: 1.85,
        压制指数: 0.45,
      });
    }

    function 读取单位战斗效果列表(单位 = {}) {
      if (!单位?.状态效果 || typeof 单位.状态效果 !== 'object') return [];
      return Object.values(单位.状态效果)
        .map(状态 => 状态?.战斗效果 || {})
        .filter(战斗效果 => 战斗效果 && typeof 战斗效果 === 'object');
    }

    function 读取状态穿透比例(战斗效果列表 = []) {
      return 战斗效果列表.reduce((总和, 战斗效果) => {
        const 原始值 = Number(战斗效果?.armor_pen || 0);
        return 总和 + (Math.abs(原始值) <= 1 ? 原始值 * 100 : 原始值);
      }, 0);
    }

    function 计算有效穿透比例(技能穿透 = 0, 状态穿透 = 0) {
      const 原始穿透 = Math.max(0, Number(技能穿透 || 0) + Number(状态穿透 || 0));
      if (原始穿透 >= 100) return 100;
      if (原始穿透 <= 70) return 原始穿透;
      return Math.min(92, 70 + (原始穿透 - 70) * 0.35);
    }

    function 计算穿透后防御值(基础防御 = 1, 技能穿透 = 0, 状态穿透 = 0) {
      const 有效穿透 = 计算有效穿透比例(技能穿透, 状态穿透);
      return Math.max(1, Number(基础防御 || 1) * (1 - 有效穿透 / 100));
    }

    function 计算有效增加闪避(原始加值 = 0) {
      const 数值 = Number(原始加值 || 0);
      if (!(数值 > 0)) return 数值;
      if (数值 <= 0.25) return 数值;
      return Math.min(0.45, 0.25 + (数值 - 0.25) * 0.4);
    }

    function 获取施法消耗系数(单位 = {}) {
      const 战斗效果列表 = 读取单位战斗效果列表(单位);
      return 战斗效果列表.reduce((倍率, 战斗效果) => {
        const 显式倍率 = Number(战斗效果?.cost_ratio || 1);
        const 增量倍率 = 1 + Math.max(0, Number(战斗效果?.cost_delta_ratio || 0));
        return Math.max(倍率, Number.isFinite(显式倍率) && 显式倍率 > 0 ? 显式倍率 : 1, 增量倍率);
      }, 1);
    }

    function 获取施法消耗绝对修正(单位 = {}) {
      return 读取单位战斗效果列表(单位).reduce((总和, 战斗效果) => 总和 + Number(战斗效果?.cost_delta || 0), 0);
    }

    function 获取动作前摇系数(单位 = {}) {
      const 战斗效果列表 = 读取单位战斗效果列表(单位);
      const 速度加成 = 战斗效果列表.reduce((总和, 战斗效果) => 总和 + Number(战斗效果?.cast_speed_bonus || 0), 0);
      const 速度惩罚 = 战斗效果列表.reduce((总和, 战斗效果) => 总和 + Number(战斗效果?.cast_speed_penalty || 0), 0);
      const 直接倍率 = 战斗效果列表.reduce((倍率, 战斗效果) => {
        const 前摇系数 = Number(战斗效果?.windup_ratio || 1);
        return Number.isFinite(前摇系数) && 前摇系数 > 0 ? 倍率 * 前摇系数 : 倍率;
      }, 1);
      const 维持前摇系数 = Math.max(1, Number(单位?.__维持前摇系数 || 1));
      const 合成倍率 = 直接倍率 * 维持前摇系数 * (1 - 速度加成 + 速度惩罚);
      return Math.max(0.25, Math.min(3, 合成倍率));
    }

    function 获取动作前摇绝对修正(单位 = {}) {
      return 读取单位战斗效果列表(单位).reduce((总和, 战斗效果) => 总和 + Number(战斗效果?.windup_delta || 0), 0);
    }

    function 动作免除经验前摇折扣(动作 = {}) {
      const 技能 = 动作?.skill && typeof 动作.skill === 'object' ? 动作.skill : {};
      const 动作类型 = String(动作?.action_type || 动作?.type || 技能?.name || '').trim();
      const 来源类别 = 读取战斗动作来源上下文(动作, 技能, '').来源类别;
      return (
        /领域/.test(动作类型) ||
        动作类型 === '穿戴装备' ||
        /斗铠|机甲/.test(动作类型) ||
        动作类型 === '武魂融合技' ||
        来源类别 === '武魂融合技'
      );
    }

    function 读取图鉴收集奖励档位() {
      const 图鉴数据 = getMvuValue('world.图鉴', {}) || {};
      const 数量 = 图鉴数据 && typeof 图鉴数据 === 'object' ? Object.keys(图鉴数据).length : 0;
      return Math.max(0, Math.min(6, Math.floor(数量 / 25)));
    }

    function 单位获得图鉴收集奖励(单位 = {}, 战斗数据 = {}) {
      if (!单位 || typeof 单位 !== 'object') return false;
      const 阵营 = 读取规划单位阵营(单位, 战斗数据 || {});
      if (阵营) return 阵营 === '玩家';
      return 是玩家操控角色(单位, 战斗数据 || {});
    }

    function 读取图鉴收集反应加成(单位 = {}, 战斗数据 = {}) {
      return 单位获得图鉴收集奖励(单位, 战斗数据) ? 读取图鉴收集奖励档位() * 0.01 : 0;
    }

    function 读取图鉴收集前摇折扣(单位 = {}, 战斗数据 = {}) {
      return 单位获得图鉴收集奖励(单位, 战斗数据) ? (读取图鉴收集奖励档位() / 6) * 0.04 : 0;
    }

    function 计算行为经验前摇折扣(单位 = {}, 目标 = null, 战斗数据 = null) {
      if (!单位 || !目标) return 0;
      const 战斗经验 = 计算行为战斗经验(单位, 目标, 战斗数据 || {});
      const 稳定度 = Math.max(0, Math.min(1, Number(战斗经验?.稳定度 ?? 0)));
      const 超出基准 = Math.max(0, 稳定度 - 0.45);
      const 基础折扣 = Math.min(0.2, (超出基准 / 0.55) * 0.2);
      return Math.min(0.24, 基础折扣 + 读取图鉴收集前摇折扣(单位, 战斗数据 || {}));
    }

    function 计算行为经验反应倍率(单位 = {}, 目标 = null, 战斗数据 = null) {
      if (!单位 || !目标) return 1;
      const 战斗经验 = 计算行为战斗经验(单位, 目标, 战斗数据 || {});
      const 稳定度 = Math.max(0, Math.min(1, Number(战斗经验?.稳定度 ?? 0)));
      const 超出基准 = Math.max(0, 稳定度 - 0.45);
      return 1 + Math.min(0.2, (超出基准 / 0.55) * 0.2) + 读取图鉴收集反应加成(单位, 战斗数据 || {});
    }

    function 套用动作实际前摇(单位, 动作, 目标 = null, 战斗数据 = null) {
      if (!动作 || typeof 动作 !== 'object' || 动作.前摇已结算 === true) return 动作;
      const 基础前摇 = Number(动作.cast_time ?? 动作.skill?.前摇 ?? 0) || 0;
      if (!(基础前摇 > 0)) {
        动作.前摇已结算 = true;
        return 动作;
      }
      const 前摇系数 = 获取动作前摇系数(单位);
      const 经验目标 = 目标 || 战斗数据?.参战者?.team_enemy?.[0] || null;
      const 技能 = 动作.skill || 动作.raw_skill || {};
      const 条件上下文 = {
        actor: 单位,
        caster: 单位,
        attacker: 单位,
        target: 经验目标,
        defender: 经验目标,
        action: 动作,
        skill: 技能,
        combatData: 战斗数据,
        当前行动: String(动作?.action_type || 动作?.type || 技能?.name || 技能?.魂技名 || '').trim(),
      };
      const 自身前摇修正 = 读取技能自身结算修正(技能, '前摇', 条件上下文);
      const 前摇绝对修正 = 获取动作前摇绝对修正(单位) + 自身前摇修正.绝对;
      const 经验折扣 = 动作免除经验前摇折扣(动作) ? 0 : 计算行为经验前摇折扣(单位, 经验目标, 战斗数据);
      动作.cast_time = Math.max(1, Math.round(基础前摇 * 前摇系数 * 自身前摇修正.倍率 * (1 - 经验折扣) + 前摇绝对修正));
      动作.前摇已结算 = true;
      return 动作;
    }

    function 套用动作队列实际前摇(单位, 动作, 目标 = null, 战斗数据 = null) {
      if (!动作 || typeof 动作 !== 'object') return 动作;
      if (Array.isArray(动作.pre_actions)) 动作.pre_actions.forEach(副动作 => 套用动作实际前摇(单位, 副动作, 目标, 战斗数据));
      return 套用动作实际前摇(单位, 动作, 目标, 战斗数据);
    }

    function isSupportLikeSkill(skill) {
      const mainType = inferMainTypeFromEffects(skill) || '';
      const skillType = getSkillType(skill);
      const hasFriendlyGrantable = skillCanGrantFriendlyMechanism(skill);
      const hasFriendlySupportPayload =
        skillTargetsFriendlySide(skill) &&
        getSkillEffects(skill).some(effect => isBattleAttributeSupportEffect(effect) || isBattleRecoverEffect(effect));
      return (
        ['增益类', '回复类', '防御类'].includes(mainType) ||
        ['辅助', '防御'].includes(skillType) ||
        hasFriendlyGrantable ||
        hasFriendlySupportPayload
      );
    }

    function 技能需要目标消耗上下文(skill = {}) {
      if (!skill) return false;
      if (isSupportLikeSkill(skill)) return true;
      const targetKind = inferSkillPrimaryTargetKind(skill);
      if (['自身', '友方单体', '友方群体', '敌方单体', '敌方群体', '全场'].includes(targetKind)) {
        return getSkillEffects(skill, { 行为规划: true }).some(effect => {
          const 原型 = String(effect?.原型 || '').trim();
          if (原型 === '伤害结算') return false;
          return !!原型;
        });
      }
      return false;
    }

    function createEmptyCombatEffectMap() {
      return {
        skip_turn: false,
        cannot_react: false,
        invincible: false,
        无视异常: false,
        skill_seal: false,
        探查屏蔽: false,
        dot_damage: 0,
        dot_damage_ratio: 0,
        armor_pen: 0,
        reaction_bonus: 0,
        reaction_penalty: 0,
        attacker_speed_bonus: 0,
        cast_speed_bonus: 0,
        cast_speed_penalty: 0,
        hit_bonus: 0,
        hit_penalty: 0,
        dodge_bonus: 0,
        dodge_penalty: 0,
        lock_level: 0,
        interrupt_bonus: 0,
        final_damage_mult: 1.0,
        received_damage_mult: 1.0,
        defense_strip: 0,
        spirit_resist_strip: 0,
        final_damage_bonus: 0,
        final_heal_mult: 1.0,
        final_heal_bonus: 0,
        shield_gain_mult: 1.0,
        shield_gain_bonus: 0,
        skill_effect_mult: 1.0,
        vit_gain_ratio: 0,
        sp_gain_ratio: 0,
        men_gain_ratio: 0,
        heal_block_ratio: 0,
        hot_heal_ratio: 0,
        cost_ratio: 1.0,
        cost_delta: 0,
        cost_delta_ratio: 0,
        windup_ratio: 1.0,
        windup_delta: 0,
        random_target_rate: 0,
        判断干扰强度: 0,
        索敌干扰强度: 0,
        stealth_level: 0,
        min_hp_floor: 0,
        death_save_count: 0,
        revive_count: 0,
        revive_heal_ratio: 0,
        damage_reflect_ratio: 0,
        damage_transfer_ratio: 0,
        damage_transfer_target: '',
        吸收来源: '',
        吸收资源: '',
        吸收转化效果: '',
        伤害吸收增幅上限: 0,
        damage_share_ratio: 0,
        damage_share_count: 0,
        cost_share_ratio: 0,
        cost_share_count: 0,
        damage_to_heal_ratio: 0,
        heal_to_damage_ratio: 0,
        heal_inversion_ratio: 0,
        invincible_tier_threshold: 0,
        每日触发次数上限: 0,
        bonus_true_damage_ratio: 0,
        element_seal_ratio: 0,
        misfortune_check_rate: 0,
        misfortune_backlash_ratio: 0,
        silence: false,
        disarm: false,
        blind: false,
        counter_attack_ratio: 0,
        damage_reduction: 0,
        block_count: 0,
        super_armor: false,
        action_lock_rounds: 0,
        interrupt_window: 0,
        multi_hit_count: 0,
        segment_damage_ratio: 0,
      };
    }

    function mergeCombatEffectMaps(base = createEmptyCombatEffectMap(), incoming = {}) {
      const seed = createEmptyCombatEffectMap();
      const result = { ...seed, ...(base || {}) };
      Object.entries(incoming || {}).forEach(([key, value]) => {
        if (!(key in seed) || value === undefined) return;
        if (['skip_turn', 'cannot_react', 'silence', 'disarm', 'blind', 'super_armor', 'invincible', '无视异常', 'skill_seal', '探查屏蔽'].includes(key)) {
          result[key] = !!result[key] || !!value;
          return;
        }
      if (
        [
          'final_damage_mult',
          'received_damage_mult',
          'final_heal_mult',
          'shield_gain_mult',
          'skill_effect_mult',
          'cost_ratio',
          'windup_ratio',
        ].includes(key)
      ) {
        result[key] = Number(result[key] ?? 1) * Number(value ?? 1);
        return;
      }
      if (['defense_strip', 'spirit_resist_strip'].includes(key)) {
        result[key] = Math.max(Number(result[key] ?? 0), Number(value ?? 0));
        return;
      }
      if (key === 'cost_delta_ratio') {
        result[key] = Math.max(Number(result[key] ?? 0), Number(value ?? 0));
        return;
      }
      if (['damage_transfer_target', '吸收来源', '吸收资源', '吸收转化效果'].includes(key)) {
        result[key] = String(value || result[key] || '').trim();
        return;
      }
        if (
          [
            'lock_level',
            'death_save_count',
            'revive_count',
            'block_count',
            'min_hp_floor',
            'damage_share_count',
            'cost_share_count',
            'invincible_tier_threshold',
            '每日触发次数上限',
            'action_lock_rounds',
            'multi_hit_count',
          ].includes(key)
        ) {
          result[key] = Math.max(Number(result[key] ?? 0), Number(value ?? 0));
          return;
        }
        result[key] = Number(result[key] ?? 0) + Number(value ?? 0);
      });
      return result;
    }

    function hasMeaningfulCombatEffect(effectMap = {}) {
      const seed = createEmptyCombatEffectMap();
      return Object.keys(seed).some(key => {
        if (['skip_turn', 'cannot_react', 'silence', 'disarm', 'blind', 'super_armor', 'invincible', '无视异常', 'skill_seal', '探查屏蔽'].includes(key)) {
          return !!effectMap[key];
        }
        const baseValue = Number(seed[key] ?? 0);
        const nextValue = Number(effectMap[key] ?? seed[key] ?? 0);
        return nextValue !== baseValue;
      });
    }

    function isBattleSkillSummaryEffect(effect = {}) {
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return false;
      const mechanism = String(effect?.原型 || '').trim();
      if (!mechanism || mechanism === '系统基础') return false;
      if (effect?.原型 && BATTLE_PROTOTYPE_REGISTRY[String(effect.原型).trim()]) return true;
        if (mechanism === '状态施加') {
        const stateName = String(effect?.状态名称 || '').trim();
        const duration = Math.max(0, Number(effect?.持续回合 ?? 0));
        const calc = effect?.计算层效果 && typeof effect.计算层效果 === 'object' ? effect.计算层效果 : {};
        const panelMods = effect?.面板修改比例 && typeof effect.面板修改比例 === 'object' ? effect.面板修改比例 : {};
        const panelDeltas = effect?.面板固定修正 && typeof effect.面板固定修正 === 'object' ? effect.面板固定修正 : {};
        const hasPanelDelta = Object.values(panelMods).some(value => Math.abs(Number(value || 1) - 1) > 0.001);
        const hasFixedDelta = Object.values(panelDeltas).some(value => Math.abs(Number(value || 0)) > 0.001);
        const hasCalc = hasMeaningfulCombatEffect(calc);
        const hasStateHint = String(effect?.特殊机制标识 || '').trim() && String(effect?.特殊机制标识 || '').trim() !== '无';
        return !!(stateName || duration > 0 || hasPanelDelta || hasFixedDelta || hasCalc || hasStateHint);
      }
      const runtimeMeta = getBattleEffectRuntimeMeta(effect);
      if (runtimeMeta && String(runtimeMeta?.运行时消费器 || '').trim()) return true;
      if (effect?.计算层效果 && typeof effect.计算层效果 === 'object' && hasMeaningfulCombatEffect(effect.计算层效果)) return true;
      if (effect?.战斗效果 && typeof effect.战斗效果 === 'object' && hasMeaningfulCombatEffect(effect.战斗效果)) return true;
      const duration = Math.max(0, Number(effect?.持续回合 ?? 0));
      if (duration > 0) return true;
      const keywords = ['状态名称', '伤害类型', '威力倍率', '护盾值', '数值', '动作', '资源类型', '抹消对象', '实体名称', '核心机制描述'];
      return keywords.some(key => {
        const value = effect?.[key];
        if (value === undefined || value === null) return false;
        if (typeof value === 'number') return Number.isFinite(value) && Math.abs(value) > 0;
        const text = String(value).trim();
        return !!text && text !== '无';
      });
    }

    function 无视异常阻断负面或削减(目标 = {}, 条目 = {}) {
      if (!目标?.状态效果) return false;
      const 战斗效果列表 = Object.values(目标.状态效果).map(状态 => 状态?.战斗效果 || {});
      if (!战斗效果列表.some(战斗效果 => 战斗效果?.无视异常 === true)) return false;
      const 类型 = String(条目?.类型 || '').trim();
      const 原型 = String(条目?.原型 || 条目?.来源原型摘要 || '').trim();
      const 计算层效果 = 条目?.战斗效果 || 条目?.计算层效果 || {};
      if (类型 === 'debuff') return true;
      if (原型 === '状态施加' && 类型 !== 'buff') return true;
      if (['属性修正', '判定修正', '资源锁定', '决策干扰', '机制抹消'].includes(原型)) return true;
      if (原型 === '资源变化' && 读取战斗数值正负(条目?.数值) < 0) return true;
      if (原型 === '护盾变化' && 读取战斗数值正负(条目?.数值) < 0) return true;
      if (原型 === '结算修正') {
        const 结算 = String(条目?.结算 || '').trim();
        const 数值 = 读取战斗数值正负(条目?.数值);
        if (['受到伤害', '消耗', '前摇', '治疗转伤害', '防御剥夺', '精神抗性剥夺', '持续伤害引爆'].includes(结算)) return true;
        if (['造成伤害', '治疗', '技能效果'].includes(结算) && 数值 < 0) return true;
      }
      return [
        'skip_turn',
        'cannot_react',
        'silence',
        'disarm',
        'blind',
        'skill_seal',
      ].some(key => 计算层效果?.[key] === true) ||
        ['reaction_penalty', 'hit_penalty', 'dodge_penalty', 'cast_speed_penalty', 'defense_strip', 'spirit_resist_strip', 'random_target_rate', 'lock_level', 'cost_delta_ratio', 'heal_block_ratio', 'heal_inversion_ratio'].some(key => Number(计算层效果?.[key] || 0) > 0);
    }

    function 读取战斗效果标签(effect = {}) {
      const 原型 = String(effect?.原型 || '').trim();
      if (原型 === '判定修正' && effect?.打断效果 === true && 读取战斗数值正负(effect?.数值) < 0) return '打断';
      return 原型;
    }

    function getBattleSkillSummaryEffects(skill = {}) {
      const effects = getSkillEffects(skill)
        .filter(effect => isBattleSkillSummaryEffect(effect))
        .map(effect => {
          const mechanism = 读取战斗效果标签(effect);
          const runtimeMeta = getBattleEffectRuntimeMeta(effect) || {};
          const targetKind = 推断战斗效果目标类型(effect);
          const targetScale = normalizeBattleSkillTargetScale(
            deriveBattleSkillTargetScaleFromKind(targetKind),
            deriveBattleSkillTargetScaleFromKind(targetKind),
          );
          const duration = Math.max(0, Number(effect?.持续回合 ?? 0));
          const calc = effect?.计算层效果 && typeof effect.计算层效果 === 'object'
            ? mergeCombatEffectMaps(createEmptyCombatEffectMap(), effect.计算层效果 || {})
            : createEmptyCombatEffectMap();
          const hint = runtimeMeta?.摘要提示 && typeof runtimeMeta.摘要提示 === 'object'
            ? { ...runtimeMeta.摘要提示 }
            : {};
          const stateName = String(effect?.状态名称 || '').trim();
          const stateFlag = String(effect?.特殊机制标识 || '').trim();
          return Object.freeze({
            标签: mechanism,
            运行时消费器: String(runtimeMeta?.运行时消费器 || '').trim(),
            目标语义: String(runtimeMeta?.目标语义 || '').trim() || 读取战斗效果运行语义(effect),
            目标: 归一化执行效果作用目标_V1(targetKind, '单体'),
            目标规模: targetScale,
            持续回合: duration,
            状态名称: stateName || mechanism,
            状态标识: stateFlag && stateFlag !== '无' ? stateFlag : '',
            计算层效果: calc,
            关键参数: {
              威力倍率: Number(effect?.威力倍率 || 0),
              护盾值: Number(effect?.护盾值 || 0),
              数值: Number(effect?.数值 || 0),
              资源类型: String(effect?.资源类型 || '').trim(),
              实体名称: String(effect?.实体名称 || '').trim(),
            },
            摘要提示: hint,
            原始效果: effect,
          });
        });
      const uniqueMap = new Map();
      effects.forEach(entry => {
          const key = [
          String(entry?.标签 || '').trim(),
          String(entry?.目标 || '').trim(),
          String(entry?.状态名称 || '').trim(),
          String(entry?.持续回合 || 0),
          String(entry?.状态标识 || '').trim(),
        ].join('|');
        if (!uniqueMap.has(key)) uniqueMap.set(key, entry);
      });
      return Array.from(uniqueMap.values());
    }

    function getBattleSkillSummaryEffectByMechanism(skill = {}, mechanism = '') {
      const normalized = String(mechanism || '').trim();
      if (!normalized) return null;
      return getBattleSkillSummaryEffects(skill).find(effect => 读取战斗效果标签(effect?.原始效果 || effect) === normalized || String(effect?.标签 || '').trim() === normalized) || null;
    }

    function 读取条件分支单位字段(unit = {}, field = '') {
      const stats = unit?.属性 && typeof unit.属性 === 'object' ? unit.属性 : {};
      const key = String(field || '').trim();
      if (!key) return '';
      if (unit[key] !== undefined) return unit[key];
      if (stats[key] !== undefined) return stats[key];
      return '';
    }

    function 读取条件分支状态表(unit = {}) {
      const direct = unit?.状态效果 && typeof unit.状态效果 === 'object' ? unit.状态效果 : null;
      const stats = unit?.属性 && typeof unit.属性 === 'object' ? unit.属性 : {};
      return direct || (stats?.状态效果 && typeof stats.状态效果 === 'object' ? stats.状态效果 : {});
    }

    function 读取条件分支单位文本(unit = {}) {
      if (!unit || typeof unit !== 'object') return '';
      const 文本列表 = [];
      const 追加 = value => {
        const 文本 = String(value || '').trim();
        if (文本) 文本列表.push(文本);
      };
      [
        unit.name,
        unit.名称,
        unit.id,
        unit.单位性质,
        unit.类型,
        unit.身份,
        unit.来源,
        unit.标准物种,
        unit.具体物种,
        unit.种族,
        unit.称号,
        unit.境界,
        unit.type,
        unit.系别,
        unit?.属性?.标准物种,
        unit?.属性?.具体物种,
        unit?.属性?.种族,
      ].forEach(追加);
      ['第1武魂', '第2武魂'].forEach(槽位 => {
        const 武魂 = unit?.[槽位];
        if (!武魂 || typeof 武魂 !== 'object') return;
        [武魂.名称, 武魂.武魂, 武魂.表象名称, 武魂.描述, 武魂.系别, 武魂.属性体系].forEach(追加);
      });
      Object.keys(unit?.社交?.势力 || {}).forEach(追加);
      return 文本列表.join(' ');
    }

    function 判断条件分支单位文本(unit = {}, value = '', op = '包含') {
      const 条件值 = String(value || '').trim();
      if (!条件值) return false;
      const 单位文本 = 读取条件分支单位文本(unit);
      const 命中 = 单位文本.includes(条件值);
      if (op === '无' || op === '!=') return !命中;
      if (op === '有' || op === '包含') return 命中;
      return 单位文本 === 条件值;
    }

    function 条件分支状态存在命中(stateMap = {}, 状态名 = '', 比较 = '包含') {
      const 状态列表 = Object.values(stateMap || {}).filter(state => state && typeof state === 'object');
      const 名称 = String(状态名 || '').trim() || '任意状态';
      const 存在 =
        名称 === '任意状态'
          ? 状态列表.length > 0
          : 名称 === '任意负面'
            ? 状态列表.some(state => String(state?.类型 || '').trim() === 'debuff')
            : 名称 === '任意增益'
              ? 状态列表.some(state => String(state?.类型 || '').trim() === 'buff')
              : !!(
                  stateMap?.[名称] ||
                  状态列表.find(state => {
                    const safeState = state && typeof state === 'object' ? state : {};
                    if (String(safeState.状态 || safeState.状态名称 || '').trim() === 名称) return true;
                    if (['持续创伤', '中毒', '流血', '灼烧', '冻伤'].includes(名称)) return 战斗效果含结构化持续伤害(safeState);
                    return false;
                  })
                );
      return 比较 === '无' ? !存在 : 存在;
    }

    function 解析条件分支对象(对象 = '目标', context = {}) {
      const key = String(对象 || '目标').trim();
      if (key === '自身') return context.actor || context.caster || null;
      if (key === '施术者') return context.caster || context.actor || null;
      if (key === '命中目标') return context.hitTarget || context.target || null;
      if (key === '召唤物') return context.summon || context.target || null;
      return context.target || null;
    }

    function 读取条件分支当前行动(context = {}) {
      const action = context?.action || context?.动作 || null;
      const skill = action?.skill || context?.skill || null;
      const 原始行动 = String(
        action?.action_type ||
        action?.type ||
        skill?.name ||
        skill?.魂技名 ||
        context?.当前行动 ||
        '',
      ).trim();
      const 行动别名 = {
        技能: '释放魂技',
        释放技能: '释放魂技',
        普通攻击: '常规攻击',
        普攻: '常规攻击',
        食用: '食用物品',
        即食: '食用物品',
        使用: '使用物品',
        装备: '穿戴装备',
      };
      return 行动别名[原始行动] || 原始行动;
    }

    function 比较条件分支数值(left, operator = '==', right = '') {
      const op = String(operator || '==').trim();
      if (op === '有') return !!left && left !== '无' && left !== '否';
      if (op === '无') return !left || left === '无' || left === '否';
      const leftNumber = /%$/.test(String(left)) ? Number(String(left).replace('%', '')) / 100 : Number(left);
      const rightNumber = /%$/.test(String(right)) ? Number(String(right).replace('%', '')) / 100 : Number(right);
      if (Number.isFinite(leftNumber) && Number.isFinite(rightNumber)) {
        if (op === '>') return leftNumber > rightNumber;
        if (op === '>=') return leftNumber >= rightNumber;
        if (op === '<') return leftNumber < rightNumber;
        if (op === '<=') return leftNumber <= rightNumber;
        if (op === '!=') return leftNumber !== rightNumber;
        return leftNumber === rightNumber;
      }
      const leftText = String(left ?? '').trim();
      const rightText = String(right ?? '').trim();
      if (op === '!=') return leftText !== rightText;
      return leftText === rightText;
    }

    function 读取条件分支资源值(unit = {}, type = '生命比例') {
      const resourceMap = {
        生命: ['HP', 'hp'],
        体力: ['sta', 'vit', '体力'],
        魂力: ['sp', '魂力'],
        精神力: ['men', '精神力'],
      };
      const label = type.replace(/比例|数值/g, '') || '生命';
      const keys = resourceMap[label] || resourceMap.生命;
      const current = Math.max(0, Number(keys.map(key => 读取条件分支单位字段(unit, key)).find(value => Number(value) > 0) || 0));
      if (/数值$/.test(type)) return current;
      const maxKeys = label === '生命'
        ? ['HP上限', 'hp_max']
        : keys.map(key => `${key}_max`).concat(`${label}上限`);
      const max = Math.max(1, Number(maxKeys.map(key => 读取条件分支单位字段(unit, key)).find(value => Number(value) > 0) || 1));
      return current / max;
    }

    function 读取条件分支环境文本(context = {}) {
      return [
        context?.环境,
        context?.battleEnvironment,
        context?.combatData?.环境,
        context?.combatData?.战斗环境,
        context?.action?.环境,
        context?.skill?.环境,
        window.BattleUIBridge?.getBattleContext?.()?.环境,
        window.BattleUIBridge?.getMVU?.('world.战斗.环境'),
      ].map(value => String(value || '').trim()).filter(Boolean).join(' ');
    }

    function 判断条件分支环境满足(value = '', op = '==', context = {}) {
      const 环境值 = String(value || '').trim();
      if (!环境值) return false;
      const 环境文本 = 读取条件分支环境文本(context);
      const 命中 = 环境文本.includes(环境值);
      return op === '!=' || op === '无' ? !命中 : 命中;
    }

    function 读取条件分支当前tick(context = {}) {
      const 候选列表 = [
        context?.tick,
        context?.当前tick,
        context?.combatData?.tick,
        context?.combatData?.当前tick,
        context?.combatData?.时间?.tick,
        window.BattleUIBridge?.getMVU?.('world.时间.tick'),
      ];
      for (const 候选 of 候选列表) {
        const 数值 = Number(候选);
        if (Number.isFinite(数值) && 数值 >= 0) return Math.floor(数值);
      }
      return 0;
    }

    function 读取条件分支时间标签列表(context = {}) {
      const tick = 读取条件分支当前tick(context);
      const 当日分钟 = ((tick * 10) % 1440 + 1440) % 1440;
      const 小时 = Math.floor(当日分钟 / 60);
      const 标签 = [];
      if (小时 >= 6 && 小时 < 18) 标签.push('白天');
      else 标签.push('黑夜');
      if (小时 >= 5 && 小时 < 8) 标签.push('清晨');
      if (小时 >= 8 && 小时 < 11) 标签.push('上午');
      if (小时 >= 11 && 小时 < 13) 标签.push('中午');
      if (小时 >= 13 && 小时 < 17) 标签.push('下午');
      if (小时 >= 17 && 小时 < 19) 标签.push('黄昏');
      if (小时 >= 19 && 小时 < 23) 标签.push('夜晚');
      if (小时 >= 23 || 小时 < 5) 标签.push('深夜');
      return 标签;
    }

    function 判断条件分支时间(value = '', op = '==', context = {}) {
      const 条件值 = String(value || '').trim();
      if (!条件值) return false;
      const 命中 = 读取条件分支时间标签列表(context).includes(条件值);
      return op === '!=' || op === '无' ? !命中 : 命中;
    }

    function 判断条件分支装备状态(unit = {}, value = '', op = '==') {
      const 装备 = unit?.装备 || {};
      const 武器 = 装备?.武器;
      const 防具 = 装备?.防具;
      const 斗铠 = 装备?.斗铠;
      const 机甲 = 装备?.机甲;
      const 条件值 = String(value || '').trim();
      let 命中 = false;
      if (条件值 === '已装备主武器') {
        命中 = !!(
          (武器 && typeof 武器 === 'object' && String(武器?.名称 || 武器?.name || 武器?.id || '').trim() && String(武器?.装备状态 || '已装备').trim() !== '未装备') ||
          (typeof 武器 === 'string' && 武器.trim() && 武器.trim() !== '无')
        );
      } else if (条件值 === '已装备斗铠') {
        命中 = String(斗铠?.装备状态 || '').trim() === '已装备';
      } else if (条件值 === '已装备机甲') {
        命中 = String(机甲?.装备状态 || '').trim() === '已装备' && String(机甲?.状态 || '').trim() !== '重创';
      } else if (条件值 === '已装备防具') {
        const 防具名 = String(防具?.名称 || 防具?.name || '').trim();
        命中 = String(防具?.装备状态 || '').trim() === '已装备' && !!防具名 && 防具名 !== '无';
      }
      return op === '!=' || op === '无' ? !命中 : 命中;
    }

    function 判断条件分支自身状态(unit = {}, value = '', op = '==') {
      const 条件值 = String(value || '').trim();
      const 状态文本 = JSON.stringify(读取条件分支状态表(unit) || {});
      let 命中 = false;
      if (条件值 === '蓄力中') {
        命中 = !!unit?.蓄力技能 || Number(unit?.蓄力剩余 || unit?.cast_time_left || 0) > 0;
      } else if (条件值 === '隐匿中') {
        命中 = /隐匿|隐身|潜行/.test(状态文本) || Number(unit?.stealth_level || unit?.隐匿等级 || 0) > 0;
      }
      return op === '!=' || op === '无' ? !命中 : 命中;
    }

    function 判断条件分支控制态(unit = {}) {
      const 状态表 = 读取条件分支状态表(unit);
      const 状态文本 = JSON.stringify(状态表 || {});
      return /眩晕|沉默|缴械|封技|硬控|束缚|禁锢|定身|控制|迟缓|位移限制/.test(状态文本) ||
        Object.values(状态表 || {}).some(state => {
          const safeState = state && typeof state === 'object' ? state : {};
          const 战斗效果 = safeState.战斗效果 && typeof safeState.战斗效果 === 'object' ? safeState.战斗效果 : {};
          return !!(战斗效果.cannot_react || 战斗效果.skip_turn || 战斗效果.disarm || 战斗效果.silence);
        });
    }

    function 判断条件分支连携前提(unit = {}, value = '', op = '==', context = {}) {
      const 条件值 = String(value || '').trim();
      let 命中 = false;
      if (条件值 === '上一动作命中') {
        命中 = context?.上一动作命中 === true || context?.actor?.__上一动作命中 === true || context?.caster?.__上一动作命中 === true;
      } else if (条件值 === '目标被控制') {
        命中 = 判断条件分支控制态(unit || context?.target || {});
      }
      return op === '!=' || op === '无' ? !命中 : 命中;
    }

    function 判断条件分支目标(目标 = '', unit = {}, context = {}) {
      const value = String(目标 || '自身').trim();
      if (value === '全场') return true;
      const actor = context.actor || context.caster || null;
      const target = unit || context.target || null;
      const actorId = String(actor?.name || actor?.id || '').trim();
      const targetId = String(target?.name || target?.id || '').trim();
      const sameActor = !!actor && !!target && (actor === target || (actorId && actorId === targetId));
      if (value === '自身') return sameActor;
      if (value === '他人') return !!target && !sameActor;
      if (value === '召唤物') return !!target && (target === context.summon || /召唤/.test(String(target?.类型 || target?.身份 || target?.来源 || '')));
      if (value === '己方' || value === '敌方') {
        const actorFaction = String(读取条件分支单位字段(actor, '势力') || 读取条件分支单位字段(actor, '阵营') || '').trim();
        const targetFaction = String(读取条件分支单位字段(target, '势力') || 读取条件分支单位字段(target, '阵营') || '').trim();
        if (!actorFaction || !targetFaction) return value === '己方' ? sameActor : !sameActor;
        const sameFaction = actorFaction === targetFaction;
        return value === '己方' ? sameFaction : !sameFaction;
      }
      return false;
    }

    function 条件分支命中(condition = {}, context = {}) {
      if (!condition || typeof condition !== 'object') return false;
      const type = String(condition.类型 || '').trim();
      const unit = 解析条件分支对象(condition.对象 || '目标', context);
      const op = String(condition.比较 || '==').trim() || '==';
      const value = condition.值 ?? '';
      if (type === '目标') return 判断条件分支目标(value, unit, context);
      if (type === '环境满足') return 判断条件分支环境满足(value, op, context);
      if (type === '时间') return 判断条件分支时间(value, op, context);
      if (type === '当前技能元素') {
        const 元素结构 = getBattleSkillElementStructure(context?.skill || {});
        const 当前元素列表 = normalizeBattleSkillAttributeTokens([
          ...(Array.isArray(context?.skill?.附带属性) ? context.skill.附带属性 : [context?.skill?.附带属性]),
          getBattleSkillAttributeSummary(context?.skill || {}).显示元素,
          ...(元素结构.核心元素 || []),
          ...(元素结构.驱动元素 || []),
          ...(元素结构.触发元素 || []),
        ]);
        const 要求元素列表 = normalizeBattleSkillAttributeTokens(value || condition.元素 || '');
        if (!要求元素列表.length) return false;
        return op === '无'
          ? 要求元素列表.every(元素 => !当前元素列表.includes(元素))
          : 要求元素列表.some(元素 => 当前元素列表.includes(元素));
      }
      if (!unit && !['当前行动', '命中', '被闪避', '暴击'].includes(type)) return false;
      if (type === '当前行动') return 比较条件分支数值(读取条件分支当前行动(context), op, value);
      if (/^(生命|体力|魂力|精神力)(比例|数值)$/.test(type)) return 比较条件分支数值(读取条件分支资源值(unit, type), op, value);
      if (type === '装备状态') return 判断条件分支装备状态(unit, value, op);
      if (type === '自身状态') return 判断条件分支自身状态(unit, value, op);
      if (type === '连携前提') return 判断条件分支连携前提(unit, value, op, context);
      if (type === '单位文本') return 判断条件分支单位文本(unit, value, op);
      if (type === '状态存在') {
        const stateMap = 读取条件分支状态表(unit);
        return 条件分支状态存在命中(stateMap, condition.状态 || value, op);
      }
      if (type === '护盾') {
        const hasShield = Object.values(读取条件分支状态表(unit)).some(state => {
          const safeState = state && typeof state === 'object' ? state : {};
          return Number(safeState.shield_value || safeState.shield || 0) > 0 || /护盾/.test(String(safeState.类型 || ''));
        });
        return op === '无' ? !hasShield : hasShield;
      }
      if (type === '命中') return op === '无' ? !context.hit : !!context.hit;
      if (type === '被闪避') return op === '无' ? !context.evaded : !!context.evaded;
      if (type === '暴击') return op === '无' ? !(context.critical || context.暴击) : !!(context.critical || context.暴击);
      return false;
    }

    function 展开条件分支效果(effect = {}, context = {}) {
      const branches = Array.isArray(effect?.条件分支) ? effect.条件分支 : [];
      if (!branches.length) return [effect];
      const 生效分支列表 = branches.filter(branch => String(branch?.处理 || '').trim() === '生效');
      if (生效分支列表.length) {
        const 命中生效门 = 生效分支列表.some(branch => {
          const conditions = Array.isArray(branch?.条件) ? branch.条件 : [];
          return conditions.length && conditions.every(condition => 条件分支命中(condition, context));
        });
        if (!命中生效门) return [];
      }
      let result = [effect];
      for (const branch of branches) {
        const conditions = Array.isArray(branch?.条件) ? branch.条件 : [];
        if (!conditions.length || !conditions.every(condition => 条件分支命中(condition, context))) continue;
        const action = String(branch?.处理 || '').trim();
        if (action === '生效') continue;
        if (action === '禁用') return [];
        if (action === '替换效果') return Array.isArray(branch?.替换效果) ? branch.替换效果 : [];
        if (action === '追加效果' && Array.isArray(branch?.追加效果)) result = [...result, ...branch.追加效果];
      }
      return result;
    }

    function getSkillEffects(skill, context = {}) {
      if (Array.isArray(skill?.__规划水合效果数组)) {
        return skill.__规划水合效果数组.filter(effect => effect && typeof effect === 'object');
      }
      const isStandardRuntimeSkill = skill?.__战斗标准技能 === true;
      const 原始效果列表 = Array.isArray(skill?._效果数组) ? skill._效果数组 : [];
      const 断言正式效果输入 = (value, path = '_效果数组', insideCondition = false) => {
        if (!value || typeof value !== 'object') return;
        if (Array.isArray(value)) {
          value.forEach((item, index) => 断言正式效果输入(item, `${path}[${index}]`, insideCondition));
          return;
        }
        const 禁止字段 = ['运行机制', '状态名称', 'cast_time', '消耗倍率', '前摇倍率', '机制标签', '结算策略'];
        const 命中字段 = 禁止字段.find(字段 => Object.prototype.hasOwnProperty.call(value, 字段));
        if (命中字段) throw new Error(`技能正式效果错误:${skill?.魂技名 || skill?.name || '未命名技能'}:${path}仍写入旧字段${命中字段}`);
        if (!insideCondition && Object.prototype.hasOwnProperty.call(value, '对象')) {
          throw new Error(`技能正式效果错误:${path}仍写入旧字段对象`);
        }
        Object.entries(value).forEach(([key, raw]) => {
          const 下级条件 = insideCondition || key === '条件';
          const 是效果槽 = ['使用效果', '授予效果', '结算效果', '替换效果', '追加效果'].includes(key);
          const 是条件分支 = key === '条件分支';
        if (raw && typeof raw === 'object' && (是效果槽 || 是条件分支 || key === '条件')) {
            断言正式效果输入(raw, `${path}.${key}`, 下级条件);
          }
        });
      };
      if (!isStandardRuntimeSkill) 断言正式效果输入(原始效果列表);
      const 来源效果列表 = String(skill?.承载方式 || '').trim() === '造物承载'
        ? []
        : 原始效果列表.filter(effect => effect && typeof effect === 'object' && String(effect?.原型 || '').trim());
      const shouldKeepByEffectiveMode = effect => {
        if (String(effect?.生效方式 || '').trim() !== '跟随主原型') return true;
        if (context?.行为规划 === true || context?.排除跟随主原型 === true) return false;
        if (Object.prototype.hasOwnProperty.call(context || {}, 'hit')) return !!context.hit;
        if (Object.prototype.hasOwnProperty.call(context || {}, '命中')) return !!context.命中;
        if (Object.prototype.hasOwnProperty.call(context || {}, '主原型成立')) return !!context.主原型成立;
        return true;
      };
      return 来源效果列表
        .filter(shouldKeepByEffectiveMode)
        .flatMap(effect => 展开条件分支效果(effect, context))
        .filter(shouldKeepByEffectiveMode)
        .flatMap(effect => 展开战斗原型数组字段(effect))
        .map(effect => hydrateBattleExecutionEffectEntry(effect, { ...context, skill }))
        .filter(effect => !物品非伤害效果超过对应等级(effect, { ...context, skill }))
        .map(effect => 应用战斗原型驱动判定(effect, { ...context, skill }))
        .filter(Boolean);
    }

    function 技能效果依赖当前行动条件(effect = {}) {
      if (!effect || typeof effect !== 'object') return false;
      const 分支列表 = Array.isArray(effect?.条件分支) ? effect.条件分支 : [];
      if (分支列表.some(分支 =>
        (Array.isArray(分支?.条件) ? 分支.条件 : []).some(条件 => String(条件?.类型 || '').trim() === '当前行动')
      )) {
        return true;
      }
      return ['使用效果', '授予效果', '结算效果'].some(字段 =>
        Array.isArray(effect?.[字段]) && effect[字段].some(内层 => 技能效果依赖当前行动条件(内层))
      );
    }

    function 收集当前行动被动效果(角色 = {}, alliedTeam = [], context = {}) {
      if (!角色 || typeof 角色 !== 'object') return [];
      const 结果 = [];
      collectPassiveCombatSkills(角色, alliedTeam).forEach(skill => {
        const 来源效果 = Array.isArray(skill?._效果数组) ? skill._效果数组 : [];
        const 当前行动效果 = 来源效果.filter(effect =>
          技能效果依赖当前行动条件(effect) ||
          String(effect?.原型 || '').trim() === '结算修正' ||
          (String(effect?.原型 || '').trim() === '资源变化' && Array.isArray(effect?.条件分支) && effect.条件分支.length > 0)
        );
        if (!当前行动效果.length) return;
        const 临时技能 = {
          ...deepClonePlain(skill),
          _效果数组: deepClonePlain(当前行动效果),
        };
        const 生效效果列表 = getSkillEffects(临时技能, context);
        if (!生效效果列表.length) return;
        const 技能名 = String(skill?.name || skill?.魂技名 || '被动技能').trim() || '被动技能';
        const 效果触发限制列表 = Array.from(new Map(
          生效效果列表
            .map(effect => effect?.触发限制 && typeof effect.触发限制 === 'object' && !Array.isArray(effect.触发限制) ? effect.触发限制 : null)
            .filter(Boolean)
            .map(限制 => [`${String(限制.周期 || '').trim()}:${Math.max(0, Math.floor(Number(限制.次数 || 0)))}`, 限制]),
        ).values());
        const 命中限制列表 = [];
        for (const 触发限制 of 效果触发限制列表) {
          const 周期 = String(触发限制.周期 || '').trim();
          const 次数 = Math.max(0, Math.floor(Number(触发限制.次数 || 0)));
          if (!(次数 > 0 && ['每日', '每战', '每回合', '每次行动'].includes(周期))) continue;
          const 限制态 = 角色.__技能限制运行态 ||= {};
          const 技能限制态 = 限制态[`被动:${技能名}:${周期}:${次数}`] ||= { 已用次数: 0 };
          if (Number(技能限制态.已用次数 || 0) >= 次数) return;
          命中限制列表.push(技能限制态);
        }
        命中限制列表.forEach(技能限制态 => {
          技能限制态.已用次数 = Math.max(0, Number(技能限制态.已用次数 || 0)) + 1;
        });
        生效效果列表.forEach(effect => {
          if (!effect || typeof effect !== 'object') return;
          结果.push({ ...effect, __当前行动被动来源: String(skill?.name || skill?.魂技名 || '被动技能').trim() || '被动技能' });
        });
      });
      return 结果;
    }

    const BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF = Object.freeze({
      掌控: 1,
      威力: 1,
      消耗: 1,
      前摇: 1,
      控制: 1,
      速度: 1,
    });
    const BATTLE_SKILL_ATTRIBUTE_COEFF_MAP = Object.freeze({
      金: Object.freeze({ 掌控: 1.02, 威力: 1.08, 消耗: 1.0, 前摇: 0.98, 控制: 0.98, 速度: 1.02 }),
      木: Object.freeze({ 掌控: 1.08, 威力: 0.96, 消耗: 0.96, 前摇: 1.0, 控制: 1.05, 速度: 1.0 }),
      水: Object.freeze({ 掌控: 1.06, 威力: 0.98, 消耗: 0.95, 前摇: 1.0, 控制: 1.04, 速度: 1.0 }),
      火: Object.freeze({ 掌控: 0.96, 威力: 1.15, 消耗: 1.06, 前摇: 1.0, 控制: 0.95, 速度: 1.02 }),
      土: Object.freeze({ 掌控: 1.0, 威力: 1.05, 消耗: 1.0, 前摇: 1.04, 控制: 1.02, 速度: 0.95 }),
      雷: Object.freeze({ 掌控: 1.0, 威力: 1.1, 消耗: 1.03, 前摇: 0.92, 控制: 0.98, 速度: 1.12 }),
      冰: Object.freeze({ 掌控: 1.04, 威力: 1.02, 消耗: 1.0, 前摇: 1.02, 控制: 1.12, 速度: 0.95 }),
      风: Object.freeze({ 掌控: 1.0, 威力: 1.02, 消耗: 0.96, 前摇: 0.94, 控制: 0.98, 速度: 1.12 }),
      光: Object.freeze({ 掌控: 1.05, 威力: 1.03, 消耗: 0.98, 前摇: 0.98, 控制: 1.02, 速度: 1.0 }),
      暗: Object.freeze({ 掌控: 1.03, 威力: 1.08, 消耗: 1.02, 前摇: 0.98, 控制: 1.04, 速度: 1.0 }),
      精神: Object.freeze({ 掌控: 1.08, 威力: 1.02, 消耗: 1.0, 前摇: 0.98, 控制: 1.14, 速度: 1.0 }),
      空间: Object.freeze({ 掌控: 1.12, 威力: 1.0, 消耗: 1.08, 前摇: 0.96, 控制: 1.08, 速度: 1.02 }),
      时间: Object.freeze({ 掌控: 1.12, 威力: 1.0, 消耗: 1.08, 前摇: 0.9, 控制: 1.1, 速度: 1.08 }),
      创造: Object.freeze({ 掌控: 1.18, 威力: 1.12, 消耗: 1.1, 前摇: 1.0, 控制: 1.12, 速度: 1.0 }),
      毁灭: Object.freeze({ 掌控: 1.08, 威力: 1.22, 消耗: 1.14, 前摇: 1.04, 控制: 1.08, 速度: 0.98 }),
    });

    function normalizeBattleSkillAttributeToken(value = '') {
      const raw = String(value || '').trim();
      if (!raw || raw === '无') return '';
      if (raw === '五行类' || raw === '元素类') return raw;
      const aliasMap = {
        金系: '金',
        木系: '木',
        水系: '水',
        火系: '火',
        土系: '土',
        雷系: '雷',
        冰系: '冰',
        风系: '风',
        光系: '光',
        暗系: '暗',
        精神系: '精神',
        精神力: '精神',
        空间系: '空间',
        时间系: '时间',
        创造系: '创造',
        毁灭系: '毁灭',
        光明: '光',
        黑暗: '暗',
        创世: '创造',
        灭世: '毁灭',
      };
      const normalized = aliasMap[raw] || raw;
      return BATTLE_SKILL_ATTRIBUTE_COEFF_MAP[normalized] ? normalized : '';
    }

    function normalizeBattleSkillAttributeTokens(list = []) {
      const source = Array.isArray(list) ? list : String(list || '').split(/[、,，/|｜；;\s]+/g);
      const expandMap = {
        五行类: ['金', '木', '水', '火', '土'],
        元素类: ['水', '火', '风', '土', '光', '暗'],
      };
      return Array.from(new Set(source
        .map(normalizeBattleSkillAttributeToken)
        .filter(Boolean)
        .flatMap(token => expandMap[token] || [token])));
    }


    const BATTLE_FUSION_BASE_ELEMENTS = Object.freeze(['水', '火', '风', '土']);
    const BATTLE_FUSION_ADVANCED_ELEMENTS = Object.freeze(['光', '暗', '空间']);
    const BATTLE_FUSION_LAW_ELEMENTS = Object.freeze(['创造', '毁灭']);
    const BATTLE_FUSION_ALLOWED_ELEMENTS = Object.freeze([
      ...BATTLE_FUSION_BASE_ELEMENTS,
      ...BATTLE_FUSION_ADVANCED_ELEMENTS,
      ...BATTLE_FUSION_LAW_ELEMENTS,
    ]);
    const BATTLE_FUSION_ELEMENT_ORDER = Object.freeze([...BATTLE_FUSION_ALLOWED_ELEMENTS]);
    const BATTLE_FUSION_SEMANTICS_MAP = Object.freeze({
      '水/火': Object.freeze({
        pattern: '水火蒸爆',
        multiplier: 1.18,
        failAdjust: 8,
        summary: '水火相激形成高压蒸爆，爆发显著提升。',
      }),
      '水/风': Object.freeze({
        pattern: '水风涡流',
        multiplier: 1.1,
        failAdjust: -4,
        summary: '水借风势形成涡流，融合稳定性更高。',
      }),
      '水/土': Object.freeze({
        pattern: '水土泽域',
        multiplier: 1.08,
        failAdjust: -2,
        summary: '水土交汇形成泥泽领域，控场更稳。',
      }),
      '火/风': Object.freeze({
        pattern: '火风炎岚',
        multiplier: 1.16,
        failAdjust: 6,
        summary: '烈焰借风成势，形成高速扩张的炎岚。',
      }),
      '冰/风': Object.freeze({
        pattern: '冰风霜灾',
        multiplier: 1.14,
        failAdjust: 2,
        summary: '寒流被风卷起，形成持续冻结的霜灾。',
      }),
      '光/暗': Object.freeze({
        pattern: '光暗蚀变',
        multiplier: 1.22,
        failAdjust: 12,
        summary: '光暗对冲本身就足够危险，容易形成湮灭蚀变。',
      }),
      '水/火/风': Object.freeze({
        pattern: '水火风暴',
        multiplier: 1.28,
        failAdjust: 10,
        summary: '蒸爆被风势卷起，形成持续爆裂风暴。',
      }),
      '水/土/风': Object.freeze({
        pattern: '水土风岚·泽域封场',
        multiplier: 1.18,
        failAdjust: 3,
        summary: '泥泽、气流与水势叠加，形成范围封场。',
      }),
      '水/火/土': Object.freeze({
        pattern: '水火土·蒸压熔壳',
        multiplier: 1.22,
        failAdjust: 8,
        summary: '蒸压与土壳并存，兼具压制与爆裂。',
      }),
      '水/火/风/土': Object.freeze({
        pattern: '四象归元·雷霆显化',
        multiplier: 1.24,
        failAdjust: 16,
        summary: '四基础元素归元后显化法则性雷霆，这不是普通雷属性，而是四象归一后的法则征兆。并且已可触发元素剥离。',
        derivedEffects: ['元素剥离'],
      }),
      '光/暗/空间': Object.freeze({
        pattern: '光暗空间·界域扭变',
        multiplier: 1.35,
        failAdjust: 14,
        summary: '光暗对冲叠加空间扭曲，形成界域级压制。',
      }),
      '水/火/风/土/光/暗/空间': Object.freeze({
        pattern: '七元素爆裂',
        multiplier: 1.38,
        failAdjust: 28,
        summary: '四基础元素与三进阶元素同时贯通，踏入真正的七元素爆裂台阶。',
      }),
      '创造/毁灭': Object.freeze({
        pattern: '创造毁灭·法则对冲',
        multiplier: 1.45,
        failAdjust: 18,
        summary: '法则互冲带来极高上限，也伴随极高失控风险。',
      }),
      '光/暗/创造/毁灭': Object.freeze({
        pattern: '光暗双法则·湮生对撞',
        multiplier: 1.6,
        failAdjust: 24,
        summary: '光暗对冲叠加双法则冲撞，接近失控边缘的极限融合。',
      }),
    });

    function sortBattleFusionElements(elements = []) {
      const normalized = normalizeBattleSkillAttributeTokens(elements).filter(token =>
        BATTLE_FUSION_ALLOWED_ELEMENTS.includes(token),
      );
      const orderIndex = token => {
        const index = BATTLE_FUSION_ELEMENT_ORDER.indexOf(token);
        return index >= 0 ? index : BATTLE_FUSION_ELEMENT_ORDER.length + token.charCodeAt(0);
      };
      return [...normalized].sort((a, b) => orderIndex(a) - orderIndex(b));
    }

    function buildBattleFusionKey(elements = []) {
      return sortBattleFusionElements(elements).join('/');
    }

    function resolveBattleFusionSemantics(elements = []) {
      const normalized = sortBattleFusionElements(elements);
      const key = normalized.join('/');
      const preset = BATTLE_FUSION_SEMANTICS_MAP[key];
      let multiplier = Number(preset?.multiplier || 1);
      let failAdjust = Number(preset?.failAdjust || 0);
      let pattern = String(preset?.pattern || (normalized.length ? normalized.join('/') : '未指定'));
      let summary = String(
        preset?.summary || (normalized.length ? `${normalized.join('/')} 并行融合。` : '未指定元素融合。'),
      );
      const derivedEffects = normalizeBattleSkillStringArray(preset?.derivedEffects || []);
      const hasLaw = normalized.some(token => token === '创造' || token === '毁灭');
      const hasSpace = normalized.includes('空间');
      const hasLightDark = normalized.includes('光') && normalized.includes('暗');
      if (!preset && hasSpace) {
        multiplier *= 1.08;
        failAdjust += 4;
        summary += ' 空间参与抬高融合上限。';
      }
      if (!preset && hasLightDark) {
        multiplier *= 1.1;
        failAdjust += 6;
        summary += ' 光暗对冲令结构更危险。';
      }
      if (!preset && hasLaw) {
        const lawCount = normalized.filter(token => token === '创造' || token === '毁灭').length;
        multiplier *= 1 + lawCount * 0.12;
        failAdjust += lawCount * 10;
        summary += ' 法则元素参与显著抬高上限与风险。';
      }
      return { key, elements: normalized, pattern, multiplier, failAdjust, summary, derivedEffects };
    }

    function extractBattleFusionElementsFromText(raw = '') {
      const text = String(raw || '').trim();
      if (!text) return [];
      if (/七元素/.test(text)) return [...BATTLE_FUSION_BASE_ELEMENTS, ...BATTLE_FUSION_ADVANCED_ELEMENTS];
      if (/四元素/.test(text)) return [...BATTLE_FUSION_BASE_ELEMENTS];
      const directMatches = [];
      ['创造', '毁灭', '空间', '光明', '黑暗', '创世', '灭世', '水', '火', '土', '风', '光', '暗'].forEach(token => {
        if (text.includes(token)) directMatches.push(normalizeBattleSkillAttributeToken(token));
      });
      const cleaned = text
        .replace(/多元素融合|元素融合|融合技|融合|蓄力|极致|调用|使用|释放|施展/g, ' ')
        .replace(/[、,，|｜/＋+]/g, ' ')
        .replace(/\s+/g, ' ')
        .trim();
      const splitMatches = cleaned
        ? cleaned
            .split(' ')
            .map(normalizeBattleSkillAttributeToken)
            .filter(token => token && BATTLE_FUSION_ALLOWED_ELEMENTS.includes(token))
        : [];
      return Array.from(new Set([...directMatches, ...splitMatches].filter(Boolean)));
    }

    function buildBattleFusionPattern(elements = []) {
      return resolveBattleFusionSemantics(elements).pattern;
    }

    function collectBattleUnlockedAttributeTokens(char = {}) {
      const collected = [];
      取角色武魂条目_战斗(char).forEach(([, spiritData]) => {
        const unlocked = Array.isArray(spiritData?.已解锁调用权) ? spiritData.已解锁调用权 : [];
        collected.push(...normalizeBattleSkillAttributeTokens(unlocked));
      });
      const bloodlineUnlocked = Array.isArray(char?.血脉之力?.已解锁调用权)
        ? char.血脉之力.已解锁调用权
        : [];
      collected.push(...normalizeBattleSkillAttributeTokens(bloodlineUnlocked));
      return Array.from(new Set(collected));
    }

    function hasBattleUnlockedAttributeSet(char = {}, required = []) {
      const unlocked = new Set(collectBattleUnlockedAttributeTokens(char));
      return normalizeBattleSkillAttributeTokens(required).every(token => unlocked.has(token));
    }

    function normalizeBattleSkillAttributeCoefficients(value = {}) {
      const normalized = { ...BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF };
      Object.keys(BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF).forEach(key => {
        const raw = Number(value?.[key] ?? BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF[key]);
        normalized[key] = Number.isFinite(raw) && raw > 0 ? raw : BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF[key];
      });
      return normalized;
    }

    function normalizeBattleSkillStringArray(value = []) {
      const source = Array.isArray(value) ? value : [];
      return Array.from(new Set(source.map(item => String(item || '').trim()).filter(Boolean)));
    }

    function normalizeBattleSkillAttributeSource(value = '') {
      const text = String(value || '').trim();
      return ['自身操控', '魂技调用'].includes(text) ? text : '无';
    }

    function normalizeBattleSkillRole(value = '') {
      const text = String(value || '').trim();
      return ['增幅器', '结构术式'].includes(text) ? text : '无';
    }

    function normalizeBattleSkillElementStructure(value = {}) {
      const source = value && typeof value === 'object' && !Array.isArray(value) ? value : {};
      return {
        模式: String(source?.模式 || '无').trim() || '无',
        核心元素: normalizeBattleSkillAttributeTokens(source?.核心元素),
        驱动元素: normalizeBattleSkillAttributeTokens(source?.驱动元素),
        约束元素: normalizeBattleSkillAttributeTokens(source?.约束元素),
        触发元素: normalizeBattleSkillAttributeTokens(source?.触发元素),
        关系: Array.isArray(source?.关系) ? deepClone(source.关系) : [],
      };
    }

    function normalizeBattleSkillWuxingInvocation(value = {}) {
      const source = value && typeof value === 'object' && !Array.isArray(value) ? value : {};
      return {
        模式: String(source?.模式 || '无').trim() || '无',
        调用链: normalizeBattleSkillStringArray(source?.调用链),
        回路闭合: !!source?.回路闭合,
        层级回溯: normalizeBattleSkillStringArray(source?.层级回溯),
        终态: String(source?.终态 || '无').trim() || '无',
        结果: String(source?.结果 || '无').trim() || '无',
      };
    }

    function normalizeBattleSkillPolarityInfo(value = {}) {
      const source = value && typeof value === 'object' && !Array.isArray(value) ? value : {};
      return {
        polarityUnlocked: !!source?.polarityUnlocked,
        polarityMode: String(source?.polarityMode || source?.polarity || '无').trim() || '无',
      };
    }

    function getBattleSkillSystemBaseParam(skill = {}, key = '') {
      if (!skill || typeof skill !== 'object' || Array.isArray(skill)) return undefined;
      return skill[key];
    }

    function getBattleSkillAttributeSummary(skill = {}) {
      return {
        属性来源: String(getBattleSkillSystemBaseParam(skill, '属性来源') || '').trim(),
        魂技作用: String(getBattleSkillSystemBaseParam(skill, '魂技作用') || '').trim(),
        显示元素: String(getBattleSkillSystemBaseParam(skill, '显示元素') || '').trim(),
      };
    }

    function getBattleSkillAttributeSource(skill = {}) {
      return normalizeBattleSkillAttributeSource(getBattleSkillAttributeSummary(skill).属性来源 || '');
    }

    function getBattleSkillRole(skill = {}) {
      return normalizeBattleSkillRole(getBattleSkillAttributeSummary(skill).魂技作用 || '');
    }

    function getBattleSkillElementStructure(skill = {}) {
      return normalizeBattleSkillElementStructure(getBattleSkillSystemBaseParam(skill, '元素构型') || {});
    }

    function getBattleSkillWuxingInvocation(skill = {}) {
      return normalizeBattleSkillWuxingInvocation(getBattleSkillSystemBaseParam(skill, '五行调用结构') || {});
    }

    function getBattleSkillPolaritySummary(skill = {}) {
      return normalizeBattleSkillPolarityInfo(getBattleSkillSystemBaseParam(skill, '极性信息') || {});
    }

    function getBattleSkillRuntimeAttributeCoefficients(skill = {}) {
      return normalizeBattleSkillAttributeCoefficients(getBattleSkillSystemBaseParam(skill, '属性系数') || {});
    }

    function getBattleSkillDisplayElement(skill = {}) {
      const explicit = String(getBattleSkillAttributeSummary(skill).显示元素 || '').trim();
      if (explicit) return explicit;
      const attached = normalizeBattleSkillAttributeTokens(skill?.附带属性);
      return attached.length ? attached.join('/') : '无';
    }

    function hasBattleSkillAttributeStructure(skill = {}) {
      const elementStructure = getBattleSkillElementStructure(skill);
      const wuxingInvocation = getBattleSkillWuxingInvocation(skill);
      const polarityInfo = getBattleSkillPolaritySummary(skill);
      return (
        getBattleSkillAttributeSource(skill) !== '无' ||
        getBattleSkillRole(skill) !== '无' ||
        elementStructure.模式 !== '无' ||
        elementStructure.核心元素.length > 0 ||
        elementStructure.驱动元素.length > 0 ||
        elementStructure.约束元素.length > 0 ||
        elementStructure.触发元素.length > 0 ||
        elementStructure.关系.length > 0 ||
        wuxingInvocation.模式 !== '无' ||
        wuxingInvocation.调用链.length > 0 ||
        wuxingInvocation.回路闭合 ||
        wuxingInvocation.层级回溯.length > 0 ||
        wuxingInvocation.终态 !== '无' ||
        wuxingInvocation.结果 !== '无' ||
        polarityInfo.polarityUnlocked ||
        polarityInfo.polarityMode !== '无'
      );
    }

    function mergeBattleSkillAttributeCoefficientProfiles(list = []) {
      const profiles = (Array.isArray(list) ? list : []).map(profile =>
        normalizeBattleSkillAttributeCoefficients(profile || {}),
      );
      if (!profiles.length) return normalizeBattleSkillAttributeCoefficients();
      const merged = {};
      Object.keys(BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF).forEach(key => {
        merged[key] = profiles.reduce((sum, profile) => sum + Number(profile?.[key] ?? 1), 0) / profiles.length;
      });
      return normalizeBattleSkillAttributeCoefficients(merged);
    }

    function buildBattleSkillAttributeCoefficientsFromAttachedAttributes(attachedAttributes = []) {
      const attached = normalizeBattleSkillAttributeTokens(attachedAttributes);
      if (!attached.length) return normalizeBattleSkillAttributeCoefficients();
      return mergeBattleSkillAttributeCoefficientProfiles(
        attached.map(attr => BATTLE_SKILL_ATTRIBUTE_COEFF_MAP[attr] || BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF),
      );
    }

    function resolveBattleSkillAttributeCoefficients(skill = {}) {
      const attached = normalizeBattleSkillAttributeTokens(skill?.附带属性);
      const hasV2Structure = hasBattleSkillAttributeStructure(skill);
      if (!attached.length)
        return hasV2Structure
          ? getBattleSkillRuntimeAttributeCoefficients(skill)
          : normalizeBattleSkillAttributeCoefficients();
      if (hasV2Structure) return getBattleSkillRuntimeAttributeCoefficients(skill);
      return buildBattleSkillAttributeCoefficientsFromAttachedAttributes(attached);
    }

    function isNeutralBattleSkillAttributeCoefficients(coeff = {}) {
      const normalized = normalizeBattleSkillAttributeCoefficients(coeff);
      return Object.keys(BATTLE_SKILL_DEFAULT_ATTRIBUTE_COEFF).every(
        key => Math.abs(Number(normalized[key] || 1) - 1) < 0.0001,
      );
    }

    function roundBattleScaledNumber(value, digits = 2) {
      const num = Number(value);
      if (!Number.isFinite(num)) return 0;
      const scaled = Number(num.toFixed(digits));
      return Number.isInteger(scaled) ? Math.trunc(scaled) : scaled;
    }

    function scaleBattleValue(value, ratio = 1, options = {}) {
      const num = Number(value);
      if (!Number.isFinite(num)) return value;
      let next = num * Number(ratio || 1);
      if (options.min !== undefined) next = Math.max(Number(options.min), next);
      if (options.max !== undefined) next = Math.min(Number(options.max), next);
      return roundBattleScaledNumber(next, options.digits ?? 2);
    }

    function scaleBattleFactor(value, ratio = 1, neutral = 1) {
      const num = Number(value);
      if (!Number.isFinite(num)) return value;
      return roundBattleScaledNumber(Number(neutral) + (num - Number(neutral)) * Number(ratio || 1), 4);
    }

    function scaleBattleLockLevel(value, ratio = 1) {
      const num = Number(value || 0);
      if (!(num > 0)) return 0;
      return Math.max(1, Math.round(num * Number(ratio || 1)));
    }

    function scaleBattleDebuffRatio(value, ratio = 1, neutral = 1) {
      const num = Number(value);
      if (!Number.isFinite(num)) return value;
      return roundBattleScaledNumber(Number(neutral) - (Number(neutral) - num) * Number(ratio || 1), 4);
    }

    function scaleBattleSupportBuffCalc(calc = {}, supportScale = 1) {
      const next = deepClone(calc || {});
      const ratio = Number(supportScale || 1);
      if (!Number.isFinite(ratio) || Math.abs(ratio - 1) < 0.0001) return next;

      ['hit_bonus', 'reaction_bonus', 'dodge_bonus', 'attacker_speed_bonus', 'cast_speed_bonus', 'interrupt_bonus'].forEach(
        key => {
          if (next[key] !== undefined) next[key] = scaleBattleValue(next[key], ratio, { digits: 4 });
        },
      );

        ['damage_reduction', 'counter_attack_ratio', 'damage_reflect_ratio', 'damage_transfer_ratio', 'damage_share_ratio', 'cost_share_ratio', 'bonus_true_damage_ratio', 'damage_absorb_ratio', 'damage_to_heal_ratio', 'heal_to_damage_ratio', 'defense_strip', 'spirit_resist_strip', 'vit_gain_ratio', 'sp_gain_ratio', 'men_gain_ratio', 'hot_heal_ratio', 'heal_inversion_ratio', 'revive_heal_ratio', 'cost_delta_ratio'].forEach(
        key => {
          if (next[key] !== undefined) next[key] = scaleBattleValue(next[key], ratio, { min: 0, digits: 4 });
        },
      );

      ['final_damage_mult', 'received_damage_mult', 'final_heal_mult', 'shield_gain_mult', 'skill_effect_mult', 'cost_ratio', 'windup_ratio'].forEach(key => {
        if (next[key] !== undefined) next[key] = scaleBattleFactor(next[key], ratio, 1);
      });

      ['final_damage_bonus', 'final_heal_bonus', 'shield_gain_bonus'].forEach(key => {
        if (next[key] !== undefined) next[key] = scaleBattleValue(next[key], ratio, { min: 0, digits: 2 });
      });
      ['cost_delta', 'windup_delta'].forEach(key => {
        if (next[key] !== undefined) next[key] = scaleBattleValue(next[key], ratio, { digits: 2 });
      });

      if (next.min_hp_floor !== undefined) next.min_hp_floor = Math.max(0, Math.round(Number(next.min_hp_floor || 0) * ratio));
      if (next.invincible_tier_threshold !== undefined)
        next.invincible_tier_threshold = Math.max(0, Number(next.invincible_tier_threshold || 0));

      return next;
    }

    function 套用技能效果增幅(skill = {}, 战斗效果列表 = []) {
      const 效果倍率 = (Array.isArray(战斗效果列表) ? 战斗效果列表 : []).reduce(
        (倍率, 战斗效果) => 倍率 * Math.max(1, Number(战斗效果?.skill_effect_mult || 1)),
        1,
      );
      if (效果倍率 <= 1.0001) return { skill, 已增幅: false, 效果倍率 };
      const nextSkill = deepClone(skill || {});
      const effects = getSkillEffects(nextSkill);
      const 数量字段 = [
        '数量',
        '个数',
        '召唤数量',
        '制造数量',
        '分身数量',
        '驱散数量',
        '窃取数量',
        '转移数量',
      ];
      const 中性倍率字段 = [
        'final_damage_mult',
        'received_damage_mult',
        'final_heal_mult',
        'shield_gain_mult',
        'cost_ratio',
        'windup_ratio',
        'mastery_ratio',
        'speed_ratio',
      ];
      const 数值字段 = [
        '威力倍率',
        '护盾值',
        '每回合伤害',
        'dot_damage',
        'final_damage_bonus',
        'final_heal_bonus',
        'shield_gain_bonus',
        'vit_gain_ratio',
        'sp_gain_ratio',
        'men_gain_ratio',
        'hot_heal_ratio',
        'heal_block_ratio',
        'revive_heal_ratio',
        'damage_reduction',
        'damage_reflect_ratio',
        'damage_transfer_ratio',
        'damage_share_ratio',
        'counter_attack_ratio',
        'damage_absorb_ratio',
        'damage_to_heal_ratio',
        'heal_to_damage_ratio',
        'bonus_true_damage_ratio',
        '引爆倍率',
        '收益倍率',
        '封禁强度',
        '气运修正',
        '反噬系数',
      ];
      const 原始效果文本 = JSON.stringify(effects);
      effects.forEach(effect => {
        const 机制名 = String(effect?.原型 || '').trim();
        if (!effect || typeof effect !== 'object' || 机制名 === '系统基础' || 机制名 === '技能效果增幅') return;
        const 是对应等级伤害 = 机制名 === '伤害结算' && 读取对应等级(effect) > 0;
        if (是对应等级伤害) return;
        const 命中数量字段 = 数量字段.filter(字段 => Number.isFinite(Number(effect[字段])));
        if (命中数量字段.length > 0) {
          命中数量字段.forEach(字段 => {
            effect[字段] = Math.max(1, Math.round(Number(effect[字段] || 0) * 效果倍率));
          });
          return;
        }
        中性倍率字段.forEach(字段 => {
          if (!Number.isFinite(Number(effect[字段]))) return;
          const 数值 = Number(effect[字段]);
          effect[字段] = 数值 >= 1 ? scaleBattleFactor(数值, 效果倍率, 1) : scaleBattleDebuffRatio(数值, 效果倍率, 1);
        });
        if (effect.数值 !== undefined && (Number.isFinite(Number(effect.数值)) || /%$/.test(String(effect.数值).trim()))) {
          const 动作 = String(effect.动作 || '').trim();
          if (机制名 === '属性变化') {
            const 是变化量 = !动作 && /%$/.test(String(effect.数值).trim());
            const 是倍率 = ['倍率提升', '倍率压制'].includes(动作) || 是变化量 || (!动作 && Number(effect.数值) > 0 && Math.abs(Number(effect.数值) - 1) > 0.001);
            effect.数值 = 是变化量
              ? 缩放战斗属性变化数值(effect.数值, 效果倍率)
              : 是倍率
                ? 动作 === '倍率压制' || (!动作 && Number(effect.数值) < 1)
                  ? scaleBattleDebuffRatio(effect.数值, 效果倍率, 1)
                  : scaleBattleFactor(effect.数值, 效果倍率, 1)
                : scaleBattleValue(effect.数值, 效果倍率, { min: 0, digits: 4 });
          } else if (机制名 === '结算修正' && ['消耗', '前摇'].includes(String(effect?.结算 || '').trim())) {
            effect.数值 = 读取战斗数值正负(effect.数值) < 0
              ? scaleBattleDebuffRatio(effect.数值, 效果倍率, 1)
              : scaleBattleFactor(effect.数值, 效果倍率, 1);
          } else if (机制名 === '掌控修正' || 机制名 === '速度修正') {
            effect.数值 = !动作 && /%$/.test(String(effect.数值).trim())
              ? 缩放战斗属性变化数值(effect.数值, 效果倍率)
              : 动作 === '倍率压制' || (!动作 && Number(effect.数值) < 1)
                ? scaleBattleDebuffRatio(effect.数值, 效果倍率, 1)
                : scaleBattleFactor(effect.数值, 效果倍率, 1);
          } else {
            effect.数值 = scaleBattleValue(effect.数值, 效果倍率, { min: 0, digits: 4 });
          }
        }
        数值字段.forEach(字段 => {
          if (Number.isFinite(Number(effect[字段]))) effect[字段] = scaleBattleValue(effect[字段], 效果倍率, { min: 0, digits: 4 });
        });
        if (effect.计算层效果 && typeof effect.计算层效果 === 'object') {
          effect.计算层效果 = scaleBattleSupportBuffCalc(effect.计算层效果, 效果倍率);
        }
      });
      return { skill: nextSkill, 已增幅: JSON.stringify(effects) !== 原始效果文本, 效果倍率 };
    }

    function scaleSkillCostText(costText, ratio = 1) {
      const text = String(costText || '').trim();
      if (!text || text === '无' || Math.abs(Number(ratio || 1) - 1) < 0.0001) return text || '无';
      return text.replace(
        /(\d+(?:\.\d+)?)(%?)/g,
        (_, numText, suffix) =>
          `${roundBattleScaledNumber(Number(numText) * Number(ratio || 1), suffix ? 2 : 0)}${suffix}`,
      );
    }

    function ensureBattleSkillAttributeBase(skill = {}) {
      if (skill?.__attributeCoeffBase && Array.isArray(skill.__attributeCoeffBase.effects))
        return skill.__attributeCoeffBase;
      const 清理正式效果快照 = value => {
        if (Array.isArray(value)) return value.map(清理正式效果快照).filter(Boolean);
        if (!value || typeof value !== 'object') return value;
        const next = deepClone(value);
        ['运行机制', '状态名称', '计算层效果', '战斗效果', '面板修改比例', '面板固定修正', '运行时消费器'].forEach(key => {
          delete next[key];
        });
        ['使用效果', '授予效果', '结算效果'].forEach(key => {
          if (Array.isArray(next[key])) next[key] = 清理正式效果快照(next[key]);
        });
        if (Array.isArray(next.条件分支)) {
          next.条件分支 = next.条件分支.map(branch => {
            const 分支 = deepClone(branch);
            ['替换效果', '追加效果'].forEach(key => {
              if (Array.isArray(分支[key])) 分支[key] = 清理正式效果快照(分支[key]);
            });
            return 分支;
          });
        }
        return next;
      };
      const runtimeMeta = getSkillRuntimeMeta(skill);
      const base = {
        effects: 清理正式效果快照(skill?._效果数组 || []),
        前摇: Number(runtimeMeta.cast_time ?? 0) || 0,
        cost_text: runtimeMeta.消耗 || '无',
      };
      skill.__attributeCoeffBase = deepClone(base);
      return skill.__attributeCoeffBase;
    }

    function ensureBattleTransientStateEffect(skill = {}) {
      const targetText = getSkillTarget(skill);
      const stateEffect = {
        原型: '状态施加',
        运行机制: '状态施加',
        状态名称: '无',
        目标: targetText,
        持续回合: 0,
        面板修改比例: {},
        计算层效果: createEmptyCombatEffectMap(),
      };
      if (!stateEffect.状态名称 || !String(stateEffect.状态名称).trim()) stateEffect.状态名称 = '无';
      if (!stateEffect.目标) stateEffect.目标 = getSkillTarget(skill);
      return stateEffect;
    }

    function applyAttributeCoeffToCombatSkill(skill = {}) {
      if (!skill || typeof skill !== 'object') return skill;
      const base = ensureBattleSkillAttributeBase(skill);
      skill._效果数组 = deepClone(base.effects || []);
      skill.前摇 = Number(base.前摇 ?? 0) || 0;
      skill.消耗 = String(base.cost_text || '无') || '无';

      const attached = normalizeBattleSkillAttributeTokens(skill?.附带属性);
      const hasV2Structure = hasBattleSkillAttributeStructure(skill);
      skill.附带属性 = attached;
      ['属性系数', '属性来源', '魂技作用', '元素构型', '五行调用结构', '极性信息', '元素'].forEach(key => {
        if (key in skill) delete skill[key];
      });
      const coeff = resolveBattleSkillAttributeCoefficients(skill);

      if ((!attached.length && !hasV2Structure) || isNeutralBattleSkillAttributeCoefficients(coeff)) return skill;

      const normalizedCoeff = normalizeBattleSkillAttributeCoefficients(coeff);
      const effects = Array.isArray(skill._效果数组) ? skill._效果数组 : [];
          const directDamageMechanisms = new Set(['伤害结算']);

      skill.消耗 = scaleSkillCostText(skill.消耗, normalizedCoeff.消耗);
      skill.前摇 = Math.max(
        skill.前摇 > 0 ? 1 : 0,
        Math.round(Number(skill.前摇 || 0) * normalizedCoeff.前摇),
      );

      effects.forEach(effect => {
        const mechanism = String(effect?.原型 || '');
        if (!mechanism) return;

        if (directDamageMechanisms.has(mechanism) && effect.威力倍率 !== undefined && !(读取对应等级(effect) > 0)) {
          effect.威力倍率 = scaleBattleValue(effect.威力倍率, coeff.威力, { min: 0, digits: 2 });
        }
        if (mechanism === '护盾' && effect.护盾值 !== undefined) {
          effect.护盾值 = scaleBattleValue(effect.护盾值, coeff.威力, { min: 0, digits: 0 });
        }
        if (mechanism === '属性变化' && effect.数值 !== undefined) {
          const property = String(effect.属性 || '').trim();
          const action = String(effect.动作 || '').trim();
          if (action === '加值' && ['vit', 'sp', 'men'].includes(property)) {
            effect.数值 = scaleBattleValue(effect.数值, coeff.威力, { min: 0, digits: 4 });
          } else if (['倍率提升', '倍率压制'].includes(action) || (!action && /%$/.test(String(effect.数值).trim())) || (!action && Number(effect.数值) > 0 && Math.abs(Number(effect.数值) - 1) > 0.001)) {
            const factor =
              property === '掌控'
                ? coeff.掌控
                : property === '控制'
                  ? coeff.控制
                  : property === '速度'
                    ? coeff.速度
                    : property === '前摇'
                      ? coeff.前摇
                      : property === '消耗'
                        ? coeff.消耗
                        : coeff.威力;
            effect.数值 = !action && /%$/.test(String(effect.数值).trim())
              ? 缩放战斗属性变化数值(effect.数值, factor)
              : action === '倍率压制' || (!action && Number(effect.数值) < 1)
                ? scaleBattleDebuffRatio(effect.数值, factor, 1)
                : scaleBattleFactor(effect.数值, factor, 1);
          }
        }
        if (mechanism === '持续恢复' && effect.数值 !== undefined) {
          effect.数值 = scaleBattleValue(effect.数值, coeff.威力, { min: 0, digits: 4 });
        }
        if (mechanism === '结算修正' && String(effect?.结算 || '').trim() === '消耗' && effect.数值 !== undefined) {
          effect.数值 = 读取战斗数值正负(effect.数值) < 0
            ? scaleBattleDebuffRatio(effect.数值, coeff.消耗, 1)
            : scaleBattleFactor(effect.数值, coeff.消耗, 1);
        }
        if (mechanism === '结算修正' && String(effect?.结算 || '').trim() === '前摇' && effect.数值 !== undefined) {
          effect.数值 = 读取战斗数值正负(effect.数值) < 0
            ? scaleBattleDebuffRatio(effect.数值, coeff.前摇, 1)
            : scaleBattleFactor(effect.数值, coeff.前摇, 1);
        }
        if (mechanism === '掌控修正' && effect.数值 !== undefined) {
          const action = String(effect.动作 || '').trim();
          effect.数值 = !action && /%$/.test(String(effect.数值).trim())
            ? 缩放战斗属性变化数值(effect.数值, coeff.掌控)
            : action === '倍率压制' || (!action && Number(effect.数值) < 1)
              ? scaleBattleDebuffRatio(effect.数值, coeff.掌控, 1)
              : scaleBattleFactor(effect.数值, coeff.掌控, 1);
        }
        if (mechanism === '速度修正' && effect.数值 !== undefined) {
          const action = String(effect.动作 || '').trim();
          effect.数值 = !action && /%$/.test(String(effect.数值).trim())
            ? 缩放战斗属性变化数值(effect.数值, coeff.速度)
            : action === '倍率压制' || (!action && Number(effect.数值) < 1)
              ? scaleBattleDebuffRatio(effect.数值, coeff.速度, 1)
              : scaleBattleFactor(effect.数值, coeff.速度, 1);
        }
        if (mechanism === '打断' && effect.中断概率 !== undefined) {
          effect.中断概率 = scaleBattleValue(effect.中断概率, coeff.控制, { min: 0, max: 1, digits: 4 });
        }
        if (mechanism === '减速' && effect.agi_ratio !== undefined) {
          effect.agi_ratio = scaleBattleDebuffRatio(effect.agi_ratio, coeff.控制, 1);
        }
        if (mechanism === '软控') {
          if (effect.reaction_penalty !== undefined)
            effect.reaction_penalty = scaleBattleValue(effect.reaction_penalty, coeff.控制, { digits: 4 });
          if (effect.cast_speed_penalty !== undefined)
            effect.cast_speed_penalty = scaleBattleValue(effect.cast_speed_penalty, coeff.控制, { digits: 4 });
          if (effect.dodge_penalty !== undefined)
            effect.dodge_penalty = scaleBattleValue(effect.dodge_penalty, coeff.控制, { digits: 4 });
        }
        if (mechanism === '位移限制') {
          if (effect.reaction_penalty !== undefined)
            effect.reaction_penalty = scaleBattleValue(effect.reaction_penalty, coeff.控制, { digits: 4 });
          if (effect.dodge_penalty !== undefined)
            effect.dodge_penalty = scaleBattleValue(effect.dodge_penalty, coeff.控制, { digits: 4 });
          if (effect.lock_level !== undefined) effect.lock_level = scaleBattleLockLevel(effect.lock_level, coeff.控制);
        }
        if (['强制位移', '位移交换', '强制绑定/锁定'].includes(mechanism)) {
          if (effect.dodge_penalty !== undefined)
            effect.dodge_penalty = scaleBattleValue(effect.dodge_penalty, coeff.控制, { digits: 4 });
          if (effect.reaction_penalty !== undefined)
            effect.reaction_penalty = scaleBattleValue(effect.reaction_penalty, coeff.控制, { digits: 4 });
          if (effect.lock_level !== undefined) effect.lock_level = scaleBattleLockLevel(effect.lock_level, coeff.控制);
        }
        if (mechanism === '标记弱点') {
          if (effect.defense_strip !== undefined)
            effect.defense_strip = scaleBattleValue(effect.defense_strip, coeff.掌控, { min: 0, max: 1, digits: 4 });
          if (effect.spirit_resist_strip !== undefined)
            effect.spirit_resist_strip = scaleBattleValue(effect.spirit_resist_strip, coeff.掌控, { min: 0, max: 1, digits: 4 });
        }
        if (mechanism === '禁疗' && effect.heal_block_ratio !== undefined) {
          effect.heal_block_ratio = scaleBattleValue(effect.heal_block_ratio, coeff.控制, {
            min: 0,
            max: 1,
            digits: 4,
          });
        }
        if (mechanism === '共享视野') {
          if (effect.reaction_bonus !== undefined)
            effect.reaction_bonus = scaleBattleValue(effect.reaction_bonus, coeff.速度, { digits: 4 });
          if (effect.hit_bonus !== undefined)
            effect.hit_bonus = scaleBattleValue(effect.hit_bonus, coeff.掌控, { digits: 4 });
          if (effect.lock_level !== undefined) effect.lock_level = scaleBattleLockLevel(effect.lock_level, coeff.掌控);
        }
        if (mechanism === '分身') {
          if (effect.dodge_bonus !== undefined)
            effect.dodge_bonus = scaleBattleValue(effect.dodge_bonus, coeff.速度, { digits: 4 });
          if (effect.attacker_speed_bonus !== undefined)
            effect.attacker_speed_bonus = scaleBattleValue(effect.attacker_speed_bonus, coeff.速度, { digits: 4 });
          if (effect.reaction_bonus !== undefined)
            effect.reaction_bonus = scaleBattleValue(effect.reaction_bonus, coeff.速度, { digits: 4 });
          if (effect.hit_bonus !== undefined)
            effect.hit_bonus = scaleBattleValue(effect.hit_bonus, coeff.掌控, { digits: 4 });
          if (effect.lock_level !== undefined) effect.lock_level = scaleBattleLockLevel(effect.lock_level, coeff.掌控);
          if (effect.final_damage_mult !== undefined)
            effect.final_damage_mult = scaleBattleFactor(effect.final_damage_mult, coeff.威力, 1);
          if (effect.received_damage_mult !== undefined)
            effect.received_damage_mult = scaleBattleFactor(effect.received_damage_mult, coeff.威力, 1);
        }
        if (mechanism === '伤害反射' && effect.反射比例 !== undefined) {
          effect.反射比例 = scaleBattleValue(effect.反射比例, coeff.防御 || coeff.威力, { min: 0, max: 1, digits: 4 });
        }
        if (mechanism === '伤害转移' && effect.转移比例 !== undefined) {
          effect.转移比例 = scaleBattleValue(effect.转移比例, coeff.防御 || coeff.威力, { min: 0, max: 1, digits: 4 });
        }
        if (mechanism === '伤害分摊' && effect.分摊比例 !== undefined) {
          effect.分摊比例 = scaleBattleValue(effect.分摊比例, coeff.防御 || coeff.威力, { min: 0, max: 1, digits: 4 });
        }
        if (mechanism === '消耗分摊' && effect.分摊比例 !== undefined) {
          effect.分摊比例 = scaleBattleValue(effect.分摊比例, coeff.防御 || coeff.威力, { min: 0, max: 1, digits: 4 });
        }
        if (mechanism === '治疗反转' && effect.反转比例 !== undefined) {
          effect.反转比例 = scaleBattleValue(effect.反转比例, coeff.控制 || coeff.威力, { min: 0, digits: 4 });
        }
        if (mechanism === '引爆持续伤害' && effect.引爆倍率 !== undefined) {
          effect.引爆倍率 = scaleBattleValue(effect.引爆倍率, coeff.威力, { min: 0, digits: 4 });
        }
        if (mechanism === '自身位移') {
          if (effect.dodge_bonus !== undefined)
            effect.dodge_bonus = scaleBattleValue(effect.dodge_bonus, coeff.速度, { digits: 4 });
          if (effect.attacker_speed_bonus !== undefined)
            effect.attacker_speed_bonus = scaleBattleValue(effect.attacker_speed_bonus, coeff.速度, { digits: 4 });
          if (effect.reaction_bonus !== undefined)
            effect.reaction_bonus = scaleBattleValue(effect.reaction_bonus, coeff.速度, { digits: 4 });
        }
        if (mechanism === '追击位移') {
          if (effect.attacker_speed_bonus !== undefined)
            effect.attacker_speed_bonus = scaleBattleValue(effect.attacker_speed_bonus, coeff.速度, { digits: 4 });
          if (effect.hit_bonus !== undefined)
            effect.hit_bonus = scaleBattleValue(effect.hit_bonus, coeff.掌控, { digits: 4 });
          if (effect.final_damage_mult !== undefined)
            effect.final_damage_mult = scaleBattleFactor(effect.final_damage_mult, coeff.威力, 1);
          if (effect.received_damage_mult !== undefined)
            effect.received_damage_mult = scaleBattleFactor(effect.received_damage_mult, coeff.威力, 1);
        }
        if (mechanism === '脱离位移') {
          if (effect.dodge_bonus !== undefined)
            effect.dodge_bonus = scaleBattleValue(effect.dodge_bonus, coeff.速度, { digits: 4 });
          if (effect.cast_speed_bonus !== undefined)
            effect.cast_speed_bonus = scaleBattleValue(effect.cast_speed_bonus, coeff.速度, { digits: 4 });
          if (effect.reaction_bonus !== undefined)
            effect.reaction_bonus = scaleBattleValue(effect.reaction_bonus, coeff.速度, { digits: 4 });
        }
        if (effect?.原型 === '状态施加') {
          effect.计算层效果 = mergeCombatEffectMaps(createEmptyCombatEffectMap(), effect.计算层效果 || {});
          const calc = effect.计算层效果;
          if (calc.hit_bonus !== undefined)
            calc.hit_bonus = scaleBattleValue(calc.hit_bonus, coeff.掌控, { digits: 4 });
          if (calc.lock_level !== undefined) calc.lock_level = scaleBattleLockLevel(calc.lock_level, coeff.掌控);
          if (calc.final_damage_mult !== undefined)
            calc.final_damage_mult = scaleBattleFactor(calc.final_damage_mult, coeff.威力, 1);
          if (calc.received_damage_mult !== undefined)
            calc.received_damage_mult = scaleBattleFactor(calc.received_damage_mult, coeff.威力, 1);
          if (calc.final_damage_bonus !== undefined)
            calc.final_damage_bonus = scaleBattleValue(calc.final_damage_bonus, coeff.威力, { digits: 2 });
          if (calc.final_heal_mult !== undefined)
            calc.final_heal_mult = scaleBattleFactor(calc.final_heal_mult, coeff.威力, 1);
          if (calc.final_heal_bonus !== undefined)
            calc.final_heal_bonus = scaleBattleValue(calc.final_heal_bonus, coeff.威力, { digits: 2 });
          if (calc.shield_gain_mult !== undefined)
            calc.shield_gain_mult = scaleBattleFactor(calc.shield_gain_mult, coeff.威力, 1);
          if (calc.shield_gain_bonus !== undefined)
            calc.shield_gain_bonus = scaleBattleValue(calc.shield_gain_bonus, coeff.威力, { digits: 2 });
          if (calc.vit_gain_ratio !== undefined)
            calc.vit_gain_ratio = scaleBattleValue(calc.vit_gain_ratio, coeff.威力, { digits: 4 });
          if (calc.sp_gain_ratio !== undefined)
            calc.sp_gain_ratio = scaleBattleValue(calc.sp_gain_ratio, coeff.威力, { digits: 4 });
          if (calc.men_gain_ratio !== undefined)
            calc.men_gain_ratio = scaleBattleValue(calc.men_gain_ratio, coeff.威力, { digits: 4 });
          if (calc.dot_damage !== undefined)
            calc.dot_damage = scaleBattleValue(calc.dot_damage, coeff.威力, { min: 0, digits: 2 });
          if (calc.dot_damage_ratio !== undefined)
            calc.dot_damage_ratio = scaleBattleValue(calc.dot_damage_ratio, coeff.威力, { min: 0, digits: 4 });
          if (calc.reaction_penalty !== undefined)
            calc.reaction_penalty = scaleBattleValue(calc.reaction_penalty, coeff.控制, { digits: 4 });
          if (calc.cast_speed_penalty !== undefined)
            calc.cast_speed_penalty = scaleBattleValue(calc.cast_speed_penalty, coeff.控制, { digits: 4 });
          if (calc.dodge_penalty !== undefined)
            calc.dodge_penalty = scaleBattleValue(calc.dodge_penalty, coeff.控制, { digits: 4 });
          if (calc.random_target_rate !== undefined)
            calc.random_target_rate = scaleBattleValue(calc.random_target_rate, coeff.控制, {
              min: 0,
              max: 1,
              digits: 4,
            });
          if (calc.reaction_bonus !== undefined)
            calc.reaction_bonus = scaleBattleValue(calc.reaction_bonus, coeff.速度, { digits: 4 });
          if (calc.attacker_speed_bonus !== undefined)
            calc.attacker_speed_bonus = scaleBattleValue(calc.attacker_speed_bonus, coeff.速度, { digits: 4 });
          if (calc.cast_speed_bonus !== undefined)
            calc.cast_speed_bonus = scaleBattleValue(calc.cast_speed_bonus, coeff.速度, { digits: 4 });
          if (calc.dodge_bonus !== undefined)
            calc.dodge_bonus = scaleBattleValue(calc.dodge_bonus, coeff.速度, { digits: 4 });
        }
      });

      const precisionDelta = roundBattleScaledNumber(coeff.掌控 - 1, 4);
      if (Math.abs(precisionDelta) > 0.0001) {
        const precisionState = ensureBattleTransientStateEffect(skill);
        const calc = precisionState.计算层效果 || (precisionState.计算层效果 = createEmptyCombatEffectMap());
        calc.hit_bonus = roundBattleScaledNumber(Number(calc.hit_bonus || 0) + precisionDelta, 4);
      }

      return skill;
    }

    function 计算技能掌控度完整度(施术者 = {}, 技能掌控度 = null) {
      if (!技能掌控度 || typeof 技能掌控度 !== 'object' || Array.isArray(技能掌控度)) return 1;
      const 中心等级 = Number(技能掌控度.中心等级);
      const 圆满等级 = Number(技能掌控度.圆满等级);
      if (!Number.isFinite(中心等级) || !Number.isFinite(圆满等级) || 圆满等级 <= 中心等级) return 1;
      const 施术等级 = Math.max(1, Number(施术者?.lv ?? 施术者?.等级 ?? 施术者?.属性?.等级 ?? 1) || 1);
      if (施术等级 >= 圆满等级) return 1;
      const 标准差 = (圆满等级 - 中心等级) / 1.8807936081512509;
      const x = (施术等级 - 中心等级) / Math.max(0.0001, 标准差);
      const t = 1 / (1 + Math.exp(-1.702 * x));
      return Math.max(0, Math.min(1, Number(t.toFixed(4))));
    }

    function 按技能掌控度缩放倍率(数值, 完整度 = 1) {
      const 原值 = Number(数值);
      if (!Number.isFinite(原值)) return 数值;
      return roundBattleScaledNumber(1 + (原值 - 1) * 完整度, 4);
    }

    function 按技能掌控度缩放数值(数值, 完整度 = 1) {
      const 原值 = Number(数值);
      if (!Number.isFinite(原值)) return 数值;
      return roundBattleScaledNumber(原值 * 完整度, 4);
    }

    function 按技能掌控度缩放技能(skill = {}, 施术者 = {}) {
      if (skill?.__技能掌控度已缩放 === true) return skill;
      const 技能掌控度 = skill?.技能掌控度;
      const 完整度 = 计算技能掌控度完整度(施术者, 技能掌控度);
      if (完整度 >= 0.9999) return skill;
      const nextSkill = deepClone(skill || {});
      nextSkill.__技能掌控度完整度 = 完整度;
      nextSkill.__技能掌控度已缩放 = true;
      const 缩放效果列表 = effects => (Array.isArray(effects) ? effects : []).forEach(effect => {
        if (!effect || typeof effect !== 'object' || String(effect.原型 || '').trim() === '系统基础') return;
        if (effect.数值 !== undefined) {
          const 动作 = String(effect.动作 || '').trim();
          effect.数值 = !动作 && /%$/.test(String(effect.数值).trim())
            ? 缩放战斗属性变化数值(effect.数值, 完整度)
            : ['倍率提升', '倍率压制'].includes(动作) || (!动作 && Number(effect.数值) > 0 && Math.abs(Number(effect.数值) - 1) > 0.001)
              ? 按技能掌控度缩放倍率(effect.数值, 完整度)
              : 按技能掌控度缩放数值(effect.数值, 完整度);
        }
        ['护盾值', '每回合伤害', 'dot_damage', 'final_damage_bonus', 'final_heal_bonus', 'shield_gain_bonus'].forEach(key => {
          if (effect[key] !== undefined) effect[key] = 按技能掌控度缩放数值(effect[key], 完整度);
        });
        if (effect.威力倍率 !== undefined && !(String(effect.原型 || '').trim() === '伤害结算' && 读取对应等级(effect) > 0)) effect.威力倍率 = 按技能掌控度缩放数值(effect.威力倍率, 完整度);
        const 面板修改比例 = effect.面板修改比例 && typeof effect.面板修改比例 === 'object' ? effect.面板修改比例 : null;
        if (面板修改比例) {
          ['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max'].forEach(key => {
            if (面板修改比例[key] !== undefined) 面板修改比例[key] = 按技能掌控度缩放倍率(面板修改比例[key], 完整度);
          });
        }
        const 计算层效果 = effect.计算层效果 && typeof effect.计算层效果 === 'object' ? effect.计算层效果 : null;
        if (计算层效果) {
          ['final_damage_mult', 'received_damage_mult', 'final_heal_mult', 'shield_gain_mult', 'cost_ratio', 'windup_ratio'].forEach(key => {
            if (计算层效果[key] !== undefined) 计算层效果[key] = 按技能掌控度缩放倍率(计算层效果[key], 完整度);
          });
          ['cost_delta', 'windup_delta'].forEach(key => {
            if (计算层效果[key] !== undefined) 计算层效果[key] = 按技能掌控度缩放数值(计算层效果[key], 完整度);
          });
          [
            'final_damage_bonus',
            'final_heal_bonus',
            'shield_gain_bonus',
            'dot_damage',
            'dot_damage_ratio',
            'vit_gain_ratio',
            'sp_gain_ratio',
            'men_gain_ratio',
            'hot_heal_ratio',
            'heal_block_ratio',
            'cost_delta_ratio',
            'bonus_true_damage_ratio',
            'damage_absorb_ratio',
            'damage_to_heal_ratio',
            'heal_to_damage_ratio',
            'reaction_bonus',
            'reaction_penalty',
            'attacker_speed_bonus',
            'cast_speed_bonus',
            'cast_speed_penalty',
            'dodge_bonus',
            'dodge_penalty',
            'interrupt_bonus',
          ].forEach(key => {
            if (计算层效果[key] !== undefined) 计算层效果[key] = 按技能掌控度缩放数值(计算层效果[key], 完整度);
          });
        }
        ['使用效果', '授予效果'].forEach(key => 缩放效果列表(effect[key]));
        (Array.isArray(effect.条件分支) ? effect.条件分支 : []).forEach(branch => {
          if (!branch || typeof branch !== 'object') return;
          缩放效果列表(branch.替换效果);
          缩放效果列表(branch.追加效果);
        });
      });
      缩放效果列表(nextSkill._效果数组);
      return nextSkill;
    }

    function getSkillRuntimeMeta(skill) {
      const baseType = inferSkillTypeFromEffects(skill);
      const baseCostRaw = skill?.消耗 ?? '无';
      const baseCost =
        typeof baseCostRaw === 'object' ? formatCostObjectToString(baseCostRaw) : String(baseCostRaw || '无').trim() || '无';
      const baseCastTime = Number(skill?.前摇 ?? 0) || 0;
      return {
        技能分类: normalizeSkillTypeLabel(skill?.技能分类 || baseType || '无'),
        消耗: baseCost,
        cast_time: baseCastTime,
      };
    }

    const BATTLE_SKILL_SIDE_EFFECT_TRIGGER_SET = new Set(['效果生效后', '命中后', '回合结束时', '效果结束后']);
    const BATTLE_SKILL_SIDE_EFFECT_TARGET_SET = new Set(['技能释放者', '效果承受者', '双方']);
    const BATTLE_SKILL_SIDE_EFFECT_STATUS_MAP = Object.freeze({
      全属性降低: '虚弱',
      自损反噬: '反噬',
      精神紊乱: '精神紊乱',
      魂力反噬: '魂力枯竭',
      命中下降: '精神紊乱',
      动作迟缓: '迟缓',
      目标错乱: '混乱',
      施法僵直: '僵直',
    });
    const BATTLE_SKILL_SIDE_EFFECT_TYPE_SET = new Set([
      '全属性降低',
      '自损反噬',
      '致死献祭',
      '精神紊乱',
      '魂力反噬',
      '命中下降',
      '动作迟缓',
      '目标错乱',
      '施法僵直',
    ]);

    function normalizeBattleSkillSideEffectEntry(value = {}) {
      if (!value || typeof value !== 'object' || Array.isArray(value)) return null;
      const 副作用类型 = String(value.副作用类型 || '').trim();
      if (!BATTLE_SKILL_SIDE_EFFECT_TYPE_SET.has(副作用类型)) return null;
      const rawTrigger = String(value.触发时机 || '效果生效后').trim();
      const 触发时机 = BATTLE_SKILL_SIDE_EFFECT_TRIGGER_SET.has(rawTrigger) ? rawTrigger : '效果生效后';
      const rawTarget = String(value.生效对象 || '技能释放者').trim();
      const 生效对象 = BATTLE_SKILL_SIDE_EFFECT_TARGET_SET.has(rawTarget) ? rawTarget : '技能释放者';
      const 持续回合 = Math.max(0, Math.round(Number(value.持续回合 || 0)));
      const rawChance = Number(value.触发概率 ?? 1);
      const 触发概率 = Number.isFinite(rawChance) ? Math.max(0, Math.min(1, Number(rawChance.toFixed(4)))) : 1;
      const normalized = { 副作用类型, 触发时机, 生效对象, 触发概率 };
      if (副作用类型 !== '致死献祭') {
        normalized.持续回合 = 持续回合 || 1;
        normalized.副作用状态 = String(value.副作用状态 || BATTLE_SKILL_SIDE_EFFECT_STATUS_MAP[副作用类型] || 副作用类型).trim();
        if (String(value.数值 ?? '').trim()) normalized.数值 = String(value.数值 ?? '').trim();
        if (String(value.副数值 ?? '').trim()) normalized.副数值 = String(value.副数值 ?? '').trim();
      }
      if (触发时机 === '效果结束后') {
        const 关联状态 = String(value.关联状态 || '').trim();
        if (关联状态) normalized.关联状态 = 关联状态;
      }
      return normalized;
    }

    function normalizeBattleSkillSideEffectList(value = []) {
      const source = Array.isArray(value) ? value : [];
      return source
        .map(item => normalizeBattleSkillSideEffectEntry(item))
        .filter(Boolean);
    }

    function getBattleSkillSideEffectList(skill = {}) {
      return normalizeBattleSkillSideEffectList(skill?.副作用列表 || []);
    }

    function buildBattleSideEffectRuntimePayload(effect = {}) {
      const 类型 = String(effect?.副作用类型 || '').trim();
      const 数值 = Math.abs(读取战斗数值正负(effect?.数值 || '0'));
      const 副数值 = Math.abs(读取战斗数值正负(effect?.副数值 || '0'));
      const 强度 = Math.max(0, 数值);
      const 副强度 = Math.max(0, 副数值);
      const 面板修改比例 = {};
      const 战斗效果 = createEmptyCombatEffectMap();
      if (类型 === '致死献祭') 战斗效果.致死 = true;
      if (类型 === '全属性降低') {
        const 倍率 = Math.max(0.01, 1 - (强度 || 0.1));
        ['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max'].forEach(key => { 面板修改比例[key] = 倍率; });
      }
      if (类型 === '自损反噬') {
        战斗效果.misfortune_backlash_ratio = 强度 || 0.03;
        战斗效果.hit_penalty = Math.max(0.03, (强度 || 0.03) * 0.5);
      }
      if (类型 === '精神紊乱') {
        战斗效果.random_target_rate = 强度 || 0.25;
        战斗效果.reaction_penalty = 副强度 || 0.08;
      }
      if (类型 === '命中下降') 战斗效果.hit_penalty = 强度 || 0.1;
      if (类型 === '魂力反噬') {
        战斗效果.sp_gain_ratio = -(强度 || 0.05);
        战斗效果.cost_delta_ratio = 副强度 || Math.max(0.05, 强度 || 0.05);
      }
      if (类型 === '动作迟缓') {
        战斗效果.reaction_penalty = 强度 || 0.15;
        战斗效果.dodge_penalty = 副强度 || 0.1;
      }
      if (类型 === '施法僵直') 战斗效果.cast_speed_penalty = 强度 || 0.2;
      if (类型 === '目标错乱') 战斗效果.random_target_rate = 强度 || 0.3;
      return { 面板修改比例, 战斗效果 };
    }

    function estimateBattleSideEffectRisk(effect = {}, owner = null) {
      const 生效对象 = String(effect?.生效对象 || '技能释放者').trim();
      if (!['技能释放者', '双方'].includes(生效对象)) return 0;
      const 类型 = String(effect?.副作用类型 || '').trim();
      const 概率 = Math.max(0, Math.min(1, Number(effect?.触发概率 ?? 1) || 0));
      if (类型 === '致死献祭') return 3;
      const { 战斗效果 } = buildBattleSideEffectRuntimePayload(effect);
      const 持续 = Math.max(1, Number(effect?.持续回合 || 1));
      const 生命压力 = owner ? Math.max(0, 1 - getCombatHpRatio(owner)) : 0;
      const 资源压力 = owner ? Math.max(
        0,
        1 - Math.min(
          1,
          Math.max(0, Number(owner.sp || owner?.属性?.魂力 || 0)) / Math.max(1, Number(owner.sp_max || owner?.属性?.魂力上限 || 1)),
        ),
      ) : 0;
      return 概率 * Math.min(3, (
        Number(战斗效果.random_target_rate || 0) +
        Number(战斗效果.cast_speed_penalty || 0) +
        Number(战斗效果.hit_penalty || 0) +
        Math.abs(Number(战斗效果.sp_gain_ratio || 0)) +
        Number(战斗效果.cost_delta_ratio || 0) +
        Number(战斗效果.misfortune_backlash_ratio || 0) * (1 + 生命压力) +
        Number(战斗效果.dot_damage_ratio || 0) * (1 + 生命压力) +
        持续 * 0.05 +
        资源压力 * Math.abs(Number(战斗效果.sp_gain_ratio || 0))
      ));
    }

    function resolveBattleSideEffectTargets(effect = {}, caster = null, targetSet = []) {
      const targets = Array.isArray(targetSet) ? targetSet.filter(Boolean) : [];
      const targetMode = String(effect?.生效对象 || '技能释放者').trim();
      if (targetMode === '效果承受者') return targets;
      if (targetMode === '双方') return Array.from(new Set([caster, ...targets].filter(Boolean)));
      return caster ? [caster] : targets;
    }

    function 技能含正向增幅效果(skill = {}) {
      const 来源效果列表 = String(skill?.承载方式 || '').trim() === '造物承载'
        ? (Array.isArray(skill?._效果数组) ? skill._效果数组 : []).flatMap(effect => Array.isArray(effect?.使用效果) ? effect.使用效果 : [])
        : (Array.isArray(skill?._效果数组) ? skill._效果数组 : []);
      return 来源效果列表.some(effect => {
        if (!effect || typeof effect !== 'object') return false;
        const 原型 = String(effect?.原型 || '').trim();
        const 数值 = 读取战斗数值正负(effect?.数值);
        if (原型 === '属性修正') return 数值 > 0;
        if (原型 !== '结算修正') return false;
        const 结算 = String(effect?.结算 || '').trim();
        if (['造成伤害', '治疗', '技能效果'].includes(结算)) return 数值 > 0;
        if (['受到伤害', '消耗', '前摇'].includes(结算)) return 数值 < 0;
        return false;
      });
    }

    function 允许食物增幅副作用叠加(skill = {}) {
      return String(skill?.承载方式 || '').trim() === '造物承载' && 技能含正向增幅效果(skill);
    }

    function 读取虚弱状态强度(状态条目 = {}) {
      const 比例表 = 状态条目?.面板修改比例 && typeof 状态条目.面板修改比例 === 'object' ? 状态条目.面板修改比例 : {};
      const 值列表 = ['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max']
        .map(key => 1 - Number(比例表[key] ?? 1))
        .filter(value => Number.isFinite(value) && value > 0);
      return 值列表.length ? Math.max(...值列表) : 0;
    }

    function 写入虚弱状态强度(状态条目 = {}, 强度 = 0) {
      const 安全强度 = Math.max(0, Math.min(1, Number(强度 || 0)));
      const 倍率 = Math.max(0, 1 - 安全强度);
      状态条目.面板修改比例 = 状态条目.面板修改比例 && typeof 状态条目.面板修改比例 === 'object' ? 状态条目.面板修改比例 : {};
      ['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max'].forEach(key => { 状态条目.面板修改比例[key] = 倍率; });
    }

    function 叠加反噬副作用(targetChar, 强度 = 0, duration = 1, sourceName = '') {
      const 安全强度 = Math.max(0, Number(强度 || 0));
      if (!targetChar?.状态效果 || !(安全强度 > 0)) return;
      const 旧状态 = targetChar.状态效果.反噬;
      const 旧效果 = 旧状态?.战斗效果 || {};
      const 新效果 = mergeCombatEffectMaps(createEmptyCombatEffectMap(), 旧效果);
      新效果.misfortune_backlash_ratio = Number(旧效果.misfortune_backlash_ratio || 0) + 安全强度;
      targetChar.状态效果.反噬 = {
        ...(旧状态 || {}),
        类型: 'debuff',
        状态: '反噬',
        状态名称: '反噬',
        层数: Math.max(1, Number(旧状态?.层数 || 0) + 1),
        描述: `由[${sourceName || '技能'}]触发`,
        duration: Math.max(Number(旧状态?.duration || 0), Math.max(1, Number(duration || 1))),
        面板修改比例: 旧状态?.面板修改比例 || {},
        面板固定修正: 旧状态?.面板固定修正 || {},
        战斗效果: 新效果,
      };
    }

    function 叠加食物增幅副作用状态(targetChar, stateName = '', 副作用状态条目 = {}, sourceName = '', logs = []) {
      const 旧状态 = targetChar?.状态效果?.[stateName];
      if (!旧状态) return false;
      const duration = Math.max(Number(旧状态.duration || 0), Number(副作用状态条目.duration || 0), 1);
      if (stateName === '虚弱') {
        const 叠加强度 = (读取虚弱状态强度(旧状态) + 读取虚弱状态强度(副作用状态条目)) * 1.5;
        const 溢出强度 = Math.max(0, 叠加强度 - 1);
        const 下个状态 = {
          ...旧状态,
          层数: Math.max(1, Number(旧状态.层数 || 1) + 1),
          描述: `由[${sourceName || '技能'}]叠加`,
          duration,
          面板固定修正: 旧状态.面板固定修正 || {},
          战斗效果: mergeCombatEffectMaps(createEmptyCombatEffectMap(), 旧状态.战斗效果 || {}),
        };
        写入虚弱状态强度(下个状态, Math.min(1, 叠加强度));
        targetChar.状态效果[stateName] = 下个状态;
        if (溢出强度 > 0) 叠加反噬副作用(targetChar, 溢出强度, duration, sourceName);
        if (Array.isArray(logs)) logs.push(`[副作用叠加] ${targetChar.name || '目标'}的[${stateName}]叠加至${Math.round(Math.min(1, 叠加强度) * 100)}%${溢出强度 > 0 ? `，溢出${Math.round(溢出强度 * 100)}%转为[反噬]` : ''}。`);
        return true;
      }
      const 合并效果 = mergeCombatEffectMaps(旧状态.战斗效果 || createEmptyCombatEffectMap(), 副作用状态条目.战斗效果 || {});
      const 空效果 = createEmptyCombatEffectMap();
      Object.keys(合并效果).forEach(key => {
        if (typeof 合并效果[key] === 'number' && Number(合并效果[key]) !== Number(空效果[key] ?? 0)) 合并效果[key] *= 1.5;
      });
      targetChar.状态效果[stateName] = {
        ...旧状态,
        层数: Math.max(1, Number(旧状态.层数 || 1) + 1),
        描述: `由[${sourceName || '技能'}]叠加`,
        duration,
        战斗效果: 合并效果,
      };
      if (Array.isArray(logs)) logs.push(`[副作用叠加] ${targetChar.name || '目标'}的[${stateName}]副作用叠加。`);
      return true;
    }

    function applyBattleSideEffectState(targetChar, effect = {}, sourceName = '', logs = [], combatData = null, options = {}) {
      if (!targetChar) return;
      const chance = Number(effect?.触发概率 ?? 1);
      if (chance < 1 && Math.random() > chance) return;
      const { 面板修改比例, 战斗效果 } = buildBattleSideEffectRuntimePayload(effect);
      const 是否致死副作用 = 战斗效果?.致死 === true;
      if (是否致死副作用) {
        设置战斗血量值(targetChar, 0);
        let 复活结果日志 = '';
        if (getCombatHpValue(targetChar) <= 0) {
          复活结果日志 = triggerReviveEffect(targetChar, targetChar?.name || '目标') || '';
        }
        if (Array.isArray(logs)) {
          logs.push(`[副作用] ${targetChar.name || '目标'}触发[${effect?.副作用类型 || '未知副作用'}](${effect?.触发时机 || '效果生效后'})`);
          if (复活结果日志) logs.push(复活结果日志);
          else logs.push(`[副作用致死] ${targetChar.name || '目标'}受到致死反噬，生命归零。`);
        }
        return;
      }
      const duration = Math.max(1, Number(effect?.持续回合 || 0));
      const 副作用类型 = String(effect?.副作用类型 || '').trim();
      const stateName = String(effect?.副作用状态 || BATTLE_SKILL_SIDE_EFFECT_STATUS_MAP[副作用类型] || 副作用类型 || '反噬').trim();
      if (!targetChar.状态效果) targetChar.状态效果 = {};
      const nextCalc = mergeCombatEffectMaps(createEmptyCombatEffectMap(), 战斗效果);
      清理非生命流失状态伤害字段(stateName, nextCalc);
      const 副作用状态条目 = {
        类型: 'debuff',
        状态: stateName,
        状态名称: stateName,
        __本回合新附加: true,
        层数: 1,
        描述: `由[${sourceName || '技能'}]触发`,
        duration,
        面板修改比例,
        面板固定修正: {},
        战斗效果: nextCalc,
      };
      if (无视异常阻断负面或削减(targetChar, 副作用状态条目)) {
        if (Array.isArray(logs)) logs.push(`[无视异常] ${targetChar.name || '目标'}免疫了[${stateName}]副作用。`);
        return;
      }
      const 持续移除命中 = 持续状态移除阻断状态附着(targetChar, stateName, 副作用状态条目, null);
      if (持续移除命中) {
        if (Array.isArray(logs)) logs.push(`[持续状态移除] ${targetChar.name || '目标'}的[${stateName}]被[${持续移除命中.key}]拦截。`);
        return;
      }
      if (options?.允许食物增幅副作用叠加 === true && 叠加食物增幅副作用状态(targetChar, stateName, 副作用状态条目, sourceName, logs)) return;
      targetChar.状态效果[stateName] = 副作用状态条目;
      if (Array.isArray(logs)) {
        logs.push(`[副作用] ${targetChar.name || '目标'}触发[${effect?.副作用类型 || '未知副作用'}](${effect?.触发时机 || '效果生效后'})`);
      }
    }

    function getSkillType(skill) {
      return getSkillRuntimeMeta(skill).技能分类;
    }

    function getSkillCastTime(skill) {
      return getSkillRuntimeMeta(skill).cast_time;
    }

    function getSkillCostText(skill) {
      return getSkillRuntimeMeta(skill).消耗;
    }

    function getSkillTarget(skill, effect = null) {
      const targetKind = effect ? 推断战斗效果目标类型(effect) : inferSkillPrimaryTargetKind(skill);
      return mapBattleTargetKindToCombatTarget(targetKind);
    }

    function getBattleSkillSourceCategory(skill) {
      return 读取战斗来源类别上下文(skill, '魂技').来源类别;
    }

    function 装备技能跳过掌控缩放(skill = {}) {
      return getBattleSkillSourceCategory(skill) === '装备技能';
    }

    function getPrimaryDamageEffect(skill, context = {}) {
      return (
        getSkillEffects(skill, context).find(effect =>
          String(effect?.原型 || '').trim() === '伤害结算',
        ) || {}
      );
    }

    function getPrimaryStateEffect(skill) {
      return getSkillEffects(skill).find(effect => String(effect?.原型 || '').trim() === '状态施加') || {};
    }

    function 读取技能实际效果列表(skill = {}, context = {}) {
      return getSkillEffects(skill, context);
    }

    function getPrimaryStateCalc(skill) {
      return getPrimaryStateEffect(skill)?.计算层效果 || createEmptyCombatEffectMap();
    }

    function 读取状态资源锁定强度(状态 = {}) {
      return (Array.isArray(状态?.资源锁定规则) ? 状态.资源锁定规则 : []).reduce(
        (最大值, 规则) => Math.max(最大值, Number(规则?.比例 || 0)),
        0,
      );
    }

    function 技能包含资源锁定原型(skill = {}) {
      return getSkillEffects(skill).some(effect => String(effect?.原型 || '').trim() === '资源锁定');
    }

    function 计算行动打断概率(skill = {}, targetChar = {}, settleResult = {}) {
      const 打断效果列表 = getSkillEffects(skill)
        .filter(effect => 读取战斗效果标签(effect) === '打断');
      const 原型概率 = 打断效果列表
        .reduce((最大值, effect) => Math.max(最大值, Number(effect?.中断概率 || effect?.interrupt_bonus || 0)), 0);
      const 融合概率 = Number(getFusionScaledInterruptChance(skill) || 0);
      const 外部概率 = Number(settleResult?.interrupt_bonus || 0);
      const 基础概率 = Math.max(原型概率, 融合概率, 外部概率);
      if (!(基础概率 > 0)) return 0;
      return Math.max(0, Math.min(1, 基础概率));
    }

    function 是否一次性武魂融合基础属性前置(状态效果 = {}) {
      if (!状态效果 || typeof 状态效果 !== 'object') return false;
      const 标识 = String(状态效果.特殊机制标识 || '').trim();
      const 目标 = String(状态效果.目标 || '').trim();
      if (目标 && !/自身|施术者/.test(目标)) return false;
      return 标识.includes('武魂融合技') && 标识.includes('一次性释放') && 标识.includes('基础属性融合');
    }

    function 套用一次性武魂融合前置属性(基础最终属性 = {}, 状态效果 = {}) {
      const 结果 = { ...(基础最终属性 || {}) };
      const 面板修改比例 =
        状态效果?.面板修改比例 && typeof 状态效果.面板修改比例 === 'object' ? 状态效果.面板修改比例 : {};
      ['str', 'def', 'agi'].forEach(属性 => {
        if (结果[属性] !== undefined && 面板修改比例[属性] !== undefined) {
          结果[属性] = Math.round(Number(结果[属性] || 0) * Number(面板修改比例[属性] || 1));
        }
      });
      ['vit_max', 'sp_max', 'men_max'].forEach(属性 => {
        if (结果[属性] !== undefined && 面板修改比例[属性] !== undefined) {
          结果[属性] = Math.round(Number(结果[属性] || 0) * Number(面板修改比例[属性] || 1));
        }
      });
      [
        ['vit', 'vit_max'],
        ['sta', 'vit_max'],
        ['sp', 'sp_max'],
        ['men', 'men_max'],
      ].forEach(([当前属性, 上限属性]) => {
        const 倍率 = Number(面板修改比例[上限属性] ?? 1);
        if (结果[当前属性] !== undefined && Number.isFinite(倍率)) {
          结果[当前属性] = Math.min(
            Number(结果[上限属性] || Infinity),
            Math.round(Number(结果[当前属性] || 0) * 倍率),
          );
        }
      });
      return 结果;
    }

    function 读取一次性武魂融合前置战斗效果(状态效果 = {}) {
      if (!是否一次性武魂融合基础属性前置(状态效果)) return null;
      const 计算层效果 = 状态效果?.计算层效果 && typeof 状态效果.计算层效果 === 'object' ? 状态效果.计算层效果 : {};
      const 即时效果 = { ...createEmptyCombatEffectMap(), ...计算层效果 };
      即时效果.skill_effect_mult = 1;
      return 即时效果;
    }

    function getPrimaryStateName(skill) {
      const state = getPrimaryStateEffect(skill);
      if (state?.状态名称 && state.状态名称 !== '无') return state.状态名称;
      const fallback = getSkillEffects(skill).find(effect =>
        [
          '感知干扰',
          '隐身',
          '破隐',
          '标记锁定',
          '目标锁定',
          '幻境',
          '催眠',
          '认知扭曲',
          '禁疗',
          '缴械',
          '嘲讽',
          '护卫',
          '减速',
          '软控',
          '位移限制',
          '强制绑定/锁定',
          '自身位移',
          '强制位移',
          '位移交换',
          '追击',
          '追击位移',
          '脱离位移',
          '反制',
          '转化',
          '复制',
          '状态交换',
          '状态转移',
          '引爆持续伤害',
          '斩盾',
          '治疗反转',
          '封技',
          '无敌金身',
          '伤害反射',
          '伤害转移',
          '伤害分摊',
          '消耗分摊',
          '硬控',
          '霸体',
          '护盾',
        ].includes(effect?.原型),
      );
      return fallback ? 读取战斗效果标签(fallback) : '';
    }

    function getPrimaryStateFlags(skill) {
      return String(getPrimaryStateEffect(skill)?.特殊机制标识 || '无');
    }

    function 技能包含原型(skill, 原型列表 = []) {
      const targets = new Set((Array.isArray(原型列表) ? 原型列表 : [原型列表]).map(item => String(item || '').trim()).filter(Boolean));
      if (!targets.size) return false;
      return getSkillEffects(skill).some(effect => targets.has(String(effect?.原型 || '').trim()));
    }

    function 读取技能原型标签(skill) {
      return Array.from(
        new Set(
          getSkillEffects(skill)
            .flatMap(effect => [effect?.原型])
            .map(effect => String(effect || '').trim())
            .filter(Boolean),
        ),
      );
    }

    function 战斗效果含结构化持续伤害(effect = {}) {
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return false;
      const 战斗效果 = effect?.战斗效果 && typeof effect.战斗效果 === 'object' ? effect.战斗效果 : {};
      if (Number(战斗效果.dot_damage || 0) > 0 || Number(战斗效果.dot_damage_ratio || 0) > 0) return true;
      if (
        String(effect?.原型 || '').trim() === '资源变化' &&
        String(effect?.资源 || '').trim() === '生命' &&
        Number(effect?.持续回合 || effect?.duration || 0) > 0 &&
        读取战斗数值正负(effect?.数值) < 0
      ) return true;
      return String(effect?.原型 || '').trim() === '状态施加' && Number(effect?.计算层效果?.dot_damage_ratio || 0) > 0;
    }

    function 读取结构化持续伤害数值(effect = {}) {
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return 0;
      if (
        String(effect?.原型 || '').trim() === '资源变化' &&
        String(effect?.资源 || '').trim() === '生命' &&
        Number(effect?.持续回合 || effect?.duration || 0) > 0 &&
        !读取战斗数值是否百分比(effect?.数值)
      ) return Math.max(0, -读取战斗数值正负(effect?.数值));
      return 0;
    }

    function 读取结构化持续伤害比例(effect = {}) {
      if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return 0;
      if (
        String(effect?.原型 || '').trim() === '资源变化' &&
        String(effect?.资源 || '').trim() === '生命' &&
        Number(effect?.持续回合 || effect?.duration || 0) > 0 &&
        读取战斗数值是否百分比(effect?.数值)
      ) return Math.max(0, -读取战斗数值正负(effect?.数值));
      return Math.max(0, Number(effect?.计算层效果?.dot_damage_ratio || 0));
    }

    const 非生命流失状态名集合 = new Set([
      '位移限制',
      '迟缓',
      '眩晕',
      '沉默',
      '致盲',
      '封技',
      '禁疗',
      '防御剥夺',
      '精神抗性剥夺',
      '标记',
      '嘲讽',
      '护卫',
      '僵直',
      '失控',
      '精神紊乱',
      '虚弱',
    ]);

    function 状态名禁止生命流失(状态名 = '') {
      const 名称 = String(状态名 || '').trim();
      return 非生命流失状态名集合.has(名称);
    }

    function 清理非生命流失状态伤害字段(状态名 = '', 战斗效果 = {}) {
      if (!战斗效果 || typeof 战斗效果 !== 'object' || !状态名禁止生命流失(状态名)) return 战斗效果;
      战斗效果.dot_damage = 0;
      战斗效果.dot_damage_ratio = 0;
      return 战斗效果;
    }

    function 估算单位持续伤害数值(单位 = {}, effect = {}) {
      const 固定伤害 = Math.max(读取结构化持续伤害数值(effect), Number(effect?.战斗效果?.dot_damage || effect?.dot_damage || 0));
      const 比例伤害 = Math.max(读取结构化持续伤害比例(effect), Number(effect?.战斗效果?.dot_damage_ratio || effect?.dot_damage_ratio || 0));
      return Math.max(0, 固定伤害 + (比例伤害 > 0 ? Math.floor(getCombatHpMaxValue(单位) * 比例伤害) : 0));
    }

    function 读取战斗原型运行消费器(effect = {}) {
      const 原型 = String(effect?.原型 || '').trim();
      if (!原型) return '';
      if (Number(effect?.延迟回合 || 0) > 0 && 战斗原型允许延迟回合(原型)) return 'delay_burst';
      if (原型 === '伤害结算') return Number(effect?.攻击段数 || effect?.段数 || 1) > 1 ? 'multi_damage' : 'direct_damage';
      if (原型 === '时窗修正') return effect?.引爆倍率 !== undefined || effect?.结算倍率 !== undefined ? 'dot_detonate' : 'time_window';
      if (原型 === '状态转移') return 'status_transfer';
      if (原型 === '状态交换') return 'status_exchange';
      if (原型 === '复制执行') return 'copy';
      if (原型 === '资源锁定') return 'resource_lock';
      if (原型 === '机制抹消') return 'mechanism_suppress';
      if (原型 === '机制授予') return 'mechanism_grant';
      if (原型 === '召唤生成') return 'summon';
      if (原型 === '时光回溯') return 'time_rewind';
      if (原型 === '规则改写') {
        const 规则 = String(effect?.规则 || '').trim();
        if (规则 === '缴械') return 'disarm';
        return 'rule_rewrite';
      }
      if (原型 === '资源转移') {
        const 转移方式 = String(effect?.资源转移方式 || '').trim();
        if (转移方式 === '共享') return 'resource_refeed';
        if (转移方式 === '均分') return 'resource_refeed';
        if (转移方式 !== '吞噬') return '';
        return 'resource_drain';
      }
      if (原型 === '资源变化') {
        const 资源 = String(effect?.资源 || '').trim();
        if (资源 === '魂力') return 'recover_sp';
        if (资源 === '精神力') return 'recover_men';
        return 'recover_vit';
      }
      if (原型 === '护盾变化') return 读取战斗数值正负(effect?.数值) < 0 ? 'shield_break' : 'shield';
      if (原型 === '属性修正') {
        const 数值 = 读取战斗数值正负(effect?.数值);
        return 数值 < 0 ? 'attribute_debuff' : 'attribute_buff';
      }
      if (原型 === '判定修正') return 'judge_effect';
      if (原型 === '决策干扰') {
        return 'judge_effect';
      }
      if (原型 === '位移执行') {
        const 位移 = String(effect?.位移类型 || '').trim();
        const 位移对象 = String(effect?.位移对象 || '').trim();
        if (位移 === '换位') return 'position_exchange';
        if (位移 === '瞬移') return 'self_shift';
        if (位移 === '脱离') return 'disengage_shift';
        if (位移 === '拉近' && 位移对象 === '自身') return 'pursuit_shift';
        return 'hostile_shift';
      }
      if (原型 === '规则防御') {
        const 规则 = String(effect?.规则 || '').trim();
        if (规则 === '免伤') return 'block';
        if (规则 === '免死') return 'death_save';
      }
      if (原型 === '结算修正') {
        const 结算 = String(effect?.结算 || '').trim();
        const 数值 = 读取战斗数值正负(effect?.数值);
        const 表 = {
          技能效果: 'skill_effect_amplify',
          反伤: 'damage_reflect',
          伤害转移: 'damage_transfer',
          伤害吸收: 'damage_absorb',
          伤害转治疗: 'damage_to_heal',
          治疗转伤害: 'heal_to_damage',
          伤害分摊: 'damage_share',
          消耗分摊: 'cost_share',
          防御穿透: 'armor_penetration',
          反击: 'counter',
          持续伤害引爆: 'dot_detonate',
          炸环: 'ring_burst_gain',
        };
        if (结算 === '造成伤害') return 数值 < 0 ? 'damage_reduce' : 'power_amplify';
        if (结算 === '受到伤害') return 数值 < 0 ? 'damage_reduce' : 'expose_weakness';
        if (结算 === '防御剥夺' || 结算 === '精神抗性剥夺') return 'expose_weakness';
        if (结算 === '治疗') return 'heal_amplify';
        if (结算 === '消耗') return 数值 < 0 ? 'cost_reduce' : 'cost_increase';
        if (结算 === '前摇') return 数值 < 0 ? 'windup_reduce' : 'windup_increase';
        return 表[结算] || 'judge_effect';
      }
      if (原型 === '状态移除') {
        const 状态 = String(effect?.状态 || '').trim();
        return 状态 === '隐匿' ? 'reveal' : 'cleanse';
      }
      if (原型 === '状态施加') {
        if (战斗效果含结构化持续伤害(effect)) return 'dot_damage';
        const 状态 = String(effect?.状态 || '').trim();
        const 表 = {
          眩晕: 'hard_control',
          迟缓: 'slow',
          位移限制: 'position_lock',
          封技: 'skill_seal',
          沉默: 'silence',
          缴械: 'disarm',
          致盲: 'blind',
          禁疗: 'anti_heal',
          治疗反转: 'heal_inversion',
          隐匿: 'stealth',
          探查屏蔽: 'sense_block',
          护盾: 'shield',
          无视异常: '无视异常',
          霸体: 'super_armor',
          反制: 'counter',
          受击反击: 'on_hit_counter',
          资源燃烧: 'resource_burn',
          持续恢复: 'recover_over_time',
          共享视野: 'shared_vision',
          标记: 'target_lock',
          护卫: 'guard',
          嘲讽: 'taunt',
        };
        return 表[状态] || 'judge_effect';
      }
      return '';
    }

    function 读取战斗效果运行语义(effect = {}) {
      const consumer = String(effect?.运行时消费器 || 读取战斗原型运行消费器(effect)).trim();
      if (!consumer) return '上下文';
      if (LOCAL_BATTLE_SELF_ONLY_CONSUMERS.has(consumer)) return '仅自身';
      if (BATTLE_SUPPORT_RUNTIME_CONSUMERS.has(consumer) || BATTLE_DEFENSE_RUNTIME_CONSUMERS.has(consumer)) return '可赋予';
      if (BATTLE_OUTPUT_RUNTIME_CONSUMERS.has(consumer) || BATTLE_CONTROL_RUNTIME_CONSUMERS.has(consumer)) return '敌对';
      return '上下文';
    }

    function getBattleEffectRuntimeMeta(effect = {}) {
      const 原型 = String(effect?.原型 || '').trim();
      if (!原型) return null;
      const consumer = String(effect?.运行时消费器 || 读取战斗原型运行消费器(effect)).trim();
      return {
        运行时消费器: consumer,
        目标语义: 读取战斗效果运行语义(effect),
        原型入口: 原型,
        摘要提示: {},
      };
    }

    const BATTLE_OUTPUT_RUNTIME_CONSUMERS = new Set([
      'direct_damage',
      'multi_damage',
      'dot_damage',
      'dot_detonate',
      'shield_break',
      'armor_penetration',
      'damage_absorb',
    ]);
    const BATTLE_CONTROL_RUNTIME_CONSUMERS = new Set([
      'hard_control',
      'soft_control',
      'position_lock',
      'interrupt',
      'skill_seal',
      'anti_heal',
      'heal_inversion',
      'cost_increase',
      'windup_increase',
      'mastery_reduce',
      'speed_reduce',
      'perception_disturb',
      'judge_effect',
      'target_lock',
      'hostile_shift',
      'position_exchange',
      'hard_lock',
      'status_transfer',
      'dispel_buff',
      'steal_buff',
      'resource_drain',
      'mechanism_suppress',
      'element_seal',
      'rule_rewrite',
      'misfortune_backlash',
      'taunt',
      'reveal',
      'slow',
      'blind',
      'silence',
      'disarm',
      'expose_weakness',
    ]);
    const BATTLE_DEFENSE_RUNTIME_CONSUMERS = new Set([
      'shield',
      'damage_reduce',
      'block',
      'super_armor',
      'death_save',
      'invincible',
      'damage_reflect',
      'damage_transfer',
      'damage_share',
      'cost_share',
      'revive',
      'guard',
      'clone',
      'counter',
      'on_hit_counter',
    ]);
    const BATTLE_SUPPORT_RUNTIME_CONSUMERS = new Set([
      'attribute_buff',
      'cost_reduce',
      'windup_reduce',
      'mastery_raise',
      'speed_raise',
      'recover_vit',
      'recover_sp',
      'recover_men',
      'recover_over_time',
      'cleanse',
      'shared_vision',
      'self_shift',
      'pursuit_shift',
      'disengage_shift',
      'damage_to_heal',
      'resource_refeed',
      'stealth',
      'power_amplify',
      'luck_interference',
      'time_rewind',
      'summon',
      'cost_share',
    ]);
    const BATTLE_MOBILITY_RUNTIME_CONSUMERS = new Set([
      'self_shift',
      'hostile_shift',
      'position_exchange',
      'pursuit_shift',
      'disengage_shift',
      'pursuit_mark',
      'stealth',
      'sense_block',
    ]);
    const BATTLE_SPECIAL_RULE_RUNTIME_CONSUMERS = new Set([
      'clone',
      'copy',
      'counter',
      'damage_to_heal',
      'heal_to_damage',
      'status_exchange',
      'status_transfer',
      'hard_lock',
      'judge_effect',
      'self_rule_rewrite',
      'self_random_variance',
      'self_mirror',
      'self_sacrifice_gain',
      'dot_detonate',
      'shield_break',
      'resource_drain',
      'resource_refeed',
      'mechanism_suppress',
      'ring_burst_gain',
      'rule_rewrite',
      'luck_interference',
      'misfortune_backlash',
      'time_rewind',
      'summon',
    ]);
    const BATTLE_SUSTAIN_RUNTIME_CONSUMERS = new Set([
      'dot_damage',
      'recover_over_time',
      'shield',
      'damage_reduce',
      'super_armor',
      'shared_vision',
      'clone',
      'guard',
      'stealth',
      'sense_block',
      'taunt',
      'slow',
      'blind',
      'silence',
      'disarm',
      'target_lock',
      'perception_disturb',
      'summon',
    ]);
    const BATTLE_TRIGGER_RUNTIME_CONSUMERS = new Set([
      'block',
      'death_save',
      'invincible',
      'damage_reflect',
      'damage_transfer',
      'damage_share',
      'cost_share',
      'revive',
      'counter',
      'on_hit_counter',
      'self_rule_rewrite',
    ]);

    const LOCAL_BATTLE_GROUP_GRANTABLE_CONSUMERS = new Set([
      'attribute_buff',
      'shield',
      'damage_reduce',
      'shared_vision',
      'recover_vit',
      'recover_sp',
      'recover_men',
      'recover_over_time',
      'cleanse',
      'damage_share',
      'resource_refeed',
    ]);
    const LOCAL_BATTLE_SELF_ONLY_CONSUMERS = new Set([
      'self_rule_rewrite',
      'self_random_variance',
      'self_mirror',
      'self_sacrifice_gain',
      'summon',
    ]);
    const LOCAL_BATTLE_MECHANISM_CONSUMER_BY_LABEL = Object.freeze({
      多段伤害: 'multi_damage',
      硬控: 'hard_control',
      软控: 'soft_control',
      位移限制: 'position_lock',
      打断: 'interrupt',
      封技: 'skill_seal',
      单属性削弱: 'attribute_debuff',
      多属性削弱: 'attribute_debuff',
      禁疗: 'anti_heal',
      治疗反转: 'heal_inversion',
      掌控压制: 'mastery_reduce',
      速度压制: 'speed_reduce',
      单属性增益: 'attribute_buff',
      多属性增益: 'attribute_buff',
      全属性增益: 'attribute_buff',
      掌控提升: 'mastery_raise',
      速度提升: 'speed_raise',
      威力增幅: 'power_amplify',
      技能效果增幅: 'skill_effect_amplify',
      元素封禁: 'element_seal',
      护盾: 'shield',
      承伤修正: 'damage_reduce',
      免伤: 'block',
      霸体: 'super_armor',
      免死: 'death_save',
      '免死/锁血': 'death_save',
      无敌金身: '无视异常',
      伤害反射: 'damage_reflect',
      伤害转移: 'damage_transfer',
      伤害分摊: 'damage_share',
      消耗分摊: 'cost_share',
      体力恢复: 'recover_vit',
      魂力恢复: 'recover_sp',
      精神恢复: 'recover_men',
      持续恢复: 'recover_over_time',
      解控: 'cleanse',
      净化: 'cleanse',
      感知干扰: 'perception_disturb',
      探查屏蔽: 'sense_block',
      标记锁定: 'judge_effect',
      共享视野: 'shared_vision',
      幻境: 'judge_effect',
      催眠: 'judge_effect',
      认知扭曲: 'judge_effect',
      目标锁定: 'target_lock',
      自身位移: 'self_shift',
      强制位移: 'hostile_shift',
      位移交换: 'position_exchange',
      追击位移: 'pursuit_shift',
      脱离位移: 'disengage_shift',
      追击: 'pursuit_mark',
      召唤: 'summon',
      分身: 'clone',
      复制: 'copy',
      反制: 'counter',
      受击反击: 'on_hit_counter',
      伤害转回复: 'damage_to_heal',
      回复转伤害: 'heal_to_damage',
      状态交换: 'status_exchange',
      状态转移: 'status_transfer',
      '强制绑定/锁定': 'hard_lock',
      规则改写: 'rule_rewrite',
      炸环: 'ring_burst_gain',
      时光回溯: 'time_rewind',
      气运干涉: 'luck_interference',
      引爆持续伤害: 'dot_detonate',
      斩盾: 'shield_break',
      驱散增益: 'dispel_buff',
      窃取增益: 'steal_buff',
      隐身: 'stealth',
      护卫: 'guard',
      嘲讽: 'taunt',
      破隐: 'reveal',
      减速: 'slow',
      迟缓: 'slow',
      致盲: 'blind',
      沉默: 'silence',
      缴械: 'disarm',
      标记弱点: 'expose_weakness',
      斩杀补伤: 'judge_effect',
      穿透: 'armor_penetration',
      伤害吸收: 'damage_absorb',
      吞噬: 'resource_drain',
      能力共享: 'resource_refeed',
      机制抹消: 'mechanism_suppress',
      资源锁定: 'resource_lock',
    });
    const LOCAL_BATTLE_DEFENSE_NATURE_BY_LABEL = Object.freeze({
      反制: '反制',
      受击反击: '反制',
      免死: '免死',
      '免死/锁血': '免死',
      无敌金身: '无视异常',
      伤害分摊: '分摊',
      消耗分摊: '分摊',
      伤害反射: '反射',
      伤害转移: '反射',
      霸体: '霸体',
      护卫: '护卫',
      分身: '分身',
      隐身: '分身',
      免伤: '免伤',
      承伤修正: '承伤修正',
      护盾: '护盾',
    });
    const LOCAL_BATTLE_RECOVER_NATURE_BY_LABEL = Object.freeze({
      体力恢复: '体力恢复',
      魂力恢复: '资源回复',
      精神恢复: '资源回复',
      持续恢复: '持续恢复',
      净化: '净化',
      解控: '净化',
      复活机制: '复活机制',
      能力共享: '资源回复',
    });
    function collectBattleMechanismTagsFromDescriptor(name = '', cond = {}) {
      const tags = [];
      const ce = cond?.战斗效果 || cond?.计算层效果 || {};
      const text = String(name || '').trim();
      const 原型 = String(cond?.原型 || cond?.运行机制 || '').trim();
      if (原型 === '规则防御') tags.push('防御机制');
      if (原型 === '机制抹消' || 原型 === '机制授予') tags.push('特殊规则');
      if (原型 === '状态施加') {
        const 状态名 = String(cond?.状态 || cond?.状态名称 || '').trim();
        if (['眩晕', '沉默', '缴械', '致盲', '封技', '迟缓', '位移限制'].includes(状态名)) tags.push('控制机制');
        if (['持续恢复'].includes(状态名)) tags.push('回复机制');
        if (['无视异常', '霸体', '护盾'].includes(状态名)) tags.push('防御机制', '增益');
      }
      if (cond?.类型 === 'buff') tags.push('增益');
      if (
        Number(cond?.shield_value || 0) > 0 ||
        /护盾|屏障|结界/.test(text)
      )
        tags.push('护盾', '防御机制', '增益');
      if (Number(ce.stealth_level || 0) > 0 || /隐身|潜行/.test(text)) tags.push('隐身', '增益');
      if (ce.探查屏蔽 === true || /探查屏蔽|感知屏蔽|精神屏蔽/.test(text)) tags.push('特殊规则', '增益');
      if (Number(ce.revive_count || 0) > 0 || /复活/.test(text)) tags.push('复活机制', '回复机制', '防御机制');
      if (
        Number(ce.death_save_count || 0) > 0 ||
        Number(ce.min_hp_floor || 0) > 0 ||
        ce.invincible === true ||
        ce.super_armor === true ||
        Number(ce.block_count || 0) > 0 ||
        Number(ce.damage_reduction || 0) > 0 ||
        Number(ce.damage_reflect_ratio || 0) > 0 ||
        Number(ce.damage_transfer_ratio || 0) > 0 ||
        Number(ce.damage_share_ratio || 0) > 0 ||
        Number(ce.cost_share_ratio || 0) > 0
      )
        tags.push('防御机制');
      if (
        Number(ce.final_heal_mult || 1) > 1 ||
        Number(ce.final_heal_bonus || 0) > 0 ||
        Number(ce.vit_gain_ratio || 0) > 0 ||
        Number(ce.sp_gain_ratio || 0) > 0 ||
        Number(ce.men_gain_ratio || 0) > 0 ||
        Number(ce.hot_heal_ratio || 0) > 0 ||
        /回血|治疗|再生|回复|回魂|回精神/.test(text)
      )
        tags.push('回复机制');
      if (
        ce.skip_turn === true ||
        ce.cannot_react === true ||
        ce.skill_seal === true ||
        Number(ce.lock_level || 0) > 0 ||
        Number(ce.reaction_penalty || 0) > 0 ||
        Number(ce.cast_speed_penalty || 0) > 0 ||
        Number(ce.dodge_penalty || 0) > 0 ||
        Number(ce.heal_block_ratio || 0) > 0 ||
        Number(ce.heal_inversion_ratio || 0) > 0 ||
        /封技|沉默|缴械|打断|锁定|禁疗|控制|硬控|软控|减速|迟缓|嘲讽|破隐/.test(text)
      )
        tags.push('控制机制');
      if (
        /分身|复制|状态交换|状态转移|引爆持续伤害|斩盾|吞噬|能力共享|机制抹消|共享视野|护卫/.test(text)
      )
        tags.push('特殊规则');
      return Array.from(new Set(tags));
    }

    function getActiveMechanismSuppressionEntries(targetChar) {
      if (!targetChar?.状态效果) return [];
      return Object.entries(targetChar.状态效果)
        .flatMap(([key, cond]) => (Array.isArray(cond?.抹消规则) ? cond.抹消规则 : []).map(rule => ({
          key,
          cond,
          tags: [读取战斗机制抹消对象摘要(rule?.抹消对象 || {})],
        })));
    }

    function 读取资源变化抹消规则(targetChar, 资源 = '', options = {}) {
      const 资源文本 = String(资源 || '').trim();
      return 战斗机制抹消命中(
        targetChar,
        '最终结果',
        资源文本 ? { 原型: '资源变化', 资源: 资源文本 } : { 原型: '资源变化' },
        { 用途: '封锁', ...options },
      );
    }

    function 读取战斗资源键标签(resourceKey = '') {
      if (resourceKey === 'hp') return '生命';
      if (resourceKey === 'vit') return '体力';
      if (resourceKey === 'men') return '精神力';
      return '魂力';
    }

    function 读取规则改写目标标识(单位 = {}) {
      return String(单位?.召唤键 || 单位?.name || 单位?.名称 || 单位?.charKey || 单位?.key || '').trim();
    }

    function 读取规则改写强度(规则 = {}) {
      const 原始值 = 读取战斗数值正负(规则?.强度 ?? 规则?.数值 ?? '100%');
      const 强度 = Math.abs(Number(原始值 || 0)) || 1;
      return Math.max(0, Math.min(2, 强度));
    }

    function 读取战斗规则改写运行态列表(combatData = null) {
      const 战斗数据 = combatData || getCurrentBattleContextSnapshot() || {};
      if (!Array.isArray(战斗数据.__规则改写运行态)) 战斗数据.__规则改写运行态 = [];
      return 战斗数据.__规则改写运行态;
    }

    function 规则改写目标命中(规则 = {}, 目标 = null) {
      if (!目标) return false;
      if (String(规则?.目标范围 || '').trim() === '全场') return true;
      const 目标标识 = 读取规则改写目标标识(目标);
      return !!目标标识 && String(规则?.目标标识 || '').trim() === 目标标识;
    }

    function 读取规则改写规则(combatData = null, 目标 = null, 规则名 = '') {
      const 名称 = String(规则名 || '').trim();
      if (!名称 || !目标) return null;
      return 读取战斗规则改写运行态列表(combatData)
        .filter(规则 => String(规则?.规则 || '').trim() === 名称)
        .filter(规则 => Math.max(0, Number(规则?.剩余回合 || 0)) > 0)
        .filter(规则 => 规则改写目标命中(规则, 目标))
        .sort((a, b) => 读取规则改写强度(b) - 读取规则改写强度(a))[0] || null;
    }

    function 登记战斗规则改写运行态(combatData = {}, effect = {}, targetUnits = [], caster = {}, skill = {}) {
      const 规则 = String(effect?.规则 || '').trim();
      if (!规则) return 0;
      if (规则 === '死亡转存活') return 0;
      const 目标文本 = String(effect?.目标 || '').trim();
      const 目标范围 = 目标文本 === '全场' ? '全场' : '目标';
      const 持续回合 = Math.max(1, Math.round(Number(effect?.持续回合 || 1)));
      const 运行态列表 = 读取战斗规则改写运行态列表(combatData);
      const 当前回合 = Math.max(0, Number(combatData?.回合 || 0));
      const 目标列表 = 目标范围 === '全场'
        ? [null]
        : dedupeCombatTargetList(Array.isArray(targetUnits) ? targetUnits : [targetUnits]).filter(Boolean);
      let 数量 = 0;
      目标列表.forEach(目标 => {
        const 目标标识 = 目标范围 === '全场' ? '全场' : 读取规则改写目标标识(目标);
        if (!目标标识) return;
        运行态列表.push({
          规则,
          目标范围,
          目标标识,
          剩余回合: 持续回合,
          强度: 读取规则改写强度(effect),
          数值: effect?.数值,
          创建回合: 当前回合,
          来源角色: caster?.name || caster?.名称 || '',
          来源技能: skill?.name || skill?.魂技名 || '',
        });
        数量 += 1;
      });
      return 数量;
    }

    function 递减战斗规则改写运行态(combatData = {}) {
      if (!combatData || !Array.isArray(combatData.__规则改写运行态)) return '';
      const 当前回合 = Math.max(0, Number(combatData?.回合 || 0));
      const 保留列表 = [];
      let 消散数量 = 0;
      combatData.__规则改写运行态.forEach(规则 => {
        if (!规则 || typeof 规则 !== 'object') return;
        if (Number(规则?.创建回合 || 0) === 当前回合 && 规则.__创建回合已保留 !== true) {
          规则.__创建回合已保留 = true;
          保留列表.push(规则);
          return;
        }
        规则.剩余回合 = Math.max(0, Number(规则?.剩余回合 || 0) - 1);
        if (规则.剩余回合 > 0) 保留列表.push(规则);
        else 消散数量 += 1;
      });
      combatData.__规则改写运行态 = 保留列表;
      return 消散数量 > 0 ? `[规则改写] ${消散数量}条临时规则改写已结束。` : '';
    }

    const 战斗机制抹消可匹配字段表 = Object.freeze({
      状态施加: Object.freeze(['状态']),
      状态移除: Object.freeze(['状态', '匹配原型', '资源']),
      状态转移: Object.freeze(['状态']),
      状态交换: Object.freeze(['状态']),
      规则防御: Object.freeze(['规则']),
      规则改写: Object.freeze(['规则']),
      资源变化: Object.freeze(['资源']),
      资源转移: Object.freeze(['资源', '资源转移方式']),
      资源锁定: Object.freeze(['资源']),
      结算修正: Object.freeze(['结算']),
      属性修正: Object.freeze(['属性']),
      复制执行: Object.freeze(['复制类型']),
    });

    const 战斗机制抹消支持原型 = new Set([
      '资源变化',
      '资源转移',
      '护盾变化',
      '属性修正',
      '判定修正',
      '结算修正',
      '炸环',
      '状态施加',
      '时窗修正',
      '状态移除',
      '规则防御',
      '状态转移',
      '状态交换',
      '资源锁定',
      '规则改写',
      '机制抹消',
      '机制授予',
      '复制执行',
      '时光回溯',
      '位移执行',
      '决策干扰',
      '召唤生成',
    ]);

    const 战斗机制抹消全部字段值 = new Set(['全部', '抹消全部', '全部状态', '全部规则', '全部资源', '全部结算', '全部属性', '全部原型', '全部转移方式', '全部复制类型']);

    function 读取战斗机制抹消可匹配字段(原型 = '') {
      const 原型名 = String(原型 || '').trim();
      return Array.isArray(战斗机制抹消可匹配字段表[原型名]) ? 战斗机制抹消可匹配字段表[原型名] : [];
    }

    function 规范化战斗机制抹消对象(value = {}) {
      const source = value && typeof value === 'object' && !Array.isArray(value)
        ? value
        : { 原型: String(value || '').trim() || '机制授予' };
      const result = { 原型: String(source.原型 || '机制授予').trim() || '机制授予' };
      if (!战斗机制抹消支持原型.has(result.原型)) return result;
      读取战斗机制抹消可匹配字段(result.原型).forEach(key => {
        const 原始列表 = Array.isArray(source[key]) ? source[key] : String(source[key] ?? '').split(/[、,，|/]/);
        if (原始列表.some(item => 战斗机制抹消全部字段值.has(String(item ?? '').trim()))) return;
        const 字段列表 = Array.from(new Set(
          原始列表
            .map(item => String(item ?? '').trim())
            .filter(item => item && item !== '无' && !战斗机制抹消全部字段值.has(item)),
        ));
        if (字段列表.length === 1) result[key] = 字段列表[0];
        else if (字段列表.length > 1) result[key] = 字段列表;
      });
      return result;
    }

    function 读取战斗机制抹消对象摘要(object = {}) {
      const matcher = 规范化战斗机制抹消对象(object);
      const parts = [String(matcher.原型 || '机制')];
      读取战斗机制抹消可匹配字段(matcher.原型).forEach(key => {
        const 字段值 = Array.isArray(matcher[key]) ? matcher[key].map(item => String(item || '').trim()).filter(Boolean).join('、') : String(matcher[key] || '').trim();
        if (字段值) parts.push(`${key}:${字段值}`);
      });
      return parts.join(' ');
    }

    function 读取战斗机制候选原型(candidate = {}) {
      const 原型 = String(candidate?.原型 || candidate?.来源原型摘要 || '').trim();
      if (原型) return 原型;
      if (Array.isArray(candidate?.抹消规则) || candidate?.战斗效果?.mechanism_suppress === true) return '机制抹消';
      if (candidate?.效果授予状态 === true || candidate?.战斗效果?.mechanism_grant === true || Array.isArray(candidate?.授予效果)) return '机制授予';
      if (Number(candidate?.shield_value || 0) > 0) return '护盾变化';
      const ce = candidate?.战斗效果 || {};
      if (Number(ce.block_count || 0) > 0 || Number(ce.death_save_count || 0) > 0 || ce.invincible === true || ce.super_armor === true || Number(ce.revive_count || 0) > 0) return '规则防御';
      return '';
    }

    function 战斗机制字段匹配(expected, values = []) {
      const 预期列表 = (Array.isArray(expected) ? expected : [expected]).map(item => String(item || '').trim()).filter(Boolean);
      if (!预期列表.length) return true;
      const 候选列表 = values.map(item => String(item || '').trim()).filter(Boolean);
      return 预期列表.some(item => 候选列表.includes(item));
    }

    function 战斗机制对象匹配(matcher = {}, candidate = {}) {
      const rule = 规范化战斗机制抹消对象(matcher);
      if (String(rule.原型 || '').trim() !== 读取战斗机制候选原型(candidate)) return false;
      if (!战斗机制字段匹配(rule.状态 || rule.状态名称, [candidate?.状态, candidate?.状态名称, candidate?.name, candidate?.key])) return false;
      if (!战斗机制字段匹配(rule.规则, [candidate?.规则, candidate?.战斗效果?.规则])) return false;
      if (!战斗机制字段匹配(rule.资源, [candidate?.资源])) return false;
      if (!战斗机制字段匹配(rule.结算, [candidate?.结算])) return false;
      if (!战斗机制字段匹配(rule.属性, [candidate?.属性])) return false;
      if (!战斗机制字段匹配(rule.匹配原型, [candidate?.匹配原型])) return false;
      if (!战斗机制字段匹配(rule.资源转移方式, [candidate?.资源转移方式])) return false;
      if (!战斗机制字段匹配(rule.复制类型, [candidate?.复制类型])) return false;
      return true;
    }

    function 读取战斗机制抹消规则列表(targetChar, point = '') {
      if (!targetChar?.状态效果) return [];
      void point;
      return Object.entries(targetChar.状态效果)
        .flatMap(([key, cond]) => (Array.isArray(cond?.抹消规则) ? cond.抹消规则 : []).map((rule, index) => ({
          key,
          状态: cond,
          规则索引: index,
          抹消对象: 规范化战斗机制抹消对象(rule?.抹消对象),
        })));
    }

    function 战斗机制抹消命中(targetChar, point = '', candidate = {}, options = {}) {
      const purpose = String(options?.用途 || '封锁').trim();
      const 命中规则 = 读取战斗机制抹消规则列表(targetChar, point).find(rule => {
        void purpose;
        if (String(candidate?.原型 || '').trim() === '伤害结算' && String(point || '').trim() !== '源动作') return false;
        return 战斗机制对象匹配(rule.抹消对象, candidate);
      }) || null;
      if (
        命中规则 &&
        options?.消费 !== false &&
        String(命中规则?.状态?.抹消规则?.[命中规则.规则索引]?.抹消方式 || '').trim() === '阻断本次'
      ) {
        命中规则.状态.抹消规则.splice(命中规则.规则索引, 1);
      }
      return 命中规则;
    }

    function 移除战斗机制抹消命中节点(targetChar, point = '', matcher = {}, options = {}) {
      if (!targetChar?.状态效果) return [];
      const excluded = new Set(Array.isArray(options.excludeKeys) ? options.excludeKeys : []);
      const removed = [];
      Object.entries(targetChar.状态效果).forEach(([key, cond]) => {
        if (excluded.has(key)) return;
        if (!cond || typeof cond !== 'object' || Array.isArray(cond)) return;
        if (String(key || '').startsWith(AUTO_PROJECTED_CONDITION_PREFIX)) return;
        const candidate = { ...cond, key, name: key };
        const 延迟效果命中 = cond.延迟效果 === true &&
          Array.isArray(cond.结算效果) &&
          String(point || '').trim() !== '源动作' &&
          cond.结算效果.some(effect => String(effect?.原型 || '').trim() !== '伤害结算' && 战斗机制对象匹配(matcher, effect));
        const 抹消规则内部命中 = Array.isArray(cond.抹消规则) &&
          cond.抹消规则.some(rule => 战斗机制对象匹配(matcher, 规范化战斗机制抹消对象(rule?.抹消对象)));
        const 候选原型 = 读取战斗机制候选原型(candidate);
        const 匹配原型 = String(规范化战斗机制抹消对象(matcher).原型 || '').trim();
        if (point === '最终结果' && 候选原型 === '机制授予' && 匹配原型 !== '机制授予') return;
        if (!延迟效果命中 && !抹消规则内部命中 && !战斗机制对象匹配(matcher, candidate)) return;
        delete targetChar.状态效果[key];
        removed.push(key);
        if (targetChar.持续效果) {
          Object.keys(targetChar.持续效果).forEach(sustainKey => {
            if (targetChar.持续效果[sustainKey]?.related_condition === key) delete targetChar.持续效果[sustainKey];
          });
        }
        if (targetChar.召唤键) 同步召唤单位镜像(targetChar);
      });
      return removed;
    }

    function 移除战斗机制抹消命中运行态(combatData = {}, point = '', matcher = {}, target = null) {
      if (!combatData || !Array.isArray(combatData.__规则改写运行态)) return [];
      if (String(point || '').trim() === '源动作') return [];
      const removed = [];
      combatData.__规则改写运行态 = combatData.__规则改写运行态.filter(规则 => {
        const candidate = { 原型: '规则改写', 规则: 规则?.规则 };
        if (target && !规则改写目标命中(规则, target)) return true;
        if (!战斗机制对象匹配(matcher, candidate)) return true;
        removed.push(String(规则?.规则 || '规则改写').trim() || '规则改写');
        return false;
      });
      return removed;
    }

    function 技能包含运行语义(skill, semanticKey = '') {
      return getSkillEffects(skill).some(effect => {
        const consumer = 读取战斗原型运行消费器(effect);
        if (semanticKey === '群体赋予') return LOCAL_BATTLE_GROUP_GRANTABLE_CONSUMERS.has(consumer);
        return 读取战斗效果运行语义(effect) === semanticKey;
      });
    }

    function skillTargetsFriendlySide(skill) {
      const 战斗目标类型 = inferSkillPrimaryTargetKind(skill);
      return ['自身', '友方单体', '友方群体', '全场'].includes(战斗目标类型);
    }

    function skillTargetsEnemySide(skill) {
      const 战斗目标类型 = inferSkillPrimaryTargetKind(skill);
      return ['敌方单体', '敌方群体', '全场'].includes(战斗目标类型);
    }

    function skillCanGrantFriendlyMechanism(skill) {
      return skillTargetsFriendlySide(skill) && 技能包含运行语义(skill, '可赋予');
    }

    function getSkillRuntimeConsumerKeys(skill) {
      return Array.from(
        new Set(
          getSkillEffects(skill)
            .flatMap(effect => {
              const direct = String(effect?.运行时消费器 || '').trim();
              return [direct, 读取战斗原型运行消费器(effect)];
            })
            .filter(Boolean),
        ),
      );
    }

    function hasBattleSkillRuntimeConsumer(skill, consumerKeys = []) {
      const targetKeys = Array.isArray(consumerKeys) ? consumerKeys : [consumerKeys];
      const keySet = new Set(
        targetKeys
          .map(key => String(key || '').trim())
          .filter(Boolean),
      );
      if (!(keySet.size > 0)) return false;
      return getSkillRuntimeConsumerKeys(skill).some(key => keySet.has(key));
    }

    function 技能具有保命倾向(skill, context = {}) {
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      return (
        summary.防御性质 !== '无' ||
        summary.回复性质 === '复活机制' ||
        hasBattleSkillRuntimeConsumer(skill, [...BATTLE_DEFENSE_RUNTIME_CONSUMERS]) ||
        hasBattleSkillRuntimeConsumer(skill, ['recover_vit', 'cleanse', 'recover_over_time']) && skillTargetsFriendlySide(skill)
      );
    }

    function 技能具有团队保护倾向(skill, context = {}) {
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      const 来源类别 = context.来源类别 || getBattleSkillSourceCategory(skill);
      const 友方目标 = skillTargetsFriendlySide(skill);
      return (
        skillCanGrantFriendlyMechanism(skill) ||
        summary.协同性 === '高' ||
        summary.回复性质 !== '无' && 友方目标 ||
        技能具有保命倾向(skill, { summary }) && 友方目标 ||
        hasBattleSkillRuntimeConsumer(skill, [
          'guard',
          'damage_share',
          'revive',
          'shield',
          'damage_reduce',
          'death_save',
          'invincible',
          'resource_refeed',
          'shared_vision',
        ]) && 友方目标 ||
        (来源类别 === '武魂融合技' && ['辅助', '防御'].includes(getSkillType(skill)))
      );
    }

    function 技能具有规则压制倾向(skill, context = {}) {
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      const mainType = context.mainType || inferMainTypeFromEffects(skill);
      const damage = context.damage || getPrimaryDamageEffect(skill);
      return (
        ['伤害类', '控制类', '削弱类', '特殊规则类'].includes(mainType) ||
        summary.控制强度 !== '无' ||
        summary.爆发级别 !== '无' ||
        skillTargetsEnemySide(skill) && 技能包含运行语义(skill, '敌对') ||
        hasBattleSkillRuntimeConsumer(skill, [
          ...BATTLE_OUTPUT_RUNTIME_CONSUMERS,
          ...BATTLE_CONTROL_RUNTIME_CONSUMERS,
          'dot_detonate',
          'shield_break',
          'resource_drain',
          'resource_refeed',
          'mechanism_suppress',
          'copy',
          'ring_burst_gain',
          'time_rewind',
          'luck_interference',
          'misfortune_backlash',
          'element_seal',
          'power_amplify',
          'skill_effect_amplify',
          'heal_to_damage',
        ]) ||
        Number(damage?.威力倍率 || 0) > 0
      );
    }

    function getSkillSummaryHint(skill, key = '', fallback = '') {
      const normalizedKey = String(key || '').trim();
      if (!normalizedKey) return fallback;
      const entries = getSkillEffects(skill)
        .map(effect => getBattleEffectRuntimeMeta(effect))
        .filter(meta => meta && meta.摘要提示 && typeof meta.摘要提示 === 'object');
      for (const meta of entries) {
        const value = String(meta.摘要提示[normalizedKey] || '').trim();
        if (value) return value;
      }
      return fallback;
    }

    function isBattleSkillDefensiveProfile(skill, context = {}) {
      const skillType = context.skillType || getSkillType(skill);
      const mainType = context.mainType || inferMainTypeFromEffects(skill);
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      const 来源类别 = context.来源类别 || getBattleSkillSourceCategory(skill);
      return (
        skillType === '防御' ||
        mainType === '防御类' ||
        summary.防御性质 !== '无' ||
        summary.回复性质 === '复活机制' ||
        技能具有保命倾向(skill, { summary }) ||
        技能具有团队保护倾向(skill, { summary, 来源类别 }) ||
        (来源类别 === '武魂融合技' && ['防御', '辅助'].includes(skillType))
      );
    }

    function isBattleSkillOffensiveProfile(skill, context = {}) {
      const skillType = context.skillType || getSkillType(skill);
      const mainType = context.mainType || inferMainTypeFromEffects(skill);
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      return (
        skillType === '输出' ||
        mainType === '伤害类' ||
        summary.爆发级别 !== '无' ||
        Number(getPrimaryDamageEffect(skill)?.威力倍率 || 0) > 0
      );
    }

    function isBattleSkillControlProfile(skill, context = {}) {
      const mainType = context.mainType || inferMainTypeFromEffects(skill);
      const calc = context.calc || getPrimaryStateCalc(skill);
      const flags = context.flags || getPrimaryStateFlags(skill);
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      return (
        mainType === '控制类' ||
        mainType === '削弱类' ||
        技能具有规则压制倾向(skill, { summary, mainType }) ||
        summary.控制强度 === '硬控' ||
        summary.控制强度 === '软控' ||
        Number(calc.lock_level || 0) > 0 ||
        Number(calc.reaction_penalty || 0) > 0 ||
        Number(calc.cast_speed_penalty || 0) > 0 ||
        Number(calc.dodge_penalty || 0) > 0 ||
        技能包含资源锁定原型(skill) ||
        flags.includes('硬控')
      );
    }

    function isBattleSkillTeamSupportProfile(skill, context = {}) {
      const skillType = context.skillType || getSkillType(skill);
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      const 来源类别 = context.来源类别 || getBattleSkillSourceCategory(skill);
      return (
        skillCanGrantFriendlyMechanism(skill) ||
        summary.协同性 === '高' ||
        summary.回复性质 !== '无' ||
        技能具有团队保护倾向(skill, { summary, 来源类别 }) ||
        (来源类别 === '武魂融合技' && ['辅助', '防御'].includes(skillType))
      );
    }

    function 是团队保护技能(skill, context = {}) {
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      const 战斗目标类型 = inferSkillPrimaryTargetKind(skill);
      const 防御性质 = String(summary.防御性质 || '无');
      const 友方目标 = ['友方单体', '友方群体', '全场'].includes(战斗目标类型);
      const 团队防御性质 = ['护盾', '减伤', '免伤', '免死', '无敌', '分摊', '护卫'].includes(防御性质);
      return (
        (友方目标 && 团队防御性质) ||
        (友方目标 && 技能具有团队保护倾向(skill, { summary })) ||
        (友方目标 && hasBattleSkillRuntimeConsumer(skill, ['guard', 'damage_share', 'shield', 'damage_reduce', 'death_save', 'invincible']))
      );
    }

    function isBattleSkillAntiHealProfile(skill, context = {}) {
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      const calc = context.calc || getPrimaryStateCalc(skill);
      return (
        hasBattleSkillRuntimeConsumer(skill, ['anti_heal', 'heal_inversion']) ||
        summary.控制强度 === '软控' && Number(calc.heal_block_ratio || 0) > 0 ||
        Number(calc.heal_inversion_ratio || 0) > 0
      );
    }

    function isBattleSkillExecuteProfile(skill, context = {}) {
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      const damage = context.damage || getPrimaryDamageEffect(skill);
      return (
        hasBattleSkillRuntimeConsumer(skill, ['judge_effect']) ||
        (summary.爆发级别 === '高' && Number(damage?.威力倍率 || 0) >= 180)
      );
    }

    function isBattleSkillShieldBreakProfile(skill, context = {}) {
      const damage = context.damage || getPrimaryDamageEffect(skill);
      return (
        hasBattleSkillRuntimeConsumer(skill, ['shield_break']) ||
        Number(damage?.防御穿透 || 0) >= 15 ||
        /破甲|穿透|粉碎|斩盾/.test(String(skill?.name || ''))
      );
    }

    function isBattleSkillDotDetonateProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['dot_detonate']);
    }

    function isBattleSkillSealProfile(skill, context = {}) {
      const calc = context.calc || getPrimaryStateCalc(skill);
      return hasBattleSkillRuntimeConsumer(skill, ['skill_seal']) || calc.skill_seal === true;
    }

    function isBattleSkillTransferProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['status_transfer']);
    }

    function isBattleSkillSharedVisionProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['shared_vision']);
    }

    function isBattleSkillHealInvertProfile(skill, context = {}) {
      const calc = context.calc || getPrimaryStateCalc(skill);
      return hasBattleSkillRuntimeConsumer(skill, ['heal_inversion']) || Number(calc.heal_inversion_ratio || 0) > 0;
    }

    function 判定技能具备真实截断资格_V1(skill = {}) {
      if (!skill || typeof skill !== 'object') return false;
      if (isBattleSkillSealProfile(skill)) return true;
      if (hasBattleSkillRuntimeConsumer(skill, ['interrupt', 'hard_control', 'skill_seal'])) return true;
      return getSkillEffects(skill, { 行为规划: true }).some(effect => {
        const 原型 = String(effect?.原型 || '').trim();
        if (原型 !== '状态施加') return false;
        const 战斗效果 = effect?.计算层效果 || {};
        return 战斗效果.skip_turn === true ||
          战斗效果.cannot_react === true ||
          战斗效果.silence === true ||
          战斗效果.skill_seal === true ||
          Number(战斗效果.lock_level || 0) > 0 ||
          Number(战斗效果.cast_speed_penalty || 0) > 0 ||
          Number(战斗效果.reaction_penalty || 0) > 0;
      });
    }

    function isBattleSkillDotPressureProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['dot_damage']);
    }

    function isBattleSkillRevealProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['reveal']);
    }

    function isBattleSkillBuffStealProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['steal_buff']);
    }

    function isBattleSkillTauntProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['taunt']);
    }

    function isBattleSkillResourceDrainProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['resource_drain']);
    }

    function isBattleSkillResourceRefeedProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['resource_refeed']);
    }

    function 读取战斗抹消对象战术标签(对象 = {}) {
      const 匹配器 = 规范化战斗机制抹消对象(对象);
      const 标签 = new Set([读取战斗机制抹消对象摘要(匹配器)]);
      if (匹配器.原型 === '护盾变化') 标签.add('护盾').add('防御机制');
      if (匹配器.原型 === '规则防御') {
        标签.add('防御机制');
        if (/复活|免死|回溯/.test(String(匹配器.规则 || ''))) 标签.add('复活机制').add('回复机制');
      }
      if (匹配器.原型 === '资源变化') 标签.add('回复机制');
      if (匹配器.原型 === '状态施加') {
        collectBattleMechanismTagsFromDescriptor(匹配器.状态 || 匹配器.状态名称 || '', 匹配器).forEach(item => 标签.add(item));
      }
      if (匹配器.原型 === '机制授予' || 匹配器.原型 === '机制抹消') 标签.add('特殊规则');
      return Array.from(标签).filter(Boolean);
    }

    function 读取战斗技能抹消战术标签(skill) {
      const effect =
        getSkillEffects(skill).find(item => item?.原型 === '机制抹消') ||
        null;
      return effect?.抹消对象 ? 读取战斗抹消对象战术标签(effect.抹消对象) : [];
    }

    function isBattleSkillMechanismSuppressProfile(skill) {
      return hasBattleSkillRuntimeConsumer(skill, ['mechanism_suppress']);
    }

    function isBattleSkillReactiveDefenseProfile(skill, context = {}) {
      const summary = context.summary || deriveBattleSummaryFromEffects(skill);
      return (
        isBattleSkillDefensiveProfile(skill, context) ||
        技能具有保命倾向(skill, { summary }) ||
        ['护盾', '减伤', '免伤', '霸体', '免死', '无敌', '分摊', '反射', '护卫'].includes(summary.防御性质)
      );
    }

    function findBattleSkillEffect(skill, matcher) {
      if (typeof matcher !== 'function') return null;
      return getSkillEffects(skill).find(effect => matcher(effect)) || null;
    }

    function isBattleRecoverEffect(effect, properties = []) {
      const resourcePropertyMap = { 生命: 'hp', 体力: 'vit', 魂力: 'sp', 精神力: 'men' };
      const 原始属性列表 = Array.isArray(effect?.属性) ? effect.属性 : [effect?.属性];
      const 原始资源列表 = Array.isArray(effect?.资源) ? effect.资源 : [effect?.资源];
      const 属性列表 = [
        ...原始属性列表.map(item => String(item || '').trim()),
        ...原始资源列表.map(item => resourcePropertyMap[String(item || '').trim()] || ''),
      ].filter(Boolean);
      const allowAll = !Array.isArray(properties) || properties.length === 0;
      const propertyMatched = allowAll || 属性列表.some(property => properties.includes(property));
      if (!propertyMatched) return false;
      if (String(effect?.原型 || '').trim() === '资源变化') return 读取战斗数值正负(effect?.数值) > 0 && 属性列表.some(property => ['hp', 'vit', 'sp', 'men'].includes(property));
      const mechanism = String(effect?.原型 || '').trim();
      if (mechanism === '持续恢复') return 属性列表.some(property => ['vit', 'sp', 'men'].includes(property));
      return (
        mechanism === '属性变化' &&
        String(effect?.动作 || '').trim() === '加值' &&
        属性列表.some(property => ['vit', 'sp', 'men'].includes(property))
      );
    }

    function isBattleAttributeSupportEffect(effect) {
      return ['属性修正', '判定修正', '结算修正', '护盾变化', '规则防御', '状态施加', '资源变化', '资源转移'].includes(String(effect?.原型 || '').trim());
    }

    function isBattleDebuffAttributeEffect(effect) {
      const prototype = String(effect?.原型 || '').trim();
      if (['属性修正', '判定修正', '结算修正', '资源变化', '资源转移', '护盾变化'].includes(prototype)) return 读取战斗数值正负(effect?.数值) < 0;
      return false;
    }

    function normalizeSkillTypeLabel(raw) {
      const text = String(raw || '无');
      if (!text || text === '无') return '无';
      if (/输出|伤害|爆发|破甲|斩杀/.test(text)) return '输出';
      if (/控制|削弱|打断|沉默|禁疗|减速|软控|位移限制|锁定|束缚|缴械|驱散|干扰|扭曲|嘲讽|破隐|封技|治疗反转/.test(text)) return '控制';
      if (/防御|护盾|免伤|霸体|免死|反制|无敌|分摊|反射/.test(text)) return '防御';
      if (/位移|机动|追击|脱离|隐身|护卫/.test(text)) return '辅助';
      if (/辅助|增益|回复|治疗/.test(text)) return '辅助';
      return text.split(/[\/|｜]/)[0] || text;
    }

    function inferSkillTypeFromEffects(skill) {
      const fromSystem = normalizeSkillTypeLabel(skill?.技能分类);
      if (fromSystem !== '无') return fromSystem;
      const summarySkillType = getSkillSummaryHint(skill, 'skillType', '');
      if (summarySkillType) return normalizeSkillTypeLabel(summarySkillType);
      if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_CONTROL_RUNTIME_CONSUMERS]) || getSkillEffects(skill).some(effect => isBattleDebuffAttributeEffect(effect)))
        return '控制';
      if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_DEFENSE_RUNTIME_CONSUMERS])) return '防御';
      if (
        skillCanGrantFriendlyMechanism(skill) ||
        hasBattleSkillRuntimeConsumer(skill, [...BATTLE_SUPPORT_RUNTIME_CONSUMERS, ...BATTLE_SPECIAL_RULE_RUNTIME_CONSUMERS]) ||
        getSkillEffects(skill).some(effect => isBattleAttributeSupportEffect(effect))
      )
        return '辅助';
      if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_OUTPUT_RUNTIME_CONSUMERS]) || Number(getPrimaryDamageEffect(skill)?.威力倍率 || 0) > 0)
        return '输出';
      if (skillTargetsEnemySide(skill) && 技能包含运行语义(skill, '敌对')) return '控制';
      return '无';
    }

    function inferMainTypeFromEffects(skill) {
      const hintedMainType = getSkillSummaryHint(skill, 'mainType', '');
      if (hintedMainType) return hintedMainType;
      if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_MOBILITY_RUNTIME_CONSUMERS])) return '位移类';
      if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_SPECIAL_RULE_RUNTIME_CONSUMERS])) return '特殊规则类';
      if (
        hasBattleSkillRuntimeConsumer(skill, [...BATTLE_CONTROL_RUNTIME_CONSUMERS]) ||
        getSkillEffects(skill).some(effect => isBattleDebuffAttributeEffect(effect))
      )
        return '控制类';
      if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_DEFENSE_RUNTIME_CONSUMERS])) return '防御类';
      if (getSkillEffects(skill).some(effect => isBattleRecoverEffect(effect))) return '回复类';
      if (
        skillCanGrantFriendlyMechanism(skill) ||
        hasBattleSkillRuntimeConsumer(skill, [...BATTLE_SUPPORT_RUNTIME_CONSUMERS]) ||
        getSkillEffects(skill).some(effect => isBattleAttributeSupportEffect(effect))
      )
        return '增益类';
      if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_OUTPUT_RUNTIME_CONSUMERS]) || Number(getPrimaryDamageEffect(skill)?.威力倍率 || 0) > 0)
        return '伤害类';
      if (skillTargetsEnemySide(skill) && 技能包含运行语义(skill, '敌对')) return '控制类';
      return '无';
    }

    function deriveBattleSummaryFromEffects(skill, baseSummary = {}) {
      const defaultSummary = createEmptyBattleSummary();
      const summary = { ...defaultSummary, ...(baseSummary || {}) };
      const runtimeMeta = getSkillRuntimeMeta(skill);
      const 来源类别 = getBattleSkillSourceCategory(skill);
      const damage = getPrimaryDamageEffect(skill);
      const state = getPrimaryStateEffect(skill);
      const stateCalc = state?.计算层效果 || {};
      const targetText = String(runtimeMeta.对象 || '');
      const power = Number(damage?.威力倍率 || 0);
      const duration = Number(state?.持续回合 || 0);
      const skillName = String(skill?.name || skill?.技能名称 || '');
      const costText = String(getSkillCostText(skill) || '无');
      const attributeSource = getBattleSkillAttributeSource(skill);
      const skillRole = getBattleSkillRole(skill);
      const elementMode = String(getBattleSkillElementStructure(skill)?.模式 || '').trim() || '无';
      const wuxingMode = String(getBattleSkillWuxingInvocation(skill)?.模式 || '').trim() || '无';
      const polarityMode = String(getBattleSkillPolaritySummary(skill)?.polarityMode || '无').trim() || '无';
      const structureMode = elementMode !== '无' ? elementMode : wuxingMode;

      if (!baseSummary?.属性建模 || baseSummary.属性建模 === defaultSummary.属性建模 || baseSummary.属性建模 === '无') {
        const modelSegments = [attributeSource, skillRole, structureMode].filter(value => value && value !== '无');
        summary.属性建模 = modelSegments.length ? modelSegments.join('/') : '无';
      }
      if (!baseSummary?.极性层级 || baseSummary.极性层级 === defaultSummary.极性层级 || baseSummary.极性层级 === '无') {
        summary.极性层级 = polarityMode && polarityMode !== '无' ? polarityMode : '无';
      }
      if (
        (!baseSummary?.风险等级 || baseSummary.风险等级 === defaultSummary.风险等级 || baseSummary.风险等级 === '中') &&
        (structureMode === '逆演归一' ||
          structureMode === '阴阳合璧' ||
          (structureMode === '元素硬控' && normalizeBattleSkillAttributeTokens(skill?.附带属性).length >= 3))
      )
        summary.风险等级 = '高';
      if (来源类别 === '武魂融合技') summary.风险等级 = '高';
      else if (来源类别 === '自创魂技' && ['逆演归一', '阴阳合璧', '元素硬控'].includes(structureMode))
        summary.风险等级 = '高';

      if (!baseSummary?.目标规模 || baseSummary.目标规模 === defaultSummary.目标规模) {
        if (targetText === '全场') summary.目标规模 = '全场';
        else if (targetText.includes('群体')) summary.目标规模 = '群体';
        else summary.目标规模 = '单体';
      }

      const hasFriendlyGrantable = skillCanGrantFriendlyMechanism(skill);
      const hintedDefenseNature = getSkillSummaryHint(skill, 'defenseNature', '');
      const hintedRecoverNature = getSkillSummaryHint(skill, 'recoverNature', '');
      const hintedControlStrength = getSkillSummaryHint(skill, 'controlStrength', '');
      const hintedCooperation = getSkillSummaryHint(skill, 'cooperation', '');
      const hintedEffectMode = getSkillSummaryHint(skill, 'effectMode', '');

      if (!summary.防御性质 || summary.防御性质 === '无') {
        if (hintedDefenseNature) summary.防御性质 = hintedDefenseNature;
        else if (hasBattleSkillRuntimeConsumer(skill, ['counter', 'on_hit_counter'])) summary.防御性质 = '反制';
        else if (hasBattleSkillRuntimeConsumer(skill, ['death_save'])) summary.防御性质 = '免死';
        else if (hasBattleSkillRuntimeConsumer(skill, ['invincible'])) summary.防御性质 = '无敌';
        else if (hasBattleSkillRuntimeConsumer(skill, ['damage_share'])) summary.防御性质 = '分摊';
        else if (hasBattleSkillRuntimeConsumer(skill, ['cost_share'])) summary.防御性质 = '分摊';
        else if (hasBattleSkillRuntimeConsumer(skill, ['damage_transfer'])) summary.防御性质 = '反射';
        else if (hasBattleSkillRuntimeConsumer(skill, ['damage_reflect'])) summary.防御性质 = '反射';
        else if (hasBattleSkillRuntimeConsumer(skill, ['super_armor'])) summary.防御性质 = '霸体';
        else if (hasBattleSkillRuntimeConsumer(skill, ['guard'])) summary.防御性质 = '护卫';
        else if (hasBattleSkillRuntimeConsumer(skill, ['clone', 'stealth'])) summary.防御性质 = '分身';
        else if (hasBattleSkillRuntimeConsumer(skill, ['block'])) summary.防御性质 = '免伤';
        else if (hasBattleSkillRuntimeConsumer(skill, ['damage_reduce'])) summary.防御性质 = '减伤';
        else if (hasBattleSkillRuntimeConsumer(skill, ['shield'])) summary.防御性质 = '护盾';
      }
      if (!summary.回复性质 || summary.回复性质 === '无') {
        if (hintedRecoverNature) summary.回复性质 = hintedRecoverNature;
        else if (getSkillEffects(skill).some(effect => isBattleRecoverEffect(effect, ['vit'])))
          summary.回复性质 = '体力恢复';
        else if (getSkillEffects(skill).some(effect => isBattleRecoverEffect(effect, ['sp', 'men'])))
          summary.回复性质 = '资源回复';
      }
      if (!summary.控制强度 || summary.控制强度 === '无') {
        if (hintedControlStrength) summary.控制强度 = hintedControlStrength;
        else if (hasBattleSkillRuntimeConsumer(skill, ['hard_control']) || stateCalc.skip_turn === true) summary.控制强度 = '硬控';
        else if (
          isBattleSkillControlProfile(skill, { calc: stateCalc, summary }) ||
          getSkillEffects(skill).some(effect => isBattleDebuffAttributeEffect(effect)) ||
          hasBattleSkillRuntimeConsumer(skill, ['soft_control', 'position_lock', 'interrupt', 'skill_seal', 'anti_heal', 'heal_inversion'])
        )
          summary.控制强度 = '软控';
      }
      if (!baseSummary?.协同性 || baseSummary.协同性 === defaultSummary.协同性 || baseSummary.协同性 === '无') {
        if (hintedCooperation) summary.协同性 = hintedCooperation;
        else if (
          targetText === '全场' ||
          targetText.includes('群体') ||
          isBattleSkillSharedVisionProfile(skill) ||
          hasBattleSkillRuntimeConsumer(skill, ['target_lock', 'dispel_buff']) ||
          (hasFriendlyGrantable && summary.目标规模 !== '单体')
        )
          summary.协同性 = '高';
        else if (
          targetText.includes('己方') ||
          targetText.includes('友方') ||
          hasFriendlyGrantable ||
          getSkillEffects(skill).some(effect => isBattleRecoverEffect(effect)) ||
          ['分身', '护卫'].includes(summary.防御性质) ||
          hasBattleSkillRuntimeConsumer(skill, ['stealth'])
        )
          summary.协同性 = '中';
        else summary.协同性 = '低';
      }
      if (来源类别 === '武魂融合技' && summary.协同性 === '低') summary.协同性 = '高';
      if (!baseSummary?.生效方式 || baseSummary.生效方式 === defaultSummary.生效方式 || baseSummary.生效方式 === '无') {
        if (hintedEffectMode) summary.生效方式 = hintedEffectMode;
        else if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_TRIGGER_RUNTIME_CONSUMERS])) summary.生效方式 = '触发';
        else if (getSkillEffects(skill).some(effect => String(effect?.原型 || '').trim() === '状态施加' && Number(effect?.延迟回合 || 0) > 0)) summary.生效方式 = '延迟';
        else if (
          duration > 1 ||
          hasBattleSkillRuntimeConsumer(skill, [...BATTLE_SUSTAIN_RUNTIME_CONSUMERS])
        )
          summary.生效方式 = '持续';
        else summary.生效方式 = '瞬发';
      }
      if (!baseSummary?.爆发级别 || baseSummary.爆发级别 === defaultSummary.爆发级别 || baseSummary.爆发级别 === '无') {
        if (power >= 300) summary.爆发级别 = '高';
        else if (power >= 160) summary.爆发级别 = '中';
        else if (power > 0) summary.爆发级别 = '低';
        else summary.爆发级别 = '无';
      }
      if (!baseSummary?.持续性 || baseSummary.持续性 === defaultSummary.持续性 || baseSummary.持续性 === '无') {
        if (duration >= 3) summary.持续性 = '长';
        else if (duration >= 2) summary.持续性 = '中';
        else if (duration > 0) summary.持续性 = '短';
        else if (hasBattleSkillRuntimeConsumer(skill, [...BATTLE_SUSTAIN_RUNTIME_CONSUMERS]))
          summary.持续性 = '中';
        else summary.持续性 = '无';
      }
      if (!baseSummary?.保留倾向 || Number(baseSummary.保留倾向 || 0) === Number(defaultSummary.保留倾向 || 0)) {
        let reserve = 0;
        if (/真身|武魂融合技|生命之火|第八魂技|第九魂技/.test(skillName)) reserve += 35;
        if (power >= 280) reserve += 20;
        if (Number(runtimeMeta.cast_time || 0) >= 25) reserve += 15;
        if (
          hasBattleSkillRuntimeConsumer(skill, ['death_save', 'block', 'counter', 'on_hit_counter', 'self_random_variance']) ||
          summary.生效方式 === '触发'
        )
          reserve += 10;
        if (/维持|启动\)/.test(costText)) reserve += 10;
        summary.保留倾向 = Math.min(90, reserve);
      }
      if (来源类别 === '武魂融合技') summary.保留倾向 = Math.max(summary.保留倾向, 75);
      else if (来源类别 === '自创魂技') summary.保留倾向 = Math.max(summary.保留倾向, 28);
      return summary;
    }

    function buildConditionTacticalSnapshot(entity) {
      const entries = Object.entries(entity?.状态效果 || {});
      const buffEntries = entries.filter(([, cond]) => cond?.类型 === 'buff');
      const debuffEntries = entries.filter(([, cond]) => cond?.类型 === 'debuff');
      const hasShielded = entries.some(
        ([name, cond]) => /护盾|屏障|结界/.test(name) || Number(cond?.战斗效果?.shield_gain_mult || 1) > 1.05,
      );
      const hasDefenseBuffed = entries.some(
        ([name, cond]) => Number(cond?.面板修改比例?.def || 1) > 1.12 || /护体|罡气|霸体|真身|免伤|减伤/.test(name),
      );
      const isLockedOrControlled = entries.some(([name, cond]) => {
        const ce = cond?.战斗效果 || {};
        return (
          ce.skip_turn === true ||
          ce.cannot_react === true ||
          Number(ce.lock_level || 0) > 0 ||
          Number(ce.reaction_penalty || 0) > 0 ||
          Number(ce.cast_speed_penalty || 0) > 0 ||
          Number(ce.dodge_penalty || 0) > 0 ||
          读取状态资源锁定强度(cond) > 0 ||
          /锁定|禁锢|眩晕|催眠|幻境|束缚|减速|迟缓|软控|位移限制|强制位移|位移交换/.test(name)
        );
      });
      const hasHealingTrend = entries.some(
        ([name, cond]) =>
          Number(cond?.战斗效果?.final_heal_mult || 1) > 1.0 ||
          Number(cond?.战斗效果?.final_heal_bonus || 0) > 0 ||
          Number(cond?.战斗效果?.vit_gain_ratio || 0) > 0 ||
          Number(cond?.战斗效果?.sp_gain_ratio || 0) > 0 ||
          Number(cond?.战斗效果?.men_gain_ratio || 0) > 0 ||
          /回血|治疗|再生|回复|回魂|回精神/.test(name),
      );
      const hasDotPressure = entries.some(([, cond]) => 战斗效果含结构化持续伤害(cond));
      const hasBadCondition = entries.some(
        ([name, cond]) => cond?.类型 === 'debuff' && !/霸体|真身|增益|护盾/.test(name),
      );
      const hasSharedVision = entries.some(([name]) => /共享视野/.test(name));
      const hasTargetLock = entries.some(
        ([name, cond]) => /目标锁定|标记锁定/.test(name) || Number(cond?.战斗效果?.lock_level || 0) > 0,
      );
      const movementLockLevel = entries.reduce(
        (最大强度, [name, cond]) => /位移限制|束缚|禁锢|定身/.test(name)
          ? Math.max(最大强度, Number(cond?.战斗效果?.lock_level || 1))
          : 最大强度,
        0,
      );
      const hasStealthed = entries.some(
        ([name, cond]) =>
          Number(cond?.战斗效果?.stealth_level || 0) > 0 || /隐身|潜行/.test(String(name || '')),
      );
      const 有探查屏蔽 = entries.some(
        ([name, cond]) =>
          cond?.战斗效果?.探查屏蔽 === true || /探查屏蔽|感知屏蔽|精神屏蔽/.test(String(name || '')),
      );
      const hasReactiveDefense = entries.some(
        ([name, cond]) =>
          cond?.战斗效果?.invincible === true ||
          cond?.战斗效果?.super_armor === true ||
          Number(cond?.战斗效果?.block_count || 0) > 0 ||
          Number(cond?.战斗效果?.death_save_count || 0) > 0 ||
          Number(cond?.战斗效果?.revive_count || 0) > 0 ||
          Number(cond?.战斗效果?.counter_attack_ratio || 0) > 0 ||
          Number(cond?.战斗效果?.damage_reflect_ratio || 0) > 0 ||
          Number(cond?.战斗效果?.damage_transfer_ratio || 0) > 0 ||
          Number(cond?.战斗效果?.damage_share_ratio || 0) > 0 ||
          Number(cond?.战斗效果?.damage_reduction || 0) > 0 ||
          /护盾|免伤|霸体|免死|反击|反制|无敌|分摊|反射|转移/.test(name),
      );
      const hasAntiHeal = entries.some(
        ([name, cond]) =>
          Number(cond?.战斗效果?.heal_block_ratio || 0) > 0 ||
          Number(cond?.战斗效果?.heal_inversion_ratio || 0) > 0 ||
          /禁疗|治疗反转/.test(name),
      );
      const suppressionEntries = getActiveMechanismSuppressionEntries(entity);
      return {
        entries,
        buffCount: buffEntries.length,
        debuffCount: debuffEntries.length,
        hasShielded,
        hasDefenseBuffed,
        isLockedOrControlled,
        hasHealingTrend,
        hasDotPressure,
        hasBadCondition,
        hasSharedVision,
        hasTargetLock,
        hasStealthed,
        有探查屏蔽,
        hasReactiveDefense,
        hasAntiHeal,
        hasMechanismSuppression: suppressionEntries.length > 0,
        suppressedMechanisms: Array.from(new Set(suppressionEntries.flatMap(entry => entry.tags))),
        movementLockLevel,
      };
    }

    function getForcedTauntTargetName(actorChar) {
      if (!actorChar?.状态效果) return '';
      const tauntEntry = Object.values(actorChar.状态效果).find(cond =>
        cond &&
        typeof cond === 'object' &&
        String(cond?.类型 || '') === 'debuff' &&
        String(cond?.强制目标名 || '').trim(),
      );
      return String(tauntEntry?.强制目标名 || '').trim();
    }

    function canBypassStealth(attackerChar, skill = null, 目标角色 = null) {
      if (目标角色 && buildConditionTacticalSnapshot(目标角色).有探查屏蔽) return false;
      const attackerSnapshot = buildConditionTacticalSnapshot(attackerChar);
      return (
        attackerSnapshot.hasSharedVision ||
        attackerSnapshot.hasTargetLock ||
        hasBattleSkillRuntimeConsumer(skill, ['shared_vision', 'target_lock'])
      );
    }

    function 读取隐匿识破率(观察者 = {}, 隐匿者 = {}) {
      const 观察者属性 = 观察者?.final || buildCombatFinalStats(观察者);
      const 隐匿者属性 = 隐匿者?.final || buildCombatFinalStats(隐匿者);
      const 观察者精神力 = Math.max(
        0,
        Number(读取战斗资源当前值(观察者, 观察者属性, 'men') || 观察者属性?.men || 观察者属性?.men_max || 0),
      );
      const 隐匿者精神力 = Math.max(
        1,
        Number(读取战斗资源当前值(隐匿者, 隐匿者属性, 'men') || 隐匿者属性?.men || 隐匿者属性?.men_max || 1),
      );
      return Math.max(0, Math.min(1, 观察者精神力 / 隐匿者精神力 - 1.5));
    }

    function 可以锁定隐匿目标(观察者 = {}, 目标 = {}, skill = null) {
      const 目标快照 = buildConditionTacticalSnapshot(目标);
      if (目标快照.有探查屏蔽) return !目标快照.hasStealthed;
      if (!目标快照.hasStealthed) return true;
      if (canBypassStealth(观察者, skill, 目标)) return true;
      const 识破率 = 读取隐匿识破率(观察者, 目标);
      return 识破率 > 0 && Math.random() < 识破率;
    }

    function 探查屏蔽阻断敌对锁定状态(目标 = {}, 状态模块 = {}) {
      if (!buildConditionTacticalSnapshot(目标).有探查屏蔽) return false;
      const 名称 = String(
        状态模块?.状态名称 ||
          状态模块?.状态 ||
          状态模块?.特殊机制标识 ||
          状态模块?.运行机制 ||
          状态模块?.运行时消费器 ||
          '',
      ).trim();
      const 计算层效果 = 状态模块?.计算层效果 || 状态模块?.战斗效果 || {};
      if (/^(标记)$|目标锁定|标记锁定|感知干扰|精神标记|精神探查|探查|锁定/.test(名称)) return true;
      return Number(计算层效果.lock_level || 0) > 0 && !/位移限制|束缚|禁锢|定身/.test(名称);
    }

    function resolveGuardRedirectTarget(initialTarget, allyTeam = []) {
      if (!initialTarget?.状态效果) return null;
      const 护卫状态列表 = Object.entries(initialTarget.状态效果).filter(([, cond]) =>
        cond &&
        typeof cond === 'object' &&
        String(cond?.类型 || '') === 'buff' &&
        String(cond?.护卫者名 || '').trim(),
      );
      for (const [状态名, 护卫状态] of 护卫状态列表) {
        const 护卫者名 = String(护卫状态?.护卫者名 || '').trim();
        const 剩余次数 = 护卫状态?.护卫剩余次数 === undefined ? Infinity : Number(护卫状态.护卫剩余次数 || 0);
        if (!护卫者名 || !(剩余次数 > 0)) continue;
        const 护卫者 = (allyTeam || []).find(
          unit =>
            unit &&
            unit.name !== initialTarget.name &&
            isCombatUnitAbleToFight(unit) &&
            isCombatUnitIdentityMatch(unit, 护卫者名),
        );
        if (!护卫者) continue;
        if (Number.isFinite(剩余次数)) {
          护卫状态.护卫剩余次数 = Math.max(0, 剩余次数 - 1);
          if (护卫状态.护卫剩余次数 <= 0 && /普防护援/.test(状态名)) delete initialTarget.状态效果[状态名];
        }
        return 护卫者;
      }
      return null;
    }

    function bindCombatMirrorField(target, source, key, options = {}) {
      if (!target || !source) return;
      if (options.preferSource && source[key] !== undefined) target[key] = source[key];
      else if (target[key] !== undefined) source[key] = target[key];
      else if (source[key] !== undefined) target[key] = source[key];

      try {
        Object.defineProperty(target, key, {
          configurable: true,
          enumerable: true,
          get() {
            return source[key];
          },
          set(value) {
            source[key] = value;
          },
        });
      } catch (e) {
        target[key] = source[key];
      }
    }

    function bindCombatAliasField(target, source, aliasKey, sourceKey, fallbackValue = 0) {
      if (!target || !source || !aliasKey || !sourceKey) return;
      if (source[sourceKey] === undefined) source[sourceKey] = fallbackValue;
      try {
        Object.defineProperty(target, aliasKey, {
          configurable: true,
          enumerable: true,
          get() {
            return source[sourceKey];
          },
          set(value) {
            source[sourceKey] = value;
          },
        });
      } catch (error) {
        target[aliasKey] = source[sourceKey];
      }
    }

    function normalizeCombatHpFields(source) {
      if (!source || typeof source !== 'object') return;
      source.HP上限 = Math.max(1, Number.isFinite(Number(source.HP上限)) ? Number(source.HP上限) : 1);
      source.HP = Math.max(
        0,
        Math.min(
          source.HP上限,
          Number.isFinite(Number(source.HP)) ? Number(source.HP) : source.HP上限,
        ),
      );
      source.体力上限 = Math.max(1, Number.isFinite(Number(source.体力上限)) ? Number(source.体力上限) : 1);
      source.体力 = Math.max(0, Math.min(source.体力上限, Number.isFinite(Number(source.体力)) ? Number(source.体力) : source.体力上限));
    }

    function bindCombatHpAliasField(target, source) {
      if (!target || !source) return;
      normalizeCombatHpFields(source);
      try {
        Object.defineProperty(target, 'hp', {
          configurable: true,
          enumerable: true,
          get() {
            return source.HP;
          },
          set(value) {
            source.HP = Math.max(0, Math.min(Number(source.HP上限 || 1), Number(value || 0)));
          },
        });
        Object.defineProperty(target, 'hp_max', {
          configurable: true,
          enumerable: true,
          get() {
            return source.HP上限;
          },
          set(value) {
            source.HP上限 = Math.max(1, Number(value || 1));
            source.HP = Math.max(0, Math.min(source.HP上限, Number(source.HP || 0)));
          },
        });
        Object.defineProperty(target, 'vit', {
          configurable: true,
          enumerable: true,
          get() {
            return source.体力;
          },
          set(value) {
            source.体力 = Math.max(0, Math.min(Number(source.体力上限 || 1), Number(value || 0)));
          },
        });
        Object.defineProperty(target, 'vit_max', {
          configurable: true,
          enumerable: true,
          get() {
            return source.体力上限;
          },
          set(value) {
            source.体力上限 = Math.max(1, Number(value || 1));
            source.体力 = Math.max(0, Math.min(source.体力上限, Number(source.体力 || 0)));
          },
        });
      } catch (error) {
        target.hp = source.HP;
        target.hp_max = source.HP上限;
        target.vit = source.体力;
        target.vit_max = source.体力上限;
      }
    }

    function bindCombatRuntimeAliases(target, source) {
      if (!target || !source) return;
      bindCombatAliasField(target, source, 'str', '力量', 0);
      bindCombatAliasField(target, source, 'def', '防御', 0);
      bindCombatAliasField(target, source, 'agi', '敏捷', 0);
      bindCombatAliasField(target, source, 'sp', '魂力', 0);
      bindCombatAliasField(target, source, 'sp_max', '魂力上限', 1);
      bindCombatAliasField(target, source, 'men', '精神力', 0);
      bindCombatAliasField(target, source, 'men_max', '精神力上限', 1);
      bindCombatAliasField(target, source, 'sta', '体力', 0);
      bindCombatAliasField(target, source, 'sta_max', '体力上限', 1);
      bindCombatHpAliasField(target, source);
    }

    function expandCombatParticipantFromMvu(participant) {
      if (!participant || typeof participant !== 'object' || Array.isArray(participant)) return participant;
      if (isTemporaryCombatParticipant(participant)) return participant;
      if (participant.属性 && typeof participant.属性 === 'object' && participant.状态 && typeof participant.状态 === 'object') {
        return participant;
      }

      const participantName = String(participant.name || '').trim();
      if (!participantName) return participant;
      const currentCharData = getMvuValue(`char.${participantName}`, undefined);
      if (!currentCharData || typeof currentCharData !== 'object') return participant;

      const expanded = deepClonePlain(currentCharData);
      expanded.name = participantName;

      if (!expanded.属性 || typeof expanded.属性 !== 'object') expanded.属性 = {};
      if (!expanded.状态 || typeof expanded.状态 !== 'object') expanded.状态 = {};

      COMBAT_STAT_KEYS.forEach(key => {
        if (participant[key] !== undefined) expanded.属性[key] = deepClonePlain(participant[key]);
      });
      COMBAT_STATUS_KEYS.forEach(key => {
        if (participant[key] !== undefined) expanded.状态[key] = deepClonePlain(participant[key]);
      });

      if (participant.属性 && typeof participant.属性 === 'object') {
        expanded.属性 = { ...expanded.属性, ...deepClonePlain(participant.属性) };
      }
      if (participant.状态 && typeof participant.状态 === 'object') {
        expanded.状态 = { ...expanded.状态, ...deepClonePlain(participant.状态) };
      }
      if (participant.状态效果 && typeof participant.状态效果 === 'object' && !Array.isArray(participant.状态效果)) {
        expanded.状态效果 = deepClonePlain(participant.状态效果);
      }
      if (participant.持续效果 && typeof participant.持续效果 === 'object' && !Array.isArray(participant.持续效果)) {
        expanded.持续效果 = deepClonePlain(participant.持续效果);
      }
      if (participant.蓄力技能 !== undefined) expanded.蓄力技能 = deepClonePlain(participant.蓄力技能);
      if (participant.决策记忆 !== undefined) expanded.决策记忆 = deepClonePlain(participant.决策记忆);
      if (participant.当前领域 !== undefined) expanded.当前领域 = deepClonePlain(participant.当前领域);
      if (participant.存活 !== undefined) expanded.状态.存活 = participant.存活 !== false;
      if (participant.势力 !== undefined) expanded.势力 = deepClonePlain(participant.势力);

      delete expanded.__combatMirrorBound;
      return expanded;
    }

    function bindCombatParticipant(char) {
      if (!char || char.__combatMirrorBound) return char;

      if (char.属性) {
        COMBAT_STAT_KEYS.forEach(key => bindCombatMirrorField(char, char.属性, key, { preferSource: true }));
        bindCombatRuntimeAliases(char, char.属性);
        bindCombatRuntimeAliases(char.属性, char.属性);
      }

      if (char.状态) {
        COMBAT_STATUS_KEYS.forEach(key => bindCombatMirrorField(char, char.状态, key));
      }

      if (!char.状态效果) {
        if (char.属性?.状态效果) char.状态效果 = char.属性.状态效果;
        else char.状态效果 = {};
      }
      char.状态效果 = normalizeCombatConditionMapForRuntime(char.状态效果);

      if (char.存活 === undefined && char.状态?.存活 !== undefined) char.存活 = char.状态.存活;

      char.__combatMirrorBound = true;
      return char;
    }

    function hydrateCombatData(combatData) {
      if (!combatData || !combatData.参战者) return combatData;
      combatData.参战者.team_player = Array.isArray(combatData.参战者.team_player)
        ? combatData.参战者.team_player.map(expandCombatParticipantFromMvu)
        : [];
      combatData.参战者.team_enemy = Array.isArray(combatData.参战者.team_enemy)
        ? combatData.参战者.team_enemy.map(expandCombatParticipantFromMvu)
        : [];
      const playerRoster = (combatData.参战者.team_player || []).filter(Boolean);
      const enemyRoster = (combatData.参战者.team_enemy || []).filter(Boolean);

      const processRoster = (roster, opposingRoster) => {
        const seen = new Set();
        roster.forEach(member => {
          if (!member || seen.has(member)) return;
          seen.add(member);
          refreshParticipantProjectedState(
            member,
            roster.filter(unit => unit && unit !== member),
            opposingRoster.filter(Boolean),
          );
        });
      };

      processRoster(playerRoster, enemyRoster);
      processRoster(enemyRoster, playerRoster);

      (combatData.参战者.team_player || []).forEach(member => {
        if (member) member.final = buildCombatFinalStats(member);
      });
      (combatData.参战者.team_enemy || []).forEach(member => {
        if (member) member.final = buildCombatFinalStats(member);
      });
      return combatData;
    }

    function normalizeSkillData(skill, fallbackName = '未知技能') {
      const normalized = deepClone(skill || {});
      const 正式技能旧字段 = ['cast_time', '对象', '结算策略', '运行机制', '状态名称', '机制标签', '技能来源', 'source_tag'];
      const 命中旧字段 = skill && typeof skill === 'object'
        ? 正式技能旧字段.find(字段 => 字段 !== 'cast_time' || skill.__战斗标准技能 !== true
          ? Object.prototype.hasOwnProperty.call(normalized, 字段)
          : false)
        : '';
      if (命中旧字段) throw new Error(`技能正式结构错误:${normalized.魂技名 || normalized.name || fallbackName}仍写入旧字段${命中旧字段}`);
      normalized.__战斗标准技能 = true;
      normalized.name = normalized.name || normalized.技能名称 || fallbackName;
      normalized.魂技名 = normalized.魂技名 || normalized.name || normalized.技能名称 || fallbackName;
      normalized.战斗摘要 = { ...createEmptyBattleSummary(), ...(normalized.战斗摘要 || {}) };
      normalized.战斗语义 = { ...createEmptySkillSemantics(), ...(normalized.战斗语义 || {}) };
      normalized.技能分类 = normalizeSkillTypeLabel(normalized.技能分类 || inferSkillTypeFromEffects(normalized) || '无');
      normalized.cast_time = Number(normalized.前摇 ?? 0) || 0;
      normalized.消耗 =
        typeof normalized.消耗 === 'object' ? formatCostObjectToString(normalized.消耗) : normalized.消耗 || '无';
      normalized.附带属性 = normalizeBattleSkillAttributeTokens(normalized.附带属性);
      normalized.使用条件 = normalized.使用条件 && typeof normalized.使用条件 === 'object' && !Array.isArray(normalized.使用条件)
        ? deepClonePlain(normalized.使用条件)
        : {};
      const 清理技能正式效果 = value => {
        if (Array.isArray(value)) return value.map(清理技能正式效果).filter(Boolean);
        if (!value || typeof value !== 'object') return value;
        const next = deepClone(value);
        ['运行机制', '状态名称', '计算层效果', '战斗效果', '面板修改比例', '面板固定修正', '运行时消费器'].forEach(key => {
          delete next[key];
        });
        ['使用效果', '授予效果', '结算效果'].forEach(key => {
          if (Array.isArray(next[key])) next[key] = 清理技能正式效果(next[key]);
        });
        if (Array.isArray(next.条件分支)) {
          next.条件分支 = next.条件分支.map(branch => {
            const 分支 = deepClone(branch);
            ['替换效果', '追加效果'].forEach(key => {
              if (Array.isArray(分支[key])) 分支[key] = 清理技能正式效果(分支[key]);
            });
            return 分支;
          });
        }
        return next;
      };
      normalized._效果数组 = (Array.isArray(normalized._效果数组) ? normalized._效果数组 : [])
        .filter(effect => effect && typeof effect === 'object')
        .map(清理技能正式效果);
      normalized.element = getBattleSkillDisplayElement(normalized);

      const runtimeMeta = getSkillRuntimeMeta(normalized);
      if (!normalized.消耗 || normalized.消耗 === '无') {
        normalized.消耗 = runtimeMeta.消耗 || '无';
      }
      if (!normalized.技能分类 || normalized.技能分类 === '无') normalized.技能分类 = runtimeMeta.技能分类 || '无';
      if (!(normalized.cast_time > 0)) {
        normalized.cast_time = Number(runtimeMeta.cast_time ?? 0) || 0;
      }
      normalized.前摇 = Number(normalized.前摇 ?? 0) || 0;

      applyAttributeCoeffToCombatSkill(normalized);
      if (skill && typeof skill === 'object') {
        const 来源上下文 = 战斗来源类别上下文表.get(skill);
        if (来源上下文) 写入战斗来源类别上下文(normalized, 来源上下文);
      }

      return normalized;
    }

    const FUSION_SELF_SPIRIT_SLOTS = ['第1武魂', '第2武魂'];

    function getFusionSkillMode(fusionSkill = {}) {
      return fusionSkill?.融合模式 === 'self' ? 'self' : 'partner';
    }

    function splitFusionPartnerText(rawValue = '') {
      return String(rawValue || '')
        .split(/[、,，+\/|｜；;]/)
        .map(item => String(item || '').trim())
        .filter(Boolean);
    }

    function normalizeFusionSkillParticipants(fusionSkill = {}) {
      const rawParticipants = Array.isArray(fusionSkill?.融合参与者) ? fusionSkill.融合参与者 : [];
      if (rawParticipants.length) {
        return rawParticipants
          .map(participant => {
            const raw = participant && typeof participant === 'object' ? participant : {};
            const roleText = String(raw.role || raw.类型 || raw.身份 || '').trim();
            const role = roleText === 'self' || /自身|自体|本体|自己/.test(roleText) ? 'self' : 'partner';
            const charName = String(
              raw.charName || raw.char_name || raw.name || raw.角色 || raw.角色名 || raw.charKey || raw.char_key || raw.key || '',
            ).trim();
            const charKey = String(raw.charKey || raw.char_key || raw.key || raw.角色键 || '').trim();
            const spirit = String(raw.spirit || raw.spiritName || raw.spirit_name || raw.武魂 || raw.来源武魂 || '').trim();
            return { role, charName, charKey, spirit };
          })
          .filter(participant => participant.charName || participant.charKey || participant.spirit);
      }
      const mode = getFusionSkillMode(fusionSkill);
      if (mode === 'self') {
        return getFusionSkillSourceSpirits(fusionSkill).map(slot => ({ role: 'self', charName: '', charKey: '', spirit: slot }));
      }
      return splitFusionPartnerText(fusionSkill?.融合对象 || '').map(name => ({
        role: 'partner',
        charName: name,
        charKey: '',
        spirit: '',
      }));
    }

    function getFusionSkillPartnerNames(fusionSkill = {}) {
      const names = normalizeFusionSkillParticipants(fusionSkill)
        .filter(participant => participant.role !== 'self')
        .map(participant => participant.charName || participant.charKey)
        .filter(Boolean);
      return Array.from(new Set(names));
    }

    function getFusionSkillPartnerName(fusionSkill = {}) {
      const names = getFusionSkillPartnerNames(fusionSkill);
      return names.length ? names.join('、') : String(fusionSkill?.融合对象 || '').trim();
    }

    function 读取融合相关度总分(charData = {}, fusionSkill = {}) {
      if (getFusionSkillMode(fusionSkill) === 'self') return 100;
      const partnerNames = getFusionSkillPartnerNames(fusionSkill);
      if (!partnerNames.length) return 0;
      const relationMap = charData?.社交?.关系 && typeof charData.社交.关系 === 'object' ? charData.社交.关系 : {};
      const scores = partnerNames.map(name => {
        const rel = relationMap[name];
        const base = Number(rel?.武魂相关度基础);
        const 基础分 = Number.isFinite(base) ? Math.max(0, Math.min(100, Math.floor(base))) : 0;
        const favor = Number(rel?.好感度 ?? 0);
        const 关系加成 = Number.isFinite(favor) ? Math.max(0, Math.min(20, Math.floor(Math.max(0, favor) / 10))) : 0;
        return Math.max(0, Math.min(100, 基础分 + 关系加成));
      });
      if (!scores.length) return 0;
      return Math.max(0, Math.min(100, Math.floor(Math.min(...scores))));
    }

    function 计算融合相关度倍率(相关度总分 = 100) {
      const 安全总分 = Math.max(0, Math.min(100, Number(相关度总分 || 0)));
      if (安全总分 < 70) return 0.9;
      const 进度 = (安全总分 - 70) / 30;
      return Number((1 + Math.max(0, Math.min(1, 进度)) * 0.25).toFixed(4));
    }

    function 获取融合技能可用性(charData, fusionSkill, alliedTeam = []) {
      if (!fusionSkill?.技能数据 || fusionSkill?.技能数据?.状态 === '未生成') {
        return { 可用: false, 原因: '融合技能未完成生成', 相关度总分: 0 };
      }
      if (getFusionSkillMode(fusionSkill) === 'self') {
        const slots = getFusionSkillSourceSpirits(fusionSkill);
        const 可用 = slots.length >= 2 && slots.every(slot => hasUsableSpiritSlot(charData, slot));
        return { 可用, 原因: 可用 ? '' : '自体融合缺少双武魂槽位', 相关度总分: 100 };
      }
      const partnerNames = getFusionSkillPartnerNames(fusionSkill);
      if (!partnerNames.length) return { 可用: false, 原因: '未配置融合对象', 相关度总分: 0 };
      const 搭档到位 = partnerNames.every(partnerName =>
        (alliedTeam || []).some(unit => isCombatUnitIdentityMatch(unit, partnerName) && isCombatUnitAbleToFight(unit)),
      );
      if (!搭档到位) {
        return { 可用: false, 原因: `搭档[${partnerNames.join('、')}]未到位`, 相关度总分: 0 };
      }
      const 相关度总分 = 读取融合相关度总分(charData || {}, fusionSkill || {});
      if (相关度总分 < 70) {
        return { 可用: false, 原因: `武魂相关度不足(${相关度总分}/70)`, 相关度总分 };
      }
      return { 可用: true, 原因: '', 相关度总分 };
    }

    function buildFusionBattleProfile(mode = 'partner', partnerCount = 1, 相关度总分 = 100) {
      const safeMode = mode === 'self' ? 'self' : 'partner';
      const safePartnerCount = Math.max(1, Math.floor(Number(partnerCount || 1)));
      const multiPartnerBonus = Math.min(0.12, Math.max(0, safePartnerCount - 1) * 0.05);
      if (safeMode === 'self') {
        return {
          mode: safeMode,
          partnerCount: 0,
          actorCostScale: 1.35,
          partnerPoolScale: 0,
          castTimeScale: 1.08,
          damageMult: 1.5,
          recoverMult: 1.38,
          shieldMult: 1.38,
          stateScale: 1.3,
          controlScale: 1.35,
          aftermathDuration: 2,
          aftermathPanelScale: 0.78,
          aftermathDamageMult: 0.76,
          aftermathHealMult: 0.78,
          aftermathShieldMult: 0.78,
          aftermathCastPenalty: 0.42,
          aftermathDodgePenalty: 0.15,
          aftermathReactionPenalty: 0.15,
          aftermathResourceBlock: 0.28,
          相关度总分: 100,
          相关度倍率: 1,
        };
      }
      const 相关度倍率 = 计算融合相关度倍率(相关度总分);
      return {
        mode: safeMode,
        partnerCount: safePartnerCount,
        actorCostScale: 1.1,
        partnerPoolScale: 0.6,
        castTimeScale: 1.15,
        damageMult: (1.65 + multiPartnerBonus) * 相关度倍率,
        recoverMult: (1.5 + multiPartnerBonus * 0.6) * 相关度倍率,
        shieldMult: (1.5 + multiPartnerBonus * 0.6) * 相关度倍率,
        stateScale: (1.42 + multiPartnerBonus * 0.5) * 相关度倍率,
        controlScale: (1.5 + multiPartnerBonus * 0.6) * 相关度倍率,
        aftermathDuration: 2,
        aftermathPanelScale: 0.82,
        aftermathDamageMult: 0.82,
        aftermathHealMult: 0.82,
        aftermathShieldMult: 0.82,
        aftermathCastPenalty: 0.35,
        aftermathDodgePenalty: 0.12,
        aftermathReactionPenalty: 0.12,
        aftermathResourceBlock: 0.22,
        相关度总分: Math.max(0, Math.min(100, Math.floor(Number(相关度总分 || 0)))),
        相关度倍率,
      };
    }

    function getFusionBattleProfileFromSkill(skill = {}) {
      return skill?.__fusion_profile && typeof skill.__fusion_profile === 'object' ? skill.__fusion_profile : null;
    }

    function buildFusionCombatSkill(fusionSkill = {}, fusionName = '武魂融合技', charData = null) {
      const skill = normalizeSkillData(fusionSkill?.技能数据, `武魂融合技·${fusionName}`);
      const mode = getFusionSkillMode(fusionSkill);
      const partnerNames = getFusionSkillPartnerNames(fusionSkill);
      const 相关度总分 = mode === 'self' ? 100 : 读取融合相关度总分(charData || {}, fusionSkill || {});
      const profile = buildFusionBattleProfile(mode, partnerNames.length || 1, 相关度总分);
      const baseCostText = String(skill.消耗 || '无').trim() || '无';
      const actorCostText = scaleSkillCostText(baseCostText, profile.actorCostScale);
      const partnerCostRatio =
        mode === 'partner' && partnerNames.length > 0
          ? Number(profile.partnerPoolScale || 0) / Math.max(1, partnerNames.length)
          : 0;
      const partnerCostText = partnerCostRatio > 0 ? scaleSkillCostText(actorCostText, partnerCostRatio) : '无';
      const baseCastTime = Number(skill.前摇 ?? 0) || 0;
      const nextCastTime = Math.max(baseCastTime > 0 ? 1 : 0, Math.round(baseCastTime * Number(profile.castTimeScale || 1)));

      skill.消耗 = actorCostText;
      skill.前摇 = nextCastTime;
      写入战斗来源类别上下文(skill, { 来源类别: '武魂融合技', 来源明细: fusionName || '武魂融合技' });
      skill.__融合模式 = mode;
      skill.__融合对象 = partnerNames.length ? partnerNames.join('、') : '无';
      skill.__fusion_profile = { ...profile, partnerNames: [...partnerNames] };
      skill.__fusion_partner_names = [...partnerNames];
      skill.__fusion_partner_cost_text = partnerCostText;
      skill.__融合相关度总分 = Number(profile?.相关度总分 || 0);
      skill.__融合相关度倍率 = Number(profile?.相关度倍率 || 1);
      skill.__fusion_display_cost_text =
        partnerCostText !== '无' && partnerNames.length
          ? `${actorCostText} | 共耗(${partnerNames.join('、')}): ${partnerCostText}`
          : actorCostText;
      return skill;
    }

    function getFusionSkillDisplayCostText(skill = {}) {
      const displayText = String(skill?.__fusion_display_cost_text || '').trim();
      if (displayText) return displayText;
      return getSkillCostText(skill);
    }

    function getFusionSkillSourceSpirits(fusionSkill = {}) {
      const rawSlots = Array.isArray(fusionSkill?.来源武魂) ? fusionSkill.来源武魂 : [];
      const slots = rawSlots
        .map(slot => String(slot || '').trim())
        .filter(slot => FUSION_SELF_SPIRIT_SLOTS.includes(slot));
      if (slots.length) return Array.from(new Set(slots));
      return getFusionSkillMode(fusionSkill) === 'self' ? [...FUSION_SELF_SPIRIT_SLOTS] : ['第1武魂'];
    }

    function hasUsableSpiritSlot(charData, slot) {
      return !!(charData && charData[slot] && typeof charData[slot] === 'object');
    }

    function isFusionSkillAvailable(charData, fusionSkill, alliedTeam = []) {
      return !!获取融合技能可用性(charData, fusionSkill, alliedTeam).可用;
    }

    function buildFusionCastNarration(fusionSkill, actorName = '施术者') {
      if (getFusionSkillMode(fusionSkill) === 'self') {
        const slots = getFusionSkillSourceSpirits(fusionSkill);
        return `${actorName}将${slots.join('与')}同频共振，自体交融，悍然施展了武魂融合技！`;
      }
      return `${actorName}与${getFusionSkillPartnerName(fusionSkill) || '同伴'}气息交融，果断施展了武魂融合技！`;
    }

    function parseResourceCostValue(costStr, label, currentValue, maxValue) {
      const text = String(costStr || '');
      const currentMatch = text.match(new RegExp(`${label}:(?:当前(?:剩余)?|剩余)(\\d+)%`));
      if (currentMatch)
        return Math.floor((Math.max(0, Number(currentValue || 0)) * parseInt(currentMatch[1], 10)) / 100);
      const baseMatch = text.match(new RegExp(`${label}:(\\d+)(%?)`));
      if (!baseMatch) return 0;
      return baseMatch[2]
        ? Math.floor((Math.max(0, Number(maxValue || 0)) * parseInt(baseMatch[1], 10)) / 100)
        : parseInt(baseMatch[1], 10);
    }

    function 读取运行态批量目标列表(skill) {
      if (!skill || !Array.isArray(skill.__批量目标列表)) return [];
      return dedupeCombatTargetList(skill.__批量目标列表.filter(Boolean));
    }

    function 计算辅助目标数量倍率(skill) {
      if (!skill || !isSupportLikeSkill(skill)) return 1;
      const 目标数量 = Math.max(1, Math.floor(Number(skill.__辅助目标数量 || 1)));
      return 1 + 0.1 * (Math.min(12, 目标数量) - 1);
    }

    function 计算运行态辅助消耗系数(skill, char, 默认单体系数 = 1) {
      if (!skill || !isSupportLikeSkill(skill)) return Math.max(1, Number(默认单体系数 || 1));
      const 批量目标列表 = 读取运行态批量目标列表(skill);
      const 批量模式 = String(skill.__批量消耗模式 || '').trim();
      const 七九单体语义 = 是否七九辅助单体语义(char, skill);
      if (批量目标列表.length > 0 && (批量模式 === '单体批量' || 七九单体语义)) {
        return 批量目标列表.reduce((总和, 目标) => 总和 + getSupportCostScale(char, 目标), 0);
      }
      if (批量目标列表.length > 0) {
        const 最高单体倍率 = 批量目标列表.reduce((最高, 目标) => Math.max(最高, getSupportCostScale(char, 目标)), 1);
        return 最高单体倍率 * (1 + 0.1 * (Math.min(12, 批量目标列表.length) - 1));
      }
      if (七九单体语义) return Math.max(1, Number(默认单体系数 || 1));
      return Math.max(1, Number(默认单体系数 || 1)) * 计算辅助目标数量倍率(skill);
    }

    function 读取运行态覆盖目标数量(skill) {
      const 批量目标列表 = 读取运行态批量目标列表(skill);
      if (批量目标列表.length > 0) return 批量目标列表.length;
      const 显式数量 = Math.max(
        0,
        Math.floor(Number(skill?.__覆盖目标数量 || skill?.__辅助目标数量 || 0)),
      );
      return 显式数量 > 0 ? 显式数量 : 1;
    }

    function 计算运行态目标数量消耗系数(skill, char, 默认单体系数 = 1) {
      const 基础系数 = Math.max(1, Number(默认单体系数 || 1));
      if (!skill) return 基础系数;
      if (isSupportLikeSkill(skill)) return 计算运行态辅助消耗系数(skill, char, 基础系数);
      const 目标数量 = Math.max(1, Math.min(12, 读取运行态覆盖目标数量(skill)));
      return 基础系数 * (1 + 0.1 * (目标数量 - 1));
    }

    function parseSkillCostForChar(skill, char, context = {}) {
      const stats = char?.属性 || char || {};
      const 使用条件 = skill?.使用条件 && typeof skill.使用条件 === 'object' && !Array.isArray(skill.使用条件) ? skill.使用条件 : {};
      const 角色等级 = Number(stats.等级 ?? char?.等级 ?? 0) || 0;
      const 最低等级 = Math.max(0, Number(使用条件.最低等级 || 0) || 0);
      if (最低等级 > 0 && 角色等级 < 最低等级) {
        return {
          reqSp: 0,
          reqVit: 0,
          reqMen: 0,
          costScale: 1,
          canCast: false,
          failureReason: `等级不足(${Math.floor(角色等级)}/${最低等级})`,
          partnerCosts: [],
        };
      }
      const 当前魂力 = Number(stats.sp ?? stats.魂力 ?? 0) || 0;
      const 当前精神力 = Number(stats.men ?? stats.精神力 ?? 0) || 0;
      const 最低魂力 = Math.max(0, Number(使用条件.最低魂力 || 0) || 0);
      const 最低精神力 = Math.max(0, Number(使用条件.最低精神力 || 0) || 0);
      if ((最低魂力 > 0 && 当前魂力 < 最低魂力) || (最低精神力 > 0 && 当前精神力 < 最低精神力)) {
        return {
          reqSp: 0,
          reqVit: 0,
          reqMen: 0,
          costScale: 1,
          canCast: false,
          failureReason: '使用条件不足',
          partnerCosts: [],
        };
      }
      const 百分比启动检查 = 检查普通魂技百分比启动消耗(skill, char, context?.action || {
        action_type: String(context?.当前行动 || '').trim() === '武魂融合技' ? '武魂融合技' : '释放魂技',
      });
      if (!百分比启动检查.可用) {
        return {
          reqSp: 0,
          reqVit: 0,
          reqMen: 0,
          costScale: 1,
          canCast: false,
          failureReason: 百分比启动检查.原因,
          partnerCosts: [],
        };
      }
      const 炸环恢复信息 = 读取技能魂环恢复标记_V1(skill, char);
      if (炸环恢复信息.恢复中) {
        return {
          reqSp: 0,
          reqVit: 0,
          reqMen: 0,
          costScale: 1,
          canCast: false,
          failureReason: `魂环恢复中(剩余${Math.max(1, Math.floor(Number(炸环恢复信息.剩余tick || 0)))}tick)`,
          partnerCosts: [],
        };
      }
      const rawCost = normalizeSkillData(skill).消耗 || getSkillCostText(skill);
      const costStr = rawCost && typeof rawCost === 'object' && !Array.isArray(rawCost)
        ? formatCostObjectToString(rawCost)
        : String(rawCost || '无');
      const presetCostScale = Number(skill?.__battleSupportCostScale);
      const 单体辅助消耗系数 =
        Number.isFinite(presetCostScale) && presetCostScale > 0
          ? presetCostScale
          : skill && char && skill.__targetForSupportCost && isSupportLikeSkill(skill)
            ? getSupportCostScale(char, skill.__targetForSupportCost)
            : 1;
      const costScale = 计算运行态目标数量消耗系数(skill, char, 单体辅助消耗系数);
      const statusCostScale = 获取施法消耗系数(char);
      const rawReqSp = parseResourceCostValue(
        costStr,
        '魂力',
        stats.sp ?? stats.魂力,
        stats.sp_max ?? stats.魂力上限,
      );
      const rawReqVit = parseResourceCostValue(
        costStr,
        '体力',
        stats.sta ?? stats.体力 ?? stats.vit,
        stats.sta_max ?? stats.体力上限 ?? stats.vit_max,
      );
      const rawReqMen = parseResourceCostValue(
        costStr,
        '精神力',
        stats.men ?? stats.精神力,
        stats.men_max ?? stats.精神力上限,
      );
      const 实际原始魂力消耗 = rawReqSp;
      const hpRatio =
        Math.max(0, Number(stats.hp ?? stats.HP ?? 0)) /
        Math.max(1, Number(stats.hp_max ?? stats.HP上限 ?? 1));
      const staminaCostScale = hpRatio <= 0.2 ? 2 : 1;
      const 自身消耗修正 = 读取技能自身结算修正(skill, '消耗', {
        ...context,
        actor: context?.actor || char,
        caster: context?.caster || char,
        attacker: context?.attacker || char,
        skill,
      });
      const 消耗绝对修正 = 获取施法消耗绝对修正(char) + 自身消耗修正.绝对;
      const 原始总消耗 = Math.max(1, 实际原始魂力消耗 + rawReqVit + rawReqMen);
      const 修正消耗 = (原始值, 倍率 = 1) => Math.max(0, Math.floor(原始值 * costScale * statusCostScale * 自身消耗修正.倍率 * 倍率 + 消耗绝对修正 * (原始值 / 原始总消耗)));
      const reqSp = 修正消耗(实际原始魂力消耗);
      const reqVit = 修正消耗(rawReqVit, staminaCostScale);
      const reqMen = 修正消耗(rawReqMen);
      const selfCanCast =
        ((stats.sp ?? stats.魂力) || 0) >= reqSp &&
        ((stats.sta ?? stats.体力 ?? stats.vit) || 0) >= reqVit &&
        ((stats.men ?? stats.精神力) || 0) >= reqMen;
      const fusionProfile = getFusionBattleProfileFromSkill(skill);
      const result = {
        reqSp,
        reqVit,
        reqMen,
        costScale: costScale * statusCostScale * 自身消耗修正.倍率,
        canCast: selfCanCast,
        failureReason: selfCanCast ? '' : '自身状态不足',
        partnerCosts: [],
      };
      if (getBattleSkillSourceCategory(skill) === '武魂融合技' && skill?.__融合可用 === false) {
        result.canCast = false;
        result.failureReason = String(skill?.__融合不可用原因 || '').trim() || '武魂融合技条件不足';
        return result;
      }
      if (!fusionProfile || fusionProfile.mode !== 'partner') return result;

      const 融合同队列表 = Array.isArray(skill?.__融合队友列表) ? skill.__融合队友列表 : [];
      const partnerUnits = resolveFusionPartnerUnitsForSkill(skill, 融合同队列表, null, char);
      const expectedNames = Array.isArray(skill?.__fusion_partner_names) ? skill.__fusion_partner_names : [];
      if (partnerUnits.length < Math.max(1, expectedNames.length)) {
        result.canCast = false;
        result.failureReason = expectedNames.length ? `搭档[${expectedNames.join('、')}]未到位` : '搭档未到位';
        return result;
      }

      const partnerCostText = String(skill?.__fusion_partner_cost_text || '无').trim() || '无';
      const partnerCosts = partnerUnits.map(unit => {
        const parsed = parseCostStringForChar(partnerCostText, unit, 1);
        return {
          ...parsed,
          unit,
          name: String(unit?.name || unit?.名称 || '搭档').trim() || '搭档',
        };
      });
      result.partnerCosts = partnerCosts;
      const failedPartner = partnerCosts.find(item => !item.canCast);
      if (failedPartner) {
        result.canCast = false;
        result.failureReason = `${failedPartner.name}状态不足`;
      }
      // Phase E: 摘出行动类型标注 + 技能谓词, 供 deductParsedCostFromUnit 推断疲劳率
      try {
        result.__action_type = String(skill?.action_type || skill?.类别 || skill?.category || '').trim();
        result.__风险等级 = String(skill?.风险等级 || skill?.summary?.风险等级 || '').trim();
        result.__来源类别 = (typeof 读取战斗来源类别上下文 === 'function')
          ? String(读取战斗来源类别上下文(skill, '魂技')?.来源类别 || '').trim()
          : '';
        // 技能谓词推断 (轻量, 不调 deriveBattleSummaryFromEffects)
        const _效果数组 = Array.isArray(skill?._效果数组) ? skill._效果数组 : [];
        const _原型集合 = _效果数组.map(e => String(e?.原型 || '').trim());
        result.__是输出 = _原型集合.some(p => /伤害结算|伤害|破甲|护盾.*斩除|护盾.*窃取/.test(p))
          || /^(伤害|输出|攻击)/.test(result.__action_type);
        result.__是治疗 = _原型集合.some(p => /生命恢复|资源变化.*生命|HP恢复/.test(p));
        result.__是控制 = _原型集合.some(p => /控制|眩晕|沉默|束缚|定身|混乱/.test(p));
        result.__是防护 = _原型集合.some(p => /护盾|防御|减伤|保护|减害/.test(p));
        result.__是召唤 = _原型集合.some(p => /^召唤生成$|召唤/.test(p));
        result.__是维持 = !!(getSkillCostText && /维持/.test(getSkillCostText(skill) || ''));
      } catch (e) { /* 缺依赖时静默 */ }
      return result;
    }

    function 计算技能消耗压力(skill, char) {
      const 空结果 = {
        可释放: true,
        失败原因: '',
        当前压力: 0,
        上限压力: 0,
        综合压力: 0,
        魂力占当前: 0,
        精神占当前: 0,
        体力占当前: 0,
      };
      if (!skill || !char) return 空结果;
      const 消耗片段 = splitSkillCostModes(getSkillCostText(skill));
      const 解析消耗 = parseSkillCostForChar({ ...skill, 消耗: 消耗片段.upfront || '无' }, char);
      const 属性 = char?.属性 || char || {};
      const 当前魂力 = Math.max(0, Number(属性.sp ?? 属性.魂力 ?? 0));
      const 当前精神力 = Math.max(0, Number(属性.men ?? 属性.精神力 ?? 0));
      const 当前体力 = Math.max(0, Number(属性.sta ?? 属性.体力 ?? 属性.vit ?? 0));
      const 魂力上限 = Math.max(1, Number(属性.sp_max ?? 属性.魂力上限 ?? 当前魂力 ?? 1));
      const 精神力上限 = Math.max(1, Number(属性.men_max ?? 属性.精神力上限 ?? 当前精神力 ?? 1));
      const 体力上限 = Math.max(1, Number(属性.sta_max ?? 属性.体力上限 ?? 属性.vit_max ?? 当前体力 ?? 1));
      const 魂力占当前 = 解析消耗.reqSp > 0 ? 解析消耗.reqSp / Math.max(1, 当前魂力) : 0;
      const 精神占当前 = 解析消耗.reqMen > 0 ? 解析消耗.reqMen / Math.max(1, 当前精神力) : 0;
      const 体力占当前 = 解析消耗.reqVit > 0 ? 解析消耗.reqVit / Math.max(1, 当前体力) : 0;
      const 当前压力 = Math.max(魂力占当前, 精神占当前, 体力占当前);
      const 上限压力 = Math.max(
        解析消耗.reqSp > 0 ? 解析消耗.reqSp / 魂力上限 : 0,
        解析消耗.reqMen > 0 ? 解析消耗.reqMen / 精神力上限 : 0,
        解析消耗.reqVit > 0 ? 解析消耗.reqVit / 体力上限 : 0,
      );
      return {
        可释放: 解析消耗.canCast !== false,
        失败原因: 解析消耗.failureReason || '',
        当前压力,
        上限压力,
        综合压力: Math.max(当前压力, 上限压力),
        魂力占当前,
        精神占当前,
        体力占当前,
        临界度: Math.max(魂力占当前, 精神占当前, 体力占当前),
      };
    }

    function 按资源压力调整权重(weight, skill, char, 选项 = {}) {
      let 修正权重 = Math.max(0, Number(weight || 0));
      if (修正权重 <= 0) return 0;
      const 消耗压力 = 计算技能消耗压力(skill, char);
      if (!消耗压力.可释放) return 0;
      const 回合 = Math.max(0, Number(选项.回合 || 0));
      const 战况推力 = Math.max(0, Math.min(1, Number(选项.战况推力 ?? 0)));
      const 是真身 = 选项.是真身 === true;
      const 消耗惩罚 = Math.ceil(消耗压力.当前压力 * 52 + 消耗压力.上限压力 * 22);
      const 临界推力 = Math.max(0, 消耗压力.临界度 - 0.45) * 78;
      const 回合推力 = Math.min(42, 回合 * 4.5) * Math.max(0, 消耗压力.综合压力 - 0.22);
      const 战况加权 = 战况推力 * (28 + 消耗压力.综合压力 * 70);
      const 保守扣权 = (1 - 战况推力) * Math.max(0, 消耗压力.综合压力 - 0.32) * (是真身 ? 70 : 48);
      修正权重 -= 消耗惩罚;
      修正权重 += 临界推力 + 回合推力 + 战况加权;
      修正权重 -= 保守扣权;
      if (是真身 && 战况推力 < 0.35) 修正权重 -= 24;
      return Math.max(0, Math.floor(修正权重));
    }

    function chooseWeightedOption(options) {
      const valid = (options || []).filter(option => option && option.weight > 0);
      if (valid.length === 0) return null;
      const totalWeight = valid.reduce((sum, option) => sum + option.weight, 0);
      let roll = Math.random() * totalWeight;
      for (const option of valid) {
        roll -= option.weight;
        if (roll <= 0) return option;
      }
      return valid[0];
    }

    function rollD100() {
      return Math.floor(Math.random() * 100) + 1;
    }

    function rollBranchByPriority(branches, phaseLabel) {
      const 有效分支 = (branches || [])
        .filter(branch => branch && Number(branch.weight || 0) > 0)
        .map(branch => {
          const 原始权重 = Math.max(0, Math.floor(Number(branch.weight || 0)));
          const 是早期真身低概率 =
            branch.__早期低概率 === true || (String(branch.name || '') === '开启真身' && 原始权重 <= 6);
          const 权重上限 = /技能选择/.test(String(phaseLabel || '')) ? 78 : 58;
          const 压缩权重 = 是早期真身低概率
            ? Math.max(1, Math.min(3, 原始权重))
            : Math.max(1, Math.min(权重上限, Math.round(4 + Math.sqrt(原始权重) * 4.8)));
          return { ...branch, __原始权重: 原始权重, __压缩权重: 压缩权重 };
        });
      if (!有效分支.length) return { option: null, trace: '', debugBranches: [] };

      const 空过权重 = /战略/.test(String(phaseLabel || ''))
        ? 110
        : /技能选择/.test(String(phaseLabel || ''))
          ? 16
          : 10;
      const 抽样池 = [...有效分支, { name: '暂不触发', __原始权重: 0, __压缩权重: 空过权重, __空过: true }];
      const 总权重 = 抽样池.reduce((sum, branch) => sum + Number(branch.__压缩权重 || 0), 0);
      let roll = Math.random() * Math.max(1, 总权重);
      let 命中分支 = 抽样池[抽样池.length - 1];
      for (const branch of 抽样池) {
        roll -= Number(branch.__压缩权重 || 0);
        if (roll <= 0) {
          命中分支 = branch;
          break;
        }
      }

      const debugBranches = 有效分支
        .sort((左, 右) => 右.__压缩权重 - 左.__压缩权重)
        .slice(0, 6)
        .map(branch => ({
          phase: String(phaseLabel || '').trim(),
          candidateName: String(branch.name || '未命名分支').trim(),
          rawWeight: Number(branch.__原始权重 || 0),
          compressedWeight: Number(branch.__压缩权重 || 0),
          candidateStatus: !命中分支.__空过 && branch === 命中分支 ? 'EXECUTED' : 'REJECTED',
          rejectionCode: !命中分支.__空过 && branch === 命中分支 ? '' : 'LOWER_PRIORITY',
        }));

      if (命中分支.__空过) return { option: null, trace: '当前未形成有效触发窗口', debugBranches };
      return {
        option: 命中分支,
        trace: '战术重心锁定',
        debugBranches,
        roll,
        weight: 命中分支.__压缩权重,
        rawWeight: 命中分支.__原始权重,
      };
    }

    function isPassiveSkillData(skill, sourceContext = {}) {
      if (!skill || typeof skill !== 'object') return false;
      if (String(skill?.承载方式 || '').trim() === '被动') return true;
      const 来源文本 = [
        sourceContext?.来源类别,
        sourceContext?.来源明细,
        读取战斗来源类别上下文(skill, '').来源类别,
        读取战斗来源类别上下文(skill, '').来源明细,
      ].map(value => String(value || '').trim()).filter(Boolean).join(' ');
      if (/血脉被动|被动/.test(来源文本)) return true;
      const 效果列表 = Array.isArray(skill?._效果数组) ? skill._效果数组 : [];
      if (效果列表.some(effect =>
        String(effect?.原型 || '').trim() === '规则改写' &&
        String(effect?.规则 || '').trim() === '死亡转存活' &&
        String(effect?.目标 || '自身').trim() === '自身'
      )) return true;
      if (效果列表.length > 0 && 效果列表.every(effect => 技能效果依赖当前行动条件(effect))) return true;
      const rawType = getSkillType(skill);
      return /被动/.test(String(rawType || ''));
    }

    function pushUnifiedSkillMapEntries(skills, skillMap, sourceTag, options = {}, sourceContext = {}) {
      const { includePassive = false, includeActive = true } = options;
      Object.entries(skillMap || {}).forEach(([skillName, skillData]) => {
        if (!skillData || skillData?.状态 === '未生成') return;
        const 来源类别 = 规范化战斗来源类别(sourceContext?.来源类别 || sourceTag, '魂技');
        const 来源明细 = String(sourceContext?.来源明细 || sourceTag || 来源类别).trim() || 来源类别;
        const 标准技能 = normalizeSkillData(skillData, skillName);
        写入战斗来源类别上下文(标准技能, {
          来源类别,
          来源明细,
          魂环路径: sourceContext?.魂环路径,
          魂技槽位: sourceContext?.魂技槽位 || skillName,
        });
        const 是否被动 = isPassiveSkillData(标准技能, { 来源类别, 来源明细 });
        if (是否被动 && !includePassive) return;
        if (!是否被动 && !includeActive) return;
        if (Array.isArray(sourceContext?.魂环路径) && sourceContext.魂环路径.length > 0) {
          标准技能.__魂环路径 = [...sourceContext.魂环路径];
        }
        skills.push(标准技能);
      });
    }

    function 套用复刻技能倍率(技能, 倍率 = 1) {
      const 比例 = Math.max(0.1, Math.min(1.5, Number(倍率 || 1)));
      if (Math.abs(比例 - 1) < 0.0001) return 技能;
      getSkillEffects(技能).forEach(effect => {
        ['威力倍率', '护盾值', '每回合伤害', 'dot_damage', 'final_damage_bonus', 'final_heal_bonus', 'shield_gain_bonus'].forEach(字段 => {
          if (Number.isFinite(Number(effect?.[字段]))) effect[字段] = scaleBattleValue(effect[字段], 比例, { min: 0, digits: 4 });
        });
        ['final_damage_mult', 'received_damage_mult', 'final_heal_mult', 'shield_gain_mult', 'skill_effect_mult'].forEach(字段 => {
          if (Number.isFinite(Number(effect?.[字段]))) effect[字段] = scaleBattleFactor(effect[字段], 比例, 1);
          if (effect?.计算层效果 && Number.isFinite(Number(effect.计算层效果[字段])))
            effect.计算层效果[字段] = scaleBattleFactor(effect.计算层效果[字段], 比例, 1);
        });
      });
      return 技能;
    }

    function collectUnifiedSkillEntries(charData, alliedTeam = [], options = {}) {
      const skills = [];
      const collectOptions = {
        includePassive: !!options.includePassive,
        includeActive: options.includeActive !== false,
        includeUnavailableFusion: !!options.includeUnavailableFusion,
      };

      取角色武魂条目_战斗(charData).forEach(([spKey, sp]) => {
        const spName = sp?.表象名称 || spKey || '武魂';
        取武魂魂灵条目_战斗(sp).forEach(([魂灵键, ss]) => {
          取魂灵魂环条目_战斗(ss).forEach(([魂环键, ring]) => {
            pushUnifiedSkillMapEntries(
              skills,
              Object.fromEntries(取魂环魂技条目_战斗(ring)),
              spName,
              collectOptions,
              { 来源类别: '魂技', 来源明细: spName, 魂环路径: [spKey, 魂灵键, 魂环键] },
            );
          });
        });
        取武魂直接魂环条目_战斗(sp).forEach(([魂环键, ring]) => {
          pushUnifiedSkillMapEntries(
            skills,
            Object.fromEntries(取魂环魂技条目_战斗(ring)),
            spName,
            collectOptions,
            { 来源类别: '魂技', 来源明细: spName, 魂环路径: [spKey, 魂环键] },
          );
        });
      });

      if (charData?.血脉之力) {
        pushUnifiedSkillMapEntries(skills, charData.血脉之力.技能 || {}, '血脉之力', collectOptions, { 来源类别: '血脉技能', 来源明细: '血脉之力' });
        pushUnifiedSkillMapEntries(skills, charData.血脉之力.被动 || {}, '血脉被动', collectOptions, { 来源类别: '血脉技能', 来源明细: '血脉被动' });
        取血脉气血魂环条目_战斗(charData.血脉之力).forEach(([, ring]) => {
          pushUnifiedSkillMapEntries(skills, Object.fromEntries(取气血魂环魂技条目_战斗(ring)), '气血魂技', collectOptions, { 来源类别: '气血魂技', 来源明细: '气血魂技' });
        });
      }

      Object.values(charData?.魂骨 || {}).forEach(bone => {
        const 合并魂骨 = 合并战斗物品定义_战斗(bone);
        pushUnifiedSkillMapEntries(skills, 合并魂骨?.附带技能 || 合并魂骨?.装备技能 || {}, '魂骨技能', collectOptions, { 来源类别: '魂骨技能', 来源明细: 合并魂骨?.名称 || 合并魂骨?.表象名称 || '魂骨技能' });
      });

      Object.entries(charData?.装备 || {}).forEach(([装备键, equip]) => {
        if (装备键 === '魂导器') return;
        if (!equip || typeof equip !== 'object' || Array.isArray(equip)) return;
        const 合并装备 = 合并战斗物品定义_战斗(equip);
        if (Number(合并装备.魂导等级 || 0) > 0) return;
        pushUnifiedSkillMapEntries(skills, 合并装备.装备技能 || {}, '装备技能', collectOptions, { 来源类别: '装备技能', 来源明细: 合并装备?.名称 || 合并装备?.name || '装备技能' });
      });
      pushUnifiedSkillMapEntries(skills, 构建魂导器技能表_战斗(charData?.装备?.魂导器), '装备技能', collectOptions, { 来源类别: '装备技能', 来源明细: '魂导器' });

      pushUnifiedSkillMapEntries(skills, charData?.自创魂技 || {}, '自创魂技', collectOptions, { 来源类别: '自创魂技', 来源明细: '自创魂技' });

      const 清理空复制效果 = 复制键 => {
        if (!charData?.复制效果?.[复制键]) return;
        const 记录 = charData.复制效果[复制键];
        if (!记录.技能列表 && !记录.属性快照) delete charData.复制效果[复制键];
        if (!Object.keys(charData.复制效果 || {}).length) delete charData.复制效果;
      };
      Object.entries(charData?.复制效果 || {}).forEach(([复制键, 记录]) => {
        if (!记录 || typeof 记录 !== 'object') return;
        const 到期tick = Math.max(0, Number(记录.到期tick || 0));
        const 当前tick = Math.max(0, Number(window.BattleUIBridge?.getMVU?.('world.时间.tick') || 0));
        if (到期tick > 0 && 当前tick >= 到期tick) {
          delete charData.复制效果[复制键];
          if (!Object.keys(charData.复制效果 || {}).length) delete charData.复制效果;
          return;
        }
        Object.entries(记录.技能列表 || {}).forEach(([技能键, item]) => {
          const 剩余次数 = item?.剩余次数 === undefined ? undefined : Math.max(0, Math.floor(Number(item.剩余次数 || 0)));
          if (剩余次数 !== undefined && 剩余次数 <= 0) {
            delete 记录.技能列表[技能键];
            if (!Object.keys(记录.技能列表 || {}).length) delete 记录.技能列表;
            清理空复制效果(复制键);
            return;
          }
          const 技能 = item?.技能数据 && typeof item.技能数据 === 'object' ? item.技能数据 : null;
          if (!技能 || typeof 技能 !== 'object') return;
          const 原名 = String(技能.name || 技能.魂技名 || 技能.技能名称 || 技能键).trim();
          const 复刻技能 = normalizeSkillData(deepClonePlain(技能), 原名);
          复刻技能.name = `复刻·${原名}`;
          复刻技能.魂技名 = 复刻技能.name;
          写入战斗来源类别上下文(复刻技能, { 来源类别: '复制技能', 来源明细: 复制键 });
          复刻技能.__复制效果键 = 复制键;
          复刻技能.__复制效果技能键 = 技能键;
          if (剩余次数 !== undefined) 复刻技能.__复刻可用次数 = 剩余次数;
          const 是否被动 = isPassiveSkillData(复刻技能);
          if (是否被动 && !collectOptions.includePassive) return;
          if (!是否被动 && !collectOptions.includeActive) return;
          skills.push(复刻技能);
        });
      });

      Object.entries(charData?.状态效果 || {}).forEach(([状态名, 状态]) => {
        if (!状态 || Number(状态.duration ?? 1) <= 0 || !状态.技能列表 || typeof 状态.技能列表 !== 'object') return;
        Object.entries(状态.技能列表 || {}).forEach(([技能键, item]) => {
          const 剩余次数 = item?.剩余次数 === undefined ? undefined : Math.max(0, Math.floor(Number(item.剩余次数 || 0)));
          if (剩余次数 !== undefined && 剩余次数 <= 0) return;
          const 技能 = item?.技能数据 && typeof item.技能数据 === 'object' ? item.技能数据 : null;
          if (!技能 || typeof 技能 !== 'object') return;
          const 原名 = String(技能.name || 技能.魂技名 || 技能.技能名称 || 技能键).trim();
          const 复刻技能 = normalizeSkillData(deepClonePlain(技能), 原名);
          复刻技能.name = `复刻·${原名}`;
          复刻技能.魂技名 = 复刻技能.name;
          写入战斗来源类别上下文(复刻技能, { 来源类别: '状态授予技能', 来源明细: 状态名 });
          复刻技能.__复制状态名 = 状态名;
          复刻技能.__复制状态技能键 = 技能键;
          if (剩余次数 !== undefined) 复刻技能.__复刻可用次数 = 剩余次数;
          const 是否被动 = isPassiveSkillData(复刻技能);
          if (是否被动 && !collectOptions.includePassive) return;
          if (!是否被动 && !collectOptions.includeActive) return;
          skills.push(复刻技能);
        });
      });

      Object.entries(charData?.状态效果 || {}).forEach(([状态名, 状态]) => {
        if (!状态 || Number(状态.duration ?? 1) <= 0 || 状态.效果授予状态 !== true && 状态.战斗效果?.mechanism_grant !== true) return;
        if (String(状态.触发条件 || '').trim() !== '主动触发') return;
        const 授予机制候选 = { ...状态, key: 状态名, name: 状态名, 原型: '机制授予' };
        const 移除规则 = 战斗机制抹消命中(charData, '中间机制', 授予机制候选, { 用途: '移除', 消费: false });
        if (移除规则) {
          delete charData.状态效果[状态名];
          if (charData.召唤键) 同步召唤单位镜像(charData);
          return;
        }
        if (战斗机制抹消命中(charData, '中间机制', 授予机制候选, { 用途: '封锁', 消费: false })) return;
        const 剩余次数 = Math.max(0, Math.floor(Number(状态.可用次数 ?? 1)));
        if (剩余次数 <= 0) return;
        const 原效果列表 = (Array.isArray(状态.授予效果) ? 状态.授予效果 : [])
          .filter(effect => effect && typeof effect === 'object')
          .filter(effect =>
            !战斗机制抹消命中(charData, '中间机制', effect, { 用途: '移除', 消费: false }) &&
            !战斗机制抹消命中(charData, '中间机制', effect, { 用途: '封锁', 消费: false }),
          );
        if (!原效果列表.length) return;
        const 临时技能名 = String(状态.来源技能 || 状态.name || 状态名.replace(/^原型授予[:：]?/, '') || '临时机制').trim() || '临时机制';
        const 临时技能 = normalizeSkillData({
          name: `授予·${临时技能名}`,
          魂技名: `授予·${临时技能名}`,
          技能分类: '辅助',
          消耗: '无',
          前摇: 10,
          _效果数组: 原效果列表.map(effect => ({
            ...deepClonePlain(effect),
            __机制授予主动触发: true,
            __机制授予状态名: 状态名,
          })),
        }, `授予·${临时技能名}`);
        写入战斗来源类别上下文(临时技能, { 来源类别: '状态授予技能', 来源明细: 状态名 });
        临时技能.__机制授予主动技能 = true;
        临时技能.__机制授予状态名 = 状态名;
        临时技能.__机制授予剩余次数 = 剩余次数;
        skills.push(临时技能);
      });

      Object.entries(charData?.武魂融合技 || {}).forEach(([fusionName, fusionSkill]) => {
        const 融合可用性 = 获取融合技能可用性(charData, fusionSkill, alliedTeam);
        if (!融合可用性.可用 && !collectOptions.includeUnavailableFusion) return;
        const 技能项 = buildFusionCombatSkill(fusionSkill, fusionName, charData);
        技能项.__融合可用 = !!融合可用性.可用;
        技能项.__融合不可用原因 = String(融合可用性.原因 || '');
        技能项.__融合相关度总分 = Number(融合可用性.相关度总分 || 技能项.__融合相关度总分 || 0);
        技能项.__融合队友列表 = Array.isArray(alliedTeam) ? alliedTeam : [];
        const 是否被动 = isPassiveSkillData(技能项);
        if (是否被动 && !collectOptions.includePassive) return;
        if (!是否被动 && !collectOptions.includeActive) return;
        skills.push(技能项);
      });

      return skills;
    }

    function collectPassiveCombatSkills(charData, alliedTeam = []) {
      return collectUnifiedSkillEntries(charData, alliedTeam, { includePassive: true, includeActive: false });
    }

    const AUTO_PROJECTED_CONDITION_PREFIX = '__auto__:';

    function clearAutoProjectedConditions(char) {
      if (!char?.状态效果) return;
      Object.keys(char.状态效果).forEach(key => {
        if (String(key).startsWith(AUTO_PROJECTED_CONDITION_PREFIX)) delete char.状态效果[key];
      });
    }

    function createProjectedCondition(description, type = 'buff', statMods = {}, combatEffects = {}, duration = 999) {
      return {
        类型: type,
        层数: 1,
        描述: description || '自动投影效果',
        duration,
        面板修改比例: {
          str: Number(statMods.str ?? 1),
          def: Number(statMods.def ?? 1),
          agi: Number(statMods.agi ?? 1),
          sp_max: Number(statMods.sp_max ?? 1),
          vit_max: Number(statMods.vit_max ?? 1),
          men_max: Number(statMods.men_max ?? 1),
        },
        战斗效果: mergeCombatEffectMaps(createEmptyCombatEffectMap(), combatEffects || {}),
      };
    }

    function projectPassiveSkillToConditions(char, skill) {
      if (!char?.状态效果 || !skill) return;
      const sourceName = skill.name || skill.技能名称 || '被动技能';
      getSkillEffects(skill).forEach((effect, index) => {
        const 原型 = String(effect?.原型 || '').trim();
        if (!原型 || !BATTLE_PROTOTYPE_REGISTRY[原型]) return;
        if (原型 === '状态施加' && effect?.状态 && effect.状态 !== '无') {
          const specialFlag = effect.特殊机制标识 || '无';
          const calc = effect.计算层效果 || {};
          const 状态名 = String(effect.状态 || '').trim();
          const isBuff =
            /增益|真身|被动/.test(specialFlag) ||
            Number(calc.hit_bonus || 0) > 0 ||
            Number(calc.reaction_bonus || 0) > 0 ||
            Number(calc.dodge_bonus || 0) > 0 ||
            Number(calc.defense_strip || 0) > 0 ||
            Number(calc.spirit_resist_strip || 0) > 0 ||
            Number(calc.final_heal_mult || 1) > 1 ||
            Number(calc.final_damage_mult || 1) > 1 ||
            calc.super_armor === true ||
            calc.探查屏蔽 === true ||
            Number(calc.min_hp_floor || 0) > 0;
          char.状态效果[`${AUTO_PROJECTED_CONDITION_PREFIX}${sourceName}:${状态名}`] =
            createProjectedCondition(
              `自动投影[${sourceName}]`,
              isBuff ? 'buff' : 'debuff',
              effect.面板修改比例 || {},
              calc,
              999,
            );
          if (effect.面板固定修正 && typeof effect.面板固定修正 === 'object') {
            char.状态效果[`${AUTO_PROJECTED_CONDITION_PREFIX}${sourceName}:${状态名}`].面板固定修正 = {
              ...effect.面板固定修正,
            };
          }
          return;
        }
        if (
          原型 === '属性修正' &&
          ((effect?.面板修改比例 && typeof effect.面板修改比例 === 'object') ||
            (effect?.面板固定修正 && typeof effect.面板固定修正 === 'object'))
        ) {
          const mult = 1 + Math.max(0, Number(effect.强化值 || 0));
          char.状态效果[`${AUTO_PROJECTED_CONDITION_PREFIX}${sourceName}:属性永久强化:${index}`] =
            createProjectedCondition(
              `自动投影[${sourceName}]·属性永久强化`,
              'buff',
              effect.面板修改比例 || { str: mult, def: mult, agi: mult, sp_max: mult, vit_max: mult, men_max: mult },
              {},
              999,
            );
          if (effect.面板固定修正 && typeof effect.面板固定修正 === 'object') {
            char.状态效果[`${AUTO_PROJECTED_CONDITION_PREFIX}${sourceName}:属性永久强化:${index}`].面板固定修正 = {
              ...effect.面板固定修正,
            };
          }
        }
      });
      const runtimeEffect = createEmptyCombatEffectMap();
      applyRuntimeMechanismEffects(skill, char, char, char, char, runtimeEffect);
      if (hasMeaningfulCombatEffect(runtimeEffect)) {
        char.状态效果[`${AUTO_PROJECTED_CONDITION_PREFIX}${sourceName}:原型投影`] = createProjectedCondition(
          `自动投影[${sourceName}]·原型效果`,
          'buff',
          {},
          runtimeEffect,
          999,
        );
      }
    }

    function getActiveStructuredDomain(char) {
      if (!char || typeof char !== 'object') return null;
      const domain = char.精神领域 || {};
      const modifiers = domain.战斗修饰 || {};
      const enabled = Object.values(modifiers).some(mod => mod?.启用);
      const isActive =
        !!domain.进行中 || String(char.当前领域 || char.状态?.当前领域 || '').includes('精神领域');
      if (!enabled || !isActive) return null;
      return { name: domain.名称 || '精神领域', modifiers };
    }

    function buildDomainSuppressionStatMods(targetStat = 'all', reduceRatio = 0.3) {
      const mult = Math.max(0.05, 1 - Math.max(0, Number(reduceRatio || 0)));
      const mods = { str: 1, def: 1, agi: 1, sp_max: 1, vit_max: 1, men_max: 1 };
      if (targetStat === 'all') return { str: mult, def: mult, agi: mult, sp_max: mult, vit_max: mult, men_max: mult };
      const keyMap = {
        str: 'str',
        def: 'def',
        agi: 'agi',
        sp: 'sp_max',
        sp_max: 'sp_max',
        vit: 'vit_max',
        vit_max: 'vit_max',
        men: 'men_max',
        men_max: 'men_max',
      };
      const mapped = keyMap[targetStat] || null;
      if (mapped) mods[mapped] = mult;
      return mods;
    }

    function resolveArmorDomainDescriptor(char) {
      const activeDomainText = String(char?.当前领域 || char?.状态?.当前领域 || '');
      if (!activeDomainText.includes('斗铠领域')) return null;

      const isFourWord = activeDomainText.includes('四字');
      const ratio = isFourWord ? 1.2 : 1.1;
      const requiredCount = isFourWord ? 2 : 1;
      const attrPool = ['sp_max', 'men_max', 'str', 'def', 'agi', 'vit_max'];
      const bracketMatch = activeDomainText.match(/\[([^\]]+)\]/);
      let selectedAttrs = bracketMatch
        ? bracketMatch[1]
            .split(/[，,]/)
            .map(attr => String(attr || '').trim())
            .filter(attr => attrPool.includes(attr))
        : [];

      if (selectedAttrs.length > requiredCount) selectedAttrs = selectedAttrs.slice(0, requiredCount);

      if (activeDomainText.includes('未定') || selectedAttrs.length < requiredCount) {
        const remaining = [...attrPool];
        selectedAttrs = [];
        while (selectedAttrs.length < requiredCount && remaining.length > 0) {
          const index = Math.floor(Math.random() * remaining.length);
          selectedAttrs.push(remaining.splice(index, 1)[0]);
        }
        char.当前领域 = isFourWord
          ? `【四字斗铠领域】全开[${selectedAttrs.join(',')}]`
          : `【三字斗铠领域】全开[${selectedAttrs.join(',')}]`;
      }

      return {
        name: String(char.当前领域 || char?.状态?.当前领域 || activeDomainText),
        ratio,
        selectedAttrs,
      };
    }

    function projectDomainConditionsForParticipant(char, opposingTeam = []) {
      if (!char?.状态效果) return;
      const armorDomain = resolveArmorDomainDescriptor(char);
      if (armorDomain) {
        const statMods = { str: 1, def: 1, agi: 1, sp_max: 1, vit_max: 1, men_max: 1 };
        armorDomain.selectedAttrs.forEach(attr => {
          statMods[attr] = armorDomain.ratio;
        });
        char.状态效果[`${AUTO_PROJECTED_CONDITION_PREFIX}斗铠领域:${armorDomain.name}`] = createProjectedCondition(
          `自动投影[${armorDomain.name}]·斗铠增幅`,
          'buff',
          statMods,
          {},
          999,
        );
      }

      const ownDomain = getActiveStructuredDomain(char);
      if (ownDomain) {
        const selfEffects = createEmptyCombatEffectMap();
        const ownMods = ownDomain.modifiers;
        if (ownMods.条件闪避?.启用) {
          const compareStat = ownMods.条件闪避.比对属性 || 'men';
          const selfValue = getMechanismDriveValue(char, char.final || char, compareStat);
          const maxEnemy = Math.max(
            0,
            ...(opposingTeam || []).map(unit => getMechanismDriveValue(unit, unit.final || unit, compareStat)),
          );
          const maxRatio = Math.max(1, Number(ownMods.条件闪避.最大倍率 || 1.5));
          if (maxEnemy <= selfValue * maxRatio) {
            selfEffects.dodge_bonus += 0.25;
            selfEffects.reaction_bonus += 0.1;
          }
        }
        if (ownMods.必中真伤?.启用) {
          const ratio = Math.max(0, Number(ownMods.必中真伤.真伤比例 || 0.1));
          selfEffects.hit_bonus += 0.12;
          selfEffects.lock_level = Math.max(Number(selfEffects.lock_level || 0), 1);
          selfEffects.final_damage_mult *= 1 + Math.min(0.35, ratio);
          selfEffects.bonus_true_damage_ratio += ratio;
        }
        if (ownMods.灵魂汲取?.启用) {
          selfEffects.damage_absorb_ratio += Math.max(0, Math.min(1, Number(ownMods.灵魂汲取.汲取比例 || 0.5)));
        }
        if (hasMeaningfulCombatEffect(selfEffects)) {
          char.状态效果[`${AUTO_PROJECTED_CONDITION_PREFIX}领域:${ownDomain.name}:自我法则`] =
            createProjectedCondition(`自动投影[${ownDomain.name}]·自我法则`, 'buff', {}, selfEffects, 999);
        }
      }

      (opposingTeam || []).forEach(enemy => {
        const enemyDomain = getActiveStructuredDomain(enemy);
        if (!enemyDomain) return;
        const mods = enemyDomain.modifiers;
        const effectMap = createEmptyCombatEffectMap();
        let statMods = { str: 1, def: 1, agi: 1, sp_max: 1, vit_max: 1, men_max: 1 };
        if (mods.属性压制?.启用)
          statMods = buildDomainSuppressionStatMods(
            mods.属性压制.目标属性 || 'all',
            mods.属性压制.削弱比例 || 0.3,
          );
        if (mods.时间迟滞?.启用) {
          const mult = Math.max(1, Number(mods.时间迟滞.蓄力倍率 || 2));
          effectMap.reaction_penalty += Math.min(0.45, (mult - 1) * 0.2);
          effectMap.cast_speed_penalty += Math.min(2, mult - 1);
          effectMap.dodge_penalty += Math.min(0.2, (mult - 1) * 0.08);
        }
        if (mods.幻境偏移?.启用) {
          const chance = Math.max(0, Math.min(0.9, Number(mods.幻境偏移.偏移概率 || 0.4)));
          effectMap.hit_penalty += Number((chance * 0.5).toFixed(2));
          effectMap.reaction_penalty += Number((chance * 0.2).toFixed(2));
          effectMap.reaction_penalty += Number((chance * 0.15).toFixed(2));
        }
        if (hasMeaningfulCombatEffect(effectMap) || Object.values(statMods).some(v => Number(v || 1) !== 1)) {
          char.状态效果[
            `${AUTO_PROJECTED_CONDITION_PREFIX}领域压制:${enemy.name || enemyDomain.name}:${char.name || '目标'}`
          ] = createProjectedCondition(`自动投影[${enemyDomain.name}]·领域压制`, 'debuff', statMods, effectMap, 999);
        }
      });
    }

    // ===== Phase E: 体力疲劳 + 全属性衰减 =====
    // 设计:
    //   1. 任何主动行动消耗基础行动疲劳, 按行动类型分级 (见 行动疲劳率表_V1)
    //   2. 体力比例越低, 全属性 (str/def/agi 6 维) 衰减越严重
    //   3. 体力 = 0 → 已有"昏迷 KO"判定接管
    const 体力衰减分段表_V1 = Object.freeze([
      { 比例下界: 0.50, 系数: 1.00 },  // ≥ 50% 充沛
      { 比例下界: 0.30, 系数: 0.90 },  // 30-50% 疲劳
      { 比例下界: 0.15, 系数: 0.75 },  // 15-30% 力竭
      { 比例下界: 0.00, 系数: 0.50 },  // 0-15% 濒崩
    ]);

    // 行动疲劳率表 — 体力 = 肉体动作能量, 物理动作扣得多, 纯施法扣得少
    // 优先级: action_type 命中纯操作型 > 装备激活 > 召唤/维持极低 > 控制/治疗/辅助低 > 输出按品质
    const 行动疲劳率表_V1 = Object.freeze({
      // 纯操作型动作 (无 skill 对应, action_type 直接命中)
      '待机':         0.003,
      '调息':         0.003,
      '稳态调整':     0.005,
      '防御':         0.012,  // 格挡仍消耗
      '移动':         0.015,
      '位移':         0.015,
      '普通攻击':     0.030,
      '攻击':         0.030,
      '闪避':         0.040,  // 高敏捷动作
    });
    // 按 skill 谓词分级 (体力消耗 = 物理动作强度, 不是技能强度)
    const 行动疲劳率_装备激活_V1 = 0.025;
    const 行动疲劳率_召唤_V1 = 0.003;        // 站着施法, 几乎不耗
    const 行动疲劳率_维持_V1 = 0.003;        // 只消耗魂力
    const 行动疲劳率_控制治疗辅助_V1 = 0.005;  // 精神力/魂力主导
    const 行动疲劳率_输出基础_V1 = 0.040;     // 普通攻击型魂技
    const 行动疲劳率_输出高品质_V1 = 0.070;   // 风险等级=高 大招
    const 行动疲劳率_输出融合技_V1 = 0.120;   // 武魂融合技, 物理+魂力双重
    const 行动疲劳率默认_V1 = 0.020;

    function 计算体力衰减系数_V1(单位 = {}) {
      const stats = 单位?.属性 || 单位 || {};
      const 体力上限 = Math.max(1, Number(stats.体力上限 ?? stats.vit_max ?? 1));
      const 当前体力 = Math.max(0, Number(stats.体力 ?? stats.vit ?? 0));
      const 比例 = 当前体力 / 体力上限;
      for (const 段 of 体力衰减分段表_V1) {
        if (比例 >= 段.比例下界) return 段.系数;
      }
      return 体力衰减分段表_V1[体力衰减分段表_V1.length - 1].系数;
    }

    // 解析 parsed 的行动类型, 返回疲劳率 (% 体力上限)
    function 推断行动疲劳率_V1(parsed = {}) {
      const 来源类别 = String(parsed?.__来源类别 || '').trim();
      const 风险等级 = String(parsed?.__风险等级 || '').trim();
      const action_type = String(parsed?.__action_type || '').trim();
      const 是输出 = !!parsed?.__是输出;
      const 是控制 = !!parsed?.__是控制;
      const 是治疗 = !!parsed?.__是治疗;
      const 是防护 = !!parsed?.__是防护;
      const 是召唤 = !!parsed?.__是召唤;
      const 是维持 = !!parsed?.__是维持;
      // 1. 纯操作型动作 (无 skill, action_type 命中) — 闪避 / 移动 / 待机 / 普通攻击 / 防御
      if (行动疲劳率表_V1[action_type] !== undefined) return 行动疲劳率表_V1[action_type];
      // 2. 装备激活 — 穿戴动作, 中等物理消耗
      if (来源类别 === '装备技能') return 行动疲劳率_装备激活_V1;
      // 3. 召唤 — 站着施法, 几乎不耗体力
      if (是召唤) return 行动疲劳率_召唤_V1;
      // 4. 维持类 — 持续状态, 只消耗魂力
      if (是维持 && !是输出) return 行动疲劳率_维持_V1;
      // 5. 控制 / 治疗 / 防护 — 精神力/魂力主导, 体力极低耗
      if ((是控制 || 是治疗 || 是防护) && !是输出) return 行动疲劳率_控制治疗辅助_V1;
      // 6. 输出类 — 物理动作强度, 按品质细分
      if (是输出) {
        if (来源类别 === '武魂融合技') return 行动疲劳率_输出融合技_V1;
        if (风险等级 === '高') return 行动疲劳率_输出高品质_V1;
        return 行动疲劳率_输出基础_V1;
      }
      // 7. 默认 (混合 / 不明类型)
      return 行动疲劳率默认_V1;
    }

    function 计算行动疲劳体力_V1(单位 = {}, parsed = {}) {
      const stats = 单位?.属性 || 单位 || {};
      const 体力上限 = Math.max(1, Number(stats.体力上限 ?? stats.vit_max ?? 1));
      const 疲劳率 = 推断行动疲劳率_V1(parsed);
      return Math.max(1, Math.round(体力上限 * 疲劳率));
    }

    function buildCombatFinalStats(char) {
      const final = deepClone(char || {});
      final.状态效果 = deepClone(char?.状态效果 || {});
      final.战斗效果 = createEmptyCombatEffectMap();
      const 当前tick = Math.max(0, Number(window.BattleUIBridge?.getMVU?.('world.时间.tick') || 0));
      const 套用复制属性快照 = 快照 => {
        if (!快照 || typeof 快照 !== 'object' || Array.isArray(快照)) return;
        [
          ['力量', 'str'],
          ['防御', 'def'],
          ['敏捷', 'agi'],
          ['体力上限', 'vit_max'],
          ['魂力上限', 'sp_max'],
          ['精神力上限', 'men_max'],
        ].forEach(([中文字段, 战斗字段]) => {
          const value = Number(快照[中文字段] ?? 快照[战斗字段]);
          if (Number.isFinite(value) && value > 0) final[战斗字段] = value;
        });
      };
      const 复制属性快照列表 = [];
      Object.values(final.状态效果 || {}).forEach(cond => {
        const mods = cond?.面板修改比例 || {};
        const deltas = cond?.面板固定修正 || {};
        final.str = Number(final.str || 0) * Number(mods.str ?? 1);
        final.def = Number(final.def || 0) * Number(mods.def ?? 1);
        final.agi = Number(final.agi || 0) * Number(mods.agi ?? 1);
        if (final.sp_max !== undefined) final.sp_max = Number(final.sp_max || 0) * Number(mods.sp_max ?? 1);
        if (final.vit_max !== undefined) final.vit_max = Number(final.vit_max || 0) * Number(mods.vit_max ?? 1);
        if (final.men_max !== undefined) final.men_max = Number(final.men_max || 0) * Number(mods.men_max ?? 1);
        final.str += Number(deltas.str || 0);
        final.def += Number(deltas.def || 0);
        final.agi += Number(deltas.agi || 0);
        if (final.sp_max !== undefined) final.sp_max += Number(deltas.sp_max || 0);
        if (final.vit_max !== undefined) final.vit_max += Number(deltas.vit_max || 0);
        if (final.men_max !== undefined) final.men_max += Number(deltas.men_max || 0);
        final.战斗效果 = mergeCombatEffectMaps(final.战斗效果, cond?.战斗效果 || {});
        if (cond?.属性快照) 复制属性快照列表.push(cond.属性快照);
      });
      复制属性快照列表.forEach(套用复制属性快照);
      Object.entries(char?.复制效果 || {}).forEach(([复制键, record]) => {
        if (!record || typeof record !== 'object') return;
        const 到期tick = Math.max(0, Number(record.到期tick || 0));
        if (到期tick > 0 && 当前tick >= 到期tick) return;
        套用复制属性快照(record.属性快照);
      });
      if (final.sp_max !== undefined && final.sp !== undefined) final.sp = Math.min(final.sp, final.sp_max);
      if (final.vit_max !== undefined && final.vit !== undefined) final.vit = Math.min(final.vit, final.vit_max);
      if (final.men_max !== undefined && final.men !== undefined) final.men = Math.min(final.men, final.men_max);
      // Phase E: 体力衰减 — 体力低于阈值时,全 6 维属性按分段系数下调
      const 体力衰减系数 = 计算体力衰减系数_V1(char);
      if (体力衰减系数 < 1.0) {
        final.str = Number(final.str || 0) * 体力衰减系数;
        final.def = Number(final.def || 0) * 体力衰减系数;
        final.agi = Number(final.agi || 0) * 体力衰减系数;
        if (final.sp_max !== undefined) final.sp_max = Number(final.sp_max || 0) * 体力衰减系数;
        if (final.vit_max !== undefined) final.vit_max = Number(final.vit_max || 0) * 体力衰减系数;
        if (final.men_max !== undefined) final.men_max = Number(final.men_max || 0) * 体力衰减系数;
        final.__体力衰减系数 = 体力衰减系数;
      }
      final.str = Math.round(Number(final.str || 0));
      final.def = Math.round(Number(final.def || 0));
      final.agi = Math.round(Number(final.agi || 0));
      if (final.sp_max !== undefined) final.sp_max = Math.round(Number(final.sp_max || 0));
      if (final.vit_max !== undefined) final.vit_max = Math.round(Number(final.vit_max || 0));
      if (final.men_max !== undefined) final.men_max = Math.round(Number(final.men_max || 0));
      return final;
    }

    function refreshParticipantProjectedState(char, alliedTeam = [], opposingTeam = []) {
      if (!char) return char;
      bindCombatParticipant(char);
      clearAutoProjectedConditions(char);
      collectPassiveCombatSkills(char, alliedTeam).forEach(skill => projectPassiveSkillToConditions(char, skill));
      projectDomainConditionsForParticipant(char, opposingTeam);
      char.final = buildCombatFinalStats(char);
      return char;
    }

    function applyStateToCharacter(targetChar, stateModule, sourceName, forceBuff) {
      if (!targetChar || !stateModule || !stateModule.状态名称 || stateModule.状态名称 === '无') return false;
      if (!targetChar.状态效果) targetChar.状态效果 = {};

      const specialFlag = stateModule.特殊机制标识 || '无';
      const calc = stateModule.计算层效果 || {};
      const hasPositiveCalc =
        Number(calc.hit_bonus || 0) > 0 ||
        Number(calc.reaction_bonus || 0) > 0 ||
        Number(calc.dodge_bonus || 0) > 0 ||
        Number(calc.vit_gain_ratio || 0) > 0 ||
        Number(calc.sp_gain_ratio || 0) > 0 ||
        Number(calc.men_gain_ratio || 0) > 0 ||
        Number(calc.final_heal_mult || 1.0) > 1.0 ||
        Number(calc.shield_gain_mult || 1.0) > 1.0 ||
        Number(calc.hot_heal_ratio || 0) > 0 ||
        calc.invincible === true ||
        calc.super_armor === true ||
        calc.探查屏蔽 === true ||
        Number(calc.min_hp_floor || 0) > 0 ||
        Number(calc.revive_count || 0) > 0;
      const isBuff =
        forceBuff === true || hasPositiveCalc || specialFlag.includes('增益') || specialFlag.includes('真身');
      if (!isBuff && 探查屏蔽阻断敌对锁定状态(targetChar, stateModule)) return false;

      const 状态条目 = {
        类型: isBuff ? 'buff' : 'debuff',
        层数: 1,
        描述: `由[${sourceName || stateModule.状态名称}]附加`,
        duration: stateModule.持续回合 || 0,
        面板修改比例: stateModule.面板修改比例 || { str: 1.0, def: 1.0, agi: 1.0, sp_max: 1.0 },
        战斗效果: {
          ...mergeCombatEffectMaps(createEmptyCombatEffectMap(), stateModule.计算层效果 || {}),
          skip_turn: stateModule.计算层效果?.skip_turn ?? false,
          cannot_react: stateModule.计算层效果?.cannot_react ?? false,
          invincible: stateModule.计算层效果?.invincible ?? false,
          skill_seal: stateModule.计算层效果?.skill_seal ?? false,
          silence: stateModule.计算层效果?.silence ?? false,
          disarm: stateModule.计算层效果?.disarm ?? false,
          blind: stateModule.计算层效果?.blind ?? false,
          探查屏蔽: stateModule.计算层效果?.探查屏蔽 ?? false,
          dot_damage: (stateModule.计算层效果?.dot_damage ?? stateModule.持续真伤dot) || 0,
          armor_pen: stateModule.计算层效果?.armor_pen ?? 0,
          reaction_bonus: stateModule.计算层效果?.reaction_bonus ?? 0,
          reaction_penalty: stateModule.计算层效果?.reaction_penalty ?? 0,
          attacker_speed_bonus: stateModule.计算层效果?.attacker_speed_bonus ?? 0,
          cast_speed_bonus: stateModule.计算层效果?.cast_speed_bonus ?? 0,
          cast_speed_penalty: stateModule.计算层效果?.cast_speed_penalty ?? 0,
          hit_bonus: stateModule.计算层效果?.hit_bonus ?? 0,
          hit_penalty: stateModule.计算层效果?.hit_penalty ?? 0,
          dodge_bonus: stateModule.计算层效果?.dodge_bonus ?? 0,
          dodge_penalty: stateModule.计算层效果?.dodge_penalty ?? 0,
          lock_level: stateModule.计算层效果?.lock_level ?? 0,
          interrupt_bonus: stateModule.计算层效果?.interrupt_bonus ?? 0,
          final_damage_mult: stateModule.计算层效果?.final_damage_mult ?? 1.0,
          received_damage_mult: stateModule.计算层效果?.received_damage_mult ?? 1.0,
          defense_strip: stateModule.计算层效果?.defense_strip ?? 0,
          spirit_resist_strip: stateModule.计算层效果?.spirit_resist_strip ?? 0,
          final_damage_bonus: stateModule.计算层效果?.final_damage_bonus ?? 0,
          final_heal_mult: stateModule.计算层效果?.final_heal_mult ?? 1.0,
          final_heal_bonus: stateModule.计算层效果?.final_heal_bonus ?? 0,
          shield_gain_mult: stateModule.计算层效果?.shield_gain_mult ?? 1.0,
          shield_gain_bonus: stateModule.计算层效果?.shield_gain_bonus ?? 0,
          vit_gain_ratio: stateModule.计算层效果?.vit_gain_ratio ?? 0,
          sp_gain_ratio: stateModule.计算层效果?.sp_gain_ratio ?? 0,
          men_gain_ratio: stateModule.计算层效果?.men_gain_ratio ?? 0,
          heal_block_ratio: stateModule.计算层效果?.heal_block_ratio ?? 0,
          hot_heal_ratio: stateModule.计算层效果?.hot_heal_ratio ?? 0,
          cost_ratio: stateModule.计算层效果?.cost_ratio ?? 1.0,
          cost_delta: stateModule.计算层效果?.cost_delta ?? 0,
          cost_delta_ratio: stateModule.计算层效果?.cost_delta_ratio ?? 0,
          windup_ratio: stateModule.计算层效果?.windup_ratio ?? 1.0,
          windup_delta: stateModule.计算层效果?.windup_delta ?? 0,
          stealth_level: stateModule.计算层效果?.stealth_level ?? 0,
          min_hp_floor: stateModule.计算层效果?.min_hp_floor ?? 0,
          death_save_count: stateModule.计算层效果?.death_save_count ?? 0,
          revive_count: stateModule.计算层效果?.revive_count ?? 0,
          revive_heal_ratio: stateModule.计算层效果?.revive_heal_ratio ?? 0,
          damage_reflect_ratio: stateModule.计算层效果?.damage_reflect_ratio ?? 0,
          damage_transfer_ratio: stateModule.计算层效果?.damage_transfer_ratio ?? 0,
          damage_transfer_target: stateModule.计算层效果?.damage_transfer_target ?? '',
          damage_share_ratio: stateModule.计算层效果?.damage_share_ratio ?? 0,
          damage_share_count: stateModule.计算层效果?.damage_share_count ?? 0,
          cost_share_ratio: stateModule.计算层效果?.cost_share_ratio ?? 0,
          cost_share_count: stateModule.计算层效果?.cost_share_count ?? 0,
          heal_inversion_ratio: stateModule.计算层效果?.heal_inversion_ratio ?? 0,
          invincible_tier_threshold: stateModule.计算层效果?.invincible_tier_threshold ?? 0,
          每日触发次数上限: stateModule.计算层效果?.每日触发次数上限 ?? 0,
          bonus_true_damage_ratio: stateModule.计算层效果?.bonus_true_damage_ratio ?? 0,
          damage_absorb_ratio: stateModule.计算层效果?.damage_absorb_ratio ?? 0,
          damage_to_heal_ratio: stateModule.计算层效果?.damage_to_heal_ratio ?? 0,
          heal_to_damage_ratio: stateModule.计算层效果?.heal_to_damage_ratio ?? 0,
          吸收来源: stateModule.计算层效果?.吸收来源 ?? '',
          吸收资源: stateModule.计算层效果?.吸收资源 ?? '',
          吸收转化效果: stateModule.计算层效果?.吸收转化效果 ?? '',
          伤害吸收增幅上限: stateModule.计算层效果?.伤害吸收增幅上限 ?? 0,
        },
      };
      if (stateModule.原型) 状态条目.来源原型 = stateModule.原型;
      const 持续移除命中 = 持续状态移除阻断状态附着(targetChar, stateModule.状态名称, 状态条目, sourceChar || targetChar);
      if (持续移除命中) return false;
      targetChar.状态效果[stateModule.状态名称] = 状态条目;
      targetChar.final = buildCombatFinalStats(targetChar);
      if (targetChar.召唤键) 同步召唤单位镜像(targetChar);
      return true;
    }

    function 登记遥控状态效果(targetChar, stateEffect = {}, sourceName = '') {
      if (!targetChar || !stateEffect || typeof stateEffect !== 'object') return '';
      const 状态名 = String(stateEffect?.状态名称 || stateEffect?.状态 || '').trim();
      if (!状态名) return '';
      if (!targetChar.状态效果 || typeof targetChar.状态效果 !== 'object') targetChar.状态效果 = {};
      const 持续回合 = Math.max(1, Number(stateEffect?.持续回合 || 3));
      const 状态键基础 = `遥控:${状态名}`;
      let 状态键 = 状态键基础;
      let 序号 = 2;
      while (targetChar.状态效果[状态键]) {
        状态键 = `${状态键基础}·${序号}`;
        序号 += 1;
      }
      const 遥控效果 = deepClonePlain(stateEffect);
      遥控效果.触发方式 = '立即触发';
      delete 遥控效果.延迟回合;
      targetChar.状态效果[状态键] = {
        类型: targetChar === stateEffect.__施术者 ? 'buff' : 'debuff',
        层数: 1,
        描述: `由[${sourceName || '技能'}]预置，等待遥控触发`,
        duration: 持续回合,
        遥控触发: true,
        遥控效果,
        面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
        战斗效果: createEmptyCombatEffectMap(),
      };
      return 状态键;
    }

    function 触发遥控状态效果(触发者 = {}, 目标 = {}, 输入文本 = '', sourceName = '遥控触发') {
      const 单位列表 = dedupeCombatTargetList([触发者, 目标]);
      const 文本 = String(输入文本 || '').trim();
      if (!/触发|发动|引爆|引动|激活/.test(文本)) return { 日志: '', 触发数: 0 };
      const 日志 = [];
      let 触发数 = 0;
      单位列表.forEach(单位 => {
        if (!单位?.状态效果 || typeof 单位.状态效果 !== 'object') return;
        Object.entries({ ...单位.状态效果 }).forEach(([状态键, 状态]) => {
          if (!状态?.遥控触发 || !状态?.遥控效果) return;
          const 状态名 = String(状态?.遥控效果?.状态名称 || 状态?.遥控效果?.状态 || 状态键).trim();
          if (状态名 && !文本.includes(状态名) && !文本.includes(状态键) && !/遥控|预置/.test(文本)) return;
          const effect = deepClonePlain(状态.遥控效果);
          delete 单位.状态效果[状态键];
          if (applyStateToCharacter(单位, effect, sourceName, 单位 === 触发者)) {
            触发数 += 1;
            日志.push(`[遥控触发] ${单位.name || 单位.名称 || '目标'}的[${状态名}]被触发。`);
          }
        });
      });
      return { 日志: 日志.join(' '), 触发数 };
    }

    function dedupeCombatTargetList(units = []) {
      const seen = new Set();
      const result = [];
      (Array.isArray(units) ? units : []).forEach(unit => {
        if (!unit) return;
        const identity = String(unit?.召唤键 || unit?.name || unit?.名称 || '').trim() || JSON.stringify([unit?.type, unit?.vit_max, unit?.str]);
        if (!identity || seen.has(identity)) return;
        seen.add(identity);
        result.push(unit);
      });
      return result;
    }

    function 读取召唤类型配置(类型 = '') {
      const 名称 = String(类型 || '魂兽').trim() || '魂兽';
      const 配置表 = {
        分身: { 属性系数: 1, 资源系数: 1, 生命系数: 0.9, 负载系数: 1.6, 默认行动模式: '自主行动' },
        本命召唤兽: { 属性系数: 1.1, 资源系数: 1.05, 生命系数: 1.1, 负载系数: 1.35, 默认行动模式: '自主行动' },
        魂兽: { 属性系数: 0.95, 资源系数: 0.9, 生命系数: 1.15, 负载系数: 1, 默认行动模式: '自主行动' },
        深渊生物: { 属性系数: 1.05, 资源系数: 0.95, 生命系数: 1, 负载系数: 1.4, 默认行动模式: '自主行动' },
        魂师: { 属性系数: 1, 资源系数: 1, 生命系数: 1, 负载系数: 1.8, 默认行动模式: '自主行动' },
        其他召唤生物: { 属性系数: 0.85, 资源系数: 0.8, 生命系数: 0.95, 负载系数: 0.8, 默认行动模式: '协同攻击' },
      };
      return { 名称, ...(配置表[名称] || 配置表.魂兽) };
    }

    function 读取行动模式负载系数(行动模式 = '') {
      const 文本 = String(行动模式 || '').trim();
      if (文本 === '自主行动') return 1.35;
      if (文本 === '护卫') return 1.1;
      return 0.85;
    }

    function 读取召唤生命系数(行动模式 = '') {
      const 文本 = String(行动模式 || '').trim();
      if (文本 === '护卫') return 1.25;
      if (文本 === '协同攻击') return 0.9;
      return 1;
    }

    function 读取召唤宿主阵营(combatData = {}, 宿主 = {}) {
      const 玩家侧 = (combatData?.参战者?.team_player || []).filter(Boolean);
      const 敌方侧 = (combatData?.参战者?.team_enemy || []).filter(Boolean);
      const 名称 = String(宿主?.name || 宿主?.名称 || '').trim();
      if (玩家侧.some(unit => unit && (unit === 宿主 || isCombatUnitIdentityMatch(unit, 名称)))) return '玩家';
      if (敌方侧.some(unit => unit && (unit === 宿主 || isCombatUnitIdentityMatch(unit, 名称)))) return '敌方';
      return '玩家';
    }

    function 确保召唤单位表(combatData = {}) {
      if (!combatData || typeof combatData !== 'object') return {};
      if (!combatData.召唤单位表 || typeof combatData.召唤单位表 !== 'object' || Array.isArray(combatData.召唤单位表)) {
        Object.defineProperty(combatData, '召唤单位表', {
          configurable: true,
          enumerable: false,
          writable: true,
          value: {},
        });
      }
      const 参战者 = combatData.参战者 || {};
      const 宿主列表 = dedupeCombatTargetList([
        ...((参战者.team_player || []).filter(Boolean)),
        ...((参战者.team_enemy || []).filter(Boolean)),
      ]);
      宿主列表.forEach(宿主 => {
        Object.entries(宿主?.状态效果 || {}).forEach(([来源状态键, 状态]) => {
          if (!状态?.召唤物 || 状态.召唤物.已消散 === true) return;
          注册召唤运行态单位(combatData, 宿主, 来源状态键, 状态, { 静默: true });
        });
      });
      return combatData.召唤单位表;
    }

    function 读取召唤单位列表(combatData = {}, 筛选 = {}) {
      const 表 = 确保召唤单位表(combatData);
      return Object.values(表).filter(单位 => {
        if (!单位 || 单位.已消散 === true) return false;
        if (筛选.阵营 && 单位.阵营 !== 筛选.阵营) return false;
        if (筛选.宿主 && !isCombatUnitIdentityMatch({ name: 单位.宿主名 }, 筛选.宿主?.name || 筛选.宿主?.名称 || 筛选.宿主)) return false;
        if (筛选.行动模式 && String(单位.行动模式 || '').trim() !== String(筛选.行动模式 || '').trim()) return false;
        return true;
      });
    }

    function 读取召唤收回锁键(宿主 = {}, 类型 = '', 名称 = '') {
      const 宿主名 = String(宿主?.name || 宿主?.名称 || '宿主').trim() || '宿主';
      return `${宿主名}::${String(类型 || '').trim()}::${String(名称 || '').trim()}`;
    }

    function 确保召唤收回锁(combatData = {}) {
      if (!combatData || typeof combatData !== 'object') return [];
      if (!Array.isArray(combatData.召唤收回锁)) combatData.召唤收回锁 = [];
      return combatData.召唤收回锁;
    }

    function 召唤已被主动收回(combatData = {}, 宿主 = {}, 类型 = '', 名称 = '') {
      return 确保召唤收回锁(combatData).includes(读取召唤收回锁键(宿主, 类型, 名称));
    }

    function 读取召唤编号基名(名称 = '') {
      return String(名称 || '召唤物').trim().replace(/#\d+$/, '') || '召唤物';
    }

    function 读取下一个召唤编号(combatData = {}, 宿主 = {}, 类型 = '', 基名 = '') {
      const 基础名 = 读取召唤编号基名(基名);
      const 宿主名 = String(宿主?.name || 宿主?.名称 || '宿主').trim() || '宿主';
      const 已用编号 = new Set();
      读取召唤单位列表(combatData, { 宿主 }).forEach(单位 => {
        if (String(单位?.类型 || '').trim() !== String(类型 || '').trim()) return;
        const 名称 = String(单位?.name || 单位?.名称 || '').trim();
        const 匹配 = 名称.match(new RegExp(`^${基础名.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}#(\\d+)$`));
        if (匹配) 已用编号.add(Number(匹配[1]));
      });
      确保召唤收回锁(combatData).forEach(锁键 => {
        const 前缀 = `${宿主名}::${String(类型 || '').trim()}::${基础名}#`;
        if (!String(锁键 || '').startsWith(前缀)) return;
        const 编号 = Number(String(锁键).slice(前缀.length));
        if (Number.isFinite(编号) && 编号 > 0) 已用编号.add(编号);
      });
      let 编号 = 1;
      while (已用编号.has(编号)) 编号 += 1;
      return 编号;
    }

    function 写入召唤收回锁(combatData = {}, 召唤单位 = {}) {
      const 锁 = 确保召唤收回锁(combatData);
      const 锁键 = 读取召唤收回锁键(召唤单位.__宿主, 召唤单位.类型, 召唤单位.name || 召唤单位.名称);
      if (锁键 && !锁.includes(锁键)) 锁.push(锁键);
    }

    function 读取战斗阵营单位列表(combatData = {}, 阵营 = '玩家') {
      const 基础列表 = 读取战斗主队单位列表(combatData, 阵营);
      return dedupeCombatTargetList([...基础列表, ...读取召唤单位列表(combatData, { 阵营 })]).filter(isCombatUnitAlive);
    }

    function 读取战斗主队单位列表(combatData = {}, 阵营 = '玩家') {
      const 参战者 = combatData?.参战者 || {};
      const 基础列表 = 阵营 === '敌方'
        ? (参战者.team_enemy || []).filter(Boolean)
        : (参战者.team_player || []).filter(Boolean);
      return dedupeCombatTargetList(基础列表.filter(Boolean));
    }

    function 召唤单位是继承型(召唤记录 = {}) {
      return 读取召唤属性继承比例(召唤记录).some(([, 比例]) => 比例 > 0);
    }

    function 读取召唤属性继承比例(召唤记录 = {}) {
      const 继承 = 召唤记录?.属性继承比例 && typeof 召唤记录.属性继承比例 === 'object' && !Array.isArray(召唤记录.属性继承比例)
        ? 召唤记录.属性继承比例
        : {};
      const 统一比例 = Number(召唤记录?.继承属性比例 || 0);
      const 读取 = 键 => Math.max(0, Math.min(1, Number(继承[键] ?? 统一比例 ?? 0)));
      return [
        ['力量', 读取('力量')],
        ['防御', 读取('防御')],
        ['敏捷', 读取('敏捷')],
        ['体力上限', 读取('体力上限')],
        ['魂力上限', 读取('魂力上限')],
        ['精神力上限', 读取('精神力上限')],
      ];
    }

    function 读取召唤属性继承对象(召唤记录 = {}, 默认比例 = 0) {
      const 表 = Object.fromEntries(读取召唤属性继承比例(召唤记录));
      return {
        力量: Math.max(0, Math.min(1, Number(表.力量 || 默认比例))),
        防御: Math.max(0, Math.min(1, Number(表.防御 || 默认比例))),
        敏捷: Math.max(0, Math.min(1, Number(表.敏捷 || 默认比例))),
        体力上限: Math.max(0, Math.min(1, Number(表.体力上限 || 默认比例))),
        魂力上限: Math.max(0, Math.min(1, Number(表.魂力上限 || 默认比例))),
        精神力上限: Math.max(0, Math.min(1, Number(表.精神力上限 || 默认比例))),
      };
    }

    function 召唤单位是分身(召唤单位 = {}) {
      return String(
        召唤单位?.类型 ||
        召唤单位?.召唤单位类型 ||
        召唤单位?.__来源状态?.召唤物?.召唤单位类型 ||
        '',
      ).trim() === '分身';
    }

    function 读取召唤稳定状态(召唤单位 = {}) {
      if (!召唤单位 || 召唤单位.已消散 === true || !isCombatUnitAlive(召唤单位)) return '消散';
      if (召唤单位.__禁用召唤技能 === true || Number(召唤单位.__精神维持率 || 1) < 0.25) return '受限';
      if (Number(召唤单位.__精神压缩 || 1) < 0.98) return '超载';
      if (Number(召唤单位.__精神维持率 || 1) < 0.5) return '不稳';
      return '稳定';
    }

    function 读取召唤类型倾向(类型 = '') {
      const 文本 = String(类型 || '').trim();
      const 倾向表 = {
        分身: { 继承: 1.35, 收割: 1.1, 高威胁: 1.0, 技能: 1.15 },
        本命召唤兽: { 护主: 1.35, 高威胁: 1.12, 收割: 1.0, 技能: 1.05 },
        魂兽: { 收割: 1.22, 高威胁: 1.05, 技能: 0.95 },
        深渊生物: { 高威胁: 1.28, 收割: 1.05, 技能: 1.08 },
        魂师: { 技能: 1.25, 高威胁: 1.12, 收割: 1.04 },
        其他召唤生物: { 收割: 1.0, 高威胁: 0.95, 技能: 0.9 },
      };
      return 倾向表[文本] || 倾向表.其他召唤生物;
    }

    function 召唤技能允许继承(skill = {}) {
      if (!skill || typeof skill !== 'object') return false;
      const 名称文本 = String(skill.name || skill.魂技名 || skill.技能名称 || '').trim();
      const 来源文本 = getBattleSkillSourceCategory(skill);
      const 技能文本 = `${名称文本} ${来源文本} ${String(skill.技能分类 || '')} ${String(skill.描述 || '')}`;
      if (/武魂融合|武魂真身|炸环|穿戴|装备|召唤|领域维持/.test(技能文本)) return false;
      const 效果列表 = getSkillEffects(skill);
      if (!效果列表.some(effect => String(effect?.原型 || '').trim() === '伤害结算')) return false;
      if (效果列表.some(effect => String(effect?.原型 || '').trim() === '召唤生成')) return false;
      const 消耗文本 = String(skill.消耗 || skill.cost || '').trim();
      if (/魂环|血脉|机甲|斗铠|融合|真身/.test(消耗文本)) return false;
      return true;
    }

    function 构建召唤普通攻击技能(召唤单位 = {}) {
      return normalizeSkillData({
        name: '普通攻击',
        技能分类: '输出',
        消耗: '无',
        前摇: 10,
        _效果数组: [{
          原型: '伤害结算',
          目标: '敌方单体',
          伤害类型: '近身攻击',
          威力倍率: Math.max(60, Math.round(Math.max(Number(召唤单位?.str || 0), Number(召唤单位?.sp_max || 0)) * 0.08)),
        }],
      }, '普通攻击');
    }

    function 构建继承召唤技能列表(宿主 = {}) {
      const 技能列表 = collectCombatSkills(宿主, [])
        .filter(召唤技能允许继承)
        .slice(0, 4)
        .map(skill => normalizeSkillData(deepClonePlain(skill), skill?.name || skill?.魂技名 || '继承技能'));
      return 技能列表;
    }

    function 构建临时召唤技能列表(召唤单位 = {}) {
      const 类型 = String(召唤单位?.类型 || '魂兽').trim();
      const 名称表 = {
        本命召唤兽: ['本命冲击', '本命撕咬', '本命压迫', '本命追击'],
        魂兽: ['兽性冲击', '撕咬打击', '重踏压制', '迅猛追击'],
        深渊生物: ['浊能冲击', '裂隙打击', '深渊压迫', '暗流追击'],
        魂师: ['魂力冲击', '魂力压制', '身法突击', '战术追击'],
        其他召唤生物: ['召唤冲击', '召唤打击', '召唤压迫', '召唤追击'],
      };
      return (名称表[类型] || 名称表.其他召唤生物).slice(0, 4).map((名称, index) => normalizeSkillData({
        name: 名称,
        技能分类: '输出',
        消耗: '无',
        前摇: Math.max(8, 14 - index),
        _效果数组: [{
          原型: '伤害结算',
          目标: '敌方单体',
          伤害类型: index % 2 === 0 ? '近身攻击' : '远程攻击',
          威力倍率: 90 + index * 15,
        }],
      }, 名称));
    }

    function 构建召唤技能列表(召唤单位 = {}, 宿主 = {}) {
      const 技能列表 = 召唤单位是继承型(召唤单位)
        ? 构建继承召唤技能列表(宿主)
        : 构建临时召唤技能列表(召唤单位);
      if (!技能列表.some(skill => String(skill?.name || skill?.魂技名 || '').trim() === '普通攻击')) {
        技能列表.unshift(构建召唤普通攻击技能(召唤单位));
      }
      return 技能列表.slice(0, 5);
    }

    function 注册召唤运行态单位(combatData = {}, 宿主 = {}, 来源状态键 = '', 来源状态 = {}, 选项 = {}) {
      if (!combatData || !宿主 || !来源状态?.召唤物) return null;
      if (!combatData.召唤单位表 || typeof combatData.召唤单位表 !== 'object' || Array.isArray(combatData.召唤单位表)) {
        Object.defineProperty(combatData, '召唤单位表', {
          configurable: true,
          enumerable: false,
          writable: true,
          value: {},
        });
      }
      const 表 = combatData.召唤单位表;
      const 召唤记录 = 来源状态.召唤物;
      const 召唤物名称 = String(召唤记录.召唤物名称 || 召唤记录.name || '召唤物').trim() || '召唤物';
      const 宿主名 = String(宿主.name || 宿主.名称 || '宿主').trim() || '宿主';
      const 召唤键 = String(召唤记录.召唤键 || `${宿主名}::${来源状态键}::${召唤物名称}`).trim();
      const 类型配置 = 读取召唤类型配置(召唤记录.召唤单位类型 || 召唤记录.类型);
      const 数量 = 类型配置.名称 === '其他召唤生物' ? 1 : Math.max(1, Number(召唤记录.召唤数量 || 召唤记录.数量 || 来源状态.层数 || 1));
      const 行动模式 = String(召唤记录.行动模式 || 类型配置.默认行动模式 || '协同攻击').trim();
      const 默认继承比例 = 类型配置.名称 === '分身' || 类型配置.名称 === '本命召唤兽' ? 0.45 : 0;
      const 属性继承比例 = 读取召唤属性继承对象(召唤记录, 默认继承比例);
      const 继承比例 = Math.max(
        属性继承比例.力量,
        属性继承比例.防御,
        属性继承比例.敏捷,
        属性继承比例.体力上限,
        属性继承比例.魂力上限,
        属性继承比例.精神力上限,
      );
      const 强度 = 继承比例 > 0 ? 继承比例 : Math.max(0.01, Number(召唤记录.强度 || 1));
      const 宿主最终 = 宿主.final || buildCombatFinalStats(宿主);
      const 数量系数 = 1 + Math.min(数量 - 1, 4) * 0.35;
      const 属性系数 = 继承比例 > 0 ? 1 : Math.max(0.12, Math.min(1.4, 0.35 + 强度 * 0.18));
      const 资源系数 = 继承比例 > 0 ? 1 : 属性系数;
      const 生命上限 = Math.max(1, Math.round(getCombatHpMaxValue(宿主) * (继承比例 > 0 ? 属性继承比例.体力上限 : 属性系数) * 数量系数 * 类型配置.生命系数 * 读取召唤生命系数(行动模式)));
      const 精神负载基准 = 继承比例 > 0 ? 继承比例 * 100 : 强度 * 35;
      const 精神负载 = Math.max(1, Math.round(数量 * 精神负载基准 * 类型配置.负载系数 * 读取行动模式负载系数(行动模式)));
      const 已有单位 = 表[召唤键];
      const 当前生命 = Math.max(0, Math.min(生命上限, Number(已有单位?.HP ?? 召唤记录.生命 ?? 召唤记录.HP ?? 生命上限)));
      const 单位 = 已有单位 || {};
      Object.assign(单位, {
        召唤键,
        name: 召唤物名称,
        名称: 召唤物名称,
        基础名称: 读取召唤编号基名(召唤记录.基础名称 || 召唤物名称),
        类型: 类型配置.名称,
        阵营: 读取召唤宿主阵营(combatData, 宿主),
        宿主名,
        来源状态键,
        行动模式,
        HP: 当前生命,
        HP上限: 生命上限,
        体力: Math.max(1, Math.round(生命上限 * 0.45)),
        体力上限: Math.max(1, Math.round(生命上限 * 0.45)),
        魂力: Math.max(0, Math.round(Number(宿主最终.sp_max || 宿主.sp_max || 0) * (继承比例 > 0 ? 属性继承比例.魂力上限 * 类型配置.资源系数 : 资源系数))),
        魂力上限: Math.max(1, Math.round(Number(宿主最终.sp_max || 宿主.sp_max || 1) * (继承比例 > 0 ? 属性继承比例.魂力上限 * 类型配置.资源系数 : 资源系数))),
        精神力: Math.max(0, Math.round(Number(宿主最终.men_max || 宿主.men_max || 0) * (继承比例 > 0 ? 属性继承比例.精神力上限 * 类型配置.资源系数 : 资源系数))),
        精神力上限: Math.max(1, Math.round(Number(宿主最终.men_max || 宿主.men_max || 1) * (继承比例 > 0 ? 属性继承比例.精神力上限 * 类型配置.资源系数 : 资源系数))),
        str: Math.max(1, Math.round(Number(宿主最终.str || 宿主.str || 1) * (继承比例 > 0 ? 属性继承比例.力量 * 类型配置.属性系数 : 属性系数))),
        def: Math.max(1, Math.round(Number(宿主最终.def || 宿主.def || 1) * (继承比例 > 0 ? 属性继承比例.防御 * 类型配置.属性系数 : 属性系数))),
        agi: Math.max(1, Math.round(Number(宿主最终.agi || 宿主.agi || 1) * (继承比例 > 0 ? 属性继承比例.敏捷 * 类型配置.属性系数 : 属性系数))),
        状态效果: 单位.状态效果 && typeof 单位.状态效果 === 'object' ? 单位.状态效果 : {},
        精神负载,
        生成回合: Math.max(0, Number(已有单位?.生成回合 ?? combatData?.回合 ?? 0)),
        可自主行动: 行动模式 === '自主行动',
        已消散: false,
        继承属性比例: 继承比例,
        属性继承比例,
        强度,
        死亡反噬: 继承比例 > 0,
        __宿主: 宿主,
        __来源状态: 来源状态,
      });
      单位.hp = 单位.HP;
      单位.hp_max = 单位.HP上限;
      单位.sp = 单位.魂力;
      单位.sp_max = 单位.魂力上限;
      单位.men = 单位.精神力;
      单位.men_max = 单位.精神力上限;
      单位.final = buildCombatFinalStats(单位);
      单位.技能列表 = 构建召唤技能列表(单位, 宿主);
      表[召唤键] = 单位;
      召唤记录.召唤键 = 召唤键;
      同步召唤单位镜像(单位);
      if (!选项.静默) 单位.__刚生成 = true;
      return 单位;
    }

    function 同步召唤单位镜像(召唤单位 = {}) {
      if (!召唤单位?.__来源状态?.召唤物) return;
      const 镜像 = 召唤单位.__来源状态.召唤物;
      镜像.召唤键 = 召唤单位.召唤键;
      镜像.召唤单位类型 = 召唤单位.类型;
      镜像.召唤物名称 = 召唤单位.name || 召唤单位.名称 || '召唤物';
      镜像.行动模式 = 召唤单位.行动模式;
      镜像.生命 = getCombatHpValue(召唤单位);
      镜像.生命上限 = getCombatHpMaxValue(召唤单位);
      镜像.精神负载 = Math.max(0, Number(召唤单位.精神负载 || 0));
      镜像.生成回合 = Math.max(0, Number(召唤单位.生成回合 || 0));
      镜像.已消散 = 召唤单位.已消散 === true;
    }

    function 召唤单位本回合刚生成(召唤单位 = {}, combatData = {}) {
      const 当前回合 = Math.max(0, Number(combatData?.回合 || 0));
      const 生成回合 = Math.max(0, Number(召唤单位?.生成回合 || 0));
      return 当前回合 > 0 && 生成回合 > 0 && 当前回合 <= 生成回合;
    }

    function 写入召唤精神控制事件(combatData = {}, 宿主 = {}, 召唤单位 = {}, 结果 = '', 附加 = {}) {
      const 召唤名 = String(召唤单位?.name || 召唤单位?.名称 || '召唤物').trim();
      if (!combatData || !召唤名) return null;
      const 宿主名 = String(宿主?.name || 宿主?.名称 || 召唤单位?.__宿主?.name || 召唤单位?.__宿主?.名称 || '').trim();
      const failReason = String(附加.failReason || 附加.failureReason || '召唤控制链受限').trim();
      return 写入战斗事件账本(combatData, {
        eventKind: 'blocked_action',
        round: Number(combatData?.回合 || 0),
        actorName: 召唤名,
        targetName: 宿主名,
        actionName: '召唤控制',
        actionType: 'summon_control',
        result: String(结果 || 'limited').trim(),
        failReason,
        targetPoolSide: 'ally',
        meta: {
          source: 'summon',
          primaryOutcome: 'blocked',
          reasonCode: 'SUMMON_CONTROL_OVERLOAD',
          reasonText: String(附加.reasonText || '宿主精神负载不足以稳定控制召唤物').trim(),
          summonName: 召唤名,
          summonType: String(召唤单位?.类型 || '').trim(),
          summonMode: String(召唤单位?.行动模式 || '').trim(),
          summonHostName: 宿主名,
          summonKey: String(召唤单位?.召唤键 || '').trim(),
          mentalLoad: Math.max(0, Number(召唤单位?.精神负载 || 0)),
          totalMentalLoad: Math.max(0, Number(附加.totalMentalLoad || 0)),
          mentalLimit: Math.max(0, Number(附加.mentalLimit || 0)),
          maintainRatio: Math.max(0, Number(附加.maintainRatio || 0)),
          compression: Math.max(0, Number(附加.compression || 0)),
          restriction: String(附加.restriction || 结果 || '').trim(),
        },
      });
    }

    function 移除召唤运行态单位(combatData = {}, 召唤单位 = {}, 原因 = '消散') {
      if (!召唤单位 || 召唤单位.已消散 === true) return '';
      召唤单位.已消散 = true;
      同步召唤单位镜像(召唤单位);
      if (combatData?.召唤单位表 && 召唤单位.召唤键) delete combatData.召唤单位表[召唤单位.召唤键];
      const 宿主 = 召唤单位.__宿主;
      if (宿主?.状态效果 && 召唤单位.来源状态键 && 宿主.状态效果[召唤单位.来源状态键]) delete 宿主.状态效果[召唤单位.来源状态键];
      return `[召唤消散] ${召唤单位.name || '召唤物'}因${原因}离场。`;
    }

    function 主动收回召唤单位(combatData = {}, 宿主 = {}, 指定名称 = '') {
      const 指定文本 = String(指定名称 || '').trim();
      const 候选列表 = 读取召唤单位列表(combatData, { 宿主 }).filter(单位 =>
        !召唤单位是分身(单位) &&
        (
          !指定文本 ||
          String(单位.召唤键 || '').trim() === 指定文本 ||
          String(单位.name || 单位.名称 || '').includes(指定文本)
        ),
      );
      if (!候选列表.length) return '[收回召唤] 当前没有可收回的召唤物。';
      const 日志 = [];
      候选列表.forEach(单位 => {
        写入召唤收回锁(combatData, 单位);
        const 名称 = 单位.name || 单位.名称 || '召唤物';
        移除召唤运行态单位(combatData, 单位, '主动收回');
        日志.push(`${名称}`);
      });
      return `[收回召唤] 已收回${日志.join('、')}。`;
    }

    function 执行召唤死亡反噬(召唤单位 = {}) {
      if (!召唤单位?.死亡反噬 || !召唤单位.__宿主) return '';
      const 宿主 = 召唤单位.__宿主;
      const 生命反噬 = Math.max(1, Math.floor(getCombatHpMaxValue(宿主) * Math.min(0.18, Math.max(0.04, Number(召唤单位.继承属性比例 || 0) * 0.22))));
      设置战斗血量值(宿主, getCombatHpValue(宿主) - 生命反噬);
      const 精神上限 = Math.max(1, Number(宿主.men_max || 宿主?.属性?.精神力上限 || 1));
      const 当前精神 = Math.max(0, Number(宿主.men ?? 宿主?.属性?.精神力 ?? 精神上限));
      const 精神反噬 = Math.max(1, Math.floor(精神上限 * Math.min(0.16, Math.max(0.03, Number(召唤单位.继承属性比例 || 0) * 0.18))));
      宿主.men = Math.max(0, 当前精神 - 精神反噬);
      if (宿主.属性 && typeof 宿主.属性 === 'object') 宿主.属性.精神力 = 宿主.men;
      return `[死亡反噬] ${召唤单位.name || '召唤物'}崩解，${宿主.name || '宿主'}承受 ${生命反噬} 点生命反噬与 ${精神反噬} 点精神震荡。`;
    }

    function 处理召唤单位死亡(combatData = {}, 召唤单位 = {}) {
      if (!召唤单位?.召唤键 || getCombatHpValue(召唤单位) > 0 || 召唤单位.已消散 === true) return '';
      const 反噬日志 = 执行召唤死亡反噬(召唤单位);
      const 消散日志 = 移除召唤运行态单位(combatData, 召唤单位, '死亡');
      return [反噬日志, 消散日志].filter(Boolean).join(' ');
    }

    function 移除宿主状态召唤单位(combatData = {}, 宿主 = {}, 来源状态键 = '', 原因 = '来源状态结束') {
      const 单位 = 读取召唤单位列表(combatData, { 宿主 }).find(item => item.来源状态键 === 来源状态键);
      return 单位 ? 移除召唤运行态单位(combatData, 单位, 原因) : '';
    }

    function 刷新召唤精神负载(combatData = {}, 宿主 = {}) {
      const 单位列表 = 读取召唤单位列表(combatData, { 宿主 });
      if (!单位列表.length) return '';
      if (!isCombatUnitAbleToFight(宿主)) {
        return 单位列表.map(单位 => 移除召唤运行态单位(combatData, 单位, '宿主失去战斗能力')).filter(Boolean).join(' ');
      }
      const 精神上限 = Math.max(1, Number(宿主.men_max || 宿主?.属性?.精神力上限 || 1));
      const 当前精神 = Math.max(0, Number(宿主.men ?? 宿主?.属性?.精神力 ?? 精神上限));
      const 总负载 = 单位列表.reduce((sum, 单位) => sum + Math.max(0, Number(单位.精神负载 || 0)), 0);
      const 操控上限 = Math.max(20, 精神上限 * 0.75);
      const 日志 = [];
      if (总负载 > 操控上限) {
        const 压缩 = Math.max(0.35, 操控上限 / Math.max(1, 总负载));
        单位列表.forEach(单位 => {
          单位.final = buildCombatFinalStats(单位);
          ['str', 'def', 'agi', 'sp_max', 'men_max'].forEach(key => {
            单位.final[key] = Math.max(1, Math.round(Number(单位.final[key] || 单位[key] || 1) * 压缩));
          });
          单位.__精神压缩 = 压缩;
          写入召唤精神控制事件(combatData, 宿主, 单位, 'overload_compressed', {
            failReason: '宿主精神负载过高，召唤物属性被压缩',
            reasonText: '宿主精神负载过高，召唤物属性被压缩',
            restriction: 'compressed',
            totalMentalLoad: 总负载,
            mentalLimit: 操控上限,
            maintainRatio: 当前精神 / 精神上限,
            compression: 压缩,
          });
        });
        日志.push(`[召唤超载] ${宿主.name || '宿主'}召唤负载过高，召唤物属性压缩至${Math.round(压缩 * 100)}%。`);
      }
      const 维持率 = 当前精神 / 精神上限;
      单位列表.forEach(单位 => {
        单位.__精神维持率 = 维持率;
        if (维持率 <= 0) {
          写入召唤精神控制事件(combatData, 宿主, 单位, 'dissipated', {
            failReason: '宿主精神力枯竭，召唤物被强制消散',
            reasonText: '宿主精神力枯竭，召唤物被强制消散',
            restriction: 'dissipated',
            totalMentalLoad: 总负载,
            mentalLimit: 操控上限,
            maintainRatio: 维持率,
          });
          日志.push(移除召唤运行态单位(combatData, 单位, '精神力枯竭'));
        } else if (单位.类型 === '深渊生物' && 维持率 < 0.25) {
          写入召唤精神控制事件(combatData, 宿主, 单位, 'recalled', {
            failReason: '宿主精神维持不足，召唤物被强制离场',
            reasonText: '宿主精神维持不足，召唤物被强制离场',
            restriction: 'recalled',
            totalMentalLoad: 总负载,
            mentalLimit: 操控上限,
            maintainRatio: 维持率,
          });
          日志.push(移除召唤运行态单位(combatData, 单位, '精神维持不足'));
        } else if (维持率 < 0.25) {
          单位.__禁用召唤技能 = true;
          写入召唤精神控制事件(combatData, 宿主, 单位, 'skill_limited', {
            failReason: '宿主精神不足，召唤物技能被禁用',
            reasonText: '宿主精神不足，召唤物只能进行基础行动',
            restriction: 'skill_disabled',
            totalMentalLoad: 总负载,
            mentalLimit: 操控上限,
            maintainRatio: 维持率,
          });
          日志.push(`[召唤受限] ${单位.name || '召唤物'}受宿主精神不足影响，只能进行基础行动。`);
        } else {
          单位.__禁用召唤技能 = false;
        }
      });
      return 日志.filter(Boolean).join(' ');
    }

    function 执行召唤回合开始(combatData = {}) {
      确保召唤单位表(combatData);
      读取召唤单位列表(combatData).forEach(单位 => {
        delete 单位.__本回合已召唤行动;
      });
      const 宿主列表 = dedupeCombatTargetList(Object.values(combatData?.召唤单位表 || {}).map(单位 => 单位.__宿主));
      return 宿主列表.map(宿主 => 刷新召唤精神负载(combatData, 宿主)).filter(Boolean).join(' ');
    }

    function 评分召唤攻击目标(召唤单位 = {}, 目标 = {}) {
      if (!目标 || !isCombatUnitAlive(目标)) return -1;
      const 倾向 = 读取召唤类型倾向(召唤单位.类型);
      const 目标终值 = 目标.final || buildCombatFinalStats(目标);
      let 分数 = 100;
      分数 += (1 - getCombatHpRatio(目标)) * 55 * Number(倾向.收割 || 1);
      分数 += Math.max(0, Number(目标终值.sp_max || 目标.sp_max || 0)) * 0.015 * Number(倾向.高威胁 || 1);
      分数 += Math.max(0, Number(目标终值.str || 目标.str || 0)) * 0.012 * Number(倾向.高威胁 || 1);
      if (目标.召唤键) 分数 += 8;
      if (目标.行动模式 === '护卫') 分数 -= 10;
      return 分数;
    }

    function 选择召唤攻击目标(召唤单位 = {}, combatData = {}) {
      const 敌阵营 = 召唤单位.阵营 === '敌方' ? '玩家' : '敌方';
      return 读取战斗阵营单位列表(combatData, 敌阵营)
        .filter(isCombatUnitAlive)
        .sort((左, 右) => 评分召唤攻击目标(召唤单位, 右) - 评分召唤攻击目标(召唤单位, 左))[0] || null;
    }

    function 评分召唤攻击技能(召唤单位 = {}, 技能 = {}, 目标 = {}) {
      const 伤害效果 = getSkillEffects(技能).find(effect => String(effect?.原型 || '').trim() === '伤害结算');
      if (!伤害效果) return -1;
      const 倾向 = 读取召唤类型倾向(召唤单位.类型);
      const 威力 = Math.max(1, Number(伤害效果.威力倍率 || 90));
      const 伤害类型 = 规范化战斗伤害类型(伤害效果.伤害类型, 技能);
      const 目标防御 = Math.max(1, Number((目标.final || buildCombatFinalStats(目标)).def || 目标.def || 1));
      let 分数 = 威力 * Number(倾向.技能 || 1);
      if ((战斗伤害是远程攻击(伤害类型) || 战斗伤害是精神攻击(伤害类型)) && 目标防御 > Number(召唤单位.final?.str || 召唤单位.str || 1)) 分数 += 18;
      if (getCombatHpRatio(目标) < 0.35) 分数 += 12 * Number(倾向.收割 || 1);
      if (召唤单位是继承型(召唤单位) && String(技能.name || 技能.魂技名 || '') !== '普通攻击') 分数 *= Number(倾向.继承 || 1);
      return 分数;
    }

    function 选择召唤攻击技能(召唤单位 = {}, 目标 = {}) {
      const 普攻 = (召唤单位.技能列表 || []).find(skill => String(skill?.name || skill?.魂技名 || '').trim() === '普通攻击') || 构建召唤普通攻击技能(召唤单位);
      if (召唤单位.__禁用召唤技能 === true || Number(召唤单位.__精神维持率 || 1) < 0.25) return 普攻;
      return [...(召唤单位.技能列表 || [])]
        .filter(skill => getSkillEffects(skill).some(effect => String(effect?.原型 || '').trim() === '伤害结算'))
        .sort((左, 右) => 评分召唤攻击技能(召唤单位, 右, 目标) - 评分召唤攻击技能(召唤单位, 左, 目标))[0] || 普攻;
    }

    function 记录召唤物规划审计(combatData = {}, 召唤单位 = {}, 目标 = {}, 技能 = {}, 来源 = '召唤物基础动作候选') {
      const 规划上下文 = 构建规划上下文(召唤单位, 目标, combatData, { combatData });
      const 优先项 = 目标 ? 读取规划目标优先项(规划上下文, 目标, '敌方') : { 理由: [], 优先级: 0 };
      记录行动闭环审计(combatData, '主动规划', {
        行动者: 召唤单位?.name || 召唤单位?.名称 || '',
        目标: 目标?.name || 目标?.名称 || '',
        技能: 技能?.name || 技能?.魂技名 || '召唤基础动作',
        战略意图: 规划上下文?.战略意图?.主意图 || '协同攻击',
        目标理由: 优先项.理由 || [],
        候选来源: 来源,
        原始净收益: 评分召唤攻击目标(召唤单位, 目标),
        战术修正: 评分召唤攻击技能(召唤单位, 技能, 目标),
        干扰强度: 读取行动判断干扰强度(召唤单位),
        最终权重: Math.max(1, 评分召唤攻击目标(召唤单位, 目标) + 评分召唤攻击技能(召唤单位, 技能, 目标)),
        选择原因: '召唤物按基础动作进入规划闭环',
        旁路: false,
      });
    }

    function 记账召唤生成事件(combatData = {}, 宿主 = {}, 动作名 = '', 召唤单位 = {}, 选项 = {}) {
      if (!combatData || !召唤单位) return null;
      const summonName = String(召唤单位?.name || 召唤单位?.名称 || 选项?.召唤物名称 || '').trim();
      if (!summonName) return null;
      const summonType = String(召唤单位?.类型 || 召唤单位?.召唤单位类型 || 选项?.召唤单位类型 || '').trim() || '魂兽';
      const summonMode = String(召唤单位?.行动模式 || 选项?.行动模式 || 读取召唤类型配置(summonType).默认行动模式 || '协同攻击').trim();
      const hostName = String(宿主?.name || 宿主?.名称 || 召唤单位?.宿主名 || '').trim();
      const actionName = normalizeBattleActionDisplayName(动作名 || 选项?.动作名 || '召唤');
      const round = Number(combatData?.回合 || 0);
      let sourceAction = [...确保战斗事件账本(combatData)].reverse().find(event =>
        String(event?.eventKind || '').trim() === 'action_start' &&
        Number(event?.round || 0) === round &&
        isSameBattleReportName(event?.actorName || '', hostName) &&
        normalizeBattleActionDisplayName(event?.actionName || event?.finalActionName || '') === actionName
      ) || null;
      if (!sourceAction && hostName && actionName) {
        sourceAction = 写入战斗事件账本(combatData, {
          eventKind: 'action_start',
          round,
          actorName: hostName,
          targetName: summonName,
          actionName,
          actionType: '召唤生成',
          targetScope: 'self',
          result: 'declared',
          actionStatus: 'DECLARED',
          meta: {
            source: 'summon_create_action_root',
            summonName,
            summonType,
            summonMode,
          },
        });
      }
      const sourceNodeId = String(选项?.sourceNodeId || sourceAction?.chainNodeId || '').trim();
      return 写入战斗事件账本(combatData, {
        eventKind: 'summon_create',
        round,
        actorName: hostName,
        actionName,
        actionType: '召唤生成',
        sourceActionName: actionName,
        sourceActionId: String(选项?.sourceActionId || sourceAction?.actionId || '').trim(),
        parentNodeId: String(选项?.parentNodeId || sourceNodeId).trim(),
        sourceNodeId,
        result: 'created',
        meta: {
          summonName,
          summonType,
          summonMode,
          mentalLoad: Math.max(0, Math.round(Number(召唤单位?.精神负载 || 0))),
          createdRound: Math.max(0, Number(召唤单位?.生成回合 || combatData?.回合 || 0)),
          hostName: 宿主?.name || 宿主?.名称 || 召唤单位?.宿主名 || '',
          hostSide: 召唤单位?.阵营 || 读取召唤宿主阵营(combatData, 宿主),
          startsNextRound: true,
        },
      });
    }

    function 查找最近宿主有效伤害事件(combatData = {}, 宿主 = {}, 目标 = {}) {
      const ledger = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger : [];
      const round = Number(combatData?.回合 || 0);
      const hostName = String(宿主?.name || 宿主?.名称 || '').trim();
      const targetName = String(目标?.name || 目标?.名称 || '').trim();
      for (let index = ledger.length - 1; index >= 0; index -= 1) {
        const event = ledger[index];
        if (!event || String(event.eventKind || '').trim() !== 'hit_result') continue;
        if (round > 0 && Number(event.round || 0) !== round) continue;
        if (hostName && !isSameBattleReportName(event.actorName || '', hostName)) continue;
        if (targetName && !isSameBattleReportName(event.targetName || '', targetName)) continue;
        if (Math.max(0, 读取事件账本数值(event, 'damage')) > 0) return event;
      }
      return null;
    }

    function 结算运行态召唤单位简易攻击(召唤单位 = {}, 目标 = {}, combatData = {}, 原因 = '召唤行动', options = {}) {
      if (!召唤单位 || !目标 || !isCombatUnitAlive(召唤单位) || !isCombatUnitAlive(目标)) return '';
      const 技能 = 选择召唤攻击技能(召唤单位, 目标);
      const 技能名 = 技能?.name || 技能?.魂技名 || '普通攻击';
      const 动作类型 = 原因 === '召唤协同追击' ? 'summon_assist' : '召唤自主行动';
      const 召唤动作事件 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: Number(combatData?.回合 || 0),
        actorName: 召唤单位?.name || 召唤单位?.名称 || '',
        targetName: 目标?.name || 目标?.名称 || '',
        actionName: 技能名,
        actionType: 动作类型,
        result: 'declared',
        meta: {
          source: 'summon',
          summonHostName: 召唤单位?.宿主名 || 召唤单位?.__宿主?.name || 召唤单位?.__宿主?.名称 || '',
          summonType: 召唤单位?.类型 || '',
          summonMode: 召唤单位?.行动模式 || '',
        },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'pass',
        round: Number(combatData?.回合 || 0),
        actorName: 目标?.name || 目标?.名称 || '',
        targetName: 召唤单位?.name || 召唤单位?.名称 || '',
        actionName: '临场应对',
        actionType: 'reaction',
        sourceActionName: 技能名,
        sourceActionId: 召唤动作事件?.actionId || '',
        parentNodeId: 召唤动作事件?.chainNodeId || '',
        sourceNodeId: 召唤动作事件?.chainNodeId || '',
        result: 'reaction_failed',
        meta: {
          source: 原因 === '召唤协同追击' ? 'summon_assist_target_reaction' : 'summon_simple_attack_target_reaction',
          reasonCode: 'REACTION_FAILED',
          reasonText: '未形成有效应招动作',
          summonName: 召唤单位?.name || 召唤单位?.名称 || '',
          summonHostName: 召唤单位?.宿主名 || 召唤单位?.__宿主?.name || 召唤单位?.__宿主?.名称 || '',
        },
      });
      const 伤害效果 = getSkillEffects(技能).find(effect => String(effect?.原型 || '').trim() === '伤害结算') || {};
      const 威力 = Math.max(60, Number(伤害效果.威力倍率 || 90));
      const 攻击属性 = Math.max(Number(召唤单位.final?.str || 召唤单位.str || 1), Number(召唤单位.final?.sp_max || 召唤单位.sp_max || 1));
      const 防御属性 = Math.max(1, Number((目标.final || buildCombatFinalStats(目标)).def || 目标.def || 1));
      const 伤害 = Math.max(1, Math.floor((攻击属性 * 威力) / Math.max(80, 防御属性 + 100)));
      const 账本起点 = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger.length : 0;
      const 伤害包 = applyResolvedDamagePackage(召唤单位, { action_type: 动作类型, type: 动作类型, skill: 技能, source: 'summon' }, {
        targetResults: [{ target: 目标, targetName: 目标.name || 目标.名称 || '目标', damage: 伤害, kind: 'primary' }],
        dmg: 伤害,
        totalProjectedDamage: 伤害,
      }, { primaryTarget: 目标, combatData });
      const 召唤伤害事件 = (Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger : [])
        .slice(Math.max(0, 账本起点))
        .reverse()
        .find(event =>
          event &&
          String(event.eventKind || '').trim() === 'hit_result' &&
          isSameBattleReportName(event.actorName || '', 召唤单位?.name || 召唤单位?.名称 || '') &&
          isSameBattleReportName(event.targetName || '', 目标?.name || 目标?.名称 || '') &&
          normalizeBattleActionDisplayName(event.actionName || '') === normalizeBattleActionDisplayName(技能名)
        ) || null;
      if (原因 === '召唤协同追击') {
        const 宿主伤害事件 = options?.hostDamageEvent || null;
        写入战斗事件账本(combatData, {
          eventKind: 'summon_assist',
          round: Number(combatData?.回合 || 0),
          actorName: 召唤单位?.name || 召唤单位?.名称 || '',
          targetName: 目标?.name || 目标?.名称 || '',
          actionName: 技能名,
          actionType: 'summon_assist',
          result: Math.max(0, 读取事件账本数值(召唤伤害事件 || {}, 'damage') || 伤害) > 0 ? 'success' : 'no_effect',
          damage: Math.max(0, 读取事件账本数值(召唤伤害事件 || {}, 'damage') || 伤害),
          sourceNodeId: String(召唤伤害事件?.chainNodeId || '').trim(),
          parentNodeId: String(召唤伤害事件?.chainNodeId || '').trim(),
          meta: {
            source: 'summon',
            damage: Math.max(0, 读取事件账本数值(召唤伤害事件 || {}, 'damage') || 伤害),
            sourceEventId: String(召唤伤害事件?.eventId || '').trim(),
            summonName: 召唤单位?.name || 召唤单位?.名称 || '',
            summonHostName: 召唤单位?.宿主名 || 召唤单位?.__宿主?.name || 召唤单位?.__宿主?.名称 || '',
            summonType: 召唤单位?.类型 || '',
            summonMode: 召唤单位?.行动模式 || '',
            hostDamageEventId: String(宿主伤害事件?.eventId || '').trim(),
            hostDamageNodeId: String(宿主伤害事件?.chainNodeId || '').trim(),
            hostName: String(options?.hostName || 宿主伤害事件?.actorName || '').trim(),
            hostActionName: normalizeBattleActionDisplayName(宿主伤害事件?.actionName || ''),
            hostDamage: Math.max(0, 读取事件账本数值(宿主伤害事件 || {}, 'damage')),
            summonHitEventId: String(召唤伤害事件?.eventId || '').trim(),
            summonHitNodeId: String(召唤伤害事件?.chainNodeId || '').trim(),
          },
        });
      }
      const 死亡日志 = 处理召唤单位死亡(combatData, 目标);
      return `[${原因}] ${召唤单位.name || '召唤物'}以[${技能名}]命中${目标.name || '目标'}。${伤害包.log ? ` ${伤害包.log}` : ''}${死亡日志 ? ` ${死亡日志}` : ''}`;
    }

    function 执行自主召唤行动(combatData = {}) {
      确保召唤单位表(combatData);
      const 日志 = [];
      读取召唤单位列表(combatData, { 行动模式: '自主行动' }).forEach(单位 => {
        if (单位.__本回合已召唤行动 || 召唤单位本回合刚生成(单位, combatData) || !isCombatUnitAlive(单位)) return;
        const 目标 = 选择召唤攻击目标(单位, combatData);
        if (!目标) return;
        单位.__本回合已召唤行动 = true;
        const 技能 = 选择召唤攻击技能(单位, 目标);
        记录召唤物规划审计(combatData, 单位, 目标, 技能, '回合尾召唤物自主行动');
        日志.push(结算运行态召唤单位简易攻击(单位, 目标, combatData, '召唤自主行动'));
      });
      return 日志.filter(Boolean).join(' ');
    }

    function 执行自主召唤行动轴回合(combatData = {}) {
      确保召唤单位表(combatData);
      const 日志 = [];
      读取召唤单位列表(combatData, { 行动模式: '自主行动' }).forEach(单位 => {
        if (单位.__本回合已召唤行动 || 召唤单位本回合刚生成(单位, combatData) || !isCombatUnitAlive(单位)) return;
        const 目标 = 选择召唤攻击目标(单位, combatData);
        if (!目标) return;
        const 技能 = 选择召唤攻击技能(单位, 目标);
        const 技能名 = 技能?.name || 技能?.魂技名 || '普通攻击';
        写入战斗事件账本(combatData, {
          eventKind: 'action_start',
          round: Number(combatData?.回合 || 0),
          actorName: 单位?.name || 单位?.名称 || '',
          targetName: 目标?.name || 目标?.名称 || '',
          actionName: 技能名,
          actionType: '召唤自主行动',
          targetScope: 'single',
          result: 'declared',
          meta: {
            source: 'summon',
            summonHostName: 单位?.宿主名 || 单位?.__宿主?.name || 单位?.__宿主?.名称 || '',
            summonType: 单位?.类型 || '',
            summonMode: 单位?.行动模式 || '',
          },
        });
        记录召唤物规划审计(combatData, 单位, 目标, 技能, '行动轴召唤物自主行动');
        const 账本起点 = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger.length : 0;
        const 结果 = runActorTurn({ char: 单位, side: 单位.阵营 === '敌方' ? 'enemy' : 'player' }, {
          combatData,
          round: Number(combatData?.回合 || 0),
          logs: [],
        });
        单位.__本回合已召唤行动 = true;
        const 当前回合 = Number(combatData?.回合 || 0);
        const 召唤动作已落地 = (Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger : [])
          .slice(Math.max(0, 账本起点))
          .some(event =>
            Number(event?.round || 0) === 当前回合 &&
            isSameBattleReportName(event?.actorName || '', 单位?.name || 单位?.名称 || '') &&
            ['hit_result', 'state_apply', 'blocked_action', 'failed_action'].includes(String(event?.eventKind || '').trim())
          );
        if (结果?.log) 日志.push(结果.log);
        if (!召唤动作已落地) {
          记录召唤物规划审计(combatData, 单位, 目标, 技能, '行动轴召唤物兜底结算');
          日志.push(结算运行态召唤单位简易攻击(单位, 目标, combatData, '召唤自主行动'));
        }
      });
      return 日志.filter(Boolean).join(' ');
    }

    function 执行协同召唤追击(宿主 = {}, 默认目标 = {}, 有效伤害 = 0, combatData = {}) {
      if (!(Number(有效伤害 || 0) > 0)) return '';
      const 日志 = [];
      const 宿主伤害事件 = 查找最近宿主有效伤害事件(combatData, 宿主, 默认目标);
      读取召唤单位列表(combatData, { 宿主, 行动模式: '协同攻击' }).forEach(单位 => {
        if (单位.__本回合已召唤行动 || 召唤单位本回合刚生成(单位, combatData) || !isCombatUnitAlive(单位) || !isCombatUnitAlive(默认目标)) return;
        单位.__本回合已召唤行动 = true;
        const 技能 = 选择召唤攻击技能(单位, 默认目标);
        记录召唤物规划审计(combatData, 单位, 默认目标, 技能, '宿主有效伤害后的召唤协同追击');
        日志.push(结算运行态召唤单位简易攻击(单位, 默认目标, combatData, '召唤协同追击', {
          hostDamageEvent: 宿主伤害事件,
          hostName: 宿主?.name || 宿主?.名称 || '',
        }));
      });
      return 日志.filter(Boolean).join(' ');
    }

    function 读取护卫召唤单位(combatData = {}, 受击者 = {}) {
      const 阵营 = 读取召唤宿主阵营(combatData, 受击者);
      return 读取召唤单位列表(combatData, { 阵营, 行动模式: '护卫' })
        .find(单位 => isCombatUnitAlive(单位) && !单位.__本回合已召唤行动 && !isCombatUnitIdentityMatch(单位, 受击者?.name || 受击者?.名称 || 受击者)) || null;
    }

    function 构建召唤夹具单位(名称, 阵营 = '玩家') {
      return bindCombatParticipant({
        name: 名称,
        type: 阵营 === '玩家' ? '强攻系' : '敏攻系',
        lv: 45,
        hp: 1200,
        hp_max: 1200,
        HP: 1200,
        HP上限: 1200,
        体力: 900,
        体力上限: 900,
        sp: 800,
        sp_max: 800,
        men: 600,
        men_max: 600,
        str: 180,
        def: 120,
        agi: 110,
        状态效果: {},
        持续效果: {},
      });
    }

    function 构建召唤夹具战斗态(配置 = {}) {
      const 宿主 = 构建召唤夹具单位('夹具宿主', '玩家');
      const 敌人 = 构建召唤夹具单位('夹具敌人', '敌方');
      const combatData = {
        回合: 1,
        战斗类型: '召唤夹具',
        阶段: 战斗阶段枚举_V1.宣告,
        参战者: { team_player: [宿主], team_enemy: [敌人] },
      };
      const 来源状态键 = `召唤:${配置.名称 || '夹具召唤物'}`;
      const 召唤状态 = {
        类型: 'buff',
        层数: 1,
        duration: 3,
        状态效果: {},
        战斗效果: createEmptyCombatEffectMap(),
          召唤物: {
            召唤单位类型: 配置.类型 || '魂兽',
            召唤物名称: 配置.名称 || '夹具召唤物',
            召唤数量: Math.max(1, Number(配置.数量 || 1)),
            行动模式: 配置.行动模式 || '自主行动',
          },
      };
      if (配置.继承属性比例 > 0) 召唤状态.召唤物.继承属性比例 = 配置.继承属性比例;
      else 召唤状态.召唤物.强度 = Number(配置.强度 || 1);
      宿主.状态效果[来源状态键] = 召唤状态;
      const 召唤单位 = 注册召唤运行态单位(combatData, 宿主, 来源状态键, 召唤状态);
      return { combatData, 宿主, 敌人, 召唤单位, 来源状态键 };
    }

    function 断言召唤夹具(条件, 文本) {
      if (!条件) throw new Error(`召唤夹具失败:${文本}`);
    }

    function 执行单个召唤夹具(名称, 执行函数) {
      const 日志 = [];
      try {
        执行函数(日志);
        return { ok: true, name: 名称, logs: 日志 };
      } catch (错误) {
        return { ok: false, name: 名称, logs: 日志, failedAt: String(错误?.message || 错误 || '未知失败') };
      }
    }

    function 运行召唤夹具(名称 = '') {
      const 夹具列表 = [];
      const 注册 = (夹具名, 执行函数) => {
        if (!名称 || 名称 === 夹具名) 夹具列表.push(执行单个召唤夹具(夹具名, 执行函数));
      };
      注册('生成入表', 日志 => {
        const { combatData, 宿主, 召唤单位 } = 构建召唤夹具战斗态();
        断言召唤夹具(!!combatData.召唤单位表?.[召唤单位.召唤键], '召唤单位未入表');
        断言召唤夹具(宿主.状态效果[召唤单位.来源状态键]?.召唤物?.召唤键 === 召唤单位.召唤键, '来源镜像未写回');
        日志.push('召唤单位入表与来源镜像成立');
      });
      注册('受击治疗护盾状态', 日志 => {
        const { combatData, 敌人, 召唤单位 } = 构建召唤夹具战斗态();
        applyResolvedDamagePackage(敌人, { action_type: '夹具攻击', skill: 构建召唤普通攻击技能(敌人) }, {
          targetResults: [{ target: 召唤单位, targetName: 召唤单位.name, damage: 80, kind: 'primary' }],
        }, { primaryTarget: 召唤单位, combatData });
        断言召唤夹具(getCombatHpValue(召唤单位) < getCombatHpMaxValue(召唤单位), '召唤物未受伤');
        设置战斗血量值(召唤单位, getCombatHpValue(召唤单位) + 30);
        applyShieldToCharacter(召唤单位, 40, 2, '夹具护盾');
        召唤单位.状态效果.夹具状态 = { 类型: 'buff', duration: 1, 战斗效果: createEmptyCombatEffectMap() };
        同步召唤单位镜像(召唤单位);
        delete 召唤单位.状态效果.夹具状态;
        同步召唤单位镜像(召唤单位);
        断言召唤夹具(召唤单位.__来源状态.召唤物.生命 === getCombatHpValue(召唤单位), '非伤害同步未写回镜像');
        日志.push('受击、治疗、护盾、状态同步成立');
      });
      注册('群体护卫协同自主', 日志 => {
        const 自主 = 构建召唤夹具战斗态({ 名称: '自主召唤', 行动模式: '自主行动' });
        const 护卫 = 构建召唤夹具战斗态({ 名称: '护卫召唤', 行动模式: '护卫' });
        const 协同 = 构建召唤夹具战斗态({ 名称: '协同召唤', 行动模式: '协同攻击' });
        applyResolvedDamagePackage(自主.敌人, { action_type: '群体夹具', skill: 构建召唤普通攻击技能(自主.敌人) }, {
          targetResults: [
            { target: 自主.宿主, targetName: 自主.宿主.name, damage: 10, kind: 'primary' },
            { target: 自主.召唤单位, targetName: 自主.召唤单位.name, damage: 10, kind: 'secondary' },
          ],
        }, { primaryTarget: 自主.宿主, combatData: 自主.combatData });
        断言召唤夹具(getCombatHpValue(自主.召唤单位) < getCombatHpMaxValue(自主.召唤单位), '群体未命中召唤物');
        断言召唤夹具(!!读取护卫召唤单位(护卫.combatData, 护卫.宿主), '护卫召唤不可读');
        断言召唤夹具(执行协同召唤追击(协同.宿主, 协同.敌人, 20, 协同.combatData).includes('召唤协同追击'), '协同追击未触发');
        断言召唤夹具(执行自主召唤行动(自主.combatData).includes('召唤自主行动'), '自主行动未触发');
        日志.push('群体、护卫、协同、自主路径成立');
      });
      注册('收回死亡精神', 日志 => {
        const 收回 = 构建召唤夹具战斗态({ 名称: '可收回召唤', 行动模式: '协同攻击' });
        主动收回召唤单位(收回.combatData, 收回.宿主, 收回.召唤单位.召唤键);
        断言召唤夹具(召唤已被主动收回(收回.combatData, 收回.宿主, 收回.召唤单位.类型, 收回.召唤单位.name), '收回锁未写入');
        断言召唤夹具(!读取召唤单位列表(收回.combatData, { 宿主: 收回.宿主 }).length, '收回后仍在表内');
        const 死亡 = 构建召唤夹具战斗态({ 名称: '继承召唤', 继承属性比例: 0.3, 行动模式: '协同攻击' });
        设置战斗血量值(死亡.召唤单位, 0);
        断言召唤夹具(处理召唤单位死亡(死亡.combatData, 死亡.召唤单位).includes('死亡反噬'), '继承型死亡未反噬');
        const 精神 = 构建召唤夹具战斗态({ 名称: '精神召唤', 行动模式: '自主行动' });
        精神.宿主.men = 0;
        断言召唤夹具(刷新召唤精神负载(精神.combatData, 精神.宿主).includes('精神'), '精神枯竭未消散');
        const 消散事件 = (精神.combatData.__battleEventLedger || []).find(event =>
          event?.eventKind === 'blocked_action' &&
          event?.actionType === 'summon_control' &&
          event?.meta?.restriction === 'dissipated' &&
          event?.meta?.reasonCode === 'SUMMON_CONTROL_OVERLOAD'
        );
        断言召唤夹具(!!消散事件?.chainNodeId, '精神消散未写入召唤控制账本');
        断言召唤夹具((精神.combatData.__battleResolutionTrace || []).some(node => node.nodeId === 消散事件.chainNodeId), '精神消散未进入因果链');
        const 受限 = 构建召唤夹具战斗态({ 名称: '受限召唤', 行动模式: '自主行动' });
        受限.宿主.men_max = 100;
        受限.宿主.men = 10;
        断言召唤夹具(刷新召唤精神负载(受限.combatData, 受限.宿主).includes('受限'), '精神不足未限制召唤技能');
        const 受限事件 = (受限.combatData.__battleEventLedger || []).find(event =>
          event?.eventKind === 'blocked_action' &&
          event?.actionType === 'summon_control' &&
          event?.meta?.restriction === 'skill_disabled' &&
          event?.meta?.reasonCode === 'SUMMON_CONTROL_OVERLOAD'
        );
        断言召唤夹具(!!受限事件?.chainNodeId, '精神受限未写入召唤控制账本');
        断言召唤夹具((受限.combatData.__battleResolutionTrace || []).some(node => node.nodeId === 受限事件.chainNodeId), '精神受限未进入因果链');
        const 超载 = 构建召唤夹具战斗态({ 名称: '超载召唤', 行动模式: '自主行动' });
        超载.宿主.men_max = 30;
        超载.宿主.men = 30;
        断言召唤夹具(刷新召唤精神负载(超载.combatData, 超载.宿主).includes('超载'), '精神负载超限未压缩召唤物');
        const 压缩事件 = (超载.combatData.__battleEventLedger || []).find(event =>
          event?.eventKind === 'blocked_action' &&
          event?.actionType === 'summon_control' &&
          event?.meta?.restriction === 'compressed' &&
          event?.meta?.reasonCode === 'SUMMON_CONTROL_OVERLOAD'
        );
        断言召唤夹具(!!压缩事件?.chainNodeId, '精神超载压缩未写入召唤控制账本');
        断言召唤夹具((超载.combatData.__battleResolutionTrace || []).some(node => node.nodeId === 压缩事件.chainNodeId), '精神超载压缩未进入因果链');
        日志.push('收回锁、死亡反噬、精神消散、受限与超载压缩结构化成立');
      });
      注册('目标池与单体收回', 日志 => {
        const 第一 = 构建召唤夹具战斗态({ 名称: '一号召唤', 行动模式: '协同攻击' });
        const 第二状态键 = '召唤:二号召唤';
        const 第二状态 = {
          类型: 'buff',
          层数: 1,
          duration: 3,
          战斗效果: createEmptyCombatEffectMap(),
          召唤物: { 召唤单位类型: '魂兽', 召唤物名称: '二号召唤', 召唤数量: 1, 行动模式: '协同攻击', 强度: 1 },
        };
        第一.宿主.状态效果[第二状态键] = 第二状态;
        const 第二单位 = 注册召唤运行态单位(第一.combatData, 第一.宿主, 第二状态键, 第二状态);
        主动收回召唤单位(第一.combatData, 第一.宿主, 第一.召唤单位.召唤键);
        const 剩余列表 = 读取召唤单位列表(第一.combatData, { 宿主: 第一.宿主 });
        断言召唤夹具(剩余列表.length === 1 && 剩余列表[0].召唤键 === 第二单位.召唤键, '单体收回误收其他召唤物');
        设置战斗血量值(第二单位, 0);
        处理召唤单位死亡(第一.combatData, 第二单位);
        断言召唤夹具(!读取战斗阵营单位列表(第一.combatData, '玩家').some(单位 => 单位.召唤键 === 第二单位.召唤键), '已消散召唤仍在目标池');
        日志.push('单体收回与已消散目标池清理成立');
      });
      注册('分身目标隔离', 日志 => {
        const 分身态 = 构建召唤夹具战斗态({ 类型: '分身', 名称: '夹具分身', 继承属性比例: 0.45, 行动模式: '协同攻击' });
        const 召唤状态键 = '召唤:普通召唤';
        const 召唤状态 = {
          类型: 'buff',
          层数: 1,
          duration: 3,
          战斗效果: createEmptyCombatEffectMap(),
          召唤物: { 召唤单位类型: '魂兽', 召唤物名称: '普通召唤', 召唤数量: 1, 行动模式: '协同攻击', 强度: 1 },
        };
        分身态.宿主.状态效果[召唤状态键] = 召唤状态;
        const 普通召唤 = 注册召唤运行态单位(分身态.combatData, 分身态.宿主, 召唤状态键, 召唤状态);
        const 召唤物目标 = resolveSkillTargetContext({}, 分身态.宿主, 分身态.敌人, 分身态.combatData, { 原型: '状态施加', 目标: '召唤物' }).targetSet;
        const 分身目标 = resolveSkillTargetContext({}, 分身态.宿主, 分身态.敌人, 分身态.combatData, { 原型: '属性修正', 目标: '分身' }).targetSet;
        断言召唤夹具(召唤物目标.some(单位 => 单位.召唤键 === 普通召唤.召唤键), '召唤物目标未包含普通召唤');
        断言召唤夹具(!召唤物目标.some(单位 => 单位.召唤键 === 分身态.召唤单位.召唤键), '召唤物目标误包含分身');
        断言召唤夹具(分身目标.length === 1 && 分身目标[0].召唤键 === 分身态.召唤单位.召唤键, '分身目标未专门命中分身');
        主动收回召唤单位(分身态.combatData, 分身态.宿主, 分身态.召唤单位.召唤键);
        断言召唤夹具(!!读取召唤单位列表(分身态.combatData, { 宿主: 分身态.宿主 }).find(单位 => 单位.召唤键 === 分身态.召唤单位.召唤键), '收回召唤误收分身');
        日志.push('分身独立目标与召唤物目标隔离成立');
      });
      注册('批量编号重复召唤', 日志 => {
        const 基础 = 构建召唤夹具战斗态({ 名称: '占位召唤', 行动模式: '协同攻击' });
        移除召唤运行态单位(基础.combatData, 基础.召唤单位, '夹具重建');
        const 生成 = () => {
          const 编号 = 读取下一个召唤编号(基础.combatData, 基础.宿主, '其他召唤生物', '比蒙巨兽');
          const 名称 = `比蒙巨兽#${编号}`;
          const 状态键 = `召唤:${名称}`;
          const 状态 = {
            类型: 'buff',
            层数: 1,
            duration: 3,
            战斗效果: createEmptyCombatEffectMap(),
            召唤物: { 召唤单位类型: '其他召唤生物', 召唤物名称: 名称, 基础名称: '比蒙巨兽', 召唤数量: 1, 行动模式: '协同攻击', 强度: 1 },
          };
          基础.宿主.状态效果[状态键] = 状态;
          return 注册召唤运行态单位(基础.combatData, 基础.宿主, 状态键, 状态);
        };
        const 第一批 = [生成(), 生成(), 生成()];
        断言召唤夹具(第一批.map(单位 => 单位.name).join('|') === '比蒙巨兽#1|比蒙巨兽#2|比蒙巨兽#3', '批量召唤未按编号生成');
        主动收回召唤单位(基础.combatData, 基础.宿主, 第一批[1].召唤键);
        const 追加 = 生成();
        const 剩余名称 = 读取召唤单位列表(基础.combatData, { 宿主: 基础.宿主 }).map(单位 => 单位.name).sort();
        断言召唤夹具(追加.name === '比蒙巨兽#4', '重复召唤未追加新编号');
        断言召唤夹具(剩余名称.join('|') === '比蒙巨兽#1|比蒙巨兽#3|比蒙巨兽#4', '编号召唤未保持独立单位');
        日志.push('其他召唤生物批量编号、单体收回、重复追加成立');
      });
      return { ok: 夹具列表.every(item => item.ok), results: 夹具列表 };
    }

    root.__LWCS_LIST_SUMMON_FIXTURES__ = () => ['生成入表', '受击治疗护盾状态', '群体护卫协同自主', '收回死亡精神', '目标池与单体收回', '分身目标隔离', '批量编号重复召唤'];
    root.__LWCS_RUN_SUMMON_FIXTURE_BATCH__ = (名称 = '') => 运行召唤夹具(名称);

    function 构建战斗回归夹具单位(名称, 系别 = '强攻系') {
      return bindCombatParticipant({
        name: 名称,
        type: 系别,
        lv: 35,
        等级: 35,
        hp: 1600,
        hp_max: 1600,
        HP: 1600,
        HP上限: 1600,
        sp: 2000,
        sp_max: 2000,
        魂力: 2000,
        魂力上限: 2000,
        sta: 1200,
        vit: 1200,
        vit_max: 1200,
        体力: 1200,
        体力上限: 1200,
        men: 900,
        men_max: 900,
        精神力: 900,
        精神力上限: 900,
        str: 系别 === '敏攻系' ? 150 : 220,
        def: 140,
        agi: 系别 === '敏攻系' ? 260 : 120,
        状态效果: {},
        持续效果: {},
        背包: {},
      });
    }

    const 战斗回归第一魂技默认名 = '锁魄毒印';

    function 构建战斗回归第一魂技(名称 = 战斗回归第一魂技默认名, 消耗 = '魂力:120') {
      return {
        魂技名: 名称,
        name: 名称,
        技能分类: '控制',
        目标: '敌方单体',
        消耗,
        前摇: 10,
        _效果数组: [
          { 原型: '状态施加', 目标: '敌方单体', 生效方式: '独立生效', 状态: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.2, dodge_penalty: 0.1, lock_level: 1 } },
          { 原型: '状态施加', 目标: '敌方单体', 生效方式: '独立生效', 状态: '中毒', 持续回合: 2, 计算层效果: { dot_damage_ratio: 0.03, hit_penalty: 0.05 } },
        ],
      };
    }

    function 战斗回归输出魂技(名称, 目标 = '敌方单体', 前摇 = 10, 威力倍率 = 90, 伤害类型 = '近身攻击') {
      return {
        魂技名: 名称,
        name: 名称,
        技能分类: '输出',
        目标,
        消耗: '无',
        前摇,
        _效果数组: [
          { 原型: '伤害结算', 目标: /群体/.test(String(目标 || '')) ? '群体' : '单体', 生效方式: '独立生效', 威力倍率, 伤害类型, 防御穿透: 0 },
        ],
      };
    }

    function 构建战斗回归夹具战斗态() {
      const 玩家 = 构建战斗回归夹具单位('夹具玩家', '强攻系');
      const 友方 = 构建战斗回归夹具单位('夹具友方', '辅助系');
      const 敌人 = 构建战斗回归夹具单位('夹具敌人', '敏攻系');
      玩家.第1武魂 = {
        表象名称: '夹具武魂',
        第1魂环: {
          第1魂技: 构建战斗回归第一魂技(),
        },
      };
      const combatData = {
        回合: 1,
        战斗类型: '回归夹具',
        战斗意图: '点到为止',
        阶段: 战斗阶段枚举_V1.宣告,
        参战者: { team_player: [玩家, 友方], team_enemy: [敌人] },
      };
      return { combatData, 玩家, 友方, 敌人 };
    }

    function 断言战斗回归夹具(条件, 文本) {
      if (!条件) throw new Error(`战斗回归夹具失败:${文本}`);
    }

    function 使用战斗回归桥接(combatData, 角色表, 执行函数) {
      const 原桥接 = root.BattleUIBridge;
      const 物品表 = { 魂技造物: {} };
      root.BattleUIBridge = {
        ...(原桥接 || {}),
        getBattleContext: () => combatData,
        getMVU: 路径 => {
          const 文本 = String(路径 || '').trim();
          if (文本 === 'world.战斗') return combatData;
          if (文本 === 'world.时间.tick') return 0;
          if (文本 === 'sys.玩家名') return '夹具玩家';
          if (文本 === '物品') return 物品表;
          const 角色命中 = 文本.match(/^char\.([^\.]+)(?:\.(.+))?$/);
          if (角色命中) {
            const 角色 = 角色表?.[角色命中[1]];
            if (!角色) return null;
            if (!角色命中[2]) return 角色;
            if (角色命中[2] === '背包') return 角色.背包 || {};
            return 角色[角色命中[2]];
          }
          return 原桥接?.getMVU?.(路径);
        },
      };
      try {
        return 执行函数(物品表);
      } finally {
        root.BattleUIBridge = 原桥接;
      }
    }

    function 执行单个战斗回归夹具(名称, 执行函数) {
      const 日志 = [];
      try {
        执行函数(日志);
        return { ok: true, name: 名称, logs: 日志 };
      } catch (错误) {
        return { ok: false, name: 名称, logs: 日志, failedAt: String(错误?.message || 错误 || '未知失败') };
      }
    }

    function 运行战斗回归夹具(名称 = '') {
      const 夹具列表 = [];
      const 注册 = (夹具名, 执行函数) => {
        if (!名称 || 名称 === 夹具名) 夹具列表.push(执行单个战斗回归夹具(夹具名, 执行函数));
      };
      注册('第一魂技槽位归属', 日志 => {
        const { 玩家 } = 构建战斗回归夹具战斗态();
        const entry = {
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技', name: '错误缓存第一魂技', 消耗: '魂力:9999' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        };
        const action = buildPlayerActionFromSerializedEntry(entry, '夹具玩家', 玩家);
        断言战斗回归夹具(!!action, '当前玩家动作未生成');
        断言战斗回归夹具(action.skill?.魂技名 === 战斗回归第一魂技默认名 || action.skill?.name === 战斗回归第一魂技默认名, '未按当前角色魂环槽位重取技能');
        断言战斗回归夹具(action.skill?.消耗 === '魂力:120', '仍沿用旧动作队列技能数据');
        断言战斗回归夹具(action.actor_name === '夹具玩家', '行动者未绑定当前玩家');
        断言战斗回归夹具(Array.isArray(action.skill.__魂环路径) && action.skill.__魂环路径.join('/') === '第1武魂/第1魂环', '魂环路径未保留');
        断言战斗回归夹具(action.skill.__魂技槽位 === '第1魂技', '魂技槽位未保留');
        断言战斗回归夹具(buildPlayerActionFromSerializedEntry(entry, '其他角色', 玩家) === null, '外部行动者动作未被拒绝');
        日志.push('第一魂技来源、槽位、行动者归属成立');
      });
      注册('固定消耗只扣一次', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const action = buildPlayerActionFromSerializedEntry({
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        }, '夹具玩家', 玩家);
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          applyActionCost(玩家, action, 敌人, combatData);
          applyActionCost(玩家, action, 敌人, combatData);
        });
        断言战斗回归夹具(Number(玩家.sp) === 1880 && Number(玩家.魂力) === 1880, `固定消耗异常:${玩家.sp}/${玩家.魂力}`);
        日志.push('固定魂力消耗只扣一次');
      });
      注册('百分比普通魂技阻断不扣费', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.第1武魂.第1魂环.第1魂技 = 构建战斗回归第一魂技('百分比错误魂技', '魂力:50%');
        const action = buildPlayerActionFromSerializedEntry({
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '百分比错误魂技' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        }, '夹具玩家', 玩家);
        let log = '';
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          log = applyActionCost(玩家, action, 敌人, combatData);
        });
        断言战斗回归夹具(action.action_type === '施法失败', '百分比普通魂技未被阻断');
        断言战斗回归夹具(Number(玩家.sp) === 2000 && Number(玩家.魂力) === 2000, '百分比普通魂技阻断后仍扣费');
        断言战斗回归夹具(/百分比启动消耗|固定数值/.test(log), '阻断日志未说明百分比启动消耗');
        日志.push('普通魂技百分比启动消耗阻断且不扣费');
      });
      注册('目标语义不串线', 日志 => {
        const { combatData, 玩家, 友方, 敌人 } = 构建战斗回归夹具战斗态();
        const selfSkill = normalizeSkillData({ name: '自稳', 技能分类: '辅助', 目标: '自身', 消耗: '无', _效果数组: [{ 原型: '状态施加', 目标: '自身', 状态名称: '自稳' }] }, '自稳');
        const allySkill = normalizeSkillData({ name: '援护', 技能分类: '辅助', 目标: '友方单体', 消耗: '无', _效果数组: [{ 原型: '护盾变化', 目标: '友方单体', 数值: '+100' }] }, '援护');
        const enemySkill = normalizeSkillData({ name: '压制', 技能分类: '输出', 目标: '敌方单体', 消耗: '无', _效果数组: [{ 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 50, 伤害类型: '近身攻击' }] }, '压制');
        const allBuff = normalizeSkillData({ name: '全场鼓舞', 技能分类: '辅助', 目标: '全场', 消耗: '无', _效果数组: [{ 原型: '属性修正', 目标: '全场', 数值: '+10%' }] }, '全场鼓舞');
        断言战斗回归夹具(resolveSkillTargetContext(selfSkill, 玩家, 敌人, combatData).primaryTarget === 玩家, '自身技能目标串线');
        断言战斗回归夹具(resolveSkillTargetContext(allySkill, 玩家, 友方, combatData).primaryTarget === 友方, '友方技能目标串线');
        断言战斗回归夹具(resolveSkillTargetContext(enemySkill, 玩家, 敌人, combatData).primaryTarget === 敌人, '敌方技能目标串线');
        断言战斗回归夹具(!判定单挑动作敌对({ action_type: '释放魂技', skill: allBuff }, 玩家, 敌人, combatData), '全场纯增益误判为敌对');
        const 临时数据 = 构建单挑临时战斗数据(玩家, 玩家, 'player', combatData);
        断言战斗回归夹具(!临时数据.参战者.team_enemy.some(unit => isCombatUnitIdentityMatch(unit, 玩家.name)), '自身动作把自己塞入敌方队列');
        日志.push('自身、友方、敌方、全场目标语义成立');
      });
      注册('造物给友方入包补丁', 日志 => {
        const { combatData, 玩家, 友方, 敌人 } = 构建战斗回归夹具战斗态();
        const skill = {
          name: '夹具造物',
          魂技名: '夹具造物',
          技能分类: '辅助',
          承载方式: '造物承载',
          消耗: '魂力:100',
          _效果数组: [
            { 数量: 1, 描述: '夹具造物', 使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '魂力', 数值: '+10' }] },
          ],
        };
        let patchOps = [];
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具友方: 友方, 夹具敌人: 敌人 }, () => {
          patchOps = 构建造物承载补丁包({ ...skill, __造物产出者: 玩家 }, 友方.背包, '夹具友方').patchOps;
        });
        const paths = patchOps.map(op => String(op?.path || ''));
        断言战斗回归夹具(paths.some(path => path.includes('/char/夹具友方/背包/夹具造物')), '造物未写入友方背包路径');
        断言战斗回归夹具(!paths.some(path => path.includes('/char/夹具玩家/背包/夹具造物')), '生成给友方误写入自己背包');
        日志.push('造物生成给友方的 patch 落点成立');
      });
      注册('非敌对短前摇先完成', 日志 => {
        const { combatData, 玩家, 友方, 敌人 } = 构建战斗回归夹具战斗态();
        const fastConstruct = {
          name: '快造物',
          魂技名: '快造物',
          技能分类: '辅助',
          承载方式: '造物承载',
          消耗: '魂力:80',
          前摇: 6,
          _效果数组: [
            { 数量: 1, 描述: '快造物', 使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '魂力', 数值: '+10' }] },
          ],
        };
        const npcAttack = normalizeSkillData({
          name: '慢压制',
          技能分类: '输出',
          消耗: '无',
          前摇: 14,
          _效果数组: [{ 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 60, 伤害类型: '近身攻击' }],
        }, '慢压制');
        const action = {
          action_type: '释放魂技',
          skill: normalizeSkillData(fastConstruct, '快造物'),
          cast_time: 6,
          造物处理: '生成给友方',
          物品接收者: 友方.name,
          target_name: 友方.name,
        };
        const target = 解析单挑动作目标(action, 玩家, 敌人, combatData);
        const playerHostile = 判定单挑动作敌对(action, 玩家, target, combatData);
        const npcAction = { action_type: '主动压迫', type: '主动压迫', skill: npcAttack, cast_time: 14 };
        const npcHostile = 判定单挑动作敌对(npcAction, 敌人, 玩家, combatData);
        const playerCompletesBeforeNpc = !playerHostile && 读取单挑动作前摇(action) <= 读取单挑动作前摇(npcAction);
        const completedReaction = { type: '无法反应', log: `[先手压制] ${getCombatReportUnitName(玩家, '我方')}刚完成【${action?.skill?.name || action?.skill?.魂技名 || action?.action_type || '行动'}】，来不及追加防守。`, skill: null, def_mult: 1 };
        let patchOps = [];
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具友方: 友方, 夹具敌人: 敌人 }, () => {
          const costLog = applyActionCost(玩家, action, target, combatData);
          const result = executeClash(action, 构建单挑配合动作(玩家, target, action), 构建单挑临时战斗数据(玩家, target, 'player', combatData));
          patchOps = result.extraPatchOps || [];
          断言战斗回归夹具(/战前消耗/.test(costLog), '先完成非敌对动作未扣费');
        });
        断言战斗回归夹具(playerCompletesBeforeNpc && npcHostile, '短前摇非敌对动作未按完成后承压处理');
        断言战斗回归夹具(!/正在执行|未能完成/.test(completedReaction.log), '已完成动作反应文本仍像未完成');
        断言战斗回归夹具(Number(玩家.sp) === 1920 && Number(玩家.魂力) === 1920, '短前摇非敌对动作扣费异常');
        断言战斗回归夹具(patchOps.some(op => String(op?.path || '').includes('/char/夹具友方/背包/快造物')), '短前摇造物未入目标友方背包');
        日志.push('短前摇非敌对动作先完成、扣费与入包成立');
      });
      注册('非敌对长前摇被打断不落地', 日志 => {
        const { combatData, 玩家, 友方, 敌人 } = 构建战斗回归夹具战斗态();
        const slowConstruct = {
          name: '慢造物',
          魂技名: '慢造物',
          技能分类: '辅助',
          承载方式: '造物承载',
          消耗: '魂力:300',
          前摇: 28,
          _效果数组: [
            { 数量: 1, 描述: '慢造物', 使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '魂力', 数值: '+10' }] },
          ],
        };
        const npcInterrupt = normalizeSkillData({
          name: '先制截断',
          技能分类: '输出',
          消耗: '无',
          前摇: 5,
          _效果数组: [
            { 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 120, 伤害类型: '近身攻击' },
            { 原型: '状态施加', 目标: '敌方单体', 状态名称: '眩晕', 计算层效果: { skip_turn: true } },
          ],
        }, '先制截断');
        const action = {
          action_type: '释放魂技',
          skill: normalizeSkillData(slowConstruct, '慢造物'),
          cast_time: 28,
          造物处理: '生成给友方',
          物品接收者: 友方.name,
          target_name: 友方.name,
        };
        const target = 解析单挑动作目标(action, 玩家, 敌人, combatData);
        const playerHostile = 判定单挑动作敌对(action, 玩家, target, combatData);
        const npcAction = { action_type: '主动压迫', type: '主动压迫', skill: npcInterrupt, cast_time: 5 };
        const npcFirst = 判定单挑动作敌对(npcAction, 敌人, 玩家, combatData) && (!playerHostile || 读取单挑动作前摇(npcAction) < 读取单挑动作前摇(action));
        断言战斗回归夹具(npcFirst, 'NPC 未在长前摇非敌对动作前抢先');
        断言战斗回归夹具(Number(玩家.sp) === 2000 && Number(玩家.魂力) === 2000, '长前摇被打断前不应扣费');
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具友方: 友方, 夹具敌人: 敌人 }, () => {
          const supportCombatData = 构建单挑临时战斗数据(玩家, target, 'player', combatData);
          断言战斗回归夹具(!supportCombatData.参战者.team_enemy.some(unit => isCombatUnitIdentityMatch(unit, 玩家.name)), '未完成非敌对动作把自己塞入敌方');
        });
        日志.push('长前摇非敌对动作可被先制，未完成前不扣费不落地');
      });
      注册('防御时 NPC 仍主动规划', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.第1武魂 = {
          表象名称: '敌方武魂',
          第1魂环: {
            第1魂技: {
              魂技名: '敌方快攻',
              name: '敌方快攻',
              技能分类: '输出',
              目标: '敌方单体',
              消耗: '无',
              前摇: 8,
              _效果数组: [{ 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 90, 伤害类型: '近身攻击' }],
            },
          },
        };
        const playerDefense = { action_type: '防御', type: '防御', skill: normalizeSkillData({ name: '防御', 技能分类: '防御', 消耗: '无', 前摇: 10 }, '防御'), cast_time: 10 };
        const npcActorEntry = { char: 敌人, side: 'enemy' };
        const npcTargets = chooseTargetForActor(npcActorEntry, { combatData }) || { enemyTarget: 玩家, allyTarget: 敌人 };
        const npcAction = buildAutoActionForActor(npcActorEntry, npcTargets, { combatData, observedTargetAction: playerDefense });
        断言战斗回归夹具(!!npcAction, 'NPC 未生成主动规划动作');
        断言战斗回归夹具(!/战术待机|观察/.test(String(npcAction.action_type || npcAction.type || '')), '玩家防御导致 NPC 冻结待机');
        断言战斗回归夹具(判定单挑动作敌对(npcAction, 敌人, 玩家, combatData), 'NPC 主动规划未形成敌对动作');
        日志.push(`玩家防御时 NPC 仍主动规划:${npcAction.skill?.name || npcAction.action_type || npcAction.type}`);
      });
      注册('非攻击动作集合不冻结 NPC', 日志 => {
        const 构建敌方快攻态 = () => {
          const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
          敌人.第1武魂 = {
            表象名称: '敌方武魂',
            第1魂环: {
              第1魂技: 战斗回归输出魂技('敌方快攻', '敌方单体', 8, 90, '近身攻击'),
            },
          };
          return { combatData, 玩家, 敌人 };
        };
        const 场景列表 = [
          {
            名称: '防御',
            动作: () => ({ action_type: '防御', type: '防御', skill: normalizeSkillData({ name: '防御', 技能分类: '防御', 消耗: '无', 前摇: 10 }, '防御'), cast_time: 10 }),
          },
          {
            名称: '闪避',
            动作: () => ({ action_type: '闪避', type: '闪避', skill: normalizeSkillData({ name: '闪避', 技能分类: '防御', 消耗: '体力:5', 前摇: 12 }, '闪避'), cast_time: 12 }),
          },
          {
            名称: '造物',
            动作: 玩家 => ({
              action_type: '释放魂技',
              type: '释放魂技',
              skill: normalizeSkillData({
                name: '战术造物',
                魂技名: '战术造物',
                技能分类: '辅助',
                承载方式: '造物承载',
                目标: '自身',
                消耗: '魂力:60',
                前摇: 8,
                _效果数组: [{ 数量: 1, 描述: '战术造物', 使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '魂力', 数值: '+10' }] }],
              }, '战术造物'),
              cast_time: 8,
              造物处理: '生成到自己背包',
              target_name: 玩家.name,
            }),
          },
          {
            名称: '自身增益',
            动作: () => ({
              action_type: '释放魂技',
              type: '释放魂技',
              skill: normalizeSkillData({
                name: '自稳增益',
                魂技名: '自稳增益',
                技能分类: '辅助',
                目标: '自身',
                消耗: '魂力:40',
                前摇: 8,
                _效果数组: [{ 原型: '状态施加', 目标: '自身', 状态名称: '自稳增益' }],
              }, '自稳增益'),
              cast_time: 8,
            }),
          },
        ];
        场景列表.forEach(场景 => {
          const { combatData, 玩家, 敌人 } = 构建敌方快攻态();
          const playerAction = 场景.动作(玩家);
          const npcActorEntry = { char: 敌人, side: 'enemy' };
          const npcTargets = chooseTargetForActor(npcActorEntry, { combatData }) || { enemyTarget: 玩家, allyTarget: 敌人 };
          const npcAction = buildAutoActionForActor(npcActorEntry, npcTargets, { combatData, observedTargetAction: playerAction });
          断言战斗回归夹具(!判定单挑动作敌对(playerAction, 玩家, 敌人, combatData), `${场景.名称} 被误判为敌对动作`);
          断言战斗回归夹具(!!npcAction, `${场景.名称} 场景 NPC 未生成主动规划`);
          断言战斗回归夹具(!/战术待机|观察/.test(String(npcAction.action_type || npcAction.type || '')), `${场景.名称} 场景 NPC 冻结待机`);
          断言战斗回归夹具(判定单挑动作敌对(npcAction, 敌人, 玩家, combatData), `${场景.名称} 场景 NPC 未形成敌对动作`);
        });
        日志.push(`非攻击动作均不冻结 NPC:${场景列表.map(item => item.名称).join('、')}`);
      });
      注册('敏攻近身来袭有通用应对', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '敏攻系';
        玩家.agi = 360;
        玩家.str = 170;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '强攻系';
        敌人.agi = 150;
        敌人.str = 260;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '反高速武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('短截拳', '敌方单体', 8, 88, '近身攻击'),
            第2魂技: 战斗回归输出魂技('横扫压制', '敌方群体', 14, 96, '远程攻击'),
          },
        };
        const playerAction = {
          action_type: '释放魂技',
          type: '释放魂技',
          skill: normalizeSkillData(战斗回归输出魂技('高速切入', '敌方单体', 8, 110, '近身攻击'), '高速切入'),
          cast_time: 8,
        };
        const 反高速窗口 = 评估反高速应对窗口(敌人, 玩家, playerAction, combatData);
        const 应招候选 = 构建应招候选池(敌人, 玩家, playerAction, combatData);
        断言战斗回归夹具(反高速窗口.成立, '敏攻近身高速攻击未打开反高速窗口');
        断言战斗回归夹具(应招候选.some(item => item.name === '防守反击' && Number(item.weight || 0) > 0), '应招候选缺少防守反击');
        断言战斗回归夹具(应招候选.some(item => item.分类 === '短前摇对轰' && Number(item.weight || 0) > 0), '应招候选缺少短前摇对轰');
        断言战斗回归夹具(应招候选.some(item => item.分类 === '范围压制' && Number(item.weight || 0) > 0), '应招候选缺少范围压制');
        日志.push(`反高速候选成立:${应招候选.map(item => `${item.name}/${item.分类}:${item.weight}`).join('，')}`);
      });
      注册('反高速主动规划保留区分', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '敏攻系';
        玩家.agi = 360;
        玩家.str = 170;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '强攻系';
        敌人.agi = 150;
        敌人.str = 260;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '反高速武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('短截拳', '敌方单体', 8, 88, '近身攻击'),
            第2魂技: 战斗回归输出魂技('横扫压制', '敌方群体', 14, 96, '远程攻击'),
          },
        };
        const playerAction = {
          action_type: '释放魂技',
          type: '释放魂技',
          skill: normalizeSkillData(战斗回归输出魂技('高速切入', '敌方单体', 8, 110, '近身攻击'), '高速切入'),
          cast_time: 8,
        };
        const availableSkills = collectCombatSkills(敌人, []);
        const strategicContext = buildStrategicCandidates(敌人, 玩家, combatData, playerAction, 1, availableSkills, [], (type, log, skill = null, extra = {}) => ({
          type,
          log,
          skill: skill ? normalizeSkillData(skill, skill.name || skill.魂技名 || type) : null,
          def_mult: 1,
          ...extra,
        }));
        strategicContext.behaviorState.规划上下文 = 构建规划上下文(敌人, 玩家, combatData, strategicContext.behaviorState);
        strategicContext.behaviorState.战局画像 = strategicContext.behaviorState.规划上下文.战局画像;
        strategicContext.behaviorState.目标优先表 = strategicContext.behaviorState.规划上下文.目标优先表;
        strategicContext.behaviorState.战略意图 = strategicContext.behaviorState.规划上下文.战略意图;
        strategicContext.behaviorState.主动回合 = true;
        const skillContext = buildNpcSkillCandidateContext(敌人, 玩家, playerAction, availableSkills, strategicContext.behaviorState, [], false, { 主动回合: true });
        const 候选 = buildTacticalCandidates(敌人, 玩家, playerAction, strategicContext.behaviorState, skillContext, (type, log, skill = null, extra = {}) => ({
          type,
          log,
          skill: skill ? normalizeSkillData(skill, skill.name || skill.魂技名 || type) : null,
          def_mult: 1,
          ...extra,
        }), false, false);
        const 范围 = 候选.find(item => item.name === '范围压制');
        const 短截 = 候选.find(item => item.name === '短前摇对轰');
        断言战斗回归夹具(!!范围 && Number(范围.weight || 0) > 0, '主动候选缺少范围压制');
        断言战斗回归夹具(!!短截 && Number(短截.weight || 0) > 0, '主动候选缺少短前摇对轰');
        断言战斗回归夹具(范围.build().type === '范围压制', '范围压制构造后丢失动作类型');
        断言战斗回归夹具(短截.build().type === '短前摇对轰', '短前摇对轰构造后丢失动作类型');
        日志.push(`主动反高速候选成立:范围${范围.weight}，短截${短截.weight}`);
      });
      注册('防守反击强于闪避反击', 日志 => {
        const { 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '敏攻系';
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '强攻系';
        敌人.final = buildCombatFinalStats(敌人);
        const 闪避动作 = 建立行为防反动作(玩家, { 防反类型: '完美闪避', 出手承诺: 0.42, 触发概率: 0.5 });
        const 防守动作 = 建立行为防反动作(敌人, { 防反类型: '硬抗换伤', 出手承诺: 0.42, 触发概率: 0.5 });
        const 闪避威力 = Number(getPrimaryDamageEffect(闪避动作.skill, { 行为规划: true })?.威力倍率 || 0);
        const 防守威力 = Number(getPrimaryDamageEffect(防守动作.skill, { 行为规划: true })?.威力倍率 || 0);
        const 闪避压制 = 计算行为防反压制倍率('完美闪避', '近身攻击');
        const 防守压制 = 计算行为防反压制倍率('硬抗换伤', '近身攻击');
        断言战斗回归夹具(防守威力 > 闪避威力, `防守反击威力未高于闪避反击:${防守威力}/${闪避威力}`);
        断言战斗回归夹具(防守压制.反应削弱倍率 > 闪避压制.反应削弱倍率, '防守反击二次反应削弱未强于闪避反击');
        断言战斗回归夹具(防守压制.闪避削弱倍率 > 闪避压制.闪避削弱倍率, '防守反击闪避削弱未强于闪避反击');
        断言战斗回归夹具(防守压制.锁定倍率 > 闪避压制.锁定倍率, '防守反击锁定压制未强于闪避反击');
        日志.push(`防反区分成立:闪避${闪避威力}/防守${防守威力}`);
      });
      注册('闪避擦伤战报不写完全避开', 日志 => {
        const lines = buildReadableBattleReportLines([
          '[第1回合] [起招] 夹具玩家以[高速切入]起招。 [应招] 夹具敌人以[伺机闪避]应对。 [擦伤命中] 夹具敌人未能完全避开，只承受 40% 伤害。 [命中结算] 夹具玩家对夹具敌人造成 32 点最终伤害。',
        ], 3);
        const text = lines.join(' ');
        断言战斗回归夹具(/未能完全脱离|擦中|未能摆脱/.test(text), `擦伤闪避未说明失败:${text}`);
        断言战斗回归夹具(!/避开攻势，未承受有效伤害|灵巧地避开/.test(text), `擦伤闪避误写成完全避开:${text}`);
        断言战斗回归夹具(/32/.test(text), `擦伤伤害未保留:${text}`);
        日志.push(`擦伤闪避战报成立:${text}`);
      });
      注册('完全闪避战报不带伤害', 日志 => {
        const lines = buildReadableBattleReportLines([
          '[第1回合] [起招] 夹具玩家以[高速切入]起招。 [应招] 夹具敌人以[伺机闪避]应对。 [主动闪避] 夹具敌人凭借敏捷优势惊险躲过了攻击。',
        ], 3);
        const text = lines.join(' ');
        断言战斗回归夹具(/闪避成功，这一击没有命中|灵巧地闪避/.test(text), `完全闪避未说明未命中:${text}`);
        断言战斗回归夹具(!/造成了\s*\d+\s*点伤害|擦中|被攻势命中|\[命中结算\]/.test(text), `完全闪避误带伤害:${text}`);
        日志.push(`完全闪避战报成立:${text}`);
      });
      注册('完全闪避不落本次攻击负面状态', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.agi = 20;
        玩家.men_max = 20;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.agi = 9999;
        敌人.final = buildCombatFinalStats(敌人);
        const 闪避可躲控制 = normalizeSkillData({
          name: '夹具毒刃牵制',
          魂技名: '夹具毒刃牵制',
          技能分类: '输出',
          目标: '敌方单体',
          消耗: '无',
          前摇: 8,
          _效果数组: [
            { 原型: '伤害结算', 目标: '敌方单体', 生效方式: '独立生效', 威力倍率: 120, 伤害类型: '近身攻击' },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '独立生效', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.1 } },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '独立生效', 状态: '中毒', 状态名称: '中毒', 持续回合: 1, 计算层效果: { dot_damage: 30 } },
          ],
        }, '夹具毒刃牵制');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 闪避可躲控制, target_name: 敌人.name, cast_time: 8 };
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0;
          result = executeClash(
            action,
            { type: '伺机闪避', action_type: '伺机闪避', skill: normalizeSkillData({ name: '伺机闪避', 技能分类: '防御', 消耗: '无', 前摇: 1 }, '伺机闪避'), def_mult: 1, log: '[应招] 夹具敌人以[伺机闪避]应对。' },
            构建单挑临时战斗数据(玩家, 敌人, 'player', combatData),
          );
        } finally {
          Math.random = 原随机;
        }
        断言战斗回归夹具(/\[(?:主动闪避|绝对闪避)\]/.test(String(result?.desc || '')), `未触发完全闪避:${result?.desc || ''}`);
        断言战斗回归夹具(!/\[状态施加\].*(位移限制|中毒)|施加了\[(?:位移限制|中毒)\]/.test(String(result?.desc || '')), `完全闪避后仍记录本次负面状态:${result?.desc || ''}`);
        断言战斗回归夹具(!敌人.状态效果?.位移限制 && !敌人.状态效果?.中毒, `完全闪避后负面状态仍落表:${Object.keys(敌人.状态效果 || {}).join('、')}`);
        断言战斗回归夹具(Number(ensureActorDecisionMemory(玩家).dodge_whiff_count || 0) > 0, `完整闪避后攻击方未记录落空:${JSON.stringify(ensureActorDecisionMemory(玩家))}`);
        const settleLog = settleConditionsAtRoundEnd(敌人, 敌人.name, combatData).log || '';
        断言战斗回归夹具(!/\[状态结算\].*(位移限制|中毒)/.test(settleLog), `完全闪避后本回合状态跳伤仍触发:${settleLog}`);
        日志.push(`完全闪避不落负面状态成立:${result?.desc || ''}`);
      });
      注册('纯控制完整闪避后不落运行态状态', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.agi = 20;
        玩家.men_max = 20;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.agi = 9999;
        敌人.final = buildCombatFinalStats(敌人);
        const 纯控制 = normalizeSkillData({
          name: '夹具锁步',
          魂技名: '夹具锁步',
          技能分类: '控制',
          目标: '敌方单体',
          消耗: '无',
          前摇: 1,
          _效果数组: [
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '独立生效', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.1 } },
          ],
        }, '夹具锁步');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 纯控制, target_name: 敌人.name, cast_time: 1 };
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0;
          result = executeClash(
            action,
            { type: '伺机闪避', action_type: '伺机闪避', skill: normalizeSkillData({ name: '伺机闪避', 技能分类: '防御', 消耗: '无', 前摇: 1 }, '伺机闪避'), def_mult: 1, log: '[应招] 夹具敌人以[伺机闪避]应对。' },
            构建单挑临时战斗数据(玩家, 敌人, 'player', combatData),
          );
        } finally {
          Math.random = 原随机;
        }
        const 目标结果 = Array.isArray(result?.targetResults) ? result.targetResults.find(entry => isCombatUnitIdentityMatch(entry?.target, 敌人)) : null;
        断言战斗回归夹具(/\[(?:主动闪避|绝对闪避)\]/.test(String(result?.desc || '')), `纯控制未触发完整闪避:${result?.desc || ''}`);
        断言战斗回归夹具(!敌人.状态效果?.位移限制, `纯控制被闪避后仍落状态:${JSON.stringify(敌人.状态效果 || {})}`);
        断言战斗回归夹具(!Array.isArray(目标结果?.时光回溯预结算落地) || !目标结果.时光回溯预结算落地.includes('状态施加'), `纯控制被闪避后仍预标记状态施加:${JSON.stringify(目标结果 || null)}`);
        日志.push(`纯控制完整闪避未落运行态状态:${result?.desc || ''}`);
      });
      注册('位移限制不结算生命流失', 日志 => {
        const { combatData, 玩家 } = 构建战斗回归夹具战斗态();
        玩家.HP = 1000;
        玩家.hp = 1000;
        玩家.HP上限 = 1000;
        玩家.hp_max = 1000;
        玩家.状态效果 = {
          位移限制: {
            类型: 'debuff',
            状态名称: '位移限制',
            duration: 1,
            战斗效果: { ...createEmptyCombatEffectMap(), dot_damage: 16, dot_damage_ratio: 0.2, cast_speed_penalty: 0.2 },
          },
          中毒: {
            类型: 'debuff',
            状态名称: '中毒',
            duration: 1,
            战斗效果: { ...createEmptyCombatEffectMap(), dot_damage: 16 },
          },
        };
        const result = settleConditionsAtRoundEnd(玩家, '玩家', combatData);
        断言战斗回归夹具(!/位移限制.*损失/.test(result.log || ''), `位移限制误触发生命流失:${result.log}`);
        断言战斗回归夹具(/中毒.*损失\s*16/.test(result.log || ''), `中毒持续伤害未正常结算:${result.log}`);
        断言战斗回归夹具(Number(玩家.hp) === 984, `位移限制和中毒疑似重复扣血:${玩家.hp};${result.log}`);
        日志.push(`位移限制不再复用中毒跳伤:${result.log}`);
      });
      注册('新附加持续状态下回合才跳伤', 日志 => {
        const { combatData, 玩家 } = 构建战斗回归夹具战斗态();
        玩家.HP = 1000;
        玩家.hp = 1000;
        玩家.HP上限 = 1000;
        玩家.hp_max = 1000;
        玩家.状态效果 = {
          中毒: {
            类型: 'debuff',
            状态名称: '中毒',
            __本回合新附加: true,
            duration: 1,
            战斗效果: { ...createEmptyCombatEffectMap(), dot_damage: 16 },
          },
        };
        const first = settleConditionsAtRoundEnd(玩家, '玩家', combatData);
        断言战斗回归夹具(!/中毒.*损失/.test(first.log || ''), `新附加中毒同回合误跳伤:${first.log}`);
        断言战斗回归夹具(Number(玩家.hp) === 1000, `新附加中毒同回合误扣血:${玩家.hp};${first.log}`);
        断言战斗回归夹具(玩家.状态效果?.中毒 && 玩家.状态效果.中毒.__本回合新附加 !== true && Number(玩家.状态效果.中毒.duration) === 1, `新附加中毒首回合状态错误:${JSON.stringify(玩家.状态效果?.中毒 || null)}`);
        const second = settleConditionsAtRoundEnd(玩家, '玩家', combatData);
        断言战斗回归夹具(/中毒.*损失\s*16/.test(second.log || ''), `中毒次回合未跳伤:${second.log}`);
        断言战斗回归夹具(Number(玩家.hp) === 984, `中毒次回合扣血异常:${玩家.hp};${second.log}`);
        断言战斗回归夹具(!玩家.状态效果?.中毒, `持续1回合中毒次回合后未结束:${JSON.stringify(玩家.状态效果?.中毒 || null)}`);
        日志.push(`新附加持续状态延后到下一回合结算:${first.log || '首回合无跳伤'} / ${second.log}`);
      });
      注册('重创流血首回合不立即跳伤', 日志 => {
        const { combatData, 玩家 } = 构建战斗回归夹具战斗态();
        玩家.HP = 1000;
        玩家.hp = 1000;
        玩家.HP上限 = 1000;
        玩家.hp_max = 1000;
        玩家.状态效果 = {
          重度流血: {
            类型: 'debuff',
            状态: '重度流血',
            状态名称: '重度流血',
            __本回合新附加: true,
            duration: 3,
            驱动属性: '魂力上限',
            效果摘要: '每回合损失80点生命',
            战斗效果: { ...createEmptyCombatEffectMap(), dot_damage: 80 },
          },
        };
        玩家.状态效果.重度流血.__状态来源键 = 记录状态来源登记(combatData, {
          stateName: '重度流血',
          targetName: 玩家.name,
          sourceActorName: '夹具敌人',
          sourceActionName: '重创打击',
          sourceActionType: 'attack',
          sourceRound: Number(combatData?.回合 || 0),
          duration: 3,
          effectSummary: '每回合损失80点生命',
          driverAttr: '魂力上限',
        });
        const first = settleConditionsAtRoundEnd(玩家, '玩家', combatData);
        断言战斗回归夹具(!/重度流血.*损失/.test(first.log || ''), `新附加重度流血同回合误跳伤:${first.log}`);
        断言战斗回归夹具(Number(玩家.hp) === 1000, `新附加重度流血同回合误扣血:${玩家.hp};${first.log}`);
        const second = settleConditionsAtRoundEnd(玩家, '玩家', combatData);
        断言战斗回归夹具(/重度流血.*损失\s*80/.test(second.log || ''), `重度流血次回合未跳伤:${second.log}`);
        断言战斗回归夹具(Number(玩家.hp) === 920, `重度流血次回合扣血异常:${玩家.hp};${second.log}`);
        日志.push(`重创流血延后到下一回合结算:${first.log || '首回合无跳伤'} / ${second.log}`);
      });
      注册('完整闪避链不触发本次状态结算', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.agi = 20;
        玩家.men_max = 20;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.agi = 9999;
        敌人.final = buildCombatFinalStats(敌人);
        const 闪避可躲控制 = normalizeSkillData({
          name: '夹具毒刃牵制',
          魂技名: '夹具毒刃牵制',
          技能分类: '输出',
          目标: '敌方单体',
          消耗: '无',
          前摇: 1,
          _效果数组: [
            { 原型: '伤害结算', 目标: '敌方单体', 生效方式: '独立生效', 威力倍率: 120, 伤害类型: '近身攻击' },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '独立生效', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.1, dot_damage: 16 } },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '独立生效', 状态: '中毒', 状态名称: '中毒', 持续回合: 1, 计算层效果: { dot_damage: 16 } },
          ],
        }, '夹具毒刃牵制');
        玩家.第1武魂.第1魂环.第1魂技 = 闪避可躲控制;
        const entry = {
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '夹具毒刃牵制', name: '夹具毒刃牵制', 消耗: '无' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
          target_name: '夹具敌人',
        };
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0;
          使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
            result = onPlayerAttack(`夹具毒刃牵制\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
              dryRun: true,
              mode: 'single_round',
              combatData,
              intentMode: '点到为止',
            });
          });
        } finally {
          Math.random = 原随机;
        }
        const logText = String(result?.logs?.join(' ') || result?.publicReport || '');
        断言战斗回归夹具(/主动闪避|绝对闪避|没有命中|规避了本次攻击/.test(logText), `完整链未形成完全闪避:${logText}`);
        断言战斗回归夹具(!/\[状态施加\].*(位移限制|中毒)|施加了\[(?:位移限制|中毒)\]/.test(logText), `完全闪避后仍施加本次状态:${logText}`);
        断言战斗回归夹具(!/\[状态结算\].*(位移限制|中毒)|受【(?:位移限制|中毒)】影响/.test(logText), `完全闪避后仍触发本次状态结算:${logText}`);
        日志.push(`完整闪避链未触发本次状态结算:${logText}`);
      });
      注册('完全闪避不触发敌对技能副作用状态', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.agi = 20;
        玩家.men_max = 20;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.agi = 9999;
        敌人.final = buildCombatFinalStats(敌人);
        const 闪避不吃后效 = normalizeSkillData({
          name: '夹具追毒飞刃',
          魂技名: '夹具追毒飞刃',
          技能分类: '输出',
          目标: '敌方单体',
          消耗: '无',
          前摇: 1,
          副作用列表: [
            { 副作用类型: '动作迟缓', 触发时机: '效果生效后', 生效对象: '效果承受者', 持续回合: 1, 数值: '20%', 副数值: '10%' },
          ],
          _效果数组: [
            { 原型: '伤害结算', 目标: '敌方单体', 生效方式: '独立生效', 威力倍率: 120, 伤害类型: '远程攻击' },
          ],
        }, '夹具追毒飞刃');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 闪避不吃后效, target_name: 敌人.name, cast_time: 1 };
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0;
          result = executeClash(
            action,
            { type: '伺机闪避', action_type: '伺机闪避', skill: normalizeSkillData({ name: '伺机闪避', 技能分类: '防御', 消耗: '无', 前摇: 1 }, '伺机闪避'), def_mult: 1, log: '[应招] 夹具敌人以[伺机闪避]应对。' },
            构建单挑临时战斗数据(玩家, 敌人, 'player', combatData),
          );
        } finally {
          Math.random = 原随机;
        }
        const 文本 = String(result?.desc || '');
        断言战斗回归夹具(/\[(?:主动闪避|绝对闪避)\]/.test(文本), `未形成完全闪避:${文本}`);
        断言战斗回归夹具(!/\[副作用\].*动作迟缓/.test(文本), `完全闪避后仍触发敌对技能副作用:${文本}`);
        断言战斗回归夹具(!敌人.状态效果?.动作迟缓, `完全闪避后敌对技能副作用仍落表:${JSON.stringify(敌人.状态效果 || {})}`);
        日志.push(`完全闪避未触发敌对技能副作用:${文本}`);
      });
      注册('群体攻击逐目标独立命中', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.name = '高速敌';
        敌人.agi = 10000;
        敌人.final = buildCombatFinalStats(敌人);
        const 低速敌 = 构建战斗回归夹具单位('低速敌', '防御系');
        低速敌.agi = 1;
        低速敌.def = 20;
        低速敌.final = buildCombatFinalStats(低速敌);
        combatData.参战者.team_enemy = [敌人, 低速敌];
        const 群体技能 = normalizeSkillData({
          name: '群体压制',
          魂技名: '群体压制',
          技能分类: '输出',
          目标: '敌方群体',
          消耗: '无',
          前摇: 10,
          _效果数组: [{ 原型: '伤害结算', 目标: '群体', 生效方式: '独立生效', 威力倍率: 90, 伤害类型: '远程攻击' }],
        }, '群体压制');
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0.01;
          result = executeClash(
            { action_type: '释放魂技', type: '释放魂技', skill: 群体技能, cast_time: 10 },
            { type: '无法反应', action_type: '无法反应', skill: null, def_mult: 1, log: '' },
            combatData,
          );
        } finally {
          Math.random = 原随机;
        }
        const 目标结果 = result?.targetResults || [];
        const 高速结果 = 目标结果.find(entry => entry.targetName === '高速敌');
        const 低速结果 = 目标结果.find(entry => entry.targetName === '低速敌');
        断言战斗回归夹具(目标结果.length === 2, `群体攻击未覆盖两个目标:${目标结果.length}`);
        断言战斗回归夹具(Number(高速结果?.damage || 0) === 0, `高速目标未独立闪避:${JSON.stringify(目标结果)}`);
        断言战斗回归夹具(Number(低速结果?.damage || 0) > 0, `低速目标未独立受击:${JSON.stringify(目标结果)}`);
        断言战斗回归夹具(/逐个独立结算/.test(String(result?.desc || '')), `群体战报未说明逐目标结算:${result?.desc || ''}`);
        日志.push(`群体逐目标结算成立:${目标结果.map(entry => `${entry.targetName}:${entry.damage}`).join('，')}`);
      });
      注册('索敌失败进入公开战报', 日志 => {
        const lines = buildReadableBattleReportLines([
          '[第2回合] [起招] 夹具玩家以[横扫压制]起招。 [索敌失败] 当前没有可被命中的有效目标。',
        ], 3);
        const text = lines.join(' ');
        断言战斗回归夹具(/没有可被命中的有效目标|未找到可被命中的有效目标/.test(text), `索敌失败未进公开战报:${text}`);
        断言战斗回归夹具(!/\[索敌失败\]/.test(text), `公开战报残留底层标签:${text}`);
        日志.push(`索敌失败战报成立:${text}`);
      });
      注册('未破防终态进入公开战报', 日志 => {
        const lines = buildReadableBattleReportLines([
          '[第2回合] [团战执行] 夹具玩家以[第一魂技]指向[夹具敌人]。 [起招] 夹具玩家以[第一魂技]起招。 [应招] 夹具敌人以[承伤硬抗]应对。 [未破防] 夹具玩家对夹具敌人仅造成 1 点强制伤害。',
        ], 3);
        const text = lines.join(' ');
        断言战斗回归夹具(/夹具玩家对夹具敌人造成了 1 点伤害/.test(text), `未破防终态未被聚合:${text}`);
        断言战斗回归夹具(!/仅记录起手动作|缺少命中|无法判断/.test(text), `未破防被误写成缺结算:${text}`);
        日志.push(`未破防终态战报成立:${text}`);
      });
      注册('第一魂技正常释放链路', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.第1武魂.第1魂环.第1魂技 = 构建战斗回归第一魂技(战斗回归第一魂技默认名, '魂力:120');
        敌人.agi = 80;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '慢反应武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('慢回击', '敌方单体', 18, 45, '近身攻击'),
          },
        };
        const entry = {
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技', name: '错误缓存第一魂技', 消耗: '魂力:9999' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        };
        const intent = `${战斗回归第一魂技默认名}\n[动作队列]${JSON.stringify([entry])}[/动作队列]`;
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0.99;
          使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
            result = onPlayerAttack(intent, { dryRun: true, combatData, intentMode: '点到为止' });
          });
        } finally {
          Math.random = 原随机;
        }
        const report = String(result?.publicReport || '');
        const rawLogText = String(result?.logs?.join(' ') || '');
        const playerSnapshot = result?.snapshot?.team_player?.find(unit => unit.name === '夹具玩家') || {};
        断言战斗回归夹具(report.includes(战斗回归第一魂技默认名), `正常释放战报缺第一魂技:${report}`);
        断言战斗回归夹具(/夹具敌人/.test(report), `正常释放战报缺目标:${report}`);
        断言战斗回归夹具(Number(playerSnapshot.sp) > 1800 && Number(playerSnapshot.sp) < 2000, `正常释放扣费异常:${playerSnapshot.sp}`);
        断言战斗回归夹具(Number(playerSnapshot.sp) === Number(playerSnapshot.魂力), `正常释放后魂力镜像不同步:${playerSnapshot.sp}/${playerSnapshot.魂力}`);
        断言战斗回归夹具(Number(playerSnapshot.sta ?? playerSnapshot.vit) === Number(playerSnapshot.体力), `正常释放后体力镜像不同步:${playerSnapshot.sta}/${playerSnapshot.vit}/${playerSnapshot.体力}`);
        断言战斗回归夹具((rawLogText.match(new RegExp(`\\[战前消耗\\]\\s*释放\\[${战斗回归第一魂技默认名}\\]`, 'g')) || []).length === 1, `第一魂技实际扣费次数异常:${rawLogText}`);
        断言战斗回归夹具((rawLogText.match(new RegExp(`\\[起招\\]\\s*夹具玩家以\\[${战斗回归第一魂技默认名}\\]起招`, 'g')) || []).length === 1, `第一魂技实际起招次数异常:${rawLogText}`);
        const 事实账本 = result?.closedLoopLedger || {};
        断言战斗回归夹具((事实账本.消耗 || []).filter(item => item.技能 === 战斗回归第一魂技默认名).length === 1, `账本扣费次数异常:${JSON.stringify(事实账本)}`);
        断言战斗回归夹具((事实账本.起招 || []).filter(item => item.行动者 === '夹具玩家' && item.技能 === 战斗回归第一魂技默认名).length === 1, `账本起招次数异常:${JSON.stringify(事实账本)}`);
        断言战斗回归夹具(!(事实账本.未闭合起招 || []).length, `正常释放存在未闭合起招:${JSON.stringify(事实账本.未闭合起招 || [])}`);
        const 事件账本 = Array.isArray(result?.eventLedger) ? result.eventLedger : [];
        断言战斗回归夹具(事件账本.some(item => item?.eventKind === 'action_start' && item?.actionName === 战斗回归第一魂技默认名), `事件账本缺动作起手:${JSON.stringify(事件账本)}`);
        const 第一魂技伤害事件 = 事件账本.filter(item => item?.eventKind === 'hit_result' && item?.actorName === '夹具玩家' && item?.targetName === '夹具敌人' && normalizeBattleActionDisplayName(item?.actionName || item?.sourceActionName || '') === 战斗回归第一魂技默认名 && Number(item?.appliedDamage || item?.meta?.appliedDamage || item?.damage || item?.meta?.damage || 0) > 0);
        const 第一魂技反击伤害事件 = 事件账本.filter(item =>
          item?.eventKind === 'counter' &&
          normalizeBattleActionDisplayName(item?.sourceActionName || item?.meta?.sourceActionName || '') === 战斗回归第一魂技默认名 &&
          Number(item?.appliedDamage || item?.meta?.appliedDamage || item?.damage || item?.meta?.damage || 0) > 0
        );
        const 第一魂技状态集合 = new Set(事件账本
          .filter(item => item?.eventKind === 'state_apply' && item?.actorName === '夹具玩家' && item?.targetName === '夹具敌人' && normalizeBattleActionDisplayName(item?.actionName || item?.sourceActionName || '') === 战斗回归第一魂技默认名)
          .map(item => String(item?.stateName || item?.meta?.stateName || '').trim())
          .filter(Boolean));
        断言战斗回归夹具(!第一魂技伤害事件.length, `第一魂技不应生成直接伤害:${JSON.stringify(第一魂技伤害事件)}`);
        断言战斗回归夹具(!第一魂技反击伤害事件.length, `第一魂技不应触发常规防反伤害:${JSON.stringify(第一魂技反击伤害事件)}`);
        断言战斗回归夹具(第一魂技状态集合.has('位移限制') && 第一魂技状态集合.has('中毒'), `第一魂技缺控制/中毒状态:${JSON.stringify(事件账本)}`);
        断言战斗回归夹具(!/错误缓存第一魂技|9999/.test(report), `正常释放串用缓存技能:${report}`);
        日志.push(`第一魂技正常释放成立:${report}`);
      });
      注册('闭环账本标记未闭合起招', 日志 => {
        const broken = 从事件账本构建行动闭环事实账本([
          { eventKind: 'action_start', round: 1, actorName: '甲', targetName: '乙', actionName: '裂空斩', actionType: 'attack', result: 'declared' },
          { eventKind: 'dodge', round: 1, actorName: '乙', actionName: '伺机闪避', actionType: 'dodge', result: 'attempted' },
        ]);
        const dodged = 从事件账本构建行动闭环事实账本([
          { eventKind: 'action_start', round: 1, actorName: '甲', targetName: '乙', actionName: '裂空斩', actionType: 'attack', result: 'declared' },
          { eventKind: 'dodge', round: 1, actorName: '乙', actionName: '伺机闪避', actionType: 'dodge', result: 'evaded' },
        ]);
        const hit = 从事件账本构建行动闭环事实账本([
          { eventKind: 'action_start', round: 1, actorName: '甲', targetName: '乙', actionName: '裂空斩', actionType: 'attack', result: 'declared' },
          { eventKind: 'defend', round: 1, actorName: '乙', actionName: '承伤硬抗', actionType: 'defend', result: 'guarded' },
          { eventKind: 'hit_result', round: 1, actorName: '甲', targetName: '乙', actionName: '裂空斩', actionType: 'attack', result: 'hit', meta: { damage: 80 } },
          { eventKind: 'state_apply', round: 1, actorName: '甲', targetName: '乙', actionName: '裂空斩', sourceActionName: '裂空斩', sourceRound: 1, actionType: 'state_apply', result: 'applied', meta: { stateName: '迟缓' } },
        ]);
        断言战斗回归夹具(broken.未闭合起招.length === 1, `未闭合起招未被账本捕捉:${JSON.stringify(broken)}`);
        断言战斗回归夹具(dodged.未闭合起招.length === 0 && dodged.闪避.length === 1, `完整闪避链被误判:${JSON.stringify(dodged)}`);
        断言战斗回归夹具(hit.未闭合起招.length === 0 && hit.命中.length === 1 && hit.状态施加.length === 1, `命中状态链账本异常:${JSON.stringify(hit)}`);
        日志.push('闭环账本可区分断链、闪避闭合、命中状态闭合');
      });
      注册('空效果技能不留下未闭合起招', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.第1武魂.第1魂环.第1魂技 = normalizeSkillData({
          name: '空壳第一魂技',
          魂技名: '空壳第一魂技',
          技能分类: '输出',
          目标: '敌方单体',
          消耗: '无',
          前摇: 1,
          _效果数组: [],
        }, '空壳第一魂技');
        const entry = {
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '空壳第一魂技', name: '空壳第一魂技', 消耗: '无' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
          target_name: 敌人.name,
        };
        const result = onPlayerAttack(`空壳第一魂技\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
          dryRun: true,
          mode: 'single_round',
          combatData,
          intentMode: '点到为止',
        });
        const logText = String(result?.logs?.join(' ') || '');
        const ledger = result?.closedLoopLedger || {};
        断言战斗回归夹具(/施法失败.*缺少可结算效果/.test(logText), `空效果技能未显式失败:${logText}`);
        断言战斗回归夹具(!(ledger.未闭合起招 || []).length, `空效果技能留下未闭合起招:${JSON.stringify(ledger.未闭合起招 || [])}`);
        日志.push(`空效果技能闭合为施法失败:${logText}`);
      });
      注册('自动续推不复用首轮手选魂技', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.HP = 99999;
        玩家.hp = 99999;
        玩家.HP上限 = 99999;
        玩家.hp_max = 99999;
        玩家.sp = 2000;
        玩家.魂力 = 2000;
        玩家.第1武魂.第1魂环.第1魂技 = {
          ...战斗回归输出魂技('续推轻击', '敌方单体', 4, 1, '近身攻击'),
          消耗: '魂力:120',
        };
        敌人.HP = 99999;
        敌人.hp = 99999;
        敌人.HP上限 = 99999;
        敌人.hp_max = 99999;
        敌人.agi = 1;
        敌人.def = 10;
        敌人.final = buildCombatFinalStats(敌人);
        const entry = {
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技', name: '错误缓存第一魂技', 消耗: '魂力:9999' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        };
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0.01;
          使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
            result = onPlayerAttack(`续推轻击\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
              dryRun: true,
              mode: 'multi_round',
              combatData,
              intentMode: '点到为止',
            });
          });
        } finally {
          Math.random = 原随机;
        }
        const 账本 = result?.closedLoopLedger || {};
        const 轨迹回合集合 = new Set((Array.isArray(result?.decisionTrace) ? result.decisionTrace : []).map(item => Number(item?.round || item?.回合 || 0)).filter(round => round > 0));
        const 首轮动作扣费 = (账本.消耗 || []).filter(item => item.技能 === '续推轻击').length;
        const 首轮动作起招 = (账本.起招 || []).filter(item => item.行动者 === '夹具玩家' && item.技能 === '续推轻击').length;
        断言战斗回归夹具(Number(result?.roundsExecuted || 0) > 1, `自动续推未进入第二回合:${result?.logs?.join(' ') || ''}`);
        断言战斗回归夹具(轨迹回合集合.has(2), `自动续推第2回合审计轨迹缺失:${JSON.stringify(Array.from(轨迹回合集合))}`);
        断言战斗回归夹具(首轮动作扣费 === 1, `自动续推重复扣首轮动作:${JSON.stringify(账本)}`);
        断言战斗回归夹具(首轮动作起招 === 1, `自动续推重复释放首轮动作:${JSON.stringify(账本)}`);
        断言战斗回归夹具(!/错误缓存第一魂技|9999/.test(String(result?.logs?.join(' ') || result?.publicReport || '')), '自动续推串用首轮缓存技能');
        日志.push(`自动续推未复用首轮手选魂技，推进${result?.roundsExecuted || 0}回合`);
      });
      注册('自动续推设置控制最大回合与伤害停推', 日志 => {
        const 构建低烈度战斗 = () => {
          const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
          玩家.HP = 99999;
          玩家.hp = 99999;
          玩家.HP上限 = 99999;
          玩家.hp_max = 99999;
          玩家.sp = 2000;
          玩家.魂力 = 2000;
          玩家.第1武魂.第1魂环.第1魂技 = {
            ...战斗回归输出魂技('续推轻击', '敌方单体', 4, 1, '近身攻击'),
            消耗: '魂力:120',
          };
          敌人.HP = 99999;
          敌人.hp = 99999;
          敌人.HP上限 = 99999;
          敌人.hp_max = 99999;
          敌人.agi = 1;
          敌人.def = 10;
          敌人.final = buildCombatFinalStats(敌人);
          return { combatData, 玩家, 敌人 };
        };
        const entry = {
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技', name: '错误缓存第一魂技', 消耗: '魂力:9999' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        };
        const intent = `续推轻击\n[动作队列]${JSON.stringify([entry])}[/动作队列]`;
        const 原随机 = Math.random;
        let maxRoundResult = null;
        let thresholdResult = null;
        try {
          Math.random = () => 0.99;
          const 第一场 = 构建低烈度战斗();
          使用战斗回归桥接(第一场.combatData, { 夹具玩家: 第一场.玩家, 夹具敌人: 第一场.敌人 }, () => {
            maxRoundResult = onPlayerAttack(intent, {
              dryRun: true,
              mode: 'multi_round',
              combatData: 第一场.combatData,
              intentMode: '点到为止',
              autoContinueConfig: { maxRounds: 2, stopDamagePercent: 100, continueChancePercent: 100 },
            });
          });
          const 第二场 = 构建低烈度战斗();
          使用战斗回归桥接(第二场.combatData, { 夹具玩家: 第二场.玩家, 夹具敌人: 第二场.敌人 }, () => {
            thresholdResult = onPlayerAttack(intent, {
              dryRun: true,
              mode: 'multi_round',
              combatData: 第二场.combatData,
              intentMode: '点到为止',
              autoContinueConfig: { maxRounds: 4, stopDamagePercent: 0, continueChancePercent: 100 },
            });
          });
        } finally {
          Math.random = 原随机;
        }
        断言战斗回归夹具(Number(maxRoundResult?.roundsExecuted || 0) === 2, `自定义最大回合未生效:${maxRoundResult?.logs?.join(' ') || ''}`);
        断言战斗回归夹具(Number(thresholdResult?.roundsExecuted || 0) === 1, `自定义伤害停推未生效:${thresholdResult?.logs?.join(' ') || ''}`);
        断言战斗回归夹具(/伤害已达生命占比0%/.test(String(thresholdResult?.logs?.join(' ') || '')), `伤害停推日志缺失:${thresholdResult?.logs?.join(' ') || ''}`);
        日志.push('自动续推最大回合、伤害停推与续推概率均可由运行时设置控制');
      });
      注册('自动续推不因同构攻防提前终止', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.HP = 99999;
        玩家.hp = 99999;
        玩家.HP上限 = 99999;
        玩家.hp_max = 99999;
        玩家.sp = 2000;
        玩家.魂力 = 2000;
        玩家.第1武魂.第1魂环.第1魂技 = {
          ...构建战斗回归第一魂技('锁魄印', '魂力:80'),
          前摇: 4,
          _效果数组: [
            { 原型: '状态施加', 目标: '单体', 状态名: '位移限制', 持续回合: 1, 计算层效果: { dodge_penalty: 0.12 } },
          ],
        };
        敌人.HP = 99999;
        敌人.hp = 99999;
        敌人.HP上限 = 99999;
        敌人.hp_max = 99999;
        敌人.第1武魂 = 敌人.第1武魂 || { 表象名称: '敌方夹具武魂' };
        敌人.第1武魂.第1魂环 = 敌人.第1武魂.第1魂环 || {};
        敌人.第1武魂.第1魂环.第1魂技 = normalizeSkillData(战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'), '敌方截击');
        敌人.第1武魂.第2魂环 = { 第1魂技: normalizeSkillData(战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'), '敌方压迫') };
        敌人.agi = Math.max(玩家.agi * 1.25, 280);
        敌人.final = buildCombatFinalStats(敌人);
        let result = null;
        const 原随机 = Math.random;
        try {
          Math.random = () => 0.01;
          使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
            result = onPlayerAttack('锁魄印', {
              dryRun: true,
              mode: 'multi_round',
              combatData,
              intentMode: '点到为止',
              autoContinueConfig: { maxRounds: 6, stopDamagePercent: 100, continueChancePercent: 100 },
            });
          });
        } finally {
          Math.random = 原随机;
        }
        const logText = String(result?.logs?.join(' ') || '');
        断言战斗回归夹具(!/续推终止.*连续/.test(logText), `连续态势仍触发提前停推:${logText}`);
        断言战斗回归夹具(Number(result?.roundsExecuted || 0) === 6, `同构攻防不应截断用户续推上限:${result?.roundsExecuted || 0};${logText}`);
        日志.push(`自动续推同构攻防未提前截断，执行${result?.roundsExecuted || 0}回合`);
      });
      注册('资源镜像回合尾同步', 日志 => {
        const { combatData, 玩家 } = 构建战斗回归夹具战斗态();
        玩家.sp = 1880;
        玩家.魂力 = 1880;
        玩家.men = 860;
        玩家.精神力 = 860;
        玩家.sta = 1100;
        玩家.vit = 1100;
        玩家.体力 = 1100;
        const result = settleConditionsAtRoundEnd(玩家, 玩家.name, combatData);
        syncCombatActionState(玩家);
        断言战斗回归夹具(Number(玩家.sp) === Number(玩家.魂力), `自然恢复后魂力镜像不同步:${玩家.sp}/${玩家.魂力};${result.log}`);
        断言战斗回归夹具(Number(玩家.men) === Number(玩家.精神力), `自然恢复后精神力镜像不同步:${玩家.men}/${玩家.精神力};${result.log}`);
        断言战斗回归夹具(Number(玩家.sta) === Number(玩家.vit) && Number(玩家.sta) === Number(玩家.体力), `回合尾体力镜像不同步:${玩家.sta}/${玩家.vit}/${玩家.体力}`);
        日志.push(`资源镜像回合尾同步成立:${result.log}`);
      });
      注册('第一魂技被先制打断不提前扣费', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.agi = 80;
        玩家.final = buildCombatFinalStats(玩家);
        玩家.第1武魂.第1魂环.第1魂技 = 构建战斗回归第一魂技('慢启动第一魂技', '魂力:120');
        玩家.第1武魂.第1魂环.第1魂技.前摇 = 64;
        敌人.agi = 360;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '先制武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('先制截击', '敌方单体', 4, 160, '近身攻击'),
          },
        };
        const entry = {
          actor_name: '夹具玩家',
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技', name: '错误缓存第一魂技', 消耗: '魂力:9999' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        };
        const intent = `慢启动第一魂技\n[动作队列]${JSON.stringify([entry])}[/动作队列]`;
        const 原随机 = Math.random;
        let result = null;
        try {
          Math.random = () => 0.99;
          使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
            result = onPlayerAttack(intent, { dryRun: true, combatData, intentMode: '点到为止' });
          });
        } finally {
          Math.random = 原随机;
        }
        const report = String(result?.publicReport || '');
        const playerSnapshot = result?.snapshot?.team_player?.find(unit => unit.name === '夹具玩家') || {};
        断言战斗回归夹具(/慢启动第一魂技/.test(report), `先制打断战报缺原技能:${report}`);
        断言战斗回归夹具(/未能(?:在本回合)?完成|打断|先手压制|来不及|仍在启动/.test(report), `先制打断战报未说明失败:${report}`);
        断言战斗回归夹具(Number(playerSnapshot.sp) === 2000, `先制打断前不应扣费:${playerSnapshot.sp}`);
        断言战斗回归夹具(!/错误缓存第一魂技|9999/.test(report), `先制打断串用缓存技能:${report}`);
        日志.push(`第一魂技先制打断成立:${report}`);
      });
      注册('非物品动作拒绝背包调用', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.背包.夹具药剂 = {
          数量: 1,
          持有者: 玩家.name,
          使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '魂力', 数值: '+100' }],
        };
        let result = null;
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          const 物品技能 = 构建战斗背包技能('夹具药剂', 读取战斗背包物品数据('夹具药剂', 玩家.背包.夹具药剂));
          result = executeClash(
            { action_type: '释放魂技', type: '释放魂技', skill: 物品技能, 物品名: '夹具药剂', cast_time: 8 },
            { type: '无法反应', action_type: '无法反应', skill: null, def_mult: 1, log: '' },
            combatData,
          );
        });
        断言战斗回归夹具(/只有选择使用物品/.test(String(result?.desc || '')), `非物品动作未拒绝:${result?.desc || ''}`);
        断言战斗回归夹具(!String(result?.desc || '').includes('[物品消耗]'), '非物品动作错误消耗背包物品');
        断言战斗回归夹具(!(result?.extraPatchOps || []).some(op => String(op?.path || '').includes('/背包/夹具药剂')), '非物品动作生成了背包消耗补丁');
        日志.push('非使用物品动作无法调用背包物品');
      });
      注册('使用物品才消费背包', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.背包.夹具药剂 = {
          数量: 1,
          持有者: 玩家.name,
          使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '魂力', 数值: '+100' }],
        };
        let result = null;
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          const 物品技能 = 构建战斗背包技能('夹具药剂', 读取战斗背包物品数据('夹具药剂', 玩家.背包.夹具药剂));
          result = executeClash(
            { action_type: '使用物品', type: '使用物品', skill: 物品技能, 物品名: '夹具药剂', cast_time: 8 },
            { type: '无法反应', action_type: '无法反应', skill: null, def_mult: 1, log: '' },
            combatData,
          );
        });
        const patchOps = result?.extraPatchOps || [];
        断言战斗回归夹具(String(result?.desc || '').includes('[物品消耗]'), `使用物品未写消耗战报:${result?.desc || ''}`);
        断言战斗回归夹具(patchOps.some(op => String(op?.path || '').includes('/char/夹具玩家/背包/夹具药剂')), '使用物品未生成背包消耗补丁');
        断言战斗回归夹具(!/只有选择使用物品/.test(String(result?.desc || '')), '合法使用物品被误判为非法调用');
        日志.push('使用物品动作才会消费背包物品');
      });
      注册('自动规划不调用背包物品', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.背包.夹具爆弹 = {
          数量: 1,
          持有者: 敌人.name,
          使用效果: [{ 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 300, 伤害类型: '远程攻击' }],
        };
        const availableSkills = collectCombatSkills(敌人, []);
        断言战斗回归夹具(!availableSkills.some(skill => String(skill?.__物品名 || '').trim() === '夹具爆弹'), '背包物品混入自动技能池');
        const npcActorEntry = { char: 敌人, side: 'enemy' };
        const npcTargets = chooseTargetForActor(npcActorEntry, { combatData }) || { enemyTarget: 玩家, allyTarget: 敌人 };
        const npcAction = buildAutoActionForActor(npcActorEntry, npcTargets, { combatData });
        断言战斗回归夹具(String(npcAction?.action_type || npcAction?.type || '') !== '使用物品', '自动规划直接调用了背包物品');
        断言战斗回归夹具(!String(npcAction?.物品名 || npcAction?.skill?.__物品名 || '').trim(), '自动规划动作携带了背包物品名');
        日志.push(`自动规划未调用背包物品:${npcAction?.skill?.name || npcAction?.action_type || npcAction?.type || '无动作'}`);
      });
      注册('团战战报多链不串联', 日志 => {
        const lines = buildReadableBattleReportLines([
          '[团战第1回合开始]',
          '[团战执行] 夹具玩家以[烈焰拳]指向[甲敌]。 [起招] 夹具玩家以[烈焰拳]起招。 [应招] 甲敌以[伺机闪避]应对。 [主动闪避] 甲敌凭借敏捷优势惊险躲过了攻击。',
          '[团战执行] 夹具友方以[冰锁]指向[乙敌]。 [起招] 夹具友方以[冰锁]起招。 [应招] 乙敌以[承伤硬抗]应对。 [命中结算] 夹具友方对乙敌造成 66 点最终伤害。',
          '[行为防反] 乙敌凭硬抗反击抓住夹具友方出手后的空门，造成33点反击伤害。',
        ], 8);
        const text = lines.join('\n');
        const playerLine = lines.find(line => /夹具玩家/.test(line)) || '';
        const allyLine = lines.find(line => /夹具友方/.test(line)) || '';
        断言战斗回归夹具(/甲敌/.test(playerLine), `玩家链缺甲敌:${text}`);
        断言战斗回归夹具(/乙敌[\s\S]*66/.test(allyLine), `友方链缺乙敌伤害:${text}`);
        断言战斗回归夹具(!/乙敌/.test(playerLine), `玩家链串到乙敌:${playerLine}`);
        断言战斗回归夹具(!/甲敌/.test(allyLine), `友方链串到甲敌:${allyLine}`);
        断言战斗回归夹具(/乙敌抓住夹具友方露出的破绽，以【Action_Missing】进行反击，造成了 33 点伤害/.test(text), `防反未挂回友方链:${text}`);
        日志.push(`团战多链战报成立:${text}`);
      });
      注册('多行动索敌失败不污染他人链', 日志 => {
        const lines = buildReadableBattleReportLines([
          '[团战第2回合开始]',
          '[团战执行] 夹具玩家以[横扫压制]指向[空位]。 [起招] 夹具玩家以[横扫压制]起招。 [索敌失败] 夹具玩家没有找到可被命中的有效目标。',
          '[团战执行] 夹具友方以[破甲刺]指向[乙敌]。 [起招] 夹具友方以[破甲刺]起招。 [应招] 乙敌以[承伤硬抗]应对。 [命中结算] 夹具友方对乙敌造成 72 点最终伤害。',
        ], 8);
        const text = lines.join('\n');
        const playerLine = lines.find(line => /夹具玩家/.test(line)) || '';
        const allyLine = lines.find(line => /夹具友方/.test(line)) || '';
        断言战斗回归夹具(/没有找到可被命中的有效目标|没有可被命中的有效目标/.test(playerLine), `玩家索敌失败未单独说明:${text}`);
        断言战斗回归夹具(!/乙敌|72/.test(playerLine), `索敌失败链串到友方命中:${playerLine}`);
        断言战斗回归夹具(/乙敌[\s\S]*72/.test(allyLine), `友方命中链缺失:${text}`);
        断言战斗回归夹具(!/索敌失败|\[索敌失败\]/.test(allyLine), `索敌失败污染友方链:${allyLine}`);
        日志.push(`多行动索敌失败隔离成立:${text}`);
      });
      注册('判定主卡隐藏内部术语', 日志 => {
        const rows = 构建判定流程展示数据([
          {
            type: '主动规划',
            行动者: '夹具玩家',
            目标: '夹具敌人',
            技能: '肉体兜底',
            回合: 1,
            最终权重: 30,
            选择原因: '无可用候选；候选池生成完毕，未检出资源或释放条件阻断',
            候选来源: '行为预演/主动战术阶段',
            候选排序结果: [{ 名称: '肉体兜底', 权重: 30 }, { 名称: '防御', 权重: 12 }],
          },
          {
            type: '主动规划',
            行动者: '夹具友方',
            目标: '',
            技能: '',
            回合: 1,
            选择原因: '无可用候选',
            候选来源: '行为预演/主动战术阶段',
            候选排序结果: [],
          },
          {
            type: '主动规划',
            行动者: '夹具友方',
            目标: '夹具敌人',
            技能: '普通攻击',
            回合: 1,
            最终权重: 50,
            选择原因: '集火',
            候选来源: '行为预演/战术阶段',
            候选排序结果: [{ 名称: '普通攻击', 权重: 50 }],
          },
        ], ['[第1回合] [团战执行] 夹具玩家以[伺机闪避]指向[夹具敌人]。']);
        const html = 渲染分回合判定流程(rows);
        const visible = html
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, '');
        断言战斗回归夹具(/原计划|改为|战术过渡|常规战术博弈/.test(visible), `主卡未显示动作流转事实:${visible}`);
        断言战斗回归夹具(/战术过渡|常规战术博弈/.test(visible), `流转主卡未弱化:${visible}`);
        断言战斗回归夹具(!/审计层|抽样候选|实战日志|权限流转|无可用候选|无资源失败|候选池生成完毕|行为预演\/战术阶段/.test(visible), `主卡泄漏内部术语:${visible}`);
        断言战斗回归夹具(!/battle-preview-debug|原始选择原因|执行校对证据/.test(html), '玩家态默认渲染了底层 Debug 明细');
        const 原开发态开关 = root.__LWCS_BATTLE_DEBUG_VISIBLE__;
        root.__LWCS_BATTLE_DEBUG_VISIBLE__ = true;
        const debugHtml = 渲染分回合判定流程(rows);
        root.__LWCS_BATTLE_DEBUG_VISIBLE__ = 原开发态开关;
        断言战斗回归夹具(/原始选择原因|执行校对证据/.test(debugHtml), '开发态 Debug 明细没有保留原始证据');
        日志.push(`判定主卡禁词成立:${visible}`);
      });
      注册('判定流程压缩重复并展示结算链', 日志 => {
        const traceList = [
          { type: '主动规划', round: 1, actor: '夹具玩家', target: '夹具敌人', skill: 战斗回归第一魂技默认名, 最终权重: 0, 选择原因: '当前未形成有效出手机会' },
          { type: '主动规划', round: 1, actor: '夹具玩家', target: '夹具敌人', skill: 战斗回归第一魂技默认名, 最终权重: 109, 选择原因: '压制收束' },
          { type: '再判定审计', round: 1, actor: '夹具玩家', target: '夹具敌人', skill: 战斗回归第一魂技默认名, 最终权重: 42, 选择原因: '行为链再判定' },
          { type: '主动规划', round: 1, actor: '夹具玩家', target: '夹具敌人', skill: 战斗回归第一魂技默认名, 最终权重: 105, 选择原因: '压制收束' },
          { type: '应招审计', round: 1, actor: '夹具敌人', target: '夹具玩家', skill: '伺机闪避', 最终权重: 20, 选择原因: '正式应招动作进入行为链' },
        ];
        const logs = [`[第1回合] [战前消耗] 释放[${战斗回归第一魂技默认名}]，自身扣除 魂力:120。 [起招] 夹具玩家以[${战斗回归第一魂技默认名}]起招。 [应招] 夹具敌人以[伺机闪避]应对。 [主动闪避] 夹具敌人凭借敏捷优势惊险躲过了攻击。`];
        const rows = [
          ...构建判定流程展示数据(traceList, logs),
          ...构建结算链侧写条目(logs),
        ];
        const html = 渲染分回合判定流程(rows);
        const visible = html.replace(/<[^>]+>/g, ' ');
        const firstSkillCount = (visible.match(new RegExp(`最终执行【${战斗回归第一魂技默认名}】`, 'g')) || []).length;
        断言战斗回归夹具(firstSkillCount <= 2, `第一魂技主卡重复刷屏:${firstSkillCount};${visible}`);
        断言战斗回归夹具(/结算链/.test(visible) && /闪避成功|没有命中|灵巧地闪避/.test(visible), `判定流程缺结算链:${visible}`);
        日志.push(`判定流程去重与结算链成立:${visible.slice(0, 180)}`);
      });
      注册('玩家手选动作补战术确立主卡', 日志 => {
        const logs = ['[第1回合] [团战执行] 唐凌雪以[锁魄印]指向[韦小枫]。 [起招] 唐凌雪以[锁魄印]起招。 [应招] 韦小枫以[承伤硬抗]应对。'];
        const rows = 构建判定流程展示数据([
          { type: '应招审计', round: 1, actor: '韦小枫', target: '唐凌雪', skill: '承伤硬抗', 最终权重: 28, 选择原因: '正式应招动作进入行为链' },
          { type: '再判定审计', round: 1, actor: '唐凌雪', target: '韦小枫', skill: '锁魄印', 最终权重: 44, 选择原因: '行为链再判定' },
        ], logs, { combatData: { 参战者: { team_player: [{ name: '唐凌雪' }], team_enemy: [{ name: '韦小枫' }] } } });
        const visible = 渲染分回合判定流程(rows).replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '').replace(/<[^>]+>/g, ' ');
        断言战斗回归夹具(/战术确立/.test(visible) && /唐凌雪/.test(visible) && /锁魄印/.test(visible), `玩家手选主卡未出现:${visible}`);
        日志.push(`玩家手选主卡成立:${visible.slice(0, 140)}`);
      });
      注册('行为链审计不被起招校正', 日志 => {
        const logs = [`[第1回合] [团战执行] 夹具玩家以[${战斗回归第一魂技默认名}]指向[夹具敌人]。 [起招] 夹具玩家以[${战斗回归第一魂技默认名}]起招。 [应招] 夹具敌人以[伺机闪避]应对。`];
        const rows = 构建判定流程展示数据([
          { type: '应招审计', round: 1, actor: '夹具敌人', target: '夹具玩家', skill: '承伤硬抗', 最终权重: 40, 选择原因: '正式应招动作进入行为链' },
          { type: '再判定审计', round: 1, actor: '夹具敌人', target: '夹具玩家', skill: '伺机闪避', 最终权重: 20, 选择原因: '行为链再判定' },
        ], logs);
        const html = 渲染分回合判定流程(rows);
        const visible = html.replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '').replace(/<[^>]+>/g, ' ');
        断言战斗回归夹具(!new RegExp(`临场变招|局势瞬息万变|战局变化后调整动作|最终执行【${战斗回归第一魂技默认名}】`).test(visible), `行为链审计被起招误校正或泄漏泛化变招:${visible}`);
        断言战斗回归夹具(/应招校验【承伤硬抗】|落点校验【伺机闪避】/.test(visible), `行为链校验未保留原动作:${visible}`);
        日志.push(`行为链审计不被起招校正成立:${visible.slice(0, 160)}`);
      });
      注册('判定流程按日志回合归桶行为链', 日志 => {
        const logs = [
          '[第1回合] [团战执行] 甲以[裂空斩]指向[乙]。 [起招] 甲以[裂空斩]起招。 [应招] 乙以[伺机闪避]应对。 [主动闪避] 乙惊险躲过了攻击。',
          '[第2回合] [团战执行] 甲以[回身突刺]指向[乙]。 [起招] 甲以[回身突刺]起招。 [应招] 乙以[承伤硬抗]应对。 [命中结算] 甲对乙造成 50 点最终伤害。',
        ];
        const rows = [
          ...构建判定流程展示数据([
            { type: '索敌规划', actor: '甲', target: '乙', skill: '', 最终权重: 18, 选择原因: '锁定目标' },
            { type: '主动规划', actor: '甲', target: '乙', skill: '裂空斩', 最终权重: 62, 选择原因: '压制收束' },
            { type: '应招审计', actor: '乙', target: '甲', skill: '伺机闪避', 最终权重: 24, 选择原因: '正式应招动作进入行为链' },
            { type: '主动规划', actor: '甲', target: '乙', skill: '回身突刺', 最终权重: 55, 选择原因: '继续追击' },
            { type: '应招审计', actor: '乙', target: '甲', skill: '承伤硬抗', 最终权重: 15, 选择原因: '正式应招动作进入行为链' },
          ], logs),
          ...构建结算链侧写条目(logs),
        ];
        const html = 渲染分回合判定流程(rows);
        const visible = html.replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '').replace(/<[^>]+>/g, ' ');
        断言战斗回归夹具(/第1回合[\s\S]*裂空斩[\s\S]*伺机闪避/.test(visible), `第1回合行为链未随日志归桶:${visible}`);
        断言战斗回归夹具(/第2回合[\s\S]*回身突刺[\s\S]*承伤硬抗/.test(visible), `第2回合行为链未随日志归桶:${visible}`);
        断言战斗回归夹具(!/其他判定片段[\s\S]*(裂空斩|回身突刺|伺机闪避|承伤硬抗)/.test(visible), `行为链仍堆入未分配片段:${visible}`);
        日志.push(`行为链按日志回合归桶成立:${visible.slice(0, 180)}`);
      });
      注册('后续回合守势主规划不再蒸发', 日志 => {
        const logs = [
          '[第1回合] [团战执行] 唐凌雪以[贴身冲拳]指向[韦小枫]。 [起招] 唐凌雪以[贴身冲拳]起招。 [应招] 韦小枫以[伺机闪避]应对。 [主动闪避] 韦小枫凭借敏捷优势惊险躲过了攻击。',
          '[团战第2回合开始]',
          '[第2回合] [起招] 韦小枫以[第1魂技]起招。 [应招] 唐凌雪以[收招转防]应对。',
        ];
        const traceList = [
          { type: '主动规划', round: 1, actor: '唐凌雪', target: '韦小枫', skill: '贴身冲拳', 最终权重: 58, 选择原因: '压制收束' },
          { type: '主动规划', round: 2, actor: '唐凌雪', target: '韦小枫', skill: '', 最终权重: 0, 选择原因: '收招转防；当前未形成有效出手机会', 候选排序结果: [{ 名称: '收招转防', 权重: 42 }] },
          { type: '应招审计', round: 2, actor: '唐凌雪', target: '韦小枫', skill: '收招转防', 最终权重: 18, 选择原因: '正式应招动作进入行为链' },
        ];
        const rows = [
          ...构建判定流程展示数据(traceList, logs),
          ...构建结算链侧写条目(logs),
        ];
        const html = 渲染分回合判定流程(rows);
        const visible = html.replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '').replace(/<[^>]+>/g, ' ');
        断言战斗回归夹具(/第2回合[\s\S]*主动规划[\s\S]*收招转防|第2回合[\s\S]*最终确认【收招转防】/.test(visible), `后续回合守势主规划仍蒸发:${visible}`);
        日志.push(`后续回合守势主规划保留:${visible.slice(0, 180)}`);
      });
      注册('表现层语义纠偏', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.HP上限 = 300;
        敌人.hp_max = 300;
        敌人.HP = 300;
        敌人.hp = 300;
        敌人.final = buildCombatFinalStats(敌人);
        const 无暇战报 = buildReadableBattleReportLines([
          '[第1回合]',
          '[团战执行] 夹具玩家以[精神震荡]指向[夹具敌人]。 [起招] 夹具玩家以[精神震荡]起招。 [应招] 夹具敌人以[无暇反应]应对。 [命中结算] 夹具玩家对夹具敌人造成 120 点最终伤害。 [状态施加] 夹具敌人获得[迟缓]。',
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/夹具敌人未能及时反应/.test(无暇战报), `无暇反应被主动化:${无暇战报}`);
        断言战斗回归夹具(!/以【无暇反应】应对/.test(无暇战报), `无暇反应仍按技能展示:${无暇战报}`);
        断言战斗回归夹具(/夹具玩家对夹具敌人造成了 120 点伤害/.test(无暇战报), `伤害主语不明确:${无暇战报}`);
        断言战斗回归夹具(/重创/.test(无暇战报), `战损烈度缺失:${无暇战报}`);
        断言战斗回归夹具(/夹具敌人陷入【迟缓】/.test(无暇战报), `状态未并入攻防链:${无暇战报}`);

        const 硬抗战报 = buildReadableBattleReportLines([
          '[第1回合]',
          '[团战执行] 夹具玩家以[裂地冲拳]指向[夹具敌人]。 [起招] 夹具玩家以[裂地冲拳]起招。 [应招] 夹具敌人以[承伤硬抗]应对。 [命中结算] 夹具玩家对夹具敌人造成 40 点最终伤害。',
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/收缩防线|稳住防线|承压|硬抗/.test(硬抗战报), `承伤硬抗未呈现主动防御语义:${硬抗战报}`);
        断言战斗回归夹具(!/避让空间不足，只能硬吃/.test(硬抗战报), `承伤硬抗仍被写成被动硬吃:${硬抗战报}`);
        断言战斗回归夹具(!/以【承伤硬抗】应对/.test(硬抗战报), `承伤硬抗仍保留主动化句式:${硬抗战报}`);
        断言战斗回归夹具(!/硬吃[^。；]*硬吃/.test(硬抗战报), `承伤硬抗文案重复:${硬抗战报}`);

        const 无伤硬抗战报 = buildReadableBattleReportLines([
          '[第2回合]',
          '[团战执行] 夹具玩家以[裂地冲拳]指向[夹具敌人]。 [起招] 夹具玩家以[裂地冲拳]起招。 [应招] 夹具敌人以[承伤硬抗]应对。',
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/未能造成实质伤害/.test(无伤硬抗战报), `硬吃无伤害未明确说明:${无伤硬抗战报}`);

        const 支援Rows = 构建判定流程展示数据([{
          type: '主动规划',
          行动者: '夹具玩家',
          目标: '夹具玩家',
          技能: '星辉护壁',
          回合: 1,
          目标语义: '自身',
          选择原因: '压制敌方续航点',
          目标理由: ['范围压制窗口打开', '安全距离仍可维持'],
          战术修正: 29,
          团队意图修正: 17,
          记忆惩罚: -3,
          最终权重: 62,
          候选排序结果: [{ 名称: '星辉护壁', 权重: 62 }, { 名称: '防御', 权重: 52 }],
        }]);
        const 支援可见 = 渲染分回合判定流程(支援Rows)
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, '');
        断言战斗回归夹具(!/压制敌方|范围压制|收割|击杀/.test(支援可见), `友方支援仍泄漏攻击理由:${支援可见}`);
        断言战斗回归夹具(/巩固防线|规避后续伤害/.test(支援可见), `友方支援缺防御语义:${支援可见}`);
        断言战斗回归夹具(/(?:施法对象：自身|作用目标：自身态势)（夹具玩家）/.test(支援可见), `自身目标未转为自然文案:${支援可见}`);
        断言战斗回归夹具(!/当前指向：【夹具玩家】|夹具玩家指向夹具玩家/.test(支援可见), `自身目标仍有机器自指:${支援可见}`);
        断言战斗回归夹具(!/主要加权|战术收益|团队意图|\(\+?\-?\d+\)|权衡：[^。！？]*\(\d+分\)/.test(支援可见), `支援主卡仍泄漏权重或计分板:${支援可见}`);

        const 压迫Rows = 构建判定流程展示数据([{
          type: '主动规划',
          行动者: '夹具敌人',
          目标: '夹具玩家',
          技能: '敌方压迫',
          回合: 1,
          目标语义: '敌方单体',
          选择原因: '压招',
          候选排序结果: [{ 名称: '敌方压迫', 权重: 64 }, { 名称: '普通攻击', 权重: 48 }],
        }]);
        const 压迫可见 = 渲染分回合判定流程(压迫Rows)
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, '');
        断言战斗回归夹具(/反制压制|逼位卡位/.test(压迫可见), `敌方压迫仍被泛化为控制:${压迫可见}`);
        断言战斗回归夹具(!/确认【控制】意图/.test(压迫可见), `敌方压迫意图仍过粗:${压迫可见}`);

        const 全场支援Rows = 构建判定流程展示数据([{
          type: '主动规划',
          行动者: '夹具玩家',
          目标: '夹具友方',
          技能: '全场鼓舞',
          hitCandidateName: '范围压制',
          finalResolvedActionName: '全场鼓舞',
          actionOverrideSource: '主动规划',
          回合: 1,
          目标语义: '友方群体',
          选择原因: '压制收束',
          目标理由: ['范围压制窗口打开'],
          战术修正: 48,
          最终权重: 274,
          候选排序结果: [{ 名称: '范围压制', 权重: 274 }, { 名称: '伺机闪避', 权重: 0 }],
        }]);
        const 全场支援可见 = 渲染分回合判定流程(全场支援Rows)
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, '');
        断言战斗回归夹具(!/先命中|战术窗口|随后落地/.test(全场支援可见), `支援技能泄漏候选覆写黑箱:${全场支援可见}`);
        断言战斗回归夹具(/支援对象：【夹具友方】/.test(全场支援可见), `全场支援仍未转为支援对象口径:${全场支援可见}`);
        断言战斗回归夹具(!/当前指向：【夹具友方】/.test(全场支援可见), `全场支援仍复用敌对目标口径:${全场支援可见}`);
        断言战斗回归夹具(!/最终执行【全场鼓舞】指向夹具友方/.test(全场支援可见), `全场支援仍复用敌对决断句式:${全场支援可见}`);
        断言战斗回归夹具(/最终以【全场鼓舞】巩固自身节奏/.test(全场支援可见), `全场支援未切入支援决断句式:${全场支援可见}`);

        const 压制Rows = 构建判定流程展示数据([{
          type: '主动规划',
          行动者: '夹具玩家',
          目标: '敌方治疗',
          技能: '普通攻击',
          回合: 1,
          目标语义: '敌方单体',
          选择原因: '集火',
          目标理由: ['治疗救场价值'],
          最终权重: 50,
          候选排序结果: [{ 名称: '普通攻击', 权重: 50 }, { 名称: '撤离', 权重: 12 }],
        }]);
        const 压制可见 = 渲染分回合判定流程(压制Rows)
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, '');
        断言战斗回归夹具(/压制敌方续航/.test(压制可见), `敌方续航压制被误删:${压制可见}`);

        const 造物Rows = 构建判定流程展示数据([{
          type: '主动规划',
          行动者: '唐凌雪',
          目标: '韦小枫',
          技能: '香喷喷牛肉干',
          回合: 4,
          目标语义: '造物承载',
          选择原因: '造物入包',
          最终权重: 42,
          候选排序结果: [{ 名称: '香喷喷牛肉干', 权重: 42 }],
        }]);
        const 造物可见 = 渲染分回合判定流程(造物Rows)
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, '');
        断言战斗回归夹具(/作用目标：造物承载/.test(造物可见), `造物动作仍复用敌方目标:${造物可见}`);
        断言战斗回归夹具(/完成造物承载|巩固自身节奏/.test(造物可见), `造物决断句仍未分流:${造物可见}`);
        断言战斗回归夹具(!/指向韦小枫/.test(造物可见), `造物动作仍错误指向敌方:${造物可见}`);

        const 守势Rows = 构建判定流程展示数据([
          {
            type: '主动规划',
            行动者: '夹具玩家',
            目标: '夹具敌人',
            技能: '收招转防',
            回合: 2,
            选择原因: '当前未形成有效出手机会',
            actionOverrideSource: '行为链再判定',
            候选排序结果: [{ 名称: '锁魄印', 权重: 55 }, { 名称: '收招转防', 权重: 42 }],
          },
          {
            type: '应招审计',
            行动者: '夹具玩家',
            目标: '夹具敌人',
            技能: '收招转防',
            回合: 2,
            选择原因: '正式应招动作进入行为链',
          },
        ]);
        const 守势可见 = 渲染分回合判定流程(守势Rows)
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, '');
        断言战斗回归夹具(/窗口已经消散|察觉战机已经溜走|主动收缩节奏/.test(守势可见), `守势退让仍只剩承压泛化语义:${守势可见}`);
        断言战斗回归夹具(/对照眼前攻势，确认以【收招转防】承住这一轮冲击/.test(守势可见), `应招审计语感未与主动规划拉开:${守势可见}`);
        日志.push(`表现层语义纠偏成立:${无暇战报} / ${支援可见} / ${守势可见}`);
      });
      注册('判定流程展示细化', 日志 => {
        const rows = 构建判定流程展示数据([
          {
            type: '索敌规划',
            行动者: '夹具敌人',
            目标: '夹具友方',
            回合: 1,
            选择原因: '目标优先表命中；唯一有效敌方目标',
            目标理由: ['后排/续航核心', '控制优先'],
          },
          {
            type: '辅助目标规划',
            行动者: '夹具玩家',
            目标: '夹具玩家',
            回合: 1,
            选择原因: '友方目标优先表命中；常规观察',
          },
          {
            type: '主动规划',
            行动者: '夹具玩家',
            目标: '甲敌',
            技能: '',
            回合: 1,
            选择原因: '当前未形成有效出手机会',
          },
          {
            type: '主动规划',
            行动者: '夹具玩家',
            目标: '乙敌',
            技能: '',
            回合: 1,
            选择原因: '常规方案收益过低',
          },
          {
            type: '主动规划',
            行动者: '夹具玩家',
            目标: '',
            技能: '',
            回合: 1,
            选择原因: '当前未形成有效出手机会',
          },
          {
            type: '主动规划',
            行动者: '夹具玩家',
            目标: '丙敌',
            技能: '裂空斩',
            回合: 1,
            选择原因: '压制收束',
            战术修正: -12,
            repeatCount: 2,
            repeatCategory: 'same_action',
            repeatDecayMultiplier: 0.1,
            repeatDecayReasonCode: 'SAME_ACTION_REPEAT_HEAVY',
            scoreBeforeRepeatDecay: 660,
            scoreAfterRepeatDecay: 66,
            候选排序结果: [
              { 名称: '裂空斩', 权重: 66, repeatCount: 2, repeatCategory: 'same_action', repeatDecayMultiplier: 0.1, repeatDecayReasonCode: 'SAME_ACTION_REPEAT_HEAVY', scoreBeforeRepeatDecay: 660, scoreAfterRepeatDecay: 66 },
              { 名称: '伺机闪避', 权重: 52 },
            ],
            最终权重: 66,
          },
          {
            type: '再判定审计',
            行动者: '夹具玩家',
            目标: '丙敌',
            技能: '裂空斩',
            回合: 1,
            选择原因: '放弃再判定',
            候选排序结果: [
              { 名称: '裂空斩', 权重: 44 },
              { 名称: '普通攻击', 权重: 30 },
            ],
            最终权重: 44,
          },
        ], []);
        const html = 渲染分回合判定流程([
          ...rows,
          {
            type: 'trace',
            trace: {
              type: '防反机制',
              行动者: '夹具敌人',
              目标: '夹具玩家',
              回合: 1,
              result: 'success',
              sourceActionName: '',
              damage: 33,
            },
          },
          {
            type: '防反机制',
            类型: '防反机制',
            行动者: '夹具友方',
            目标: '',
            回合: 1,
            roundPhase: 'action_result',
            phaseBucket: 'action_result',
            sourceActionName: '短刀',
            sourceActionType: 'counter',
            failReason: '距离不够',
            result: 'fail',
          },
        ]);
        const visible = html
          .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '')
          .replace(/<[^>]+>/g, ' ');
        断言战斗回归夹具(!/辅助目标规划/.test(visible), `无效辅助规划仍上屏:${visible}`);
        断言战斗回归夹具(/锁定对手/.test(visible) && /夹具友方/.test(visible), `索敌主卡仍未转为对手口径:${visible}`);
        断言战斗回归夹具(!/锁定目标/.test(visible), `索敌主卡仍在使用机械目标口径:${visible}`);
        断言战斗回归夹具(!/试图掌住战局节奏/.test(visible), `目标规划仍泄漏泛化战略词:${visible}`);
        断言战斗回归夹具(/目标遍历详情/.test(visible), `多目标失败推演未折叠:${visible}`);
        断言战斗回归夹具(/中间推演/.test(visible), `中间推演未归并:${visible}`);
        断言战斗回归夹具(!/重复使用惩罚|记忆惩罚/.test(visible), `重复衰减仍显示为线性惩罚:${visible}`);
        断言战斗回归夹具(/重复释放衰减\s*x0\.10/.test(visible), `重复衰减倍率未展示:${visible}`);
        断言战斗回归夹具(!/直接采用【裂空斩】/.test(visible), `放弃再判定仍映射为直接采用:${visible}`);
        断言战斗回归夹具(/动作沿用原计划/.test(visible), `keep_original 未改为事实语气:${visible}`);
        断言战斗回归夹具(!/未发现更优解|结合当前局势|战术倾向保留/.test(visible), `keep_original 仍泄漏黑箱解释:${visible}`);
        断言战斗回归夹具(/抓住\s*夹具玩家\s*露出的破绽，以\s*【Action_Missing】\s*进行反击[^。]*33\s*点伤害/.test(visible), `防反成功缺动作来源:${visible}`);
        断言战斗回归夹具(/尝试以\s*【短刀】\s*反打，但距离不够/.test(visible), `防反失败原因未展示:${visible}`);
        日志.push(`判定流程展示细化成立:${visible.slice(0, 220)}`);
      });
      注册('展示层P1护栏回归', 日志 => {
        const rows = 构建判定流程展示数据([
          {
            type: '应招审计',
            行动者: '夹具敌人',
            目标: '夹具玩家',
            技能: '无法反应',
            回合: 1,
            最终权重: 0,
            选择原因: '正式应招动作进入行为链',
            候选排序结果: [{ 名称: '无法反应', 权重: 0 }, { 名称: '承伤硬抗', 权重: 0 }],
          },
          {
            type: '再判定审计',
            行动者: '夹具玩家',
            目标: '夹具敌人',
            技能: '流火散射',
            回合: 1,
            最终权重: 66,
            选择原因: '战术重心锁定',
            候选排序结果: [
              { 名称: '抢落点', 权重: 66, candidateStatus: 'EXECUTED' },
              { 名称: '放弃再判定', 权重: 20, candidateStatus: 'REJECTED', rejectionCode: 'LOWER_PRIORITY' },
            ],
          },
        ], []);
        const settlementRows = 构建结算链侧写条目([
          '[第1回合] [命中结算] 夹具玩家对夹具敌人造成 88 点最终伤害。 [状态结算] 夹具敌人受[重度流血]影响，额外损失 18 点HP',
        ], {
          combatData: {
            参战者: {
              team_player: [{ name: '夹具玩家' }],
              team_enemy: [{ name: '夹具敌人' }],
            },
          },
        });
        const counterRows = [{
          type: 'trace',
          trace: {
            type: '防反机制',
            行动者: '夹具敌人',
            目标: '夹具玩家',
            回合: 1,
            result: 'success',
            sourceActionName: '',
            damage: 33,
          },
        }];
        const html = 渲染分回合判定流程([...rows, ...counterRows, ...settlementRows]);
        const visible = html.replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/g, '').replace(/<[^>]+>/g, ' ');
        断言战斗回归夹具(/无法反应/.test(visible) && !/评分为0/.test(visible), `0分审计仍泄漏评分口径:${visible}`);
        断言战斗回归夹具(/▱▱▱▱▱▱▱▱▱▱/.test(visible), `0分候选条未渲染为空条:${visible}`);
        断言战斗回归夹具(/回合结算/.test(visible) && /重度流血/.test(visible), `状态结算未进入独立容器:${visible}`);
        断言战斗回归夹具(/【Action_Missing】/.test(visible), `防反缺动作名未显性占位:${visible}`);
        断言战斗回归夹具(!/战术推演池|原始:\d+/.test(visible), `再判定仍泄漏旧候选池格式:${visible}`);
        断言战斗回归夹具(!/维持原定计划使用【流火散射】/.test(visible), `抢落点命中仍被映射为维持原案:${visible}`);
        日志.push(`展示层P1护栏成立:${visible.slice(0, 240)}`);
      });
      注册('全场纯增益不触发敌对', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 全场增益 = normalizeSkillData({
          name: '全场鼓舞',
          魂技名: '全场鼓舞',
          技能分类: '辅助',
          目标: '全场',
          消耗: '无',
          前摇: 6,
          _效果数组: [
            { 原型: '属性修正', 目标: '全场', 数值: '+20', 属性: '防御' },
            { 原型: '护盾变化', 目标: '全场', 数值: '+50' },
          ],
        }, '全场鼓舞');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 全场增益, cast_time: 6 };
        断言战斗回归夹具(判定单挑动作敌对(action, 玩家, 敌人, combatData) === false, '全场纯增益被误判敌对');
        const targetContext = resolveSkillTargetContext(全场增益, 玩家, 敌人, combatData, 全场增益._效果数组[0]);
        断言战斗回归夹具(targetContext.targetSet.some(unit => unit.name === '夹具玩家'), '全场增益未覆盖施术者');
        断言战斗回归夹具(targetContext.targetSet.some(unit => unit.name === '夹具敌人'), '全场增益未覆盖场上其他单位');
        日志.push(`全场纯增益非敌对成立:${targetContext.targetSet.map(unit => unit.name).join('、')}`);
      });
      注册('全场伤害触发敌对', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 全场伤害 = normalizeSkillData({
          name: '全场震荡',
          魂技名: '全场震荡',
          技能分类: '输出',
          目标: '全场',
          消耗: '无',
          前摇: 6,
          _效果数组: [
            { 原型: '伤害结算', 目标: '全场', 生效方式: '独立生效', 威力倍率: 40, 伤害类型: '远程攻击' },
          ],
        }, '全场震荡');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 全场伤害, cast_time: 6 };
        断言战斗回归夹具(判定单挑动作敌对(action, 玩家, 敌人, combatData) === true, '全场伤害未判定为敌对');
        const targetContext = resolveSkillTargetContext(全场伤害, 玩家, 敌人, combatData, 全场伤害._效果数组[0]);
        断言战斗回归夹具(targetContext.targetSet.some(unit => unit.name === '夹具玩家'), '全场伤害未覆盖玩家侧');
        断言战斗回归夹具(targetContext.targetSet.some(unit => unit.name === '夹具敌人'), '全场伤害未覆盖敌方侧');
        日志.push(`全场伤害敌对成立:${targetContext.targetSet.map(unit => unit.name).join('、')}`);
      });
      注册('NPC敌方削弱目标取玩家侧', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 削弱 = normalizeSkillData({
          name: '敌方削弱',
          魂技名: '敌方削弱',
          技能分类: '削弱',
          目标: '敌方单体',
          消耗: '无',
          前摇: 8,
          _效果数组: [
            { 原型: '属性修正', 目标: '敌方单体', 数值: '-30', 属性: '防御' },
          ],
        }, '敌方削弱');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 削弱, target_name: 玩家.name, cast_time: 8 };
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          const supportCost = resolveSupportCostContext(敌人, action);
          断言战斗回归夹具(supportCost.目标?.name === '夹具玩家', `NPC敌方单体辅助上下文未取玩家侧:${supportCost.目标?.name || '无'}`);
          const actualTarget = 解析动作实际覆盖目标上下文(敌人, action, 玩家, combatData);
          断言战斗回归夹具(actualTarget.目标?.name === '夹具玩家', `NPC敌方削弱实际目标未取玩家侧:${actualTarget.目标?.name || '无'}`);
          断言战斗回归夹具(!actualTarget.目标列表.some(unit => unit.name === '夹具敌人'), 'NPC敌方削弱误覆盖自己队伍');
          日志.push(`NPC敌方削弱目标成立:${actualTarget.目标列表.map(unit => unit.name).join('、') || actualTarget.目标?.name}`);
        });
      });
      注册('控制削弱完成后扣费落状态', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 控制 = normalizeSkillData({
          name: '夹具封技',
          魂技名: '夹具封技',
          技能分类: '控制',
          目标: '敌方单体',
          消耗: '魂力:120',
          前摇: 8,
          _效果数组: [
            { 原型: '状态施加', 目标: '敌方单体', 状态: '封技夹具', 状态名称: '封技夹具', 持续回合: 2, 计算层效果: { skill_seal: true } },
          ],
        }, '夹具封技');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 控制, target_name: 敌人.name, cast_time: 8 };
        let costLog = '';
        let result = null;
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          costLog = applyActionCost(玩家, action, 敌人, combatData);
          result = executeClash(
            action,
            构建单挑反应动作({ action_type: '无法反应', type: '无法反应' }, 敌人, 玩家),
            构建单挑临时战斗数据(玩家, 敌人, 'player', combatData),
          );
        });
        断言战斗回归夹具(/战前消耗/.test(costLog), `控制完成未扣费:${costLog}`);
        断言战斗回归夹具(Number(玩家.sp) === 1880 && Number(玩家.魂力) === 1880, `控制扣费异常:${玩家.sp}/${玩家.魂力}`);
        断言战斗回归夹具(!!敌人.状态效果?.封技夹具, `控制状态未落地:${result?.desc || ''}`);
        断言战斗回归夹具(/封技夹具/.test(String(result?.desc || '')), `控制战报缺状态:${result?.desc || ''}`);
        日志.push(`控制完成扣费落状态成立:${costLog} ${result?.desc || ''}`);
      });
      注册('控制资源不足不扣费不落地', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.sp = 50;
        玩家.魂力 = 50;
        const 控制 = normalizeSkillData({
          name: '高耗封技',
          魂技名: '高耗封技',
          技能分类: '控制',
          目标: '敌方单体',
          消耗: '魂力:120',
          前摇: 8,
          _效果数组: [
            { 原型: '状态施加', 目标: '敌方单体', 状态: '高耗封技状态', 状态名称: '高耗封技状态', 持续回合: 2, 计算层效果: { skill_seal: true } },
          ],
        }, '高耗封技');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 控制, target_name: 敌人.name, cast_time: 8 };
        let costLog = '';
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          costLog = applyActionCost(玩家, action, 敌人, combatData);
        });
        断言战斗回归夹具(action.action_type === '施法失败', '资源不足控制未标记失败');
        断言战斗回归夹具(Number(玩家.sp) === 50 && Number(玩家.魂力) === 50, `资源不足仍扣费:${玩家.sp}/${玩家.魂力}`);
        断言战斗回归夹具(!敌人.状态效果?.高耗封技状态, '资源不足控制提前落状态');
        断言战斗回归夹具(/状态枯竭|无法支撑|施法失败/.test(costLog), `资源不足日志不明确:${costLog}`);
        日志.push(`控制资源不足阻断成立:${costLog}`);
      });
      注册('施法失败误入结算不落状态', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 控制 = normalizeSkillData({
          name: '失败封技',
          魂技名: '失败封技',
          技能分类: '控制',
          目标: '敌方单体',
          消耗: '魂力:120',
          前摇: 8,
          _效果数组: [
            { 原型: '状态施加', 目标: '敌方单体', 状态: '失败不应落地', 状态名称: '失败不应落地', 持续回合: 2, 计算层效果: { skill_seal: true } },
          ],
        }, '失败封技');
        const action = { action_type: '施法失败', type: '释放魂技', skill: 控制, target_name: 敌人.name, cast_time: 8 };
        let result = null;
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人 }, () => {
          result = executeClash(action, 构建单挑配合动作(玩家, 敌人, action), 构建单挑临时战斗数据(玩家, 敌人, 'player', combatData));
        });
        断言战斗回归夹具(Number(玩家.sp) === 2000 && Number(玩家.魂力) === 2000, `失败动作误扣费:${玩家.sp}/${玩家.魂力}`);
        断言战斗回归夹具(!敌人.状态效果?.['失败不应落地'], `失败动作误落状态:${result?.desc || ''}`);
        断言战斗回归夹具(/未完成|失败|取消/.test(String(result?.desc || '')), `失败动作结算日志不明确:${result?.desc || ''}`);
        日志.push(`施法失败误入结算被拦截:${result?.desc || ''}`);
      });
      注册('团战NPC友方辅助不串敌我', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.name = '夹具敌方辅助';
        敌人.type = '辅助系';
        敌人.sp = 2000;
        敌人.魂力 = 2000;
        敌人.final = buildCombatFinalStats(敌人);
        const 敌方残血队友 = 构建战斗回归夹具单位('夹具敌方残血', '强攻系');
        敌方残血队友.hp = 200;
        敌方残血队友.HP = 200;
        敌方残血队友.final = buildCombatFinalStats(敌方残血队友);
        combatData.参战者.team_enemy = [敌人, 敌方残血队友];
        敌人.第1武魂 = {
          表象名称: '辅助夹具武魂',
          第1魂环: {
            第1魂技: normalizeSkillData({
              name: '夹具护援',
              魂技名: '夹具护援',
              技能分类: '辅助',
              目标: '友方单体',
              消耗: '魂力:80',
              前摇: 6,
              _效果数组: [
                { 原型: '状态施加', 目标: '友方单体', 状态: '护盾', 状态名称: '护援夹具', 数值: 120, 持续回合: 2 },
              ],
            }, '夹具护援'),
          },
        };
        const result = 使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌方辅助: 敌人, 夹具敌方残血: 敌方残血队友 }, () => {
          return runActorTurn({ char: 敌人, side: 'enemy' }, { combatData, observedTargetAction: { action_type: '防御', type: '防御', skill: normalizeSkillData({ name: '防御', 技能分类: '防御', 消耗: '无' }, '防御') } });
        });
        断言战斗回归夹具(/夹具护援|支援|辅助|护援/.test(String(result?.log || '')), `NPC辅助行动日志缺支援:${result?.log || ''}`);
        断言战斗回归夹具(Number(敌人.sp) < 2000 && Number(敌人.sp) > 1800, `NPC辅助扣费异常:${敌人.sp};${result?.log || ''}`);
        断言战斗回归夹具(
          !!敌方残血队友.状态效果?.护盾 && String(敌方残血队友.状态效果.护盾?.来源技能 || '').includes('夹具护援'),
          `友方辅助未落到敌方队友:${result?.log || ''}`,
        );
        断言战斗回归夹具(!玩家.状态效果?.护盾, 'NPC友方辅助误落到玩家');
        断言战斗回归夹具(!/夹具敌方辅助对夹具敌方残血造成|命中结算.*夹具敌方辅助对夹具敌方残血/.test(String(result?.log || '')), `NPC辅助被当成攻击队友:${result?.log || ''}`);
        日志.push(`团战NPC友方辅助不串敌我成立:${result?.log || ''}`);
      });
      注册('友方群体护盾只落己方', 日志 => {
        const { combatData, 玩家, 友方, 敌人 } = 构建战斗回归夹具战斗态();
        const 群体护盾 = normalizeSkillData({
          name: '夹具群体护盾',
          魂技名: '夹具群体护盾',
          技能分类: '辅助',
          目标: '友方群体',
          消耗: '魂力:60',
          前摇: 6,
          _效果数组: [
            { 原型: '护盾变化', 目标: '友方群体', 数值: '+80', 持续回合: 2 },
          ],
        }, '夹具群体护盾');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 群体护盾, target_name: 玩家.name, cast_time: 6 };
        let result = null;
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具友方: 友方, 夹具敌人: 敌人 }, () => {
          result = executeClash(action, 构建单挑配合动作(玩家, 玩家, action), 构建单挑临时战斗数据(玩家, 玩家, 'player', combatData));
        });
        断言战斗回归夹具(Number(玩家.状态效果?.夹具群体护盾?.shield_value || 0) > 0, `群体护盾未落到施术者:${result?.desc || ''}`);
        断言战斗回归夹具(Number(友方.状态效果?.夹具群体护盾?.shield_value || 0) > 0, `群体护盾未落到友方:${result?.desc || ''}`);
        断言战斗回归夹具(!敌人.状态效果?.夹具群体护盾, '友方群体护盾误落到敌方');
        日志.push(`友方群体护盾落点成立:${result?.desc || ''}`);
      });
      注册('敌方群体伤害跟随状态逐目标', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.agi = 9999;
        玩家.men_max = 9999;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.agi = 1;
        敌人.def = 500;
        敌人.final = buildCombatFinalStats(敌人);
        const 敌方二号 = 构建战斗回归夹具单位('夹具敌方二号', '强攻系');
        敌方二号.agi = 1;
        敌方二号.def = 500;
        敌方二号.final = buildCombatFinalStats(敌方二号);
        combatData.参战者.team_enemy = [敌人, 敌方二号];
        const 群体压制 = normalizeSkillData({
          name: '夹具群体压制',
          魂技名: '夹具群体压制',
          技能分类: '输出',
          目标: '敌方群体',
          消耗: '魂力:50',
          前摇: 8,
          _效果数组: [
            { 原型: '伤害结算', 目标: '敌方群体', 生效方式: '独立生效', 威力倍率: 10, 伤害类型: '远程攻击' },
            { 原型: '状态施加', 目标: '敌方群体', 生效方式: '跟随主原型', 状态: '迟缓', 数值: '+10%', 持续回合: 2 },
          ],
        }, '夹具群体压制');
        const action = { action_type: '释放魂技', type: '释放魂技', skill: 群体压制, target_name: 敌人.name, cast_time: 8 };
        let result = null;
        使用战斗回归桥接(combatData, { 夹具玩家: 玩家, 夹具敌人: 敌人, 夹具敌方二号: 敌方二号 }, () => {
          result = executeClash(action, 构建单挑反应动作({ action_type: '防御', type: '防御' }, 敌人, 玩家), 构建单挑临时战斗数据(玩家, 敌人, 'player', combatData));
        });
        const 命中目标名 = (result?.targetResults || []).filter(entry => Number(entry?.damage || 0) > 0).map(entry => entry.target?.name || entry.targetName || '');
        断言战斗回归夹具(命中目标名.includes('夹具敌人') && 命中目标名.includes('夹具敌方二号'), `群体伤害未逐目标命中:${result?.desc || ''}`);
        const 目标结果去重 = new Set((result?.targetResults || []).map(entry => entry.target?.name || entry.targetName || '').filter(Boolean));
        断言战斗回归夹具(目标结果去重.size === 2 && (result?.targetResults || []).length === 2, `群体结算目标重复:${(result?.targetResults || []).map(entry => entry.target?.name || entry.targetName || '').join('、')};${result?.desc || ''}`);
        断言战斗回归夹具(!!敌人.状态效果?.迟缓 && !!敌方二号.状态效果?.迟缓, `跟随状态未落到全部命中目标:${result?.desc || ''}`);
        断言战斗回归夹具(!玩家.状态效果?.迟缓, '敌方群体跟随状态误落到玩家');
        日志.push(`敌方群体伤害跟随状态成立:${result?.desc || ''}`);
      });
      注册('护盾先吸收再扣血', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        applyShieldToCharacter(敌人, 80, 2, '夹具护盾');
        const beforeHp = getCombatHpValue(敌人);
        const action = { action_type: '释放魂技', type: '释放魂技', skill: normalizeSkillData(战斗回归输出魂技('夹具破盾击', '敌方单体', 8, 80, '近身攻击'), '夹具破盾击') };
        const settleResult = {
          dmg: 120,
          desc: '',
          targetResults: [{ target: 敌人, targetName: 敌人.name, damage: 120, kind: 'primary' }],
        };
        const applied = applyResolvedDamagePackage(玩家, action, settleResult, { primaryTarget: 敌人, combatData });
        const lostHp = beforeHp - getCombatHpValue(敌人);
        断言战斗回归夹具(lostHp === 40, `护盾吸收后扣血异常:${lostHp};${applied.log || ''}`);
        断言战斗回归夹具(!敌人.状态效果?.夹具护盾, '护盾耗尽后未移除');
        断言战斗回归夹具(/护盾吸收/.test(applied.log || ''), `护盾吸收日志缺失:${applied.log || ''}`);
        日志.push(`护盾吸收顺序成立:${applied.log || ''}`);
      });
      注册('UI动作声明契约不丢字段', 日志 => {
        const { combatData, 玩家, 友方, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.自创魂技 = {
          夹具援护: {
            魂技名: '夹具援护',
            name: '夹具援护',
            技能分类: '辅助',
            目标: '友方单体',
            消耗: '魂力:30',
            前摇: 6,
            使用条件: { 最低等级: 1 },
            _效果数组: [
              { 原型: '护盾变化', 目标: '友方单体', 数值: '+80' },
            ],
          },
        };
        玩家.背包.夹具药剂 = {
          名称: '夹具药剂',
          数量: 1,
          使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '魂力', 数值: '+10' }],
        };
        const actions = ui_getAvailableActions(玩家, combatData);
        const firstAction = actions.find(action => action.name === 战斗回归第一魂技默认名);
        const supportAction = actions.find(action => action.name === '夹具援护');
        const itemAction = actions.find(action => action.name === '夹具药剂');
        断言战斗回归夹具(!!firstAction && !!supportAction && !!itemAction, `UI动作缺失:${actions.map(action => action.name).join('、')}`);
        supportAction.target_name = 友方.name;
        const 序列化 = action => buildSerializedEntryFromAction(action);
        const firstEntry = 序列化(firstAction);
        const supportEntry = 序列化(supportAction);
        const itemEntry = 序列化(itemAction);
        断言战斗回归夹具(firstEntry.actor_name === 玩家.name, '第一魂技UI声明缺行动者');
        断言战斗回归夹具(firstEntry.__魂环路径?.join('/') === '第1武魂/第1魂环' && firstEntry.__魂技槽位 === '第1魂技', '第一魂技UI声明缺魂环槽位');
        断言战斗回归夹具(firstEntry.skill?.目标 === '敌方单体' && firstEntry.skill?.技能分类 === '控制', '第一魂技UI声明缺目标/分类');
        断言战斗回归夹具(supportEntry.target_name === 友方.name, `友方辅助UI声明目标错误:${supportEntry.target_name}`);
        断言战斗回归夹具(supportEntry.skill?.目标 === '友方单体' && supportEntry.skill?.技能分类 === '辅助', '友方辅助UI声明缺目标/分类');
        断言战斗回归夹具(supportEntry.skill?.使用条件?.最低等级 === 1, '友方辅助UI声明缺使用条件');
        断言战斗回归夹具(itemEntry.action_type === '使用物品' && itemEntry.物品名 === '夹具药剂', '物品UI声明缺使用物品契约');
        断言战斗回归夹具(itemEntry.skill?.__物品名 === '夹具药剂' && itemEntry.skill?.承载方式 === '物品使用', '物品UI声明缺物品技能标记');
        const parsedSupport = buildPlayerActionFromSerializedEntry(supportEntry, 玩家.name, 玩家);
        断言战斗回归夹具(parsedSupport?.target_name === 友方.name, `友方辅助解析目标丢失:${parsedSupport?.target_name || '无'}`);
        断言战斗回归夹具(resolveSkillTargetContext(parsedSupport.skill, 玩家, 友方, combatData, null, parsedSupport).primaryTarget === 友方, '友方辅助解析后目标未落友方');
        断言战斗回归夹具(!判定单挑动作敌对(parsedSupport, 玩家, 敌人, combatData), '友方辅助UI声明被误判敌对');
        日志.push('UI动作声明保留来源、目标、物品契约字段');
      });
      注册('团战多窗口应招不串链', 日志 => {
        const { combatData, 玩家, 友方 } = 构建战斗回归夹具战斗态();
        玩家.name = '甲方攻手';
        友方.name = '乙方攻手';
        玩家.agi = 900;
        友方.agi = 850;
        玩家.final = buildCombatFinalStats(玩家);
        友方.final = buildCombatFinalStats(友方);
        const 甲敌 = 构建战斗回归夹具单位('甲敌', '防御系');
        const 乙敌 = 构建战斗回归夹具单位('乙敌', '防御系');
        甲敌.agi = 80;
        乙敌.agi = 70;
        甲敌.final = buildCombatFinalStats(甲敌);
        乙敌.final = buildCombatFinalStats(乙敌);
        combatData.战斗类型 = '团战';
        combatData.参战者.team_player = [玩家, 友方];
        combatData.参战者.team_enemy = [甲敌, 乙敌];
        setActorFocusTarget(玩家, 甲敌, 'shared_vision_focus', 2);
        setActorFocusTarget(友方, 乙敌, 'shared_vision_focus', 2);
        玩家.第1武魂.第1魂环.第1魂技 = 战斗回归输出魂技('甲方压制', '敌方单体', 8, 80, '近身攻击');
        友方.第1武魂 = {
          表象名称: '乙方武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('乙方刺击', '敌方单体', 8, 80, '近身攻击'),
          },
        };
        const 原随机 = Math.random;
        const 回合日志 = [];
        let 甲结果 = null;
        let 乙结果 = null;
        try {
          Math.random = () => 0.98;
          甲结果 = runActorTurn({ char: 玩家, side: 'player' }, { combatData, round: 1, logs: 回合日志 });
          乙结果 = runActorTurn({ char: 友方, side: 'player' }, { combatData, round: 1, logs: 回合日志 });
        } finally {
          Math.random = 原随机;
        }
        断言战斗回归夹具(甲结果?.target === '甲敌', `甲方目标串线:${甲结果?.target || '无'};${甲结果?.log || ''}`);
        断言战斗回归夹具(乙结果?.target === '乙敌', `乙方目标串线:${乙结果?.target || '无'};${乙结果?.log || ''}`);
        断言战斗回归夹具(!/乙敌/.test(String(甲结果?.log || '')), `甲方日志串入乙敌:${甲结果?.log || ''}`);
        断言战斗回归夹具(!/甲敌/.test(String(乙结果?.log || '')), `乙方日志串入甲敌:${乙结果?.log || ''}`);
        断言战斗回归夹具(!/抓住乙方攻手出手后的空门/.test(String(甲结果?.log || '')), `甲方防反串乙方:${甲结果?.log || ''}`);
        断言战斗回归夹具(!/抓住甲方攻手出手后的空门/.test(String(乙结果?.log || '')), `乙方防反串甲方:${乙结果?.log || ''}`);
        const report = buildReadableBattleReportLines(['[团战第1回合开始]', 甲结果.log, 乙结果.log], 8).join('\n');
        const 甲行 = report.split('\n').find(line => /甲方攻手/.test(line)) || '';
        const 乙行 = report.split('\n').find(line => /乙方攻手/.test(line)) || '';
        断言战斗回归夹具(/甲敌/.test(甲行) && !/乙敌/.test(甲行), `公开战报甲链串线:${report}`);
        断言战斗回归夹具(/乙敌/.test(乙行) && !/甲敌/.test(乙行), `公开战报乙链串线:${report}`);
        日志.push(`团战多窗口应招隔离成立:${report}`);
      });
      注册('预演结果导出与UI可见文本一致', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const entry = {
          actor_name: 玩家.name,
          action_type: '释放魂技',
          skill: { 魂技名: 战斗回归第一魂技默认名, name: 战斗回归第一魂技默认名, 消耗: '魂力:120' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
          target_name: 敌人.name,
        };
        let result = null;
        使用战斗回归桥接(combatData, { [玩家.name]: 玩家, [敌人.name]: 敌人 }, () => {
          result = onPlayerAttack(`${战斗回归第一魂技默认名}\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
            dryRun: true,
            mode: 'single_round',
            combatData,
            intentMode: '点到为止',
          });
        });
        const text = 导出战斗记录可见文本(result, 'preview');
        断言战斗回归夹具(/^预演结果/m.test(text), `导出缺预演结果头:${text}`);
        断言战斗回归夹具(/推进1回合/.test(text), `导出缺推进回合文本:${text}`);
        断言战斗回归夹具(/判定流程/.test(text), `导出缺判定流程标题:${text}`);
        断言战斗回归夹具(!/原始日志|判定 Trace JSON|用途：|说明：|随机种子：|【场景】/.test(text), `导出混入非UI文本:${text}`);
        日志.push(`预演结果导出保持UI口径:${text.split('\n').slice(0, 8).join(' | ')}`);
      });
      注册('公开战报不再自指普攻', 日志 => {
        const { combatData } = 构建战斗回归夹具战斗态();
        const text = buildReadableBattleReportLines([
          '[团战第1回合开始]',
          '[主动压迫] 夹具敌人判断对手没有主动进攻，改以[普通攻击]前压争夺节奏。 [起招] 夹具敌人以[普通攻击]起招。 [先手压制] 夹具玩家刚完成【星辉护壁】，来不及追加防守。 [命中结算] 夹具敌人对夹具玩家造成 57 点最终伤害。 [护盾吸收] 夹具玩家的[星辉护壁护盾]吸收了 57 点伤害。'
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/夹具敌人施展【普通攻击】指向夹具玩家/.test(text), `普攻目标未指向玩家:${text}`);
        断言战斗回归夹具(!/夹具敌人施展【普通攻击】指向夹具敌人/.test(text), `公开战报仍自指普攻:${text}`);
        日志.push(`公开战报普攻目标正确:${text}`);
      });
      注册('缺事件账本不再回退旧战报解析', 日志 => {
        const text = buildPublicBattleReportBlock({
          battleLog: ['[第1回合] [起招] 夹具玩家以[裂地冲拳]起招。 [命中结算] 夹具玩家对夹具敌人造成 120 点最终伤害。'],
          combatData: { 战斗意图: '点到为止', 裁断结果: '未分胜负' },
          battleOutcome: { type: 'single_round_probe', label: '单回合试探' },
          modeLabel: '单回合',
          roundCount: 1,
          eventLedger: [],
        });
        断言战斗回归夹具(!/本回合未形成可展示结果/.test(text), `缺事件账本时仍向玩家补假结果:${text}`);
        断言战斗回归夹具(!/裂地冲拳/.test(text), `缺事件账本时仍泄漏旧battleLog文本:${text}`);
        日志.push(`缺事件账本时公开战报不再回退旧文本:${text}`);
      });
      注册('缺事件账本时战报面板不回退publicReport字符串', 日志 => {
        const rows = 提取战斗结果战报行({
          logs: ['[第1回合] [起招] 夹具玩家以[裂地冲拳]起招。 [命中结算] 夹具玩家对夹具敌人造成 120 点最终伤害。'],
          combatData: {
            战斗意图: '点到为止',
            裁断结果: '未分胜负',
            参战者: {
              team_player: [{ name: '夹具玩家' }],
              team_enemy: [{ name: '夹具敌人' }],
            },
          },
          publicReport: '<战斗公开战报>\n第1回合：夹具玩家施展【裂地冲拳】指向夹具敌人，造成了 120 点伤害。\n</战斗公开战报>',
          eventLedger: [],
        });
        断言战斗回归夹具(Array.isArray(rows) && rows.length === 0, `缺事件账本时仍从publicReport字符串回退:${JSON.stringify(rows)}`);
        日志.push('缺事件账本时战报面板不再从publicReport字符串反解析');
      });
      注册('状态结算可追溯附着来源', 日志 => {
        const { combatData } = 构建战斗回归夹具战斗态();
        const text = buildReadableBattleReportLines([
          '[团战第1回合开始]',
          '[第1回合] [起招] 夹具玩家以[毒牙牵制]起招。 [命中结算] 夹具玩家对夹具敌人造成 187 点最终伤害。 [状态施加] 夹具敌人进入[中毒]。',
          '[团战第2回合开始]',
          '[第2回合] [状态结算] 夹具敌人受[中毒]影响，额外损失 18 点HP。'
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/并使夹具敌人陷入【中毒】/.test(text), `状态附着未并回原动作:${text}`);
        断言战斗回归夹具(/该状态由第1回合夹具玩家施展【毒牙牵制】附加/.test(text), `状态结算仍无法追溯来源:${text}`);
        日志.push(`状态结算来源可追溯:${text}`);
      });
      注册('敌方抢先压制时玩家动作闭合为行动受阻', 日志 => {
        const { result } = 生成战斗判定样本结果(9);
        const ledger = Array.isArray(result?.eventLedger) ? result.eventLedger : [];
        const 玩家闭合事件 = ledger.find(item =>
          isSameBattleReportName(item?.actorName || '', '夹具玩家') &&
          normalizeBattleActionDisplayName(item?.actionName || item?.sourceActionName || '') === '第二魂技·青影蛇群' &&
          ['hit_result', 'state_apply', 'blocked_action', 'failed_action', 'target_fail'].includes(String(item?.eventKind || '').trim())
        );
        const text = 导出战斗记录可见文本(result, 'preview');
        断言战斗回归夹具(!!玩家闭合事件, `敌方抢先压制时玩家动作仍未闭合:${JSON.stringify(ledger)}`);
        断言战斗回归夹具(
          /夹具玩家(?:的)?【第二魂技·青影蛇群】(?:尚未铺开，便被这轮交锋截断|被对手压住节奏，未能完成出手|本回合对抗结束，未能形成有效出手|被对手抢先压制|被对手先手压制)|夹具玩家刚欲催动【第二魂技·青影蛇群】|夹具玩家施展【第二魂技·青影蛇群】|并使夹具敌人陷入【迟缓】/.test(text),
          `玩家动作闭合结果未进入可见战报:${text}`,
        );
        日志.push(`敌方抢先压制时玩家动作已闭合:${玩家闭合事件?.eventKind || 'unknown'} ${玩家闭合事件?.failReason || ''} | ${text.split('\n').slice(0, 8).join(' | ')}`);
      });
      注册('反击后状态施加不串到防反链', 日志 => {
        const { combatData } = 构建战斗回归夹具战斗态();
        const text = buildReadableBattleReportLines([
          '[团战第1回合开始]',
          '[第1回合] [起招] 夹具敌人以[毒牙牵制]起招。 [应招] 夹具玩家以[承伤硬抗]应对。 [命中结算] 夹具敌人对夹具玩家造成 64 点最终伤害。 [行为防反] 夹具玩家凭借力守势抓住夹具敌人出手后的空门反击，造成了 12 点伤害。 [状态施加] 夹具玩家进入[中毒]。'
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/夹具敌人施展【毒牙牵制】指向夹具玩家/.test(text), `主动作未建立:${text}`);
        断言战斗回归夹具(/并使夹具玩家陷入【中毒】/.test(text), `状态未并回受击者:${text}`);
        断言战斗回归夹具(!/随后夹具玩家凭【借力守势】[^。]+并使夹具玩家陷入【中毒】/.test(text), `状态串到防反链:${text}`);
        日志.push(`反击后状态归属正确:${text}`);
      });
      注册('公开战报保留DOT来源括号', 日志 => {
        const { combatData } = 构建战斗回归夹具战斗态();
        const text = buildReadableBattleReportLines([
          '[团战第2回合开始]',
          '[第2回合] [状态结算] 夹具玩家受[中毒]影响，额外损失 18 点HP（该状态由第1回合夹具敌人施展【毒牙牵制】附加）'
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/该状态由第1回合夹具敌人施展【毒牙牵制】附加/.test(text), `公开战报丢失DOT来源:${text}`);
        日志.push(`DOT来源括号已保留:${text}`);
      });
      注册('自身增益动作队列不误取消', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.第1武魂.第1魂环.第1魂技 = normalizeSkillData({
          name: '星辉护壁', 魂技名: '星辉护壁', 技能分类: '辅助', 目标: '自身', 消耗: '魂力:80', 前摇: 8,
          _效果数组: [{ 原型: '护盾变化', 目标: '自身', 数值: '+120', 护盾名称: '星辉护壁护盾' }],
        }, '星辉护壁');
        const entry = {
          actor_name: 玩家.name,
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技', name: '错误缓存第一魂技', 消耗: '魂力:9999' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        };
        let result = null;
        使用战斗回归桥接(combatData, { [玩家.name]: 玩家, [敌人.name]: 敌人 }, () => {
          result = onPlayerAttack(`星辉护壁\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
            dryRun: true,
            mode: 'single_round',
            combatData,
            intentMode: '点到为止',
          });
        });
        const text = `${result?.publicReport || ''}\n${(result?.logs || []).join(' ')}`;
        断言战斗回归夹具(!/动作取消/.test(text), `自身增益动作队列仍被误取消:${text}`);
        日志.push(`自身增益动作队列未误取消:${text.split('\n')[0] || text}`);
      });
      注册('自保候选不串旧敌方目标', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.第1武魂.第1魂环.第1魂技 = normalizeSkillData({
          name: '星辉护壁', 魂技名: '星辉护壁', 技能分类: '辅助', 目标: '自身', 消耗: '魂力:80', 前摇: 8,
          _效果数组: [{ 原型: '护盾变化', 目标: '自身', 数值: '+120', 护盾名称: '星辉护壁护盾' }],
        }, '星辉护壁');
        const entry = {
          actor_name: 玩家.name,
          action_type: '释放魂技',
          skill: { 魂技名: '错误缓存第一魂技', name: '错误缓存第一魂技', 消耗: '魂力:9999' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
        };
        let result = null;
        使用战斗回归桥接(combatData, { [玩家.name]: 玩家, [敌人.name]: 敌人 }, () => {
          result = onPlayerAttack(`星辉护壁\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
            dryRun: true,
            mode: 'single_round',
            combatData,
            intentMode: '点到为止',
          });
        });
        const 主动规划 = (result?.decisionTrace || []).find(trace =>
          (trace?.type === '主动规划' || trace?.类型 === '主动规划') &&
          (trace?.actor === 敌人.name || trace?.行动者 === 敌人.name) &&
          Array.isArray(trace?.候选排序结果) &&
          trace.候选排序结果.some(候选 =>
            /伺机闪避|肉体兜底|危机自保|承伤硬抗|防御|借力守势|坚壁反制|收招转防/.test(String(候选?.名称 || ''))
          )
        );
        const 自保候选 = (主动规划?.候选排序结果 || []).filter(候选 =>
          /伺机闪避|肉体兜底|危机自保|承伤硬抗|防御|借力守势|坚壁反制|收招转防/.test(String(候选?.名称 || ''))
        );
        断言战斗回归夹具(!!主动规划, `未找到敌方主动规划审计:${JSON.stringify(result?.decisionTrace || [])}`);
        断言战斗回归夹具(自保候选.length > 0, `未找到自保候选:${JSON.stringify(主动规划?.候选排序结果 || [])}`);
        断言战斗回归夹具(
          自保候选.every(候选 => String(候选?.目标 || '') === 敌人.name),
          `自保候选仍串旧目标:${JSON.stringify(自保候选)}`,
        );
        日志.push(`自保候选目标已归自身:${自保候选.map(候选 => `${候选.名称}->${候选.目标}`).join('；')}`);
      });
      注册('非攻击动作不触发防反链', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 自身护盾 = {
          action_type: '释放魂技',
          type: '释放魂技',
          skill: normalizeSkillData({
            name: '星辉护壁', 魂技名: '星辉护壁', 技能分类: '辅助', 目标: '自身', 消耗: '魂力:80', 前摇: 8,
            _效果数组: [{ 原型: '护盾变化', 目标: '自身', 数值: '+120', 护盾名称: '星辉护壁护盾' }],
          }, '星辉护壁'),
          target_name: 玩家.name,
        };
        断言战斗回归夹具(!单挑动作可触发攻击反应链(自身护盾, 玩家, 玩家, combatData), '自身增益动作仍被识别为可触发反应链攻击');
        const 防反日志 = 执行行为防反结算(玩家, 敌人, 自身护盾, { type: '承伤硬抗', log: '', skill: null, def_mult: 1 }, {
          __行为防反候选: { 防反类型: '硬抗换伤', 防反方: 敌人, 攻击方: 玩家, 触发概率: 0.5, 出手承诺: 0.2, 战斗数据: combatData },
        }, { primaryAppliedDamage: 18 }, combatData);
        断言战斗回归夹具(!防反日志, `非攻击动作仍触发防反:${防反日志}`);
        const 被动承压防反日志 = 执行行为防反结算(玩家, 敌人, {
          action_type: '常规攻击',
          type: '常规攻击',
          skill: normalizeSkillData({
            name: '裂地冲拳', 技能分类: '输出', 目标: '敌方单体', 消耗: '无', 前摇: 8,
            _效果数组: [{ 原型: '伤害结算', 目标: '单体', 威力倍率: 80, 伤害类型: '近身攻击', 防御穿透: 0 }],
          }, '裂地冲拳'),
        }, { type: '无法反应', action_type: '无法反应', log: '', skill: null, def_mult: 1 }, {
          __行为防反候选: {
            防反类型: '硬抗换伤',
            防反方: 敌人,
            攻击方: 玩家,
            触发概率: 1,
            出手承诺: 0.25,
            战斗数据: combatData,
            需要主动防御成立: true,
            被动承压: true,
          },
        }, { primaryAppliedDamage: 26 }, combatData);
        断言战斗回归夹具(!被动承压防反日志, `被动承压仍错误触发硬抗反击:${被动承压防反日志}`);
        const 原随机 = Math.random;
        Math.random = () => 0;
        const 主动承压动作 = {
          action_type: '释放魂技',
          type: '释放魂技',
          skill: normalizeSkillData({
            name: '裂地冲拳', 技能分类: '输出', 目标: '敌方单体', 消耗: '无', 前摇: 8,
            _效果数组: [{ 原型: '伤害结算', 目标: '单体', 威力倍率: 80, 伤害类型: '近身攻击', 防御穿透: 0 }],
          }, '裂地冲拳'),
        };
        const 主动承压反应 = { type: '承伤硬抗', action_type: '承伤硬抗', log: '', skill: null, def_mult: 1 };
        const 主动承压候选 = {
          防反类型: '硬抗换伤',
          防反方: 敌人,
          攻击方: 玩家,
          触发概率: 1,
          出手承诺: 0.25,
          反应余量: 1.2,
          战斗数据: combatData,
          需要主动防御成立: true,
          被动承压: false,
        };
        const 主动承压门禁 = 单挑动作可触发攻击反应链(主动承压动作, 玩家, 敌人, combatData);
        const 主动承压概率 = 计算行为防反概率({
          ...主动承压候选,
          原动作: 主动承压动作,
          反应动作: 主动承压反应,
          战斗数据: combatData,
          实际伤害: 26,
        });
        let 主动承压防反日志 = '';
        try {
          主动承压防反日志 = 执行行为防反结算(玩家, 敌人, 主动承压动作, 主动承压反应, {
            __行为防反候选: 主动承压候选,
          }, { primaryAppliedDamage: 26 }, combatData);
        } finally {
          Math.random = 原随机;
        }
        断言战斗回归夹具(/\[行为防反\]/.test(主动承压防反日志), `主动承伤硬抗未触发防反:${主动承压防反日志};门禁:${主动承压门禁};概率:${主动承压概率}`);
        日志.push('非攻击动作未触发防反链，主动承压与被动承压的防反资格已区分');
      });
      注册('防反来源优先使用真实动作', 日志 => {
        const { combatData, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.第1武魂 = {
          表象名称: '敌方武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('敌方截击', '敌方单体', 6, 90, '近身攻击'),
          },
        };
        const 来源 = 选择行为防反动作来源(敌人, combatData, '硬抗换伤');
        断言战斗回归夹具(来源?.sourceActionName === '敌方截击', `防反来源未优先取真实动作:${JSON.stringify(来源)}`);
        const 防反动作 = 建立行为防反动作(敌人, { ...来源, 防反类型: '硬抗换伤', 触发概率: 0.4, 出手承诺: 0.2, 战斗数据: combatData });
        断言战斗回归夹具((防反动作?.sourceActionName || '') === '敌方截击', `防反动作未透传真实来源:${JSON.stringify(防反动作)}`);
        日志.push(`防反来源已切到真实动作:${来源?.sourceActionName || '无'}`);
      });
      注册('控制第一魂技防反伤害不污染来源', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.第1武魂 = {
          表象名称: '敌方武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('敌方截击', '敌方单体', 6, 90, '近身攻击'),
          },
        };
        const 第一魂技动作 = {
          action_type: '释放魂技',
          type: '释放魂技',
          skill: normalizeSkillData(构建战斗回归第一魂技(战斗回归第一魂技默认名, '魂力:120'), 战斗回归第一魂技默认名),
          target_name: 敌人.name,
        };
        const 原随机 = Math.random;
        Math.random = () => 0;
        try {
          const 日志文本 = 执行行为防反结算(玩家, 敌人, 第一魂技动作, {
            type: '伺机闪避',
            action_type: '伺机闪避',
            log: '',
            skill: null,
            def_mult: 1,
          }, {
            __行为防反候选: {
              防反类型: '完美闪避',
              防反方: 敌人,
              攻击方: 玩家,
              原动作: 第一魂技动作,
              触发概率: 1,
              出手承诺: 0.35,
              战斗数据: combatData,
              sourceActionName: '敌方截击',
              sourceActionType: 'skill_counter',
            },
          }, { primaryAppliedDamage: 0 }, combatData);
          const 账本 = Array.isArray(combatData.__battleEventLedger) ? combatData.__battleEventLedger : [];
          const 第一魂技伤害事件 = 账本.filter(event =>
            String(event?.eventKind || '').trim() === 'hit_result' &&
            normalizeBattleActionDisplayName(event?.actionName || event?.sourceActionName || '') === 战斗回归第一魂技默认名 &&
            Math.max(0, Number(event?.appliedDamage ?? event?.damage ?? event?.meta?.appliedDamage ?? event?.meta?.damage ?? 0)) > 0
          );
          const 防反事件 = [...账本].reverse().find(event =>
            String(event?.eventKind || '').trim() === 'counter' &&
            String(event?.result || '').trim() === 'success' &&
            normalizeBattleActionDisplayName(event?.actionName || '') === '敌方截击'
          );
          断言战斗回归夹具(!日志文本, `纯控制第一魂技不应触发伤害型防反:${日志文本}`);
          断言战斗回归夹具(!第一魂技伤害事件.length, `控制第一魂技被污染成伤害来源:${JSON.stringify(第一魂技伤害事件)}`);
          断言战斗回归夹具(!防反事件, `纯控制第一魂技仍生成伤害型防反事件:${JSON.stringify(防反事件)}`);
          日志.push('控制第一魂技不会生成伤害型防反，伤害来源未被污染');
        } finally {
          Math.random = 原随机;
        }
      });
      注册('状态反伤必须写入防反账本', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.状态效果 = {
          荆棘反震: {
            类型: 'buff',
            duration: 2,
            战斗效果: {
              ...createEmptyCombatEffectMap(),
              counter_attack_ratio: 0.5,
            },
          },
        };
        const 攻击动作 = {
          action_type: '释放魂技',
          type: '释放魂技',
          skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 100, '近身攻击'), '裂地冲拳'),
          target_name: 敌人.name,
        };
        写入战斗事件账本(combatData, {
          eventKind: 'action_start',
          round: Number(combatData?.回合 || 0),
          actorName: 玩家.name,
          targetName: 敌人.name,
          actionName: '裂地冲拳',
          actionType: '释放魂技',
          result: 'started',
        });
        const 结算结果 = {
          result: 'hit',
          desc: '',
          dmg: 100,
          targetResults: [{ target: 敌人, targetName: 敌人.name, damage: 100, kind: 'primary' }],
        };
        const 攻击前血量 = getCombatHpValue(玩家);
        const 伤害包 = applyResolvedDamagePackage(玩家, 攻击动作, 结算结果, {
          primaryTarget: 敌人,
          combatData,
        });
        const 账本 = Array.isArray(combatData.__battleEventLedger) ? combatData.__battleEventLedger : [];
        const 状态反伤事件 = 账本.find(event =>
          String(event?.eventKind || '').trim() === 'counter' &&
          String(event?.result || '').trim() === 'success' &&
          normalizeBattleActionDisplayName(event?.actionName || '') === '荆棘反震' &&
          normalizeBattleActionDisplayName(event?.sourceActionName || '') === '裂地冲拳'
        );
        const 状态反伤窗口 = 账本.find(event =>
          String(event?.eventKind || '').trim() === 'counter_window' &&
          normalizeBattleActionDisplayName(event?.actionName || '') === '荆棘反震' &&
          normalizeBattleActionDisplayName(event?.sourceActionName || '') === '裂地冲拳'
        );
        const 轨迹 = Array.isArray(状态反伤事件?.meta?.settlementTrace) ? 状态反伤事件.meta.settlementTrace : [];
        const 读取轨迹值 = key => 轨迹.find(item => String(item?.key || '').trim() === key)?.value;
        断言战斗回归夹具(Number(伤害包.primaryAppliedDamage || 0) > 0, `主伤害未正常落地:${JSON.stringify(伤害包)}`);
        断言战斗回归夹具(getCombatHpValue(玩家) === 攻击前血量 - 50, `状态反伤扣血异常:${攻击前血量}->${getCombatHpValue(玩家)}`);
        断言战斗回归夹具(!!状态反伤窗口, `状态反伤缺防反窗口:${JSON.stringify(账本)}`);
        断言战斗回归夹具(!!状态反伤事件, `状态反伤缺防反账本:${JSON.stringify(账本)}`);
        断言战斗回归夹具(读取轨迹值('sourceAction') === '荆棘反震', `状态反伤来源动作串线:${JSON.stringify(轨迹)}`);
        断言战斗回归夹具(读取轨迹值('counteredAction') === '裂地冲拳', `状态反伤被反制动作缺失:${JSON.stringify(轨迹)}`);
        日志.push('状态反伤已写入 counter_window/counter 账本，反伤来源与被反制动作分离');
      });
      注册('防反失败真实原因优先于概率文案', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 原随机 = Math.random;
        Math.random = () => 0.99;
        try {
          const 失败日志 = 执行行为防反结算(玩家, 敌人, {
            action_type: '常规攻击',
            type: '常规攻击',
            skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 80, '近身攻击'), '裂地冲拳'),
          }, { type: '伺机闪避', action_type: '伺机闪避', log: '', skill: null, def_mult: 1 }, {
            __行为防反候选: {
              防反类型: '完美闪避',
              防反方: 敌人,
              攻击方: 玩家,
              触发概率: 0.4,
              出手承诺: 0.2,
              战斗数据: combatData,
              sourceActionName: '短刀',
              failReason: '距离不够',
            },
          }, { primaryAppliedDamage: 16 }, combatData);
          const 账本 = Array.isArray(combatData.__battleEventLedger) ? combatData.__battleEventLedger : [];
          const 失败事件 = [...账本].reverse().find(item => item?.eventKind === 'counter' && item?.result === 'fail');
          断言战斗回归夹具(/尝试以\[短刀\]反打，但距离不够/.test(失败日志), `防反失败真实原因未优先展示:${失败日志}`);
          断言战斗回归夹具(!/概率:/.test(失败日志), `存在真实失败原因时仍拼接概率:${失败日志}`);
          断言战斗回归夹具(String(失败事件?.failReason || '') === '距离不够', `失败账本未保留真实原因:${JSON.stringify(失败事件)}`);
          日志.push(`防反失败优先原因:${失败日志}`);
        } finally {
          Math.random = 原随机;
        }
      });
      注册('防反失败自动提取来不及反应原因', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 原随机 = Math.random;
        Math.random = () => 0.99;
        try {
          const 失败日志 = 执行行为防反结算(玩家, 敌人, {
            action_type: '常规攻击',
            type: '常规攻击',
            skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 80, '近身攻击'), '裂地冲拳'),
          }, { type: '无法反应', action_type: '无法反应', log: '[先手压制] 夹具敌人刚完成【前序动作】，来不及追加防守。', skill: null, def_mult: 1 }, {
            __行为防反候选: {
              防反类型: '完美闪避',
              防反方: 敌人,
              攻击方: 玩家,
              触发概率: 0.4,
              出手承诺: 0.2,
              战斗数据: combatData,
              sourceActionName: '短刀',
            },
          }, { primaryAppliedDamage: 16 }, combatData);
          断言战斗回归夹具(/来不及反应|来不及追加防守/.test(失败日志), `防反失败未自动提取来不及反应:${失败日志}`);
          断言战斗回归夹具(!/概率:/.test(失败日志), `来不及反应仍被概率文案覆盖:${失败日志}`);
          日志.push(`防反失败自动提取来不及反应:${失败日志}`);
        } finally {
          Math.random = 原随机;
        }
      });
      注册('防反失败自动提取资源不足原因', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 原随机 = Math.random;
        Math.random = () => 0.99;
        try {
          const 失败日志 = 执行行为防反结算(玩家, 敌人, {
            action_type: '常规攻击',
            type: '常规攻击',
            skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 80, '近身攻击'), '裂地冲拳'),
          }, { type: '伺机闪避', action_type: '伺机闪避', log: '[状态枯竭] 自身状态不足，无法支撑反打。', skill: null, def_mult: 1 }, {
            __行为防反候选: {
              防反类型: '完美闪避',
              防反方: 敌人,
              攻击方: 玩家,
              触发概率: 0.4,
              出手承诺: 0.2,
              战斗数据: combatData,
              sourceActionName: '短刀',
            },
          }, { primaryAppliedDamage: 16 }, combatData);
          断言战斗回归夹具(/资源不足/.test(失败日志), `防反失败未自动提取资源不足:${失败日志}`);
          断言战斗回归夹具(!/概率:/.test(失败日志), `资源不足仍被概率文案覆盖:${失败日志}`);
          日志.push(`防反失败自动提取资源不足:${失败日志}`);
        } finally {
          Math.random = 原随机;
        }
      });
      注册('防反失败自动提取动作被拒原因', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 原随机 = Math.random;
        Math.random = () => 0.99;
        try {
          const 失败日志 = 执行行为防反结算(玩家, 敌人, {
            action_type: '常规攻击',
            type: '常规攻击',
            skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 80, '近身攻击'), '裂地冲拳'),
          }, { type: '伺机闪避', action_type: '伺机闪避', log: '[门禁拒绝] 本次反打动作被拒绝执行。', skill: null, def_mult: 1 }, {
            __行为防反候选: {
              防反类型: '完美闪避',
              防反方: 敌人,
              攻击方: 玩家,
              触发概率: 0.4,
              出手承诺: 0.2,
              战斗数据: combatData,
              sourceActionName: '短刀',
            },
          }, { primaryAppliedDamage: 16 }, combatData);
          断言战斗回归夹具(/动作被拒/.test(失败日志), `防反失败未自动提取动作被拒:${失败日志}`);
          断言战斗回归夹具(!/概率:/.test(失败日志), `动作被拒仍被概率文案覆盖:${失败日志}`);
          日志.push(`防反失败自动提取动作被拒:${失败日志}`);
        } finally {
          Math.random = 原随机;
        }
      });
      注册('防反失败自动提取被打断原因', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 原随机 = Math.random;
        Math.random = () => 0.99;
        try {
          const 失败日志 = 执行行为防反结算(玩家, 敌人, {
            action_type: '常规攻击',
            type: '常规攻击',
            skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 80, '近身攻击'), '裂地冲拳'),
          }, { type: '伺机闪避', action_type: '伺机闪避', log: '[抢招失败] 反打动作被对手攻势打断，未能成形。', skill: null, def_mult: 1 }, {
            __行为防反候选: {
              防反类型: '完美闪避',
              防反方: 敌人,
              攻击方: 玩家,
              触发概率: 0.4,
              出手承诺: 0.2,
              战斗数据: combatData,
              sourceActionName: '短刀',
            },
          }, { primaryAppliedDamage: 16 }, combatData);
          断言战斗回归夹具(/被打断/.test(失败日志), `防反失败未自动提取被打断:${失败日志}`);
          断言战斗回归夹具(!/概率:/.test(失败日志), `被打断仍被概率文案覆盖:${失败日志}`);
          日志.push(`防反失败自动提取被打断:${失败日志}`);
        } finally {
          Math.random = 原随机;
        }
      });
      注册('系统型防反按窗口归一显示名', 日志 => {
        const { 敌人 } = 构建战斗回归夹具战斗态();
        const 防守反击 = 建立行为防反动作(敌人, { 防反类型: '硬抗换伤', 触发概率: 0.4, 出手承诺: 0.2 });
        const 闪避反击 = 建立行为防反动作(敌人, { 防反类型: '完美闪避', 触发概率: 0.4, 出手承诺: 0.2 });
        const 绝地反扑 = 建立行为防反动作(敌人, { 防反类型: '以命换伤', 触发概率: 0.4, 出手承诺: 0.2, 以命换伤: true });
        断言战斗回归夹具((防守反击?.sourceActionName || '') === '防守反击', `硬抗换伤兜底名异常:${JSON.stringify(防守反击)}`);
        断言战斗回归夹具((闪避反击?.sourceActionName || '') === '闪避反击', `完美闪避兜底名异常:${JSON.stringify(闪避反击)}`);
        断言战斗回归夹具((绝地反扑?.sourceActionName || '') === '绝地反扑', `以命换伤兜底名异常:${JSON.stringify(绝地反扑)}`);
        断言战斗回归夹具(normalizeBattleActionDisplayName('系统反击') === '借势反打', '系统反击显示名未归一为借势反打');
        日志.push(`系统型防反显示名:硬抗=${防守反击?.sourceActionName};闪避=${闪避反击?.sourceActionName};搏命=${绝地反扑?.sourceActionName}`);
      });
      注册('防反零伤害改写为未造成实质伤害', 日志 => {
        const combatData = 构建战斗回归夹具战斗态().combatData;
        const text = buildReadableBattleReportLines([
          '[第1回合] [团战执行] 夹具玩家以[裂地冲拳]指向[夹具敌人]。 [起招] 夹具玩家以[裂地冲拳]起招。 [应招] 夹具敌人以[承伤硬抗]应对。 [命中结算] 夹具玩家对夹具敌人造成 115 点最终伤害。 [行为防反] 夹具敌人凭防守反击抓住夹具玩家出手后的空门反击，造成了 0 点伤害。',
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(/未能造成实质伤害/.test(text), `防反零伤害未改写:${text}`);
        断言战斗回归夹具(!/造成了 0 点伤害/.test(text), `防反零伤害仍以数字直出:${text}`);
        日志.push(`防反零伤害文案成立:${text}`);
      });
      注册('状态来源登记写入运行时账本', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const action = {
          action_type: '释放魂技', type: '释放魂技',
          skill: normalizeSkillData({
            name: '毒牙牵制', 魂技名: '毒牙牵制', 技能分类: '输出', 目标: '敌方单体', 消耗: '魂力:60', 前摇: 8,
            _效果数组: [
              { 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 80, 伤害类型: '近身攻击' },
              { 原型: '状态施加', 目标: '敌方单体', 生效方式: '跟随主原型', 状态: '中毒', 状态名称: '中毒', 持续回合: 2, 计算层效果: { dot_damage: 20 } },
            ],
          }, '毒牙牵制'),
        };
        使用战斗回归桥接(combatData, { [玩家.name]: 玩家, [敌人.name]: 敌人 }, () => {
          executeClash(action, 构建单挑反应动作({ action_type: '防御', type: '防御' }, 敌人, 玩家), 构建单挑临时战斗数据(玩家, 敌人, 'player', combatData));
        });
        const 登记 = combatData?.__行动闭环诊断?.状态来源登记 || [];
        const 命中 = 登记.find(item => item?.stateName === '中毒' && item?.targetName === 敌人.name);
        断言战斗回归夹具(!!命中, `状态来源登记缺中毒:${JSON.stringify(登记)}`);
        断言战斗回归夹具(命中?.sourceActorName === 玩家.name && 命中?.sourceActionName === '毒牙牵制', `状态来源登记字段异常:${JSON.stringify(命中)}`);
        日志.push(`状态来源登记已写入:${JSON.stringify(命中)}`);
      });
      注册('敌对动作目标不得落友方或自身', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const actorEntry = { char: 敌人, side: 'enemy' };
        const targets = chooseTargetForActor(actorEntry, { combatData }) || { enemyTarget: 玩家, allyTarget: 敌人 };
        const action = buildAutoActionForActor(actorEntry, targets, { combatData, observedTargetAction: { action_type: '常规攻击', type: '常规攻击', skill: normalizeSkillData(战斗回归输出魂技('夹具攻击', '敌方单体', 8, 70, '近身攻击'), '夹具攻击') } });
        const 目标 = 解析单挑动作目标(action, 敌人, 玩家, combatData);
        if (单挑动作可触发攻击反应链(action, 敌人, 目标, combatData)) {
          断言战斗回归夹具((combatData?.参战者?.team_player || []).some(unit => isCombatUnitIdentityMatch(unit, 目标?.name || 目标)), `敌对动作落到了非玩家侧:${目标?.name || '无'};${action?.decision_log || ''}`);
          断言战斗回归夹具(!(combatData?.参战者?.team_enemy || []).some(unit => isCombatUnitIdentityMatch(unit, 目标?.name || 目标)), `敌对动作落到了友方或自身:${目标?.name || '无'};${action?.decision_log || ''}`);
        }
        const 探针 = combatData?.__行动闭环诊断?.目标权重探针 || [];
        断言战斗回归夹具(探针.some(item => item?.targetPoolSide === 'hostile'), `目标权重探针未写入敌对池:${JSON.stringify(探针)}`);
        日志.push(`敌对动作目标阵营正确:${目标?.name || '无'}`);
      });
      注册('玩家主动入口敌方规划不再转火友方', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 友方 = 构建战斗回归夹具单位('夹具友方', '辅助系');
        combatData.参战者.team_player = [玩家, 友方];
        敌人.type = '敏攻系';
        敌人.agi = 420;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '锁敌武魂',
          第1魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'), '敌方截击'),
          },
          第2魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'), '敌方压迫'),
          },
        };
        const actorEntry = { char: 敌人, side: 'enemy' };
        const action = buildAutoActionForActor(actorEntry, { enemyTarget: 玩家, allyTarget: 敌人 }, {
          combatData,
          observedTargetAction: {
            action_type: '释放魂技',
            type: '技能',
            cast_time: 10,
            skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 10, 88, '近身攻击'), '裂地冲拳'),
          },
        });
        const 解析目标 = 解析单挑动作目标(action, 敌人, 玩家, combatData);
        const 目标名 = String(解析目标?.name || 解析目标?.名称 || action?.target_name || '').trim();
        断言战斗回归夹具(isSameBattleReportName(目标名, 玩家.name), `玩家主动入口时敌方规划仍转火友方:${目标名};${action?.decision_log || ''}`);
        日志.push(`玩家主动入口敌方规划保持锁定当前玩家:${目标名}`);
      });
      注册('反敏攻闪避后不继续普攻前压', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '敏攻系';
        玩家.agi = 999;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '强攻系';
        敌人.agi = 80;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '反制武魂',
          第1魂环: {
            第1魂技: normalizeSkillData({
              name: '缚影压制',
              魂技名: '缚影压制',
              技能分类: '控制',
              目标: '敌方单体',
              消耗: '无',
              前摇: 8,
              _效果数组: [
                { 原型: '状态施加', 目标: '敌方单体', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.2 } },
              ],
            }, '缚影压制'),
          },
        };
        recordActorActionWhiffedByDodge(敌人, '普通攻击', 玩家);
        const actorEntry = { char: 敌人, side: 'enemy' };
        const action = buildAutoActionForActor(actorEntry, { enemyTarget: 玩家, allyTarget: 敌人 }, {
          combatData,
          observedTargetAction: { action_type: '伺机闪避', type: '伺机闪避', skill: normalizeSkillData({ name: '伺机闪避', 技能分类: '防御', 目标: '自身', 消耗: '无', 前摇: 4 }, '伺机闪避') },
        });
        const 动作名 = String(action?.skill?.name || action?.skill?.魂技名 || action?.type || action?.action_type || '');
        断言战斗回归夹具(!/普通攻击|常规攻击/.test(动作名), `反敏攻后仍继续普攻:${动作名};${action?.decision_log || ''}`);
        断言战斗回归夹具(/缚影压制|反敏攻|收招转防|稳态调整/.test(`${动作名} ${action?.decision_log || ''}`), `反敏攻后未切换到控制/稳态:${动作名};${action?.decision_log || ''}`);
        日志.push(`反敏攻闪避后动作切换:${动作名}`);
      });
      注册('敏攻压制态势下不再回落单体直伤硬追', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '敏攻系';
        玩家.agi = 1200;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '强攻系';
        敌人.agi = 90;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '压制武魂',
          第1魂环: {
            第1魂技: normalizeSkillData({
              name: '缚影压制',
              魂技名: '缚影压制',
              技能分类: '控制',
              目标: '敌方单体',
              消耗: '无',
              前摇: 8,
              _效果数组: [
                { 原型: '状态施加', 目标: '敌方单体', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.2, lock_level: 1 } },
              ],
            }, '缚影压制'),
          },
          第2魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('贴身冲拳', '敌方单体', 8, 88, '近身攻击'), '贴身冲拳'),
          },
        };
        recordActorActionWhiffedByDodge(敌人, '贴身冲拳', 玩家);
        recordActorActionWhiffedByDodge(敌人, '普通攻击', 玩家);
        const actorEntry = { char: 敌人, side: 'enemy' };
        const action = buildAutoActionForActor(actorEntry, { enemyTarget: 玩家, allyTarget: 敌人 }, {
          combatData,
          observedTargetAction: { action_type: '常规攻击', type: '常规攻击', skill: normalizeSkillData(战斗回归输出魂技('高速切入', '敌方单体', 6, 72, '近身攻击'), '高速切入') },
        });
        const 动作名 = String(action?.skill?.name || action?.skill?.魂技名 || action?.type || action?.action_type || '');
        断言战斗回归夹具(!/普通攻击|常规攻击|贴身冲拳/.test(动作名), `敏攻压制态势下仍单体直伤硬追:${动作名};${action?.decision_log || ''}`);
        断言战斗回归夹具(/缚影压制|收招转防|反敏攻/.test(`${动作名} ${action?.decision_log || ''}`), `敏攻压制态势下未切反敏攻动作:${动作名};${action?.decision_log || ''}`);
        日志.push(`敏攻压制态势已避开直伤硬追:${动作名}`);
      });
      注册('敏攻闪避后不重复同一单体压制技能', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '敏攻系';
        玩家.agi = 1250;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '强攻系';
        敌人.agi = 92;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '蛇影武魂',
          第1魂环: {
            第1魂技: normalizeSkillData({
              name: '毒牙牵制',
              魂技名: '毒牙牵制',
              技能分类: '控制',
              目标: '敌方单体',
              消耗: '无',
              前摇: 8,
              _效果数组: [
                { 原型: '状态施加', 目标: '敌方单体', 状态: '中毒', 状态名称: '中毒', 持续回合: 2, 计算层效果: { dot_damage_ratio: 0.03, dodge_penalty: 0.08 } },
              ],
            }, '毒牙牵制'),
          },
          第2魂环: {
            第1魂技: normalizeSkillData({
              name: '青影蛇群',
              魂技名: '青影蛇群',
              技能分类: '控制',
              目标: '敌方群体',
              消耗: '无',
              前摇: 10,
              _效果数组: [
                { 原型: '伤害结算', 目标: '敌方群体', 威力倍率: 68, 伤害类型: '近身攻击', 防御穿透: 0 },
                { 原型: '状态施加', 目标: '敌方群体', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.2 } },
              ],
            }, '青影蛇群'),
          },
        };
        recordActorActionWhiffedByDodge(敌人, '毒牙牵制', 玩家);
        recordActorActionWhiffedByDodge(敌人, '普通攻击', 玩家);
        const actorEntry = { char: 敌人, side: 'enemy' };
        const action = buildAutoActionForActor(actorEntry, { enemyTarget: 玩家, allyTarget: 敌人 }, {
          combatData,
          observedTargetAction: { action_type: '伺机闪避', type: '伺机闪避', skill: normalizeSkillData({ name: '伺机闪避', 技能分类: '防御', 目标: '自身', 消耗: '无', 前摇: 4 }, '伺机闪避') },
        });
        const 动作名 = String(action?.skill?.name || action?.skill?.魂技名 || action?.type || action?.action_type || '');
        断言战斗回归夹具(!/毒牙牵制/.test(动作名), `敏攻闪避后仍重复同一单体压制技能:${动作名};${action?.decision_log || ''}`);
        断言战斗回归夹具(/青影蛇群|收招转防|反敏攻/.test(`${动作名} ${action?.decision_log || ''}`), `敏攻闪避后未切到范围压制或收招:${动作名};${action?.decision_log || ''}`);
        日志.push(`敏攻闪避后改用:${动作名}`);
      });
      注册('敏攻系主动规划可选控制型第二魂技', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '强攻系';
        玩家.agi = 180;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '敏攻系';
        敌人.agi = 420;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '蛇影武魂',
          第1魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'), '敌方截击'),
          },
          第2魂环: {
            第2魂技: normalizeSkillData({
              name: '第二魂技·青影蛇群',
              魂技名: '第二魂技·青影蛇群',
              技能分类: '控制',
              目标: '敌方群体',
              消耗: '无',
              前摇: 10,
              _效果数组: [
                { 原型: '伤害结算', 目标: '敌方群体', 威力倍率: 72, 伤害类型: '远程攻击', 命中修正: 0.12 },
                { 原型: '状态施加', 目标: '敌方群体', 状态: '迟缓', 状态名称: '迟缓', 持续回合: 2, 计算层效果: { cast_speed_penalty: 0.18, dodge_penalty: 0.08 } },
              ],
            }, '第二魂技·青影蛇群'),
          },
        };
        const actorEntry = { char: 敌人, side: 'enemy' };
        const action = buildAutoActionForActor(actorEntry, { enemyTarget: 玩家, allyTarget: 敌人 }, {
          combatData,
          observedTargetAction: {
            action_type: '释放魂技',
            type: '技能',
            cast_time: 12,
            skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 12, 96, '近身攻击'), '裂地冲拳'),
          },
        });
        const 动作名 = String(action?.skill?.name || action?.skill?.魂技名 || action?.type || action?.action_type || '');
        断言战斗回归夹具(/第二魂技·青影蛇群|敏攻截断|连锁控制|范围压制/.test(`${动作名} ${action?.decision_log || ''}`), `敏攻系主动规划未抬起控制型第二魂技:${动作名};${action?.decision_log || ''}`);
        日志.push(`敏攻系主动规划可抬起控制型第二魂技:${动作名}`);
      });
      注册('范围控制来袭且无真截断时不再强势对轰', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '强攻系';
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '敏攻系';
        敌人.agi = 360;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '快攻武魂',
          第1魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'), '敌方截击'),
            第2魂技: normalizeSkillData(战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'), '敌方压迫'),
          },
        };
        const playerAction = {
          action_type: '释放魂技',
          type: '释放魂技',
          cast_time: 12,
          skill: normalizeSkillData({
            name: '第二魂技·青影蛇群',
            魂技名: '第二魂技·青影蛇群',
            技能分类: '控制',
            目标: '敌方群体',
            消耗: '魂力:140',
            前摇: 12,
            _效果数组: [
              { 原型: '伤害结算', 目标: '敌方群体', 威力倍率: 72, 伤害类型: '远程攻击', 命中修正: 0.12 },
              { 原型: '状态施加', 目标: '敌方群体', 状态: '迟缓', 状态名称: '迟缓', 持续回合: 2, 计算层效果: { cast_speed_penalty: 0.18, dodge_penalty: 0.08 } },
            ],
          }, '第二魂技·青影蛇群'),
        };
        const ratio = calculateReactionRatio(玩家, 敌人, playerAction, combatData);
        const reaction = determineNpcAction(combatData, playerAction, ratio);
        const 动作名 = String(reaction?.skill?.name || reaction?.skill?.魂技名 || reaction?.type || reaction?.action_type || '');
        断言战斗回归夹具(!/强势对轰/.test(String(reaction?.type || '')), `范围控制来袭时仍走强势对轰:${reaction?.type || '无'};${reaction?.log || ''}`);
        断言战斗回归夹具(/伺机闪避|危机自保|防御|闪避/.test(`${动作名} ${reaction?.type || ''}`), `范围控制来袭时未转入防守应对:${动作名};${reaction?.log || ''}`);
        日志.push(`范围控制来袭改为防守应对:${reaction?.type || 动作名}`);
      });
      注册('范围控制主动技在无真截断时不再默认被先手压制', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '强攻系';
        玩家.final = buildCombatFinalStats(玩家);
        玩家.第1武魂.第2魂环 ||= {};
        玩家.第1武魂.第2魂环.第2魂技 = normalizeSkillData({
          name: '第二魂技·青影蛇群',
          魂技名: '第二魂技·青影蛇群',
          技能分类: '控制',
          目标: '敌方群体',
          消耗: '魂力:140',
          前摇: 12,
          _效果数组: [
            { 原型: '伤害结算', 目标: '敌方群体', 威力倍率: 72, 伤害类型: '远程攻击', 命中修正: 0.12 },
            { 原型: '状态施加', 目标: '敌方群体', 状态: '迟缓', 状态名称: '迟缓', 持续回合: 2, 计算层效果: { cast_speed_penalty: 0.18, dodge_penalty: 0.08 } },
          ],
        }, '第二魂技·青影蛇群');
        敌人.type = '敏攻系';
        敌人.agi = 280;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '快攻武魂',
          第1魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'), '敌方截击'),
            第2魂技: normalizeSkillData(战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'), '敌方压迫'),
          },
        };
        const entry = {
          actor_name: 玩家.name,
          action_type: '释放魂技',
          skill: { 魂技名: '第二魂技·青影蛇群', name: '第二魂技·青影蛇群', 消耗: '魂力:140' },
          __魂环路径: ['第1武魂', '第2魂环'],
          __魂技槽位: '第2魂技',
          target_name: 敌人.name,
        };
        let result = null;
        使用战斗回归桥接(combatData, { [玩家.name]: 玩家, [敌人.name]: 敌人 }, () => {
          result = onPlayerAttack(`第二魂技·青影蛇群\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
            dryRun: true,
            mode: 'single_round',
            combatData,
            intentMode: '点到为止',
          });
        });
        const text = 导出战斗记录可见文本(result, 'preview');
        断言战斗回归夹具(!/第二魂技·青影蛇群】被对手抢先压制/.test(text), `无真截断时范围控制仍被默认先手压制:${text}`);
        断言战斗回归夹具(/施展【第二魂技·青影蛇群】指向夹具敌人|陷入【迟缓】|陷入【减速】/.test(text), `范围控制主动技未形成可见落地结果:${text}`);
        日志.push(`范围控制主动技在无真截断时成功落地:${text.split('\n').slice(0, 6).join(' | ')}`);
      });
      注册('未触发闪避克制时仍允许继续压制', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '强攻系';
        玩家.agi = 120;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '控制系';
        敌人.agi = 140;
        敌人.final = buildCombatFinalStats(敌人);
        敌人.第1武魂 = {
          表象名称: '压制武魂',
          第1魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'), '敌方截击'),
          },
          第2魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'), '敌方压迫'),
          },
        };
        const actorEntry = { char: 敌人, side: 'enemy' };
        const action = buildAutoActionForActor(actorEntry, { enemyTarget: 玩家, allyTarget: 敌人 }, {
          combatData,
          observedTargetAction: { action_type: '常规攻击', type: '常规攻击', skill: normalizeSkillData(战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 72, '近身攻击'), '裂地冲拳') },
        });
        const 动作名 = String(action?.skill?.name || action?.skill?.魂技名 || action?.type || action?.action_type || '');
        断言战斗回归夹具(/敌方截击|敌方压迫/.test(动作名), `未触发闪避克制时仍被过度压成守势:${动作名};${action?.decision_log || ''}`);
        断言战斗回归夹具(!/收招转防|当前不宜继续前压/.test(`${动作名} ${action?.decision_log || ''}`), `未触发闪避克制时误判为必须转守:${动作名};${action?.decision_log || ''}`);
        日志.push(`未触发闪避克制时继续压制:${动作名}`);
      });
      注册('行为链换招不泄漏占位技能名', 日志 => {
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        玩家.type = '强攻系';
        玩家.agi = 80;
        玩家.final = buildCombatFinalStats(玩家);
        敌人.type = '敏攻系';
        敌人.agi = 999;
        敌人.final = buildCombatFinalStats(敌人);
        玩家.第1武魂 = {
          表象名称: '夹具武魂',
          第1魂环: {
            第1魂技: normalizeSkillData(战斗回归输出魂技('贴身冲拳', '敌方单体', 8, 88, '近身攻击'), '贴身冲拳'),
          },
        };
        const logs = buildReadableBattleReportLines([
          '[团战第1回合开始]',
          '[第1回合] [起招] 唐凌雪以[贴身冲拳]起招。 [应招] 韦小枫以[伺机闪避]应对。 [主动闪避] 韦小枫凭借敏捷优势惊险躲过了攻击。',
          '[团战第2回合开始]',
          '[第2回合] [起招] 韦小枫以[第1魂技]起招。 [应招] 唐凌雪以[收招转防]应对。'
        ], 8, { combatData }).join('\n');
        断言战斗回归夹具(!/换招技能|未命名技能/.test(logs), `公开战报泄漏内部占位技能名:${logs}`);
        断言战斗回归夹具(!/【第1魂技】|【第2魂技】/.test(logs), `公开战报仍残留魂技槽位占位:${logs}`);
        日志.push('行为链换招未再泄漏占位技能名');
      });
      return { ok: 夹具列表.every(item => item.ok), results: 夹具列表 };
    }

    function 生成自然召唤闭环调试结果(配置 = {}) {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      const 技能名 = String(配置.技能名 || '影蛇分身').trim() || '影蛇分身';
      const 召唤单位类型 = String(配置.召唤单位类型 || '分身').trim() || '分身';
      const 召唤物名称 = String(配置.召唤物名称 || 技能名).trim() || 技能名;
      const 属性继承比例 = 配置.属性继承比例 && typeof 配置.属性继承比例 === 'object'
        ? 配置.属性继承比例
        : { 力量: 0.72, 防御: 0.36, 敏捷: 0.68, 体力上限: 0.38, 魂力上限: 0.35, 精神力上限: 0.35 };
      const 技能 = normalizeSkillData({
        name: 技能名, 魂技名: 技能名, 技能分类: '召唤', 目标: '自身', 消耗: String(配置.消耗 || '魂力:90 | 精神力:45').trim(), 前摇: Number(配置.前摇 || 8),
        _效果数组: [{
          原型: '召唤生成', 目标: '自身', 生效方式: '独立生效', 召唤单位类型, 召唤物名称, 召唤数量: 1, 持续回合: Number(配置.持续回合 || 3),
          属性继承比例,
        }],
      }, 技能名);
      玩家.第1武魂.第1魂环.第1魂技 = 技能;
      敌人.第1武魂 = {
        表象名称: '敌方夹具武魂',
        第1魂环: {
          第1魂技: 战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'),
          第2魂技: 战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'),
        },
      };
      敌人.type = String(配置.敌人系别 || '敏攻系').trim() || '敏攻系';
      敌人.agi = Math.max(1, Number(配置.敌人敏捷 || 80));
      敌人.final = buildCombatFinalStats(敌人);
      const entry = {
        actor_name: 玩家.name,
        action_type: '释放魂技',
        skill: { 魂技名: 技能名, name: 技能名, 消耗: 技能.消耗 || '无' },
        __魂环路径: ['第1武魂', '第1魂环'],
        __魂技槽位: '第1魂技',
        target_name: 玩家.name,
      };
      let 首轮结果 = null;
      使用战斗回归桥接(combatData, { [玩家.name]: 玩家, 夹具友方: 构建战斗回归夹具单位('夹具友方', '辅助系'), [敌人.name]: 敌人 }, () => {
        首轮结果 = onPlayerAttack(`${技能名}\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
          dryRun: true,
          mode: 'single_round',
          combatData,
          intentMode: '点到为止',
        });
      });
      const 自然战斗数据 = 首轮结果?.combatData && typeof 首轮结果.combatData === 'object' ? 首轮结果.combatData : combatData;
      自然战斗数据.回合 = Math.max(1, Number(自然战斗数据.回合 || 1)) + 1;
      const 次轮日志 = [];
      const 召唤回合开始日志 = 执行召唤回合开始(自然战斗数据);
      if (召唤回合开始日志) 次轮日志.push(`[第${自然战斗数据.回合}回合] ${召唤回合开始日志}`);
      const 召唤行动日志 = 执行自主召唤行动轴回合(自然战斗数据);
      if (召唤行动日志) 次轮日志.push(`[第${自然战斗数据.回合}回合] ${召唤行动日志}`);
      const eventLedger = 自然战斗数据.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData: 自然战斗数据 });
      const result = {
        preview: true,
        intentText: String(配置.intentText || `使用【${技能名}】后观察召唤物下一回合行动。`).trim(),
        mode: 'battle_preview',
        battleMode: String(配置.battleMode || 'summon_closure').trim(),
        modeLabel: String(配置.modeLabel || '召唤闭环').trim(),
        intentMode: '点到为止',
        logs: [...(Array.isArray(首轮结果?.logs) ? 首轮结果.logs : []), ...次轮日志],
        roundsExecuted: 2,
        battleOutcome: { type: '未分胜负', label: String(配置.modeLabel || '召唤闭环').trim() },
        publicReportBlocks,
        publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
        combatData: 自然战斗数据,
        decisionTrace: collectBattleDecisionTrace(自然战斗数据),
        resolutionTrace: 自然战斗数据.__battleResolutionTrace || [],
        eventLedger,
      };
      return { result, combatData: 自然战斗数据, 玩家, 敌人 };
    }

    function 生成召唤默认模式调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      const 自然战斗数据 = combatData;
      const 创建召唤 = (动作名, 记录 = {}) => {
        const 召唤单位类型 = String(记录.召唤单位类型 || '魂兽').trim() || '魂兽';
        const 类型配置 = 读取召唤类型配置(召唤单位类型);
        const 行动模式 = String(记录.行动模式 || 类型配置.默认行动模式 || '协同攻击').trim();
        const 召唤物名称 = String(记录.召唤物名称 || '召唤物').trim();
        const 状态 = {
          类型: 'buff',
          层数: 1,
          duration: Math.max(1, Number(记录.持续回合 || 2)),
          描述: `由[${动作名}]生成的${召唤单位类型}`,
          召唤物: {
            召唤单位类型,
            召唤物名称,
            基础名称: 读取召唤编号基名(召唤物名称),
            召唤数量: 1,
            行动模式,
          },
        };
        if (记录.属性继承比例) 状态.召唤物.属性继承比例 = 记录.属性继承比例;
        else 状态.召唤物.强度 = Math.max(0.01, Number(记录.强度 || 1));
        const 状态键 = 写入持续原型唯一状态条目(玩家, `召唤:${召唤物名称}`, 状态);
        const 召唤单位 = 注册召唤运行态单位(自然战斗数据, 玩家, 状态键, 状态);
        if (召唤单位) 记账召唤生成事件(自然战斗数据, 玩家, 动作名, 召唤单位, { 召唤单位类型, 行动模式, 召唤物名称 });
      };
      创建召唤('本命兽契', {
        召唤单位类型: '本命召唤兽',
        召唤物名称: '玄契灵兽',
        持续回合: 3,
        属性继承比例: { 力量: 0.5, 防御: 0.48, 敏捷: 0.42, 体力上限: 0.5, 魂力上限: 0.42, 精神力上限: 0.45 },
      });
      创建召唤('唤影小兽', {
        召唤单位类型: '其他召唤生物',
        召唤物名称: '唤影小兽#1',
        持续回合: 2,
        强度: 1,
      });
      const eventLedger = 自然战斗数据.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData: 自然战斗数据 });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'summon_default_modes',
          modeLabel: '召唤默认模式',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '召唤默认模式' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData: 自然战斗数据,
          resolutionTrace: 自然战斗数据.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData: 自然战斗数据,
        玩家,
        敌人,
      };
    }

    function 生成二层防反闭环调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      const 主动作 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '裂地冲拳',
        actionType: 'attack',
        result: 'declared',
        meta: { reasonCode: 'ACTION_COMMITTED' },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'defend',
        round: 1,
        actorName: 敌人.name,
        targetName: 玩家.name,
        actionName: '承伤硬抗',
        actionType: 'reaction',
        sourceActionName: '裂地冲拳',
        sourceActionId: 主动作?.actionId || '',
        parentNodeId: 主动作?.chainNodeId || '',
        sourceNodeId: 主动作?.chainNodeId || '',
        result: 'guarded_hit',
        meta: {
          source: 'second_counter_fixture_target_reaction',
          reasonCode: 'REACTION_FAILED',
          reasonText: '以守势承接本次落点',
          damageAfterReaction: 96,
        },
      });
      const 主命中 = 写入战斗事件账本(combatData, {
        eventKind: 'hit_result',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '裂地冲拳',
        actionType: 'attack',
        sourceActionName: '裂地冲拳',
        sourceActionId: 主动作?.actionId || '',
        result: 'hit',
        effectCapability: { hasDamageEffect: true, effectKinds: ['伤害结算'] },
        meta: {
          damage: 96,
          rawDamage: 120,
          incomingDamage: 120,
          reactiveDamage: 96,
          finalDamage: 96,
          defenseValue: 60,
          defenseThreshold: 60,
          actualDefense: 60,
          formulaTrace: {
            damageType: '近身攻击',
            skillPower: 96,
            attackValue: 180,
            defenseValue: 60,
            baseDamage: 120,
            formulaText: '夹具预结算威力×攻防折算',
            meleeContactScale: 1.04,
            fusionDamageMult: 1,
          },
        },
      });
      const 防反窗口 = 写入战斗事件账本(combatData, {
        eventKind: 'counter_window',
        round: 1,
        actorName: 敌人.name,
        targetName: 玩家.name,
        actionName: '敌方截击',
        actionType: 'counter_window',
        sourceActionName: '裂地冲拳',
        sourceActionId: 主动作?.actionId || '',
        sourceNodeId: 主动作?.chainNodeId || '',
        result: 'opened',
        meta: { counterDepth: 1, reasonCode: 'COUNTER_WINDOW_OPENED' },
      });
      const 防反动作 = 写入战斗事件账本(combatData, {
        eventKind: 'counter',
        round: 1,
        actorName: 敌人.name,
        targetName: 玩家.name,
        actionName: '敌方截击',
        actionType: 'counter',
        sourceActionName: '裂地冲拳',
        sourceActionId: 主动作?.actionId || '',
        sourceNodeId: 主动作?.chainNodeId || '',
        result: 'success',
        meta: {
          damage: 42,
          counterDepth: 1,
          counterWindowEventId: 防反窗口?.eventId || '',
          counterWindowNodeId: 防反窗口?.chainNodeId || '',
        },
      });
      const 玩家起手事件 = 写入战斗事件账本(combatData, {
        eventKind: 'dodge',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '伺机闪避',
        actionType: 'counter_secondary_reaction',
        sourceActionName: '敌方截击',
        sourceActionId: 防反动作?.actionId || '',
        parentNodeId: 防反动作?.chainNodeId || '',
        sourceNodeId: 防反动作?.chainNodeId || '',
        result: 'failed',
        meta: {
          source: 'counter_secondary_reaction',
          counterActionEventId: 防反动作?.eventId || '',
          counterActionNodeId: 防反动作?.chainNodeId || '',
          counterDepth: 1,
          reactionMargin: 0.42,
          damageAfterReaction: 42,
        },
      });
      const 反防反窗口 = 写入战斗事件账本(combatData, {
        eventKind: 'counter_window',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '收招反压',
        actionType: 'counter_window',
        sourceActionName: '敌方截击',
        sourceActionId: 防反动作?.actionId || '',
        sourceNodeId: 防反动作?.chainNodeId || '',
        parentNodeId: 防反动作?.chainNodeId || '',
        result: 'opened',
        meta: { counterDepth: 2, reasonCode: 'COUNTER_WINDOW_OPENED' },
      });
      const 反防反动作 = 写入战斗事件账本(combatData, {
        eventKind: 'counter',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '收招反压',
        actionType: 'counter',
        sourceActionName: '敌方截击',
        sourceActionId: 防反动作?.actionId || '',
        sourceNodeId: 防反动作?.chainNodeId || '',
        parentNodeId: 防反动作?.chainNodeId || '',
        result: 'success',
        meta: {
          damage: 18,
          counterDepth: 2,
          counterWindowEventId: 反防反窗口?.eventId || '',
          counterWindowNodeId: 反防反窗口?.chainNodeId || '',
        },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'defend',
        round: 1,
        actorName: 敌人.name,
        targetName: 玩家.name,
        actionName: '收招转防',
        actionType: 'counter_secondary_reaction',
        sourceActionName: '收招反压',
        sourceActionId: 反防反动作?.actionId || '',
        parentNodeId: 反防反动作?.chainNodeId || '',
        sourceNodeId: 反防反动作?.chainNodeId || '',
        result: 'guarded_hit',
        meta: {
          source: 'counter_secondary_reaction',
          counterActionEventId: 反防反动作?.eventId || '',
          counterActionNodeId: 反防反动作?.chainNodeId || '',
          counterDepth: 2,
          reactionMargin: 0.31,
          damageAfterReaction: 18,
        },
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'second_counter',
          modeLabel: '二层防反闭环',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '二层防反闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
          debugAnchors: {
            mainActionNodeId: 主动作?.chainNodeId || '',
            mainHitNodeId: 主命中?.chainNodeId || '',
            counterActionNodeId: 防反动作?.chainNodeId || '',
          },
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成回合末状态聚合调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 2;
      const 友方敌人 = 构建战斗回归夹具单位('夹具敌人乙', '敏攻系');
      combatData.参战者.team_enemy = [敌人, 友方敌人];
      [敌人, 友方敌人].forEach((目标, index) => {
        写入战斗事件账本(combatData, {
          eventKind: 'state_tick',
          round: 2,
          actorName: 玩家.name,
          targetName: 目标.name || 目标.名称,
          targetId: `fixture-dot-target-${index + 1}`,
          actionName: '毒牙牵制',
          actionType: 'state_tick',
          sourceActionName: '毒牙牵制',
          sourceRound: 1,
          result: '损失',
          meta: {
            stateName: '中毒',
            amount: index === 0 ? 32 : 28,
            resource: '生命值',
            sourceActorName: 玩家.name,
            sourceActionName: '毒牙牵制',
            sourceRound: 1,
          },
        });
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'state_tick_aggregation',
          modeLabel: '回合末状态聚合',
          roundsExecuted: 2,
          battleOutcome: { type: '未分胜负', label: '回合末状态聚合' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成回合末恢复资源聚合调试结果() {
      const { combatData, 玩家 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 3;
      const 友方甲 = 构建战斗回归夹具单位('夹具友方甲', '辅助系');
      const 友方乙 = 构建战斗回归夹具单位('夹具友方乙', '防御系');
      combatData.参战者.team_player = [玩家, 友方甲, 友方乙];
      [友方甲, 友方乙].forEach((目标, index) => {
        写入战斗事件账本(combatData, {
          eventKind: 'state_tick',
          round: 3,
          actorName: 玩家.name,
          targetName: 目标.name || 目标.名称,
          targetId: `fixture-hot-target-${index + 1}`,
          actionName: '回春光环',
          actionType: 'state_tick',
          sourceActionName: '回春光环',
          sourceRound: 2,
          result: '恢复',
          meta: {
            stateName: '回春',
            amount: index === 0 ? 24 : 26,
            resource: '生命值',
            sourceActorName: 玩家.name,
            sourceActionName: '回春光环',
            sourceRound: 2,
          },
        });
      });
      [友方甲, 友方乙].forEach((目标, index) => {
        写入战斗事件账本(combatData, {
          eventKind: 'state_tick',
          round: 3,
          actorName: 玩家.name,
          targetName: 目标.name || 目标.名称,
          targetId: `fixture-resource-target-${index + 1}`,
          actionName: '聚魂阵',
          actionType: 'state_tick',
          sourceActionName: '聚魂阵',
          sourceRound: 2,
          result: '恢复',
          meta: {
            stateName: '聚魂',
            amount: index === 0 ? 15 : 18,
            resource: '魂力',
            sourceActorName: 玩家.name,
            sourceActionName: '聚魂阵',
            sourceRound: 2,
          },
        });
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'hot_resource_tick_aggregation',
          modeLabel: '回合末恢复与资源聚合',
          roundsExecuted: 3,
          battleOutcome: { type: '未分胜负', label: '回合末恢复与资源聚合' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
      };
    }

    function 生成状态抵抗闭环调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      const 起手 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '锁魄印',
        actionType: 'control',
        result: 'declared',
        meta: { reasonCode: 'ACTION_COMMITTED' },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'state_apply',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '锁魄印',
        actionType: 'control',
        sourceActionName: '锁魄印',
        sourceActionId: 起手?.actionId || '',
        sourceNodeId: 起手?.chainNodeId || '',
        result: 'resisted',
        reasonCode: 'REACTION_SUCCEEDED',
        reasonText: '目标抵住本次状态附着',
        meta: {
          stateName: '位移限制',
          successRate: 0.42,
          roll: 0.67,
          successRateReason: '硬控基础78% - 属性差36%（夹具抵抗）',
          successRateBreakdown: '附着成功率：42%，硬控基础78% - 属性差36%（夹具抵抗），检定67 > 42，未通过',
          driverAttr: '精神力',
          failureReason: 'state_resisted',
        },
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'state_resisted_closure',
          modeLabel: '状态抵抗闭环',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '状态抵抗闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成状态免疫闭环调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      const 起手 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '锁魄印',
        actionType: 'control',
        result: 'declared',
        meta: { reasonCode: 'ACTION_COMMITTED' },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'state_apply',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '锁魄印',
        actionType: 'control',
        sourceActionName: '锁魄印',
        sourceActionId: 起手?.actionId || '',
        sourceNodeId: 起手?.chainNodeId || '',
        result: 'immune',
        reasonCode: 'REACTION_SUCCEEDED',
        reasonText: '目标免疫本次状态附着',
        meta: {
          stateName: '位移限制',
          successRate: 0.42,
          roll: 0.99,
          successRateReason: '硬控基础78% - 属性差36%（夹具免疫）',
          successRateBreakdown: '附着成功率：42%，硬控基础78% - 属性差36%（夹具免疫），检定99 > 42，未通过',
          driverAttr: '精神力',
          failureReason: 'state_immune',
        },
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'state_immune_closure',
          modeLabel: '状态免疫闭环',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '状态免疫闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成普通召唤协同攻击调试结果() {
      const { combatData, 宿主, 敌人, 召唤单位 } = 构建召唤夹具战斗态({
        类型: '其他召唤生物',
        名称: '唤影小兽',
        行动模式: '协同攻击',
        强度: 1.4,
      });
      combatData.回合 = 2;
      const 宿主技能 = 战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 96, '近身攻击');
      const 宿主伤害 = 96;
      const 玩家起手事件 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 2,
        actorName: 宿主.name || 宿主.名称 || '',
        targetName: 敌人.name || 敌人.名称 || '',
        actionName: '裂地冲拳',
        actionType: 'attack',
        result: 'declared',
      });
      applyResolvedDamagePackage(宿主, { action_type: '释放魂技', type: '释放魂技', skill: 宿主技能 }, {
        targetResults: [{ target: 敌人, targetName: 敌人.name || 敌人.名称, damage: 宿主伤害, kind: 'primary' }],
        dmg: 宿主伤害,
        totalProjectedDamage: 宿主伤害,
      }, { primaryTarget: 敌人, combatData });
      const 协同日志 = 执行协同召唤追击(宿主, 敌人, 宿主伤害, combatData);
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '宿主造成有效伤害后观察普通召唤物协同追击。',
          mode: 'battle_preview',
          battleMode: 'summon_assist_closure',
          modeLabel: '普通召唤物协同攻击闭环',
          intentMode: '点到为止',
          logs: [协同日志].filter(Boolean),
          roundsExecuted: 2,
          battleOutcome: { type: '未分胜负', label: '普通召唤物协同攻击闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        宿主,
        敌人,
        召唤单位,
      };
    }

    function 生成多召唤协同攻击调试结果() {
      const { combatData, 宿主, 敌人, 召唤单位 } = 构建召唤夹具战斗态({
        类型: '其他召唤生物',
        名称: '唤影小兽甲',
        行动模式: '协同攻击',
        强度: 1.1,
      });
      const 第二状态 = {
        类型: 'buff',
        层数: 1,
        duration: 3,
        状态效果: {},
        战斗效果: createEmptyCombatEffectMap(),
        召唤物: {
          召唤单位类型: '其他召唤生物',
          召唤物名称: '唤影小兽乙',
          召唤数量: 1,
          行动模式: '协同攻击',
          强度: 1.2,
        },
      };
      宿主.状态效果['召唤:唤影小兽乙'] = 第二状态;
      const 第二单位 = 注册召唤运行态单位(combatData, 宿主, '召唤:唤影小兽乙', 第二状态);
      combatData.回合 = 2;
      const 宿主技能 = 战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 88, '近身攻击');
      const 宿主伤害 = 88;
      写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 2,
        actorName: 宿主.name || 宿主.名称 || '',
        targetName: 敌人.name || 敌人.名称 || '',
        actionName: '裂地冲拳',
        actionType: 'attack',
        result: 'declared',
      });
      applyResolvedDamagePackage(宿主, { action_type: '释放魂技', type: '释放魂技', skill: 宿主技能 }, {
        targetResults: [{ target: 敌人, targetName: 敌人.name || 敌人.名称, damage: 宿主伤害, kind: 'primary' }],
        dmg: 宿主伤害,
        totalProjectedDamage: 宿主伤害,
      }, { primaryTarget: 敌人, combatData });
      const 协同日志 = 执行协同召唤追击(宿主, 敌人, 宿主伤害, combatData);
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '宿主造成有效伤害后观察多只普通召唤物协同追击。',
          mode: 'battle_preview',
          battleMode: 'multi_summon_assist_closure',
          modeLabel: '多召唤物协同攻击闭环',
          intentMode: '点到为止',
          logs: [协同日志].filter(Boolean),
          roundsExecuted: 2,
          battleOutcome: { type: '未分胜负', label: '多召唤物协同攻击闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        宿主,
        敌人,
        召唤单位,
        第二单位,
      };
    }

    function 生成团战召唤行动轴调试结果() {
      const { combatData, 宿主, 敌人, 召唤单位 } = 构建召唤夹具战斗态({
        类型: '分身',
        名称: '团战影蛇分身',
        行动模式: '自主行动',
        继承属性比例: 0.45,
      });
      combatData.回合 = 2;
      combatData.战斗类型 = '团战召唤行动轴夹具';
      召唤单位.创建回合 = 1;
      召唤单位.行动模式 = '自主行动';
      const queue = generateActionQueue(combatData);
      const declarationLogs = [];
      记录团战行动轴声明(queue, combatData, declarationLogs);
      const summonEntry = queue.find(entry => entry?.char?.召唤键 === 召唤单位.召唤键);
      const turnResult = summonEntry ? runActorTurn(summonEntry, { combatData }) : null;
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '团战行动轴纳入自主行动召唤物并走同源结算。',
          mode: 'battle_preview',
          battleMode: 'team_summon_action_axis',
          modeLabel: '团战召唤物行动轴闭环',
          intentMode: '点到为止',
          logs: [...declarationLogs, turnResult?.log].filter(Boolean),
          roundsExecuted: 2,
          battleOutcome: { type: '未分胜负', label: '团战召唤物行动轴闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
          queueActors: queue.map(entry => ({
            name: entry?.char?.name || entry?.char?.名称 || '',
            side: entry?.side || '',
            isSummon: !!entry?.char?.召唤键,
            declaredActionName: normalizeBattleActionDisplayName(entry?.__declaredActionName || ''),
            declaredTargetName: String(entry?.__declaredTargetName || '').trim(),
          })),
          summonTurnResult: turnResult,
        },
        combatData,
        宿主,
        敌人,
        召唤单位,
        queue,
        turnResult,
      };
    }

    function 生成自动行动起手闭环调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '自动行动起手闭环夹具';
      玩家.type = '强攻系';
      玩家.第1武魂 ||= { 表象名称: '夹具武魂' };
      玩家.第1武魂.第1魂环 ||= {};
      玩家.第1武魂.第1魂环.第1魂技 = 战斗回归输出魂技('自动强攻', '敌方单体', 8, 72, '近身攻击');
      const queue = generateActionQueue(combatData);
      const declarationLogs = [];
      记录团战行动轴声明(queue, combatData, declarationLogs);
      const actorEntry = queue.find(entry => !entry?.char?.召唤键 && entry?.char === 玩家) || queue.find(entry => !entry?.char?.召唤键);
      const turnResult = actorEntry ? runActorTurn(actorEntry, { combatData }) : null;
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '自动行动必须在执行前写入 action_start 并进入 action_decision trace。',
          mode: 'battle_preview',
          battleMode: 'auto_actor_action_start',
          modeLabel: '自动行动起手闭环',
          intentMode: '点到为止',
          logs: [...declarationLogs, turnResult?.log].filter(Boolean),
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '自动行动起手闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
          queueActors: queue.map(entry => ({
            name: entry?.char?.name || entry?.char?.名称 || '',
            side: entry?.side || '',
            isSummon: !!entry?.char?.召唤键,
            declaredActionName: normalizeBattleActionDisplayName(entry?.__declaredActionName || ''),
            declaredTargetName: String(entry?.__declaredTargetName || '').trim(),
          })),
          actorName: actorEntry?.char?.name || actorEntry?.char?.名称 || '',
          turnResult,
        },
        combatData,
        玩家,
        敌人,
        queue,
        turnResult,
      };
    }

    function 生成保底资源回稳调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 5;
      combatData.战斗类型 = '保底资源回稳夹具';
      玩家.sp = 0;
      玩家.魂力 = 0;
      玩家.sta = 0;
      玩家.vit = 0;
      玩家.体力 = 0;
      玩家.属性 ||= {};
      玩家.属性.sp = 0;
      玩家.属性.魂力 = 0;
      玩家.属性.sta = 0;
      玩家.属性.vit = 0;
      玩家.属性.体力 = 0;
      写入战斗事件账本(combatData, {
        eventKind: 'blocked_action',
        round: 5,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '战术待机',
        actionType: '战术待机',
        result: 'no_effect',
        failReason: '夹具玩家的【战术待机】稳住身位',
        primaryOutcome: 'no_effect',
        meta: {
          source: 'auto_actor',
          reasonCode: 'NO_EFFECTIVE_OPENING',
          reasonText: '绝对保底动作',
        },
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const runtimeSnapshot = 补水战斗运行态(combatData, eventLedger, { source: 'fallback_resource_fixture' });
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'fallback_resource_recovery',
          modeLabel: '保底资源回稳',
          roundsExecuted: 5,
          battleOutcome: { type: '未分胜负', label: '保底资源回稳' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
          runtimeSnapshot: cloneBattleRuntimeAuditSnapshot(runtimeSnapshot),
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成非敌对动作不触发反应调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '非敌对反应过滤夹具';
      const 起手 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        targetName: 玩家.name,
        actionName: '香喷喷牛肉干',
        actionType: '造物承载',
        result: 'declared',
        primaryOutcome: 'item_created',
        meta: {
          isHostile: false,
          reasonCode: 'ACTION_COMMITTED',
          reasonText: '非敌对造物动作不打开常规反应窗口',
        },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'create',
        round: 1,
        actorName: 玩家.name,
        targetName: 玩家.name,
        actionName: '香喷喷牛肉干',
        actionType: '造物承载',
        sourceActionName: '香喷喷牛肉干',
        sourceActionId: 起手?.actionId || '',
        sourceNodeId: 起手?.chainNodeId || '',
        result: 'created',
        primaryOutcome: 'item_created',
        createdName: '香喷喷牛肉干',
        count: 1,
        meta: {
          isHostile: false,
          createdName: '香喷喷牛肉干',
          count: 1,
          reasonCode: 'ACTION_COMMITTED',
        },
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'non_hostile_reaction_filter',
          modeLabel: '非敌对反应过滤',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '非敌对反应过滤' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成目标池硬规则调试结果() {
      const { combatData } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '目标池硬规则夹具';
      const 攻击者 = 构建战斗回归夹具单位('夹具敌方刺客', '敏攻系');
      const 嘲讽者 = 构建战斗回归夹具单位('夹具嘲讽肉盾', '防御系');
      const 隐匿者 = 构建战斗回归夹具单位('夹具隐匿刺客', '敏攻系');
      攻击者.状态效果 ||= {};
      攻击者.状态效果.被嘲讽 = {
        类型: 'debuff',
        状态名称: '嘲讽',
        强制目标名: 嘲讽者.name,
        duration: 1,
      };
      隐匿者.状态效果 ||= {};
      隐匿者.状态效果.隐匿 = {
        类型: 'buff',
        状态名称: '隐匿',
        战斗效果: { stealth_level: 1, 探查屏蔽: false },
        duration: 1,
      };
      攻击者.men = 100;
      攻击者.men_max = 100;
      隐匿者.men = 1000;
      隐匿者.men_max = 1000;
      const 单体技能 = 战斗回归输出魂技('锁定打击', '敌方单体', 8, 72, '近身攻击');
      const 嘲讽索敌 = findTarget(攻击者, [隐匿者, 嘲讽者], combatData);
      delete 攻击者.状态效果.被嘲讽;
      const 隐匿过滤索敌 = chooseEnemyTargetForSkill(攻击者, [隐匿者, 嘲讽者], 单体技能, null, combatData);
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'target_pool_hard_rules',
          modeLabel: '目标池硬规则',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '目标池硬规则' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
          targetRuleSnapshot: {
            tauntSelected: 嘲讽索敌?.name || 嘲讽索敌?.名称 || '',
            stealthFilteredSelected: 隐匿过滤索敌?.name || 隐匿过滤索敌?.名称 || '',
            tauntName: 嘲讽者.name,
            stealthName: 隐匿者.name,
          },
        },
        combatData,
        攻击者,
        嘲讽者,
        隐匿者,
      };
    }

    function 生成上限拦截调试结果() {
      const { combatData, 玩家 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '上限拦截夹具';
      写入战斗事件账本(combatData, {
        eventKind: 'failed_action',
        round: 1,
        actorName: 玩家.name,
        targetName: 玩家.name,
        actionName: '影蛇分身',
        actionType: '召唤生成',
        result: 'fail',
        failReason: '造物已达上限',
        reasonCode: 'CAP_REACHED',
        primaryOutcome: 'cap_reached',
        meta: {
          reasonCode: 'CAP_REACHED',
          primaryOutcome: 'cap_reached',
          summonName: '影蛇分身',
          summonType: '分身',
          currentCount: 2,
          cap: 2,
        },
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'cap_reached',
          modeLabel: '上限拦截',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '上限拦截' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
      };
    }

    function 生成状态生命周期规则调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '状态生命周期规则夹具';
      玩家.状态效果 ||= {};
      玩家.状态效果.专注 = {
        类型: 'buff',
        状态名称: '专注',
        duration: 1,
        战斗效果: { ...createEmptyCombatEffectMap(), cast_speed_bonus: 0.05 },
      };
      玩家.状态效果.凝滞 = {
        类型: 'debuff',
        状态名称: '凝滞',
        duration: 2,
        战斗效果: { ...createEmptyCombatEffectMap(), cast_speed_penalty: 0.08 },
      };
      const 起手 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        actorSide: 'player',
        targetName: 玩家.name,
        actionName: '状态调律',
        actionType: '辅助',
        result: 'declared',
        primaryOutcome: 'action_committed',
      });
      写入战斗事件账本(combatData, {
        eventKind: 'state_replace',
        round: 1,
        actorName: 玩家.name,
        actorSide: 'player',
        targetName: 玩家.name,
        actionName: '状态调律',
        actionType: 'state_replace',
        sourceActionName: '状态调律',
        sourceActionId: 起手?.actionId || '',
        result: 'refresh',
        primaryOutcome: 'state_refresh',
        duration: 3,
        calculationTrace: [
          ['stateName', '专注'],
          ['stackMode', 'refresh_duration'],
          ['previousDuration', 1],
          ['nextDuration', 3],
        ],
        meta: {
          stateName: '专注',
          previousDuration: 1,
          nextDuration: 3,
          calculationTrace: [
            ['stateName', '专注'],
            ['stackMode', 'refresh_duration'],
            ['previousDuration', 1],
            ['nextDuration', 3],
          ],
          stackMode: 'refresh_duration',
          replaceReason: 'same_state_refresh_duration',
        },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'state_remove',
        round: 1,
        actorName: 玩家.name,
        actorSide: 'player',
        targetName: 玩家.name,
        actionName: '状态调律',
        actionType: 'state_remove',
        sourceActionName: '状态调律',
        sourceActionId: 起手?.actionId || '',
        result: 'removed',
        primaryOutcome: 'state_remove',
        calculationTrace: [
          ['stateName', '凝滞'],
          ['stackMode', 'cleanse_or_dispel'],
          ['previousDuration', 2],
          ['nextDuration', 0],
        ],
        meta: {
          stateName: '凝滞',
          removedStates: ['凝滞'],
          calculationTrace: [
            ['stateName', '凝滞'],
            ['stackMode', 'cleanse_or_dispel'],
            ['previousDuration', 2],
            ['nextDuration', 0],
          ],
          stackMode: 'cleanse_or_dispel',
          replaceReason: 'state_remove_effect',
          previousDuration: 2,
          nextDuration: 0,
        },
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          mode: 'battle_preview',
          battleMode: 'state_lifecycle_rules',
          modeLabel: '状态生命周期规则',
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '状态生命周期规则' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
          turnResult: null,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成预声明目标丢失调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '预声明目标丢失夹具';
      玩家.type = '强攻系';
      玩家.第1武魂 ||= { 表象名称: '夹具武魂' };
      玩家.第1武魂.第1魂环 ||= {};
      玩家.第1武魂.第1魂环.第1魂技 = 战斗回归输出魂技('预声明强攻', '敌方单体', 8, 72, '近身攻击');
      const 替补敌人 = 构建战斗回归夹具单位('替补敌人', '防御系');
      combatData.参战者.team_enemy = [敌人, 替补敌人];
      const queue = generateActionQueue(combatData);
      const declarationLogs = [];
      记录团战行动轴声明(queue, combatData, declarationLogs);
      const actorEntry = queue.find(entry => !entry?.char?.召唤键 && entry?.char === 玩家) || queue.find(entry => !entry?.char?.召唤键);
      const declaredTargetName = String(actorEntry?.__declaredTargetName || '').trim();
      if (declaredTargetName && isSameBattleReportName(declaredTargetName, 敌人.name || 敌人.名称 || '')) {
        设置战斗血量值(敌人, 0);
      } else if (declaredTargetName && isSameBattleReportName(declaredTargetName, 替补敌人.name || 替补敌人.名称 || '')) {
        设置战斗血量值(替补敌人, 0);
      }
      const turnResult = actorEntry ? runActorTurn(actorEntry, { combatData }) : null;
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '预声明目标在行动前失效时必须显式闭合为目标丢失，而不是静默换靶。',
          mode: 'battle_preview',
          battleMode: 'declared_target_lost',
          modeLabel: '预声明目标丢失闭环',
          intentMode: '点到为止',
          logs: [...declarationLogs, turnResult?.log].filter(Boolean),
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '预声明目标丢失闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
          queueActors: queue.map(entry => ({
            name: entry?.char?.name || entry?.char?.名称 || '',
            side: entry?.side || '',
            isSummon: !!entry?.char?.召唤键,
            declaredActionName: normalizeBattleActionDisplayName(entry?.__declaredActionName || ''),
            declaredTargetName: String(entry?.__declaredTargetName || '').trim(),
          })),
          actorName: actorEntry?.char?.name || actorEntry?.char?.名称 || '',
          declaredTargetName,
          turnResult,
        },
        combatData,
        玩家,
        敌人,
        替补敌人,
        queue,
        turnResult,
      };
    }

    function 生成多宿主召唤协同调试结果() {
      const { combatData, 宿主, 敌人, 召唤单位 } = 构建召唤夹具战斗态({
        类型: '其他召唤生物',
        名称: '宿主甲影兽',
        行动模式: '协同攻击',
        强度: 1.05,
      });
      宿主.name = '夹具宿主甲';
      宿主.名称 = '夹具宿主甲';
      召唤单位.宿主名 = '夹具宿主甲';
      召唤单位.__宿主 = 宿主;
      const 宿主乙 = 构建召唤夹具单位('夹具宿主乙', '玩家');
      combatData.参战者.team_player.push(宿主乙);
      const 乙状态键 = '召唤:宿主乙影兽';
      const 乙状态 = {
        类型: 'buff',
        层数: 1,
        duration: 3,
        状态效果: {},
        战斗效果: createEmptyCombatEffectMap(),
        召唤物: {
          召唤单位类型: '其他召唤生物',
          召唤物名称: '宿主乙影兽',
          召唤数量: 1,
          行动模式: '协同攻击',
          强度: 1.15,
        },
      };
      宿主乙.状态效果[乙状态键] = 乙状态;
      const 乙召唤单位 = 注册召唤运行态单位(combatData, 宿主乙, 乙状态键, 乙状态);
      combatData.回合 = 2;
      const 甲技能 = 战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 84, '近身攻击');
      const 乙技能 = 战斗回归输出魂技('穿影击', '敌方单体', 8, 72, '近身攻击');
      写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 2,
        actorName: 宿主.name || 宿主.名称 || '',
        targetName: 敌人.name || 敌人.名称 || '',
        actionName: '裂地冲拳',
        actionType: 'attack',
        result: 'declared',
      });
      applyResolvedDamagePackage(宿主, { action_type: '释放魂技', type: '释放魂技', skill: 甲技能 }, {
        targetResults: [{ target: 敌人, targetName: 敌人.name || 敌人.名称, damage: 84, kind: 'primary' }],
        dmg: 84,
        totalProjectedDamage: 84,
      }, { primaryTarget: 敌人, combatData });
      const 甲协同日志 = 执行协同召唤追击(宿主, 敌人, 84, combatData);
      写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 2,
        actorName: 宿主乙.name || 宿主乙.名称 || '',
        targetName: 敌人.name || 敌人.名称 || '',
        actionName: '穿影击',
        actionType: 'attack',
        result: 'declared',
      });
      applyResolvedDamagePackage(宿主乙, { action_type: '释放魂技', type: '释放魂技', skill: 乙技能 }, {
        targetResults: [{ target: 敌人, targetName: 敌人.name || 敌人.名称, damage: 72, kind: 'primary' }],
        dmg: 72,
        totalProjectedDamage: 72,
      }, { primaryTarget: 敌人, combatData });
      const 乙协同日志 = 执行协同召唤追击(宿主乙, 敌人, 72, combatData);
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '两个宿主分别造成有效伤害后观察各自召唤物协同追击归属。',
          mode: 'battle_preview',
          battleMode: 'multi_host_summon_assist_closure',
          modeLabel: '多宿主召唤协同闭环',
          intentMode: '点到为止',
          logs: [甲协同日志, 乙协同日志].filter(Boolean),
          roundsExecuted: 2,
          battleOutcome: { type: '未分胜负', label: '多宿主召唤协同闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        宿主甲: 宿主,
        宿主乙,
        敌人,
        甲召唤单位: 召唤单位,
        乙召唤单位,
      };
    }

    function 生成敌我双方召唤协同调试结果() {
      const { combatData, 宿主, 敌人, 召唤单位 } = 构建召唤夹具战斗态({
        类型: '其他召唤生物',
        名称: '玩家影兽',
        行动模式: '协同攻击',
        强度: 1.05,
      });
      宿主.name = '玩家宿主';
      宿主.名称 = '玩家宿主';
      敌人.name = '敌方宿主';
      敌人.名称 = '敌方宿主';
      召唤单位.宿主名 = '玩家宿主';
      召唤单位.__宿主 = 宿主;
      const 敌方状态键 = '召唤:敌方影兽';
      const 敌方状态 = {
        类型: 'buff',
        层数: 1,
        duration: 3,
        状态效果: {},
        战斗效果: createEmptyCombatEffectMap(),
        召唤物: {
          召唤单位类型: '其他召唤生物',
          召唤物名称: '敌方影兽',
          召唤数量: 1,
          行动模式: '协同攻击',
          强度: 1.1,
        },
      };
      敌人.状态效果[敌方状态键] = 敌方状态;
      const 敌方召唤单位 = 注册召唤运行态单位(combatData, 敌人, 敌方状态键, 敌方状态);
      combatData.回合 = 2;
      const 玩家技能 = 战斗回归输出魂技('裂地冲拳', '敌方单体', 8, 82, '近身攻击');
      const 敌方技能 = 战斗回归输出魂技('影刃反扑', '敌方单体', 8, 76, '近身攻击');
      const 玩家起手事件 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 2,
        actorName: 宿主.name || 宿主.名称 || '',
        targetName: 敌人.name || 敌人.名称 || '',
        actionName: '裂地冲拳',
        actionType: 'attack',
        result: 'declared',
      });
      写入战斗事件账本(combatData, {
        eventKind: 'pass',
        round: 2,
        actorName: 敌人.name || 敌人.名称 || '',
        targetName: 宿主.name || 宿主.名称 || '',
        actionName: '临场应对',
        actionType: 'reaction',
        sourceActionName: '裂地冲拳',
        sourceActionId: 玩家起手事件?.actionId || '',
        parentNodeId: 玩家起手事件?.chainNodeId || '',
        sourceNodeId: 玩家起手事件?.chainNodeId || '',
        result: 'reaction_failed',
        meta: { source: 'summon_assist_fixture_reaction', reasonCode: 'REACTION_FAILED', reasonText: '未形成有效应招动作' },
      });
      applyResolvedDamagePackage(宿主, { action_type: '释放魂技', type: '释放魂技', skill: 玩家技能 }, {
        targetResults: [{ target: 敌人, targetName: 敌人.name || 敌人.名称, damage: 82, kind: 'primary' }],
        dmg: 82,
        totalProjectedDamage: 82,
      }, { primaryTarget: 敌人, combatData });
      const 玩家协同日志 = 执行协同召唤追击(宿主, 敌人, 82, combatData);
      const 敌方起手事件 = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 2,
        actorName: 敌人.name || 敌人.名称 || '',
        targetName: 宿主.name || 宿主.名称 || '',
        actionName: '影刃反扑',
        actionType: 'attack',
        result: 'declared',
      });
      写入战斗事件账本(combatData, {
        eventKind: 'pass',
        round: 2,
        actorName: 宿主.name || 宿主.名称 || '',
        targetName: 敌人.name || 敌人.名称 || '',
        actionName: '临场应对',
        actionType: 'reaction',
        sourceActionName: '影刃反扑',
        sourceActionId: 敌方起手事件?.actionId || '',
        parentNodeId: 敌方起手事件?.chainNodeId || '',
        sourceNodeId: 敌方起手事件?.chainNodeId || '',
        result: 'reaction_failed',
        meta: { source: 'summon_assist_fixture_reaction', reasonCode: 'REACTION_FAILED', reasonText: '未形成有效应招动作' },
      });
      applyResolvedDamagePackage(敌人, { action_type: '释放魂技', type: '释放魂技', skill: 敌方技能 }, {
        targetResults: [{ target: 宿主, targetName: 宿主.name || 宿主.名称, damage: 76, kind: 'primary' }],
        dmg: 76,
        totalProjectedDamage: 76,
      }, { primaryTarget: 宿主, combatData });
      const 敌方协同日志 = 执行协同召唤追击(敌人, 宿主, 76, combatData);
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '敌我双方宿主分别造成有效伤害后观察各自协同召唤物阵营归属。',
          mode: 'battle_preview',
          battleMode: 'opposing_side_summon_assist_closure',
          modeLabel: '敌我双方召唤协同闭环',
          intentMode: '点到为止',
          logs: [玩家协同日志, 敌方协同日志].filter(Boolean),
          roundsExecuted: 2,
          battleOutcome: { type: '未分胜负', label: '敌我双方召唤协同闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家宿主: 宿主,
        敌方宿主: 敌人,
        玩家召唤单位: 召唤单位,
        敌方召唤单位,
      };
    }

    function 生成敌我双方自主召唤行动轴调试结果() {
      const 原随机 = Math.random;
      try {
        let seed = 87321;
        Math.random = () => {
          seed = (seed * 233 + 7919) % 104729;
          return seed / 104729;
        };
        const { combatData, 宿主, 敌人, 召唤单位 } = 构建召唤夹具战斗态({
          类型: '分身',
          名称: '玩家自主影兽',
          行动模式: '自主行动',
          继承属性比例: 0.42,
        });
        宿主.name = '玩家宿主';
        宿主.名称 = '玩家宿主';
        敌人.name = '敌方宿主';
        敌人.名称 = '敌方宿主';
        召唤单位.name = '玩家自主影兽';
        召唤单位.名称 = '玩家自主影兽';
        召唤单位.宿主名 = '玩家宿主';
        召唤单位.__宿主 = 宿主;
        召唤单位.创建回合 = 1;
        宿主.__行动防反禁用 = true;
        敌人.__行动防反禁用 = true;
        const 玩家召唤记忆 = ensureActorDecisionMemory(召唤单位);
        玩家召唤记忆.focus_target = 敌人.name;
        玩家召唤记忆.focus_reason = 'shared_vision_focus';
        玩家召唤记忆.focus_ttl = 2;
        const 敌方状态键 = '召唤:敌方自主影兽';
        const 敌方状态 = {
          类型: 'buff',
          层数: 1,
          duration: 3,
          状态效果: {},
          战斗效果: createEmptyCombatEffectMap(),
          召唤物: {
            召唤单位类型: '分身',
            召唤物名称: '敌方自主影兽',
            召唤数量: 1,
            行动模式: '自主行动',
            继承属性比例: 0.4,
          },
        };
        敌人.状态效果[敌方状态键] = 敌方状态;
        const 敌方召唤单位 = 注册召唤运行态单位(combatData, 敌人, 敌方状态键, 敌方状态);
        敌方召唤单位.创建回合 = 1;
        const 敌方召唤记忆 = ensureActorDecisionMemory(敌方召唤单位);
        敌方召唤记忆.focus_target = 宿主.name;
        敌方召唤记忆.focus_reason = 'shared_vision_focus';
        敌方召唤记忆.focus_ttl = 2;
        combatData.回合 = 2;
        combatData.战斗类型 = '敌我双方自主召唤行动轴夹具';
        const queue = generateActionQueue(combatData);
        const declarationLogs = [];
        记录团战行动轴声明(queue, combatData, declarationLogs);
        const summonEntries = queue.filter(entry => ['玩家自主影兽', '敌方自主影兽'].includes(String(entry?.char?.name || entry?.char?.名称 || '').trim()));
        const turnResults = summonEntries.map(entry => runActorTurn(entry, { combatData })).filter(Boolean);
        const eventLedger = combatData.__battleEventLedger || [];
        const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
        return {
          result: {
            preview: true,
            intentText: '敌我双方自主行动召唤物进入团战行动轴并分别锁定敌对阵营。',
            mode: 'battle_preview',
            battleMode: 'opposing_side_summon_action_axis',
            modeLabel: '敌我双方自主召唤行动轴闭环',
            intentMode: '点到为止',
            logs: [...declarationLogs, ...turnResults.map(item => item?.log).filter(Boolean)],
            roundsExecuted: 2,
            battleOutcome: { type: '未分胜负', label: '敌我双方自主召唤行动轴闭环' },
            publicReportBlocks,
            publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
            combatData,
            decisionTrace: collectBattleDecisionTrace(combatData),
            resolutionTrace: combatData.__battleResolutionTrace || [],
            eventLedger,
            queueActors: queue.map(entry => ({
              name: entry?.char?.name || entry?.char?.名称 || '',
              side: entry?.side || '',
              isSummon: !!entry?.char?.召唤键,
              declaredActionName: normalizeBattleActionDisplayName(entry?.__declaredActionName || ''),
              declaredTargetName: String(entry?.__declaredTargetName || '').trim(),
            })),
            summonTurnResults: turnResults,
          },
          combatData,
          玩家宿主: 宿主,
          敌方宿主: 敌人,
          玩家召唤单位: 召唤单位,
          敌方召唤单位,
          queue,
          turnResults,
        };
      } finally {
        Math.random = 原随机;
      }
    }

    function 生成战斗判定样本文本(数量 = 100) {
      const 技能库 = [
        () => 构建战斗回归第一魂技(战斗回归第一魂技默认名, '魂力:120'),
        () => 战斗回归输出魂技('裂地冲拳', '敌方单体', 10, 88, '近身攻击'),
        () => 战斗回归输出魂技('流火散射', '敌方群体', 14, 96, '远程攻击'),
        () => normalizeSkillData({
          name: '毒牙牵制', 魂技名: '毒牙牵制', 技能分类: '输出', 目标: '敌方单体', 消耗: '魂力:60', 前摇: 8,
          _效果数组: [
            { 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 80, 伤害类型: '近身攻击' },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '跟随主原型', 状态: '中毒', 状态名称: '中毒', 持续回合: 2, 计算层效果: { dot_damage_ratio: 0.03 } },
          ],
        }, '毒牙牵制'),
        () => normalizeSkillData({
          name: '裂伤追咬', 魂技名: '裂伤追咬', 技能分类: '输出', 目标: '敌方单体', 消耗: '魂力:70', 前摇: 8,
          _效果数组: [
            { 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 90, 伤害类型: '近身攻击' },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '跟随主原型', 状态: '重度流血', 状态名称: '重度流血', 持续回合: 2, 计算层效果: { dot_damage: 80 } },
          ],
        }, '裂伤追咬'),
        () => normalizeSkillData({
          name: '锁魄印', 魂技名: '锁魄印', 技能分类: '控制', 目标: '敌方单体', 消耗: '魂力:80', 前摇: 10,
          _效果数组: [{ 原型: '状态施加', 目标: '敌方单体', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.25, lock_level: 1 } }],
        }, '锁魄印'),
        () => normalizeSkillData({
          name: '第二魂技·青影蛇群', 魂技名: '第二魂技·青影蛇群', 技能分类: '控制', 目标: '敌方群体', 消耗: '魂力:140', 前摇: 12,
          _效果数组: [
            { 原型: '伤害结算', 目标: '群体', 生效方式: '独立生效', 威力倍率: 72, 伤害类型: '远程攻击', 命中修正: 0.12 },
            { 原型: '状态施加', 目标: '敌方群体', 生效方式: '跟随主原型', 状态: '迟缓', 状态名称: '迟缓', 持续回合: 2, 计算层效果: { cast_speed_penalty: 0.18, dodge_penalty: 0.08 } },
          ],
        }, '第二魂技·青影蛇群'),
        () => normalizeSkillData({
          name: '香喷喷牛肉干', 魂技名: '香喷喷牛肉干', 技能分类: '辅助', 目标: '自身', 承载方式: '造物承载', 消耗: '魂力:50', 前摇: 6,
          _效果数组: [{ 数量: 1, 描述: '香喷喷牛肉干', 使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '体力', 数值: '+80' }] }],
        }, '香喷喷牛肉干'),
      ];
      const 原随机 = Math.random;
      const 块 = [];
      const 构建自然召唤闭环样本 = () => {
        const 结果 = 生成自然召唤闭环调试结果()?.result || {};
        return 导出战斗记录可见文本(结果, 'preview');
      };
      const 构建可信战术侧写样本 = () => {
        const 结果 = 生成可信战术侧写调试结果()?.result || {};
        return 导出战斗记录可见文本(结果, 'preview');
      };
      try {
        for (let index = 1; index <= Math.max(1, Number(数量 || 100)); index += 1) {
          let seed = index * 9301 + 49297;
          Math.random = () => {
            seed = (seed * 233 + 7919) % 104729;
            return seed / 104729;
          };
          const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
          const 技能 = 技能库[(index - 1) % 技能库.length]();
          玩家.第1武魂.第1魂环.第1魂技 = 技能;
          敌人.第1武魂 = {
            表象名称: '敌方夹具武魂',
            第1魂环: {
              第1魂技: 战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'),
              第2魂技: 战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'),
            },
          };
          if (/第二魂技|青影蛇群/.test(String(技能.name || 技能.魂技名 || ''))) {
            玩家.第1武魂.第2魂环 ||= {};
            玩家.第1武魂.第2魂环.第2魂技 = 技能;
          }
          玩家.type = index % 5 === 0 ? '敏攻系' : '强攻系';
          敌人.type = '敏攻系';
          敌人.agi = index % 4 === 0 ? 980 : 220 + (index % 6) * 40;
          敌人.final = buildCombatFinalStats(敌人);
          if (index % 9 === 0) recordActorActionWhiffedByDodge(玩家, '普通攻击', 敌人);
          const entry = {
            actor_name: 玩家.name,
            action_type: '释放魂技',
            skill: { 魂技名: 技能.name || 技能.魂技名, name: 技能.name || 技能.魂技名, 消耗: 技能.消耗 || '无' },
            __魂环路径: /第二魂技|青影蛇群/.test(String(技能.name || 技能.魂技名 || '')) ? ['第1武魂', '第2魂环'] : ['第1武魂', '第1魂环'],
            __魂技槽位: /第二魂技|青影蛇群/.test(String(技能.name || 技能.魂技名 || '')) ? '第2魂技' : '第1魂技',
            target_name: /自身|友方|造物|食物|药剂|召唤/.test(String(技能.目标 || '') + String(技能.技能分类 || '') + String(技能.承载方式 || '')) ? 玩家.name : 敌人.name,
          };
          const 是召唤技能 = getSkillEffects(技能).some(effect => String(effect?.原型 || '').trim() === '召唤生成');
          let result = null;
          使用战斗回归桥接(combatData, { [玩家.name]: 玩家, 夹具友方: 构建战斗回归夹具单位('夹具友方', '辅助系'), [敌人.name]: 敌人 }, () => {
            result = onPlayerAttack(`${技能.name || 技能.魂技名}\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
              dryRun: true,
              mode: !是召唤技能 && index % 6 === 0 ? 'multi_round' : 'single_round',
              combatData,
              intentMode: '点到为止',
              autoContinueConfig: { maxRounds: 4, stopDamagePercent: 100, continueChancePercent: 100 },
            });
          });
          const 可见文本 = 导出战斗记录可见文本(result, 'preview');
          块.push(`==================== 样本 ${String(index).padStart(3, '0')} ====================\n${可见文本.trim()}`);
        }
      } finally {
        Math.random = 原随机;
      }
      块.push(`==================== 样本 SUMMON_NATURAL ====================\n${构建自然召唤闭环样本().trim()}`);
      块.push(`==================== 样本 TRUSTED_TACTICAL_NARRATION ====================\n${构建可信战术侧写样本().trim()}`);
      return 块.join('\n\n');
    }

    function 生成战斗判定样本结果(索引 = 1) {
      const 技能库 = [
        () => 构建战斗回归第一魂技(战斗回归第一魂技默认名, '魂力:120'),
        () => 战斗回归输出魂技('裂地冲拳', '敌方单体', 10, 88, '近身攻击'),
        () => 战斗回归输出魂技('流火散射', '敌方群体', 14, 96, '远程攻击'),
        () => normalizeSkillData({
          name: '毒牙牵制', 魂技名: '毒牙牵制', 技能分类: '输出', 目标: '敌方单体', 消耗: '魂力:60', 前摇: 8,
          _效果数组: [
            { 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 80, 伤害类型: '近身攻击' },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '跟随主原型', 状态: '中毒', 状态名称: '中毒', 持续回合: 2, 计算层效果: { dot_damage_ratio: 0.03 } },
          ],
        }, '毒牙牵制'),
        () => normalizeSkillData({
          name: '裂伤追咬', 魂技名: '裂伤追咬', 技能分类: '输出', 目标: '敌方单体', 消耗: '魂力:70', 前摇: 8,
          _效果数组: [
            { 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率: 90, 伤害类型: '近身攻击' },
            { 原型: '状态施加', 目标: '敌方单体', 生效方式: '跟随主原型', 状态: '重度流血', 状态名称: '重度流血', 持续回合: 2, 计算层效果: { dot_damage: 80 } },
          ],
        }, '裂伤追咬'),
        () => normalizeSkillData({
          name: '影蛇分身', 魂技名: '影蛇分身', 技能分类: '召唤', 目标: '自身', 消耗: '魂力:90 | 精神力:45', 前摇: 8,
          _效果数组: [{
            原型: '召唤生成', 目标: '自身', 生效方式: '独立生效', 召唤单位类型: '分身', 召唤物名称: '影蛇分身', 召唤数量: 1, 持续回合: 3,
            属性继承比例: { 力量: 0.42, 防御: 0.36, 敏捷: 0.48, 体力上限: 0.38, 魂力上限: 0.35, 精神力上限: 0.35 },
          }],
        }, '影蛇分身'),
        () => normalizeSkillData({
          name: '锁魄印', 魂技名: '锁魄印', 技能分类: '控制', 目标: '敌方单体', 消耗: '魂力:80', 前摇: 10,
          _效果数组: [{ 原型: '状态施加', 目标: '敌方单体', 状态: '位移限制', 状态名称: '位移限制', 持续回合: 1, 计算层效果: { cast_speed_penalty: 0.25, lock_level: 1 } }],
        }, '锁魄印'),
        () => normalizeSkillData({
          name: '影蛇分身', 魂技名: '影蛇分身', 技能分类: '召唤', 目标: '自身', 消耗: '魂力:90 | 精神力:45', 前摇: 8,
          _效果数组: [{
            原型: '召唤生成', 目标: '自身', 生效方式: '独立生效', 召唤单位类型: '分身', 召唤物名称: '影蛇分身', 召唤数量: 1, 持续回合: 3,
            属性继承比例: { 力量: 0.42, 防御: 0.36, 敏捷: 0.48, 体力上限: 0.38, 魂力上限: 0.35, 精神力上限: 0.35 },
          }],
        }, '影蛇分身'),
        () => normalizeSkillData({
          name: '第二魂技·青影蛇群', 魂技名: '第二魂技·青影蛇群', 技能分类: '控制', 目标: '敌方群体', 消耗: '魂力:140', 前摇: 12,
          _效果数组: [
            { 原型: '伤害结算', 目标: '群体', 生效方式: '独立生效', 威力倍率: 72, 伤害类型: '远程攻击', 命中修正: 0.12 },
            { 原型: '状态施加', 目标: '敌方群体', 生效方式: '跟随主原型', 状态: '迟缓', 状态名称: '迟缓', 持续回合: 2, 计算层效果: { cast_speed_penalty: 0.18, dodge_penalty: 0.08 } },
          ],
        }, '第二魂技·青影蛇群'),
        () => normalizeSkillData({
          name: '香喷喷牛肉干', 魂技名: '香喷喷牛肉干', 技能分类: '辅助', 目标: '自身', 承载方式: '造物承载', 消耗: '魂力:50', 前摇: 6,
          _效果数组: [{ 数量: 1, 描述: '香喷喷牛肉干', 使用效果: [{ 原型: '资源变化', 目标: '自身', 资源: '体力', 数值: '+80' }] }],
        }, '香喷喷牛肉干'),
      ];
      const index = Math.max(1, Math.floor(Number(索引 || 1)));
      const 原随机 = Math.random;
      try {
        let seed = index * 9301 + 49297;
        Math.random = () => {
          seed = (seed * 233 + 7919) % 104729;
          return seed / 104729;
        };
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        const 技能 = 技能库[(index - 1) % 技能库.length]();
        玩家.第1武魂.第1魂环.第1魂技 = 技能;
        敌人.第1武魂 = {
          表象名称: '敌方夹具武魂',
          第1魂环: {
            第1魂技: 战斗回归输出魂技('敌方截击', '敌方单体', 6, 88, '近身攻击'),
            第2魂技: 战斗回归输出魂技('敌方压迫', '敌方单体', 10, 96, '近身攻击'),
          },
        };
        if (/第二魂技|青影蛇群/.test(String(技能.name || 技能.魂技名 || ''))) {
          玩家.第1武魂.第2魂环 ||= {};
          玩家.第1武魂.第2魂环.第2魂技 = 技能;
        }
        玩家.type = index % 5 === 0 ? '敏攻系' : '强攻系';
        敌人.type = '敏攻系';
        敌人.agi = index % 4 === 0 ? 980 : 220 + (index % 6) * 40;
        敌人.final = buildCombatFinalStats(敌人);
        if (index % 9 === 0) recordActorActionWhiffedByDodge(玩家, '普通攻击', 敌人);
        const entry = {
          actor_name: 玩家.name,
          action_type: '释放魂技',
          skill: { 魂技名: 技能.name || 技能.魂技名, name: 技能.name || 技能.魂技名, 消耗: 技能.消耗 || '无' },
          __魂环路径: /第二魂技|青影蛇群/.test(String(技能.name || 技能.魂技名 || '')) ? ['第1武魂', '第2魂环'] : ['第1武魂', '第1魂环'],
          __魂技槽位: /第二魂技|青影蛇群/.test(String(技能.name || 技能.魂技名 || '')) ? '第2魂技' : '第1魂技',
          target_name: /自身|友方|造物|食物|药剂|召唤/.test(String(技能.目标 || '') + String(技能.技能分类 || '') + String(技能.承载方式 || '')) ? 玩家.name : 敌人.name,
        };
        let result = null;
        使用战斗回归桥接(combatData, { [玩家.name]: 玩家, 夹具友方: 构建战斗回归夹具单位('夹具友方', '辅助系'), [敌人.name]: 敌人 }, () => {
          result = onPlayerAttack(`${技能.name || 技能.魂技名}\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
            dryRun: true,
            mode: index % 6 === 0 ? 'multi_round' : 'single_round',
            combatData,
            intentMode: '点到为止',
            autoContinueConfig: { maxRounds: 4, stopDamagePercent: 100, continueChancePercent: 100 },
          });
        });
        return { index, result, combatData, 玩家, 敌人 };
      } finally {
        Math.random = 原随机;
      }
    }

    function 生成可信战术侧写调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '可信战术侧写夹具';
      玩家.name = '唐凌雪';
      玩家.名称 = '唐凌雪';
      敌人.name = '韦小枫';
      敌人.名称 = '韦小枫';
      const actionEvent = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '锁魄印',
        actionType: '释放魂技',
        targetScope: 'single',
        targetPoolSide: 'enemy',
        result: 'declared',
      });
      写入战斗事件账本(combatData, {
        eventKind: 'state_apply',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '锁魄印',
        sourceActionName: '锁魄印',
        sourceActionId: actionEvent?.actionId || '',
        sourceNodeId: actionEvent?.chainNodeId || '',
        stateName: '位移限制',
        duration: 1,
        result: 'applied',
        primaryOutcome: 'state_applied',
        meta: {
          stateName: '位移限制',
          successRate: 100,
          successRateReason: '必中/无法抵抗',
          successRateBreakdown: '附着成功率：100%，必中/无法抵抗',
        },
      });
      记录行动闭环审计(combatData, '主动规划', {
        actionId: actionEvent?.actionId || '',
        回合: 1,
        行动者: 玩家.name,
        目标: 敌人.name,
        技能: '锁魄印',
        战略意图: 'CONTROL',
        目标理由: ['控制收益:位移限制可限制反扑'],
        候选来源: '主动候选结构化评分',
        原始权重: 70,
        战术修正: 22,
        目标修正: 18,
        最终权重: 110,
        选择原因: '结构化候选链锁定',
        hitCandidateName: '锁魄印',
        selectedCandidateName: '锁魄印',
        finalResolvedActionName: '锁魄印',
        目标语义: '敌方单体',
        承载方式: '释放魂技',
        候选排序结果: [
          {
            candidateId: 'trusted_lock_soul',
            candidateName: '锁魄印',
            名称: '锁魄印',
            score: 110,
            权重: 110,
            candidateStatus: 'EXECUTED',
            rejectionCode: '',
            effectTags: ['CONTROL_RESTRICTION'],
            candidateSource: 'ACTIVE_SCORER',
          },
          {
            candidateId: 'trusted_basic_attack',
            candidateName: '普通攻击',
            名称: '普通攻击',
            score: 96,
            权重: 96,
            candidateStatus: 'REJECTED',
            rejectionCode: 'CONTROL_GAP',
            effectTags: ['DIRECT_DAMAGE'],
            rejectedByEffectGap: 'CONTROL_RESTRICTION_GAP',
            candidateSource: 'ACTIVE_SCORER',
          },
        ],
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '可信候选链生成 DM 战术侧写。',
          mode: 'battle_preview',
          battleMode: 'trusted_tactical_narration',
          modeLabel: '可信战术侧写闭环',
          intentMode: '点到为止',
          logs: [],
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '可信战术侧写闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成直攻压制战术侧写调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = '直攻压制战术侧写夹具';
      玩家.name = '唐凌雪';
      玩家.名称 = '唐凌雪';
      敌人.name = '韦小枫';
      敌人.名称 = '韦小枫';
      const actionEvent = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '裂地冲拳',
        actionType: '释放魂技',
        targetScope: 'single',
        targetPoolSide: 'enemy',
        result: 'declared',
      });
      写入战斗事件账本(combatData, {
        eventKind: 'defend',
        round: 1,
        actorName: 敌人.name,
        targetName: 玩家.name,
        actionName: '承伤硬抗',
        sourceActionName: '裂地冲拳',
        sourceRound: 1,
        sourceActionId: actionEvent?.actionId || '',
        sourceNodeId: actionEvent?.chainNodeId || '',
        result: 'guarded',
        primaryOutcome: 'guarded',
        meta: {
          sourceActorName: 玩家.name,
          sourceActionName: '裂地冲拳',
          reactionType: 'defend',
          reactionOutcome: 'guarded',
        },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'hit_result',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: '裂地冲拳',
        sourceActionName: '裂地冲拳',
        sourceActionId: actionEvent?.actionId || '',
        sourceNodeId: actionEvent?.chainNodeId || '',
        appliedDamage: 96,
        damage: 96,
        result: 'full_hit',
        primaryOutcome: 'full_hit',
        effectCapability: { hasDamageEffect: true, effectKinds: ['伤害结算'] },
        meta: {
          appliedDamage: 96,
          damage: 96,
          incomingDamage: 96,
          defenseThreshold: 0,
          actualDefense: 137,
          shieldAbsorb: 0,
          finalDamage: 96,
          damageType: '近身攻击',
          skillPower: 88,
          attackValue: 150,
          defenseValue: 137,
          baseDamage: 96,
          formulaAttackValue: 150,
          formulaDefenseValue: 137,
          formulaText: '威力88 × 攻势150 / 防守137',
          damageFormulaText: '伤害 = 威力88 × 攻势150 / 防守137 = 96',
          formulaTrace: {
            damageType: '近身攻击',
            skillPower: 88,
            attackValue: 150,
            defenseValue: 137,
            baseDamage: 96,
            formulaText: '威力88 × 攻势150 / 防守137',
          },
          settlementTrace: [
            { key: 'sourceAction', label: '来源动作', value: '裂地冲拳' },
            { key: 'attacker', label: '攻方', value: 玩家.name },
            { key: 'target', label: '目标', value: 敌人.name },
            { key: 'result', label: '结果', value: 'full_hit' },
            { key: 'incomingDamage', label: '入参伤害', value: 96 },
            { key: 'actualDefense', label: '有效防御', value: 137 },
            { key: 'defenseThreshold', label: '破防阈值', value: 0 },
            { key: 'shieldAbsorb', label: '护盾吸收', value: 0 },
            { key: 'finalDamage', label: '最终伤害', value: 96 },
          ],
        },
      });
      记录行动闭环审计(combatData, '主动规划', {
        actionId: actionEvent?.actionId || '',
        回合: 1,
        行动者: 玩家.name,
        目标: 敌人.name,
        技能: '裂地冲拳',
        战略意图: 'DIRECT_PRESSURE',
        目标理由: ['攻势收益:裂地冲拳能稳定推进战线'],
        候选来源: '主动候选结构化评分',
        原始权重: 72,
        战术修正: 18,
        目标修正: 16,
        最终权重: 106,
        选择原因: '结构化候选链锁定',
        hitCandidateName: '裂地冲拳',
        selectedCandidateName: '裂地冲拳',
        finalResolvedActionName: '裂地冲拳',
        目标语义: '敌方单体',
        承载方式: '释放魂技',
        候选排序结果: [
          {
            candidateId: 'trusted_direct_punch',
            candidateName: '裂地冲拳',
            名称: '裂地冲拳',
            score: 106,
            权重: 106,
            candidateStatus: 'EXECUTED',
            rejectionCode: '',
            effectTags: ['DIRECT_DAMAGE'],
            candidateSource: 'ACTIVE_SCORER',
          },
          {
            candidateId: 'trusted_probe',
            candidateName: '试探掌',
            名称: '试探掌',
            score: 92,
            权重: 92,
            candidateStatus: 'REJECTED',
            rejectionCode: 'DIRECT_PRESSURE_GAP',
            effectTags: ['DIRECT_DAMAGE'],
            rejectedByEffectGap: 'DIRECT_PRESSURE_GAP',
            candidateSource: 'ACTIVE_SCORER',
          },
        ],
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: '可信直攻压制候选链生成 DM 战术侧写。',
          mode: 'battle_preview',
          battleMode: 'trusted_direct_pressure_tactical_narration',
          modeLabel: '可信直攻压制侧写闭环',
          intentMode: '点到为止',
          logs: [],
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: '可信直攻压制侧写闭环' },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成主导原因战术侧写调试结果(配置 = {}) {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      combatData.回合 = 1;
      combatData.战斗类型 = `${配置.label || '主导原因'}战术侧写夹具`;
      玩家.name = '唐凌雪';
      玩家.名称 = '唐凌雪';
      敌人.name = '韦小枫';
      敌人.名称 = '韦小枫';
      const finalAction = String(配置.finalAction || '定势一击').trim();
      const altAction = String(配置.altAction || '普通攻击').trim();
      const actionEvent = 写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name,
        targetName: 敌人.name,
        actionName: finalAction,
        actionType: '释放魂技',
        targetScope: 'single',
        targetPoolSide: 'enemy',
        result: 'declared',
      });
      if (配置.ledgerKind === 'shield') {
        写入战斗事件账本(combatData, {
          eventKind: 'shield_create',
          round: 1,
          actorName: 玩家.name,
          targetName: '夹具友方',
          actionName: finalAction,
          sourceActionName: finalAction,
          sourceActionId: actionEvent?.actionId || '',
          sourceNodeId: actionEvent?.chainNodeId || '',
          result: 'shielded',
          meta: { amount: 120, shield: 120, sourceAction: finalAction },
        });
      } else {
        写入战斗事件账本(combatData, {
          eventKind: 'defend',
          round: 1,
          actorName: 敌人.name,
          targetName: 玩家.name,
          actionName: '承伤硬抗',
          sourceActionName: finalAction,
          sourceRound: 1,
          sourceActionId: actionEvent?.actionId || '',
          sourceNodeId: actionEvent?.chainNodeId || '',
          result: 'guarded',
          primaryOutcome: 'guarded',
          meta: {
            sourceActorName: 玩家.name,
            sourceActionName: finalAction,
            reactionType: 'defend',
            reactionOutcome: 'guarded',
          },
        });
        写入战斗事件账本(combatData, {
          eventKind: 'hit_result',
          round: 1,
          actorName: 玩家.name,
          targetName: 敌人.name,
          actionName: finalAction,
          sourceActionName: finalAction,
          sourceActionId: actionEvent?.actionId || '',
          sourceNodeId: actionEvent?.chainNodeId || '',
          appliedDamage: Number(配置.damage || 88),
          damage: Number(配置.damage || 88),
          result: 'full_hit',
          primaryOutcome: 'full_hit',
          effectCapability: { hasDamageEffect: true, effectKinds: ['伤害结算'] },
          meta: {
            appliedDamage: Number(配置.damage || 88),
            damage: Number(配置.damage || 88),
            finalDamage: Number(配置.damage || 88),
            damageFormulaText: `伤害 = 威力${Number(配置.power || 80)} × 攻势150 / 防守140 = ${Number(配置.damage || 88)}`,
            formulaTrace: {
              damageType: '魂技冲击',
              skillPower: Number(配置.power || 80),
              attackValue: 150,
              defenseValue: 140,
              baseDamage: Number(配置.damage || 88),
              formulaText: `威力${Number(配置.power || 80)} × 攻势150 / 防守140`,
            },
            settlementTrace: [
              { key: 'sourceAction', label: '来源动作', value: finalAction },
              { key: 'attacker', label: '攻方', value: 玩家.name },
              { key: 'target', label: '目标', value: 敌人.name },
              { key: 'result', label: '结果', value: 'full_hit' },
              { key: 'incomingDamage', label: '入参伤害', value: Number(配置.damage || 88) },
              { key: 'actualDefense', label: '有效防御', value: 140 },
              { key: 'defenseThreshold', label: '破防阈值', value: 0 },
              { key: 'shieldAbsorb', label: '护盾吸收', value: 0 },
              { key: 'finalDamage', label: '最终伤害', value: Number(配置.damage || 88) },
            ],
          },
        });
      }
      记录行动闭环审计(combatData, '主动规划', {
        actionId: actionEvent?.actionId || '',
        回合: 1,
        行动者: 玩家.name,
        目标: 配置.ledgerKind === 'shield' ? '夹具友方' : 敌人.name,
        displayTargetName: 配置.ledgerKind === 'shield' ? '夹具友方' : 敌人.name,
        技能: finalAction,
        战略意图: 配置.dominantReason,
        目标理由: Array.isArray(配置.targetReasons) ? 配置.targetReasons : [],
        候选来源: '主动候选结构化评分',
        原始权重: Number(配置.base || 70),
        战术修正: Number(配置.tactical || 16),
        目标修正: Number(配置.target || 14),
        资源修正: Number(配置.resource || 0),
        职责修正: Number(配置.role || 0),
        最终权重: Number(配置.finalScore || 100),
        选择原因: '结构化候选链锁定',
        hitCandidateName: finalAction,
        selectedCandidateName: finalAction,
        finalResolvedActionName: finalAction,
        目标语义: 配置.ledgerKind === 'shield' ? '友方单体' : '敌方单体',
        承载方式: '释放魂技',
        目标价值分解: 配置.targetBreakdown || {},
        候选排序结果: [
          {
            candidateId: `trusted_${配置.dominantReason || 'reason'}_final`,
            candidateName: finalAction,
            名称: finalAction,
            score: Number(配置.finalScore || 100),
            权重: Number(配置.finalScore || 100),
            candidateStatus: 'EXECUTED',
            rejectionCode: '',
            effectTags: Array.isArray(配置.selectedTags) ? 配置.selectedTags : [],
            candidateSource: 'ACTIVE_SCORER',
          },
          {
            candidateId: `trusted_${配置.dominantReason || 'reason'}_alt`,
            candidateName: altAction,
            名称: altAction,
            score: Number(配置.altScore || 88),
            权重: Number(配置.altScore || 88),
            candidateStatus: 'REJECTED',
            rejectionCode: String(配置.altRejectionCode || 'DIRECT_PRESSURE_GAP'),
            effectTags: Array.isArray(配置.altTags) ? 配置.altTags : [],
            rejectedByEffectGap: String(配置.rejectedByEffectGap || ''),
            candidateSource: 'ACTIVE_SCORER',
          },
        ],
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          preview: true,
          intentText: `可信${配置.label || '主导原因'}候选链生成 DM 战术侧写。`,
          mode: 'battle_preview',
          battleMode: `trusted_${String(配置.dominantReason || 'reason').toLowerCase()}_tactical_narration`,
          modeLabel: `可信${配置.label || '主导原因'}侧写闭环`,
          intentMode: '点到为止',
          logs: [],
          roundsExecuted: 1,
          battleOutcome: { type: '未分胜负', label: `可信${配置.label || '主导原因'}侧写闭环` },
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
          combatData,
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: combatData.__battleResolutionTrace || [],
          eventLedger,
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成斩杀战术侧写调试结果() {
      return 生成主导原因战术侧写调试结果({
        label: '斩杀',
        dominantReason: 'LETHAL',
        finalAction: '追魂断击',
        altAction: '普通攻击',
        base: 74,
        tactical: 20,
        target: 18,
        finalScore: 112,
        altScore: 97,
        selectedTags: ['DIRECT_DAMAGE', 'LETHAL'],
        altTags: ['DIRECT_DAMAGE'],
        altRejectionCode: 'LETHAL_GAP',
        rejectedByEffectGap: 'LETHAL_GAP',
        targetBreakdown: { lethalWindow: 24 },
        targetReasons: ['终结窗口:目标已露颓势'],
        damage: 132,
        power: 116,
      });
    }

    function 生成资源压力战术侧写调试结果() {
      return 生成主导原因战术侧写调试结果({
        label: '资源压力',
        dominantReason: 'RESOURCE_PRESSURE',
        finalAction: '凝息掌',
        altAction: '第二魂技',
        base: 76,
        tactical: 22,
        target: 20,
        resource: -18,
        finalScore: 100,
        altScore: 96,
        selectedTags: ['DIRECT_DAMAGE', 'RESOURCE_STABLE'],
        altTags: ['DIRECT_DAMAGE'],
        altRejectionCode: 'RESOURCE_PRESSURE',
        rejectedByEffectGap: 'RESOURCE_PRESSURE',
        targetReasons: ['资源后果:当前动作更利于维持后续魂力节奏'],
        damage: 74,
        power: 68,
      });
    }

    function 生成保护协同战术侧写调试结果() {
      return 生成主导原因战术侧写调试结果({
        label: '保护协同',
        dominantReason: 'PROTECT_ALLY',
        finalAction: '护心屏障',
        altAction: '普通攻击',
        ledgerKind: 'shield',
        base: 58,
        tactical: 18,
        target: 10,
        role: 16,
        finalScore: 102,
        altScore: 90,
        selectedTags: ['PROTECT_ALLY'],
        altTags: ['DIRECT_DAMAGE'],
        altRejectionCode: 'PROTECT_ALLY_GAP',
        rejectedByEffectGap: 'PROTECT_ALLY_GAP',
        targetReasons: ['保护收益:己方防线需要支援'],
      });
    }

    function 生成同分破平战术侧写调试结果() {
      return 生成主导原因战术侧写调试结果({
        label: '同分破平',
        dominantReason: 'TIE_BREAK',
        finalAction: '锁魄印',
        altAction: '缠身掌',
        base: 70,
        tactical: 18,
        target: 12,
        finalScore: 100,
        altScore: 100,
        selectedTags: ['CONTROL_RESTRICTION'],
        altTags: ['DIRECT_DAMAGE'],
        altRejectionCode: 'DIRECT_PRESSURE_GAP',
        targetReasons: ['战术重心:收益相近时优先锁定当前意图'],
        damage: 66,
        power: 62,
      });
    }

    function 生成战斗AOE链路调试结果() {
      const 原随机 = Math.random;
      try {
        Math.random = () => 0.01;
        const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
        敌人.name = '高速敌';
        敌人.agi = 10000;
        敌人.final = buildCombatFinalStats(敌人);
        const 低速敌 = 构建战斗回归夹具单位('低速敌', '防御系');
        低速敌.agi = 1;
        低速敌.def = 90;
        低速敌.final = buildCombatFinalStats(低速敌);
        combatData.参战者.team_enemy = [敌人, 低速敌];
        const 群体技能 = normalizeSkillData({
          name: '群体压制',
          魂技名: '群体压制',
          技能分类: '输出',
          目标: '敌方群体',
          消耗: '无',
          前摇: 10,
          _效果数组: [{ 原型: '伤害结算', 目标: '群体', 生效方式: '独立生效', 威力倍率: 90, 伤害类型: '远程攻击' }],
        }, '群体压制');
        玩家.第1武魂.第1魂环.第1魂技 = 群体技能;
        const entry = {
          actor_name: 玩家.name,
          action_type: '释放魂技',
          skill: { 魂技名: '群体压制', name: '群体压制', 消耗: '无' },
          __魂环路径: ['第1武魂', '第1魂环'],
          __魂技槽位: '第1魂技',
          target_name: 敌人.name,
        };
        let result = null;
        使用战斗回归桥接(combatData, {
          [玩家.name]: 玩家,
          [敌人.name]: 敌人,
          [低速敌.name]: 低速敌,
          夹具友方: 构建战斗回归夹具单位('夹具友方', '辅助系'),
        }, () => {
          result = onPlayerAttack(`群体压制\n[动作队列]${JSON.stringify([entry])}[/动作队列]`, {
            dryRun: true,
            mode: 'single_round',
            combatData,
            intentMode: '点到为止',
          });
        });
        return { result, combatData, 玩家, 敌人, 低速敌 };
      } finally {
        Math.random = 原随机;
      }
    }

    function 生成非伤害AOE链路调试结果() {
      const { combatData, 玩家 } = 构建战斗回归夹具战斗态();
      const 唐三 = 构建战斗回归夹具单位('唐三', '强攻系');
      const 小舞 = 构建战斗回归夹具单位('小舞', '敏攻系');
      combatData.参战者.team_player = [玩家, 唐三, 小舞];
      写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name || '夹具玩家',
        targetName: '友方阵列',
        targetScope: 'ally_group',
        actionName: '七宝护阵',
        actionType: '释放魂技',
        result: 'declared',
        meta: { targetScope: 'ally_group' },
      });
      [
        { target: 唐三, amount: 160, eventId: 'fixture-group-shield-tangsan' },
        { target: 小舞, amount: 140, eventId: 'fixture-group-shield-xiaowu' },
      ].forEach(item => {
        写入战斗事件账本(combatData, {
          eventId: item.eventId,
          eventKind: 'shield_create',
          round: 1,
          actorName: 玩家.name || '夹具玩家',
          targetName: item.target.name,
          targetId: `unit-${item.target.name}`,
          targetScope: 'ally_group',
          actionName: '七宝护阵',
          actionType: '释放魂技',
          result: 'shielded',
          meta: {
            targetScope: 'ally_group',
            amount: item.amount,
            shield: item.amount,
            sourceAction: '七宝护阵',
          },
        });
      });
      const eventLedger = combatData.__battleEventLedger || [];
      const publicReportBlocks = 构建事件账本公开战报Blocks(eventLedger, 8, { combatData });
      return {
        result: {
          eventLedger,
          resolutionTrace: combatData.__battleResolutionTrace || [],
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
        },
        combatData,
        玩家,
        唐三,
        小舞,
      };
    }

    function 生成战斗变招链路调试结果() {
      const { combatData, 玩家, 敌人 } = 构建战斗回归夹具战斗态();
      写入战斗事件账本(combatData, {
        eventKind: 'action_start',
        round: 1,
        actorName: 玩家.name || '夹具玩家',
        targetName: 敌人.name || '夹具敌人',
        actionName: '收招转防',
        actionType: 'replan_fixture',
        initialActionName: '毒牙牵制',
        finalActionName: '收招转防',
        discardedActionName: '毒牙牵制',
        result: 'declared',
        replanReasonCode: 'TACTICAL_DISADVANTAGE',
        replanReasonText: '没有合适出手窗口',
        meta: {
          replanReasonCode: 'TACTICAL_DISADVANTAGE',
          replanReasonText: '没有合适出手窗口',
        },
      });
      写入战斗事件账本(combatData, {
        eventKind: 'failed_action',
        round: 1,
        actorName: 玩家.name || '夹具玩家',
        targetName: 敌人.name || '夹具敌人',
        actionName: '收招转防',
        actionType: 'replan_fixture',
        initialActionName: '毒牙牵制',
        finalActionName: '收招转防',
        discardedActionName: '毒牙牵制',
        result: 'no_effective_opening',
        failReason: 'no_valid_window',
        replanReasonCode: 'TACTICAL_DISADVANTAGE',
        replanReasonText: '没有合适出手窗口',
        meta: {
          failureReason: 'no_valid_window',
          reasonCode: 'NO_EFFECTIVE_OPENING',
          reasonText: '未形成有效出手机会',
          replanReasonCode: 'TACTICAL_DISADVANTAGE',
          replanReasonText: '没有合适出手窗口',
        },
      });
      return {
        result: {
          eventLedger: combatData.__battleEventLedger || [],
          resolutionTrace: combatData.__battleResolutionTrace || [],
          publicReportBlocks: 构建事件账本公开战报Blocks(combatData.__battleEventLedger || [], 8, { combatData }),
        },
        combatData,
        玩家,
        敌人,
      };
    }

    function 生成召唤精神控制公开战报调试结果() {
      const { combatData, 宿主, 召唤单位 } = 构建召唤夹具战斗态({
        类型: '其他召唤生物',
        名称: '精神受限兽',
        行动模式: '自主行动',
        强度: 1,
      });
      combatData.回合 = 2;
      宿主.men = 100;
      宿主.精神力 = 100;
      宿主.men_max = 1000;
      宿主.精神力上限 = 1000;
      宿主.属性 = { ...(宿主.属性 || {}), 精神力: 100, 精神力上限: 1000 };
      const log = 刷新召唤精神负载(combatData, 宿主);
      const publicReportBlocks = 构建事件账本公开战报Blocks(combatData.__battleEventLedger || [], 8, { combatData });
      return {
        result: {
          log,
          eventLedger: combatData.__battleEventLedger || [],
          resolutionTrace: combatData.__battleResolutionTrace || [],
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
        },
        combatData,
        宿主,
        召唤单位,
      };
    }

    function 生成多召唤精神负载调试结果() {
      const { combatData, 宿主 } = 构建召唤夹具战斗态({
        类型: '其他召唤生物',
        名称: '负载兽甲',
        行动模式: '协同攻击',
        强度: 6,
      });
      const 第二状态 = {
        类型: 'buff',
        层数: 1,
        duration: 3,
        状态效果: {},
        战斗效果: createEmptyCombatEffectMap(),
        召唤物: {
          召唤单位类型: '其他召唤生物',
          召唤物名称: '负载兽乙',
          召唤数量: 1,
          行动模式: '协同攻击',
          强度: 5,
        },
      };
      宿主.状态效果['召唤:负载兽乙'] = 第二状态;
      const 第二单位 = 注册召唤运行态单位(combatData, 宿主, '召唤:负载兽乙', 第二状态);
      combatData.回合 = 2;
      宿主.men = 900;
      宿主.精神力 = 900;
      宿主.men_max = 200;
      宿主.精神力上限 = 200;
      宿主.属性 = { ...(宿主.属性 || {}), 精神力: 900, 精神力上限: 200 };
      const log = 刷新召唤精神负载(combatData, 宿主);
      const publicReportBlocks = 构建事件账本公开战报Blocks(combatData.__battleEventLedger || [], 8, { combatData });
      return {
        result: {
          log,
          eventLedger: combatData.__battleEventLedger || [],
          resolutionTrace: combatData.__battleResolutionTrace || [],
          publicReportBlocks,
          publicReport: publicReportBlocks.map(item => 序列化公开战报Blocks(item?.blocks || [])).filter(Boolean).join('\n'),
        },
        combatData,
        宿主,
        第二单位,
      };
    }

    root.__LWCS_LIST_BATTLE_REGRESSION_FIXTURES__ = () => ['第一魂技槽位归属', '固定消耗只扣一次', '百分比普通魂技阻断不扣费', '目标语义不串线', '造物给友方入包补丁', '非敌对短前摇先完成', '非敌对长前摇被打断不落地', '防御时 NPC 仍主动规划', '非攻击动作集合不冻结 NPC', '敏攻近身来袭有通用应对', '反高速主动规划保留区分', '防守反击强于闪避反击', '闪避擦伤战报不写完全避开', '完全闪避战报不带伤害', '完全闪避不落本次攻击负面状态', '位移限制不结算生命流失', '新附加持续状态下回合才跳伤', '完整闪避链不触发本次状态结算', '群体攻击逐目标独立命中', '索敌失败进入公开战报', '第一魂技正常释放链路', '闭环账本标记未闭合起招', '空效果技能不留下未闭合起招', '自动续推不复用首轮手选魂技', '自动续推设置控制最大回合与伤害停推', '自动续推不因同构攻防提前终止', '资源镜像回合尾同步', '第一魂技被先制打断不提前扣费', '非物品动作拒绝背包调用', '使用物品才消费背包', '自动规划不调用背包物品', '团战战报多链不串联', '多行动索敌失败不污染他人链', '判定主卡隐藏内部术语', '判定流程压缩重复并展示结算链', '行为链审计不被起招校正', '表现层语义纠偏', '判定流程展示细化', '全场纯增益不触发敌对', '全场伤害触发敌对', 'NPC敌方削弱目标取玩家侧', '控制削弱完成后扣费落状态', '控制资源不足不扣费不落地', '施法失败误入结算不落状态', '团战NPC友方辅助不串敌我', '友方群体护盾只落己方', '敌方群体伤害跟随状态逐目标', '护盾先吸收再扣血', 'UI动作声明契约不丢字段', '团战多窗口应招不串链', '预演结果导出与UI可见文本一致', '公开战报不再自指普攻', '状态结算可追溯附着来源', '自身增益动作队列不误取消', '自保候选不串旧敌方目标', '非攻击动作不触发防反链', '防反来源优先使用真实动作', '控制第一魂技防反伤害不污染来源', '状态反伤必须写入防反账本', '状态来源登记写入运行时账本', '敌对动作目标不得落友方或自身', '反敏攻闪避后不继续普攻前压', '敏攻闪避后不重复同一单体压制技能'];
    root.__LWCS_RUN_BATTLE_REGRESSION_FIXTURE_BATCH__ = (名称 = '') => 运行战斗回归夹具(名称);
    root.__LWCS_GENERATE_BATTLE_DECISION_SAMPLES__ = (数量 = 100) => 生成战斗判定样本文本(数量);
    root.__LWCS_DEBUG_BATTLE_SAMPLE_RESULT__ = (索引 = 1) => 生成战斗判定样本结果(索引);
    root.__LWCS_DEBUG_BATTLE_TRUSTED_TACTICAL_NARRATION_RESULT__ = () => 生成可信战术侧写调试结果();
    root.__LWCS_DEBUG_BATTLE_DIRECT_PRESSURE_TACTICAL_NARRATION_RESULT__ = () => 生成直攻压制战术侧写调试结果();
    root.__LWCS_DEBUG_BATTLE_LETHAL_TACTICAL_NARRATION_RESULT__ = () => 生成斩杀战术侧写调试结果();
    root.__LWCS_DEBUG_BATTLE_RESOURCE_PRESSURE_TACTICAL_NARRATION_RESULT__ = () => 生成资源压力战术侧写调试结果();
    root.__LWCS_DEBUG_BATTLE_PROTECT_ALLY_TACTICAL_NARRATION_RESULT__ = () => 生成保护协同战术侧写调试结果();
    root.__LWCS_DEBUG_BATTLE_TIE_BREAK_TACTICAL_NARRATION_RESULT__ = () => 生成同分破平战术侧写调试结果();
    root.__LWCS_HYDRATE_BATTLE_RUNTIME_IMPL__ = (combatData = {}, eventLedger = null, options = {}) => 补水战斗运行态(combatData, eventLedger, options);
    root.__LWCS_DEBUG_BATTLE_AOE_TRACE_RESULT__ = () => 生成战斗AOE链路调试结果();
    root.__LWCS_DEBUG_BATTLE_NON_DAMAGE_AOE_TRACE_RESULT__ = () => 生成非伤害AOE链路调试结果();
    root.__LWCS_DEBUG_BATTLE_REPLAN_TRACE_RESULT__ = () => 生成战斗变招链路调试结果();
    root.__LWCS_DEBUG_BATTLE_SUMMON_NATURAL_RESULT__ = () => 生成自然召唤闭环调试结果();
    root.__LWCS_DEBUG_BATTLE_BOUND_SUMMON_NATURAL_RESULT__ = () => 生成自然召唤闭环调试结果({
      技能名: '本命兽契',
      召唤单位类型: '本命召唤兽',
      召唤物名称: '玄契灵兽',
      消耗: '魂力:110 | 精神力:70',
      属性继承比例: { 力量: 0.78, 防御: 0.5, 敏捷: 0.72, 体力上限: 0.52, 魂力上限: 0.44, 精神力上限: 0.46 },
      敌人敏捷: 80,
      intentText: '使用【本命兽契】后观察本命召唤兽下一回合行动。',
      battleMode: 'bound_summon_closure',
      modeLabel: '本命召唤兽闭环',
    });
    root.__LWCS_DEBUG_BATTLE_SUMMON_DEFAULT_MODES_RESULT__ = () => 生成召唤默认模式调试结果();
    root.__LWCS_DEBUG_BATTLE_SECOND_COUNTER_RESULT__ = () => 生成二层防反闭环调试结果();
    root.__LWCS_DEBUG_BATTLE_STATE_TICK_AGGREGATION_RESULT__ = () => 生成回合末状态聚合调试结果();
    root.__LWCS_DEBUG_BATTLE_HOT_RESOURCE_TICK_AGGREGATION_RESULT__ = () => 生成回合末恢复资源聚合调试结果();
    root.__LWCS_DEBUG_BATTLE_STATE_RESISTED_RESULT__ = () => 生成状态抵抗闭环调试结果();
    root.__LWCS_DEBUG_BATTLE_STATE_IMMUNE_RESULT__ = () => 生成状态免疫闭环调试结果();
    root.__LWCS_DEBUG_BATTLE_SUMMON_ASSIST_RESULT__ = () => 生成普通召唤协同攻击调试结果();
    root.__LWCS_DEBUG_BATTLE_MULTI_SUMMON_ASSIST_RESULT__ = () => 生成多召唤协同攻击调试结果();
    root.__LWCS_DEBUG_BATTLE_TEAM_SUMMON_ACTION_RESULT__ = () => 生成团战召唤行动轴调试结果();
    root.__LWCS_DEBUG_BATTLE_AUTO_ACTOR_ACTION_RESULT__ = () => 生成自动行动起手闭环调试结果();
    root.__LWCS_DEBUG_BATTLE_FALLBACK_RESOURCE_RESULT__ = () => 生成保底资源回稳调试结果();
    root.__LWCS_DEBUG_BATTLE_NON_HOSTILE_REACTION_FILTER_RESULT__ = () => 生成非敌对动作不触发反应调试结果();
    root.__LWCS_DEBUG_BATTLE_TARGET_POOL_HARD_RULES_RESULT__ = () => 生成目标池硬规则调试结果();
    root.__LWCS_DEBUG_BATTLE_CAP_REACHED_RESULT__ = () => 生成上限拦截调试结果();
    root.__LWCS_DEBUG_BATTLE_STATE_LIFECYCLE_RESULT__ = () => 生成状态生命周期规则调试结果();
    root.__LWCS_DEBUG_BATTLE_DECLARED_TARGET_LOST_RESULT__ = () => 生成预声明目标丢失调试结果();
    root.__LWCS_DEBUG_BATTLE_MULTI_HOST_SUMMON_ASSIST_RESULT__ = () => 生成多宿主召唤协同调试结果();
    root.__LWCS_DEBUG_BATTLE_OPPOSING_SIDE_SUMMON_ASSIST_RESULT__ = () => 生成敌我双方召唤协同调试结果();
    root.__LWCS_DEBUG_BATTLE_OPPOSING_SIDE_SUMMON_ACTION_RESULT__ = () => 生成敌我双方自主召唤行动轴调试结果();
    root.__LWCS_DEBUG_BATTLE_SUMMON_CONTROL_PUBLIC_RESULT__ = () => 生成召唤精神控制公开战报调试结果();
    root.__LWCS_DEBUG_BATTLE_MULTI_SUMMON_CONTROL_RESULT__ = () => 生成多召唤精神负载调试结果();

    function 读取事件链状态(container = null) {
      const 状态 = container && typeof container === 'object' ? container : {};
      if (状态 && typeof 状态 === 'object' && (Array.isArray(状态.复制键集合) || Array.isArray(状态.反制键集合) || '复制深度' in 状态)) {
        if (!状态.id) 状态.id = `skill-chain-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
        状态.复制深度 = Math.max(0, Number(状态.复制深度 || 0));
        if (!Array.isArray(状态.复制键集合)) 状态.复制键集合 = [];
        if (!Array.isArray(状态.反制键集合)) 状态.反制键集合 = [];
        return 状态;
      }
      if (!状态.__技能事件链 || typeof 状态.__技能事件链 !== 'object') {
        状态.__技能事件链 = {
          id: `skill-chain-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`,
          复制深度: 0,
          复制键集合: [],
          反制键集合: [],
        };
      }
      if (!Array.isArray(状态.__技能事件链.复制键集合)) 状态.__技能事件链.复制键集合 = [];
      if (!Array.isArray(状态.__技能事件链.反制键集合)) 状态.__技能事件链.反制键集合 = [];
      return 状态.__技能事件链;
    }

    function 生成技能复制键(skill = {}) {
      return String(skill?.name || skill?.魂技名 || skill?.技能名称 || JSON.stringify(skill?._效果数组 || [])).trim();
    }

    function 技能含复制执行(skill = {}) {
      const 扫描 = value => {
        if (!value || typeof value !== 'object') return false;
        if (Array.isArray(value)) return value.some(扫描);
        if (String(value?.原型 || '').trim() === '复制执行') return true;
        return ['使用效果', '授予效果', '结算效果', '替换效果', '追加效果', '条件分支'].some(key => 扫描(value[key]));
      };
      return 扫描(skill?._效果数组);
    }

    function findCombatTargetInSet(targetSet = [], expectedTarget = null) {
      if (!expectedTarget) return null;
      return (
        (Array.isArray(targetSet) ? targetSet : []).find(target =>
          isCombatUnitIdentityMatch(target, expectedTarget?.name || expectedTarget),
        ) || null
      );
    }

    function 计算目标威胁分(target = {}) {
      const finalStat = target?.final || target || {};
      const 快照 = buildConditionTacticalSnapshot(target);
      return (
        Number(finalStat.str || target.str || target.力量 || 0) * 0.35 +
        Number(finalStat.men_max || target.men_max || target.精神力上限 || 0) * 0.25 +
        Number(finalStat.sp_max || target.sp_max || target.魂力上限 || 0) * 0.18 +
        Number(finalStat.agi || target.agi || target.敏捷 || 0) * 0.12 +
        (快照.hasTargetLock ? 500 : 0) +
        (快照.hasReactiveDefense ? 120 : 0)
      );
    }

    function 排序战斗目标列表(targetSet = [], attacker = null, preferredTarget = null, effect = null, effectiveTargetKind = '') {
      const preferredName = String(preferredTarget?.name || preferredTarget?.名称 || preferredTarget || '').trim();
      const forcedName = getForcedTauntTargetName(attacker);
      return [...(Array.isArray(targetSet) ? targetSet : [])]
        .map((target, index) => {
          return { target, index, threat: 计算目标威胁分(target) };
        })
        .sort((a, b) => {
          const aForced = forcedName && isCombatUnitIdentityMatch(a.target, forcedName) ? 1 : 0;
          const bForced = forcedName && isCombatUnitIdentityMatch(b.target, forcedName) ? 1 : 0;
          if (aForced !== bForced) return bForced - aForced;
          const aPreferred = preferredName && isCombatUnitIdentityMatch(a.target, preferredName) ? 1 : 0;
          const bPreferred = preferredName && isCombatUnitIdentityMatch(b.target, preferredName) ? 1 : 0;
          if (aPreferred !== bPreferred) return bPreferred - aPreferred;
          const aLocked = buildConditionTacticalSnapshot(a.target).hasTargetLock ? 1 : 0;
          const bLocked = buildConditionTacticalSnapshot(b.target).hasTargetLock ? 1 : 0;
          if (aLocked !== bLocked) return bLocked - aLocked;
          if (a.threat !== b.threat) return b.threat - a.threat;
          return a.index - b.index;
        })
        .map(entry => entry.target);
    }

    function 读取施术者召唤物目标列表(attacker = {}, combatData = {}) {
      return 读取召唤单位列表(combatData, { 宿主: attacker })
        .filter(单位 => isCombatUnitAlive(单位) && !召唤单位是分身(单位));
    }

    function 读取施术者分身目标列表(attacker = {}, combatData = {}) {
      return 读取召唤单位列表(combatData, { 宿主: attacker })
        .filter(单位 => isCombatUnitAlive(单位) && 召唤单位是分身(单位));
    }

    function resolveSkillTargetContext(skill, attacker, defender, combatData, effect = null) {
      if (String(effect?.原型 || '').trim() === '时光回溯') {
        return resolveTimeRewindTargetContext(skill, attacker, defender, combatData, effect);
      }
      const runtimeMeta = getSkillRuntimeMeta(skill);
      const baseTargetKind = effect ? 推断战斗效果目标类型(effect) : inferSkillPrimaryTargetKind(skill);
      const baseTargetScale = normalizeBattleSkillTargetScale(
        runtimeMeta?.目标规模 || '',
        deriveBattleSkillTargetScaleFromKind(baseTargetKind),
      );
      const explicitTargetText = String(effect?.目标 || '').trim();
      const targetModifiers = normalizeBattleSkillTargetModifiers(
        Array.isArray(effect?.目标修饰) && effect.目标修饰.length
          ? effect.目标修饰
          : runtimeMeta.目标修饰 || [],
      );
      const resolutionStrategy = deriveBattleTargetResolutionStrategy(baseTargetKind);
      const baseTargetText = explicitTargetText || mapBattleTargetKindToCombatTarget(baseTargetKind);
      const effectiveTargetKind = baseTargetKind;
      const targetText = mapBattleTargetKindToCombatTarget(effectiveTargetKind);
      确保召唤单位表(combatData || {});
      const 玩家侧单位 = 读取战斗阵营单位列表(combatData || {}, '玩家');
      const 敌方侧单位 = 读取战斗阵营单位列表(combatData || {}, '敌方');
      const 攻击者名 = String(attacker?.name || attacker?.名称 || '').trim();
      const 攻击者在敌方 = 敌方侧单位.some(unit => unit === attacker || isCombatUnitIdentityMatch(unit, 攻击者名));
      const alliedUnits = dedupeCombatTargetList([
        attacker,
        ...(攻击者在敌方 ? 敌方侧单位 : 玩家侧单位),
      ]).filter(isCombatUnitAlive);
      const hostileUnits = dedupeCombatTargetList([
        defender,
        ...(攻击者在敌方 ? 玩家侧单位 : 敌方侧单位),
      ]).filter(isCombatUnitAlive);
          const allUnits = dedupeCombatTargetList([...alliedUnits, ...hostileUnits]).filter(isCombatUnitAlive);
          const 指定目标名 = String(skill?.__指定目标名 || skill?.target_name || skill?.物品接收者 || '').trim();
          let targetSet = [];
      switch (effectiveTargetKind) {
        case '召唤物':
          targetSet = 读取施术者召唤物目标列表(attacker, combatData);
          break;
        case '分身':
          targetSet = 读取施术者分身目标列表(attacker, combatData);
          break;
        case '全场': {
          const prioritizedTarget = findCombatTargetInSet(allUnits, defender);
          targetSet = prioritizedTarget
            ? [prioritizedTarget, ...allUnits.filter(target => !isCombatUnitIdentityMatch(target, prioritizedTarget.name))]
            : allUnits;
          break;
        }
        case '自身':
          targetSet = attacker ? [attacker] : [];
          break;
        case '友方群体':
          if (是否七九辅助单体语义(attacker, skill)) {
            const alliedTarget = findCombatTargetInSet(alliedUnits, attacker) || attacker || alliedUnits[0] || null;
            targetSet = alliedTarget ? [alliedTarget] : [];
          } else {
            targetSet = alliedUnits;
          }
          break;
        case '友方单体': {
          const alliedTarget =
            (指定目标名 ? findCombatTargetInSet(alliedUnits, 指定目标名) : null) ||
            findCombatTargetInSet(alliedUnits, defender) ||
            findCombatTargetInSet(alliedUnits, attacker) ||
            attacker;
          targetSet = alliedTarget ? [alliedTarget] : [];
          break;
        }
        case '敌方群体':
          targetSet = hostileUnits;
          break;
        case '敌方单体':
        default: {
          const hostileTarget = findCombatTargetInSet(hostileUnits, defender) || hostileUnits[0] || null;
          targetSet = hostileTarget ? [hostileTarget] : [];
          break;
        }
      }
      const 运行态批量目标列表 = 读取运行态批量目标列表(skill).filter(isCombatUnitAlive);
      if (
        运行态批量目标列表.length > 0 &&
        isSupportLikeSkill(skill) &&
        ['自身', '友方单体', '友方群体'].includes(effectiveTargetKind)
      ) {
        targetSet = 运行态批量目标列表;
      }
      if (effectiveTargetKind !== '分身' && 推断战斗机制默认方向(effect || {}) === '友方') {
        targetSet = targetSet.filter(目标对象 => !召唤单位是分身(目标对象));
      }
      if (targetModifiers.includes('受隐身筛选') && effectiveTargetKind === '敌方单体') {
        const 可锁定目标 = targetSet.filter(target => 可以锁定隐匿目标(attacker, target, skill));
        targetSet = 可锁定目标.length
          ? 可锁定目标
          : hostileUnits.filter(target => 可以锁定隐匿目标(attacker, target, skill));
      }
      if (targetModifiers.includes('可被嘲讽') && ['敌方单体', '敌方群体'].includes(effectiveTargetKind)) {
        const forcedTargetName = getForcedTauntTargetName(attacker);
        if (forcedTargetName) {
          const forcedTarget = hostileUnits.find(target => isCombatUnitIdentityMatch(target, forcedTargetName));
          if (forcedTarget) {
            targetSet =
              effectiveTargetKind === '敌方单体'
                ? [forcedTarget]
                : [forcedTarget, ...targetSet.filter(target => !isCombatUnitIdentityMatch(target, forcedTarget.name))];
          }
        }
      }
      targetSet = 排序战斗目标列表(targetSet, attacker, defender, effect, effectiveTargetKind);
      const primaryTarget = targetSet[0] || null;
      return {
        targetKind: effectiveTargetKind,
        targetScale: baseTargetScale,
        directionId: '',
        directionSemantic: '上下文',
        targetModifiers,
        resolutionStrategy,
        targetText,
        alliedSet: alliedUnits,
        hostileSet: hostileUnits,
        targetSet,
        primaryTarget,
      };
    }

    function resolveSkillEffectTargetCharacter(skill, effect, attacker, defender, combatData = null) {
      return resolveSkillTargetContext(skill, attacker, defender, combatData, effect).primaryTarget;
    }

    function resolveSkillEffectTargetCharacters(skill, effect, attacker, defender, combatData) {
      return resolveSkillTargetContext(skill, attacker, defender, combatData, effect).targetSet;
    }

    function 读取行为规划场上单位集合(actor = {}, behaviorState = {}) {
      const combatData = behaviorState?.combatData || {};
      确保召唤单位表(combatData);
      const 玩家侧 = 读取战斗阵营单位列表(combatData, '玩家');
      const 敌方侧 = 读取战斗阵营单位列表(combatData, '敌方');
      const actorId = actor?.name || actor?.名称 || actor;
      const actorInPlayerSide = 玩家侧.some(unit => isCombatUnitIdentityMatch(unit, actorId));
      const actorInEnemySide = 敌方侧.some(unit => isCombatUnitIdentityMatch(unit, actorId));
      if (actorInPlayerSide) return { 己方: 玩家侧, 敌方: 敌方侧, 全部: dedupeCombatTargetList([...玩家侧, ...敌方侧]) };
      if (actorInEnemySide) return { 己方: 敌方侧, 敌方: 玩家侧, 全部: dedupeCombatTargetList([...玩家侧, ...敌方侧]) };
      return {
        己方: dedupeCombatTargetList([actor]).filter(isCombatUnitAlive),
        敌方: dedupeCombatTargetList([behaviorState?.primaryTarget || behaviorState?.target]).filter(isCombatUnitAlive),
        全部: dedupeCombatTargetList([actor, behaviorState?.primaryTarget || behaviorState?.target]).filter(isCombatUnitAlive),
      };
    }

    function 解析效果规划目标列表(effect = {}, actor = {}, primaryTarget = {}, behaviorState = {}, skill = null) {
      const 场上单位 = 读取行为规划场上单位集合(actor, behaviorState);
      const 目标类型 = 推断战斗效果目标类型(effect, inferSkillPrimaryTargetKind(skill || {}));
      let 目标列表 = [];
      if (目标类型 === '全场') 目标列表 = 场上单位.全部;
      else if (目标类型 === '自身') 目标列表 = [actor];
      else if (目标类型 === '友方群体') 目标列表 = 场上单位.己方;
      else if (目标类型 === '敌方群体') 目标列表 = 场上单位.敌方;
      else if (目标类型 === '友方单体') 目标列表 = [场上单位.己方.find(unit => !actor || !primaryTarget || isCombatUnitIdentityMatch(unit, primaryTarget?.name || primaryTarget)) || actor];
      else 目标列表 = [primaryTarget || 场上单位.敌方[0]];
      return dedupeCombatTargetList(目标列表).filter(isCombatUnitAlive);
    }

    function 读取行为规划单位立场(target = {}, actor = {}, behaviorState = {}) {
      const 场上单位 = 读取行为规划场上单位集合(actor, behaviorState);
      const targetId = target?.name || target?.名称 || target;
      if (场上单位.己方.some(unit => isCombatUnitIdentityMatch(unit, targetId))) return '己方';
      if (场上单位.敌方.some(unit => isCombatUnitIdentityMatch(unit, targetId))) return '敌方';
      return isCombatUnitIdentityMatch(target, actor?.name || actor) ? '己方' : '敌方';
    }

    function 读取规划单位名称(unit = {}) {
      return String(unit?.name || unit?.名称 || '').trim();
    }

    function 读取规划单位系别(unit = {}) {
      return String(unit?.type || unit?.系别 || unit?.单位性质 || '').trim();
    }

    function 读取规划单位阵营(unit = {}, combatData = {}) {
      const 名称 = 读取规划单位名称(unit);
      if (!名称) return '';
      if (读取战斗阵营单位列表(combatData, '玩家').some(成员 => isCombatUnitIdentityMatch(成员, 名称))) return '玩家';
      if (读取战斗阵营单位列表(combatData, '敌方').some(成员 => isCombatUnitIdentityMatch(成员, 名称))) return '敌方';
      return '';
    }

    function 确保队伍临时意图(combatData = {}) {
      if (!combatData || typeof combatData !== 'object') return { 玩家: {}, 敌方: {} };
      if (!combatData.__队伍临时意图 || typeof combatData.__队伍临时意图 !== 'object') {
        Object.defineProperty(combatData, '__队伍临时意图', {
          configurable: true,
          enumerable: false,
          writable: true,
          value: { 玩家: {}, 敌方: {} },
        });
      }
      combatData.__队伍临时意图.玩家 ||= {};
      combatData.__队伍临时意图.敌方 ||= {};
      return combatData.__队伍临时意图;
    }

    function 读取规划资源比例(unit = {}, 资源 = '魂力') {
      if (资源 === '精神力') return Math.max(0, Number(unit?.men ?? unit?.属性?.精神力 ?? 0)) / Math.max(1, Number(unit?.men_max ?? unit?.属性?.精神力上限 ?? 1));
      if (资源 === '体力') return Math.max(0, Number(unit?.sta ?? unit?.vit ?? unit?.属性?.体力 ?? 0)) / Math.max(1, Number(unit?.sta_max ?? unit?.vit_max ?? unit?.属性?.体力上限 ?? 1));
      return Math.max(0, Number(unit?.sp ?? unit?.属性?.魂力 ?? 0)) / Math.max(1, Number(unit?.sp_max ?? unit?.属性?.魂力上限 ?? 1));
    }

    function 读取规划维持压力(unit = {}) {
      const 显式负荷 = Math.max(0, Number(unit?.__维持负荷 || 0));
      const 持续负荷 = Object.values(unit?.持续效果 || {}).reduce((总和, effect) => 总和 + Math.max(0, Number(effect?.维持负荷 || 0)), 0);
      return 限制行为概率(Math.max(显式负荷, 持续负荷), 0, 1);
    }

    function 读取友方支援窗口_V1(单位列表 = []) {
      const 列表 = Array.isArray(单位列表) ? 单位列表 : [];
      const 残血数量 = 列表.filter(单位 => getCombatHpRatio(单位) < 0.62).length;
      const 重伤数量 = 列表.filter(单位 => getCombatHpRatio(单位) < 0.42).length;
      const 异常数量 = 列表.filter(单位 => {
        const 快照 = buildConditionTacticalSnapshot(单位);
        return 快照.hasBadCondition || 快照.isLockedOrControlled;
      }).length;
      const 蓄力待保护数量 = 列表.filter(单位 => 单位?.蓄力技能).length;
      return {
        残血数量,
        重伤数量,
        异常数量,
        蓄力待保护数量,
        有治疗窗口: 残血数量 > 0 || 异常数量 > 0,
        有保核窗口: 重伤数量 > 0 || 异常数量 > 0 || 蓄力待保护数量 > 0,
      };
    }

    function 评分规划核心单位(unit = {}) {
      const 快照 = buildConditionTacticalSnapshot(unit);
      const 系别 = 读取规划单位系别(unit);
      return (
        计算目标威胁分(unit) +
        (['治疗系', '辅助系', '食物系', '控制系'].includes(系别) ? 180 : 0) +
        (unit?.蓄力技能 ? 130 : 0) +
        (快照.hasShielded || 快照.hasReactiveDefense ? 80 : 0) +
        (读取规划维持压力(unit) > 0 ? 120 : 0) -
        (1 - getCombatHpRatio(unit)) * 90
      );
    }

    function 构建战局画像(actor = {}, target = null, combatData = {}, behaviorState = {}) {
      const 场上单位 = 读取行为规划场上单位集合(actor, { ...behaviorState, combatData, primaryTarget: target, target });
      const 己方 = dedupeCombatTargetList(场上单位.己方).filter(isCombatUnitAlive);
      const 敌方 = dedupeCombatTargetList(场上单位.敌方).filter(isCombatUnitAlive);
      const 全部 = dedupeCombatTargetList([...己方, ...敌方]).filter(isCombatUnitAlive);
      const 按核心排序 = 列表 => [...列表].sort((左, 右) => 评分规划核心单位(右) - 评分规划核心单位(左));
      const 我方核心 = 按核心排序(己方)[0] || actor || null;
      const 敌方核心 = 按核心排序(敌方)[0] || target || null;
      const 治疗目标 = [...己方]
        .sort((左, 右) => {
          const 左快照 = buildConditionTacticalSnapshot(左);
          const 右快照 = buildConditionTacticalSnapshot(右);
          return (1 - getCombatHpRatio(右)) * 100 + (右快照.hasBadCondition ? 18 : 0) - ((1 - getCombatHpRatio(左)) * 100 + (左快照.hasBadCondition ? 18 : 0));
        })[0] || actor || null;
      const 控制目标 = [...敌方]
        .sort((左, 右) => {
          const 左快照 = buildConditionTacticalSnapshot(左);
          const 右快照 = buildConditionTacticalSnapshot(右);
          const 评分 = 单位 => {
            const 快照 = buildConditionTacticalSnapshot(单位);
            const 系别 = 读取规划单位系别(单位);
            return 评分规划核心单位(单位) + (['治疗系', '控制系', '辅助系'].includes(系别) ? 140 : 0) + (单位?.蓄力技能 ? 160 : 0) - (快照.isLockedOrControlled ? 120 : 0);
          };
          return 评分(右) - 评分(左) + (右快照.hasBadCondition ? -8 : 0) - (左快照.hasBadCondition ? -8 : 0);
        })[0] || 敌方核心 || null;
      const 敌方召唤 = 读取召唤单位列表(combatData).filter(单位 => 敌方.some(敌方单位 => isCombatUnitIdentityMatch(单位, 敌方单位?.name || 敌方单位)));
      const 我方召唤 = 读取召唤单位列表(combatData).filter(单位 => 己方.some(己方单位 => isCombatUnitIdentityMatch(单位, 己方单位?.name || 己方单位)));
      const 敌方维持者 = 敌方.filter(单位 => 读取规划维持压力(单位) > 0);
      const 我方维持者 = 己方.filter(单位 => 读取规划维持压力(单位) > 0);
      const 资源压力 = 限制行为概率(
        Math.max(0, 1 - 读取规划资源比例(actor, '魂力')) * 0.45 +
          Math.max(0, 1 - 读取规划资源比例(actor, '精神力')) * 0.35 +
          Math.max(0, 1 - 读取规划资源比例(actor, '体力')) * 0.2,
        0,
        1,
      );
      const 终局压力 = 限制行为概率(
        Math.max(0, 0.42 - getCombatHpRatio(actor)) * 1.4 +
          Math.max(0, 0.38 - Math.min(1, ...敌方.map(getCombatHpRatio))) * 1.2 +
          (behaviorState?.threatProfile?.lethalRisk ? 0.35 : 0),
        0,
        1,
      );
      const 记忆 = actor?.决策记忆 || {};
      const 最高重复 = Math.max(0, ...Object.values(记忆.recent_actions || {}).map(次数 => Number(次数 || 0)));
      return {
        我方核心,
        敌方核心,
        治疗目标,
        控制目标,
        集火目标: target || 敌方核心,
        保护目标: 治疗目标 || 我方核心,
        资源压力,
        终局压力,
        召唤压力: 限制行为概率((敌方召唤.length - 我方召唤.length) * 0.12 + 敌方召唤.length * 0.08, 0, 1),
        维持压力: 限制行为概率(敌方维持者.reduce((总和, 单位) => 总和 + 读取规划维持压力(单位), 0) - 我方维持者.reduce((总和, 单位) => 总和 + 读取规划维持压力(单位), 0) * 0.45, 0, 1),
        行为多样性压力: 限制行为概率(最高重复 / 4, 0, 1),
        己方列表: 己方,
        敌方列表: 敌方,
        全部列表: 全部,
        敌方召唤,
        我方召唤,
        敌方维持者,
        我方维持者,
      };
    }

    function 计算目标优先表(actor = {}, combatData = {}, 战局画像 = null) {
      const 画像 = 战局画像 || 构建战局画像(actor, null, combatData, { combatData });
      const 阵营 = 读取规划单位阵营(actor, combatData) || '玩家';
      const 团队意图 = 确保队伍临时意图(combatData)[阵营] || {};
      const 构建敌方项 = unit => {
        const 快照 = buildConditionTacticalSnapshot(unit);
        const 系别 = 读取规划单位系别(unit);
        const 名称 = 读取规划单位名称(unit);
        const 宿主召唤数 = 读取召唤单位列表(combatData, { 宿主: unit }).length;
        const 单体威胁 = 计算对手单体威胁_V1(unit, 画像, { combatData });
        const 血量比例 = getCombatHpRatio(unit);
        const 同侧残血待救 = (画像.敌方列表 || []).some(其他 => 其他 !== unit && getCombatHpRatio(其他) > 0 && getCombatHpRatio(其他) < 0.42);
        const 克制调整 = 计算目标克制调整_V1(actor, unit);
        const 距离系数 = 读取规划距离优先系数_V1(unit);
        const 理由 = [];
        let 分数 = 24 + Math.min(70, 计算目标威胁分(unit) / 55);
        if (血量比例 < 0.35) { 分数 += 42; 理由.push('残血可收割'); }
        if (单体威胁 > 0.45 && 血量比例 > 0 && 血量比例 < 0.42) { 分数 += 34; 理由.push('高威胁可斩杀'); }
        if (['治疗系', '辅助系', '食物系'].includes(系别)) { 分数 += 38; 理由.push('后排/续航核心'); }
        if (同侧残血待救 && ['治疗系', '辅助系', '食物系'].includes(系别)) { 分数 += 58; 理由.push('压治疗核心'); }
        if (系别 === '控制系') { 分数 += 32; 理由.push('控制核心'); }
        if (读取规划维持压力(unit) > 0) { 分数 += 44; 理由.push('维持释放者'); }
        if (宿主召唤数 > 0) { 分数 += Math.min(48, 20 + 宿主召唤数 * 10); 理由.push('召唤宿主'); }
        if (unit?.蓄力技能) { 分数 += 46; 理由.push('蓄力高威胁'); }
        if (快照.hasShielded || 快照.hasReactiveDefense) { 分数 += 18; 理由.push('护盾/防御核心'); }
        if (快照.hasTargetLock) { 分数 += 42; 理由.push('被标记'); }
        if (团队意图.集火目标 && isCombatUnitIdentityMatch(unit, 团队意图.集火目标)) {
          const 集火加成 = unit?.蓄力技能 && 单体威胁 > 0.48 ? 34 : 52;
          分数 += 集火加成;
          理由.push('团队集火');
        }
        if (画像.控制目标 && isCombatUnitIdentityMatch(unit, 画像.控制目标?.name || 画像.控制目标)) { 分数 += 18; 理由.push('控制优先'); }
        if (克制调整 !== 0) {
          分数 *= 1 + 克制调整;
          理由.push(克制调整 > 0 ? '系别克制' : '系别不利');
        }
        if (距离系数 < 1 && !(unit?.蓄力技能 && 单体威胁 > 0.55)) {
          分数 *= 距离系数;
          理由.push('距离衰减');
        }
        return { 单位: unit, 名称, 优先级: Math.max(1, Math.round(分数)), 理由: 理由.length ? 理由 : ['常规威胁'] };
      };
      const 构建友方项 = unit => {
        const 快照 = buildConditionTacticalSnapshot(unit);
        const 系别 = 读取规划单位系别(unit);
        const 名称 = 读取规划单位名称(unit);
        const 理由 = [];
        const 血量比例 = getCombatHpRatio(unit);
        const 有伤情 = 血量比例 < 0.82;
        const 有保护窗口 = 血量比例 < 0.68 || 快照.hasBadCondition || 快照.isLockedOrControlled || unit?.蓄力技能;
        let 分数 = 8 + (1 - 血量比例) * 95;
        if (有保护窗口 && ['治疗系', '辅助系', '食物系', '控制系'].includes(系别)) { 分数 += 20; 理由.push('团队核心'); }
        if (有保护窗口 && 画像.我方核心 && isCombatUnitIdentityMatch(unit, 画像.我方核心?.name || 画像.我方核心)) { 分数 += 18; 理由.push('我方核心'); }
        if (快照.hasBadCondition || 快照.isLockedOrControlled) { 分数 += 36; 理由.push('异常/控制待处理'); }
        if (unit?.蓄力技能) { 分数 += 22; 理由.push('蓄力待保护'); }
        if (!有伤情 && !有保护窗口) 分数 = Math.min(分数, 12);
        if (团队意图.保护目标 && isCombatUnitIdentityMatch(unit, 团队意图.保护目标)) { 分数 += 34; 理由.push('团队保护'); }
        return { 单位: unit, 名称, 优先级: Math.max(1, Math.round(分数)), 理由: 理由.length ? 理由 : ['常规观察'] };
      };
      const 敌方 = (画像.敌方列表 || []).map(构建敌方项).sort((左, 右) => 右.优先级 - 左.优先级);
      const 友方 = (画像.己方列表 || []).map(构建友方项).sort((左, 右) => 右.优先级 - 左.优先级);
      return {
        敌方,
        友方,
        集火目标: 敌方[0]?.单位 || null,
        保护目标: 友方[0]?.单位 || null,
      };
    }

    function 读取规划目标优先项(规划上下文 = {}, target = {}, 目标立场 = '敌方') {
      const 名称 = 读取规划单位名称(target);
      const 列表 = 目标立场 === '友方' ? 规划上下文?.目标优先表?.友方 || [] : 规划上下文?.目标优先表?.敌方 || [];
      return 列表.find(项 => isCombatUnitIdentityMatch(项.单位, 名称)) || { 单位: target, 名称, 优先级: 20, 理由: ['常规目标'] };
    }

    function 推断战略意图(actor = {}, target = null, 战局画像 = {}, behaviorState = {}) {
      const 系别 = 读取规划单位系别(actor);
      const 候选 = new Map();
      const 加权 = (名称, 分值) => 候选.set(名称, Math.max(0, Number(候选.get(名称) || 0) + Number(分值 || 0)));
      const 基础权重 = 系别基础战略意图权重_V1[系别] || 系别基础战略意图权重默认_V1;
      Object.entries(基础权重).forEach(([意图, 分值]) => 加权(意图, 分值));
      const 敌方最低血 = Math.min(1, ...(战局画像.敌方列表 || []).map(getCombatHpRatio));
      const 我方最低血 = Math.min(1, ...(战局画像.己方列表 || []).map(getCombatHpRatio));
      const 支援窗口 = 读取友方支援窗口_V1(战局画像.己方列表 || []);
      const 危机 = 判定危机层级_V1(战局画像, behaviorState, actor);
      Object.entries(危机.加权 || {}).forEach(([意图, 分值]) => {
        if (意图 === '治疗' && !支援窗口.有治疗窗口) return;
        加权(意图, 分值);
      });
      // 非危机态势仍走数据表;残血/来袭危机统一由 危机层级 处理,避免重复暴叠。
      应用态势战略意图加权_V1(
        { 敌方最低血, 我方最低血, 我方残血触发: 支援窗口.有治疗窗口, 战局画像, behaviorState },
        加权,
      );
      if (支援窗口.有治疗窗口) {
        加权('治疗', 18 + 支援窗口.残血数量 * 12 + 支援窗口.异常数量 * 10);
      }
      if (支援窗口.有保核窗口) {
        加权('保核', 14 + 支援窗口.重伤数量 * 14 + 支援窗口.蓄力待保护数量 * 10);
      }
      // 复杂谓词(对象 / 函数调用)留在代码里
      if ((战局画像.控制目标 && ['治疗系', '控制系', '辅助系'].includes(读取规划单位系别(战局画像.控制目标))) || behaviorState?.isChargingHighThreat) 加权('控制', 30);
      if ((战局画像.敌方列表 || []).some(单位 => {
        const 快照 = buildConditionTacticalSnapshot(单位);
        return 快照.hasShielded || 快照.hasDefenseBuffed || 快照.hasReactiveDefense;
      })) 加权('破防', 24);
      if ((战局画像.己方列表 || []).some(单位 => buildConditionTacticalSnapshot(单位).hasBadCondition)) 加权('驱散', 24);
      // Phase 2.A: 集火窗口检测 — 既定集火目标残血时,意图层强化集火 (与目标层 +52 集火加分叠加)
      const 集火目标 = 战局画像.集火目标;
      if (集火目标 && getCombatHpRatio(集火目标) < 时机决策阈值_V1.集火窗口_目标残血阈值) {
        加权('集火', 30);
      }
      // Phase 2.B: 节奏感 — 领先时加速输出,落后时减速防守
      const 己方列表 = 战局画像.己方列表 || [];
      const 敌方列表 = 战局画像.敌方列表 || [];
      if (己方列表.length && 敌方列表.length) {
        const 我方平均血 = 己方列表.reduce((s, u) => s + getCombatHpRatio(u), 0) / 己方列表.length;
        const 敌方平均血 = 敌方列表.reduce((s, u) => s + getCombatHpRatio(u), 0) / 敌方列表.length;
        const 平均血差 = 我方平均血 - 敌方平均血;
        if (平均血差 > 时机决策阈值_V1.节奏感_领先差) {
          加权('击杀', 20); 加权('终局爆发', 20);
        } else if (平均血差 < -时机决策阈值_V1.节奏感_落后差) {
          加权('保核', 24); 加权('拖回合', 18);
        }
      }
      // Phase 3.D: 反击意图 — 上回合被打掉 ≥ 阈值 HP 时主动反击
      // 自管理 HP 快照在 actor.决策记忆.last_inference,跨回合时计算 lastTurnTakenDamage
      try {
        const 当前回合_phase3d = Number(behaviorState?.round || 0);
        const 当前HP = Number(actor?.HP || actor?.属性?.HP || 0);
        const HP上限 = Math.max(1, Number(actor?.HP上限 || actor?.属性?.HP上限 || 1));
        if (actor && typeof actor === 'object') {
          if (!actor.决策记忆) actor.决策记忆 = {};
          const 上次推断 = actor.决策记忆.last_inference || null;
          if (上次推断 && 当前回合_phase3d > 上次推断.round) {
            const 受击差 = Math.max(0, 上次推断.hp - 当前HP);
            const 受击比例 = 受击差 / HP上限;
            if (受击比例 > 时机决策阈值_V1.反击意图_伤害阈值) {
              加权('反击', 28);
              加权('击杀', 12);
            }
          }
          // 仅当跨回合时刷新快照,避免同回合多次推断时连锁覆盖
          if (!上次推断 || 当前回合_phase3d > 上次推断.round) {
            actor.决策记忆.last_inference = { round: 当前回合_phase3d, hp: 当前HP };
          }
        }
      } catch (error) {
        console.warn('[Phase 3.D] 反击意图判定异常,降级跳过', error?.message);
      }
      // Phase D: 战场情境表 — 8 个独立情境数据驱动加权(取代过去对"残血/压力"两线的单点依赖)
      try {
        应用战场情境加权_V1(战局画像, behaviorState, actor, 加权, { 跳过危机情境: 危机.层级 !== '无危机' });
      } catch (error) {
        console.warn('[Phase D] 战场情境加权异常,降级跳过', error?.message);
      }
      // Phase 4: 对手认知 + 危机互斥层级 — 用累计威胁和意图预测替代单体 max
      try {
        const 对手意图 = 汇总对手意图预测_V1(战局画像, behaviorState);
        if (对手意图.爆发 >= 28 || 对手意图.输出 >= 32) {
          加权('控制', 22);
          加权('保核', 危机.层级 === '致命危机' ? 18 : 8);
        }
        if (对手意图.治疗 >= 24 || 对手意图.保核 >= 28) {
          加权('击杀', 18);
          加权('资源消耗', 14);
        }
        if (对手意图.控制 >= 28) {
          加权('驱散', 12);
          加权('控制', 10);
        }
        if (危机.层级 !== '致命危机' && 判定反击窗口_V1(actor, 战局画像, behaviorState)) {
          加权('击杀', 30);
          加权('反击', 28);
          加权('控制', 16);
          加权('破防', 12);
          加权('保核', -22);
          加权('拖回合', -20);
        }
      } catch (error) {
        console.warn('[Phase 4] 对手认知判定异常,降级跳过', error?.message);
      }
      const 意图列表 = [...候选.entries()]
        .map(([名称, 权重]) => ({ 名称, 权重: Math.round(权重) }))
        .filter(项 => 项.权重 > 0)
        .sort((左, 右) => 右.权重 - 左.权重);
      return {
        主意图: 意图列表[0]?.名称 || '集火',
        意图列表,
        权重表: Object.fromEntries(意图列表.map(项 => [项.名称, 项.权重])),
      };
    }

    function 构建规划上下文(actor = {}, target = null, combatData = {}, behaviorState = {}) {
      const 战局画像 = behaviorState?.战局画像 || 构建战局画像(actor, target, combatData, behaviorState);
      const 目标优先表 = behaviorState?.目标优先表 || 计算目标优先表(actor, combatData, 战局画像);
      const 战略意图 = behaviorState?.战略意图 || 推断战略意图(actor, target, 战局画像, behaviorState);
      const 阵营 = 读取规划单位阵营(actor, combatData) || '玩家';
      const 记忆 = actor?.决策记忆 || {};
      return {
        actor,
        target,
        combatData,
        战局画像,
        角色定位: 读取规划单位系别(actor) || '未知',
        团队意图: 确保队伍临时意图(combatData)[阵营] || {},
        目标优先表,
        战略意图,
        资源压力: 战局画像.资源压力,
        行为历史摘要: {
          上次行动: String(记忆.last_action || ''),
          近期行动: { ...(记忆.recent_actions || {}) },
          关注对象: String(记忆.关注对象 || ''),
          关注原因: String(记忆.focus_reason || ''),
        },
        来袭窗口摘要: behaviorState?.threatProfile || {},
      };
    }

// === Phase A 重构: 计算技能战术修正 → 5 层架构 ===
// 6 个新函数:
//   计算战场指标_V1     共享指标计算(orchestrator 用一次, 各层复用)
//   计算基础意图修正_V1 基础层 (目标优先级 + 12 项 加意图)
//   计算情境修正_V1     情境修正层 (节奏/切换/收割/救急/破盾/打断/多样性/重启)
//   计算机制时机修正_V1 机制专属层 (融合/大招/召唤/稀缺/装备)
//   计算资源约束修正_V1 资源约束层 (保留/不可释放/两回合预判/终局放行/高耗低收益)
//   计算否决修正_V1     一票否决层 (无实际收益)
// 主函数 计算技能战术修正 改成调度器, 行为零变化 (加权数值 / 触发条件 / 顺序全部一致)

    function 计算战场指标_V1(skill = {}, actor = {}, target = null, 规划上下文 = {}, 基础收益 = {}) {
      const 效果列表 = getSkillEffects(skill, { 行为规划: true, actor, caster: actor, target, defender: target, skill });
      const 规划技能 = { ...skill, __规划水合效果数组: 效果列表 };
      const summary = deriveBattleSummaryFromEffects(规划技能);
      const mainType = inferMainTypeFromEffects(规划技能) || '无';
      const 战斗目标类型 = inferSkillPrimaryTargetKind(skill);
      const 目标立场 = ['自身', '友方单体', '友方群体'].includes(战斗目标类型) ? '友方' : '敌方';
      const 规划目标 = 选择技能规划目标_V1(规划技能, actor, 规划上下文, target || (目标立场 === '友方' ? 规划上下文.目标优先表?.保护目标 : 规划上下文.目标优先表?.集火目标));
      const 有效目标 = 规划目标 || target;
      const 目标项 = 读取规划目标优先项(规划上下文, 有效目标, 目标立场);
      const 意图 = 规划上下文.战略意图?.权重表 || {};
      const 技能文本 = JSON.stringify(效果列表 || []) + String(skill?.name || skill?.魂技名 || '');
      const 是输出 = mainType === '伤害类' || getSkillType(skill) === '输出' || 效果列表.some(effect => String(effect?.原型 || '').trim() === '伤害结算');
      const 是控制 = mainType === '控制类' || isBattleSkillControlProfile(规划技能, { summary });
      const 是治疗 = 效果列表.some(effect => isBattleRecoverEffect(effect, ['vit']));
      const 是防护 = summary.防御性质 !== '无' || 是团队保护技能(规划技能, { summary });
      const 是驱散 = 效果列表.some(effect => String(effect?.原型 || '').trim() === '状态移除' || String(effect?.原型 || '').trim() === '机制抹消');
      const 是破防 = isBattleSkillShieldBreakProfile(规划技能) || /防御剥夺|精神抗性剥夺|防御穿透|护盾/.test(技能文本);
      const 是召唤 = 效果列表.some(effect => String(effect?.原型 || '').trim() === '召唤生成');
      const 是维持 = 拆分行为规划消耗模式(getSkillCostText(skill)).含维持;
      const 技能消耗压力 = 计算技能消耗压力(skill, actor);
      const 是高耗 = 技能消耗压力.综合压力 >= 0.34 || 技能消耗压力.可释放 === false;
      const 可斩杀 = getCombatHpRatio(有效目标 || {}) < 0.36 || Number(基础收益?.净收益 || 0) >= 120;
      const 技能名 = String(skill?.name || skill?.魂技名 || skill?.技能名称 || '').trim();
      const 记忆候选键 = 读取行为记忆候选键_V1(skill, { name: 技能名 });
      const 重复次数 = 读取归一行为记忆值_V1(规划上下文.行为历史摘要?.近期行动 || {}, 记忆候选键, 0);
      const 来源类别 = 读取战斗来源类别上下文(skill, '魂技').来源类别;
      const 是融合技 = 来源类别 === '武魂融合技';
      const 是大招 = summary.风险等级 === '高' && !是融合技;
      const 是稀缺技能 = ['血脉技能', '气血魂技', '魂骨技能'].includes(来源类别);
      const 是装备技能 = 来源类别 === '装备技能';
      const 敌方最低血 = Math.min(1, ...(规划上下文.战局画像?.敌方列表 || []).map(getCombatHpRatio));
      const 我方最低血 = Math.min(1, ...(规划上下文.战局画像?.己方列表 || []).map(getCombatHpRatio));
      const 当前回合 = Number(规划上下文.combatData?.回合 || 0);
      const 终局压力 = Number(规划上下文.战局画像?.终局压力 || 0);
      const 目标快照 = buildConditionTacticalSnapshot(有效目标 || {});
      const 目标血量比例 = 有效目标 ? getCombatHpRatio(有效目标) : 1;
      const 保护目标血量比例 = getCombatHpRatio(规划上下文.战局画像?.治疗目标 || 规划上下文.战局画像?.保护目标 || actor);
      const 预估伤害 = 是输出 && 有效目标
        ? estimateIncomingActionThreat(actor, 有效目标, {
            action_type: '技能', type: '技能',
            cast_time: getSkillCastTime(skill),
            skill: 规划技能,
          }, 规划上下文.combatData || {}).projectedDamageRatio
        : 0;
      const 行动类别 = 推断行动类别_V1({
        是输出, 是控制, 是治疗, 是防护, 是破防, 是召唤, 可斩杀, 是融合技, 是大招, 是稀缺技能,
      });
      const 标准行为状态 = 读取标准规划行为状态_V1(规划上下文);
      const 一阶风险 = 评估我方行动后一阶风险_V1(行动类别, actor, 有效目标, 规划上下文.战局画像 || {}, 标准行为状态);
      const 防御动作类型 = 判定防御动作类型_V1(规划技能, { name: 技能名 });
      const 防御动作质量 = 评分防御动作质量_V1(防御动作类型, actor, 有效目标 || actor, 标准行为状态);
      const 上回合效果标签 = 推断上回合效果标签_V1(actor, 有效目标, 规划上下文);
      return {
        skill, actor, target: 有效目标, 原始目标: target, 规划目标, 规划上下文, 基础收益,
        效果列表, 规划技能, summary, mainType, 战斗目标类型, 目标立场, 目标项, 意图, 技能文本,
        是输出, 是控制, 是治疗, 是防护, 是驱散, 是破防, 是召唤, 是维持,
        技能消耗压力, 是高耗, 可斩杀, 技能名, 重复次数,
        来源类别, 是融合技, 是大招, 是稀缺技能, 是装备技能,
        敌方最低血, 我方最低血, 当前回合, 终局压力,
        目标快照, 目标血量比例, 保护目标血量比例, 预估伤害, 行动类别, 一阶风险, 标准行为状态,
        记忆候选键, 防御动作类型, 防御动作质量, 上回合效果标签,
      };
    }

    function 计算基础意图修正_V1(指标) {
      const { 目标项, 目标立场, 意图, 是输出, 是控制, 是治疗, 是防护, 是驱散, 是破防, 是维持, 可斩杀, 规划上下文 } = 指标;
      const 轨迹 = []; let delta = 0;
      if (目标项?.优先级 > 0) {
        const 增量 = Math.round(Number(目标项.优先级 || 0) * (目标立场 === '友方' ? 0.16 : 0.14));
        delta += 增量;
        轨迹.push(`目标:${目标项.名称 || '未知'}(${(目标项.理由 || []).join('/')}) +${增量}`);
      }
      const 加意图 = (名称, 条件, 系数 = 0.36) => {
        if (!条件 || !意图[名称]) return;
        const 增量 = Math.round(Number(意图[名称] || 0) * 系数);
        delta += 增量;
        轨迹.push(`意图:${名称} +${增量}`);
      };
      const 技能文本 = 指标.技能文本;
      加意图('击杀', 是输出 && 可斩杀, 0.5);
      加意图('集火', 是输出 && 目标立场 === '敌方', 0.34);
      加意图('控制', 是控制, 0.42);
      加意图('保核', 是防护 && 目标立场 === '友方', 0.42);
      加意图('治疗', 是治疗, 0.48);
      加意图('驱散', 是驱散, 0.42);
      加意图('破防', 是破防, 0.44);
      加意图('破召唤', 是输出 && 规划上下文.战局画像?.召唤压力 > 0.12, 0.26);
      加意图('维持压制', 是维持 || 是控制, 0.32);
      加意图('资源消耗', /资源转移|资源锁定|消耗/.test(技能文本), 0.32);
      加意图('拖回合', 是防护 || 是治疗, 0.28);
      加意图('终局爆发', 是输出 && 可斩杀, 0.46);
      delta = 应用层级clamp_V1(delta, 战术修正层级量级_V1.基础层);
      return { delta, 轨迹 };
    }

    function 计算情境修正_V1(指标) {
      const { actor, target, 目标项, 目标立场, 是输出, 是控制, 是治疗, 是防护, 是破防, 可斩杀, 规划上下文,
        技能名, 重复次数, 当前回合, 目标快照, 目标血量比例, 保护目标血量比例, 预估伤害, 一阶风险, 行动类别,
        防御动作质量, 上回合效果标签 } = 指标;
      const 轨迹 = []; let delta = 0;
      // Phase 3.G: 目标切换惩罚
      const 上次行动目标 = String(actor?.决策记忆?.last_action_target || '').trim();
      const 当前目标名 = String(目标项?.名称 || target?.name || target?.名称 || '').trim();
      if (上次行动目标 && 当前目标名 && 上次行动目标 !== 当前目标名 && 目标立场 === '敌方') {
        const 上次目标存活 = (规划上下文.战局画像?.敌方列表 || []).some(单位 => {
          const 名 = String(单位?.name || 单位?.名称 || '').trim();
          return 名 === 上次行动目标 && (单位?.状态?.存活 !== false) && getCombatHpRatio(单位) > 0;
        });
        const 切换收益显著 = target && 计算对手单体威胁_V1(target, 规划上下文.战局画像, 规划上下文) > 0.48 && (target?.蓄力技能 || 一阶风险?.风险变化 < -0.28);
        if (上次目标存活 && !切换收益显著) {
          delta -= 15;
          轨迹.push(`目标切换 ${上次行动目标}→${当前目标名} -15`);
        } else if (上次目标存活 && 切换收益显著) {
          delta += 10;
          轨迹.push(`目标切换豁免 ${当前目标名} +10`);
        }
      }
      // Phase 2.C: 行为多样性扣减(含动态阶段缩放)
      if (重复次数 > 0 && !(可斩杀 || 是治疗 && getCombatHpRatio(规划上下文.战局画像?.治疗目标 || actor) < 0.45 || 规划上下文.来袭窗口摘要?.lethalRisk)) {
        const 上次同技 = String(规划上下文.行为历史摘要?.上次行动 || '').trim() === 技能名;
        const 阶段缩放 = 当前回合 < 时机决策阈值_V1.多样性早期回合 ? 时机决策阈值_V1.多样性早期缩放
                      : 当前回合 < 时机决策阈值_V1.多样性中期回合 ? 时机决策阈值_V1.多样性中期缩放
                      : 1.0;
        const 扣减原始 = 重复次数 * 技能战术修正阈值_V1.行为多样性单次重复加成
          + 规划上下文.战局画像.行为多样性压力 * 技能战术修正阈值_V1.行为多样性压力倍增
          + (上次同技 ? 技能战术修正阈值_V1.行为多样性同技额外 : 0);
        const 扣减 = Math.round(Math.min(技能战术修正阈值_V1.行为多样性惩罚峰值 * 阶段缩放, 扣减原始 * 阶段缩放));
        delta -= 扣减;
        轨迹.push(`行为多样性 -${扣减}`);
      }
      // Phase 3.I: 遗忘机制重启加成
      if (重复次数 === 0) {
        const 上次使用回合 = Number(actor?.决策记忆?.action_last_used_round?.[技能名] || 0);
        if (上次使用回合 > 0 && 当前回合 - 上次使用回合 >= 时机决策阈值_V1.遗忘机制_重启回合阈值) {
          delta += 时机决策阈值_V1.遗忘机制_重启加成;
          轨迹.push(`遗忘重启 +${时机决策阈值_V1.遗忘机制_重启加成}`);
        }
      }
      // 输出类: 低伤刮痧 / 有效伤害 / 收割窗口
      if (是输出) {
        if (预估伤害 >= 0 && 预估伤害 < 0.015 && !是控制 && !是破防) {
          const 扣减 = 预估伤害 < 0.006 ? 78 : 58;
          delta -= 扣减;
          轨迹.push(`低伤刮痧 -${扣减}`);
        } else if (预估伤害 >= 0.04) {
          const 增量 = Math.min(42, Math.round(预估伤害 * 180));
          delta += 增量;
          轨迹.push(`有效伤害 +${增量}`);
        }
        if (目标血量比例 < 0.28 && 预估伤害 >= 0.015) {
          delta += 34;
          轨迹.push('收割窗口 +34');
        }
      }
      // 治疗类: 过疗风险 / 救急治疗
      if (是治疗) {
        if (目标血量比例 > 0.88 && !目标快照.hasBadCondition && !规划上下文.来袭窗口摘要?.lethalRisk) {
          delta -= 42;
          轨迹.push('过疗风险 -42');
        } else if (目标血量比例 < 0.58 || 保护目标血量比例 < 0.58 || 规划上下文.来袭窗口摘要?.lethalRisk) {
          delta += 46;
          轨迹.push('救急治疗 +46');
        }
      }
      // 防护类: 保核承压 / 重复防护
      if (是防护) {
        if (目标血量比例 < 0.7 || 保护目标血量比例 < 0.7 || 规划上下文.来袭窗口摘要?.severeDamage) {
          delta += 34;
          轨迹.push('保核承压 +34');
        } else if (目标快照.hasShielded || 目标快照.hasReactiveDefense) {
          delta -= 24;
          轨迹.push('重复防护 -24');
        }
      }
      if (防御动作质量?.修正) {
        delta += 防御动作质量.修正;
        轨迹.push(`${防御动作质量.理由} ${防御动作质量.修正 > 0 ? '+' : ''}${防御动作质量.修正}`);
      }
      // 控制 + 蓄力/维持: 打断/破维持
      if (是控制 && (target?.蓄力技能 || 读取规划维持压力(target || {}) > 0)) {
        const 增量 = target?.蓄力技能 ? 42 : 30;
        delta += 增量;
        轨迹.push(`打断/破维持 +${增量}`);
      }
      // 破防 + 已护盾/防御buff/反应防御: 破盾破防
      if (是破防 && (目标快照.hasShielded || 目标快照.hasDefenseBuffed || 目标快照.hasReactiveDefense)) {
        delta += 38;
        轨迹.push('破盾破防 +38');
      }
      if (一阶风险?.风险变化 < -0.25) {
        const 增量 = Math.min(34, Math.round(Math.abs(一阶风险.风险变化) * 72));
        delta += 增量;
        轨迹.push(`一阶前瞻:${一阶风险.理由} +${增量}`);
      } else if (一阶风险?.风险变化 > 0.18) {
        const 扣减 = Math.min(28, Math.round(一阶风险.风险变化 * 70));
        delta -= 扣减;
        轨迹.push(`一阶前瞻:${一阶风险.理由} -${扣减}`);
      }
      if (行动类别 === '输出' && 上回合效果标签?.has('控制')) {
        delta += 技能时机约束表_V1.控制后输出加成;
        轨迹.push(`连招-控制后输出 +${技能时机约束表_V1.控制后输出加成}`);
      }
      if ((行动类别 === '终局爆发' || 是输出) && 上回合效果标签?.has('破防')) {
        delta += 技能时机约束表_V1.破防后爆发加成;
        轨迹.push(`连招-破防后爆发 +${技能时机约束表_V1.破防后爆发加成}`);
      }
      if (行动类别 === '资源保留' && 上回合效果标签?.has('治疗')) {
        delta += 技能时机约束表_V1.治疗后拖回合加成;
        轨迹.push(`连招-治疗后拖回合 +${技能时机约束表_V1.治疗后拖回合加成}`);
      }
      if (是防护 && 上回合效果标签?.has('召唤')) {
        delta += 技能时机约束表_V1.召唤后护宿主加成;
        轨迹.push(`连招-召唤后护宿主 +${技能时机约束表_V1.召唤后护宿主加成}`);
      }
      delta = 应用层级clamp_V1(delta, 战术修正层级量级_V1.情境修正层);
      return { delta, 轨迹 };
    }

    function 计算机制时机修正_V1(指标) {
      const { actor, skill, 是融合技, 是大招, 是稀缺技能, 是装备技能, 是召唤, 来源类别,
        敌方最低血, 我方最低血, 当前回合, 终局压力, 可斩杀, 规划上下文, 技能名, 是高耗, 是控制, 记忆候选键 } = 指标;
      const 轨迹 = []; let delta = 0;
      // Phase 3.A: 融合技时机
      if (是融合技) {
        if (敌方最低血 < 时机决策阈值_V1.融合技释放_敌方残血阈值) {
          delta += 80;
          轨迹.push('融合技-斩杀窗口 +80');
        } else if (终局压力 > 0.55 || 可斩杀) {
          delta += 60;
          轨迹.push('融合技-终局窗口 +60');
        } else {
          delta -= 30;
          轨迹.push('融合技-时机未到 -30');
        }
      }
      // Phase 3.B: 大招节奏感
      if (是大招) {
        const 大招可释放 = 敌方最低血 < 时机决策阈值_V1.大招释放_敌方残血阈值
          || 我方最低血 < 时机决策阈值_V1.大招释放_我方残血阈值
          || 当前回合 >= 时机决策阈值_V1.大招释放_最早回合;
        if (!大招可释放) {
          delta -= 50;
          轨迹.push('大招-早期克制 -50');
        }
      }
      // Phase 3.C: 召唤时机
      if (是召唤) {
        const 我方人数 = (规划上下文.战局画像?.己方列表 || []).length;
        const 敌方人数 = (规划上下文.战局画像?.敌方列表 || []).length;
        const 我方召唤数 = (规划上下文.战局画像?.我方召唤数 || 0);
        if (敌方人数 - 我方人数 >= 时机决策阈值_V1.召唤_数量劣势触发) {
          delta += 40;
          轨迹.push('召唤-数量劣势 +40');
        }
        if (我方最低血 < 时机决策阈值_V1.召唤_我方残血触发) {
          delta += 30;
          轨迹.push('召唤-保护需求 +30');
        }
        if (我方召唤数 >= 时机决策阈值_V1.召唤_过载阈值) {
          delta -= 60;
          轨迹.push('召唤-已过载 -60');
        }
      }
      // Phase 3.E: 稀缺技能时机 (与 3.A 融合 / 3.B 大招 互斥)
      if (是稀缺技能 && !是融合技 && !是大招) {
        if (敌方最低血 < 时机决策阈值_V1.稀缺技能_敌方残血阈值) {
          delta += 60;
          轨迹.push(`稀缺技能[${来源类别}]-斩杀窗口 +60`);
        } else if (终局压力 > 时机决策阈值_V1.稀缺技能_终局压力阈值 || 可斩杀) {
          delta += 40;
          轨迹.push(`稀缺技能[${来源类别}]-终局窗口 +40`);
        } else {
          delta -= 20;
          轨迹.push(`稀缺技能[${来源类别}]-时机未到 -20`);
        }
      }
      // Phase 3.F: 装备技能释放时机
      if (是装备技能) {
        const 装备 = actor?.装备 || {};
        const 已装备数 = ['斗铠', '机甲', '武器', '防具'].filter(部位 =>
          String(装备?.[部位]?.装备状态 || '').trim() === '已装备').length;
        const 技能名_ = String(skill?.name || skill?.魂技名 || skill?.技能名称 || '');
        if (已装备数 >= 1 && /(穿戴|装备|展开|附体|召唤机甲)/.test(技能名_)) {
          delta -= 80;
          轨迹.push('装备-已激活避免重复 -80');
        } else if (我方最低血 < 时机决策阈值_V1.装备_紧急释放_我方残血阈值
          || 终局压力 > 时机决策阈值_V1.装备_紧急释放_终局压力阈值) {
          delta += 50;
          轨迹.push('装备-紧急释放窗口 +50');
        } else if (当前回合 < 时机决策阈值_V1.装备_早期克制回合) {
          delta -= 20;
          轨迹.push('装备-早期克制 -20');
        } else {
          delta += 20;
          轨迹.push('装备-标准穿戴时机 +20');
        }
      }
      const 上次使用回合 = 读取归一行为记忆值_V1(actor?.决策记忆?.action_last_used_round || {}, 记忆候选键 || [技能名], 0);
      if (上次使用回合 > 0 && 当前回合 >= 上次使用回合) {
        const 间隔 = 当前回合 - 上次使用回合;
        const 重复阈值 = 是融合技 ? 技能时机约束表_V1.融合技重复回合
          : 是大招 ? 技能时机约束表_V1.大招重复回合
          : 是稀缺技能 ? 技能时机约束表_V1.稀缺技能重复回合
          : 是装备技能 ? 技能时机约束表_V1.装备技能重复回合
          : 技能时机约束表_V1.普通技能重复回合;
        if (间隔 < 重复阈值 && !(可斩杀 || 是控制 && 规划上下文.来袭窗口摘要?.lethalRisk)) {
          轨迹.push('近期重复交由效用衰减层处理');
        }
      }
      delta = 应用层级clamp_V1(delta, 战术修正层级量级_V1.机制专属层);
      return { delta, 轨迹 };
    }

    function 计算资源约束修正_V1(指标) {
      const { 意图, 是高耗, 可斩杀, 规划上下文, 技能消耗压力, 基础收益, 一阶风险 } = 指标;
      const 轨迹 = []; let delta = 0;
      // 保留资源(意图驱动)
      if (意图.保留资源 && 是高耗 && !可斩杀 && !规划上下文.来袭窗口摘要?.lethalRisk) {
        const 扣减 = Math.round(Math.min(60, 意图.保留资源 * 0.62));
        delta -= 扣减;
        轨迹.push(`保留资源 -${扣减}`);
      }
      // 资源不可释放 / 两回合预判 / 终局放行
      const 断档压力 = Math.max(
        Number(规划上下文.资源压力 || 0),
        Number(技能消耗压力.综合压力 || 0),
        Number(技能消耗压力.当前压力 || 0),
      );
      if (技能消耗压力.可释放 === false && !可斩杀) {
        const 扣减 = Math.round(Math.min(100, 90 + 断档压力 * 50));
        delta -= 扣减;
        轨迹.push(`资源不可释放 -${扣减}`);
      } else if (是高耗 && !可斩杀 && !规划上下文.来袭窗口摘要?.lethalRisk) {
        const 扣减 = Math.round(Math.min(86, 18 + 断档压力 * 90));
        delta -= 扣减;
        轨迹.push(`两回合资源预判 -${扣减}`);
      } else if (是高耗 && 可斩杀 && 断档压力 <= 0.32) {
        delta += 28;
        轨迹.push('终局资源放行 +28');
      } else if (是高耗 && 可斩杀 && 断档压力 > 0.32 && !规划上下文.来袭窗口摘要?.lethalRisk) {
        const 扣减 = Math.round(Math.min(72, 12 + 断档压力 * 110));
        delta -= 扣减;
        轨迹.push(`斩杀高耗资源断档 -${扣减}`);
      }
      // 高耗低收益
      if (是高耗 && Number(基础收益?.净收益 || 0) < 45 && !可斩杀 && !规划上下文.来袭窗口摘要?.lethalRisk) {
        delta -= 32;
        轨迹.push('高耗低收益 -32');
      }
      if (是高耗 && !可斩杀 && 一阶风险?.风险变化 > 0.18 && !规划上下文.来袭窗口摘要?.lethalRisk) {
        const 扣减 = Math.min(34, Math.round(18 + 一阶风险.风险变化 * 40));
        delta -= 扣减;
        轨迹.push(`高耗前瞻风险 -${扣减}`);
      }
      if (是高耗 && !可斩杀 && !规划上下文.来袭窗口摘要?.lethalRisk && 断档压力 > 0.62) {
        const 扣减 = Math.min(技能时机约束表_V1.资源断档惩罚, Math.round(断档压力 * 44));
        delta -= 扣减;
        轨迹.push(`释放后资源断档 -${扣减}`);
      }
      delta = 应用层级clamp_V1(delta, 战术修正层级量级_V1.资源约束层);
      return { delta, 轨迹 };
    }

    function 计算否决修正_V1(指标) {
      const { 是输出, 是控制, 是治疗, 是防护, 是召唤, 基础收益 } = 指标;
      const 轨迹 = []; let delta = 0;
      if (!是输出 && !是治疗 && !是防护 && !是控制 && !是召唤 && Number(基础收益?.净收益 || 0) <= 8) {
        delta -= 26;
        轨迹.push('无实际收益 -26');
      }
      delta = 应用层级clamp_V1(delta, 战术修正层级量级_V1.一票否决层);
      return { delta, 轨迹 };
    }

    // ===== 主调度器 =====
    function 计算技能战术修正(skill = {}, actor = {}, target = null, 规划上下文 = {}, 基础收益 = {}) {
      if (!skill || !规划上下文?.战局画像) return { 修正值: 0, 轨迹: [] };
      const 指标 = 计算战场指标_V1(skill, actor, target, 规划上下文, 基础收益);
      const r1 = 计算基础意图修正_V1(指标);
      const r2 = 计算情境修正_V1(指标);
      const r3 = 计算机制时机修正_V1(指标);
      const r4 = 计算资源约束修正_V1(指标);
      const r5 = 计算否决修正_V1(指标);
      const 总修正 = 应用层级clamp_V1(r1.delta + r2.delta + r3.delta + r4.delta + r5.delta, 战术修正层级量级_V1.总修正层);
      return {
        修正值: Math.round(总修正),
        轨迹: [...r1.轨迹, ...r2.轨迹, ...r3.轨迹, ...r4.轨迹, ...r5.轨迹],
      };
    }


    function 读取行为规划资源键(effect = {}) {
      const 文本 = String(effect?.资源 || effect?.属性 || '').trim();
      if (/生命|HP|hp/i.test(文本)) return 'hp';
      if (/体力|vit|sta/i.test(文本)) return 'vit';
      if (/魂力|sp/i.test(文本)) return 'sp';
      if (/精神|men/i.test(文本)) return 'men';
      return 'vit';
    }

    function 读取行为规划资源当前值(unit = {}, key = 'vit') {
      if (key === 'sp') return Math.max(0, Number(unit?.sp ?? unit?.属性?.魂力 ?? 0));
      if (key === 'men') return Math.max(0, Number(unit?.men ?? unit?.属性?.精神力 ?? 0));
      if (key === 'hp') return getCombatHpValue(unit);
      return getCombatStaminaValue(unit);
    }

    function 读取行为规划资源上限(unit = {}, key = 'vit') {
      if (key === 'sp') return Math.max(1, Number(unit?.sp_max ?? unit?.属性?.魂力上限 ?? 1));
      if (key === 'men') return Math.max(1, Number(unit?.men_max ?? unit?.属性?.精神力上限 ?? 1));
      if (key === 'hp') return getCombatHpMaxValue(unit);
      return getCombatStaminaMaxValue(unit);
    }

    function 计算行为规划数值变动(rawValue, baseValue = 1) {
      const 数值 = 读取战斗数值正负(rawValue);
      if (读取战斗数值是否百分比(rawValue) || Math.abs(数值) <= 1) return Math.floor(Math.max(1, Number(baseValue || 1)) * 数值);
      return Math.floor(数值);
    }

    function 读取行为规划属性键列表(effect = {}) {
      const 原始列表 = Array.isArray(effect?.属性) ? effect.属性 : [effect?.属性];
      return 原始列表
        .map(属性 => 战斗机制中文键英文名[String(属性 || '').trim()] || String(属性 || '').trim())
        .filter(属性 => ['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max'].includes(属性));
    }

    function 读取行为规划属性值(unit = {}, key = 'str') {
      const final = unit?.final || unit?.最终属性 || {};
      const stats = unit?.属性 && typeof unit.属性 === 'object' ? unit.属性 : {};
      const 中文键 = { str: '力量', def: '防御', agi: '敏捷', vit_max: '体力上限', sp_max: '魂力上限', men_max: '精神力上限' }[key] || key;
      return Math.max(1, Number(final?.[key] ?? unit?.[key] ?? stats?.[key] ?? stats?.[中文键] ?? 1));
    }

    function 读取行为规划属性权重(key = 'str') {
      return {
        str: 0.75,
        agi: 0.62,
        def: 0.7,
        vit_max: 0.72,
        sp_max: 0.52,
        men_max: 0.58,
      }[key] || 0.5;
    }

    function 估算行为规划状态价值(effect = {}) {
      const 状态 = String(effect?.状态 || '').trim();
      const 计算层效果 = effect?.计算层效果 || {};
      if (/眩晕|麻痹|僵直|硬控/.test(状态) || 计算层效果.skip_turn === true || 计算层效果.cannot_react === true) return 95;
      if (/沉默|封技/.test(状态) || 计算层效果.skill_seal === true || 计算层效果.silence === true) return 78;
      if (/缴械|致盲|位移限制|迟缓|软控/.test(状态) || 计算层效果.disarm === true || 计算层效果.blind === true) return 58;
      if (/无敌|霸体|免死/.test(状态) || 计算层效果.invincible === true || 计算层效果.super_armor === true) return 82;
      if (/探查屏蔽|感知屏蔽|精神屏蔽/.test(状态) || 计算层效果.探查屏蔽 === true) return 62;
      if (/中毒|流血|灼烧|冻伤|持续创伤/.test(状态) || 战斗效果含结构化持续伤害({ 战斗效果: 计算层效果 })) return 48;
      return 28;
    }

    function 计算行为规划时间系数(effect = {}) {
      const 延迟回合 = Math.max(0, Number(effect?.延迟回合 || 0));
      const 持续回合 = Math.max(0, Number(effect?.持续回合 || 0));
      const 延迟系数 = 延迟回合 > 0 ? 1 / (1 + 延迟回合 * 0.35) : 1;
      const 持续系数 = 持续回合 > 0 ? Math.min(2.4, 1 + 持续回合 * 0.24) : 1;
      const 遥控系数 = String(effect?.触发方式 || '').trim() === '遥控触发' ? 0.45 : 1;
      return Math.max(0.15, 延迟系数 * 持续系数 * 遥控系数);
    }

    function 估算规则防御释放权重(effect = {}, target = {}, behaviorState = {}, skill = null) {
      const 规则 = String(effect?.规则 || '').trim();
      const 次数 = Math.max(1, Number(effect?.次数 || 1));
      const 生命比例 = getCombatHpValue(target) / Math.max(1, getCombatHpMaxValue(target));
      const 次数系数 = 1 + Math.min(次数 - 1, 3) * 0.28;
      const 来袭风险 = behaviorState?.threatProfile || {};
      const 命中自身 = 来袭风险.targetHitsDefender === true;
      const 高威胁系数 =
        behaviorState?.isChargingHighThreat || behaviorState?.开局绝境窗口
          ? 1.35
          : Number(behaviorState?.团队护援压力 || 0) >= 45
            ? 1.18
            : 1;
      if (规则 === '免死') {
        const 被动技能 = skill && isPassiveSkillData(skill);
        const 濒死系数 = 1 + Math.max(0, 0.62 - 生命比例) * 1.9;
        const 主动折损 = 被动技能 ? 1 : 命中自身 && 来袭风险.lethalRisk === true ? 0.78 : 0.38;
        return Math.min(140, 68 * 次数系数 * 濒死系数 * 高威胁系数 * 主动折损);
      }
      if (规则 === '免伤') {
        const 预估伤害比例 = Math.max(0, Number(来袭风险.projectedDamageRatio || 0));
        const 风险系数 =
          命中自身 && 来袭风险.lethalRisk === true
            ? 2.35
            : 命中自身 && 来袭风险.severeDamage === true
              ? 1.85
              : 命中自身 && 来袭风险.moderateDamage === true
                ? 1.35
                : 命中自身 && 预估伤害比例 > 0
                  ? Math.min(1, 0.45 + 预估伤害比例 * 2.2)
                  : 0.22;
        const 承压系数 = 1 + Math.max(0, 0.78 - 生命比例) * 0.65;
        return Math.min(150, 52 * 次数系数 * 承压系数 * 高威胁系数 * 风险系数);
      }
      return 0;
    }

    function 估算位移执行释放权重(effect = {}, actor = {}, target = {}, behaviorState = {}) {
      const 位移类型 = String(effect?.位移类型 || '').trim();
      const 位移对象 = String(effect?.位移对象 || '').trim();
      const 目标立场 = 读取行为规划单位立场(target, actor, behaviorState);
      const 自身快照 = buildConditionTacticalSnapshot(actor);
      const 目标快照 = buildConditionTacticalSnapshot(target);
      const 自身血量压力 = Math.max(0, 1 - getCombatHpRatio(actor));
      const 目标血量压力 = Math.max(0, 1 - getCombatHpRatio(target));
      const 位移幅度 = Math.max(1, Math.min(30, Number(effect?.距离 || 1)));
      const 位移强度 = Math.max(0.04, Math.min(0.45, Math.abs(读取战斗数值正负(effect?.数值 || 0)) || 位移幅度 * 0.04));
      const 强度分 = 位移强度 * 120;
      const 位移受限折损 = Math.max(0.25, 1 - Math.min(0.65, Number(自身快照.movementLockLevel || 0) * 0.22));
      let 收益 = 0;
      if (['瞬移', '脱离'].includes(位移类型) || 位移对象 === '自身') {
        收益 = 26 + 强度分 + 自身血量压力 * 70 + (自身快照.hasBadCondition ? 12 : 0);
        if (位移类型 === '拉近' && 目标立场 === '敌方') 收益 = 24 + 强度分 + 目标血量压力 * 52 + (目标快照.hasReactiveDefense ? 10 : 0);
      } else if (位移类型 === '拉近') {
        收益 = 30 + 强度分 + 目标血量压力 * 58 + (目标快照.hasStealthed ? 18 : 0);
      } else if (位移类型 === '击退') {
        收益 = 34 + 强度分 + Math.min(40, 计算目标威胁分(target) / 60) + (目标快照.hasTargetLock ? 10 : 0);
      } else if (位移类型 === '换位' || 位移对象 === '自身与目标') {
        收益 = 28 + 强度分 + Math.min(46, 计算目标威胁分(target) / 70) - (目标立场 === '己方' ? 34 : 0);
      }
      if (目标立场 === '己方' && !['瞬移', '脱离'].includes(位移类型) && 位移对象 !== '自身') 收益 *= -0.6;
      return Math.round(Math.max(-80, Math.min(120, 收益 * 位移受限折损)));
    }

    function 估算复制执行释放权重(effect = {}, actor = {}, target = {}, behaviorState = {}) {
      const 复制类型文本 = String(effect?.复制类型 || '复制技能').trim();
      const 复制类型 = /全部/.test(复制类型文本) ? '复制全部' : /属性/.test(复制类型文本) ? '复制属性' : '复制技能';
      const 复制模式 = String(effect?.复制模式 || '即时镜像').trim();
      if (复制模式 === '复刻友方') return 0;
      if (读取行为规划单位立场(target, actor, behaviorState) !== '敌方') return -30;
      const 读取削减比例 = value => {
        const text = String(value ?? '').trim();
        const n = Number(text.replace('%', ''));
        if (!Number.isFinite(n)) return 0.2;
        return Math.max(0, Math.min(0.95, text.includes('%') || n > 1 ? n / 100 : n));
      };
      const 复制倍率 = 1 - 读取削减比例(effect?.削减比例);
      let 收益 = 0;
      if (/属性|全部/.test(复制类型)) {
        const 属性差收益 = ['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max'].reduce((总和, 属性键) => {
          const 目标值 = 读取行为规划属性值(target, 属性键);
          const 自身值 = 读取行为规划属性值(actor, 属性键);
          const 增幅 = Math.max(0, (目标值 * 复制倍率 - 自身值) / Math.max(1, 自身值));
          return 总和 + Math.min(34, 增幅 * 80 * 读取行为规划属性权重(属性键));
        }, 0);
        收益 += 属性差收益;
      }
      if (/技能|全部/.test(复制类型)) {
        const 本次技能 = target?.蓄力技能?.skill || behaviorState?.observedTargetAction?.skill || null;
        if (!本次技能) return 收益 > 0 ? Math.round(收益) : 0;
        if (技能含复制执行(本次技能)) return Math.round(收益);
        const 技能威胁 = deriveBattleSummaryFromEffects(本次技能);
        const 威力 = Number(getPrimaryDamageEffect(本次技能)?.威力倍率 || 0);
        const 技能收益 =
          22 +
          Math.min(70, 威力 * 0.22) +
          (技能威胁.控制强度 === '硬控' ? 38 : 技能威胁.控制强度 !== '无' ? 20 : 0) +
          (技能威胁.防御性质 !== '无' ? 18 : 0) +
          (技能威胁.回复性质 !== '无' ? 14 : 0);
        收益 += 技能收益;
      }
      const 时间系数 = Math.min(1.35, 0.8 + Math.max(0, Number(effect?.保留回合 || 1)) * 0.12);
      return Math.round(Math.max(0, Math.min(160, 收益 * 时间系数)));
    }

    function 估算召唤生成释放权重(effect = {}, actor = {}, target = {}, behaviorState = {}) {
      if (!actor) return 0;
      const combatData = behaviorState?.combatData || {};
      const 召唤单位类型 = String(effect?.召唤单位类型 || '魂兽').trim() || '魂兽';
      const 召唤物名称 = String(effect?.召唤物名称 || effect?.名称 || '召唤物').trim() || '召唤物';
      const 原始数量 = Math.max(1, Math.floor(Number(effect?.召唤数量 || effect?.数量 || 1)));
      const 召唤数量 = 召唤单位类型 === '其他召唤生物' || 召唤单位类型 === '分身' ? Math.min(12, 原始数量) : 1;
      const 已有召唤列表 = 读取召唤单位列表(combatData, { 宿主: actor }).filter(isCombatUnitAlive);
      const 已有同类数量 = 已有召唤列表.filter(单位 => {
        const 单位类型 = String(单位?.类型 || 单位?.召唤单位类型 || '').trim();
        const 单位名称 = String(单位?.基础名称 || 单位?.name || 单位?.名称 || '').replace(/#\d+$/, '').trim();
        return 单位类型 === 召唤单位类型 && (
          召唤单位类型 === '本命召唤兽' ||
          单位名称 === 读取召唤编号基名(召唤物名称)
        );
      }).length;
      if (召唤单位类型 !== '其他召唤生物' && 已有同类数量 > 0) return -28;
      const 默认继承比例 = 召唤单位类型 === '分身' || 召唤单位类型 === '本命召唤兽' ? 0.45 : 0;
      const 属性继承比例 = 读取召唤属性继承对象(effect, 默认继承比例);
      const 平均继承比例 = [
        属性继承比例.力量,
        属性继承比例.防御,
        属性继承比例.敏捷,
        属性继承比例.体力上限,
        属性继承比例.魂力上限,
        属性继承比例.精神力上限,
      ].reduce((总和, 比例) => 总和 + Number(比例 || 0), 0) / 6;
      const 固定强度 = Math.max(0.01, Number(effect?.强度 || effect?.召唤强度 || 1));
      const 召唤强度 = 平均继承比例 > 0 ? 平均继承比例 * 1.25 : 固定强度;
      const 行动模式 = String(effect?.行动模式 || 读取召唤类型配置(召唤单位类型).默认行动模式 || '协同攻击').trim();
      const 承伤规则 = String(effect?.承伤规则 || '').trim();
      const 模式系数 = 行动模式 === '自主行动' ? 1.28 : 行动模式 === '护卫' ? 1.08 : 0.9;
      const 承伤系数 = /护卫|承伤|分摊/.test(`${行动模式}${承伤规则}`) ? 1.16 : 1;
      const 类型系数 = {
        本命召唤兽: 1.35,
        分身: 0.86,
        魂师: 1.15,
        魂兽: 1,
        深渊生物: 1.08,
        其他召唤生物: 0.74,
      }[召唤单位类型] || 0.95;
      const 数量系数 = 1 + Math.min(召唤数量 - 1, 11) * (召唤单位类型 === '其他召唤生物' ? 0.42 : 0.26);
      const 现有压力折损 = 召唤单位类型 === '其他召唤生物'
        ? Math.max(0.42, 1 - Math.min(0.5, 已有同类数量 * 0.08))
        : 1;
      const 自身血量压力 = Math.max(0, 1 - getCombatHpRatio(actor));
      const 目标威胁 = Math.min(36, 计算目标威胁分(target || behaviorState?.primaryTarget || behaviorState?.target) / 75);
      const 资源压力 =
        1 - 限制行为概率(
          (Number(actor.sp || actor?.属性?.魂力 || 0) + Number(actor.men || actor?.属性?.精神力 || 0) + Number(actor.sta ?? actor?.体力 ?? actor?.属性?.体力 ?? 0)) /
          Math.max(1, Number(actor.sp_max || actor?.属性?.魂力上限 || 0) + Number(actor.men_max || actor?.属性?.精神力上限 || 0) + Number(actor.sta_max ?? actor?.体力上限 ?? actor?.属性?.体力上限 ?? 0)),
          0,
          1,
        );
      const 基础收益 = 28 + 召唤强度 * 46 + 目标威胁 + 自身血量压力 * 28;
      const 资源折损 = Math.max(0.42, 1 - 资源压力 * 0.55);
      return Math.round(Math.max(-40, Math.min(190, 基础收益 * 类型系数 * 模式系数 * 承伤系数 * 数量系数 * 现有压力折损 * 资源折损)));
    }

    function 估算炸环释放权重(effect = {}, actor = {}, target = {}, behaviorState = {}, skill = null) {
      if (!actor || !skill) return 0;
      const 目标生命压力 = 1 - getCombatHpRatio(target || behaviorState?.primaryTarget || behaviorState?.target || {});
      const 自身生命压力 = 1 - getCombatHpRatio(actor);
      const 来袭 = behaviorState?.threatProfile || {};
      const 终局窗口 = behaviorState?.isDesperateNoEscape === true || 自身生命压力 > 0.6 || 目标生命压力 > 0.62 || 来袭.lethalRisk === true || 来袭.severeDamage === true;
      const 可炸列表 = 收集角色可炸魂环列表_V1(actor)
        .filter(项 => !读取魂环路径恢复标记_V1(actor, 项.路径).恢复中);
      const 技能路径 = Array.isArray(skill.__魂环路径) ? skill.__魂环路径 : [];
      const 技能魂环 = 技能路径.length
        ? 可炸列表.find(项 => 路径相同_V1(项.路径, 技能路径))
        : null;
      const 候选列表 = 技能魂环 ? [技能魂环] : 可炸列表.slice(0, 1);
      if (!候选列表.length) return 0;
      const 总年限 = 候选列表.reduce((总和, 项) => 总和 + Math.max(100, Number(项.年限 || 100)), 0);
      const 年限收益 = Math.min(92, Math.log10(Math.max(100, 总年限)) * 18);
      const 强化倍率 = Math.max(0.1, Number(effect?.强化倍率 || effect?.burstEnhanceRatio || effect?.数值 || 1));
      const 恢复惩罚 = Math.min(54, 计算炸环恢复时长tick_V1(候选列表) / 1440 * 8);
      const 战况收益 = 终局窗口 ? 58 : Number(behaviorState?.round || 0) <= 1 ? -38 : -12;
      return Math.round(Math.max(0, Math.min(150, 年限收益 * Math.min(2.2, 强化倍率) + 战况收益 - 恢复惩罚)));
    }

    function 估算时光回溯释放权重(effect = {}, actor = {}, target = {}, behaviorState = {}, skill = null) {
      const 发动方式 = 解析时光回溯发动方式(effect);
      const 来袭 = behaviorState?.threatProfile || {};
      if (发动方式 === '主动') {
        const 目标威胁 = Math.min(62, 计算目标威胁分(target || behaviorState?.primaryTarget || behaviorState?.target) / 55);
        const 高威胁收益 = behaviorState?.isChargingHighThreat || 来袭.incomingAttackIntent ? 34 : 6;
        const 压制值 = Math.max(0.05, Math.min(0.95, Number(effect?.时光回溯反应压制值 || 时光回溯反应压制值)));
        return Math.round(Math.max(0, Math.min(135, 目标威胁 + 高威胁收益 + 压制值 * 90)));
      }
      const 保护目标 = target || actor;
      if (查找时光回溯状态条目(保护目标)) return -28;
      const 消耗 = skill ? 计算时光回溯专属消耗(skill, 保护目标, effect, {
        damage: Math.max(0, Number(来袭.projectedDamage || 0)),
      }) : { canCast: true };
      const 伤害比例 = Math.max(0, Number(来袭.projectedDamageRatio || 0));
      let 收益 = 18 + 伤害比例 * 120;
      if (来袭.lethalRisk === true) 收益 += 76;
      else if (来袭.severeDamage === true || 来袭.severeControl === true) 收益 += 52;
      else if (来袭.moderateDamage === true) 收益 += 28;
      if (!来袭.targetHitsDefender && !来袭.incomingAttackIntent) 收益 -= 42;
      if (消耗.canCast === false) 收益 -= 44;
      return Math.round(Math.max(-60, Math.min(150, 收益)));
    }

    function 估算结算修正选项规划权重(effect = {}, actor = {}, target = {}, behaviorState = {}, skill = null) {
      const 结算 = String(effect?.结算 || '').trim();
      const 数值 = Math.abs(读取战斗数值正负(effect?.数值 || '20%')) || 0.2;
      const 快照 = buildConditionTacticalSnapshot(target || {});
      const 来袭 = behaviorState?.threatProfile || {};
      if (结算 === '持续伤害引爆') return 快照.hasDotPressure || 快照.debuffCount > 0 ? 62 + 快照.debuffCount * 8 : 0;
      if (结算 === '防御穿透' ||结算 === '防御剥夺' || 结算 === '精神抗性剥夺') return 快照.hasShielded || 快照.hasDefenseBuffed || 快照.hasReactiveDefense ? 54 : 12;
      if (结算 === '反击') return 来袭.incomingAttackIntent || 来袭.targetHitsDefender ? 48 + Math.round(数值 * 40) : 10;
      if (结算 === '治疗') return getCombatHpRatio(target || actor) < 0.62 ? 42 + Math.round(数值 * 56) : 8;
      if (['造成伤害', '受到伤害', '伤害吸收', '伤害转治疗', '治疗转伤害', '反伤', '伤害转移', '伤害分摊', '消耗分摊'].includes(结算)) return Math.round(Math.max(12, Math.min(95, 数值 * 110)));
      if (结算 === '消耗' || 结算 === '前摇' || 结算 === '技能效果') return Math.round(Math.max(10, Math.min(72, 数值 * 90)));
      return 0;
    }

    function 估算机制授予规划权重(effect = {}, actor = {}, target = {}, behaviorState = {}, skill = null) {
      const 授予效果列表 = Array.isArray(effect?.授予效果) ? effect.授予效果 : [];
      if (!授予效果列表.length) return 22;
      const 汇总 = 授予效果列表.reduce((总和, 授予效果) => {
        const 原型 = String(授予效果?.原型 || '').trim();
        if (原型 === '结算修正') return 总和 + 估算结算修正选项规划权重(授予效果, actor, target, behaviorState, skill) * 0.72;
        if (原型 === '状态施加') return 总和 + Math.max(0, 估算行为规划状态价值(授予效果)) * 0.62;
        return 总和 + 18;
      }, 0);
      const 次数 = Math.max(1, Number(effect?.可用次数 || 1));
      return Math.round(Math.max(0, Math.min(130, 汇总 * (1 + Math.min(次数 - 1, 3) * 0.18))));
    }

    function 估算机制抹消规划权重(effect = {}, actor = {}, target = {}, behaviorState = {}) {
      const 快照 = buildConditionTacticalSnapshot(target || {});
      const 抹消对象 = 规范化战斗机制抹消对象(effect?.抹消对象 || {});
      const 摘要 = 读取战斗机制抹消对象摘要(抹消对象);
      let 收益 = 28;
      if (/增益|特殊规则/.test(摘要) && 快照.buffCount > 0) 收益 += 24 + 快照.buffCount * 7;
      if (/护盾|防御/.test(摘要) && (快照.hasShielded || 快照.hasDefenseBuffed || 快照.hasReactiveDefense)) 收益 += 44;
      if (/隐身/.test(摘要) && 快照.hasStealthed) 收益 += 46;
      if (/回复|复活/.test(摘要) && 快照.hasHealingTrend) 收益 += 36;
      if (behaviorState?.isChargingHighThreat) 收益 += 12;
      return Math.round(Math.max(0, Math.min(130, 收益)));
    }

    function 估算规则改写规划权重(effect = {}, actor = {}, target = {}, behaviorState = {}) {
      const 规则 = String(effect?.规则 || '').trim();
      const 快照 = buildConditionTacticalSnapshot(target || {});
      const 来袭 = behaviorState?.threatProfile || {};
      if (规则 === '死亡转存活') {
        const 生命比例 = getCombatHpRatio(target || actor);
        return 生命比例 < 0.32 || 来袭.lethalRisk === true ? 96 : 18;
      }
      if (规则 === '缴械') return behaviorState?.isChargingHighThreat || 来袭.incomingAttackIntent ? 68 : 24;
      return 0;
    }

    function 估算资源锁定规划权重(effect = {}, actor = {}, target = {}, behaviorState = {}) {
      const 资源列表 = Array.isArray(effect?.资源) ? effect.资源 : [effect?.资源 || '魂力'];
      const 锁定类型 = String(effect?.锁定类型 || '').trim();
      const 比例 = Math.max(0.05, Math.min(1, Math.abs(读取战斗数值正负(effect?.数值 || effect?.比例 || '50%')) || 0.5));
      const 目标资源压力 = 资源列表.reduce((总和, 资源) => {
        const 键 = 读取行为规划资源键({ 资源 });
        const 当前 = 读取行为规划资源当前值(target, 键);
        const 上限 = 读取行为规划资源上限(target, 键);
        return 总和 + 当前 / Math.max(1, 上限);
      }, 0) / Math.max(1, 资源列表.length);
      let 收益 = 24 + 比例 * 70 + 目标资源压力 * 28;
      if (/回复/.test(锁定类型) && buildConditionTacticalSnapshot(target).hasHealingTrend) 收益 += 24;
      if (/转化/.test(锁定类型) && behaviorState?.isChargingHighThreat) 收益 += 12;
      return Math.round(Math.max(0, Math.min(125, 收益)));
    }

    function 读取行为规划维持释放原型状态(effect = {}) {
      const 原型 = String(effect?.原型 || '').trim();
      if (['伤害结算', '资源变化', '资源转移', '护盾变化', '属性修正', '判定修正', '状态施加', '时窗修正', '状态移除', '资源锁定', '决策干扰'].includes(原型)) return '维持释放';
      if (原型 === '结算修正' && ['造成伤害', '受到伤害', '治疗', '消耗', '前摇', '技能效果', '防御剥夺', '精神抗性剥夺', '伤害转治疗', '治疗转伤害'].includes(String(effect?.结算 || '').trim())) return '维持释放';
      if (原型 === '规则改写' && String(effect?.规则 || '').trim() === '缴械') return '维持释放';
      if (原型 === '机制抹消') return '维持释放';
      if (['规则防御', '状态转移', '状态交换', '机制授予', '复制执行', '位移执行', '召唤生成'].includes(原型)) return '维持存在';
      return '一次性';
    }

    function 拆分行为规划消耗模式(costText = '无') {
      const 文本 = String(costText || '无').trim();
      if (!文本 || 文本 === '无') return { 启动消耗: '无', 维持消耗: '', 含维持: false };
      const 片段 = 文本.split(/维持[:：]?/);
      return {
        启动消耗: (片段[0] || '无').trim() || '无',
        维持消耗: (片段[1] || '').trim(),
        含维持: 片段.length > 1,
      };
    }

    function 计算行为规划资源压力(costText = '无', actor = {}, 覆盖目标数量 = 1) {
      const 技能壳 = {
        消耗: String(costText || '无').trim() || '无',
        __覆盖目标数量: Math.max(1, Math.min(12, Math.floor(Number(覆盖目标数量 || 1)))),
      };
      const 消耗 = parseSkillCostForChar(技能壳, actor, { actor, caster: actor, skill: 技能壳, 当前行动: '行为规划' });
      const stats = actor?.属性 || actor || {};
      const 魂力上限 = Math.max(1, Number(stats.sp_max ?? stats.魂力上限 ?? actor?.sp_max ?? 1));
      const 体力上限 = Math.max(1, Number(stats.sta_max ?? stats.体力上限 ?? stats.vit_max ?? actor?.sta_max ?? 1));
      const 精神上限 = Math.max(1, Number(stats.men_max ?? stats.精神力上限 ?? actor?.men_max ?? 1));
      return {
        消耗,
        压力: 限制行为概率(
          Math.max(0, Number(消耗.reqSp || 0)) / 魂力上限 +
            Math.max(0, Number(消耗.reqVit || 0)) / 体力上限 +
            Math.max(0, Number(消耗.reqMen || 0)) / 精神上限,
          0,
          1,
        ),
      };
    }

    function 估算行为规划维持负荷(skill = {}, effectList = [], sustainCost = '无', actor = {}, behaviorState = {}) {
      const 目标列表 = dedupeCombatTargetList(
        (Array.isArray(effectList) ? effectList : [])
          .flatMap(effect => 解析效果规划目标列表(effect, actor, behaviorState?.primaryTarget || behaviorState?.target, behaviorState, skill))
          .filter(Boolean),
      );
      const 覆盖目标数量 = Math.max(1, 目标列表.length);
      const { 压力 } = 计算行为规划资源压力(sustainCost, actor, 覆盖目标数量);
      const 目标类型列表 = (Array.isArray(effectList) ? effectList : []).map(effect => 推断战斗效果目标类型(effect, inferSkillPrimaryTargetKind(skill || {})));
      const 范围系数 = 目标类型列表.some(类型 => 类型 === '全场')
        ? 0.12
        : 目标类型列表.some(类型 => String(类型 || '').includes('群体'))
          ? 0.08
          : 0.03;
      const 威力倍率 = Math.max(...(Array.isArray(effectList) ? effectList : []).map(effect => Number(effect?.威力倍率 || 0)), 0);
      const 威力系数 = 限制行为概率(威力倍率 / 300, 0, 1) * 0.12;
      const 效果文本 = JSON.stringify(effectList || []);
      const 控制系数 = /眩晕|沉默|封技|麻痹|混乱|僵直|失控|skip_turn|cannot_react|silence|skill_seal/.test(效果文本)
        ? 0.12
        : /迟缓|致盲|缴械|虚弱|紊乱|dodge_penalty|reaction_penalty|random_target_rate|lock_level/.test(效果文本)
          ? 0.06
          : 0;
      return 限制行为概率(压力 * 0.35 + 范围系数 + 威力系数 + 控制系数, 0.03, 0.45);
    }

    function 估算行为规划维持存在收益(effect = {}, actor = {}, target = {}, behaviorState = {}, skill = null) {
      const 原型 = String(effect?.原型 || '').trim();
      if (原型 === '召唤生成') return Math.max(0, 估算召唤生成释放权重(effect, actor, target, behaviorState)) * 0.28;
      if (原型 === '规则防御') return Math.max(0, 估算规则防御释放权重(effect, target || actor, behaviorState, skill)) * 0.34;
      if (原型 === '状态转移') return Math.max(0, 估算状态转移释放权重(effect, actor, target, behaviorState)) * 0.24;
      if (原型 === '状态交换') return Math.max(0, 估算状态交换释放权重(effect, actor, target, behaviorState)) * 0.24;
      if (原型 === '机制授予') return Math.max(0, 估算机制授予规划权重(effect, actor, target, behaviorState, skill)) * 0.32;
      if (原型 === '复制执行') return Math.max(0, 估算复制执行释放权重(effect, actor, target, behaviorState)) * 0.22;
      if (原型 === '位移执行') return Math.max(0, 估算位移执行释放权重(effect, actor, target, behaviorState)) * 0.2;
      return 0;
    }

    function 估算行为规划技能维持收益(skill = {}, context = {}, effectList = [], behaviorState = {}) {
      const 消耗模式 = 拆分行为规划消耗模式(getSkillCostText(skill));
      if (!消耗模式.含维持) return { 净收益: 0, 维持释放数量: 0, 维持存在数量: 0, 维持负荷: 0 };
      const actor = context.actor || context.caster || {};
      const primaryTarget = context.primaryTarget || context.target || behaviorState?.primaryTarget || behaviorState?.target || null;
      const 分组 = (Array.isArray(effectList) ? effectList : []).reduce((结果, effect) => {
        const 状态 = 读取行为规划维持释放原型状态(effect);
        if (状态 === '维持释放') 结果.维持释放效果列表.push(effect);
        else if (状态 === '维持存在') 结果.维持存在效果列表.push(effect);
        return 结果;
      }, { 维持释放效果列表: [], 维持存在效果列表: [] });
      if (!分组.维持释放效果列表.length && !分组.维持存在效果列表.length) return { 净收益: -18, 维持释放数量: 0, 维持存在数量: 0, 维持负荷: 0 };
      const 覆盖目标数量 = Math.max(
        1,
        dedupeCombatTargetList(
          分组.维持释放效果列表
            .flatMap(effect => 解析效果规划目标列表(effect, actor, primaryTarget, behaviorState, skill))
            .filter(Boolean),
        ).length,
      );
      const { 压力: 维持消耗压力, 消耗: 维持消耗 } = 计算行为规划资源压力(消耗模式.维持消耗 || '无', actor, 覆盖目标数量);
      const 维持负荷 = 估算行为规划维持负荷(skill, 分组.维持释放效果列表, 消耗模式.维持消耗, actor, behaviorState);
      const 来袭 = behaviorState?.threatProfile || {};
      const 中断风险 = 限制行为概率(
        维持消耗.canCast === false ? 0.82 : 维持消耗压力 * 0.58 + 维持负荷 * 0.7 + (来袭.severeControl ? 0.18 : 0),
        0,
        0.88,
      );
      const 后续回合 = Math.max(0.35, Math.min(3, 2.4 - 中断风险 * 1.45));
      const 维持释放收益 = 分组.维持释放效果列表.reduce((总和, effect) => {
        const 单项 = 评估效果规划净收益(effect, { ...context, behaviorState, skill });
        return 总和 + Number(单项.净收益 || 0);
      }, 0);
      const 维持存在收益 = 分组.维持存在效果列表.reduce((总和, effect) => {
        return 总和 + 估算行为规划维持存在收益(effect, actor, primaryTarget, behaviorState, skill);
      }, 0);
      const 净收益 = Math.round(
        Math.max(0, 维持释放收益) * 后续回合 * 0.55 +
          维持存在收益 -
          维持消耗压力 * 88 * 后续回合 -
          维持负荷 * 112,
      );
      return {
        净收益,
        维持释放数量: 分组.维持释放效果列表.length,
        维持存在数量: 分组.维持存在效果列表.length,
        维持负荷,
        维持消耗压力,
        后续回合,
      };
    }

    function 结算修正规划对目标有益(effect = {}) {
      const 结算 = String(effect?.结算 || '').trim();
      const 数值 = 读取战斗数值正负(effect?.数值);
      if (['造成伤害', '治疗', '伤害吸收', '伤害转治疗', '反伤', '反击', '伤害转移', '伤害分摊', '消耗分摊', '技能效果'].includes(结算)) return true;
      if (['受到伤害', '治疗转伤害', '持续伤害引爆', '防御剥夺', '精神抗性剥夺'].includes(结算)) return false;
      if (结算 === '消耗' || 结算 === '前摇') return 数值 < 0;
      return true;
    }

    function 规则改写规划对目标有益(effect = {}) {
      const 规则 = String(effect?.规则 || '').trim();
      if (规则 === '死亡转存活' || 规则 === '伤害转治疗') return true;
      if (规则 === '缴械' || 规则 === '治疗转伤害') return false;
      return false;
    }

    function 判定修正规划对目标有益(effect = {}) {
      return 读取战斗数值正负(effect?.数值 || '10%') >= 0;
    }

    function 估算判定修正选项规划权重(effect = {}, actor = {}, target = {}, behaviorState = {}, skill = null) {
      const 判定 = String(effect?.判定 || '命中').trim();
      const 数值 = Math.abs(读取战斗数值正负(effect?.数值 || '10%')) || 0.1;
      const 目标快照 = buildConditionTacticalSnapshot(target || {});
      const 来袭 = behaviorState?.threatProfile || {};
      const 目标威胁 = Math.min(54, 计算目标威胁分(target || {}) / 80);
      const 持续系数 = Math.min(1.8, 1 + Math.max(0, Number(effect?.持续回合 || 0)) * 0.16);
      let 权重 = 0;
      if (判定 === '命中') {
        权重 = 24 + 数值 * 120;
        if (读取行为规划单位立场(target, actor, behaviorState) === '敌方') {
          if (target?.蓄力技能 || behaviorState?.isChargingHighThreat) 权重 += 22;
          if (目标快照.hasStealthed || 目标快照.hasReactiveDefense) 权重 += 18;
          if (getCombatHpRatio(target) < 0.38) 权重 += 18;
        } else if (skill && isBattleSkillDefensiveProfile(skill)) 权重 += 10;
      } else if (判定 === '闪避') {
        权重 = 22 + 数值 * 110;
        if (来袭.targetHitsDefender || 来袭.incomingAttackIntent) 权重 += 来袭.lethalRisk ? 48 : 来袭.severeDamage ? 34 : 18;
        if (getCombatHpRatio(target || actor) < 0.55) 权重 += 16;
        if (target?.__本回合闪避成功次数) 权重 -= Math.min(26, Number(target.__本回合闪避成功次数 || 0) * 12);
      } else if (判定 === '反应') {
        权重 = 28 + 数值 * 130;
        if (effect?.打断效果 === true || 读取战斗数值正负(effect?.数值 || 0) < 0) {
          if (target?.蓄力技能 || behaviorState?.isChargingHighThreat || 来袭.incomingAttackIntent) 权重 += 42;
          if (目标快照.isLockedOrControlled) 权重 -= 18;
        } else if (来袭.severeControl || 来袭.lethalRisk || 来袭.severeDamage) 权重 += 36;
      }
      return Math.round(Math.max(0, Math.min(150, (权重 + 目标威胁 * 0.35) * 持续系数)));
    }

    function 时窗修正规划对目标有益(effect = {}, target = {}) {
      const 调整方式 = String(effect?.调整方式 || '延长').trim() || '延长';
      const 快照 = buildConditionTacticalSnapshot(target || {});
      const 有负面 = 快照.debuffCount > 0 || 快照.hasBadCondition || 快照.hasDotPressure || 快照.isLockedOrControlled;
      const 有增益 = 快照.buffCount > 0 || 快照.hasShielded || 快照.hasHealingTrend || 快照.hasReactiveDefense;
      if (调整方式 === '延长') return 有增益 || !有负面;
      return 有负面 && !有增益;
    }

    function 估算时窗修正选项规划权重(effect = {}, actor = {}, target = {}, behaviorState = {}) {
      const 调整字段 = String(effect?.调整字段 || '持续回合').trim() || '持续回合';
      const 调整方式 = String(effect?.调整方式 || '延长').trim() || '延长';
      const 快照 = buildConditionTacticalSnapshot(target || {});
      const 是友方 = 读取行为规划单位立场(target, actor, behaviorState) === '己方';
      const 调整数 = Math.max(1, Number(effect?.调整回合 || effect?.调整tick || effect?.调整次数 || 1));
      let 基础 = 16 + Math.min(36, 调整数 * 10);
      if (调整字段 === '持续回合' || 调整字段 === '持续tick') {
        const 有持续伤害 = 快照.hasDotPressure;
        const 有控制 = 快照.isLockedOrControlled;
        const 有增益 = 快照.buffCount > 0 || 快照.hasShielded || 快照.hasHealingTrend || 快照.hasReactiveDefense;
        const 有负面 = 快照.debuffCount > 0 || 快照.hasBadCondition || 有持续伤害 || 有控制;
        if (调整方式 === '延长') {
          if (是友方 && 有增益) 基础 += 44;
          else if (!是友方 && 有负面) 基础 += 48;
          else 基础 -= 18;
        } else {
          if (是友方 && 有负面) 基础 += 50;
          else if (!是友方 && 有增益) 基础 += 46;
          else 基础 -= 16;
          if (effect?.结算倍率 && 有持续伤害 && !是友方) 基础 += 30;
        }
      } else if (调整字段 === '有效期tick') {
        基础 += 调整方式 === '延长' ? (是友方 ? 22 : 10) : (是友方 ? 8 : 22);
      } else if (['触发次数', '使用次数', '可用次数'].includes(调整字段)) {
        const 有次数资源 =
          Object.values(target?.__技能限制运行态 || {}).some(限制态 => Number(限制态?.可用次数 ?? 限制态?.次数 ?? 0) > 0) ||
          快照.hasReactiveDefense ||
          快照.hasShielded;
        if (有次数资源) 基础 += 调整方式 === '延长' ? (是友方 ? 34 : 12) : (是友方 ? 10 : 34);
        else 基础 -= 12;
      }
      if (behaviorState?.isChargingHighThreat && 调整方式 === '压缩' && !是友方) 基础 += 16;
      return Math.round(Math.max(0, Math.min(135, 基础)));
    }

    function 计算换招特殊原型惩罚(skill = {}, actor = {}, target = {}, behaviorState = {}, threatProfile = {}) {
      const 效果列表 = getSkillEffects(skill, { 行为规划: true, actor, caster: actor, target, defender: target, skill });
      const 原型集合 = new Set(效果列表.map(effect => String(effect?.原型 || '').trim()).filter(Boolean));
      let 惩罚 = 0;
      const 高威胁窗口 = threatProfile.lethalRisk === true || threatProfile.severeDamage === true || threatProfile.severeControl === true || Number(threatProfile.projectedDamageRatio || 0) >= 0.28;
      if (原型集合.has('修炼增益')) 惩罚 += 100;
      if (原型集合.has('召唤生成')) 惩罚 += 高威胁窗口 ? 24 : 46;
      if (原型集合.has('炸环')) 惩罚 += 高威胁窗口 || getCombatHpRatio(actor) < 0.42 ? 18 : 74;
      if (效果列表.some(effect => String(effect?.原型 || '').trim() === '时光回溯' && 解析时光回溯发动方式(effect) !== '主动')) {
        惩罚 += 高威胁窗口 ? 18 : 62;
      }
      return 惩罚;
    }

    function 评估效果对单位规划收益(effect = {}, actor = {}, target = {}, behaviorState = {}, skill = null) {
      const 原型 = String(effect?.原型 || '').trim();
      const 数值 = 读取战斗数值正负(effect?.数值);
      const 正向 = 数值 >= 0;
      const consumer = String(effect?.运行时消费器 || 读取战斗原型运行消费器(effect)).trim();
      const 目标立场 = 读取行为规划单位立场(target, actor, behaviorState);
      const 友方 = 目标立场 === '己方';
      let 原始收益 = 0;
      if (原型 === '伤害结算') {
        const 威力倍率 = Math.max(1, Number(effect?.威力倍率 || 100));
        const 生命比例 = getCombatHpValue(target) / Math.max(1, getCombatHpMaxValue(target));
        原始收益 = Math.min(160, 威力倍率 * 0.35 + (生命比例 < 0.35 ? 42 : 0));
        if (战斗伤害是真实攻击(effect?.伤害类型)) 原始收益 *= 1.18;
        return 友方 ? -原始收益 : 原始收益;
      }
      if (原型 === '资源变化') {
        const 资源键 = 读取行为规划资源键(effect);
        const 当前 = 读取行为规划资源当前值(target, 资源键);
        const 上限 = 读取行为规划资源上限(target, 资源键);
        const 变动 = 计算行为规划数值变动(effect?.数值, 上限);
        const 缺口系数 = 资源键 === 'hp' ? 1 + (1 - 当前 / 上限) * 1.2 : 0.75 + (1 - 当前 / 上限) * 0.85;
        原始收益 = Math.min(150, Math.abs(变动) / 上限 * 110 * 缺口系数);
        const 对目标有益 = 变动 >= 0;
        return (友方 === 对目标有益) ? 原始收益 : -原始收益;
      }
      if (原型 === '资源转移') {
        const 资源键 = 读取行为规划资源键(effect);
        const 当前 = 读取行为规划资源当前值(target, 资源键);
        const 上限 = 读取行为规划资源上限(target, 资源键);
        const 变动 = Math.abs(计算行为规划数值变动(effect?.数值, 上限));
        const 实际量 = Math.min(当前, Math.max(0, 变动));
        const 方式 = String(effect?.资源转移方式 || '').trim();
        const 缺口 = Math.max(0, 1 - 当前 / 上限);
        const 生命击杀系数 = 资源键 === 'hp' && 当前 / 上限 < 0.35 ? 1.35 : 1;
        if (方式 === '共享') {
          const 自身当前 = 读取行为规划资源当前值(actor, 资源键);
          const 自身上限 = 读取行为规划资源上限(actor, 资源键);
          const 转化比例 = Math.max(0, Math.min(2, Number(effect?.转化比例 ?? 1) || 1));
          const 自身可付出 = Math.min(自身当前, 变动);
          const 目标可获得 = Math.min(Math.max(0, 上限 - 当前), 自身可付出 * 转化比例);
          const 目标收益 = Math.min(150, 目标可获得 / 上限 * 115 * (0.8 + 缺口 * 1.4));
          const 自身代价 = Math.min(90, 自身可付出 / Math.max(1, 自身上限) * 70);
          return (友方 ? 目标收益 : -目标收益) - 自身代价;
        }
        原始收益 = Math.min(150, 实际量 / 上限 * 120 * 生命击杀系数);
        if (方式 === '吞噬') {
          const 自身当前 = 读取行为规划资源当前值(actor, 资源键);
          const 自身上限 = 读取行为规划资源上限(actor, 资源键);
          const 自身缺口 = Math.max(0, 自身上限 - 自身当前);
          const 转化比例 = Math.max(0, Math.min(2, Number(effect?.转化比例 ?? 1) || 1));
          原始收益 += Math.min(80, Math.min(自身缺口, 实际量 * 转化比例) / Math.max(1, 自身上限) * 95);
        }
        return 友方 ? -原始收益 : 原始收益;
      }
      if (原型 === '护盾变化') {
        if (数值 >= 0) {
          const 变动 = 计算行为规划数值变动(effect?.数值, getCombatHpMaxValue(target));
          const 生命比例 = getCombatHpValue(target) / Math.max(1, getCombatHpMaxValue(target));
          原始收益 = Math.min(130, Math.max(0, 变动) / Math.max(1, getCombatHpMaxValue(target)) * 100 * (1.2 + (1 - 生命比例)));
          return 友方 ? 原始收益 : -原始收益;
        }
        const 当前护盾 = 读取当前护盾总量(target);
        const 削减 = 读取战斗数值是否百分比(effect?.数值) || Math.abs(数值) <= 1 ? 当前护盾 * Math.abs(数值 || 1) : Math.abs(数值);
        原始收益 = Math.min(120, Math.max(0, Math.min(当前护盾, 削减)) / Math.max(1, getCombatHpMaxValue(target)) * 140);
        return 友方 ? -原始收益 : 原始收益;
      }
      if (原型 === '属性修正') {
        const 属性键列表 = 读取行为规划属性键列表(effect);
        原始收益 = 属性键列表.reduce((总和, 属性键) => {
          const 当前 = 读取行为规划属性值(target, 属性键);
          const 变动 = 读取战斗数值是否百分比(effect?.数值) ? 当前 * 数值 : 数值;
          return 总和 + Math.min(90, Math.abs(变动) / Math.max(1, 当前) * 100 * 读取行为规划属性权重(属性键));
        }, 0);
        const 对目标有益 = 数值 >= 0;
        return (友方 === 对目标有益) ? 原始收益 : -原始收益;
      }
      if (原型 === '状态移除') {
        原始收益 = 估算状态移除释放权重(effect, actor, target, { 目标立场: 友方 ? '友方' : '敌方' });
        return 原始收益;
      }
      if (原型 === '状态转移') {
        return 估算状态转移释放权重(effect, actor, target, behaviorState);
      }
      if (原型 === '状态交换') {
        return 估算状态交换释放权重(effect, actor, target, behaviorState);
      }
      if (原型 === '召唤生成') {
        return 估算召唤生成释放权重(effect, actor, target, behaviorState);
      }
      if (原型 === '规则防御') {
        if (!友方) return -估算规则防御释放权重(effect, target, behaviorState, skill);
        原始收益 = 估算规则防御释放权重(effect, target, behaviorState, skill);
        return 原始收益;
      }
      if (原型 === '位移执行') {
        return 估算位移执行释放权重(effect, actor, target, behaviorState);
      }
      if (原型 === '复制执行') {
        return 估算复制执行释放权重(effect, actor, target, behaviorState);
      }
      if (原型 === '炸环') {
        return 估算炸环释放权重(effect, actor, target, behaviorState, skill);
      }
      if (原型 === '时光回溯') {
        return 估算时光回溯释放权重(effect, actor, target, behaviorState, skill);
      }
      if (原型 === '修炼增益') {
        return 0;
      }
      if (原型 === '状态施加') {
        const 元素承伤修正 = 构建状态元素承伤修正(effect);
        const 元素承伤价值 = Object.values(元素承伤修正 || {}).reduce((总和, 倍率) => {
          const 修正倍率 = Number(倍率 || 1);
          if (!Number.isFinite(修正倍率) || 修正倍率 <= 1) return 总和;
          return 总和 + Math.min(70, (修正倍率 - 1) * 90);
        }, 0);
        const 持续系数 = Math.min(2.2, 1 + Math.max(0, Number(effect?.持续回合 || 0)) * 0.18);
        const 威胁系数 = 友方 ? 1 : Math.min(1.35, 1 + 计算目标威胁分(target) / 1200);
        const 状态价值 = 估算行为规划状态价值(effect) + Math.round(元素承伤价值 * 持续系数 * 威胁系数);
        const 状态类型 = String(effect?.类型 || effect?.状态类型 || '').trim();
        const 计算层效果 = effect?.计算层效果 || {};
        const 对目标有益 =
          状态类型 === 'buff' ||
          计算层效果.invincible === true ||
          计算层效果.super_armor === true ||
          Number(计算层效果.final_damage_mult || 1) > 1 ||
          Number(计算层效果.shield_gain_bonus || 0) > 0;
        const 明确负面 =
          状态类型 === 'debuff' ||
          计算层效果.skip_turn === true ||
          计算层效果.cannot_react === true ||
          计算层效果.silence === true ||
          计算层效果.disarm === true ||
          计算层效果.blind === true ||
          Number(计算层效果.dot_damage || 0) > 0 ||
          Number(计算层效果.reaction_penalty || 0) > 0 ||
          Number(计算层效果.dodge_penalty || 0) > 0;
        const beneficial = 对目标有益 && !明确负面;
        return (友方 === beneficial) ? 状态价值 : -状态价值;
      }
      if (原型 === '结算修正') {
        原始收益 = 估算结算修正选项规划权重(effect, actor, target, behaviorState, skill);
        return (友方 === 结算修正规划对目标有益(effect)) ? 原始收益 : -原始收益;
      }
      if (原型 === '机制授予') {
        原始收益 = 估算机制授予规划权重(effect, actor, target, behaviorState, skill);
        return 友方 ? 原始收益 : -原始收益;
      }
      if (原型 === '机制抹消') {
        原始收益 = 估算机制抹消规划权重(effect, actor, target, behaviorState);
        return 友方 ? -原始收益 : 原始收益;
      }
      if (原型 === '规则改写') {
        原始收益 = 估算规则改写规划权重(effect, actor, target, behaviorState);
        return (友方 === 规则改写规划对目标有益(effect)) ? 原始收益 : -原始收益;
      }
      if (原型 === '资源锁定') {
        原始收益 = 估算资源锁定规划权重(effect, actor, target, behaviorState);
        return 友方 ? -原始收益 : 原始收益;
      }
      if (原型 === '决策干扰') {
        原始收益 = Math.max(18, Math.min(92, Math.abs(数值 || 0.35) * 100));
        return 友方 ? -原始收益 : 原始收益;
      }
      if (原型 === '判定修正') {
        原始收益 = 估算判定修正选项规划权重(effect, actor, target, behaviorState, skill);
        return (友方 === 判定修正规划对目标有益(effect)) ? 原始收益 : -原始收益;
      }
      if (原型 === '时窗修正') {
        原始收益 = 估算时窗修正选项规划权重(effect, actor, target, behaviorState);
        return (友方 === 时窗修正规划对目标有益(effect, target)) ? 原始收益 : -原始收益;
      }
      return 0;
    }

    function 评估效果规划净收益(effect = {}, context = {}) {
      const actor = context.actor || context.caster || {};
      const primaryTarget = context.primaryTarget || context.target || null;
      const behaviorState = context.behaviorState || {};
      const skill = context.skill || null;
      const 目标列表 = 解析效果规划目标列表(effect, actor, primaryTarget, behaviorState, skill);
      if (!目标列表.length) return { 净收益: 0, 目标数量: 0, 弱参与: true };
      const 时间系数 = 计算行为规划时间系数(effect);
      if (String(effect?.原型 || '').trim() === '资源转移' && String(effect?.资源转移方式 || '').trim() === '均分') {
        const 资源键 = 读取行为规划资源键(effect);
        const 均分目标列表 = dedupeCombatTargetList(String(effect?.目标 || '').trim() === '单体' ? [actor, ...目标列表] : 目标列表).filter(isCombatUnitAlive);
        if (均分目标列表.length < 2) return { 净收益: 0, 目标数量: 均分目标列表.length, 弱参与: true };
        const 当前总量 = 均分目标列表.reduce((总和, unit) => 总和 + 读取行为规划资源当前值(unit, 资源键), 0);
        const 平均值 = 当前总量 / 均分目标列表.length;
        const 强度 = Math.max(0, Math.min(1, Math.abs(读取战斗数值正负(effect?.数值 || '100%')) || 1));
        const 净收益 = 均分目标列表.reduce((总和, unit) => {
          const 当前 = 读取行为规划资源当前值(unit, 资源键);
          const 上限 = 读取行为规划资源上限(unit, 资源键);
          const 变化量 = (平均值 - 当前) * 强度;
          const 立场 = 读取行为规划单位立场(unit, actor, behaviorState);
          const 价值 = Math.min(120, Math.abs(变化量) / Math.max(1, 上限) * (资源键 === 'hp' ? 135 : 95));
          const 对单位有益 = 变化量 > 0;
          return 总和 + ((立场 === '己方') === 对单位有益 ? 价值 : -价值);
        }, 0) * 时间系数;
        return {
          净收益: Math.round(净收益),
          目标数量: 均分目标列表.length,
          弱参与: 净收益 === 0,
        };
      }
      const 单体收益列表 = 目标列表.map(target => 评估效果对单位规划收益(effect, actor, target, behaviorState, skill) * 时间系数);
      let 净收益 = 单体收益列表.reduce((总和, value) => 总和 + value, 0);
      const 目标类型 = 推断战斗效果目标类型(effect, inferSkillPrimaryTargetKind(skill || {}));
      if ((目标类型 === '全场' || String(目标类型 || '').includes('群体')) && 目标列表.length > 1) {
        const 正收益 = 单体收益列表.filter(value => value > 0).sort((a, b) => b - a);
        const 负收益 = 单体收益列表.filter(value => value < 0).reduce((总和, value) => 总和 + value, 0);
        const 递减正收益 = 正收益.reduce((总和, value, index) => 总和 + value * Math.max(0.24, Math.pow(0.72, index)), 0);
        净收益 = 递减正收益 + 负收益;
      }
      if (String(effect?.原型 || '').trim() === '规则防御' && 目标列表.length > 1) {
        const 正收益 = 单体收益列表.filter(value => value > 0).sort((a, b) => b - a);
        const 封顶系数 = 1 + Math.min(正收益.length - 1, 3) * 0.35;
        净收益 = 正收益.length ? 正收益[0] * 封顶系数 : Math.min(0, 净收益);
      }
      return {
        净收益: Math.round(净收益),
        目标数量: 目标列表.length,
        弱参与: 净收益 === 0,
      };
    }

    function 评估技能规划净收益(skill = {}, context = {}) {
      const 规划目标 = context.primaryTarget || context.target || null;
      const 规划行为状态 = { ...(context.behaviorState || {}), primaryTarget: 规划目标, target: 规划目标 };
      const 规划上下文 =
        context.规划上下文 ||
        (context.actor
          ? 构建规划上下文(context.actor, 规划目标, 规划行为状态.combatData || context.combatData || {}, 规划行为状态)
          : null);
      if (规划上下文) {
        规划行为状态.战局画像 = 规划上下文.战局画像;
        规划行为状态.目标优先表 = 规划上下文.目标优先表;
        规划行为状态.战略意图 = 规划上下文.战略意图;
        规划行为状态.规划上下文 = 规划上下文;
      }
      const 效果列表 = getSkillEffects(skill, { 行为规划: true, actor: context.actor, caster: context.actor, target: 规划目标 });
      const 汇总 = 效果列表.reduce((结果, effect) => {
        const 单项 = 评估效果规划净收益(effect, { ...context, behaviorState: 规划行为状态, skill });
        结果.净收益 += Number(单项.净收益 || 0);
        结果.目标数量 += Number(单项.目标数量 || 0);
        if (单项.弱参与) 结果.弱参与数量 += 1;
        return 结果;
      }, { 净收益: 0, 目标数量: 0, 弱参与数量: 0 });
      const 维持收益 = 估算行为规划技能维持收益(skill, context, 效果列表, 规划行为状态);
      汇总.维持收益 = 维持收益;
      汇总.净收益 += Number(维持收益.净收益 || 0);
      const 既有维持负荷 = Math.max(0, Number(context.actor?.__维持负荷 || 0));
      if (既有维持负荷 > 0) {
        const 前摇 = Math.max(0, Number(skill?.前摇 ?? getSkillCastTime(skill) ?? 0));
        汇总.既有维持负荷 = 既有维持负荷;
        汇总.净收益 -= Math.round(既有维持负荷 * (46 + Math.min(36, 前摇 * 1.2)));
      }
      if (规划上下文) {
        const 战术前净收益 = Number(汇总.净收益 || 0);
        const 有基础参与 =
          效果列表.length > 0 &&
          (
            Math.abs(Number(汇总.净收益 || 0) - Number(维持收益.净收益 || 0)) > 0 ||
            Number(维持收益.净收益 || 0) !== 0 ||
            (
              Number(汇总.目标数量 || 0) > 0 &&
              Number(汇总.弱参与数量 || 0) < 效果列表.length
            )
          );
        let 战术修正 = 有基础参与
          ? 计算技能战术修正(skill, context.actor || {}, 规划目标, 规划上下文, 汇总)
          : { 修正值: 0, 轨迹: [] };
        if (战术前净收益 <= 0 && Number(战术修正.修正值 || 0) > 0) {
          const 原修正 = Number(战术修正.修正值 || 0);
          const 保留修正 = Math.min(原修正, Math.max(0, Math.floor(Math.abs(战术前净收益) * 0.35)));
          战术修正 = {
            修正值: 保留修正,
            轨迹: [...(战术修正.轨迹 || []), `敌我净收益门 ${原修正}->${保留修正}`],
          };
        }
        汇总.战局画像 = {
          我方核心: 读取规划单位名称(规划上下文.战局画像?.我方核心),
          敌方核心: 读取规划单位名称(规划上下文.战局画像?.敌方核心),
          治疗目标: 读取规划单位名称(规划上下文.战局画像?.治疗目标),
          控制目标: 读取规划单位名称(规划上下文.战局画像?.控制目标),
          集火目标: 读取规划单位名称(规划上下文.目标优先表?.集火目标),
          保护目标: 读取规划单位名称(规划上下文.目标优先表?.保护目标),
          资源压力: 规划上下文.资源压力,
          终局压力: 规划上下文.战局画像?.终局压力,
          召唤压力: 规划上下文.战局画像?.召唤压力,
          维持压力: 规划上下文.战局画像?.维持压力,
          行为多样性压力: 规划上下文.战局画像?.行为多样性压力,
        };
        汇总.战略意图 = 规划上下文.战略意图;
        汇总.目标理由 = 战术修正.轨迹;
        汇总.战术修正 = 战术修正.修正值;
        汇总.净收益 += 战术修正.修正值;
      }
      汇总.净收益 = Math.round(汇总.净收益);
      return 汇总;
    }

    function removeNegativeConditionsByCleanse(targetChar, maxCount = 1) {
      if (!targetChar?.状态效果) return [];
      const removed = [];
      const scoreCondition = (cond = {}) => {
        const ce = cond?.战斗效果 || {};
        return (
          (ce.skip_turn ? 1000 : 0) +
          (ce.cannot_react ? 800 : 0) +
          Number(ce.lock_level || 0) * 100 +
          Number(ce.dot_damage || 0) / 10 +
          Number(cond?.duration || 0)
        );
      };
      const removable = Object.entries(targetChar.状态效果)
        .filter(
          ([key, cond]) => cond?.类型 === 'debuff' && !String(key || '').startsWith(AUTO_PROJECTED_CONDITION_PREFIX),
        )
        .sort((a, b) => scoreCondition(b[1]) - scoreCondition(a[1]));
      removable.slice(0, Math.max(1, Number(maxCount || 1))).forEach(([key]) => {
        delete targetChar.状态效果[key];
        removed.push(key);
        if (targetChar.持续效果) {
          Object.keys(targetChar.持续效果).forEach(sustainKey => {
            if (targetChar.持续效果[sustainKey]?.related_condition === key)
              delete targetChar.持续效果[sustainKey];
          });
        }
      });
      if (removed.length > 0) targetChar.final = buildCombatFinalStats(targetChar);
      return removed;
    }

    function removePositiveConditionsByDispel(targetChar, maxCount = 1) {
      if (!targetChar?.状态效果) return [];
      const removed = [];
      const scoreCondition = (cond = {}) => {
        const ce = cond?.战斗效果 || {};
        return (
          Number(cond?.shield_value || 0) +
          Number(ce.death_save_count || 0) * 800 +
          Number(ce.block_count || 0) * 300 +
          Number(ce.damage_reduction || 0) * 500 +
          Math.max(0, Number(ce.final_damage_mult || 1) - 1) * 300 +
          Math.max(0, Number(ce.received_damage_mult || 1) - 1) * 220 +
          Math.max(0, Number(ce.final_heal_mult || 1) - 1) * 260 +
          Math.max(0, Number(ce.shield_gain_mult || 1) - 1) * 220 +
          Number(cond?.duration || 0)
        );
      };
      const removable = Object.entries(targetChar.状态效果)
        .filter(
          ([key, cond]) => cond?.类型 === 'buff' && !String(key || '').startsWith(AUTO_PROJECTED_CONDITION_PREFIX),
        )
        .sort((a, b) => scoreCondition(b[1]) - scoreCondition(a[1]));
      removable.slice(0, Math.max(1, Number(maxCount || 1))).forEach(([key]) => {
        delete targetChar.状态效果[key];
        removed.push(key);
        if (targetChar.持续效果) {
          Object.keys(targetChar.持续效果).forEach(sustainKey => {
            if (targetChar.持续效果[sustainKey]?.related_condition === key)
              delete targetChar.持续效果[sustainKey];
          });
        }
      });
      if (removed.length > 0) targetChar.final = buildCombatFinalStats(targetChar);
      return removed;
    }

    function 读取状态移除数量(value = 1) {
      const text = String(value ?? '').trim();
      if (text === '全部') return Infinity;
      const count = Math.max(1, Math.round(Number(text || 1)));
      return Number.isFinite(count) ? count : 1;
    }

    function 状态条目匹配移除原型(cond = {}, effect = {}) {
      const 匹配原型 = String(effect?.匹配原型 || '').trim();
      if (!匹配原型 || 匹配原型 === '无') return true;
      const 资源 = String(effect?.资源 || '').trim();
      const 数值方向 = String(effect?.数值方向 || '任意').trim() || '任意';
      const ce = cond?.战斗效果 || {};
      if (匹配原型 === '资源变化' && 资源 === '生命') {
        const 是持续伤害 = 战斗效果含结构化持续伤害(cond);
        const 是持续恢复 = Number(ce.hot_heal_ratio || 0) > 0 || /持续恢复|恢复|治疗|愈合|回春/.test(`${cond?.状态 || ''} ${cond?.状态名称 || ''} ${cond?.描述 || ''}`);
        if (数值方向 === '负向') return 是持续伤害;
        if (数值方向 === '正向') return 是持续恢复;
        return 是持续伤害 || 是持续恢复;
      }
      if (匹配原型 === '护盾变化') return Number(cond?.shield_value || 0) > 0 || Number(ce.shield_gain_bonus || 0) > 0;
      return false;
    }

    function 读取状态移除驱动成功率(effect = {}, attacker = {}, targetChar = {}) {
      if (!String(effect?.驱动属性 || '').trim() || String(effect?.影响方向 || '').trim() !== '成功率') return 1;
      if (!attacker || !targetChar || attacker === targetChar) return 1;
      const 缩放系数 = 计算原型驱动缩放系数(
        effect,
        attacker,
        attacker?.final || buildCombatFinalStats(attacker),
        targetChar,
        targetChar?.final || buildCombatFinalStats(targetChar),
      );
      return Math.max(0.05, Math.min(1, Number.isFinite(缩放系数) ? 缩放系数 : 1));
    }

    function 计算状态施加成功率审计(effect = {}, attacker = {}, targetChar = {}) {
      const 驱动属性 = String(effect?.驱动属性 || '').trim();
      const 构建审计 = (成功率, 来源, 扩展 = {}) => ({
        successRate: Math.max(0, Math.min(1, Number.isFinite(Number(成功率)) ? Number(成功率) : 1)),
        reason: String(来源 || '').trim(),
        ...扩展,
      });
      if (!attacker || !targetChar) return 构建审计(1, '来源字段不足');
      if (attacker === targetChar) return 构建审计(1, '自身默认生效');
      const 目标最终属性 = targetChar?.final || buildCombatFinalStats(targetChar);
      const 状态目标 = String(effect?.目标 || '').trim();
      const 状态文本 = [
        effect?.类型,
        effect?.状态,
        effect?.状态名称,
        effect?.效果描述,
        effect?.计算层效果?.skip_turn === true ? '眩晕' : '',
        effect?.计算层效果?.cannot_react === true ? '无法反应' : '',
        effect?.计算层效果?.silence === true ? '沉默' : '',
        effect?.计算层效果?.skill_seal === true ? '封技' : '',
        Number(effect?.计算层效果?.random_target_rate || 0) > 0 ? '混乱' : '',
      ].map(值 => String(值 || '').trim()).join('|');
      const 是负面状态 = !['自身', '友方', '友方单体', '友方群体', '召唤物', '分身'].includes(状态目标) &&
        (/debuff|眩晕|麻痹|僵直|混乱|沉默|封技|失控|精神紊乱|恐惧|无法反应|致盲|锁定|位移限制|资源燃烧|虚弱|防御剥夺|精神抗性剥夺/.test(状态文本));
      if (!是负面状态) return 构建审计(1, '非负面默认生效');
      const 计算层效果 = effect?.计算层效果 || {};
      if (
        effect?.必中 === true ||
        effect?.必定命中 === true ||
        /必定|必中|无法抵抗/.test(String(effect?.生效方式 || '') + String(effect?.触发限制 || '')) ||
        计算层效果.status_guaranteed === true ||
        计算层效果.ignore_status_resist === true
      ) return 构建审计(1, '必中/无法抵抗');
      const 防守最终属性 = 是负面状态 && 判断精神力驱动攻击_战斗(effect, effect?.__skill || {})
        ? { ...目标最终属性, men_max: 计算紫极魔瞳防守精神攻势值_战斗(targetChar, 目标最终属性, effect, effect?.__skill || {}) }
        : 目标最终属性;
      if (!驱动属性 || 驱动属性 === '无' || String(effect?.影响方向 || '').trim() !== '成功率') {
        const 是硬控 = /眩晕|麻痹|僵直|混乱|沉默|封技|失控|无法反应|锁定|位移限制/.test(状态文本) ||
          计算层效果.skip_turn === true ||
          计算层效果.cannot_react === true ||
          计算层效果.silence === true ||
          计算层效果.skill_seal === true ||
          Number(计算层效果.lock_level || 0) > 0;
        const 攻方压制 = Math.max(
          Number(attacker?.final?.men_max || attacker?.men_max || attacker?.sp_max || attacker?.sp || 0),
          Number(attacker?.final?.sp_max || attacker?.sp_max || attacker?.sp || 0),
        );
        const 守方抗性 = Math.max(
          Number(防守最终属性?.men_max || targetChar?.men_max || 0),
          Number(防守最终属性?.sp_max || targetChar?.sp_max || 0),
          Number(防守最终属性?.def || targetChar?.def || 0) * 8,
        );
        const 属性差 = 攻方压制 > 0 && 守方抗性 > 0
          ? Math.max(-0.18, Math.min(0.16, (攻方压制 - 守方抗性) / Math.max(攻方压制, 守方抗性, 1) * 0.18))
          : 0;
        const 基础率 = 是硬控 ? 0.78 : 0.88;
        const 成功率 = Math.max(0.55, Math.min(0.95, 基础率 + 属性差));
        const 属性差百分比 = Math.round(属性差 * 100);
        return 构建审计(
          成功率,
          `${是硬控 ? '硬控' : '负面状态'}基础${Math.round(基础率 * 100)}%${属性差百分比 >= 0 ? ' + ' : ' - '}属性差${Math.abs(属性差百分比)}%（攻方压制${Math.round(攻方压制)} / 守方抗性${Math.round(守方抗性)}）`,
          { baseRate: 基础率, attributeDelta: 属性差, attackerPressure: 攻方压制, defenderResistance: 守方抗性 },
        );
      }
      const 缩放系数 = 计算原型驱动缩放系数(
        effect,
        attacker,
        attacker?.final || buildCombatFinalStats(attacker),
        targetChar,
        防守最终属性,
      );
      return 构建审计(
        Math.max(0.05, Math.min(1, Number.isFinite(缩放系数) ? 缩放系数 : 1)),
        `驱动属性${驱动属性}缩放${Number.isFinite(缩放系数) ? 缩放系数.toFixed(2) : '1.00'}`,
        { driverAttr: 驱动属性, driverScale: Number.isFinite(缩放系数) ? 缩放系数 : 1 },
      );
    }

    function 读取状态施加驱动成功率(effect = {}, attacker = {}, targetChar = {}) {
      return 计算状态施加成功率审计(effect, attacker, targetChar).successRate;
    }

    function 格式化状态附着成功率拆分(状态成功率审计 = {}, 状态附着检定 = null, 附着通过 = true) {
      const successRate = Number(状态成功率审计?.successRate);
      if (!Number.isFinite(successRate)) return '附着成功率：缺少 successRate 数值，无法生成计算式';
      const ratePct = Math.round(successRate <= 1 ? successRate * 100 : successRate);
      const source = String(状态成功率审计?.reason || '').trim() || '来源字段不足';
      const roll = Number(状态附着检定);
      const rollPart = Number.isFinite(roll) && roll > 0
        ? `，检定${Math.round(roll <= 1 ? roll * 100 : roll)} ${附着通过 ? '<=' : '>'} ${ratePct}${附着通过 ? '，通过' : '，未通过'}`
        : '';
      return `附着成功率：${ratePct}%，${source}${rollPart}`;
    }

    function 读取状态移除候选状态(targetChar, effect = {}) {
      if (!targetChar?.状态效果) return [];
      const 状态 = String(effect?.状态 || '').trim();
      const typeMatches = (key, cond = {}) => {
        if (!状态 || 状态 === '任意状态') return true;
        if (状态 === '任意负面') return String(cond?.类型 || '') === 'debuff';
        if (状态 === '任意增益') return String(cond?.类型 || '') === 'buff';
        return key === 状态 || String(cond?.状态 || '').trim() === 状态;
      };
      return Object.entries(targetChar.状态效果)
        .filter(([key]) => !String(key || '').startsWith(AUTO_PROJECTED_CONDITION_PREFIX))
        .filter(([key, cond]) => typeMatches(key, cond))
        .filter(([, cond]) => 状态条目匹配移除原型(cond, effect));
    }

    function 读取状态移除状态价值(key = '', cond = {}) {
      const 名称 = String(key || cond?.状态 || '').trim();
      const 战斗效果 = cond?.战斗效果 || {};
      if (/眩晕|麻痹|僵直|混乱|沉默|封技/.test(名称)) return 90;
      if (/缴械|致盲|位移限制|迟缓/.test(名称)) return 65;
      if (/禁疗|治疗反转/.test(名称)) return 60;
      if (/隐匿|隐身|潜行/.test(名称) || Number(战斗效果.stealth_level || 0) > 0) return 72;
      if (/中毒|流血|灼烧|冻伤|持续创伤|资源燃烧/.test(名称) || 战斗效果含结构化持续伤害(cond))
        return 45 + Math.min(18, Math.max(0, Number(cond?.duration || cond?.持续回合 || 0)) * 3);
      if (
        /无敌|霸体|护盾|真身/.test(名称) ||
        战斗效果.invincible === true ||
        战斗效果.super_armor === true ||
        Number(cond?.shield_value || 0) > 0
      )
        return 85;
      return 25;
    }

    function 估算状态移除释放权重(effect = {}, caster = {}, targetChar = {}, 选项 = {}) {
      const 候选 = 读取状态移除候选状态(targetChar, effect).filter(([, cond]) => {
        const 类型 = String(cond?.类型 || '').trim();
        if (选项.目标立场 === '敌方') return 类型 === 'buff';
        return 类型 === 'debuff';
      });
      if (!候选.length) return 0;
      const 数量 = 读取状态移除数量(effect?.数量 ?? 1);
      const 命中数量 = Number.isFinite(数量) ? Math.min(候选.length, 数量) : 候选.length;
      if (命中数量 <= 0) return 0;
      const 排序 = [...候选].sort((左, 右) => 读取状态移除状态价值(右[0], 右[1]) - 读取状态移除状态价值(左[0], 左[1]));
      const 状态价值 = 读取状态移除状态价值(排序[0][0], 排序[0][1]);
      const 目标价值 = 选项.目标立场 === '敌方'
        ? (buildConditionTacticalSnapshot(targetChar).hasReactiveDefense ? 1.25 : 1)
        : (buildConditionTacticalSnapshot(targetChar).isLockedOrControlled ? 1.28 : 1);
      const 数量系数 = 1 + Math.min(命中数量 - 1, 3) * 0.35;
      const 驱动成功率 = 读取状态移除驱动成功率(effect, caster, targetChar);
      let 时机系数 = 1;
      if (选项.高威胁窗口 && 选项.目标立场 !== '敌方') 时机系数 += 0.22;
      if (选项.久攻不下 && 选项.目标立场 === '敌方') 时机系数 += 0.18;
      return Math.max(0, Math.round(状态价值 * 目标价值 * 数量系数 * 驱动成功率 * 时机系数));
    }

    function 估算状态移除目标列表释放权重(effect = {}, caster = {}, targetList = [], 选项 = {}) {
      const 权重列表 = dedupeCombatTargetList(Array.isArray(targetList) ? targetList : [targetList])
        .filter(Boolean)
        .map(targetChar => 估算状态移除释放权重(effect, caster, targetChar, 选项))
        .filter(value => value > 0)
        .sort((a, b) => b - a);
      const 折损 = [1, 0.65, 0.45, 0.25];
      const 总分 = 权重列表.slice(0, 折损.length).reduce((sum, value, index) => sum + value * 折损[index], 0);
      return Math.max(0, Math.round(Math.min(260, 总分)));
    }

    function 估算状态交换释放权重(effect = {}, actor = {}, targetChar = {}, behaviorState = {}) {
      if (!actor || !targetChar) return 0;
      if (读取行为规划单位立场(targetChar, actor, behaviorState) !== '敌方') return -40;
      const 状态筛选 = String(effect?.状态 || '任意负面').trim() || '任意负面';
      const 自身候选 = 读取状态移除候选状态(actor, { 状态: 状态筛选 })
        .filter(([key, cond]) => String(cond?.类型 || '').trim() === 'debuff' && 状态转移条目可转移(key, cond));
      const 目标候选 = 读取状态移除候选状态(targetChar, { 状态: '任意增益' })
        .filter(([key, cond]) => String(cond?.类型 || '').trim() === 'buff' && 状态转移条目可转移(key, cond));
      if (!自身候选.length || !目标候选.length) return 0;
      const 自身价值 = Math.max(...自身候选.map(([key, cond]) => 读取状态移除状态价值(key, cond)));
      const 目标价值 = Math.max(...目标候选.map(([key, cond]) => 读取状态移除状态价值(key, cond)));
      const 目标威胁系数 = Math.min(1.35, 1 + 计算目标威胁分(targetChar) / 1000);
      const 自身压力系数 = buildConditionTacticalSnapshot(actor).isLockedOrControlled ? 1.2 : 1;
      return Math.round((自身价值 + 目标价值) * 目标威胁系数 * 自身压力系数);
    }

    function 状态转移端点有效(端点 = '') {
      return ['自身', '目标', '友方', '敌方'].includes(String(端点 || '').trim());
    }

    function 读取状态转移端点候选列表(端点 = '', actor = {}, primaryTarget = {}, behaviorState = {}, 展开目标侧 = false) {
      const 文本 = String(端点 || '').trim();
      const 场上单位 = 读取行为规划场上单位集合(actor, { ...behaviorState, primaryTarget });
      if (文本 === '自身') return [actor].filter(Boolean);
      if (文本 === '目标') {
        if (展开目标侧) {
          const 目标标识 = primaryTarget?.name || primaryTarget?.名称 || primaryTarget;
          if (场上单位.己方.some(unit => isCombatUnitIdentityMatch(unit, 目标标识))) return 场上单位.己方 || [];
          if (场上单位.敌方.some(unit => isCombatUnitIdentityMatch(unit, 目标标识))) return 场上单位.敌方 || [];
        }
        return [primaryTarget].filter(Boolean);
      }
      if (文本 === '友方') return 场上单位.己方 || [];
      if (文本 === '敌方') return 场上单位.敌方 || [];
      return [];
    }

    function 状态转移条目可转移(key = '', cond = {}) {
      const 文本 = `${String(key || '')} ${String(cond?.状态 || '')} ${String(cond?.状态名称 || '')} ${String(cond?.描述 || '')}`;
      if (/护盾|屏障|结界|领域|场地|召唤|真身|炸环|免死|复活|回溯/.test(文本)) return false;
      const 战斗效果 = cond?.战斗效果 || {};
      return !(
        Number(战斗效果.death_save_count || 0) > 0 ||
        Number(战斗效果.revive_count || 0) > 0 ||
        String(cond?.特殊机制标识 || '').includes('时光回溯') ||
        战斗效果.invincible === true
      );
    }

    function 估算状态转移释放权重(effect = {}, actor = {}, targetChar = {}, behaviorState = {}) {
      if (!actor || !targetChar) return 0;
      const 来源 = String(effect?.来源 || '').trim();
      const 去向 = String(effect?.去向 || '').trim();
      if (!状态转移端点有效(来源) || !状态转移端点有效(去向)) return 0;
      const 展开目标侧 = 来源 === 去向 && 来源 === '目标';
      const 来源候选列表 = 读取状态转移端点候选列表(来源, actor, targetChar, behaviorState, 展开目标侧)
        .filter(unit => 读取状态移除候选状态(unit, effect).some(([key, cond]) => 状态转移条目可转移(key, cond)));
      const 去向候选列表 = 读取状态转移端点候选列表(去向, actor, targetChar, behaviorState, 展开目标侧);
      const 来源角色 = 来源候选列表[0] || null;
      const 去向角色 = 去向候选列表.find(unit => unit && 来源角色 && !isCombatUnitIdentityMatch(unit, 来源角色.name || 来源角色)) || null;
      if (!来源角色 || !去向角色 || 来源角色 === 去向角色) return 0;
      const 候选 = 读取状态移除候选状态(来源角色, effect)
        .filter(([key, cond]) => 状态转移条目可转移(key, cond));
      if (!候选.length) return 0;
      const 数量 = 读取状态移除数量(effect?.数量 ?? 1);
      const 命中数量 = Number.isFinite(数量) ? Math.min(候选.length, 数量) : 候选.length;
      if (命中数量 <= 0) return 0;
      const 排序 = [...候选].sort((左, 右) => 读取状态移除状态价值(右[0], 右[1]) - 读取状态移除状态价值(左[0], 左[1]));
      const 主状态 = 排序[0] || [];
      const 状态价值 = 读取状态移除状态价值(主状态[0], 主状态[1]);
      const 类型 = String主状态 => String(String主状态?.类型 || '').trim();
      const 主类型 = 类型(主状态[1]);
      const 来源立场 = 读取行为规划单位立场(来源角色, actor, behaviorState);
      const 去向立场 = 读取行为规划单位立场(去向角色, actor, behaviorState);
      let 收益方向 = 0;
      if (来源 === '目标' && 去向 === '目标' && 来源立场 === 去向立场) {
        if (主类型 === 'debuff') 收益方向 = 来源立场 === '敌方' ? 0.55 : -1;
        else if (主类型 === 'buff') 收益方向 = 来源立场 === '敌方' ? 0 : 0.35;
      } else if (来源立场 === '己方') {
        if (主类型 === 'debuff') 收益方向 = 去向立场 === '敌方' ? 1 : -1;
        else if (主类型 === 'buff') 收益方向 = 去向立场 === '敌方' ? -1 : 0.45;
      } else {
        if (主类型 === 'buff') 收益方向 = 来源立场 === '敌方' ? 1 : -1;
        else if (主类型 === 'debuff') 收益方向 = 来源立场 === '敌方' ? -1 : 0.45;
      }
      if (!收益方向) return 0;
      const 数量系数 = 1 + Math.min(命中数量 - 1, 3) * 0.35;
      const 来源快照 = buildConditionTacticalSnapshot(来源角色);
      const 来源价值 = 来源立场 === '己方'
        ? (来源快照.isLockedOrControlled || 来源快照.hasBadCondition ? 1.22 : 1)
        : (来源快照.hasReactiveDefense || 来源快照.buffCount > 1 ? 1.2 : 1);
      return Math.round(状态价值 * 数量系数 * 来源价值 * 收益方向);
    }

    function removeConditionsByPrototypeFilter(targetChar, effect = {}, attacker = null, options = {}) {
      if (!targetChar?.状态效果) return [];
      const maxCount = 读取状态移除数量(effect?.数量 ?? 1);
      const chance = 读取状态移除驱动成功率(effect, attacker, targetChar);
      const removed = [];
      let 驱动失败数 = 0;
      const 排除状态键集合 = new Set((options.排除状态键列表 || []).map(key => String(key || '').trim()).filter(Boolean));
      const candidates = 读取状态移除候选状态(targetChar, effect);
      for (const [key] of candidates) {
        if (排除状态键集合.has(String(key || '').trim())) continue;
        if (removed.length >= maxCount) break;
        if (chance < 1 && Math.random() > chance) {
          驱动失败数 += 1;
          continue;
        }
        delete targetChar.状态效果[key];
        if (targetChar.持续效果) {
          Object.keys(targetChar.持续效果).forEach(sustainKey => {
            if (targetChar.持续效果[sustainKey]?.related_condition === key) delete targetChar.持续效果[sustainKey];
          });
        }
        removed.push(key);
      }
      if (removed.length > 0) targetChar.final = buildCombatFinalStats(targetChar);
      if (驱动失败数 > 0) removed.__概率失败数 = 驱动失败数;
      return removed;
    }

    function countConditionsByPrototypeFilter(targetChar, effect = {}) {
      return 读取状态移除候选状态(targetChar, effect).length;
    }

    function 移除持续原型状态条目(char, key = '') {
      if (!char?.状态效果 || !char.状态效果[key]) return null;
      const snapshot = deepClonePlain(char.状态效果[key]);
      delete char.状态效果[key];
      if (char.持续效果) {
        Object.keys(char.持续效果).forEach(sustainKey => {
          if (char.持续效果[sustainKey]?.related_condition === key) delete char.持续效果[sustainKey];
        });
      }
      if (char.召唤键) 同步召唤单位镜像(char);
      return snapshot;
    }

    function 写入持续原型唯一状态条目(char, baseKey = '状态', cond = {}) {
      if (!char) return '';
      if (!char.状态效果 || typeof char.状态效果 !== 'object') char.状态效果 = {};
      let key = String(baseKey || '状态').trim() || '状态';
      if (!char.状态效果[key]) {
        char.状态效果[key] = cond;
        if (char.召唤键) 同步召唤单位镜像(char);
        return key;
      }
      let index = 1;
      while (char.状态效果[`${key}·${index}`]) index += 1;
      key = `${key}·${index}`;
      char.状态效果[key] = cond;
      if (char.召唤键) 同步召唤单位镜像(char);
      return key;
    }

    function 读取持续原型战斗单位列表(combatData = null) {
      const 战斗数据 = combatData || getCurrentBattleContextSnapshot() || {};
      return dedupeCombatTargetList([
        ...((战斗数据?.参战者?.team_player || []).filter(Boolean)),
        ...((战斗数据?.参战者?.team_enemy || []).filter(Boolean)),
      ]).filter(Boolean);
    }

    function 查找持续原型战斗单位(combatData = null, rawName = '') {
      const 名称 = String(rawName || '').trim();
      if (!名称) return null;
      return 读取持续原型战斗单位列表(combatData).find(unit => isCombatUnitIdentityMatch(unit, 名称)) || null;
    }

    function 读取持续原型资源键列表(资源 = '') {
      const 文本 = (Array.isArray(资源) ? 资源 : String(资源 || '').split(/[、,，/|｜；;\s]+/g))
        .map(item => String(item || '').trim())
        .filter(Boolean)
        .join('、');
      const keys = [];
      if (/生命|HP|hp/i.test(文本)) keys.push('hp');
      if (/体力|vit|sta/i.test(文本)) keys.push('vit');
      if (/魂力|sp/i.test(文本) || /双|混合|全部/.test(文本)) keys.push('sp');
      if (/精神|men/i.test(文本) || /双|混合|全部/.test(文本)) keys.push('men');
      return keys.length ? Array.from(new Set(keys)) : ['sp'];
    }

    function 读取持续原型资源当前值(char, 资源键 = 'sp') {
      if (资源键 === 'hp') return getCombatHpValue(char);
      if (资源键 === 'vit') return getCombatStaminaValue(char);
      return Math.max(0, Number(char?.[资源键] ?? char?.属性?.[资源键 === 'sp' ? '魂力' : '精神力'] ?? 0));
    }

    function 读取持续原型资源上限值(char, 资源键 = 'sp') {
      if (资源键 === 'hp') return getCombatHpMaxValue(char);
      if (资源键 === 'vit') return getCombatStaminaMaxValue(char);
      return Math.max(1, Number(char?.[资源键 === 'sp' ? 'sp_max' : 'men_max'] ?? char?.属性?.[资源键 === 'sp' ? '魂力上限' : '精神力上限'] ?? 1));
    }

    function 设置持续原型资源当前值(char, 资源键 = 'sp', value = 0) {
      const nextValue = Math.max(0, Number(value) || 0);
      if (资源键 === 'hp') return 设置战斗血量值(char, nextValue);
      if (资源键 === 'vit') return 设置战斗体力值(char, nextValue);
      char[资源键] = nextValue;
      if (char?.属性 && typeof char.属性 === 'object') char.属性[资源键 === 'sp' ? '魂力' : '精神力'] = nextValue;
    }

    function 计算持续原型资源变化量(rawValue, maxValue = 0) {
      const text = String(rawValue ?? '').trim();
      const parsed = 读取战斗数值正负(text);
      if (!Number.isFinite(parsed) || parsed === 0) return 0;
      return /%$/.test(text) || Math.abs(parsed) <= 1
        ? Math.max(1, Math.floor(Math.max(1, Number(maxValue || 0)) * Math.abs(parsed)))
        : Math.floor(Math.abs(parsed));
    }

    function 读取持续原型单位立场(unit = {}, caster = {}, combatData = null) {
      if (!unit || !caster) return '未知';
      if (unit === caster || isCombatUnitIdentityMatch(unit, caster?.name || caster?.名称 || caster)) return '己方';
      const 战斗数据 = combatData || getCurrentBattleContextSnapshot() || {};
      const 玩家侧 = (战斗数据?.参战者?.team_player || []).filter(Boolean);
      const 敌方侧 = (战斗数据?.参战者?.team_enemy || []).filter(Boolean);
      const casterName = caster?.name || caster?.名称 || caster;
      const casterInPlayer = 玩家侧.some(item => item === caster || isCombatUnitIdentityMatch(item, casterName));
      const unitName = unit?.name || unit?.名称 || unit;
      if (casterInPlayer) return 玩家侧.some(item => item === unit || isCombatUnitIdentityMatch(item, unitName)) ? '己方' : '敌方';
      return 敌方侧.some(item => item === unit || isCombatUnitIdentityMatch(item, unitName)) ? '己方' : '敌方';
    }

    function 读取持续状态转移端点候选(端点 = '', caster = {}, target = {}, combatData = null, 展开目标侧 = false) {
      const 文本 = String(端点 || '').trim();
      const 战斗数据 = combatData || getCurrentBattleContextSnapshot() || {};
      const 玩家侧 = (战斗数据?.参战者?.team_player || []).filter(Boolean);
      const 敌方侧 = (战斗数据?.参战者?.team_enemy || []).filter(Boolean);
      const casterName = caster?.name || caster?.名称 || caster;
      const casterInPlayer = 玩家侧.some(item => item === caster || isCombatUnitIdentityMatch(item, casterName));
      if (文本 === '自身') return [caster].filter(Boolean);
      if (文本 === '目标') {
        if (展开目标侧) {
          const targetName = target?.name || target?.名称 || target;
          if (玩家侧.some(item => item === target || isCombatUnitIdentityMatch(item, targetName))) return 玩家侧;
          if (敌方侧.some(item => item === target || isCombatUnitIdentityMatch(item, targetName))) return 敌方侧;
        }
        return [target].filter(Boolean);
      }
      if (文本 === '友方') return casterInPlayer ? 玩家侧 : 敌方侧;
      if (文本 === '敌方') return casterInPlayer ? 敌方侧 : 玩家侧;
      return [];
    }

    function 选择持续原型可转移状态(char, effect = {}, preferredTypes = ['any'], expectedState = '') {
      if (!char?.状态效果) return null;
      const stateText = String(expectedState || '').trim();
      for (const expectedType of preferredTypes) {
        const candidates = 读取状态移除候选状态(char, effect)
          .filter(([key, cond]) => 状态转移条目可转移(key, cond))
          .filter(([, cond]) => expectedType === 'any' || String(cond?.类型 || '').trim() === expectedType)
          .filter(([key, cond]) => {
            if (!stateText || ['任意状态', '任意增益', '任意负面'].includes(stateText)) return true;
            return `${String(key || '')} ${String(cond?.状态 || '')} ${String(cond?.状态名称 || '')} ${String(cond?.描述 || '')}`.includes(stateText);
          })
          .sort((a, b) => 读取状态移除状态价值(b[0], b[1]) - 读取状态移除状态价值(a[0], a[1]));
        if (candidates.length) return { key: candidates[0][0], cond: candidates[0][1] };
      }
      return null;
    }

    function 执行持续资源转移回合尾(char, cond = {}, effect = {}, label = '', combatData = null) {
      const 转移方式 = String(effect?.资源转移方式 || '').trim();
          if (!['吞噬', '共享', '均分', '转移'].includes(转移方式)) return '';
      const caster = 查找持续原型战斗单位(combatData, cond.来源角色) || char;
      const resourceKeys = 读取持续原型资源键列表(effect?.资源 || '');
      const convertRatio = Math.max(0, Math.min(2, Number(effect?.转化比例 ?? 1) || 1));
      const logs = [];
      if (转移方式 === '均分') {
        const targets = (Array.isArray(cond.持续目标列表) ? cond.持续目标列表 : [])
          .map(name => 查找持续原型战斗单位(combatData, name))
          .filter(isCombatUnitAlive);
        const units = dedupeCombatTargetList(targets.length ? targets : [char, caster].filter(Boolean)).filter(isCombatUnitAlive);
        if (units.length < 2) return '';
        const strength = Math.max(0, Math.min(1, Math.abs(读取战斗数值正负(effect?.数值 || '100%')) || 1));
        resourceKeys.forEach(resourceKey => {
          const total = units.reduce((sum, unit) => sum + 读取持续原型资源当前值(unit, resourceKey), 0);
          const average = total / units.length;
          units.forEach(unit => {
            const before = 读取持续原型资源当前值(unit, resourceKey);
            const next = Math.max(0, Math.min(读取持续原型资源上限值(unit, resourceKey), Math.round(before + (average - before) * strength)));
            const delta = next - before;
            if (!delta || 读取资源变化抹消规则(unit, 读取战斗资源键标签(resourceKey))) return;
            设置持续原型资源当前值(unit, resourceKey, next);
            logs.push(`${unit === caster ? '自身' : unit?.name || '目标'}${读取战斗资源键标签(resourceKey)}${delta > 0 ? '+' : ''}${delta}`);
          });
        });
      } else {
        resourceKeys.forEach(resourceKey => {
          const maxValue = 读取持续原型资源上限值(char, resourceKey);
          const amount = Math.min(读取持续原型资源当前值(char, resourceKey), 计算持续原型资源变化量(effect?.数值, maxValue));
          if (!(amount > 0)) return;
          if (转移方式 === '共享') {
            if (!caster || caster === char) return;
            const casterCurrent = 读取持续原型资源当前值(caster, resourceKey);
            const paid = Math.min(casterCurrent, amount);
            if (!(paid > 0) || 读取资源变化抹消规则(caster, 读取战斗资源键标签(resourceKey)) || 读取资源变化抹消规则(char, 读取战斗资源键标签(resourceKey))) return;
            const gain = Math.max(1, Math.floor(paid * convertRatio));
            设置持续原型资源当前值(caster, resourceKey, casterCurrent - paid);
            设置持续原型资源当前值(char, resourceKey, Math.min(maxValue, 读取持续原型资源当前值(char, resourceKey) + gain));
            logs.push(`自身共享${paid}点${读取战斗资源键标签(resourceKey)}给${label}`);
            return;
          }
          if (读取资源变化抹消规则(char, 读取战斗资源键标签(resourceKey))) return;
          设置持续原型资源当前值(char, resourceKey, 读取持续原型资源当前值(char, resourceKey) - amount);
          logs.push(`${label}损失${amount}点${读取战斗资源键标签(resourceKey)}`);
          if (转移方式 === '吞噬' && caster) {
            const recover = Math.max(0, Math.floor(amount * convertRatio));
            if (recover > 0 && !读取资源变化抹消规则(caster, 读取战斗资源键标签(resourceKey))) {
              设置持续原型资源当前值(caster, resourceKey, Math.min(读取持续原型资源上限值(caster, resourceKey), 读取持续原型资源当前值(caster, resourceKey) + recover));
              logs.push(`自身回补${recover}点${读取战斗资源键标签(resourceKey)}`);
            }
          }
        });
      }
      return logs.length ? `[持续资源转移] ${logs.join('，')}。` : '';
    }

    function 执行持续状态转移回合尾(char, cond = {}, effect = {}, label = '', combatData = null) {
      const caster = 查找持续原型战斗单位(combatData, cond.来源角色) || char;
      const target = 查找持续原型战斗单位(combatData, cond.目标角色) || char;
      const 来源 = String(effect?.来源 || '自身').trim();
      const 去向 = String(effect?.去向 || '目标').trim();
      if (!['自身', '目标', '友方', '敌方'].includes(来源) || !['自身', '目标', '友方', '敌方'].includes(去向)) return '';
      const count = String(effect?.数量 || '').trim() === '全部' ? 99 : Math.max(1, Math.floor(Number(effect?.数量 || 1)) || 1);
      const logs = [];
      const 展开目标侧 = 来源 === '目标' && 去向 === '目标';
      for (let index = 0; index < count; index += 1) {
        let moved = false;
        const sources = 读取持续状态转移端点候选(来源, caster, target, combatData, 展开目标侧);
        const receivers = 读取持续状态转移端点候选(去向, caster, target, combatData, 展开目标侧);
        if (展开目标侧 && dedupeCombatTargetList([...sources, ...receivers]).length < 2) return '';
        for (const sourceChar of sources) {
          for (const receiver of receivers) {
            if (!sourceChar || !receiver || sourceChar === receiver || isCombatUnitIdentityMatch(receiver, sourceChar?.name || sourceChar?.名称 || sourceChar)) continue;
            const sourceSide = 读取持续原型单位立场(sourceChar, caster, combatData);
            const receiverSide = 读取持续原型单位立场(receiver, caster, combatData);
            const preferredTypes = sourceSide === '己方'
              ? (receiverSide === '敌方' ? ['debuff', 'buff'] : ['debuff'])
              : (receiverSide === '己方' ? ['buff', 'debuff'] : ['buff']);
            const candidate = 选择持续原型可转移状态(sourceChar, effect, preferredTypes, effect?.状态 || '任意状态');
            if (!candidate) continue;
            const snapshot = 移除持续原型状态条目(sourceChar, candidate.key);
            if (!snapshot) continue;
            snapshot.描述 = `由[${cond.来源技能 || '持续效果'}]持续转移`;
            const nextKey = 写入持续原型唯一状态条目(receiver, candidate.key, snapshot);
            logs.push(`${sourceChar === caster ? '自身' : sourceChar.name || '来源'}的[${candidate.key}]转移到${receiver === caster ? '自身' : receiver.name || '目标'}为[${nextKey}]`);
            moved = true;
            break;
          }
          if (moved) break;
        }
        if (!moved) break;
      }
      return logs.length ? `[持续状态转移] ${logs.join('；')}。` : '';
    }

    function 执行持续状态交换回合尾(char, cond = {}, effect = {}, label = '', combatData = null) {
      const caster = 查找持续原型战斗单位(combatData, cond.来源角色);
      const target = 查找持续原型战斗单位(combatData, cond.目标角色) || char;
      if (!caster || !target || caster === target) return '';
      const ownCandidate = 选择持续原型可转移状态(caster, { 状态: effect?.状态 || '任意负面' }, ['debuff'], effect?.状态 || '任意负面');
      const targetCandidate = 选择持续原型可转移状态(target, { 状态: '任意增益' }, ['buff'], '任意增益');
      if (!ownCandidate || !targetCandidate) return '';
      const ownSnapshot = 移除持续原型状态条目(caster, ownCandidate.key);
      const targetSnapshot = 移除持续原型状态条目(target, targetCandidate.key);
      if (!ownSnapshot || !targetSnapshot) return '';
      ownSnapshot.描述 = `由[${cond.来源技能 || '持续效果'}]持续交换至${target.name || '目标'}`;
      targetSnapshot.描述 = `由[${cond.来源技能 || '持续效果'}]持续交换至${caster.name || '自身'}`;
      const ownNewKey = 写入持续原型唯一状态条目(target, ownCandidate.key, ownSnapshot);
      const targetNewKey = 写入持续原型唯一状态条目(caster, targetCandidate.key, targetSnapshot);
      return `[持续状态交换] 自身的[${ownCandidate.key}]与${target.name || label}的[${targetCandidate.key}]交换为[${targetNewKey}]/[${ownNewKey}]。`;
    }

    function 执行持续原型回合尾效果(char, key = '', cond = {}, label = '', combatData = null) {
      const effect = cond?.持续原型效果;
      if (!effect || typeof effect !== 'object') return '';
      const 原型 = String(effect?.原型 || '').trim();
      if (原型 === '资源转移') return 执行持续资源转移回合尾(char, cond, effect, label, combatData);
      if (原型 === '状态移除') {
        const caster = 查找持续原型战斗单位(combatData, cond.来源角色) || char;
        const removed = removeConditionsByPrototypeFilter(char, effect, caster, { 排除状态键列表: [key] });
        if (removed.length > 0) return `[持续状态移除] ${label}移除了[${removed.join('/')}].`;
        if (removed.__概率失败数 > 0) return `[持续状态移除] ${label}命中匹配状态，但驱动判定失败。`;
        return '';
      }
      if (原型 === '状态转移') return 执行持续状态转移回合尾(char, cond, effect, label, combatData);
      if (原型 === '状态交换') return 执行持续状态交换回合尾(char, cond, effect, label, combatData);
      return '';
    }

    function 持续状态移除阻断状态附着(targetChar, 状态名 = '', 新状态条目 = {}, attacker = null) {
      if (!targetChar?.状态效果) return null;
      const 临时状态键 = `__待附着状态_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 8)}`;
      targetChar.状态效果[临时状态键] = {
        ...(新状态条目 || {}),
        状态: 状态名,
        状态名称: 状态名,
      };
      try {
        const 命中条目 = Object.entries(targetChar.状态效果 || {}).find(([key, cond]) => {
          if (key === 临时状态键 || String(cond?.特殊机制标识 || '').trim() !== '持续状态移除') return false;
          const effect = cond?.持续原型效果;
          if (!effect || String(effect?.原型 || '').trim() !== '状态移除') return false;
          const 候选 = 读取状态移除候选状态(targetChar, effect);
          return 候选.some(([候选键]) => 候选键 === 临时状态键) && 读取状态移除驱动成功率(effect, attacker, targetChar) >= Math.random();
        });
        return 命中条目 ? { key: 命中条目[0], cond: 命中条目[1] } : null;
      } finally {
        delete targetChar.状态效果[临时状态键];
      }
    }

    function 读取战斗延迟效果资源键(资源 = '') {
      const 文本 = String(资源 || '').trim();
      if (/生命|HP|hp/i.test(文本)) return 'hp';
      if (/体力|vit|sta/i.test(文本)) return 'vit';
      if (/精神|men/i.test(文本)) return 'men';
      return 'sp';
    }

    function 读取战斗延迟效果资源上限(char, 资源键 = 'sp') {
      if (资源键 === 'hp') return getCombatHpMaxValue(char);
      if (资源键 === 'vit') return getCombatStaminaMaxValue(char);
      if (资源键 === 'men') return Math.max(1, Number(char?.men_max || char?.属性?.精神力上限 || 1));
      return Math.max(1, Number(char?.sp_max || char?.属性?.魂力上限 || 1));
    }

    function 设置战斗延迟效果资源值(char, 资源键 = 'sp', value = 0) {
      if (!char || typeof char !== 'object') return 0;
      const 上限 = 读取战斗延迟效果资源上限(char, 资源键);
      const 下值 = Math.max(0, Math.min(上限, Number(value || 0)));
      if (资源键 === 'hp') return 设置战斗血量值(char, 下值);
      if (资源键 === 'vit') return 设置战斗体力值(char, 下值);
      char[资源键] = 下值;
      const 属性 = char.属性 && typeof char.属性 === 'object' ? char.属性 : null;
      if (属性) {
        if (资源键 === 'sp') 属性.魂力 = 下值;
        if (资源键 === 'men') 属性.精神力 = 下值;
      }
      同步战斗资源镜像字段(char);
      return 下值;
    }

    function 读取延迟最终结果抹消规则(char, effect = {}) {
      const 原型 = String(effect?.原型 || '').trim();
      if (!原型 || 原型 === '伤害结算') return null;
      if (原型 === '资源变化') return null;
      if (原型 === '状态施加') {
        const 状态名 = String(effect?.状态 || '').trim();
        return 战斗机制抹消命中(char, '最终结果', { 原型, 状态: 状态名, 状态名称: 状态名 }, { 用途: '封锁' });
      }
      if (原型 === '属性修正') {
        const 属性名 = 战斗机制中文键英文名[String(effect?.属性 || '').trim()] || String(effect?.属性 || '').trim();
        return 战斗机制抹消命中(char, '最终结果', { 原型, 属性: 属性名 }, { 用途: '封锁' });
      }
      return 战斗机制抹消命中(char, '最终结果', effect, { 用途: '封锁' });
    }

    function 执行战斗延迟内部效果(char, effect = {}, label = '目标') {
      if (!char || !effect || typeof effect !== 'object') return '';
      if (!char.状态效果) char.状态效果 = {};
      const 原型 = String(effect?.原型 || '').trim();
      const 数值文本 = String(effect?.数值 ?? '').trim();
      const 数值 = 读取战斗数值正负(数值文本 || effect?.威力倍率 || 0);
      if (原型 === '伤害结算') {
        const 伤害类型 = 规范化战斗伤害类型(effect?.伤害类型);
        const 倍率 = Math.max(1, Number(effect?.威力倍率 || 100));
        const 最大生命 = getCombatHpMaxValue(char);
        const 伤害 = 伤害类型 === '真实攻击'
          ? Math.max(1, Math.floor(最大生命 * Math.min(1, 倍率 / 1000)))
          : Math.max(1, Math.floor(最大生命 * Math.min(1, 倍率 / 1800)));
        设置战斗血量值(char, getCombatHpValue(char) - 伤害);
        return `[延迟效果] ${label}受到${伤害}点${伤害类型}。`;
      }
      if (原型 === '资源变化') {
        const 资源键 = 读取战斗延迟效果资源键(effect?.资源);
        const 当前值 = 资源键 === 'hp' ? getCombatHpValue(char) : Math.max(0, Number(char?.[资源键] || 0));
        const 上限 = 读取战斗延迟效果资源上限(char, 资源键);
        const 变化 = /%$/.test(数值文本) || Math.abs(数值) <= 1
          ? Math.floor(上限 * 数值)
          : Math.floor(数值);
        const 下一值 = Math.max(0, Math.min(上限, 当前值 + 变化));
        const 实际变化 = 下一值 - 当前值;
        if (!实际变化) return '';
        const 资源变化抹消规则 = 读取资源变化抹消规则(char, 读取战斗资源键标签(资源键));
        if (资源变化抹消规则) return `[机制抹消] ${label}对【${读取战斗机制抹消对象摘要(资源变化抹消规则.抹消对象)}】存在封锁，延迟资源变化未能落地。`;
        设置战斗延迟效果资源值(char, 资源键, 下一值);
        return `[延迟效果] ${label}${实际变化 >= 0 ? '恢复' : '损失'}${Math.abs(实际变化)}点${String(effect?.资源 || '资源') || '资源'}。`;
      }
      if (原型 === '护盾变化') {
        if (数值 >= 0) {
          const 变化 = /%$/.test(数值文本) || Math.abs(数值) <= 1
            ? Math.floor(getCombatHpMaxValue(char) * Math.abs(数值))
            : Math.floor(Math.abs(数值));
          if (!(变化 > 0)) return '';
          const 最终结果抹消规则 = 读取延迟最终结果抹消规则(char, effect);
          if (最终结果抹消规则) return `[机制抹消] ${label}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，延迟${原型 || '效果'}未能落地。`;
          applyShieldToCharacter(char, 变化, Math.max(1, Number(effect?.持续回合 || 1)), '延迟护盾', { 已检查机制抹消: true });
          return `[延迟效果] ${label}获得${变化}点护盾。`;
        }
        if (!(读取当前护盾总量(char) > 0)) return '';
        const 最终结果抹消规则 = 读取延迟最终结果抹消规则(char, effect);
        if (最终结果抹消规则) return `[机制抹消] ${label}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，延迟${原型 || '效果'}未能落地。`;
        const 削减 = 斩除目标当前护盾(char, 数值文本 || effect?.数值 || '-100%');
        return `[延迟效果] ${label}被削减${削减}点护盾。`;
      }
      if (原型 === '属性修正') {
        const 属性名 = 战斗机制中文键英文名[String(effect?.属性 || '').trim()] || String(effect?.属性 || '').trim();
        if (!属性名) return '';
        if (!char.状态效果) char.状态效果 = {};
        const 修正 = 读取战斗数值正负(effect?.数值 || 0);
        if (!修正) return '';
        const 最终结果抹消规则 = 读取延迟最终结果抹消规则(char, effect);
        if (最终结果抹消规则) return `[机制抹消] ${label}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，延迟${原型 || '效果'}未能落地。`;
        const 是百分比 = 读取战斗数值是否百分比(effect?.数值);
        char.状态效果[`延迟属性:${String(effect?.属性 || 属性名)}`] = {
          类型: 修正 >= 0 ? 'buff' : 'debuff',
          层数: 1,
          描述: '延迟效果属性修正',
          来源原型摘要: '属性修正',
          属性: 属性名,
          duration: Math.max(1, Number(effect?.持续回合 || 1)),
          面板修改比例: 是百分比 ? { [属性名]: Math.max(0.1, 1 + 修正) } : {},
          面板固定修正: 是百分比 ? {} : { [属性名]: 修正 },
          战斗效果: { ...createEmptyCombatEffectMap() },
        };
        return `[延迟效果] ${label}获得${String(effect?.属性 || 属性名)}修正。`;
      }
      if (原型 === '状态施加') {
        const 状态名 = String(effect?.状态 || '').trim();
        if (!状态名) return '';
        const 最终结果抹消规则 = 读取延迟最终结果抹消规则(char, effect);
        if (最终结果抹消规则) return `[机制抹消] ${label}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，延迟${原型 || '效果'}未能落地。`;
        const 旧状态 = char.状态效果[状态名] && typeof char.状态效果[状态名] === 'object'
          ? char.状态效果[状态名]
          : null;
        const 下一持续 = Math.max(Number(effect?.持续回合 || 0), Number(旧状态?.duration || 0));
        const 延迟状态条目 = {
          类型: ['自身', '友方', '友方单体', '友方群体', '召唤物', '分身'].includes(String(effect?.目标 || '').trim()) ? 'buff' : 'debuff',
          层数: 1,
          来源原型摘要: '状态施加',
          描述: '延迟效果状态施加',
          duration: 下一持续,
          面板修改比例: { ...(effect?.面板修改比例 || {}) },
          战斗效果: {
            ...createEmptyCombatEffectMap(),
            ...(effect?.计算层效果 || {}),
          },
        };
        if (effect?.元素承伤修正 && typeof effect.元素承伤修正 === 'object' && Object.keys(effect.元素承伤修正).length) {
          延迟状态条目.元素承伤修正 = deepClonePlain(effect.元素承伤修正);
        }
        const 持续移除命中 = 持续状态移除阻断状态附着(char, 状态名, 延迟状态条目, null);
        if (持续移除命中) return `[持续状态移除] ${label}的[${状态名}]被[${持续移除命中.key}]拦截。`;
        char.状态效果[状态名] = 延迟状态条目;
        char.final = buildCombatFinalStats(char);
        return `[延迟效果] ${label}获得[${状态名}]。`;
      }
      return '';
    }

    function getCombatSoulCoreCount(char) {
      return Math.max(0, Math.floor(Number(char?.魂核?.核心?.数量 || 0)));
    }

      function settleConditionsAtRoundEnd(char, label, combatData = null) {
      if (!char) return { log: '', totalDot: 0, expired: [] };

      let totalDot = 0;
      let expired = [];
      let parts = [];
      const conditionMap =
        char.状态效果 && typeof char.状态效果 === 'object' && !Array.isArray(char.状态效果)
          ? char.状态效果
          : {};
      const 清理到期炸环恢复标记 = () => {
        const 当前tick = 读取当前世界tick_V1();
        let 清理数量 = 0;
        取角色武魂条目_战斗(char).forEach(([, 武魂数据]) => {
          取武魂魂灵条目_战斗(武魂数据).forEach(([, 魂灵数据]) => {
            取魂灵魂环条目_战斗(魂灵数据).forEach(([魂环键, 魂环数据]) => {
              if (!魂环数据 || typeof 魂环数据 !== 'object') return;
              const 恢复tick = Math.max(0, Number(魂环数据?.炸环恢复tick || 0));
              if (!(恢复tick > 0 && 恢复tick <= 当前tick)) return;
              delete 魂环数据.炸环恢复tick;
              if (Object.prototype.hasOwnProperty.call(魂环数据, '炸环恢复时间')) delete 魂环数据.炸环恢复时间;
              清理数量 += 1;
              parts.push(`[炸环恢复] ${label}第${魂环键}魂环已恢复。`);
            });
          });
          取武魂直接魂环条目_战斗(武魂数据).forEach(([魂环键, 魂环数据]) => {
            if (!魂环数据 || typeof 魂环数据 !== 'object') return;
            const 恢复tick = Math.max(0, Number(魂环数据?.炸环恢复tick || 0));
            if (!(恢复tick > 0 && 恢复tick <= 当前tick)) return;
            delete 魂环数据.炸环恢复tick;
            if (Object.prototype.hasOwnProperty.call(魂环数据, '炸环恢复时间')) delete 魂环数据.炸环恢复时间;
            清理数量 += 1;
            parts.push(`[炸环恢复] ${label}第${魂环键}魂环已恢复。`);
          });
        });
        return 清理数量;
      };
      清理到期炸环恢复标记();

        Object.keys(conditionMap).forEach(key => {
          let cond = conditionMap[key];
          if (!cond) return;
          if (cond.__本回合新附加 === true) {
            delete cond.__本回合新附加;
            return;
          }

          let combatEffects = cond.战斗效果 || {};
          const sideEffects = normalizeBattleSkillSideEffectList(cond.副作用列表 || []);
          const 状态结算名 = String(cond?.状态名称 || cond?.状态 || key || '').trim();
          const 状态来源 = 查找状态来源登记(combatData, {
            applicationId: String(cond?.__状态来源键 || '').trim(),
            stateName: 状态结算名,
            targetName: char?.name || char?.名称 || label,
            maxRound: Number(combatData?.回合 || 0),
          });
          const 状态来源文本 = 状态来源?.sourceActorName && 状态来源?.sourceActionName
            ? `（该状态由第${Number(状态来源.sourceRound || 状态来源.round || 0)}回合${状态来源.sourceActorName}施展【${状态来源.sourceActionName}】附加）`
            : '';
          let dot = 状态名禁止生命流失(状态结算名) ? 0 : 估算单位持续伤害数值(char, cond);
          let hotHealRatio = Math.max(0, Number(combatEffects.hot_heal_ratio || 0));
          if (dot > 0) {
            if (读取资源变化抹消规则(char, '生命')) {
              parts.push(`[机制抹消] ${label}的生命变化被封锁，[${key}]未能造成持续损失。`);
            } else {
              设置战斗血量值(char, getCombatHpValue(char) - dot);
              totalDot += dot;
              写入战斗事件账本(combatData, {
                eventKind: 'state_tick',
                round: Number(combatData?.回合 || 0),
                actorName: String(状态来源?.sourceActorName || '').trim(),
                targetName: char?.name || char?.名称 || label,
                actionName: String(状态来源?.sourceActionName || '').trim(),
                actionType: 'state_tick',
                sourceActionName: String(状态来源?.sourceActionName || '').trim(),
                sourceRound: Number(状态来源?.sourceRound || 状态来源?.round || 0),
                result: '损失',
                applicationId: String(状态来源?.applicationId || cond?.__状态来源键 || '').trim(),
                duration: Math.max(0, Number(cond?.duration || 状态来源?.duration || 0)),
                effectSummary: String(cond?.效果摘要 || 状态来源?.effectSummary || '').trim(),
                driverAttr: String(cond?.驱动属性 || 状态来源?.driverAttr || '').trim(),
                meta: {
                  stateName: 状态结算名,
                  amount: dot,
                  resource: '生命值',
                  sourceActorName: String(状态来源?.sourceActorName || '').trim(),
                  sourceActionName: String(状态来源?.sourceActionName || '').trim(),
                  sourceRound: Number(状态来源?.sourceRound || 状态来源?.round || 0),
                  applicationId: String(状态来源?.applicationId || cond?.__状态来源键 || '').trim(),
                },
              });
              parts.push(`[状态结算] ${label}受[${key}]影响，额外损失 ${dot} 点HP${状态来源文本}`);
            }
          }
          if (hotHealRatio > 0) {
            const maxVit = getCombatHpMaxValue(char);
            const hotHeal = Math.floor(maxVit * hotHealRatio);
            const healInvertRatio = Math.max(0, Number(combatEffects.heal_inversion_ratio || 0));
            if (healInvertRatio > 0) {
              const invertedDamage = Math.max(1, Math.floor(hotHeal * Math.max(1, healInvertRatio)));
              设置战斗血量值(char, getCombatHpValue(char) - invertedDamage);
              if (invertedDamage > 0) {
                写入战斗事件账本(combatData, {
                  eventKind: 'state_tick',
                  round: Number(combatData?.回合 || 0),
                  actorName: String(状态来源?.sourceActorName || '').trim(),
                  targetName: char?.name || char?.名称 || label,
                  actionName: String(状态来源?.sourceActionName || '').trim(),
                  actionType: 'state_tick',
                  sourceActionName: String(状态来源?.sourceActionName || '').trim(),
                  sourceRound: Number(状态来源?.sourceRound || 状态来源?.round || 0),
                  result: '损失',
                  applicationId: String(状态来源?.applicationId || cond?.__状态来源键 || '').trim(),
                  duration: Math.max(0, Number(cond?.duration || 状态来源?.duration || 0)),
                  effectSummary: String(cond?.效果摘要 || 状态来源?.effectSummary || '').trim(),
                  driverAttr: String(cond?.驱动属性 || 状态来源?.driverAttr || '').trim(),
                  meta: {
                    stateName: 状态结算名,
                    amount: invertedDamage,
                    resource: '生命值',
                    sourceActorName: String(状态来源?.sourceActorName || '').trim(),
                    sourceActionName: String(状态来源?.sourceActionName || '').trim(),
                    sourceRound: Number(状态来源?.sourceRound || 状态来源?.round || 0),
                    applicationId: String(状态来源?.applicationId || cond?.__状态来源键 || '').trim(),
                  },
                });
              }
              if (invertedDamage > 0) parts.push(`[状态结算] ${label}的[${key}]治疗被反转，反而损失 ${invertedDamage} 点HP${状态来源文本}`);
            } else {
              const 当前生命 = getCombatHpValue(char);
              const 下一生命 = Math.min(maxVit, 当前生命 + hotHeal);
              const 实际恢复 = Math.max(0, 下一生命 - 当前生命);
              if (实际恢复 > 0 && 读取资源变化抹消规则(char, '生命')) {
                parts.push(`[机制抹消] ${label}的回复回路被封锁，[${key}]未能提供恢复。`);
                return;
              }
              设置战斗血量值(char, 下一生命);
              if (实际恢复 > 0) {
                写入战斗事件账本(combatData, {
                  eventKind: 'state_tick',
                  round: Number(combatData?.回合 || 0),
                  actorName: String(状态来源?.sourceActorName || '').trim(),
                  targetName: char?.name || char?.名称 || label,
                  actionName: String(状态来源?.sourceActionName || '').trim(),
                  actionType: 'state_tick',
                  sourceActionName: String(状态来源?.sourceActionName || '').trim(),
                  sourceRound: Number(状态来源?.sourceRound || 状态来源?.round || 0),
                  result: '恢复',
                  applicationId: String(状态来源?.applicationId || cond?.__状态来源键 || '').trim(),
                  duration: Math.max(0, Number(cond?.duration || 状态来源?.duration || 0)),
                  effectSummary: String(cond?.效果摘要 || 状态来源?.effectSummary || '').trim(),
                  driverAttr: String(cond?.驱动属性 || 状态来源?.driverAttr || '').trim(),
                  meta: {
                    stateName: 状态结算名,
                    amount: 实际恢复,
                    resource: '生命值',
                    sourceActorName: String(状态来源?.sourceActorName || '').trim(),
                    sourceActionName: String(状态来源?.sourceActionName || '').trim(),
                    sourceRound: Number(状态来源?.sourceRound || 状态来源?.round || 0),
                    applicationId: String(状态来源?.applicationId || cond?.__状态来源键 || '').trim(),
                  },
                });
              }
              if (实际恢复 > 0) parts.push(`[状态结算] ${label}受[${key}]影响，额外恢复 ${实际恢复} 点HP${状态来源文本}`);
            }
          }
          const vitRecoverRatio = Math.max(0, Number(combatEffects.vit_gain_ratio || 0));
          if (vitRecoverRatio > 0) {
            const maxVit = getCombatStaminaMaxValue(char);
            const beforeVit = getCombatStaminaValue(char);
            const recoverVit = Math.max(1, Math.floor(maxVit * vitRecoverRatio));
            const nextVit = Math.min(maxVit, beforeVit + recoverVit);
            const actualRecoverVit = Math.max(0, nextVit - beforeVit);
            if (actualRecoverVit > 0) {
              if (读取资源变化抹消规则(char, '体力')) {
                parts.push(`[机制抹消] ${label}的体力变化被封锁，[${key}]未能提供恢复。`);
              } else {
                设置战斗体力值(char, nextVit);
                parts.push(`[状态结算] ${label}受[${key}]影响，恢复 ${actualRecoverVit} 点体力${状态来源文本}`);
              }
            }
          }
          const spRecoverRatio = Math.max(0, Number(combatEffects.sp_gain_ratio || 0));
          if (spRecoverRatio > 0) {
            const maxSp = Math.max(0, Number(char.sp_max || char?.属性?.魂力上限 || 0));
            const beforeSp = Math.max(0, Number(char.sp || char?.属性?.魂力 || 0));
            const recoverSp = Math.max(1, Math.floor(maxSp * spRecoverRatio));
            const nextSp = Math.min(maxSp, beforeSp + recoverSp);
            const actualRecoverSp = Math.max(0, nextSp - beforeSp);
            if (actualRecoverSp > 0) {
              if (读取资源变化抹消规则(char, '魂力')) {
                parts.push(`[机制抹消] ${label}的魂力变化被封锁，[${key}]未能提供恢复。`);
              } else {
                设置战斗延迟效果资源值(char, 'sp', nextSp);
                parts.push(`[状态结算] ${label}受[${key}]影响，恢复 ${actualRecoverSp} 点魂力${状态来源文本}`);
              }
            }
          }
          const menRecoverRatio = Math.max(0, Number(combatEffects.men_gain_ratio || 0));
          if (menRecoverRatio > 0) {
            const maxMen = Math.max(0, Number(char.men_max || char?.属性?.精神力上限 || 0));
            const beforeMen = Math.max(0, Number(char.men || char?.属性?.精神力 || 0));
            const recoverMen = Math.max(1, Math.floor(maxMen * menRecoverRatio));
            const nextMen = Math.min(maxMen, beforeMen + recoverMen);
            const actualRecoverMen = Math.max(0, nextMen - beforeMen);
            if (actualRecoverMen > 0) {
              if (读取资源变化抹消规则(char, '精神力')) {
                parts.push(`[机制抹消] ${label}的精神力变化被封锁，[${key}]未能提供恢复。`);
              } else {
                设置战斗延迟效果资源值(char, 'men', nextMen);
                parts.push(`[状态结算] ${label}受[${key}]影响，恢复 ${actualRecoverMen} 点精神力${状态来源文本}`);
              }
            }
          }
          const vitDrainRatio = Math.min(0, Number(combatEffects.vit_gain_ratio || 0));
          if (vitDrainRatio < 0) {
            const maxVit = getCombatStaminaMaxValue(char);
            const beforeVit = getCombatStaminaValue(char);
            const drainVit = Math.min(beforeVit, Math.max(1, Math.floor(maxVit * Math.min(0.03, Math.abs(vitDrainRatio)))));
            if (drainVit > 0) {
              if (读取资源变化抹消规则(char, '体力')) {
                parts.push(`[机制抹消] ${label}的体力变化被封锁，[${key}]未能造成流失。`);
              } else {
                设置战斗体力值(char, Math.max(0, beforeVit - drainVit));
                parts.push(`[状态结算] ${label}受[${key}]影响，流失 ${drainVit} 点体力${状态来源文本}`);
              }
            }
          }
          const spDrainRatio = Math.min(0, Number(combatEffects.sp_gain_ratio || 0));
          if (spDrainRatio < 0) {
            const maxSp = Math.max(0, Number(char.sp_max || char?.属性?.魂力上限 || 0));
            const beforeSp = Math.max(0, Number(char.sp || char?.属性?.魂力 || 0));
            const drainSp = Math.min(beforeSp, Math.max(1, Math.floor(maxSp * Math.min(0.03, Math.abs(spDrainRatio)))));
            if (drainSp > 0) {
              if (读取资源变化抹消规则(char, '魂力')) {
                parts.push(`[机制抹消] ${label}的魂力变化被封锁，[${key}]未能造成流失。`);
              } else {
                设置战斗延迟效果资源值(char, 'sp', Math.max(0, beforeSp - drainSp));
                parts.push(`[状态结算] ${label}受[${key}]影响，流失 ${drainSp} 点魂力${状态来源文本}`);
              }
            }
          }
          const menDrainRatio = Math.min(0, Number(combatEffects.men_gain_ratio || 0));
          if (menDrainRatio < 0) {
            const maxMen = Math.max(0, Number(char.men_max || char?.属性?.精神力上限 || 0));
            const beforeMen = Math.max(0, Number(char.men || char?.属性?.精神力 || 0));
            const drainMen = Math.min(beforeMen, Math.max(1, Math.floor(maxMen * Math.min(0.03, Math.abs(menDrainRatio)))));
            if (drainMen > 0) {
              if (读取资源变化抹消规则(char, '精神力')) {
                parts.push(`[机制抹消] ${label}的精神力变化被封锁，[${key}]未能造成流失。`);
              } else {
                设置战斗延迟效果资源值(char, 'men', Math.max(0, beforeMen - drainMen));
                parts.push(`[状态结算] ${label}受[${key}]影响，流失 ${drainMen} 点精神力${状态来源文本}`);
              }
            }
          }
          if (getCombatHpValue(char) <= 0) {
            const reviveLog = triggerReviveEffect(char, label);
            if (reviveLog) parts.push(reviveLog);
          }

          sideEffects
            .filter(item => String(item?.触发时机 || '').trim() === '回合结束时')
            .forEach(item => {
              const boundState = String(item?.关联状态 || '').trim();
              if (boundState && boundState !== key) return;
              applyBattleSideEffectState(char, item, key, parts, getCurrentBattleContextSnapshot());
            });

          const 持续原型日志 = 执行持续原型回合尾效果(char, key, cond, label, combatData);
          if (持续原型日志) parts.push(持续原型日志);

          if (typeof cond.duration === 'number') {
            cond.duration -= 1;
            if (cond.duration <= 0) expired.push(key);
          }
        });

        const 到期顺序 = [
          ...expired.filter(key => conditionMap[key]?.延迟效果 === true),
          ...expired.filter(key => conditionMap[key]?.延迟效果 !== true),
        ];
        到期顺序.forEach(key => {
          const expiredCond = conditionMap[key];
          if (expiredCond?.延迟效果 && Array.isArray(expiredCond?.结算效果)) {
            expiredCond.结算效果.forEach(effect => {
              const 延迟日志 = 执行战斗延迟内部效果(char, effect, label);
              if (延迟日志) parts.push(延迟日志);
            });
          }
          const endSideEffects = normalizeBattleSkillSideEffectList(expiredCond?.副作用列表 || []);
          endSideEffects
            .filter(item => String(item?.触发时机 || '').trim() === '效果结束后')
            .forEach(item => {
              const boundState = String(item?.关联状态 || '').trim();
              if (boundState && boundState !== key) return;
              applyBattleSideEffectState(char, item, key, parts, getCurrentBattleContextSnapshot());
            });
          delete conditionMap[key];
          if (String(char.当前领域 || '') === String(key)) {
            char.当前领域 = '无';
          }
          if (expiredCond?.召唤物 && combatData) {
            const 召唤消散日志 = 移除宿主状态召唤单位(combatData, char, key, '来源状态结束');
            if (召唤消散日志) parts.push(召唤消散日志);
          }
          if (expiredCond?.召唤物 && Array.isArray(char.召唤行动队列)) {
            char.召唤行动队列 = char.召唤行动队列.filter(行动 => String(行动?.来源状态 || '') !== key);
          }
          if (char.持续效果) {
            Object.keys(char.持续效果).forEach(sustainKey => {
              if (char.持续效果[sustainKey]?.related_condition === key) delete char.持续效果[sustainKey];
            });
          }
          parts.push(`[状态消散] ${label}的[${key}]已结束`);
        });

        const 禁用本回合自然恢复 = char.__禁用本回合自然恢复 === true;
        if (char.__禁用本回合自然恢复 !== undefined) delete char.__禁用本回合自然恢复;

        const 读取回合末资源锁定比例 = (resourceLabel, lockType) => Math.min(
          1,
          Object.values(conditionMap).reduce((maxVal, cond) => {
            const 规则列表 = Array.isArray(cond?.资源锁定规则) ? cond.资源锁定规则 : [];
            return Math.max(maxVal, 规则列表.reduce((规则最大值, 规则) => {
              const 资源命中 = Array.isArray(规则?.资源) && 规则.资源.includes(resourceLabel);
              const 类型命中 = String(规则?.锁定类型 || '').trim() === lockType;
              return 资源命中 && 类型命中 ? Math.max(规则最大值, Number(规则?.比例 || 0)) : 规则最大值;
            }, 0));
          }, 0),
        );
        const coreCount = getCombatSoulCoreCount(char);
        let naturalSpRatio = 0.005;
        let naturalMenRatio = 0.005;
        if (coreCount >= 1) naturalSpRatio += 0.01;
        if (coreCount >= 2) naturalMenRatio += 0.01;
        if (coreCount >= 3) naturalSpRatio += 0.01;

        const maxSp = Math.max(0, Number(char.sp_max || 0));
        const maxMen = Math.max(0, Number(char.men_max || 0));
        const 魂力回复锁定比例 = 读取回合末资源锁定比例('魂力', '回复锁定');
        const 精神回复锁定比例 = 读取回合末资源锁定比例('精神力', '回复锁定');
        if (!禁用本回合自然恢复 && maxSp > 0 && naturalSpRatio > 0 && 魂力回复锁定比例 < 1) {
          const beforeSp = Math.max(0, Number(char.sp || 0));
          const recoverSp = Math.max(0, Math.floor(maxSp * naturalSpRatio * (1 - 魂力回复锁定比例)));
          设置战斗延迟效果资源值(char, 'sp', Math.min(maxSp, beforeSp + recoverSp));
          const actualRecoverSp = Math.max(0, Number(char.sp || 0) - beforeSp);
          if (actualRecoverSp > 0) parts.push(`[自然恢复] ${label}回合末恢复 ${actualRecoverSp} 点魂力`);
        }
        if (!禁用本回合自然恢复 && maxMen > 0 && naturalMenRatio > 0 && 精神回复锁定比例 < 1) {
          const beforeMen = Math.max(0, Number(char.men || 0));
          const recoverMen = Math.max(0, Math.floor(maxMen * naturalMenRatio * (1 - 精神回复锁定比例)));
          设置战斗延迟效果资源值(char, 'men', Math.min(maxMen, beforeMen + recoverMen));
          const actualRecoverMen = Math.max(0, Number(char.men || 0) - beforeMen);
          if (actualRecoverMen > 0) parts.push(`[自然恢复] ${label}回合末恢复 ${actualRecoverMen} 点精神力`);
        }

        if (char.召唤键) 同步召唤单位镜像(char);
        return { log: parts.join(' '), totalDot, expired };
      }

      function splitSkillCostModes(costStr) {
        const raw = String(costStr || '无').trim();
        if (!raw || raw === '无') return { upfront: '无', sustain: '', hasSustain: false };

        const parts = raw.split(/维持[:：]?/);
        return {
          upfront: (parts[0] || '无').trim() || '无',
          sustain: (parts[1] || '').trim(),
          hasSustain: parts.length > 1,
        };
      }

      function 单位已有同名维持技能(unit = {}, skill = {}) {
        const 技能名 = String(skill?.name || skill?.魂技名 || skill?.技能名称 || '').trim();
        if (!技能名 || !splitSkillCostModes(getSkillCostText(skill)).hasSustain) return false;
        return Object.values(unit?.持续效果 || {}).some(effect => {
          const 维持技能名 = String(effect?.name || effect?.技能快照?.name || effect?.技能快照?.魂技名 || '').trim();
          return 维持技能名 === 技能名;
        });
      }

      function parseCostStringForChar(costStr, char, options = 1) {
        const 技能 = { 消耗: costStr || '无' };
        if (options && typeof options === 'object') {
          if (Number(options.覆盖目标数量 || 0) > 0) 技能.__覆盖目标数量 = Math.max(1, Math.floor(Number(options.覆盖目标数量 || 1)));
          if (Array.isArray(options.覆盖目标列表) && options.覆盖目标列表.length > 0) 技能.__批量目标列表 = options.覆盖目标列表;
          if (String(options.批量模式 || '').trim()) 技能.__批量消耗模式 = String(options.批量模式 || '').trim();
          if (Number(options.单体系数 || 0) > 0) 技能.__battleSupportCostScale = Number(options.单体系数 || 1);
          return parseSkillCostForChar(技能, char, options.条件上下文 || {});
        }
        技能.__battleSupportCostScale = Number(options || 1) || 1;
        return parseSkillCostForChar(技能, char);
      }

      function formatParsedCost(parsed) {
        if (!parsed) return '无消耗';
        const parts = [];
        if (parsed.reqSp) parts.push(`魂力:${parsed.reqSp}`);
        if (parsed.reqVit) parts.push(`体力:${parsed.reqVit}`);
        if (parsed.reqMen) parts.push(`精神力:${parsed.reqMen}`);
        return parts.length > 0 ? parts.join(' ') : '无消耗';
      }

      const 维持释放原型集合 = new Set([
        '伤害结算',
        '资源变化',
        '资源转移',
        '护盾变化',
        '属性修正',
        '判定修正',
        '状态施加',
        '时窗修正',
        '状态移除',
        '资源锁定',
        '决策干扰',
      ]);
      const 维持释放结算集合 = new Set(['造成伤害', '受到伤害', '治疗', '消耗', '前摇', '技能效果', '防御剥夺', '精神抗性剥夺', '伤害转治疗', '治疗转伤害']);
      const 维持释放规则改写集合 = new Set(['缴械']);
      const 维持存在原型集合 = new Set(['规则防御', '状态转移', '状态交换', '机制授予', '复制执行', '位移执行', '召唤生成']);

      function 效果支持维持释放(effect = {}) {
        const 原型 = String(effect?.原型 || '').trim();
        if (维持释放原型集合.has(原型)) return true;
        if (原型 === '结算修正') return 维持释放结算集合.has(String(effect?.结算 || '').trim());
        if (原型 === '规则改写') return 维持释放规则改写集合.has(String(effect?.规则 || '').trim());
        if (原型 === '机制抹消') return true;
        return false;
      }

      function 效果仅维持存在(effect = {}) {
        const 原型 = String(effect?.原型 || '').trim();
        return 维持存在原型集合.has(原型);
      }

      function 构建维持效果分组(skill = {}, context = {}) {
        const 效果列表 = getSkillEffects(skill, context).filter(effect => effect && typeof effect === 'object');
        return {
          维持释放效果列表: 效果列表.filter(效果支持维持释放).map(deepClonePlain),
          维持存在效果列表: 效果列表.filter(效果仅维持存在).map(deepClonePlain),
        };
      }

      function 读取维持效果目标列表(char = {}, effect = {}, skill = {}, combatData = null) {
        const 战斗数据 = combatData || getCurrentBattleContextSnapshot() || {};
        确保召唤单位表(战斗数据);
        const 双方 = 读取战斗双方队伍(战斗数据, char);
        const 兜底目标 = 双方.敌方[0] || 双方.友方.find(单位 => !isCombatUnitIdentityMatch(单位, char?.name || char?.名称 || char)) || char || null;
        const 目标上下文 = resolveSkillTargetContext(skill, char, 兜底目标, 战斗数据, effect);
        return dedupeCombatTargetList((目标上下文.targetSet || []).filter(Boolean)).filter(isCombatUnitAlive);
      }

      function 读取维持释放覆盖目标列表(char = {}, effectList = [], skill = {}, combatData = null) {
        return dedupeCombatTargetList(
          (Array.isArray(effectList) ? effectList : [])
            .flatMap(effect => 读取维持效果目标列表(char, effect, skill, combatData))
            .filter(Boolean),
        ).filter(isCombatUnitAlive);
      }

      function 计算维持消耗压力(parsed = {}, char = {}) {
        const stats = char?.属性 || char || {};
        const 魂力上限 = Math.max(1, Number(stats.sp_max ?? stats.魂力上限 ?? char?.sp_max ?? 1));
        const 体力上限 = Math.max(1, Number(stats.sta_max ?? stats.体力上限 ?? stats.vit_max ?? char?.sta_max ?? 1));
        const 精神上限 = Math.max(1, Number(stats.men_max ?? stats.精神力上限 ?? char?.men_max ?? 1));
        return 限制行为概率(
          Math.max(0, Number(parsed.reqSp || 0)) / 魂力上限 +
          Math.max(0, Number(parsed.reqVit || 0)) / 体力上限 +
          Math.max(0, Number(parsed.reqMen || 0)) / 精神上限,
          0,
          1,
        );
      }

      function 计算维持负荷(skill = {}, effectList = [], sustainCost = '无', char = {}, combatData = null) {
        const 覆盖目标列表 = 读取维持释放覆盖目标列表(char, effectList, skill, combatData);
        const 覆盖目标数量 = Math.max(1, 覆盖目标列表.length);
        const parsed = parseCostStringForChar(sustainCost, char, {
          覆盖目标数量,
          覆盖目标列表,
          批量模式: 覆盖目标数量 > 1 ? '原生群体' : '',
        });
        const 消耗压力 = 计算维持消耗压力(parsed, char);
        const 目标类型列表 = (Array.isArray(effectList) ? effectList : []).map(effect => 推断战斗效果目标类型(effect));
        const 范围系数 = 目标类型列表.some(类型 => 类型 === '全场')
          ? 0.12
          : 目标类型列表.some(类型 => String(类型 || '').includes('群体'))
            ? 0.08
            : 0.03;
        const 威力倍率 = Math.max(...(Array.isArray(effectList) ? effectList : []).map(effect => Number(effect?.威力倍率 || 0)), 0);
        const 威力系数 = 限制行为概率(威力倍率 / 300, 0, 1) * 0.12;
        const 效果文本 = JSON.stringify(effectList || []);
        const 控制系数 = /眩晕|沉默|封技|麻痹|混乱|僵直|失控|skip_turn|cannot_react|silence|skill_seal/.test(效果文本)
          ? 0.12
          : /迟缓|致盲|缴械|虚弱|紊乱|dodge_penalty|reaction_penalty|random_target_rate|lock_level/.test(效果文本)
            ? 0.06
            : 0;
        return 限制行为概率(消耗压力 * 0.35 + 范围系数 + 威力系数 + 控制系数, 0.03, 0.45);
      }

      function 刷新维持运行态负荷(char = {}) {
        const 维持列表 = Object.values(char?.持续效果 || {}).filter(Boolean);
        const 总负荷 = 限制行为概率(
          维持列表.reduce((总和, effect) => 总和 + Number(effect?.维持负荷 || 0), 0),
          0,
          0.75,
        );
        if (总负荷 > 0) {
          char.__维持负荷 = 总负荷;
          char.__维持反应惩罚 = Math.min(0.25, 总负荷 * 0.5);
          char.__维持前摇系数 = 1 + 总负荷 * 0.35;
        } else {
          delete char.__维持负荷;
          delete char.__维持反应惩罚;
          delete char.__维持前摇系数;
        }
      }

      function 构建维持释放伤害承载消耗(skill = {}, sustainCost = '无', char = {}, targetList = []) {
        const 目标数量 = Math.max(1, Math.min(12, (Array.isArray(targetList) ? targetList : []).filter(Boolean).length || 1));
        const costParts = splitSkillCostModes(getSkillCostText(skill));
        const 启动消耗 = parseSkillCostForChar(
          { ...skill, 消耗: costParts.upfront || '无', __覆盖目标数量: 目标数量 },
          char,
        );
        const 维持消耗 = parseSkillCostForChar(
          { 消耗: sustainCost || '无', __覆盖目标数量: 目标数量 },
          char,
        );
        const 取平均 = (启动值, 维持值) => {
          const 列表 = [Number(启动值 || 0), Number(维持值 || 0)].filter(数值 => 数值 > 0);
          if (!列表.length) return 0;
          return Math.max(1, Math.floor(列表.reduce((总和, 数值) => 总和 + 数值, 0) / 列表.length));
        };
        return formatCostObjectToString({
          魂力: 取平均(启动消耗.reqSp, 维持消耗.reqSp),
          体力: 取平均(启动消耗.reqVit, 维持消耗.reqVit),
          精神力: 取平均(启动消耗.reqMen, 维持消耗.reqMen),
        });
      }

      function 构建维持释放战斗数据(caster = {}, primaryTarget = null, combatData = null) {
        const 战斗数据 = combatData || getCurrentBattleContextSnapshot() || {};
        确保召唤单位表(战斗数据);
        const 双方 = 读取战斗双方队伍(战斗数据, caster);
        const 友方列表 = dedupeCombatTargetList([caster, ...双方.友方]).filter(isCombatUnitAlive);
        const 敌方列表 = dedupeCombatTargetList(双方.敌方).filter(isCombatUnitAlive);
        const 主敌方 =
          primaryTarget && 敌方列表.some(单位 => isCombatUnitIdentityMatch(单位, primaryTarget?.name || primaryTarget?.名称 || primaryTarget))
            ? primaryTarget
            : 敌方列表[0] || primaryTarget || null;
        return {
          ...战斗数据,
          参战者: {
            team_player: 友方列表,
            team_enemy: dedupeCombatTargetList([主敌方, ...敌方列表].filter(Boolean)),
          },
        };
      }

      function 执行维持释放效果(char = {}, effect = {}, label = '', combatData = null) {
        const 维持释放效果列表 = Array.isArray(effect?.维持释放效果列表) ? effect.维持释放效果列表 : [];
        const 维持存在效果列表 = Array.isArray(effect?.维持存在效果列表) ? effect.维持存在效果列表 : [];
        if (!维持释放效果列表.length) {
          return 维持存在效果列表.length
            ? `[维持状态] ${effect.name || '维持效果'}维持中，未重复释放一次性效果。`
            : '';
        }
        const 技能快照 = deepClonePlain(effect?.技能快照 || {});
        const 覆盖目标列表 = 读取维持释放覆盖目标列表(char, 维持释放效果列表, 技能快照, combatData);
        if (!覆盖目标列表.length) return `[维持释放] ${effect.name || '维持效果'}重扫当前目标，但没有可作用目标。`;
        const 主目标 = 覆盖目标列表[0];
        const 维持技能 = normalizeSkillData({
          ...技能快照,
          name: 技能快照.name || effect.name || '维持释放',
          魂技名: 技能快照.魂技名 || 技能快照.name || effect.name || '维持释放',
          消耗: '无',
          前摇: 0,
          _效果数组: deepClonePlain(维持释放效果列表),
          __维持释放伤害承载消耗: 构建维持释放伤害承载消耗(技能快照, effect.sustain_cost || effect.维持消耗 || '无', char, 覆盖目标列表),
        }, 技能快照.name || effect.name || '维持释放');
        const 维持行动 = {
          type: '维持释放',
          action_type: '维持释放',
          skill: 维持技能,
          cast_time: 0,
          target_name: 主目标?.name || 主目标?.名称 || '',
          __维持释放: true,
        };
        const 反应动作 = { type: '无法反应', log: '[维持压力] 后台持续释放并入当前战斗压力。', skill: null, def_mult: 1 };
        const 维持战斗数据 = 构建维持释放战斗数据(char, 主目标, combatData);
        const 结算结果 = executeClash(维持行动, 反应动作, 维持战斗数据);
        const 伤害落地 = applyResolvedDamagePackage(char, 维持行动, 结算结果, {
          primaryTarget: 主目标,
          combatData: 维持战斗数据,
        });
        const 目标文本 = 覆盖目标列表.map(目标 => 目标?.name || 目标?.名称 || '目标').join('、');
        const 日志 = [`[维持释放] ${effect.name || '维持效果'}重扫当前目标，命中 ${目标文本}。`];
        if (结算结果?.desc) 日志.push(结算结果.desc);
        if (伤害落地?.log) 日志.push(伤害落地.log);
        if (label) void label;
        return 日志.join(' ');
      }

      function registerSustainEffect(char, key, config) {
        if (!char || !config || !config.sustain_cost || config.sustain_cost === '无') return;
        if (!char.持续效果) char.持续效果 = {};
        char.持续效果[key] = { ...config };
        刷新维持运行态负荷(char);
      }

      function resolveActionSustainConfig(char, actionType, skill, stateName, combatData = null) {
        const skillName = skill?.name || actionType || stateName || '持续效果';
        const costParts = splitSkillCostModes(getSkillCostText(skill));
        const 原始关联状态名 = String(stateName || getPrimaryStateName(skill) || '').trim();
        const 关联状态名 = 原始关联状态名 && 原始关联状态名 !== '无' ? 原始关联状态名 : '';
        const isWuxingEscape = actionType === '五行遁法';
        const 条件上下文 = {
          actor: char,
          caster: char,
          attacker: char,
          target: null,
          defender: null,
          skill,
          combatData: combatData || getCurrentBattleContextSnapshot(),
          当前行动: String(actionType || skillName || '').trim(),
        };
        const 维持效果分组 = skill ? 构建维持效果分组(skill, 条件上下文) : { 维持释放效果列表: [], 维持存在效果列表: [] };
        const 自身状态维持效果 = 维持效果分组.维持释放效果列表.find(effect =>
          String(effect?.原型 || '').trim() === '状态施加' &&
          String(effect?.目标 || '').trim() === '自身' &&
          String(effect?.状态名称 || effect?.状态 || '').trim(),
        );
        const 自身状态维持名 = String(自身状态维持效果?.状态名称 || 自身状态维持效果?.状态 || '').trim();
        const 是自身状态维持 = isWuxingEscape || !!自身状态维持名 || String(`${关联状态名}${skillName}`).includes('真身');
        const 维持关联状态名 = 自身状态维持名 || 关联状态名 || (isWuxingEscape ? '五行遁法' : skillName);

        if (costParts.hasSustain && costParts.sustain && costParts.sustain !== '无') {
          const 技能快照 = deepClonePlain(skill || {});
          const 维持负荷 = 计算维持负荷(技能快照, 维持效果分组.维持释放效果列表, costParts.sustain, char, combatData);
          return {
            name: skillName,
            sustain_cost: costParts.sustain,
            技能快照,
            施术者名: String(char?.name || char?.名称 || '').trim(),
            目标语义: skill ? getSkillTarget(skill) : '',
            维持释放效果列表: 维持效果分组.维持释放效果列表,
            维持存在效果列表: 维持效果分组.维持存在效果列表,
            维持消耗: costParts.sustain,
            关联状态: 是自身状态维持 ? 维持关联状态名 : '',
            剩余回合: 0,
            维持负荷,
            effect_type: actionType?.includes('领域')
              ? 'domain'
              : actionType === '点燃生命之火'
                ? 'life_fire'
                : 是自身状态维持
                  ? 'condition'
                  : 'generic',
            related_condition: 是自身状态维持 ? 维持关联状态名 : '',
          };
        }

        if (actionType === '点燃生命之火') {
          return {
            name: skillName,
            sustain_cost: `体力:${Math.max(1, Math.floor((char?.vit_max || 1) * 0.05))}`,
            技能快照: deepClonePlain(skill || {}),
            施术者名: String(char?.name || char?.名称 || '').trim(),
            目标语义: skill ? getSkillTarget(skill) : '',
            维持释放效果列表: 维持效果分组.维持释放效果列表,
            维持存在效果列表: 维持效果分组.维持存在效果列表,
            维持消耗: `体力:${Math.max(1, Math.floor((char?.vit_max || 1) * 0.05))}`,
            关联状态: '',
            剩余回合: 0,
            维持负荷: 计算维持负荷(skill || {}, 维持效果分组.维持释放效果列表, `体力:${Math.max(1, Math.floor((char?.vit_max || 1) * 0.05))}`, char, combatData),
            effect_type: 'life_fire',
            related_condition: '',
          };
        }

        if (['展开斗铠领域', '展开精神领域', '展开武魂领域'].includes(actionType)) {
          const 维持消耗 = `精神力:${Math.max(30, Math.floor((char?.men_max || 1) * 0.05))}`;
          return {
            name: skillName,
            sustain_cost: 维持消耗,
            技能快照: deepClonePlain(skill || {}),
            施术者名: String(char?.name || char?.名称 || '').trim(),
            目标语义: skill ? getSkillTarget(skill) : '',
            维持释放效果列表: 维持效果分组.维持释放效果列表,
            维持存在效果列表: 维持效果分组.维持存在效果列表,
            维持消耗,
            关联状态: '',
            剩余回合: 0,
            维持负荷: 计算维持负荷(skill || {}, 维持效果分组.维持释放效果列表, 维持消耗, char, combatData),
            effect_type: 'domain',
            related_condition: '',
          };
        }

        if ((关联状态名 || skillName).includes('真身')) {
          const 维持消耗 = `魂力:${Math.max(50, Math.floor((char?.sp_max || 1) * 0.05))}`;
          return {
            name: skillName,
            sustain_cost: 维持消耗,
            技能快照: deepClonePlain(skill || {}),
            施术者名: String(char?.name || char?.名称 || '').trim(),
            目标语义: skill ? getSkillTarget(skill) : '',
            维持释放效果列表: 维持效果分组.维持释放效果列表,
            维持存在效果列表: 维持效果分组.维持存在效果列表,
            维持消耗,
            关联状态: 关联状态名 || skillName,
            剩余回合: 0,
            维持负荷: 计算维持负荷(skill || {}, 维持效果分组.维持释放效果列表, 维持消耗, char, combatData),
            effect_type: 'condition',
            related_condition: 关联状态名 || skillName,
          };
        }

        return null;
      }

        function settleSustainEffectsAtRoundEnd(char, label, combatData = null) {
          const logs = [];
          const broken = [];
          if (!char?.持续效果) return { log: '', broken };

          Object.entries(char.持续效果).forEach(([key, effect]) => {
            if (!effect) return;

            if (effect.effect_type === 'domain' && (!char.当前领域 || char.当前领域 === '无')) {
              delete char.持续效果[key];
              刷新维持运行态负荷(char);
              return;
            }
            if (effect.effect_type === 'life_fire' && !char.血脉之力?.生命之火) {
              delete char.持续效果[key];
              刷新维持运行态负荷(char);
              return;
            }
            if (
              effect.effect_type === 'condition' &&
              effect.related_condition &&
              !char.状态效果?.[effect.related_condition]
            ) {
              delete char.持续效果[key];
              刷新维持运行态负荷(char);
              return;
            }

            const 维持释放效果列表 = Array.isArray(effect.维持释放效果列表) ? effect.维持释放效果列表 : [];
            const 覆盖目标列表 = 读取维持释放覆盖目标列表(
              char,
              维持释放效果列表,
              effect.技能快照 || {},
              combatData,
            );
            const parsed = parseCostStringForChar(
              effect.sustain_cost,
              char,
              {
                覆盖目标数量: Math.max(1, 覆盖目标列表.length || Number(effect.__覆盖目标数量 || 1) || 1),
                覆盖目标列表,
                批量模式: 覆盖目标列表.length > 1 ? '原生群体' : '',
                单体系数: Number(effect.support_cost_scale || 1) || 1,
              },
            );
            if (!parsed.canCast) {
              if (effect.effect_type === 'domain') char.当前领域 = '无';
              else if (effect.effect_type === 'life_fire' && char.血脉之力)
                char.血脉之力.生命之火 = false;
              else if (effect.effect_type === 'condition' && effect.related_condition && char.状态效果)
                delete char.状态效果[effect.related_condition];

              delete char.持续效果[key];
              刷新维持运行态负荷(char);
              broken.push(effect.name || key);
              logs.push(`[维持中断] ${label}已无力维持[${effect.name || key}]，效果自动解除`);
              return;
            }

            deductParsedCostFromUnit(char, parsed);
            logs.push(`[维持结算] ${label}维持[${effect.name || key}]，消耗 ${formatParsedCost(parsed)}`);
            const 维持释放日志 = 执行维持释放效果(char, effect, label, combatData);
            if (维持释放日志) logs.push(维持释放日志);
          });

          刷新维持运行态负荷(char);
          return { log: logs.join(' '), broken };
        }

      const 行为经验随机缓存 = new WeakMap();

      function 计算行为等级分(角色 = {}) {
        const 等级 = getCombatUnitTierNumber(角色);
        if (等级 >= 99) return 50;
        if (等级 >= 95) return 45;
        if (等级 >= 90) return 40;
        if (等级 >= 80) return 35;
        if (等级 >= 70) return 30;
        if (等级 >= 60) return 25;
        if (等级 >= 50) return 20;
        if (等级 >= 40) return 15;
        if (等级 >= 30) return 10;
        if (等级 >= 20) return 5;
        return 0;
      }

      function 计算行为天赋分(角色 = {}) {
        const 属性 = 角色?.属性 && typeof 角色.属性 === 'object' ? 角色.属性 : 角色 || {};
        const 天赋梯队 = String(属性.天赋梯队 || 角色.天赋梯队 || '').trim() || '正常';
        const 天赋分表 = {
          绝世妖孽: 50,
          顶级天才: 40,
          天才: 30,
          优秀: 20,
          正常: 10,
          劣等: 0,
          天赋极差: -10,
        };
        return Object.prototype.hasOwnProperty.call(天赋分表, 天赋梯队) ? 天赋分表[天赋梯队] : 10;
      }

      function 是玩家操控角色(角色 = {}, 战斗数据 = {}) {
        const 角色名 = String(角色?.name || 角色?.名称 || '').trim();
        const 系统玩家名 = String(window.BattleUIBridge?.getMVU?.('sys.玩家名') || '').trim();
        const 参战玩家名 = String(战斗数据?.参战者?.team_player?.[0]?.name || 战斗数据?.参战者?.team_player?.[0]?.名称 || '').trim();
        if (!角色名) return false;
        if (系统玩家名) return 角色名 === 系统玩家名;
        return !!参战玩家名 && 角色名 === 参战玩家名;
      }

      function 读取行为战斗记录次数(角色 = {}, 目标 = {}, 战斗数据 = {}) {
        const 目标名 = String(目标?.name || 目标?.名称 || '').trim();
        if (!目标名) return 0;
        const 角色历史 = 角色?.战斗历史 && typeof 角色.战斗历史 === 'object' ? 角色.战斗历史 : {};
        const 图鉴键 = 解析图鉴键与模板(目标)?.key || 目标名;
        return Math.max(0, Number(角色历史?.[图鉴键]?.次数 || 0), Number(角色历史?.[目标名]?.次数 || 0));
      }

      function 读取行为随机经验值(角色 = {}) {
        if (!角色 || typeof 角色 !== 'object') return Math.floor(Math.random() * 40) + 1;
        if (!行为经验随机缓存.has(角色)) 行为经验随机缓存.set(角色, Math.floor(Math.random() * 40) + 1);
        return 行为经验随机缓存.get(角色);
      }

      function 计算行为战斗经验(角色 = {}, 目标 = {}, 战斗数据 = {}) {
        const 记录分 = Math.min(40, 读取行为战斗记录次数(角色, 目标, 战斗数据) * 0.5);
        const 玩家操控 = 是玩家操控角色(角色, 战斗数据);
        const 随机分 = 玩家操控 ? 0 : 读取行为随机经验值(角色);
        const 基础经验分 = 玩家操控 ? 记录分 : Math.min(40, 随机分 + 记录分);
        const 天赋分 = 计算行为天赋分(角色);
        const 等级分 = 计算行为等级分(角色);
        const 总分 = 基础经验分 + 天赋分 + 等级分;
        const 稳定度 = 限制行为概率(总分 / 140, 0, 1);
        return {
          基础经验分,
          随机分,
          记录分,
          天赋分,
          等级分,
          总分,
          稳定度,
          玩家操控,
        };
      }

      function 获取行为经验稳定度(战斗状态 = {}) {
        const 稳定度 = Number(战斗状态?.战斗经验?.稳定度 ?? 战斗状态?.经验稳定度 ?? 0.45);
        return 限制行为概率(稳定度, 0, 1);
      }

      function 读取角色决策干扰强度(角色 = {}, 字段名 = '') {
        if (!角色?.状态效果 || !字段名) return 0;
        const 总和 = Object.values(角色.状态效果).reduce((累计, 状态) => 累计 + Number(状态?.战斗效果?.[字段名] || 0), 0);
        return 限制行为概率(总和, 0, 1);
      }

      function 读取技能决策干扰强度(skill = {}, 字段名 = '') {
        if (!字段名) return 0;
        const 总和 = getSkillEffects(skill).reduce((累计, effect) => {
          if (String(effect?.原型 || '').trim() !== '决策干扰') return 累计;
          const 干扰 = String(effect?.干扰 || '').trim();
          const 匹配判断 = 字段名 === '判断干扰强度' && 干扰 === '判断干扰';
          const 匹配索敌 = 字段名 === '索敌干扰强度' && 干扰 === '索敌干扰';
          if (匹配判断 || 匹配索敌) return 累计 + Math.abs(读取战斗数值正负(effect?.数值));
          return 累计;
        }, 0);
        return 限制行为概率(总和, 0, 1);
      }

      function 扰动候选权重列表(候选列表 = [], 干扰强度 = 0) {
        const 强度 = 限制行为概率(干扰强度, 0.01, 1);
        if (!(强度 > 0.01)) return 候选列表;
        const 有效候选 = (候选列表 || []).filter(候选 => 候选 && Number(候选.weight || 0) > 0);
        if (有效候选.length <= 1) return 候选列表;
        const 平均权重 = 有效候选.reduce((累计, 候选) => 累计 + Number(候选.weight || 0), 0) / 有效候选.length;
        return (候选列表 || []).map(候选 => {
          if (!候选 || !(Number(候选.weight || 0) > 0)) return 候选;
          const 原权重 = Number(候选.weight || 0);
          return {
            ...候选,
            weight: Math.max(1, Math.round(原权重 * (1 - 强度) + 平均权重 * 强度)),
            __干扰强度: 强度,
          };
        });
      }

      function 扰动索敌候选列表(候选列表 = [], 干扰强度 = 0) {
        return 扰动候选权重列表(候选列表, 干扰强度);
      }

      function 读取行动判断干扰强度(actor = {}, skill = null) {
        return 限制行为概率(
          读取角色决策干扰强度(actor, '判断干扰强度') + (skill ? 读取技能决策干扰强度(skill, '判断干扰强度') : 0),
          0,
          1,
        );
      }

      function 读取索敌干扰强度(actor = {}, skill = null) {
        return 限制行为概率(
          读取角色决策干扰强度(actor, '索敌干扰强度') + (skill ? 读取技能决策干扰强度(skill, '索敌干扰强度') : 0),
          0,
          1,
        );
      }

      function 确保行动闭环诊断(combatData = {}) {
        if (!combatData || typeof combatData !== 'object') return null;
        if (!Object.prototype.hasOwnProperty.call(combatData, '__行动闭环诊断')) {
          Object.defineProperty(combatData, '__行动闭环诊断', {
            enumerable: false,
            configurable: true,
            writable: true,
            value: {
              主动规划次数: 0,
              索敌规划次数: 0,
              辅助目标规划次数: 0,
              应招审计次数: 0,
              再判定审计次数: 0,
              换招审计次数: 0,
              目标闭环缺失: 0,
              团队意图未消费: 0,
              规划旁路残留: 0,
              审计轨迹: [],
              真实样本轨迹: [],
              事实账本: null,
            },
          });
        }
        combatData.__行动闭环诊断.审计轨迹 ||= [];
        combatData.__行动闭环诊断.真实样本轨迹 ||= [];
        combatData.__行动闭环诊断.状态来源登记 ||= [];
        combatData.__行动闭环诊断.目标权重探针 ||= [];
        return combatData.__行动闭环诊断;
      }

      let 战斗运行时应用ID序号 = 0;
      function 生成战斗运行时应用ID(prefix = 'battle-event') {
        战斗运行时应用ID序号 = (战斗运行时应用ID序号 + 1) % 1000000;
        return `${prefix}-${Date.now().toString(36)}-${战斗运行时应用ID序号.toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
      }

      function 记录状态来源登记(combatData = {}, payload = {}) {
        const 诊断 = 确保行动闭环诊断(combatData?.__父级战斗数据 || combatData);
        if (!诊断) return '';
        const entry = {
          applicationId: String(payload.applicationId || 生成战斗运行时应用ID('state-src')).trim(),
          stateName: String(payload.stateName || '').trim(),
          targetName: String(payload.targetName || '').trim(),
          sourceActorName: String(payload.sourceActorName || '').trim(),
          sourceActionName: String(payload.sourceActionName || '').trim(),
          sourceActionType: String(payload.sourceActionType || '').trim(),
          sourceRound: Number(payload.sourceRound || combatData?.回合 || 0),
          duration: Math.max(0, Number(payload.duration || 0)),
          effectSummary: String(payload.effectSummary || '').trim(),
          driverAttr: String(payload.driverAttr || '').trim(),
          round: Number(payload.round || combatData?.回合 || 0),
          eventKind: 'state_apply',
        };
        if (!entry.stateName || !entry.targetName) return '';
        诊断.状态来源登记.push(entry);
        if (诊断.状态来源登记.length > 400) 诊断.状态来源登记.splice(0, 诊断.状态来源登记.length - 400);
        return entry.applicationId;
      }

      function 查找状态来源登记(combatData = {}, 条件 = {}) {
        const 诊断 = 确保行动闭环诊断(combatData?.__父级战斗数据 || combatData);
        if (!诊断) return null;
        const applicationId = String(条件.applicationId || '').trim();
        const stateName = String(条件.stateName || '').trim();
        const targetName = String(条件.targetName || '').trim();
        const sourceRound = Number(条件.maxRound || combatData?.回合 || 0);
        const 列表 = Array.isArray(诊断.状态来源登记) ? 诊断.状态来源登记 : [];
        if (applicationId) {
          return [...列表].reverse().find(item => String(item?.applicationId || '').trim() === applicationId) || null;
        }
        return [...列表].reverse().find(item => {
          if (stateName && String(item?.stateName || '').trim() !== stateName) return false;
          if (targetName && !isSameBattleReportName(item?.targetName || '', targetName)) return false;
          return Number(item?.sourceRound || item?.round || 0) <= sourceRound;
        }) || null;
      }

      function 构建状态结果效果摘要(战斗效果 = {}) {
        const effects = 战斗效果 && typeof 战斗效果 === 'object' ? 战斗效果 : {};
        const parts = [];
        const dotDamage = Number(effects.dot_damage || 0);
        const dotRatio = Number(effects.dot_damage_ratio || 0);
        if (dotDamage > 0) parts.push(`每回合损失${Math.round(dotDamage)}点生命`);
        if (dotRatio > 0) parts.push(`每回合损失目标生命${Math.round(dotRatio * 100)}%`);
        if (effects.skip_turn === true) parts.push('跳过行动');
        if (effects.cannot_react === true) parts.push('无法反应');
        if (effects.silence === true || effects.skill_seal === true) parts.push('技能封锁');
        if (Number(effects.cast_speed_penalty || 0) > 0) parts.push(`行动迟滞${Math.round(Number(effects.cast_speed_penalty || 0) * 100)}%`);
        if (Number(effects.random_target_rate || 0) > 0) parts.push(`偏转概率${Math.round(Number(effects.random_target_rate || 0) * 100)}%`);
        return parts.join('，');
      }

      function 记录目标权重探针(combatData = {}, payload = {}) {
        const 诊断 = 确保行动闭环诊断(combatData?.__父级战斗数据 || combatData);
        if (!诊断) return null;
        const entry = {
          round: Number(payload.round || combatData?.回合 || 0),
          actor: String(payload.actor || '').trim(),
          targetPoolSide: String(payload.targetPoolSide || '').trim(),
          targetType: String(payload.targetType || '').trim(),
          selectedTarget: String(payload.selectedTarget || '').trim(),
          source: String(payload.source || '').trim(),
          candidates: Array.isArray(payload.candidates) ? payload.candidates.map(item => ({
            target: String(item?.target || '').trim(),
            baseWeight: Number(item?.baseWeight || 0),
            finalWeight: Number(item?.finalWeight ?? item?.weight ?? 0),
            tagReasons: Array.isArray(item?.tagReasons) ? item.tagReasons : [],
            poolSide: String(item?.poolSide || '').trim(),
          })) : [],
        };
        诊断.目标权重探针.push(entry);
        if (诊断.目标权重探针.length > 240) 诊断.目标权重探针.splice(0, 诊断.目标权重探针.length - 240);
        return entry;
      }

      function 确保战斗事件账本(combatData = {}) {
        if (!combatData || typeof combatData !== 'object') return [];
        if (!Object.prototype.hasOwnProperty.call(combatData, '__battleEventLedger')) {
          Object.defineProperty(combatData, '__battleEventLedger', {
            enumerable: false,
            configurable: true,
            writable: true,
            value: [],
          });
        }
        if (!Array.isArray(combatData.__battleEventLedger)) combatData.__battleEventLedger = [];
        return combatData.__battleEventLedger;
      }

      function 生成战斗动作事实ID(prefix = 'battle-action') {
        return 生成战斗运行时应用ID(prefix);
      }

      function 确保战斗判定因果链(combatData = {}) {
        const rootData = combatData?.__父级战斗数据 || combatData;
        if (!rootData || typeof rootData !== 'object') return [];
        if (!Object.prototype.hasOwnProperty.call(rootData, '__battleResolutionTrace')) {
          Object.defineProperty(rootData, '__battleResolutionTrace', {
            enumerable: false,
            configurable: true,
            writable: true,
            value: [],
          });
        }
        if (!Array.isArray(rootData.__battleResolutionTrace)) rootData.__battleResolutionTrace = [];
        return rootData.__battleResolutionTrace;
      }

      function 推断战斗因果节点配置(event = {}) {
        const kind = String(event?.eventKind || '').trim();
        const result = String(event?.result || '').trim();
        if (kind === 'action_start') return { nodeKind: 'action_decision', nodeLayer: 'intent', primaryOutcome: 'action_committed' };
        if (kind === 'reaction_window') return { nodeKind: 'reaction_window', nodeLayer: 'system_check', primaryOutcome: 'reaction_window_opened' };
        if (['dodge', 'defend', 'pass'].includes(kind)) return { nodeKind: 'reaction_decision', nodeLayer: 'system_check', primaryOutcome: kind === 'dodge' ? (/evaded|miss|dodge_success|闪避成功|未命中/.test(result) ? 'dodged' : 'reaction_failed') : (kind === 'defend' ? 'guarded' : 'reaction_failed') };
        if (kind === 'hit_result') return { nodeKind: 'damage_settlement', nodeLayer: 'settlement', primaryOutcome: 读取战报事件Outcome(event) };
        if (kind === 'state_apply') {
          const stateName = 读取事件账本状态名(event);
          const isControl = /控制|眩晕|沉默|封技|定身|束缚|禁锢|僵直|击退|减速|迟缓|位移限制|skip_turn|cannot_react|silence/i.test(stateName);
          const immune = /immune|immunity|免疫|无视异常/.test(result);
          const resisted = /resist|resisted|抵抗|豁免|未附着/.test(result);
          return {
            nodeKind: 'state_settlement',
            nodeLayer: 'settlement',
            primaryOutcome: immune ? (isControl ? 'control_immune' : 'state_immune') : (isControl ? (resisted ? 'control_resisted' : 'control_applied') : (resisted ? 'state_resisted' : 'state_applied')),
          };
        }
        if (kind === 'state_tick') return { nodeKind: 'state_settlement', nodeLayer: 'settlement', primaryOutcome: 'state_tick' };
        if (kind === 'state_replace' || kind === 'state_remove') return { nodeKind: 'state_settlement', nodeLayer: 'settlement', primaryOutcome: kind };
        if (kind === 'blocked_settlement') return { nodeKind: 'final_result', nodeLayer: 'settlement', primaryOutcome: 'interrupted' };
        if (kind === 'resource_change') return { nodeKind: 'final_result', nodeLayer: 'settlement', primaryOutcome: 'resource_recovered' };
        if (kind === 'counter_window') return { nodeKind: 'counter_window', nodeLayer: 'system_check', primaryOutcome: result === 'opened' ? 'counter_window_opened' : 'no_valid_window' };
        if (kind === 'counter') return { nodeKind: result === 'fail' ? 'counter_window' : 'counter_action', nodeLayer: result === 'fail' ? 'system_check' : 'settlement', primaryOutcome: result === 'fail' ? 'no_valid_window' : 'full_hit' };
        if (kind === 'summon_assist') return { nodeKind: 'summon_assist', nodeLayer: 'settlement', primaryOutcome: 'summon_action' };
        if (kind === 'summon_create') return { nodeKind: 'final_result', nodeLayer: 'settlement', primaryOutcome: 'summon_created' };
        if (kind === 'create' || kind === 'shield_create') return { nodeKind: 'final_result', nodeLayer: 'settlement', primaryOutcome: kind === 'create' ? 'item_created' : 'guarded' };
        if (['blocked_action', 'failed_action', 'target_fail'].includes(kind)) return { nodeKind: 'final_result', nodeLayer: 'settlement', primaryOutcome: 'interrupted' };
        return { nodeKind: kind || 'event', nodeLayer: 'settlement', primaryOutcome: String(event?.primaryOutcome || event?.meta?.primaryOutcome || '').trim() || 'no_effect' };
      }

      function 标准化战斗ReasonCode(code = '', fallback = 'UNKNOWN_REASON') {
        const value = String(code || '').trim().toUpperCase();
        const allowed = new Set([
          'INTERRUPTED_BY_SPEED', 'TARGET_REPOSITIONED', 'RESOURCE_INSUFFICIENT', 'CONTROLLED',
          'OUT_OF_RANGE', 'TACTICAL_DISADVANTAGE', 'COUNTER_WINDOW_OPENED', 'COUNTER_WINDOW_MISSED',
          'SUMMON_CONTROL_OVERLOAD', 'NO_VALID_TARGET', 'TARGET_LOST', 'REACTION_FAILED',
          'REACTION_SUCCEEDED', 'ACTION_COMMITTED', 'NO_EFFECTIVE_OPENING', 'NO_STRUCTURED_SETTLEMENT', 'UNKNOWN_REASON',
          'LEGACY_CUSTOM_REASON',
        ]);
        return allowed.has(value) ? value : fallback;
      }

      function 推断战斗默认ReasonCode(event = {}, primaryOutcome = '') {
        const kind = String(event?.eventKind || '').trim();
        const result = String(event?.result || '').trim();
        const outcome = String(primaryOutcome || event?.primaryOutcome || event?.meta?.primaryOutcome || '').trim();
        if (kind === 'counter_window') return result === 'opened' ? 'COUNTER_WINDOW_OPENED' : 'COUNTER_WINDOW_MISSED';
        if (kind === 'counter') return result === 'fail' ? 'COUNTER_WINDOW_MISSED' : 'COUNTER_WINDOW_OPENED';
        if (['dodge', 'defend', 'pass'].includes(kind)) {
          return /failed|fail|失败|未能/.test(result) || outcome === 'reaction_failed' ? 'REACTION_FAILED' : 'REACTION_SUCCEEDED';
        }
        if (['blocked_action', 'failed_action', 'target_fail'].includes(kind)) {
          if (/资源|魂力|精神力|体力|resource/i.test(String(event?.failReason || event?.failureReason || event?.result || ''))) return 'RESOURCE_INSUFFICIENT';
          if (/目标|target/i.test(String(event?.failReason || event?.failureReason || event?.result || ''))) return 'NO_VALID_TARGET';
          return 'ACTION_COMMITTED';
        }
        if (outcome === 'no_valid_window') return 'NO_EFFECTIVE_OPENING';
        if (outcome === 'miss' || outcome === 'dodged' || /miss|evade|dodge|未命中|闪避/.test(result)) return 'REACTION_SUCCEEDED';
        if (outcome === 'reaction_failed') return 'REACTION_FAILED';
        if (outcome === 'interrupted') return 'INTERRUPTED_BY_SPEED';
        return 'ACTION_COMMITTED';
      }

      function 推断战斗目标范围标记(targetKind = '') {
        const kind = String(targetKind || '').trim();
        if (kind === '敌方群体') return 'enemy_group';
        if (kind === '友方群体') return 'ally_group';
        if (kind === '全场') return 'all_units';
        if (kind === '自身') return 'self';
        if (/群体|范围/.test(kind)) return 'enemy_group';
        return kind ? 'single' : '';
      }

      function 标准化战斗阵营侧(value = '') {
        const side = String(value || '').trim();
        if (/^(player|玩家|我方)$/i.test(side)) return 'player';
        if (/^(enemy|敌方|对方)$/i.test(side)) return 'enemy';
        return '';
      }

      function 推断战斗目标阵营侧(actorSide = '', targetPoolSide = '') {
        const side = 标准化战斗阵营侧(actorSide);
        const pool = String(targetPoolSide || '').trim();
        if (!side) return '';
        if (/^(hostile|enemy|敌对|敌方)$/i.test(pool)) return side === 'player' ? 'enemy' : 'player';
        if (/^(allied|ally|self|友方|己方)$/i.test(pool)) return side;
        return '';
      }

      function 推断战斗单位阵营侧(combatData = {}, name = '', fallback = '') {
        const normalizedFallback = 标准化战斗阵营侧(fallback);
        const unitName = String(name || '').trim();
        if (!unitName) return normalizedFallback;
        if (读取战斗主队单位列表(combatData, '玩家').some(unit => isCombatUnitIdentityMatch(unit, unitName))) return 'player';
        if (读取战斗主队单位列表(combatData, '敌方').some(unit => isCombatUnitIdentityMatch(unit, unitName))) return 'enemy';
        const summon = Object.values(确保召唤单位表(combatData)).find(unit => isCombatUnitIdentityMatch(unit, unitName));
        if (summon?.阵营 === '玩家') return 'player';
        if (summon?.阵营 === '敌方') return 'enemy';
        if (读取战斗阵营单位列表(combatData, '玩家').some(unit => isCombatUnitIdentityMatch(unit, unitName))) return 'player';
        if (读取战斗阵营单位列表(combatData, '敌方').some(unit => isCombatUnitIdentityMatch(unit, unitName))) return 'enemy';
        return normalizedFallback;
      }

      function 推断战斗事件阵营侧(combatData = {}, event = {}) {
        const meta = event?.meta && typeof event.meta === 'object' ? event.meta : {};
        const actorName = String(event?.actorName || '').trim();
        const targetName = String(event?.targetName || '').trim();
        const targetScope = String(event?.targetScope || meta.targetScope || '').trim();
        const actorSide = 标准化战斗阵营侧(event?.actorSide || event?.side || meta.actorSide || meta.side || '')
          || 推断战斗单位阵营侧(combatData, actorName);
        const targetSide = 标准化战斗阵营侧(event?.targetSide || meta.targetSide || '')
          || 推断战斗单位阵营侧(combatData, targetName)
          || 推断战斗目标阵营侧(actorSide, event?.targetPoolSide || meta.targetPoolSide || '')
          || (['ally_group', 'self'].includes(targetScope) ? actorSide : '')
          || (['enemy_group', 'area'].includes(targetScope) ? 推断战斗目标阵营侧(actorSide, 'hostile') : '')
          || (targetName && actorName && isSameBattleReportName(targetName, actorName) ? actorSide : '');
        return { actorSide, targetSide };
      }

      function 推断战斗行动目标范围(action = null) {
        const skill = action?.skill || action?.技能 || null;
        return skill ? 推断战斗目标范围标记(inferSkillPrimaryTargetKind(skill)) : '';
      }

      function 事件目标分支名称(event = {}) {
        const kind = String(event?.eventKind || '').trim();
        if (['dodge', 'defend', 'pass'].includes(kind)) return String(event.actorName || '').trim();
        return String(event.targetName || '').trim();
      }

      function 需要目标分支(scope = '') {
        return ['enemy_group', 'ally_group', 'all_units', 'area'].includes(String(scope || '').trim());
      }

      function 写入战斗目标分支节点(combatData = {}, event = {}, sourceNodeId = '') {
        const trace = 确保战斗判定因果链(combatData);
        const parentNodeId = String(sourceNodeId || '').trim();
        const targetName = 事件目标分支名称(event);
        const targetScope = String(event.targetScope || event.meta?.targetScope || '').trim();
        if (!trace || !parentNodeId || !targetName || !需要目标分支(targetScope)) return null;
        const branchKey = [parentNodeId, targetName].join('|');
        const existing = trace.find(node => node?.nodeKind === 'target_branch' && String(node?.parentNodeId || '').trim() === parentNodeId && String(node?.targetName || '').trim() === targetName);
        if (existing) return existing;
        const eventSides = 推断战斗事件阵营侧(combatData, event);
        const branchActorSide = 推断战斗单位阵营侧(combatData, String(event.actorName || '').trim()) || eventSides.actorSide;
        const branchTargetSide = 推断战斗单位阵营侧(combatData, targetName) || eventSides.targetSide;
        const node = {
          nodeId: String(生成战斗运行时应用ID('battle-trace-target-branch')).trim(),
          parentNodeId,
          round: Number(event.round || event.sourceRound || 0),
          phase: 'action',
          nodeKind: 'target_branch',
          nodeLayer: 'system_check',
          actorName: String(event.actorName || '').trim(),
          actorSide: branchActorSide,
          targetName,
          targetSide: branchTargetSide,
          targetId: String(event.targetId || '').trim(),
          targetScope: 'single',
          initialActionName: normalizeBattleActionDisplayName(event.initialActionName || event.actionName || event.sourceActionName || ''),
          finalActionName: normalizeBattleActionDisplayName(event.finalActionName || event.actionName || event.sourceActionName || ''),
          discardedActionName: '',
          source: 'target_branch',
          result: 'branched',
          primaryOutcome: 'target_branch',
          failureReason: '',
          reasonCode: '',
          reasonText: '',
          replanReasonCode: '',
          replanReasonText: '',
          ledgerEventIds: [],
          calculationTrace: [{ key: 'targetName', label: '目标', value: targetName }, { key: 'sourceScope', label: '来源范围', value: targetScope }],
          counterDepth: 0,
          counterRootNodeId: parentNodeId,
          branchKey,
        };
        trace.push(node);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return node;
      }

      function 写入战斗反应窗口节点(combatData = {}, event = {}, parentNodeId = '') {
        const kind = String(event?.eventKind || '').trim();
        if (!['dodge', 'defend', 'pass'].includes(kind)) return null;
        const trace = 确保战斗判定因果链(combatData);
        const parent = String(parentNodeId || event.parentNodeId || event.sourceNodeId || '').trim();
        const actorName = String(event.actorName || '').trim();
        if (!trace || !parent || !actorName) return null;
        const actionName = normalizeBattleActionDisplayName(event.finalActionName || event.actionName || event.meta?.finalActionName || event.meta?.actionName || '应招');
        const existing = trace.find(node =>
          String(node?.nodeKind || '').trim() === 'reaction_window' &&
          String(node?.parentNodeId || '').trim() === parent &&
          String(node?.actorName || '').trim() === actorName &&
          normalizeBattleActionDisplayName(node?.finalActionName || '') === actionName
        );
        if (existing) return existing;
        const { actorSide, targetSide } = 推断战斗事件阵营侧(combatData, event);
        const node = {
          nodeId: String(生成战斗运行时应用ID('battle-trace-reaction-window')).trim(),
          parentNodeId: parent,
          round: Number(event.round || event.sourceRound || 0),
          phase: 'action',
          nodeKind: 'reaction_window',
          nodeLayer: 'system_check',
          actorName,
          actorSide,
          targetName: String(event.targetName || '').trim(),
          targetSide,
          targetId: String(event.targetId || '').trim(),
          targetScope: 'single',
          initialActionName: actionName,
          finalActionName: actionName,
          discardedActionName: '',
          source: 'reaction_window',
          result: 'opened',
          primaryOutcome: 'reaction_window_opened',
          failureReason: '',
          reasonCode: 'REACTION_SUCCEEDED',
          reasonText: '察觉到当前攻势，获得应招窗口',
          replanReasonCode: '',
          replanReasonText: '',
          ledgerEventIds: [],
          calculationTrace: [
            { key: 'reactor', label: '应招方', value: actorName },
            { key: 'sourceAction', label: '来源动作', value: normalizeBattleActionDisplayName(event.sourceActionName || '') },
            { key: 'sourceActor', label: '攻势来源', value: String(event.targetName || '').trim() },
          ],
          counterDepth: 0,
          counterRootNodeId: parent,
        };
        trace.push(node);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return node;
      }

      function 写入战斗变招决策节点(combatData = {}, event = {}, parentNode = null) {
        if (String(event?.eventKind || '').trim() !== 'action_start') return null;
        const parentNodeId = String(parentNode?.nodeId || event.parentNodeId || event.chainNodeId || '').trim();
        if (!parentNodeId) return null;
        const trace = 确保战斗判定因果链(combatData);
        const initialActionName = normalizeBattleActionDisplayName(event.initialActionName || event.meta?.initialActionName || event.actionName || '');
        const finalActionName = normalizeBattleActionDisplayName(event.finalActionName || event.meta?.finalActionName || event.actionName || '');
        const discardedActionName = normalizeBattleActionDisplayName(event.discardedActionName || event.meta?.discardedActionName || (initialActionName && finalActionName && initialActionName !== finalActionName ? initialActionName : ''));
        if (!trace || !finalActionName || !discardedActionName || discardedActionName === finalActionName) return null;
        const existing = trace.find(node =>
          String(node?.nodeKind || '').trim() === 'replan_decision' &&
          String(node?.parentNodeId || '').trim() === parentNodeId &&
          normalizeBattleActionDisplayName(node?.discardedActionName || '') === discardedActionName &&
          normalizeBattleActionDisplayName(node?.finalActionName || '') === finalActionName
        );
        if (existing) return existing;
        const reasonCode = 标准化战斗ReasonCode(event.replanReasonCode || event.meta?.replanReasonCode || 'TACTICAL_DISADVANTAGE', 'TACTICAL_DISADVANTAGE');
        const replanReasonText = String(event.replanReasonText || event.meta?.replanReasonText || event.reasonText || event.meta?.reasonText || '').trim();
        const { actorSide, targetSide } = 推断战斗事件阵营侧(combatData, event);
        const node = {
          nodeId: String(生成战斗运行时应用ID('battle-trace-replan-decision')).trim(),
          parentNodeId,
          round: Number(event.round || event.sourceRound || 0),
          phase: 'action',
          nodeKind: 'replan_decision',
          nodeLayer: 'intent',
          actorName: String(event.actorName || '').trim(),
          actorSide,
          targetName: String(event.targetName || '').trim(),
          targetSide,
          targetId: String(event.targetId || '').trim(),
          targetScope: String(event.targetScope || event.meta?.targetScope || '').trim() || (event.targetName ? 'single' : ''),
          initialActionName: discardedActionName,
          finalActionName,
          discardedActionName,
          source: 'replan_decision',
          result: 'replanned',
          primaryOutcome: 'action_committed',
          failureReason: '',
          reasonCode,
          reasonText: replanReasonText,
          replanReasonCode: reasonCode,
          replanReasonText,
          ledgerEventIds: [String(event.eventId || '').trim()].filter(Boolean),
          calculationTrace: [
            { key: 'discardedAction', label: '废弃动作', value: discardedActionName },
            { key: 'finalAction', label: '最终动作', value: finalActionName },
            { key: 'reasonCode', label: '变招原因', value: reasonCode },
          ],
          counterDepth: Math.max(0, Number(event.meta?.counterDepth || 0)),
          counterRootNodeId: parentNodeId,
        };
        trace.push(node);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return node;
      }

      function 写入战斗命中检定节点(combatData = {}, event = {}, parentNodeId = '') {
        if (String(event?.eventKind || '').trim() !== 'hit_result') return null;
        const parent = String(parentNodeId || event.parentNodeId || event.sourceNodeId || '').trim();
        if (!parent) return null;
        const trace = 确保战斗判定因果链(combatData);
        const result = String(event.result || '').trim();
        const actorName = String(event.actorName || '').trim();
        const targetName = String(event.targetName || '').trim();
        const actionName = normalizeBattleActionDisplayName(event.finalActionName || event.actionName || event.meta?.finalActionName || event.meta?.actionName || '');
        const existing = trace.find(node =>
          String(node?.nodeKind || '').trim() === 'hit_check' &&
          String(node?.parentNodeId || '').trim() === parent &&
          String(node?.actorName || '').trim() === actorName &&
          String(node?.targetName || '').trim() === targetName &&
          normalizeBattleActionDisplayName(node?.finalActionName || '') === actionName
        );
        if (existing) return existing;
        const missed = /miss|evade|dodge|未命中|闪避/.test(result);
        const meta = event.meta && typeof event.meta === 'object' ? event.meta : {};
        const { actorSide, targetSide } = 推断战斗事件阵营侧(combatData, event);
        const node = {
          nodeId: String(生成战斗运行时应用ID('battle-trace-hit-check')).trim(),
          parentNodeId: parent,
          round: Number(event.round || event.sourceRound || 0),
          phase: 'action',
          nodeKind: 'hit_check',
          nodeLayer: 'system_check',
          actorName,
          actorSide,
          targetName,
          targetSide,
          targetId: String(event.targetId || '').trim(),
          targetScope: String(event.targetScope || meta.targetScope || '').trim() || (targetName ? 'single' : ''),
          initialActionName: actionName,
          finalActionName: actionName,
          discardedActionName: '',
          source: 'hit_check',
          result: result || (missed ? 'miss' : 'hit'),
          primaryOutcome: missed ? 'miss' : 'damage',
          failureReason: missed ? String(event.failureReason || event.failReason || meta.failureReason || 'dodged').trim() : '',
          reasonCode: missed ? 标准化战斗ReasonCode(event.reasonCode || meta.reasonCode || 'REACTION_SUCCEEDED', 'REACTION_SUCCEEDED') : 'ACTION_COMMITTED',
          reasonText: missed ? String(event.reasonText || meta.reasonText || '目标成功规避本次落点').trim() : '落点检定通过，进入伤害结算',
          replanReasonCode: '',
          replanReasonText: '',
          ledgerEventIds: [String(event.eventId || '').trim()].filter(Boolean),
          calculationTrace: [
            { key: 'sourceAction', label: '来源动作', value: actionName },
            { key: 'attacker', label: '攻方', value: actorName },
            { key: 'target', label: '守方', value: targetName },
            { key: 'result', label: '命中结果', value: result || (missed ? 'miss' : 'hit') },
            { key: 'failureReason', label: '失败原因', value: missed ? String(event.failureReason || event.failReason || meta.failureReason || 'dodged').trim() : '' },
            { key: 'dodgeRate', label: '闪避率', value: meta.dodgeRate },
            { key: 'dodgeRoll', label: '闪避投点', value: meta.dodgeRoll },
            { key: 'grazeMultiplier', label: '擦伤倍率', value: meta.grazeMultiplier },
          ].filter(item => item.value !== undefined && item.value !== null && String(item.value).trim() !== ''),
          counterDepth: Math.max(0, Number(meta.counterDepth || 0)),
          counterRootNodeId: parent,
        };
        trace.push(node);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return node;
      }

      function 写入战斗状态检定节点(combatData = {}, event = {}, parentNodeId = '') {
        if (String(event?.eventKind || '').trim() !== 'state_apply') return null;
        const parent = String(parentNodeId || event.parentNodeId || event.sourceNodeId || '').trim();
        if (!parent) return null;
        const trace = 确保战斗判定因果链(combatData);
        const meta = event.meta && typeof event.meta === 'object' ? event.meta : {};
        const stateName = 读取事件账本状态名(event);
        const actorName = String(event.actorName || '').trim();
        const targetName = String(event.targetName || '').trim();
        const actionName = normalizeBattleActionDisplayName(event.finalActionName || event.actionName || event.meta?.finalActionName || event.meta?.actionName || '');
        if (!trace || !stateName || !targetName) return null;
        const existing = trace.find(node =>
          String(node?.nodeKind || '').trim() === 'state_check' &&
          String(node?.parentNodeId || '').trim() === parent &&
          String(node?.targetName || '').trim() === targetName &&
          String(读取结算轨迹值(node?.calculationTrace, 'stateName') || '').trim() === stateName
        );
        if (existing) return existing;
        const result = String(event.result || '').trim();
        const immune = /immune|immunity|免疫|无视异常/.test(result);
        const resisted = /resist|resisted|抵抗|豁免|未附着/.test(result);
        const isControl = /控制|眩晕|沉默|封技|定身|束缚|禁锢|僵直|击退|减速|迟缓|位移限制|skip_turn|cannot_react|silence/i.test(stateName);
        const failed = immune || resisted;
        const { actorSide, targetSide } = 推断战斗事件阵营侧(combatData, event);
        const successRateValue = Number(meta.successRate);
        const rollValue = Number(meta.roll);
        const driverAttrText = String(event.driverAttr || meta.driverAttr || '').trim();
        const successRateBreakdown = (() => {
          if (!Number.isFinite(successRateValue) || successRateValue <= 0) return '';
          const ratePct = Math.round(successRateValue <= 1 ? successRateValue * 100 : successRateValue);
          const rollPct = Number.isFinite(rollValue) && rollValue > 0 ? Math.round(rollValue <= 1 ? rollValue * 100 : rollValue) : null;
          const source = String(meta.successRateReason || meta.stateSuccessRateReason || '').trim();
          if (ratePct >= 100) return `附着成功率：100%，${source || (targetSide === actorSide ? '友方或非负面状态默认生效' : '必中/无法抵抗/非负面默认生效')}`;
          const driverPart = driverAttrText ? `，驱动属性${driverAttrText}` : '';
          const rollPart = rollPct !== null ? `，检定${rollPct}${failed ? ' > ' : ' <= '}${ratePct}${failed ? '，未通过' : '，通过'}` : '';
          return `附着成功率：最终${ratePct}%${driverPart}${source ? `，${source}` : '，基础拆分未记录'}${rollPart}`;
        })();
        if (successRateBreakdown && !meta.successRateBreakdown) meta.successRateBreakdown = successRateBreakdown;
        const node = {
          nodeId: String(生成战斗运行时应用ID('battle-trace-state-check')).trim(),
          parentNodeId: parent,
          round: Number(event.round || event.sourceRound || 0),
          phase: 'action',
          nodeKind: 'state_check',
          nodeLayer: 'system_check',
          actorName,
          actorSide,
          targetName,
          targetSide,
          targetId: String(event.targetId || '').trim(),
          targetScope: String(event.targetScope || meta.targetScope || '').trim() || 'single',
          initialActionName: actionName,
          finalActionName: actionName,
          discardedActionName: '',
          source: 'state_check',
          result: result || (immune ? 'immune' : (resisted ? 'resisted' : 'applied')),
          primaryOutcome: immune ? (isControl ? 'control_immune' : 'state_immune') : (isControl ? (resisted ? 'control_resisted' : 'control_applied') : (resisted ? 'state_resisted' : 'state_applied')),
          failureReason: failed ? String(event.failureReason || event.failReason || meta.failureReason || meta.reason || (immune ? 'state_immune' : 'state_resisted')).trim() : '',
          reasonCode: failed ? 标准化战斗ReasonCode(event.reasonCode || meta.reasonCode || 'REACTION_SUCCEEDED', 'REACTION_SUCCEEDED') : 'ACTION_COMMITTED',
          reasonText: failed ? String(event.reasonText || meta.reasonText || (immune ? '目标免疫本次状态附着' : '目标抵住本次状态附着')).trim() : '状态附着检定通过，进入状态结算',
          replanReasonCode: '',
          replanReasonText: '',
          ledgerEventIds: [String(event.eventId || '').trim()].filter(Boolean),
          calculationTrace: [
            { key: 'sourceAction', label: '来源动作', value: actionName },
            { key: 'attacker', label: '施加方', value: actorName },
            { key: 'target', label: '目标', value: targetName },
            { key: 'stateName', label: '状态', value: stateName },
            { key: 'result', label: '附着结果', value: result || (immune ? 'immune' : (resisted ? 'resisted' : 'applied')) },
            { key: 'successRate', label: '附着成功率', value: meta.successRate },
            { key: 'roll', label: '附着投点', value: meta.roll },
            { key: 'driverAttr', label: '驱动属性', value: driverAttrText },
            { key: 'successRateBreakdown', label: '成功率来源', value: successRateBreakdown },
          ].filter(item => item.value !== undefined && item.value !== null && String(item.value).trim() !== ''),
          counterDepth: Math.max(0, Number(meta.counterDepth || 0)),
          counterRootNodeId: parent,
        };
        trace.push(node);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return node;
      }

      function 写入行动轴初始意图节点(combatData = {}, entry = {}, target = null, action = null, timingBucket = '') {
        const actor = entry?.char || null;
        const actorName = String(actor?.name || actor?.名称 || '').trim();
        const actionName = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.action_type || action?.type || '行动');
        if (!actorName || !actionName) return null;
        const trace = 确保战斗判定因果链(combatData);
        const round = Number(combatData?.回合 || 0);
        const targetName = String(target?.name || target?.名称 || action?.target_name || '').trim();
        const actorSide = 标准化战斗阵营侧(entry?.side || (读取规划单位阵营(actor, combatData) === '敌方' ? 'enemy' : 'player'));
        const targetSide = targetName
          ? (读取规划单位阵营(target, combatData) === '敌方' ? 'enemy' : 'player')
          : actorSide;
        const existing = trace.find(node =>
          String(node?.nodeKind || '').trim() === 'initial_intent' &&
          Number(node?.round || 0) === round &&
          String(node?.actorName || '').trim() === actorName &&
          normalizeBattleActionDisplayName(node?.initialActionName || '') === actionName &&
          String(node?.targetName || '').trim() === targetName
        );
        if (existing) return existing;
        const node = {
          nodeId: String(生成战斗运行时应用ID('battle-trace-initial-intent')).trim(),
          parentNodeId: '',
          round,
          phase: 'action_planning',
          nodeKind: 'initial_intent',
          nodeLayer: 'intent',
          actorName,
          actorSide,
          targetName,
          targetSide,
          targetId: String(target?.id || target?.key || '').trim(),
          targetScope: 推断战斗行动目标范围(action) || (targetName ? 'single' : 'self'),
          initialActionName: actionName,
          finalActionName: '',
          discardedActionName: '',
          source: 'action_queue',
          result: 'planned',
          primaryOutcome: 'action_planned',
          failureReason: '',
          reasonCode: 'ACTION_COMMITTED',
          reasonText: '行动轴初始意图声明',
          replanReasonCode: '',
          replanReasonText: '',
          ledgerEventIds: [],
          calculationTrace: [
            { key: 'actorSide', label: '阵营', value: String(entry?.side || '').trim() },
            { key: 'targetName', label: '目标', value: targetName },
            { key: 'plannedAction', label: '初始意图', value: actionName },
            { key: 'castTime', label: '前摇', value: Math.max(0, Number(action?.cast_time ?? action?.skill?.前摇 ?? 0)) },
            { key: 'timingBucket', label: '行动窗口', value: String(timingBucket || '').trim() },
          ].filter(item => String(item.value ?? '').trim()),
          counterDepth: 0,
          counterRootNodeId: '',
        };
        trace.push(node);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return node;
      }

      function 查找行动轴初始意图节点(combatData = {}, event = {}) {
        const trace = 确保战斗判定因果链(combatData?.__父级战斗数据 || combatData);
        const round = Number(event?.round || 0);
        const actorName = String(event?.actorName || '').trim();
        if (!(round > 0) || !actorName) return null;
        const actionName = normalizeBattleActionDisplayName(event?.initialActionName || event?.actionName || event?.sourceActionName || '');
        const targetName = String(event?.targetName || '').trim();
        const candidates = trace.filter(node =>
          String(node?.nodeKind || '').trim() === 'initial_intent' &&
          Number(node?.round || 0) === round &&
          String(node?.actorName || '').trim() === actorName
        );
        if (!candidates.length) return null;
        return candidates.find(node =>
          (!actionName || normalizeBattleActionDisplayName(node?.initialActionName || '') === actionName) &&
          (!targetName || !String(node?.targetName || '').trim() || String(node?.targetName || '').trim() === targetName)
        ) || candidates.find(node =>
          !targetName || !String(node?.targetName || '').trim() || String(node?.targetName || '').trim() === targetName
        ) || candidates[0];
      }

      function 构建事件最小结算轨迹(event = {}) {
        const kind = String(event?.eventKind || '').trim();
        const meta = event?.meta && typeof event.meta === 'object' ? event.meta : {};
        const 事件自身动作名 = normalizeBattleActionDisplayName(
          event.finalActionName || event.actionName || meta.finalActionName || meta.actionName || event.skillName || meta.skillName || '',
        );
        const 事件来源动作名 = normalizeBattleActionDisplayName(event.sourceActionName || meta.sourceActionName || '');
        const 结算来源动作名 = ['state_tick', 'state_replace', 'state_remove'].includes(kind)
          ? (事件自身动作名 || 事件来源动作名)
          : 事件自身动作名;
        if (['dodge', 'defend', 'pass'].includes(kind)) {
          const actionName = 事件自身动作名 || '应招';
          const sourceAction = 事件来源动作名;
          const reactionTrace = meta.reactionTrace && typeof meta.reactionTrace === 'object' ? meta.reactionTrace : {};
          const trace = [
            { key: 'reactor', label: '应招方', value: String(event.actorName || '').trim() },
            { key: 'sourceActor', label: '攻势来源', value: String(event.targetName || '').trim() },
            { key: 'sourceAction', label: '来源动作', value: sourceAction },
            { key: 'reactionActorName', label: '反应方', value: String(reactionTrace.reactionActorName || event.actorName || '').trim() },
            { key: 'sourceActorName', label: '攻方', value: String(reactionTrace.sourceActorName || event.targetName || '').trim() },
            { key: 'reactionRole', label: '反应职责', value: String(reactionTrace.reactionRole || '').trim() },
            { key: 'reactionOutcome', label: '反应结果', value: String(reactionTrace.reactionOutcome || event.result || '').trim() },
            { key: 'initialReaction', label: '初始反应', value: normalizeBattleActionDisplayName(event.initialActionName || meta.initialActionName || actionName) },
            { key: 'finalReaction', label: '最终反应', value: normalizeBattleActionDisplayName(event.finalActionName || meta.finalActionName || actionName) },
            { key: 'reactionKind', label: '反应类型', value: String(meta.reactionType || event.actionType || kind).trim() },
            { key: 'result', label: '反应结果', value: String(event.result || '').trim() || (kind === 'pass' ? 'reaction_failed' : 'attempted') },
            { key: 'reasonCode', label: '原因枚举', value: 标准化战斗ReasonCode(event.reasonCode || meta.reasonCode || '', 推断战斗默认ReasonCode(event, kind === 'pass' ? 'reaction_failed' : '')) },
          ];
          [
            ['reactionRatio', '反应比值'],
            ['reactionValue', '反应值'],
            ['sourceActionSpeed', '攻方速度'],
            ['castTimeGap', '前摇差'],
            ['attackerCastTime', '攻方前摇'],
            ['reactorCastTime', '应招前摇'],
            ['threatScore', '威胁评分'],
            ['attackerSpeed', '攻方出手速度'],
            ['defenderReaction', '防守反应值'],
            ['reactionAgility', '应招敏捷'],
            ['reactionMental', '应招精神'],
            ['sourceAgility', '攻方敏捷'],
            ['castPenalty', '前摇速度惩罚'],
            ['attackerAgility', '攻方敏捷'],
            ['defenderAgility', '防守方敏捷'],
            ['defenderMentalMax', '防守方精神上限'],
            ['attackerSpeedBonus', '攻方速度加值'],
            ['castSpeedBonus', '前摇加速'],
            ['castSpeedPenalty', '前摇减速'],
            ['defenderReactionBonus', '防守反应加值'],
            ['defenderReactionPenalty', '防守反应惩罚'],
            ['defenderAgilityMult', '敏捷倍率'],
            ['maintainReactionPenalty', '维持惩罚'],
            ['reactionBudget', '反应预算'],
            ['dodgeRate', '闪避率'],
            ['dodgeRoll', '闪避投点'],
            ['actualDefense', '有效防御'],
            ['defenseThreshold', '破防阈值'],
          ].forEach(([key, label]) => {
            const raw = reactionTrace[key] ?? meta[key];
            if (raw !== undefined) trace.push({ key, label, value: Number(raw || 0) });
          });
          if (String(meta.replanReasonCode || '').trim()) trace.push({ key: 'replanReasonCode', label: '变招原因', value: 标准化战斗ReasonCode(meta.replanReasonCode, 'TACTICAL_DISADVANTAGE') });
          if (String(meta.reactionLog || '').trim()) trace.push({ key: 'reactionLog', label: '反应记录', value: String(meta.reactionLog || '').trim() });
          return trace.filter(item => item.value !== undefined && item.value !== null && String(item.value).trim() !== '');
        }
        if (kind === 'hit_result') {
          const finalDamage = Math.max(0, 读取事件账本数值(event, 'damage'));
          const incomingDamage = Math.max(0, Number(meta.incomingDamage || finalDamage || 0));
          const defenseThreshold = Math.max(0, Number(meta.defenseThreshold || 0));
          const formulaTrace = meta.formulaTrace && typeof meta.formulaTrace === 'object' ? meta.formulaTrace : meta;
          const trace = [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'attacker', label: '攻方', value: String(event.actorName || '').trim() },
            { key: 'target', label: '守方', value: String(event.targetName || '').trim() },
            { key: 'result', label: '命中结果', value: String(event.result || '').trim() || 'hit' },
            { key: 'failureReason', label: '失败原因', value: String(event.failureReason || event.failReason || meta.failureReason || '').trim() },
            { key: 'incomingDamage', label: '基础/入参伤害', value: Math.round(incomingDamage) },
            { key: 'defenseThreshold', label: '防御/破防阈值', value: Math.round(defenseThreshold) },
            { key: 'shieldAbsorb', label: '护盾吸收', value: Math.max(0, Number(meta.shieldAbsorb || 0)) },
            { key: 'finalDamage', label: '最终伤害', value: Math.round(finalDamage) },
          ];
          if (meta.reactiveDamage !== undefined) trace.push({ key: 'reactiveDamage', label: '反应后伤害', value: Math.max(0, Math.round(Number(meta.reactiveDamage || 0))) });
          if (String(meta.breakType || '').trim()) trace.push({ key: 'breakType', label: '破防结果', value: String(meta.breakType || '').trim() });
          if (meta.dodgeRate !== undefined) trace.push({ key: 'dodgeRate', label: '闪避率', value: Math.round(Number(meta.dodgeRate || 0)) });
          if (meta.dodgeRoll !== undefined) trace.push({ key: 'dodgeRoll', label: '闪避投点', value: Math.round(Number(meta.dodgeRoll || 0)) });
          [
            ['segmentIndex', '伤害段序号'],
            ['segmentCount', '伤害段数'],
            ['actualDefense', '有效防御'],
            ['defenseStrip', '防御剥夺'],
            ['spiritResistStrip', '精神抗性剥夺'],
            ['soulDriveScale', '魂力驱动倍率'],
            ['spiritDriveScale', '精神驱动倍率'],
            ['positionDamageScale', '定位倍率'],
            ['costDamageScale', '消耗加成'],
            ['fluctuation', '波动倍率'],
            ['grazeMultiplier', '擦伤倍率'],
            ['damageReduction', '减伤倍率'],
            ['jadeHandReduction', '玄玉手减免'],
            ['receivedDamageMult', '承伤倍率'],
            ['elementDamageMult', '元素承伤倍率'],
            ['finalDamageMult', '最终伤害倍率'],
            ['finalDamageBonus', '最终伤害加值'],
            ['activeReactionShield', '主动反应护盾'],
          ].forEach(([key, label]) => {
            if (meta[key] !== undefined) trace.push({ key, label, value: Number(meta[key] || 0) });
          });
          [
            ['skillPower', '威力倍率'],
            ['attackValue', '公式攻势值'],
            ['defenseValue', '公式防守值'],
            ['baseDamage', '基础公式伤害'],
            ['meleeContactScale', '近身接战系数'],
            ['fusionDamageMult', '融合技伤害倍率'],
          ].forEach(([key, label]) => {
            if (formulaTrace[key] !== undefined) trace.push({ key, label, value: Number(formulaTrace[key] || 0) });
          });
          if (String(formulaTrace.damageType || '').trim()) trace.push({ key: 'damageType', label: '伤害类型', value: String(formulaTrace.damageType || '').trim() });
          if (String(formulaTrace.formulaText || '').trim()) trace.push({ key: 'baseFormulaText', label: '基础公式', value: String(formulaTrace.formulaText || '').trim() });
          return trace;
        }
        if (kind === 'state_apply') {
          const trace = [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'attacker', label: '施加方', value: String(event.actorName || '').trim() },
            { key: 'target', label: '目标', value: String(event.targetName || '').trim() },
            { key: 'stateName', label: '状态', value: 读取事件账本状态名(event) },
            { key: 'result', label: '附着结果', value: String(event.result || '').trim() || 'applied' },
            { key: 'duration', label: '持续回合', value: Math.max(0, Number(event.duration || 0)) },
          ];
          if (meta.successRate !== undefined) trace.push({ key: 'successRate', label: '附着成功率', value: Math.round(Number(meta.successRate || 0) * 100) });
          if (meta.roll !== undefined) trace.push({ key: 'roll', label: '附着投点', value: Math.round(Number(meta.roll || 0) * 100) });
          const driverAttr = String(event.driverAttr || meta.driverAttr || '').trim();
          if (driverAttr) trace.push({ key: 'driverAttr', label: '驱动属性', value: driverAttr });
          return trace;
        }
        if (kind === 'state_tick') {
          return [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'target', label: '目标', value: String(event.targetName || '').trim() },
            { key: 'stateName', label: '状态', value: 读取事件账本状态名(event) },
            { key: 'result', label: '结算结果', value: String(event.result || '').trim() || 'tick' },
            { key: 'amount', label: '结算数值', value: Math.max(0, Number(event?.meta?.amount ?? event?.amount ?? 0)) },
            { key: 'resource', label: '结算资源', value: String(event?.meta?.resource || '生命值').trim() },
          ];
        }
        if (kind === 'state_replace' || kind === 'state_remove') {
          const trace = [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'actor', label: '来源方', value: String(event.actorName || '').trim() },
            { key: 'target', label: '目标', value: String(event.targetName || '').trim() },
            { key: 'stateName', label: '状态', value: String(meta.stateName || event.stateName || 读取事件账本状态名(event) || '').trim() },
            { key: 'stackMode', label: '叠加规则', value: String(meta.stackMode || '').trim() },
            { key: 'previousDuration', label: '原持续', value: Math.max(0, Number(meta.previousDuration || 0)) },
            { key: 'nextDuration', label: '新持续', value: Math.max(0, Number(meta.nextDuration ?? event.duration ?? 0)) },
            { key: 'result', label: '结算结果', value: String(event.result || '').trim() || kind },
          ];
          if (String(meta.replaceReason || '').trim()) trace.push({ key: 'replaceReason', label: '变更原因', value: String(meta.replaceReason || '').trim() });
          return trace.filter(item => item.value !== undefined && item.value !== null && String(item.value).trim() !== '');
        }
        if (kind === 'counter') {
          const damage = Math.max(0, Math.round(读取事件账本数值(event, 'damage')));
          return [
            { key: 'sourceAction', label: '来源动作', value: 事件自身动作名 },
            { key: 'counteredAction', label: '被反制动作', value: 事件来源动作名 },
            { key: 'attacker', label: '反击方', value: String(event.actorName || '').trim() },
            { key: 'target', label: '目标', value: String(event.targetName || '').trim() },
            { key: 'result', label: '反击结果', value: String(event.result || '').trim() || (damage > 0 ? 'hit' : 'no_effect') },
            { key: 'failureReason', label: '失败原因', value: String(event.failureReason || event.failReason || meta.failureReason || '').trim() },
            { key: 'counterDepth', label: '防反层级', value: Math.max(0, Number(meta.counterDepth || 0)) },
            { key: 'finalDamage', label: '最终伤害', value: damage },
          ];
        }
        if (kind === 'resource_change') {
          const delta = Number(meta.delta ?? meta.amount ?? event.delta ?? event.amount ?? 0);
          return [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'actor', label: '来源方', value: String(event.actorName || '').trim() },
            { key: 'target', label: '目标', value: String(event.targetName || event.actorName || '').trim() },
            { key: 'resource', label: '资源', value: String(meta.resource || event.resource || '').trim() },
            { key: 'delta', label: '变化量', value: Math.round(delta) },
            { key: 'result', label: '结算结果', value: String(event.result || '').trim() || (delta >= 0 ? 'gain' : 'loss') },
          ];
        }
        if (kind === 'shield_create') {
          return [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'actor', label: '来源方', value: String(event.actorName || '').trim() },
            { key: 'target', label: '目标', value: String(event.targetName || event.actorName || '').trim() },
            { key: 'shieldValue', label: '护盾值', value: Math.max(0, Math.round(Number(meta.shieldValue ?? meta.amount ?? event.amount ?? event.damage ?? 0))) },
            { key: 'result', label: '结算结果', value: String(event.result || '').trim() || 'created' },
          ];
        }
        if (kind === 'summon_create') {
          return [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'actor', label: '召唤者', value: String(event.actorName || '').trim() },
            { key: 'summonName', label: '召唤物', value: String(event.summonName || event.createdName || meta.summonName || '').trim() },
            { key: 'summonType', label: '召唤类型', value: String(event.summonType || meta.summonType || '').trim() },
            { key: 'summonMode', label: '行动模式', value: String(event.summonMode || meta.summonMode || '').trim() },
            { key: 'mentalLoad', label: '精神负载', value: Math.max(0, Math.round(Number(event.mentalLoad ?? meta.mentalLoad ?? 0))) },
            { key: 'result', label: '结算结果', value: String(event.result || '').trim() || 'created' },
          ];
        }
        if (kind === 'create') {
          return [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'actor', label: '来源方', value: String(event.actorName || '').trim() },
            { key: 'createdName', label: '造物', value: String(event.createdName || meta.createdName || event.targetName || '').trim() },
            { key: 'createdType', label: '造物类型', value: String(event.createdType || meta.createdType || event.actionType || '').trim() },
            { key: 'result', label: '结算结果', value: String(event.result || '').trim() || 'created' },
          ];
        }
        if (['blocked_action', 'failed_action', 'target_fail'].includes(kind)) {
          const trace = [
            { key: 'sourceAction', label: '来源动作', value: 结算来源动作名 },
            { key: 'actor', label: '行动者', value: String(event.actorName || '').trim() },
            { key: 'target', label: '目标', value: String(event.targetName || '').trim() },
            { key: 'result', label: '结算结果', value: String(event.result || '').trim() || 'failed' },
            { key: 'failureReason', label: '失败原因', value: String(event.failureReason || event.failReason || meta.failureReason || '').trim() },
            { key: 'reasonCode', label: '原因枚举', value: 标准化战斗ReasonCode(event.reasonCode || meta.reasonCode || '', 推断战斗默认ReasonCode(event, 'interrupted')) },
          ];
          if (String(event.actionType || '').trim() === 'summon_control') {
            [
              ['summonName', '召唤物', String(meta.summonName || event.actorName || '').trim()],
              ['summonType', '召唤类型', String(meta.summonType || '').trim()],
              ['summonMode', '行动模式', String(meta.summonMode || '').trim()],
              ['summonHostName', '宿主', String(meta.summonHostName || event.targetName || '').trim()],
              ['mentalLoad', '精神负载', Math.max(0, Math.round(Number(meta.mentalLoad || 0)))],
              ['totalMentalLoad', '总精神负载', Math.max(0, Math.round(Number(meta.totalMentalLoad || 0)))],
              ['mentalLimit', '精神控制上限', Math.max(0, Math.round(Number(meta.mentalLimit || 0)))],
              ['maintainRatio', '精神维持率', Number(Number(meta.maintainRatio || 0).toFixed(3))],
              ['compression', '超载压缩', Number(Number(meta.compression || 0).toFixed(3))],
              ['restriction', '限制结果', String(meta.restriction || event.result || '').trim()],
            ].forEach(([key, label, value]) => {
              if (value === '' || value === null || value === undefined) return;
              trace.push({ key, label, value });
            });
          }
          return trace;
        }
        return [];
      }

      function 写入战斗因果链节点(combatData = {}, event = {}, refs = {}) {
        const trace = 确保战斗判定因果链(combatData);
        if (!trace) return null;
        const config = 推断战斗因果节点配置(event);
        const sourceNodeId = String(event.sourceNodeId || refs.matchedSourceAction?.chainNodeId || refs.matchedAction?.chainNodeId || '').trim();
        const parentNodeId = String(event.parentNodeId || sourceNodeId || '').trim();
        const nodeId = String(event.chainNodeId || 生成战斗运行时应用ID(`battle-trace-${config.nodeKind || 'node'}`)).trim();
        const finalActionName = normalizeBattleActionDisplayName(event.finalActionName || event.actionName || event.meta?.finalActionName || event.meta?.actionName || '');
        const initialActionName = normalizeBattleActionDisplayName(event.initialActionName || event.meta?.initialActionName || event.actionName || event.meta?.actionName || finalActionName);
        const calculationTrace = Array.isArray(event.meta?.settlementTrace)
          ? event.meta.settlementTrace
          : 构建事件最小结算轨迹(event);
        const primaryOutcome = String(event.primaryOutcome || event.meta?.primaryOutcome || config.primaryOutcome || '').trim();
        const defaultReasonCode = 推断战斗默认ReasonCode(event, primaryOutcome);
        const { actorSide, targetSide } = 推断战斗事件阵营侧(combatData, event);
        const node = {
          nodeId,
          parentNodeId,
          round: Number(event.round || event.sourceRound || 0),
          phase: ['state_tick', 'round_recover'].includes(String(event.eventKind || '').trim()) ? 'round_end' : 'action',
          nodeKind: config.nodeKind,
          nodeLayer: config.nodeLayer,
          actorName: String(event.actorName || '').trim(),
          actorSide,
          targetName: String(event.targetName || '').trim(),
          targetSide,
          targetId: String(event.targetId || '').trim(),
          targetScope: String(event.targetScope || event.meta?.targetScope || '').trim() || (event.targetName ? 'single' : ''),
          initialActionName,
          finalActionName,
          discardedActionName: normalizeBattleActionDisplayName(event.discardedActionName || event.meta?.discardedActionName || ''),
          source: String(event.meta?.source || event.actionType || event.eventKind || '').trim(),
          result: String(event.result || '').trim(),
          primaryOutcome,
          failureReason: String(event.failureReason || event.failReason || event.meta?.failureReason || '').trim(),
          reasonCode: 标准化战斗ReasonCode(event.reasonCode || event.meta?.reasonCode || '', defaultReasonCode || (event.failReason ? 'LEGACY_CUSTOM_REASON' : '')),
          reasonText: String(event.reasonText || event.meta?.reasonText || '').trim(),
          replanReasonCode: 标准化战斗ReasonCode(event.replanReasonCode || event.meta?.replanReasonCode || '', ''),
          replanReasonText: String(event.replanReasonText || event.meta?.replanReasonText || '').trim(),
          ledgerEventIds: [String(event.eventId || '').trim()].filter(Boolean),
          calculationTrace,
          counterDepth: Math.max(0, Number(event.meta?.counterDepth || 0)),
          counterRootNodeId: String(event.meta?.counterRootNodeId || parentNodeId || '').trim(),
        };
        trace.push(node);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return node;
      }

      function 读取状态Tick聚合种类(event = {}) {
        const result = String(event?.result || '').trim();
        const resource = String(event?.meta?.resource || '生命值').trim();
        if (/魂力|精神力|体力|资源/.test(resource)) return 'resource_tick';
        if (/恢复|heal|hot/i.test(result)) return 'heal_tick';
        return 'state_tick';
      }

      function 同步回合末状态聚合节点(combatData = {}, event = {}, traceNode = null) {
        const kind = String(event?.eventKind || '').trim();
        if (kind !== 'state_tick') return null;
        const trace = 确保战斗判定因果链(combatData?.__父级战斗数据 || combatData);
        if (!Array.isArray(trace) || !traceNode?.nodeId) return null;
        const round = Number(event.round || event.sourceRound || 0);
        const stateName = 读取事件账本状态名(event);
        const aggregateKind = 读取状态Tick聚合种类(event);
        if (!(round > 0) || !stateName) return null;
        const tickNodes = trace.filter(node =>
          node &&
          String(node.nodeKind || '').trim() === 'state_settlement' &&
          String(node.primaryOutcome || '').trim() === 'state_tick' &&
          Number(node.round || 0) === round &&
          String(读取结算轨迹值(node.calculationTrace, 'stateName') || '').trim() === stateName &&
          读取状态Tick聚合种类({ result: node.result, meta: { resource: 读取结算轨迹值(node.calculationTrace, 'resource') || '生命值' } }) === aggregateKind
        );
        if (tickNodes.length < 2) return null;
        const nodeId = `battle-trace-aggregation-${round}-${aggregateKind}-${stateName}`.replace(/\s+/g, '-');
        const childNodeIds = tickNodes.map(node => String(node.nodeId || '').trim()).filter(Boolean);
        const ledgerEventIds = tickNodes.flatMap(node => Array.isArray(node.ledgerEventIds) ? node.ledgerEventIds : []).filter(Boolean);
        const totalAmount = tickNodes.reduce((sum, node) => sum + Math.max(0, Number(读取结算轨迹值(node.calculationTrace, 'amount') || 0)), 0);
        const existing = trace.find(node => String(node?.nodeId || '').trim() === nodeId);
        const payload = {
          nodeId,
          parentNodeId: '',
          round,
          phase: 'round_end',
          nodeKind: 'aggregation',
          nodeLayer: 'presentation',
          actorName: '',
          actorSide: '',
          targetName: '',
          targetSide: '',
          targetId: '',
          targetScope: 'all_units',
          initialActionName: '',
          finalActionName: '',
          discardedActionName: '',
          source: 'trace_projection',
          result: 'aggregated',
          primaryOutcome: aggregateKind,
          failureReason: '',
          reasonCode: '',
          reasonText: '',
          replanReasonCode: '',
          replanReasonText: '',
          aggregateKind,
          stateName,
          childNodeIds,
          ledgerEventIds,
          calculationTrace: [
            { key: 'aggregateKind', label: '聚合类型', value: aggregateKind },
            { key: 'stateName', label: '状态', value: stateName },
            { key: 'childCount', label: '子结算数', value: childNodeIds.length },
            { key: 'totalAmount', label: '合计数值', value: Math.round(totalAmount) },
          ],
          counterDepth: 0,
          counterRootNodeId: '',
        };
        if (existing) Object.assign(existing, payload);
        else trace.push(payload);
        if (trace.length > 1000) trace.splice(0, trace.length - 1000);
        return existing || payload;
      }
      function 查找最近账本动作事件(ledger = [], criteria = {}) {
        const round = Number(criteria.round || 0);
        const actorName = String(criteria.actorName || '').trim();
        const actionName = normalizeBattleActionDisplayName(criteria.actionName || '');
        if (!(round > 0) || !actorName || !actionName) return null;
        for (let index = (Array.isArray(ledger) ? ledger.length : 0) - 1; index >= 0; index -= 1) {
          const event = ledger[index];
          if (!event || String(event.eventKind || '').trim() !== 'action_start') continue;
          if (Number(event.round || 0) !== round) continue;
          if (!isSameBattleReportName(String(event.actorName || '').trim(), actorName)) continue;
          if (normalizeBattleActionDisplayName(event.actionName || '') !== actionName) continue;
          return event;
        }
        return null;
      }

      function 写入战斗事件账本(combatData = {}, payload = {}) {
        const rootData = combatData?.__父级战斗数据 || combatData;
        const ledger = 确保战斗事件账本(rootData);
        const eventKind = String(payload.eventKind || '').trim();
        const round = Number(payload.round || combatData?.回合 || 0);
        const actorName = String(payload.actorName || '').trim();
        const targetName = String(payload.targetName || '').trim();
        const actionName = normalizeBattleActionDisplayName(payload.actionName || '');
        const sourceActionName = normalizeBattleActionDisplayName(payload.sourceActionName || '');
        const sourceRound = Number(payload.sourceRound || round || 0);
        const matchedAction = eventKind === 'action_start' || eventKind === 'counter' || !actionName
          ? null
          : 查找最近账本动作事件(ledger, { round, actorName, actionName });
        const closedActionKinds = new Set(['hit_result', 'state_apply', 'resource_change', 'create', 'summon_create', 'summon_assist', 'shield_create', 'blocked_action', 'failed_action', 'target_fail', 'blocked_settlement', 'counter_window']);
        const sourceActorName = eventKind === 'counter' || eventKind === 'counter_window'
          ? targetName
          : (['defend', 'dodge', 'pass'].includes(eventKind) ? targetName : actorName);
        const matchedSourceAction = sourceActionName
          ? 查找最近账本动作事件(ledger, {
              round: sourceRound,
              actorName: sourceActorName,
              actionName: sourceActionName,
            })
          : null;
        const matchedInitialIntent = eventKind === 'action_start'
          ? 查找行动轴初始意图节点(combatData?.__父级战斗数据 || combatData, { ...payload, round, actorName, targetName, actionName })
          : null;
        const actionId = String(
          payload.actionId ||
          matchedAction?.actionId ||
          (eventKind === 'action_start' || eventKind === 'counter' || closedActionKinds.has(eventKind)
            ? 生成战斗动作事实ID(eventKind === 'counter' ? 'battle-counter-action' : 'battle-action')
            : '')
        ).trim();
        const sourceActionId = String(
          payload.sourceActionId ||
          matchedSourceAction?.actionId ||
          (eventKind !== 'action_start' && eventKind !== 'counter' ? matchedAction?.actionId : '') ||
          ''
        ).trim();
        const eventMeta = payload.meta && typeof payload.meta === 'object' ? { ...payload.meta } : {};
        const inferredPrimaryOutcome = String(payload.primaryOutcome || eventMeta.primaryOutcome || 读取战报事件Outcome({ ...payload, meta: eventMeta }) || '').trim();
        const inferredAppliedDamage = (() => {
          const raw = Number(payload.appliedDamage ?? payload.damage ?? eventMeta.appliedDamage ?? eventMeta.damage ?? (eventKind === 'state_tick' ? eventMeta.amount : 0) ?? 0);
          return Number.isFinite(raw) ? Math.max(0, Math.round(Math.abs(raw))) : 0;
        })();
        if (inferredPrimaryOutcome) eventMeta.primaryOutcome = inferredPrimaryOutcome;
        if (eventKind === 'hit_result' || eventKind === 'counter' || eventKind === 'state_tick') {
          eventMeta.appliedDamage = inferredAppliedDamage;
          if (eventMeta.damage === undefined && inferredAppliedDamage > 0) eventMeta.damage = inferredAppliedDamage;
        }
        if (eventKind === 'hit_result' && inferredAppliedDamage > 0) {
          const formula = eventMeta.formulaTrace && typeof eventMeta.formulaTrace === 'object' ? eventMeta.formulaTrace : eventMeta;
          const attackValue = Number(formula.attackValue || eventMeta.formulaAttackValue || 0);
          const defenseValue = Number(formula.defenseValue || eventMeta.formulaDefenseValue || eventMeta.actualDefense || 0);
          const missingRatioOperand = !(attackValue > 0) || !(defenseValue > 0);
          if (missingRatioOperand) {
            const runtime = 确保战斗运行态(rootData);
            runtime.attackDefenseRatioMissingOperandCount = Number(runtime.attackDefenseRatioMissingOperandCount || 0) + 1;
            eventMeta.attackDefenseRatioAudit = {
              missingOperand: true,
              attackValue: Number.isFinite(attackValue) ? attackValue : 0,
              defenseValue: Number.isFinite(defenseValue) ? defenseValue : 0,
              ratio: null,
            };
          } else {
            const ratio = attackValue / defenseValue;
            if (!Number.isFinite(ratio) || ratio > 5 || ratio < 0.1) {
            const runtime = 确保战斗运行态(rootData);
            runtime.attackDefenseRatioOutOfRangeCount = Number(runtime.attackDefenseRatioOutOfRangeCount || 0) + 1;
            eventMeta.attackDefenseRatioAudit = {
              outOfRange: true,
              attackValue: Number.isFinite(attackValue) ? attackValue : 0,
              defenseValue: Number.isFinite(defenseValue) ? defenseValue : 0,
              ratio: Number.isFinite(ratio) ? ratio : null,
            };
            }
          }
        }
        const inferredActionStatus = (() => {
          const explicit = String(payload.actionStatus || eventMeta.actionStatus || '').trim().toUpperCase();
          if (['DECLARED', 'SELECTED', 'LOCKED', 'EXECUTING', 'COMPLETED', 'ABORTED', 'FAILED_PRECHECK'].includes(explicit)) return explicit;
          const resultText = String(payload.result || eventMeta.result || payload.failReason || eventMeta.failureReason || '').trim();
          if (eventKind === 'action_start') return 'DECLARED';
          if (eventKind === 'target_fail' || /PRECHECK|资源不足|冷却|沉默|缴械|达到上限|CAP_REACHED/.test(`${resultText} ${payload.reasonCode || eventMeta.reasonCode || ''}`)) return 'FAILED_PRECHECK';
          if (eventKind === 'blocked_settlement' || /打断|截断|中断|动作流产|target_lost|目标丢失|死亡|DEAD|INTERRUPT|ABORT/i.test(resultText)) return 'ABORTED';
          if (['hit_result', 'state_apply', 'state_tick', 'resource_change', 'create', 'summon_create', 'summon_assist', 'shield_create', 'blocked_action', 'failed_action'].includes(eventKind)) return 'COMPLETED';
          return '';
        })();
        if (inferredActionStatus) eventMeta.actionStatus = inferredActionStatus;
        const eventSides = 推断战斗事件阵营侧(rootData, {
          ...payload,
          actorName,
          targetName,
          targetPoolSide: String(payload.targetPoolSide || eventMeta.targetPoolSide || '').trim(),
          meta: eventMeta,
        });
        const event = {
          eventId: String(payload.eventId || 生成战斗运行时应用ID('battle-ledger')).trim(),
          eventKind,
          round,
          actorName,
          actorSide: eventSides.actorSide,
          targetName,
          targetSide: eventSides.targetSide,
          targetId: String(payload.targetId || payload.targetKey || payload.target_id || '').trim(),
          targetScope: String(payload.targetScope || eventMeta.targetScope || matchedSourceAction?.targetScope || matchedAction?.targetScope || '').trim() || (targetName ? 'single' : 'self'),
          actionName,
          initialActionName: normalizeBattleActionDisplayName(payload.initialActionName || eventMeta.initialActionName || actionName),
          finalActionName: normalizeBattleActionDisplayName(payload.finalActionName || eventMeta.finalActionName || actionName),
          discardedActionName: normalizeBattleActionDisplayName(payload.discardedActionName || eventMeta.discardedActionName || ''),
          actionType: String(payload.actionType || '').trim(),
          actionId,
          sourceActionName,
          sourceActionId,
          sourceRound: Number(payload.sourceRound || (sourceActionId ? sourceRound : 0)),
          chainNodeId: String(payload.chainNodeId || '').trim(),
          parentNodeId: String(payload.parentNodeId || matchedInitialIntent?.nodeId || matchedSourceAction?.chainNodeId || matchedAction?.chainNodeId || '').trim(),
          sourceNodeId: String(payload.sourceNodeId || matchedInitialIntent?.nodeId || matchedSourceAction?.chainNodeId || matchedAction?.chainNodeId || '').trim(),
          result: String(payload.result || '').trim(),
          actionStatus: inferredActionStatus,
          failReason: String(payload.failReason || '').trim(),
          primaryOutcome: inferredPrimaryOutcome,
          appliedDamage: inferredAppliedDamage,
          effectCapability: payload.effectCapability && typeof payload.effectCapability === 'object'
            ? {
                hasDamageEffect: payload.effectCapability.hasDamageEffect === true,
                effectKinds: Array.isArray(payload.effectCapability.effectKinds)
                  ? payload.effectCapability.effectKinds.map(kind => String(kind || '').trim()).filter(Boolean).slice(0, 12)
                  : [],
              }
            : null,
          targetPoolSide: String(payload.targetPoolSide || '').trim(),
          applicationId: String(payload.applicationId || '').trim(),
          duration: Math.max(0, Number(payload.duration || 0)),
          effectSummary: String(payload.effectSummary || '').trim(),
          driverAttr: String(payload.driverAttr || '').trim(),
          meta: eventMeta,
        };
        if (event.eventKind === 'counter' && String(eventMeta.counterWindowNodeId || '').trim()) {
          event.parentNodeId = String(eventMeta.counterWindowNodeId || '').trim();
        }
        if (event.eventKind === 'counter') {
          const counterActionName = normalizeBattleActionDisplayName(event.actionName || '');
          const counteredActionName = normalizeBattleActionDisplayName(event.sourceActionName || '');
          const trace = Array.isArray(event.meta.settlementTrace) && event.meta.settlementTrace.length
            ? event.meta.settlementTrace
                .map(item => item && typeof item === 'object' ? { ...item } : null)
                .filter(Boolean)
            : 构建事件最小结算轨迹(event);
          const upsertTraceItem = (key, label, value) => {
            const existing = trace.find(item => String(item?.key || '').trim() === key);
            if (existing) {
              existing.label = label;
              existing.value = value;
            } else {
              trace.push({ key, label, value });
            }
          };
          upsertTraceItem('sourceAction', '来源动作', counterActionName);
          if (counteredActionName) upsertTraceItem('counteredAction', '被反制动作', counteredActionName);
          upsertTraceItem('attacker', '反击方', actorName);
          upsertTraceItem('target', '目标', targetName);
          upsertTraceItem('finalDamage', '最终伤害', inferredAppliedDamage);
          if (trace.length > 40) {
            const redundantIndex = trace.findIndex(item => String(item?.key || '').trim() === 'counterProbability');
            if (redundantIndex >= 0) trace.splice(redundantIndex, 1);
          }
          event.meta.settlementTrace = trace;
        }
        const settlementTraceKinds = new Set(['hit_result', 'state_apply', 'state_tick', 'resource_change', 'shield_create', 'summon_create', 'create', 'blocked_action', 'failed_action', 'target_fail', 'blocked_settlement']);
        if (settlementTraceKinds.has(event.eventKind) && !Array.isArray(event.meta.settlementTrace)) {
          event.meta.settlementTrace = 构建事件最小结算轨迹(event);
        }
        if (!event.eventKind) return null;
        const sourceRootNodeId = String(event.sourceNodeId || matchedSourceAction?.chainNodeId || matchedAction?.chainNodeId || '').trim();
        const branchNode = event.eventKind !== 'action_start' && !(event.eventKind === 'counter' && String(eventMeta.counterWindowNodeId || '').trim())
          ? 写入战斗目标分支节点(combatData?.__父级战斗数据 || combatData, event, sourceRootNodeId)
          : null;
        if (branchNode) {
          event.parentNodeId = branchNode.nodeId;
          event.sourceNodeId = sourceRootNodeId;
          event.meta.targetBranchNodeId = branchNode.nodeId;
        }
        const reactionWindowNode = ['dodge', 'defend', 'pass'].includes(event.eventKind)
          ? 写入战斗反应窗口节点(combatData?.__父级战斗数据 || combatData, event, event.parentNodeId || sourceRootNodeId)
          : null;
        if (reactionWindowNode) {
          event.parentNodeId = reactionWindowNode.nodeId;
          event.sourceNodeId = sourceRootNodeId || reactionWindowNode.parentNodeId || event.sourceNodeId;
          event.meta.reactionWindowNodeId = reactionWindowNode.nodeId;
        }
        const hitCheckNode = event.eventKind === 'hit_result'
          ? 写入战斗命中检定节点(combatData?.__父级战斗数据 || combatData, event, event.parentNodeId || sourceRootNodeId)
          : null;
        if (hitCheckNode) {
          event.parentNodeId = hitCheckNode.nodeId;
          event.sourceNodeId = sourceRootNodeId || hitCheckNode.parentNodeId || event.sourceNodeId;
          event.meta.hitCheckNodeId = hitCheckNode.nodeId;
        }
        const stateCheckNode = event.eventKind === 'state_apply'
          ? 写入战斗状态检定节点(combatData?.__父级战斗数据 || combatData, event, event.parentNodeId || sourceRootNodeId)
          : null;
        if (stateCheckNode) {
          event.parentNodeId = stateCheckNode.nodeId;
          event.sourceNodeId = sourceRootNodeId || stateCheckNode.parentNodeId || event.sourceNodeId;
          event.meta.stateCheckNodeId = stateCheckNode.nodeId;
        }
        const traceNode = 写入战斗因果链节点(combatData?.__父级战斗数据 || combatData, event, { matchedAction, matchedSourceAction });
        if (traceNode) {
          event.chainNodeId = traceNode.nodeId;
          event.parentNodeId = traceNode.parentNodeId || event.parentNodeId;
          event.sourceNodeId = event.sourceNodeId || traceNode.parentNodeId || '';
          写入战斗变招决策节点(combatData?.__父级战斗数据 || combatData, event, traceNode);
          同步回合末状态聚合节点(combatData?.__父级战斗数据 || combatData, event, traceNode);
        }
        ledger.push(event);
        if (判定事件是保底资源恢复入口(event)) 写入保底资源恢复事实(combatData?.__父级战斗数据 || combatData, event);
        if (ledger.length > 800) ledger.splice(0, ledger.length - 800);
        return event;
      }

      function 记账动作消耗到事件账本(combatData = {}, round = 0, action = null, actor = null, parsedCost = null) {
        if (!combatData || !action || !actor || !parsedCost) return null;
        return 写入战斗事件账本(combatData, {
          eventKind: 'action_cost',
          round,
          actorName: actor?.name || actor?.名称 || '',
          actionName: action?.skill?.name || action?.skill?.魂技名 || action?.action_type || action?.type || '',
          actionType: action?.action_type || action?.type || '',
          result: 'paid',
          meta: {
            costText: formatParsedCost(parsedCost),
            reqSp: Number(parsedCost.reqSp || 0),
            reqVit: Number(parsedCost.reqVit || 0),
            reqMen: Number(parsedCost.reqMen || 0),
          },
        });
      }

      function 从事件账本构建行动闭环事实账本(eventLedger = []) {
        const 事件列表 = (Array.isArray(eventLedger) ? eventLedger : [])
          .map(item => item && typeof item === 'object' ? 归一战斗事件记录(item) : null)
          .filter(Boolean);
        const 账本 = {
          消耗: [],
          行动完成: [],
          护盾生成: [],
          起招: [],
          应招: [],
          命中: [],
          状态施加: [],
          状态结算: [],
          防反: [],
          闪避: [],
          擦伤: [],
          索敌失败: [],
          行动受阻: [],
          未闭合起招: [],
        };
        const 起招列表 = [];
        const 闭合键集合 = new Set();
        const 归一动作键 = event => [
          Number(event?.round || event?.sourceRound || 0),
          String(event?.actorName || '').trim(),
          normalizeBattleActionDisplayName(event?.actionName || ''),
          String(event?.targetName || '').trim(),
        ].join('|');
        const 标记闭合 = event => {
          if (!event || typeof event !== 'object') return;
          闭合键集合.add(归一动作键(event));
        };
        const 标记来袭动作闭合 = event => {
          const round = Number(event?.round || event?.sourceRound || 0);
          const defender = String(event?.actorName || '').trim();
          if (!(round > 0) || !defender) return;
          起招列表
            .filter(item => item.round === round && item.目标 && isSameBattleReportName(item.目标, defender))
            .forEach(item => 闭合键集合.add(item.__key));
        };
        事件列表.forEach(event => {
          const round = Number(event.round || event.sourceRound || 0);
          const actor = String(event.actorName || '').trim();
          const target = String(event.targetName || '').trim();
          const actionName = normalizeBattleActionDisplayName(event.actionName || '');
          const sourceActionName = normalizeBattleActionDisplayName(event.sourceActionName || '');
          const result = String(event.result || '').trim();
          const amount = Number(event?.meta?.amount ?? event?.meta?.damage ?? 0);
          const resource = String(event?.meta?.resource || '').trim();
          const stateName = String(event?.meta?.stateName || '').trim();
          switch (String(event.eventKind || '').trim()) {
            case 'action_cost':
              账本.消耗.push({
                round,
                技能: actionName,
                行动者: actor,
                明细: String(event?.meta?.costText || '').trim(),
              });
              break;
            case 'action_start':
              起招列表.push({
                round,
                行动者: actor,
                技能: actionName,
                目标: target,
                __key: 归一动作键(event),
              });
              账本.起招.push({
                round,
                行动者: actor,
                技能: actionName,
                目标: target,
              });
              break;
            case 'complete':
              标记闭合(event);
              账本.行动完成.push({ round, 行动者: actor, 技能: actionName });
              break;
            case 'shield_create':
              标记闭合(event);
              账本.护盾生成.push({ round, 目标: target || actor, 数值: amount });
              break;
            case 'create':
              标记闭合(event);
              账本.行动完成.push({ round, 行动者: actor, 技能: actionName });
              break;
            case 'dodge':
              if (/evaded|dodged|success|complete/.test(result)) 标记来袭动作闭合(event);
              标记闭合(event);
              账本.应招.push({ round, 行动者: actor, 技能: actionName, 类型: '闪避', 来源技能: sourceActionName });
              账本.闪避.push({ round, 行动者: actor, 技能: actionName, 来源技能: sourceActionName, 结果: result || 'evaded' });
              break;
            case 'defend':
            case 'pass':
            case 'support':
              标记闭合(event);
              账本.应招.push({ round, 行动者: actor, 技能: actionName, 来源技能: sourceActionName, 类型: String(event.eventKind || '').trim() });
              break;
            case 'hit_result':
              标记闭合(event);
              账本.命中.push({ round, 攻击者: actor, 目标: target, 技能: actionName, 来源技能: sourceActionName, 伤害: amount, 结果: result || 'hit' });
              if (result === 'graze') {
                账本.擦伤.push({ round, 攻击者: actor, 目标: target, 技能: actionName, 来源技能: sourceActionName, 伤害: amount });
              }
              break;
            case 'state_apply':
              标记闭合(event);
              账本.状态施加.push({
                round,
                来源角色: actor,
                目标: target,
                技能: actionName,
                来源技能: sourceActionName,
                状态: stateName,
                持续回合: Number(event.duration || 0),
              });
              break;
            case 'state_tick':
              账本.状态结算.push({
                round,
                来源角色: actor,
                目标: target,
                技能: sourceActionName,
                结算动作: actionName,
                状态: stateName,
                数值: amount,
                资源: resource,
              });
              break;
            case 'counter':
              标记闭合(event);
              账本.防反.push({
                round,
                行动者: actor,
                目标: target,
                技能: actionName,
                被反制技能: sourceActionName,
                结果: result || 'success',
                伤害: amount,
                原因: String(event.failReason || '').trim(),
              });
              break;
            case 'target_fail':
              标记闭合(event);
              账本.索敌失败.push({ round, 行动者: actor, 技能: actionName, 来源技能: sourceActionName, 文本: String(event.failReason || '').trim() });
              break;
            case 'blocked_action':
            case 'failed_action':
              标记闭合(event);
              账本.行动受阻.push({ round, 行动者: actor, 技能: actionName, 来源技能: sourceActionName, 文本: String(event.failReason || '').trim() });
              break;
            default:
              break;
          }
        });
        起招列表.forEach(item => {
          if (!闭合键集合.has(item.__key)) {
            账本.未闭合起招.push({
              round: item.round,
              行动者: item.行动者,
              技能: item.技能,
              目标: item.目标,
            });
          }
        });
        账本.计数 = Object.fromEntries(Object.keys(账本).map(key => [key, Array.isArray(账本[key]) ? 账本[key].length : 0]));
        return 账本;
      }

      function 合并战斗事件账本(runtimeLedger = [], parsedLedger = []) {
        const merged = [];
        const seen = new Set();
        [...(Array.isArray(runtimeLedger) ? runtimeLedger : []), ...(Array.isArray(parsedLedger) ? parsedLedger : [])]
          .map(item => item && typeof item === 'object' ? 归一战斗事件记录(item) : null)
          .filter(Boolean)
          .forEach(item => {
            const metaText = String(item?.meta?.text || '').trim();
            const metaAmount = Number(item?.meta?.amount ?? 0);
            const metaDamage = Number(item?.meta?.damage ?? 0);
            const metaState = String(item?.meta?.stateName || '').trim();
            const metaResource = String(item?.meta?.resource || '').trim();
            const metaBreakType = String(item?.meta?.breakType || '').trim();
            const key = [
              item.eventKind,
              item.round,
              item.actorName,
              item.targetName,
              item.actionName,
              item.actionType,
              item.sourceActionName,
              item.sourceRound,
              item.result,
              item.failReason,
              item.applicationId,
              item.duration,
              item.effectSummary,
              item.driverAttr,
              metaText,
              metaAmount,
              metaDamage,
              metaState,
              metaResource,
              metaBreakType,
            ].join('|');
            if (seen.has(key)) return;
            seen.add(key);
            merged.push(item);
          });
        return merged;
      }

      function 补齐事件账本未闭合敌对动作(eventLedger = []) {
        const events = (Array.isArray(eventLedger) ? eventLedger : [])
          .map(item => item && typeof item === 'object' ? 归一战斗事件记录(item) : null)
          .filter(Boolean);
        const 闭合事件类型 = new Set(['hit_result', 'state_apply', 'create', 'summon_create', 'shield_create', 'support', 'defend', 'dodge', 'pass', 'blocked_action', 'failed_action', 'target_fail']);
        const 闭合键集合 = new Set();
        const 闭合动作ID集合 = new Set();
        const 构建闭合键 = event => [
          Number(event?.round || event?.sourceRound || 0),
          String(event?.actorName || '').trim(),
          normalizeBattleActionDisplayName(event?.actionName || event?.finalActionName || ''),
        ].join('|');
        events.forEach(event => {
          if (!event || typeof event !== 'object') return;
          if (!闭合事件类型.has(String(event.eventKind || '').trim())) return;
          [event.actionId, event.sourceActionId].map(id => String(id || '').trim()).filter(Boolean).forEach(id => 闭合动作ID集合.add(id));
          const 动作名 = normalizeBattleActionDisplayName(event.actionName || event.finalActionName || '');
          if (动作名) 闭合键集合.add([Number(event.round || event.sourceRound || 0), String(event.actorName || '').trim(), 动作名].join('|'));
          const 来源动作名 = normalizeBattleActionDisplayName(event.sourceActionName || '');
          const 是反应闭合 = ['counter', 'dodge', 'defend', 'pass'].includes(String(event.eventKind || '').trim());
          if (是反应闭合 && 来源动作名) 闭合键集合.add([Number(event.sourceRound || event.round || 0), String(event.actorName || '').trim(), 来源动作名].join('|'));
        });
        events
          .filter(event => String(event?.eventKind || '').trim() === 'action_start')
          .forEach(event => {
            const 动作名 = normalizeBattleActionDisplayName(event.actionName || '');
            if (event.actionId && 闭合动作ID集合.has(String(event.actionId || '').trim())) return;
            const 闭合键 = [Number(event.round || 0), String(event.actorName || '').trim(), 动作名].join('|');
            if (!动作名 || 闭合键集合.has(闭合键)) return;
            const targetPoolSide = String(event.targetPoolSide || '').trim();
            const actionType = String(event.actionType || '').trim();
            if (targetPoolSide !== 'hostile' && !/攻击|魂技|普攻|常规攻击|释放/.test(actionType)) return;
            const 补写事件 = 归一战斗事件记录({
              ...event,
              eventKind: 'blocked_action',
              actionType: actionType || 'blocked_action',
              sourceActionName: 动作名,
              sourceRound: Number(event.round || 0),
              result: 'blocked',
              failReason: String(event.failReason || '本回合对抗结束，未能形成有效出手').trim(),
            });
            if (!Array.isArray(补写事件.meta?.settlementTrace)) {
              补写事件.meta = 补写事件.meta && typeof 补写事件.meta === 'object' ? 补写事件.meta : {};
              补写事件.meta.settlementTrace = 构建事件最小结算轨迹(补写事件);
            }
            events.push(补写事件);
            闭合键集合.add(闭合键);
          });
        return events;
      }

      function 写入行动闭环事实账本(combatData = {}, eventLedger = []) {
        const 诊断 = 确保行动闭环诊断(combatData);
        if (!诊断) return null;
        诊断.事实账本 = 从事件账本构建行动闭环事实账本(eventLedger);
        return 诊断.事实账本;
      }

      function 记录行动闭环审计(combatData = {}, 类型 = '', 详情 = {}) {
        const 目标战斗数据 = combatData?.__父级战斗数据 || combatData;
        const 诊断 = 确保行动闭环诊断(目标战斗数据);
        if (!诊断 || !类型) return null;
        const 计数键 = {
          主动规划: '主动规划次数',
          索敌规划: '索敌规划次数',
          辅助目标规划: '辅助目标规划次数',
          应招审计: '应招审计次数',
          再判定审计: '再判定审计次数',
          换招审计: '换招审计次数',
        }[类型] || '';
        if (计数键) 诊断[计数键] = Number(诊断[计数键] || 0) + 1;
        if (详情.旁路 === true) 诊断.规划旁路残留 = Number(诊断.规划旁路残留 || 0) + 1;
        if (详情.目标闭环缺失 === true) 诊断.目标闭环缺失 = Number(诊断.目标闭环缺失 || 0) + 1;
        if (详情.团队意图未消费 === true) 诊断.团队意图未消费 = Number(诊断.团队意图未消费 || 0) + 1;
        const 排序审计 = 详情.候选排序审计 || {};
        const 轨迹 = {
          类型,
          战斗ID: 详情.战斗ID || 目标战斗数据?.战斗ID || 目标战斗数据?.id || '',
          回合: Number(详情.回合 ?? 目标战斗数据?.回合 ?? 0),
          行动者: 详情.行动者 || '',
          目标: 详情.目标 || '',
          目标语义: 详情.目标语义 || '',
          承载方式: 详情.承载方式 || '',
          技能: 详情.技能 || '',
          actionId: String(详情.actionId || 详情.动作ID || '').trim(),
          sourceActionId: String(详情.sourceActionId || 详情.来源动作ID || '').trim(),
          战略意图: 详情.战略意图 || '',
          目标理由: Array.isArray(详情.目标理由) ? 详情.目标理由 : [],
          候选来源: 详情.候选来源 || '',
          原始净收益: Number(详情.原始净收益 || 0),
          原始权重: Number(详情.原始权重 || 0),
          战术修正: Number(详情.战术修正 || 0),
          目标修正: Number(详情.目标修正 || 0),
          对手意图修正: Number(详情.对手意图修正 || 0),
          类别预算修正: Number(详情.类别预算修正 || 0),
          防御质量修正: Number(详情.防御质量修正 || 0),
          资源修正: Number(详情.资源修正 || 0),
          连招修正: Number(详情.连招修正 || 0),
          记忆惩罚: Number(详情.记忆惩罚 || 0),
          repeatCount: Number(详情.repeatCount || 0),
          repeatCategory: String(详情.repeatCategory || ''),
          repeatDecayMultiplier: Number(详情.repeatDecayMultiplier || 1),
          repeatDecayReasonCode: String(详情.repeatDecayReasonCode || ''),
          repeatExceptionCode: String(详情.repeatExceptionCode || ''),
          repeatDecayReasonText: String(详情.repeatDecayReasonText || ''),
          scoreBeforeRepeatDecay: Number(详情.scoreBeforeRepeatDecay || 0),
          scoreAfterRepeatDecay: Number(详情.scoreAfterRepeatDecay || 0),
          团队意图修正: Number(详情.团队意图修正 || 0),
          行为多样性修正: Number(详情.行为多样性修正 || 0),
          干扰强度: Number(详情.干扰强度 || 0),
          最终权重: Number(详情.最终权重 || 0),
          选择原因: 详情.选择原因 || '',
          hitCandidateName: 详情.hitCandidateName || '',
          finalResolvedActionName: 详情.finalResolvedActionName || '',
          actionOverrideSource: 详情.actionOverrideSource || '',
          压力档位: 详情.压力档位 || '',
          目标修正理由: Array.isArray(详情.目标修正理由) ? 详情.目标修正理由 : [],
          候选排序审计: 排序审计,
          候选排序结果: Array.isArray(详情.候选排序结果) ? 详情.候选排序结果 : [],
          对手意图摘要: 详情.对手意图摘要 || 排序审计.对手适应摘要 || {},
          效果原型分类: 详情.效果原型分类 || 排序审计.效果原型分类 || {},
          分类来源: 详情.分类来源 || 排序审计.分类来源 || '',
          目标价值修正: Number(详情.目标价值修正 ?? 排序审计.目标价值修正 ?? 0),
          目标价值分解: 详情.目标价值分解 || 排序审计.目标价值分解 || {},
          二回合前瞻修正: Number(详情.二回合前瞻修正 ?? 排序审计.二回合前瞻修正 ?? 0),
          前瞻理由: Array.isArray(详情.前瞻理由) ? 详情.前瞻理由 : (排序审计.前瞻理由 || []),
          资源后果: Array.isArray(详情.资源后果) ? 详情.资源后果 : (排序审计.资源后果 || []),
          敌方下回合风险: Array.isArray(详情.敌方下回合风险) ? 详情.敌方下回合风险 : (排序审计.敌方下回合风险 || []),
          己方下回合窗口: Array.isArray(详情.己方下回合窗口) ? 详情.己方下回合窗口 : (排序审计.己方下回合窗口 || []),
          职责画像: 详情.职责画像 || 排序审计.职责画像 || {},
          职责修正: Number(详情.职责修正 ?? 排序审计.职责修正 ?? 0),
          职责理由: Array.isArray(详情.职责理由) ? 详情.职责理由 : (排序审计.职责理由 || []),
          目标切换收益: Number(详情.目标切换收益 ?? 排序审计.目标切换收益 ?? 0),
          切换成本: Number(详情.切换成本 ?? 排序审计.切换成本 ?? 0),
          保护链理由: Array.isArray(详情.保护链理由) ? 详情.保护链理由 : (排序审计.保护链理由 || []),
          仇恨压力理由: Array.isArray(详情.仇恨压力理由) ? 详情.仇恨压力理由 : (排序审计.仇恨压力理由 || []),
          协同理由: Array.isArray(详情.协同理由) ? 详情.协同理由 : (排序审计.协同理由 || []),
        };
        诊断.审计轨迹.push(轨迹);
        if (诊断.审计轨迹.length > 480) 诊断.审计轨迹.splice(0, 诊断.审计轨迹.length - 480);
        if (类型 === '主动规划') {
          const 样本 = {
            战斗ID: 轨迹.战斗ID,
            回合: 轨迹.回合,
            actor: 详情.actor快照 || { 名称: 轨迹.行动者 },
            敌方列表: Array.isArray(详情.敌方列表) ? 详情.敌方列表 : [],
            己方列表: Array.isArray(详情.己方列表) ? 详情.己方列表 : [],
            候选排序: 轨迹.候选排序结果,
            最终行动: 轨迹.技能 || 轨迹.选择原因,
            候选命中: 轨迹.hitCandidateName || '',
            最终落地动作: 轨迹.finalResolvedActionName || 轨迹.技能 || '',
            最终目标: 轨迹.目标,
            对手意图摘要: 轨迹.对手意图摘要,
            候选排序审计: 轨迹.候选排序审计,
            异常检测结果: [],
          };
          诊断.真实样本轨迹.push(样本);
          if (诊断.真实样本轨迹.length > 240) 诊断.真实样本轨迹.splice(0, 诊断.真实样本轨迹.length - 240);
          if (typeof window !== 'undefined') {
            window.__战斗真实行为审计样本 ||= [];
            window.__战斗真实行为审计样本.push(样本);
            if (window.__战斗真实行为审计样本.length > 500) window.__战斗真实行为审计样本.splice(0, window.__战斗真实行为审计样本.length - 500);
          }
        }
        return 轨迹;
      }

      function 回填行动闭环审计最终动作(combatData = {}, actor = null, patch = {}) {
        const 目标战斗数据 = combatData?.__父级战斗数据 || combatData;
        const 诊断 = 确保行动闭环诊断(目标战斗数据);
        const 行动者 = String(actor?.name || actor?.名称 || actor || '').trim();
        const 回合 = Number(patch?.round ?? 目标战斗数据?.回合 ?? 0);
        if (!诊断 || !行动者 || !(回合 > 0)) return null;
        const 最终动作 = normalizeBattleActionDisplayName(
          patch?.finalResolvedActionName ||
          patch?.技能 ||
          patch?.actionName ||
          '',
        );
        const 候选匹配动作 = trace => {
          if (!最终动作) return true;
          const names = [
            trace?.finalResolvedActionName,
            trace?.技能,
            trace?.hitCandidateName,
            ...(Array.isArray(trace?.候选排序结果) ? trace.候选排序结果.map(候选 => 候选?.candidateName || 候选?.名称 || 候选?.name || '') : []),
          ].map(name => normalizeBattleActionDisplayName(name || '')).filter(Boolean);
          return names.includes(最终动作);
        };
        const 轨迹 = [...(诊断.审计轨迹 || [])].reverse().find(item =>
          String(item?.类型 || '').trim() === '主动规划' &&
          String(item?.行动者 || '').trim() === 行动者 &&
          Number(item?.回合 || 0) === 回合 &&
          候选匹配动作(item)
        );
        if (!轨迹) return null;
        const 命中候选名 = normalizeBattleActionDisplayName(patch?.hitCandidateName || 轨迹.hitCandidateName || 轨迹.技能 || '');
        if (最终动作) {
          轨迹.finalResolvedActionName = 最终动作;
          轨迹.技能 = 最终动作;
          let 匹配候选 = false;
          const 已执行候选 = Array.isArray(轨迹.候选排序结果)
            ? 轨迹.候选排序结果.find(候选 => ['EXECUTED', 'LOCKED', 'SELECTED'].includes(String(候选?.candidateStatus || '').trim().toUpperCase()))
            : null;
          const 已执行候选名 = normalizeBattleActionDisplayName(已执行候选?.candidateName || 已执行候选?.名称 || 已执行候选?.name || '');
          const 候选执行冲突 = !!(已执行候选名 && 已执行候选名 !== 最终动作);
          if (Array.isArray(轨迹.候选排序结果)) {
            轨迹.候选排序结果 = 轨迹.候选排序结果.map(候选 => {
              const 候选名 = normalizeBattleActionDisplayName(候选?.candidateName || 候选?.名称 || 候选?.name || '');
              if (候选执行冲突) {
                if (候选名 === 最终动作) 匹配候选 = true;
                return 候选;
              }
              if (候选名 !== 最终动作) {
                const 当前候选状态 = String(候选?.candidateStatus || '').trim().toUpperCase();
                if (['EXECUTED', 'LOCKED', 'SELECTED'].includes(当前候选状态)) {
                  const filtered = Number(候选?.权重 ?? 候选?.score ?? 0) <= 0;
                  return {
                    ...候选,
                    candidateStatus: 战斗候选状态转移(当前候选状态, filtered ? 'FILTERED' : 'REJECTED'),
                    rejectionCode: filtered ? 'FILTERED_BY_SCORE' : 'LOWER_PRIORITY',
                  };
                }
                return 候选;
              }
              匹配候选 = true;
              const 当前候选状态 = String(候选?.candidateStatus || '').trim().toUpperCase();
              return {
                ...候选,
                candidateStatus: 战斗候选状态转移(当前候选状态, 'EXECUTED'),
                rejectionCode: '',
              };
            });
          }
          轨迹.actionOverrideSource = 候选执行冲突
            ? String(patch?.actionOverrideSource || 轨迹.actionOverrideSource || '候选最终动作冲突').trim()
            : 匹配候选
            ? ''
            : String(
                patch?.actionOverrideSource ||
                轨迹.actionOverrideSource ||
                '',
              ).trim();
        }
        if (patch?.目标语义 !== undefined) 轨迹.目标语义 = String(patch.目标语义 || '').trim();
        if (patch?.承载方式 !== undefined) 轨迹.承载方式 = String(patch.承载方式 || '').trim();
        if (patch?.目标 !== undefined) 轨迹.目标 = String(patch.目标 || '').trim();
        if (patch?.选择原因) 轨迹.选择原因 = String(patch.选择原因 || '').trim();
        return 轨迹;
      }

      function 按权重抽取目标(候选列表 = []) {
        const 有效候选 = (候选列表 || []).filter(候选 => 候选 && 候选.target && Number(候选.weight || 0) > 0);
        if (!有效候选.length) return null;
        let roll = Math.random() * Math.max(1, 有效候选.reduce((累计, 候选) => 累计 + Number(候选.weight || 0), 0));
        for (const 候选 of 有效候选) {
          roll -= Number(候选.weight || 0);
          if (roll <= 0) return 候选.target;
        }
        return 有效候选[0].target;
      }

      function 污染索敌候选池(actor = {}, 正常候选 = [], 错误阵营 = [], 干扰强度 = 0) {
        const 强度 = 限制行为概率(干扰强度, 0, 1);
        if (!(强度 > 0) || !(错误阵营 || []).length) return 正常候选;
        const 错误候选 = (错误阵营 || [])
          .filter(单位 => 单位 && 单位 !== actor && isCombatUnitAbleToFight(单位))
          .map(单位 => ({ target: 单位, weight: Math.max(1, Math.round(40 * 强度)), __索敌污染: true }));
        return [...正常候选, ...错误候选];
      }

      function 技能偏向友方目标(skill = {}, candidate = {}) {
        const 类型 = String(getSkillType(skill) || '').trim();
        const 文本 = [JSON.stringify(skill || {}), candidate?.name, candidate?.行动名, candidate?.分支名].map(值 => String(值 || '')).join('|');
        if (判定候选固定自身目标_V1(skill, candidate)) return true;
        return /治疗|回复|恢复|辅助|增益|护盾|护卫|护援|防御/.test(类型 + 文本) && !/伤害结算|攻击|削弱|控制/.test(类型 + 文本);
      }

      function 读取战斗双方队伍(combatData = {}, actor = {}) {
        const 玩家队 = dedupeCombatTargetList((combatData?.参战者?.team_player || []).filter(Boolean)).filter(isCombatUnitAbleToFight);
        const 敌方队 = dedupeCombatTargetList((combatData?.参战者?.team_enemy || []).filter(Boolean)).filter(isCombatUnitAbleToFight);
        const 在玩家队 = 玩家队.some(单位 => 单位 === actor || isCombatUnitIdentityMatch(单位, actor?.name || ''));
        const 在敌方队 = 敌方队.some(单位 => 单位 === actor || isCombatUnitIdentityMatch(单位, actor?.name || ''));
        if (在玩家队) return { 友方: 玩家队, 敌方: 敌方队 };
        if (在敌方队) return { 友方: 敌方队, 敌方: 玩家队 };
        return { 友方: dedupeCombatTargetList([actor]).filter(Boolean), 敌方: dedupeCombatTargetList([...玩家队, ...敌方队]).filter(单位 => 单位 && 单位 !== actor) };
      }

      function 选择受干扰目标(actor = {}, skill = null, combatData = {}, fallbackEnemyTeam = []) {
        const 双方 = 读取战斗双方队伍(combatData, actor);
        const 规划上下文 = 构建规划上下文(actor, null, combatData, { combatData });
        const 正常队伍 = 技能偏向友方目标(skill) ? 双方.友方 : (fallbackEnemyTeam?.length ? fallbackEnemyTeam : 双方.敌方);
        const 错误队伍 = 技能偏向友方目标(skill) ? 双方.敌方 : 双方.友方;
        const 正常候选 = (正常队伍 || [])
          .filter(单位 => 单位 && (单位 === actor || isCombatUnitAbleToFight(单位)))
          .map(单位 => {
            const 友方技能 = 技能偏向友方目标(skill);
            const 优先项 = 读取规划目标优先项(规划上下文, 单位, 友方技能 ? '友方' : '敌方');
            const 基础权重 = 友方技能 ? Math.round((1 - getCombatHpRatio(单位)) * 90 + 20) : scoreEnemyTargetForSkill(actor, 单位, skill || {}, combatData);
            return { target: 单位, weight: Math.max(1, Math.round(基础权重 + Number(优先项.优先级 || 0) * 0.35)), __目标理由: 优先项.理由 };
          });
        const 干扰强度 = 读取索敌干扰强度(actor, skill);
        return 按权重抽取目标(扰动索敌候选列表(污染索敌候选池(actor, 正常候选, 错误队伍, 干扰强度), 干扰强度));
      }

      function 读取索敌错误队伍(actor = {}, 正常队伍 = [], skill = null, combatData = {}) {
        const 双方 = 读取战斗双方队伍(combatData, actor);
        const 正常集合 = new Set((正常队伍 || []).filter(Boolean));
        const 反向队伍 = 技能偏向友方目标(skill) ? 双方.敌方 : 双方.友方;
        return (反向队伍 || []).filter(单位 => 单位 && !正常集合.has(单位));
      }

      function 应用行为经验误判(权重 = 0, 战斗状态 = {}) {
        const 原权重 = Number(权重 || 0);
        if (!(原权重 > 0)) return 0;
        const 稳定度 = 获取行为经验稳定度(战斗状态);
        const 回归系数 = 0.72 + 稳定度 * 0.28;
        const 平滑权重 = 45 + (原权重 - 45) * 回归系数;
        const 随机幅度 = Math.round((1 - 稳定度) * 26);
        const 误判 = 随机幅度 > 0 ? Math.floor(Math.random() * (随机幅度 * 2 + 1)) - 随机幅度 : 0;
        return Math.max(0, Math.floor(平滑权重 + 误判));
      }

      function 计算行为实力差距(强方 = {}, 弱方 = {}) {
        const 强方等级 = getCombatUnitTierNumber(强方);
        const 弱方等级 = getCombatUnitTierNumber(弱方);
        const 等级差 = Math.max(0, 强方等级 - 弱方等级) / 100;
        const 强方属性 = 强方?.final || buildCombatFinalStats(强方);
        const 弱方属性 = 弱方?.final || buildCombatFinalStats(弱方);
        const 强方总值 =
          Number(强方属性.str || 强方.str || 0) +
          Number(强方属性.def || 强方.def || 0) +
          Number(强方属性.agi || 强方.agi || 0) +
          Number(强方属性.sp_max || 强方.sp_max || 0) * 0.15 +
          Number(强方属性.men_max || 强方.men_max || 0) * 0.3;
        const 弱方总值 =
          Number(弱方属性.str || 弱方.str || 0) +
          Number(弱方属性.def || 弱方.def || 0) +
          Number(弱方属性.agi || 弱方.agi || 0) +
          Number(弱方属性.sp_max || 弱方.sp_max || 0) * 0.15 +
          Number(弱方属性.men_max || 弱方.men_max || 0) * 0.3;
        const 面板差 = Math.max(0, 强方总值 - 弱方总值) / Math.max(1, 强方总值);
        return 限制行为概率(等级差 * 0.65 + 面板差 * 0.35, 0, 1);
      }

      function 读取战斗图鉴命中(目标 = {}) {
        const 图鉴数据 = getMvuValue('world.图鉴', {}) || {};
        if (!图鉴数据 || typeof 图鉴数据 !== 'object') return null;
        const 候选键 = new Set(
          [
            目标?.name,
            目标?.名称,
            目标?.具体物种,
            目标?.标准物种,
            目标?.种族,
            目标?.单位性质,
            目标?.type,
            目标?.系别,
          ]
            .map(值 => String(值 || '').trim())
            .filter(Boolean),
        );
        if (!候选键.size) return null;
        for (const [图鉴名, 条目] of Object.entries(图鉴数据)) {
          const 图鉴键 = String(图鉴名 || '').trim();
          const 条目名称 = String(条目?.名称 || 条目?.具体物种 || 条目?.标准物种 || 条目?.种族 || '').trim();
          if (候选键.has(图鉴键) || 候选键.has(条目名称)) return 条目 || {};
        }
        return null;
      }

      function 获取魂兽年限档(年限 = 1) {
        const 数值 = Math.max(1, Math.floor(Number(年限 || 1)));
        if (数值 >= 100000) return { 名称: '十万年', 下限: 100000, 上限: 999999999 };
        if (数值 >= 10000) return { 名称: '万年', 下限: 10000, 上限: 99999 };
        if (数值 >= 1000) return { 名称: '千年', 下限: 1000, 上限: 9999 };
        if (数值 >= 100) return { 名称: '百年', 下限: 100, 上限: 999 };
        return { 名称: '十年', 下限: 1, 上限: 99 };
      }

      function 解析图鉴键与模板(目标 = {}) {
        const 单位性质 = String(目标?.单位性质 || '').trim();
        if (单位性质 === '魂兽') {
          const 具体物种 = String(目标?.具体物种 || 目标?.name || 目标?.名称 || 目标?.标准物种 || '').trim();
          if (!具体物种) return null;
          const 年限档 = 获取魂兽年限档(目标?.年限 || 目标?.属性?.年龄 || 1);
          return {
            key: `${年限档.名称}${具体物种}`,
            value: {
              类型: '魂兽',
              名称: 具体物种,
              具体物种,
              标准物种: String(目标?.标准物种 || '').trim(),
              物种品质: String(目标?.物种品质 || '普通').trim() || '普通',
              年限档: 年限档.名称,
              年限下限: 年限档.下限,
              年限上限: 年限档.上限,
              对标等级: Number(目标?.属性?.等级 || 0),
              常见系别: String(目标?.系别 || 目标?.type || '').trim(),
              标准技能: clonePersistedCombatValue(目标?.自创魂技 || {}),
            },
          };
        }
        if (单位性质 === '深渊') {
          const 标准种族 = String(目标?.标准种族 || 目标?.name || 目标?.名称 || '').trim();
          if (!标准种族) return null;
          return {
            key: 标准种族,
            value: {
              类型: '深渊',
              名称: 标准种族,
              标准种族,
              常见级别: String(目标?.级别 || '').trim(),
              对标等级: Number(目标?.属性?.等级 || 0),
              常见系别: String(目标?.系别 || 目标?.type || '').trim(),
              标准技能: clonePersistedCombatValue(目标?.自创魂技 || {}),
            },
          };
        }
        return null;
      }

      function 可识别对手实力详情(观察者 = {}, 目标 = {}, 战斗数据 = {}) {
        const 回合 = Math.max(0, Number(战斗数据?.回合 || 0));
        const 观察者受伤 = 1 - getCombatHpRatio(观察者);
        const 目标受伤 = 1 - getCombatHpRatio(目标);
        const 图鉴条目 = 读取战斗图鉴命中(目标);
        return {
          可识别: !!图鉴条目 || 观察者受伤 >= 0.2 || 目标受伤 >= 0.2 || 回合 >= 5,
          来源: 图鉴条目 ? '图鉴' : 回合 >= 5 ? '交手回合' : 观察者受伤 >= 0.2 || 目标受伤 >= 0.2 ? '伤势反馈' : '未知',
          图鉴条目,
        };
      }

      function 计算实力压制威胁(强方 = {}, 弱方 = {}, 战斗数据 = {}) {
        const 识别 = 可识别对手实力详情(弱方, 强方, 战斗数据);
        if (!识别.可识别) {
          return {
            是威胁: false,
            是严重威胁: false,
            威胁等级: 0,
            等级差: 0,
            魂力压制: 1,
            精神压制: 1,
            实力差距: 0,
            已识别: false,
            识别来源: 识别.来源,
          };
        }
        const 强方等级 = getCombatUnitTierNumber(强方);
        const 弱方等级 = getCombatUnitTierNumber(弱方);
        const 等级差 = Math.max(0, 强方等级 - 弱方等级);
        const 强方当前魂力 = Math.max(0, Number(强方?.sp ?? 强方?.属性?.魂力 ?? 0));
        const 弱方当前魂力 = Math.max(0, Number(弱方?.sp ?? 弱方?.属性?.魂力 ?? 0));
        const 强方当前精神力 = Math.max(0, Number(强方?.men ?? 强方?.属性?.精神力 ?? 0));
        const 弱方当前精神力 = Math.max(0, Number(弱方?.men ?? 弱方?.属性?.精神力 ?? 0));
        const 魂力压制 = 强方当前魂力 / Math.max(1, 弱方当前魂力);
        const 精神压制 = 强方当前精神力 / Math.max(1, 弱方当前精神力);
        const 实力差距 = 计算行为实力差距(强方, 弱方);
        let 威胁等级 = 0;
        if (等级差 >= 10) 威胁等级 += 2;
        else if (等级差 >= 5) 威胁等级 += 1;
        if (魂力压制 >= 1.8 || 精神压制 >= 1.8) 威胁等级 += 2;
        else if (魂力压制 >= 1.35 || 精神压制 >= 1.35) 威胁等级 += 1;
        if (实力差距 >= 0.32) 威胁等级 += 2;
        else if (实力差距 >= 0.18) 威胁等级 += 1;
        return {
          是威胁: 威胁等级 >= 2,
          是严重威胁: 威胁等级 >= 4,
          威胁等级,
          等级差,
          魂力压制,
          精神压制,
          实力差距,
          已识别: true,
          识别来源: 识别.来源,
        };
      }

      function 创建默认行为倾向() {
        return {
          输出: 1,
          控制: 1,
          防御: 1,
          恢复: 1,
          辅助: 1,
          破局: 1,
          高耗: 1,
          真身: 1,
          闪避: 1,
        };
      }

      function 评估战斗行为局势(自身 = {}, 对手 = {}, 行为状态 = {}) {
        const 回合 = Math.max(0, Number(行为状态?.round ?? 行为状态?.combatData?.回合 ?? 0));
        const 识别 = 可识别对手实力详情(自身, 对手, 行为状态?.combatData || {});
        const 自身血量 = getCombatHpRatio(自身);
        const 对手血量 = getCombatHpRatio(对手);
        const 自身魂力 = Math.max(0, Number(自身?.sp || 0)) / Math.max(1, Number(自身?.sp_max || 1));
        const 自身精神 = Math.max(0, Number(自身?.men || 0)) / Math.max(1, Number(自身?.men_max || 1));
        const 对手快照 = buildConditionTacticalSnapshot(对手);
        const 自身快照 = buildConditionTacticalSnapshot(自身);
        const 记忆 = ensureActorDecisionMemory(自身);
        const 近期受挫次数 = Object.values(记忆.countered_skills || {}).reduce(
          (总数, 次数) => 总数 + Math.max(0, Number(次数 || 0)),
          0,
        );
        const 倾向 = 创建默认行为倾向();
        let 阶段 = 识别.可识别 ? '识别后' : '试探';
        let 局势 = '均势';
        const 标签 = [];

      if (!识别.可识别) {
        标签.push('情报不足');
          const 试探强度 = Math.max(0.25, Math.min(1, 1 - 回合 / 6));
          倾向.输出 *= 1 - 0.06 * 试探强度;
          倾向.控制 *= 1 + 0.05 * 试探强度;
          倾向.防御 *= 1 + 0.04 * 试探强度;
          倾向.辅助 *= 自身血量 > 0.86 && 自身魂力 > 0.68 && 自身精神 > 0.68 ? 0.9 : 1 + 0.04 * 试探强度;
          倾向.破局 *= 1 + 0.04 * 试探强度;
          倾向.高耗 *= 1 - 0.24 * 试探强度;
          倾向.真身 *= 1 - 0.72 * 试探强度;
          倾向.闪避 *= 1 + 0.03 * 试探强度;
        }

        if (自身血量 < 0.35 || 自身魂力 < 0.22 || 自身精神 < 0.22 || 自身快照.hasBadCondition) {
          局势 = '自保';
          标签.push('自身承压');
          倾向.防御 *= 1.35;
          倾向.恢复 *= 1.45;
          倾向.闪避 *= 1.18;
          倾向.输出 *= 0.82;
          倾向.高耗 *= 自身血量 < 0.35 ? 1.1 : 0.75;
        }

        if (对手血量 < 0.35) {
          局势 = '收割';
          阶段 = '决胜';
          标签.push('收割窗口');
          倾向.输出 *= 1.35;
          倾向.控制 *= 1.12;
          倾向.破局 *= 1.25;
          倾向.恢复 *= 0.72;
          倾向.高耗 *= 1.18;
        }

        const 打不动 =
          (回合 >= 3 && 对手血量 > 0.86) ||
          近期受挫次数 >= 2 ||
          对手快照.hasShielded ||
          对手快照.hasDefenseBuffed ||
          对手快照.hasReactiveDefense;
        if (打不动) {
          局势 = 局势 === '收割' ? 局势 : '破局';
          标签.push('破局需求');
          倾向.破局 *= 1.55;
          倾向.控制 *= 1.18;
          倾向.输出 *= 0.95;
          倾向.防御 *= 0.88;
          倾向.恢复 *= 0.8;
        }

        if (行为状态?.实力压制威胁?.是严重威胁) {
          局势 = '劣势';
          标签.push('实力压制');
          倾向.防御 *= 1.45;
          倾向.控制 *= 1.28;
          倾向.破局 *= 1.25;
          倾向.真身 *= 1.35;
          倾向.高耗 *= 1.18;
        } else if (行为状态?.实力压制威胁?.是威胁) {
          局势 = 局势 === '均势' ? '劣势' : 局势;
          标签.push('轻度压制');
          倾向.防御 *= 1.18;
          倾向.控制 *= 1.12;
        }

        if (识别.可识别 && 局势 === '均势' && 回合 >= 5) {
          标签.push('进入明牌');
          倾向.输出 *= 1.12;
          倾向.辅助 *= 0.92;
          倾向.高耗 *= 1.06;
        }

        return {
          阶段,
          局势,
          标签,
          可识别: 识别.可识别,
          识别来源: 识别.来源,
          倾向,
        };
      }

      function 读取局势倾向系数(行为状态 = {}, 名称 = '') {
        const 系数 = Number(行为状态?.局势评估?.倾向?.[名称]);
        return Number.isFinite(系数) && 系数 > 0 ? 系数 : 1;
      }

      function 判定行为绝境换伤窗口(防反方, 攻击方, 战斗数据 = {}, 预计伤害 = 0) {
        const 战斗类型 = String(战斗数据?.战斗类型 || '').trim();
        const 是虚拟战 = /虚拟|升灵台|模拟/.test(战斗类型);
        if (是虚拟战) return false;
        const 是生死战 = /死战|生死|突发遭遇|伏击|追杀/.test(战斗类型 || '突发遭遇');
        const 允许撤离 = 战斗数据?.允许撤离 !== false;
        if (!是生死战 || 允许撤离) return false;
        const 承伤比 = Math.max(0, Number(预计伤害 || 0)) / Math.max(1, getCombatHpMaxValue(防反方));
        const 实力差距 = 计算行为实力差距(攻击方, 防反方);
        return 实力差距 >= 0.28 && 承伤比 >= 0.22;
      }

      function getBehaviorProfile(actor, target, battleState = {}) {
          const hpRatio = getCombatHpRatio(actor);
          const targetHpRatio = getCombatHpRatio(target);
          const profile = {
            aggression: 10,
            caution: 10,
            control: 10,
            mobility: 10,
            burst: 10,
            finisher: 0,
          };

          switch (读取规划单位系别(actor)) {
            case '强攻系':
              profile.aggression += 25;
              profile.burst += 20;
              profile.caution -= 5;
              break;
            case '敏攻系':
              profile.mobility += 30;
              profile.aggression += 15;
              profile.caution += 5;
              break;
            case '防御系':
              profile.caution += 30;
              profile.control += 10;
              profile.aggression -= 10;
              break;
            case '控制系':
              profile.control += 30;
              profile.caution += 10;
              break;
            case '辅助系':
            case '治疗系':
            case '食物系':
              profile.caution += 25;
              profile.control += 15;
              profile.aggression -= 15;
              break;
          }

          if (hpRatio < 0.35) {
            profile.caution += 25;
            profile.burst += 10;
          }
          if (hpRatio < 0.2) {
            profile.caution += 35;
            profile.aggression -= 5;
          }
          if (targetHpRatio < 0.3) {
            profile.finisher += 25;
            profile.aggression += 10;
          }
          if (battleState.combatType === '死战' || battleState.isDeadlyFight) {
            profile.aggression += 15;
            profile.burst += 10;
          }
          if (battleState.combatType === '擂台切磋') {
            profile.caution += 10;
            profile.aggression -= 5;
            profile.burst -= 5;
          }
          if (battleState.isChargingHighThreat) {
            profile.control += 20;
            profile.mobility += 10;
          }
          if (battleState.实力压制威胁?.是威胁) {
            profile.caution += battleState.实力压制威胁.是严重威胁 ? 28 : 16;
            profile.mobility += battleState.实力压制威胁.是严重威胁 ? 14 : 8;
            profile.aggression -= battleState.实力压制威胁.是严重威胁 ? 12 : 6;
          }
          const 局势评估 = battleState?.局势评估 || null;
          if (局势评估?.阶段 === '试探') {
            profile.caution += 6;
            profile.control += 5;
            profile.burst -= 8;
            profile.aggression -= 2;
          }
          if (局势评估?.局势 === '破局') {
            profile.control += 10;
            profile.burst += 8;
            profile.caution -= 4;
          } else if (局势评估?.局势 === '收割') {
            profile.aggression += 14;
            profile.finisher += 18;
            profile.burst += 10;
          } else if (局势评估?.局势 === '自保' || 局势评估?.局势 === '劣势') {
            profile.caution += 14;
            profile.control += 6;
            profile.aggression -= 8;
          }
          if ((actor?.men_max || 0) > (target?.men_max || 0) * 1.2) profile.control += 10;

          return profile;
        }

        function adjustBehaviorWeight(branchName, baseWeight, actor, target, battleState = {}) {
          if (!baseWeight || baseWeight <= 0) return 0;
          const profile = getBehaviorProfile(actor, target, battleState);
          let weight = baseWeight;

          if (['武魂融合技', '点燃生命之火'].includes(branchName))
            weight += profile.burst * 0.8 + profile.aggression * 0.4;
          else if (branchName === '开启真身') {
            weight += profile.burst * 0.25 + profile.aggression * 0.12;
            if (
              Number(battleState.round || 0) <= 4 &&
              Math.abs(Number((target?.lv || 1) - (actor?.lv || 1))) <= 4 &&
              getCombatHpRatio(actor) > 0.85 &&
              !battleState.isChargingHighThreat
            )
              weight = Math.min(weight, 2);
            // Phase 3.H: 真身激活时机感 — 残血决战 / 我方危急 +50% 权重
            const 我方HP_phase3h = getCombatHpRatio(actor);
            const 敌方最低血_phase3h = Math.min(1, ...(battleState?.combatData?.参战者?.team_enemy || [])
              .map(unit => getCombatHpRatio(unit)));
            if (敌方最低血_phase3h < 时机决策阈值_V1.真身领域_决战_敌方残血阈值
              || 我方HP_phase3h < 时机决策阈值_V1.真身领域_紧急释放_我方残血阈值) {
              weight *= 1.5;
            }
          }
          else if (branchName === '展开领域') {
            weight += profile.control * 0.8 + profile.caution * 0.3;
            // Phase 3.H: 领域展开时机感 — 我方危急时 +40% 权重
            const 我方HP_phase3h2 = getCombatHpRatio(actor);
            if (我方HP_phase3h2 < 时机决策阈值_V1.真身领域_紧急释放_我方残血阈值) {
              weight *= 1.4;
            }
          }
          else if (branchName === '召唤魂灵') weight += profile.control * 0.5 + profile.caution * 0.3;
          else if (branchName === '穿戴装备') weight += profile.caution * 0.9;
          else if (branchName === '亡命奔逃') weight += profile.caution * 1.1 - profile.aggression * 0.4;
          else if (branchName === '危机自保') weight += profile.caution * 0.7 + profile.control * 0.5;
          else if (branchName === '护援队友') weight += profile.caution * 0.8 + profile.control * 0.35 - profile.aggression * 0.15;
          else if (branchName === '强势对轰')
            weight += profile.aggression * 0.9 + profile.burst * 0.5 + profile.finisher * 0.5 - profile.caution * 0.3;
          else if (branchName === '短前摇对轰')
            weight += profile.aggression * 0.75 + profile.burst * 0.42 + profile.mobility * 0.25 - profile.caution * 0.18;
          else if (branchName === '范围压制')
            weight += profile.aggression * 0.62 + profile.control * 0.45 + profile.burst * 0.38 - profile.caution * 0.14;
          else if (branchName === '伺机闪避') weight += profile.mobility * 0.9 + profile.caution * 0.4;
          else if (branchName === '肉体兜底') weight += profile.caution * 0.2;

          if (branchName === '开启真身') weight *= 读取局势倾向系数(battleState, '真身');
          else if (branchName === '强势对轰' || branchName === '短前摇对轰' || branchName === '范围压制' || branchName === '乘胜追击' || branchName === '连段爆发' || branchName === '压血收束')
            weight *= 读取局势倾向系数(battleState, '输出');
          else if (branchName === '破防强攻' || branchName === '斩盾' || branchName === '断疗压制' || branchName === '逆疗封锁')
            weight *= 读取局势倾向系数(battleState, '破局');
          else if (branchName === '危机自保' || branchName === '借力守势' || branchName === '坚壁反制' || branchName === '肉体兜底')
            weight *= 读取局势倾向系数(battleState, '防御');
          else if (branchName === '稳态回气') weight *= 读取局势倾向系数(battleState, '恢复');
          else if (branchName === '伺机闪避') weight *= 读取局势倾向系数(battleState, '闪避');
          else if (branchName === '战术协同' || branchName === '统筹增援') weight *= 读取局势倾向系数(battleState, '辅助');

          const 稳定度 = 获取行为经验稳定度(battleState);
          const 随机幅度 = Math.max(2, Math.round(3 + (1 - 稳定度) * 18));
          weight += Math.floor(Math.random() * (随机幅度 * 2 + 1)) - 随机幅度;
          return Math.max(0, Math.floor(weight));
        }

        function ensureActorDecisionMemory(actor) {
          if (!actor)
            return {
              last_action: '',
              recent_actions: {},
              关注对象: '',
              focus_reason: '',
              focus_ttl: 0,
              countered_skills: {},
              dodge_whiff_actions: {},
            };
          if (!actor.决策记忆) {
            actor.决策记忆 = {
              last_action: '',
              recent_actions: {},
              关注对象: '',
              focus_reason: '',
              focus_ttl: 0,
              countered_skills: {},
              dodge_whiff_actions: {},
            };
          }
          if (!actor.决策记忆.recent_actions) actor.决策记忆.recent_actions = {};
          if (!actor.决策记忆.countered_skills) actor.决策记忆.countered_skills = {};
          if (!actor.决策记忆.dodge_whiff_actions) actor.决策记忆.dodge_whiff_actions = {};
          if (actor.决策记忆.关注对象 === undefined) actor.决策记忆.关注对象 = '';
          if (actor.决策记忆.focus_reason === undefined) actor.决策记忆.focus_reason = '';
          if (actor.决策记忆.focus_ttl === undefined) actor.决策记忆.focus_ttl = 0;
          return actor.决策记忆;
        }

        function getActorFocusedTarget(actor, candidates = []) {
          const memory = ensureActorDecisionMemory(actor);
          if (!memory.关注对象 || Number(memory.focus_ttl || 0) <= 0) return null;
          const target = (candidates || []).find(
            unit => unit && unit.name === memory.关注对象 && isCombatUnitAbleToFight(unit),
          );
          if (target) return target;
          memory.关注对象 = '';
          memory.focus_reason = '';
          memory.focus_ttl = 0;
          return null;
        }

      function setActorFocusTarget(actor, target, reason = '', ttl = 2) {
          const memory = ensureActorDecisionMemory(actor);
          if (!target || getCombatHpValue(target) <= 0) {
            memory.关注对象 = '';
            memory.focus_reason = '';
            memory.focus_ttl = 0;
            return;
          }
          memory.关注对象 = target.name || '';
          memory.focus_reason = String(reason || '');
          memory.focus_ttl = Math.max(0, Number(ttl || 0));
        }

        function scoreCandidateAction(actor, target, battleState, candidate) {
          if (!candidate) return 0;
          const memory = ensureActorDecisionMemory(actor);
          const branchName = candidate.name || '未命名动作';
          const candidateSkillName = normalizeBattleActionDisplayName(
            candidate?.skill?.name ||
            candidate?.skill?.魂技名 ||
            candidate?.skill?.技能名称 ||
            candidate?.__预览技能?.name ||
            candidate?.__预览技能?.魂技名 ||
            candidate?.__预览技能?.技能名称 ||
            '',
          );
          let weight = Math.max(0, Math.floor(candidate.weight || 0));
          const 威胁分层 = 计算行为威胁分层(battleState?.threatProfile || battleState?.来袭窗口摘要 || {});

          const 记忆候选键 = 读取行为记忆候选键_V1(candidate?.skill || candidate?.__预览技能 || {}, candidate);
          const repeatCount = 读取归一行为记忆值_V1(memory.recent_actions || {}, 记忆候选键.length ? 记忆候选键 : [branchName], 0);
          if (规范化行为记忆键_V1(memory.last_action) === 规范化行为记忆键_V1(branchName)) weight -= 8;
          weight -= 计算保守动作惩罚(actor, branchName, 威胁分层, {
            有职责技能: (battleState?.skillContext?.评分技能列表 || []).some(项目 => 项目?.skill && Number(项目.weight || 0) >= 24),
          });
          weight += 计算战术切换奖励(actor, branchName, 威胁分层);
          const 防御质量 = 评分防御动作质量_V1(
            判定防御动作类型_V1(candidate?.skill || candidate?.__预览技能 || {}, candidate),
            actor,
            target || actor,
            battleState || {},
          );
          if (防御质量.修正) weight += 防御质量.修正;

          const targetHpRatio = getCombatHpRatio(target);
          if (Number(memory.focus_ttl || 0) > 0) {
            if (
              branchName === '乘胜追击' &&
              ['control_window', 'dot_pressure', 'shared_vision_focus', 'finisher'].includes(memory.focus_reason)
            )
              weight += 10;
            if (branchName === '破防强攻' && memory.focus_reason === 'armor_break_window') weight += 8;
            if (branchName === '断疗压制' && memory.focus_reason === 'anti_heal_window') weight += 8;
          }
          if (['强势对轰', '短前摇对轰', '范围压制'].includes(branchName) && targetHpRatio < 0.35) weight += 10;
          if (branchName === '亡命奔逃' && getCombatHpRatio(actor) > 0.35) weight -= 15;
          if (branchName === '危机自保' && battleState?.isChargingHighThreat) weight += 8;
          if (branchName === '危机自保' && 威胁分层.低威胁 && getCombatHpRatio(actor) > 0.55) weight -= 22;
          if (branchName === '肉体兜底' && 威胁分层.低威胁) weight -= 28;
          if (branchName === '伺机闪避' && 威胁分层.低威胁 && 读取规划单位系别(actor) !== '敏攻系') weight -= 18;
          const 闪避克制次数 = 读取闪避克制记忆(actor, target);
          const 目标敏攻拉扯 = 闪避克制次数 > 0 && (
            读取规划单位系别(target) === '敏攻系' ||
            Number(target?.final?.agi ?? target?.agi ?? 0) > Number(actor?.final?.agi ?? actor?.agi ?? 0)
          );
          const 同技能被闪惩罚 = candidateSkillName
            ? getActorSkillDodgeWhiffPenalty(actor, candidateSkillName, target)
            : 0;
          if (同技能被闪惩罚 > 0) {
            weight -= 同技能被闪惩罚;
            const 候选技能 = candidate?.skill || candidate?.__预览技能 || null;
            const 候选具备伤害原型 = !!候选技能 && getSkillEffects(候选技能, { 行为规划: true })
              .some(effect => String(effect?.原型 || '').trim() === '伤害结算' && (
                Number(effect?.威力倍率 || effect?.威力 || effect?.基础倍率 || 0) > 0 ||
                Number(effect?.段数 || 0) > 0 ||
                /伤害/.test(String(effect?.伤害类型 || effect?.结算 || ''))
              ));
            if (目标敏攻拉扯 && 候选具备伤害原型) {
              const 目标文本 = String(getSkillTarget(候选技能 || {}) || '').trim();
              if (!/群体|全场/.test(目标文本)) weight -= 14 + 闪避克制次数 * 8;
            }
          }
          if (目标敏攻拉扯) {
            if (/普通攻击|常规攻击|主动压迫|低风险/.test(branchName)) weight -= 42 + 闪避克制次数 * 18;
            if (/控制截断|压招|抢落点|范围压制|命中修正|收招转防|短前摇对轰|控制压制/.test(branchName)) weight += 24 + 闪避克制次数 * 10;
          }

          const 重复衰减 = 计算重复效用衰减_V1({
            重复次数: repeatCount,
            行动类别: branchName,
            可斩杀: targetHpRatio > 0 && targetHpRatio < 0.18,
            紧急治疗: /治疗|恢复|回春/.test(branchName) && getCombatHpRatio(actor) < 0.45,
            致命截断: battleState?.threatProfile?.lethalRisk === true || battleState?.来袭窗口摘要?.lethalRisk === true,
          });
          const 衰减前权重 = Math.max(0, Math.floor(weight));
          weight = Math.round(衰减前权重 * Number(重复衰减.repeatDecayMultiplier || 1));
          candidate.__repeatDecayAudit = {
            ...重复衰减,
            scoreBeforeRepeatDecay: 衰减前权重,
            scoreAfterRepeatDecay: weight,
            repeatDecayReasonText: 格式化重复效用衰减说明_V1(重复衰减),
          };
          return 应用行为经验误判(Math.max(0, weight), battleState);
        }

        function chooseActorActionByCandidates(actor, target, battleState, candidates, phaseLabel) {
          const 规划上下文 =
            battleState?.规划上下文 ||
            (battleState?.combatData ? 构建规划上下文(actor, target, battleState.combatData, battleState) : null);
          const 干扰强度 = 读取行动判断干扰强度(actor);
          let scoredCandidates = (candidates || []).map(candidate => {
            const 预览技能 = (() => {
              if (candidate?.skill) return candidate.skill;
              if (candidate?.__预览技能) return candidate.__预览技能;
              return null;
            })();
            const 规划收益 = 预览技能 && 规划上下文
              ? 评估技能规划净收益(预览技能, {
                  actor,
                  primaryTarget: target,
                  behaviorState: { ...(battleState || {}), combatData: battleState?.combatData },
                  规划上下文,
                })
              : { 净收益: Number(candidate?.weight || 0) * 0.25, 目标理由: [], 战术修正: 0 };
            const 资源结果 = !预览技能 ? { canCast: true } : parseSkillCostForChar(预览技能, actor, {
              actor,
              caster: actor,
              target,
              defender: target,
              action: { action_type: '行为规划', type: candidate?.name || '', skill: 预览技能 },
              skill: 预览技能,
              combatData: battleState?.combatData,
              当前行动: '行为规划',
            });
            const 资源可行 = 资源结果.canCast !== false;
            const 原始净收益 = Number(规划收益.净收益 || 0);
            const 启发式修正 = scoreCandidateAction(actor, target, battleState, candidate) - Number(candidate?.weight || 0);
            const 团队意图 = battleState?.规划上下文?.团队意图 || {};
            const 目标名 = 读取规划单位名称(target);
            const 团队意图修正 =
              团队意图.集火目标 && 目标名 && 团队意图.集火目标 === 目标名 && !技能偏向友方目标(预览技能 || {}, candidate)
                ? 18
                : 团队意图.保护目标 && 目标名 && 团队意图.保护目标 === 目标名 && 技能偏向友方目标(预览技能 || {}, candidate)
                  ? 18
                  : 0;
            const 技能消耗压力 = 预览技能 ? 计算技能消耗压力(预览技能, actor) : { 综合压力: 0, 当前压力: 0, 可释放: true };
            return {
              ...candidate,
              skill: 预览技能 || candidate?.skill,
              target,
              原始权重: Number(candidate?.weight || 0),
              weight: Number(candidate?.weight || 0),
              原始净收益,
              净收益: 原始净收益,
              战术修正: Number(规划收益.战术修正 || 0) + 启发式修正 + 团队意图修正,
              高耗: 技能消耗压力.综合压力 >= 0.34 || 技能消耗压力.可释放 === false,
              断档压力: Math.max(Number(技能消耗压力.综合压力 || 0), Number(技能消耗压力.当前压力 || 0), Number(规划上下文?.资源压力 || 0)),
              资源压力: Number(规划上下文?.资源压力 || 0),
              资源可行,
              可斩杀: target ? getCombatHpRatio(target) < 0.36 || 原始净收益 >= 120 : false,
              可打断: target?.蓄力技能 && /控制|打断|封技|沉默|眩晕/.test(String(candidate?.name || 预览技能?.name || 预览技能?.魂技名 || '')),
              终局压力: Number(规划上下文?.战局画像?.终局压力 || 0),
              __主动规划审计: {
                候选来源: candidate?.__候选来源 || phaseLabel || '主动候选',
                技能: 预览技能?.name || 预览技能?.魂技名 || candidate?.name || '',
                原始净收益,
                战术修正: Number(规划收益.战术修正 || 0) + 启发式修正,
                团队意图修正,
                干扰强度,
                目标理由: 规划收益.目标理由 || [],
                战略意图: 规划上下文?.战略意图?.主意图 || '',
                资源可行,
                硬门槛失败: !!预览技能 && 原始净收益 < 0,
              },
            };
          });
          scoredCandidates = 排序行为规划候选_V1(actor, 规划上下文?.战局画像 || {}, scoredCandidates, {
            ...(battleState || {}),
            ...(规划上下文 || {}),
            规划上下文,
            目标优先表: 规划上下文?.目标优先表 || {},
          }).map(候选 => {
            const 审计 = 候选.__主动规划审计 || {};
            const 排序审计 = 候选.__候选排序审计 || {};
            const 净收益硬门槛失败 = !!候选.skill && Number(审计.原始净收益 || 0) < 0;
            const 最终权重 = 候选.资源可行 === false || 净收益硬门槛失败 ? 0 : Number(候选.weight || 0);
            return {
              ...候选,
              weight: Math.max(0, Math.round(最终权重)),
              __主动规划审计: {
                ...审计,
                候选排序审计: 排序审计,
                目标: 排序审计.目标 || 读取规划单位名称(候选.target),
                原始权重: 排序审计.原始权重,
                目标修正: 排序审计.目标修正,
                对手意图修正: 排序审计.对手意图修正,
                类别预算修正: 排序审计.类别预算修正,
                防御质量修正: 排序审计.防御质量修正,
                资源修正: 排序审计.资源修正,
                高价复读惩罚: 排序审计.高价复读惩罚,
                连招修正: 排序审计.连招修正,
                记忆惩罚: 排序审计.记忆惩罚,
                repeatCount: 排序审计.repeatCount || 0,
                repeatCategory: 排序审计.repeatCategory || '',
                repeatDecayMultiplier: 排序审计.repeatDecayMultiplier || 1,
                repeatDecayReasonCode: 排序审计.repeatDecayReasonCode || '',
                repeatExceptionCode: 排序审计.repeatExceptionCode || '',
                repeatDecayReasonText: 排序审计.repeatDecayReasonText || '',
                scoreBeforeRepeatDecay: 排序审计.scoreBeforeRepeatDecay || 0,
                scoreAfterRepeatDecay: 排序审计.scoreAfterRepeatDecay || 0,
                目标价值修正: 排序审计.目标价值修正,
                二回合前瞻修正: 排序审计.二回合前瞻修正,
                职责修正: 排序审计.职责修正,
                效果原型分类: 排序审计.效果原型分类,
                分类来源: 排序审计.分类来源,
                目标价值分解: 排序审计.目标价值分解,
                目标切换收益: 排序审计.目标切换收益,
                切换成本: 排序审计.切换成本,
                保护链理由: 排序审计.保护链理由 || [],
                仇恨压力理由: 排序审计.仇恨压力理由 || [],
                协同理由: 排序审计.协同理由 || [],
                前瞻理由: 排序审计.前瞻理由 || [],
                资源后果: 排序审计.资源后果 || [],
                敌方下回合风险: 排序审计.敌方下回合风险 || [],
                己方下回合窗口: 排序审计.己方下回合窗口 || [],
                职责画像: 排序审计.职责画像,
                职责理由: 排序审计.职责理由 || [],
                最终权重,
                选择原因: 排序审计.选择原因,
                压力档位: 排序审计.压力档位,
                目标修正理由: 排序审计.目标修正理由 || [],
                硬门槛失败: 净收益硬门槛失败,
              },
            };
          });
          const 合法候选数 = scoredCandidates.filter(候选 => Number(候选?.weight || 0) > 0).length;
          if (合法候选数 <= 1) {
            scoredCandidates = scoredCandidates.map(候选 => {
              const 审计 = 候选.__主动规划审计 || {};
              if (Number(审计.repeatDecayMultiplier || 1) >= 0.999 || 审计.repeatExceptionCode) return 候选;
              const 更新衰减 = {
                repeatDecayMultiplier: 审计.repeatDecayMultiplier,
                repeatDecayReasonCode: 审计.repeatDecayReasonCode,
                repeatExceptionCode: 'ONLY_LEGAL_ACTION',
              };
              return {
                ...候选,
                __主动规划审计: {
                  ...审计,
                  repeatExceptionCode: 'ONLY_LEGAL_ACTION',
                  repeatDecayReasonText: 格式化重复效用衰减说明_V1(更新衰减),
                  候选排序审计: {
                    ...(审计.候选排序审计 || {}),
                    repeatExceptionCode: 'ONLY_LEGAL_ACTION',
                    repeatDecayReasonText: 格式化重复效用衰减说明_V1(更新衰减),
                  },
                },
              };
            });
          }
          const 是主动阶段 = /主动/.test(String(phaseLabel || '')) || /^ACTIVE_/i.test(String(phaseLabel || ''));
          if (是主动阶段) {
            const 紧急窗口 =
              battleState?.isChargingHighThreat ||
              battleState?.threatProfile?.lethalRisk === true ||
              battleState?.threatProfile?.severeDamage === true ||
              getCombatHpRatio(actor) < 0.35;
            const 最高技能收益 = scoredCandidates.reduce((最高, 候选) => {
              const 审计 = 候选?.__主动规划审计 || {};
              const 候选名 = String(候选?.name || '').trim();
              const 技能名 = String(审计.技能 || '').trim();
              if (!技能名 || ['穿戴装备', '伺机闪避', '肉体兜底'].includes(候选名) || /斗铠附体|召唤机甲|伺机闪避|肉体兜底/.test(技能名)) return 最高;
              return Math.max(最高, Number(审计.原始净收益 || 0), Number(候选?.weight || 0));
            }, 0);
            if (最高技能收益 >= 45) {
              scoredCandidates = scoredCandidates.map(候选 => {
                const 审计 = 候选?.__主动规划审计 || {};
                const 候选名 = String(候选?.name || '').trim();
                const 技能名 = String(审计.技能 || '').trim();
                const 是兜底 = ['伺机闪避', '肉体兜底'].includes(候选名) || /伺机闪避|肉体兜底/.test(技能名);
                const 是装备 = 候选名 === '穿戴装备' || /斗铠附体|召唤机甲/.test(技能名);
                const 是低压真身 = 候选名 === '开启真身' && !紧急窗口 && 最高技能收益 >= 80;
                if ((!紧急窗口 && 是兜底) || (!紧急窗口 && 是装备 && 最高技能收益 >= 60) || 是低压真身) {
                  return {
                    ...候选,
                    weight: 0,
                    __主动规划审计: {
                      ...审计,
                      选择原因: '已有高价值技能候选，低价值战略/兜底动作退场',
                    },
                  };
                }
                if (!紧急窗口 && 是装备) {
                  return {
                    ...候选,
                    weight: Math.min(Number(候选.weight || 0), 6),
                    __主动规划审计: {
                      ...审计,
                      选择原因: '非必要装备窗口降权',
                    },
                  };
                }
                return 候选;
              });
            }
          }
          if (/行为预演|应招|战术阶段/.test(String(phaseLabel || ''))) {
            const 最优规避 = scoredCandidates
              .filter(候选 => /伺机闪避|闪避|位移规避/.test(`${候选?.name || ''} ${候选?.__主动规划审计?.技能 || ''}`))
              .sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0))[0];
            if (最优规避) {
              scoredCandidates = scoredCandidates.map(候选 => {
                const 名称文本 = `${候选?.name || ''} ${候选?.__主动规划审计?.技能 || ''}`;
                if (!/肉体兜底|承伤硬抗|硬抗/.test(名称文本)) return 候选;
                const 优势 = Number(最优规避.weight || 0) - Number(候选.weight || 0);
                if (优势 < 24) return 候选;
                return {
                  ...候选,
                  weight: 0,
                  __主动规划审计: {
                    ...(候选.__主动规划审计 || {}),
                    选择原因: '规避候选显著优于通用硬抗，硬抗退出本次行为预演抽样',
                  },
                };
              });
            }
          }
          const 存在非复读合法候选 = scoredCandidates.some(候选 => {
            const 审计 = 候选?.__主动规划审计 || {};
            return Number(候选?.weight || 0) > 0 && !(Number(审计.repeatDecayMultiplier || 1) < 0.999 && !审计.repeatExceptionCode);
          });
          if (存在非复读合法候选) {
            scoredCandidates = scoredCandidates.map(候选 => {
              const 审计 = 候选?.__主动规划审计 || {};
              if (Number(候选?.weight || 0) <= 0 || Number(审计.repeatDecayMultiplier || 1) >= 0.999 || 审计.repeatExceptionCode) return 候选;
              return {
                ...候选,
                weight: 0,
                __主动规划审计: {
                  ...审计,
                  选择原因: '重复动作缺少战术豁免，本轮交给其他合法候选',
                  候选排序审计: {
                    ...(审计.候选排序审计 || {}),
                    选择原因: '重复动作缺少战术豁免，本轮交给其他合法候选',
                  },
                },
              };
            });
          }
          const 扰动后候选 = 是主动阶段 ? scoredCandidates : 扰动候选权重列表(scoredCandidates, 干扰强度);
          const 选择结果 = 是主动阶段
            ? (() => {
                const option = [...scoredCandidates]
                  .filter(候选 => 候选 && Number(候选.weight || 0) > 0)
                  .sort((左, 右) => {
                    const 左审计 = 左?.__主动规划审计 || {};
                    const 右审计 = 右?.__主动规划审计 || {};
                    const 左目标价值 = Number(左审计.目标修正 || 0) + Number(左审计.目标价值修正 || 0);
                    const 右目标价值 = Number(右审计.目标修正 || 0) + Number(右审计.目标价值修正 || 0);
                    return Number(右.weight || 0) - Number(左.weight || 0) ||
                      Number(右审计.战术修正 || 0) - Number(左审计.战术修正 || 0) ||
                      右目标价值 - 左目标价值 ||
                      Number(右审计.资源修正 || 0) - Number(左审计.资源修正 || 0) ||
                      Number(右审计.原始权重 || 0) - Number(左审计.原始权重 || 0) ||
                      String(左?.name || 左审计.技能 || '').localeCompare(String(右?.name || 右审计.技能 || ''), 'zh-Hans-CN');
                  })[0] || null;
                return { option, trace: option ? '战术重心锁定' : '当前未形成有效出手机会' };
              })()
            : rollBranchByPriority(扰动后候选, phaseLabel);
          const 命中审计 = 选择结果.option?.__主动规划审计 || {};
          if (battleState?.combatData) {
            const 规划战局 = 规划上下文?.战局画像 || {};
            const 快照单位 = 单位 => ({
              名称: 单位?.name || 单位?.名称 || '',
              系别: 读取规划单位系别(单位 || {}),
              HP: Number(单位?.HP ?? 单位?.hp ?? 单位?.属性?.HP ?? 0),
              HP上限: Number(单位?.HP上限 ?? 单位?.hp_max ?? 单位?.属性?.HP上限 ?? 0),
              魂力: Number(单位?.sp ?? 单位?.属性?.魂力 ?? 0),
              魂力上限: Number(单位?.sp_max ?? 单位?.属性?.魂力上限 ?? 0),
              精神力: Number(单位?.men ?? 单位?.属性?.精神力 ?? 0),
              精神力上限: Number(单位?.men_max ?? 单位?.属性?.精神力上限 ?? 0),
              距离: 单位?.距离 || 单位?.站位距离 || 单位?.distance || 单位?.range_distance || '',
              蓄力技能: 单位?.蓄力技能 === true,
              蓄力剩余: Number(单位?.蓄力剩余 ?? 单位?.cast_time_left ?? 0),
              是否核心: 判定规划核心单位_V1(单位 || {}),
            });
            const 候选排序结果 = scoredCandidates.map((候选, index) => {
              const 名称 = 候选?.skill?.name || 候选?.skill?.魂技名 || 候选?.__预览技能?.name || 候选?.__预览技能?.魂技名 || 候选?.name || '';
              const selected = 选择结果.option && 候选 === 选择结果.option;
              const weight = Number(候选?.weight || 0);
              return {
                candidateId: `candidate_${index + 1}_${normalizeBattleActionDisplayName(名称) || 'action'}`,
                candidateName: 名称,
                名称,
                战术名称: 候选?.name || '',
                score: weight,
                权重: weight,
                candidateStatus: 战斗候选状态转移('', selected ? 'EXECUTED' : (weight > 0 ? 'REJECTED' : 'FILTERED')),
                rejectionCode: selected ? '' : (weight > 0 ? 'LOWER_PRIORITY' : 'FILTERED_BY_SCORE'),
                repeatCount: Number(候选?.__主动规划审计?.repeatCount || 0),
                repeatCategory: String(候选?.__主动规划审计?.repeatCategory || ''),
                repeatDecayMultiplier: Number(候选?.__主动规划审计?.repeatDecayMultiplier || 1),
                repeatDecayReasonCode: String(候选?.__主动规划审计?.repeatDecayReasonCode || ''),
                repeatExceptionCode: String(候选?.__主动规划审计?.repeatExceptionCode || ''),
                scoreBeforeRepeatDecay: Number(候选?.__主动规划审计?.scoreBeforeRepeatDecay || 0),
                scoreAfterRepeatDecay: Number(候选?.__主动规划审计?.scoreAfterRepeatDecay || weight),
                目标: 候选?.__主动规划审计?.目标 || 候选?.target?.name || 候选?.target?.名称 || '',
                审计: 候选?.__主动规划审计?.候选排序审计 || {},
              };
            });
            记录行动闭环审计(battleState.combatData, /应招/.test(String(phaseLabel || '')) ? '应招审计' : '主动规划', {
              行动者: actor?.name || actor?.名称 || '',
              目标: 命中审计.目标 || target?.name || target?.名称 || '',
              技能: 命中审计.技能 || 选择结果.option?.name || '',
              回合: Number(battleState?.round ?? battleState?.combatData?.回合 ?? 0),
              战略意图: 命中审计.战略意图 || '',
              目标理由: 命中审计.目标理由 || [],
              候选来源: 命中审计.候选来源 || phaseLabel || '',
              原始净收益: 命中审计.原始净收益 || 0,
              战术修正: 命中审计.战术修正 || 0,
              原始权重: 命中审计.原始权重 || 0,
              目标修正: 命中审计.目标修正 || 0,
              对手意图修正: 命中审计.对手意图修正 || 0,
              类别预算修正: 命中审计.类别预算修正 || 0,
              防御质量修正: 命中审计.防御质量修正 || 0,
              资源修正: 命中审计.资源修正 || 0,
              高价复读惩罚: 命中审计.高价复读惩罚 || 0,
              连招修正: 命中审计.连招修正 || 0,
              记忆惩罚: 命中审计.记忆惩罚 || 0,
              repeatCount: 命中审计.repeatCount || 0,
              repeatCategory: 命中审计.repeatCategory || '',
              repeatDecayMultiplier: 命中审计.repeatDecayMultiplier || 1,
              repeatDecayReasonCode: 命中审计.repeatDecayReasonCode || '',
              repeatExceptionCode: 命中审计.repeatExceptionCode || '',
              repeatDecayReasonText: 命中审计.repeatDecayReasonText || '',
              scoreBeforeRepeatDecay: 命中审计.scoreBeforeRepeatDecay || 0,
              scoreAfterRepeatDecay: 命中审计.scoreAfterRepeatDecay || 0,
              目标价值修正: 命中审计.目标价值修正 || 0,
              二回合前瞻修正: 命中审计.二回合前瞻修正 || 0,
              职责修正: 命中审计.职责修正 || 0,
              团队意图修正: 命中审计.团队意图修正 || 0,
              干扰强度,
              最终权重: Number(选择结果.option?.weight || 0),
              选择原因: 选择结果.trace || (选择结果.option ? '规划候选命中' : '当前未形成有效出手机会'),
              hitCandidateName: normalizeBattleActionDisplayName(选择结果.option?.skill?.name || 选择结果.option?.skill?.魂技名 || 选择结果.option?.__预览技能?.name || 选择结果.option?.__预览技能?.魂技名 || 选择结果.option?.name || 命中审计.技能 || ''),
              selectedCandidateName: normalizeBattleActionDisplayName(选择结果.option?.skill?.name || 选择结果.option?.skill?.魂技名 || 选择结果.option?.__预览技能?.name || 选择结果.option?.__预览技能?.魂技名 || 选择结果.option?.name || 命中审计.技能 || ''),
              finalResolvedActionName: normalizeBattleActionDisplayName(命中审计.finalResolvedActionName || ''),
              actionOverrideSource: String(命中审计.actionOverrideSource || '').trim(),
              承载方式: String(命中审计.承载方式 || 选择结果.option?.skill?.承载方式 || 选择结果.option?.__预览技能?.承载方式 || '').trim(),
              压力档位: 命中审计.压力档位 || '',
              目标修正理由: 命中审计.目标修正理由 || [],
              候选排序审计: 命中审计.候选排序审计 || {},
              候选排序结果,
              对手意图摘要: 命中审计.候选排序审计?.对手适应摘要 || {},
              效果原型分类: 命中审计.效果原型分类 || {},
              分类来源: 命中审计.分类来源 || '',
              目标价值分解: 命中审计.目标价值分解 || {},
              目标切换收益: 命中审计.目标切换收益 || 0,
              切换成本: 命中审计.切换成本 || 0,
              保护链理由: 命中审计.保护链理由 || [],
              仇恨压力理由: 命中审计.仇恨压力理由 || [],
              协同理由: 命中审计.协同理由 || [],
              前瞻理由: 命中审计.前瞻理由 || [],
              资源后果: 命中审计.资源后果 || [],
              敌方下回合风险: 命中审计.敌方下回合风险 || [],
              己方下回合窗口: 命中审计.己方下回合窗口 || [],
              职责画像: 命中审计.职责画像 || {},
              职责理由: 命中审计.职责理由 || [],
              actor快照: 快照单位(actor),
              己方列表: (规划战局.己方列表 || []).map(快照单位),
              敌方列表: (规划战局.敌方列表 || []).map(快照单位),
              旁路: false,
            });
          }
          return 选择结果;
        }

        function getActorSkillCounterPenalty(actor, skillName) {
          if (!actor || !skillName) return 0;
          const memory = ensureActorDecisionMemory(actor);
          return Math.min(45, Number(memory.countered_skills?.[skillName] || 0) * 18);
        }

        function getActorSkillDodgeWhiffPenalty(actor, skillName, target = null) {
          if (!actor || !skillName) return 0;
          const memory = ensureActorDecisionMemory(actor);
          const targetName = String(target?.name || target?.名称 || target || '').trim();
          const sameTarget = !targetName || !memory.last_action_target || memory.last_action_target === targetName;
          if (!sameTarget) return 0;
          const normalizedName = normalizeBattleActionDisplayName(skillName);
          const whiffKey = `闪避未中:${normalizedName}`;
          const whiffCount = Math.max(
            0,
            Number(memory.dodge_whiff_actions?.[normalizedName] || 0),
            Number(memory.countered_skills?.[whiffKey] || 0),
          );
          return Math.min(72, whiffCount * 26);
        }

        function recordActorActionMemory(actor, actionName, options = {}) {
          if (!actor || !actionName) return;
          const memory = ensureActorDecisionMemory(actor);
          // Phase 3.I 准备: 记录每个技能的 last_used_round, 供"距上次 ≥ 5 回合 → 重启加成"使用
          if (!memory.action_last_used_round || typeof memory.action_last_used_round !== 'object') {
            memory.action_last_used_round = {};
          }
          const 当前回合_record = Number(options?.round || 0);
          Object.keys(memory.recent_actions).forEach(key => {
            memory.recent_actions[key] = Math.max(0, (memory.recent_actions[key] || 0) - 1);
            if (memory.recent_actions[key] <= 0) {
              delete memory.recent_actions[key];
              // 计数清零时不删 last_used_round, 留作"重启加成"判定依据
            }
          });
          if (Number(memory.focus_ttl || 0) > 0) {
            memory.focus_ttl = Math.max(0, Number(memory.focus_ttl || 0) - 1);
            if (memory.focus_ttl <= 0) {
              memory.关注对象 = '';
              memory.focus_reason = '';
            }
          }
          Object.keys(memory.countered_skills || {}).forEach(key => {
            memory.countered_skills[key] = Math.max(0, Number(memory.countered_skills[key] || 0) - 1);
            if (memory.countered_skills[key] <= 0) delete memory.countered_skills[key];
          });
          Object.keys(memory.dodge_whiff_actions || {}).forEach(key => {
            memory.dodge_whiff_actions[key] = Math.max(0, Number(memory.dodge_whiff_actions[key] || 0) - 1);
            if (memory.dodge_whiff_actions[key] <= 0) delete memory.dodge_whiff_actions[key];
          });
          const 原计数 = 读取归一行为记忆值_V1(memory.recent_actions, [actionName], 0);
          写入归一行为记忆值_V1(memory.recent_actions, actionName, 原计数 + 2);
          memory.last_action = actionName;
          if (当前回合_record > 0) 写入归一行为记忆值_V1(memory.action_last_used_round, actionName, 当前回合_record);
          // Phase 3.G: 记录上次行动目标
          const 目标名_record = options?.target
            ? String(options.target?.name || options.target?.名称 || options.target || '').trim()
            : '';
          if (目标名_record) memory.last_action_target = 目标名_record;
        }

        function recordActorSkillCountered(actor, skillName, reason = '') {
          if (!actor || !skillName) return;
          const memory = ensureActorDecisionMemory(actor);
          memory.countered_skills[skillName] = Math.min(3, Number(memory.countered_skills[skillName] || 0) + 1);
          memory.last_action_countered = true;
          memory.last_action_failed = true;
          if (reason) memory.last_counter_reason = String(reason);
        }

        function recordActorActionWhiffedByDodge(actor, actionName = '', target = null) {
          if (!actor || !actionName) return;
          const memory = ensureActorDecisionMemory(actor);
          memory.last_action_failed = true;
          memory.last_action_countered = true;
          memory.last_counter_reason = '闪避未命中';
          memory.dodge_whiff_count = Math.min(5, Number(memory.dodge_whiff_count || 0) + 1);
          const normalizedActionName = normalizeBattleActionDisplayName(actionName);
          const key = `闪避未中:${normalizedActionName}`;
          memory.countered_skills[key] = Math.min(3, Number(memory.countered_skills?.[key] || 0) + 1);
          memory.dodge_whiff_actions[normalizedActionName] = Math.min(3, Number(memory.dodge_whiff_actions?.[normalizedActionName] || 0) + 1);
          const targetName = String(target?.name || target?.名称 || target || '').trim();
          if (targetName) memory.last_action_target = targetName;
        }

        function 读取闪避克制记忆(actor = {}, target = null) {
          const memory = ensureActorDecisionMemory(actor);
          const targetName = String(target?.name || target?.名称 || target || '').trim();
          const sameTarget = !targetName || !memory.last_action_target || memory.last_action_target === targetName;
          return sameTarget ? Math.max(0, Number(memory.dodge_whiff_count || 0)) : 0;
        }

        function broadcastActorFocusToTeam(actorEntry, battleState, target, reason = '', ttl = 2) {
          if (!actorEntry?.char || !battleState?.combatData || !target) return;
          const team =
            actorEntry.side === 'player'
              ? 读取战斗主队单位列表(battleState.combatData, '玩家')
              : 读取战斗主队单位列表(battleState.combatData, '敌方');
          team.forEach(member => {
            if (!member || member.name === actorEntry.char.name) return;
            const memory = ensureActorDecisionMemory(member);
            const hasActiveFocus = memory && memory.关注对象 && Number(memory.focus_ttl || 0) > 0;
            if (hasActiveFocus && memory.关注对象 !== (target.name || '')) return;
            setActorFocusTarget(member, target, reason, Math.max(1, Number(ttl || 0) - 1));
          });
        }

        function evaluateActionCountered(action, finalTarget, reactionAction, settleResult, 技能目标类型 = '') {
          if (!action?.skill || !finalTarget) return false;
          if (
            String(技能目标类型 || '').includes('己方') ||
            String(技能目标类型 || '').includes('友方') ||
            String(技能目标类型 || '').includes('自身')
          )
            return false;
          const skillType = getSkillType(action.skill);
          const snapshot = buildConditionTacticalSnapshot(finalTarget);
          const resultText = String(settleResult?.desc || '');
          if (skillType === '输出') {
            return (
              Number(settleResult?.dmg || 0) <= 1 &&
              (reactionAction?.type !== '无法反应' || /躲过|规避|挡下|碾碎|打断|未破防/.test(resultText))
            );
          }
          if (skillType === '控制') {
            const landed = snapshot.isLockedOrControlled || snapshot.hasBadCondition || snapshot.hasAntiHeal;
            return !landed && (reactionAction?.type !== '无法反应' || /免疫|躲过|规避|挡下/.test(resultText));
          }
          return false;
        }

        function updateActorFocusFromAction(
          actor,
          action,
          finalTarget,
          fallbackEnemyTarget = null,
          settleResult = null,
        ) {
          if (!actor || !action) return null;
          const skill = action.skill || {};
          const targetMode = getSkillTarget(skill);
          let focusTarget = null;
          let focusReason = '';
          let ttl = 0;

          if (targetMode.includes('己方') || targetMode.includes('友方')) {
            if (
              isBattleSkillSharedVisionProfile(skill) &&
              fallbackEnemyTarget &&
              getCombatHpValue(fallbackEnemyTarget) > 0
            ) {
              focusTarget = fallbackEnemyTarget;
              focusReason = 'shared_vision_focus';
              ttl = 3;
            }
          } else if (
            !targetMode.includes('自身') &&
            finalTarget &&
            finalTarget !== actor &&
            getCombatHpValue(finalTarget) > 0
          ) {
            const snapshot = buildConditionTacticalSnapshot(finalTarget);
            const hpRatio = getCombatHpRatio(finalTarget);
            focusTarget = finalTarget;
            if (
              snapshot.isLockedOrControlled &&
              (isBattleSkillControlProfile(skill, { calc: getPrimaryStateCalc(skill) }) ||
                Number(getPrimaryStateCalc(skill)?.lock_level || 0) > 0 ||
                Number(getPrimaryStateCalc(skill)?.reaction_penalty || 0) > 0 ||
                Number(getPrimaryStateCalc(skill)?.cast_speed_penalty || 0) > 0 ||
                Number(getPrimaryStateCalc(skill)?.dodge_penalty || 0) > 0 ||
                技能包含资源锁定原型(skill))
            ) {
              focusReason = 'control_window';
              ttl = 3;
            } else if (
              isBattleSkillAntiHealProfile(skill, { calc: getPrimaryStateCalc(skill) }) &&
              (snapshot.hasAntiHeal || Number(getPrimaryStateCalc(skill)?.heal_block_ratio || 0) > 0)
            ) {
              focusReason = 'anti_heal_window';
              ttl = 3;
            } else if (
              Number(getPrimaryDamageEffect(skill)?.防御穿透 || 0) >= 15 ||
              /破甲|穿透|粉碎/.test(String(skill?.name || ''))
            ) {
              focusReason = 'armor_break_window';
              ttl = 2;
            } else if (
              snapshot.hasDotPressure ||
              isBattleSkillDotPressureProfile(skill) ||
              Number(settleResult?.dmg || 0) >= getCombatHpMaxValue(finalTarget) * 0.22
            ) {
              focusReason = 'dot_pressure';
              ttl = 2;
            } else if (isBattleSkillExecuteProfile(skill, { damage: getPrimaryDamageEffect(skill) }) || hpRatio < 0.45) {
              focusReason = 'finisher';
              ttl = hpRatio < 0.35 ? 3 : 2;
            }
          }
        setActorFocusTarget(actor, focusTarget, focusReason, ttl);
        return { target: focusTarget, reason: focusReason, ttl };
      }

      function buildBattleStateContext(actor, target, combatData, extra = {}) {
        const canFlee = combatData?.允许撤离 !== false;
        const isDesperateNoEscape = getCombatHpRatio(actor) < 0.3 && !canFlee;
        const 战斗经验 = 计算行为战斗经验(actor, target, combatData);
        const 规划上下文 = 构建规划上下文(actor, target, combatData, { ...extra, combatData, actor, target });
        return {
          combatType: combatData?.战斗类型 || '突发遭遇',
          isDeadlyFight:
            extra.isDeadlyFight ??
            ((combatData?.战斗类型 || '突发遭遇') === '死战' ||
              (combatData?.战斗类型 || '突发遭遇') === '突发遭遇'),
          ratio: extra.ratio ?? 1,
          playerPower: extra.playerPower ?? 0,
          isChargingHighThreat: !!extra.isChargingHighThreat,
          round: Math.max(0, Number(extra.round ?? combatData?.回合 ?? 0)),
          actorHpRatio: getCombatHpRatio(actor),
          targetHpRatio: getCombatHpRatio(target),
          canFlee: canFlee,
          isDesperateNoEscape: isDesperateNoEscape,
          战斗经验,
          经验稳定度: 战斗经验.稳定度,
          规划上下文,
          战局画像: 规划上下文.战局画像,
          目标优先表: 规划上下文.目标优先表,
          战略意图: 规划上下文.战略意图,
          actor,
          target,
          combatData,
          ...extra,
        };
      }

      function 建立普防护援技能() {
        return normalizeSkillData(
          {
            name: '普防护援',
            魂技名: '普防护援',
            技能分类: '防御',
            消耗: '无',
            前摇: 6,
            战斗摘要: {
              防御性质: '护卫',
              协同性: '高',
              生效方式: '持续',
              持续性: '短',
              爆发级别: '无',
            },
            战斗语义: {
              作用目标: '友方单体',
            },
            _效果数组: [
              {
                原型: '结算修正',
                目标: '单体',
                结算: '受到伤害',
                数值: '-8%',
                持续回合: 2,
              },
              {
                原型: '状态施加',
                目标: '单体',
                状态: '护卫',
                数值: '+8%',
                持续回合: 2,
              },
            ],
          },
          '普防护援',
        );
      }

      function 计算队友护援压力(自身, 队友, 攻击者, 行为状态 = {}) {
        if (!队友 || !isCombatUnitAbleToFight(队友) || isCombatUnitIdentityMatch(队友, 自身?.name || 自身)) return 0;
        const 队友快照 = buildConditionTacticalSnapshot(队友);
        const 队友血量 = getCombatHpRatio(队友);
        const 队友系别 = 读取规划单位系别(队友);
        let 压力 = Math.floor((1 - 队友血量) * 90);
        if (['辅助系', '治疗系', '食物系', '召唤系', '控制系'].includes(队友系别)) 压力 += 28;
        else if (['强攻系', '敏攻系', '精神系', '元素系'].includes(队友系别)) 压力 += 14;
        else if (队友系别 === '防御系') 压力 -= 10;
        if (队友快照.hasBadCondition || 队友快照.isLockedOrControlled) 压力 += 18;
        if (队友快照.hasShielded || 队友快照.hasReactiveDefense) 压力 -= 14;
        if (队友.蓄力技能) 压力 += 18;
        const 攻击记忆 = ensureActorDecisionMemory(攻击者);
        if (攻击记忆.关注对象 && isCombatUnitIdentityMatch(队友, 攻击记忆.关注对象)) 压力 += 24;
        if (行为状态?.isChargingHighThreat) 压力 += 12;
        if (行为状态?.实力压制威胁?.是严重威胁) 压力 += 22;
        else if (行为状态?.实力压制威胁?.是威胁) 压力 += 10;
        return Math.max(0, Math.floor(压力));
      }

      function 选择护援队友(自身, 己方团队 = [], 攻击者 = null, 行为状态 = {}) {
        const 队友列表 = dedupeCombatTargetList(己方团队)
          .filter(队友 => 队友 && isCombatUnitAbleToFight(队友) && !isCombatUnitIdentityMatch(队友, 自身?.name || 自身));
        if (!队友列表.length) return null;
        return 队友列表
          .map(队友 => ({ 队友, 压力: 计算队友护援压力(自身, 队友, 攻击者, 行为状态) }))
          .sort((左, 右) => 右.压力 - 左.压力)[0] || null;
      }

      function normalizeBattleIntentMode(value) {
        const text = String(value || '').trim();
        if (['点到为止', '尽量生擒', '重伤压制', '必杀'].includes(text)) return text;
        return '点到为止';
      }

      function getBattleRelationRestraintFactor(attacker, target) {
        const attackerName = String(attacker?.name || '').trim();
        const relationMap = attacker?.社交?.关系 && typeof attacker.社交.关系 === 'object' ? attacker.社交.关系 : {};
        const relationData = relationMap[target?.name] || relationMap[attackerName] || {};
        const relationText = String(relationData?.关系 || '').trim();
        if (/恋人|生死之交|挚友/.test(relationText)) return 1.0;
        if (/亲密/.test(relationText)) return 0.82;
        if (/朋友/.test(relationText)) return 0.68;
        if (/认识/.test(relationText)) return 0.55;
        if (/敌视/.test(relationText)) return 0.18;
        if (/仇敌/.test(relationText)) return 0.05;
        return 0.4;
      }

      function getBattleEnvironmentSafetyFactor(combatData = {}) {
        const env = String(combatData?.环境 || '').trim();
        let score = 0.35;
        if (/试炼馆|斗魂场|演武|擂台|学院|城|塔|馆/.test(env)) score += 0.35;
        if (/荒野|黑市|深海|核心区|伏击|追杀/.test(env)) score -= 0.25;
        return Math.max(0, Math.min(1, score));
      }

      function getBattlePowerGapFactor(attacker, target) {
        const keys = ['力量', '防御', '敏捷', '体力上限', '精神力上限'];
        const diffs = keys.map(key => {
          const left = Math.max(1, Number(attacker?.[key] || attacker?.属性?.[key] || 1));
          const right = Math.max(1, Number(target?.[key] || target?.属性?.[key] || 1));
          return Math.abs(left - right) / Math.max(left, right, 1);
        });
        const avg = diffs.reduce((sum, value) => sum + value, 0) / Math.max(1, diffs.length);
        return Math.max(0, Math.min(1, avg));
      }

      function getBattleDesperationFactor(attacker) {
        const hpRatio = getCombatHpRatio(attacker);
        const staRatio = getCombatStaminaRatio(attacker);
        const score = ((1 - hpRatio) * 0.45) + ((1 - staRatio) * 0.55);
        return Math.max(0, Math.min(1, score));
      }

      function buildHpSuggestionPayload(attacker, defender, combatData, appliedDamage = 0) {
        const intentMode = normalizeBattleIntentMode(combatData?.战斗意图);
        const relationFactor = getBattleRelationRestraintFactor(attacker, defender);
        const environmentFactor = getBattleEnvironmentSafetyFactor(combatData);
        const powerGapFactor = getBattlePowerGapFactor(attacker, defender);
        const desperationFactor = getBattleDesperationFactor(attacker);
        const targetHpMax = getCombatHpMaxValue(defender);
        const targetHpCurrent = getCombatHpValue(defender);
        const controlScore = Math.max(0, Math.min(1, relationFactor * 0.45 + environmentFactor * 0.35 + powerGapFactor * 0.2 - desperationFactor * 0.35));
        let range = [0.01, 0.08];
        let suggestedResult = '重伤压制';
        if (intentMode === '尽量生擒') {
          range = controlScore >= 0.6 ? [0.2, 0.4] : [0.12, 0.28];
          suggestedResult = controlScore >= 0.58 ? '生擒制服' : '外界中断';
        } else if (intentMode === '重伤压制') {
          range = controlScore >= 0.6 ? [0.12, 0.24] : [0.05, 0.16];
          suggestedResult = '重伤压制';
        } else if (intentMode === '必杀') {
          range = [0, 0.15];
          suggestedResult = '致死确认';
        } else if (controlScore >= 0.75) {
          range = [0.35, 0.55];
          suggestedResult = '点到为止';
        } else if (controlScore >= 0.45) {
          range = [0.18, 0.35];
          suggestedResult = '点到为止';
        } else if (controlScore >= 0.2) {
          range = [0.08, 0.18];
          suggestedResult = '外界中断';
        }
        if (intentMode !== '必杀' && targetHpCurrent <= Math.ceil(targetHpMax * range[0])) {
          suggestedResult = intentMode === '尽量生擒' ? '生擒制服' : '点到为止';
        }
        const recommendedEndHp = Math.max(0, Math.min(targetHpCurrent, Math.round(targetHpMax * ((range[0] + range[1]) / 2))));
        const expectedDamage = Math.max(0, Math.max(Math.round(appliedDamage || 0), targetHpCurrent - recommendedEndHp));
        const desperationLevel = desperationFactor >= 0.75 ? '几乎失控' : desperationFactor >= 0.45 ? '失手偏高' : '稳住';
        return {
          建议终点HP区间: `${Math.round(range[0] * 100)}% - ${Math.round(range[1] * 100)}%`,
          前端推荐终点HP: recommendedEndHp,
          预计HP伤害: expectedDamage,
          前端建议结果: suggestedResult,
          裁断约束: {
            可致死: intentMode === '必杀',
            可外界介入: environmentFactor >= 0.55 || relationFactor >= 0.58,
            关系收手系数: Number(relationFactor.toFixed(3)),
            场地安全系数: Number(environmentFactor.toFixed(3)),
            实力差距系数: Number(powerGapFactor.toFixed(3)),
            绝境失手系数: Number(desperationFactor.toFixed(3)),
            失手等级: desperationLevel,
          },
        };
      }

      function chooseAndBuildActorAction(actor, target, battleState, candidates, phaseLabel, logPrefix = '') {
        const choice = chooseActorActionByCandidates(actor, target, battleState, candidates, phaseLabel);
        if (!choice.option) return null;

        const action = choice.option.build();
        const 候选命中名 = normalizeBattleActionDisplayName(choice?.option?.skill?.name || choice?.option?.skill?.魂技名 || choice?.option?.__预览技能?.name || choice?.option?.__预览技能?.魂技名 || choice?.option?.name || '');
        const 最终落地动作名 = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.type || choice?.option?.name || '');
        const 动作覆写来源 = 候选命中名 && 最终落地动作名 && 候选命中名 !== 最终落地动作名
          ? (phaseLabel || '主动规划')
          : '';
        if (choice?.option?.__主动规划审计 && typeof choice.option.__主动规划审计 === 'object') {
          choice.option.__主动规划审计.finalResolvedActionName = 最终落地动作名;
          choice.option.__主动规划审计.actionOverrideSource = 动作覆写来源;
          choice.option.__主动规划审计.hitCandidateName = 候选命中名;
          choice.option.__主动规划审计.目标语义 = String(
            action?.skill?.目标 ||
            action?.skill?.target ||
            choice.option.__主动规划审计.目标语义 ||
            ''
          ).trim();
          choice.option.__主动规划审计.承载方式 = String(
            action?.skill?.承载方式 ||
            choice.option.__主动规划审计.承载方式 ||
            ''
          ).trim();
        }
        if (action && typeof action === 'object') {
          action.__命中候选名 = 候选命中名;
          action.__最终落地动作名 = 最终落地动作名;
          action.__动作覆写来源 = 动作覆写来源;
          action.__目标语义 = String(action?.skill?.目标 || action?.skill?.target || '').trim();
          action.__承载方式 = String(action?.skill?.承载方式 || '').trim();
        }
        recordActorActionMemory(
          actor,
          action?.skill?.name || action?.skill?.技能名称 || action?.type || choice.option.name,
          { target: action?.target || target, round: Number(battleState?.combatData?.回合 || 0) },
        );
        const 经验 = battleState?.战斗经验;
        const 经验日志 = 经验
          ? ` [行为经验] 经验:${Math.round(Number(经验.总分 || 0))}/稳定:${Math.round(Number(经验.稳定度 || 0) * 100)}%。`
          : '';
        action.log = `${logPrefix}${choice.trace}${经验日志} ${action.log}`.trim();
        return action;
      }

      function clearCombatAdjudicationHints(combatData) {
        if (!combatData || typeof combatData !== 'object') return;
        delete combatData.阶段;
        delete combatData.裁断约束;
        delete combatData.前端建议结果;
        delete combatData.建议终点HP区间;
        delete combatData.前端推荐终点HP;
        delete combatData.预计HP伤害;
        delete combatData.本次操作;
      }

      function normalizeSoulTowerPendingSettlement(settlement = {}) {
        if (!settlement || typeof settlement !== 'object' || Array.isArray(settlement)) return null;
        const floor = Math.max(0, Math.floor(Number(settlement.层数 || 0)));
        const spirit = normalizeSoulTowerDiscountSpiritRecord(settlement.五折魂灵 || {});
        if (!(floor > 0 && spirit.层数 > 0)) return null;
        return {
          状态: '待选择',
          层数: floor,
          区域标签: String(settlement.区域标签 || '').trim() || getSoulTowerGateMeta(floor).gateLabel,
          区间标签: String(settlement.区间标签 || '').trim() || getSoulTowerGateMeta(floor).gateRangeLabel,
          守塔名称: String(settlement.守塔名称 || '').trim() || spirit.名称 || '',
          五折魂灵: spirit,
          下一层: Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(floor + 1, Math.floor(Number(settlement.下一层 || floor + 1)))),
          可继续: settlement.可继续 !== false && floor < SOUL_TOWER_TOTAL_FLOORS,
        };
      }

      function hasSoulTowerPendingSettlement(combatData = {}) {
        return !!normalizeSoulTowerPendingSettlement(combatData?.魂灵塔待结算);
      }

      function buildSoulTowerPendingSettlement(combatData = {}, defender = null) {
        const floor = Math.max(1, Math.floor(Number(combatData?.floor || 1)));
        const meta = getSoulTowerGateMeta(floor);
        const spiritRecord = buildSoulTowerDiscountSpiritRecordFromGuardian(defender, floor);
        if (!(spiritRecord.层数 > 0)) return null;
        return {
          状态: '待选择',
          层数: floor,
          区域标签: meta.gateLabel,
          区间标签: meta.gateRangeLabel,
          守塔名称: String(defender?.name || spiritRecord.名称 || '').trim(),
          五折魂灵: spiritRecord,
          下一层: Math.min(SOUL_TOWER_TOTAL_FLOORS, floor + 1),
          可继续: floor < SOUL_TOWER_TOTAL_FLOORS,
        };
      }

      function collectCombatSkills(charData, alliedTeam = []) {
        return collectUnifiedSkillEntries(charData, alliedTeam, { includePassive: false, includeActive: true });
      }

      function 创建行为链(先手 = {}, 后手 = {}) {
        return {
          链节点列表: [],
          当前先手: 先手,
          当前后手: 后手,
          主动权持有者: 先手,
          链深度: 0,
          事件链: [],
          已换招角色: new Set(),
        };
      }

      function 构建行为规划技能(skill = {}, target = null, actor = null, combatData = null) {
        const 施术者 = actor || {};
        const 规划上下文 = {
          行为规划: true,
          actor: 施术者,
          caster: 施术者,
          attacker: 施术者,
          target,
          defender: target,
          action: { action_type: '释放魂技', type: '技能', skill },
          skill,
          combatData,
          当前行动: '释放魂技',
          hit: true,
          命中: true,
          主原型成立: true,
        };
        const 规划技能 = { ...skill, __规划水合效果数组: getSkillEffects(skill, 规划上下文), __条件分支规划源技能: skill };
        写入战斗来源类别上下文(规划技能, 读取战斗来源类别上下文(skill, '魂技'));
        return 规划技能;
      }

      function 追加行为链节点(行为链, 节点 = {}) {
        if (!行为链 || 行为链.链深度 >= 5) return false;
        const 完整节点 = {
          阶段: String(节点.阶段 || '收束'),
          行动者: 节点.行动者 || null,
          目标: 节点.目标 || null,
          动作: 节点.动作 || '',
          技能: 节点.技能 || null,
          前摇: Math.max(0, Number(节点.前摇 || 0)),
          窗口: 节点.窗口,
          投点: 节点.投点,
          结果: 节点.结果 || '',
          战报: String(节点.战报 || '').trim(),
        };
        行为链.链节点列表.push(完整节点);
        行为链.事件链.push(完整节点.战报);
        行为链.链深度 += 1;
        return true;
      }

      function 渲染行为链战报(行为链) {
        const 文本 = (行为链?.链节点列表 || [])
          .map(节点 => 节点.战报 || `[${节点.阶段}] ${节点.结果 || ''}`)
          .filter(Boolean)
          .join(' ');
        if (行为链 && 行为链.链深度 >= 5) return `${文本} [收束] 双方连续变招已到极限，本回合攻防链收束。`;
        return 文本;
      }

      function 获取行动前摇(action = {}) {
        return Math.max(0, Number(action?.cast_time ?? action?.skill?.前摇 ?? 10) || 10);
      }

      function 获取行动威力倍率(action = {}) {
        return Math.max(0, Number(getPrimaryDamageEffect(action?.skill || {})?.威力倍率 || 0));
      }

      function 战斗候选状态转移(当前状态 = '', 下个状态 = '') {
        const from = String(当前状态 || '').trim().toUpperCase();
        const to = String(下个状态 || '').trim().toUpperCase();
        const allowed = ['SCORED', 'SAMPLED', 'LOCKED', 'EXECUTED', 'REJECTED', 'FILTERED', 'ABORTED', 'SELECTED', 'SHADOWED'];
        if (!to) return '';
        if (!allowed.includes(to)) throw new Error(`candidate_invalid_status:${to}`);
        if (
          (from === 'EXECUTED' && to === 'REJECTED') ||
          (from === 'EXECUTED' && to === 'FILTERED') ||
          (from === 'LOCKED' && to === 'FILTERED') ||
          (from === 'ABORTED' && to === 'REJECTED')
        ) {
          throw new Error(`candidate_illegal_transition:${from}->${to}`);
        }
        return to;
      }

      function 估算技能消耗压力(单位 = {}, skill = {}) {
        if (!skill || typeof skill !== 'object') return 0;
        const 消耗 = parseSkillCostForChar(skill, 单位);
        const 可用 = Math.max(1, Number(单位?.sp || 0) + Number(单位?.sta ?? 单位?.vit ?? 0) + Number(单位?.men || 0));
        return 限制行为概率((Number(消耗.reqSp || 0) + Number(消耗.reqVit || 0) + Number(消耗.reqMen || 0)) / 可用, 0, 1);
      }

      function 计算再判定窗口(先手 = {}, 后手 = {}, 先手动作 = {}, 后手动作 = {}) {
        const 先手属性 = 先手.final || buildCombatFinalStats(先手);
        const 后手属性 = 后手.final || buildCombatFinalStats(后手);
        const 后手前摇暴露 = 限制行为概率(获取行动前摇(后手动作) / 40, 0, 1) * 0.45;
        const 后手威力暴露 = 限制行为概率(获取行动威力倍率(后手动作) / 300, 0, 1) * 0.25;
        const 后手消耗暴露 = 估算技能消耗压力(后手, 后手动作?.skill || {}) * 0.20;
        const 反应差修正 = 限制行为概率((Number(先手属性.agi || 先手.agi || 1) / Math.max(1, Number(后手属性.agi || 后手.agi || 1)) - 1) * 0.15, -0.15, 0.15);
        const 先手投入惩罚 = 限制行为概率(获取行动前摇(先手动作) / 40, 0, 1) * 0.20;
        return 限制行为概率(0.10 + 后手前摇暴露 + 后手威力暴露 + 后手消耗暴露 + 反应差修正 - 先手投入惩罚, 0, 1);
      }

      function 估算反噬概率(先手 = {}, 后手 = {}, 原动作 = {}, 新技能 = {}, 窗口 = 0) {
        const 先手属性 = 先手.final || buildCombatFinalStats(先手);
        const 后手属性 = 后手.final || buildCombatFinalStats(后手);
        const 新招前摇压力 = 限制行为概率(getSkillCastTime(newSkillData(新技能)) / 40, 0, 1);
        const 原招投入度 = 限制行为概率(获取行动前摇(原动作) / 40, 0, 1);
        const 精神控制修正 = 限制行为概率((Number(先手属性.men_max || 先手.men_max || 1) / Math.max(1, Number(后手属性.men_max || 后手.men_max || 1)) - 1) * 0.10, -0.10, 0.10);
        return 限制行为概率(0.08 + (1 - 窗口) * 0.55 + 原招投入度 * 0.20 + 新招前摇压力 * 0.12 - 精神控制修正, 0.05, 0.95);
      }

      function newSkillData(skill = {}) {
        if (!skill || typeof skill !== 'object' || Array.isArray(skill)) return null;
        const 技能名 = String(skill.name || skill.魂技名 || skill.技能名称 || '').trim();
        const 效果数组 = Array.isArray(skill._效果数组) ? skill._效果数组.filter(Boolean) : [];
        if (!技能名 && !效果数组.length) return null;
        return normalizeSkillData(skill, 技能名 || '未命名技能');
      }

      function 评估敌对重复状态收益衰减(skill = {}, target = {}, options = {}) {
        if (!skill || !target?.状态效果 || typeof target.状态效果 !== 'object') return { 惩罚: 0, 状态列表: [] };
        const 状态列表 = getSkillEffects(skill, { 行为规划: true, target, defender: target })
          .filter(effect => String(effect?.原型 || '').trim() === '状态施加')
          .filter(effect => !/自身|友方/.test(String(effect?.目标 || getSkillTarget(skill) || '').trim()))
          .map(effect => String(effect?.状态名称 || effect?.状态 || '').trim())
          .filter(Boolean);
        if (!状态列表.length) return { 惩罚: 0, 状态列表: [] };
        const 伤害威力 = Number(getPrimaryDamageEffect(skill)?.威力倍率 || 0);
        const 伤害比例 = Number(options?.projectedDamageRatio || 0);
        const 低伤状态技 = 伤害威力 <= 80 && 伤害比例 < 0.03;
        const 已重复同技 = Math.max(0, Number(options?.recentSameActionCount || 0));
        const 已有状态 = Object.entries(target.状态效果 || {}).filter(([, cond]) => Math.max(0, Number(cond?.duration ?? cond?.持续回合 ?? 0)) > 0);
        const 命中状态 = 状态列表.filter(状态名 => 已有状态.some(([key, cond]) => {
          const 文本 = `${key} ${cond?.状态名称 || ''} ${cond?.名称 || ''}`;
          return 文本.includes(状态名) || 状态名.includes(String(key || ''));
        }));
        if (!命中状态.length && !(低伤状态技 && 已重复同技 > 0)) return { 惩罚: 0, 状态列表: [] };
        const 惩罚 = 低伤状态技 && 已重复同技 > 0
          ? 140 + 已重复同技 * 48 + 命中状态.length * 36
          : 24 + 命中状态.length * 10 + 已重复同技 * 8;
        return { 惩罚: Math.round(惩罚), 状态列表: 命中状态.length ? 命中状态 : 状态列表 };
      }

      function 计算换招成功率(先手 = {}, 后手 = {}, 原动作 = {}, 新技能 = {}, 窗口 = 0) {
        const 先手属性 = 先手.final || buildCombatFinalStats(先手);
        const 后手属性 = 后手.final || buildCombatFinalStats(后手);
        const 反应余量 = 限制行为概率(Number(先手属性.agi || 先手.agi || 1) / Math.max(1, Number(后手属性.agi || 后手.agi || 1)), 0, 2);
        const 新招前摇压力 = 限制行为概率(getSkillCastTime(newSkillData(新技能)) / 40, 0, 1);
        const 原招投入度 = 限制行为概率(获取行动前摇(原动作) / 40, 0, 1);
        const 精神控制修正 = 限制行为概率((Number(先手属性.men_max || 先手.men_max || 1) / Math.max(1, Number(后手属性.men_max || 后手.men_max || 1)) - 1) * 0.10, -0.10, 0.10);
        return 限制行为概率(0.15 + 窗口 * 0.55 + 反应余量 * 0.10 + 精神控制修正 - 原招投入度 * 0.20 - 新招前摇压力 * 0.15, 0.05, 0.90);
      }

      const 行为链换招窗口门槛 = 0.28;

      function 行动可换招(单位 = {}, 原动作 = {}) {
        if (!原动作?.skill || !['技能', '魂技', '释放技能'].some(词 => String(原动作.action_type || 原动作.type || '').includes(词)) && !原动作.skill) return false;
        if (['防御', '闪避', '撤离', '穿戴装备'].includes(String(原动作.action_type || 原动作.type || ''))) return false;
        return !Object.values(单位?.状态效果 || {}).some(状态 => {
          const 效果 = 状态?.战斗效果 || {};
          return 效果.skip_turn || 效果.cannot_react || 效果.silence || 效果.skill_seal || 效果.disarm;
        });
      }

      function 技能可进入换招候选池(单位 = {}, skill = {}) {
        if (!skill || typeof skill !== 'object' || skill.状态 === '未生成') return false;
        const 技能名 = String(skill.name || skill.魂技名 || skill.技能名称 || '').trim();
        if (!技能名) return false;
        const 技能效果数组 = getSkillEffects(skill, { 行为规划: true });
        if (!Array.isArray(技能效果数组) || !技能效果数组.some(effect => effect && typeof effect === 'object')) return false;
        if (单位已有同名维持技能(单位, skill)) return false;
        const 目标类型 = inferSkillPrimaryTargetKind(skill);
        const 纯友方支援 =
          ['自身', '友方单体', '友方群体'].includes(目标类型) &&
          isSupportLikeSkill(skill) &&
          !getPrimaryDamageEffect(skill) &&
          !isBattleSkillDefensiveProfile(skill) &&
          !isBattleSkillReactiveDefenseProfile(skill) &&
          !isBattleSkillCounterProfile(skill) &&
          !hasBattleSkillRuntimeConsumer(skill, ['self_shift', 'disengage_shift', 'position_exchange', 'pursuit_shift']);
        if (纯友方支援) return false;
        const 状态战斗效果 = Object.values(单位?.状态效果 || {}).map(状态 => 状态?.战斗效果 || {});
        if (状态战斗效果.some(效果 => 效果.skip_turn === true || 效果.cannot_react === true || 效果.silence === true || 效果.skill_seal === true || 效果.disarm === true)) return false;
        return parseSkillCostForChar(skill, 单位).canCast !== false;
      }

      function 推断行为链技能倾向(skill = {}) {
        if (!skill || typeof skill !== 'object') return '硬抗';
        if (Number(getPrimaryDamageEffect(skill, { 行为规划: true })?.威力倍率 || 0) > 0) return 获取行动前摇({ skill }) <= 12 ? '短前摇对轰' : '承伤硬抗';
        if (isBattleSkillDefensiveProfile(skill) || isBattleSkillReactiveDefenseProfile(skill)) return '防御';
        if (hasBattleSkillRuntimeConsumer(skill, ['self_shift', 'disengage_shift', 'position_exchange', 'pursuit_shift'])) return '位移规避';
        if (isBattleSkillControlProfile(skill) || hasBattleSkillRuntimeConsumer(skill, ['interrupt', 'hard_control', 'skill_seal'])) return '控制截断';
        if (isBattleSkillCounterProfile(skill) || hasBattleSkillRuntimeConsumer(skill, ['counter', 'on_hit_counter', 'damage_reflect'])) return '反制技能';
        return '状态/装备触发';
      }

      function 计算行为威胁分层(威胁 = {}) {
        const 分数 = 限制行为概率(
          Number(威胁?.projectedDamageRatio || 0) +
            (威胁?.severeControl ? 0.26 : 0) +
            (威胁?.lethalRisk ? 0.45 : 0) +
            (威胁?.shieldBreakThreat ? 0.08 : 0) +
            (威胁?.dotDetonateThreat ? 0.08 : 0),
          0,
          1,
        );
        const 致命 = 威胁?.lethalRisk === true || 分数 >= 0.58;
        const 高威胁 = 致命 || 威胁?.severeDamage === true || 威胁?.severeControl === true || 分数 >= 0.32;
        const 中威胁 = 高威胁 || 威胁?.moderateDamage === true || 分数 >= 0.16;
        const 低威胁 = !中威胁 && !威胁?.antiHealThreat && !威胁?.skillSealThreat;
        return { 分数, 致命, 高威胁, 中威胁, 低威胁 };
      }

      function 计算保守动作惩罚(单位 = {}, 动作名 = '', 威胁分层 = {}, 选项 = {}) {
        const 名称 = String(动作名 || '').trim();
        if (!名称 || 威胁分层.高威胁 || 威胁分层.致命) return 0;
        const 记忆 = ensureActorDecisionMemory(单位);
        const 近期次数 = 读取归一行为记忆值_V1(记忆.recent_actions || {}, [名称], 0);
        const 是支援系 = ['辅助系', '治疗系', '食物系', '召唤系'].includes(读取规划单位系别(单位));
        let 惩罚 = 近期次数 * 12;
        if (['肉体兜底', '伺机闪避', '防御', '危机自保', '收招转防'].includes(名称)) 惩罚 += 威胁分层.低威胁 ? 24 : 10;
        if (近期次数 >= 2 && ['肉体兜底', '伺机闪避', '防御', '危机自保', '收招转防'].includes(名称)) {
          惩罚 += (近期次数 - 1) * (威胁分层.低威胁 ? 24 : 14);
        }
        if (名称 === '肉体兜底') 惩罚 += 威胁分层.低威胁 ? 18 : 6;
        if (名称 === '伺机闪避' && 读取规划单位系别(单位) !== '敏攻系') 惩罚 += 威胁分层.低威胁 ? 12 : 4;
        if (是支援系 && 选项.有职责技能) 惩罚 += 16;
        return Math.max(0, Math.round(惩罚));
      }

      function 计算战术切换奖励(单位 = {}, 动作名 = '', 威胁分层 = {}) {
        const 名称 = String(动作名 || '').trim();
        const 上次行动 = String(ensureActorDecisionMemory(单位).last_action || '').trim();
        if (!名称 || !上次行动 || 名称 === 上次行动 || 威胁分层.致命) return 0;
        if (['强势对轰', '控制截断', '反制技能', '短前摇对轰', '破防强攻', '断疗压制', '支援补位', '统筹增援', '换招'].includes(名称)) return 10;
        return 0;
      }

      function 评估反高速应对窗口(防守方 = {}, 攻击方 = {}, 攻击动作 = {}, combatData = {}) {
        const 攻击者系别 = 读取规划单位系别(攻击方);
        const 防守敏捷 = Number(防守方?.final?.agi ?? 防守方?.agi ?? 防守方?.属性?.敏捷 ?? 0);
        const 攻击敏捷 = Number(攻击方?.final?.agi ?? 攻击方?.agi ?? 攻击方?.属性?.敏捷 ?? 0);
        const 敏捷倍率 = 攻击敏捷 / Math.max(1, 防守敏捷);
        const 伤害效果 = getPrimaryDamageEffect(攻击动作?.skill || {});
        const 近身高速攻击 =
          攻击者系别 === '敏攻系' &&
          战斗伤害是近身攻击(伤害效果?.伤害类型) &&
          (getSkillCastTime(攻击动作?.skill) <= 16 || 获取行动前摇(攻击动作) <= 16);
        const 闪避记忆 = Math.max(0, Number(攻击方?.__本回合闪避成功次数 || 0), Number(防守方?.决策记忆?.opponent_dodge_count || 0));
        const 久攻不下 = Number(combatData?.回合 || combatData?.round || 0) >= 3 && getCombatHpRatio(攻击方) > 0.55;
        const 距离拉扯 = /闪避|游击|拉开|脱离|瞬移/.test(String(ensureActorDecisionMemory(攻击方).last_action || ''));
        const 成立 = 敏捷倍率 >= 1.22 || 近身高速攻击 || 闪避记忆 >= 1 || 久攻不下 || 距离拉扯;
        const 强度 = 限制行为概率(
          (敏捷倍率 - 1) * 0.5 +
            (近身高速攻击 ? 0.35 : 0) +
            Math.min(0.25, 闪避记忆 * 0.12) +
            (久攻不下 ? 0.16 : 0) +
            (距离拉扯 ? 0.12 : 0),
          0,
          1,
        );
        return { 成立, 强度, 敏捷倍率, 近身高速攻击, 闪避记忆, 久攻不下, 距离拉扯 };
      }

      function 选取范围压制技能(技能列表 = []) {
        return (技能列表 || [])
          .map(skill => newSkillData(skill))
          .filter(skill => skill && getPrimaryDamageEffect(skill))
          .filter(skill => {
            const 目标 = String(getSkillTarget(skill) || '').trim();
            return /群体|全场/.test(目标);
          })
          .sort((a, b) => Number(getPrimaryDamageEffect(b)?.威力倍率 || 0) - Number(getPrimaryDamageEffect(a)?.威力倍率 || 0))[0] || null;
      }

      function 构建应招候选池(后手 = {}, 先手 = {}, 先手动作 = {}, combatData = {}, 预设应招 = null) {
        const 双方 = 读取战斗双方队伍(combatData, 后手);
        const 先手威胁 = estimateIncomingActionThreat(先手, 后手, 先手动作, combatData);
        const 规划上下文 = 构建规划上下文(后手, 先手, combatData, {
          combatData,
          threatProfile: 先手威胁,
          observedTargetAction: 先手动作,
          isChargingHighThreat: 先手威胁.severeDamage || 先手威胁.lethalRisk || 先手威胁.severeControl,
        });
        const 威胁分层 = 计算行为威胁分层(先手威胁);
        const 威胁系数 = 威胁分层.分数;
        const 反高速窗口 = 评估反高速应对窗口(后手, 先手, 先手动作, combatData);
        const 防御意图 = Number(规划上下文.战略意图?.权重表?.保核 || 0) + Number(规划上下文.战略意图?.权重表?.拖回合 || 0);
        const 反制意图 = Number(规划上下文.战略意图?.权重表?.控制 || 0) + Number(规划上下文.战略意图?.权重表?.击杀 || 0);
        const 来袭技能文本 = JSON.stringify(先手动作?.skill || {});
        const 来袭含控制减益 = 先手威胁.severeControl === true ||
          /状态施加|debuff|控制|眩晕|麻痹|僵直|混乱|沉默|封技|失控|无法反应|致盲|锁定|位移限制|迟缓|中毒|流血|防御剥夺|精神抗性剥夺|cast_speed_penalty|dodge_penalty|reaction_penalty/.test(来袭技能文本);
        const 试探阶段 = Number(combatData?.回合 || combatData?.round || 1) <= 1 && !先手威胁.lethalRisk && !先手威胁.severeDamage;
        const 后手记忆 = ensureActorDecisionMemory(后手);
        const 来袭动作名 = normalizeBattleActionDisplayName(先手动作?.skill?.name || 先手动作?.skill?.魂技名 || 先手动作?.action_type || 先手动作?.type || '');
        const 来袭者名 = String(先手?.name || 先手?.名称 || '').trim();
        const 同来袭重复次数 = 来袭动作名 && 后手记忆.last_incoming_action === 来袭动作名 && (!来袭者名 || 后手记忆.last_incoming_actor === 来袭者名)
          ? Math.max(0, Number(后手记忆.same_incoming_action_count || 0))
          : 0;
        const 防御基础 = (威胁分层.低威胁 ? 12 : 威胁分层.高威胁 ? 34 : 23) - (来袭含控制减益 ? 10 : 0);
        const 适应规避加权 = 同来袭重复次数 > 0 ? Math.min(42, 18 + 同来袭重复次数 * 12) : 0;
        const 适应硬抗惩罚 = 同来袭重复次数 > 0 ? Math.min(56, 22 + 同来袭重复次数 * 14) : 0;
        const 闪避基础 = (威胁分层.低威胁 ? 8 : 威胁分层.高威胁 ? 27 : 18) + (试探阶段 ? 18 : 0) + (来袭含控制减益 ? 20 : 0) + 适应规避加权;
        const 硬抗基础 = (威胁分层.低威胁 ? 52 : 威胁分层.高威胁 ? 16 : 32) - (试探阶段 ? 24 : 0) - (来袭含控制减益 ? 28 : 0) - 适应硬抗惩罚;
        const 反高速加权 = 反高速窗口.成立 ? Math.round(反高速窗口.强度 * 34) : 0;
        const 候选 = [
          { name: '防御', action: { type: '防御', action_type: '防御', skill: null, cast_time: 4 }, weight: Math.round(防御基础 + 威胁系数 * 42 + 防御意图 * 0.18 + Math.round(反高速加权 * 0.45) - 计算保守动作惩罚(后手, '防御', 威胁分层)), 分类: '防御' },
          { name: '闪避', action: { type: '闪避', action_type: '闪避', skill: null, cast_time: 6 }, weight: Math.round(闪避基础 + 威胁系数 * 34 + (读取规划单位系别(后手) === '敏攻系' ? 14 : 0) - Math.round(反高速加权 * 0.25) - 计算保守动作惩罚(后手, '伺机闪避', 威胁分层)), 分类: '闪避' },
          { name: '承伤硬抗', action: { type: '硬抗', action_type: '硬抗', skill: null, cast_time: 0 }, weight: Math.round(硬抗基础 + (1 - 威胁系数) * 26 + Math.round(反高速加权 * 0.7) + 计算战术切换奖励(后手, '承伤硬抗', 威胁分层)), 分类: '承伤硬抗' },
        ];
        if (反高速窗口.成立) {
          候选.push({
            name: '防守反击',
            action: { type: '硬抗', action_type: '硬抗', skill: null, cast_time: 0, __反高速应对: true },
            weight: Math.round(24 + 反高速窗口.强度 * 58 + (读取规划单位系别(后手) === '防御系' ? 12 : 0) + (读取规划单位系别(后手) === '强攻系' ? 9 : 0) - 计算保守动作惩罚(后手, '承伤硬抗', 威胁分层)),
            分类: '承伤硬抗',
            __规划轨迹: ['对手高速近身或持续拉扯，转入防守反击窗口'],
          });
        }
        if (预设应招 && (预设应招.skill || !/待机/.test(String(预设应招.type || 预设应招.action_type || '')))) {
          候选.push({
            name: 预设应招.skill?.name || 预设应招.skill?.魂技名 || 预设应招.type || '预设应招',
            action: 预设应招,
            weight: Math.round(42 + 威胁系数 * 35),
            分类: 推断行为链技能倾向(预设应招.skill || {}),
          });
        }
        collectCombatSkills(后手, 双方.友方)
          .map(skill => newSkillData(skill))
          .filter(skill => 技能可进入换招候选池(后手, skill))
          .forEach(skill => {
            const 分类 = 推断行为链技能倾向(skill);
            const 前摇压力 = 限制行为概率(getSkillCastTime(skill) / 40, 0, 1);
            const 威力 = 限制行为概率(Number(getPrimaryDamageEffect(skill)?.威力倍率 || 0) / 300, 0, 1);
            const 目标规模 = String(getSkillTarget(skill) || '').trim();
            const 范围压制价值 = 反高速窗口.成立 && /群体|全场/.test(目标规模) ? 0.42 + 反高速窗口.强度 * 0.36 : 0;
            const 短前摇价值 = 反高速窗口.成立 && 分类 === '短前摇对轰' ? 0.26 + 反高速窗口.强度 * 0.32 : 0;
            const 防御价值 = ['防御', '位移规避'].includes(分类) ? (威胁分层.低威胁 ? 0.08 : 0.35) + (来袭含控制减益 && 分类 === '位移规避' ? 0.28 : 0) : 分类 === '反制技能' ? 0.26 : 0;
            const 截断价值 = ['控制截断', '短前摇对轰'].includes(分类) ? (威胁分层.低威胁 ? 0.34 : 0.25) + (来袭含控制减益 ? 0.22 : 0) : 0;
            const 低威胁推进奖励 = 威胁分层.低威胁 && ['控制截断', '短前摇对轰', '反制技能'].includes(分类) ? 18 : 0;
            const 适应技能奖励 = 同来袭重复次数 > 0 && /闪避|位移规避|控制截断|短前摇对轰|反制技能|范围压制/.test(分类)
              ? Math.min(48, 16 + 同来袭重复次数 * 12)
              : 0;
            const 规划收益 = 评估技能规划净收益(skill, {
              actor: 后手,
              primaryTarget: 先手,
              behaviorState: { combatData, threatProfile: 先手威胁, observedTargetAction: 先手动作, isChargingHighThreat: 威胁系数 >= 0.45 },
              规划上下文,
            });
            const 反高速动作类型 = 范围压制价值 > 0 ? '范围压制' : 分类;
            候选.push({
              name: skill.name || skill.魂技名 || 分类,
              action: { type: 反高速动作类型, action_type: 反高速动作类型, skill, cast_time: getSkillCastTime(skill) || 10 },
              weight: Math.max(1, Math.round(18 + 威胁系数 * 42 + 防御价值 * 60 + 截断价值 * 50 + 范围压制价值 * 62 + 短前摇价值 * 52 + 威力 * 35 + 低威胁推进奖励 + 适应技能奖励 + Math.max(-28, Math.min(42, Number(规划收益.净收益 || 0) * 0.12)) + 反制意图 * Math.max(截断价值, 范围压制价值, 短前摇价值) * 0.18 + 计算战术切换奖励(后手, 反高速动作类型, 威胁分层) - 前摇压力 * (范围压制价值 > 0 ? 18 : 28) - 计算保守动作惩罚(后手, 分类, 威胁分层))),
              分类: 反高速动作类型,
              __规划轨迹: [
                ...(规划收益.目标理由 || []),
                ...(范围压制价值 > 0 ? ['范围攻击用于压缩高速目标闪避空间'] : []),
                ...(适应技能奖励 > 0 ? [`已连续见过${来袭动作名}，提高规避/截断/反制权重`] : []),
              ],
            });
          });
        const 最优规避或截断 = 候选
          .filter(候选项 => /闪避|位移规避|控制截断|短前摇对轰/.test(`${候选项.name || ''} ${候选项.分类 || ''}`))
          .sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0))[0];
        if (最优规避或截断 && (试探阶段 || 来袭含控制减益)) {
          候选.forEach(候选项 => {
            const 是通用防守 = !候选项.action?.skill && /防御|承伤硬抗/.test(`${候选项.name || ''} ${候选项.分类 || ''}`);
            if (!是通用防守) return;
            const 优势 = Number(最优规避或截断.weight || 0) - Number(候选项.weight || 0);
            if (来袭含控制减益 || 优势 >= 24) {
              候选项.weight = 0;
              候选项.__规划轨迹 = [
                ...(候选项.__规划轨迹 || []),
                来袭含控制减益
                  ? '来袭包含控制或减益，通用防御不能替代规避/截断判定'
                  : '试探阶段规避收益显著更高，通用硬抗退出应招池',
              ];
            }
          });
        }
        return 候选.filter(候选项 => Number(候选项.weight || 0) > 0);
      }

      function 选择行为链应招动作(后手 = {}, 先手 = {}, 先手动作 = {}, combatData = {}, 预设应招 = null) {
        const 候选 = 构建应招候选池(后手, 先手, 先手动作, combatData, 预设应招);
        const 抽取 = rollBranchByPriority(扰动候选权重列表(候选, 读取行动判断干扰强度(后手)), '行为链/应招');
        const 来袭动作名 = normalizeBattleActionDisplayName(先手动作?.skill?.name || 先手动作?.skill?.魂技名 || 先手动作?.action_type || 先手动作?.type || '');
        if (来袭动作名) {
          const 记忆 = ensureActorDecisionMemory(后手);
          const 来袭者名 = String(先手?.name || 先手?.名称 || '').trim();
          const 同来袭 = 记忆.last_incoming_action === 来袭动作名 && (!来袭者名 || 记忆.last_incoming_actor === 来袭者名);
          记忆.same_incoming_action_count = 同来袭 ? Math.min(5, Number(记忆.same_incoming_action_count || 0) + 1) : 1;
          记忆.last_incoming_action = 来袭动作名;
          记忆.last_incoming_actor = 来袭者名;
          记忆.last_reaction_action = 抽取.option?.name || 抽取.option?.action?.type || '';
        }
        记录行动闭环审计(combatData, '应招审计', {
          行动者: 后手?.name || 后手?.名称 || '',
          目标: 先手?.name || 先手?.名称 || '',
          技能: 抽取.option?.action?.skill?.name || 抽取.option?.action?.skill?.魂技名 || 抽取.option?.name || '',
          战略意图: 构建规划上下文(后手, 先手, combatData, { combatData, threatProfile: estimateIncomingActionThreat(先手, 后手, 先手动作, combatData) })?.战略意图?.主意图 || '',
          目标理由: 抽取.option?.__规划轨迹 || [],
          候选来源: '行为链应招候选池',
          原始净收益: Number(抽取.option?.weight || 0),
          干扰强度: 读取行动判断干扰强度(后手),
          最终权重: Number(抽取.option?.weight || 0),
          选择原因: 抽取.trace || '应招候选命中',
          旁路: false,
        });
        return 抽取.option?.action || 预设应招 || { type: '硬抗', action_type: '硬抗', skill: null, cast_time: 0 };
      }

      function 构建再判定候选池(窗口 = 0, 先手 = {}, 后手 = {}, 先手动作 = {}, 后手动作 = {}, combatData = {}, 行为链 = null) {
        const 后手威胁 = estimateIncomingActionThreat(后手, 先手, 后手动作, combatData);
        const 规划上下文 = 构建规划上下文(先手, 后手, combatData, {
          combatData,
          threatProfile: 后手威胁,
          observedTargetAction: 后手动作,
          isChargingHighThreat: 后手威胁.severeDamage || 后手威胁.lethalRisk || 后手威胁.severeControl,
        });
        const 威胁分层 = 计算行为威胁分层(后手威胁);
        const 威胁系数 = 威胁分层.分数;
        const 可换招 = 窗口 >= 行为链换招窗口门槛 && 存在可换招技能(先手, 先手动作, combatData, 行为链);
        const 意图 = 规划上下文.战略意图?.权重表 || {};
        const 行动记忆 = ensureActorDecisionMemory(先手);
        const 当前动作名 = String(先手动作?.skill?.name || 先手动作?.skill?.魂技名 || 先手动作?.action_type || '').trim();
        const 同技重复次数 = 读取归一行为记忆值_V1(行动记忆?.recent_actions || {}, [当前动作名], 0);
        const 连续压制次数 = 读取归一行为记忆值_V1(行动记忆?.recent_actions || {}, ['主动压迫', '反敏攻压制'], 0);
        const 当前动作技能 = 先手动作?.skill || null;
        const 当前动作具备真截断资格 = !!当前动作技能 && 判定技能具备真实截断资格_V1(当前动作技能);
        const 当前动作属于群体压制 = /群体|全场/.test(String(getSkillTarget(当前动作技能 || {}) || '').trim());
        const 连续前压应转守 =
          !威胁分层.高威胁 &&
          !威胁分层.致命 &&
          (
            连续压制次数 >= 2 ||
            (同技重复次数 >= 1 && !当前动作具备真截断资格 && !当前动作属于群体压制)
          );
        const 可收招转防 = 窗口 >= 0.18 && (威胁分层.中威胁 || Number(意图.保核 || 0) >= 58 || getCombatHpRatio(先手) < 0.45);
        const 候选池 = [
          { name: '抢落点', weight: Math.round(30 + (1 - 窗口) * 20 + 获取行动威力倍率(先手动作) / 20 + Number(意图.击杀 || 0) * 0.12 + (威胁分层.低威胁 ? 10 : 0)), action: '抢落点' },
          { name: '压招', weight: 窗口 >= 0.15 ? Math.round(20 + 窗口 * 35 + 威胁系数 * 25 + Number(意图.控制 || 0) * 0.16 + (威胁分层.低威胁 ? 8 : 0)) : 4, action: '压招' },
          { name: '偏转', weight: 窗口 >= 0.15 ? Math.round(16 + 窗口 * 28 + 威胁系数 * 30) : 0, action: '偏转' },
          { name: '收招转防', weight: 可收招转防 ? Math.round(10 + 窗口 * 22 + 威胁系数 * 38 + Number(意图.保核 || 0) * 0.14 - 计算保守动作惩罚(先手, '收招转防', 威胁分层)) : 0, action: '收招转防' },
          { name: '换招', weight: 可换招 ? Math.round(20 + 窗口 * 70 + 威胁系数 * 35 + Math.max(Number(意图.破防 || 0), Number(意图.驱散 || 0), Number(意图.维持压制 || 0)) * 0.16) : 0, action: '换招' },
          { name: '放弃再判定', weight: 威胁分层.低威胁 ? 20 : 威胁系数 > 0.35 ? 6 : 12, action: '放弃再判定' },
        ];
        if (连续前压应转守) {
          候选池.forEach(候选 => {
            if (!候选) return;
            if (['抢落点', '压招', '换招'].includes(候选.name)) 候选.weight = Math.max(0, Number(候选.weight || 0) - 28);
            if (候选.name === '收招转防') 候选.weight = Math.max(Number(候选.weight || 0), 52 + 连续压制次数 * 10 + 同技重复次数 * 8);
            if (候选.name === '放弃再判定') 候选.weight = Math.max(Number(候选.weight || 0), 28 + 连续压制次数 * 8);
          });
        }
        return 候选池;
      }

      function 应用再判定动作效果(再判定动作 = '', 先手动作 = {}, 后手动作 = {}, 窗口 = 0) {
        const 强度 = Number((0.04 + 限制行为概率(窗口, 0, 1) * 0.16).toFixed(4));
        先手动作.__行为链修正 ||= {};
        后手动作.__行为链修正 ||= {};
        if (再判定动作 === '抢落点') {
          先手动作.__行为链修正.hit_bonus = Math.max(Number(先手动作.__行为链修正.hit_bonus || 0), 强度);
          先手动作.__行为链修正.landing_bonus = Math.max(Number(先手动作.__行为链修正.landing_bonus || 0), 强度);
          return `抢落点提高当前技能落地与命中，强度${Math.round(强度 * 100)}%。`;
        }
        if (再判定动作 === '压招') {
          后手动作.__行为链修正.hit_penalty = Math.max(Number(后手动作.__行为链修正.hit_penalty || 0), 强度);
          后手动作.__行为链修正.interrupt_penalty = Math.max(Number(后手动作.__行为链修正.interrupt_penalty || 0), 强度);
          return `压招压低后手命中与打断效率，强度${Math.round(强度 * 100)}%。`;
        }
        if (再判定动作 === '偏转') {
          后手动作.__行为链修正.random_target_rate = Math.max(Number(后手动作.__行为链修正.random_target_rate || 0), 强度);
          后手动作.__行为链修正.damage_reduction = Math.max(Number(后手动作.__行为链修正.damage_reduction || 0), 强度 * 0.5);
          return `偏转扰乱后手落点，强度${Math.round(强度 * 100)}%。`;
        }
        if (再判定动作 === '收招转防') {
          先手动作.__行为链修正.damage_reduction = Math.max(Number(先手动作.__行为链修正.damage_reduction || 0), 强度);
          先手动作.__行为链修正.dodge_bonus = Math.max(Number(先手动作.__行为链修正.dodge_bonus || 0), 强度 * 0.75);
          先手动作.__行为链收招 = true;
          return `收招转防放弃部分攻势并获得防御/闪避收益，强度${Math.round(强度 * 100)}%。`;
        }
        return '';
      }

      function 选择换招技能(单位 = {}, 对手 = {}, 原动作 = {}, 后手动作 = {}, combatData = {}, 窗口 = 0, 行为链 = null) {
        if (!行动可换招(单位, 原动作) || 窗口 < 行为链换招窗口门槛 || 行为链?.已换招角色?.has(单位.name || 单位.名称 || '')) return null;
        const 原技能名 = String(原动作?.skill?.name || 原动作?.skill?.魂技名 || '').trim();
        const 队伍 = 读取战斗双方队伍(combatData, 单位).友方 || [];
        const 后手威胁 = estimateIncomingActionThreat(对手, 单位, 后手动作, combatData);
        const 后手威胁评分 = 限制行为概率(Number(后手威胁.projectedDamageRatio || 0) + (后手威胁.severeControl ? 0.35 : 0) + (后手威胁.lethalRisk ? 0.45 : 0), 0, 1);
        const 威胁分层 = 计算行为威胁分层(后手威胁);
        const 行动记忆 = ensureActorDecisionMemory(单位);
        const 原动作重复次数 = 读取归一行为记忆值_V1(行动记忆?.recent_actions || {}, [原技能名], 0);
        const 连续压制次数 = 读取归一行为记忆值_V1(行动记忆?.recent_actions || {}, ['主动压迫', '反敏攻压制'], 0);
        const 连续前压应收招 =
          !威胁分层.高威胁 &&
          !威胁分层.致命 &&
          (连续压制次数 >= 2 || 原动作重复次数 >= 1);
        const 规划上下文 = 构建规划上下文(单位, 对手, combatData, {
          combatData,
          threatProfile: 后手威胁,
          observedTargetAction: 后手动作,
          isChargingHighThreat: 后手威胁评分 >= 0.5,
        });
        const 原技能剩余价值 = 限制行为概率((获取行动威力倍率(原动作) / 300) * 0.65 + (1 - 限制行为概率(获取行动前摇(原动作) / 40, 0, 1)) * 0.35, 0, 1);
        const 当前资源压力 = 1 - 限制行为概率((Number(单位.sp || 0) + Number(单位.men || 0) + Number(单位.sta ?? 单位.vit ?? 0)) / Math.max(1, Number(单位.sp_max || 0) + Number(单位.men_max || 0) + Number(单位.sta_max ?? 单位.vit_max ?? 0)), 0, 1);
        const 候选 = collectCombatSkills(单位, 队伍)
          .map(skill => newSkillData(skill))
          .filter(skill =>
            skill &&
            String(skill.name || skill.魂技名 || '').trim() &&
            String(skill.name || skill.魂技名 || '').trim() !== 原技能名 &&
            !/^换招技能|未命名技能$/.test(String(skill.name || skill.魂技名 || '').trim())
          )
          .filter(skill => 技能可进入换招候选池(单位, skill))
          .map(skill => {
            const 克制评分 = 限制行为概率((Number(getPrimaryDamageEffect(skill)?.威力倍率 || 0) / 320) + (isBattleSkillDefensiveProfile(skill) ? 0.25 : 0) + (isBattleSkillControlProfile(skill) ? 0.25 : 0), 0, 1);
            const 反噬预估 = 估算反噬概率(单位, 对手, 原动作, skill, 窗口);
            const 规划收益 = 评估技能规划净收益(skill, {
              actor: 单位,
              primaryTarget: 对手,
              behaviorState: {
                combatData,
                threatProfile: 后手威胁,
                observedTargetAction: 后手动作,
                isChargingHighThreat: 后手威胁评分 >= 0.5,
              },
              规划上下文,
            });
            const 原技能分类 = inferMainTypeFromEffects(原动作?.skill || {});
            const 新技能分类 = inferMainTypeFromEffects(skill || {});
            const 意图改变加成 =
              原技能分类 && 新技能分类 && 原技能分类 !== 新技能分类
                ? Math.max(Number(规划上下文.战略意图?.权重表?.破防 || 0), Number(规划上下文.战略意图?.权重表?.治疗 || 0), Number(规划上下文.战略意图?.权重表?.控制 || 0)) * 0.18
                : -10;
            const 特殊原型惩罚 = 计算换招特殊原型惩罚(skill, 单位, 对手, { combatData, threatProfile: 后手威胁 }, 后手威胁);
            const 新技能具备真截断资格 = 判定技能具备真实截断资格_V1(skill);
            const 新技能属于群体压制 = /群体|全场/.test(String(getSkillTarget(skill || {}) || '').trim());
            const 连续前压换招惩罚 =
              连续前压应收招 && !新技能具备真截断资格 && !新技能属于群体压制
                ? 42 + 连续压制次数 * 10 + 原动作重复次数 * 8
                : 0;
            return {
              skill,
              name: skill.name || skill.魂技名 || '换招技能',
              weight: Math.max(0, Math.round(
                20 +
                  窗口 * 70 +
                  后手威胁评分 * 45 +
                  克制评分 * 35 +
                  Math.max(-30, Math.min(46, Number(规划收益.净收益 || 0) * 0.18)) +
                  意图改变加成 -
                  原技能剩余价值 * 30 -
                  当前资源压力 * 45 -
                  反噬预估 * 60 -
                  特殊原型惩罚 -
                  连续前压换招惩罚,
              )),
            };
          });
        const 结果 = rollBranchByPriority(扰动候选权重列表(候选, 读取行动判断干扰强度(单位)), '行为链/换招技能池');
        return 结果.option?.skill ? { skill: 结果.option.skill, trace: 结果.trace } : null;
      }

      function 存在可换招技能(单位 = {}, 原动作 = {}, combatData = {}, 行为链 = null) {
        if (!行动可换招(单位, 原动作) || 行为链?.已换招角色?.has(单位.name || 单位.名称 || '')) return false;
        const 原技能名 = String(原动作?.skill?.name || 原动作?.skill?.魂技名 || '').trim();
        const 队伍 = 读取战斗双方队伍(combatData, 单位).友方 || [];
        return collectCombatSkills(单位, 队伍)
          .map(skill => newSkillData(skill))
          .some(skill =>
            skill &&
            String(skill.name || skill.魂技名 || '').trim() &&
            String(skill.name || skill.魂技名 || '').trim() !== 原技能名 &&
            !/^换招技能|未命名技能$/.test(String(skill.name || skill.魂技名 || '').trim()) &&
            技能可进入换招候选池(单位, skill)
          );
      }

      function 结算换招反噬消耗(单位 = {}, 原动作 = {}) {
        if (!原动作?.skill) return { 成功: true, 日志: '' };
        const 解析消耗 = parseSkillCostForChar({ ...原动作.skill, 消耗: splitSkillCostModes(getSkillCostText(原动作.skill)).upfront || '无' }, 单位);
        const 不足 = !解析消耗.canCast;
        设置战斗延迟效果资源值(单位, 'sp', 读取单位资源当前值(单位, 'sp') - Number(解析消耗.reqSp || 0));
        设置战斗体力值(单位, 读取单位资源当前值(单位, 'vit') - Number(解析消耗.reqVit || 0));
        设置战斗延迟效果资源值(单位, 'men', 读取单位资源当前值(单位, 'men') - Number(解析消耗.reqMen || 0));
        return {
          成功: !不足,
          日志: `[反噬] ${单位.name || '行动者'}上一魂技脉络回冲，额外消耗${formatParsedCost(解析消耗)}${不足 ? '，资源不足，经脉紊乱，新招中断' : ''}。`,
        };
      }

      function 自动行为链再判定(先手 = {}, 后手 = {}, 先手动作 = {}, 后手动作 = {}, combatData = {}) {
        const 行为链 = 创建行为链(先手, 后手);
        追加行为链节点(行为链, {
          阶段: '起招',
          行动者: 先手,
          目标: 后手,
          动作: 先手动作.action_type || 先手动作.type || '起招',
          技能: 先手动作.skill || null,
          前摇: 获取行动前摇(先手动作),
          战报: `[起招] ${先手.name || '先手'}以[${先手动作.skill?.name || 先手动作.action_type || '行动'}]起招。`,
        });
        追加行为链节点(行为链, {
          阶段: '应招',
          行动者: 后手,
          目标: 先手,
          动作: 后手动作.type || 后手动作.action_type || '应招',
          技能: 后手动作.skill || null,
          前摇: 获取行动前摇(后手动作),
          战报: String(后手动作.type || 后手动作.action_type || '').trim() === '无法反应'
            ? `[先手压制] ${后手.name || '后手'}来不及完整应对${先手.name || '先手'}的起招。`
            : `[应招] ${后手.name || '后手'}以[${后手动作.skill?.name || 后手动作.type || '应招'}]应对。`,
        });
        const 应招规划上下文 = 构建规划上下文(后手, 先手, combatData, {
          combatData,
          threatProfile: estimateIncomingActionThreat(先手, 后手, 先手动作, combatData),
        });
        记录行动闭环审计(combatData, '应招审计', {
          行动者: 后手?.name || 后手?.名称 || '',
          目标: 先手?.name || 先手?.名称 || '',
          技能: 后手动作?.skill?.name || 后手动作?.skill?.魂技名 || 后手动作?.type || 后手动作?.action_type || '',
          战略意图: 应招规划上下文?.战略意图?.主意图 || '',
          目标理由: 应招规划上下文?.目标优先表?.敌方?.find?.(项 => 项.单位 === 先手)?.理由 || [],
          候选来源: '正式行为链应招动作',
          原始净收益: 0,
          干扰强度: 读取行动判断干扰强度(后手),
          最终权重: 0,
          选择原因: '正式应招动作进入行为链',
          旁路: false,
        });
        const 窗口 = 计算再判定窗口(先手, 后手, 先手动作, 后手动作);
        const 再判定候选 = 构建再判定候选池(窗口, 先手, 后手, 先手动作, 后手动作, combatData, 行为链);
        const 再判定选项 = [...再判定候选]
          .filter(候选 => 候选 && Number(候选.weight || 0) > 0)
          .sort((左, 右) =>
            Number(右.weight || 0) - Number(左.weight || 0) ||
            String(左.name || '').localeCompare(String(右.name || ''), 'zh-Hans-CN')
          )[0] || null;
        const 再判定 = {
          option: 再判定选项,
          trace: 再判定选项 ? '战术重心锁定' : '当前未形成有效触发窗口',
        };
        const 再判定动作 = 再判定.option?.action || '放弃再判定';
        const 再判定候选排序结果 = 再判定候选.map((候选, index) => {
          const 名称 = String(候选?.name || '').trim();
          const selected = 再判定.option && 候选 === 再判定.option;
          const weight = Number(候选?.weight || 0);
          return {
            candidateId: `recheck_candidate_${index + 1}_${normalizeBattleActionDisplayName(名称) || 'action'}`,
            candidateName: 名称,
            名称,
            score: weight,
            权重: weight,
            candidateStatus: 战斗候选状态转移('', selected ? 'EXECUTED' : (weight > 0 ? 'REJECTED' : 'FILTERED')),
            rejectionCode: selected ? '' : (weight > 0 ? 'LOWER_PRIORITY' : 'FILTERED_BY_SCORE'),
          };
        });
        const 再判定执行者 = 后手;
        const 再判定目标 = 先手;
        const 来源动作名 = normalizeBattleActionDisplayName(先手动作?.skill?.name || 先手动作?.skill?.魂技名 || 先手动作?.action_type || '');
        const 再判定审计详情 = {
          行动者: 再判定执行者?.name || 再判定执行者?.名称 || '',
          目标: 再判定目标?.name || 再判定目标?.名称 || '',
          技能: 再判定动作,
          sourceActionName: 来源动作名,
          reactionActorName: 再判定执行者?.name || 再判定执行者?.名称 || '',
          sourceActorName: 先手?.name || 先手?.名称 || '',
          replanActionName: 再判定动作,
          replanRole: 'defender',
          战略意图: 构建规划上下文(再判定执行者, 再判定目标, combatData, { combatData, threatProfile: estimateIncomingActionThreat(先手, 后手, 先手动作, combatData) })?.战略意图?.主意图 || '',
          候选来源: '行为链再判定候选池',
          原始净收益: Number(再判定.option?.weight || 0),
          干扰强度: 读取行动判断干扰强度(再判定执行者),
          最终权重: Number(再判定.option?.weight || 0),
          选择原因: 再判定.trace || 再判定动作,
          hitCandidateName: 再判定动作,
          selectedCandidateName: 再判定动作,
          finalResolvedActionName: 再判定动作,
          actionOverrideSource: '',
          候选排序结果: 再判定候选排序结果,
          旁路: false,
        };
        追加行为链节点(行为链, {
          阶段: '再判定',
          行动者: 再判定执行者,
          目标: 再判定目标,
          窗口,
          结果: 再判定动作,
          战报: `[再判定] ${再判定执行者.name || '后手'}获得${窗口.toFixed(2)}窗口，选择${再判定动作}应对${来源动作名 || '来袭动作'}。`,
        });
        const 再判定效果日志 = 应用再判定动作效果(再判定动作, 先手动作, 后手动作, 窗口);
        if (再判定效果日志) {
          追加行为链节点(行为链, {
            阶段: 再判定动作,
            行动者: 再判定执行者,
            目标: 再判定目标,
            战报: `[${再判定动作}] ${再判定执行者.name || '后手'}${再判定效果日志}`,
          });
        }
        if (再判定动作 === '收招转防') {
          const 原动作名 = String(先手动作?.skill?.name || 先手动作?.skill?.魂技名 || 先手动作?.action_type || '行动').trim() || '行动';
          const 收招防御技能 = normalizeSkillData({
            name: '收招转防',
            魂技名: '收招转防',
            技能分类: '防御',
            目标: '自身',
            消耗: '无',
            前摇: 8,
            _效果数组: [],
          }, '收招转防');
          先手动作.__原定动作名 ||= 原动作名;
          先手动作.__原定技能名 ||= 原动作名;
          先手动作.action_type = '收招转防';
          先手动作.type = '收招转防';
          先手动作.skill = 收招防御技能;
          先手动作.cast_time = Math.min(Number(先手动作.cast_time || 8) || 8, 8);
          先手动作.target_name = String(先手?.name || 先手?.名称 || '').trim();
          先手动作.decision_log = `[收招转防] ${先手.name || '先手'}确认继续前压收益不足，收住攻势，转入守势稳住防线。`;
          再判定审计详情.finalResolvedActionName = '收招转防';
          再判定审计详情.技能 = '收招转防';
          再判定审计详情.replanActionName = '收招转防';
          再判定审计详情.actionOverrideSource = '再判定';
        }
        记录行动闭环审计(combatData, '再判定审计', 再判定审计详情);
        if (再判定动作 === '换招') {
          const 换招 = 选择换招技能(先手, 后手, 先手动作, 后手动作, combatData, 窗口, 行为链);
          if (换招?.skill) {
            const 换招候选排序结果 = collectCombatSkills(先手, 读取战斗双方队伍(combatData, 先手).友方 || [])
              .map(skill => newSkillData(skill))
              .filter(skill =>
                skill &&
                String(skill.name || skill.魂技名 || '').trim() &&
                String(skill.name || skill.魂技名 || '').trim() !== String(先手动作?.__原定技能名 || 先手动作?.skill?.name || 先手动作?.skill?.魂技名 || '').trim() &&
                !/^换招技能|未命名技能$/.test(String(skill.name || skill.魂技名 || '').trim()) &&
                技能可进入换招候选池(先手, skill)
              )
              .map((skill, index) => {
                const 名称 = String(skill?.name || skill?.魂技名 || '').trim();
                const selected = 名称 === String(换招.skill?.name || 换招.skill?.魂技名 || '').trim();
                return {
                  candidateId: `swap_candidate_${index + 1}_${normalizeBattleActionDisplayName(名称) || 'action'}`,
                  candidateName: 名称,
                  名称,
                  score: selected ? 100 : 0,
                  权重: selected ? 100 : 0,
                  candidateStatus: 战斗候选状态转移('', selected ? 'EXECUTED' : 'FILTERED'),
                  rejectionCode: selected ? '' : 'FILTERED_BY_SCORE',
                };
              });
            const 换招审计详情 = {
              行动者: 先手?.name || 先手?.名称 || '',
              目标: 后手?.name || 后手?.名称 || '',
              技能: 换招.skill?.name || 换招.skill?.魂技名 || '',
              战略意图: 构建规划上下文(先手, 后手, combatData, { combatData, threatProfile: estimateIncomingActionThreat(后手, 先手, 后手动作, combatData) })?.战略意图?.主意图 || '',
              候选来源: '行为链换招技能池',
              干扰强度: 读取行动判断干扰强度(先手),
              选择原因: 换招.trace || '换招候选命中',
              hitCandidateName: normalizeBattleActionDisplayName(换招.skill?.name || 换招.skill?.魂技名 || ''),
              selectedCandidateName: normalizeBattleActionDisplayName(换招.skill?.name || 换招.skill?.魂技名 || ''),
              finalResolvedActionName: normalizeBattleActionDisplayName(换招.skill?.name || 换招.skill?.魂技名 || ''),
              actionOverrideSource: '换招',
              候选排序结果: 换招候选排序结果,
            };
            const 成功率 = 计算换招成功率(先手, 后手, 先手动作, 换招.skill, 窗口);
            const 投点 = Math.random();
            const 成功 = 投点 <= 成功率;
            追加行为链节点(行为链, {
              阶段: '换招',
              行动者: 先手,
              目标: 后手,
              技能: 换招.skill,
              投点,
              结果: 成功 ? '通过' : '失败',
              战报: `[换招] ${先手.name || '先手'}尝试换出[${换招.skill.name || 换招.skill.魂技名 || '新招'}]，成功率${Math.round(成功率 * 100)}%，Roll ${Math.round(投点 * 100)}，${成功 ? '通过' : '失败'}。`,
            });
            const 反噬概率 = 成功 ? 估算反噬概率(先手, 后手, 先手动作, 换招.skill, 窗口) : 1;
            const 反噬投点 = Math.random();
            const 触发反噬 = !成功 || 反噬投点 <= 反噬概率;
            if (触发反噬) {
              const 反噬 = 结算换招反噬消耗(先手, 先手动作);
              追加行为链节点(行为链, {
                阶段: '换招反噬',
                行动者: 先手,
                投点: 反噬投点,
                结果: 反噬.成功 ? '反噬后继续' : '经脉紊乱',
                战报: `${反噬.日志} 反噬概率${Math.round(反噬概率 * 100)}%，Roll ${Math.round(反噬投点 * 100)}。`,
              });
              if (!反噬.成功 || !成功) {
                先手动作.action_type = '换招失败';
                先手动作.skill = null;
              }
            }
            if (成功 && (!触发反噬 || 先手动作.action_type !== '换招失败')) {
              const 新动作 = { ...先手动作, action_type: '换招', type: '换招', skill: 换招.skill, cast_time: getSkillCastTime(换招.skill) || 10 };
              const 新招消耗日志 = applyActionCost(先手, 新动作, 后手, combatData);
              if (新招消耗日志) 追加行为链节点(行为链, { 阶段: '换招', 行动者: 先手, 技能: 换招.skill, 战报: 新招消耗日志 });
              Object.assign(先手动作, 新动作);
              行为链.已换招角色.add(先手.name || 先手.名称 || '');
            } else {
              换招审计详情.finalResolvedActionName = normalizeBattleActionDisplayName(先手动作?.skill?.name || 先手动作?.skill?.魂技名 || 先手动作?.action_type || '');
              if (先手动作.action_type === '换招失败') 换招审计详情.actionOverrideSource = '换招失败';
            }
            记录行动闭环审计(combatData, '换招审计', 换招审计详情);
          }
        }
        追加行为链节点(行为链, {
          阶段: '收束',
          行动者: 行为链.主动权持有者,
          战报: `[收束] 本回合行为链结束，${行为链.主动权持有者?.name || '先手'}保持主动权。`,
        });
        return { 行为链, 日志: 渲染行为链战报(行为链) };
      }

      function getBattleActionDisplayName(action) {
        const skill = action?.skill && typeof action.skill === 'object' ? action.skill : {};
        return String(
          skill.魂技名 ||
            skill.name ||
            skill.技能名称 ||
            action?.name ||
            action?.action_type ||
            action?.type ||
            '战斗行动',
        ).trim();
      }

      function shouldShowBattleActionTarget(action) {
        const skill = action?.skill && typeof action.skill === 'object' ? action.skill : {};
        const actionType = String(action?.action_type || action?.type || '');
        const skillTargetText = String(
          getSkillTarget(skill) ||
            skill.目标 ||
            (Array.isArray(skill._效果数组)
              ? skill._效果数组.map(effect => `${effect?.目标 || ''}`).join(' ')
              : ''),
        );
        if (/防御|闪避|撤离|治疗|回复|恢复|造物|食物/.test(actionType) && !/攻击|伤害/.test(actionType)) return false;
        if (/自身|己方|友方|召唤物/.test(skillTargetText) && !/敌|对手/.test(skillTargetText)) return false;
        return true;
      }

      function buildVisibleBattlePlayerInput(rawInput, action, combatData) {
        const name = getBattleActionDisplayName(action);
        const skill = action?.skill && typeof action.skill === 'object' ? action.skill : {};
        if (String(skill?.承载方式 || '').trim() === '造物承载') {
          const mode = String(action?.造物处理 || '生成到自己背包').trim() || '生成到自己背包';
          const target = String(action?.食用目标 || action?.物品接收者 || action?.target_name || '').trim();
          if (mode === '生成给友方' && target) return `凝成【${name}】，交给【${target}】。`;
          if (mode === '立即自用') return `凝成并立即使用【${name}】。`;
          if (mode === '立即给目标使用' && target) return `凝成【${name}】，并立即让【${target}】使用。`;
          return `凝成【${name}】。`;
        }
        const fallbackTarget =
          action?.target_name ||
          combatData?.参战者?.team_enemy?.[0]?.name ||
          window.BattleUIBridge?.getMVU?.('world.战斗.参战者.team_enemy.0.name') ||
          '';
        if (shouldShowBattleActionTarget(action) && fallbackTarget) return `对【${fallbackTarget}】使用【${name}】。`;
        if (/防御|闪避|撤离/.test(name)) return `执行【${name}】。`;
        return `使用【${name}】。`;
      }

      function 计算撤离压制分数(单位 = {}, 对手 = {}, 立场 = '撤离') {
        bindCombatParticipant(单位);
        bindCombatParticipant(对手);
        const 最终属性 = 单位.final || buildCombatFinalStats(单位);
        const 对手最终属性 = 对手.final || buildCombatFinalStats(对手);
        const 敏捷 = Math.max(0, Number(最终属性.agi || 单位.agi || 0));
        const 当前精神 = 读取战斗资源当前值(单位, 最终属性, 'men');
        const 精神上限 = 读取战斗资源上限值(单位, 最终属性, 'men');
        const 当前体力 = Math.max(0, Number(最终属性.vit || 单位.vit || 单位.sta || 0));
        const 体力上限 = Math.max(1, Number(最终属性.vit_max || 单位.vit_max || 单位.sta_max || 1));
        const 精神比例 = Math.max(0, Math.min(1, 当前精神 / Math.max(1, 精神上限)));
        const 体力比例 = Math.max(0, Math.min(1, 当前体力 / 体力上限));
        const 效果列表 = 单位?.状态效果 && typeof 单位.状态效果 === 'object'
          ? Object.values(单位.状态效果).map(状态 => 状态?.战斗效果 || {})
          : [];
        const 锁定压力 = 效果列表.reduce((合计, 效果) => 合计 + Number(效果.lock_level || 0) * 18, 0);
        const 闪避修正 = 效果列表.reduce(
          (合计, 效果) => 合计 + Number(效果.dodge_bonus || 0) * 100 - Number(效果.dodge_penalty || 0) * 100,
          0,
        );
        const 反应修正 = 效果列表.reduce(
          (合计, 效果) => 合计 + Number(效果.reaction_bonus || 0) * 80 - Number(效果.reaction_penalty || 0) * 80,
          0,
        );
        const 硬控惩罚 = 效果列表.some(效果 => 效果.skip_turn === true || 效果.cannot_react === true) ? 999999 : 0;
        const 精神压制 = 计算当前资源压制倍率(单位, 最终属性, 对手, 对手最终属性, 'men', {
          下限: 0.35,
          上限: 1.65,
          压制指数: 0.35,
        });
        const 基础分 = 敏捷 * 0.72 + 当前精神 * 0.012 + 精神上限 * 0.025;
        const 状态分 = 基础分 * (0.35 + 精神比例 * 0.4 + 体力比例 * 0.25) * 精神压制;
        const 立场倍率 = 立场 === '追击' ? 1.08 : 1;
        return Math.max(0, 状态分 * 立场倍率 + 闪避修正 + 反应修正 - 锁定压力 - 硬控惩罚);
      }

      function 结算撤离判定(撤离单位, 追击单位) {
        const 撤离者名 = getCombatReportUnitName(撤离单位, '我方角色');
        const 追击者名 = getCombatReportUnitName(追击单位, '敌方角色');
        const 撤离分 = 计算撤离压制分数(撤离单位, 追击单位, '撤离');
        const 追击分 = 计算撤离压制分数(追击单位, 撤离单位, '追击');
        const 比值 = 撤离分 / Math.max(1, 追击分);
        const 投点 = Math.random();
        const 成功率 = Math.max(0.03, Math.min(0.92, (比值 - 0.72) * 0.55));
        const 半成功率 = Math.max(成功率, Math.min(0.96, 成功率 + 0.24));
        if (比值 >= 1.18 || 投点 < 成功率) {
          return {
            dmg: 0,
            desc: `[脱离成功] ${撤离者名}以更严格的闪避节奏强行拉开战圈，成功摆脱${追击者名}的追击。(撤离:${Math.round(撤离分)} 追击:${Math.round(追击分)} Roll:${投点.toFixed(2)})`,
            extraPatchOps: [],
            撤离结果: '成功',
            targetResults: [],
          };
        }
        if (比值 >= 0.9 || 投点 < 半成功率) {
          const 追击伤害 = Math.max(1, Math.floor(getCombatHpMaxValue(撤离单位) * 0.04));
          return {
            dmg: 追击伤害,
            desc: `[脱离半成] ${撤离者名}拉开距离但未能完全甩脱${追击者名}，撤离窗口尚未稳定，承受一次追击压迫。(撤离:${Math.round(撤离分)} 追击:${Math.round(追击分)} Roll:${投点.toFixed(2)})`,
            extraPatchOps: [],
            撤离结果: '半成',
            targetResults: [{ target: 撤离单位, targetName: 撤离者名, damage: 追击伤害, kind: '撤离追击' }],
          };
        }
        const 追击伤害 = Math.max(1, Math.floor(getCombatHpMaxValue(撤离单位) * 0.08));
        return {
          dmg: 追击伤害,
          desc: `[脱离失败] ${撤离者名}试图撤离，但步点与精神判断都被${追击者名}咬住，撤离失败并遭到追击惩罚。(撤离:${Math.round(撤离分)} 追击:${Math.round(追击分)} Roll:${投点.toFixed(2)})`,
          extraPatchOps: [],
          撤离结果: '失败',
          targetResults: [{ target: 撤离单位, targetName: 撤离者名, damage: 追击伤害, kind: '撤离追击' }],
        };
      }

      function resolvePassivePlayerStance(playerAction, npcAction, 玩家单位 = null, NPC单位 = null) {
        const 玩家名 = getCombatReportUnitName(玩家单位, '我方角色');
        const 对手名 = getCombatReportUnitName(NPC单位, '敌方角色');
        const actionName = String(
          playerAction?.action_type || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '防御',
        );
        const npcSkill = npcAction?.skill || null;
        const npcSkillType = getSkillType(npcSkill);
        const npcMainType = inferMainTypeFromEffects(npcSkill);
        const npcIsHostile =
          单挑动作是对轰(npcAction) ||
          npcSkillType === '输出' ||
          npcMainType === '伤害类' ||
          技能包含原型(npcSkill, ['伤害结算']);

        if (/防御/.test(actionName)) {
          return {
            dmg: 0,
            desc: npcIsHostile
              ? `[守势成立] ${玩家名}没有主动出手，而是稳住架势硬接${对手名}的试探，成功守住了这一轮攻势。`
              : `[守势对峙] ${玩家名}摆出防御姿态，和${对手名}短暂拉扯，谁都没有真正打出有效战果。`,
            extraPatchOps: [],
          };
        }

        if (/闪避/.test(actionName)) {
          return {
            dmg: 0,
            desc: npcIsHostile
              ? `[闪避成立] ${玩家名}没有主动出手，而是借身法拉开步点，避开了${对手名}的试探性压迫。`
              : `[游走观察] ${玩家名}保持闪避姿态，和${对手名}都在试探彼此的节奏。`,
            extraPatchOps: [],
          };
        }

        if (/撤离/.test(actionName) && 玩家单位 && NPC单位) {
          return 结算撤离判定(玩家单位, NPC单位);
        }

        if (/撤离/.test(actionName)) {
          return {
            dmg: 0,
            desc: `[脱离尝试] ${玩家名}没有继续交锋，而是主动寻找脱离战圈的机会，本回合未形成正面碰撞。`,
            extraPatchOps: [],
          };
        }

        return {
          dmg: 0,
          desc: `[守势调整] ${玩家名}本回合没有主动出手，和${对手名}进入短暂对峙。`,
          extraPatchOps: [],
        };
      }

      function 读取单挑动作前摇(action = {}) {
        const raw = Number(action?.cast_time ?? action?.前摇 ?? action?.skill?.前摇 ?? getSkillCastTime(action?.skill) ?? 10);
        return Math.max(0, Number.isFinite(raw) ? raw : 10);
      }

      function 单挑动作是造物承载(action = {}) {
        const skill = action?.skill || action?.raw_skill || {};
        return String(skill?.承载方式 || '').trim() === '造物承载';
      }

      function 单挑动作是防守反应(action = {}) {
        return ['防御', '闪避', '撤离'].includes(String(action?.action_type || action?.type || '').trim());
      }

      function 单挑动作是非攻击落地(action = {}) {
        const actionType = String(action?.action_type || action?.type || '').trim();
        return !判定单挑动作敌对(action) ||
          ['防御', '闪避', '撤离', '观察', '战术待机', '收回召唤', '穿戴装备', '被控挨打', '蓄力挨打', '无法反应', '施法失败', '索敌失败', '未完成动作', '动作未确认'].includes(actionType) ||
          单挑动作是造物承载(action);
      }

      function 单挑动作可触发攻击反应链(action = {}, actor = null, target = null, combatData = null) {
        const actionType = String(action?.action_type || action?.type || '').trim();
        if (!action || ['施法失败', '索敌失败', '未完成动作', '动作未确认', '被控挨打', '蓄力挨打', '无法反应'].includes(actionType)) return false;
        if (单挑动作是非攻击落地(action)) return false;
        return 判定单挑动作敌对(action, actor, target, combatData);
      }

      function 单挑动作是对轰(action = {}) {
        return ['强势对轰', '短前摇对轰', '范围压制'].includes(String(action?.action_type || action?.type || action || '').trim());
      }

      function 判定单挑动作敌对(action = {}, actor = null, target = null, combatData = null) {
        const actionType = String(action?.action_type || action?.type || '').trim();
        if (!action || ['防御', '闪避', '撤离', '观察', '战术待机', '收回召唤', '穿戴装备', '被控挨打', '蓄力挨打', '施法失败', '索敌失败', '未完成动作', '动作未确认', '无法反应'].includes(actionType)) return false;
        const skill = action?.skill || {};
        if (单挑动作是造物承载(action)) {
          const mode = String(action?.造物处理 || '生成到自己背包').trim() || '生成到自己背包';
          if (['生成到自己背包', '生成给友方', '立即自用'].includes(mode)) return false;
        }
        const targetKind = inferSkillPrimaryTargetKind(skill);
        if (['自身', '友方单体', '友方群体', '召唤物', '分身'].includes(targetKind)) return false;
        if (target && actor && isCombatUnitIdentityMatch(target, actor?.name || actor)) return false;
        const skillType = getSkillType(skill);
        const effects = getSkillEffects(skill, { 行为规划: true });
        if (!effects.length) return ['常规攻击', '普通攻击', '控制截断'].includes(actionType) || 单挑动作是对轰(actionType) || ['输出', '控制', '削弱'].includes(skillType);
        const 效果具备敌对证据 = effect => {
          const 原型 = String(effect?.原型 || '').trim();
          if (原型 === '伤害结算') return Number(effect?.威力倍率 || effect?.威力 || effect?.基础倍率 || 0) > 0;
          const 目标类型 = 推断战斗效果目标类型(effect, targetKind);
          if (['自身', '友方单体', '友方群体', '召唤物', '分身'].includes(目标类型)) return false;
          const 默认方向 = 推断战斗机制默认方向(effect);
          if (['资源锁定', '机制抹消', '决策干扰'].includes(原型)) return 默认方向 !== '友方';
          if (原型 === '状态施加') return 默认方向 === '敌方';
          if (['资源变化', '护盾变化', '属性修正', '判定修正'].includes(原型)) return 读取战斗数值正负(effect?.数值) < 0;
          if (原型 === '结算修正') {
            const 结算 = String(effect?.结算 || '').trim();
            return ['防御穿透', '防御剥夺', '精神抗性剥夺', '持续伤害引爆', '消耗', '前摇', '反击', '治疗转伤害'].includes(结算) && 默认方向 !== '友方';
          }
          return 默认方向 === '敌方';
        };
        return effects.some(effect => {
          return 效果具备敌对证据(effect);
        });
      }

      function 解析单挑动作目标(action = {}, actor = null, opponent = null, combatData = null) {
        if (!action || !actor) return opponent || null;
        const actorName = String(actor?.name || actor?.名称 || '').trim();
        const targetName = String(action?.target_name || action?.食用目标 || action?.物品接收者 || '').trim();
        const namedTarget = targetName ? findCombatUnitByName(combatData, targetName) : null;
        if (单挑动作是造物承载(action)) {
          const mode = String(action?.造物处理 || '生成到自己背包').trim() || '生成到自己背包';
          action.造物处理 = mode;
          if (mode === '生成到自己背包' || mode === '立即自用') {
            action.物品接收者 = actorName;
            if (mode === '立即自用') {
              action.target_name = actorName;
              action.食用目标 = actorName;
            } else {
              delete action.target_name;
              delete action.食用目标;
            }
            return actor;
          }
          if (mode === '生成给友方') {
            const friend = namedTarget && !isCombatUnitIdentityMatch(namedTarget, opponent?.name || opponent) ? namedTarget : actor;
            action.物品接收者 = String(friend?.name || friend?.名称 || actorName).trim() || actorName;
            action.target_name = action.物品接收者;
            delete action.食用目标;
            return friend;
          }
          if (mode === '立即给目标使用') {
            const target = namedTarget || actor;
            const safeName = String(target?.name || target?.名称 || actorName).trim() || actorName;
            action.target_name = safeName;
            action.物品接收者 = safeName;
            action.食用目标 = safeName;
            return target;
          }
        }
        const targetKind = inferSkillPrimaryTargetKind(action?.skill || {});
        if (targetKind === '自身') {
          action.target_name = actorName;
          return actor;
        }
        if (['友方单体', '友方群体', '召唤物', '分身'].includes(targetKind)) {
          const target = namedTarget && !isCombatUnitIdentityMatch(namedTarget, opponent?.name || opponent) ? namedTarget : actor;
          action.target_name = String(target?.name || target?.名称 || actorName).trim() || actorName;
          return target;
        }
        if (namedTarget && opponent && isCombatUnitIdentityMatch(namedTarget, opponent?.name || opponent)) return namedTarget;
        return opponent || namedTarget || actor;
      }

      function 构建单挑临时战斗数据(actor, target, side, combatData = {}) {
        const playerTeam = 读取战斗主队单位列表(combatData, '玩家');
        const enemyTeam = 读取战斗主队单位列表(combatData, '敌方');
        const activeTeam = side === 'enemy' ? enemyTeam : playerTeam;
        const opposingTeam = side === 'enemy' ? playerTeam : enemyTeam;
        const actorAllies = activeTeam.filter(unit => unit && unit !== actor && !isCombatUnitIdentityMatch(unit, actor?.name || actor));
        const targetInActiveTeam = target && activeTeam.some(unit => isCombatUnitIdentityMatch(unit, target?.name || target));
        const targetInOpposingTeam = target && opposingTeam.some(unit => isCombatUnitIdentityMatch(unit, target?.name || target));
        const opposingTargets = targetInOpposingTeam
          ? [target, ...opposingTeam.filter(unit => unit && unit !== target && !isCombatUnitIdentityMatch(unit, target?.name || target))]
          : opposingTeam;
        const safeOpposingTargets = opposingTargets.length
          ? opposingTargets
          : target && !targetInActiveTeam
            ? [target]
            : [];
        return {
          ...combatData,
          __父级战斗数据: combatData,
          __规则改写运行态: combatData.__规则改写运行态,
          回合: combatData.回合,
          战斗意图: combatData.战斗意图,
          参战者: {
            team_player: [actor, ...actorAllies].filter(Boolean),
            team_enemy: safeOpposingTargets.filter(Boolean),
          },
        };
      }

      function 构建单挑反应动作(action = {}, reactor = null, attackerUnit = null) {
        const reactorName = getCombatReportUnitName(reactor, '防守方');
        const attackerName = getCombatReportUnitName(attackerUnit, '攻击方');
        const type = String(action?.action_type || action?.type || '').trim();
        const 原定动作名 = String(action?.__原定技能名 || action?.__原定动作名 || action?.skill?.name || action?.skill?.魂技名 || action?.action_type || '行动').trim() || '行动';
        if (type === '闪避') {
          return {
            type: '伺机闪避',
            log: `[应招] ${reactorName}按原定动作拉开身位，试图避开${attackerName}的攻势。`,
            skill: normalizeSkillData({ name: '伺机闪避', 技能分类: '防御', 消耗: '体力:5%', 前摇: 12 }, '伺机闪避'),
            def_mult: 1,
          };
        }
        if (type === '防御') {
          return {
            type: '肉体兜底',
            log: `[应招] ${reactorName}按原定动作收缩防线，准备承受${attackerName}的攻势。`,
            skill: normalizeSkillData({ name: '承伤硬抗', 技能分类: '防御', 消耗: '无', 前摇: 10 }, '承伤硬抗'),
            def_mult: 1.2,
          };
        }
        if (判定单挑动作敌对(action, reactor, attackerUnit)) {
          return {
            type: '强势对轰',
            log: `[抢招对轰] ${reactorName}原定出手尚未完全落地，只能以【${action?.skill?.name || action?.action_type || '反击'}】抢招对冲。`,
            skill: action.skill,
            def_mult: 1,
          };
        }
        return {
          type: '无法反应',
          log: type === '蓄力挨打'
            ? `[先手压制] ${reactorName}正在为【${原定动作名}】争取出手窗口，未能及时转入防守应对${attackerName}的先手。`
            : `[先手压制] ${reactorName}正在执行【${原定动作名}】，未能及时转入防守应对${attackerName}的先手。`,
          skill: null,
          def_mult: 1,
        };
      }

      function 构建单挑配合动作(actor, target, action = {}) {
        const actorName = getCombatReportUnitName(actor, '施术者');
        const targetName = getCombatReportUnitName(target, actorName);
        const skillName = action?.skill?.name || action?.skill?.魂技名 || action?.action_type || '行动';
        const selfTarget = isCombatUnitIdentityMatch(target, actorName);
        const isCreation = 单挑动作是造物承载(action);
        return {
          type: '配合',
          log: isCreation
            ? `[行动完成] ${actorName}完成【${skillName}】的造物凝聚。`
            : selfTarget
              ? `[行动完成] ${actorName}完成【${skillName}】。`
              : `[配合] ${actorName}将【${skillName}】交给${targetName}承接。`,
          skill: null,
          def_mult: 1,
        };
      }

      function onPlayerAttack(playerInput, options = {}) {
        const dryRun = options.dryRun === true;
        const sourceCombatData = options.combatData || window.BattleUIBridge?.getMVU('world.战斗');
        let combatData = dryRun ? deepClonePlain(sourceCombatData || {}) : sourceCombatData;
        确保召唤单位表(combatData);
        hydrateCombatData(combatData);
        let defender = combatData.参战者.team_enemy?.[0];
        let attacker = combatData.参战者.team_player?.[0];
        if (!attacker || !defender) return { ok: false, reason: 'combat_roster_empty' };
        syncCombatActionState(attacker);
        syncCombatActionState(defender);
        const 本次战斗起始HP快照 = {
          attackerHp: getCombatHpValue(attacker),
          attackerHpMax: getCombatHpMaxValue(attacker),
          defenderHp: getCombatHpValue(defender),
          defenderHpMax: getCombatHpMaxValue(defender),
        };
        const mode = options.mode === 'multi_round' ? 'multi_round' : 'single_round';
        const modeLabel = mode === 'multi_round' ? '自动续推' : '单回合';
        const 自动续推设置 = 规范化自动续推设置(options.autoContinueConfig || {}, mode);
        const maxRounds = 自动续推设置.maxRounds;
        combatData.战斗意图 = normalizeBattleIntentMode(options.intentMode || combatData.战斗意图 || '点到为止');
        const 本次诊断 = 确保行动闭环诊断(combatData);
        if (本次诊断) {
          本次诊断.__本次会话序号 = Number(本次诊断.__本次会话序号 || 0) + 1;
          本次诊断.审计轨迹 = [];
          本次诊断.真实样本轨迹 = [];
          本次诊断.主动规划次数 = 0;
          本次诊断.索敌规划次数 = 0;
          本次诊断.辅助目标规划次数 = 0;
          本次诊断.应招审计次数 = 0;
          本次诊断.再判定审计次数 = 0;
          本次诊断.换招审计次数 = 0;
          本次诊断.目标闭环缺失 = 0;
          本次诊断.团队意图未消费 = 0;
          本次诊断.规划旁路残留 = 0;
          本次诊断.事实账本 = null;
        }

        // --- 第一步：环境定调与状态快照 ---
        // 1. 状态快照与控制拦截 (完全基于 Schema 属性驱动)
        defender.is_controlled = false;
        defender.temp_agi_mult = 1.0;
        defender.temp_reaction_bonus = 0;
        defender.temp_reaction_penalty = 0;
        defender.temp_dodge_bonus = 0;
        defender.temp_dodge_penalty = 0;
        defender.temp_lock_level = 0;
        defender.temp_cannot_react = false;

        if (defender.状态效果) {
          for (let key in defender.状态效果) {
            let cond = defender.状态效果[key];
            const ce = cond.战斗效果 || {};
            if (ce.skip_turn === true || ce.cannot_react === true) {
              defender.is_controlled = true;
            }
            if (cond.面板修改比例 && cond.面板修改比例.agi !== undefined) {
              defender.temp_agi_mult *= cond.面板修改比例.agi;
            }
            defender.temp_reaction_bonus += Number(ce.reaction_bonus || 0);
            defender.temp_reaction_penalty += Number(ce.reaction_penalty || 0);
            defender.temp_dodge_bonus += Number(ce.dodge_bonus || 0);
            defender.temp_dodge_penalty += Number(ce.dodge_penalty || 0);
            defender.temp_lock_level += Number(ce.lock_level || 0);
            if (ce.cannot_react === true) defender.temp_cannot_react = true;
          }
        }
        defender.temp_agi_mult = Math.max(0.1, defender.temp_agi_mult);

        const startingRound = Math.max(0, Number(combatData.回合 || 0) - 1);
        let roundCount = 0;
        const battleLog = [];
        let clashExtraPatchOps = [];
        let continueSimulation = true;
        let visiblePlayerInput = '';
        let 撤离结算结果 = '';
      let 本次最大单击HP比例 = 0;
      let 首轮手选动作签名 = '';
      let 首轮手选技能名 = '';
      const 读取本次动作签名 = 动作 => {
          const 技能 = 动作?.skill || {};
          const 技能名 = String(技能.name || 技能.魂技名 || 动作?.action_type || 动作?.type || '').trim();
          const 魂环路径 = Array.isArray(技能.__魂环路径) ? 技能.__魂环路径.join('/') : '';
          const 魂技槽位 = String(技能.__魂技槽位 || '').trim();
          if (!技能名) return '';
          return [动作?.action_type || 动作?.type || '', 技能名, 魂环路径, 魂技槽位].join('|');
        };
        const buildAutoPlayerContinuationAction = () => {
          const actorEntry = { char: attacker, side: 'player' };
          const targets = chooseTargetForActor(actorEntry, { combatData });
          const autoAction = buildAutoActionForActor(actorEntry, targets || { enemyTarget: defender, allyTarget: attacker }, { combatData });
          if (!autoAction) return null;
          const 自动技能名 = String(autoAction?.skill?.name || autoAction?.skill?.魂技名 || autoAction?.type || autoAction?.action_type || '').trim();
          const 自动动作重复次数 = 读取归一行为记忆值_V1(ensureActorDecisionMemory(attacker).recent_actions || {}, [自动技能名], 0);
          const 自动动作效果 = getSkillEffects(autoAction?.skill || {}, { 行为规划: true, target: targets?.enemyTarget || defender, defender: targets?.enemyTarget || defender });
          const 自动动作伤害威力 = Number(getPrimaryDamageEffect(autoAction?.skill || {})?.威力倍率 || 0);
          const 自动动作敌对状态数 = 自动动作效果.filter(effect => String(effect?.原型 || '').trim() === '状态施加' && !/自身|友方/.test(String(effect?.目标 || getSkillTarget(autoAction?.skill || {}) || '').trim())).length;
          const 自动动作消耗压力 = 计算技能消耗压力(autoAction?.skill || {}, attacker);
          const 自动动作低收益状态复读 = 自动动作重复次数 >= 1 && 自动动作敌对状态数 > 0 && 自动动作伤害威力 <= 80;
          const 自动动作高价复读 = 自动动作重复次数 >= 1 && Math.max(Number(自动动作消耗压力.综合压力 || 0), Number(自动动作消耗压力.当前压力 || 0)) >= 0.18 && 自动动作伤害威力 <= 120;
          const 需要避开重复动作 =
            自动技能名 &&
            (自动动作重复次数 >= 2 || 自动动作低收益状态复读 || 自动动作高价复读) &&
            !/收招转防|战术观察|防御|危机自保|承伤硬抗|伺机闪避|闪避|借力守势|坚壁反制/.test(自动技能名);
          if (需要避开重复动作) {
            const 备选技能 = collectCombatSkills(attacker, [])
              .map(skill => newSkillData(skill))
              .filter(skill => {
                const 名称 = String(skill?.name || skill?.魂技名 || skill?.技能名称 || '').trim();
                if (!名称 || 名称 === 自动技能名 || 名称 === 首轮手选技能名) return false;
                if (!skillTargetsEnemySide(skill)) return false;
                if (!getSkillEffects(skill, { 行为规划: true }).some(effect => ['伤害结算', '状态施加', '资源锁定', '决策干扰', '判定修正'].includes(String(effect?.原型 || '').trim()))) return false;
                return parseSkillCostForChar(skill, attacker, {
                  actor: attacker,
                  caster: attacker,
                  target: targets?.enemyTarget || defender,
                  defender: targets?.enemyTarget || defender,
                  skill,
                  combatData,
                  当前行动: '自动续推轮换',
                }).canCast !== false;
              })
              .sort((左, 右) => {
                const 左重复 = 读取归一行为记忆值_V1(ensureActorDecisionMemory(attacker).recent_actions || {}, [左.name || 左.魂技名 || 左.技能名称 || ''], 0);
                const 右重复 = 读取归一行为记忆值_V1(ensureActorDecisionMemory(attacker).recent_actions || {}, [右.name || 右.魂技名 || 右.技能名称 || ''], 0);
                const 左伤害 = Number(getPrimaryDamageEffect(左)?.威力倍率 || 0);
                const 右伤害 = Number(getPrimaryDamageEffect(右)?.威力倍率 || 0);
                return 左重复 - 右重复 || (右伤害 > 0 ? 1 : 0) - (左伤害 > 0 ? 1 : 0) || 右伤害 - 左伤害 || getSkillCastTime(左) - getSkillCastTime(右);
              })[0] || null;
            if (备选技能) {
              const 轮换动作名 = normalizeBattleActionDisplayName(备选技能.name || 备选技能.魂技名 || '轮换魂技');
              回填行动闭环审计最终动作(combatData, attacker, {
                round: Number(combatData?.回合 || roundCount || 0),
                finalResolvedActionName: 轮换动作名,
                hitCandidateName: 自动技能名 || 首轮手选技能名 || '',
                actionOverrideSource: '续推轮换',
                目标: targets?.enemyTarget?.name || defender?.name || '',
                目标语义: String(备选技能?.目标 || '').trim(),
                承载方式: String(备选技能?.承载方式 || '').trim(),
              });
              return {
                action_type: '释放魂技',
                type: '释放魂技',
                skill: normalizeSkillData(备选技能, 备选技能.name || 备选技能.魂技名 || '轮换魂技'),
                cast_time: getSkillCastTime(备选技能),
                target_name: targets?.enemyTarget?.name || defender?.name || '',
                player_auto_continuation: true,
                decision_log: `[自动续推] 避开已完成的【${自动技能名 || 首轮手选技能名 || '首轮动作'}】，轮换为【${备选技能.name || 备选技能.魂技名 || '战术动作'}】继续试探。`,
              };
            }
            if (autoAction && !自动动作低收益状态复读 && !自动动作高价复读 && !/普通攻击|常规攻击/.test(自动技能名)) {
              return {
                ...autoAction,
                action_type: autoAction.action_type || autoAction.type || '释放魂技',
                type: autoAction.type || autoAction.action_type || '释放魂技',
                target_name: autoAction.target_name || targets?.enemyTarget?.name || defender?.name || '',
                player_auto_continuation: true,
                decision_log: `[自动续推] 未找到更优轮换动作，继续执行当前评估最优的【${自动技能名 || '战术动作'}】。`,
              };
            }
            if (自动动作低收益状态复读 || 自动动作高价复读) {
              return {
                action_type: '常规攻击',
                type: '常规攻击',
                skill: normalizeSkillData({
                  name: '普通攻击',
                  魂技名: '普通攻击',
                  技能分类: '输出',
                  目标: '敌方单体',
                  消耗: '无',
                  前摇: 10,
                  _效果数组: [{ 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 }],
                }, '普通攻击'),
                cast_time: 10,
                target_name: targets?.enemyTarget?.name || defender?.name || '',
                player_auto_continuation: true,
                decision_log: 自动动作高价复读
                  ? `[自动续推] 【${自动技能名 || '魂技'}】连续消耗过高且缺少终结窗口，本回合改用【普通攻击】保留资源。`
                  : `[自动续推] 【${自动技能名 || '状态技'}】已连续压制但缺少新伤害窗口，本回合改用【普通攻击】重新试探。`,
              };
            }
            return {
              action_type: '收招转防',
              type: '收招转防',
              skill: normalizeSkillData({
                name: '收招转防',
                魂技名: '收招转防',
                技能分类: '防御',
                目标: '自身',
                消耗: '无',
                前摇: 4,
                _效果数组: [],
              }, '收招转防'),
              cast_time: 4,
              target_name: attacker?.name || '',
              player_auto_continuation: true,
              decision_log: `[自动续推] ${自动动作重复次数 >= 2 ? `连续沿用【${自动技能名 || '同一动作'}】未能拉开新局面` : `首轮手选魂技已经完成`}，本回合改为收招转防。`,
              def_mult: 1,
            };
          }
          return {
            ...autoAction,
            action_type: autoAction.action_type || autoAction.type || '常规攻击',
            type: autoAction.type || autoAction.action_type || '常规攻击',
            target_name: autoAction.target_name || targets?.enemyTarget?.name || defender?.name || '',
            player_auto_continuation: true,
          };
        };
        const 构建动作叙事审计事实 = (actor = null, action = null, target = null, extra = {}) => {
          const skill = action?.skill || {};
          const actionName = normalizeBattleActionDisplayName(skill.name || skill.魂技名 || action?.action_type || action?.type || '');
          if (!actor || !actionName) return null;
          const targetName = String(target?.name || target?.名称 || extra.targetName || '').trim();
          const effects = [
            ...getSkillEffects(skill, { 行为规划: true, target, defender: target }),
            ...(Array.isArray(skill?._效果数组) ? skill._效果数组 : []),
            ...(Array.isArray(skill?.效果数组) ? skill.效果数组 : []),
          ];
          const damagePower = Number(getPrimaryDamageEffect(skill)?.威力倍率 || 0);
          const 判定限制反扑效果 = effect => {
            const text = `${effect?.原型 || ''} ${effect?.状态 || ''} ${effect?.状态名称 || ''} ${JSON.stringify(effect?.计算层效果 || {})}`;
            return /状态施加|资源锁定|决策干扰|判定修正/.test(String(effect?.原型 || '')) &&
              /控制|位移限制|锁定|限制|束缚|麻痹|眩晕|封锁|迟缓|沉默|减速|缴械|封技|cannot_react|skip_turn|silence|cast_speed|lock_level|dodge_penalty|reaction_penalty/i.test(text);
          };
          const 读取效果标签 = effectList => {
            const tags = new Set();
            (Array.isArray(effectList) ? effectList : []).forEach(effect => {
              const text = `${effect?.原型 || ''} ${effect?.状态 || ''} ${effect?.状态名称 || ''} ${JSON.stringify(effect?.计算层效果 || {})}`;
              if (判定限制反扑效果(effect)) tags.add('CONTROL_RESTRICTION');
              if (/伤害结算/.test(String(effect?.原型 || '')) || Number(effect?.威力倍率 || 0) > 0) tags.add('DIRECT_DAMAGE');
              if (/中毒|流血|灼烧|持续创伤|dot_damage|dot_damage_ratio/i.test(text)) tags.add('ATTRITION');
              if (/治疗|恢复|护盾|防御|驱散|净化|资源变化/.test(text)) tags.add('SUPPORT');
            });
            return [...tags];
          };
          const controlEffects = effects.filter(判定限制反扑效果);
          const healOrGuardEffects = effects.filter(effect => /治疗|恢复|护盾|防御|驱散|净化|资源变化/.test(`${effect?.原型 || ''} ${effect?.状态 || ''} ${effect?.状态名称 || ''}`));
          const costPressure = 计算技能消耗压力(skill, actor);
          const dominantReason = controlEffects.length
            ? 'CONTROL'
            : healOrGuardEffects.length
              ? 'PROTECT_ALLY'
              : Number(costPressure?.当前压力 || costPressure?.综合压力 || 0) >= 0.28
                ? 'RESOURCE_PRESSURE'
                : 'DIRECT_PRESSURE';
          const baseScore = Math.max(40, Math.round(damagePower || 40) + controlEffects.length * 34 + healOrGuardEffects.length * 22);
          const alternatives = collectCombatSkills(actor, [])
            .map(item => newSkillData(item))
            .filter(item => {
              const name = normalizeBattleActionDisplayName(item?.name || item?.魂技名 || item?.技能名称 || '');
              return name && name !== actionName && skillTargetsEnemySide(item) === skillTargetsEnemySide(skill);
            });
          if (!alternatives.some(item => /普通攻击|常规攻击/.test(normalizeBattleActionDisplayName(item?.name || item?.魂技名 || '')))) {
            alternatives.push(normalizeSkillData({
              name: '普通攻击',
              魂技名: '普通攻击',
              技能分类: '输出',
              目标: skillTargetsEnemySide(skill) ? '敌方单体' : '自身',
              消耗: '无',
              前摇: 10,
              _效果数组: [{ 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击' }],
            }, '普通攻击'));
          }
          const rejectionCode = dominantReason === 'CONTROL'
            ? 'CONTROL_GAP'
            : dominantReason === 'PROTECT_ALLY'
              ? 'PROTECT_ALLY_GAP'
              : dominantReason === 'RESOURCE_PRESSURE'
                ? 'RESOURCE_PRESSURE'
                : 'DIRECT_PRESSURE_GAP';
          const alternative = alternatives[0] || null;
          const alternativeName = normalizeBattleActionDisplayName(alternative?.name || alternative?.魂技名 || alternative?.技能名称 || '');
          const alternativeEffects = alternative
            ? [
                ...getSkillEffects(alternative, { 行为规划: true, target, defender: target }),
                ...(Array.isArray(alternative?._效果数组) ? alternative._效果数组 : []),
                ...(Array.isArray(alternative?.效果数组) ? alternative.效果数组 : []),
              ]
            : [];
          const finalEffectTags = 读取效果标签(effects);
          const alternativeEffectTags = 读取效果标签(alternativeEffects);
          const controlGapVerified = finalEffectTags.includes('CONTROL_RESTRICTION') && !alternativeEffectTags.includes('CONTROL_RESTRICTION');
          const candidateRows = [{
            candidateId: `locked_${actionName}`,
            candidateName: actionName,
            名称: actionName,
            score: baseScore,
            权重: baseScore,
            candidateStatus: 战斗候选状态转移('', 'EXECUTED'),
            rejectionCode: '',
            effectTags: finalEffectTags,
            candidateSource: 'EXECUTED_ACTION_FACT',
          }];
          if (alternativeName) {
            candidateRows.push({
              candidateId: `alternative_${alternativeName}`,
              candidateName: alternativeName,
              名称: alternativeName,
              score: Math.max(1, baseScore - (dominantReason === 'CONTROL' ? 8 : 12)),
              权重: Math.max(1, baseScore - (dominantReason === 'CONTROL' ? 8 : 12)),
              candidateStatus: 战斗候选状态转移('', 'REJECTED'),
              rejectionCode: dominantReason === 'CONTROL' && !controlGapVerified ? 'LOWER_PRIORITY' : rejectionCode,
              effectTags: alternativeEffectTags,
              rejectedByEffectGap: dominantReason === 'CONTROL' ? (controlGapVerified ? 'CONTROL_RESTRICTION_GAP' : '') : '',
              candidateSource: 'SYNTHETIC_BASELINE',
            });
          }
          const reasonText = [
            dominantReason === 'CONTROL' ? `控制收益:${controlEffects.map(effect => effect?.状态名称 || effect?.状态 || effect?.原型 || '限制').filter(Boolean).join('、') || '限制反扑'}` : '',
            dominantReason === 'PROTECT_ALLY' ? '保护收益:己方防线需要支援' : '',
            dominantReason === 'RESOURCE_PRESSURE' ? '资源后果:当前动作更利于维持后续魂力节奏' : '',
            dominantReason === 'DIRECT_PRESSURE' ? '攻势收益:当前动作能稳定推进战线' : '',
          ].filter(Boolean);
          return {
            actionName,
            targetName,
            dominantReason,
            baseScore,
            candidateRows,
            reasonText,
            targetSemantics: String(getSkillTarget(skill) || extra.targetPoolSide || '').trim(),
            carryMode: String(skill.承载方式 || action?.action_type || action?.type || '').trim(),
          };
        };
        const 记账动作起手 = (action = null, actor = null, target = null, extra = {}) => {
          if (!action || !actor) return null;
          const actorSide = 读取规划单位阵营(actor, combatData) === '敌方' ? 'enemy' : 'player';
          const narrativeFact = 构建动作叙事审计事实(actor, action, target, extra);
          const event = 写入战斗事件账本(combatData, {
            eventKind: 'action_start',
            round: roundCount,
            actorName: actor?.name || actor?.名称 || '',
            actorSide,
            targetName: target?.name || target?.名称 || extra.targetName || '',
            targetSide: 推断战斗目标阵营侧(actorSide, extra.targetPoolSide || ''),
            actionName: action?.skill?.name || action?.skill?.魂技名 || action?.action_type || action?.type || '',
            actionType: action?.action_type || action?.type || '',
            targetScope: String(extra.targetScope || 推断战斗行动目标范围(action) || '').trim(),
            targetPoolSide: String(extra.targetPoolSide || '').trim(),
            result: 'declared',
          });
          if (narrativeFact) {
            记录行动闭环审计(combatData, '战术确立', {
              actionId: event?.actionId || '',
              行动者: actor?.name || actor?.名称 || '',
              目标: narrativeFact.targetName,
              技能: narrativeFact.actionName,
              回合: roundCount,
              战略意图: narrativeFact.dominantReason,
              目标理由: narrativeFact.reasonText,
              候选来源: '结构化动作事实',
              原始权重: narrativeFact.baseScore,
              最终权重: narrativeFact.baseScore,
              选择原因: '结构化动作事实锁定',
              hitCandidateName: narrativeFact.actionName,
              selectedCandidateName: narrativeFact.actionName,
              finalResolvedActionName: narrativeFact.actionName,
              目标语义: narrativeFact.targetSemantics,
              承载方式: narrativeFact.carryMode,
              候选排序结果: narrativeFact.candidateRows,
              旁路: false,
            });
          }
          return event;
        };
        const 记账应招动作 = (reactionAction = null, reactor = null, attackerUnit = null, sourceActionEvent = null, sourceAction = null, extra = {}) => {
          if (!reactionAction || !reactor || !attackerUnit || !sourceActionEvent) return null;
          const reactionType = String(reactionAction.type || reactionAction.action_type || '').trim();
          const reactionSkillName = normalizeBattleActionDisplayName(reactionAction?.skill?.name || reactionAction?.skill?.魂技名 || reactionType || '应招');
          const sourceActionName = sourceActionEvent?.actionName || sourceAction?.skill?.name || sourceAction?.skill?.魂技名 || sourceAction?.action_type || sourceAction?.type || '';
          const sourceActorName = String(attackerUnit?.name || attackerUnit?.名称 || '').trim();
          const reactionActorName = String(reactor?.name || reactor?.名称 || '').trim();
          const ratioTrace = combatData && typeof combatData.__lastReactionRatioTrace === 'object' ? combatData.__lastReactionRatioTrace : {};
          let eventKind = 'pass';
          let result = 'failed';
          let reactionRole = 'defender';
          if (/伺机闪避|闪避|dodge/i.test(reactionType + reactionSkillName)) {
            eventKind = 'dodge';
            result = /成功|evaded|dodge_success/i.test(String(reactionAction.log || reactionAction.result || '')) ? 'evaded' : 'attempted';
            reactionRole = 'dodge';
          } else if (/防御|承伤硬抗|硬抗|偏转|收招转防|借力守势|坚壁|guard|defend/i.test(reactionType + reactionSkillName)) {
            eventKind = 'defend';
            result = 'guarded';
            reactionRole = /偏转|防反|counter/i.test(reactionType + reactionSkillName) ? 'counter' : 'guard';
          } else if (/无法反应|来不及|被控|控制|抢招失败|no reaction/i.test(reactionType + String(reactionAction.log || ''))) {
            eventKind = 'pass';
            result = 'reaction_failed';
          }
          const reactionTrace = {
            sourceActionId: String(sourceActionEvent?.actionId || '').trim(),
            sourceActorName,
            sourceActionName: normalizeBattleActionDisplayName(sourceActionName),
            reactionActorName,
            reactionActionName: reactionSkillName,
            reactionRole,
            reactionOutcome: result,
            parentNodeId: String(sourceActionEvent?.chainNodeId || '').trim(),
            reactionValue: Number(ratioTrace.defenderReaction || 0),
            sourceActionSpeed: Number(ratioTrace.attackerSpeed || 0),
            reactionRatio: Number(extra.reactionRatio || ratioTrace.reactionRatio || 0),
            reactionAgility: Number(ratioTrace.defenderAgility || 0),
            reactionMental: Number(ratioTrace.defenderMentalMax || 0),
            sourceAgility: Number(ratioTrace.attackerAgility || 0),
            castPenalty: Number(ratioTrace.castPenalty || 0),
            defenderAgilityComponent: Number(ratioTrace.defenderAgilityComponent || 0),
            defenderMentalComponent: Number(ratioTrace.defenderMentalComponent || 0),
            defenderReactionBonus: Number(ratioTrace.defenderReactionBonus || 0),
            defenderReactionPenalty: Number(ratioTrace.defenderReactionPenalty || 0),
            defenderAgilityMult: Number(ratioTrace.defenderAgilityMult || 1),
            reactionBaseBeforeMultiplier: Number(ratioTrace.reactionBaseBeforeMultiplier || 0),
            maintainReactionPenalty: Number(ratioTrace.maintainReactionPenalty || 0),
            maintainReactionMultiplier: Number(ratioTrace.maintainReactionMultiplier || 1),
            reactionBudget: Number(ratioTrace.reactionBudget || 0),
            reactionBudgetMultiplier: Number(ratioTrace.reactionBudgetMultiplier || 1),
            experienceReactionMultiplier: Number(ratioTrace.experienceReactionMultiplier || 1),
            ratioPostMultiplier: Number(ratioTrace.ratioPostMultiplier || 1),
            firstStrikePenalty: ratioTrace.firstStrikePenalty === true,
            cannotReact: ratioTrace.cannotReact === true,
          };
          return 写入战斗事件账本(combatData, {
            eventKind,
            round: Number(combatData?.回合 || roundCount || 0),
            actorName: reactionActorName,
            targetName: sourceActorName,
            actionName: reactionSkillName,
            initialActionName: normalizeBattleActionDisplayName(extra.initialReactionName || reactionSkillName),
            finalActionName: reactionSkillName,
            discardedActionName: normalizeBattleActionDisplayName(extra.discardedReactionName || ''),
            actionType: 'reaction',
            sourceActionName,
            sourceActionId: sourceActionEvent?.actionId || '',
            parentNodeId: sourceActionEvent?.chainNodeId || '',
            sourceNodeId: sourceActionEvent?.chainNodeId || '',
            result,
            meta: {
              reactionTrace,
              source: String(extra.source || 'reaction_action').trim(),
              reactionType,
              reactionLog: String(reactionAction.log || '').trim(),
              sourceActionName,
              initialActionName: normalizeBattleActionDisplayName(extra.initialReactionName || reactionSkillName),
              finalActionName: reactionSkillName,
              discardedActionName: normalizeBattleActionDisplayName(extra.discardedReactionName || ''),
              reactionRatio: reactionTrace.reactionRatio,
              attackerCastTime: Number(extra.attackerCastTime || 0),
              reactorCastTime: Number(extra.reactorCastTime || 0),
              castTimeGap: Number(extra.castTimeGap || 0),
              threatScore: Number(extra.threatScore || 0),
              reasonCode: String(extra.reasonCode || (eventKind === 'pass' ? 'REACTION_FAILED' : 'REACTION_SUCCEEDED')).trim(),
              reasonText: String(extra.reasonText || (eventKind === 'pass' ? '未能取得有效应招窗口' : '行为层选择该应招动作')).trim(),
              replanReasonCode: String(extra.replanReasonCode || '').trim(),
            },
          });
        };
        const 记账非敌对落地 = (action = null, actor = null, target = null, settle = null) => {
          if (!action || !actor) return;
          const 动作类型 = String(action?.action_type || action?.type || '').trim();
          const 动作名 = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || 动作类型 || '');
          const 结果文本 = String(settle?.desc || '').trim();
          let eventKind = 'pass';
          let result = 'pass';
          if (/伺机闪避|闪避/.test(动作类型) || /闪避/.test(动作名)) {
            eventKind = 'dodge';
            result = /避开|闪避成功|躲过/.test(结果文本) ? 'evaded' : 'attempted';
          } else if (/防御|危机自保|承伤硬抗|肉体兜底|收招转防|借力守势|坚壁反制/.test(动作类型) || /防御|承伤硬抗|收招转防|借力守势|坚壁反制/.test(动作名)) {
            eventKind = 'defend';
            result = /守住|稳住|防线|对峙/.test(结果文本) ? 'stance_hold' : 'guarded';
          } else if (/造物/.test(String(action?.skill?.承载方式 || '').trim())) {
            eventKind = 'create';
            result = 'created';
          } else if (/支援|补位|轮换/.test(动作类型)) {
            eventKind = 'support';
            result = 'support';
          } else if (/观察|待机|守势维持|守势对峙/.test(`${动作类型} ${动作名} ${结果文本}`)) {
            eventKind = 'pass';
            result = /观察/.test(`${动作类型} ${动作名} ${结果文本}`) ? 'observe' : 'stance_hold';
          }
          写入战斗事件账本(combatData, {
            eventKind,
            round: Number(combatData?.回合 || 0),
            actorName: actor?.name || actor?.名称 || '',
            targetName: target?.name || target?.名称 || '',
            actionName: 动作名 || 动作类型,
            actionType: 动作类型,
            result,
            failReason: '',
          });
        };
        const 动作已在新增账本中落地 = (ledgerStartIndex = 0, actor = null, action = null) => {
          const ledger = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger : [];
          const actorName = String(actor?.name || actor?.名称 || '').trim();
          const actionName = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.action_type || action?.type || '');
          if (!actorName || !actionName) return false;
          return ledger.slice(Math.max(0, Number(ledgerStartIndex || 0))).some(item => {
            if (!item || typeof item !== 'object') return false;
            if (!isSameBattleReportName(String(item.actorName || '').trim(), actorName)) return false;
            const itemActionName = normalizeBattleActionDisplayName(item.actionName || item.sourceActionName || '');
            if (!itemActionName || itemActionName !== actionName) return false;
            return ['hit_result', 'state_apply', 'create', 'summon_create', 'shield_create', 'support'].includes(String(item.eventKind || '').trim());
          });
        };
        const 动作已在新增账本中闭合 = (ledgerStartIndex = 0, actor = null, action = null) => {
          const ledger = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger : [];
          const actorName = String(actor?.name || actor?.名称 || '').trim();
          const actionName = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.__原定技能名 || action?.__原定动作名 || action?.action_type || action?.type || '');
          if (!actorName || !actionName) return false;
          return ledger.slice(Math.max(0, Number(ledgerStartIndex || 0))).some(item => {
            if (!item || typeof item !== 'object') return false;
            if (!isSameBattleReportName(String(item.actorName || '').trim(), actorName)) return false;
            const itemActionName = normalizeBattleActionDisplayName(item.actionName || item.sourceActionName || '');
            if (!itemActionName || itemActionName !== actionName) return false;
            return ['hit_result', 'state_apply', 'create', 'summon_create', 'shield_create', 'support', 'defend', 'dodge', 'pass', 'blocked_action', 'failed_action', 'target_fail'].includes(String(item.eventKind || '').trim());
          });
        };
        const 记账动作受阻 = (action = null, actor = null, target = null, failReason = '', extra = {}) => {
          if (!action || !actor) return null;
          const 动作名 = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.__原定技能名 || action?.__原定动作名 || action?.action_type || action?.type || '');
          if (!动作名) return null;
          return 写入战斗事件账本(combatData, {
            eventKind: 'blocked_action',
            round: Number(combatData?.回合 || 0),
            actorName: actor?.name || actor?.名称 || '',
            targetName: target?.name || target?.名称 || '',
            actionName: 动作名,
            actionType: String(extra.actionType || action?.action_type || action?.type || 'blocked_action').trim(),
            sourceActionName: 动作名,
            sourceRound: Number(combatData?.回合 || 0),
            result: 'blocked',
            failReason: String(failReason || '被对手抢先压制，未能形成有效出手').trim(),
            targetPoolSide: String(extra.targetPoolSide || 'hostile').trim(),
          });
        };

        while (
          roundCount < maxRounds &&
          continueSimulation &&
          isCombatUnitAbleToFight(attacker) &&
          isCombatUnitAbleToFight(defender)
        ) {
          roundCount++;
          combatData.回合 = startingRound + roundCount;
          let roundLog = `[第${roundCount}回合] `;
          记录时光回溯回合快照(attacker);
          记录时光回溯回合快照(defender);
          const 召唤回合开始日志 = 执行召唤回合开始(combatData);
          if (召唤回合开始日志) roundLog += `${召唤回合开始日志} `;
          const 召唤行动轴日志 = 执行自主召唤行动轴回合(combatData);
          if (召唤行动轴日志) roundLog += `${召唤行动轴日志} `;

          let isCharging = attacker.蓄力技能 != null;
          let playerAction = null;

          // 1. 玩家硬控拦截扫描
          let isPlayerControlled = false;
          if (attacker.状态效果) {
            for (let key in attacker.状态效果) {
              if (
                attacker.状态效果[key].战斗效果 &&
                attacker.状态效果[key].战斗效果.skip_turn === true
              ) {
                isPlayerControlled = true;
                break;
              }
            }
          }

          if (isPlayerControlled) {
            roundLog += `[状态受控] 玩家处于硬控状态，本回合无法动作！ `;
            playerAction = { action_type: '被控挨打', cast_time: 100, skill: null };
            attacker.蓄力技能 = null;
          } else if (isCharging) {
            playerAction = attacker.蓄力技能;
            const 原定蓄力名 = String(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.__原定技能名 || playerAction?.__原定动作名 || playerAction?.action_type || '行动').trim() || '行动';
            if (playerAction.cast_time <= 40) {
              roundLog += `[蓄力完成] 玩家完成了蓄力，释放了[${原定蓄力名}]！ `;
              attacker.蓄力技能 = null;
            } else {
              playerAction.cast_time -= 30;
              roundLog += `[蓄力中] 玩家正在为[${原定蓄力名}]蓄力，当前剩余前摇：${playerAction.cast_time}。本回合无法完整出手！ `;
              playerAction = { action_type: '蓄力挨打', cast_time: 100, skill: null, __原定动作名: 原定蓄力名, __原定技能名: 原定蓄力名, __蓄力剩余前摇: playerAction.cast_time };
            }
          } else {
            playerAction = mode === 'multi_round' && roundCount > 1
              ? (buildAutoPlayerContinuationAction() || parsePlayerIntent('普通攻击', combatData))
              : parsePlayerIntent(playerInput, combatData);
            if (playerAction?.__解析失败日志) {
              roundLog += `${playerAction.__解析失败日志} `;
              battleLog.push(replaceBattleReportGenericNames(roundLog, { player: attacker, enemy: defender }));
              continueSimulation = false;
              break;
            }
            if (playerAction?.player_auto_continuation && playerAction.decision_log) roundLog += `${playerAction.decision_log} `;
            playerAction = 去重动作队列友方辅助(attacker, playerAction);
            if (
              roundCount === 1 &&
              !playerAction?.player_auto_continuation &&
              (playerAction?.action_type === '释放魂技' || Array.isArray(playerAction?.skill?.__魂环路径))
            ) {
              首轮手选动作签名 = 读取本次动作签名(playerAction);
              首轮手选技能名 = String(playerAction?.skill?.name || playerAction?.skill?.魂技名 || '').trim();
            }
            const 玩家动作目标 = 解析单挑动作目标(playerAction, attacker, defender, combatData) || defender;
            const 指定敌方目标 = 玩家动作目标 && (combatData?.参战者?.team_enemy || []).some(unit => isCombatUnitIdentityMatch(unit, 玩家动作目标.name || 玩家动作目标))
              ? 玩家动作目标
              : null;
            if (
              指定敌方目标 &&
              (combatData?.参战者?.team_enemy || []).some(unit => isCombatUnitIdentityMatch(unit, 指定敌方目标.name || 指定敌方目标))
            ) {
              defender = 指定敌方目标;
              syncCombatActionState(defender);
            }
            套用动作队列实际前摇(attacker, playerAction, 玩家动作目标, combatData);
            if (!visiblePlayerInput) visiblePlayerInput = buildVisibleBattlePlayerInput(playerInput, playerAction, combatData);

            // 💥【终极动作序列时间片机制】一回合能做出的总行动上限为 cast_time = 40！
            let totalTimeCost = 0;
            let validPreActions = [];
            let carryOverAction = null;

            // 1. 先把预先声明的副动作逐个填入时间槽
            if (playerAction.pre_actions && playerAction.pre_actions.length > 0) {
              for (let i = 0; i < playerAction.pre_actions.length; i++) {
                let pa = playerAction.pre_actions[i];
                if (totalTimeCost + pa.cast_time <= 40) {
                  totalTimeCost += pa.cast_time;
                  validPreActions.push(pa);
                } else {
                  // 只要超了40，后面所有的动作（包括没放出来的副动作和主动作）全部转为蓄力拖延到下一回合
                  carryOverAction = pa;
                  carryOverAction.cast_time -= Math.max(0, 40 - totalTimeCost);
                  break;
                }
              }
            }

            // 2. 如果副动作还没爆表，轮到尝试塞入主动作
            if (!carryOverAction) {
              if (totalTimeCost + playerAction.cast_time <= 40) {
                totalTimeCost += playerAction.cast_time;
                // 全套连招完成！顺利打出！
              } else {
                // 主动作超时了，转为蓄力
                carryOverAction = playerAction;
                carryOverAction.cast_time -= Math.max(0, 40 - totalTimeCost);
              }
            }

            // 执行成功挤入本回合时间片的副动作
            validPreActions.forEach(preAct => {
              let preCostLog = applyActionCost(attacker, preAct, 玩家动作目标, combatData);
              if (preCostLog) roundLog += preCostLog + ' ';
              if (preAct.action_type === '穿戴装备') {
                应用战斗装备变化(attacker, () => {
                  const 装备槽 = ensureBattleEquipmentSlot(attacker, preAct.equip_target);
                  if (装备槽) 装备槽.装备状态 = '已装备';
                });
                roundLog += `[连招生效] 玩家在电光火石间成功穿戴了${preAct.equip_target === 'armor' ? '斗铠' : '机甲'}！ `;
              } else {
                const 前置结算 = 执行连放前置动作结算(attacker, 玩家动作目标, preAct, combatData);
                if (前置结算.log) roundLog += `${前置结算.log} `;
                if (Array.isArray(前置结算.extraPatchOps) && 前置结算.extraPatchOps.length)
                  clashExtraPatchOps.push(...前置结算.extraPatchOps);
              }
              清理动作运行态字段(preAct);
            });
            // 为了让后续流程还能认得出主动作，必须将原本的 pre_actions 覆写为实际生效的这几个
            playerAction.pre_actions = validPreActions;

            // 3. 判定本回合到底是出手，还是蓄力挨打
            if (carryOverAction) {
              attacker.蓄力技能 = carryOverAction;
              const 原定动作名 = String(carryOverAction.skill?.name || carryOverAction.skill?.魂技名 || carryOverAction.__原定技能名 || carryOverAction.__原定动作名 || carryOverAction.action_type || '行动').trim() || '行动';
              roundLog += `[连招中断/转蓄力] ${getCombatReportUnitName(attacker, '玩家')}的【${原定动作名}】前摇过长，本回合转入蓄力，当前剩余前摇：${carryOverAction.cast_time}。 `;
              // 主动作被没收，变成了“蓄力挨打”态，用来触发后续的防御与闪避博弈
              playerAction = { action_type: '蓄力挨打', cast_time: 100, skill: null, __原定动作名: 原定动作名, __原定技能名: 原定动作名, __蓄力剩余前摇: carryOverAction.cast_time };
            }
          }

          if (playerAction?.action_type === '收回召唤') {
            roundLog += `${主动收回召唤单位(combatData, attacker, playerAction.target_name || '')} `;
            playerAction = { action_type: '防御', cast_time: 10, skill: normalizeSkillData({ name: '收回召唤', 技能分类: '辅助', 消耗: '无', 前摇: 10 }, '收回召唤') };
          }

          const 玩家动作目标 = 解析单挑动作目标(playerAction, attacker, defender, combatData) || defender;
          const 玩家动作敌对 = 单挑动作可触发攻击反应链(playerAction, attacker, 玩家动作目标, combatData);
          const isPassivePlayerTurn = 单挑动作是防守反应(playerAction);
          const npcActorEntry = { char: defender, side: 'enemy' };
          const npcTargets = {
            // 玩家主动入口只演算围绕当前玩家的对抗链；敌方若要转火其他玩家侧单位，由团战行动轴 runActorTurn() 负责。
            enemyTarget: attacker,
            allyTarget: findAllyTarget(defender, 读取战斗阵营单位列表(combatData, '敌方'), combatData),
          };
          记录行动闭环审计(combatData, '索敌规划', {
            行动者: defender?.name || defender?.名称 || '',
            目标: attacker?.name || attacker?.名称 || '',
            战略意图: '玩家主动入口对抗链',
            目标理由: ['当前玩家行动触发对抗'],
            候选来源: '玩家主动入口固定对抗目标',
            原始净收益: 100,
            最终权重: 100,
            选择原因: '当前玩家动作触发的单链对抗目标',
          });
          let npcDeclaredAction = buildAutoActionForActor(npcActorEntry, npcTargets, { combatData, observedTargetAction: playerAction }) || {
            action_type: '战术待机',
            type: '战术待机',
            cast_time: 10,
            skill: normalizeSkillData({ name: '战术待机', 技能分类: '辅助', 消耗: '无', 前摇: 10 }, '战术待机'),
            decision_log: `[战术待机] ${defender.name || '对手'}暂未形成明确攻势。`,
          };
          npcDeclaredAction = 去重动作队列友方辅助(defender, npcDeclaredAction);
          const npcDeclaredTarget = 解析单挑动作目标(npcDeclaredAction, defender, attacker, combatData) || attacker;
          套用动作队列实际前摇(defender, npcDeclaredAction, npcDeclaredTarget, combatData);
          const npcDeclaredHostile = 单挑动作可触发攻击反应链(npcDeclaredAction, defender, npcDeclaredTarget, combatData);
          const npcDeclaredNonAttack = 单挑动作是非攻击落地(npcDeclaredAction);
          const 玩家前摇 = 读取单挑动作前摇(playerAction);
          const npc前摇 = 读取单挑动作前摇(npcDeclaredAction);
          const 构建单挑行动窗口 = 前摇 => {
            const value = Math.max(0, Math.min(39, Math.floor(Number(前摇 || 0))));
            const start = Math.floor(value / 10) * 10;
            return `${start}-${start + 9}`;
          };
          写入行动轴初始意图节点(combatData, { char: attacker, side: 'player' }, 玩家动作目标 || defender, playerAction, 构建单挑行动窗口(玩家前摇));
          写入行动轴初始意图节点(combatData, { char: defender, side: 'enemy' }, npcDeclaredTarget || attacker, npcDeclaredAction, 构建单挑行动窗口(npc前摇));
          const 玩家技能 = playerAction?.skill || null;
          const 玩家技能名 = normalizeBattleActionDisplayName(玩家技能?.name || 玩家技能?.魂技名 || playerAction?.action_type || playerAction?.type || '');
          const 玩家目标文本 = String(getSkillTarget(玩家技能 || {}) || '').trim();
          const 玩家疑似范围控制命名 = /群|散射|风暴|领域|锁|印|牵制/.test(玩家技能名);
          const 玩家技能偏友方 = 技能偏向友方目标(玩家技能 || {}, playerAction);
          const 玩家技能自用 = inferSkillPrimaryTargetKind(玩家技能 || {}) === '自身' || /自身/.test(玩家目标文本);
          const 玩家范围控制敌对 =
            !!玩家技能 &&
            !玩家技能偏友方 &&
            !玩家技能自用 &&
            (
              isBattleSkillControlProfile(玩家技能) ||
              /群体|全场/.test(玩家目标文本) ||
              玩家疑似范围控制命名 ||
              hasBattleSkillRuntimeConsumer(玩家技能, ['interrupt', 'hard_control', 'skill_seal'])
            );
          const npcDeclaredSkill = npcDeclaredAction?.skill || null;
          const npc真截断资格 = !!npcDeclaredSkill && 判定技能具备真实截断资格_V1(npcDeclaredSkill);
          const 玩家范围控制可延后落地 = 玩家范围控制敌对 && !npc真截断资格;
          const npcShouldActFirst =
            npcDeclaredHostile && !npcDeclaredNonAttack &&
            (!玩家范围控制敌对 || npc真截断资格) &&
            (isPassivePlayerTurn ||
              !玩家动作敌对 ||
              npc前摇 < 玩家前摇 ||
              (npc前摇 === 玩家前摇 && Number(defender.agi || 0) > Number(attacker.agi || 0)));
          let 主动结算方 = attacker;
          let 被动结算目标 = 玩家动作目标 || defender;
          let 主动结算动作 = playerAction;
          let 反应结算动作 = null;
          let 主动结算战斗数据 = combatData;
          let 本轮NPC先手 = false;
          let 玩家非敌对动作已执行 = false;
          let 玩家非敌对动作已完成 = false;
          let 玩家非敌对动作释放失败 = false;
          let 行为链结果 = null;
          let settleResult = null;
          const 本回合玩家动作账本起点 = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger.length : 0;
          const 本回合账本起点 = 本回合玩家动作账本起点;
          const 扣除玩家动作成本 = (动作 = playerAction, 目标 = 玩家动作目标) => {
            if (!动作 || 动作.action_type === '施法失败' || 动作.__主动成本已结算 === true) return 动作?.action_type !== '施法失败';
            const costLog = applyActionCost(attacker, 动作, 目标, combatData);
            if (costLog) roundLog += `${costLog} `;
            return 动作.action_type !== '施法失败';
          };

          const 执行非敌对玩家动作 = () => {
            if (玩家非敌对动作已执行) return { dmg: 0, desc: '', extraPatchOps: [] };
            玩家非敌对动作已执行 = true;
            const target = 玩家动作目标 || attacker;
            if (!扣除玩家动作成本(playerAction, target)) {
              玩家非敌对动作释放失败 = true;
              return { dmg: 0, desc: '', extraPatchOps: [] };
            }
            const supportCombatData = 构建单挑临时战斗数据(attacker, target, 'player', combatData);
            const supportReaction = 构建单挑配合动作(attacker, target, playerAction);
            const supportResult = executeClash(playerAction, supportReaction, supportCombatData);
            if (Array.isArray(supportResult.extraPatchOps) && supportResult.extraPatchOps.length) {
              clashExtraPatchOps.push(...supportResult.extraPatchOps);
              supportResult.__extraPatchOps已收集 = true;
            }
            roundLog += `${supportReaction.log} ${supportResult.desc} `;
            玩家非敌对动作已完成 = true;
            return supportResult;
          };
          const 扣除NPC主动动作成本 = () => {
            if (npcDeclaredAction.__主动成本已结算 === true) return;
            const costLog = applyActionCost(defender, npcDeclaredAction, attacker, combatData);
            if (costLog) roundLog += `${costLog} `;
          };

          if (npcShouldActFirst) {
            const npcActionStartEvent = 记账动作起手(npcDeclaredAction, defender, attacker, { targetPoolSide: 'hostile' });
            if (玩家动作敌对) 记账动作起手(playerAction, attacker, 玩家动作目标 || defender, { targetPoolSide: 'hostile' });
            if (!isPassivePlayerTurn && !玩家动作敌对 && 玩家前摇 <= npc前摇) 执行非敌对玩家动作();
            主动结算方 = defender;
            被动结算目标 = attacker;
            主动结算动作 = npcDeclaredAction;
            反应结算动作 = 玩家非敌对动作已完成
              ? { type: '无法反应', log: `[先手压制] ${getCombatReportUnitName(attacker, '我方')}刚完成【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '行动'}】，来不及追加防守。`, skill: null, def_mult: 1 }
              : 构建单挑反应动作(playerAction, attacker, defender);
            主动结算战斗数据 = 构建单挑临时战斗数据(defender, attacker, 'enemy', combatData);
            主动结算动作.target_name = attacker.name || attacker.名称 || '';
            本轮NPC先手 = true;
            扣除NPC主动动作成本();
            if (反应结算动作?.skill && 反应结算动作.skill === playerAction?.skill && !扣除玩家动作成本(playerAction, defender)) {
              反应结算动作 = { type: '无法反应', log: `[抢招失败] ${getCombatReportUnitName(attacker, '我方')}未能支撑【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '行动'}】的启动消耗，抢招未成。`, skill: null, def_mult: 1 };
            }
            const 结算前账本长度 = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger.length : 0;
            行为链结果 = 自动行为链再判定(defender, attacker, 主动结算动作, 反应结算动作, 主动结算战斗数据);
            记账应招动作(反应结算动作, attacker, defender, npcActionStartEvent, 主动结算动作, {
              source: 'single_duel_reaction',
              reactionRatio: calculateReactionRatio(defender, attacker, 主动结算动作, 主动结算战斗数据),
              attackerCastTime: 读取单挑动作前摇(主动结算动作),
              reactorCastTime: 读取单挑动作前摇(playerAction),
              castTimeGap: 读取单挑动作前摇(playerAction) - 读取单挑动作前摇(主动结算动作),
              threatScore: Math.max(0, Number(getPrimaryDamageEffect(主动结算动作?.skill, { 行为规划: true })?.威力倍率 || 0)),
              initialReactionName: normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || ''),
              discardedReactionName: normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || '') !== normalizeBattleActionDisplayName(反应结算动作?.skill?.name || 反应结算动作?.skill?.魂技名 || 反应结算动作?.type || 反应结算动作?.action_type || '') ? normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || '') : '',
              replanReasonCode: normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || '') !== normalizeBattleActionDisplayName(反应结算动作?.skill?.name || 反应结算动作?.skill?.魂技名 || 反应结算动作?.type || 反应结算动作?.action_type || '') ? 'INTERRUPTED_BY_SPEED' : '',
            });
            settleResult = executeClash(主动结算动作, 反应结算动作, 主动结算战斗数据);
            const 玩家动作尚未落地 = 玩家动作敌对 && !动作已在新增账本中落地(结算前账本长度, attacker, playerAction);
            const 玩家仍可行动 =
              isCombatUnitAbleToFight(attacker) &&
              !Object.values(attacker?.状态效果 || {}).some(状态 => {
                const 效果 = 状态?.战斗效果 || {};
                return 效果.skip_turn === true || 效果.cannot_react === true;
              });
            if (玩家动作尚未落地 && 玩家范围控制可延后落地 && 玩家仍可行动) {
              const 玩家延后战斗数据 = 构建单挑临时战斗数据(attacker, defender, 'player', combatData);
              const 玩家延后反应 = {
                type: '无法反应',
                log: `[后续出手] ${getCombatReportUnitName(attacker, '我方')}顶住先手压制，继续完成【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '行动'}】。`,
                skill: null,
                def_mult: 1,
              };
              const 玩家延后结算 = executeClash(playerAction, 玩家延后反应, 玩家延后战斗数据);
              if (Array.isArray(玩家延后结算.extraPatchOps) && 玩家延后结算.extraPatchOps.length && 玩家延后结算.__extraPatchOps已收集 !== true) {
                clashExtraPatchOps.push(...玩家延后结算.extraPatchOps);
              }
              roundLog += ` ${玩家延后反应.log} ${玩家延后结算.desc}`;
            }
            if (
              玩家动作敌对 &&
              !(玩家范围控制可延后落地 && 玩家仍可行动 && 动作已在新增账本中落地(结算前账本长度, attacker, playerAction)) &&
              !动作已在新增账本中落地(结算前账本长度, attacker, playerAction)
            ) {
              const 受阻原因 =
                反应结算动作?.type === '强势对轰'
                  ? '被对手抢先压制，未能形成有效出手'
                  : '被对手先手压制，未能形成有效出手';
              记账动作受阻(playerAction, attacker, defender, 受阻原因, { targetPoolSide: 'hostile' });
            }
            roundLog += replaceBattleReportGenericNames(
              `${主动结算动作.decision_log ? 主动结算动作.decision_log + ' ' : ''}${行为链结果?.日志 ? 行为链结果.日志 + ' ' : ''}${反应结算动作.log} ${settleResult.desc}`,
              { player: defender, enemy: attacker },
            );
          } else if (玩家动作敌对) {
            const playerActionStartEvent = 记账动作起手(playerAction, attacker, 玩家动作目标 || defender, { targetPoolSide: 'hostile' });
            if (!扣除玩家动作成本(playerAction, 玩家动作目标)) {
              battleLog.push(replaceBattleReportGenericNames(roundLog, { player: attacker, enemy: defender }));
              continueSimulation = false;
              break;
            }
            const reactionRatio = calculateReactionRatio(attacker, defender, playerAction, combatData);
            const npcAction = determineNpcAction(combatData, playerAction, reactionRatio);
            反应结算动作 = npcAction;
            行为链结果 = 自动行为链再判定(attacker, defender, playerAction, npcAction, combatData);
            记账应招动作(npcAction, defender, attacker, playerActionStartEvent, playerAction, {
              source: 'single_duel_reaction',
              reactionRatio,
              attackerCastTime: 读取单挑动作前摇(playerAction),
              reactorCastTime: 读取单挑动作前摇(npcDeclaredAction),
              castTimeGap: 读取单挑动作前摇(npcDeclaredAction) - 读取单挑动作前摇(playerAction),
              threatScore: Math.max(0, Number(getPrimaryDamageEffect(playerAction?.skill, { 行为规划: true })?.威力倍率 || 0)),
              initialReactionName: normalizeBattleActionDisplayName(npcDeclaredAction?.skill?.name || npcDeclaredAction?.skill?.魂技名 || npcDeclaredAction?.action_type || npcDeclaredAction?.type || ''),
              discardedReactionName: normalizeBattleActionDisplayName(npcDeclaredAction?.skill?.name || npcDeclaredAction?.skill?.魂技名 || npcDeclaredAction?.action_type || npcDeclaredAction?.type || '') !== normalizeBattleActionDisplayName(npcAction?.skill?.name || npcAction?.skill?.魂技名 || npcAction?.type || npcAction?.action_type || '') ? normalizeBattleActionDisplayName(npcDeclaredAction?.skill?.name || npcDeclaredAction?.skill?.魂技名 || npcDeclaredAction?.action_type || npcDeclaredAction?.type || '') : '',
              replanReasonCode: normalizeBattleActionDisplayName(npcDeclaredAction?.skill?.name || npcDeclaredAction?.skill?.魂技名 || npcDeclaredAction?.action_type || npcDeclaredAction?.type || '') !== normalizeBattleActionDisplayName(npcAction?.skill?.name || npcAction?.skill?.魂技名 || npcAction?.type || npcAction?.action_type || '') ? 'TACTICAL_DISADVANTAGE' : '',
            });
            const 玩家主动结算前账本长度 = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger.length : 0;
            settleResult = executeClash(playerAction, npcAction, combatData);
            if (
              /强势对轰|主动压迫|抢先压制/.test(String(npcAction?.type || npcAction?.action_type || '')) &&
              !动作已在新增账本中落地(玩家主动结算前账本长度, attacker, playerAction)
            ) {
              记账动作受阻(playerAction, attacker, defender, '被对手抢先压制，未能形成有效出手', { targetPoolSide: 'hostile' });
            }
            roundLog += `${行为链结果?.日志 ? 行为链结果.日志 + ' ' : ''}${npcAction.log} ${settleResult.desc}`;
          } else if (isPassivePlayerTurn) {
            if (!扣除玩家动作成本(playerAction, 玩家动作目标)) {
              battleLog.push(replaceBattleReportGenericNames(roundLog, { player: attacker, enemy: defender }));
              continueSimulation = false;
              break;
            }
            反应结算动作 = npcDeclaredAction;
            settleResult = resolvePassivePlayerStance(playerAction, npcDeclaredAction, attacker, defender);
            记账非敌对落地(playerAction, attacker, defender, settleResult);
            roundLog += `${npcDeclaredAction.decision_log ? npcDeclaredAction.decision_log + ' ' : ''}${settleResult.desc}`;
          } else {
            settleResult = 执行非敌对玩家动作();
            记账非敌对落地(playerAction, attacker, defender, settleResult);
            if (玩家非敌对动作释放失败) {
              // 释放失败已经在 roundLog 中说明，避免继续把对手行动缝进同一条失败链。
            } else if (npcDeclaredHostile && !npcDeclaredNonAttack) {
              const npcActionStartEvent = 记账动作起手(npcDeclaredAction, defender, attacker, { targetPoolSide: 'hostile' });
              主动结算方 = defender;
              被动结算目标 = attacker;
              主动结算动作 = npcDeclaredAction;
              反应结算动作 = 玩家非敌对动作已完成
                ? { type: '无法反应', log: `[先手压制] ${getCombatReportUnitName(attacker, '我方')}刚完成【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '行动'}】，来不及追加防守。`, skill: null, def_mult: 1 }
                : 构建单挑反应动作(playerAction, attacker, defender);
              主动结算战斗数据 = 构建单挑临时战斗数据(defender, attacker, 'enemy', combatData);
              主动结算动作.target_name = attacker.name || attacker.名称 || '';
              本轮NPC先手 = true;
              扣除NPC主动动作成本();
              if (反应结算动作?.skill && 反应结算动作.skill === playerAction?.skill && !扣除玩家动作成本(playerAction, defender)) {
                反应结算动作 = { type: '无法反应', log: `[抢招失败] ${getCombatReportUnitName(attacker, '我方')}未能支撑【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '行动'}】的启动消耗，抢招未成。`, skill: null, def_mult: 1 };
              }
              const 结算前账本长度 = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger.length : 0;
              行为链结果 = 自动行为链再判定(defender, attacker, 主动结算动作, 反应结算动作, 主动结算战斗数据);
              记账应招动作(反应结算动作, attacker, defender, npcActionStartEvent, 主动结算动作, {
                source: 'single_duel_reaction',
                reactionRatio: calculateReactionRatio(defender, attacker, 主动结算动作, 主动结算战斗数据),
                attackerCastTime: 读取单挑动作前摇(主动结算动作),
                reactorCastTime: 读取单挑动作前摇(playerAction),
                castTimeGap: 读取单挑动作前摇(playerAction) - 读取单挑动作前摇(主动结算动作),
                threatScore: Math.max(0, Number(getPrimaryDamageEffect(主动结算动作?.skill, { 行为规划: true })?.威力倍率 || 0)),
                initialReactionName: normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || ''),
                discardedReactionName: normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || '') !== normalizeBattleActionDisplayName(反应结算动作?.skill?.name || 反应结算动作?.skill?.魂技名 || 反应结算动作?.type || 反应结算动作?.action_type || '') ? normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || '') : '',
                replanReasonCode: normalizeBattleActionDisplayName(playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || playerAction?.type || '') !== normalizeBattleActionDisplayName(反应结算动作?.skill?.name || 反应结算动作?.skill?.魂技名 || 反应结算动作?.type || 反应结算动作?.action_type || '') ? 'INTERRUPTED_BY_SPEED' : '',
              });
              settleResult = executeClash(主动结算动作, 反应结算动作, 主动结算战斗数据);
              const 玩家动作尚未落地 = 玩家动作敌对 && !动作已在新增账本中落地(结算前账本长度, attacker, playerAction);
              const 玩家仍可行动 =
                isCombatUnitAbleToFight(attacker) &&
                !Object.values(attacker?.状态效果 || {}).some(状态 => {
                  const 效果 = 状态?.战斗效果 || {};
                  return 效果.skip_turn === true || 效果.cannot_react === true;
                });
              if (玩家动作尚未落地 && 玩家范围控制可延后落地 && 玩家仍可行动) {
                const 玩家延后战斗数据 = 构建单挑临时战斗数据(attacker, defender, 'player', combatData);
                const 玩家延后反应 = {
                  type: '无法反应',
                  log: `[后续出手] ${getCombatReportUnitName(attacker, '我方')}顶住先手压制，继续完成【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '行动'}】。`,
                  skill: null,
                  def_mult: 1,
                };
                const 玩家延后结算 = executeClash(playerAction, 玩家延后反应, 玩家延后战斗数据);
                if (Array.isArray(玩家延后结算.extraPatchOps) && 玩家延后结算.extraPatchOps.length && 玩家延后结算.__extraPatchOps已收集 !== true) {
                  clashExtraPatchOps.push(...玩家延后结算.extraPatchOps);
                }
                roundLog += ` ${玩家延后反应.log} ${玩家延后结算.desc}`;
              }
              if (
                玩家动作敌对 &&
                !(玩家范围控制可延后落地 && 玩家仍可行动 && 动作已在新增账本中落地(结算前账本长度, attacker, playerAction)) &&
                !动作已在新增账本中落地(结算前账本长度, attacker, playerAction)
              ) {
                const 受阻原因 =
                  反应结算动作?.type === '强势对轰'
                    ? '被对手抢先压制，未能形成有效出手'
                    : '被对手先手压制，未能形成有效出手';
                记账动作受阻(playerAction, attacker, defender, 受阻原因, { targetPoolSide: 'hostile' });
              }
              roundLog += replaceBattleReportGenericNames(
                `${主动结算动作.decision_log ? 主动结算动作.decision_log + ' ' : ''}${行为链结果?.日志 ? 行为链结果.日志 + ' ' : ''}${反应结算动作.log} ${settleResult.desc}`,
                { player: defender, enemy: attacker },
              );
            } else if (npcDeclaredAction.decision_log) {
              记账非敌对落地(npcDeclaredAction, defender, attacker, { desc: npcDeclaredAction.decision_log });
              roundLog += ` ${npcDeclaredAction.decision_log}`;
            }
          }
          if (Array.isArray(settleResult.extraPatchOps) && settleResult.extraPatchOps.length && settleResult.__extraPatchOps已收集 !== true)
            clashExtraPatchOps.push(...settleResult.extraPatchOps);

          if (attacker.蓄力技能 != null) {
            let damageRatio = Number(settleResult.totalProjectedDamage || settleResult.dmg || 0) / getCombatHpMaxValue(attacker);
            let isControlled = attacker.状态效果 && attacker.状态效果['眩晕'];

            let hasSuperArmor = false;
            if (attacker.状态效果) {
              for (let key in attacker.状态效果) {
                if (
                  attacker.状态效果[key].战斗效果?.super_armor === true ||
                  key.includes('霸体') ||
                  key.includes('真身')
                )
                  hasSuperArmor = true;
              }
            }

            const 压制动作 = 本轮NPC先手 ? 主动结算动作 : 反应结算动作;
            const npcSkillType = getSkillType(压制动作?.skill);
            if (压制动作?.type === '控制截断' && npcSkillType === '控制' && getSkillCastTime(压制动作.skill) < 10) {
              const npc精神压制 = 计算当前资源压制倍率(defender, defender.final || {}, attacker, attacker.final || {}, 'men', {
                下限: 0,
                上限: 2,
                压制指数: 1,
              });
              if (npc精神压制 >= 1 || defender.agi > attacker.agi) {
                if (hasSuperArmor) {
                  roundLog += ` NPC释放[${压制动作.skill.name}]试图打断，但玩家处于霸体状态，强行免疫了控制！`;
                } else {
                  let backlashDmg = Math.floor(getCombatHpMaxValue(attacker) * 0.05);
                  设置战斗血量值(attacker, getCombatHpValue(attacker) - backlashDmg);
                  if (!attacker.状态效果) attacker.状态效果 = {};
                  const 打断反噬状态 = {
                    类型: 'debuff',
                    状态: '僵直',
                    状态名称: '僵直',
                    层数: 1,
                    描述: '施法被打断的反噬',
                    duration: 1,
                    面板修改比例: { str: 1.0, def: 1.0, agi: 1.0, sp_max: 1.0 },
                    战斗效果: { skip_turn: true, dot_damage: 0, armor_pen: 0 },
                  };
                  const 持续移除命中 = 持续状态移除阻断状态附着(attacker, '僵直', 打断反噬状态, defender);
                  if (持续移除命中) roundLog += ` NPC释放[${压制动作.skill.name}]成功打断玩家施法！玩家遭到反噬，承受 ${backlashDmg} 点真伤，[僵直]被[${持续移除命中.key}]拦截！`;
                  else {
                    attacker.状态效果['僵直'] = 打断反噬状态;
                    roundLog += ` NPC释放[${压制动作.skill.name}]成功打断玩家施法！玩家遭到反噬，承受 ${backlashDmg} 点真伤并陷入[僵直]！`;
                  }
                  attacker.蓄力技能 = null;
                  let attackerUpkeep = settleSustainEffectsAtRoundEnd(attacker, '玩家', combatData);
                  let defenderUpkeep = settleSustainEffectsAtRoundEnd(defender, 'NPC', combatData);
                  if (attackerUpkeep.log) roundLog += ` ${attackerUpkeep.log}`;
                  if (defenderUpkeep.log) roundLog += ` ${defenderUpkeep.log}`;
                  let attackerRoundEnd = settleConditionsAtRoundEnd(attacker, '玩家', combatData);
                  let defenderRoundEnd = settleConditionsAtRoundEnd(defender, 'NPC', combatData);
                  const 规则改写回合尾日志 = 递减战斗规则改写运行态(combatData);
                  if (attackerRoundEnd.log) roundLog += ` ${attackerRoundEnd.log}`;
                  if (defenderRoundEnd.log) roundLog += ` ${defenderRoundEnd.log}`;
                  if (规则改写回合尾日志) roundLog += ` ${规则改写回合尾日志}`;
                  syncCombatActionState(attacker);
                  syncCombatActionState(defender);
                  if (!isCombatUnitAbleToFight(attacker) || !isCombatUnitAbleToFight(defender))
                    continueSimulation = false;
                  battleLog.push(replaceBattleReportGenericNames(roundLog, { player: attacker, enemy: defender }));
                  continue;
                }
              }
            }

            if (!hasSuperArmor && (damageRatio >= 0.3 || isControlled)) {
              roundLog += ` [蓄力打断] 玩家受到重创或硬控，蓄力被强制打断！`;
              attacker.蓄力技能 = null;
            } else if (hasSuperArmor && isControlled) {
              roundLog += ` [霸体强抗] 玩家遭遇硬控，但凭借霸体强行稳住阵脚，蓄力继续！`;
              delete attacker.状态效果['眩晕'];
            }
          }

          // 简单判断被动方是否被打断 (如果主动方伤害极高或带有硬控，视为打断)
          const activeStateCalc = getPrimaryStateCalc(主动结算动作?.skill);
          const activeInterruptChance = 计算行动打断概率(主动结算动作?.skill, 被动结算目标, settleResult);
          let isPassiveInterrupted =
            activeStateCalc.skip_turn === true ||
            getPrimaryStateFlags(主动结算动作?.skill).includes('硬控') ||
            (activeInterruptChance > 0 && Math.random() <= activeInterruptChance);

          // --- 第四步：打击烈度与破防标尺 ---
          const damagePackage = applyResolvedDamagePackage(主动结算方, 主动结算动作, settleResult, {
            primaryTarget: 被动结算目标,
            combatData: 主动结算战斗数据,
          });
          let appliedDamage = damagePackage.primaryAppliedDamage;
          本次最大单击HP比例 = Math.max(
            本次最大单击HP比例,
            Math.max(0, Number(appliedDamage || 0)) / getCombatHpMaxValue(被动结算目标),
            Math.max(0, Number(settleResult.totalProjectedDamage || settleResult.dmg || 0)) / getCombatHpMaxValue(主动结算方),
          );
          if (damagePackage.log) roundLog += ` ${damagePackage.log}`;
          const 召唤协同日志 = 主动结算方?.召唤键
            ? ''
            : 执行协同召唤追击(主动结算方, 被动结算目标, damagePackage.primaryAppliedDamage, combatData);
          if (召唤协同日志) roundLog += ` ${召唤协同日志}`;
          if (settleResult?.撤离结果 === '成功') {
            撤离结算结果 = '成功';
            combatData.前端建议结果 = '强制撤离';
            combatData.裁断结果 = '玩家成功撤离';
            combatData.进行中 = false;
            continueSimulation = false;
          }
          const 行为防反日志 = 执行行为防反结算(
            主动结算方,
            被动结算目标,
            主动结算动作,
            反应结算动作 || { type: '无法反应', log: '', skill: null, def_mult: 1 },
            settleResult,
            damagePackage,
            主动结算战斗数据,
          );
          if (行为防反日志) roundLog += ` ${行为防反日志}`;
          isPassiveInterrupted = isPassiveInterrupted || appliedDamage / getCombatHpMaxValue(被动结算目标) >= 0.15;
          if (
            本轮NPC先手 &&
            !玩家动作敌对 &&
            !isPassivePlayerTurn &&
            !isPassiveInterrupted &&
            isCombatUnitAbleToFight(attacker)
          ) {
            if (playerAction?.action_type === '蓄力挨打') {
              写入战斗事件账本(combatData, {
                eventKind: 'blocked_action',
                round: roundCount,
                actorName: attacker?.name || attacker?.名称 || '',
                actionName: playerAction?.__原定技能名 || playerAction?.__原定动作名 || '行动',
                actionType: 'blocked_action',
                failReason: '仍在启动，未能在本回合完成',
                result: 'blocked',
              });
              roundLog += ` [行动受阻] ${getCombatReportUnitName(attacker, '我方')}的【${playerAction?.__原定技能名 || playerAction?.__原定动作名 || '行动'}】仍在启动，未能在本回合完成。`;
            } else {
              const 后续非敌对结果 = 执行非敌对玩家动作();
              if (Array.isArray(后续非敌对结果.extraPatchOps) && 后续非敌对结果.extraPatchOps.length && 后续非敌对结果.__extraPatchOps已收集 !== true)
                clashExtraPatchOps.push(...后续非敌对结果.extraPatchOps);
            }
          } else if (本轮NPC先手 && !玩家动作敌对 && !isPassivePlayerTurn && isPassiveInterrupted && !玩家非敌对动作已完成) {
            写入战斗事件账本(combatData, {
              eventKind: 'blocked_action',
              round: roundCount,
              actorName: attacker?.name || attacker?.名称 || '',
              actionName: playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.__原定技能名 || playerAction?.__原定动作名 || playerAction?.action_type || '行动',
              actionType: 'blocked_action',
              failReason: '被对手攻势打断，未能完成',
              result: 'blocked',
            });
            roundLog += ` [行动受阻] ${getCombatReportUnitName(attacker, '我方')}的【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.__原定技能名 || playerAction?.__原定动作名 || playerAction?.action_type || '行动'}】被对手攻势打断，未能完成。`;
          }
          if (反应结算动作?.type === '穿戴装备') {
            if (!isPassiveInterrupted) {
              const 装备目标 = 反应结算动作.equip_target || 反应结算动作.skill?.equip_target;
              应用战斗装备变化(被动结算目标, () => {
                const 装备槽 = ensureBattleEquipmentSlot(被动结算目标, 装备目标);
                if (装备槽) 装备槽.装备状态 = '已装备';
              });
              roundLog += ` [装备生效] ${getCombatReportUnitName(被动结算目标, '防守方')}成功穿戴了${装备目标 === 'armor' ? '斗铠' : '机甲'}，防御力大增！`;
            } else {
              roundLog += ` [穿戴失败] ${getCombatReportUnitName(主动结算方, '攻击方')}的猛烈攻击强行打断了${getCombatReportUnitName(被动结算目标, '防守方')}的装备穿戴过程！`;
            }
          }
          const fusionAftermathLog = applyFusionActionAftermath(主动结算方, 主动结算动作, 主动结算战斗数据);
          if (fusionAftermathLog) roundLog += ` ${fusionAftermathLog}`;
          if (
            本轮NPC先手 &&
            玩家动作敌对 &&
            !动作已在新增账本中闭合(本回合玩家动作账本起点, attacker, playerAction)
          ) {
            const 兜底受阻原因 =
              玩家范围控制可延后落地 && isCombatUnitAbleToFight(attacker)
                ? '先手压制后的对抗收束中未能形成有效出手'
                : '被对手先手压制，未能形成有效出手';
            记账动作受阻(playerAction, attacker, defender, 兜底受阻原因, { targetPoolSide: 'hostile' });
            roundLog += ` [行动受阻] ${getCombatReportUnitName(attacker, '我方')}的【${playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.__原定技能名 || playerAction?.__原定动作名 || playerAction?.action_type || '行动'}】${兜底受阻原因}。`;
          }
          清理动作运行态字段(playerAction);

          // --- 第五步：装备护主与战损结算 ---
          let combatType = combatData.战斗类型 || '突发遭遇';

          if (getCombatHpValue(defender) < getCombatHpMaxValue(defender) * 0.1) {
            let hasMech = 战斗机甲已装备(defender);
            let hasArmor = defender.装备?.斗铠?.装备状态 === '已装备';

            if (combatType === '擂台切磋' && getCombatHpValue(defender) <= getCombatHpMaxValue(defender) * 0.05) {
              设置战斗血量值(defender, Math.floor(getCombatHpMaxValue(defender) * 0.05)); // 强制锁血 5%
              roundLog += ` [擂台保护] 胜负已分！裁判强行介入，终止了致命一击！`;
              continueSimulation = false; // 强制结束战斗
            } else if (hasMech || hasArmor) {
              设置战斗血量值(defender, Math.floor(getCombatHpMaxValue(defender) * 0.1));
              let armorLog = applyArmorDamage(defender);
              roundLog += ` [装备护主] 致命打击触发替死锁血！NPC HP强制锁定在10%！${armorLog}`;
            } else if (判断虚拟战斗类型(combatType)) {
              roundLog += ` [虚拟环境] 本场为虚拟战斗，允许完整承受击杀结果，现实损伤由退出结算处理。`;
            } else {
            let saveLog = triggerDeathSave(defender);
            if (saveLog) {
              if (!/金龙不灭真身/.test(saveLog)) 设置战斗血量值(defender, Math.floor(getCombatHpMaxValue(defender) * 0.1));
              roundLog += ` ${saveLog}`;
            }
          }
          }

          let attackerUpkeep = settleSustainEffectsAtRoundEnd(attacker, '玩家', combatData);
          let defenderUpkeep = settleSustainEffectsAtRoundEnd(defender, 'NPC', combatData);
          if (attackerUpkeep.log) roundLog += ` ${attackerUpkeep.log}`;
          if (defenderUpkeep.log) roundLog += ` ${defenderUpkeep.log}`;

          let attackerRoundEnd = settleConditionsAtRoundEnd(attacker, '玩家', combatData);
          let defenderRoundEnd = settleConditionsAtRoundEnd(defender, 'NPC', combatData);
          const 规则改写回合尾日志 = 递减战斗规则改写运行态(combatData);
          if (attackerRoundEnd.log) roundLog += ` ${attackerRoundEnd.log}`;
          if (defenderRoundEnd.log) roundLog += ` ${defenderRoundEnd.log}`;
          if (规则改写回合尾日志) roundLog += ` ${规则改写回合尾日志}`;

          syncCombatActionState(attacker);
          syncCombatActionState(defender);
          if (!isCombatUnitAbleToFight(attacker) || !isCombatUnitAbleToFight(defender)) {
            if (isCombatUnitAlive(attacker) && !isCombatUnitAbleToFight(attacker))
              roundLog += ` [体力耗尽] 玩家体力归零，陷入昏迷，无法继续行动。`;
            if (isCombatUnitAlive(defender) && !isCombatUnitAbleToFight(defender))
              roundLog += ` [体力耗尽] NPC体力归零，陷入昏迷，无法继续行动。`;
            continueSimulation = false;
          } else if (attacker.蓄力技能 == null) {
            if (mode === 'single_round') {
              continueSimulation = false;
              roundLog += ` [单回合仲裁] 当前模式为单回合，本次暗箱演算到此结束。`;
            } else {
              const 本轮攻防烈度 = Math.max(
                Math.max(0, Number(appliedDamage || 0)) / getCombatHpMaxValue(被动结算目标),
                Math.max(0, Number(settleResult.totalProjectedDamage || settleResult.dmg || 0)) / getCombatHpMaxValue(主动结算方),
              );
              const continueThresholdReached = 本轮攻防烈度 >= 自动续推设置.stopDamageRatio;
              if (continueThresholdReached) {
                continueSimulation = false;
                roundLog += ` [续推终止] 本回合伤害已达生命占比${Math.round(自动续推设置.stopDamagePercent)}%，暗箱续推停止。`;
              } else {
                const continueRoll = Math.random();
                const continueHit = continueRoll < 自动续推设置.continueChance;
                continueSimulation = continueHit;
                roundLog += ` [续推判定] 本回合伤害约为生命占比${Math.round(本轮攻防烈度 * 100)}%，未达到${Math.round(自动续推设置.stopDamagePercent)}%，按${Math.round(自动续推设置.continueChancePercent)}%概率续推。Roll:${continueRoll.toFixed(2)} 判定:${continueHit ? '继续' : '停止'}。`;
              }
            }
          }

          battleLog.push(replaceBattleReportGenericNames(roundLog, { player: attacker, enemy: defender }));
        }

        combatData.回合 = startingRound + roundCount;
        syncCombatActionState(attacker);
        syncCombatActionState(defender);
        combatData.阶段 = !isCombatUnitAbleToFight(attacker) || !isCombatUnitAbleToFight(defender) ? 战斗阶段枚举_V1.回合结算 : 战斗阶段枚举_V1.宣告;
        const unresolvedReason =
          isCombatUnitAbleToFight(attacker) && isCombatUnitAbleToFight(defender)
            ? mode === 'single_round'
              ? 'single_round_limit'
              : 'simulation_stopped'
            : '';
        const battleOutcome = buildFrontEndBattleOutcome(attacker, defender, {
          unresolvedReason,
          formalEncounter: roundCount > 0,
        });
        let extraPatchOps = [];
        let settleResult = settleBattle(attacker, defender, battleOutcome, {
          mode,
          roundCount,
          maxRounds,
          unresolvedReason,
          combatData,
        });
        if (clashExtraPatchOps.length) extraPatchOps.push(...clashExtraPatchOps);
        if (settleResult.log) battleLog.push(replaceBattleReportGenericNames(settleResult.log, { player: attacker, enemy: defender }));
        if (settleResult.extraPatchOps) extraPatchOps.push(...settleResult.extraPatchOps);
        const runtimeLedger = Array.isArray(combatData.__battleEventLedger) ? combatData.__battleEventLedger.slice() : [];
        const eventLedger = 补齐事件账本未闭合敌对动作(合并战斗事件账本(runtimeLedger, []));
        combatData.__battleEventLedger = eventLedger;
        写入行动闭环事实账本(combatData, eventLedger);
        const towerPendingSettlement =
          isSoulTowerCombatTypeValue(combatData?.战斗类型 || '') && battleOutcome.isVictory === true
            ? buildSoulTowerPendingSettlement(combatData, defender)
            : null;
        if (towerPendingSettlement) {
          combatData.魂灵塔待结算 = towerPendingSettlement;
          combatData.进行中 = true;
          combatData.裁断结果 = '';
          clearCombatAdjudicationHints(combatData);
          if (dryRun) {
            const 公开战报 = buildPublicBattleReportBlock({ battleLog, combatData, battleOutcome, modeLabel, roundCount, eventLedger });
            return buildBattlePreviewResult({
              intentText: visiblePlayerInput || String(playerInput || '战斗行动'),
              mode,
              modeLabel,
              battleLog,
              combatData,
              battleOutcome,
              publicReport: 公开战报,
              roundCount,
              pendingSettlement: towerPendingSettlement,
              intentMode: combatData.战斗意图,
            });
          }
          const pendingUpdate =
            window.BattleUIBridge?.persistCombatData?.(combatData, {
              analysis:
                'Frontend soul tower battle already reached a deterministic win state. Persist the pending settlement choice and wait for the player to choose whether to stop or continue.',
              extraPatchOps,
              syncHpRecoveryOnly: false,
            }) || null;
          return {
            intentText: visiblePlayerInput || String(playerInput || '战斗行动'),
            mode: 'tower_pending_choice',
            battleMode: mode,
            modeLabel,
            intentMode: combatData.战斗意图,
            logs: [...battleLog],
            roundsExecuted: roundCount,
            publicReport: buildPublicBattleReportBlock({ battleLog, combatData, battleOutcome, modeLabel, roundCount, eventLedger }),
            decisionTrace: collectBattleDecisionTrace(combatData),
            resolutionTrace: collectBattleResolutionTrace(combatData),
            publicReportBlocks: 构建事件账本公开战报Blocks(eventLedger, 8, { combatData }).map(item => cloneBattleRuntimeAuditSnapshot(item)),
            closedLoopLedger: cloneBattleRuntimeAuditSnapshot(combatData?.__行动闭环诊断?.事实账本 || null),
            eventLedger: (Array.isArray(eventLedger) ? eventLedger : []).map(item => cloneBattleRuntimeAuditSnapshot(item)),
            pendingSettlement: towerPendingSettlement,
            mvuUpdate: pendingUpdate,
          };
        }

        const hpSuggestion = buildHpSuggestionPayload(
          attacker,
          defender,
          combatData,
          Math.max(0, Number(settleResult?.dmg || 0)),
        );
        combatData.前端建议结果 = hpSuggestion.前端建议结果;
        combatData.裁断约束 = hpSuggestion.裁断约束;
        combatData.建议终点HP区间 = hpSuggestion.建议终点HP区间;
        combatData.前端推荐终点HP = hpSuggestion.前端推荐终点HP;
        combatData.预计HP伤害 = hpSuggestion.预计HP伤害;
        const 压制战裁断 = 构建压制战HP截断裁断(combatData, 本次战斗起始HP快照, attacker, defender);
        if (压制战裁断?.必须结束) {
          combatData.前端建议结果 = '压制战HP截断';
          combatData.建议终点HP区间 = `${压制战裁断.败方剩余HP比例}%`;
          combatData.前端推荐终点HP = Math.round(getCombatHpMaxValue(压制战裁断.败方 === (attacker?.name || attacker?.名称) ? attacker : defender) * 压制战裁断.败方剩余HP比例 / 100);
          combatData.预计HP伤害 = `${压制战裁断.败方}已损失${压制战裁断.HP损失比例}% HP`;
        }
        if (判断虚拟战斗类型(combatData.战斗类型)) {
          combatData.虚拟裁断数据 = {
            最后一击HP比例: Number(Math.max(0, 本次最大单击HP比例).toFixed(4)),
          };
        } else {
          delete combatData.虚拟裁断数据;
        }
        if (撤离结算结果 === '成功') {
          combatData.前端建议结果 = '强制撤离';
          combatData.裁断结果 = '玩家成功撤离';
          combatData.进行中 = false;
          combatData.建议终点HP区间 = '保持当前HP，战斗结束';
          combatData.前端推荐终点HP = getCombatHpValue(attacker);
          combatData.预计HP伤害 = 0;
        }
        combatData.本次操作 = {
          批次ID: `battle-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
          模式: mode,
          起始回合: startingRound + 1,
          结束回合: combatData.回合,
          玩家输入: String(visiblePlayerInput || playerInput || '战斗行动'),
          结算状态: `${battleOutcome.type}/pending_ai_confirm`,
          AI确认结果: '',
        };
        const 压制战规则文本 = 压制战裁断
          ? 压制战裁断.必须结束
            ? `\n[压制等级裁断]\n本场存在刻意压制等级：${压制战裁断.压制摘要.join('；')}。${压制战裁断.败方} 已损失 ${压制战裁断.HP损失比例}% HP，超过 10% 截断线；必须判定战斗结束，胜方=${压制战裁断.胜方}，败方=${压制战裁断.败方}，败方剩余HP比例=${压制战裁断.败方剩余HP比例}。`
            : `\n[压制等级裁断]\n本场存在刻意压制等级：${压制战裁断.压制摘要.join('；')}。仅在任意一方本次战斗累计HP损失达到或超过10%时，才必须立刻结束并判负；当前尚未触发强制截断。`
          : '';
        const 虚拟战规则文本 = 判断虚拟战斗类型(combatData.战斗类型)
          ? `\n[虚拟战规则]\n本场为虚拟战斗，永远允许被击杀，败方剩余HP比例可以为0。若现实建档角色在虚拟战中死亡，模块会让其退出虚拟世界并恢复全部HP，同时按最后一击伤害占总生命比例削减精神力；最后一击比例=${Math.round(本次最大单击HP比例 * 100)}%，精神力消耗最低40%、被秒杀或超过100%时消耗90%。`
          : '';
        const 裁断约束文本 = `可致死：${combatData.裁断约束?.可致死 ? '是' : '否'}；可外界介入：${combatData.裁断约束?.可外界介入 ? '是' : '否'}；关系收手系数：${combatData.裁断约束?.关系收手系数}；场地安全系数：${combatData.裁断约束?.场地安全系数}；实力差距系数：${combatData.裁断约束?.实力差距系数}；绝境失手系数：${combatData.裁断约束?.绝境失手系数}；失手等级：${combatData.裁断约束?.失手等级}`;
        const 公开战报 = buildPublicBattleReportBlock({ battleLog, combatData, battleOutcome, modeLabel, roundCount, eventLedger });
        const 裁断卷宗 = buildBattleAdjudicationDossier({ combatData, battleOutcome, 压制战规则文本, 虚拟战规则文本, 裁断约束文本 });
        const mvuUpdate =
          dryRun
            ? null
            : window.BattleUIBridge?.persistCombatData?.(combatData, {
                analysis:
                  'Frontend battle arbitration updates deterministic battle state changes (resource consumption, equipment/domain toggles, ongoing status effects, sustain effects, charging state and battle context), but does not directly settle final HP, survival or the final裁断结果. Let the plot continuation decide the terminal landing.',
                extraPatchOps,
                syncHpRecoveryOnly: false,
              }) || null;

        try {
          const safeClone = value => {
            try { return typeof structuredClone === 'function' ? structuredClone(value) : JSON.parse(JSON.stringify(value)); }
            catch (_) { try { return JSON.parse(JSON.stringify(value)); } catch (__) { return null; } }
          };
          root[dryRun ? '__LWCS_LAST_BATTLE_PREVIEW_SNAPSHOT__' : '__LWCS_LAST_BATTLE_SNAPSHOT__'] = {
            时间戳: Date.now(),
            模式: mode,
            模式标签: modeLabel,
            战斗类型: combatData.战斗类型 || '未知',
            回合数: roundCount,
            起始回合: startingRound,
            结束回合: combatData.回合,
            阶段: combatData.阶段 || 战斗阶段枚举_V1.无,
            参战者: safeClone(combatData.参战者),
            裁断结果: combatData.裁断结果 || '',
            压制战裁断: 压制战裁断 ? safeClone(压制战裁断) : null,
            战报: [...battleLog],
            patchOps数: Array.isArray(extraPatchOps) ? extraPatchOps.length : 0,
          };
        } catch (error) {
          console.warn('[battle] 缓存战斗快照失败', error);
        }

        if (dryRun) {
          return buildBattlePreviewResult({
            intentText: visiblePlayerInput || String(playerInput || '战斗行动'),
            mode,
            modeLabel,
            battleLog,
            combatData,
            battleOutcome,
            publicReport: 公开战报,
            dossier: 裁断卷宗,
            roundCount,
            intentMode: combatData.战斗意图,
            eventLedger,
          });
        }
        const 战斗上下文登记 = registerBattleSettlementContext({
          id: combatData.本次操作?.批次ID,
          公开战报,
          裁断卷宗,
          来源: 'BattleUI_Module',
        });
        const userBattleMessage = [
          String(visiblePlayerInput || playerInput || '战斗行动').split('\n')[0] || '战斗行动',
          公开战报,
        ].filter(Boolean).join('\n\n');
        sendToAI(userBattleMessage, '', {
          mvuUpdate,
          requestKind: 'battle_settlement_plot',
        });
        return {
          intentText: userBattleMessage,
          mode: 'engine_arbitrated',
          battleMode: mode,
          modeLabel,
          intentMode: combatData.战斗意图,
          logs: [...battleLog],
          roundsExecuted: roundCount,
          publicReport: 公开战报,
          publicReportBlocks: 构建事件账本公开战报Blocks(eventLedger, 8, { combatData }).map(item => cloneBattleRuntimeAuditSnapshot(item)),
          decisionTrace: collectBattleDecisionTrace(combatData),
          resolutionTrace: collectBattleResolutionTrace(combatData),
          closedLoopLedger: cloneBattleRuntimeAuditSnapshot(combatData?.__行动闭环诊断?.事实账本 || null),
          eventLedger: (Array.isArray(eventLedger) ? eventLedger : []).map(item => cloneBattleRuntimeAuditSnapshot(item)),
          battleSettlementContext: 战斗上下文登记,
          aiRequest: root.__lastBattleAIRequest || null,
        };
      }

        root.BattleUIBridge = Object.assign(root.BattleUIBridge || {}, {
        __executePlayerBattleIntentImpl(playerInput, options = {}) {
          const battleMode = options.mode === 'multi_round' ? 'multi_round' : 'single_round';
          const autoContinueConfig = 规范化自动续推设置(options.autoContinueConfig || root.BattleUI?.state?.autoContinueConfig || {}, battleMode);
          const result = onPlayerAttack(String(playerInput || ''), {
            mode: battleMode,
            intentMode: options.intentMode,
            dryRun: options.dryRun === true,
            combatData: options.combatData,
            autoContinueConfig,
          });
          return result || {
            intentText: String(playerInput || ''),
            mode: 'engine_arbitrated',
            battleMode,
            aiRequest: root.__lastBattleAIRequest || null,
          };
        },
        __executeBattleFlowImpl(combatData, options = {}) {
          const state = root.BattleUI?.state || {};
          const actionList = typeof root.BattleUI?.读取可提交战斗动作队列 === 'function'
            ? root.BattleUI.读取可提交战斗动作队列(state)
            : [
                ...(state.selectedPreActions || []),
                state.selectedSkillActions?.[state.selectedSkillActions.length - 1] || state.selectedAction,
              ].filter(Boolean).filter(action => action && action.enabled !== false);
          if (!actionList.length && !String(options.intentText || '').trim()) throw new Error('battle_action_unavailable');
          const intentText =
            String(options.intentText || '').trim() ||
            String(root.BattleUI?.buildIntentText?.(actionList) || '').trim();
          if (!intentText) throw new Error('battle_intent_empty');
          return root.BattleUIBridge.__executePlayerBattleIntentImpl(intentText, options);
        },
      });

      // ==========================================
      // 📍 2. 战前消耗与战后结算区 (彻底净化版)
      // ==========================================
      function findCombatUnitByName(combatData, rawName = '') {
        const wanted = String(rawName || '').trim();
        if (!combatData || !wanted) return null;
        const roster = [
          ...(combatData?.参战者?.team_player || []),
          ...(combatData?.参战者?.team_enemy || []),
        ].filter(Boolean);
        return roster.find(unit => String(unit?.name || '').trim() === wanted) || null;
      }

      function isCombatUnitIdentityMatch(unit, rawIdentity = '') {
        if (unit && rawIdentity && unit === rawIdentity) return true;
        const wanted = rawIdentity && typeof rawIdentity === 'object'
          ? String(rawIdentity.name || rawIdentity.名称 || rawIdentity.charKey || rawIdentity.char_key || rawIdentity.key || '').trim()
          : String(rawIdentity || '').trim();
        if (!unit || !wanted) return false;
        const candidates = [
          unit.name,
          unit.名称,
          unit.charKey,
          unit.char_key,
          unit.key,
        ]
          .map(value => String(value || '').trim())
          .filter(Boolean);
        return candidates.includes(wanted);
      }

      function getCurrentBattleContextSnapshot() {
        return window.BattleUIBridge?.getBattleContext?.() || window.BattleUIBridge?.getMVU?.('world.战斗') || null;
      }

      function getBattleRosterUnits(combatData) {
        return [
          ...(combatData?.参战者?.team_player || []),
          ...(combatData?.参战者?.team_enemy || []),
        ].filter(Boolean);
      }

      function resolveFusionPartnerUnitsForSkill(skill = {}, alliedTeam = [], battleContext = null, actorRef = null) {
        const partnerNames = Array.isArray(skill?.__fusion_partner_names) ? skill.__fusion_partner_names : [];
        if (!partnerNames.length) return [];
        const roster = [
          ...(Array.isArray(alliedTeam) ? alliedTeam : []),
          ...getBattleRosterUnits(battleContext || getCurrentBattleContextSnapshot()),
        ].filter(Boolean);
        const actorName = String(actorRef?.name || actorRef?.名称 || actorRef?.charKey || '').trim();
        const matchedUnits = [];
        partnerNames.forEach(partnerName => {
          const unit = roster.find(candidate => {
            if (!candidate) return false;
            if (actorName && isCombatUnitIdentityMatch(candidate, actorName)) return false;
            if (matchedUnits.includes(candidate)) return false;
            return isCombatUnitIdentityMatch(candidate, partnerName);
          });
          if (unit) matchedUnits.push(unit);
        });
        return matchedUnits;
      }

      function deductParsedCostFromUnit(targetUnit, parsed = {}) {
        if (!targetUnit || !parsed) return;
        const stats = targetUnit.属性 || targetUnit;
        // Phase E: 任何主动行动叠加分级行动疲劳 (按 action_type / 风险等级 / 来源类别 推断)
        // 跳过条件: parsed.skipFatigue === true (内部分摊 / 反伤 / 被动结算等非主动行动)
        if (!parsed.__已注入行动疲劳 && parsed.skipFatigue !== true) {
          const 行动疲劳 = 计算行动疲劳体力_V1(targetUnit, parsed);
          parsed.reqVit = Math.max(0, Number(parsed.reqVit || 0)) + 行动疲劳;
          parsed.__已注入行动疲劳 = true;
          parsed.__行动疲劳数值 = 行动疲劳;
          parsed.__行动疲劳率 = 推断行动疲劳率_V1(parsed);
        }
        const 下一魂力 = Math.max(0, Number(stats.sp ?? stats.魂力 ?? 0) - Number(parsed.reqSp || 0));
        const 下一体力 = Math.max(0, Number(stats.sta ?? stats.体力 ?? stats.vit ?? 0) - Number(parsed.reqVit || 0));
        const 下一精神力 = Math.max(0, Number(stats.men ?? stats.精神力 ?? 0) - Number(parsed.reqMen || 0));
        stats.sp = 下一魂力;
        stats.魂力 = 下一魂力;
        stats.sta = 下一体力;
        stats.vit = 下一体力;
        stats.体力 = 下一体力;
        stats.men = 下一精神力;
        stats.精神力 = 下一精神力;
        if (targetUnit.召唤键) 同步召唤单位镜像(targetUnit);
      }

      function 读取单位资源当前值(单位 = {}, 资源键 = '') {
        const stats = 单位?.属性 || 单位 || {};
        if (资源键 === 'sp') return Math.max(0, Number(stats.sp ?? stats.魂力 ?? 0));
        if (资源键 === 'vit') return Math.max(0, Number(stats.sta ?? stats.体力 ?? stats.vit ?? 0));
        if (资源键 === 'men') return Math.max(0, Number(stats.men ?? stats.精神力 ?? 0));
        return 0;
      }

      function 计算消耗分摊状态(单位 = {}) {
        const 状态列表 = Object.entries(单位?.状态效果 || {})
          .map(([状态名, 状态]) => ({ 状态名, 战斗效果: 状态?.战斗效果 || {} }))
          .filter(item => Number(item.战斗效果.cost_share_ratio || 0) > 0 && Number(item.战斗效果.cost_share_count || 0) > 0);
        if (!状态列表.length) return null;
        return 状态列表.reduce((当前, 候选) => {
          const 当前比例 = Number(当前?.战斗效果?.cost_share_ratio || 0);
          const 候选比例 = Number(候选.战斗效果.cost_share_ratio || 0);
          return 候选比例 > 当前比例 ? 候选 : 当前;
        }, 状态列表[0]);
      }

      function 计算分摊消耗项(原值 = 0, 分摊比例 = 0, 分摊人数 = 1) {
        const 总值 = Math.max(0, Math.floor(Number(原值 || 0)));
        const 分出总值 = Math.max(0, Math.floor(总值 * Math.max(0, Math.min(1, Number(分摊比例 || 0)))));
        if (!(分出总值 > 0) || !(分摊人数 > 0)) return { 自身值: 总值, 单位值列表: [] };
        const 基础值 = Math.max(0, Math.floor(分出总值 / 分摊人数));
        const 单位值列表 = Array.from({ length: 分摊人数 }, (_, index) =>
          index === 分摊人数 - 1 ? Math.max(0, 分出总值 - 基础值 * Math.max(0, 分摊人数 - 1)) : 基础值,
        );
        return {
          自身值: Math.max(0, 总值 - 分出总值),
          单位值列表,
        };
      }

      function 应用消耗分摊(解析消耗 = {}, 施术者 = {}, 战斗上下文 = null) {
        const 分摊状态 = 计算消耗分摊状态(施术者);
        if (!分摊状态) return 解析消耗;
        const 分摊比例 = Math.max(0, Math.min(1, Number(分摊状态.战斗效果.cost_share_ratio || 0)));
        const 分摊人数上限 = Math.max(0, Math.round(Number(分摊状态.战斗效果.cost_share_count || 0)));
        if (!(分摊比例 > 0) || !(分摊人数上限 > 0)) return 解析消耗;
        const 分摊对象 = getCombatAlliesForUnit(战斗上下文 || getCurrentBattleContextSnapshot(), 施术者)
          .filter(单位 => 单位 && isCombatUnitAlive(单位))
          .slice(0, 分摊人数上限);
        if (!分摊对象.length) return 解析消耗;
        const 分摊人数 = 分摊对象.length;
        const 魂力分摊 = 计算分摊消耗项(解析消耗.reqSp, 分摊比例, 分摊人数);
        const 体力分摊 = 计算分摊消耗项(解析消耗.reqVit, 分摊比例, 分摊人数);
        const 精神分摊 = 计算分摊消耗项(解析消耗.reqMen, 分摊比例, 分摊人数);
        const 分摊消耗 = 分摊对象.map((unit, index) => ({
          unit,
          name: String(unit?.name || unit?.名称 || '队友').trim() || '队友',
          reqSp: 魂力分摊.单位值列表[index] || 0,
          reqVit: 体力分摊.单位值列表[index] || 0,
          reqMen: 精神分摊.单位值列表[index] || 0,
        }));
        const 不足对象 = 分摊消耗.find(item =>
          读取单位资源当前值(item.unit, 'sp') < item.reqSp ||
          读取单位资源当前值(item.unit, 'vit') < item.reqVit ||
          读取单位资源当前值(item.unit, 'men') < item.reqMen,
        );
        const 自身可支付 =
          读取单位资源当前值(施术者, 'sp') >= 魂力分摊.自身值 &&
          读取单位资源当前值(施术者, 'vit') >= 体力分摊.自身值 &&
          读取单位资源当前值(施术者, 'men') >= 精神分摊.自身值;
        return {
          ...解析消耗,
          reqSp: 魂力分摊.自身值,
          reqVit: 体力分摊.自身值,
          reqMen: 精神分摊.自身值,
          canCast: Boolean((解析消耗.canCast || 解析消耗.failureReason === '自身状态不足') && 自身可支付 && !不足对象),
          failureReason: 解析消耗.canCast && !自身可支付
            ? '自身分摊后状态不足'
            : 不足对象
              ? `${不足对象.name}分摊状态不足`
              : 解析消耗.failureReason || '',
          分摊消耗列表: 分摊消耗,
          消耗分摊状态名: 分摊状态.状态名,
        };
      }

      function scaleFusionPanelModifierMap(panelMods = {}, fusionProfile = null) {
        if (!fusionProfile) return { ...(panelMods || {}) };
        const next = { ...(panelMods || {}) };
        const ratio = Number(fusionProfile.stateScale || 1);
        Object.keys(next).forEach(key => {
          const value = Number(next[key]);
          if (!Number.isFinite(value) || Math.abs(value - 1) < 0.0001) return;
          next[key] = value >= 1 ? scaleBattleFactor(value, ratio, 1) : scaleBattleDebuffRatio(value, ratio, 1);
        });
        return next;
      }

      function scaleFusionCombatEffectMap(calc = {}, fusionProfile = null) {
        if (!fusionProfile) return { ...(calc || {}) };
        const next = mergeCombatEffectMaps(createEmptyCombatEffectMap(), calc || {});
        const damageScale = Number(fusionProfile.damageMult || 1);
        const recoverScale = Number(fusionProfile.recoverMult || 1);
        const shieldScale = Number(fusionProfile.shieldMult || 1);
        const stateScale = Number(fusionProfile.stateScale || 1);
        const controlScale = Number(fusionProfile.controlScale || 1);

        ['hit_bonus', 'reaction_bonus', 'dodge_bonus', 'attacker_speed_bonus', 'cast_speed_bonus', 'interrupt_bonus'].forEach(
          key => {
            if (next[key] !== undefined) next[key] = scaleBattleValue(next[key], controlScale, { digits: 4 });
          },
        );
        ['reaction_penalty', 'hit_penalty', 'dodge_penalty', 'cast_speed_penalty'].forEach(key => {
          if (next[key] !== undefined) next[key] = scaleBattleValue(next[key], controlScale, { digits: 4 });
        });
        ['damage_reduction', 'counter_attack_ratio', 'damage_reflect_ratio', 'damage_transfer_ratio', 'damage_share_ratio', 'cost_share_ratio', 'vit_gain_ratio', 'sp_gain_ratio', 'men_gain_ratio', 'hot_heal_ratio', 'heal_block_ratio', 'random_target_rate', '判断干扰强度', '索敌干扰强度', 'stealth_level', 'bonus_true_damage_ratio', 'damage_absorb_ratio', 'damage_to_heal_ratio', 'heal_to_damage_ratio', 'heal_inversion_ratio', 'revive_heal_ratio', 'cost_delta_ratio'].forEach(
          key => {
            if (next[key] !== undefined) next[key] = scaleBattleValue(next[key], key.includes('heal') || key.includes('gain') ? recoverScale : stateScale, {
              min: 0,
              max: key.includes('ratio') || key.includes('reduction') || key.includes('block') ? 1 : undefined,
              digits: 4,
            });
          },
        );
        if (next.lock_level !== undefined) next.lock_level = scaleBattleLockLevel(next.lock_level, controlScale);
        if (next.dot_damage !== undefined) next.dot_damage = scaleBattleValue(next.dot_damage, damageScale, { min: 0, digits: 2 });
        ['death_save_count', 'revive_count', 'damage_share_count', 'cost_share_count', '每日触发次数上限'].forEach(key => {
          if (next[key] !== undefined) next[key] = Math.max(0, Math.round(Number(next[key] || 0) * stateScale));
        });
        if (next.invincible_tier_threshold !== undefined) next.invincible_tier_threshold = Math.max(0, Number(next.invincible_tier_threshold || 0));
        if (next.final_damage_mult !== undefined) next.final_damage_mult = scaleBattleFactor(next.final_damage_mult, damageScale, 1);
        if (next.received_damage_mult !== undefined) next.received_damage_mult = scaleBattleFactor(next.received_damage_mult, damageScale, 1);
        if (next.final_damage_bonus !== undefined) next.final_damage_bonus = scaleBattleValue(next.final_damage_bonus, damageScale, { min: 0, digits: 2 });
        if (next.final_heal_mult !== undefined) next.final_heal_mult = scaleBattleFactor(next.final_heal_mult, recoverScale, 1);
        if (next.final_heal_bonus !== undefined) next.final_heal_bonus = scaleBattleValue(next.final_heal_bonus, recoverScale, { min: 0, digits: 2 });
        if (next.shield_gain_mult !== undefined) next.shield_gain_mult = scaleBattleFactor(next.shield_gain_mult, shieldScale, 1);
        if (next.shield_gain_bonus !== undefined) next.shield_gain_bonus = scaleBattleValue(next.shield_gain_bonus, shieldScale, { min: 0, digits: 2 });
        if (next.min_hp_floor !== undefined) next.min_hp_floor = Math.max(0, Math.round(Number(next.min_hp_floor || 0) * stateScale));
        if (next.death_save_count !== undefined) next.death_save_count = Math.max(0, Math.round(Number(next.death_save_count || 0) * stateScale));
        return next;
      }

      function getFusionScaledInterruptChance(skill = {}) {
        const interruptEffect = getSkillEffects(skill).find(effect => 读取战斗效果标签(effect) === '打断') || {};
        const baseChance = Number(interruptEffect?.中断概率 || interruptEffect?.interrupt_bonus || 0);
        const fusionProfile = getFusionBattleProfileFromSkill(skill);
        if (!fusionProfile) return baseChance;
        return scaleBattleValue(baseChance, Number(fusionProfile.controlScale || 1), { min: 0, max: 1, digits: 4 });
      }

      function createFusionAftermathCondition(fusionProfile = {}, skillName = '武魂融合技') {
        return {
          类型: 'debuff',
          层数: 1,
          描述: `施展[${skillName}]后的共鸣透支`,
          duration: Math.max(1, Math.round(Number(fusionProfile.aftermathDuration || 2))),
          面板修改比例: {
            str: Number(fusionProfile.aftermathPanelScale || 0.82),
            def: Number(fusionProfile.aftermathPanelScale || 0.82),
            agi: Number(fusionProfile.aftermathPanelScale || 0.82),
            sp_max: 1.0,
          },
          战斗效果: {
            ...createEmptyCombatEffectMap(),
            final_damage_mult: Number(fusionProfile.aftermathDamageMult || 0.82),
            final_heal_mult: Number(fusionProfile.aftermathHealMult || 0.82),
            shield_gain_mult: Number(fusionProfile.aftermathShieldMult || 0.82),
            cast_speed_penalty: Number(fusionProfile.aftermathCastPenalty || 0.35),
            dodge_penalty: Number(fusionProfile.aftermathDodgePenalty || 0.12),
            reaction_penalty: Number(fusionProfile.aftermathReactionPenalty || 0.12),
          },
          资源锁定规则: [
            { 资源: ['魂力', '精神力'], 锁定类型: '回复锁定', 比例: Number(fusionProfile.aftermathResourceBlock || 0.22) },
          ],
        };
      }

      function applyFusionAftermathCondition(targetUnit, fusionProfile = {}, skillName = '武魂融合技') {
        if (!targetUnit) return false;
        if (!targetUnit.状态效果) targetUnit.状态效果 = {};
        const nextCondition = createFusionAftermathCondition(fusionProfile, skillName);
        const existing = targetUnit.状态效果['融合后虚弱'];
        if (existing && typeof existing === 'object') {
          existing.duration = Math.max(Number(existing.duration || 0), Number(nextCondition.duration || 0));
          existing.描述 = nextCondition.描述;
          existing.面板修改比例 = nextCondition.面板修改比例;
          existing.战斗效果 = nextCondition.战斗效果;
          existing.资源锁定规则 = nextCondition.资源锁定规则;
        } else {
          const 持续移除命中 = 持续状态移除阻断状态附着(targetUnit, '融合后虚弱', nextCondition, null);
          if (持续移除命中) return false;
          targetUnit.状态效果['融合后虚弱'] = nextCondition;
        }
        return true;
      }

      function applyFusionActionAftermath(attackerChar, playerAction, battleContext = null) {
        if (!playerAction?.skill || playerAction.__fusion_aftermath_applied) return '';
        const fusionProfile = getFusionBattleProfileFromSkill(playerAction.skill);
        if (!fusionProfile) return '';
        const skillName = String(playerAction.skill?.name || playerAction.skill?.魂技名 || '武魂融合技').trim() || '武魂融合技';
        const partnerUnits = Array.isArray(playerAction.__fusion_partner_units) && playerAction.__fusion_partner_units.length
          ? playerAction.__fusion_partner_units
          : resolveFusionPartnerUnitsForSkill(playerAction.skill, [], battleContext, attackerChar);
        const affectedUnits = [attackerChar, ...partnerUnits].filter((unit, index, list) => unit && list.indexOf(unit) === index);
        if (!affectedUnits.length) return '';
        affectedUnits.forEach(unit => applyFusionAftermathCondition(unit, fusionProfile, skillName));
        playerAction.__fusion_aftermath_applied = true;
        const affectedNames = affectedUnits.map(unit => unit?.name || unit?.名称 || '施术者').filter(Boolean);
        return `[融合反噬] ${affectedNames.join('、')}施展[${skillName}]后经络与精神同步透支，全部陷入[融合后虚弱]。`;
      }

      function 读取施术者队伍列表(battleContext, attackerChar, 读取友方 = true) {
        const 玩家队伍 = Array.isArray(battleContext?.参战者?.team_player) ? battleContext.参战者.team_player : [];
        const 敌方队伍 = Array.isArray(battleContext?.参战者?.team_enemy) ? battleContext.参战者.team_enemy : [];
        const 在玩家队 = 玩家队伍.some(单位 => isCombatUnitIdentityMatch(单位, attackerChar?.name || attackerChar?.名称 || attackerChar));
        const 基础队伍 = 在玩家队 ? 玩家队伍 : 敌方队伍;
        const 对侧队伍 = 在玩家队 ? 敌方队伍 : 玩家队伍;
        const 目标队伍 = 读取友方 ? 基础队伍 : 对侧队伍;
        const 候选队伍 = 读取友方 ? [attackerChar, ...目标队伍] : [...目标队伍];
        return dedupeCombatTargetList(候选队伍.filter(Boolean)).filter(isCombatUnitAlive);
      }

      function 读取动作批量目标原始列表(playerAction = {}) {
        const 候选列表 = [
          playerAction.__批量目标列表,
          playerAction.__批量目标名称列表,
          playerAction.目标列表,
          playerAction.目标名称列表,
        ];
        const 结果 = [];
        候选列表.forEach(候选 => {
          if (Array.isArray(候选)) {
            候选.forEach(项目 => {
              if (项目 !== undefined && 项目 !== null && 项目 !== '') 结果.push(项目);
            });
          } else if (typeof 候选 === 'string' && 候选.trim()) {
            候选
              .split(/[、,，;；\s]+/)
              .map(项目 => 项目.trim())
              .filter(Boolean)
              .forEach(项目 => 结果.push(项目));
          }
        });
        return 结果;
      }

      function 解析动作批量目标列表(battleContext, attackerChar, playerAction, 读取友方 = true) {
        const 原始列表 = 读取动作批量目标原始列表(playerAction);
        if (!原始列表.length) return [];
        const 默认队伍 = 读取施术者队伍列表(battleContext, attackerChar, 读取友方);
        const 全体命中 = 原始列表.some(项目 => /全队|全体|所有人|所有队友|全部队友/.test(String(项目 || '')));
        if (全体命中) return 默认队伍;
        const 全部单位 = dedupeCombatTargetList([
          ...(battleContext?.参战者?.team_player || []),
          ...(battleContext?.参战者?.team_enemy || []),
          attackerChar,
        ]).filter(Boolean);
        return dedupeCombatTargetList(
          原始列表
            .map(项目 => {
              if (项目 && typeof 项目 === 'object') {
                const 名称 = String(项目?.name || 项目?.名称 || '').trim();
                return 名称 ? findCombatUnitByName(battleContext, 名称) || 全部单位.find(单位 => isCombatUnitIdentityMatch(单位, 名称)) || 项目 : 项目;
              }
              const 名称 = String(项目 || '').trim();
              return findCombatUnitByName(battleContext, 名称) || 全部单位.find(单位 => isCombatUnitIdentityMatch(单位, 名称)) || null;
            })
            .filter(Boolean),
        ).filter(isCombatUnitAlive);
      }

      function 选取最高魂力单位(队伍 = []) {
        return (
          [...(Array.isArray(队伍) ? 队伍 : [])].sort(
            (左, 右) => Number(右?.sp_max || 右?.属性?.魂力上限 || 0) - Number(左?.sp_max || 左?.属性?.魂力上限 || 0),
          )[0] || null
        );
      }

      function 建立辅助消耗上下文(目标列表 = [], 批量模式 = '', 兜底目标 = null) {
        const 有效目标列表 = dedupeCombatTargetList((Array.isArray(目标列表) ? 目标列表 : []).filter(Boolean)).filter(isCombatUnitAlive);
        const 目标 = 选取最高魂力单位(有效目标列表) || 兜底目标 || 有效目标列表[0] || null;
        return {
          目标,
          目标数量: Math.max(1, 有效目标列表.length || (目标 ? 1 : 0)),
          目标列表: 有效目标列表,
          批量模式,
        };
      }

      function resolveSupportCostContext(attackerChar, playerAction) {
        const skill = playerAction?.skill;
        if (!skill || !技能需要目标消耗上下文(skill)) return { 目标: null, 目标数量: 1, 目标列表: [], 批量模式: '' };
        const targetText = String(getSkillTarget(skill) || '').trim();
        if (!targetText) return { 目标: null, 目标数量: 1, 目标列表: [], 批量模式: '' };
        if (/自身/.test(targetText)) return 建立辅助消耗上下文([attackerChar].filter(Boolean), '', attackerChar || null);
        const battleContext = window.BattleUIBridge?.getBattleContext?.() || window.BattleUIBridge?.getMVU?.('world.战斗') || null;
        const preferredName = String(playerAction?.target_name || '').trim();
        if (/己方|友方/.test(targetText)) {
          const 友方队伍 = 读取施术者队伍列表(battleContext, attackerChar, true);
          const 七九单体语义 = 是否七九辅助单体语义(attackerChar, skill);
          if (/群体/.test(targetText) && !七九单体语义) {
            return 建立辅助消耗上下文(友方队伍, '原生群体', attackerChar || null);
          }
          const 批量目标列表 = 解析动作批量目标列表(battleContext, attackerChar, playerAction, true);
          if (批量目标列表.length > 0) return 建立辅助消耗上下文(批量目标列表, '单体批量', attackerChar || null);
          const ally = findCombatUnitByName(battleContext, preferredName);
          return 建立辅助消耗上下文([ally || attackerChar].filter(Boolean), '', attackerChar || null);
        }
        if (/敌/.test(targetText)) {
          const 敌方队伍 = 读取施术者队伍列表(battleContext, attackerChar, false);
          if (/群体/.test(targetText)) {
            return 建立辅助消耗上下文(敌方队伍, '原生群体', null);
          }
          const 指定目标 = findCombatUnitByName(battleContext, preferredName);
          const 敌方目标 = 指定目标 && 敌方队伍.some(unit => isCombatUnitIdentityMatch(unit, 指定目标.name || 指定目标.名称 || 指定目标))
            ? 指定目标
            : 敌方队伍[0] || null;
          return 建立辅助消耗上下文([敌方目标].filter(Boolean), '', 敌方目标);
        }
        return 建立辅助消耗上下文([attackerChar].filter(Boolean), '', attackerChar || null);
      }

      function resolveSupportCostTarget(attackerChar, playerAction) {
        return resolveSupportCostContext(attackerChar, playerAction).目标;
      }

      function 解析动作实际覆盖目标上下文(attackerChar, playerAction, defenderChar = null, battleContext = null) {
        const skill = playerAction?.skill;
        if (!skill) return { 目标: defenderChar || null, 目标数量: 1, 目标列表: [], 批量模式: '' };
        if (技能需要目标消耗上下文(skill)) return resolveSupportCostContext(attackerChar, playerAction);
        const 战斗上下文 = battleContext || window.BattleUIBridge?.getBattleContext?.() || window.BattleUIBridge?.getMVU?.('world.战斗') || null;
        const 条件上下文 = {
          actor: attackerChar,
          caster: attackerChar,
          attacker: attackerChar,
          target: defenderChar,
          defender: defenderChar,
          action: playerAction,
          skill,
          combatData: 战斗上下文,
          当前行动: String(playerAction?.action_type || playerAction?.type || skill?.name || skill?.魂技名 || '').trim(),
        };
        const 主效果 =
          getSkillEffects(skill, 条件上下文).find(effect => String(effect?.原型 || '').trim() === '伤害结算') ||
          getSkillEffects(skill, 条件上下文).find(effect => effect && typeof effect === 'object') ||
          null;
        const 目标上下文 = resolveSkillTargetContext(skill, attackerChar, defenderChar, 战斗上下文, 主效果);
        const 目标列表 = dedupeCombatTargetList((目标上下文.targetSet || []).filter(Boolean)).filter(isCombatUnitAlive);
        const 目标数量 = Math.max(1, 目标列表.length || (defenderChar ? 1 : 0));
        const 批量模式 = 目标数量 > 1 ? '原生群体' : '';
        return {
          目标: 目标列表[0] || defenderChar || null,
          目标数量,
          目标列表,
          批量模式,
        };
      }

      function 读取技能时光回溯效果列表(skill = {}, context = {}) {
        return getSkillEffects(skill, context).filter(effect =>
          effect && typeof effect === 'object' && String(effect?.原型 || '').trim() === '时光回溯',
        );
      }

      function 技能仅含被动时光回溯(skill = {}, context = {}) {
        const 效果列表 = getSkillEffects(skill, context).filter(effect => effect && typeof effect === 'object');
        return 效果列表.length > 0 && 效果列表.every(effect =>
          String(effect?.原型 || '').trim() === '时光回溯' && String(effect?.发动方式 || '被动').trim() !== '主动',
        );
      }

      function 读取技能释放限制(skill = {}, context = {}) {
        const 限制列表 = [];
        if (skill?.触发限制 && typeof skill.触发限制 === 'object' && !Array.isArray(skill.触发限制)) 限制列表.push(skill.触发限制);
        getSkillEffects(skill, context).forEach(effect => {
          if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return;
          if (effect.触发限制 && typeof effect.触发限制 === 'object' && !Array.isArray(effect.触发限制)) 限制列表.push(effect.触发限制);
        });
        return 限制列表;
      }

      function 收集技能环境满足条件(skill = {}) {
        const 结果 = [];
        const visit = effect => {
          if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return;
          (Array.isArray(effect.条件分支) ? effect.条件分支 : []).forEach(branch => {
            const 处理 = String(branch?.处理 || '').trim();
            if (处理 !== '生效' && 处理 !== '禁用') return;
            (Array.isArray(branch?.条件) ? branch.条件 : []).forEach(condition => {
              if (String(condition?.类型 || '').trim() === '环境满足') {
                const 值 = String(condition?.值 || '').trim();
                if (值) 结果.push(值);
              }
            });
          });
          ['使用效果', '授予效果', '结算效果', '追加效果', '替换效果'].forEach(字段名 => {
            (Array.isArray(effect?.[字段名]) ? effect[字段名] : []).forEach(visit);
          });
        };
        (Array.isArray(skill?._效果数组) ? skill._效果数组 : []).forEach(visit);
        return Array.from(new Set(结果));
      }

      function 检查技能释放限制(attackerChar, playerAction = {}, defenderChar = null) {
        const skill = playerAction.skill || {};
        const 条件上下文 = {
          actor: attackerChar,
          caster: attackerChar,
          attacker: attackerChar,
          target: defenderChar,
          defender: defenderChar,
          action: playerAction,
          skill,
          combatData: getCurrentBattleContextSnapshot(),
          当前行动: String(playerAction?.action_type || playerAction?.type || skill?.name || skill?.魂技名 || '').trim(),
        };
        const 日志片段 = [];
        const 技能名 = String(skill?.name || skill?.魂技名 || playerAction.action_type || '技能').trim() || '技能';
        if (playerAction.action_type === '施法失败') return { 可释放: false, 原因: '行动已受限', 日志: '' };
        const 普通攻击 = !playerAction?.skill || 技能名 === '普通攻击' || playerAction?.action_type === '常规攻击';
        const 状态战斗效果 = attackerChar?.状态效果 ? Object.values(attackerChar.状态效果).map(状态 => 状态?.战斗效果 || {}) : [];
        if (!普通攻击 && 状态战斗效果.some(效果 => 效果?.silence === true)) return { 可释放: false, 原因: '沉默压制', 日志: '' };
        if (!普通攻击 && 状态战斗效果.some(效果 => 效果?.skill_seal === true)) return { 可释放: false, 原因: '封技压制', 日志: '' };
        const 限制态 = attackerChar.__技能限制运行态 ||= {};
        const 技能限制态 = 限制态[技能名] ||= { 已用次数: 0 };

        for (const 限制 of 读取技能释放限制(skill, 条件上下文)) {
          const 周期 = String(限制?.周期 || '').trim();
          const 次数 = Math.max(0, Math.floor(Number(限制?.次数 || 0)));
          if (次数 > 0 && ['每日', '每战', '每次施放', '每次行动'].includes(周期) && Number(技能限制态.已用次数 || 0) >= 次数) {
            return { 可释放: false, 原因: `${周期 || '当前周期'}次数已耗尽`, 日志: '' };
          }
        }

        const 环境条件列表 = 收集技能环境满足条件(skill);
        if (环境条件列表.length) {
          const 战斗环境 = 读取条件分支环境文本({ actor: attackerChar, target: defenderChar, action: playerAction, skill });
          const 未满足 = 环境条件列表.find(条件 => !战斗环境.includes(条件));
          if (未满足) return { 可释放: false, 原因: `环境满足[${未满足}]未满足`, 日志: '' };
          日志片段.push(`[环境满足] ${环境条件列表.join('/')}。`);
        }

        const 环境文本 = JSON.stringify(getSkillEffects(skill, 条件上下文) || []);
        const 目标状态条件 = Array.from(环境文本.matchAll(/"目标状态条件":"([^"]+)"/g)).map(item => item[1]).filter(Boolean);
        if (目标状态条件.length) {
          const 目标状态文本 = JSON.stringify(defenderChar?.状态效果 || {});
          const 未满足 = 目标状态条件.find(条件 => !目标状态文本.includes(条件));
          if (未满足) return { 可释放: false, 原因: `目标状态[${未满足}]不存在`, 日志: '' };
        }

        技能限制态.已用次数 = Math.max(0, Number(技能限制态.已用次数 || 0)) + 1;
        return { 可释放: true, 原因: '', 日志: 日志片段.length ? `${日志片段.join(' ')} ` : '' };
      }

      function applyActionCost(attackerChar, playerAction, defenderChar = null, battleContext = null) {
        const stats = attackerChar.属性 || attackerChar;
        const status = attackerChar.状态 || {};
        let log = '';
        let supportCostContext = { 目标: null, 目标数量: 1, 目标列表: [], 批量模式: '' };
        if (!playerAction || typeof playerAction !== 'object') return '';
        if (playerAction.__主动成本已结算 === true) return '';
        if (playerAction.skill && 普通魂技含非法百分比消耗(playerAction, playerAction.skill, attackerChar)) {
          playerAction.action_type = '施法失败';
          playerAction.__主动成本已结算 = true;
          const 技能名 = String(playerAction.skill?.name || playerAction.skill?.魂技名 || '魂技').trim() || '魂技';
          const 施术者 = String(attackerChar?.name || attackerChar?.名称 || '施术者').trim() || '施术者';
          return `[释放受限] ${施术者}的【${技能名}】仍写入百分比启动消耗；普通魂环魂技和自创魂技必须使用固定数值，已取消本次释放且未扣除资源。`;
        }

        const 释放限制 = 检查技能释放限制(attackerChar, playerAction, defenderChar);
        if (!释放限制.可释放) {
          playerAction.action_type = '施法失败';
          playerAction.__主动成本已结算 = true;
          return `[释放受限] ${释放限制.原因}，无法释放[${playerAction.skill?.name || playerAction.skill?.魂技名 || playerAction.action_type || '技能'}]。`;
        }
        if (释放限制.日志) log += 释放限制.日志;
        playerAction.__主动成本已结算 = true;

        const 动作条件上下文 = {
          actor: attackerChar,
          caster: attackerChar,
          attacker: attackerChar,
          target: defenderChar,
          defender: defenderChar,
          action: playerAction,
          skill: playerAction.skill,
          combatData: getCurrentBattleContextSnapshot(),
          当前行动: String(playerAction?.action_type || playerAction?.type || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '').trim(),
        };
        if (playerAction.skill) {
          supportCostContext = 解析动作实际覆盖目标上下文(attackerChar, playerAction, defenderChar, battleContext);
          if (supportCostContext.目标) playerAction.skill.__targetForSupportCost = supportCostContext.目标;
          playerAction.skill.__辅助目标数量 = Math.max(1, Math.floor(Number(supportCostContext.目标数量 || 1)));
          playerAction.skill.__覆盖目标数量 = Math.max(1, Math.floor(Number(supportCostContext.目标数量 || 1)));
          if (Array.isArray(supportCostContext.目标列表) && supportCostContext.目标列表.length > 0) {
            playerAction.skill.__批量目标列表 = supportCostContext.目标列表;
            playerAction.skill.__批量消耗模式 = supportCostContext.批量模式 || '';
          }
        }

        // 💥 1. 通用真实面板扣费逻辑 (支持固定值与百分比，绝不硬编码！)
        if (playerAction.skill && 技能仅含被动时光回溯(playerAction.skill, 动作条件上下文)) {
          playerAction.skill.消耗 = '无';
        }
        if (playerAction.skill && playerAction.skill.消耗 !== '无') {
          const costParts = splitSkillCostModes(playerAction.skill.消耗);
          const 消耗条件上下文 = {
            actor: attackerChar,
            caster: attackerChar,
            attacker: attackerChar,
            target: defenderChar,
            defender: defenderChar,
            action: playerAction,
            skill: playerAction.skill,
            combatData: getCurrentBattleContextSnapshot(),
            当前行动: String(playerAction?.action_type || playerAction?.type || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '').trim(),
          };
          const parsedCost = 应用消耗分摊(parseSkillCostForChar(
            { ...playerAction.skill, 消耗: costParts.upfront || '无' },
            attackerChar,
            消耗条件上下文,
          ), attackerChar, getCurrentBattleContextSnapshot());
          delete playerAction.skill.__targetForSupportCost;
          delete playerAction.skill.__辅助目标数量;
          delete playerAction.skill.__覆盖目标数量;
          delete playerAction.skill.__批量目标列表;
          delete playerAction.skill.__批量消耗模式;

          if (parsedCost.canCast) {
            deductParsedCostFromUnit(attackerChar, parsedCost);
            playerAction.__fusion_partner_units = Array.isArray(parsedCost.partnerCosts)
              ? parsedCost.partnerCosts.map(item => item.unit).filter(Boolean)
              : [];
            const partnerCostParts = [];
            (parsedCost.partnerCosts || []).forEach(partnerCost => {
              deductParsedCostFromUnit(partnerCost.unit, partnerCost);
              partnerCostParts.push(`${partnerCost.name}:${formatParsedCost(partnerCost)}`);
            });
            const 分摊消耗片段 = [];
            (parsedCost.分摊消耗列表 || []).forEach(分摊消耗 => {
              deductParsedCostFromUnit(分摊消耗.unit, 分摊消耗);
              分摊消耗片段.push(`${分摊消耗.name}:${formatParsedCost(分摊消耗)}`);
            });

            const sustainConfig = resolveActionSustainConfig(
              attackerChar,
              playerAction.action_type,
              playerAction.skill,
              getPrimaryStateName(playerAction.skill) || '无',
              battleContext,
            );
            const shouldRegisterSustain =
              costParts.hasSustain ||
              ['展开斗铠领域', '展开精神领域', '展开武魂领域', '点燃生命之火'].includes(playerAction.action_type) ||
              (sustainConfig?.related_condition || sustainConfig?.name || '').includes('真身');
            if (shouldRegisterSustain && sustainConfig) {
              if (costParts.hasSustain && costParts.sustain) sustainConfig.sustain_cost = costParts.sustain;
              sustainConfig.support_cost_scale = parsedCost.costScale || 1;
              registerSustainEffect(
                attackerChar,
                `${playerAction.action_type || 'skill'}:${playerAction.skill.name}`,
                sustainConfig,
              );
            }

            记账动作消耗到事件账本(battleContext, Number(battleContext?.回合 || 0), playerAction, attackerChar, parsedCost);
            log = `[战前消耗] 释放[${playerAction.skill.name}]，自身扣除 ${formatParsedCost(parsedCost)}${partnerCostParts.length ? `；共鸣分担 ${partnerCostParts.join('；')}` : ''}${分摊消耗片段.length ? `；消耗分摊 ${分摊消耗片段.join('；')}` : ''}。${shouldRegisterSustain ? '(已登记持续维持)' : ''}`;
          } else {
            playerAction.action_type = '施法失败';
            log = `[状态枯竭] ${parsedCost.failureReason || '自身状态不足'}，无法支撑[${playerAction.skill.name}]的启动消耗，施法失败！`;
            return log;
          }
        }

        if (playerAction.action_type === '召唤命令') {
          const 来源状态 = String(playerAction?.来源状态 || '').trim();
          const 召唤状态 = 来源状态 ? attackerChar?.状态效果?.[来源状态] : null;
          const 召唤物 = 召唤状态?.召唤物 && typeof 召唤状态.召唤物 === 'object' ? 召唤状态.召唤物 : null;
          if (召唤物 && String(召唤物.行动模式 || '').trim() === '自主行动') {
            if (!Array.isArray(attackerChar.召唤行动队列)) attackerChar.召唤行动队列 = [];
            attackerChar.召唤行动队列.push({
              来源状态,
              召唤物名称: String(playerAction?.召唤物名称 || 召唤物.召唤物名称 || '').trim(),
              召唤动作: String(playerAction?.召唤动作 || '').trim() || '自主行动',
              命令成本: String(召唤物.命令成本 || playerAction?.skill?.消耗 || '').trim(),
              命令前摇: Number(召唤物.命令前摇 || playerAction?.skill?.前摇 || 0),
            });
            log += ` [召唤命令] ${召唤物.召唤物名称 || '召唤物'}加入自主行动队列。`;
          }
        }

        // 💥 2. 纯机制类状态切换 (不涉及具体数值伤害，仅改变底层状态)
        if (playerAction.action_type === '吸血反哺') {
          let ratio = playerAction.heal_ratio > 0 ? playerAction.heal_ratio : 0.3;
          let spGain = Math.floor(stats.sp_max * ratio);
          let vitGain = Math.floor((stats.sta_max || stats.vit_max || 1) * ratio);
          设置战斗延迟效果资源值(attackerChar, 'sp', Number(stats.sp ?? stats.魂力 ?? 0) + spGain);
          设置战斗体力值(attackerChar, Number(stats.sta ?? stats.体力 ?? stats.vit ?? 0) + vitGain);
          log = `[机制反哺] 触发吸血/减耗机制，强制恢复状态！`;
        } else if (playerAction.action_type === '五行剥离') {
          if (!hasBattleUnlockedAttributeSet(attackerChar, ['金', '木', '水', '火', '土'])) {
            playerAction.action_type = '法则失败';
            log = `[权限不足] 尚未集齐金木水火土五行调用权，无法发动五行剥离！`;
          } else if (!defenderChar) {
            playerAction.action_type = '法则失败';
            log = `[目标丢失] 当前没有可被五行剥离锁定的目标！`;
          } else {
            applyStateToCharacter(
              defenderChar,
              {
                状态名称: '五行剥离',
                特殊机制标识: '削弱/高阶衍生',
                持续回合: 2,
                面板修改比例: { str: 1.0, def: 0.75, agi: 0.92, sp_max: 1.0, vit_max: 1.0, men_max: 0.95 },
                计算层效果: {
                  ...createEmptyCombatEffectMap(),
                  reaction_penalty: 0.12,
                  lock_level: 1,
                },
              },
              '五行剥离',
              false,
            );
            log = `[五行剥离] 集齐五行后的另类调用发动，目标五行护持被强行剥离，结构与反应同步受压！`;
          }
        } else if (playerAction.action_type === '五行遁法') {
          const attackerBaseSp = Math.max(1, Number(attackerChar?.sp_max || attackerChar?.属性?.sp_max || 1));
          const defenderBaseSp = Math.max(1, Number(defenderChar?.sp_max || defenderChar?.属性?.sp_max || 1));
          const armorReady = defenderChar?.装备?.斗铠?.装备状态 === '已装备';
          const mechReady =
            defenderChar?.装备?.机甲?.装备状态 === '已装备' && defenderChar?.装备?.机甲?.状态 !== '重创';
          const canDisableMetalEquip = defenderBaseSp <= attackerBaseSp * 1.1;
          if (!hasBattleUnlockedAttributeSet(attackerChar, ['金', '木', '水', '火', '土'])) {
            playerAction.action_type = '法则失败';
            log = `[权限不足] 尚未集齐金木水火土五行调用权，无法发动五行遁法！`;
          } else {
            applyStateToCharacter(
              attackerChar,
              {
                状态名称: '五行遁法',
                特殊机制标识: '增益/高阶衍生',
                持续回合: 2,
                面板修改比例: { str: 1.0, def: 1.0, agi: 1.12, sp_max: 1.0, vit_max: 1.0, men_max: 1.05 },
                计算层效果: {
                  ...createEmptyCombatEffectMap(),
                  dodge_bonus: 0.18,
                  reaction_bonus: 0.1,
                  attacker_speed_bonus: 0.08,
                  cast_speed_bonus: 0.08,
                },
              },
              '五行遁法',
              true,
            );
            const disabledEquipments = [];
            if (canDisableMetalEquip && armorReady) {
              应用战斗装备变化(defenderChar, () => {
                defenderChar.装备.斗铠.装备状态 = '失效';
              });
              disabledEquipments.push('斗铠');
            }
            if (canDisableMetalEquip && mechReady) {
              应用战斗装备变化(defenderChar, () => {
                defenderChar.装备.机甲.装备状态 = '失效';
              });
              disabledEquipments.push('机甲');
            }
            log = `[五行遁法] 集齐五行后的另类调用发动，自身遁入五行流转，身法、规避与反应显著提升！`;
            if (disabledEquipments.length)
              log += ` [金属失效] 对方基础魂力未超过自身 10%，${disabledEquipments.join('、')}被五行遁法压制而暂时失效！`;
            else if ((armorReady || mechReady) && !canDisableMetalEquip)
              log += ` [装备稳固] 对方基础魂力超出压制阈值，金属装备未被撼动。`;
          }
        } else if (playerAction.action_type === '点燃生命之火') {
          if (attackerChar.血脉之力) {
            attackerChar.血脉之力.生命之火 = true;
            const sustainConfig = resolveActionSustainConfig(
              attackerChar,
              playerAction.action_type,
              playerAction.skill,
              '',
            );
            if (sustainConfig)
              registerSustainEffect(
                attackerChar,
                `life_fire:${playerAction.skill?.name || '点燃生命之火'}`,
                sustainConfig,
              );
            log += ` [底蕴透支] 强行点燃生命之火！气血如烘炉般燃烧，全属性临时翻倍！(警告：战后熄灭将面临毁灭性反噬)`;
          } else {
            playerAction.action_type = '法则失败';
            log = `[状态枯竭] 未觉醒相关血脉，无法点燃生命之火！`;
          }
        }
        // 领域状态记录，扣费已在上面通用逻辑完成。
        else if (playerAction.action_type === '展开斗铠领域') {
          status.当前领域 =
            attackerChar.装备?.斗铠?.等级 >= 4 ? '【四字斗铠领域】全开(未定)' : '【三字斗铠领域】全开(未定)';
          const sustainConfig = resolveActionSustainConfig(
            attackerChar,
            playerAction.action_type,
            playerAction.skill,
            '',
          );
          if (sustainConfig) registerSustainEffect(attackerChar, `domain:${playerAction.action_type}`, sustainConfig);
          log += ` [领域降临] 斗铠法则主场展开！`;
        } else if (playerAction.action_type === '展开精神领域') {
          status.当前领域 = '【精神领域】全开';
          const sustainConfig = resolveActionSustainConfig(
            attackerChar,
            playerAction.action_type,
            playerAction.skill,
            '',
          );
          if (sustainConfig) registerSustainEffect(attackerChar, `domain:${playerAction.action_type}`, sustainConfig);
          log += ` [领域降临] 精神法则主场展开！`;
        } else if (playerAction.action_type === '展开武魂领域') {
          status.当前领域 = '【武魂领域】全开';
          const sustainConfig = resolveActionSustainConfig(
            attackerChar,
            playerAction.action_type,
            playerAction.skill,
            '',
          );
          if (sustainConfig) registerSustainEffect(attackerChar, `domain:${playerAction.action_type}`, sustainConfig);
          log += ` [领域降临] 武魂本源领域展开！`;
        }

        return log;
      }

      function 清理动作运行态字段(playerAction) {
        if (!playerAction || typeof playerAction !== 'object') return;
        delete playerAction.__批量目标列表;
        delete playerAction.__批量目标名称列表;
        delete playerAction.__批量消耗模式;
        delete playerAction.__覆盖目标数量;
        delete playerAction.__连放来源;
        delete playerAction.__技能事件链;
        if (playerAction.skill && typeof playerAction.skill === 'object') {
          delete playerAction.skill.__targetForSupportCost;
          delete playerAction.skill.__辅助目标数量;
          delete playerAction.skill.__覆盖目标数量;
          delete playerAction.skill.__批量目标列表;
          delete playerAction.skill.__批量消耗模式;
          delete playerAction.skill.__技能事件链;
        }
        if (Array.isArray(playerAction.pre_actions)) playerAction.pre_actions.forEach(副动作 => 清理动作运行态字段(副动作));
      }

      function 清理战斗阶段触发锁(...单位列表) {
        单位列表.filter(Boolean).forEach(单位 => {
          delete 单位.__本阶段已触发免死;
          delete 单位.__本阶段已触发复活;
        });
      }

      function 动作是否仅前置挂载(action = {}) {
        const 动作类型 = String(action?.action_type || action?.type || '').trim();
        return 动作类型 === '穿戴装备' || /领域/.test(动作类型) || 动作类型 === '点燃生命之火';
      }

      function 执行连放前置动作结算(攻击方, 默认目标, action, combatData = {}) {
        if (!action?.skill || action.action_type === '施法失败' || 动作是否仅前置挂载(action)) {
          return { log: '', extraPatchOps: [] };
        }
        const targetContext = resolveSkillTargetContext(action.skill, 攻击方, 默认目标, combatData, getPrimaryDamageEffect(action.skill));
        const targetsFriendly = ['自身', '友方单体', '友方群体'].includes(targetContext.targetKind);
        const primaryTarget = targetContext.primaryTarget || (targetsFriendly ? 攻击方 : 默认目标);
        if (!primaryTarget) return { log: '', extraPatchOps: [] };
        const reactionAction = targetsFriendly
          ? { type: '配合', log: `${primaryTarget.name || '目标'}接受了${攻击方.name || '施术者'}的连放辅助。`, skill: null, def_mult: 1.0 }
          : determineNpcAction(combatData, action, calculateReactionRatio(攻击方, primaryTarget, action, combatData));
        const settleResult = executeClash(action, reactionAction, combatData);
        const damagePackage = applyResolvedDamagePackage(攻击方, action, settleResult, {
          primaryTarget,
          combatData,
        });
        const 防反日志 = targetsFriendly
          ? ''
          : 执行行为防反结算(攻击方, primaryTarget, action, reactionAction, settleResult, damagePackage, combatData);
        const fusionAftermathLog = applyFusionActionAftermath(攻击方, action, combatData);
        const 伤害日志 = damagePackage.log ? ` ${damagePackage.log}` : '';
        const 防反文本 = 防反日志 ? ` ${防反日志}` : '';
        const 融合文本 = fusionAftermathLog ? ` ${fusionAftermathLog}` : '';
        const log = `[连放结算] ${攻击方.name || '施术者'}追加[${action.skill?.name || action.action_type}]指向[${primaryTarget.name || '目标'}]。 ${reactionAction.log || ''} ${settleResult.desc || ''}${伤害日志}${防反文本}${融合文本}`;
        return {
          log,
          extraPatchOps: Array.isArray(settleResult?.extraPatchOps) ? settleResult.extraPatchOps : [],
        };
      }

      function settleBattle(attackerChar, defenderChar, battleOutcome = {}, options = {}) {
        let log = '';
        let extraPatchOps = [];
        let attackerStats = attackerChar.属性 || attackerChar;
        let defenderStats = defenderChar.属性 || defenderChar;
        let defenderName = defenderChar.name || '敌人';

        // 读取战斗类型
        let combatData = options.combatData || window.BattleUIBridge?.getMVU('world.战斗');
        let combatType = combatData.战斗类型 || '突发遭遇';
        const preferredPlayerName = String(window.BattleUIBridge?.getMVU('sys.玩家名') || '').trim();
        const attackerName = String(
          attackerChar?.name || combatData?.参战者?.team_player?.[0]?.name || preferredPlayerName || '',
        ).trim();
        let inventory = (attackerName ? window.BattleUIBridge?.getMVU(`char.${attackerName}.背包`) : null) || {};
        const currentTick = Number(window.BattleUIBridge?.getMVU('world.时间.tick') || 0);
        const defenderFlags = getCombatTargetSpeciesFlags(defenderChar);
        const isBeast = defenderFlags.isBeast;
        const isAbyss = defenderFlags.isAbyss;
        const isBeastOrAbyss = defenderFlags.isBeastOrAbyss;
        const validDefenderName = !!(defenderName && defenderName !== '敌人' && defenderName !== '未知');
        const outcome =
          battleOutcome && typeof battleOutcome === 'object'
            ? battleOutcome
            : buildFrontEndBattleOutcome(attackerChar, defenderChar, {
                unresolvedReason: options.unresolvedReason || '',
                formalEncounter: true,
              });
        const isVictoryOutcome = outcome.isVictory === true;
        const isDrawOutcome = outcome.isDraw === true;
        const isUnresolvedOutcome = outcome.isUnresolved === true;
        const 图鉴模板 = isBeastOrAbyss && validDefenderName ? 解析图鉴键与模板(defenderChar) : null;
        const 战斗历史对手名 = 图鉴模板?.key || defenderName;
        const shouldRecordCombatHistory = outcome.formalEncounter !== false && validDefenderName && !!战斗历史对手名;
        let historyCountBefore = 0;
        if (shouldRecordCombatHistory) {
          const existingHistory = attackerChar?.战斗历史?.[战斗历史对手名];
          historyCountBefore = Math.max(0, Number(existingHistory?.次数 || 0));
        }

        // --- 触发世界战斗图鉴录入 ---
        let bestiary = window.BattleUIBridge?.getMVU('world.图鉴') || {};
        if (isBeastOrAbyss && validDefenderName) {
          if (图鉴模板?.key) {
            let monsterEntry = bestiary[图鉴模板.key];
            const 图鉴路径前缀 = `/world/图鉴/${escapeJsonPointerSegment(图鉴模板.key)}`;
            if (!monsterEntry) {
              extraPatchOps.push({
                op: 'add',
                path: 图鉴路径前缀,
                value: 图鉴模板.value,
              });
            } else {
              ['类型', '名称', '具体物种', '标准物种', '物种品质', '年限档', '年限下限', '年限上限', '标准种族', '常见级别', '对标等级', '常见系别', '标准技能']
                .forEach(字段 => {
                  if (图鉴模板.value[字段] === undefined) return;
                  extraPatchOps.push({
                    op: 'add',
                    path: `${图鉴路径前缀}/${escapeJsonPointerSegment(字段)}`,
                    value: 图鉴模板.value[字段],
                  });
                });
            }
          } else if (!bestiary[defenderName]) {
            extraPatchOps.push({
              op: 'add',
              path: `/world/图鉴/${escapeJsonPointerSegment(defenderName)}`,
              value: {
                类型: isBeast ? '魂兽' : isAbyss ? '深渊' : '怪物',
                名称: defenderName,
                标准技能: clonePersistedCombatValue(defenderChar?.自创魂技 || {}),
              },
            });
          }
        }

        if (combatType === '升灵台虚拟战斗') {
          if (isVictoryOutcome) {
            const ticket = combatData.ascension_ticket || '初级升灵台门票';
            const ticketRange = getAscensionTicketAgeRange(ticket);
            const riotPeriod = isAscensionRiotTicket(ticket);
            const defenderIsSoulMaster = isCombatUnitSoulMaster(defenderChar);
            let killedAge = Math.max(
              ticketRange.min,
              Math.min(ticketRange.max, Number(combatData.killed_age || defenderStats.age || ticketRange.min || 100)),
            );
            const 结算队伍 = 获取升灵台结算队伍(combatData, attackerName);
            const 参与人数 = Math.max(1, 结算队伍.length);
            const ticketPercent = ticketRange.max > ticketRange.min
              ? (killedAge - ticketRange.min) / (ticketRange.max - ticketRange.min)
              : 0;
            const rewardMultiplier = defenderIsSoulMaster
              ? (riotPeriod ? getAscensionRiotRewardMultiplier(ticketPercent) : 0)
              : 1;
            const 非暴动期魂师战 = defenderIsSoulMaster && !riotPeriod;
            let 可结算成员数 = 0;
            let 无魂环成员数 = 0;
            let 有效增益样本 = 0;
            结算队伍.forEach(成员 => {
              const 魂环条目列表 = [];
              取角色武魂条目_战斗(成员.角色数据).forEach(([武魂键, 武魂数据]) => {
                取武魂魂灵条目_战斗(武魂数据).forEach(([魂灵键, 魂灵数据]) => {
                  取魂灵魂环条目_战斗(魂灵数据).forEach(([魂环键, 魂环数据]) => 魂环条目列表.push({ 路径: [武魂键, 魂灵键, 魂环键], 魂环键, 魂环数据 }));
                });
                取武魂直接魂环条目_战斗(武魂数据).forEach(([魂环键, 魂环数据]) => 魂环条目列表.push({ 路径: [武魂键, 魂环键], 魂环键, 魂环数据 }));
              });
              if (!魂环条目列表.length) {
                无魂环成员数 += 1;
                return;
              }
              可结算成员数 += 1;
              let 成员增益 = 非暴动期魂师战 ? 0 : Math.floor((killedAge / 参与人数 / 魂环条目列表.length) * rewardMultiplier);
              if (!Number.isFinite(成员增益) || 成员增益 < 0) 成员增益 = 0;
              有效增益样本 += 成员增益;
              魂环条目列表.forEach(({ 路径, 魂环数据 }) => {
                const 旧年限原值 = Number(魂环数据?.年限);
                const 旧年限 = Number.isFinite(旧年限原值) && 旧年限原值 > 0 ? Math.floor(旧年限原值) : 10;
                extraPatchOps.push({
                  op: 'replace',
                  path: `/char/${escapeJsonPointerSegment(成员.角色键)}/${路径.map(片段 => escapeJsonPointerSegment(片段)).join('/')}/年限`,
                  value: 旧年限 + 成员增益,
                });
              });
            });
            const 增益摘要 = 可结算成员数 > 0
              ? `可结算成员${可结算成员数}人，单成员单魂环平均增益约 ${Math.floor(有效增益样本 / 可结算成员数)} 年`
              : '全队暂无可结算魂环';
            const 无魂环摘要 = 无魂环成员数 > 0 ? `，${无魂环成员数}名成员无魂环未吸收` : '';
            if (非暴动期魂师战) {
              log = `[升灵台结算] 队伍击败了魂师，但当前非暴动期，全队不结算升灵奖励。`;
            } else if (defenderIsSoulMaster) {
              log = `[升灵台结算] 队伍击败暴动期魂师！对手处于档位区间${Math.round(ticketPercent * 100)}%，倍率约 ${rewardMultiplier.toFixed(2)} 倍；${增益摘要}${无魂环摘要}。`;
            } else {
              log = `[升灵台结算] 虚拟魂兽被击溃，升灵能量按队伍(${参与人数}人)平分；${增益摘要}${无魂环摘要}。`;
            }
            const 升灵台退出补丁 = 构建角色位置补丁(attackerName, 升灵台退出地点);
            if (升灵台退出补丁) extraPatchOps.push(升灵台退出补丁);
            combatData.试炼状态 = '';
            extraPatchOps.push(构建试炼状态补丁(''));
          } else if (outcome.isDefeat === true || isDrawOutcome) {
            log = `[升灵台保护] 玩家受到致命创伤，升灵台保护机制触发！一道接引光芒落下，强制将其弹出升灵台。(虚拟战败，无实质损伤，但终止了本次历练)`;
            设置战斗血量值(attackerChar, 1);
            const 升灵台退出补丁 = 构建角色位置补丁(attackerName, 升灵台退出地点);
            if (升灵台退出补丁) extraPatchOps.push(升灵台退出补丁);
            combatData.试炼状态 = '';
            extraPatchOps.push(构建试炼状态补丁(''));
          } else {
            log = `[升灵台仲裁] 本次交锋暂未分出胜负，升灵台能量回路仍在持续运转。`;
          }
          return { log, extraPatchOps };
        }

        if (combatType === '魂灵塔冲塔') {
          const rosterCheck = validateSoulTowerCombatRoster(combatData);
          if (!rosterCheck.ok) {
            log = `[魂灵塔资格驳回] ${rosterCheck.message}`;
            return { log, extraPatchOps };
          }
          let floor = combatData.floor || 1;
          const gateMeta = getSoulTowerGateMeta(floor);
          if (isVictoryOutcome) {
            log = `🏆[冲塔成功] ${gateMeta.gateLabel}${gateMeta.isGateBoss ? '关底' : ''}镇守第 ${floor} 层的 ${defenderName} 被彻底击溃！当前守塔魂灵已锁定为可选购买目标，请决定是结束冲塔并保留资格，还是继续冲击下一层。`;
          } else if (outcome.isDefeat === true || isDrawOutcome) {
            log = `💀[冲塔失败] 玩家遭到 ${defenderName} 重创，魂灵塔阵法排斥之力发动，将其强行传送出塔外！(请 AI 描写重伤弹出塔外的虚弱状态)`;
            设置战斗血量值(attackerChar, 1);
            const 魂灵塔退出补丁 = 构建角色位置补丁(attackerName, 魂灵塔退出地点);
            if (魂灵塔退出补丁) extraPatchOps.push(魂灵塔退出补丁);
            combatData.试炼状态 = '';
            extraPatchOps.push(构建试炼状态补丁(''));
          } else {
            log = `[冲塔仲裁] 本次交锋尚未形成通关结果，魂灵塔考核仍在继续。`;
          }
          return { log, extraPatchOps };
        }

        if (isVictoryOutcome) {
          let lvDiff = defenderStats.lv - attackerStats.lv;

          if (lvDiff <= -5) {
            log = `[实战结算] 击败了对手，但因等级碾压(等级差≤-5)，毫无实战感悟，收益为 0。`;
          } else {
            let myTalentScore =
              { 绝世妖孽: 5, 顶级天才: 4, 天才: 3, 优秀: 2, 正常: 1, 劣等: 0 }[attackerStats.talent_tier] || 1;
            let targetTalentScore = 1;

            if (isBeastOrAbyss) {
              if (defenderStats.age >= 100000) targetTalentScore = 5;
              else if (defenderStats.age >= 10000) targetTalentScore = 3;
              else if (defenderStats.age >= 1000) targetTalentScore = 2;
              else targetTalentScore = 0;
            } else {
              targetTalentScore =
                { 绝世妖孽: 5, 顶级天才: 4, 天才: 3, 优秀: 2, 正常: 1, 劣等: 0 }[defenderStats.talent_tier] || 1;
            }
            let talentMult = Math.max(0.1, 1 + (targetTalentScore - myTalentScore) * 0.2);

            let lvMult = 1.0;
            if (lvDiff >= 3) lvMult = 1.5 + (lvDiff - 3) * 0.2;

            let historyMult = 1.0;
            if (!isBeastOrAbyss) {
              if (historyCountBefore === 1) historyMult = 0.5;
              else if (historyCountBefore === 2) historyMult = 0.1;
              else if (historyCountBefore >= 3) historyMult = 0;
            }

            let finalMult = talentMult * lvMult * historyMult;

            if (finalMult <= 0) {
              log = `[实战结算] 击败了对手，但因多次交手已无新感悟，收益递减为 0。`;
            } else {
              let baseGain = 10;
              let finalGain = Math.floor(baseGain * finalMult);

              if (finalGain > 0) {
                if (!attackerStats.训练加成)
                  attackerStats.训练加成 = { str: 0, def: 0, agi: 0, vit_max: 0, men_max: 0, sp_max: 0 };
                attackerStats.训练加成.str += finalGain;
                attackerStats.训练加成.def += finalGain;
                attackerStats.训练加成.agi += finalGain;
                attackerStats.训练加成.vit_max += finalGain;
                attackerStats.训练加成.men_max += Math.floor(finalGain * 0.5);
                log = `[实战结算] 战斗胜利！(等级差:${lvDiff}, 天赋乘区:${talentMult.toFixed(1)}, 递减:${historyMult}) 获得 ${finalGain} 点实战六维成长！`;
              } else {
                log = `[实战结算] 战斗胜利，但综合评估后收益微乎其微。`;
              }
            }
          }
          if (combatType === '擂台切磋') {
            log += ` 🏆[擂台结算] 堂堂正正地赢得了比赛！(请 AI 描写观众的欢呼与对手的反应)`;
          } else if (isBeastOrAbyss) {
            if (isAbyss) {
              log += outcome.isLethalCandidate
                ? ` [深渊濒死] 深渊生物已被打入致命区，但正式死亡与战功结算仍待后续裁断。`
                : ` [深渊压制] 深渊生物已失去继续作战能力，后续生死由剧情与 AI 裁断。`;
            } else {
              log += outcome.isLethalCandidate
                ? ` [魂兽濒死] 魂兽已被打入濒死边缘，是否正式死亡与是否形成猎魂结算仍待后续裁断。`
                : ` [魂兽压制] 魂兽已被重创压制，是否死亡与是否形成猎魂结算仍待后续裁断。`;
            }
          } else {
            log += outcome.isLethalCandidate
              ? ` [致命压制] 敌人已被打入濒死边缘，生死仍待后续裁断。`
              : ` [战果确认] 敌人已被彻底制服，暂未触发致死结算。`;
          }
        } else if (isDrawOutcome) {
          log = `[实战结算] 双方均已失去继续作战能力，战局以两败俱伤暂告段落。`;
        } else if (isUnresolvedOutcome) {
          if (options.unresolvedReason === 'single_round_limit') {
            log = `[实战结算] 单回合仲裁结束，当前尚未分出胜负，双方仍在激烈对峙。`;
          } else if (options.unresolvedReason === 'simulation_stopped') {
            log = `[实战结算] 暗箱续推暂告一段落，当前仍未决出胜负，战局进入新的对峙阶段。`;
          } else if (outcome.label) {
            log = `[实战结算] ${outcome.label}，本次交锋暂未形成最终战果。`;
          } else {
            log = `[实战结算] 当前仍未决出胜负，战局进入新的对峙阶段。`;
          }
        } else {
          log = `[实战结算] 自身失去继续作战能力，本次交锋未能取胜。`;
        }

        if (shouldRecordCombatHistory) {
          if (!attackerChar.战斗历史 || typeof attackerChar.战斗历史 !== 'object') attackerChar.战斗历史 = {};
          if (!attackerChar.战斗历史[战斗历史对手名] || typeof attackerChar.战斗历史[战斗历史对手名] !== 'object') {
            attackerChar.战斗历史[战斗历史对手名] = { 次数: 0, 胜: 0, 负: 0, 平: 0, 最近结果: '无', 最近tick: 0 };
          }
          attackerChar.战斗历史[战斗历史对手名].次数 = Math.max(
            0,
            Number(attackerChar.战斗历史[战斗历史对手名].次数 || 0),
          ) + 1;
          const 最近结果 = isVictoryOutcome ? '胜' : outcome.isDefeat === true ? '负' : '平';
          attackerChar.战斗历史[战斗历史对手名][最近结果] = Math.max(
            0,
            Number(attackerChar.战斗历史[战斗历史对手名][最近结果] || 0),
          ) + 1;
          attackerChar.战斗历史[战斗历史对手名].最近结果 = 最近结果;
          attackerChar.战斗历史[战斗历史对手名].最近tick = currentTick;
        }

        return { log, extraPatchOps };
      }

      function ensureBattleEquipmentSlot(角色对象, 装备目标 = 'armor') {
        if (!角色对象 || typeof 角色对象 !== 'object') return null;
        if (!角色对象.装备 || typeof 角色对象.装备 !== 'object') 角色对象.装备 = {};
        const 装备键 = 装备目标 === 'armor' ? '斗铠' : '机甲';
        if (!角色对象.装备[装备键] || typeof 角色对象.装备[装备键] !== 'object') {
          角色对象.装备[装备键] =
            装备键 === '斗铠'
              ? { 等级: 0, 名称: '无', 领域: '无', 材质: '无', 装备状态: '未装备', parts: {} }
              : { 等级: '无', 型号: '无', 材质: '无', 状态: '完好', 装备状态: '未装备', 武装: '无', 品质系数: 1.0 };
        }
        const 装备槽 = 角色对象.装备[装备键];
        if (装备键 === '斗铠') {
          if (!装备槽.parts || typeof 装备槽.parts !== 'object') 装备槽.parts = {};
          if (!('装备状态' in 装备槽)) 装备槽.装备状态 = '未装备';
        } else {
          if (!('状态' in 装备槽)) 装备槽.状态 = '完好';
          if (!('装备状态' in 装备槽)) 装备槽.装备状态 = '未装备';
          if (!('品质系数' in 装备槽)) 装备槽.品质系数 = 1.0;
        }
        return 装备槽;
      }

      function 创建空战斗装备加成() {
        return { 力量: 0, 防御: 0, 敏捷: 0, 体力上限: 0, 精神力上限: 0, 魂力上限: 0 };
      }

      function 读取战斗装备加成快照(单位) {
        const 计算装备加成 = root.__LWCS_CALC_ACTIVE_EQUIPMENT_BONUS__ || window.__LWCS_CALC_ACTIVE_EQUIPMENT_BONUS__;
        if (typeof 计算装备加成 !== 'function') throw new Error('战斗装备加成接口未加载');
        const 原始加成 = 计算装备加成(单位 || {});
        const 加成 = 创建空战斗装备加成();
        Object.keys(加成).forEach(字段 => {
          加成[字段] = Math.floor(Number(原始加成?.[字段] || 0));
        });
        return 加成;
      }

      function 应用战斗装备加成差值(单位, 旧加成) {
        if (!单位 || typeof 单位 !== 'object') return;
        const 新加成 = 读取战斗装备加成快照(单位);
        const 属性字段 = [
          ['力量', 'str'],
          ['防御', 'def'],
          ['敏捷', 'agi'],
          ['魂力上限', 'sp_max'],
          ['精神力上限', 'men_max'],
          ['体力上限', 'vit_max'],
        ];
        if (!单位.属性 || typeof 单位.属性 !== 'object') 单位.属性 = {};
        let 存在变化 = false;
        属性字段.forEach(([中文字段, 战斗字段]) => {
          const 差值 = Math.floor(Number(新加成[中文字段] || 0) - Number(旧加成?.[中文字段] || 0));
          if (差值 === 0) return;
          存在变化 = true;
          const 当前值 = Number(单位.属性[中文字段] ?? 单位[战斗字段] ?? 0);
          单位.属性[中文字段] = Math.max(中文字段.endsWith('上限') ? 1 : 0, Math.floor(当前值 + 差值));
          单位[战斗字段] = 单位.属性[中文字段];
          if (中文字段 === '体力上限') 单位.sta_max = 单位.属性[中文字段];
        });
        if (!存在变化) return;
        if (Number.isFinite(Number(单位.属性.魂力)) && Number.isFinite(Number(单位.属性.魂力上限)))
          单位.属性.魂力 = Math.max(0, Math.min(Number(单位.属性.魂力上限), Number(单位.属性.魂力 || 0)));
        if (Number.isFinite(Number(单位.属性.精神力)) && Number.isFinite(Number(单位.属性.精神力上限)))
          单位.属性.精神力 = Math.max(0, Math.min(Number(单位.属性.精神力上限), Number(单位.属性.精神力 || 0)));
        if (Number.isFinite(Number(单位.属性.体力)) && Number.isFinite(Number(单位.属性.体力上限)))
          单位.属性.体力 = Math.max(0, Math.min(Number(单位.属性.体力上限), Number(单位.属性.体力 || 0)));
        单位.属性.HP上限 = Math.max(1, Number(单位.属性.体力上限 || 单位.属性.HP上限 || 1));
        单位.属性.HP = Math.max(0, Math.min(Number(单位.属性.HP上限), Number(单位.属性.HP || 单位.hp || 0)));
        bindCombatRuntimeAliases(单位, 单位.属性);
        bindCombatHpAliasField(单位, 单位.属性);
        单位.final = buildCombatFinalStats(单位);
      }

      function 应用战斗装备变化(单位, 变更函数) {
        const 旧加成 = 读取战斗装备加成快照(单位);
        if (typeof 变更函数 === 'function') 变更函数();
        应用战斗装备加成差值(单位, 旧加成);
      }

      function applyArmorDamage(defender) {
        let log = '';
        const 机甲槽 = ensureBattleEquipmentSlot(defender, 'mech');
        const 斗铠槽 = ensureBattleEquipmentSlot(defender, 'armor');
        if (机甲槽?.等级 !== '无' && 机甲槽?.状态 !== '重创') {
          if (机甲槽.状态 === '完好') {
            应用战斗装备变化(defender, () => {
              机甲槽.状态 = '受损';
              机甲槽.品质系数 = 0.5;
            });
            log = `[战损] 敌方最外层的机甲装甲大面积凹陷，状态降级为【受损】！`;
          } else if (机甲槽.状态 === '受损') {
            应用战斗装备变化(defender, () => {
              机甲槽.状态 = '重创';
              机甲槽.品质系数 = 0;
            });
            log = `[战损] 敌方机甲核心法阵爆裂，状态降级为【重创】，彻底瘫痪！`;
          }
          return log;
        }
        if (斗铠槽?.装备状态 === '已装备') {
          let parts = Object.keys(斗铠槽.parts || {});
          let intactParts = parts.filter(p => 斗铠槽.parts?.[p]?.状态 === '完好');
          if (intactParts.length > 0) {
            let targetPart = intactParts[Math.floor(Math.random() * intactParts.length)];
            应用战斗装备变化(defender, () => {
              斗铠槽.parts[targetPart].状态 = '碎裂';
              斗铠槽.parts[targetPart].品质系数 = 0;
            });
            log = `[战损] 敌方贴身的斗铠【${targetPart}】承受不住透体的重击，轰然碎裂！`;
          }
        }
        return log;
      }

      function 扣除黄金瀑布触发体力(单位 = {}, 状态文本 = '') {
        if (!/黄金瀑布/.test(String(状态文本 || ''))) return { 可触发: true, 消耗量: 0 };
        const 消耗量 = Math.max(1, Math.floor(getCombatStaminaMaxValue(单位) * 0.1));
        if (getCombatStaminaValue(单位) < 消耗量) return { 可触发: false, 消耗量 };
        设置战斗体力值(单位, getCombatStaminaValue(单位) - 消耗量);
        return { 可触发: true, 消耗量 };
      }

      function 写入下次伤害增幅状态(单位 = {}, 增幅比例 = 0, 来源名 = '伤害吸收', 上限比例 = 0.5) {
        if (!单位 || typeof 单位 !== 'object') return 0;
        const 上限 = Math.max(0, Math.min(1, Number(上限比例 || 0.5)));
        const 增幅 = Math.max(0, Math.min(上限, Number(增幅比例 || 0)));
        if (!(增幅 > 0)) return 0;
        if (!单位.状态效果) 单位.状态效果 = {};
        const 状态名 = `${来源名 || '伤害吸收'}·下次伤害增幅`;
        const 旧状态 = 单位.状态效果[状态名];
        const 旧增幅 = Math.max(0, Number(旧状态?.战斗效果?.final_damage_mult || 1) - 1);
        const 新增幅 = Math.min(上限, 旧增幅 + 增幅);
        if (!(新增幅 > 旧增幅)) return 0;
        单位.状态效果[状态名] = {
          类型: 'buff',
          层数: 1,
          来源技能: 来源名 || '伤害吸收',
          描述: `由[${来源名 || '伤害吸收'}]蓄积，下次造成伤害时生效`,
          duration: 1,
          __一次性伤害增幅: true,
          面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
          战斗效果: { ...createEmptyCombatEffectMap(), final_damage_mult: 1 + 新增幅 },
        };
        if (单位.召唤键) 同步召唤单位镜像(单位);
        return 新增幅 - 旧增幅;
      }

      function 消耗一次性伤害增幅状态(单位 = {}) {
        if (!单位?.状态效果) return [];
        const 消耗列表 = [];
        Object.entries(单位.状态效果).forEach(([状态名, 状态]) => {
          if (状态?.__一次性伤害增幅 !== true) return;
          const 增幅 = Math.max(0, Number(状态?.战斗效果?.final_damage_mult || 1) - 1);
          delete 单位.状态效果[状态名];
          消耗列表.push({ 状态名, 增幅 });
        });
        if (消耗列表.length && 单位.召唤键) 同步召唤单位镜像(单位);
        return 消耗列表;
      }

      function triggerDeathSave(defender) {
        if (!defender?.状态效果) return null;
        if (defender.__本阶段已触发免死) return null;
        const candidate = Object.entries(defender.状态效果)
          .map(([key, cond]) => ({ key, cond, ce: cond?.战斗效果 || {} }))
          .filter(entry => Number(entry.ce.death_save_count || 0) > 0)
          .sort((a, b) => Number(b.ce.death_save_count || 0) - Number(a.ce.death_save_count || 0))[0];
        if (!candidate) return null;
        const 候选状态文本 = `${candidate.key} ${candidate.cond?.状态 || ''} ${candidate.cond?.状态名称 || ''} ${candidate.cond?.来源技能 || ''} ${candidate.cond?.描述 || ''}`;
        const 黄金瀑布扣费 = 扣除黄金瀑布触发体力(defender, 候选状态文本);
        if (!黄金瀑布扣费.可触发) return null;
        if (!candidate.cond.战斗效果) candidate.cond.战斗效果 = {};
        const nextCount = Math.max(0, Number(candidate.cond.战斗效果.death_save_count || 0) - 1);
        candidate.cond.战斗效果.death_save_count = nextCount;
        defender.__本阶段已触发免死 = true;
        const 状态文本 = 候选状态文本;
        if (/金龙不灭真身/.test(状态文本)) {
          设置战斗延迟效果资源值(defender, 'sp', 0);
          设置战斗体力值(defender, getCombatStaminaMaxValue(defender));
          设置战斗血量值(defender, getCombatHpMaxValue(defender));
          return `[濒死守护] ${defender.name || '目标'}触发[${candidate.key}]，抽尽魂力并以金龙不灭真身补满体力！剩余保护次数:${nextCount}`;
        }
        return `[濒死守护] ${defender.name || '目标'}触发[${candidate.key}]，强行保住最后一口气！剩余保护次数:${nextCount}`;
      }

      function formatBattleDayKeyFromTick(tickValue) {
        const safeTick = Math.max(0, Number(tickValue || 0));
        const totalMinutes = safeTick * 10;
        const days = Math.floor(totalMinutes / (24 * 60));
        const years = Math.floor(days / 360);
        const months = Math.floor((days % 360) / 30) + 1;
        const currentDay = (days % 30) + 1;
        return `${20000 + years}-${months}-${currentDay}`;
      }

      function getCombatUnitTierNumber(entity = {}) {
        const candidates = [
          entity?.final?.lv,
          entity?.lv,
          entity?.等级,
          entity?.对标等级,
          entity?.属性?.对标等级,
          entity?.属性?.等级,
        ];
        for (const candidate of candidates) {
          const parsed = Number(candidate);
          if (Number.isFinite(parsed)) return parsed;
        }
        const text = [
          entity?.境界,
          entity?.属性?.境界,
          entity?.战力对标,
          entity?.属性?.战力对标,
          entity?.称号,
          entity?.属性?.称号,
        ]
          .map(value => String(value || '').trim())
          .filter(Boolean)
          .join(' ');
        if (/准神/.test(text)) return 99.5;
        if (/神级|百级|真神|一级神|二级神|三级神/.test(text)) return 100;
        return 0;
      }

      function ensureEffectTriggerLedger(char) {
        if (!char || typeof char !== 'object') return {};
        if (!char.效果触发记录 || typeof char.效果触发记录 !== 'object') char.效果触发记录 = {};
        return char.效果触发记录;
      }

      function consumeDailyMechanismTrigger(char, mechanismKey = '', limitPerDay = 0, currentTick = null) {
        const safeLimit = Math.max(0, Number(limitPerDay || 0));
        if (!(safeLimit > 0)) return { allowed: true, remaining: Infinity, used: 0, dayKey: '' };
        const ledger = ensureEffectTriggerLedger(char);
        const dayKey = formatBattleDayKeyFromTick(
          currentTick == null ? Number(window.BattleUIBridge?.getMVU('world.时间.tick') || 0) : currentTick,
        );
        const key = String(mechanismKey || '未命名机制').trim() || '未命名机制';
        let entry = ledger[key];
        if (!entry || typeof entry !== 'object' || String(entry.日期 || '') !== dayKey) {
          entry = { 日期: dayKey, 次数: 0 };
          ledger[key] = entry;
        }
        if (Number(entry.次数 || 0) >= safeLimit) {
          return {
            allowed: false,
            remaining: 0,
            used: Number(entry.次数 || 0),
            dayKey,
          };
        }
        entry.次数 = Math.max(0, Number(entry.次数 || 0)) + 1;
        return {
          allowed: true,
          remaining: Math.max(0, safeLimit - Number(entry.次数 || 0)),
          used: Number(entry.次数 || 0),
          dayKey,
        };
      }

      function getCombatAlliesForUnit(combatData, unit) {
        if (!combatData || !unit) return [];
        const playerSide = (combatData?.参战者?.team_player || []).filter(Boolean);
        const enemySide = (combatData?.参战者?.team_enemy || []).filter(Boolean);
        if (playerSide.some(member => isCombatUnitIdentityMatch(member, unit?.name || unit))) {
          return playerSide.filter(member => member && !isCombatUnitIdentityMatch(member, unit?.name || unit));
        }
        if (enemySide.some(member => isCombatUnitIdentityMatch(member, unit?.name || unit))) {
          return enemySide.filter(member => member && !isCombatUnitIdentityMatch(member, unit?.name || unit));
        }
        return [];
      }

      function getCombatSideForUnit(combatData, unit) {
        if (!combatData || !unit) return [];
        const playerSide = (combatData?.参战者?.team_player || []).filter(Boolean);
        const enemySide = (combatData?.参战者?.team_enemy || []).filter(Boolean);
        if (playerSide.some(member => isCombatUnitIdentityMatch(member, unit?.name || unit))) return playerSide;
        if (enemySide.some(member => isCombatUnitIdentityMatch(member, unit?.name || unit))) return enemySide;
        return [];
      }

      function 读取伤害转移目标列表(转移对象, attacker, defender, combatData) {
        const 对象 = String(转移对象 || '攻击者').trim() || '攻击者';
        const 攻击方队伍 = getCombatSideForUnit(combatData, attacker).filter(unit => isCombatUnitAlive(unit));
        const 防守方队友 = getCombatAlliesForUnit(combatData, defender).filter(unit => isCombatUnitAlive(unit));
        if (对象 === '攻击方队友') return 攻击方队伍.filter(unit => !isCombatUnitIdentityMatch(unit, attacker?.name || attacker));
        if (对象 === '攻击方任意') return 攻击方队伍;
        if (对象 === '防守方队友') return 防守方队友;
        return attacker && isCombatUnitAlive(attacker) ? [attacker] : [];
      }

      function collectShieldConditionEntries(char) {
        if (!char?.状态效果) return [];
        const 读取护盾优先级 = (key = '', cond = {}) => {
          const 文本 = `${key} ${cond?.护盾类型 || ''} ${cond?.类型 || ''} ${cond?.描述 || ''}`;
          if (/规则|法则|无敌|金身/.test(文本)) return 1;
          if (/元素|属性|抗性/.test(文本)) return 2;
          if (/共享|分摊|队伍|团队/.test(文本)) return 4;
          return 3;
        };
        return Object.entries(char.状态效果)
          .map(([key, cond]) => ({ key, cond, shieldValue: Math.max(0, Number(cond?.shield_value || 0)) }))
          .filter(entry => entry.shieldValue > 0)
          .sort((a, b) => 读取护盾优先级(a.key, a.cond) - 读取护盾优先级(b.key, b.cond) || b.shieldValue - a.shieldValue);
      }

      function 斩除目标当前护盾(char, rawValue) {
        const 护盾列表 = collectShieldConditionEntries(char);
        if (!护盾列表.length) return 0;
        const 当前护盾总量 = 护盾列表.reduce((总量, entry) => 总量 + Math.max(0, Number(entry.shieldValue || 0)), 0);
        const 解析数值 = 读取战斗数值正负(rawValue);
        const 需要削减 = 读取战斗数值是否百分比(rawValue) || Math.abs(解析数值) <= 1
          ? Math.floor(当前护盾总量 * Math.abs(解析数值 || 1))
          : Math.floor(Math.abs(解析数值));
        let 剩余削减 = Math.max(0, Math.min(当前护盾总量, 需要削减));
        let 实际削减 = 0;
        护盾列表.forEach(entry => {
          if (剩余削减 <= 0) return;
          const 扣除 = Math.min(entry.shieldValue, 剩余削减);
          entry.cond.shield_value = Math.max(0, entry.shieldValue - 扣除);
          剩余削减 -= 扣除;
          实际削减 += 扣除;
          if (entry.cond.shield_value <= 0) delete char.状态效果[entry.key];
        });
        if (char.召唤键) 同步召唤单位镜像(char);
        return Math.max(0, Math.floor(实际削减));
      }

      function 读取当前护盾总量(char) {
        if (!char?.状态效果 || typeof char.状态效果 !== 'object') return 0;
        return Object.values(char.状态效果).reduce((总量, 状态) => 总量 + Math.max(0, Number(状态?.shield_value || 0)), 0);
      }

      function 计算护盾软上限(char) {
        const 生命上限 = Math.max(1, getCombatHpMaxValue(char));
        const 魂力上限 = Math.max(1, Number(char?.sp_max || char?.魂力上限 || 1));
        return Math.max(300, Math.floor(生命上限 * 1.2 + 魂力上限 * 0.35));
      }

      function 计算护盾实得值(char, shieldAmount) {
        const 新增护盾 = Math.max(0, Math.floor(Number(shieldAmount || 0)));
        if (!(新增护盾 > 0)) return 0;
        const 当前护盾 = 读取当前护盾总量(char);
        const 软上限 = 计算护盾软上限(char);
        const 常规空间 = Math.max(0, 软上限 - 当前护盾);
        const 常规获得 = Math.min(新增护盾, 常规空间);
        const 超额获得 = Math.max(0, 新增护盾 - 常规获得);
        return Math.max(0, Math.floor(常规获得 + 超额获得 * 0.35));
      }

    function triggerReviveEffect(targetChar, label = '目标') {
      if (!targetChar || typeof targetChar !== 'object') return null;
      if (targetChar.__本阶段已触发复活) return null;
      if (!targetChar.状态效果 || typeof targetChar.状态效果 !== 'object') targetChar.状态效果 = {};
      const 状态复活候选 = Object.entries(targetChar.状态效果)
        .map(([key, cond]) => ({ key, cond, ce: cond?.战斗效果 || {} }))
        .filter(entry => Number(entry.ce.revive_count || 0) > 0)
        .sort((a, b) => Number(b.ce.revive_count || 0) - Number(a.ce.revive_count || 0))[0];
      const 被动复活候选 = collectPassiveCombatSkills(targetChar, [targetChar])
        .map(skill => ({
          skill,
          effect: getSkillEffects(skill).find(effect =>
            String(effect?.原型 || '').trim() === '规则改写' &&
            String(effect?.规则 || '').trim() === '死亡转存活' &&
            String(effect?.目标 || '自身').trim() === '自身'
          ),
        }))
        .filter(entry => entry.effect)
        .find(entry => {
          const 限制 = entry.effect?.触发限制 && typeof entry.effect.触发限制 === 'object' && !Array.isArray(entry.effect.触发限制)
            ? entry.effect.触发限制
            : null;
          if (!限制) return true;
          const 周期 = String(限制.周期 || '').trim();
          const 次数 = Math.max(0, Math.floor(Number(限制.次数 || 0)));
          if (!(次数 > 0) || !['每日', '每战', '每回合', '每次满足'].includes(周期)) return true;
          const 技能名 = String(entry.skill?.name || entry.skill?.魂技名 || '被动技能').trim() || '被动技能';
          const 限制态 = targetChar.__技能限制运行态 ||= {};
          const 技能限制态 = 限制态[`被动:${技能名}:死亡转存活`] ||= { 已用次数: 0 };
          return Number(技能限制态.已用次数 || 0) < 次数;
        });
      if (!状态复活候选 && !被动复活候选) return null;
      if (状态复活候选) {
        if (战斗机制抹消命中(targetChar, '最终结果', { 原型: '规则防御', 规则: '复活' }, { 用途: '封锁' })) {
          return `[复活受阻] ${label}的复活机制已被机制抹消封锁，无法触发！`;
        }
        if (!状态复活候选.cond.战斗效果) 状态复活候选.cond.战斗效果 = {};
        const nextCount = Math.max(0, Number(状态复活候选.cond.战斗效果.revive_count || 0) - 1);
        状态复活候选.cond.战斗效果.revive_count = nextCount;
        const healRatio = Math.max(0.05, Number(状态复活候选.cond.战斗效果.revive_heal_ratio || 0.25));
        const maxVit = getCombatHpMaxValue(targetChar);
        const restoreAmount = Math.max(1, Math.floor(maxVit * healRatio));
        设置战斗血量值(targetChar, Math.min(maxVit, Math.max(restoreAmount, getCombatHpValue(targetChar) + restoreAmount)));
        targetChar.__本阶段已触发复活 = true;
        return `[复活触发] ${label}借[${状态复活候选.key}]重燃战意，恢复 ${restoreAmount} 点HP！剩余复活次数:${nextCount}`;
      }
      if (战斗机制抹消命中(targetChar, '最终结果', { 原型: '规则改写', 规则: '死亡转存活' }, { 用途: '封锁' })) {
        return `[复活受阻] ${label}的死亡转存活规则已被机制抹消封锁，无法触发！`;
      }
      const 复活来源 = 被动复活候选?.effect || {};
      if (被动复活候选?.skill) {
        const 限制 = 复活来源?.触发限制 && typeof 复活来源.触发限制 === 'object' && !Array.isArray(复活来源.触发限制)
          ? 复活来源.触发限制
          : null;
        const 次数 = Math.max(0, Math.floor(Number(限制?.次数 || 0)));
        if (次数 > 0) {
          const 技能名 = String(被动复活候选.skill?.name || 被动复活候选.skill?.魂技名 || '被动技能').trim() || '被动技能';
          const 限制态 = targetChar.__技能限制运行态 ||= {};
          const 技能限制态 = 限制态[`被动:${技能名}:死亡转存活`] ||= { 已用次数: 0 };
          技能限制态.已用次数 = Math.max(0, Number(技能限制态.已用次数 || 0)) + 1;
        }
      }
      const healRatio = Math.max(0.05, Math.abs(Number(读取战斗数值正负(复活来源?.数值 ?? 复活来源?.强度 ?? '+25%')) || 0.25));
      const maxVit = getCombatHpMaxValue(targetChar);
      const restoreAmount = Math.max(1, Math.floor(maxVit * Math.min(1, healRatio)));
      设置战斗血量值(targetChar, Math.min(maxVit, Math.max(restoreAmount, getCombatHpValue(targetChar) + restoreAmount)));
      targetChar.__本阶段已触发复活 = true;
      const 来源名 = 被动复活候选?.skill?.name || 被动复活候选?.skill?.魂技名 || '死亡转存活';
      return `[复活触发] ${label}触发[${来源名}]，按死亡转存活规则恢复 ${restoreAmount} 点HP！`;
    }

      function applyShieldToCharacter(targetChar, shieldAmount, duration = 1, sourceName = '护盾', options = {}) {
        const amount = 计算护盾实得值(targetChar, shieldAmount);
        if (!targetChar || amount <= 0) return 0;
        if (options?.已检查机制抹消 !== true && 战斗机制抹消命中(targetChar, '最终结果', { 原型: '护盾变化' }, { 用途: '封锁' })) return 0;
        if (!targetChar.状态效果) targetChar.状态效果 = {};
        const stateName = /护盾|屏障|结界/.test(String(sourceName || ''))
          ? String(sourceName || '护盾')
          : `${sourceName || '护盾'}护盾`;
        const existing = targetChar.状态效果[stateName];
        if (existing) {
          existing.duration = Math.max(Number(existing.duration || 0), Number(duration || 0));
          existing.shield_value = Math.max(0, Number(existing.shield_value || 0)) + amount;
          if (targetChar.召唤键) 同步召唤单位镜像(targetChar);
          return amount;
        }
        targetChar.状态效果[stateName] = {
          类型: 'buff',
          层数: 1,
          描述: `由[${sourceName || stateName}]附加`,
          来源原型摘要: '护盾变化',
          duration: Number(duration || 0),
          面板修改比例: { str: 1.0, def: 1.0, agi: 1.0, sp_max: 1.0 },
          战斗效果: { ...createEmptyCombatEffectMap() },
          shield_value: amount,
        };
        if (targetChar.召唤键) 同步召唤单位镜像(targetChar);
        return amount;
      }

      function resolveReactiveDefenseOnDamage(attacker, defender, incomingDamage, options = {}) {
        let damage = Math.max(0, Math.floor(Number(incomingDamage || 0)));
        const 事件链 = 读取事件链状态(options?.事件链 || options || {});
        if (!defender?.状态效果 || damage <= 0) return { damage, counterDamage: 0, counterEntries: [], log: '', sharedDamageEntries: [], 事件链 };
          const parts = [];
          let counterDamage = 0;
          const counterEntries = [];
          const sharedDamageEntries = [];
        for (const [key, cond] of Object.entries(defender.状态效果)) {
          const ce = cond?.战斗效果 || {};
          if (Number(ce.block_count || 0) > 0) {
            const 状态文本 = `${key} ${cond?.状态 || ''} ${cond?.状态名称 || ''} ${cond?.来源技能 || ''} ${cond?.描述 || ''}`;
            const 黄金瀑布扣费 = 扣除黄金瀑布触发体力(defender, 状态文本);
            if (!黄金瀑布扣费.可触发) continue;
            if (cond.战斗效果) cond.战斗效果.block_count = Math.max(0, Number(ce.block_count || 0) - 1);
            damage = 0;
            parts.push(`[免伤触发] ${defender.name || '目标'}以[${key}]抵消了本次攻击！${黄金瀑布扣费.消耗量 > 0 ? `消耗${黄金瀑布扣费.消耗量}点体力。` : ''}`);
            return { damage, counterDamage, counterEntries, log: parts.join(' '), sharedDamageEntries, 事件链 };
          }
        }
        if (damage > 0) {
          const 护盾条目 = collectShieldConditionEntries(defender);
          if (护盾条目.length) {
            const 护盾抹消规则 = 战斗机制抹消命中(defender, '最终结果', { 原型: '护盾变化' }, { 用途: '封锁' });
            if (护盾抹消规则) {
              parts.push(`[机制抹消] ${defender.name || '目标'}对【${读取战斗机制抹消对象摘要(护盾抹消规则.抹消对象)}】存在封锁，护盾未能吸收伤害。`);
            } else {
              护盾条目.forEach(({ key, cond, shieldValue }) => {
                if (damage <= 0) return;
                const absorbed = Math.min(damage, shieldValue);
                cond.shield_value = Math.max(0, shieldValue - absorbed);
                damage -= absorbed;
                parts.push(`[护盾吸收] ${defender.name || '目标'}的[${key}]吸收了 ${absorbed} 点伤害。`);
                if (cond.shield_value <= 0) {
                  delete defender.状态效果[key];
                  parts.push(`[护盾破碎] ${defender.name || '目标'}的[${key}]被彻底击碎。`);
                }
              });
            }
          }
        }
        if (damage > 0) {
          const allies = getCombatAlliesForUnit(options?.combatData || getCurrentBattleContextSnapshot(), defender).filter(
            ally => isCombatUnitAlive(ally),
          );
          for (const [key, cond] of Object.entries(defender.状态效果)) {
            const ce = cond?.战斗效果 || {};
            const shareRatio = Math.max(0, Math.min(1, Number(ce.damage_share_ratio || 0)));
            const shareCount = Math.max(0, Math.round(Number(ce.damage_share_count || 0)));
            if (!(shareRatio > 0) || !(shareCount > 0) || !allies.length) continue;
            const selectedAllies = allies.slice(0, shareCount);
            if (!selectedAllies.length) continue;
            const sharedTotal = Math.max(1, Math.floor(damage * shareRatio));
            const selfRemain = Math.max(0, damage - sharedTotal);
            const perTargetDamage = Math.max(1, Math.floor(sharedTotal / selectedAllies.length));
            damage = selfRemain;
            selectedAllies.forEach((ally, index) => {
              const finalSharedDamage =
                index === selectedAllies.length - 1
                  ? Math.max(1, sharedTotal - perTargetDamage * Math.max(0, selectedAllies.length - 1))
                  : perTargetDamage;
              sharedDamageEntries.push({
                target: ally,
                damage: Math.max(1, finalSharedDamage),
                kind: 'shared',
                from: defender,
              });
            });
            parts.push(
              `[伤害分摊] ${defender.name || '目标'}借[${key}]将 ${sharedTotal} 点伤害分摊给${selectedAllies
                .map(ally => ally?.name || '队友')
                .join('、')}。`,
            );
            break;
          }
        }
        if (damage > 0) {
          for (const [key, cond] of Object.entries(defender.状态效果)) {
            const ce = cond?.战斗效果 || {};
            const ratio = Number(ce.counter_attack_ratio || 0);
            if (ratio > 0) {
              const 反制键 = `${defender.name || '目标'}:${key}:counter`;
              if (事件链.反制键集合.includes(反制键)) {
                parts.push(`[反制截断] ${defender.name || '目标'}的[${key}]已在本事件链触发过反击。`);
              } else {
                事件链.反制键集合.push(反制键);
                const entryDamage = Math.max(1, Math.floor(damage * ratio));
                counterEntries.push({ sourceStateName: key, counterKind: 'state_counter', damage: entryDamage });
                counterDamage = Math.max(counterDamage, entryDamage);
                parts.push(`[反击触发] ${defender.name || '目标'}借[${key}]展开反击，回震 ${entryDamage} 点伤害！`);
              }
            }
            const reflectRatio = Number(ce.damage_reflect_ratio || 0);
            if (reflectRatio > 0) {
              const 反射键 = `${defender.name || '目标'}:${key}:reflect`;
              if (事件链.反制键集合.includes(反射键)) {
                parts.push(`[反射截断] ${defender.name || '目标'}的[${key}]已在本事件链触发过反射。`);
                continue;
              }
              事件链.反制键集合.push(反射键);
              const reflectDamage = Math.max(1, Math.floor(damage * reflectRatio));
              counterEntries.push({ sourceStateName: key, counterKind: 'state_reflect', damage: reflectDamage });
              counterDamage = Math.max(counterDamage, reflectDamage);
              parts.push(`[伤害反射] ${defender.name || '目标'}借[${key}]反弹了 ${reflectDamage} 点伤害！`);
            }
            const 转移比例 = Number(ce.damage_transfer_ratio || 0);
            if (转移比例 > 0) {
              const 转移键 = `${defender.name || '目标'}:${key}:transfer`;
              if (事件链.反制键集合.includes(转移键)) {
                parts.push(`[转移截断] ${defender.name || '目标'}的[${key}]已在本事件链触发过伤害转移。`);
                continue;
              }
              const 转移目标列表 = 读取伤害转移目标列表(ce.damage_transfer_target, attacker, defender, options?.combatData || getCurrentBattleContextSnapshot());
              const 转移目标 = 转移目标列表.find(unit => unit && !isCombatUnitIdentityMatch(unit, defender?.name || defender));
              if (!转移目标) continue;
              事件链.反制键集合.push(转移键);
              const 转移伤害 = Math.max(1, Math.floor(damage * Math.max(0, Math.min(1, 转移比例))));
              sharedDamageEntries.push({
                target: 转移目标,
                damage: 转移伤害,
                kind: 'transfer',
                from: defender,
              });
              parts.push(`[伤害转移] ${defender.name || '目标'}借[${key}]将 ${转移伤害} 点伤害转给${转移目标.name || '目标'}。`);
            }
          }
        }
        return { damage, counterDamage, counterEntries, log: parts.join(' '), sharedDamageEntries, 事件链 };
      }

      function resolveCastInterruptOnDamage(
        targetChar,
        attackAction,
        inflictedDamage,
        settleResult = {},
        label = '目标',
      ) {
        if (!targetChar?.蓄力技能) return '';
        const targetEffects = targetChar.状态效果
          ? Object.values(targetChar.状态效果).map(c => c?.战斗效果 || {})
          : [];
        const hasSuperArmor =
          targetEffects.some(ce => ce?.super_armor === true) ||
          Object.keys(targetChar.状态效果 || {}).some(key => /霸体|真身/.test(String(key || '')));
        const damageRatio = Math.max(0, Number(inflictedDamage || 0)) / getCombatHpMaxValue(targetChar);
        const stateCalc = getPrimaryStateCalc(attackAction?.skill);
        const specialFlags = String(getPrimaryStateEffect(attackAction?.skill)?.特殊机制标识 || '无');
        const interruptChance = 计算行动打断概率(attackAction?.skill, targetChar, settleResult);
        const hardControl =
          stateCalc.skip_turn === true || stateCalc.cannot_react === true || specialFlags.includes('硬控');
        const controlled = targetEffects.some(ce => ce?.skip_turn === true || ce?.cannot_react === true);
        const interruptTriggered = interruptChance > 0 && Math.random() <= Math.min(1, interruptChance);
        if (hasSuperArmor && (hardControl || interruptTriggered))
          return `[霸体强抗] ${label}凭借霸体稳住阵脚，蓄力未被打断！`;
        if (!hasSuperArmor && (damageRatio >= 0.3 || hardControl || controlled || interruptTriggered)) {
          targetChar.蓄力技能 = null;
          return interruptTriggered
            ? `[打断生效] ${label}的蓄力被强行打断！`
            : `[蓄力打断] ${label}受到重创或控制，蓄力被强制打断！`;
        }
        return '';
      }

      function summarizeProjectedDamageEntries(targetResults = []) {
        const entries = [];
        const entryMap = new Map();
        const kindPriority = kind => {
          const text = String(kind || '').trim();
          if (text === 'primary') return 5;
          if (text === 'secondary') return 4;
          if (text === 'transfer') return 3;
          if (text === 'dot_detonate' || text === 'dot_compress') return 2;
          if (text === 'effect') return 1;
          return 0;
        };
        (Array.isArray(targetResults) ? targetResults : []).forEach((entry, index) => {
          if (!entry?.target) return;
          const targetName = String(
            entry.targetName ||
              entry.target?.name ||
              entry.target?.名称 ||
              entry.target?.charKey ||
              entry.target?.char_key ||
              entry.target?.key ||
              '',
          ).trim();
          const key = targetName || `target-${index}`;
          const damage = Math.max(0, Math.floor(Number(entry?.damage || 0)));
          const cleanSegments = Array.isArray(entry?.damageSegments)
            ? entry.damageSegments.map(value => Math.max(0, Math.floor(Number(value || 0)))).filter(value => value > 0)
            : [];
          const normalized = {
            ...entry,
            targetName: targetName || entry.targetName || '目标',
            damage,
            kind: entry?.kind || (index === 0 ? 'primary' : 'secondary'),
            damageSegments: cleanSegments,
          };
          const existing = entryMap.get(key);
          if (!existing) {
            entryMap.set(key, normalized);
            entries.push(normalized);
            return;
          }
          const beforeDamage = Math.max(0, Number(existing.damage || 0));
          const incomingDamage = damage;
          const existingSegments = Array.isArray(existing.damageSegments)
            ? existing.damageSegments.map(value => Math.max(0, Math.floor(Number(value || 0)))).filter(value => value > 0)
            : [];
          const incomingSegments = cleanSegments;
          existing.damage = Math.max(0, Math.floor(beforeDamage + incomingDamage));
          if (incomingDamage > 0 || incomingSegments.length > 0 || existingSegments.length > 0) {
            const mergedSegments = [
              ...(existingSegments.length ? existingSegments : (beforeDamage > 0 && incomingDamage > 0 ? [beforeDamage] : [])),
              ...(incomingSegments.length ? incomingSegments : (incomingDamage > 0 ? [incomingDamage] : [])),
            ].filter(value => value > 0);
            if (mergedSegments.length > 1 || incomingSegments.length > 0 || existingSegments.length > 0) {
              existing.damageSegments = mergedSegments;
            }
          }
          if (kindPriority(normalized.kind) > kindPriority(existing.kind)) {
            existing.kind = normalized.kind;
            if (normalized.kind === 'primary') existing.target = normalized.target;
          }
          if (!existing.targetName && normalized.targetName) existing.targetName = normalized.targetName;
          if (normalized.被时光回溯规避 === true) existing.被时光回溯规避 = true;
          if (Array.isArray(normalized.时光回溯预结算落地)) {
            const labels = new Set(Array.isArray(existing.时光回溯预结算落地) ? existing.时光回溯预结算落地 : []);
            normalized.时光回溯预结算落地.forEach(label => labels.add(label));
            existing.时光回溯预结算落地 = Array.from(labels).filter(Boolean);
          }
          if (Array.isArray(normalized.logs) && normalized.logs.length) {
            existing.logs = [...(Array.isArray(existing.logs) ? existing.logs : []), ...normalized.logs];
          }
        });
        const primaryEntry = entries.find(entry => entry.kind === 'primary') || entries[0] || null;
        const primaryDamage = Math.max(0, Number(primaryEntry?.damage || 0));
        const totalDamage = entries.reduce((sum, entry) => sum + Math.max(0, Number(entry?.damage || 0)), 0);
        return {
          entries,
          primaryEntry,
          primaryDamage,
          totalDamage,
        };
      }

      function refreshSettleResultProjectedDamage(settleResult = {}) {
        const summary = summarizeProjectedDamageEntries(settleResult?.targetResults || []);
        if (summary.entries.length > 0) {
          settleResult.targetResults = summary.entries;
          settleResult.dmg = summary.primaryDamage;
          settleResult.totalProjectedDamage = summary.totalDamage;
        } else {
          settleResult.dmg = Math.max(0, Math.floor(Number(settleResult?.dmg || 0)));
          settleResult.totalProjectedDamage = settleResult.dmg;
          settleResult.targetResults = [];
        }
        return settleResult;
      }

      function appendProjectedDamageToSettleResult(settleResult = {}, targetChar, damage = 0, kind = 'secondary') {
        if (!settleResult || !targetChar) return settleResult;
        const normalizedDamage = Math.max(0, Math.floor(Number(damage || 0)));
        if (!(normalizedDamage > 0)) return settleResult;
        const targetName = targetChar?.name || targetChar?.名称 || '目标';
        if (!Array.isArray(settleResult.targetResults)) settleResult.targetResults = [];
        const existing = settleResult.targetResults.find(entry => entry?.target && isCombatUnitIdentityMatch(entry.target, targetName));
        if (existing) {
          existing.damage = Math.max(0, Number(existing.damage || 0)) + normalizedDamage;
          if (existing.kind !== 'primary' && kind === 'primary') existing.kind = 'primary';
        } else {
          settleResult.targetResults.push({
            target: targetChar,
            targetName,
            damage: normalizedDamage,
            kind,
          });
        }
        return refreshSettleResultProjectedDamage(settleResult);
      }

      function applyResolvedDamagePackage(attacker, attackAction, settleResult = {}, options = {}) {
        const 事件链 = 读取事件链状态(options?.事件链 || attackAction || settleResult || {});
        const primaryTarget = options.primaryTarget || null;
        const targetResults =
          Array.isArray(settleResult?.targetResults) && settleResult.targetResults.length
            ? settleResult.targetResults
            : primaryTarget
              ? [{ target: primaryTarget, targetName: primaryTarget?.name || '目标', damage: Number(settleResult?.dmg || 0), kind: 'primary' }]
              : [];
        const logParts = [];
        const 当前结算技能 = attackAction?.skill || {};
        const 当前结算效果列表 = getSkillEffects(当前结算技能);
        const 当前结算效果能力 = (() => {
          const kinds = [...new Set((Array.isArray(当前结算效果列表) ? 当前结算效果列表 : [])
            .map(effect => String(effect?.原型 || '').trim())
            .filter(Boolean))];
          const hasDamageEffect = (Array.isArray(当前结算效果列表) ? 当前结算效果列表 : []).some(effect =>
            String(effect?.原型 || '').trim() === '伤害结算' &&
            Math.max(0, Number(effect?.威力倍率 || effect?.威力 || effect?.基础倍率 || 0)) > 0
          );
          return {
            hasDamageEffect,
            effectKinds: kinds,
          };
        })();
        const 结算防守参考 = options?.primaryTarget || targetResults[0]?.target || null;
        const 结算修正命中单位 = (effect, unit) => {
          if (!effect || !unit) return false;
          if (String(effect?.目标 || '').trim() === '全场') return true;
          return resolveSkillEffectTargetCharacters(当前结算技能, effect, attacker, 结算防守参考, options?.combatData || getCurrentBattleContextSnapshot())
            .some(targetObj => isCombatUnitIdentityMatch(targetObj, unit));
        };
        const 读取本次结算修正比例 = (结算名, unit) => {
          return 当前结算效果列表.reduce((最大值, effect) => {
            if (String(effect?.原型 || '').trim() !== '结算修正') return 最大值;
            if (String(effect?.结算 || '').trim() !== 结算名) return 最大值;
            if (!当前技能元素匹配结算修正(当前结算技能, effect)) return 最大值;
            if (!结算修正命中单位(effect, unit)) return 最大值;
            const 比例 = Math.max(0, Math.abs(读取战斗数值正负(effect?.数值 || '100%')) || 1);
            return Math.max(最大值, Math.min(1, 比例));
          }, 0);
        };
        const 恢复伤害吸收资源 = (unit, 资源, 数量) => {
          const 恢复量 = Math.max(0, Math.floor(Number(数量 || 0)));
          if (!unit || !(恢复量 > 0)) return 0;
          const 资源名 = ['生命', '体力', '魂力', '精神力'].includes(String(资源 || '').trim()) ? String(资源 || '').trim() : '生命';
          if (资源名 === '生命') 设置战斗血量值(unit, Math.min(getCombatHpMaxValue(unit), getCombatHpValue(unit) + 恢复量));
          else if (资源名 === '体力') 设置战斗体力值(unit, getCombatStaminaValue(unit) + 恢复量);
          else if (资源名 === '魂力') 设置战斗延迟效果资源值(unit, 'sp', Number(unit.sp || 0) + 恢复量);
          else 设置战斗延迟效果资源值(unit, 'men', Number(unit.men || 0) + 恢复量);
          return 恢复量;
        };
        let primaryAppliedDamage = 0;
        let totalAppliedDamage = 0;
        const ledgerActionType = String(attackAction?.action_type || attackAction?.type || 'attack').trim() || 'attack';
        const ledgerActionName = normalizeBattleActionDisplayName(attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '');
        const ledgerSourceMeta = attackAction?.source === 'summon' || attacker?.召唤键
          ? {
              source: 'summon',
              summonHostName: attacker?.宿主名 || attacker?.__宿主?.name || attacker?.__宿主?.名称 || '',
              summonType: attacker?.类型 || '',
              summonMode: attacker?.行动模式 || '',
            }
          : {};
        const 标记战斗运行态死亡 = unit => {
          if (!unit || typeof unit !== 'object') return false;
          if (getCombatHpValue(unit) > 0) return false;
          if (!unit.__battleRuntime || typeof unit.__battleRuntime !== 'object') unit.__battleRuntime = {};
          unit.__battleRuntime.isDead = true;
          return true;
        };
        const 战斗单位已死亡 = unit => !unit || getCombatHpValue(unit) <= 0 || unit?.__battleRuntime?.isDead === true;
        const 写入段结算中止 = (targetChar, reason = 'ACTOR_DEAD') => 写入战斗事件账本(options?.combatData || {}, {
          eventKind: 'blocked_settlement',
          round: Number(options?.combatData?.回合 || 0),
          actorName: attacker?.name || attacker?.名称 || '',
          targetName: targetChar?.name || targetChar?.名称 || '',
          actionName: attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '',
          actionType: ledgerActionType,
          result: 'blocked',
          failReason: reason === 'TARGET_DEAD' ? '目标已失去战斗能力，后续段数中止' : '行动者已失去战斗能力，后续段数中止',
          primaryOutcome: 'interrupted',
          meta: {
            ...ledgerSourceMeta,
            hook: 'pre_hit',
            reasonCode: reason,
            reasonText: reason === 'TARGET_DEAD' ? '目标已失去战斗能力' : '行动者已失去战斗能力',
          },
        });
        const 写入状态反伤账本 = (targetChar, counterEntry = {}, counterDamage = 0) => {
          const combatData = options?.combatData || {};
          if (!combatData || typeof combatData !== 'object') return null;
          const damage = Math.max(0, Math.round(Number(counterDamage || counterEntry?.damage || 0)));
          if (!(damage > 0)) return null;
          const round = Number(combatData?.回合 || 0);
          const actorName = String(targetChar?.name || targetChar?.名称 || '').trim();
          const targetName = String(attacker?.name || attacker?.名称 || '').trim();
          const counterActionName = normalizeBattleActionDisplayName(counterEntry?.sourceStateName || (counterEntry?.counterKind === 'state_reflect' ? '状态反射' : '状态反击'));
          const counteredActionName = ledgerActionName || normalizeBattleActionDisplayName(attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '');
          if (!actorName || !targetName || !counterActionName || !counteredActionName) return null;
          const ledger = Array.isArray(combatData.__battleEventLedger) ? combatData.__battleEventLedger : [];
          const sourceAction = 查找最近账本动作事件(ledger, {
            round,
            actorName: targetName,
            actionName: counteredActionName,
          });
          const counterWindowEvent = 写入战斗事件账本(combatData, {
            eventKind: 'counter_window',
            round,
            actorName,
            targetName,
            actionName: counterActionName,
            actionType: 'state_counter_window',
            sourceActionName: counteredActionName,
            sourceActionId: sourceAction?.actionId || '',
            parentNodeId: sourceAction?.chainNodeId || '',
            sourceNodeId: sourceAction?.chainNodeId || '',
            result: 'opened',
            meta: {
              counterDepth: 1,
              counterKind: String(counterEntry?.counterKind || 'state_counter').trim(),
              sourceStateName: counterActionName,
              reasonCode: 'COUNTER_WINDOW_OPENED',
            },
          });
          return 写入战斗事件账本(combatData, {
            eventKind: 'counter',
            round,
            actorName,
            targetName,
            actionName: counterActionName,
            actionType: 'counter',
            sourceActionName: counteredActionName,
            sourceActionId: sourceAction?.actionId || '',
            parentNodeId: counterWindowEvent?.chainNodeId || sourceAction?.chainNodeId || '',
            sourceNodeId: counterWindowEvent?.chainNodeId || sourceAction?.chainNodeId || '',
            result: 'success',
            meta: {
              damage,
              counterDepth: 1,
              counterKind: String(counterEntry?.counterKind || 'state_counter').trim(),
              sourceStateName: counterActionName,
              reasonCode: 'REACTION_SUCCEEDED',
              counterWindowEventId: counterWindowEvent?.eventId || '',
              counterWindowNodeId: counterWindowEvent?.chainNodeId || '',
              settlementTrace: [
                { key: 'sourceAction', label: '来源动作', value: counterActionName },
                { key: 'counteredAction', label: '被反制动作', value: counteredActionName },
                { key: 'attacker', label: '反击方', value: actorName },
                { key: 'target', label: '目标', value: targetName },
                { key: 'result', label: '反击结果', value: 'success' },
                { key: 'counterDepth', label: '防反层级', value: 1 },
                { key: 'finalDamage', label: '最终伤害', value: damage },
              ],
            },
          });
        };
        const 确保伤害包目标反应前置 = (targetChar = null, targetEntry = {}) => {
          const combatData = options?.combatData || {};
          const ledger = Array.isArray(combatData?.__battleEventLedger) ? combatData.__battleEventLedger : [];
          const round = Number(combatData?.回合 || 0);
          const actorName = String(attacker?.name || attacker?.名称 || '').trim();
          const targetName = String(targetChar?.name || targetChar?.名称 || targetEntry?.targetName || '').trim();
          const actionName = ledgerActionName || normalizeBattleActionDisplayName(targetEntry?.actionName || '');
          if (!round || !actorName || !targetName || !actionName || isSameBattleReportName(actorName, targetName)) return null;
          const sourceAction = 查找最近账本动作事件(ledger, { round, actorName, actionName });
          if (!sourceAction?.chainNodeId) return null;
          const existingReaction = ledger.some(event =>
            ['pass', 'dodge', 'defend'].includes(String(event?.eventKind || '').trim()) &&
            Number(event?.round || 0) === round &&
            isSameBattleReportName(event?.actorName || '', targetName) &&
            isSameBattleReportName(event?.targetName || '', actorName) &&
            (
              String(event?.sourceActionId || '').trim() === String(sourceAction.actionId || '').trim() ||
              String(event?.sourceNodeId || '').trim() === String(sourceAction.chainNodeId || '').trim() ||
              normalizeBattleActionDisplayName(event?.sourceActionName || event?.actionName || '') === actionName
            )
          );
          if (existingReaction) return null;
          return 写入战斗事件账本(combatData, {
            eventKind: 'pass',
            round,
            actorName: targetName,
            targetName: actorName,
            actionName: '临场应对',
            actionType: 'reaction',
            sourceActionName: actionName,
            sourceActionId: sourceAction?.actionId || '',
            parentNodeId: sourceAction?.chainNodeId || '',
            sourceNodeId: sourceAction?.chainNodeId || '',
            result: 'reaction_failed',
            meta: {
              source: 'damage_package_target_reaction',
              reasonCode: 'REACTION_FAILED',
              reasonText: '未形成有效应招动作',
            },
          });
        };
        targetResults.forEach((targetEntry, index) => {
          let targetChar = targetEntry?.target;
          if (!targetChar) return;
          if (战斗单位已死亡(attacker)) {
            写入段结算中止(targetChar, 'ACTOR_DEAD');
            return;
          }
          const 护卫召唤 = targetChar.召唤键 ? null : 读取护卫召唤单位(options?.combatData || getCurrentBattleContextSnapshot(), targetChar);
          if (护卫召唤) {
            logParts.push(`[召唤护卫承伤] ${护卫召唤.name || '召唤物'}替${targetChar.name || '目标'}承受本次攻击。`);
            护卫召唤.__本回合已召唤行动 = true;
            targetChar = 护卫召唤;
          }
          bindCombatParticipant(targetChar);
          const targetFinalStat = targetChar.final || buildCombatFinalStats(targetChar);
          const targetConditionEffects = targetChar.状态效果
            ? Object.values(targetChar.状态效果).map(c => c?.战斗效果 || {})
            : [];
          if (targetEntry?.evaded !== true) 确保伤害包目标反应前置(targetChar, targetEntry);
          if (targetEntry?.evaded === true) {
            写入战斗事件账本(options?.combatData || {}, {
              eventKind: 'hit_result',
              round: Number(options?.combatData?.回合 || 0),
              actorName: attacker?.name || attacker?.名称 || '',
              targetName: targetChar?.name || targetChar?.名称 || targetEntry?.targetName || '',
              actionName: attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '',
              actionType: ledgerActionType,
              result: 'miss',
              failureReason: 'dodged',
              effectCapability: 当前结算效果能力,
              meta: {
                ...ledgerSourceMeta,
                damage: 0,
                incomingDamage: 0,
                reactiveDamage: 0,
                defenseThreshold: 0,
                shieldAbsorb: 0,
                failureReason: 'dodged',
                dodgeRate: Number(targetEntry?.dodgeRate || 0),
                dodgeRoll: Number(targetEntry?.dodgeRoll || 0),
              },
            });
            if (targetEntry?.kind === 'primary' || (index === 0 && !primaryAppliedDamage)) primaryAppliedDamage = 0;
            return;
          }
          if (targetEntry?.被时光回溯规避 === true) {
            if (targetEntry?.kind === 'primary' || (index === 0 && !primaryAppliedDamage)) primaryAppliedDamage = 0;
            return;
          }
          const incomingDamage = Math.max(0, Math.floor(Number(targetEntry?.damage || 0)));
          if (!(incomingDamage > 0)) return;
          const 段伤害列表 = Array.isArray(targetEntry?.damageSegments) && targetEntry.damageSegments.length
            ? targetEntry.damageSegments.map(段伤害 => Math.max(0, Math.floor(Number(段伤害 || 0)))).filter(段伤害 => 段伤害 > 0)
            : [incomingDamage];
          let appliedDamage = 0;
          段伤害列表.forEach((段伤害, 段序号) => {
            if (战斗单位已死亡(attacker)) {
              写入段结算中止(targetChar, 'ACTOR_DEAD');
              return;
            }
            if (战斗单位已死亡(targetChar)) {
              写入段结算中止(targetChar, 'TARGET_DEAD');
              return;
            }
            let 待结算段伤害 = 段伤害;
            const 伤害转治疗比例 = Math.max(
              读取本次结算修正比例('伤害转治疗', targetChar),
              targetConditionEffects.reduce((最大值, ce) => Math.max(最大值, Number(ce.damage_to_heal_ratio || 0)), 0),
            );
            if (伤害转治疗比例 > 0) {
              const 治疗量 = Math.max(1, Math.min(待结算段伤害, Math.floor(待结算段伤害 * Math.min(1, 伤害转治疗比例))));
              设置战斗血量值(targetChar, Math.min(getCombatHpMaxValue(targetChar), getCombatHpValue(targetChar) + 治疗量));
              待结算段伤害 = Math.max(0, 待结算段伤害 - 治疗量);
              logParts.push(`[结算修正] ${targetChar.name || '目标'}本次伤害有 ${治疗量} 点转为治疗。`);
              if (!(待结算段伤害 > 0)) return;
            }
            const reactiveDefense = resolveReactiveDefenseOnDamage(attacker, targetChar, 待结算段伤害, { ...options, 事件链 });
            let finalDamage = Math.max(0, Math.floor(Number(reactiveDefense.damage || 0) * 读取目标元素承伤倍率(targetChar, attackAction?.skill || {})));
            if (reactiveDefense.counterDamage > 0) {
              设置战斗血量值(attacker, getCombatHpValue(attacker) - reactiveDefense.counterDamage);
              const counterEntries = Array.isArray(reactiveDefense.counterEntries) && reactiveDefense.counterEntries.length
                ? reactiveDefense.counterEntries
                : [{ sourceStateName: '状态反击', counterKind: 'state_counter', damage: reactiveDefense.counterDamage }];
              counterEntries
                .filter(entry => Math.round(Number(entry?.damage || 0)) === Math.round(Number(reactiveDefense.counterDamage || 0)))
                .slice(0, 1)
                .forEach(entry => 写入状态反伤账本(targetChar, entry, reactiveDefense.counterDamage));
              标记战斗运行态死亡(attacker);
            }
            if (reactiveDefense.log) logParts.push(reactiveDefense.log);
            if (战斗单位已死亡(attacker)) {
              写入段结算中止(targetChar, 'ACTOR_DEAD');
              return;
            }
            if (!(finalDamage > 0)) return;
            const 基础破防阈值 = Math.max(1, Number(targetFinalStat.def || targetChar.def || 0)) * 0.008;
            const defThreshold = 基础破防阈值 / Math.max(1, 段伤害列表.length > 1 ? Math.sqrt(段伤害列表.length) : 1);
            const ignoreDefenseThreshold = ['dot_detonate', 'shield_break'].includes(String(targetEntry?.kind || '').trim());
            const 伤害效果 = 当前结算效果列表.find(effect => String(effect?.原型 || '').trim() === '伤害结算') || {};
            const 伤害类型 = String(targetEntry?.damageType || 伤害效果?.伤害类型 || '入参伤害').trim();
            const 技能威力 = Math.max(1, Number(targetEntry?.skillPower || 伤害效果?.威力倍率 || 段伤害 || incomingDamage || finalDamage || 1));
            const 攻防攻击值 = Number(targetEntry?.formulaAttackValue || 计算物理伤害攻势值(attacker, attacker?.final || buildCombatFinalStats(attacker), 伤害类型) || 段伤害 || finalDamage || 1);
            const 攻防防御值 = Math.max(1, Number(targetEntry?.formulaDefenseValue || targetEntry?.actualDefense || targetFinalStat.def || targetChar.def || 1));
            const ledgerFormulaTrace = {
              damageType: 伤害类型,
              skillPower: 技能威力,
              attackValue: Number(targetEntry?.formulaAttackValue || 攻防攻击值),
              defenseValue: Number(targetEntry?.formulaDefenseValue || 攻防防御值),
              baseDamage: Number(targetEntry?.baseFormulaDamage || 段伤害 || finalDamage),
              formulaText: String(targetEntry?.baseFormulaText || (伤害效果?.威力倍率 ? '威力×预结算攻防折算' : '预结算入参段伤害')).trim(),
              meleeContactScale: Number(targetEntry?.meleeContactScale || (/近身/.test(伤害类型) ? 1.04 : 1)),
              fusionDamageMult: Number(targetEntry?.fusionDamageMult || 1),
            };
            const 段标签 = 段伤害列表.length > 1 ? `第${段序号 + 1}段` : '';
            let 本段实际伤害 = 0;
            if (!ignoreDefenseThreshold && finalDamage < defThreshold) {
              const 破防接近度 = finalDamage / Math.max(1, defThreshold);
              const 韧性削伤 = 破防接近度 >= 0.22
                ? Math.max(1, Math.floor(finalDamage * (0.32 + Math.min(0.46, 破防接近度 * 0.48))))
                : 1;
              设置战斗血量值(targetChar, getCombatHpValue(targetChar) - 韧性削伤);
              标记战斗运行态死亡(targetChar);
              appliedDamage += 韧性削伤;
              本段实际伤害 = 韧性削伤;
              写入战斗事件账本(options?.combatData || {}, {
                eventKind: 'hit_result',
                round: Number(options?.combatData?.回合 || 0),
                actorName: attacker?.name || attacker?.名称 || '',
                targetName: targetChar?.name || targetChar?.名称 || targetEntry?.targetName || '',
                actionName: attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '',
                actionType: ledgerActionType,
                result: 韧性削伤 > 1 ? 'graze' : 'chip',
                meta: {
                  ...ledgerSourceMeta,
                  damage: 韧性削伤,
                  incomingDamage: 段伤害,
                  reactiveDamage: finalDamage,
                  defenseThreshold: Math.round(defThreshold),
                  breakType: 韧性削伤 > 1 ? 'partial_break' : 'forced_chip',
                  segmentIndex: 段序号 + 1,
                  segmentCount: 段伤害列表.length,
                  actualDefense: Math.round(Number(targetEntry?.actualDefense || 0)),
                  defenseStrip: Number(targetEntry?.defenseStrip || 0),
                  spiritResistStrip: Number(targetEntry?.spiritResistStrip || 0),
                  soulDriveScale: Number(targetEntry?.soulDriveScale || 1),
                  spiritDriveScale: Number(targetEntry?.spiritDriveScale || 1),
                  positionDamageScale: Number(targetEntry?.positionDamageScale || 1),
                  costDamageScale: Number(targetEntry?.costDamageScale || 1),
                  fluctuation: Number(targetEntry?.fluctuation || 1),
                  grazeMultiplier: Number(targetEntry?.grazeMultiplier || 1),
                  dodgeRate: targetEntry?.dodgeRate ?? undefined,
                  dodgeRoll: targetEntry?.dodgeRoll ?? undefined,
                  damageReduction: Number(targetEntry?.damageReduction || 0),
                  jadeHandReduction: Number(targetEntry?.jadeHandReduction || 0),
                  receivedDamageMult: Number(targetEntry?.receivedDamageMult || 1),
                  elementDamageMult: Number(targetEntry?.elementDamageMult || 1),
                  finalDamageMult: Number(targetEntry?.finalDamageMult || 1),
                  finalDamageBonus: Number(targetEntry?.finalDamageBonus || 0),
                  activeReactionShield: Math.max(0, Number(targetEntry?.activeReactionShield || 0)),
                  formulaTrace: ledgerFormulaTrace,
                },
                effectCapability: 当前结算效果能力,
              });
              if (韧性削伤 > 1) {
                logParts.push(
                  targetEntry?.kind === 'primary'
                    ? `[未完全破防] ${attacker.name || '攻击方'}${段标签 ? `的${段标签}` : ''}对${targetChar.name || '目标'}造成 ${韧性削伤} 点韧性削伤。`
                    : `[群体擦伤] ${targetChar.name || '目标'}${段标签}承受 ${韧性削伤} 点擦伤伤害。`,
                );
              } else {
                logParts.push(
                  targetEntry?.kind === 'primary'
                    ? `[未破防] ${attacker.name || '攻击方'}${段标签 ? `的${段标签}` : ''}对${targetChar.name || '目标'}仅造成 1 点强制伤害。`
                    : `[群体擦伤] ${targetChar.name || '目标'}${段标签}仅承受 1 点擦伤伤害。`,
                );
              }
            } else {
              设置战斗血量值(targetChar, getCombatHpValue(targetChar) - finalDamage);
              标记战斗运行态死亡(targetChar);
              appliedDamage += finalDamage;
              本段实际伤害 = finalDamage;
              写入战斗事件账本(options?.combatData || {}, {
                eventKind: 'hit_result',
                round: Number(options?.combatData?.回合 || 0),
                actorName: attacker?.name || attacker?.名称 || '',
                targetName: targetChar?.name || targetChar?.名称 || targetEntry?.targetName || '',
                actionName: attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '',
                actionType: ledgerActionType,
                result: 'hit',
                meta: {
                  ...ledgerSourceMeta,
                  damage: finalDamage,
                  incomingDamage: 段伤害,
                  reactiveDamage: finalDamage,
                  defenseThreshold: Math.round(defThreshold),
                  breakType: '',
                  segmentIndex: 段序号 + 1,
                  segmentCount: 段伤害列表.length,
                  actualDefense: Math.round(Number(targetEntry?.actualDefense || 0)),
                  defenseStrip: Number(targetEntry?.defenseStrip || 0),
                  spiritResistStrip: Number(targetEntry?.spiritResistStrip || 0),
                  soulDriveScale: Number(targetEntry?.soulDriveScale || 1),
                  spiritDriveScale: Number(targetEntry?.spiritDriveScale || 1),
                  positionDamageScale: Number(targetEntry?.positionDamageScale || 1),
                  costDamageScale: Number(targetEntry?.costDamageScale || 1),
                  fluctuation: Number(targetEntry?.fluctuation || 1),
                  grazeMultiplier: Number(targetEntry?.grazeMultiplier || 1),
                  dodgeRate: targetEntry?.dodgeRate ?? undefined,
                  dodgeRoll: targetEntry?.dodgeRoll ?? undefined,
                  damageReduction: Number(targetEntry?.damageReduction || 0),
                  jadeHandReduction: Number(targetEntry?.jadeHandReduction || 0),
                  receivedDamageMult: Number(targetEntry?.receivedDamageMult || 1),
                  elementDamageMult: Number(targetEntry?.elementDamageMult || 1),
                  finalDamageMult: Number(targetEntry?.finalDamageMult || 1),
                  finalDamageBonus: Number(targetEntry?.finalDamageBonus || 0),
                  activeReactionShield: Math.max(0, Number(targetEntry?.activeReactionShield || 0)),
                  formulaTrace: ledgerFormulaTrace,
                },
                effectCapability: 当前结算效果能力,
              });
              logParts.push(
                targetEntry?.kind === 'primary'
                  ? `[命中结算] ${attacker.name || '攻击方'}${段标签 ? `的${段标签}` : ''}对${targetChar.name || '目标'}造成 ${finalDamage} 点最终伤害。`
                  : `[群体命中] ${targetChar.name || '目标'}${段标签}承受 ${finalDamage} 点伤害。`,
              );
              if (finalDamage > Number(targetFinalStat.sp_max || targetChar.sp_max || 0) * 0.5) {
                if (!targetChar.状态效果) targetChar.状态效果 = {};
                const 重创状态条目 = {
                  类型: 'debuff',
                  状态: '重度流血',
                  状态名称: '重度流血',
                  __本回合新附加: true,
                  层数: 1,
                  描述: '重创导致的流血',
                  duration: 3,
                  面板修改比例: { str: 1.0, def: 1.0, agi: 1.0, sp_max: 1.0 },
                  战斗效果: {
                    ...createEmptyCombatEffectMap(),
                    dot_damage: Math.floor(getCombatHpMaxValue(targetChar) * 0.05),
                  },
                };
                const 持续移除命中 = 持续状态移除阻断状态附着(targetChar, '重度流血', 重创状态条目, attacker);
                if (持续移除命中) {
                  logParts.push(`[持续状态移除] ${targetChar.name || '目标'}的[重度流血]被[${持续移除命中.key}]拦截。`);
                } else {
                  targetChar.状态效果['重度流血'] = 重创状态条目;
                  重创状态条目.__状态来源键 = 记录状态来源登记(options?.combatData || {}, {
                    stateName: '重度流血',
                    targetName: targetChar?.name || targetChar?.名称 || targetEntry?.targetName || '',
                    sourceActorName: attacker?.name || attacker?.名称 || '',
                    sourceActionName: attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '',
                    sourceActionType: attackAction?.action_type || attackAction?.type || 'attack',
                    sourceRound: Number(options?.combatData?.回合 || 0),
                    duration: Math.max(0, Number(重创状态条目.duration || 0)),
                    effectSummary: 构建状态结果效果摘要(重创状态条目.战斗效果 || {}),
                    driverAttr: '魂力上限',
                  });
                  写入战斗事件账本(options?.combatData || {}, {
                    eventKind: 'state_apply',
                    round: Number(options?.combatData?.回合 || 0),
                    actorName: attacker?.name || attacker?.名称 || '',
                    targetName: targetChar?.name || targetChar?.名称 || targetEntry?.targetName || '',
                    actionName: attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '',
                    actionType: attackAction?.action_type || attackAction?.type || 'attack',
                    sourceActionName: attackAction?.skill?.name || attackAction?.skill?.魂技名 || attackAction?.action_type || attackAction?.type || '',
                    sourceRound: Number(options?.combatData?.回合 || 0),
                    result: 'applied',
                    applicationId: 重创状态条目.__状态来源键,
                    duration: Math.max(0, Number(重创状态条目.duration || 0)),
                    effectSummary: 构建状态结果效果摘要(重创状态条目.战斗效果 || {}),
                    driverAttr: '魂力上限',
                    meta: { stateName: '重度流血' },
                  });
                  logParts.push(`[重创打击] ${targetChar.name || '目标'}被附加[重度流血]状态！`);
                }
              }
            }
            const 受到伤害吸收条目 = [];
            当前结算效果列表.forEach(effect => {
              if (String(effect?.原型 || '').trim() !== '结算修正') return;
              if (String(effect?.结算 || '').trim() !== '伤害吸收') return;
              if (String(effect?.吸收来源 || '造成伤害').trim() !== '受到伤害') return;
              if (!当前技能元素匹配结算修正(当前结算技能, effect)) return;
              if (!结算修正命中单位(effect, targetChar)) return;
              const 比例 = Math.max(0, 读取战斗数值正负(effect?.数值));
              if (比例 <= 0) return;
              const 资源 = ['生命', '体力', '魂力', '精神力'].includes(String(effect?.吸收资源 || '').trim()) ? String(effect?.吸收资源 || '').trim() : '生命';
              受到伤害吸收条目.push({
                资源,
                比例,
                转化效果: ['恢复资源', '下次造成伤害'].includes(String(effect?.转化效果 || '').trim()) ? String(effect?.转化效果 || '').trim() : '恢复资源',
                增幅上限: Math.max(0, Math.min(1, Math.abs(读取战斗数值正负(effect?.增幅上限 || '+50%')))),
                来源名: String(当前结算技能?.name || 当前结算技能?.魂技名 || '伤害吸收').trim() || '伤害吸收',
              });
            });
            targetConditionEffects.forEach(ce => {
              const 比例 = Math.max(0, Number(ce.damage_absorb_ratio || 0));
              if (比例 <= 0 || String(ce.吸收来源 || '造成伤害').trim() !== '受到伤害') return;
              const 资源 = ['生命', '体力', '魂力', '精神力'].includes(String(ce.吸收资源 || '').trim()) ? String(ce.吸收资源 || '').trim() : '生命';
              受到伤害吸收条目.push({
                资源,
                比例,
                转化效果: ['恢复资源', '下次造成伤害'].includes(String(ce.吸收转化效果 || '').trim()) ? String(ce.吸收转化效果 || '').trim() : '恢复资源',
                增幅上限: Math.max(0, Math.min(1, Number(ce.伤害吸收增幅上限 || 0.5))),
                来源名: String(ce.来源技能 || '伤害吸收').trim() || '伤害吸收',
              });
            });
            受到伤害吸收条目.forEach(({ 资源, 比例, 转化效果, 增幅上限, 来源名 }) => {
              const 吸收量 = 本段实际伤害 * Math.max(0, Number(比例 || 0));
              if (转化效果 === '下次造成伤害') {
                const 增幅增加 = 写入下次伤害增幅状态(targetChar, 吸收量 / Math.max(1, getCombatStaminaMaxValue(targetChar)), 来源名, 增幅上限);
                if (增幅增加 > 0) logParts.push(`[伤害吸收] ${targetChar.name || '目标'}将承受伤害蓄为下次伤害增幅+${Math.round(增幅增加 * 100)}%。`);
                return;
              }
              const 恢复量 = 恢复伤害吸收资源(targetChar, 资源, 吸收量);
              if (恢复量 > 0) logParts.push(`[伤害吸收] ${targetChar.name || '目标'}受到伤害时恢复了 ${恢复量} 点${资源}。`);
            });
            if (Array.isArray(reactiveDefense.sharedDamageEntries) && reactiveDefense.sharedDamageEntries.length) {
              reactiveDefense.sharedDamageEntries.forEach(sharedEntry => {
                const sharedTarget = sharedEntry?.target;
                const sharedDamage = Math.max(0, Math.floor(Number(sharedEntry?.damage || 0)));
                if (!sharedTarget || !(sharedDamage > 0)) return;
                bindCombatParticipant(sharedTarget);
                设置战斗血量值(sharedTarget, getCombatHpValue(sharedTarget) - sharedDamage);
                同步召唤单位镜像(sharedTarget);
                logParts.push(
                  sharedEntry?.kind === 'transfer'
                    ? `[转移结算] ${sharedTarget.name || '目标'}承受了由${targetChar.name || '目标'}转出的 ${sharedDamage} 点伤害。`
                    : `[分摊结算] ${sharedTarget.name || '队友'}替${targetChar.name || '目标'}承受了 ${sharedDamage} 点伤害。`,
                );
                if (getCombatHpValue(sharedTarget) <= 0) {
                  const reviveLog = triggerReviveEffect(sharedTarget, sharedTarget.name || '队友');
                  if (reviveLog) logParts.push(reviveLog);
                  const 召唤死亡日志 = 处理召唤单位死亡(options?.combatData || getCurrentBattleContextSnapshot(), sharedTarget);
                  if (召唤死亡日志) logParts.push(召唤死亡日志);
                }
              });
            }
          });
          const interruptLog = resolveCastInterruptOnDamage(
            targetChar,
            attackAction,
            appliedDamage,
            settleResult,
            targetChar.name || `目标${index + 1}`,
          );
          if (interruptLog) logParts.push(interruptLog);
          if (getCombatHpValue(targetChar) <= 0) {
            const reviveLog = triggerReviveEffect(targetChar, targetChar.name || `目标${index + 1}`);
            if (reviveLog) logParts.push(reviveLog);
            const 召唤死亡日志 = 处理召唤单位死亡(options?.combatData || getCurrentBattleContextSnapshot(), targetChar);
            if (召唤死亡日志) logParts.push(召唤死亡日志);
          }
          if (targetEntry?.kind === 'primary' || (index === 0 && !primaryAppliedDamage)) {
            primaryAppliedDamage = appliedDamage;
          }
          totalAppliedDamage += appliedDamage;
        });
        return {
          log: logParts.join(' '),
          primaryAppliedDamage,
          totalAppliedDamage,
          事件链,
        };
      }

      // ==========================================
      // 📍 战斗生存/失能判定
      // ==========================================
      function getCombatHpValue(char) {
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char || {};
        return Math.max(0, Number(char?.hp ?? stats.hp ?? char?.HP ?? stats.HP ?? 0));
      }

      function getCombatHpMaxValue(char) {
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char || {};
        return Math.max(1, Number(char?.hp_max ?? stats.hp_max ?? char?.HP上限 ?? stats.HP上限 ?? 1));
      }

      function getCombatStaminaValue(char) {
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char || {};
        return Math.max(0, Number(char?.体力 ?? stats.体力 ?? char?.sta ?? stats.sta ?? char?.vit ?? stats.vit ?? 0));
      }

      function getCombatStaminaMaxValue(char) {
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char || {};
        return Math.max(1, Number(char?.体力上限 ?? stats.体力上限 ?? char?.sta_max ?? stats.sta_max ?? char?.vit_max ?? stats.vit_max ?? 1));
      }

      function 同步战斗资源镜像字段(char) {
        if (!char || typeof char !== 'object') return;
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char;
        const syncValue = (runtimeKeys, chineseKey, maxRuntimeKeys, maxChineseKey) => {
          const runtimeKey = runtimeKeys.find(key => Object.prototype.hasOwnProperty.call(char, key) && char[key] !== undefined) || runtimeKeys[0];
          const rawValue =
            char[runtimeKey] ??
            char[chineseKey] ??
            stats[runtimeKey] ??
            stats[chineseKey] ??
            0;
          const rawMax =
            maxRuntimeKeys.map(key => char[key] ?? stats[key]).find(value => value !== undefined) ??
            char[maxChineseKey] ??
            stats[maxChineseKey] ??
            rawValue ??
            1;
          const maxValue = Math.max(1, Number(rawMax || 1));
          const nextValue = Math.max(0, Math.min(maxValue, Number(rawValue || 0)));
          runtimeKeys.forEach(key => {
            char[key] = nextValue;
          });
          char[chineseKey] = nextValue;
          maxRuntimeKeys.forEach(key => {
            char[key] = maxValue;
          });
          char[maxChineseKey] = maxValue;
          if (stats && stats !== char) {
            stats[chineseKey] = nextValue;
            stats[maxChineseKey] = maxValue;
          }
        };
        syncValue(['sp'], '魂力', ['sp_max'], '魂力上限');
        syncValue(['men'], '精神力', ['men_max'], '精神力上限');
        syncValue(['sta', 'vit'], '体力', ['sta_max', 'vit_max'], '体力上限');
      }

      function 设置战斗血量值(char, value) {
        if (!char || typeof char !== 'object') return 0;
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char;
        const nextValue = Math.max(0, Math.min(getCombatHpMaxValue(char), Number(value || 0)));
      if ('hp' in char || Object.prototype.hasOwnProperty.call(char, 'hp')) char.hp = nextValue;
      else char.HP = nextValue;
      if (stats && typeof stats === 'object') stats.HP = nextValue;
      if (char.召唤键) 同步召唤单位镜像(char);
      return nextValue;
    }

      function 设置战斗体力值(char, value) {
        if (!char || typeof char !== 'object') return 0;
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char;
        const nextValue = Math.max(0, Math.min(getCombatStaminaMaxValue(char), Number(value || 0)));
      if ('sta' in char || Object.prototype.hasOwnProperty.call(char, 'sta')) char.sta = nextValue;
      else char.体力 = nextValue;
      if (stats && typeof stats === 'object') stats.体力 = nextValue;
      if (char.召唤键) 同步召唤单位镜像(char);
      return nextValue;
    }

      function getCombatHpRatio(char) {
        return getCombatHpValue(char) / getCombatHpMaxValue(char);
      }

      function 判断虚拟战斗类型(战斗类型 = '') {
        return /虚拟|升灵台|魂灵塔|模拟/.test(String(战斗类型 || ''));
      }

      function 读取参战者压制摘要(combatData = {}) {
        const participants = combatData?.参战者 && typeof combatData.参战者 === 'object' ? combatData.参战者 : {};
        return [
          ...(Array.isArray(participants.team_player) ? participants.team_player : []),
          ...(Array.isArray(participants.team_enemy) ? participants.team_enemy : []),
        ]
          .map(unit => unit && unit.实力压制 && unit.实力压制.刻意压制等级 === true
            ? `${unit.name || unit.名称 || '参战者'}：原${unit.实力压制.原始等级 || '?'}级 -> 压制到${unit.实力压制.压制等级 || '?'}级`
            : '')
          .filter(Boolean);
      }

      function 构建压制战HP截断裁断(combatData, 起始快照 = {}, attacker, defender) {
        if (判断虚拟战斗类型(combatData?.战斗类型)) return null;
        const 压制摘要 = 读取参战者压制摘要(combatData);
        if (!压制摘要.length) return null;
        const 候选 = [
          {
            name: attacker?.name || attacker?.名称 || '己方',
            损失比例: Math.max(0, (Number(起始快照.attackerHp || 0) - getCombatHpValue(attacker)) / Math.max(1, Number(起始快照.attackerHpMax || getCombatHpMaxValue(attacker)))),
            剩余比例: Math.max(0, Math.min(100, Math.round(getCombatHpValue(attacker) / getCombatHpMaxValue(attacker) * 100))),
            对手: defender?.name || defender?.名称 || '敌方',
          },
          {
            name: defender?.name || defender?.名称 || '敌方',
            损失比例: Math.max(0, (Number(起始快照.defenderHp || 0) - getCombatHpValue(defender)) / Math.max(1, Number(起始快照.defenderHpMax || getCombatHpMaxValue(defender)))),
            剩余比例: Math.max(0, Math.min(100, Math.round(getCombatHpValue(defender) / getCombatHpMaxValue(defender) * 100))),
            对手: attacker?.name || attacker?.名称 || '己方',
          },
        ];
        const 命中 = 候选.filter(item => item.损失比例 >= 0.1).sort((a, b) => b.损失比例 - a.损失比例)[0];
        if (!命中) return { 压制摘要, 必须结束: false };
        return {
          压制摘要,
          必须结束: true,
          胜方: 命中.对手,
          败方: 命中.name,
          败方剩余HP比例: 命中.剩余比例,
          HP损失比例: Number((命中.损失比例 * 100).toFixed(1)),
        };
      }

      function getCombatStaminaRatio(char) {
        return getCombatStaminaValue(char) / getCombatStaminaMaxValue(char);
      }

      function 提取行为防反失败原因(候选 = {}, 反应动作 = null, 防反方 = null, 攻击方 = null) {
        const 显式原因 = String(候选?.failReason || '').trim();
        if (显式原因) return 显式原因;
        const 反应类型 = String(反应动作?.type || 反应动作?.action_type || '').trim();
        const 反应日志 = String(反应动作?.log || '').trim();
        if (/距离不够|距离不足/.test(反应日志)) return '距离不够';
        if (/状态枯竭|资源不足|无法支撑/.test(反应日志)) return '资源不足';
        if (/来不及追加防守|来不及反应/.test(反应日志)) return '来不及反应';
        if (/被控状态|无法反应|无暇反应|无反应/.test(`${反应类型} ${反应日志}`)) return '无法反应';
        if (/被打断|中断|打断/.test(反应日志)) return '被打断';
        if (/动作被拒|被拒绝|拒绝执行|拒绝/.test(反应日志)) return '动作被拒';
        if (防反方?.temp_cannot_react === true) return '无法反应';
        if (!isCombatUnitAbleToFight(防反方) && isCombatUnitAbleToFight(攻击方)) return '失去反打能力';
        return '';
      }

      function 读取行为防反深度(动作 = null) {
        if (!动作 || typeof 动作 !== 'object') return 0;
        const 显式深度 = Number(动作.__counterDepth ?? 动作.counterDepth ?? 动作.meta?.counterDepth ?? 0);
        if (显式深度 > 0) return Math.max(0, Math.floor(显式深度));
        return 动作.__行为防反 === true ? 1 : 0;
      }

      function 执行行为防反结算(攻击方, 防反方, 原动作, 反应动作, 交锋结果, 伤害包, 战斗数据) {
        const 候选 = 交锋结果?.__行为防反候选;
        const 原防反深度 = 读取行为防反深度(原动作);
        if (!候选 || 原防反深度 >= 2) return '';
        const 当前防反深度 = 原防反深度 + 1;
        if (!单挑动作可触发攻击反应链(原动作, 攻击方, 防反方, 战斗数据)) return '';
        const 原动作具备伤害结算 = getSkillEffects(原动作?.skill || {}, { 行为规划: true })
          .some(effect => String(effect?.原型 || '').trim() === '伤害结算' && Number(effect?.威力倍率 || effect?.威力 || effect?.基础倍率 || 0) > 0);
        const 实际承伤 = Math.max(0, Number(伤害包?.primaryAppliedDamage || 0));
        if (!原动作具备伤害结算 && !(实际承伤 > 0)) return '';
        if (String(候选.防反类型 || '').trim() === '硬抗换伤' && 候选.需要主动防御成立 === true) {
          const 反应类型 = String(反应动作?.type || 反应动作?.action_type || '').trim();
          const 反应已成立 = /肉体兜底|承伤硬抗|危机自保|防御|收招转防|借力守势|坚壁反制/.test(反应类型);
          if (!反应已成立 || 候选.被动承压 === true) return '';
        }
        const 防反者 = 候选.防反方 || 防反方;
        const 目标 = 候选.攻击方 || 攻击方;
        const 允许以命换伤 = 候选.以命换伤 === true && getCombatStaminaValue(防反者) > 0;
        if (!防反者 || !目标 || (!isCombatUnitAbleToFight(防反者) && !允许以命换伤) || !isCombatUnitAbleToFight(目标)) return '';

        let 触发概率 = Number(候选.触发概率 || 0);
        if (候选.防反类型 === '硬抗换伤') {
          触发概率 = 计算行为防反概率({
            ...候选,
            原动作,
            反应动作,
            战斗数据,
            实际伤害: 实际承伤,
          });
        }
        if (!(触发概率 > 0)) return '';

        const 投点 = Math.random();
        const 概率文本 = `${Math.round(触发概率 * 100)}%`;
        if (投点 >= 触发概率) {
          const 失败原因 = 提取行为防反失败原因(候选, 反应动作, 防反者, 目标);
          const 反击动作名 = String(候选.sourceActionName || 候选.sourceActionType || '系统反击').trim() || '系统反击';
          const 被反制动作名 = String(候选.原动作?.skill?.name || 候选.原动作?.skill?.魂技名 || 原动作?.skill?.name || 原动作?.skill?.魂技名 || 原动作?.action_type || 原动作?.type || '').trim();
          const 展示失败原因 = 失败原因 || '未形成稳定反打条件';
          写入战斗事件账本(战斗数据, {
            eventKind: 'counter',
            round: Number(战斗数据?.回合 || 0),
            actorName: 防反者?.name || 防反者?.名称 || '',
            targetName: 目标?.name || 目标?.名称 || '',
            actionName: 反击动作名,
            actionType: 'counter',
            sourceActionName: 被反制动作名,
            result: 'fail',
            failReason: 展示失败原因,
            meta: { probability: 概率文本, counterDepth: 当前防反深度 },
          });
          return `[防反错失] ${防反者.name || '防守方'}尝试以[${反击动作名}]反打，但${展示失败原因}${失败原因 ? '' : `（概率:${概率文本}）`}。`;
        }

        const 反应槽校验 = 战斗单位可消耗反应槽(战斗数据, 防反者);
        if (!反应槽校验.ok) {
          const 被反制动作名 = String(候选.原动作?.skill?.name || 候选.原动作?.skill?.魂技名 || 原动作?.skill?.name || 原动作?.skill?.魂技名 || 原动作?.action_type || 原动作?.type || '').trim();
          写入战斗事件账本(战斗数据, {
            eventKind: 'counter',
            round: Number(战斗数据?.回合 || 0),
            actorName: 防反者?.name || 防反者?.名称 || '',
            targetName: 目标?.name || 目标?.名称 || '',
            actionName: 候选.sourceActionName || 候选.防反类型 || '防反',
            actionType: 'counter',
            sourceActionName: 被反制动作名,
            result: 'fail',
            failReason: 反应槽校验.reason === 'FACTION_REACTION_LIMIT' ? '本阵营反应池已耗尽' : '本回合已经完成过反应',
            meta: {
              probability: 概率文本,
              counterDepth: 当前防反深度,
              reasonCode: 反应槽校验.reason,
              reactionType: 候选.防反类型,
            },
          });
          return `[防反错失] ${防反者.name || '防守方'}抓到窗口，但反应槽已耗尽，未能继续反打。`;
        }
        消耗战斗反应槽(战斗数据, 防反者, 候选.防反类型 || 'counter');
        const 防反动作 = 建立行为防反动作(防反者, { ...候选, 触发概率, counterDepth: 当前防反深度 });
        const 被反制动作名 = String(候选.原动作?.skill?.name || 候选.原动作?.skill?.魂技名 || 原动作?.skill?.name || 原动作?.skill?.魂技名 || 原动作?.action_type || 原动作?.type || '').trim();
        const 防反名预览 = String(防反动作?.skill?.name || 防反动作?.skill?.魂技名 || 候选.sourceActionName || (允许以命换伤 ? '以命换伤' : 候选.防反类型)).trim();
        const 防反窗口事件 = 写入战斗事件账本(战斗数据, {
          eventKind: 'counter_window',
          round: Number(战斗数据?.回合 || 0),
          actorName: 防反者?.name || 防反者?.名称 || '',
          targetName: 目标?.name || 目标?.名称 || '',
          actionName: 防反名预览,
          actionType: 'counter_window',
          sourceActionName: 被反制动作名,
          result: 'opened',
          meta: { probability: 概率文本, counterDepth: 当前防反深度, reasonCode: 'COUNTER_WINDOW_OPENED' },
        });
        const 防反战斗数据 = {
          战斗类型: 战斗数据?.战斗类型 || '突发遭遇',
          回合: Number(战斗数据?.回合 || 0),
          先攻: 防反者.name || '防反方',
          参战者: {
            team_player: [防反者],
            team_enemy: [目标],
          },
        };
        const 原降低闪避 = 目标.temp_dodge_penalty;
        const 原锁定强度 = 目标.temp_lock_level;
        try {
          const 出手承诺 = Number(候选.出手承诺 || 0);
          const 原攻击类型 = 规范化战斗伤害类型(getPrimaryDamageEffect(原动作?.skill || {})?.伤害类型, 原动作?.skill || {});
          const 是闪避反击 = 候选.防反类型 === '完美闪避';
          const 防反压制 = 计算行为防反压制倍率(候选.防反类型, 原攻击类型);
          const 反应削弱倍率 = 防反压制.反应削弱倍率;
          const 闪避削弱倍率 = 防反压制.闪避削弱倍率;
          目标.temp_dodge_penalty = Number(目标.temp_dodge_penalty || 0) + 出手承诺 * 闪避削弱倍率;
          目标.temp_lock_level = Number(目标.temp_lock_level || 0) + Math.round(出手承诺 * 防反压制.锁定倍率);
          const 基础反应余量 = calculateReactionRatio(防反者, 目标, 防反动作, 防反战斗数据);
          const 出手反应折损 = 限制行为概率(1 - 出手承诺 * 反应削弱倍率, 是闪避反击 ? 0.46 : 0.28, 是闪避反击 ? 0.96 : 0.88);
          const 二次反应余量 = 基础反应余量 * 出手反应折损;
          const 二次反应动作 = 目标.temp_cannot_react
            ? { type: '无法反应', log: `${目标.name || '攻击方'}出手已满，二次反应受限。`, skill: null, def_mult: 1.0 }
            : determineNpcAction(防反战斗数据, 防反动作, 二次反应余量);
          const 防反起手事件 = 写入战斗事件账本(防反战斗数据, {
            eventKind: 'action_start',
            round: Number(战斗数据?.回合 || 0),
            actorName: 防反者?.name || 防反者?.名称 || '',
            targetName: 目标?.name || 目标?.名称 || '',
            actionName: 防反名预览,
            finalActionName: 防反名预览,
            actionType: 'counter',
            sourceActionName: 被反制动作名,
            parentNodeId: 防反窗口事件?.chainNodeId || '',
            sourceNodeId: 防反窗口事件?.chainNodeId || '',
            result: 'started',
            meta: {
              source: 'counter_action_start',
              counterDepth: 当前防反深度,
              counterWindowEventId: 防反窗口事件?.eventId || '',
              counterWindowNodeId: 防反窗口事件?.chainNodeId || '',
              reasonCode: 'COUNTER_WINDOW_OPENED',
            },
          });
          const 防反结果 = executeClash(
            防反动作,
            二次反应动作,
            防反战斗数据,
          );
          const 防反伤害包 = applyResolvedDamagePackage(防反者, 防反动作, 防反结果, {
            primaryTarget: 目标,
            combatData: 防反战斗数据,
          });
          const 防反结算事件 = [...(防反战斗数据.__battleEventLedger || [])].reverse().find(event =>
            String(event?.eventKind || '').trim() === 'hit_result' &&
            isSameBattleReportName(event?.actorName || '', 防反者?.name || 防反者?.名称 || '') &&
            isSameBattleReportName(event?.targetName || '', 目标?.name || 目标?.名称 || '')
          );
          const 防反结算轨迹 = Array.isArray(防反结算事件?.meta?.settlementTrace)
            ? 防反结算事件.meta.settlementTrace
                .map(item => item && typeof item === 'object' ? {
                  key: String(item.key || '').trim(),
                  label: String(item.label || '').trim(),
                  value: item.value,
                } : null)
                .filter(item => item && item.key)
            : [];
          const 伤害文本 = 防反伤害包.primaryAppliedDamage > 0 ? `，造成${防反伤害包.primaryAppliedDamage}点反击伤害` : '';
          const 反应文本 = 二次反应动作?.log ? ` ${二次反应动作.log}` : '';
          const 防反名 = 防反名预览;
          const 防反事件 = 写入战斗事件账本(战斗数据, {
            eventKind: 'counter',
            round: Number(战斗数据?.回合 || 0),
            actorName: 防反者?.name || 防反者?.名称 || '',
            targetName: 目标?.name || 目标?.名称 || '',
            actionName: 防反名,
            actionType: 'counter',
            sourceActionName: 被反制动作名,
            parentNodeId: 防反窗口事件?.chainNodeId || '',
            sourceNodeId: 防反窗口事件?.chainNodeId || '',
            result: 'success',
            meta: {
              damage: Number(防反伤害包.primaryAppliedDamage || 0),
              probability: 概率文本,
              counterDepth: 当前防反深度,
              counterActionStartEventId: 防反起手事件?.eventId || '',
              counterActionStartNodeId: 防反起手事件?.chainNodeId || '',
              counterWindowEventId: 防反窗口事件?.eventId || '',
              counterWindowNodeId: 防反窗口事件?.chainNodeId || '',
              settlementTrace: [
                { key: 'sourceAction', label: '来源动作', value: 防反名 },
                { key: 'counteredAction', label: '被反制动作', value: 被反制动作名 },
                { key: 'attacker', label: '反击方', value: 防反者?.name || 防反者?.名称 || '' },
                { key: 'target', label: '目标', value: 目标?.name || 目标?.名称 || '' },
                { key: 'result', label: '反击结果', value: 'success' },
                { key: 'counterDepth', label: '防反层级', value: 当前防反深度 },
                { key: 'counterProbability', label: '防反概率', value: 概率文本 },
                { key: 'secondaryReactionMargin', label: '二次反应余量', value: Number(Number(二次反应余量 || 0).toFixed(3)) },
                ...防反结算轨迹.filter(item => !['sourceAction', 'counteredAction', 'attacker', 'target', 'result', 'failureReason', 'counterDepth'].includes(item.key)),
              ],
            },
          });
          if (防反事件?.chainNodeId) {
            const 二次反应名 = normalizeBattleActionDisplayName(二次反应动作?.type || 二次反应动作?.action_type || '无法反应');
            const 二次反应事件类型 = /闪避|撤离|规避/.test(二次反应名)
              ? 'dodge'
              : /硬抗|防御|守势|兜底|承伤/.test(二次反应名)
                ? 'defend'
                : 'pass';
            const 防反命中结果 = String(防反结算事件?.result || 防反结果?.result || '').trim();
            const 防反已规避 = /miss|evade|dodge|未命中|闪避/.test(防反命中结果) || Number(防反伤害包.primaryAppliedDamage || 0) <= 0;
            写入战斗事件账本(战斗数据, {
              eventKind: 二次反应事件类型,
              round: Number(战斗数据?.回合 || 0),
              actorName: 目标?.name || 目标?.名称 || '',
              targetName: 防反者?.name || 防反者?.名称 || '',
              actionName: 二次反应名,
              actionType: 'counter_secondary_reaction',
              sourceActionName: 防反名,
              sourceActionId: 防反事件.actionId || '',
              parentNodeId: 防反事件.chainNodeId || '',
              sourceNodeId: 防反事件.chainNodeId || '',
              result: 二次反应事件类型 === 'dodge'
                ? (防反已规避 ? 'evaded' : 'failed')
                : (二次反应事件类型 === 'defend' ? (防反已规避 ? 'guarded' : 'guarded_hit') : 'failed'),
              meta: {
                source: 'counter_secondary_reaction',
                counterActionEventId: 防反事件.eventId || '',
                counterActionNodeId: 防反事件.chainNodeId || '',
                counterDepth: 当前防反深度,
                reactionMargin: Number(Number(二次反应余量 || 0).toFixed(3)),
                reactionPenaltyScale: Number(Number(出手反应折损 || 0).toFixed(3)),
                damageAfterReaction: Number(防反伤害包.primaryAppliedDamage || 0),
              },
            });
          }
          return `[行为防反] ${防反者.name || '防守方'}凭${防反名}抓住${目标.name || '攻击方'}出手后的空门${伤害文本}(概率:${概率文本}，二次反应:${Math.round(二次反应余量 * 100)}%)。${反应文本} ${防反结果.desc || ''}${防反伤害包.log ? ` ${防反伤害包.log}` : ''}`;
        } finally {
          if (原降低闪避 === undefined) delete 目标.temp_dodge_penalty;
          else 目标.temp_dodge_penalty = 原降低闪避;
          if (原锁定强度 === undefined) delete 目标.temp_lock_level;
          else 目标.temp_lock_level = 原锁定强度;
        }
      }

      function 计算行为防反压制倍率(防反类型 = '', 原攻击类型 = '') {
        const 是闪避反击 = String(防反类型 || '').trim() === '完美闪避';
        return {
          反应削弱倍率: 是闪避反击 ? 0.52 : 0.82 + (原攻击类型 === '近身攻击' ? 0.18 : 0),
          闪避削弱倍率: 是闪避反击 ? 0.28 : 0.52 + (原攻击类型 === '近身攻击' ? 0.16 : 0),
          锁定倍率: 是闪避反击 ? 3 : 5,
        };
      }

      function isCombatUnitAlive(char) {
        return getCombatHpValue(char) > 0;
      }

      function isCombatUnitAbleToFight(char) {
        return isCombatUnitAlive(char) && getCombatStaminaValue(char) > 0;
      }

      function syncCombatActionState(char) {
        if (!char || typeof char !== 'object') return;
        同步战斗资源镜像字段(char);
        if (char.召唤键) 同步召唤单位镜像(char);
      }

    function 确保战斗运行态(combatData = {}) {
      const rootData = combatData?.__父级战斗数据 || combatData;
      if (!rootData || typeof rootData !== 'object') return {};
      if (!rootData.__battleRuntime || typeof rootData.__battleRuntime !== 'object') {
        Object.defineProperty(rootData, '__battleRuntime', {
          enumerable: false,
          configurable: true,
          writable: true,
          value: {},
        });
      }
      const runtime = rootData.__battleRuntime;
      if (!runtime.unitReactionCount || typeof runtime.unitReactionCount !== 'object') runtime.unitReactionCount = {};
      if (!runtime.factionReactionCount || typeof runtime.factionReactionCount !== 'object') runtime.factionReactionCount = {};
      if (!runtime.counterCount || typeof runtime.counterCount !== 'object') runtime.counterCount = {};
      if (!runtime.reactionFatigue || typeof runtime.reactionFatigue !== 'object') runtime.reactionFatigue = {};
      return runtime;
    }

    function 读取战斗单位运行态键(unit = {}, fallback = '') {
      return String(unit?.charKey || unit?.char_key || unit?.key || unit?.name || unit?.名称 || fallback || '').trim();
    }

    function 读取战斗单位阵营名(combatData = {}, unit = {}) {
      const name = 读取战斗单位运行态键(unit);
      if (读取战斗阵营单位列表(combatData, '玩家').some(item => item === unit || isCombatUnitIdentityMatch(item, name))) return 'player';
      if (读取战斗阵营单位列表(combatData, '敌方').some(item => item === unit || isCombatUnitIdentityMatch(item, name))) return 'enemy';
      return String(unit?.阵营 || unit?.side || '').trim() || 'unknown';
    }

    function 战斗单位可消耗反应槽(combatData = {}, unit = {}) {
      const runtime = 确保战斗运行态(combatData);
      const unitKey = 读取战斗单位运行态键(unit);
      if (!unitKey) return { ok: false, reason: 'UNKNOWN_UNIT' };
      const faction = 读取战斗单位阵营名(combatData, unit);
      const factionLimit = Math.max(1, Number(combatData?.__battleReactionFactionLimit || 3));
      if (Number(runtime.unitReactionCount[unitKey] || 0) >= 1) return { ok: false, reason: 'UNIT_REACTION_LIMIT' };
      if (Number(runtime.factionReactionCount[faction] || 0) >= factionLimit) return { ok: false, reason: 'FACTION_REACTION_LIMIT' };
      return { ok: true, unitKey, faction };
    }

    function 消耗战斗反应槽(combatData = {}, unit = {}, reason = 'counter') {
      const check = 战斗单位可消耗反应槽(combatData, unit);
      if (!check.ok) return check;
      const runtime = 确保战斗运行态(combatData);
      runtime.unitReactionCount[check.unitKey] = Number(runtime.unitReactionCount[check.unitKey] || 0) + 1;
      runtime.counterCount[check.unitKey] = Number(runtime.counterCount[check.unitKey] || 0) + 1;
      runtime.factionReactionCount[check.faction] = Number(runtime.factionReactionCount[check.faction] || 0) + 1;
      runtime.reactionFatigue[check.unitKey] = {
        round: Number(combatData?.回合 || 0),
        reason,
        clearAt: 'round_end',
      };
      if (!unit.__battleRuntime || typeof unit.__battleRuntime !== 'object') unit.__battleRuntime = {};
      unit.__battleRuntime.reactedCount = runtime.unitReactionCount[check.unitKey];
      unit.__battleRuntime.counterCount = runtime.counterCount[check.unitKey];
      unit.__battleRuntime.reactionFatigue = true;
      return check;
    }

    function BattleDirectorRoundStart(combatData = {}) {
      const runtime = 确保战斗运行态(combatData);
      runtime.unitReactionCount = {};
      runtime.factionReactionCount = {};
      runtime.counterCount = {};
      const 单位列表 = dedupeCombatTargetList([
        ...读取战斗阵营单位列表(combatData, '玩家'),
        ...读取战斗阵营单位列表(combatData, '敌方'),
      ]).filter(Boolean);
      单位列表.forEach(单位 => {
        const key = 读取战斗单位运行态键(单位);
        if (单位.__battleRuntime && typeof 单位.__battleRuntime === 'object') {
          if (单位.__battleRuntime.reactionFatigue) {
            写入战斗事件账本(combatData, {
              eventKind: 'runtime_trace',
              round: Number(combatData?.回合 || 0),
              actorName: 单位?.name || 单位?.名称 || '',
              actionName: 'clear_fatigue',
              result: 'cleared',
              primaryOutcome: 'no_effect',
              meta: { traceType: 'clear_fatigue', unitKey: key },
            });
          }
          delete 单位.__battleRuntime.reactedCount;
          delete 单位.__battleRuntime.counterCount;
          delete 单位.__battleRuntime.reactionFatigue;
        }
      });
      runtime.reactionFatigue = {};
      runtime.lastRoundStart = Number(combatData?.回合 || 0);
    }

    function 查找战斗运行态单位(combatData = {}, name = '') {
      const wanted = String(name || '').trim();
      if (!wanted) return null;
      const units = dedupeCombatTargetList([
        ...读取战斗阵营单位列表(combatData, '玩家'),
        ...读取战斗阵营单位列表(combatData, '敌方'),
        ...Object.values(确保召唤单位表(combatData) || {}),
      ]).filter(Boolean);
      return units.find(unit => isCombatUnitIdentityMatch(unit, wanted)) || null;
    }

    function 补水战斗运行态(combatData = {}, eventLedger = null, options = {}) {
      const rootData = combatData?.__父级战斗数据 || combatData;
      if (!rootData || typeof rootData !== 'object') return {};
      const ledger = Array.isArray(eventLedger) ? eventLedger : (Array.isArray(rootData.__battleEventLedger) ? rootData.__battleEventLedger : []);
      const runtime = 确保战斗运行态(rootData);
      const currentRound = Number(rootData?.回合 || 0);
      runtime.unitReactionCount = {};
      runtime.factionReactionCount = {};
      runtime.counterCount = {};
      runtime.reactionFatigue = {};
      runtime.fieldCreationCount = {};
      runtime.inventoryCreationCount = {};
      ledger.forEach(event => {
        if (!event || typeof event !== 'object') return;
        const kind = String(event.eventKind || '').trim();
        const round = Number(event.round || event.sourceRound || 0);
        const actorName = String(event.actorName || '').trim();
        const actor = 查找战斗运行态单位(rootData, actorName);
        const unitKey = 读取战斗单位运行态键(actor || {}, actorName);
        if (['counter', 'defend', 'dodge', 'pass', 'summon_assist'].includes(kind) && round === currentRound && unitKey) {
          const source = String(event.meta?.source || event.actionType || '').trim();
          if (kind === 'counter' || kind === 'summon_assist' || /reaction|counter|应招|防反/.test(source)) {
            const faction = 读取战斗单位阵营名(rootData, actor || { name: actorName, 阵营: event.actorSide });
            runtime.unitReactionCount[unitKey] = Math.max(Number(runtime.unitReactionCount[unitKey] || 0), 1);
            runtime.counterCount[unitKey] = Math.max(Number(runtime.counterCount[unitKey] || 0), kind === 'counter' || kind === 'summon_assist' ? 1 : 0);
            runtime.factionReactionCount[faction] = Number(runtime.factionReactionCount[faction] || 0) + 1;
            runtime.reactionFatigue[unitKey] = { round, reason: kind, clearAt: 'round_end', hydrated: true };
            if (actor && typeof actor === 'object') {
              if (!actor.__battleRuntime || typeof actor.__battleRuntime !== 'object') actor.__battleRuntime = {};
              actor.__battleRuntime.reactedCount = runtime.unitReactionCount[unitKey];
              actor.__battleRuntime.counterCount = runtime.counterCount[unitKey];
              actor.__battleRuntime.reactionFatigue = true;
            }
          }
        }
        if (kind === 'summon_create' || kind === 'create') {
          const createdName = String(event.summonName || event.createdName || event.meta?.summonName || event.meta?.createdName || event.meta?.itemName || event.actionName || '').trim();
          if (!createdName) return;
          const bucket = kind === 'summon_create' ? runtime.fieldCreationCount : runtime.inventoryCreationCount;
          bucket[createdName] = Number(bucket[createdName] || 0) + Math.max(1, Number(event.meta?.count || event.count || 1));
        }
      });
      runtime.llmBattleSummary = 构建LLM战斗语义摘要(ledger, { combatData: rootData }, { maxRounds: 3 });
      runtime.hydratedFromLedger = true;
      runtime.hydratedAtRound = currentRound;
      runtime.hydratedEventCount = ledger.length;
      runtime.hydratedSource = String(options?.source || 'battle_resume').trim();
      return runtime;
    }

    function 判定事件是保底资源恢复入口(event = {}) {
      const kind = String(event.eventKind || '').trim();
      if (!['blocked_action', 'failed_action', 'target_fail'].includes(kind)) return false;
      const action = normalizeBattleActionDisplayName(event.finalActionName || event.actionName || event.meta?.finalActionName || event.meta?.actionName || '');
      const reason = String(event.failReason || event.failureReason || event.meta?.failureReason || event.meta?.reasonText || '').trim();
      const source = String(event.meta?.source || event.source || '').trim();
      if (!/战术待机|待机|观察|守势维持|守势对峙|收招转防|防御|承伤硬抗|肉体兜底/.test(action)) return false;
      if (!/no_effective_opening|NO_EFFECTIVE_OPENING|未形成主动结算效果|没有形成主动结算效果|稳住身位|观察战局/.test(`${reason} ${event.reasonCode || event.meta?.reasonCode || ''}`)) return false;
      return /auto_actor|ai_fallback|fallback|internal|system/i.test(source) || String(event.actorSide || '').trim() === 'player';
    }

    function 写入保底资源恢复事实(combatData = {}, sourceEvent = {}) {
      const rootData = combatData?.__父级战斗数据 || combatData;
      const ledger = Array.isArray(rootData?.__battleEventLedger) ? rootData.__battleEventLedger : [];
      const sourceEventId = String(sourceEvent.eventId || '').trim();
      if (!rootData || !sourceEventId || ledger.some(event => String(event?.meta?.fallbackSourceEventId || '').trim() === sourceEventId)) return null;
      const actor = 查找战斗运行态单位(rootData, sourceEvent.actorName);
      if (!actor) return null;
      const stats = actor.属性 || actor;
      const maxSp = Math.max(0, Number(stats.sp_max ?? stats.魂力上限 ?? actor.sp_max ?? actor.魂力上限 ?? 0));
      const curSp = Math.max(0, Number(stats.sp ?? stats.魂力 ?? actor.sp ?? actor.魂力 ?? 0));
      const maxVit = Math.max(0, Number(stats.vit_max ?? stats.体力上限 ?? actor.vit_max ?? actor.体力上限 ?? 0));
      const curVit = Math.max(0, Number(stats.sta ?? stats.体力 ?? stats.vit ?? actor.sta ?? actor.体力 ?? actor.vit ?? 0));
      const spGain = maxSp > curSp ? Math.min(15, Math.max(1, Math.round(maxSp * 0.04)), Math.round(maxSp - curSp)) : 0;
      const vitGain = maxVit > curVit ? Math.min(15, Math.max(1, Math.round(maxVit * 0.04)), Math.round(maxVit - curVit)) : 0;
      const resourceKey = spGain > 0 ? 'sp' : (vitGain > 0 ? 'vit' : '');
      const amount = resourceKey === 'sp' ? spGain : vitGain;
      if (!(amount > 0)) return null;
      if (resourceKey === 'sp') 设置战斗延迟效果资源值(actor, 'sp', curSp + amount);
      else 设置战斗体力值(actor, curVit + amount);
      return 写入战斗事件账本(rootData, {
        eventKind: 'resource_change',
        round: Number(sourceEvent.round || rootData?.回合 || 0),
        actorName: sourceEvent.actorName,
        targetName: sourceEvent.actorName,
        actionName: sourceEvent.actionName || sourceEvent.finalActionName || '收招回稳',
        actionType: 'resource_change',
        sourceActionName: sourceEvent.actionName || sourceEvent.finalActionName || '',
        sourceActionId: sourceEvent.actionId || sourceEvent.sourceActionId || '',
        sourceRound: Number(sourceEvent.round || rootData?.回合 || 0),
        result: 'gain',
        primaryOutcome: 'resource_recovered',
        meta: {
          source: 'fallback_resource_recovery',
          fallbackSourceEventId: sourceEventId,
          resourceKey,
          resource: resourceKey === 'sp' ? '魂力' : '体力',
          amount,
          delta: amount,
          reasonCode: 'NO_EFFECTIVE_OPENING',
          reasonText: '绝对保底动作回稳资源，避免资源枯竭后无限空过',
        },
      });
    }

    function 清理本回合多威胁运行态(combatData = {}) {
      BattleDirectorRoundStart(combatData);
      if (combatData && typeof combatData === 'object') delete combatData.__队伍临时意图;
      const 单位列表 = dedupeCombatTargetList([
        ...读取战斗阵营单位列表(combatData, '玩家'),
        ...读取战斗阵营单位列表(combatData, '敌方'),
      ]).filter(Boolean);
      单位列表.forEach(单位 => {
        delete 单位.__本回合闪避成功次数;
        delete 单位.__本回合反应预算;
        delete 单位.__本回合对轰次数;
        delete 单位.__本回合防御承压池;
        delete 单位.__本回合对轰覆盖池;
        delete 单位.__本回合防御池剩余;
      });
    }

    function 构建时光回溯回合快照(单位 = {}) {
      if (!单位 || typeof 单位 !== 'object') return null;
      return {
        HP: getCombatHpValue(单位),
        体力: getCombatStaminaValue(单位),
        魂力: 读取战斗资源当前值(单位, 单位.final || {}, 'sp'),
        精神力: 读取战斗资源当前值(单位, 单位.final || {}, 'men'),
        蓄力技能: 单位.蓄力技能 ? deepClonePlain(单位.蓄力技能) : null,
        cast_time: Number(单位.cast_time || 0),
        cast_time_left: Number(单位.cast_time_left || 0),
        蓄力剩余: Number(单位.蓄力剩余 || 0),
        _current_cast_time: Number(单位._current_cast_time || 0),
        action_declared: 单位.action_declared === true,
        is_controlled: 单位.is_controlled === true,
        __技能限制运行态: deepClonePlain(单位.__技能限制运行态 || {}),
      };
    }

    function 记录时光回溯回合快照(单位 = {}) {
      if (!单位 || typeof 单位 !== 'object') return null;
      const 快照 = 构建时光回溯回合快照(单位);
      if (!快照) return null;
      单位.__时光回溯回合快照 = 快照;
      return 快照;
    }

    function 记录战斗上下文时光回溯快照(combatData = {}) {
      const 单位列表 = dedupeCombatTargetList([
        ...((combatData?.参战者?.team_player || []).filter(Boolean)),
        ...((combatData?.参战者?.team_enemy || []).filter(Boolean)),
      ]).filter(Boolean);
      单位列表.forEach(记录时光回溯回合快照);
    }

    function 恢复时光回溯回合快照(单位 = {}) {
      const 快照 = 单位?.__时光回溯回合快照;
      if (!单位 || !快照 || typeof 快照 !== 'object') return false;
      设置战斗血量值(单位, 快照.HP);
      设置战斗体力值(单位, 快照.体力);
      if ('sp' in 单位 || Object.prototype.hasOwnProperty.call(单位, 'sp')) 单位.sp = Math.max(0, Number(快照.魂力 || 0));
      else 单位.魂力 = Math.max(0, Number(快照.魂力 || 0));
      if ('men' in 单位 || Object.prototype.hasOwnProperty.call(单位, 'men')) 单位.men = Math.max(0, Number(快照.精神力 || 0));
      else 单位.精神力 = Math.max(0, Number(快照.精神力 || 0));
      if (单位.属性 && typeof 单位.属性 === 'object') {
        单位.属性.魂力 = Math.max(0, Number(快照.魂力 || 0));
        单位.属性.精神力 = Math.max(0, Number(快照.精神力 || 0));
      }
      单位.蓄力技能 = 快照.蓄力技能 ? deepClonePlain(快照.蓄力技能) : null;
      单位.cast_time = Number(快照.cast_time || 0);
      单位.cast_time_left = Number(快照.cast_time_left || 0);
      单位.蓄力剩余 = Number(快照.蓄力剩余 || 0);
      单位._current_cast_time = Number(快照._current_cast_time || 0);
      单位.action_declared = 快照.action_declared === true;
      单位.is_controlled = 快照.is_controlled === true;
      单位.__技能限制运行态 = deepClonePlain(快照.__技能限制运行态 || {});
      重算时光回溯行动派生值(单位);
      syncCombatActionState(单位);
      return true;
    }

    function 重算时光回溯行动派生值(单位 = {}) {
      if (!单位 || typeof 单位 !== 'object') return;
      if (!单位.状态 || typeof 单位.状态 !== 'object') 单位.状态 = {};
      const 蓄力剩余 = Math.max(0, Number(单位.蓄力剩余 || 单位.cast_time_left || 单位._current_cast_time || 0));
      单位.cast_time_left = 蓄力剩余;
      单位._current_cast_time = 蓄力剩余;
      const 有蓄力技能 = !!单位.蓄力技能;
      单位.action_declared = 有蓄力技能 ? true : 单位.action_declared === true;
      单位.is_controlled = 单位.is_controlled === true;
      单位.状态.蓄力 = 有蓄力技能 && 蓄力剩余 > 0 ? '蓄力中' : '无';
      单位.状态.受控 = 单位.is_controlled ? '受控' : '正常';
    }

    function 查找时光回溯状态条目(单位 = {}) {
      if (!单位?.状态效果 || typeof 单位.状态效果 !== 'object') return null;
      const 键名 = Object.keys(单位.状态效果).find(键 => {
        const 状态 = 单位.状态效果?.[键];
        const 特殊机制标识 = String(状态?.特殊机制标识 || '').trim();
        return (特殊机制标识.includes('时光回溯') || String(键 || '').includes('时光回溯')) && String(状态?.发动方式 || '被动').trim() === '被动';
      });
      if (!键名) return null;
      return { 键名, 状态: 单位.状态效果[键名] };
    }

    function 解析时光回溯发动方式(effect = {}) {
      const 发动方式 = String(effect?.发动方式 || '').trim();
      return 发动方式 === '主动' ? '主动' : '被动';
    }

    function 写入时光回溯反应压制(目标 = {}, 压制值 = 时光回溯反应压制值, 来源 = '时光回溯') {
      if (!目标) return false;
      if (!目标.状态效果 || typeof 目标.状态效果 !== 'object') 目标.状态效果 = {};
      const 状态写入器 = typeof putConditionWithUniqueKey === 'function'
        ? putConditionWithUniqueKey
        : (角色, 基础键, 状态) => {
            let 键 = 基础键;
            if (!角色.状态效果[键]) {
              角色.状态效果[键] = 状态;
              return 键;
            }
            let 序号 = 1;
            while (角色.状态效果[`${键}·${序号}`]) 序号 += 1;
            键 = `${键}·${序号}`;
            角色.状态效果[键] = 状态;
            return 键;
          };
      状态写入器(目标, '时光回溯反制', {
        类型: 'debuff',
        层数: 1,
        描述: `受到${来源}的时序压制`,
        duration: 1,
        特殊机制标识: '时光回溯/反应压制',
        面板修改比例: { str: 1.0, def: 1.0, agi: 1.0, sp_max: 1.0 },
        战斗效果: {
          ...createEmptyCombatEffectMap(),
          reaction_penalty: Math.max(0, Math.min(0.95, Number(压制值 || 0))),
        },
      });
      syncCombatActionState(目标);
      return true;
    }

    function 计算时光回溯规避伤害(entry = {}) {
      const 段伤害总和 = Array.isArray(entry?.damageSegments)
        ? entry.damageSegments.reduce((总和, 段伤害) => 总和 + Math.max(0, Number(段伤害 || 0)), 0)
        : 0;
      return Math.max(0, Number(entry?.damage || 0), 段伤害总和);
    }

    function 计算时光回溯触发消耗系数(单位 = {}, entry = {}) {
      const 规避伤害 = 计算时光回溯规避伤害(entry);
      if (!(规避伤害 > 0)) return 1;
      const 当前生命 = Math.max(1, getCombatHpValue(单位));
      const 最大生命 = Math.max(1, getCombatHpMaxValue(单位));
      const 致死 = 规避伤害 >= 当前生命;
      const 系数 = 1 + (规避伤害 / 当前生命) * 1.15 + (规避伤害 / 最大生命) * 0.65 + (致死 ? 0.75 : 0);
      return Math.max(1, Math.min(4, Number(系数.toFixed(2))));
    }

    function 放大时光回溯触发消耗(parsed = {}, 单位 = {}, entry = {}) {
      const 系数 = 计算时光回溯触发消耗系数(单位, entry);
      if (!(系数 > 1)) return { ...parsed, 时光回溯触发消耗系数: 1 };
      const 放大值 = 原值 => {
        const 数值 = Math.max(0, Number(原值 || 0));
        return 数值 > 0 ? Math.max(1, Math.ceil(数值 * 系数)) : 0;
      };
      const stats = 单位?.属性 || 单位 || {};
      const reqSp = 放大值(parsed.reqSp);
      const reqVit = 放大值(parsed.reqVit);
      const reqMen = 放大值(parsed.reqMen);
      const 可释放 =
        Number(stats.sp ?? stats.魂力 ?? 0) >= reqSp &&
        Number(stats.sta ?? stats.体力 ?? stats.vit ?? 0) >= reqVit &&
        Number(stats.men ?? stats.精神力 ?? 0) >= reqMen;
      return {
        ...parsed,
        reqSp,
        reqVit,
        reqMen,
        canCast: parsed.canCast !== false && 可释放,
        failureReason: parsed.canCast === false
          ? parsed.failureReason
          : 可释放
            ? ''
            : '回溯触发代价过高',
        时光回溯触发消耗系数: 系数,
      };
    }

    function 计算时光回溯专属消耗(skill = {}, 单位 = {}, effect = {}, entry = {}) {
      void effect;
      const 消耗片段 = splitSkillCostModes(getSkillCostText(skill));
      const parsed = parseSkillCostForChar({ ...skill, 消耗: 消耗片段.upfront || '无' }, 单位, {
        actor: 单位,
        caster: 单位,
        attacker: 单位,
        target: 单位,
        defender: 单位,
        skill,
        当前行动: String(skill?.name || skill?.魂技名 || '时光回溯').trim(),
      });
      return 放大时光回溯触发消耗(parsed, 单位, entry);
    }

    function 扣除时光回溯专属消耗(skill = {}, 单位 = {}, effect = {}, entry = {}) {
      const parsed = 计算时光回溯专属消耗(skill, 单位, effect, entry);
      if (!parsed.canCast) return { 成功: false, 日志: parsed.failureReason || '状态不足', 消耗: parsed };
      deductParsedCostFromUnit(单位, parsed);
      const 系数 = Number(parsed.时光回溯触发消耗系数 || 1);
      const 系数文本 = 系数 > 1.01 ? `（规避伤害触发x${系数.toFixed(2)}）` : '';
      return { 成功: true, 日志: `${formatParsedCost(parsed)}${系数文本}`, 消耗: parsed };
    }

    function 解析时光回溯目标文本(effect = {}) {
      const 目标文本 = String(effect?.目标 || '').trim();
      if (目标文本 === '全场') return '全场';
      if (目标文本 === '群体') return '群体';
      return '单体';
    }

    function resolveTimeRewindTargetContext(skill, attacker, defender, combatData, effect = null) {
      const alliedUnits = dedupeCombatTargetList([
        attacker,
        ...((combatData?.参战者?.team_player || []).filter(Boolean)),
      ]).filter(isCombatUnitAlive);
      const hostileUnits = dedupeCombatTargetList([
        defender,
        ...((combatData?.参战者?.team_enemy || []).filter(Boolean)),
      ]).filter(isCombatUnitAlive);
      const allUnits = dedupeCombatTargetList([...alliedUnits, ...hostileUnits]).filter(isCombatUnitAlive);
      const damageTargetContext = resolveSkillTargetContext(skill, attacker, defender, combatData);
      const 主目标集合 = dedupeCombatTargetList([
        ...(damageTargetContext?.targetSet || []),
        defender,
      ]).filter(isCombatUnitAlive);
      const 目标语义 = 解析时光回溯目标文本(effect);
      let targetSet = [];
      if (目标语义 === '单体') targetSet = dedupeCombatTargetList([attacker, ...主目标集合]).filter(isCombatUnitAlive);
      else if (目标语义 === '群体') targetSet = 主目标集合;
      else if (目标语义 === '全场') targetSet = allUnits;
      else targetSet = 主目标集合;
      return {
        targetKind: 目标语义,
        targetScale: 目标语义 === '全场' ? '全场' : 目标语义 === '群体' ? '群体' : '单体',
        directionId: '',
        directionSemantic: '时光回溯',
        targetModifiers: [],
        resolutionStrategy: 'contextual',
        targetText: 目标语义,
        alliedSet: alliedUnits,
        hostileSet: hostileUnits,
        targetSet,
        primaryTarget: targetSet[0] || null,
      };
    }

    function 目标已被时光回溯规避(result = {}, targetObj = null) {
      if (!targetObj || !Array.isArray(result?.targetResults)) return false;
      return result.targetResults.some(entry =>
        entry?.target &&
        isCombatUnitIdentityMatch(entry.target, targetObj) &&
        entry?.被时光回溯规避 === true,
      );
    }

    function 本次结算存在时光回溯规避(result = {}) {
      return Array.isArray(result?.targetResults) && result.targetResults.some(entry => entry?.被时光回溯规避 === true);
    }

    function 执行主动时光回溯效果(skill = {}, attacker = {}, defender = {}, combatData = {}, context = {}) {
      const 效果列表 = getSkillEffects(skill, context).filter(effect =>
        String(effect?.原型 || '').trim() === '时光回溯' && 解析时光回溯发动方式(effect) === '主动',
      );
      if (!效果列表.length) return '';
      const 日志 = [];
      效果列表.forEach(effect => {
        const 运行效果 = deepClonePlain(effect);
        原型驱动缩放(运行效果, {}, 运行效果, attacker, attacker?.final || {}, defender, defender?.final || {});
        const 概率 = Math.max(0, Math.min(1, Number(运行效果?.时光回溯成功率 ?? 1)));
        if (Math.random() > 概率) {
          日志.push(`[时光回溯] 主动时序压制未能成形。`);
          return;
        }
        const 目标上下文 = resolveTimeRewindTargetContext(skill, attacker, defender, combatData, 运行效果);
        const 压制值 = Math.max(0, Math.min(0.95, Number(运行效果?.时光回溯反应压制值 || 时光回溯反应压制值)));
        const 压制目标 = (目标上下文.targetSet || []).filter(目标 =>
          目标 && !isCombatUnitIdentityMatch(目标, attacker?.name || attacker?.名称 || attacker),
        );
        压制目标.forEach(目标 => 写入时光回溯反应压制(目标, 压制值, '主动时光回溯'));
        if (压制目标.length) 日志.push(`[时光回溯] ${attacker.name || '施术者'}主动抢占时序，压低${压制目标.map(目标 => 目标.name || 目标.名称 || '目标').join('、')}反应。`);
      });
      return 日志.join(' ');
    }

    function 预结算触发时光回溯(attacker = {}, entry = {}, skill = {}) {
      const 目标 = entry?.target;
      if (!目标 || !目标.状态效果) return null;
      const 存在伤害落地 = Number(entry?.damage || 0) > 0 || (Array.isArray(entry?.damageSegments) && entry.damageSegments.some(段伤害 => Number(段伤害 || 0) > 0));
      const 存在效果落地 = Array.isArray(entry?.时光回溯预结算落地) && entry.时光回溯预结算落地.length > 0;
      if (!存在伤害落地 && !存在效果落地)
        return null;
      const 条目 = 查找时光回溯状态条目(目标);
      if (!条目) return null;
      const 概率 = Math.max(0, Math.min(1, Number(条目.状态?.时光回溯成功率 ?? 1)));
      if (Math.random() > 概率) {
        delete 目标.状态效果[条目.键名];
        return `[时光回溯] ${entry.targetName || 目标.name || '目标'}的回溯判定失败，本次结算继续。`;
      }
      const 消耗结果 = 扣除时光回溯专属消耗(skill, 目标, 条目.状态, entry);
      if (!消耗结果.成功) return `[时光回溯] ${entry.targetName || 目标.name || '目标'}${消耗结果.日志 || '资源不足'}，回溯未触发。`;
      if (!恢复时光回溯回合快照(目标)) {
        delete 目标.状态效果[条目.键名];
        return `[时光回溯] ${entry.targetName || 目标.name || '目标'}缺少本回合开始快照，回溯壳消散。`;
      }
      写入时光回溯反应压制(attacker, Number(条目.状态?.时光回溯反应压制值 || 时光回溯反应压制值), '被动时光回溯');
      delete 目标.状态效果[条目.键名];
      entry.damage = 0;
      entry.damageSegments = [];
      entry.被时光回溯规避 = true;
      syncCombatActionState(attacker);
      syncCombatActionState(目标);
      return `[时光回溯] ${entry.targetName || 目标.name || '目标'}自身触发回溯消耗${消耗结果.日志}，在正式结算落地前回到本回合开始战斗态，${attacker.name || '攻击方'}的反应被时序压制。`;
    }

      function getCombatTargetSpeciesFlags(char = {}) {
        const social = char?.社交 && typeof char.社交 === 'object' ? char.社交 : {};
        const factionMap = social?.势力 && typeof social.势力 === 'object' ? social.势力 : {};
        const factionNames = Object.keys(factionMap);
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char || {};
        const age = Math.max(0, Number(stats?.age ?? stats?.年龄 ?? 0));
        const isBeast =
          factionNames.includes('魂兽一族') ||
          factionNames.some(name => String(name || '').includes('魂兽')) ||
          age >= 100;
        const isAbyss =
          factionNames.includes('深渊生物') ||
          factionNames.some(name => String(name || '').includes('深渊'));
        return {
          isBeast,
          isAbyss,
          isBeastOrAbyss: isBeast || isAbyss,
        };
      }

      function isCombatUnitSoulMaster(char = {}) {
        const unitNature = String(char?.单位性质 || '').trim();
        const identity = String(char?.身份 || '').trim();
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char || {};
        return (
          unitNature === '人类' &&
          identity === '魂师' &&
          stats?.普通人 !== true
        );
      }

      function parseAscensionTicketTier(ticket = '') {
        const text = String(ticket || '').trim();
        if (/高级/.test(text)) return '高级';
        if (/中级/.test(text)) return '中级';
        return '初级';
      }

      function isAscensionRiotTicket(ticket = '') {
        return /暴动期/.test(String(ticket || ''));
      }

      function getAscensionTicketAgeRange(ticket = '') {
        const tier = parseAscensionTicketTier(ticket);
        const map = {
          初级: { min: 100, max: 3000 },
          中级: { min: 3000, max: 20000 },
          高级: { min: 20000, max: 100000 },
        };
        return map[tier] || map.初级;
      }

      function getAscensionRiotRewardMultiplier(percent = 0) {
        const safePercent = Math.max(0, Math.min(1, Number(percent) || 0));
        if (safePercent <= 0.1) return 5;
        if (safePercent <= 0.2) return 4;
        if (safePercent <= 0.5) {
          const t = (safePercent - 0.2) / 0.3;
          return 4 - t * 3;
        }
        return 1;
      }

      function isCombatUnitEvilSoulMaster(char = {}) {
        const stats = char?.属性 && typeof char.属性 === 'object' ? char.属性 : char || {};
        const social = char?.社交 && typeof char.社交 === 'object' ? char.社交 : {};
        const factionMap = social?.势力 && typeof social.势力 === 'object' ? social.势力 : {};
        const factionNames = Object.keys(factionMap).map(name => String(name || '').trim());
        const identityTexts = [
          stats?.主身份,
          social?.主身份,
          social?.身份,
          social?.称号 && typeof social.称号 === 'object' ? Object.keys(social.称号).join('/') : '',
          stats?.标签,
          social?.标签,
        ]
          .map(text => String(text || '').trim())
          .filter(Boolean)
          .join('/');
        return (
          stats?.邪魂师 === true ||
          stats?.is_evil === true ||
          stats?.evil === true ||
          /邪魂/.test(identityTexts) ||
          factionNames.some(name => /邪魂|圣灵教|深渊/.test(name))
        );
      }

      function buildFrontEndBattleOutcome(attacker, defender, options = {}) {
        const attackerAlive = isCombatUnitAlive(attacker);
        const defenderAlive = isCombatUnitAlive(defender);
        const attackerAbleToFight = isCombatUnitAbleToFight(attacker);
        const defenderAbleToFight = isCombatUnitAbleToFight(defender);
        const unresolvedReason = String(options.unresolvedReason || '').trim();
        const formalEncounter = options.formalEncounter !== false;
        const outcome = {
          type: '未分胜负',
          label: '对峙未决',
          formalEncounter,
          unresolvedReason,
          attackerAlive,
          defenderAlive,
          attackerAbleToFight,
          defenderAbleToFight,
          isVictory: false,
          isDefeat: false,
          isDraw: false,
          isUnresolved: false,
          isGeneralVictory: false,
          isLethalCandidate: false,
        };

        if (!attackerAbleToFight && !defenderAbleToFight) {
          outcome.type = '两败俱伤';
          outcome.label = attackerAlive || defenderAlive ? '双方同时失能' : '同归于尽';
          outcome.isDraw = true;
          return outcome;
        }

        if (!defenderAbleToFight) {
          outcome.type = defenderAlive ? '一般胜利' : '致命压制待裁断';
          outcome.label = defenderAlive ? '制服取胜' : '致命压制待裁断';
          outcome.isVictory = true;
          outcome.isGeneralVictory = defenderAlive;
          outcome.isLethalCandidate = !defenderAlive;
          return outcome;
        }

        if (!attackerAbleToFight) {
          outcome.type = '失败';
          outcome.label = attackerAlive ? '力竭落败' : '重创败退';
          outcome.isDefeat = true;
          return outcome;
        }

        outcome.isUnresolved = true;
        if (unresolvedReason === 'single_round_limit') {
          outcome.label = '单回合试探';
        } else if (unresolvedReason === 'simulation_stopped') {
          outcome.label = '续推中断';
        } else if (unresolvedReason === 'external_interrupt') {
          outcome.label = '外界中断';
        }
        return outcome;
      }

      // ==========================================
      // 📍 核心结算区：动作博弈与交锋
      // ==========================================
      function calculateReactionRatio(attacker, defender, playerAction, combatData) {
        const atkCombat = attacker?.状态效果
          ? Object.values(attacker.状态效果).map(c => c?.战斗效果 || {})
          : [];
        const totalAtkSpeedBonus = atkCombat.reduce((sum, ce) => sum + Number(ce.attacker_speed_bonus || 0), 0);
        const totalCastSpeedBonus = atkCombat.reduce((sum, ce) => sum + Number(ce.cast_speed_bonus || 0), 0);
        const totalCastSpeedPenalty = atkCombat.reduce((sum, ce) => sum + Number(ce.cast_speed_penalty || 0), 0);

        let castPenalty = Math.min(
          0.9,
          Math.max(
            0,
            playerAction.cast_time / 100 +
              (playerAction?.前摇已结算 === true ? 0 : -totalCastSpeedBonus + totalCastSpeedPenalty),
          ),
        );
        let attackerSpeed = attacker.agi * (1 - castPenalty) + totalAtkSpeedBonus;

        const defenderAgilityMult = Number(defender.temp_agi_mult || 1.0);
        const defenderAgilityComponent = Number(defender.agi || 0) * defenderAgilityMult;
        const defenderMentalComponent = Number(defender.men_max || 0);
        const defenderReactionBonus = Number(defender.temp_reaction_bonus || 0);
        const defenderReactionPenalty = Number(defender.temp_reaction_penalty || 0);
        const reactionBaseBeforeMultiplier =
          defenderAgilityComponent + defenderMentalComponent + defenderReactionBonus - defenderReactionPenalty;
        const maintainReactionPenalty = Number(defender.__维持反应惩罚 || 0);
        const maintainReactionMultiplier = 1 - Math.min(0.25, Math.max(0, maintainReactionPenalty));
        const reactionBudget = Number(defender.__本回合反应预算 || 0);
        const reactionBudgetMultiplier = Math.max(0.2, 1 + Math.min(0, reactionBudget));
        const experienceReactionMultiplier = 计算行为经验反应倍率(defender, attacker, combatData);
        let defenderReaction =
          reactionBaseBeforeMultiplier *
          maintainReactionMultiplier *
          reactionBudgetMultiplier *
          experienceReactionMultiplier;
        let ratio = defenderReaction / Math.max(1, attackerSpeed);

        const cannotReact = defender.temp_cannot_react === true;
        if (cannotReact) ratio = 0;

        const firstStrikePenalty = combatData && combatData.先攻 !== '无' && combatData.先攻 !== defender.name;
        const ratioPostMultiplier = firstStrikePenalty ? 0.5 : 1;
        if (firstStrikePenalty) {
          ratio *= 0.5;
        }

        if (combatData && typeof combatData === 'object') {
          combatData.__lastReactionRatioTrace = {
            reactionRatio: ratio,
            attackerSpeed,
            defenderReaction,
            castPenalty,
            attackerAgility: Number(attacker?.agi || 0),
            defenderAgility: Number(defender?.agi || 0),
            defenderMentalMax: Number(defender?.men_max || 0),
            attackerSpeedBonus: totalAtkSpeedBonus,
            castSpeedBonus: totalCastSpeedBonus,
            castSpeedPenalty: totalCastSpeedPenalty,
            defenderAgilityComponent,
            defenderMentalComponent,
            defenderReactionBonus,
            defenderReactionPenalty,
            defenderAgilityMult,
            reactionBaseBeforeMultiplier,
            maintainReactionPenalty,
            maintainReactionMultiplier,
            reactionBudget,
            reactionBudgetMultiplier,
            experienceReactionMultiplier,
            ratioPostMultiplier,
            cannotReact,
            firstStrikePenalty,
          };
        }
        return ratio;
      }

      function 读取行动前摇分桶(action = {}) {
        const 前摇 = Number(action?.cast_time ?? action?.前摇 ?? action?.skill?.前摇 ?? getSkillCastTime(action?.skill) ?? 10) || 10;
        const 起点 = Math.floor(Math.min(39, Math.max(0, 前摇)) / 10) * 10;
        return `${起点}-${起点 + 9}`;
      }

      function 读取来袭窗口状态(目标 = {}, action = {}) {
        const 分桶 = 读取行动前摇分桶(action);
        const 原始状态 = 目标?.__本回合防御承压池?.[分桶];
        if (原始状态 && typeof 原始状态 === 'object' && !Array.isArray(原始状态)) return { 分桶, ...原始状态 };
        const 数量 = Math.max(1, Math.floor(Number(原始状态 || 1)));
        return { 分桶, 数量 };
      }

      function 读取来袭窗口威胁数(目标 = {}, action = {}) {
        return Math.max(1, Math.floor(Number(读取来袭窗口状态(目标, action).数量 || 1)));
      }

      function 读取技能对轰覆盖数(skill = {}, 当前窗口威胁数 = 1) {
        const 威胁数 = Math.max(1, Math.floor(Number(当前窗口威胁数 || 1)));
        const 目标文本 = String(getSkillTarget(skill) || '').trim();
        if (/全场/.test(目标文本)) return Math.min(12, 威胁数);
        if (/群体/.test(目标文本)) return Math.min(威胁数, Math.max(2, Math.floor(Number(skill?.__覆盖目标数量 || skill?.__辅助目标数量 || 威胁数 || 1))));
        return 1;
      }

      function 消耗来袭窗口对轰覆盖(目标 = {}, action = {}) {
        const 分桶 = 读取行动前摇分桶(action);
        const 覆盖池 = 目标?.__本回合对轰覆盖池;
        const 剩余覆盖 = Math.max(0, Math.floor(Number(覆盖池?.[分桶] || 0)));
        if (!(剩余覆盖 > 0)) return false;
        覆盖池[分桶] = 剩余覆盖 - 1;
        return true;
      }

      function 登记来袭窗口对轰覆盖(目标 = {}, action = {}, 覆盖数量 = 1) {
        const 额外覆盖 = Math.max(0, Math.floor(Number(覆盖数量 || 1)) - 1);
        if (!(额外覆盖 > 0) || !目标) return;
        const 分桶 = 读取行动前摇分桶(action);
        目标.__本回合对轰覆盖池 = 目标.__本回合对轰覆盖池 || {};
        目标.__本回合对轰覆盖池[分桶] = Math.max(0, Math.floor(Number(目标.__本回合对轰覆盖池[分桶] || 0))) + 额外覆盖;
      }

      function 结算来袭窗口防御承压(目标 = {}, action = {}, 防御强度 = 0, 来袭强度 = 0) {
        const 分桶 = 读取行动前摇分桶(action);
        const 威胁数 = 读取来袭窗口威胁数(目标, action);
        目标.__本回合防御池剩余 = 目标.__本回合防御池剩余 || {};
        if (目标.__本回合防御池剩余[分桶] === undefined) {
          目标.__本回合防御池剩余[分桶] = Math.max(0, Number(防御强度 || 0));
        }
        const 剩余防御池 = Math.max(0, Number(目标.__本回合防御池剩余[分桶] || 0));
        const 抵消 = Math.min(剩余防御池, Math.max(0, Number(来袭强度 || 0)));
        目标.__本回合防御池剩余[分桶] = Math.max(0, 剩余防御池 - 抵消);
        return { 分桶, 威胁数, 抵消, 剩余防御池: 目标.__本回合防御池剩余[分桶] };
      }

      function 限制行为概率(数值 = 0, 下限 = 0, 上限 = 1) {
        const 安全数值 = Number(数值);
        if (!Number.isFinite(安全数值)) return 下限;
        return Math.max(下限, Math.min(上限, 安全数值));
      }

      function 读取行为体力比(角色 = {}) {
        const 当前体力 = Number(角色?.sta ?? 角色?.体力 ?? 角色?.属性?.体力 ?? 0);
        const 体力上限 = Math.max(1, Number(角色?.sta_max ?? 角色?.体力上限 ?? 角色?.属性?.体力上限 ?? 1));
        return 限制行为概率(当前体力 / 体力上限, 0, 1);
      }

      function 读取行为规划持续伤害条目(角色 = {}) {
        return Object.entries(角色?.状态效果 || {})
          .map(([状态名, 状态]) => {
            if (!状态 || typeof 状态 !== 'object') return null;
            const 每回合伤害 = 估算单位持续伤害数值(角色, 状态);
            if (!(每回合伤害 > 0)) return null;
            return {
              状态名,
              状态,
              每回合伤害,
              剩余回合: Math.max(0, Number(状态?.duration || 状态?.持续回合 || 0)),
            };
          })
          .filter(Boolean);
      }

      function 估算行为规划持续伤害期望(角色 = {}, 效果列表 = []) {
        const 当前持续伤害列表 = 读取行为规划持续伤害条目(角色);
        const 已有持续伤害期望 = 当前持续伤害列表.reduce(
          (总和, 条目) => 总和 + 条目.每回合伤害 * Math.max(1, 条目.剩余回合),
          0,
        );
        const 引爆伤害期望 = 效果列表.reduce((总和, effect) => {
          if (String(effect?.原型 || '').trim() !== '结算修正') return 总和;
          if (String(effect?.结算 || '').trim() !== '持续伤害引爆') return 总和;
          const 引爆倍率 = Math.max(0.1, Number(effect?.引爆倍率 || effect?.detonate_ratio || 1));
          return 总和 + 当前持续伤害列表.reduce((小计, 条目) => 小计 + Math.max(1, Math.floor(条目.每回合伤害 * 引爆倍率)), 0);
        }, 0);
        const 压缩伤害期望 = 效果列表.reduce((总和, effect) => {
          if (String(effect?.原型 || '').trim() !== '时窗修正') return 总和;
          if (String(effect?.调整方式 || '').trim() !== '压缩') return 总和;
          const 调整字段 = String(effect?.调整字段 || '持续回合').trim() || '持续回合';
          if (调整字段 !== '持续回合' && 调整字段 !== '持续tick') return 总和;
          const 压缩回合 = Math.max(
            1,
            Math.min(3, Number(effect?.调整回合 || effect?.延长回合 || effect?.压缩回合 || effect?.consume_rounds || effect?.extend_rounds || 1)),
          );
          const 时窗数值 = 读取战斗数值正负(effect?.结算倍率);
          const 压缩倍率 = Math.max(1, Number(effect?.压缩倍率 || effect?.compress_ratio || 时窗数值 || 1.2));
          return 总和 + 当前持续伤害列表.reduce((小计, 条目) => {
            const 可压缩回合 = Math.max(0, Math.min(压缩回合, 条目.剩余回合));
            return 小计 + Math.max(0, Math.floor(条目.每回合伤害 * 可压缩回合 * 压缩倍率));
          }, 0);
        }, 0);
        const 新增持续伤害期望 = 效果列表.reduce((总和, effect) => {
          if (String(effect?.原型 || '').trim() !== '状态施加') return 总和;
          if (
            (Number(effect?.延迟回合 || 0) > 0 && 战斗原型允许延迟回合(effect?.原型)) ||
            String(effect?.触发方式 || '').trim() === '遥控触发'
          ) return 总和;
          const 每回合伤害 = 估算单位持续伤害数值(角色, {
            ...effect,
            战斗效果: effect?.计算层效果 || {},
          });
          if (!(每回合伤害 > 0)) return 总和;
          const 持续回合 = Math.max(1, Number(effect?.持续回合 || 0));
          return 总和 + 每回合伤害 * 持续回合;
        }, 0);
        return Math.max(0, Math.floor(已有持续伤害期望 + 引爆伤害期望 + 压缩伤害期望 + 新增持续伤害期望));
      }

      function 计算行为出手承诺(动作 = {}) {
        if (!动作 || 动作.__行为防反 === true) return 0.08;
        const 技能 = 动作.skill && typeof 动作.skill === 'object' ? 动作.skill : {};
        const 动作名 = String(动作.action_type || 动作.type || 技能.name || 技能.魂技名 || '').trim();
        if (/防御|闪避|撤离|观察|架势|守势/.test(动作名) && !/攻击|伤害|强攻/.test(动作名)) return 0.04;

        const 技能分类 = getSkillType(技能);
        const 战斗目标类型 = inferSkillPrimaryTargetKind(技能);
        if (['自身', '友方单体', '友方群体'].includes(战斗目标类型)) return 0.05;

        const 主伤害 = getPrimaryDamageEffect(技能) || {};
        const 效果列表 = getSkillEffects(技能, { 行为规划: true });
        const 威力倍率 = Number(主伤害.威力倍率 || 0);
        const 伤害类型 = String(主伤害.伤害类型 || '');
        let 承诺 = 动作名 === '常规攻击' || 技能.name === '普通攻击' ? 0.16 : 0.1;

        if (技能分类 === '输出') 承诺 += 0.16;
        if (技能分类 === '控制') 承诺 += 0.08;
        if (战斗目标类型 === '敌方单体') 承诺 += 0.04;
        if (战斗目标类型 === '敌方群体' || 战斗目标类型 === '全场') 承诺 += 0.02;
        if (战斗伤害是近身攻击(伤害类型)) 承诺 += 0.16;
        else if (战斗伤害是远程攻击(伤害类型)) 承诺 += 0.07;
        else if (战斗伤害是精神攻击(伤害类型)) 承诺 += 0.05;
        else if (战斗伤害是真实攻击(伤害类型)) 承诺 += 0.1;
        承诺 += Math.min(0.22, Math.max(0, 威力倍率 - 80) / 500);
        if (效果列表.some(effect => ['多段伤害', '追击', '追击位移', '强制位移', '位移交换'].includes(读取战斗效果标签(effect))))
          承诺 += 0.08;
        if (效果列表.some(effect => 读取战斗效果标签(effect) === '自残换收益')) 承诺 += 0.1;

        return 限制行为概率(承诺, 0.03, 0.82);
      }

      function 计算行为防反倾向(角色 = {}, 防反类型 = '') {
        const 系别 = String(角色?.type || 角色?.系别 || '').trim();
        if (防反类型 === '完美闪避') {
          if (系别 === '敏攻系') return 0.14;
          if (系别 === '精神系' || 系别 === '控制系') return 0.06;
          if (系别 === '防御系') return -0.04;
          return 0;
        }
        if (防反类型 === '硬抗换伤') {
          if (系别 === '防御系') return 0.16;
          if (系别 === '强攻系') return 0.12;
          if (系别 === '敏攻系') return -0.03;
          if (['辅助系', '治疗系', '食物系', '召唤系'].includes(系别)) return -0.06;
        }
        return 0;
      }

      function 计算行为防反概率(配置 = {}) {
        const 原动作 = 配置.原动作 || {};
        if (读取行为防反深度(原动作) >= 2) return 0;
        const 防反方 = 配置.防反方;
        const 攻击方 = 配置.攻击方;
        if (!isCombatUnitAbleToFight(防反方) || !isCombatUnitAbleToFight(攻击方)) return 0;
        const 防反类型 = String(配置.防反类型 || '').trim();
        const 反应余量 = Number(配置.反应余量 || 0);
        if (反应余量 < 0.85) return 0;

        const 出手承诺 = Number.isFinite(Number(配置.出手承诺))
          ? Number(配置.出手承诺)
          : 计算行为出手承诺(原动作);
        const 体力比 = 读取行为体力比(防反方);
        const 战斗经验 = 配置.战斗经验 || 计算行为战斗经验(防反方, 攻击方, 配置.战斗数据 || {});
        const 经验稳定度 = 限制行为概率(Number(战斗经验?.稳定度 || 0), 0, 1);
        let 概率 = 防反类型 === '完美闪避' ? 0.06 : 0.05;
        概率 += 限制行为概率((反应余量 - 0.85) * 0.16, 0, 0.22);
        概率 += 出手承诺 * 0.32;
        概率 += 计算行为防反倾向(防反方, 防反类型);
        概率 += (体力比 - 0.35) * 0.18;
        概率 += (经验稳定度 - 0.45) * 0.12;

        if (防反类型 === '完美闪避') {
          const 闪避率 = Number(配置.闪避率 || 0);
          const 闪避投点 = Number(配置.闪避投点 || 0);
          const 闪避余量 = (闪避率 - 闪避投点) / 100;
          if (闪避余量 < 0.08) return 0;
          概率 += 限制行为概率(闪避余量 * 0.55, 0, 0.28);
          if (String(配置.反应动作?.type || '') !== '伺机闪避') 概率 *= 0.55;
          return 限制行为概率(概率, 0, 0.68);
        }

        if (防反类型 === '硬抗换伤') {
          const 伤害值 = Math.max(0, Number(配置.实际伤害 ?? 配置.预计伤害 ?? 0));
          const 承伤比 = 伤害值 / Math.max(1, getCombatHpMaxValue(防反方));
          const 战斗类型 = String(配置.战斗类型 || 配置.战斗数据?.战斗类型 || '').trim();
          const 是虚拟战 = /虚拟|升灵台|模拟/.test(战斗类型);
          const 允许绝境换伤 = 配置.以命换伤 === true && !是虚拟战;
          if (!(伤害值 > 0) || (承伤比 > 0.45 && !允许绝境换伤)) return 0;
          if (承伤比 <= 0.12) 概率 += 0.18;
          else if (承伤比 <= 0.25) 概率 += 0.08;
          else if (允许绝境换伤) {
            const 实力差距 = 计算行为实力差距(攻击方, 防反方);
            概率 = Math.max(概率 * 0.45, 0.04 + Math.min(0.16, 实力差距 * 0.18));
            概率 -= Math.max(0, 承伤比 - 0.45) * 0.28;
            概率 += (1 - 经验稳定度) * 0.05;
          } else 概率 -= (承伤比 - 0.25) * 0.9;
          if (!/肉体兜底|承伤硬抗|硬抗/.test(String(配置.反应动作?.type || 配置.反应动作?.action_type || ''))) 概率 *= 0.7;
          return 限制行为概率(概率, 0, 允许绝境换伤 ? 0.42 : 0.58);
        }

        return 0;
      }

      function 建立行为防反动作(防反方, 防反候选 = {}) {
        const 防反类型 = 防反候选.以命换伤 === true
          ? '以命换伤'
          : String(防反候选.防反类型 || '行为防反').trim();
        const 出手承诺 = Number(防反候选.出手承诺 || 0);
        const 触发概率 = Number(防反候选.触发概率 || 0);
        const 防反深度 = Math.max(1, Math.min(2, Math.floor(Number(防反候选.counterDepth || 防反候选.__counterDepth || 1))));
        const 系别 = String(防反方?.type || 防反方?.系别 || '').trim();
        const 兜底动作名 = 防反类型 === '完美闪避'
          ? '闪避反击'
          : 防反类型 === '硬抗换伤'
            ? '防守反击'
            : 防反类型 === '以命换伤'
              ? '绝地反扑'
              : '借势反打';
        const 动作名 = String(防反候选.sourceActionName || '').trim() || 兜底动作名;
        const 基础威力 = 防反类型 === '完美闪避' ? 55 : 防反类型 === '以命换伤' ? 92 : 70;
        const 系别倍率 = 防反类型 === '完美闪避'
          ? 系别 === '敏攻系'
            ? 1.18
            : 系别 === '精神系'
              ? 1.08
              : 1
          : 系别 === '防御系'
            ? 1.2
            : 系别 === '强攻系'
              ? 1.15
              : 1;
        const 威力倍率 = Math.max(35, Math.floor(基础威力 * 系别倍率 * (1 + 出手承诺 * 0.65 + Math.max(0, 触发概率 - 0.25))));
        const 伤害类型 = 系别 === '精神系' ? '精神攻击' : 系别 === '元素系' ? '远程攻击' : '近身攻击';
        return {
          action_type: '行为防反',
          type: '行为防反',
          cast_time: 0,
          __行为防反: true,
          __counterDepth: 防反深度,
          counterDepth: 防反深度,
          sourceActionName: 动作名,
          sourceActionType: String(防反候选.sourceActionType || 'counter').trim(),
          skill: normalizeSkillData(
            {
              name: 动作名,
              技能分类: '输出',
              消耗: '无',
              前摇: 0,
              _效果数组: [
                { 原型: '伤害结算', 目标: '单体', 生效方式: '独立生效', 威力倍率, 伤害类型, 防御穿透: 0 },
              ],
            },
            `${防反类型}防反`,
          ),
        };
      }

      function estimateIncomingActionThreat(attacker, defender, playerAction, combatData) {
        let skill = playerAction?.skill && typeof playerAction.skill === 'object' ? playerAction.skill : null;
        const defaultThreat = {
          targetHitsDefender: false,
          projectedDamage: 0,
          projectedDamageRatio: 0,
          projectedRemainingHp: getCombatHpValue(defender),
          lethalRisk: false,
          severeDamage: false,
          moderateDamage: false,
          severeControl: false,
          shieldBreakThreat: false,
          antiHealThreat: false,
          skillSealThreat: false,
          dotDetonateThreat: false,
          incomingAttackIntent: false,
          attackerTier: getCombatUnitTierNumber(attacker),
          bypassesInvincible: getCombatUnitTierNumber(attacker) >= 100,
        };
        if (!attacker || !defender || !skill) return defaultThreat;
        if (!装备技能跳过掌控缩放(skill)) skill = 按技能掌控度缩放技能(skill, attacker);

        const 伤害规划上下文 = {
          行为规划: true,
          actor: attacker,
          caster: attacker,
          attacker,
          target: defender,
          defender,
          hit: true,
          命中: true,
          主原型成立: true,
        };
        const 预结算效果列表 = getSkillEffects(skill, 伤害规划上下文);
        const 预结算技能 = { ...skill, __规划水合效果数组: 预结算效果列表 };
        const 预结算伤害效果 = 预结算效果列表.find(effect =>
          String(effect?.原型 || '').trim() === '伤害结算',
        ) || {};
        const targetContext = resolveSkillTargetContext(skill, attacker, defender, combatData, 预结算伤害效果);
        const targetHitsDefender = targetContext.targetSet.some(target => isCombatUnitIdentityMatch(target, defender?.name || defender));
        if (!targetHitsDefender) return defaultThreat;

        let attackerFinalStat = attacker.final || buildCombatFinalStats(attacker);
        const defenderFinalStat = defender.final || buildCombatFinalStats(defender);
        const pClash = 预结算伤害效果;
        const pStateEffect = 预结算效果列表.find(effect => String(effect?.原型 || '').trim() === '状态施加') || {};
        const pStateCalc = pStateEffect?.计算层效果 || createEmptyCombatEffectMap();
        const 副作用风险 = getBattleSkillSideEffectList(skill).reduce((风险, 条目) => {
          const 生效对象 = String(条目?.生效对象 || '技能释放者').trim();
          if (!['技能释放者', '双方'].includes(生效对象)) return 风险;
          const 类型 = String(条目?.副作用类型 || '').trim();
          const 状态 = String(条目?.副作用状态 || BATTLE_SKILL_SIDE_EFFECT_STATUS_MAP[类型] || '').trim();
          风险.致死 = 风险.致死 || 类型 === '致死献祭';
          风险.行动风险 += estimateBattleSideEffectRisk(条目, attacker);
          if (['失控', '反噬', '精神紊乱', '僵直', '麻痹', '混乱', '魂力枯竭'].includes(状态)) 风险.行动风险 += Math.max(0, Math.min(1, Number(条目?.触发概率 ?? 1) || 0)) * 0.2;
          return 风险;
        }, { 行动风险: 0, 致死: false });
        const fusionPrecastEffect = 读取一次性武魂融合前置战斗效果(pStateEffect);
        if (fusionPrecastEffect) {
          attackerFinalStat = 套用一次性武魂融合前置属性(attackerFinalStat, pStateEffect);
        }
        const skillSummary = deriveBattleSummaryFromEffects(预结算技能);
        const mainType = inferMainTypeFromEffects(预结算技能);
        const incomingAttackIntent =
          Number(pClash?.威力倍率 || 0) > 0 ||
          mainType === '伤害类' ||
          isBattleSkillOffensiveProfile(预结算技能, { skillType: getSkillType(skill), mainType, summary: skillSummary }) ||
          /攻击|伤害|爆发|斩|击|吞噬|追击/.test(
            String(playerAction?.action_type || '') + String(skill?.name || skill?.技能名称 || ''),
          );
        const attackerConditionEffects = attacker.状态效果
          ? Object.values(attacker.状态效果).map(c => c?.战斗效果 || {})
          : [];
        if (fusionPrecastEffect) attackerConditionEffects.push(fusionPrecastEffect);
        const defenderConditionEffects = defender.状态效果
          ? Object.values(defender.状态效果).map(c => c?.战斗效果 || {})
          : [];
        const 对应等级 = 构建对应等级数据(pClash);
        const 使用对应等级 = !!对应等级;
        const 攻势单位 = 使用对应等级 ? 对应等级.单位 : attacker;
        const 攻势最终属性 = 使用对应等级 ? 对应等级.最终属性 : attackerFinalStat;
        const 预结算附加穿透 = 使用对应等级 ? 0 : 预结算效果列表.reduce((总和, effect) => {
          if (String(effect?.原型 || '').trim() !== '结算修正') return 总和;
          if (String(effect?.结算 || '').trim() !== '防御穿透') return 总和;
          const 原始值 = Number(读取战斗数值正负(effect?.数值));
          if (!Number.isFinite(原始值) || 原始值 === 0) return 总和;
          return 总和 + (Math.abs(原始值) <= 1 ? 原始值 * 100 : 原始值);
        }, 0);
        const skillPower = Math.max(0, Number(pClash?.威力倍率 || 0));
        const dmgType = 规范化战斗伤害类型(pClash?.伤害类型, skill);
        const conditionArmorPen = 使用对应等级 ? 0 : 读取状态穿透比例(attackerConditionEffects);
        const 目标防御剥夺 = Math.min(
          0.9,
          defenderConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.defense_strip || 0)), 0),
        );
        const 目标精神抗性剥夺 = Math.min(
          0.9,
          defenderConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.spirit_resist_strip || 0)), 0),
        );
        let projectedDamage = 0;
        let actualDef = 计算穿透后防御值(
          Number(defenderFinalStat.def || defender.def || 1),
          Number(pClash?.防御穿透 || 0) + 预结算附加穿透,
          conditionArmorPen,
        );
        if (!战斗伤害是精神攻击(dmgType) && !战斗伤害是真实攻击(dmgType)) actualDef = Math.max(1, actualDef * (1 - 目标防御剥夺));
        const soulDriveScale = 使用对应等级 ? 1 : getSoulDriveScale({ ...attacker, final: attackerFinalStat }, defender);
        const spiritDriveScale = 使用对应等级 ? 1 : getSpiritDriveScale({ ...attacker, final: attackerFinalStat }, defender);
        const 定位伤害倍率 = 使用对应等级 ? 1 : 计算定位伤害倍率(attacker, defender, dmgType);
        const 消耗加成系数 = 使用对应等级 ? 1 : 计算伤害消耗加成系数(skill, attacker);
        if (战斗伤害是真实攻击(dmgType)) {
          const 真实驱动 = Math.max(1, 计算精神伤害攻势值(攻势单位, 攻势最终属性));
          projectedDamage = skillPower * Math.max(1, Math.sqrt(真实驱动)) * 0.12 * 消耗加成系数;
        } else if (战斗伤害是近身攻击(dmgType)) {
          const 近身攻势 = 计算物理伤害攻势值(攻势单位, 攻势最终属性, dmgType);
          projectedDamage =
            skillPower *
            (近身攻势 / actualDef) *
            soulDriveScale *
            定位伤害倍率 *
            消耗加成系数;
        } else if (战斗伤害是远程攻击(dmgType)) {
          const 远程攻势 = 计算物理伤害攻势值(攻势单位, 攻势最终属性, dmgType);
          projectedDamage =
            skillPower *
            (远程攻势 / actualDef) *
            soulDriveScale *
            定位伤害倍率 *
            消耗加成系数;
        } else if (战斗伤害是精神攻击(dmgType)) {
          projectedDamage =
            skillPower *
            (计算精神伤害攻势值(攻势单位, 攻势最终属性) /
              Math.max(1, 计算紫极魔瞳防守精神攻势值_战斗(defender, defenderFinalStat, pClash, skill) * (1 - 目标精神抗性剥夺))) *
            spiritDriveScale *
            定位伤害倍率 *
            消耗加成系数;
        }
        const attackerFinalDamageMult = 使用对应等级 ? 1 : attackerConditionEffects.reduce(
          (mult, ce) => mult * Number(ce.final_damage_mult || 1.0),
          1.0,
        );
        const attackerFinalDamageBonus = 使用对应等级 ? 0 : attackerConditionEffects.reduce(
          (sum, ce) => sum + Number(ce.final_damage_bonus || 0),
          0,
        );
        const defenderReduction = Math.min(
          0.9,
          defenderConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.damage_reduction || 0)), 0),
        );
        const defenderReceivedDamageMult = defenderConditionEffects.reduce(
          (mult, ce) => mult * Number(ce.received_damage_mult || 1.0),
          1.0,
        );
        const extraTrueDamage = 使用对应等级 ? 0 : attackerConditionEffects.reduce(
          (sum, ce) =>
            sum + Math.floor((Number(attackerFinalStat.men_max || attacker.men_max || 0) || 0) * Number(ce.bonus_true_damage_ratio || 0)),
          0,
        );
        const defenderElementReceivedDamageMult = 读取目标元素承伤倍率(defender, skill);
        projectedDamage = projectedDamage * (1 - defenderReduction) * defenderReceivedDamageMult * defenderElementReceivedDamageMult * attackerFinalDamageMult + attackerFinalDamageBonus + extraTrueDamage;
        const shieldPressure = collectShieldConditionEntries(defender).reduce((sum, entry) => sum + Number(entry.shieldValue || 0), 0);
        const 状态伤害期望 = 估算行为规划持续伤害期望(defender, 预结算效果列表);
        const projectedDamageAfterShield = Math.max(0, Math.floor(projectedDamage - shieldPressure) + 状态伤害期望);
        const currentHp = getCombatHpValue(defender);
        const maxHp = getCombatHpMaxValue(defender);
        const projectedDamageRatio = projectedDamageAfterShield / maxHp;
        const projectedRemainingHp = Math.max(0, currentHp - projectedDamageAfterShield);
        const executeThreat =
          currentHp / maxHp <= 0.45 &&
          预结算效果列表.some(effect =>
            String(effect?.原型 || '').trim() === '结算修正' &&
            String(effect?.结算 || '').trim() === '造成伤害' &&
            读取战斗数值正负(effect?.数值) > 0 &&
            String(effect?.驱动属性 || '').trim() === '体力上限',
          );
        const severeControl =
          预结算效果列表.length > 0 && (
          pStateCalc.skip_turn === true ||
          pStateCalc.cannot_react === true ||
          pStateCalc.skill_seal === true ||
          Number(pStateCalc.lock_level || 0) >= 2 ||
          Number(pStateCalc.reaction_penalty || 0) >= 0.2 ||
          Number(pStateCalc.cast_speed_penalty || 0) >= 0.2 ||
          副作用风险.行动风险 >= 0.35 ||
          isBattleSkillControlProfile(预结算技能, { mainType, calc: pStateCalc, summary: skillSummary }));
        const enemySnapshot = buildConditionTacticalSnapshot(defender);
        const shieldBreakThreat =
          enemySnapshot.hasShielded &&
          isBattleSkillShieldBreakProfile(预结算技能, { damage: pClash });
        const antiHealThreat = isBattleSkillAntiHealProfile(预结算技能, { calc: pStateCalc });
        const dotDetonateThreat = isBattleSkillDotDetonateProfile(预结算技能) && enemySnapshot.debuffCount > 0;
        const skillSealThreat = isBattleSkillSealProfile(预结算技能, { calc: pStateCalc });
        const lethalRisk =
          projectedDamageAfterShield >= currentHp * 0.95 ||
          projectedRemainingHp <= 0 ||
          (executeThreat && currentHp / maxHp <= 0.45);
        const severeDamage =
          lethalRisk ||
          projectedDamageRatio >= 0.3 ||
          projectedDamageAfterShield >= currentHp * 0.5 ||
          skillPower >= 260;
        const 状态伤害中度威胁 = 状态伤害期望 >= 100 || 状态伤害期望 >= currentHp * 0.12;
        const moderateDamage =
          severeDamage ||
          状态伤害中度威胁 ||
          projectedDamageRatio >= 0.18 ||
          projectedDamageAfterShield >= currentHp * 0.28 ||
          skillPower >= 180;
        return {
          ...defaultThreat,
          targetHitsDefender,
          projectedDamage: projectedDamageAfterShield,
          projectedDamageRatio: Number(projectedDamageRatio.toFixed(4)),
          projectedRemainingHp,
          lethalRisk,
          severeDamage,
          moderateDamage,
          severeControl,
          shieldBreakThreat,
          antiHealThreat,
          skillSealThreat,
          dotDetonateThreat,
          incomingAttackIntent,
          sideEffectSelfRisk: Number(副作用风险.行动风险.toFixed(4)),
          sideEffectLethalRisk: 副作用风险.致死,
        };
      }

      function formatBattleTickToCalendarDateText(tickValue) {
        const safeTick = Math.max(0, Number(tickValue || 0));
        const totalMinutes = safeTick * 10;
        const days = Math.floor(totalMinutes / (24 * 60));
        const years = Math.floor(days / 360);
        const months = Math.floor((days % 360) / 30) + 1;
        const currentDay = (days % 30) + 1;
        const remainderMinutes = totalMinutes % (24 * 60);
        const hours = Math.floor(remainderMinutes / 60);
        const mins = remainderMinutes % 60;
        return `斗罗历${20000 + years}年${months}月${currentDay}日 ${hours.toString().padStart(2, '0')}:${mins.toString().padStart(2, '0')}`;
      }

      function 构建造物承载补丁包(技能 = {}, 背包状态 = {}, 持有者名参数 = '', 选项参数 = {}) {
        const 原始效果列表 = Array.isArray(技能?._效果数组) ? 技能._效果数组 : [];
        const 水合效果列表 = String(技能?.承载方式 || '').trim() === '造物承载'
          ? 原始效果列表
              .filter(效果 => 效果 && typeof 效果 === 'object' && String(效果?.原型 || '').trim())
              .flatMap(效果 => 展开战斗原型数组字段(效果))
              .map(效果 => hydrateBattleExecutionEffectEntry(效果, { skill: 技能 }))
              .filter(Boolean)
          : getSkillEffects(技能);
        const 有效期修正列表 = 水合效果列表.filter(效果 =>
          效果 &&
          typeof 效果 === 'object' &&
          !Array.isArray(效果) &&
          String(效果?.原型 || '').trim() === '时窗修正' &&
          String(效果?.调整字段 || '').trim() === '有效期tick',
        );
        const 调制造物有效期 = 基础tick => 有效期修正列表.reduce((当前tick, 效果) => {
          const 步长 = Math.max(1, Math.min(1008, Math.floor(Number(效果?.调整tick || 效果?.有效期tick || 1))));
          return String(效果?.调整方式 || '').trim() === '压缩'
            ? Math.max(0, 当前tick - 步长)
            : 当前tick + 步长;
        }, Math.max(0, Number(基础tick || 0)));
        const 造物模板列表 = String(技能?.承载方式 || '').trim() === '造物承载'
          ? 原始效果列表.filter(效果 =>
              效果 &&
              typeof 效果 === 'object' &&
              !Array.isArray(效果) &&
              !String(效果?.原型 || '').trim() &&
              Array.isArray(效果?.使用效果) &&
              效果.使用效果.length > 0,
            )
          : [];
        if (!造物模板列表.length) return { patchOps: [], log: '' };

        const 补丁列表 = [];
        const 日志列表 = [];
        const 默认持有者名 = String(window.BattleUIBridge?.getMVU('sys.玩家名') || '').trim();
        const 当前tick = Number(window.BattleUIBridge?.getMVU('world.时间.tick') || 0);
        const 持有者名 = String(持有者名参数 || 默认持有者名 || '').trim();
        if (!持有者名) return { patchOps: [], log: '' };
        const 持有者背包路径 = `/char/${escapeJsonPointerSegment(持有者名)}/背包`;
        const 根层物品 = window.BattleUIBridge?.getMVU('物品') || {};
        const 根层魂技造物 =
          根层物品 && 根层物品.魂技造物 && typeof 根层物品.魂技造物 === 'object' && !Array.isArray(根层物品.魂技造物)
            ? 根层物品.魂技造物
            : {};
        const 原始产出者属性 = 技能?.__造物产出者 && typeof 技能.__造物产出者 === 'object' && !Array.isArray(技能.__造物产出者)
          ? 技能.__造物产出者
          : {};
        const 产出者名 = String(原始产出者属性?.name || 原始产出者属性?.名称 || 选项参数.产出者名 || '').trim();
        const 技能名 = String(技能?.魂技名 || 技能?.name || '造物承载魂技').trim() || '造物承载魂技';
        const 产出者属性快照 = 原始产出者属性.final && typeof 原始产出者属性.final === 'object' && !Array.isArray(原始产出者属性.final)
          ? deepClonePlain(原始产出者属性.final)
          : buildCombatFinalStats(原始产出者属性);

        let 剩余即时消耗 = Math.max(0, Math.floor(Number(选项参数.即时消耗数量 || 0)));
        造物模板列表.forEach((造物模板, 索引) => {
          const 基础物品名 = String(技能?.魂技名 || 技能?.name || '临时造物').trim() || '临时造物';
          const 物品名 = 造物模板列表.length > 1 ? `${基础物品名}·${索引 + 1}` : 基础物品名;
          const 转义物品名 = escapeJsonPointerSegment(物品名);
          const 原始数量 = Math.max(1, Math.floor(Number(造物模板?.数量 || 1)));
          const 即时消耗 = Math.min(原始数量, 剩余即时消耗);
          剩余即时消耗 -= 即时消耗;
          const 入包数量 = Math.max(0, 原始数量 - 即时消耗);
          const 相对有效期tick = 调制造物有效期(造物模板?.有效期tick || 0);
          const 物品定义 = {
            阶位: Math.max(0, Math.floor(Number(造物模板?.阶位 || 0))),
            品质: String(造物模板?.品质 || '普通'),
            描述: String(造物模板?.描述 || 技能?.效果描述 || 技能?.描述 || '使用后触发对应魂技效果').trim(),
            基础价格: Math.max(0, Math.floor(Number(造物模板?.基础价格 || 0))),
            默认货币: String(造物模板?.默认货币 || '联邦币'),
            装备槽位: String(造物模板?.装备槽位 || '无'),
            基础耐久: Math.max(0, Math.floor(Number(造物模板?.基础耐久 || 0))),
            使用条件: 造物模板?.使用条件 && typeof 造物模板.使用条件 === 'object' && !Array.isArray(造物模板.使用条件) ? deepClone(造物模板.使用条件) : {},
            使用效果: deepClone(Array.isArray(造物模板?.使用效果) ? 造物模板.使用效果 : []),
            造物产出者属性: deepClonePlain(产出者属性快照),
            属性加成: 造物模板?.属性加成 && typeof 造物模板.属性加成 === 'object' && !Array.isArray(造物模板.属性加成) ? deepClone(造物模板.属性加成) : {},
            副职业参数: 造物模板?.副职业参数 && typeof 造物模板.副职业参数 === 'object' && !Array.isArray(造物模板.副职业参数) ? deepClone(造物模板.副职业参数) : {},
          };
          const 使用副作用列表 = deepClone(Array.isArray(造物模板?.副作用列表) ? 造物模板.副作用列表 : []);
          if (使用副作用列表.length) 物品定义.副作用列表 = 使用副作用列表;
          const 下一背包物品 = {
            数量: 入包数量,
            来源: String(技能?.魂技名 || 技能?.name || 物品名),
            持有者: 持有者名,
            造物产出者属性: deepClonePlain(产出者属性快照),
          };
          if (相对有效期tick > 0) {
            下一背包物品.有效期至tick = 当前tick + 相对有效期tick;
          } else {
            下一背包物品.有效期至tick = 0;
          }

          const 当前背包物品 = 背包状态[物品名];
          const 背包物品路径 = `${持有者背包路径}/${转义物品名}`;
          const 下一背包物品值 =
            当前背包物品 && typeof 当前背包物品 === 'object'
              ? {
                  ...deepClonePlain(当前背包物品),
                  数量: Number(当前背包物品.数量 || 0) + 入包数量,
                  造物产出者属性: deepClonePlain(产出者属性快照),
                }
            : 下一背包物品;
          const 下一根层物品 = { ...deepClonePlain(根层魂技造物[物品名] || {}), ...物品定义 };
          if (!使用副作用列表.length) delete 下一根层物品.副作用列表;
          appendJsonPatchDiff(补丁列表, `/物品/魂技造物/${转义物品名}`, 根层魂技造物[物品名], 下一根层物品);
          if (入包数量 > 0) appendJsonPatchDiff(补丁列表, 背包物品路径, 当前背包物品, 下一背包物品值);

          日志列表.push(`生成了造物【${物品名}】×${原始数量}${即时消耗 > 0 ? `，即时使用${即时消耗}件` : ''}${入包数量 > 0 ? `，入包${入包数量}件` : ''}`);
        });

        return {
          patchOps: 补丁列表,
          log: 日志列表.length
            ? ` [造物承载] ${产出者名 ? `${产出者名}以【${技能名}】完成造物：` : ''}${日志列表.join('，')}。`
            : '',
        };
      }

      function 读取战斗背包物品数据(物品名 = '', 背包状态 = {}) {
        const 全部物品 = window.BattleUIBridge?.getMVU('物品') || {};
        let 根物品 = {};
        let 根物品分类 = '';
        if (全部物品 && typeof 全部物品 === 'object' && !Array.isArray(全部物品)) {
          Object.entries(全部物品).some(([分类名, 分类表]) => {
            if (!分类表 || typeof 分类表 !== 'object' || Array.isArray(分类表)) return false;
            if (!分类表[物品名] || typeof 分类表[物品名] !== 'object' || Array.isArray(分类表[物品名])) return false;
            根物品 = 分类表[物品名];
            根物品分类 = String(分类名 || '').trim();
            return true;
          });
        }
        const 安全根物品 = 根物品 && typeof 根物品 === 'object' && !Array.isArray(根物品) ? 根物品 : {};
        const 安全背包状态 = 背包状态 && typeof 背包状态 === 'object' && !Array.isArray(背包状态) ? 背包状态 : {};
        const 批次列表 = (Array.isArray(安全背包状态.批次) ? 安全背包状态.批次 : []).filter(
          批次 => 批次 && typeof 批次 === 'object' && !Array.isArray(批次) && Number(批次.数量 || 0) > 0,
        );
        const 首批 = 批次列表[0] || {};
        const 总数量 =
          Math.max(0, Math.floor(Number(安全背包状态.数量 || 0))) +
          批次列表.reduce((总数, 批次) => 总数 + Math.max(0, Math.floor(Number(批次.数量 || 0))), 0);
        const 取数组字段 = 字段名 =>
          deepClone(
            Array.isArray(安全背包状态?.[字段名]) && 安全背包状态[字段名].length
              ? 安全背包状态[字段名]
              : Array.isArray(安全根物品?.[字段名])
                ? 安全根物品[字段名]
                : [],
          );
        return {
          ...deepClonePlain(安全根物品),
          ...deepClonePlain(安全背包状态),
          ...deepClonePlain(首批),
          物品分类: String(安全背包状态?.物品分类 || 安全背包状态?.分类 || 安全根物品?.物品分类 || 安全根物品?.分类 || 根物品分类 || '').trim(),
          数量: 总数量,
          使用效果: 取数组字段('使用效果'),
          副作用列表: 取数组字段('副作用列表'),
          造物产出者属性: deepClonePlain(
            安全背包状态?.造物产出者属性 && typeof 安全背包状态.造物产出者属性 === 'object' && !Array.isArray(安全背包状态.造物产出者属性)
              ? 安全背包状态.造物产出者属性
              : 安全根物品?.造物产出者属性 && typeof 安全根物品.造物产出者属性 === 'object' && !Array.isArray(安全根物品.造物产出者属性)
                ? 安全根物品.造物产出者属性
                : {},
          ),
          使用条件: deepClonePlain(
            安全背包状态?.使用条件 && typeof 安全背包状态.使用条件 === 'object' && !Array.isArray(安全背包状态.使用条件)
              ? 安全背包状态.使用条件
              : 安全根物品?.使用条件 && typeof 安全根物品.使用条件 === 'object' && !Array.isArray(安全根物品.使用条件)
                ? 安全根物品.使用条件
                : {},
          ),
        };
      }

      function 选择行为防反动作来源(防反方 = {}, 战斗数据 = null, 防反类型 = '') {
        const 技能可作为行为防反动作 = skill => {
          const 名称 = String(skill?.name || skill?.魂技名 || '').trim();
          if (!名称 || /^普通攻击$/.test(名称)) return false;
          const 分类 = String(skill?.技能分类 || skill?.类型 || skill?.category || '').trim();
          const 目标 = String(skill?.目标 || skill?.作用目标 || '').trim();
          const 承载方式 = String(skill?.承载方式 || '').trim();
          if (/自身|友方|己方/.test(目标) || /造物承载|物品使用/.test(承载方式)) return false;
          if (/辅助|治疗|防护|资源|食物|药剂|造物/.test(`${分类} ${名称} ${承载方式}`) && !/攻击|输出|控制|压制|截击|伤害|封技|打断/.test(`${分类} ${名称}`)) return false;
          const effects = [];
          const 扫描效果 = value => {
            if (!value || typeof value !== 'object') return;
            if (Array.isArray(value)) {
              value.forEach(扫描效果);
              return;
            }
            if (value.原型 || value.type) effects.push(value);
            ['使用效果', '授予效果', '结算效果', '追加效果', '替换效果', '条件分支'].forEach(key => 扫描效果(value[key]));
          };
          扫描效果(skill?._效果数组);
          const hasHostileEffect = effects.some(effect => {
            const 原型 = String(effect?.原型 || effect?.type || '').trim();
            const effectTarget = String(effect?.目标 || '').trim();
            if (/自身|友方|己方/.test(effectTarget)) return false;
            return ['伤害结算', '状态施加', '判定修正', '决策干扰', '资源锁定', '机制抹消', '位移执行'].includes(原型);
          });
          if (hasHostileEffect) return true;
          return /攻击|输出|控制|压制|截击|冲拳|斩|击|刺|咬|炮|锁|牵制/.test(`${分类} ${名称}`);
        };
        const 可用技能 = collectCombatSkills(防反方, [])
          .filter(skill => {
            const 名称 = String(skill?.name || skill?.魂技名 || '').trim();
            if (!技能可作为行为防反动作(skill)) return false;
            if (inferSkillPrimaryTargetKind(skill) === '自身') return false;
            const mainType = inferMainTypeFromEffects(skill);
            const summary = deriveBattleSummaryFromEffects(skill);
            return isBattleSkillOffensiveProfile(skill, { skillType: getSkillType(skill), mainType, summary })
              || isBattleSkillControlProfile(skill, { mainType, summary });
          })
          .sort((left, right) => (getSkillCastTime(left) || 10) - (getSkillCastTime(right) || 10));
        const 选中技能 = 可用技能.find(skill => !/^普通攻击$/.test(String(skill?.name || skill?.魂技名 || '').trim())) || 可用技能[0] || null;
        const 防反类型文本 = String(防反类型 || '').trim();
        const 兜底动作名 = 防反类型文本 === '完美闪避'
          ? '闪避反击'
          : 防反类型文本 === '硬抗换伤'
            ? '防守反击'
            : 防反类型文本 === '以命换伤'
              ? '绝地反扑'
              : '借势反打';
        if (!选中技能) return { sourceActionName: 兜底动作名, sourceActionType: 'system_counter', sourceSkill: null };
        return {
          sourceActionName: String(选中技能?.name || 选中技能?.魂技名 || 兜底动作名).trim() || 兜底动作名,
          sourceActionType: 'skill_counter',
          sourceSkill: 选中技能,
        };
      }

      function 读取战斗魂导等级(物品数据 = {}) {
        const 等级 = Math.floor(Number(物品数据?.魂导等级 || 0));
        return Number.isFinite(等级) ? Math.max(0, Math.min(12, 等级)) : 0;
      }

      function 判断战斗一次性魂导道具(物品名 = '', 物品数据 = {}) {
        const 分类 = String(物品数据?.物品分类 || 物品数据?.分类 || '').trim();
        const 文本 = `${物品名} ${分类} ${String(物品数据?.描述 || '')}`;
        return 分类 === '一次性道具' || /奶瓶|定装|炮弹|炸弹|爆弹|弹\b|弹$/.test(文本);
      }

      function 判断战斗可装配魂导器(物品名 = '', 物品数据 = {}) {
        return 读取战斗魂导等级(物品数据) > 0 && !判断战斗一次性魂导道具(物品名, 物品数据);
      }

      function 读取战斗背包使用效果倍率(物品数据 = {}) {
        const 品质系数 = Number(物品数据?.品质系数 ?? 1);
        if (!Number.isFinite(品质系数) || Math.abs(品质系数 - 1) < 0.0001) return 1;
        return Math.max(0.8, Math.min(1.25, roundBattleScaledNumber(1 + (品质系数 - 1) * 0.25, 4)));
      }

      function 按战斗背包批次倍率缩放使用效果(效果列表 = [], 物品数据 = {}) {
        const 倍率 = 读取战斗背包使用效果倍率(物品数据);
        const 输出 = deepClone(Array.isArray(效果列表) ? 效果列表 : []);
        if (Math.abs(倍率 - 1) < 0.0001) return 输出;
        const 缩放字段列表 = ['数值', '威力倍率', '效果倍率', '结算倍率', '强化倍率', '引爆倍率', '持续伤害', '治疗量', '恢复量', '护盾值'];
        const 缩放效果列表 = effects =>
          (Array.isArray(effects) ? effects : []).forEach(effect => {
            if (!effect || typeof effect !== 'object' || Array.isArray(effect)) return;
            if (['耐久修复', 'durability_repair'].includes(String(effect.原型 || effect.type || '').trim())) return;
            缩放字段列表.forEach(字段名 => {
              if (effect[字段名] !== undefined) effect[字段名] = scaleBattleValue(effect[字段名], 倍率, { digits: 4 });
            });
            ['使用效果', '授予效果', '结算效果'].forEach(字段名 => 缩放效果列表(effect[字段名]));
            (Array.isArray(effect.条件分支) ? effect.条件分支 : []).forEach(分支 => {
              缩放效果列表(分支?.替换效果);
              缩放效果列表(分支?.追加效果);
            });
          });
        缩放效果列表(输出);
        return 输出;
      }

      function 校验战斗背包物品可用(物品名 = '', 物品数据 = {}, 使用者名 = '', 当前tick = 0) {
        const 数量 = Math.max(0, Number(物品数据?.数量 || 0));
        if (!(数量 > 0)) return { 可用: false, 原因: '剩余数量不足' };
        if (判断战斗可装配魂导器(物品名, 物品数据)) return { 可用: false, 原因: '魂导器需要装配后使用' };
        const 持有者 = String(物品数据?.持有者 || '').trim();
        if (持有者 && 使用者名 && 持有者 !== 使用者名) return { 可用: false, 原因: `当前持有者不是${使用者名}` };
        const 有效期至tick = Math.max(0, Number(物品数据?.有效期至tick || 0));
        if (有效期至tick > 0 && 当前tick > 有效期至tick) return { 可用: false, 原因: `【${物品名}】已经过期` };
        if (!Array.isArray(物品数据?.使用效果) || !物品数据.使用效果.length)
          return { 可用: false, 原因: '没有可执行的使用效果' };
        return { 可用: true, 原因: '' };
      }

      function 构建战斗背包技能(物品名 = '', 物品数据 = {}) {
        const 动作名 = `使用${物品名}`;
        return normalizeSkillData(
          {
            name: 动作名,
            魂技名: 动作名,
            技能分类: '辅助',
            承载方式: '物品使用',
            目标: '自身',
            __物品名: 物品名,
            __造物产出者属性: deepClonePlain(物品数据?.造物产出者属性 || {}),
            消耗: '无',
            前摇: 8,
            _效果数组: 按战斗背包批次倍率缩放使用效果(物品数据?.使用效果, 物品数据),
            副作用列表: deepClone(Array.isArray(物品数据?.副作用列表) ? 物品数据.副作用列表 : []),
          },
          动作名,
        );
      }

      function 扣减战斗背包数量(背包项 = {}, 消耗数量 = 1) {
        const 当前 = deepClonePlain(背包项 && typeof 背包项 === 'object' && !Array.isArray(背包项) ? 背包项 : {});
        let 剩余扣减 = Math.max(1, Math.floor(Number(消耗数量 || 1)));
        const 批次列表 = (Array.isArray(当前.批次) ? 当前.批次 : [])
          .map(批次 => deepClonePlain(批次))
          .filter(批次 => 批次 && typeof 批次 === 'object' && !Array.isArray(批次) && Number(批次.数量 || 0) > 0);
        for (const 批次 of 批次列表) {
          if (剩余扣减 <= 0) break;
          if (批次.剩余使用次数 !== undefined) {
            const 剩余次数 = Math.max(0, Math.floor(Number(批次.剩余使用次数 || 0)));
            if (剩余次数 > 1) {
              批次.剩余使用次数 = 剩余次数 - 1;
              剩余扣减 -= 1;
              continue;
            }
            delete 批次.剩余使用次数;
          }
          const 当前批次数量 = Math.max(0, Math.floor(Number(批次.数量 || 0)));
          const 扣减 = Math.min(当前批次数量, 剩余扣减);
          批次.数量 = 当前批次数量 - 扣减;
          剩余扣减 -= 扣减;
        }
        当前.批次 = 批次列表.filter(批次 => Number(批次.数量 || 0) > 0);
        const 普通数量 = Math.max(0, Math.floor(Number(当前.数量 || 0)));
        当前.数量 = 剩余扣减 > 0 ? Math.max(0, 普通数量 - 剩余扣减) : 普通数量;
        if (!当前.批次.length) delete 当前.批次;
        const 总数量 =
          Math.max(0, Math.floor(Number(当前.数量 || 0))) +
          (Array.isArray(当前.批次) ? 当前.批次 : []).reduce((总数, 批次) => 总数 + Math.max(0, Math.floor(Number(批次.数量 || 0))), 0);
        if (总数量 <= 0) return undefined;
        if (当前.数量 <= 0 && 当前.批次) 当前.数量 = 0;
        return 当前;
      }

      function 构建战斗背包消耗补丁包(物品名 = '', 背包状态 = {}, 持有者名参数 = '', 消耗数量参数 = 1) {
        const 补丁列表 = [];
        const 安全物品名 = String(物品名 || '').trim();
        const 安全持有者 = String(持有者名参数 || '').trim();
        const 消耗数量 = Math.max(1, Math.floor(Number(消耗数量参数 || 1)));
        if (!安全物品名 || !安全持有者) return { patchOps: 补丁列表, log: '' };
        const 当前物品 = 背包状态?.[安全物品名];
        if (!当前物品 || typeof 当前物品 !== 'object') return { patchOps: 补丁列表, log: '' };
        const 当前数量 =
          Math.max(0, Math.floor(Number(当前物品.数量 || 0))) +
          (Array.isArray(当前物品.批次) ? 当前物品.批次 : []).reduce((总数, 批次) => 总数 + Math.max(0, Math.floor(Number(批次?.数量 || 0))), 0);
        if (!(当前数量 > 0)) return { patchOps: 补丁列表, log: '' };
        const 下个物品 = 扣减战斗背包数量(当前物品, 消耗数量);
        const 下个数量 = 下个物品
          ? Math.max(0, Math.floor(Number(下个物品.数量 || 0))) +
            (Array.isArray(下个物品.批次) ? 下个物品.批次 : []).reduce((总数, 批次) => 总数 + Math.max(0, Math.floor(Number(批次?.数量 || 0))), 0)
          : 0;
        const 物品路径 = `/char/${escapeJsonPointerSegment(安全持有者)}/背包/${escapeJsonPointerSegment(安全物品名)}`;
        appendJsonPatchDiff(补丁列表, 物品路径, 当前物品, 下个物品);
        return {
          patchOps: 补丁列表,
          log: ` [物品消耗] 【${安全物品名}】${下个数量 > 0 ? `剩余${下个数量}件` : '已耗尽'}。`,
        };
      }

      function 构建造物即时使用技能(技能 = {}, 动作 = {}) {
        const 效果列表 = Array.isArray(技能?._效果数组) ? 技能._效果数组 : [];
        const 造物效果 = 效果列表.find(效果 =>
          效果 &&
          typeof 效果 === 'object' &&
          !String(效果?.原型 || '').trim() &&
          Array.isArray(效果?.使用效果)
        );
        if (!造物效果 || !Array.isArray(造物效果.使用效果) || !造物效果.使用效果.length) return null;
        const 目标名 = String(动作?.食用目标 || 动作?.target_name || '').trim();
        const 使用效果列表 = deepClone(造物效果.使用效果).map(效果 => {
          if (!效果 || typeof 效果 !== 'object') return 效果;
          const 下一效果 = { ...效果 };
          if (!String(下一效果.目标 || '').trim()) 下一效果.目标 = 目标名 ? '单体' : '自身';
          return 下一效果;
        });
        const 即时技能 = {
          ...deepClone(技能 || {}),
          name: `${技能?.name || 技能?.魂技名 || '造物承载魂技'}·即时使用`,
          魂技名: `${技能?.魂技名 || 技能?.name || '造物承载魂技'}·即时使用`,
          承载方式: '物品使用',
          目标: '自身',
          _效果数组: 使用效果列表,
        };
        const 使用副作用列表 = deepClone(Array.isArray(造物效果?.副作用列表) ? 造物效果.副作用列表 : []);
        if (使用副作用列表.length) 即时技能.副作用列表 = 使用副作用列表;
        else delete 即时技能.副作用列表;
        return 即时技能;
      }

      function 是否战斗即时使用造物(action = {}, skill = {}) {
        if (action?.立即使用 !== true && action?.立即食用 !== true && action?.即食 !== true) return false;
        return !!构建造物即时使用技能(skill, action);
      }

      function 读取复刻技能次数记录(角色 = {}, 技能 = {}) {
        const 复制效果键 = String(技能?.__复制效果键 || '').trim();
        const 复制效果技能键 = String(技能?.__复制效果技能键 || '').trim();
        const 技能记录 = 复制效果键 && 复制效果技能键 ? 角色?.复制效果?.[复制效果键]?.技能列表?.[复制效果技能键] : null;
        if (技能记录) return { 类型: '复制效果', 复制效果键, 技能键: 复制效果技能键, 记录: 技能记录 };
        const 状态名 = String(技能?.__复制状态名 || '').trim();
        const 状态技能键 = String(技能?.__复制状态技能键 || '').trim();
        const 状态技能记录 = 状态名 && 状态技能键 ? 角色?.状态效果?.[状态名]?.技能列表?.[状态技能键] : null;
        if (状态技能记录) return { 类型: '复制状态', 状态名, 技能键: 状态技能键, 记录: 状态技能记录 };
        return null;
      }

      function 准备复刻技能使用权(角色 = {}, 技能 = {}) {
        const 次数记录 = 读取复刻技能次数记录(角色, 技能);
        if (!次数记录 || 次数记录.记录?.剩余次数 === undefined) return { 可释放: true, 日志: '', 提交: () => '' };
        const 剩余次数 = Math.max(0, Math.floor(Number(次数记录.记录.剩余次数 || 0)));
        if (剩余次数 <= 0) return { 可释放: false, 日志: ` [技能复刻] [${次数记录.技能键}]可用次数已耗尽。`, 提交: () => '' };
        return {
          可释放: true,
          日志: '',
          提交: () => {
            const 当前记录 = 读取复刻技能次数记录(角色, 技能);
            if (!当前记录 || 当前记录.记录?.剩余次数 === undefined) return '';
            const 当前剩余 = Math.max(0, Math.floor(Number(当前记录.记录.剩余次数 || 0)));
            if (当前剩余 <= 0) return '';
            当前记录.记录.剩余次数 = 当前剩余 - 1;
            if (当前记录.记录.剩余次数 > 0) return ` [技能复刻] [${当前记录.技能键}]剩余${当前记录.记录.剩余次数}次。`;
            if (当前记录.类型 === '复制效果') {
              delete 角色.复制效果[当前记录.复制效果键].技能列表[当前记录.技能键];
              if (!Object.keys(角色.复制效果[当前记录.复制效果键].技能列表 || {}).length) delete 角色.复制效果[当前记录.复制效果键].技能列表;
              if (!角色.复制效果[当前记录.复制效果键].技能列表 && !角色.复制效果[当前记录.复制效果键].属性快照) delete 角色.复制效果[当前记录.复制效果键];
              if (!Object.keys(角色.复制效果 || {}).length) delete 角色.复制效果;
            } else if (当前记录.类型 === '复制状态') {
              delete 角色.状态效果[当前记录.状态名].技能列表[当前记录.技能键];
              if (!Object.keys(角色.状态效果[当前记录.状态名].技能列表 || {}).length) delete 角色.状态效果[当前记录.状态名];
            }
            return ` [技能复刻] 消耗[${当前记录.技能键}]最后1次复刻。`;
          },
        };
      }

      function 准备主动机制授予技能(角色 = {}, 技能 = {}) {
        const 状态名 = String(技能?.__机制授予状态名 || '').trim();
        if (!状态名 || 技能?.__机制授予主动技能 !== true) return { 可释放: true, 日志: '', 提交: () => '' };
        const 状态 = 角色?.状态效果?.[状态名];
        const 移除授予状态 = () => {
          if (角色?.状态效果) delete 角色.状态效果[状态名];
          if (角色?.召唤键) 同步召唤单位镜像(角色);
        };
        if (!状态 || String(状态.触发条件 || '').trim() !== '主动触发') return { 可释放: false, 日志: ` [机制授予] [${状态名}]已失效。`, 提交: () => '' };
        const 授予机制候选 = { ...状态, key: 状态名, name: 状态名, 原型: '机制授予' };
        const 完整授予移除规则 = 战斗机制抹消命中(角色, '中间机制', 授予机制候选, { 用途: '移除' });
        if (完整授予移除规则) {
          移除授予状态();
          return { 可释放: false, 日志: ` [机制授予] [${状态名}]被抹消。`, 提交: () => '' };
        }
        const 授予封锁规则 = 战斗机制抹消命中(角色, '中间机制', 授予机制候选, { 用途: '封锁' });
        if (授予封锁规则) {
          移除授予状态();
          return { 可释放: false, 日志: ` [机制授予] [${状态名}]被抹消。`, 提交: () => '' };
        }
        const 原效果列表 = getSkillEffects(技能).filter(effect => effect?.__机制授予主动触发 === true);
        const 可触发效果 = 原效果列表.filter(effect => {
          const 实际机制抹消规则 =
            战斗机制抹消命中(角色, '中间机制', effect, { 用途: '移除' }) ||
            战斗机制抹消命中(角色, '中间机制', effect, { 用途: '封锁' });
          return !实际机制抹消规则;
        });
        if (!可触发效果.length) return { 可释放: false, 日志: ` [机制授予] [${状态名}]的授予效果被抹消。`, 提交: () => '' };
        技能._效果数组 = [
          ...(Array.isArray(技能._效果数组) ? 技能._效果数组 : []).filter(effect => effect?.__机制授予主动触发 !== true),
          ...可触发效果.map(effect => deepClonePlain(effect)),
        ];
        return {
          可释放: true,
          日志: '',
          提交: () => {
            const 当前状态 = 角色?.状态效果?.[状态名];
            if (!当前状态 || String(当前状态.触发条件 || '').trim() !== '主动触发') return '';
            const 剩余次数 = Math.max(1, Math.floor(Number(当前状态.可用次数 ?? 1))) - 1;
            if (剩余次数 <= 0) 移除授予状态();
            else 当前状态.可用次数 = 剩余次数;
            return ` [机制授予] 主动触发[${状态名}]${剩余次数 > 0 ? `，剩余${剩余次数}次` : '，授予权已耗尽'}。`;
          },
        };
      }

      function 准备临时技能使用权(角色 = {}, 技能 = {}) {
        const 复刻结果 = 准备复刻技能使用权(角色, 技能);
        if (!复刻结果.可释放) return 复刻结果;
        const 授予结果 = 准备主动机制授予技能(角色, 技能);
        if (!授予结果.可释放) return 授予结果;
        return {
          可释放: true,
          日志: `${复刻结果.日志 || ''}${授予结果.日志 || ''}`,
          提交: () => `${复刻结果.提交 ? 复刻结果.提交() : ''}${授予结果.提交 ? 授予结果.提交() : ''}`,
        };
      }

      function 触发行动前效果授予(角色 = {}, 技能 = {}, 敌方 = null, 动作 = {}) {
        if (!角色?.状态效果 || typeof 角色.状态效果 !== 'object') return { skill: 技能, 日志: '', 追加效果: [] };
        const 授予条目 = Object.entries(角色.状态效果).filter(([, 状态]) => {
          if (!状态 || typeof 状态 !== 'object') return false;
          return 状态.效果授予状态 === true || 状态.战斗效果?.mechanism_grant === true;
        });
        if (!授予条目.length) return { skill: 技能, 日志: '', 追加效果: [] };
        const 追加效果 = [];
        const 日志 = [];
        const 本次效果 = getSkillEffects(技能, { 行为规划: true });
        const 本次原型集 = new Set(本次效果.map(effect => String(effect?.原型 || '').trim()).filter(Boolean));
        const 本次行动类型 = String(动作?.action_type || 动作?.type || '').trim();
        const 授予触发条件匹配 = 状态 => {
          const 触发条件 = String(状态?.触发条件 || '').trim() || '随下次行动触发';
          if (触发条件 !== '随下次行动触发') return false;
          return 本次行动类型 === '技能' || !!技能 || 本次原型集.size > 0;
        };
        const 授予效果目标列表 = effect => {
          const 目标 = String(effect?.目标 || '').trim();
          if (/敌方|目标|单体|群体/.test(目标)) return 敌方 ? [敌方] : [];
          return [角色];
        };
        const 授予效果可执行 = effect => {
          const 原型名 = String(effect?.原型 || '').trim();
          const targets = 授予效果目标列表(effect);
          if (/敌方|目标|单体|群体/.test(String(effect?.目标 || '').trim()) && !targets.length) return false;
          if (原型名 === '状态移除') return targets.some(target => countConditionsByPrototypeFilter(target, effect) > 0);
          if (原型名 === '状态交换') return true;
          if (原型名 === '状态转移') {
            const 来源 = String(effect?.来源 || '自身').trim();
            const 去向 = String(effect?.去向 || '目标').trim();
            if (!['自身', '目标', '友方', '敌方'].includes(来源) || !['自身', '目标', '友方', '敌方'].includes(去向)) return false;
            const 端点列表 = 端点 => (端点 === '自身' || 端点 === '友方' ? [角色] : targets);
            const 来源列表 = 端点列表(来源);
            const 去向列表 = 端点列表(去向);
            return 来源列表.some(source =>
              source &&
              countConditionsByPrototypeFilter(source, effect) > 0 &&
              去向列表.some(receiver => receiver && !isCombatUnitIdentityMatch(receiver, source?.name || source)),
            );
          }
          return true;
        };
        const 移除授予状态 = 状态名 => {
          delete 角色.状态效果[状态名];
          if (角色.召唤键) 同步召唤单位镜像(角色);
        };
        授予条目.forEach(([状态名, 状态]) => {
          const 授予机制候选 = {
            ...状态,
            key: 状态名,
            name: 状态名,
            原型: '机制授予',
          };
          const 完整授予移除规则 = 战斗机制抹消命中(角色, '中间机制', 授予机制候选, { 用途: '移除' });
          if (完整授予移除规则) {
            移除授予状态(状态名);
            日志.push(`[${状态名}被抹消]`);
            return;
          }
          const 中间机制抹消规则 = 战斗机制抹消命中(角色, '中间机制', 授予机制候选, { 用途: '封锁' });
          if (中间机制抹消规则) {
            移除授予状态(状态名);
            日志.push(`[${状态名}被抹消]`);
            return;
          }
          if (!授予触发条件匹配(状态)) return;
          const 原效果列表 = Array.isArray(状态.授予效果) ? 状态.授予效果 : [];
          const 可执行效果 = 原效果列表
            .filter(effect => effect && typeof effect === 'object')
            .map(effect => {
              const next = deepClone(effect);
              const meta = getBattleEffectRuntimeMeta(next) || {};
              const 目标语义 = String(meta?.目标语义 || '').trim();
              if (目标语义 === '敌对') {
                next.目标 = '单体';
              } else if (目标语义 === '仅自身' || meta?.仅自身 === true) {
                next.目标 = '自身';
              }
              next.__授予前置触发 = true;
              return next;
            })
            .filter(effect => {
              const 实际机制抹消规则 =
                战斗机制抹消命中(角色, '中间机制', effect, { 用途: '移除' }) ||
                战斗机制抹消命中(角色, '中间机制', effect, { 用途: '封锁' });
              if (!实际机制抹消规则) return true;
              日志.push(`[${状态名}:${读取战斗机制抹消对象摘要(实际机制抹消规则.抹消对象)}被抹消]`);
              return false;
            });
          if (!角色.状态效果?.[状态名]) return;
          const 需要敌方 = 可执行效果.some(effect => {
            const meta = getBattleEffectRuntimeMeta(effect) || {};
            const 目标文本 = String(effect?.目标 || '').trim();
            return meta?.目标语义 === '敌对' || ['单体', '群体'].includes(目标文本);
          });
          if (需要敌方 && !敌方) return;
          if (!可执行效果.length) return;
          if (!可执行效果.every(授予效果可执行)) return;
          追加效果.push(...可执行效果);
          const 剩余次数 = Math.max(1, Math.floor(Number(状态.可用次数 ?? 1))) - 1;
          if (剩余次数 <= 0) 移除授予状态(状态名);
          else 状态.可用次数 = 剩余次数;
          日志.push(`[${状态名}]`);
        });
        if (!追加效果.length) return { skill: 技能, 日志: 日志.length ? ` [机制授予] ${日志.join('、')}。` : '', 追加效果: [] };
        const nextSkill = deepClone(技能 || {});
        nextSkill._效果数组 = [...(Array.isArray(nextSkill._效果数组) ? nextSkill._效果数组 : []), ...追加效果];
        return { skill: nextSkill, 日志: ` [机制授予] 触发${日志.join('、')}。`, 追加效果 };
      }

      function executeClash(playerAction, npcAction, combatData) {
        hydrateCombatData(combatData);
        let attacker = combatData.参战者.team_player?.[0];
        let attackerFinalStat = attacker.final || attacker;
        const 指定目标单位 = findCombatUnitByName(combatData, playerAction?.target_name || '');
        let defender =
          指定目标单位 && (combatData?.参战者?.team_enemy || []).some(unit => isCombatUnitIdentityMatch(unit, 指定目标单位.name || 指定目标单位))
            ? 指定目标单位
            : combatData.参战者.team_enemy?.[0];
        let defenderFinalStat = defender.final || defender;
        const 本次动作类型 = String(playerAction?.action_type || playerAction?.type || '').trim();
        const 本次携带物品名 = String(playerAction?.物品名 || playerAction?.skill?.__物品名 || '').trim();
        const 本次为物品动作 = /^(使用物品|食用物品)$/.test(本次动作类型);
        const 本次使用物品名 = 本次为物品动作 ? 本次携带物品名 : '';
        const 本次为维持释放 = playerAction?.__维持释放 === true;
        清理战斗阶段触发锁(attacker, defender, ...(combatData?.参战者?.team_player || []), ...(combatData?.参战者?.team_enemy || []));
        let result = { dmg: 0, desc: '', extraPatchOps: [], targetResults: [] };
        if (本次动作类型 === '施法失败') {
          const actorName = attacker?.name || attacker?.名称 || '施术者';
          const skillName = playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.__原定技能名 || playerAction?.__原定动作名 || '行动';
          result.desc += ` [行动取消] ${actorName}的【${skillName}】未能完成，本次结算不落地。`;
          return result;
        }
        const 读取临时技能单位摘要 = 单位 => {
          if (!单位) return '';
          const 属性 = 单位.属性 || 单位;
          return JSON.stringify({
            HP: Number(属性.HP ?? 单位.hp ?? 0),
            魂力: Number(属性.魂力 ?? 单位.sp ?? 0),
            体力: Number(属性.体力 ?? 单位.vit ?? 单位.sta ?? 0),
            精神力: Number(属性.精神力 ?? 单位.men ?? 0),
            状态效果: Object.entries(单位.状态效果 || {}).map(([键, 值]) => [
              键,
              Number(值?.duration ?? 0),
              Number(值?.层数 ?? 0),
              Number(值?.shield_value ?? 值?.护盾值 ?? 0),
              Array.isArray(值?.抹消规则) ? 值.抹消规则.length : 0,
            ]),
          });
        };
        const 创建临时技能落地快照 = (...单位列表) => ({
          描述长度: String(result.desc || '').length,
          伤害: Number(result.dmg || result.totalProjectedDamage || 0),
          目标结果数: Array.isArray(result.targetResults) ? result.targetResults.length : 0,
          单位列表,
          单位摘要: 单位列表.map(读取临时技能单位摘要),
        });
        const 判断临时技能真实落地 = 快照 => {
          if (!快照) return false;
          const 新描述 = String(result.desc || '').slice(快照.描述长度);
          const 有伤害 = Number(result.dmg || result.totalProjectedDamage || 0) > Number(快照.伤害 || 0) ||
            (Array.isArray(result.targetResults) && result.targetResults.some(entry => Number(entry?.damage || 0) > 0));
          const 状态或资源变化 = (快照.单位列表 || [])
            .map(读取临时技能单位摘要)
            .some((摘要, index) => 摘要 !== 快照.单位摘要[index]);
          const 成功文本 = /移除了|施加了|获得\[|进入\[|机制抹消|原型授予|技能复刻|属性复制|护盾|回复|恢复|流失|扣减/.test(新描述);
          const 失败文本 = /没有匹配状态|被抹消|被阻断|未能|失败|不可复刻|未捕捉|未找到可复制/.test(新描述);
          return 有伤害 || 状态或资源变化 || (成功文本 && !失败文本);
        };
        const 技能是临时技能 = 技能 => !!(
          技能 &&
          (
            技能.__机制授予主动技能 === true ||
            技能.__复制效果键 ||
            技能.__复制状态名
          )
        );
        if (本次携带物品名 && !本次为物品动作) {
          result.desc += ' [物品使用失败] 只有选择使用物品时才能调用背包物品。';
          playerAction.action_type = '施法失败';
          return result;
        }
        if (本次为物品动作 && !本次使用物品名) {
          result.desc += ' [物品使用失败] 未指定要使用的背包物品。';
          playerAction.action_type = '施法失败';
          return result;
        }
        const 判断NPC临时技能对抗结算落地 = 快照 => {
          if (!快照 || !技能是临时技能(npcAction?.skill)) return false;
          const 技能名 = String(npcAction.skill?.name || npcAction.skill?.魂技名 || '').trim();
          const 新描述 = String(result.desc || '').slice(快照.描述长度);
          const 命中玩家 = Array.isArray(result.targetResults) && result.targetResults.some(entry =>
            entry?.target &&
            isCombatUnitIdentityMatch(entry.target, attacker?.name || attacker) &&
            Number(entry?.damage || 0) > 0,
          );
          if (命中玩家) return true;
          if (!技能名 || !新描述.includes(`NPC的[${技能名}]`)) return false;
          return /威力更胜|完美挡下|成功打断|试图打断/.test(新描述);
        };

        playerAction.skill = normalizeSkillData(
          playerAction.skill || {
            name: '普通攻击',
            技能分类: '输出',
            消耗: '无',
            前摇: 10,
            _效果数组: [
              { 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 },
            ],
          },
          playerAction.skill?.name || '普通攻击',
        );
        if (!本次为物品动作 && !装备技能跳过掌控缩放(playerAction.skill)) playerAction.skill = 按技能掌控度缩放技能(playerAction.skill, attacker);
        if (本次为物品动作) {
          const 当前tick = Number(window.BattleUIBridge?.getMVU('world.时间.tick') || 0);
          const 使用者名 = String(attacker?.name || window.BattleUIBridge?.getMVU('sys.玩家名') || '').trim();
          const 当前背包 = attacker?.背包 && typeof attacker.背包 === 'object' ? attacker.背包 : {};
          const 物品数据 = 读取战斗背包物品数据(本次使用物品名, 当前背包?.[本次使用物品名]);
          const 校验结果 = 校验战斗背包物品可用(本次使用物品名, 物品数据, 使用者名, 当前tick);
          if (!校验结果.可用) {
            result.desc += ` [物品使用失败] ${校验结果.原因}。`;
            playerAction.action_type = '施法失败';
            return result;
          }
          const 消耗补丁包 = 构建战斗背包消耗补丁包(本次使用物品名, 当前背包, 使用者名, 1);
          if (消耗补丁包.patchOps.length) result.extraPatchOps.push(...消耗补丁包.patchOps);
          if (消耗补丁包.log) result.desc += 消耗补丁包.log;
        }
        const 玩家临时技能使用权 = 准备临时技能使用权(attacker, playerAction.skill);
        if (!玩家临时技能使用权.可释放) {
          result.desc += 玩家临时技能使用权.日志;
          playerAction.action_type = '施法失败';
          return result;
        }
        if (玩家临时技能使用权.日志) result.desc += 玩家临时技能使用权.日志;
        if (npcAction.skill) {
          npcAction.skill = normalizeSkillData(
            npcAction.skill,
            npcAction.skill.name || npcAction.skill.技能名称 || '未知技能',
          );
        }
        const NPC临时技能使用权 = npcAction.skill ? 准备临时技能使用权(defender, npcAction.skill) : { 可释放: true, 日志: '', 提交: () => '' };
        if (!NPC临时技能使用权.可释放) {
          result.desc += NPC临时技能使用权.日志;
          npcAction.type = '行动失败';
          npcAction.skill = null;
        } else if (NPC临时技能使用权.日志) {
          result.desc += NPC临时技能使用权.日志;
        }
        const 玩家临时技能快照 = 创建临时技能落地快照(attacker, defender);
        const NPC临时技能快照 = npcAction.skill ? 创建临时技能落地快照(defender, attacker) : null;
        let 玩家临时技能已生效 = false;
        let NPC临时技能已生效 = false;
        let 本次为即时使用造物 = false;
        let 本次原始造物技能 = null;
        if (是否战斗即时使用造物(playerAction, playerAction.skill)) {
          本次原始造物技能 = deepClonePlain(playerAction.skill);
          const 即时使用技能 = 构建造物即时使用技能(playerAction.skill, playerAction);
          if (即时使用技能) {
            本次为即时使用造物 = true;
            playerAction.skill = normalizeSkillData(即时使用技能, 即时使用技能.name || 即时使用技能.魂技名 || '造物承载魂技·即时使用');
            result.desc += ` [造物即时使用] ${playerAction.target_name ? `目标:${playerAction.target_name}。` : '默认自身。'}`;
          }
        }
        if (!本次为即时使用造物 && !本次为维持释放) {
          const 授予触发结果 = 触发行动前效果授予(attacker, playerAction.skill, defender, playerAction);
          if (授予触发结果.日志) {
            playerAction.skill = normalizeSkillData(
              授予触发结果.skill,
              授予触发结果.skill?.name || 授予触发结果.skill?.魂技名 || playerAction.skill?.name || '技能',
            );
            if (!本次为物品动作 && !装备技能跳过掌控缩放(playerAction.skill)) playerAction.skill = 按技能掌控度缩放技能(playerAction.skill, attacker);
            playerAction.__授予前置效果列表 = Array.isArray(授予触发结果.追加效果) ? 授予触发结果.追加效果 : [];
            result.desc += 授予触发结果.日志;
          }
        }
        if (npcAction.skill) {
          const NPC授予触发结果 = 触发行动前效果授予(defender, npcAction.skill, attacker, npcAction);
          if (NPC授予触发结果.日志) {
            npcAction.skill = normalizeSkillData(
              NPC授予触发结果.skill,
              NPC授予触发结果.skill?.name || NPC授予触发结果.skill?.魂技名 || npcAction.skill?.name || 'NPC技能',
            );
            npcAction.__授予前置效果列表 = Array.isArray(NPC授予触发结果.追加效果) ? NPC授予触发结果.追加效果 : [];
            result.desc += NPC授予触发结果.日志;
          }
        }
        const 遥控触发结果 = 触发遥控状态效果(
          attacker,
          defender,
          playerAction.玩家输入 || playerAction.raw_input || playerAction.input || playerAction.action_type || '',
          playerAction.skill?.name || playerAction.skill?.魂技名 || playerAction.action_type || '遥控触发',
        );
        if (遥控触发结果.日志) result.desc += ` ${遥控触发结果.日志}`;

        const 选定目标名 = String(playerAction?.target_name || '').trim();
        const 条件分支目标 = findCombatUnitByName(combatData, 选定目标名) || defender;
        const 当前技能条件上下文 = {
          actor: attacker,
          caster: attacker,
          carrier: attacker,
          equipper: attacker,
          attacker,
          target: 条件分支目标,
          defender: 条件分支目标,
          combatData,
          action: playerAction,
          skill: playerAction?.skill || null,
          当前行动: String(playerAction?.action_type || playerAction?.type || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '').trim(),
        };
        if (本次为物品动作 && playerAction?.skill?.__造物产出者属性 && typeof playerAction.skill.__造物产出者属性 === 'object')
          当前技能条件上下文.造物产出者属性 = playerAction.skill.__造物产出者属性;
        const 当前行动被动效果 = 本次为维持释放 ? [] : 收集当前行动被动效果(attacker, combatData?.参战者?.team_player || [], 当前技能条件上下文);
        if (当前行动被动效果.length) {
          const 当前主动水合效果 = getSkillEffects(playerAction.skill, 当前技能条件上下文);
          playerAction.skill = {
            ...deepClonePlain(playerAction.skill || {}),
            __规划水合效果数组: [...当前主动水合效果, ...当前行动被动效果],
          };
          当前技能条件上下文.skill = playerAction.skill;
        }
        const getEffect = (sk, types) => getSkillEffects(sk, 当前技能条件上下文).find(e => {
          const 原型 = String(e?.原型 || '').trim();
          return types.includes(原型);
        }) || {};

        const 是延迟登记效果 = effect =>
          Number(effect?.延迟回合 || 0) > 0 &&
          战斗原型允许延迟回合(effect?.原型);
        const 是遥控触发效果 = effect => String(effect?.触发方式 || '').trim() === '遥控触发';
        let pClash = getSkillEffects(playerAction.skill, 当前技能条件上下文).find(e =>
          String(e?.原型 || '').trim() === '伤害结算' && !是延迟登记效果(e),
        ) || {};
        const 非延迟状态施加效果列表 = () =>
          getSkillEffects(playerAction.skill, 当前技能条件上下文).filter(effect =>
            String(effect?.原型 || '').trim() === '状态施加' && !是延迟登记效果(effect) && !是遥控触发效果(effect),
          );
        const 独立状态施加效果列表 = () =>
          非延迟状态施加效果列表().filter(effect => String(effect?.生效方式 || '').trim() !== '跟随主原型');
        const 跟随状态施加效果列表 = () =>
          非延迟状态施加效果列表().filter(effect => String(effect?.生效方式 || '').trim() === '跟随主原型');
        let pState = 独立状态施加效果列表()[0] || {};
        let 授予前置效果列表 = [];
        let 执行授予前置效果 = () => {};
        if (!pState || typeof pState !== 'object' || Array.isArray(pState)) pState = {};
        if (!pState.状态名称) pState.状态名称 = '无';
        if (!pState.特殊机制标识) pState.特殊机制标识 = '无';
        if (!pState.目标) pState.目标 = getSkillTarget(playerAction.skill);
        if (!pState.面板修改比例 || typeof pState.面板修改比例 !== 'object') pState.面板修改比例 = {};
        if (!pState.计算层效果 || typeof pState.计算层效果 !== 'object')
          pState.计算层效果 = createEmptyCombatEffectMap();
        if (typeof pState.持续回合 !== 'number') pState.持续回合 = Number(pState.持续回合 || 0);
        const 读取源动作抹消规则 = (effect, options = {}) => 战斗机制抹消命中(attacker, '源动作', effect, { 用途: '封锁', ...options });
        const 描述源动作抹消 = 源动作抹消规则 => {
          if (!源动作抹消规则) return false;
          result.desc += ` [机制抹消] ${attacker?.name || '施术者'}无法使用【${读取战斗机制抹消对象摘要(源动作抹消规则.抹消对象)}】，本次效果被阻断。`;
          return true;
        };
        const 源动作被机制抹消 = effect => {
          const 源动作抹消规则 = 读取源动作抹消规则(effect);
          if (!源动作抹消规则) return false;
          return 描述源动作抹消(源动作抹消规则);
        };
        let nClash = getEffect(npcAction.skill, [
          '伤害结算',
          '护盾',
          '减伤',
          '免伤',
        ]);

        applyRuntimeMechanismEffects(
          playerAction.skill,
          attacker,
          attackerFinalStat,
          defender,
          defenderFinalStat,
          pState,
          当前技能条件上下文,
        );
        const 主动时光回溯日志 = 本次为维持释放 ? '' : 执行主动时光回溯效果(playerAction.skill, attacker, defender, combatData, 当前技能条件上下文);
        if (主动时光回溯日志) result.desc += ` ${主动时光回溯日志}`;

        const fusionPrecastEffect = 读取一次性武魂融合前置战斗效果(pState);
        if (fusionPrecastEffect) {
          attackerFinalStat = 套用一次性武魂融合前置属性(attackerFinalStat, pState);
          result.desc += ` [武魂融合] 基础属性融合先于本次释放生效。`;
        }

        let aStr = attackerFinalStat.str;
        let aAgi = attackerFinalStat.agi;
        let dAgi = defenderFinalStat.agi;

        let hasSuperArmor = false;
        if (attacker.状态效果) {
          for (let key in attacker.状态效果) {
            if (
              attacker.状态效果[key].战斗效果?.super_armor === true ||
              key.includes('霸体') ||
              key.includes('真身')
            )
              hasSuperArmor = true;
          }
        }

        const npcSkillType = getSkillType(npcAction.skill);
        if (npcAction.type === '控制截断' && npcSkillType === '控制' && getSkillCastTime(npcAction.skill) < 10) {
          const npc精神压制 = 计算当前资源压制倍率(defender, defenderFinalStat, attacker, attackerFinalStat, 'men', {
            下限: 0,
            上限: 2,
            压制指数: 1,
          });
          if (npc精神压制 >= 1 || dAgi > aAgi) {
            if (hasSuperArmor) {
              result.desc = `NPC释放[${npcAction.skill.name}]试图打断，但玩家处于霸体状态，强行免疫了控制！`;
            } else {
              result.desc = `NPC释放[${npcAction.skill.name}]成功打断玩家施法！玩家遭到反噬。`;
              return result;
            }
          }
        }

        let attackerConditionEffects = attacker.状态效果
          ? Object.values(attacker.状态效果).map(c => c?.战斗效果 || {})
          : [];
        if (fusionPrecastEffect) attackerConditionEffects.push(fusionPrecastEffect);
        const defenderConditionEffects = defender.状态效果
          ? Object.values(defender.状态效果).map(c => c?.战斗效果 || {})
          : [];
        const 技能效果增幅结果 = 套用技能效果增幅(playerAction.skill, attackerConditionEffects);
        if (技能效果增幅结果.已增幅) {
          playerAction.skill = normalizeSkillData(
            技能效果增幅结果.skill,
            技能效果增幅结果.skill?.name || 技能效果增幅结果.skill?.技能名称 || playerAction?.skill?.name || '技能',
          );
          当前技能条件上下文.skill = playerAction.skill;
          pClash = getSkillEffects(playerAction.skill, 当前技能条件上下文).find(e =>
            String(e?.原型 || '').trim() === '伤害结算' && !是延迟登记效果(e),
          ) || {};
          pState = 独立状态施加效果列表()[0] || {};
          if (!pState || typeof pState !== 'object' || Array.isArray(pState)) pState = {};
          if (!pState.状态名称) pState.状态名称 = '无';
          if (!pState.特殊机制标识) pState.特殊机制标识 = '无';
          if (!pState.目标) pState.目标 = getSkillTarget(playerAction.skill);
          if (!pState.面板修改比例 || typeof pState.面板修改比例 !== 'object') pState.面板修改比例 = {};
          if (!pState.计算层效果 || typeof pState.计算层效果 !== 'object')
            pState.计算层效果 = createEmptyCombatEffectMap();
          if (typeof pState.持续回合 !== 'number') pState.持续回合 = Number(pState.持续回合 || 0);
          const branchFusionPrecastEffect = 读取一次性武魂融合前置战斗效果(pState);
          if (!fusionPrecastEffect && branchFusionPrecastEffect) {
            attackerFinalStat = 套用一次性武魂融合前置属性(attackerFinalStat, pState);
            attackerConditionEffects.push(branchFusionPrecastEffect);
            aStr = attackerFinalStat.str;
            aAgi = attackerFinalStat.agi;
            result.desc += ` [武魂融合] 基础属性融合先于本次释放生效。`;
          }
          result.desc += ` [技能效果增幅] 本次技能效果倍率 x${技能效果增幅结果.效果倍率.toFixed(2)}，有数量时优先生效。`;
        }
        if (String(pClash?.原型 || '').trim() === '伤害结算' && 源动作被机制抹消(pClash)) pClash = {};

        const actorCharData = attacker?.name ? window.BattleUIBridge?.getMVU(`char.${attacker.name}`) : null;
        const preferredPlayerName = String(window.BattleUIBridge?.getMVU('sys.玩家名') || '').trim();
        const attackerName = String(attacker?.name || preferredPlayerName || '').trim();
        const creationOwnerName = String(playerAction?.物品接收者 || playerAction?.食用目标 || attackerName).trim() || attackerName;
        const creationOwnerCharData = creationOwnerName ? window.BattleUIBridge?.getMVU(`char.${creationOwnerName}`) : null;
        const canPersistCreation = !!creationOwnerName;
        const creationPatchBundle = 构建造物承载补丁包(
          { ...(本次原始造物技能 || playerAction.skill), __造物产出者: { ...attacker, final: attackerFinalStat } },
          creationOwnerCharData?.背包 || (creationOwnerName === attackerName ? actorCharData?.背包 || attacker?.背包 : {}) || {},
          creationOwnerName,
          { 即时消耗数量: 本次为即时使用造物 ? 1 : 0 },
        );
        if (!本次为维持释放 && canPersistCreation && creationPatchBundle.patchOps.length > 0) {
          result.extraPatchOps = creationPatchBundle.patchOps;
          result.desc += creationPatchBundle.log;
          写入战斗事件账本(combatData, {
            eventKind: 'create',
            round: Number(combatData?.回合 || 0),
            actorName: attackerName,
            targetName: creationOwnerName,
            actionName: (本次原始造物技能 || playerAction.skill)?.name || (本次原始造物技能 || playerAction.skill)?.魂技名 || skillName || '造物承载',
            actionType: 'create',
            sourceActionName: (本次原始造物技能 || playerAction.skill)?.name || (本次原始造物技能 || playerAction.skill)?.魂技名 || skillName || '造物承载',
            sourceRound: Number(combatData?.回合 || 0),
            result: 'created',
            meta: {
              text: String(creationPatchBundle.log || '').replace(/^\s*\[造物承载\]\s*/, '').trim(),
              ownerName: creationOwnerName,
              patchOpsCount: Array.isArray(creationPatchBundle.patchOps) ? creationPatchBundle.patchOps.length : 0,
            },
          });
          const hasDirectClash = Number(pClash.威力倍率 || 0) > 0;
          const hasStateApply = String(pState?.状态名称 || '无') !== '无';
          if (!本次为即时使用造物 && !hasDirectClash && !hasStateApply) return result;
        }

        const skillRuntimeMeta = getSkillRuntimeMeta(playerAction.skill);
        let grazeMultiplier = 1.0;
        if (是延迟登记效果(pClash)) pClash = {};

        let pCalc = pState.计算层效果 || {};
        const currentSkillHitBonus = Number(pCalc.hit_bonus || 0);
        const currentSkillHitPenalty = Number(pCalc.hit_penalty || 0);
        const currentSkillDodgePenalty = Number(pCalc.dodge_penalty || 0);
        const currentSkillLockLevel = Number(pCalc.lock_level || 0);
        const attackerIsSilenced = attackerConditionEffects.some(ce => ce?.silence === true);
        const attackerIsDisarmed = attackerConditionEffects.some(ce => ce?.disarm === true);
        const attackerIsBlinded = attackerConditionEffects.some(ce => ce?.blind === true);
        const attackerIsSkillSealed = attackerConditionEffects.some(ce => ce?.skill_seal === true);
        const attackerInterruptBonus = attackerConditionEffects.reduce(
          (sum, ce) => sum + Number(ce?.interrupt_bonus || 0),
          0,
        );
        const actionEffects = getSkillEffects(playerAction.skill, 当前技能条件上下文);
        授予前置效果列表 = actionEffects.filter(effect => effect?.__授予前置触发 === true);
        if (!授予前置效果列表.length && Array.isArray(playerAction.__授予前置效果列表)) {
          授予前置效果列表 = playerAction.__授予前置效果列表;
        }
        const 常规行动效果 = actionEffects.filter(effect => effect?.__授予前置触发 !== true);
        const 主原型效果 = 常规行动效果.find(effect =>
          effect &&
          !是遥控触发效果(effect) &&
          String(effect?.生效方式 || '').trim() !== '跟随主原型' &&
          !(Number(effect?.延迟回合 || 0) > 0 && 战斗原型允许延迟回合(effect?.原型))
        ) || null;
        const 主原型成立目标集合 = new Set();
        const 构建原型效果匹配键 = effect => {
          if (!effect) return '';
          return [
            effect?.原型,
            effect?.目标,
            effect?.状态,
            effect?.结算,
            effect?.资源 || effect?.资源类型,
            effect?.判定,
            effect?.位移类型,
            effect?.伤害类型,
            effect?.数值 ?? effect?.护盾值 ?? effect?.恢复比例 ?? effect?.回复比例 ?? '',
          ].map(value => String(value ?? '').trim()).join('::');
        };
        const 主原型匹配键 = 构建原型效果匹配键(主原型效果);
        const 是主原型效果 = effect => !!effect && 主原型匹配键 && 构建原型效果匹配键(effect) === 主原型匹配键;
        const 登记主原型成立目标 = 目标对象 => {
          if (!目标对象) return;
          const 目标名 = String(目标对象?.name || 目标对象?.名称 || '').trim();
          if (目标名) 主原型成立目标集合.add(目标名);
        };
        const 状态施加视为友方或增益 = (状态施加效果 = {}) => {
          const 状态目标 = String(状态施加效果?.目标 || '').trim();
          return (
            状态目标 === '自身' ||
            状态目标 === '友方单体' ||
            状态目标 === '友方群体' ||
            String(状态施加效果?.特殊机制标识 || '无').includes('增益') ||
            状态施加效果?.计算层效果?.无视异常 === true ||
            状态施加效果?.计算层效果?.super_armor === true ||
            状态施加效果?.计算层效果?.探查屏蔽 === true ||
            Number(状态施加效果?.计算层效果?.min_hp_floor || 0) > 0 ||
            Number(状态施加效果?.计算层效果?.hot_heal_ratio || 0) > 0
          );
        };
          const 目标本次明确规避主效果 = 目标对象 => Array.isArray(result.targetResults) && result.targetResults.some(entry => {
            if (!entry?.target || !目标对象 || !isCombatUnitIdentityMatch(entry.target, 目标对象)) return false;
            const 日志文本 = Array.isArray(entry.logs) ? entry.logs.join(' ') : String(entry.log || '');
            return (
              (entry?.evaded === true || Number(entry?.damage || 0) <= 0) &&
              /绝对闪避|主动闪避|闪避判定|完全躲过|规避了本次攻击|没有命中/.test(日志文本)
            );
          });
        const directHealEffect = 常规行动效果.find(effect => String(effect?.原型 || '').trim() !== '资源变化' && isBattleRecoverEffect(effect, ['hp'])) || null;
        const directSpEffect = 常规行动效果.find(effect => String(effect?.原型 || '').trim() !== '资源变化' && isBattleRecoverEffect(effect, ['sp'])) || null;
        const directMenEffect = 常规行动效果.find(effect => String(effect?.原型 || '').trim() !== '资源变化' && isBattleRecoverEffect(effect, ['men'])) || null;
        const directStatusRemoveEffects = 常规行动效果.filter(effect => String(effect?.原型 || '').trim() === '状态移除');
        const directCleanseEffect = 常规行动效果.find(effect => ['解控', '净化'].includes(读取战斗效果标签(effect))) || null;
        const directDispelEffect = 常规行动效果.find(effect => 读取战斗效果标签(effect) === '驱散增益') || null;
        const directStealBuffEffect = 常规行动效果.find(effect => 读取战斗效果标签(effect) === '窃取增益') || null;
        const directTauntEffect = 常规行动效果.find(effect => 读取战斗效果标签(effect) === '嘲讽') || null;
        const directGuardEffect = 常规行动效果.find(effect => 读取战斗效果标签(effect) === '护卫') || null;
        const 是指定原型 = (effect, 原型名) => String(effect?.原型 || '').trim() === 原型名;
        const 原型数值 = effect => 读取战斗数值正负(effect?.数值);
        const 护盾削减原型列表 = 常规行动效果.filter(effect => 是指定原型(effect, '护盾变化') && 原型数值(effect) < 0);
        const directShieldBreakEffect = 护盾削减原型列表[0] || null;
        const directResourceBurnEffect = 常规行动效果.find(effect =>
          !是遥控触发效果(effect) &&
          effect?.原型 === '状态施加' &&
          String(effect?.状态 || '').trim() === '资源燃烧'
        ) || null;
        const 遥控状态施加效果列表 = 常规行动效果.filter(effect =>
          是遥控触发效果(effect) &&
          String(effect?.原型 || '').trim() === '状态施加'
        );
        const directResourceLockEffect = 常规行动效果.find(effect => effect?.原型 === '资源锁定') || null;
        const directLifeLinkEffect = 常规行动效果.find(effect => 读取战斗效果标签(effect) === '生命链接') || null;
        const directDotExtendEffect = 常规行动效果.find(effect => effect?.原型 === '时窗修正' && String(effect?.调整方式 || '').trim() === '延长') || null;
        const directDotCompressEffect = 常规行动效果.find(effect => effect?.原型 === '时窗修正' && String(effect?.调整方式 || '').trim() === '压缩') || null;
        const directDotDetonateEffect = 常规行动效果.find(effect => effect?.原型 === '结算修正' && String(effect?.结算 || '').trim() === '持续伤害引爆') || null;
        const directStatusTransferEffect = 常规行动效果.find(effect => effect?.原型 === '状态转移') || null;
        const directExecuteEffect = 常规行动效果.find(effect =>
          String(effect?.原型 || '').trim() === '结算修正' &&
          String(effect?.结算 || '').trim() === '造成伤害' &&
          读取战斗数值正负(effect?.数值) > 0 &&
          String(effect?.驱动属性 || '').trim() === '体力上限',
        ) || null;
        const 直接结算修正命中单位 = (effect, unit) => {
          if (!effect || !unit) return false;
          if (String(effect?.目标 || '').trim() === '全场') return true;
          return resolveSkillEffectTargetCharacters(playerAction.skill, effect, attacker, defender, combatData)
            .some(targetObj => isCombatUnitIdentityMatch(targetObj, unit));
        };
        const 读取直接结算转换比例 = (结算名, unit) => {
          return 常规行动效果.reduce((最大值, effect) => {
            if (String(effect?.原型 || '').trim() !== '结算修正') return 最大值;
            if (String(effect?.结算 || '').trim() !== 结算名) return 最大值;
            if (!当前技能元素匹配结算修正(playerAction.skill || {}, effect)) return 最大值;
            if (!直接结算修正命中单位(effect, unit)) return 最大值;
            const 比例 = Math.max(0, Math.abs(读取战斗数值正负(effect?.数值 || '100%')) || 1);
            return Math.max(最大值, Math.min(1, 比例));
          }, 0);
        };
        const 当前行动附加穿透 = 常规行动效果.reduce((总和, effect) => {
          if (String(effect?.原型 || '').trim() !== '结算修正') return 总和;
          if (String(effect?.结算 || '').trim() !== '防御穿透') return 总和;
          const 原始值 = Number(读取战斗数值正负(effect?.数值));
          if (!Number.isFinite(原始值) || 原始值 === 0) return 总和;
          return 总和 + (Math.abs(原始值) <= 1 ? 原始值 * 100 : 原始值);
        }, 0);
        const directRuleRewriteEffects = 常规行动效果.filter(effect =>
          String(effect?.原型 || '').trim() === '规则改写' &&
          ['缴械', '死亡转存活'].includes(String(effect?.规则 || '').trim()),
        );
        const directShieldEffect = 常规行动效果.find(effect =>
          读取战斗效果标签(effect) === '护盾' || (effect?.原型 === '护盾变化' && 读取战斗数值正负(effect?.数值) > 0),
        ) || null;
        const directSummonEffect =
          常规行动效果.find(effect => 是指定原型(effect, '召唤生成')) ||
          null;
        const directCopyEffect = 常规行动效果.find(effect => effect?.原型 === '复制执行') || null;
        const directGrantEffect = 常规行动效果.find(effect => effect?.原型 === '机制授予') || null;
        const directMechanismSuppressEffect = 常规行动效果.find(effect => effect?.原型 === '机制抹消') || null;
        const directStateExchangeEffects = 常规行动效果.filter(effect => effect?.原型 === '状态交换');
        const directPrototypeEffects = 常规行动效果.filter(effect =>
          effect?.原型 && (
            (战斗原型允许延迟回合(effect.原型) && Number(effect.延迟回合 || 0) > 0) ||
            (effect.原型 === '资源变化' && 读取战斗数值正负(effect?.数值) < 0) ||
            effect.原型 === '资源转移' ||
            (effect.原型 === '护盾变化' && 读取战斗数值正负(effect?.数值) < 0) ||
            (effect.原型 === '结算修正' && ['持续伤害引爆', '伤害分摊'].includes(String(effect?.结算 || '').trim()) && effect !== directDotDetonateEffect) ||
            (effect.原型 === '状态转移' && effect !== directStatusTransferEffect) ||
            (effect.原型 === '资源变化' && 读取战斗数值正负(effect?.数值) > 0) ||
            (effect.原型 === '时窗修正' && effect !== directDotExtendEffect && effect !== directDotCompressEffect) ||
            effect.原型 === '位移执行'
          ),
        );
        const 时光回溯预结算原型集合 = new Set([
          '状态施加',
          '资源变化',
          '资源转移',
          '资源锁定',
          '机制抹消',
          '状态交换',
          '状态转移',
          '状态移除',
          '护盾变化',
          '位移执行',
          '时窗修正',
          '结算修正',
          '机制授予',
        ]);
        const 标记时光回溯预结算落地 = (目标列表 = [], 标签 = '效果落地') => {
          const 目标数组 = dedupeCombatTargetList(Array.isArray(目标列表) ? 目标列表 : [目标列表]).filter(Boolean);
          if (!Array.isArray(result.targetResults)) result.targetResults = [];
          目标数组.forEach(targetObj => {
            let entry = result.targetResults.find(item => item?.target && isCombatUnitIdentityMatch(item.target, targetObj));
            if (!entry) {
              entry = {
                target: targetObj,
                targetName: targetObj?.name || targetObj?.名称 || '目标',
                damage: 0,
                kind: 'effect',
                damageSegments: [],
              };
              result.targetResults.push(entry);
            }
            if (!Array.isArray(entry.时光回溯预结算落地)) entry.时光回溯预结算落地 = [];
            if (!entry.时光回溯预结算落地.includes(标签)) entry.时光回溯预结算落地.push(标签);
          });
        };
        const 标记时光回溯效果落地 = effect => {
          if (!effect || !时光回溯预结算原型集合.has(String(effect?.原型 || '').trim())) return;
          if (String(effect?.原型 || '').trim() === '状态施加') {
            const 状态目标 = String(effect?.目标 || '').trim();
            const 视为友方或增益 =
              状态目标 === '自身' ||
              状态目标 === '友方单体' ||
              状态目标 === '友方群体' ||
              String(effect?.特殊机制标识 || '无').includes('增益') ||
              effect?.计算层效果?.无视异常 === true ||
              effect?.计算层效果?.super_armor === true ||
              effect?.计算层效果?.探查屏蔽 === true ||
              Number(effect?.计算层效果?.min_hp_floor || 0) > 0 ||
              Number(effect?.计算层效果?.hot_heal_ratio || 0) > 0;
            if (!视为友方或增益) return;
          }
          if (String(effect?.原型 || '').trim() === '结算修正' && !['持续伤害引爆', '伤害分摊'].includes(String(effect?.结算 || '').trim())) return;
          const targets = resolveSkillEffectTargetCharacters(playerAction.skill, effect, attacker, defender, combatData);
          标记时光回溯预结算落地(targets, String(effect?.原型 || '效果落地'));
        };
        const selfMirrorEffect = actionEffects.find(effect => 读取战斗效果标签(effect) === '自身也受影响') || null;
        const directSelfSacrificeEffect = actionEffects.find(effect => 读取战斗效果标签(effect) === '自残换收益') || null;
        const directRingBurstEffect = actionEffects.find(effect => 读取战斗效果标签(effect) === '炸环') || null;
        const skillName = String(
          playerAction?.skill?.name || playerAction?.skill?.技能名称 || playerAction?.action_type || '',
        );
        const fusionProfile = getFusionBattleProfileFromSkill(playerAction.skill);
        const 引爆移除状态标记集 = new Set();
        const 原始技能效果列表 = Array.isArray(playerAction?.skill?._效果数组) ? playerAction.skill._效果数组 : [];
        const 效果目标包含自身 = effect =>
          resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData, effect).targetSet.some(targetObj =>
            targetObj === attacker || isCombatUnitIdentityMatch(targetObj, attacker?.name || attacker),
          );
        const 条件分支禁用自身效果 = effect => {
          if (!Array.isArray(effect?.条件分支) || effect.条件分支.length <= 0) return false;
          if (!效果目标包含自身(effect)) return false;
          return 展开条件分支效果(effect, 当前技能条件上下文).length <= 0;
        };
        const 能力共享自身预禁用 = false;
        const 资源变化自身预禁用 = 原始技能效果列表.some(effect =>
          String(effect?.原型 || '').trim() === '资源变化' &&
          读取战斗数值正负(effect?.数值) > 0 &&
          条件分支禁用自身效果(effect),
        );
        if (能力共享自身预禁用 || 资源变化自身预禁用) attacker.__禁用本回合自然恢复 = true;
        if (getSkillEffects(playerAction.skill, 当前技能条件上下文).some(effect => String(effect?.原型 || '').trim() === '资源锁定')) {
          [...(combatData?.参战者?.team_player || []), ...(combatData?.参战者?.team_enemy || [])].forEach(unit => {
            if (unit) unit.__禁用本回合自然恢复 = true;
          });
        }
        let 能力共享自身禁用命中 = 能力共享自身预禁用;
        const isBasicAttack =
          !playerAction?.skill || skillName === '普通攻击' || playerAction?.action_type === '常规攻击';
        const 本次对应等级 = 构建对应等级数据(pClash);
        const 使用对应等级 = !!本次对应等级;
        const 攻势单位 = 使用对应等级 ? 本次对应等级.单位 : attacker;
        const 攻势最终属性 = 使用对应等级 ? 本次对应等级.最终属性 : attackerFinalStat;
        const 攻势敏捷 = Math.max(1, Number(攻势最终属性.agi || 攻势单位.agi || aAgi || 1));
        const 攻势精神上限 = Math.max(1, Number(攻势最终属性.men_max || 攻势单位.men_max || attackerFinalStat.men_max || 1));
        const 是近身接战动作 = !使用对应等级 && 战斗伤害是近身攻击(pClash.伤害类型);
        const 原动作具备即时伤害分支 = actionEffects.some(effect =>
          String(effect?.原型 || '').trim() === '伤害结算' &&
          Math.max(0, Number(effect?.威力倍率 || effect?.威力 || effect?.基础倍率 || 0)) > 0
        );
        const 登记行为防反候选 = (防反类型, 防反方, 参数 = {}) => {
          if (!防反方 || 读取行为防反深度(playerAction) >= 2 || result.__行为防反候选) return;
          if (原动作具备即时伤害分支 !== true) return;
          const 反应余量 = Number(
            参数.反应余量 ?? calculateReactionRatio(attacker, 防反方, playerAction, combatData),
          );
          const 出手承诺 = 计算行为出手承诺(playerAction);
          const 触发概率 = 计算行为防反概率({
            防反类型,
            防反方,
            攻击方: attacker,
            原动作: playerAction,
            反应动作: npcAction,
            反应余量,
            出手承诺,
            战斗数据: combatData,
            闪避率: 参数.闪避率,
            闪避投点: 参数.闪避投点,
            预计伤害: 参数.预计伤害,
            实际伤害: 参数.实际伤害,
            以命换伤: 参数.以命换伤,
          });
          if (!(触发概率 > 0)) return;
          const 来源动作 = 选择行为防反动作来源(防反方, combatData, 防反类型);
          result.__行为防反候选 = {
            防反类型,
            防反方,
            攻击方: attacker,
            原动作: playerAction,
            反应动作: npcAction,
            战斗数据: combatData,
            反应余量,
            出手承诺,
            触发概率,
            预计伤害: Math.max(0, Number(参数.预计伤害 || 0)),
            以命换伤: 参数.以命换伤 === true,
            闪避率: Number(参数.闪避率 || 0),
            闪避投点: Number(参数.闪避投点 || 0),
            sourceActionName: String(参数.sourceActionName || 来源动作.sourceActionName || '').trim(),
            sourceActionType: String(参数.sourceActionType || 来源动作.sourceActionType || '').trim(),
            failReason: String(参数.failReason || '').trim(),
          };
        };
        const mirrorEffectToSelf = effect => (effect ? { ...effect, 目标: '自身' } : null);
        const effectTargetsSelf = effect =>
          !!effect && resolveSkillEffectTargetCharacter(playerAction.skill, effect, attacker, defender) === attacker;
        const damageTargetContext = resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData, pClash);
        const resolvedDamageTargets = dedupeCombatTargetList(damageTargetContext.targetSet).filter(Boolean);
        const 主状态规避目标上下文 =
          pState.状态名称 && pState.状态名称 !== '无' && !状态施加视为友方或增益(pState)
            ? resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData, pState)
            : null;
        const 主状态规避目标列表 = dedupeCombatTargetList(主状态规避目标上下文?.targetSet || []).filter(Boolean);
        const resolvedPrimaryTargets = resolvedDamageTargets.length > 0 ? resolvedDamageTargets : 主状态规避目标列表;
        const primaryResolvedTarget = damageTargetContext.primaryTarget || 主状态规避目标上下文?.primaryTarget || resolvedPrimaryTargets[0] || null;
        const targetsFriendlySkill = ['自身', '友方单体', '友方群体'].includes(damageTargetContext.targetKind);
        const hostileTargetRedirectedToSelf = false;
        const 主状态需要过规避门禁 = pState.状态名称 && pState.状态名称 !== '无' && !状态施加视为友方或增益(pState);
        if (attackerIsSilenced && !isBasicAttack) {
          playerAction.action_type = '施法失败';
          result.desc = `[沉默压制] ${attacker.name || '攻击方'}被沉默，无法顺利释放[${skillName || '技能'}]！`;
          result.interrupt_bonus = attackerInterruptBonus;
          return result;
        }
        if (attackerIsDisarmed && (isBasicAttack || 是近身接战动作)) {
          result.desc = `[缴械压制] ${attacker.name || '攻击方'}被缴械，无法完成${isBasicAttack ? '普通攻击' : '近战技'}！`;
          result.interrupt_bonus = attackerInterruptBonus;
          return result;
        }
        const 缴械规则 = 读取规则改写规则(combatData, attacker, '缴械');
        if (缴械规则 && (isBasicAttack || 是近身接战动作)) {
          const 缴械强度 = 读取规则改写强度(缴械规则);
          if (缴械强度 >= 1 || Math.random() < 缴械强度) {
            result.desc = `[规则改写] ${attacker.name || '攻击方'}的武器/近战收益被临时缴械规则阻断。`;
            result.interrupt_bonus = attackerInterruptBonus;
            return result;
          }
          result.desc += ` [规则改写] ${attacker.name || '攻击方'}顶住缴械规则，近战收益被压低。`;
          pClash.威力倍率 = Math.max(0, Number(pClash.威力倍率 || 0) * Math.max(0.1, 1 - 缴械强度));
        }
        if (attackerIsSkillSealed && !isBasicAttack) {
          playerAction.action_type = '施法失败';
          result.desc = `[封技压制] ${attacker.name || '攻击方'}的技能回路被封死，当前无法施展[${skillName || '技能'}]！`;
          result.interrupt_bonus = attackerInterruptBonus;
          return result;
        }
        const attackerHitBonus = 使用对应等级 ? 0 : attackerConditionEffects.reduce((sum, ce) => sum + Number(ce.hit_bonus || 0), 0) + (掌握唐门功法_战斗(attacker, '控鹤擒龙') ? 0.05 : 0);
        const attackerHitPenalty =
          使用对应等级
            ? 0
            : attackerConditionEffects.reduce((sum, ce) => sum + Number(ce.hit_penalty || 0), 0) +
              (attackerIsBlinded ? 0.35 : 0);

        let remainPower = pClash.威力倍率 || 0;
        const 有可结算战斗效果 =
          Number(remainPower || 0) > 0 ||
          (pState.状态名称 && pState.状态名称 !== '无') ||
          授予前置效果列表.length > 0 ||
          [
            directHealEffect,
            directSpEffect,
            directMenEffect,
            directCleanseEffect,
            directDispelEffect,
            directStealBuffEffect,
            directTauntEffect,
            directGuardEffect,
            directShieldBreakEffect,
            directResourceBurnEffect,
            directResourceLockEffect,
            directLifeLinkEffect,
            directDotExtendEffect,
            directDotCompressEffect,
            directDotDetonateEffect,
            directStatusTransferEffect,
            directExecuteEffect,
            directShieldEffect,
            directSummonEffect,
            directCopyEffect,
            directGrantEffect,
            directMechanismSuppressEffect,
          ].some(Boolean) ||
          directStatusRemoveEffects.length > 0 ||
          directRuleRewriteEffects.length > 0 ||
          directStateExchangeEffects.length > 0 ||
          directPrototypeEffects.length > 0;
        if (!有可结算战斗效果 && isBattleTacticalFallbackAction(skillName || playerAction?.action_type || playerAction?.type || '')) {
          result.desc = `[守势调整] ${构建守势待机短句(attacker.name || '行动者', skillName || playerAction?.action_type || playerAction?.type || '守势调整')}。`;
          result.targetResults = [];
          return result;
        }
        if (!有可结算战斗效果 && !本次为物品动作 && !本次为维持释放 && !本次原始造物技能) {
          playerAction.action_type = '施法失败';
          result.desc = `[施法失败] ${attacker.name || '施术者'}的【${skillName || playerAction?.skill?.name || '技能'}】缺少可结算效果，本次动作未落地。`;
          写入战斗事件账本(combatData, {
            eventKind: 'failed_action',
            round: Number(combatData?.回合 || 0),
            actorName: attacker?.name || attacker?.名称 || '',
            targetName: playerAction?.target_name || defender?.name || defender?.名称 || '',
            actionName: skillName || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '',
            actionType: 'failed_action',
            actionId: playerAction?.actionId || playerAction?.__actionId || '',
            sourceActionId: playerAction?.actionId || playerAction?.__actionId || '',
            failReason: '缺少可结算效果',
            result: 'fail',
          });
          return result;
        }
        const 当前来袭窗口 = 读取来袭窗口状态(defender, playerAction);
        const 当前窗口威胁数 = Math.max(1, Math.floor(Number(当前来袭窗口.数量 || 1)));
        if (remainPower > 0 && !targetsFriendlySkill && 消耗来袭窗口对轰覆盖(defender, playerAction)) {
          result.desc = `[群攻对轰覆盖] ${defender.name || '防守方'}先前的群攻/全场对轰余波拦下了${attacker.name || '攻击方'}的同窗来袭。`;
          result.targetResults = [];
          return result;
        }

        if (单挑动作是对轰(npcAction)) {
          const 对轰覆盖数 = 读取技能对轰覆盖数(npcAction.skill, 当前窗口威胁数);
          const 对轰威力 = Number(nClash.威力倍率 || 0);
          remainPower -= 对轰威力;
          defender.__本回合对轰次数 = Math.max(0, Number(defender.__本回合对轰次数 || 0)) + 1;
          defender.__本回合反应预算 = Math.max(
            -2,
            Number(defender.__本回合反应预算 || 0) + (remainPower <= 0 ? -0.35 : -0.45),
          );
          if (remainPower <= 0) {
            remainPower = 0;
            登记来袭窗口对轰覆盖(defender, playerAction, 对轰覆盖数);
            result.desc = `NPC的[${npcAction.skill.name}]威力更胜一筹，碾碎了玩家的攻击！${对轰覆盖数 > 1 ? ` [群攻对轰] 覆盖同窗${Math.min(对轰覆盖数, 当前窗口威胁数)}重威胁。` : 当前窗口威胁数 > 1 ? ` [单体对轰] 只关闭当前来袭，剩余${当前窗口威胁数 - 1}重威胁仍在窗口内。` : ''}`;
          } else {
            result.desc = `双方对轰！玩家威力占优，余威继续命中。${对轰覆盖数 > 1 ? ` [群攻对轰] 群攻余波尝试覆盖同窗威胁，但未能完全压住当前主招。` : 当前窗口威胁数 > 1 ? ` [单体对轰] 只处理当前来袭，剩余${当前窗口威胁数 - 1}重威胁继续存在。` : ''}`;
          }
        } else if (npcAction.type === '危机自保' && npcSkillType === '防御') {
          const 护盾变化防御值 = getSkillEffects(npcAction.skill, 当前技能条件上下文)
            .filter(effect => String(effect?.原型 || '').trim() === '护盾变化')
            .reduce((最大值, effect) => Math.max(最大值, Math.abs(读取战斗数值正负(effect?.数值 ?? effect?.护盾值 ?? 0))), 0);
          const 防御强度 = Math.max(
            Number(nClash.威力倍率 || 0),
            Number(nClash.护盾值 || 0),
            护盾变化防御值,
            Number(pClash.威力倍率 || 0) * Math.max(0.25, Number(npcAction.def_mult || 1) - 0.5),
          );
          const 防御承压 = 结算来袭窗口防御承压(defender, playerAction, 防御强度, remainPower);
          remainPower = Math.max(0, remainPower - Number(防御承压.抵消 || 0));
          if (remainPower <= 0) {
            remainPower = 0;
            result.desc = `NPC的防御技能完美挡下了这一击。${防御承压.威胁数 > 1 ? ` [防御承压] 同窗${防御承压.威胁数}重威胁共用防御池，剩余承压${Math.round(防御承压.剩余防御池)}。` : ''}`;
          } else {
            result.desc = `攻击突破了NPC的防御技能阻碍。${防御承压.威胁数 > 1 ? ` [部分破防] 同窗${防御承压.威胁数}重威胁压垮部分防御池。` : ''}`;
          }
        }

        let dmgType = 规范化战斗伤害类型(pClash.伤害类型, playerAction.skill);

        const conditionArmorPen = 使用对应等级 ? 0 : 读取状态穿透比例(attackerConditionEffects);
        const isAOE =
          resolvedPrimaryTargets.length > 1 ||
          damageTargetContext.targetKind === '全场' ||
          主状态规避目标上下文?.targetKind === '全场';
        const 是近身攻击 = 战斗伤害是近身攻击(dmgType);
        const 是远程攻击 = 战斗伤害是远程攻击(dmgType);
        const 是精神攻击 = 战斗伤害是精神攻击(dmgType);
        const 是真实攻击 = 战斗伤害是真实攻击(dmgType);
        let fluctuation = 0.9 + Math.random() * 0.2;
        const 原始最终伤害倍率 = 使用对应等级 ? 1 : attackerConditionEffects.reduce(
          (mult, ce) => mult * Number(ce.final_damage_mult || 1.0),
          1.0,
        );
        const totalFinalDamageMult = 原始最终伤害倍率;
        const 原始最终伤害加值 = 使用对应等级 ? 0 : attackerConditionEffects.reduce(
          (sum, ce) => sum + Number(ce.final_damage_bonus || 0),
          0,
        );
        const totalFinalDamageBonus = 原始最终伤害加值;
        const 造成伤害吸收汇总 = { 生命: Number(pClash.伤害吸收比例 || 0), 体力: 0, 魂力: 0, 精神力: 0 };
        常规行动效果.forEach(effect => {
          if (String(effect?.原型 || '').trim() !== '结算修正') return;
          if (String(effect?.结算 || '').trim() !== '伤害吸收') return;
          if (!当前技能元素匹配结算修正(playerAction.skill || {}, effect)) return;
          if (String(effect?.吸收来源 || '造成伤害').trim() !== '造成伤害') return;
          const 比例 = Math.max(0, 读取战斗数值正负(effect?.数值));
          if (比例 <= 0) return;
          const 资源 = ['生命', '体力', '魂力', '精神力'].includes(String(effect?.吸收资源 || '').trim()) ? String(effect?.吸收资源 || '').trim() : '生命';
          造成伤害吸收汇总[资源] += 比例;
        });
        attackerConditionEffects.forEach(ce => {
          const 比例 = Math.max(0, Number(ce.damage_absorb_ratio || 0));
          if (比例 > 0) {
            if (String(ce.吸收来源 || '造成伤害').trim() !== '造成伤害') return;
            const 资源 = ['生命', '体力', '魂力', '精神力'].includes(String(ce.吸收资源 || '').trim()) ? String(ce.吸收资源 || '').trim() : '生命';
            造成伤害吸收汇总[资源] += 比例;
          }
        });
        if (Number(pClash.威力倍率 || 0) > 0 && !resolvedDamageTargets.length) {
          result.desc += ` [索敌失败] 当前没有可被命中的有效目标。`;
          写入战斗事件账本(combatData, {
            eventKind: 'target_fail',
            round: Number(combatData?.回合 || 0),
            actorName: attacker?.name || attacker?.名称 || '',
            actionName: skillName || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '',
            actionType: 'target_fail',
            failReason: '当前没有可被命中的有效目标',
            result: 'fail',
          });
        }

        const resolveProjectedDamageAgainstTarget = (targetObj, index) => {
          if (!targetObj) return null;
          bindCombatParticipant(targetObj);
          const targetFinalStat = targetObj.final || buildCombatFinalStats(targetObj);
          const targetConditionEffects = targetObj.状态效果
            ? Object.values(targetObj.状态效果).map(c => c?.战斗效果 || {})
            : [];
          const targetName = targetObj === attacker ? '自身' : targetObj.name || `目标${index + 1}`;
          const targetUsesReactionAction =
            targetObj === primaryResolvedTarget && !targetsFriendlySkill && targetObj !== attacker;
          const targetEffectiveAgi = Math.max(
            Number(targetFinalStat.agi || 0),
            Number(targetObj.agi || 0),
            Number(targetObj?.属性?.agi || 0),
            Number(targetObj?.属性?.敏捷 || 0),
          ) * Number(targetObj.temp_agi_mult || 1);
          const targetDodgeBonus = 计算有效增加闪避(Number(targetObj.temp_dodge_bonus || 0) + (掌握唐门功法_战斗(targetObj, '鬼影迷踪步') ? 0.05 : 0));
          const targetDodgePenalty = Number(targetObj.temp_dodge_penalty || 0) + currentSkillDodgePenalty;
          const targetLockLevel = Number(targetObj.temp_lock_level || 0) + currentSkillLockLevel;
          const localLogParts = [];
          let localGrazeMultiplier = grazeMultiplier;
          let localDodgeRate = null;
          let localDodgeRoll = null;
          const allowEvasion = !targetsFriendlySkill || (hostileTargetRedirectedToSelf && targetObj === attacker);
          if (allowEvasion && (remainPower > 0 || 主状态需要过规避门禁)) {
            const hitDelta = 使用对应等级 ? 0 : currentSkillHitBonus + attackerHitBonus - (currentSkillHitPenalty + attackerHitPenalty);
            if (targetUsesReactionAction && !isAOE && npcAction.type === '伺机闪避') {
              const absoluteDodgeThreshold = (攻势敏捷 + 攻势精神上限) * 1.5;
              if (targetEffectiveAgi + targetDodgeBonus > absoluteDodgeThreshold) {
                localLogParts.push(`[绝对闪避] ${targetName}残影一晃，完全躲过了攻击。`);
                recordActorActionWhiffedByDodge(attacker, playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '动作', targetObj);
                登记行为防反候选('完美闪避', targetObj, {
                  闪避率: 100,
                  闪避投点: 0,
                  反应动作: npcAction,
                });
                return {
                  target: targetObj,
                  targetName,
                  damage: 0,
                  kind: index === 0 ? 'primary' : 'secondary',
                  evaded: true,
                  logs: localLogParts,
                };
              }
            }
            const 范围压制削弱 = isAOE ? (damageTargetContext.targetKind === '全场' ? 10 : 7) : 0;
            const 近身闪避削弱 = 是近身攻击 ? 6 : 0;
            const 远程闪避修正 = 是远程攻击 ? 2 : 0;
            const dodgeScale = targetUsesReactionAction && !isAOE && npcAction.type === '伺机闪避'
              ? (是近身攻击 ? 26 : 是远程攻击 ? 28 : 26)
              : (是近身攻击 ? 8 : 是远程攻击 ? 11 : 10);
            const grazeWindow = targetUsesReactionAction && !isAOE && npcAction.type === '伺机闪避'
              ? (是近身攻击 ? 11 : 16)
              : (是近身攻击 ? 4 : 6);
            let dodgeRate = (targetEffectiveAgi / Math.max(1, 攻势敏捷 + 攻势精神上限)) * dodgeScale;
            dodgeRate += targetDodgeBonus * 100 - targetDodgePenalty * 100;
            dodgeRate -= hitDelta * 100;
            dodgeRate -= targetLockLevel * 8;
            dodgeRate -= 范围压制削弱 + 近身闪避削弱 - 远程闪避修正;
            dodgeRate -= Math.max(0, Number(targetObj.__本回合闪避成功次数 || 0)) * 22;
            dodgeRate = Math.max(0, Math.min(targetUsesReactionAction ? 48 : 24, dodgeRate));
            const dodgeRoll = Math.random() * 100;
            localDodgeRate = dodgeRate;
            localDodgeRoll = dodgeRoll;
            if (dodgeRoll <= dodgeRate) {
              localLogParts.push(
                targetUsesReactionAction && !isAOE && npcAction.type === '伺机闪避'
                  ? `[主动闪避] ${targetName}凭借敏捷优势惊险躲过了攻击。`
                  : `[闪避判定] ${targetName}提前预判轨迹，规避了本次攻击。`,
              );
              if (targetUsesReactionAction && !isAOE) {
                targetObj.__本回合闪避成功次数 = Math.max(0, Number(targetObj.__本回合闪避成功次数 || 0)) + 1;
                recordActorActionWhiffedByDodge(attacker, playerAction?.skill?.name || playerAction?.skill?.魂技名 || playerAction?.action_type || '动作', targetObj);
                登记行为防反候选('完美闪避', targetObj, {
                  闪避率: dodgeRate,
                  闪避投点: dodgeRoll,
                  反应动作: npcAction,
                });
              }
              return {
                target: targetObj,
                targetName,
                damage: 0,
                kind: index === 0 ? 'primary' : 'secondary',
                evaded: true,
                dodgeRate,
                dodgeRoll,
                logs: localLogParts,
              };
            }
            if (dodgeRoll < dodgeRate + grazeWindow) {
              const grazePercent = (dodgeRoll - dodgeRate) / Math.max(1, grazeWindow);
              if (targetUsesReactionAction && !isAOE && npcAction.type === '伺机闪避') {
                localGrazeMultiplier = 0.3 + 0.5 * grazePercent;
              } else {
                localGrazeMultiplier = 0.45 + 0.35 * grazePercent;
              }
              localLogParts.push(
                `[擦伤命中] ${targetName}未能完全避开，只承受 ${(localGrazeMultiplier * 100).toFixed(0)}% 伤害。`,
              );
              if (attackerIsBlinded) localLogParts.push(`[目盲干扰] 攻击轨迹受到视觉偏差影响。`);
            }
          }

          let actualDef = 计算穿透后防御值(
            Number(targetFinalStat.def || targetObj.def || 1),
            Number(pClash.防御穿透 || 0) + (使用对应等级 ? 0 : 当前行动附加穿透),
            conditionArmorPen,
          );
          const 目标防御剥夺 = Math.min(
            0.9,
            targetConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.defense_strip || 0)), 0),
          );
          const 目标精神抗性剥夺 = Math.min(
            0.9,
            targetConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.spirit_resist_strip || 0)), 0),
          );
          if (!是精神攻击 && !是真实攻击) actualDef = Math.max(1, actualDef * (1 - 目标防御剥夺));
          const soulDriveScale = 使用对应等级 ? 1 : getSoulDriveScale({ ...attacker, final: attackerFinalStat }, targetObj);
          const spiritDriveScale = 使用对应等级 ? 1 : getSpiritDriveScale({ ...attacker, final: attackerFinalStat }, targetObj);
          const 定位伤害倍率 = 使用对应等级 ? 1 : 计算定位伤害倍率(attacker, targetObj, dmgType);
          let projectedDamage = 0;
          const 消耗加成系数 = 使用对应等级 ? 1 : 计算伤害消耗加成系数(playerAction.skill, attacker);
          let formulaAttackValue = 0;
          let formulaDefenseValue = 1;
          let formulaBaseDamage = 0;
          let formulaText = '';
          if (是真实攻击) {
            const 真实驱动 = Math.max(1, 计算精神伤害攻势值(攻势单位, 攻势最终属性));
            formulaAttackValue = 真实驱动;
            formulaDefenseValue = 1;
            projectedDamage = remainPower * Math.max(1, Math.sqrt(真实驱动)) * 0.12 * 消耗加成系数;
            formulaText = '威力×√真实驱动×0.12×消耗加成';
          } else if (是近身攻击) {
            formulaAttackValue = 计算物理伤害攻势值(攻势单位, 攻势最终属性, dmgType);
            formulaDefenseValue = actualDef;
            projectedDamage = remainPower * (formulaAttackValue / actualDef) * soulDriveScale * 定位伤害倍率 * 1.04 * 消耗加成系数;
            formulaText = '威力×(力量/有效防御)×魂力驱动×定位×近身系数×消耗加成';
          } else if (是远程攻击) {
            formulaAttackValue = 计算物理伤害攻势值(攻势单位, 攻势最终属性, dmgType);
            formulaDefenseValue = actualDef;
            projectedDamage =
              remainPower *
              (formulaAttackValue / actualDef) *
              soulDriveScale *
              定位伤害倍率 *
              消耗加成系数;
            formulaText = '威力×(远程物理攻势/有效防御)×魂力驱动×定位×消耗加成';
          } else if (是精神攻击) {
            formulaAttackValue = 计算精神伤害攻势值(攻势单位, 攻势最终属性);
            formulaDefenseValue = Math.max(1, 计算紫极魔瞳防守精神攻势值_战斗(targetObj, targetFinalStat, pClash, playerAction.skill) * (1 - 目标精神抗性剥夺));
            projectedDamage =
              remainPower *
              (formulaAttackValue / formulaDefenseValue) *
              spiritDriveScale *
              定位伤害倍率 *
              消耗加成系数;
            formulaText = '威力×(精神攻势/精神防守)×精神驱动×定位×消耗加成';
          }
          formulaBaseDamage = projectedDamage;

          if (targetUsesReactionAction && npcAction.type === '肉体兜底') {
            projectedDamage /= 1.2;
            localLogParts.push(`[肉体兜底] ${targetName}强行收缩防御，用肉身硬抗了这一击。`);
          }

          const activeReactionShield = targetUsesReactionAction ? Math.max(0, Number(nClash.护盾值 || 0)) : 0;
          if (activeReactionShield > 0) {
            projectedDamage = Math.max(0, projectedDamage - activeReactionShield);
            if (projectedDamage === 0) {
              localLogParts.push(`[主动护盾] ${targetName}借临时护盾完全吃下了本次冲击。`);
            } else {
              localLogParts.push(`[主动护盾] ${targetName}先被护盾吸收一部分冲击，余威继续压入本体。`);
            }
          }

          projectedDamage *= fluctuation * localGrazeMultiplier;
          const targetDamageReduction = Math.min(
            0.9,
            targetConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.damage_reduction || 0)), 0),
          );
          const 玄玉手防护减免 = targetUsesReactionAction && (npcAction.type === '危机自保' || npcAction.type === '肉体兜底') && 掌握唐门功法_战斗(targetObj, '玄玉手') ? 0.05 : 0;
          const targetReceivedDamageMult = targetConditionEffects.reduce(
            (mult, ce) => mult * Number(ce.received_damage_mult || 1.0),
            1.0,
          );
          const targetElementReceivedDamageMult = 读取目标元素承伤倍率(targetObj, playerAction.skill || {});
          projectedDamage = projectedDamage * (1 - Math.min(0.9, targetDamageReduction + 玄玉手防护减免)) * targetReceivedDamageMult * targetElementReceivedDamageMult * totalFinalDamageMult + totalFinalDamageBonus;

          if (
            !使用对应等级 &&
            directExecuteEffect &&
            String(directExecuteEffect?.原型 || '').trim() === '结算修正' &&
            String(directExecuteEffect?.结算 || '').trim() === '造成伤害' &&
            读取战斗数值正负(directExecuteEffect?.数值) > 0 &&
            getCombatHpValue(targetObj) / Math.max(1, getCombatHpMaxValue(targetObj)) <= 0.45
          ) {
            const execMult = 1 + Math.max(0, 读取战斗效果数值(directExecuteEffect?.数值, 0));
            if (execMult !== 1) {
              projectedDamage *= execMult;
              localLogParts.push(`[斩杀补伤] ${targetName}跌入收割阈值，追加杀伤生效。`);
            }
          }

          if (!使用对应等级 && fusionProfile && projectedDamage > 0) {
            projectedDamage *= Number(fusionProfile.damageMult || 1);
          }
          const damageFormulaTrace = {
            damageType: dmgType,
            skillPower: remainPower,
            formulaAttackValue,
            formulaDefenseValue,
            baseFormulaDamage: formulaBaseDamage,
            baseFormulaText: formulaText,
            meleeContactScale: 是近身攻击 ? 1.04 : 1,
            fusionDamageMult: fusionProfile ? Number(fusionProfile.damageMult || 1) : 1,
          };
          if (targetUsesReactionAction && !isAOE && npcAction.type === '肉体兜底' && projectedDamage > 0) {
            const 允许绝境换伤 = 判定行为绝境换伤窗口(targetObj, attacker, combatData, projectedDamage);
            const 结算后生命 = getCombatHpValue(targetObj) - projectedDamage;
            if (结算后生命 <= 0 && !允许绝境换伤) {
              return {
                target: targetObj,
                targetName,
                damage: Math.max(0, Math.floor(projectedDamage)),
                kind: index === 0 ? 'primary' : 'secondary',
                actualDefense: actualDef,
                defenseStrip: 目标防御剥夺,
                spiritResistStrip: 目标精神抗性剥夺,
                soulDriveScale,
                spiritDriveScale,
                positionDamageScale: 定位伤害倍率,
                costDamageScale: 消耗加成系数,
                fluctuation,
                grazeMultiplier: localGrazeMultiplier,
                damageReduction: targetDamageReduction,
                jadeHandReduction: 玄玉手防护减免,
                receivedDamageMult: targetReceivedDamageMult,
                elementDamageMult: targetElementReceivedDamageMult,
                finalDamageMult: totalFinalDamageMult,
                finalDamageBonus: totalFinalDamageBonus,
                activeReactionShield,
                dodgeRate: localDodgeRate,
                dodgeRoll: localDodgeRoll,
                ...damageFormulaTrace,
                logs: localLogParts,
              };
            }
            登记行为防反候选('硬抗换伤', targetObj, {
              预计伤害: projectedDamage,
              反应动作: npcAction,
              以命换伤: 允许绝境换伤 && 结算后生命 <= 0,
              需要主动防御成立: true,
              被动承压: false,
            });
          }
          return {
            target: targetObj,
            targetName,
            damage: Math.max(0, Math.floor(projectedDamage)),
            kind: index === 0 ? 'primary' : 'secondary',
            actualDefense: actualDef,
            defenseStrip: 目标防御剥夺,
            spiritResistStrip: 目标精神抗性剥夺,
            soulDriveScale,
            spiritDriveScale,
            positionDamageScale: 定位伤害倍率,
            costDamageScale: 消耗加成系数,
            fluctuation,
            grazeMultiplier: localGrazeMultiplier,
            damageReduction: targetDamageReduction,
            jadeHandReduction: 玄玉手防护减免,
            receivedDamageMult: targetReceivedDamageMult,
            elementDamageMult: targetElementReceivedDamageMult,
            finalDamageMult: totalFinalDamageMult,
            finalDamageBonus: totalFinalDamageBonus,
            activeReactionShield,
            dodgeRate: localDodgeRate,
            dodgeRoll: localDodgeRoll,
            ...damageFormulaTrace,
            logs: localLogParts,
          };
        };

        const projectedTargetEntries = resolvedPrimaryTargets
          .map((targetObj, index) => resolveProjectedDamageAgainstTarget(targetObj, index))
          .filter(Boolean);
        const 主原型命中目标集合 = new Set(
          projectedTargetEntries
            .filter(entry => Number(entry?.damage || 0) > 0)
            .map(entry => String(entry?.target?.name || entry?.target?.名称 || entry?.targetName || '').trim())
            .filter(Boolean),
        );
        if (是主原型效果(pClash)) {
          projectedTargetEntries
            .filter(entry => Number(entry?.damage || 0) > 0)
            .forEach(entry => 登记主原型成立目标(entry.target));
        }
        const 多段次数 = Math.max(1, Math.floor(Number(pClash?.计算层效果?.multi_hit_count || pClash?.攻击段数 || 1)));
        const 段伤害倍率 = Number(pClash?.计算层效果?.segment_damage_ratio || (多段次数 > 1 ? 1 / 多段次数 : 1));
        if (多段次数 > 1) {
          projectedTargetEntries.forEach(entry => {
            const 总伤害 = Math.max(0, Math.floor(Number(entry?.damage || 0)));
            const 基础段伤害 = Array.from({ length: 多段次数 }, () => Math.max(0, Math.floor(总伤害 * 段伤害倍率)));
            const 已分配 = 基础段伤害.reduce((sum, value) => sum + value, 0);
            const 差额 = 总伤害 - 已分配;
            if (基础段伤害.length) 基础段伤害[基础段伤害.length - 1] = Math.max(0, 基础段伤害[基础段伤害.length - 1] + 差额);
            entry.damageSegments = 基础段伤害.filter(value => value > 0);
            if (!entry.logs) entry.logs = [];
            entry.logs.push(`[多段结算] ${entry.targetName || '目标'}被拆为${多段次数}段连续命中。`);
          });
        }
        result.targetResults = projectedTargetEntries.map(entry => ({
          target: entry.target,
          targetName: entry.targetName,
          damage: entry.damage,
          kind: entry.kind,
          evaded: entry.evaded === true,
          logs: Array.isArray(entry.logs) ? entry.logs.slice() : [],
          damageSegments: entry.damageSegments,
          actualDefense: entry.actualDefense,
          defenseStrip: entry.defenseStrip,
          spiritResistStrip: entry.spiritResistStrip,
          soulDriveScale: entry.soulDriveScale,
          spiritDriveScale: entry.spiritDriveScale,
          positionDamageScale: entry.positionDamageScale,
          costDamageScale: entry.costDamageScale,
          fluctuation: entry.fluctuation,
          grazeMultiplier: entry.grazeMultiplier,
          dodgeRate: entry.dodgeRate,
          dodgeRoll: entry.dodgeRoll,
          damageReduction: entry.damageReduction,
          jadeHandReduction: entry.jadeHandReduction,
          receivedDamageMult: entry.receivedDamageMult,
          elementDamageMult: entry.elementDamageMult,
          finalDamageMult: entry.finalDamageMult,
          finalDamageBonus: entry.finalDamageBonus,
          activeReactionShield: entry.activeReactionShield,
          damageType: entry.damageType,
          skillPower: entry.skillPower,
          formulaAttackValue: entry.formulaAttackValue,
          formulaDefenseValue: entry.formulaDefenseValue,
          baseFormulaDamage: entry.baseFormulaDamage,
          baseFormulaText: entry.baseFormulaText,
          meleeContactScale: entry.meleeContactScale,
          fusionDamageMult: entry.fusionDamageMult,
        }));
        const 主目标结果 = projectedTargetEntries[0] || null;
        if (
          primaryResolvedTarget &&
          primaryResolvedTarget !== attacker &&
          !targetsFriendlySkill &&
          npcAction &&
          String(npcAction.type || npcAction.action_type || '').trim()
        ) {
          const 反应动作名 = normalizeBattleActionDisplayName(npcAction.skill?.name || npcAction.skill?.魂技名 || npcAction.type || npcAction.action_type || '');
          const 反应类型 = String(npcAction.type || npcAction.action_type || '').trim();
          let 事件类型 = '';
          let 结果类型 = '';
          if (/伺机闪避|闪避/.test(反应类型) || 主目标结果?.evaded === true) {
            事件类型 = 'dodge';
            结果类型 = 主目标结果?.evaded === true ? 'evaded' : 'attempted';
          } else if (/承伤硬抗|肉体兜底|硬抗|防御|危机自保|收招转防|借力守势|坚壁反制/.test(反应类型)) {
            事件类型 = 'defend';
            结果类型 = Number(主目标结果?.damage || 0) > 0 ? 'guarded_hit' : 'guarded';
          } else if (/无法反应|待机|观察|守势维持|守势对峙/.test(反应类型)) {
            事件类型 = 'pass';
            结果类型 = 'pass';
          }
          if (事件类型) {
            const 记账反应动作名 = 事件类型 === 'dodge' && !/闪避/.test(String(反应动作名 || 反应类型 || ''))
              ? '伺机闪避'
              : (反应动作名 || 反应类型);
            写入战斗事件账本(combatData, {
              eventKind: 事件类型,
              round: Number(combatData?.回合 || 0),
              actorName: primaryResolvedTarget?.name || primaryResolvedTarget?.名称 || '',
              targetName: attacker?.name || attacker?.名称 || '',
              actionName: 记账反应动作名,
              actionType: 反应类型,
              sourceActionName: skillName || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '',
              sourceRound: Number(combatData?.回合 || 0),
              result: 结果类型,
            });
          }
        }
        const projectedLogs = projectedTargetEntries.flatMap(entry => entry.logs || []).filter(Boolean);
        if (projectedLogs.length > 0) result.desc += ` ${projectedLogs.join(' ')}`;
        常规行动效果.forEach(标记时光回溯效果落地);
        if (pState.状态名称 && pState.状态名称 !== '无') {
          const 预结算状态目标 = String(pState?.目标 || '').trim();
          const 预结算状态需要命中 =
            !['自身', '友方单体', '友方群体'].includes(预结算状态目标) &&
            !String(pState?.特殊机制标识 || '无').includes('增益') &&
            pState?.计算层效果?.无视异常 !== true &&
            pState?.计算层效果?.super_armor !== true &&
            pState?.计算层效果?.探查屏蔽 !== true &&
            !(Number(pState?.计算层效果?.min_hp_floor || 0) > 0) &&
            !(Number(pState?.计算层效果?.hot_heal_ratio || 0) > 0);
          if (!预结算状态需要命中) {
            标记时光回溯预结算落地(resolveSkillEffectTargetCharacters(
              playerAction.skill,
              pState,
              attacker,
              defender,
              combatData,
            ), '状态施加');
          }
        }
        const 时光回溯日志列表 = [];
        result.targetResults.forEach(entry => {
          const 时光回溯日志 = 预结算触发时光回溯(attacker, entry, playerAction.skill);
          if (时光回溯日志) 时光回溯日志列表.push(时光回溯日志);
        });
        if (时光回溯日志列表.length > 0) {
          refreshSettleResultProjectedDamage(result);
          result.desc += ` ${时光回溯日志列表.join(' ')}`;
        }
        if (fusionProfile && projectedTargetEntries.some(entry => Number(entry.damage || 0) > 0)) {
          result.desc += ` [融合共鸣] 武魂交叠后的极限共振将杀伤再度推高。`;
        }
        refreshSettleResultProjectedDamage(result);
        result = applyHighTierMechanics(attacker, defender, playerAction, result);

        if (directSelfSacrificeEffect && Number(directSelfSacrificeEffect.体力代价 || 0) > 0) {
          result.desc += ` [自残换收益] 额外献祭 ${Number(directSelfSacrificeEffect.体力代价 || 0)} 点体力，换取 x${Number(directSelfSacrificeEffect.增伤倍率 || 1.25).toFixed(2)} 威力。`;
        }
        const 状态反噬比例 = attackerConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.misfortune_backlash_ratio || 0)), 0);
        if (状态反噬比例 > 0 && !isBasicAttack && playerAction.action_type !== '施法失败') {
          const 状态反噬伤害 = Math.max(1, Math.floor(getCombatHpMaxValue(attacker) * Math.min(0.03, 状态反噬比例)));
          设置战斗血量值(attacker, getCombatHpValue(attacker) - 状态反噬伤害);
          result.desc += ` [反噬结算] ${attacker.name || '施术者'}力量逆冲，损失${状态反噬伤害}点生命。`;
        }
        if (isAOE && Array.isArray(result.targetResults) && result.targetResults.length > 1) {
          result.desc += ` [群体索敌] 共锁定 ${result.targetResults.length} 个目标，逐个独立结算。`;
        }

        if (Number(result.totalProjectedDamage || 0) > 0 && !Number(result.backlash_dmg || 0)) {
          const 伤害量 = Number(result.totalProjectedDamage || 0);
          const 吸收生命 = Math.floor(伤害量 * Math.max(0, Number(造成伤害吸收汇总.生命 || 0)));
          if (吸收生命 > 0) {
            设置战斗血量值(attacker, Math.min(getCombatHpMaxValue(attacker), getCombatHpValue(attacker) + 吸收生命));
            result.desc += ` [伤害吸收] ${attacker.name || '施术者'}造成伤害时恢复了 ${吸收生命} 点生命。`;
          }
          const 吸收体力 = Math.floor(伤害量 * Math.max(0, Number(造成伤害吸收汇总.体力 || 0)));
          if (吸收体力 > 0) {
            设置战斗体力值(attacker, getCombatStaminaValue(attacker) + 吸收体力);
            result.desc += ` [伤害吸收] ${attacker.name || '施术者'}造成伤害时恢复了 ${吸收体力} 点体力。`;
          }
          const 吸收魂力 = Math.floor(伤害量 * Math.max(0, Number(造成伤害吸收汇总.魂力 || 0)));
          if (吸收魂力 > 0) {
            设置战斗延迟效果资源值(attacker, 'sp', Number(attacker.sp || 0) + 吸收魂力);
            result.desc += ` [伤害吸收] ${attacker.name || '施术者'}造成伤害时恢复了 ${吸收魂力} 点魂力。`;
          }
          const 吸收精神力 = Math.floor(伤害量 * Math.max(0, Number(造成伤害吸收汇总.精神力 || 0)));
          if (吸收精神力 > 0) {
            设置战斗延迟效果资源值(attacker, 'men', Number(attacker.men || 0) + 吸收精神力);
            result.desc += ` [伤害吸收] ${attacker.name || '施术者'}造成伤害时恢复了 ${吸收精神力} 点精神力。`;
          }
        }

        if (Number(result.backlash_dmg || 0) > 0) {
          const backlashDamage = Math.max(0, Math.floor(Number(result.backlash_dmg || 0)));
          设置战斗血量值(attacker, getCombatHpValue(attacker) - backlashDamage);
          result.desc += ` [反噬结算] 玩家额外承受了 ${backlashDamage} 点反噬伤害。`;
        }
        if (Number(result.totalProjectedDamage || 0) > 0) {
          result.desc += isAOE
            ? ` 本次共投射 ${Number(result.totalProjectedDamage || 0)} 点伤害。`
            : ` 造成了 ${Number(result.dmg || 0)} 点最终伤害。`;
          const 一次性增幅消耗 = 消耗一次性伤害增幅状态(attacker);
          if (一次性增幅消耗.length) {
            result.desc += ` [蓄力释放] ${attacker.name || '施术者'}释放了${一次性增幅消耗
              .map(条目 => `[${条目.状态名}]+${Math.round(Number(条目.增幅 || 0) * 100)}%`)
              .join('、')}。`;
          }
          if (selfMirrorEffect && primaryResolvedTarget !== attacker) {
            const selfEchoDamage = Math.max(1, Math.floor(Number(result.dmg || 0)));
            设置战斗血量值(attacker, getCombatHpValue(attacker) - selfEchoDamage);
            result.desc += ` [自身反馈] 技能余波同步反噬自身，额外承受 ${selfEchoDamage} 点伤害。`;
          }
        }
        const isHostileSkillTarget =
          !targetsFriendlySkill || hostileTargetRedirectedToSelf;
        if (isHostileSkillTarget) {
          const brokenStealth = removeConditionsByPrototypeFilter(attacker, { 状态: '隐匿', 数量: '全部' }, attacker);
          if (brokenStealth.length > 0) {
            result.desc += ` [破隐结算] ${attacker.name || '施术者'}出手后现出身形，失去了[${brokenStealth.join('/')}]。`;
          }
        }
        const getEffectTargetContext = effect =>
          resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData, effect);
        const 过滤被时光回溯规避目标列表 = (目标列表 = []) =>
          dedupeCombatTargetList(Array.isArray(目标列表) ? 目标列表 : [目标列表])
            .filter(targetObj => targetObj && !目标已被时光回溯规避(result, targetObj));
        const resolveDirectMechanismTargetList = (effect, options = {}) => {
          const mechanism = 读取战斗效果标签(effect);
          const prototype = String(effect?.原型 || '').trim();
          if (
            ['资源锁定', '生命链接'].includes(mechanism) ||
            ['状态转移', '资源锁定'].includes(prototype)
          ) {
            const hostileTarget = defender && defender !== attacker ? defender : null;
            const friendlyTarget = attacker || null;
            const contextHint = String(effect?.目标 || '').trim();
            const pickTarget = contextHint === '自身' ? friendlyTarget : hostileTarget || friendlyTarget;
            const 目标列表 = 过滤被时光回溯规避目标列表(pickTarget ? [pickTarget] : []);
            if (options.single === true) return 目标列表[0] || null;
            return 目标列表;
          }
          const targetContext = getEffectTargetContext(effect);
          let targets = Array.isArray(targetContext?.targetSet) ? [...targetContext.targetSet] : [];
          if (String(effect?.目标 || '').trim() === '自身' && (!targets.length || targets.every(targetObj => !targetObj))) {
            targets = attacker ? [attacker] : [];
          }
          const 可落地目标 = 过滤被时光回溯规避目标列表(targets);
          if (options.single === true) return 可落地目标[0] || null;
          return 可落地目标;
        };

        const applyRuleRewriteEffect = effect => {
          if (!effect) return false;
          const 规则 = String(effect?.规则 || '').trim();
          if (!['缴械', '死亡转存活'].includes(规则)) return false;
          const targetUnits = String(effect?.目标 || '').trim() === '全场'
            ? []
            : resolveDirectMechanismTargetList(effect);
          if (String(effect?.目标 || '').trim() !== '全场' && !targetUnits.length) return false;
          const 源动作抹消规则 = 读取源动作抹消规则(effect, { 消费: false });
          if (源动作抹消规则) {
            描述源动作抹消(源动作抹消规则);
            return false;
          }
          const 数量 = 登记战斗规则改写运行态(combatData, effect, targetUnits, attacker, playerAction.skill);
          if (!(数量 > 0)) return false;
          const 目标摘要 = String(effect?.目标 || '').trim() === '全场'
            ? '全场'
            : targetUnits.map(targetObj => targetObj === attacker ? '自身' : targetObj?.name || targetObj?.名称 || '目标').join('、');
          result.desc += ` [规则改写] ${目标摘要 || '目标'}的【${规则}】临时规则已生效。`;
          return true;
        };

        directRuleRewriteEffects.forEach(applyRuleRewriteEffect);

        const 登记持续原型运行壳 = (effect, targetUnits = []) => {
          const 原型 = String(effect?.原型 || '').trim();
          if (!['资源转移', '状态移除', '状态转移', '状态交换'].includes(原型)) return 0;
          const 持续回合 = Math.max(0, Math.min(10, Math.round(Number(effect?.持续回合 || 0))));
          if (!(持续回合 > 0)) return 0;
          let 目标列表 = dedupeCombatTargetList(Array.isArray(targetUnits) ? targetUnits : [targetUnits]).filter(isCombatUnitAlive);
          if (!目标列表.length) 目标列表 = 过滤被时光回溯规避目标列表(resolveSkillEffectTargetCharacters(playerAction.skill, effect, attacker, defender, combatData)).filter(isCombatUnitAlive);
          if (!目标列表.length) return 0;
          const 持续目标列表 = 目标列表.map(unit => String(unit?.name || unit?.名称 || '').trim()).filter(Boolean);
          const 来源角色名 = String(attacker?.name || attacker?.名称 || '').trim();
          const 来源技能名 = String(playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '技能').trim();
          let 写入数量 = 0;
          目标列表.forEach(targetObj => {
            if (!targetObj) return;
            const targetName = String(targetObj?.name || targetObj?.名称 || '目标').trim();
            const 是增益 = targetObj === attacker || ['自身', '友方', '友方单体', '友方群体'].includes(String(effect?.目标 || '').trim());
            const 状态键 = putConditionWithUniqueKey(targetObj, `持续${原型}:${来源技能名}`, {
              类型: 是增益 ? 'buff' : 'debuff',
              层数: 1,
              来源技能: 来源技能名,
              来源角色: 来源角色名,
              来源原型摘要: 原型,
              描述: `由[${来源技能名}]建立持续${原型}窗口`,
              duration: 持续回合,
              持续回合,
              持续原型效果: deepClonePlain(effect),
              持续目标列表,
              目标角色: targetName,
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果: createEmptyCombatEffectMap(),
              特殊机制标识: `持续${原型}`,
            });
            if (状态键) 写入数量 += 1;
          });
          if (写入数量 > 0) result.desc += ` [持续${原型}] 已建立${持续回合}回合窗口。`;
          return 写入数量;
        };

        const applyImmediateRecoveryEffect = (effect, resourceKey, labelText) => {
          if (!effect || Number(result.backlash_dmg || 0) > 0) return 0;
          const effectTargetContext = getEffectTargetContext(effect);
          const targetUnits = resolveDirectMechanismTargetList(effect);
          if (!targetUnits.length) return 0;
          const 源动作抹消规则 = 读取源动作抹消规则(effect, { 消费: false });
          const isFriendly = ['自身', '友方单体', '友方群体'].includes(effectTargetContext.targetKind);
          let totalRecovered = 0;
          targetUnits.forEach(targetObj => {
            const resolvedEffect = effect;
            const rawValue = resolvedEffect.数值 ?? resolvedEffect.恢复比例 ?? resolvedEffect.回复比例 ?? 0;
            const value = 读取战斗数值正负(rawValue);
            if (!(value > 0)) return;
            const targetFinalStat = targetObj?.final || buildCombatFinalStats(targetObj);
            const supportScale = isFriendly ? getSupportEffectScale(attacker, targetObj) : 1;
            const targetConditionEffects = targetObj.状态效果
              ? Object.values(targetObj.状态效果).map(c => c?.战斗效果 || {})
              : [];
            let maxResource = 0;
            if (resourceKey === 'hp') maxResource = getCombatHpMaxValue(targetObj);
            else if (resourceKey === 'vit') maxResource = getCombatStaminaMaxValue(targetObj);
            else if (resourceKey === 'sp') maxResource = Number(targetFinalStat.sp_max || targetObj.sp_max || 0);
            else maxResource = Number(targetFinalStat.men_max || targetObj.men_max || 0);
            if (!(maxResource > 0)) return;
            const totalHealBlockRatio =
              resourceKey === 'hp'
                ? Math.min(
                    1,
                    targetConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.heal_block_ratio || 0)), 0),
                  )
                : 0;
            const totalFinalHealMult =
              resourceKey === 'hp'
                ? attackerConditionEffects.reduce((mult, ce) => mult * Number(ce.final_heal_mult || 1.0), 1.0)
                : 1.0;
            const totalFinalHealBonus =
              resourceKey === 'hp'
                ? attackerConditionEffects.reduce((sum, ce) => sum + Number(ce.final_heal_bonus || 0), 0)
                : 0;
            const totalResourceBlockRatio = 读取资源锁定比例(targetObj, resourceKey, '回复锁定');
            const baseAmount = /%$/.test(String(rawValue ?? '').trim()) || Math.abs(value) <= 1
              ? maxResource * value
              : value;
            let amount = Math.floor(
              baseAmount *
                supportScale *
                (resourceKey === 'hp' ? totalFinalHealMult * (1 - Math.max(totalHealBlockRatio, totalResourceBlockRatio)) : 1 - totalResourceBlockRatio),
            );
            if (fusionProfile) amount = Math.floor(amount * Number(fusionProfile.recoverMult || 1));
            if (resourceKey === 'hp') amount += totalFinalHealBonus;
            amount = Math.max(0, amount);
            if (!amount) return;
            const convertToDamage = 读取战斗效果标签(resolvedEffect) === '伤害结算';
            if (convertToDamage) {
              const damage = Math.max(1, Math.floor(amount));
              设置战斗血量值(targetObj, getCombatHpValue(targetObj) - damage);
              totalRecovered += damage;
              result.desc += ` [条件分支] ${targetObj === attacker ? '自身' : targetObj.name}的${labelText}效果转化为 ${damage} 点伤害。`;
              return;
            }
            const healInvertRatio =
              resourceKey === 'hp'
                ? Math.max(0, targetConditionEffects.reduce((maxVal, ce) => Math.max(maxVal, Number(ce.heal_inversion_ratio || 0)), 0))
                : 0;
            if (resourceKey === 'hp' && healInvertRatio > 0) {
              const invertedDamage = Math.max(1, Math.floor(amount * Math.max(1, healInvertRatio)));
              设置战斗血量值(targetObj, getCombatHpValue(targetObj) - invertedDamage);
              totalRecovered += invertedDamage;
              result.desc += ` [治疗反转] ${targetObj === attacker ? '自身' : targetObj.name}的治疗被扭成伤害，反而损失了 ${invertedDamage} 点HP。`;
              return;
            }
            const 治疗转伤害比例 = Math.max(
              读取直接结算转换比例('治疗转伤害', targetObj),
              targetConditionEffects.reduce((最大值, ce) => Math.max(最大值, Number(ce.heal_to_damage_ratio || 0)), 0),
            );
            if (治疗转伤害比例 > 0) {
              const damage = Math.max(1, Math.floor(amount * Math.min(1, 治疗转伤害比例)));
              if (resourceKey === 'hp') 设置战斗血量值(targetObj, getCombatHpValue(targetObj) - damage);
              else {
                const 当前值 = resourceKey === 'vit'
                  ? getCombatStaminaValue(targetObj)
                  : Math.max(0, Number(targetObj?.[resourceKey] || 0));
                if (resourceKey === 'vit') 设置战斗体力值(targetObj, 当前值 - damage);
                else targetObj[resourceKey] = Math.max(0, 当前值 - damage);
                if (targetObj.召唤键) 同步召唤单位镜像(targetObj);
              }
              totalRecovered += damage;
              result.desc += ` [结算修正] ${targetObj === attacker ? '自身' : targetObj.name}的${labelText}恢复被转为 ${damage} 点损失。`;
              return;
            }
            const 当前资源值 = resourceKey === 'hp'
              ? getCombatHpValue(targetObj)
              : resourceKey === 'vit'
                ? getCombatStaminaValue(targetObj)
                : Math.max(0, Number(targetObj?.[resourceKey] || 0));
            const 下一资源值 = Math.min(maxResource, 当前资源值 + amount);
            const 实际恢复量 = Math.max(0, 下一资源值 - 当前资源值);
            if (!(实际恢复量 > 0)) return;
            if (源动作抹消规则) {
              描述源动作抹消(源动作抹消规则);
              return;
            }
            const 资源变化抹消规则 = 读取资源变化抹消规则(targetObj, labelText);
            if (资源变化抹消规则) {
              result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name}对【${读取战斗机制抹消对象摘要(资源变化抹消规则.抹消对象)}】存在封锁，${labelText}恢复未能生效。`;
              return;
            }
            if (resourceKey === 'hp') 设置战斗血量值(targetObj, 下一资源值);
            else if (resourceKey === 'vit') 设置战斗体力值(targetObj, 下一资源值);
            else targetObj[resourceKey] = 下一资源值;
            if (targetObj.召唤键) 同步召唤单位镜像(targetObj);
            totalRecovered += 实际恢复量;
            if (是主原型效果(effect)) 登记主原型成立目标(targetObj);
            result.desc += ` [机制触发] ${targetObj === attacker ? '自身' : targetObj.name}恢复了 ${实际恢复量} 点${labelText}。`;
          });
          return totalRecovered;
        };

        const applyShieldEffect = effect => {
          if (!effect || Number(result.backlash_dmg || 0) > 0) return 0;
          const effectTargetContext = getEffectTargetContext(effect);
          const targetUnits = 过滤被时光回溯规避目标列表(effectTargetContext.targetSet);
          let totalShield = 0;
          targetUnits.forEach(targetObj => {
            const isFriendly =
              targetObj === attacker || ['自身', '友方单体', '友方群体'].includes(effectTargetContext.targetKind);
            const supportScale = isFriendly ? getSupportEffectScale(attacker, targetObj) : 1;
            const totalShieldGainMult = attackerConditionEffects.reduce(
              (mult, ce) => mult * Number(ce.shield_gain_mult || 1.0),
              1.0,
            );
            const totalShieldGainBonus = attackerConditionEffects.reduce(
              (sum, ce) => sum + Number(ce.shield_gain_bonus || 0),
              0,
            );
            const rawShield = effect.护盾值 ?? effect.数值 ?? 0;
            const shieldValue = 读取战斗数值正负(rawShield);
            const targetMax = targetObj ? getCombatHpMaxValue(targetObj) : 0;
            const baseShield = /%$/.test(String(rawShield ?? '').trim()) || Math.abs(shieldValue) <= 1
              ? Math.floor(targetMax * Math.abs(shieldValue))
              : Math.floor(Math.abs(shieldValue));
            let shieldAmount = Math.floor(baseShield * supportScale * totalShieldGainMult) + totalShieldGainBonus;
            if (fusionProfile) shieldAmount = Math.floor(shieldAmount * Number(fusionProfile.shieldMult || 1));
            shieldAmount = Math.max(0, shieldAmount);
            if (!shieldAmount) return;
            applyShieldToCharacter(
              targetObj,
              shieldAmount,
              Number(effect.持续回合 || 1),
              String(effect.状态 || skillName || '护盾'),
            );
            写入战斗事件账本(combatData, {
              eventKind: 'shield_create',
              round: Number(combatData?.回合 || 0),
              actorName: attacker?.name || attacker?.名称 || '',
              targetName: targetObj?.name || targetObj?.名称 || '',
              actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '护盾',
              actionType: 'support',
              sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '护盾',
              sourceRound: Number(combatData?.回合 || 0),
              result: 'shielded',
              duration: Math.max(0, Number(effect.持续回合 || 1)),
              meta: { amount: shieldAmount, stateName: String(effect.状态 || skillName || '护盾').trim() },
            });
            totalShield += shieldAmount;
            if (是主原型效果(effect)) 登记主原型成立目标(targetObj);
            result.desc += ` [护盾生成] ${targetObj === attacker ? '自身' : targetObj.name}获得 ${shieldAmount} 点护盾。`;
          });
          return totalShield;
        };

        执行授予前置效果 = () => {
          if (!授予前置效果列表.length) return;
          授予前置效果列表.forEach(effect => {
            if (!effect || typeof effect !== 'object') return;
            const 原型名 = String(effect?.原型 || '').trim();
            if (原型名 === '护盾变化' && 读取战斗数值正负(effect?.数值) > 0) {
              applyShieldEffect(effect);
              return;
            }
            if (原型名 === '资源变化' && 读取战斗数值正负(effect?.数值) > 0) {
              const 资源文本 = String(effect?.资源 || effect?.资源类型 || effect?.resource_type || '').trim();
              const 授予目标 = /目标|敌方|单体/.test(String(effect?.目标 || '').trim()) ? defender : attacker;
              const 读取授予资源量 = (rawValue, maxValue) => {
                const text = String(rawValue ?? '').trim();
                const value = 读取战斗数值正负(text);
                if (!(value > 0)) return 0;
                return /%$/.test(text) || Math.abs(value) <= 1
                  ? Math.max(1, Math.floor(Math.max(1, Number(maxValue || 0)) * value))
                  : Math.floor(value);
              };
              if (/魂力/.test(资源文本)) {
                const maxValue = Math.max(1, Number((授予目标?.final || {}).sp_max || 授予目标?.sp_max || 授予目标?.属性?.魂力上限 || 0));
                const amount = 读取授予资源量(effect?.数值, maxValue);
                设置战斗延迟效果资源值(授予目标, 'sp', Math.min(maxValue, Math.max(0, Number(授予目标.sp || 授予目标?.属性?.魂力 || 0)) + amount));
              } else if (/精神/.test(资源文本)) {
                const maxValue = Math.max(1, Number((授予目标?.final || {}).men_max || 授予目标?.men_max || 授予目标?.属性?.精神力上限 || 0));
                const amount = 读取授予资源量(effect?.数值, maxValue);
                设置战斗延迟效果资源值(授予目标, 'men', Math.min(maxValue, Math.max(0, Number(授予目标.men || 授予目标?.属性?.精神力 || 0)) + amount));
              } else {
                applyImmediateRecoveryEffect(effect, /体力/.test(资源文本) ? 'vit' : 'hp', /体力/.test(资源文本) ? '体力' : '生命');
              }
              return;
            }
            runDirectMechanismConsumer(effect);
          });
        };

        const applyRingBurstEffect = effect => {
          if (!effect || !attacker || !playerAction?.skill) return false;
          const 魂环路径 = Array.isArray(playerAction.skill.__魂环路径) ? playerAction.skill.__魂环路径 : [];
          if (!魂环路径.length) {
            result.desc += ` [炸环失败] 当前技能未绑定魂环路径。`;
            return false;
          }
          const 路径去重映射 = new Map();
          const 记录路径 = 路径 => {
            if (!Array.isArray(路径) || !路径.length) return;
            const 标准路径 = 路径.map(片段 => String(片段));
            const 键 = 标准路径.join('>');
            if (!键 || 路径去重映射.has(键)) return;
            路径去重映射.set(键, 标准路径);
          };
          记录路径(魂环路径);
          const 额外炸环路径来源 = [
            ...(Array.isArray(playerAction?.__炸环选择路径列表) ? playerAction.__炸环选择路径列表 : []),
            ...(Array.isArray(playerAction?.skill?.__炸环选择路径列表) ? playerAction.skill.__炸环选择路径列表 : []),
          ];
          额外炸环路径来源.forEach(记录路径);
          const 生效炸环路径列表 = Array.from(路径去重映射.values());
          const 不可用路径 = [];
          生效炸环路径列表.forEach(路径 => {
            const 魂环对象 = 按路径读取对象_V1(attacker, 路径);
            if (!魂环对象 || typeof 魂环对象 !== 'object') {
              不可用路径.push({ 路径, 原因: '路径无效' });
              return;
            }
            const 恢复检查 = 读取魂环路径恢复标记_V1(attacker, 路径);
            if (恢复检查.恢复中) {
              不可用路径.push({
                路径,
                原因: `恢复中(剩余${Math.max(1, Math.floor(Number(恢复检查.剩余tick || 0)))}tick)`,
              });
            }
          });
          if (不可用路径.length) {
            const 首条 = 不可用路径[0];
            result.desc += ` [炸环失败] 指定魂环不可用：${String(首条?.路径?.[首条.路径.length - 1] || '未知魂环')} ${首条.原因}。`;
            return false;
          }
          const 已炸魂环列表 = [];
          for (const 路径 of 生效炸环路径列表) {
            const 魂环对象 = 按路径读取对象_V1(attacker, 路径);
            const 年限 = Math.max(100, Number(魂环对象?.年限 || 100));
            已炸魂环列表.push({
              路径,
              年限,
              档位类型: 判定魂环档位类型_V1(年限),
              魂环键: String(路径?.[路径.length - 1] || ''),
            });
          }
          const 当前tick = 读取当前世界tick_V1();
          const 恢复时长tick = 计算炸环恢复时长tick_V1(已炸魂环列表);
          const 恢复截止tick = 当前tick + 恢复时长tick;
          const 恢复时间文本 = formatBattleTickToCalendarDateText(恢复截止tick);
          const 角色名 = String(attacker?.name || '').trim();
          for (const 路径 of 生效炸环路径列表) {
            const 写回成功 = 写入角色魂环恢复标记_V1(attacker, 路径, 恢复截止tick, 恢复时间文本);
            if (!写回成功) {
              result.desc += ` [炸环失败] 魂环路径写回失败：${String(路径?.[路径.length - 1] || '未知魂环')}。`;
              return false;
            }
            if (角色名) {
              result.extraPatchOps.push(...构建魂环恢复标记补丁_V1(角色名, 路径, 恢复截止tick, 恢复时间文本));
            }
          }
          if (!attacker.状态效果) attacker.状态效果 = {};
          const 总年限 = 已炸魂环列表.reduce((总和, 项) => 总和 + Math.max(100, Number(项.年限 || 100)), 0);
          const 基础增幅 = Math.max(0.01, Math.abs(读取战斗数值正负(effect?.强化倍率 || '+100%')));
          const 面板修改比例 = 构建炸环自身属性增幅_V1(总年限, 基础增幅);
          const 十万年类比 = 构建炸环十万年基准类比文本_V1(基础增幅);
          const 增幅状态键 = `炸环:${playerAction.skill.name || playerAction.skill.魂技名 || '技能'}`;
          const 力量倍率 = Number(面板修改比例.str || 1);
          const 魂力倍率 = Number(面板修改比例.sp_max || 1);
          attacker.状态效果[增幅状态键] = {
            类型: 'buff',
            层数: 1,
            描述: `由[${playerAction.skill.name || '炸环'}]触发，下次魂技释放时临时拔升自身基础属性${十万年类比 ? `；10万年基准：${十万年类比}` : ''}`,
            duration: 1,
            面板修改比例,
            战斗效果: createEmptyCombatEffectMap(),
            炸环参数: { 强化倍率: 基础增幅, 总年限 },
          };
          const 炸环明细文本 = 已炸魂环列表
            .map(项 => `${项.魂环键 || '魂环'}(${项.档位类型}/${Math.floor(Number(项.年限 || 0))}年)`)
            .join('、');
          result.desc += ` [炸环] 已炸${已炸魂环列表.length}枚魂环：${炸环明细文本}。预计于${恢复时间文本}恢复。下次魂技释放时自身属性增幅，力系x${力量倍率.toFixed(2)}，魂力x${魂力倍率.toFixed(2)}。${十万年类比 ? `10万年基准：${十万年类比}。` : ''}`;
          return true;
        };

        const applySummonEffect = 效果 => {
          if (!效果 || !attacker) return false;
          if (!attacker.状态效果) attacker.状态效果 = {};
          const 召唤数量 = Math.max(1, Number(效果?.召唤数量 || 效果?.数量 || 1));
          const 召唤单位类型 = String(效果?.召唤单位类型 || '魂兽').trim() || '魂兽';
          const 允许继承 = 召唤单位类型 === '分身' || 召唤单位类型 === '本命召唤兽';
          if (!允许继承 && (效果?.继承属性比例 !== undefined || 效果?.属性继承比例 !== undefined)) {
            result.desc += ` [召唤失败] ${召唤单位类型}不允许继承属性比例。`;
            return false;
          }
          const 默认继承比例 = 召唤单位类型 === '分身' || 召唤单位类型 === '本命召唤兽' ? 0.45 : 0;
          const 属性继承比例 = 读取召唤属性继承对象(效果, 默认继承比例);
          const 继承比例 = Math.max(
            属性继承比例.力量,
            属性继承比例.防御,
            属性继承比例.敏捷,
            属性继承比例.体力上限,
            属性继承比例.魂力上限,
            属性继承比例.精神力上限,
          );
          if (允许继承 && !(继承比例 > 0)) {
            result.desc += ` [召唤失败] ${召唤单位类型}必须携带继承属性比例。`;
            return false;
          }
          if (允许继承 && 效果?.强度 !== undefined) {
            result.desc += ` [召唤失败] 继承型召唤不应携带强度。`;
            return false;
          }
          if (!允许继承 && 效果?.强度 === undefined) {
            result.desc += ` [召唤失败] 模板召唤缺少强度。`;
            return false;
          }
          if (召唤单位类型 === '本命召唤兽' && 召唤数量 !== 1) {
            result.desc += ` [召唤失败] 本命召唤兽数量必须为1。`;
            return false;
          }
          if (!['分身', '其他召唤生物'].includes(召唤单位类型) && 召唤数量 !== 1) {
            result.desc += ` [召唤失败] ${召唤单位类型}不允许批量召唤。`;
            return false;
          }
          const 召唤强度 = 继承比例 > 0 ? 继承比例 : Math.max(0.01, Number(效果?.强度 || 1));
          const 持续回合 = Math.max(1, Number(效果?.持续回合 || 2));
          const 召唤物名称 = String(效果?.召唤物名称 || `${skillName || '技能'}召唤物`).trim();
          const 同名召唤上限 = Math.max(1, Number(效果?.场上上限 || 效果?.同名上限 || 效果?.max_count || 2));
          const 当前同名召唤数 = 读取召唤单位列表(combatData, { 宿主: attacker }).filter(单位 =>
            isCombatUnitAlive(单位) &&
            String(单位?.类型 || '').trim() === 召唤单位类型 &&
            String(单位?.name || 单位?.名称 || '').trim().replace(/#\d+$/, '') === 召唤物名称.replace(/#\d+$/, '')
          ).length;
          if (当前同名召唤数 + 召唤数量 > 同名召唤上限) {
            result.desc += ` [召唤失败] ${召唤单位类型}【${召唤物名称}】场上数量已达上限。`;
            写入战斗事件账本(combatData, {
              eventKind: 'failed_action',
              round: Number(combatData?.回合 || 0),
              actorName: attacker?.name || attacker?.名称 || '',
              actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '召唤',
              actionType: '召唤生成',
              result: 'fail',
              failReason: '造物已达上限',
              reasonCode: 'CAP_REACHED',
              primaryOutcome: 'cap_reached',
              meta: {
                reasonCode: 'CAP_REACHED',
                primaryOutcome: 'cap_reached',
                summonName: 召唤物名称,
                summonType: 召唤单位类型,
                currentCount: 当前同名召唤数,
                cap: 同名召唤上限,
              },
            });
            return false;
          }
          const 类型配置 = 读取召唤类型配置(召唤单位类型);
          const 行动模式 = String(效果?.行动模式 || 类型配置.默认行动模式 || '协同攻击').trim();
          const 承伤规则 = 行动模式 === '护卫' ? String(效果?.承伤规则 || '护卫承伤').trim() : '';
          if (召唤单位类型 !== '其他召唤生物' && 召唤已被主动收回(combatData, attacker, 召唤单位类型, 召唤物名称)) {
            result.desc += ` [召唤失败] ${召唤单位类型}【${召唤物名称}】已在本场主动收回，不能再次召唤。`;
            return false;
          }
          const 已存在召唤 = 读取召唤单位列表(combatData, { 宿主: attacker }).find(单位 =>
            String(单位?.类型 || '').trim() === 召唤单位类型 &&
            (
              召唤单位类型 === '本命召唤兽' ||
              String(单位?.name || 单位?.名称 || '').trim() === 召唤物名称
            ) &&
            isCombatUnitAlive(单位),
          );
          if (召唤单位类型 !== '其他召唤生物' && 已存在召唤) {
            result.desc += ` [召唤失败] ${召唤单位类型}【${召唤物名称}】已在场，不能重复召唤。`;
            return false;
          }
          const 构建召唤状态 = (单位名称, 单位数量) => {
            const 计算层效果 = 效果?.计算层效果 && typeof 效果.计算层效果 === 'object' ? 效果.计算层效果 : {};
            const 战斗效果 = createEmptyCombatEffectMap();
            if (召唤单位类型 !== '分身') {
              战斗效果.final_damage_mult = Number(计算层效果.final_damage_mult || Math.min(1.5, 1 + 召唤强度 * 0.12 + 单位数量 * 0.03));
            }
            if (召唤单位类型 !== '分身' && /护卫|承伤|分摊/.test(行动模式 + 承伤规则)) {
              战斗效果.damage_reduction = Number(计算层效果.damage_reduction || Math.min(0.3, 召唤强度 * 0.08));
            }
            const 召唤物记录 = { 召唤单位类型, 召唤物名称: 单位名称, 基础名称: 读取召唤编号基名(召唤物名称), 召唤数量: 单位数量, 行动模式 };
            if (继承比例 > 0) {
              召唤物记录.继承属性比例 = 继承比例;
              召唤物记录.属性继承比例 = 属性继承比例;
            }
            else 召唤物记录.强度 = 召唤强度;
            if (承伤规则) 召唤物记录.承伤规则 = 承伤规则;
            return {
              类型: 'buff',
              层数: 单位数量,
              描述: `由[${skillName || '召唤'}]生成的${召唤单位类型}`,
              duration: 持续回合,
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果,
              召唤物: 召唤物记录,
            };
          };
          const 已生成 = [];
          if (召唤单位类型 === '其他召唤生物') {
            for (let 序号 = 0; 序号 < 召唤数量; 序号 += 1) {
              const 编号 = 读取下一个召唤编号(combatData, attacker, 召唤单位类型, 召唤物名称);
              const 单位名称 = `${读取召唤编号基名(召唤物名称)}#${编号}`;
              const 状态 = 构建召唤状态(单位名称, 1);
              const 状态键 = putConditionWithUniqueKey(attacker, `召唤:${单位名称}`, 状态);
              const 召唤单位 = 注册召唤运行态单位(combatData, attacker, 状态键, 状态);
              if (召唤单位) {
                已生成.push(召唤单位.name || 单位名称);
                记账召唤生成事件(combatData, attacker, skillName || '召唤', 召唤单位, { 召唤单位类型, 行动模式, 召唤物名称: 单位名称 });
              }
            }
            result.desc += ` [召唤] ${attacker.name || '施术者'}召唤${召唤单位类型}【${召唤物名称}】×${召唤数量}${已生成.length ? `：${已生成.join('、')}` : ''}。`;
            return !!已生成.length;
          }
          const 状态 = 构建召唤状态(召唤物名称, 召唤数量);
          const 状态键 = putConditionWithUniqueKey(attacker, `召唤:${召唤物名称}`, 状态);
          const 召唤单位 = 注册召唤运行态单位(combatData, attacker, 状态键, 状态);
          if (召唤单位) 记账召唤生成事件(combatData, attacker, skillName || '召唤', 召唤单位, { 召唤单位类型, 行动模式, 召唤物名称 });
          result.desc += ` [召唤] ${attacker.name || '施术者'}召唤${召唤单位类型}【${召唤物名称}】×${召唤数量}，进入[${状态键}]${召唤单位 ? '并形成战斗单位' : ''}。`;
          return true;
        };

        const removeConditionWithSustain = (char, key) => {
          if (!char?.状态效果 || !char.状态效果[key]) return null;
          const snapshot = deepClone(char.状态效果[key]);
          delete char.状态效果[key];
          if (char.持续效果) {
            Object.keys(char.持续效果).forEach(sustainKey => {
              if (char.持续效果[sustainKey]?.related_condition === key) delete char.持续效果[sustainKey];
            });
          }
          if (char.召唤键) 同步召唤单位镜像(char);
          return snapshot;
        };

        const putConditionWithUniqueKey = (char, baseKey, cond) => {
          if (!char) return '';
          if (!char.状态效果) char.状态效果 = {};
          let key = String(baseKey || '状态').trim() || '状态';
          if (!char.状态效果[key]) {
            char.状态效果[key] = cond;
            if (char.召唤键) 同步召唤单位镜像(char);
            return key;
          }
          let index = 1;
          while (char.状态效果[`${key}·${index}`]) index += 1;
          const nextKey = `${key}·${index}`;
          char.状态效果[nextKey] = cond;
          if (char.召唤键) 同步召唤单位镜像(char);
          return nextKey;
        };

        const isTransferableConditionEntry = ([key, cond], expectedType = 'any') => {
          if (!key || !cond) return false;
          if (String(key).startsWith(AUTO_PROJECTED_CONDITION_PREFIX)) return false;
          if (expectedType !== 'any' && String(cond?.类型 || '') !== expectedType) return false;
          if (Number(cond?.shield_value || 0) > 0) return false;
          if (cond?.field_desc) return false;
          if (cond?.召唤物) return false;
          const text = `${String(key)} ${String(cond?.描述 || '')} ${String(cond?.特殊机制标识 || '')}`;
          if (/护盾|屏障|结界|领域|场地|召唤|真身|炸环|免死|复活|回溯/.test(text)) return false;
          const ce = cond?.战斗效果 || {};
          if (
            Number(ce.death_save_count || 0) > 0 ||
            Number(ce.revive_count || 0) > 0 ||
            String(cond?.特殊机制标识 || '').includes('时光回溯') ||
            ce.invincible === true
          ) return false;
          return true;
        };

        const applyResourceDurationEffect = effect => {
          if (!effect) return 0;
          const 持续回合 = Math.max(1, Math.min(10, Math.round(Number(effect?.持续回合 || 1))));
          const targetUnits = resolveDirectMechanismTargetList(effect);
          if (!targetUnits.length) return 0;
          const 源动作抹消规则 = 读取源动作抹消规则(effect, { 消费: false });
          if (源动作抹消规则) {
            描述源动作抹消(源动作抹消规则);
            return 0;
          }
          const 资源文本 = String(effect?.资源 || '').trim();
          const rawValue = effect?.数值 ?? 0;
          const 数值 = 读取战斗数值正负(rawValue);
          if (!Number.isFinite(数值) || 数值 === 0) return 0;
          let applied = 0;
          targetUnits.forEach(targetObj => {
            if (!targetObj) return;
            const 战斗效果 = createEmptyCombatEffectMap();
            const 写比例 = (字段名, maxValue) => {
              if (!字段名 || !(maxValue > 0)) return false;
              const ratio = /%$/.test(String(rawValue ?? '').trim()) || Math.abs(数值) <= 1
                ? 数值
                : 数值 / Math.max(1, maxValue);
              if (!Number.isFinite(ratio) || ratio === 0) return false;
              战斗效果[字段名] = ratio;
              return true;
            };
            let labelText = 资源文本 || '资源';
            let ok = false;
            if (/魂力/.test(资源文本)) ok = 写比例('sp_gain_ratio', 读取转移资源上限值(targetObj, 'sp'));
            else if (/精神/.test(资源文本)) ok = 写比例('men_gain_ratio', 读取转移资源上限值(targetObj, 'men'));
            else if (/体力/.test(资源文本)) ok = 写比例('vit_gain_ratio', 读取转移资源上限值(targetObj, 'vit'));
            else {
              labelText = '生命';
              const maxHp = getCombatHpMaxValue(targetObj);
              const ratio = /%$/.test(String(rawValue ?? '').trim()) || Math.abs(数值) <= 1
                ? 数值
                : 数值 / Math.max(1, maxHp);
              if (数值 < 0 && !/%$/.test(String(rawValue ?? '').trim()) && Math.abs(数值) > 1) {
                战斗效果.dot_damage = Math.abs(Math.floor(数值));
                ok = true;
              } else if (Number.isFinite(ratio) && ratio !== 0) {
                if (ratio < 0) 战斗效果.dot_damage_ratio = Math.abs(ratio);
                else 战斗效果.hot_heal_ratio = ratio;
                ok = true;
              }
            }
            if (!ok) return;
            putConditionWithUniqueKey(targetObj, `${labelText}${数值 >= 0 ? '持续恢复' : '持续流失'}`, {
              类型: 数值 >= 0 ? 'buff' : 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]施加${labelText}${数值 >= 0 ? '持续恢复' : '持续流失'}`,
              duration: 持续回合,
              持续回合,
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果,
            });
            applied += 1;
          });
          if (applied > 0) result.desc += ` [资源持续] 已为${applied}个目标登记持续资源变化。`;
          return applied;
        };

        const scoreTransferableCondition = (cond = {}) => {
          const ce = cond?.战斗效果 || {};
          let score = Number(cond?.duration || 0);
          score += ce.skip_turn ? 1000 : 0;
          score += ce.cannot_react ? 800 : 0;
          score += Number(ce.lock_level || 0) * 100;
          score += Number(ce.counter_attack_ratio || 0) * 120;
          score += Number(ce.damage_reduction || 0) * 100;
          score += Math.max(0, Number(ce.final_damage_mult || 1) - 1) * 120;
          score += Math.max(0, Number(ce.received_damage_mult || 1) - 1) * 100;
          score += Number(ce.defense_strip || 0) * 95;
          score += Number(ce.spirit_resist_strip || 0) * 95;
          score += Math.max(0, Number(ce.final_heal_mult || 1) - 1) * 100;
          score += Number(ce.dot_damage || 0) / 5;
          score += Number(ce.heal_block_ratio || 0) * 100;
          score += 读取状态资源锁定强度(cond) * 100;
          score += Number(ce.reaction_bonus || 0) * 60;
          score += Number(ce.reaction_penalty || 0) * 60;
          score += Number(ce.dodge_bonus || 0) * 60;
          score += Number(ce.dodge_penalty || 0) * 60;
          score += Number(ce.hit_bonus || 0) * 60;
          score += Number(ce.cast_speed_bonus || 0) * 60;
          score += Number(ce.cast_speed_penalty || 0) * 60;
          Object.values(cond?.面板修改比例 || {}).forEach(value => {
            score += Math.abs(Number(value || 1) - 1) * 100;
          });
          return score;
        };

        const pickTransferableCondition = (char, preferredTypes = ['any'], expectedState = '') => {
          if (!char?.状态效果) return null;
          const stateText = String(expectedState || '').trim();
          for (const expectedType of preferredTypes) {
            const candidates = Object.entries(char.状态效果)
              .filter(entry => isTransferableConditionEntry(entry, expectedType))
              .filter(([key, cond]) => {
                if (!stateText || ['任意状态', '任意增益', '任意负面'].includes(stateText)) return true;
                return `${String(key || '')} ${String(cond?.状态 || '')} ${String(cond?.状态名称 || '')} ${String(cond?.描述 || '')}`.includes(stateText);
              })
              .sort((a, b) => scoreTransferableCondition(b[1]) - scoreTransferableCondition(a[1]));
            if (candidates.length > 0) return { key: candidates[0][0], cond: candidates[0][1] };
          }
          return null;
        };

        const applyStatusTransferEffect = effect => {
          if (!effect) return false;
          const targetObj = resolveDirectMechanismTargetList(effect, { single: true });
          const targetContext = resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData, effect);
          const 目标主集合 = dedupeCombatTargetList(targetContext?.targetSet || []).filter(Boolean);
          const rawTransferCount = String(effect?.数量 || '').trim();
          const transferCount = rawTransferCount === '全部' ? 99 : Math.max(1, Math.floor(Number(effect?.数量 || 1)) || 1);
          const 目标状态 = String(effect?.状态 || '').trim();
          const 显式来源 = String(effect?.来源 || '').trim();
          const 显式去向 = String(effect?.去向 || '').trim();
          if (!['自身', '目标', '友方', '敌方'].includes(显式来源) || !['自身', '目标', '友方', '敌方'].includes(显式去向)) {
            result.desc += ` [状态转移] 来源和去向无效。`;
            return false;
          }
          const 读取目标所在侧候选 = () => {
            const 目标标识列表 = (目标主集合.length ? 目标主集合 : [targetObj])
              .map(目标对象 => 目标对象?.name || 目标对象?.名称 || 目标对象)
              .filter(Boolean);
            if ((targetContext.alliedSet || []).some(unit => 目标标识列表.some(标识 => isCombatUnitIdentityMatch(unit, 标识)))) return targetContext.alliedSet || [];
            if ((targetContext.hostileSet || []).some(unit => 目标标识列表.some(标识 => isCombatUnitIdentityMatch(unit, 标识)))) return targetContext.hostileSet || [];
            return 目标主集合.length ? 目标主集合 : [targetObj].filter(Boolean);
          };
          const 读取端点候选 = 端点 => {
            if (端点 === '自身') return [attacker].filter(Boolean);
            if (端点 === '目标') return 显式来源 === 显式去向 ? 读取目标所在侧候选() : (目标主集合.length ? 目标主集合 : [targetObj].filter(Boolean));
            if (端点 === '友方') return targetContext.alliedSet || [];
            if (端点 === '敌方') return targetContext.hostileSet || [];
            return [];
          };
          const 构建转移方案 = () => {
            const 来源候选 = 读取端点候选(显式来源 || '自身');
            const 去向候选 = 读取端点候选(显式去向 || '目标');
            if (显式来源 === '目标' && 显式去向 === '目标' && dedupeCombatTargetList([...来源候选, ...去向候选]).length < 2) {
              return [];
            }
            return 来源候选.flatMap(sourceChar =>
              去向候选
                .filter(receiver => sourceChar && receiver && !isCombatUnitIdentityMatch(receiver, sourceChar.name || sourceChar))
                .map(receiver => {
                  const 来源立场 = 读取行为规划单位立场(sourceChar, attacker, { primaryTarget: targetObj });
                  const 去向立场 = 读取行为规划单位立场(receiver, attacker, { primaryTarget: targetObj });
                  const 优先状态类型 = 显式来源 === '目标' && 显式去向 === '目标'
                    ? ['any']
                    : 来源立场 === '己方'
                    ? (去向立场 === '敌方' ? ['debuff', 'buff'] : ['debuff'])
                    : (去向立场 === '己方' ? ['buff', 'debuff'] : ['buff']);
                  return { sourceChar, receiver, preferredTypes: 优先状态类型, keySuffix: '' };
                }),
            );
          };
          const transferLogs = [];
          for (let index = 0; index < transferCount; index += 1) {
            let movedOne = false;
            for (const plan of 构建转移方案()) {
              const sourceChar = plan.sourceChar;
              const receiver = plan.receiver;
              if (!sourceChar || !receiver || sourceChar === receiver) continue;
              const candidate = pickTransferableCondition(sourceChar, plan.preferredTypes, 目标状态);
              if (!candidate) continue;
              const moved = removeConditionWithSustain(sourceChar, candidate.key);
              if (!moved) continue;
              moved.描述 = `由[${skillName || '技能'}]自${sourceChar.name || '自身'}转移至${receiver.name || '自身'}`;
              const nextKey = putConditionWithUniqueKey(receiver, `${candidate.key}${plan.keySuffix || ''}`, moved);
              if (sourceChar.召唤键) 同步召唤单位镜像(sourceChar);
              if (receiver.召唤键) 同步召唤单位镜像(receiver);
              transferLogs.push(
                `${sourceChar === attacker ? '自身' : sourceChar.name}的[${candidate.key}]被转移到${receiver === attacker ? '自身' : receiver.name}，现为[${nextKey}]`,
              );
              movedOne = true;
              break;
            }
            if (!movedOne) break;
          }
          if (!transferLogs.length) {
            if (显式来源 === '目标' && 显式去向 === '目标' && 读取目标所在侧候选().length < 2) {
              result.desc += ` [状态转移] 目标侧缺少可接收对象，没有可接收状态的目标。`;
            } else {
              result.desc += ` [状态转移] 没有找到可转移的状态。`;
            }
            return false;
          }
          result.desc += ` [状态转移] ${transferLogs.join('；')}。`;
          return true;
        };

        const applyDotDetonateEffect = effect => {
          if (!effect) return 0;
          const 目标列表 = resolveDirectMechanismTargetList(effect);
          const targetUnits = 目标列表.length
            ? 目标列表
            : (defender && defender !== attacker ? [defender] : (attacker ? [attacker] : []));
          const ratio = Math.max(0.1, Number(effect?.引爆倍率 || effect?.detonate_ratio || 1));
          let totalAddedDamage = 0;
          targetUnits.forEach(targetObj => {
            if (!targetObj?.状态效果) return;
            let consumedDot = 0;
            const 目标标记名 = String(targetObj?.name || (targetObj === attacker ? '自身' : '') || '').trim();
            Object.entries(targetObj.状态效果).forEach(([key, cond]) => {
              if (!战斗效果含结构化持续伤害(cond)) return;
              const dotDamage = Math.max(
                估算单位持续伤害数值(targetObj, cond),
              );
              if (!(dotDamage > 0)) return;
              consumedDot += dotDamage;
              if (effect?.消耗持续伤害 !== false) {
                const 移除快照 = removeConditionWithSustain(targetObj, key);
                if (移除快照) 引爆移除状态标记集.add(`${目标标记名}::${String(key || '').trim()}`);
                else if (targetObj?.状态效果?.[key]) {
                  delete targetObj.状态效果[key];
                  if (targetObj.召唤键) 同步召唤单位镜像(targetObj);
                  引爆移除状态标记集.add(`${目标标记名}::${String(key || '').trim()}`);
                }
              }
            });
            if (!(consumedDot > 0)) return;
            const bonusDamage = Math.max(1, Math.floor(consumedDot * ratio));
            appendProjectedDamageToSettleResult(result, targetObj, bonusDamage, 'dot_detonate');
            totalAddedDamage += bonusDamage;
            result.desc += ` [持续引爆] ${targetObj === attacker ? '自身' : targetObj.name}身上的持续伤害被引爆，追加 ${bonusDamage} 点伤害。`;
          });
          return totalAddedDamage;
        };

        const applyDotDurationAdjustEffect = effect => {
          if (!effect) return false;
          const 调整字段 = String(effect?.调整字段 || '').trim() || '持续回合';
          const 调整方式 = String(effect?.调整方式 || '').trim() === '压缩' ? '压缩' : '延长';
          const targetUnits = resolveDirectMechanismTargetList(effect);
          if (!targetUnits.length) return false;
          const roundStep = Math.max(
            1,
            Math.min(3, Number(effect?.调整回合 || effect?.延长回合 || effect?.压缩回合 || effect?.consume_rounds || effect?.extend_rounds || 1)),
          );
          const tickStep = Math.max(1, Math.min(1008, Number(effect?.调整tick || effect?.有效期tick || roundStep * 6 || 6)));
          const countStep = Math.max(1, Math.min(3, Number(effect?.调整次数 || effect?.调整回合 || 1)));
          const currentTick = Math.max(0, Number(window.BattleUIBridge?.getMVU?.('world.时间.tick') || 0));
          const 单日使用次数上限 = Math.max(1, Math.min(3, Math.floor(Number(effect?.单日使用次数上限 || 1))));
          let 每日额度已消费 = false;
          let 每日额度允许 = true;
          const 消费时窗每日额度 = () => {
            if (每日额度已消费) return 每日额度允许;
            每日额度已消费 = true;
            const 技能名 = String(playerAction?.skill?.魂技名 || playerAction?.skill?.name || playerAction?.action_type || '时窗修正').trim() || '时窗修正';
            const quota = consumeDailyMechanismTrigger(attacker, `${技能名}:时窗修正:${调整字段}:${调整方式}`, 单日使用次数上限, currentTick);
            每日额度允许 = quota.allowed;
            if (!quota.allowed) result.desc += ` [时窗修正] ${attacker?.name || '施术者'}今日时窗修正次数已用尽。`;
            return 每日额度允许;
          };
          let 已产生DOT压缩伤害 = false;
          let changed = false;
          targetUnits.forEach(targetObj => {
            if (!targetObj) return;
            if (!targetObj.状态效果 || typeof targetObj.状态效果 !== 'object') targetObj.状态效果 = {};
            if (调整字段 === '有效期tick') return;
            if (['触发次数', '使用次数', '可用次数'].includes(调整字段)) {
              let touched = false;
              if (['使用次数', '可用次数'].includes(调整字段) && targetObj.__技能限制运行态 && typeof targetObj.__技能限制运行态 === 'object') {
                Object.entries(targetObj.__技能限制运行态).forEach(([技能名, limitState]) => {
                  if (!limitState || typeof limitState !== 'object') return;
                  const 目标技能名 = String(effect?.目标技能 || '').trim();
                  if (目标技能名 && 技能名 !== 目标技能名) return;
                  const fieldName = 调整字段 === '可用次数'
                    ? '可用次数'
                    : (limitState.已用次数 !== undefined ? '已用次数' : '可用次数');
                  if (limitState[fieldName] === undefined) return;
                  const before = Math.max(0, Number(limitState[fieldName] || 0));
                  const after = fieldName === '已用次数'
                    ? (调整方式 === '延长' ? Math.max(0, before - countStep) : before + countStep)
                    : (调整方式 === '延长' ? before + countStep : Math.max(0, before - countStep));
                  if (after === before) return;
                  if (!消费时窗每日额度()) return;
                  limitState[fieldName] = after;
                  touched = true;
                });
              }
              Object.values(targetObj.状态效果 || {}).forEach(cond => {
                if (!cond || typeof cond !== 'object') return;
                const fieldName = 调整字段 === '使用次数' || 调整字段 === '可用次数' ? '可用次数' : '次数';
                if (cond[fieldName] === undefined) return;
                const before = Math.max(0, Number(cond[fieldName] || 0));
                const after = 调整方式 === '延长' ? before + countStep : Math.max(0, before - countStep);
                if (after === before) return;
                if (!消费时窗每日额度()) return;
                cond[fieldName] = after;
                touched = true;
              });
              if (!touched) return;
              changed = true;
              if (targetObj.召唤键) 同步召唤单位镜像(targetObj);
              result.desc += ` [次数调制] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}的${调整字段}已${调整方式 === '延长' ? '增加' : '减少'}。`;
              return;
            }
            if (调整字段 === '冷却回合') {
              if (!targetObj.__技能限制运行态 || typeof targetObj.__技能限制运行态 !== 'object') return;
              let touched = false;
              Object.entries(targetObj.__技能限制运行态).forEach(([技能名, limitState]) => {
                if (!limitState || typeof limitState !== 'object') return;
                const 目标技能名 = String(effect?.目标技能 || '').trim();
                if (目标技能名 && 技能名 !== 目标技能名) return;
                if (limitState.冷却剩余 === undefined) return;
                const before = Math.max(0, Number(limitState.冷却剩余 || 0));
                const after = 调整方式 === '延长' ? before + roundStep : Math.max(0, before - roundStep);
                if (after === before) return;
                if (!消费时窗每日额度()) return;
                limitState.冷却剩余 = after;
                touched = true;
              });
              if (!touched) return;
              changed = true;
              result.desc += ` [冷却调制] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}的技能冷却已${调整方式 === '延长' ? '延长' : '压缩'}。`;
              return;
            }

            Object.entries(targetObj.状态效果).forEach(([key, cond]) => {
              if (!cond || typeof cond !== 'object') return;
              const ce = cond?.战斗效果 || {};
              const dotDamage = 估算单位持续伤害数值(targetObj, cond);
              const durationRaw = Math.max(0, Number(cond?.duration || cond?.持续回合 || 0));
              if (调整字段 === '持续回合' || 调整字段 === '持续tick') {
                if (调整方式 === '压缩' && 战斗效果含结构化持续伤害(cond) && dotDamage > 0) {
                  const usableRounds = Math.max(0, Math.min(roundStep, durationRaw));
                  if (usableRounds <= 0) return;
                  if (!消费时窗每日额度()) return;
                  const 时窗数值 = Math.abs(读取战斗数值正负(effect?.结算倍率));
                  const compressRatio = Math.max(1, Number(effect?.压缩倍率 || effect?.compress_ratio || 时窗数值 || 1.2));
                  const bonusDamage = Math.max(1, Math.floor(dotDamage * usableRounds * compressRatio));
                  appendProjectedDamageToSettleResult(result, targetObj, bonusDamage, 'dot_compress');
                  cond.duration = Math.max(0, durationRaw - usableRounds);
                  if (cond.duration <= 0) removeConditionWithSustain(targetObj, key);
                  已产生DOT压缩伤害 = true;
                  changed = true;
                  return;
                }
                const after = 调整方式 === '延长' ? Math.max(durationRaw, 1) + roundStep : Math.max(0, durationRaw - roundStep);
                if (after === durationRaw) return;
                if (!消费时窗每日额度()) return;
                cond.duration = after;
                if (cond.持续回合 !== undefined) cond.持续回合 = after;
                if (after <= 0) removeConditionWithSustain(targetObj, key);
                changed = true;
              }
            });
            if (changed && targetObj.召唤键) 同步召唤单位镜像(targetObj);
          });
          if (changed) {
            if (调整字段 === '持续回合' || 调整字段 === '持续tick') {
              if (调整方式 === '延长') result.desc += ` [持续调制] 目标持续时窗被延长。`;
              if (调整方式 === '压缩') result.desc += 已产生DOT压缩伤害
                ? ` [持续压缩] 已将部分持续伤害折算为即时打击。`
                : ` [持续压缩] 目标持续时窗已压缩。`;
            }
          }
          return changed;
        };

        const applyShieldBreakEffect = effect => {
          if (!effect) return 0;
          const targetUnits = resolveDirectMechanismTargetList(effect);
          let totalRemovedShield = 0;
          targetUnits.forEach(targetObj => {
            const removedShield = 斩除目标当前护盾(targetObj, effect?.数值 || '-100%');
            if (!(removedShield > 0)) return;
            totalRemovedShield += removedShield;
            result.desc += ` [斩盾] ${targetObj === attacker ? '自身' : targetObj.name}被削减 ${removedShield} 点护盾。`;
          });
          return totalRemovedShield;
        };

        const resolveTransferResourceKeys = rawType => {
          const source = Array.isArray(rawType) ? rawType : String(rawType || '').split(/[、,，/|｜；;\s]+/g);
          const text = source.map(item => String(item || '').trim()).filter(Boolean).join('、');
          const keys = [];
          if (/生命|HP|hp/i.test(text)) keys.push('hp');
          if (/体力|vit|sta/i.test(text)) keys.push('vit');
          if (/魂力|sp/i.test(text) || /双|混合|全部/.test(text)) keys.push('sp');
          if (/精神|men/i.test(text) || /双|混合|全部/.test(text)) keys.push('men');
          return keys.length ? Array.from(new Set(keys)) : ['sp'];
        };

        const getTransferResourceLabel = resourceKey => 读取战斗资源键标签(resourceKey);

        const 读取转移资源当前值 = (targetObj, resourceKey) => {
          if (resourceKey === 'hp') return getCombatHpValue(targetObj);
          if (resourceKey === 'vit') return getCombatStaminaValue(targetObj);
          return Math.max(0, Number(targetObj?.[resourceKey] ?? targetObj?.属性?.[resourceKey === 'sp' ? '魂力' : '精神力'] ?? 0));
        };

        const 读取转移资源上限值 = (targetObj, resourceKey) => {
          if (resourceKey === 'hp') return getCombatHpMaxValue(targetObj);
          if (resourceKey === 'vit') return getCombatStaminaMaxValue(targetObj);
          return Math.max(1, Number(targetObj?.[resourceKey === 'sp' ? 'sp_max' : 'men_max'] ?? targetObj?.属性?.[resourceKey === 'sp' ? '魂力上限' : '精神力上限'] ?? 1));
        };

        const 设置转移资源当前值 = (targetObj, resourceKey, value) => {
          const nextValue = Math.max(0, Number(value) || 0);
          if (resourceKey === 'hp') return 设置战斗血量值(targetObj, nextValue);
          if (resourceKey === 'vit') return 设置战斗体力值(targetObj, nextValue);
          return 设置战斗延迟效果资源值(targetObj, resourceKey, nextValue);
        };

        const 资源变化落地被抹消 = (targetObj, resourceKey, 动作文本 = '资源变化', options = {}) => {
          const 资源变化抹消规则 = 读取资源变化抹消规则(targetObj, getTransferResourceLabel(resourceKey), options);
          if (!资源变化抹消规则) return false;
          result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}对【${读取战斗机制抹消对象摘要(资源变化抹消规则.抹消对象)}】存在封锁，${动作文本}未能生效。`;
          return true;
        };

        const 计算资源变化量 = (rawValue, maxValue) => {
          const text = String(rawValue ?? '').trim();
          const parsed = 读取战斗数值正负(text);
          if (!Number.isFinite(parsed) || parsed === 0) return 0;
          const amount = /%$/.test(text) || Math.abs(parsed) <= 1
            ? Math.floor(Math.max(1, Number(maxValue || 0)) * Math.abs(parsed))
            : Math.floor(Math.abs(parsed));
          return Math.max(1, amount);
        };
        const 计算资源转移正向收益 = amount => Math.max(0, Math.floor(Number(amount || 0)));

        const 读取资源锁定比例 = (targetObj, resourceKey, lockType) => {
          const label = getTransferResourceLabel(resourceKey);
          return Math.min(
            1,
            Object.values(targetObj?.状态效果 || {}).reduce((maxVal, 状态) => {
              const 规则列表 = Array.isArray(状态?.资源锁定规则) ? 状态.资源锁定规则 : [];
              const 命中比例 = 规则列表.reduce((规则最大值, 规则) => {
                const 资源命中 = Array.isArray(规则?.资源) && 规则.资源.includes(label);
                const 类型命中 = String(规则?.锁定类型 || '').trim() === lockType;
                return 资源命中 && 类型命中 ? Math.max(规则最大值, Number(规则?.比例 || 0)) : 规则最大值;
              }, 0);
              return Math.max(maxVal, 命中比例);
            }, 0),
          );
        };

        const 计算资源锁定后获得量 = (targetObj, resourceKey, amount, lockType) => {
          const ratio = 读取资源锁定比例(targetObj, resourceKey, lockType);
          return Math.max(0, Math.floor(Number(amount || 0) * (1 - ratio)));
        };

        const applyResourceTransferEffect = effect => {
          if (!effect) return 0;
          const 转移方式 = String(effect?.资源转移方式 || '').trim();
          if (!['吞噬', '共享', '均分', '转移'].includes(转移方式)) return 0;
          const targetUnits = resolveDirectMechanismTargetList(effect);
          const resourceKeys = resolveTransferResourceKeys(effect?.资源 || '');
          const convertRatio = Math.max(0, Math.min(2, Number(effect?.转化比例 ?? 1) || 1));
          const 回收汇总 = new Map();
          let totalChanged = 0;
          const 写入资源变化事件 = (targetObj, resourceKey, delta, reason = '') => {
            const amount = Math.round(Number(delta || 0));
            if (!targetObj || !amount) return null;
            const label = getTransferResourceLabel(resourceKey);
            return 写入战斗事件账本(combatData, {
              eventKind: 'resource_change',
              round: Number(combatData?.回合 || 0),
              actorName: attackerName,
              targetName: targetObj?.name || targetObj?.名称 || (targetObj === attacker ? attackerName : '目标'),
              actionName: skillName || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '资源转移',
              actionType: 'resource_change',
              sourceActionName: skillName || playerAction?.skill?.name || playerAction?.skill?.魂技名 || '资源转移',
              sourceRound: Number(combatData?.回合 || 0),
              result: amount > 0 ? 'gain' : 'loss',
              meta: {
                resourceKey,
                resource: label,
                amount: Math.abs(amount),
                delta: amount,
                transferMode: 转移方式,
                reason,
              },
            });
          };
          if (转移方式 === '均分') {
            const 均分强度 = Math.max(0, Math.min(1, Math.abs(读取战斗数值正负(effect?.数值 || '100%')) || 1));
            const 均分目标列表 = dedupeCombatTargetList(String(effect?.目标 || '').trim() === '单体' ? [attacker, ...targetUnits] : targetUnits).filter(isCombatUnitAlive);
            if (均分目标列表.length < 2) return 0;
            resourceKeys.forEach(resourceKey => {
              const 资源总量 = 均分目标列表.reduce((总和, targetObj) => 总和 + 读取转移资源当前值(targetObj, resourceKey), 0);
              const 平均值 = 资源总量 / 均分目标列表.length;
              均分目标列表.forEach(targetObj => {
                const 当前值 = 读取转移资源当前值(targetObj, resourceKey);
                const 上限值 = 读取转移资源上限值(targetObj, resourceKey);
                const 原始变化 = (平均值 - 当前值) * 均分强度;
                const 新值 = Math.max(0, Math.min(上限值, Math.round(当前值 + 原始变化)));
                const 变动量 = Math.abs(新值 - 当前值);
                if (!(变动量 > 0)) return;
                if (资源变化落地被抹消(targetObj, resourceKey, '资源均分')) return;
                设置转移资源当前值(targetObj, resourceKey, 新值);
                写入资源变化事件(targetObj, resourceKey, 新值 - 当前值, '资源均分');
                totalChanged += 变动量;
                result.desc += ` [均分] ${targetObj === attacker ? '自身' : targetObj.name}的${getTransferResourceLabel(resourceKey)}调整 ${新值 - 当前值 >= 0 ? '+' : ''}${新值 - 当前值}。`;
              });
            });
            return totalChanged;
          }
          targetUnits.forEach(targetObj => {
            resourceKeys.forEach(resourceKey => {
              const targetCurrent = 读取转移资源当前值(targetObj, resourceKey);
              const targetMax = 读取转移资源上限值(targetObj, resourceKey);
              const rawAmount = 计算资源变化量(effect?.数值, targetMax);
              if (!(rawAmount > 0)) return;
              if (转移方式 === '共享' || 转移方式 === '转移') {
                if (targetObj === attacker) return;
                const 自身当前值 = 读取转移资源当前值(attacker, resourceKey);
                const 自身支付量 = Math.min(自身当前值, rawAmount);
                if (!(自身支付量 > 0)) return;
                const 目标获得量 = 计算资源锁定后获得量(targetObj, resourceKey, 计算资源转移正向收益(Math.max(1, Math.floor(自身支付量 * convertRatio))), '转化锁定');
                const 目标新值 = Math.min(targetMax, targetCurrent + 目标获得量);
                const 实际获得量 = Math.max(0, 目标新值 - targetCurrent);
                if (!(实际获得量 > 0)) return;
                const 目标抹消规则 = 读取资源变化抹消规则(targetObj, getTransferResourceLabel(resourceKey), { 消费: false });
                const 自身抹消规则 = 读取资源变化抹消规则(attacker, getTransferResourceLabel(resourceKey), { 消费: false });
                if (目标抹消规则 || 自身抹消规则) {
                  if (目标抹消规则) {
                    result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}对【${读取战斗机制抹消对象摘要(目标抹消规则.抹消对象)}】存在封锁，资源${转移方式}获得未能生效。`;
                  }
                  if (自身抹消规则) {
                    result.desc += ` [机制抹消] 自身对【${读取战斗机制抹消对象摘要(自身抹消规则.抹消对象)}】存在封锁，资源${转移方式}消耗未能生效。`;
                  }
                  return;
                }
                设置转移资源当前值(attacker, resourceKey, 自身当前值 - 自身支付量);
                设置转移资源当前值(targetObj, resourceKey, 目标新值);
                写入资源变化事件(attacker, resourceKey, -自身支付量, 转移方式 + '支付');
                写入资源变化事件(targetObj, resourceKey, 实际获得量, 转移方式 + '获得');
                totalChanged += 实际获得量 + 自身支付量;
                result.desc += ` [${转移方式}] 自身消耗 ${自身支付量} 点${getTransferResourceLabel(resourceKey)}，${targetObj === attacker ? '自身' : targetObj.name}获得 ${实际获得量} 点。`;
                return;
              }
              const drainAmount = Math.min(targetCurrent, rawAmount);
              if (!(drainAmount > 0)) return;
              if (资源变化落地被抹消(targetObj, resourceKey, `${转移方式}资源扣除`)) return;
              设置转移资源当前值(targetObj, resourceKey, targetCurrent - drainAmount);
              写入资源变化事件(targetObj, resourceKey, -drainAmount, 转移方式 + '资源扣除');
              totalChanged += drainAmount;
              if (转移方式 === '吞噬') 回收汇总.set(resourceKey, (回收汇总.get(resourceKey) || 0) + drainAmount);
              result.desc += ` [${转移方式}] ${targetObj === attacker ? '自身' : targetObj.name}损失 ${drainAmount} 点${getTransferResourceLabel(resourceKey)}。`;
            });
          });
          if (转移方式 === '吞噬') {
            回收汇总.forEach((amount, resourceKey) => {
              const recoverAmount = 计算资源锁定后获得量(attacker, resourceKey, 计算资源转移正向收益(Math.max(0, Math.floor(amount * convertRatio))), '转化锁定');
              if (!(recoverAmount > 0)) return;
              const current = 读取转移资源当前值(attacker, resourceKey);
              const maxValue = 读取转移资源上限值(attacker, resourceKey);
              const afterValue = Math.min(maxValue, current + recoverAmount);
              const actualGain = Math.max(0, afterValue - current);
              if (!(actualGain > 0)) return;
              if (资源变化落地被抹消(attacker, resourceKey, '吞噬资源回补')) return;
              设置转移资源当前值(attacker, resourceKey, afterValue);
              写入资源变化事件(attacker, resourceKey, actualGain, '吞噬资源回补');
              totalChanged += actualGain;
              result.desc += ` [吞噬] 自身回补 ${actualGain} 点${getTransferResourceLabel(resourceKey)}。`;
            });
          }
          return totalChanged;
        };

        const applyLifeLinkEffect = effect => {
          if (!effect) return false;
          const targetUnits = resolveDirectMechanismTargetList(effect);
          const prototypeRatio = Math.abs(读取战斗数值正负(effect?.数值));
          const shareRatio = Math.max(0, Math.min(1, Number(effect?.分摊比例 || effect?.share_ratio || prototypeRatio || 0.35)));
          const 分摊人数 = Math.max(1, Math.round(Number(effect?.分摊人数 || effect?.数量 || effect?.damage_share_count || 1)));
          let linked = false;
          targetUnits.forEach(targetObj => {
            if (!targetObj || targetObj === attacker) return;
            if (!targetObj.状态效果) targetObj.状态效果 = {};
            targetObj.状态效果['生命链接'] = {
              类型: 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]施加生命链接`,
              duration: Math.max(1, Number(effect?.持续回合 || 2)),
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果: {
                ...createEmptyCombatEffectMap(),
                damage_share_ratio: shareRatio,
                damage_share_count: 分摊人数,
              },
            };
            linked = true;
          });
          if (linked) result.desc += ` [生命链接] 已建立生命链接并同步承伤规则。`;
          return linked;
        };

        const 应用消耗分摊效果 = 效果 => {
          if (!效果) return false;
          const 目标单位列表 = resolveDirectMechanismTargetList(效果);
          const 原型比例 = Math.abs(读取战斗数值正负(效果?.数值));
          const 分摊比例 = Math.max(0, Math.min(1, Number(效果?.分摊比例 || 效果?.cost_share_ratio || 原型比例 || 0.35)));
          const 分摊人数 = Math.max(1, Math.round(Number(效果?.分摊人数 || 效果?.数量 || 效果?.cost_share_count || 1)));
          let applied = false;
          目标单位列表.forEach(targetObj => {
            if (!targetObj) return;
            if (!targetObj.状态效果) targetObj.状态效果 = {};
            targetObj.状态效果['消耗分摊'] = {
              类型: 'buff',
              层数: 1,
              描述: `由[${skillName || '技能'}]建立消耗分摊`,
              duration: Math.max(1, Number(效果?.持续回合 || 2)),
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果: {
                ...createEmptyCombatEffectMap(),
                cost_share_ratio: 分摊比例,
                cost_share_count: 分摊人数,
              },
            };
            applied = true;
          });
          if (applied) result.desc += ` [消耗分摊] 已建立消耗分摊链。`;
          return applied;
        };

        const applyResourceBurnEffect = effect => {
          if (!effect) return 0;
          const targetUnits = resolveDirectMechanismTargetList(effect);
          const burnRatio = Math.max(0, Number(effect?.每回合燃烧 || effect?.burn_ratio || 0.12));
          const resourceKeys = resolveTransferResourceKeys(effect?.资源 || '');
          let totalBurned = 0;
          targetUnits.forEach(targetObj => {
            resourceKeys.forEach(resourceKey => {
              if (resourceKey === 'hp') {
                result.desc += ` [资源变化阻断] 生命负数必须通过伤害结算处理。`;
                return;
              }
              const maxVal = 读取转移资源上限值(targetObj, resourceKey);
              const rawBurn = effect?.数值 ?? effect?.每回合燃烧 ?? effect?.burn_ratio ?? -burnRatio;
              const burnAmount = 计算资源变化量(rawBurn, maxVal);
              const beforeValue = 读取转移资源当前值(targetObj, resourceKey);
              const afterValue = Math.max(0, beforeValue - burnAmount);
              const actualBurn = Math.max(0, beforeValue - afterValue);
              if (!(actualBurn > 0)) return;
              const 资源变化抹消规则 = 读取资源变化抹消规则(targetObj, getTransferResourceLabel(resourceKey));
              if (资源变化抹消规则) {
                result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name}对【${读取战斗机制抹消对象摘要(资源变化抹消规则.抹消对象)}】存在封锁，资源扣除未能生效。`;
                return;
              }
              设置转移资源当前值(targetObj, resourceKey, afterValue);
              totalBurned += actualBurn;
              const label = Number(effect?.持续回合 || 0) > 0 ? '资源流失' : '资源泯灭';
              result.desc += ` [${label}] ${targetObj === attacker ? '自身' : targetObj.name}损失 ${actualBurn} 点${getTransferResourceLabel(resourceKey)}。`;
            });
          });
          return totalBurned;
        };

        const applyResourceChangePulseEffect = effect => {
          if (!effect) return 0;
          const 变化值 = 读取战斗数值正负(effect?.数值);
          if (!(变化值 !== 0)) return 0;
          if (变化值 < 0) return applyResourceBurnEffect(effect);
          const 资源文本 = String(effect?.资源 || '').trim();
          if (/生命|HP|hp/i.test(资源文本)) return applyImmediateRecoveryEffect(effect, 'hp', '生命');
          if (/体力|vit|sta/i.test(资源文本)) return applyImmediateRecoveryEffect(effect, 'vit', '体力');
          if (/精神|men/i.test(资源文本)) return applyImmediateRecoveryEffect(effect, 'men', '精神力');
          return applyImmediateRecoveryEffect(effect, 'sp', '魂力');
        };

        const applyResourceLockEffect = effect => {
          if (!effect) return false;
          const targetUnits = resolveDirectMechanismTargetList(effect);
          const compiled = 编译原型战斗效果(effect) || effect || {};
          const lockRatio = Math.max(0, Math.min(1, Math.abs(读取战斗数值正负(compiled?.数值 ?? effect?.数值))));
          const lockType = String(effect?.锁定类型 || '').trim();
          if (!lockRatio || !['资源池锁定', '回复锁定', '转化锁定'].includes(lockType)) return false;
          const resourceKeys = resolveTransferResourceKeys(effect?.资源 || '');
          const resourceLabels = resourceKeys.map(getTransferResourceLabel);
          let applied = false;
          targetUnits.forEach(targetObj => {
            if (!targetObj?.状态效果) targetObj.状态效果 = {};
            const 规则 = { 资源: resourceLabels, 锁定类型: lockType, 比例: lockRatio };
            targetObj.状态效果['资源锁定'] = {
              类型: 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]施加${resourceLabels.join('/')} ${lockType}`,
              duration: Math.max(1, Number(effect?.持续回合 || 2)),
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果: createEmptyCombatEffectMap(),
              资源锁定规则: [规则],
            };
            if (lockType === '资源池锁定') {
              resourceKeys.forEach(resourceKey => {
                const maxValue = 读取转移资源上限值(targetObj, resourceKey);
                const current = 读取转移资源当前值(targetObj, resourceKey);
                const availableCap = Math.max(0, Math.floor(maxValue * (1 - lockRatio)));
                if (current > availableCap && !资源变化落地被抹消(targetObj, resourceKey, '资源池锁定扣减')) 设置转移资源当前值(targetObj, resourceKey, availableCap);
              });
            }
            targetObj.__禁用本回合自然恢复 = true;
            applied = true;
          });
          if (applied) result.desc += ` [资源锁定] 目标${resourceLabels.join('/')}的${lockType}已生效。`;
          return applied;
        };

        const applyCostIncreaseEffect = effect => {
          if (!effect) return false;
          const targetUnits = resolveDirectMechanismTargetList(effect);
          const compiled = 编译原型战斗效果(effect) || effect || {};
          const costRatio = Number(compiled?.计算层效果?.cost_ratio || effect?.cost_ratio || 1);
          const costDelta = Number(compiled?.计算层效果?.cost_delta ?? effect?.cost_delta ?? 0);
          const costDeltaRatio = Number(compiled?.计算层效果?.cost_delta_ratio ?? effect?.cost_delta_ratio ?? 0);
          let applied = false;
          targetUnits.forEach(targetObj => {
            if (!targetObj?.状态效果) targetObj.状态效果 = {};
            targetObj.状态效果['消耗'] = {
              类型: 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]修正消耗`,
              duration: Math.max(1, Number(effect?.持续回合 || 2)),
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果: {
                ...createEmptyCombatEffectMap(),
                cost_ratio: Number.isFinite(costRatio) && costRatio > 0 ? costRatio : 1,
                cost_delta: Number.isFinite(costDelta) ? costDelta : 0,
                cost_delta_ratio: Number.isFinite(costDeltaRatio) ? costDeltaRatio : 0,
              },
            };
            applied = true;
          });
          if (applied) result.desc += ` [消耗] 技能消耗已修正。`;
          return applied;
        };

        const applyMechanismSuppressEffect = effect => {
          if (!effect) return false;
          const targetUnits = resolveSkillEffectTargetCharacters(playerAction.skill, effect, attacker, defender, combatData);
          const 抹消规则 = {
            抹消对象: 规范化战斗机制抹消对象(effect?.抹消对象),
            持续回合: Math.max(1, Number(effect?.持续回合 || 1)),
          };
          if (!targetUnits.length || !String(抹消规则.抹消对象?.原型 || '').trim()) return false;
          let applied = false;
          targetUnits.forEach(targetObj => {
            if (!targetObj) return;
            const 最终结果抹消规则 = 战斗机制抹消命中(targetObj, '最终结果', { 原型: '机制抹消' }, { 用途: '封锁' });
            if (最终结果抹消规则) {
              result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，本次机制抹消未能落地。`;
              return;
            }
            const stateKey = putConditionWithUniqueKey(targetObj, `机制抹消:${读取战斗机制抹消对象摘要(抹消规则.抹消对象)}`, {
              类型: 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]抹消机制`,
              duration: Math.max(1, Number(effect?.持续回合 || 1)),
              来源原型摘要: '机制抹消',
              抹消规则: [deepClonePlain(抹消规则)],
              战斗效果: { ...createEmptyCombatEffectMap(), mechanism_suppress: true },
            });
            const removed = 移除战斗机制抹消命中节点(targetObj, '最终结果', 抹消规则.抹消对象, { excludeKeys: [stateKey] });
            const removedRuntime = 移除战斗机制抹消命中运行态(combatData, '最终结果', 抹消规则.抹消对象, targetObj);
            result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}被施加【${读取战斗机制抹消对象摘要(抹消规则.抹消对象)}】机制抹消。`;
            if (removed.length) result.desc += ` 已移除[${removed.join('/')}]。`;
            if (removedRuntime.length) result.desc += ` 已移除运行机制[${removedRuntime.join('/')}]。`;
            applied = true;
          });
          return applied;
        };

        const applyMovementEffect = effect => {
          if (!effect) return false;
          const 位移类型 = String(effect?.位移类型 || '').trim();
          const 位移对象 = String(effect?.位移对象 || '').trim();
          const 目标文本 = String(effect?.目标 || '').trim();
          const 位移幅度 = Math.max(1, Math.min(30, Number(effect?.距离 || 1)));
          const 位移强度 = Math.max(0.04, Math.min(0.45, Math.abs(读取战斗数值正负(effect?.数值 || 0)) || 位移幅度 * 0.04));
          const 位移成功率 = Math.max(0, Math.min(1, Number(effect?.位移成功率 ?? 1)));
          const 持续回合 = Math.max(1, Math.min(10, Math.round(Number(effect?.持续回合 || 1))));
          const 近身反击风险 = Math.max(0, Math.min(0.3, (6 - 位移幅度) * 0.05));
          const 构建友方位移效果 = (效果强度 = 位移强度) => ({
            ...createEmptyCombatEffectMap(),
            dodge_bonus: 效果强度,
            hit_bonus: 效果强度,
          });
          const 构建敌向位移效果 = (效果强度 = 位移强度) => {
            const 战斗效果 = createEmptyCombatEffectMap();
            if (位移类型 === '拉近') {
              战斗效果.hit_bonus = 效果强度;
              战斗效果.lock_level = 效果强度 >= 0.16 ? 1 : 0;
              战斗效果.reaction_penalty = 效果强度;
              战斗效果.dodge_penalty = Math.max(0.04, 效果强度 * 0.5);
            } else if (位移类型 === '击退') {
              战斗效果.reaction_penalty = 效果强度;
              战斗效果.dodge_penalty = Math.max(0.04, 效果强度 * 0.5);
              战斗效果.hit_penalty = Math.max(0.04, 效果强度 * 0.5);
            } else if (位移类型 === '换位') {
              战斗效果.random_target_rate = 效果强度;
              战斗效果.reaction_penalty = 效果强度;
              战斗效果.dodge_penalty = Math.max(0.04, 效果强度 * 0.5);
            }
            return 战斗效果;
          };
          const 是友方承载目标 = 目标对象 => 目标对象 === attacker || ['自身', '友方', '友方单体', '友方群体', '召唤物', '分身'].includes(目标文本);
          const 存在有效战斗效果 = 战斗效果 => Object.entries(战斗效果 || {}).some(([字段名, 字段值]) => {
            if (typeof 字段值 === 'boolean') return 字段值;
            if (['final_damage_mult', 'received_damage_mult', 'final_heal_mult', 'shield_gain_mult', 'cost_ratio', 'windup_ratio', 'skill_effect_mult'].includes(字段名)) return Number(字段值 || 1) !== 1;
            if (['cost_delta', 'windup_delta'].includes(字段名)) return Number(字段值 || 0) !== 0;
            return Number(字段值 || 0) !== 0;
          });
          if (!['拉近', '击退', '换位', '瞬移', '脱离'].includes(位移类型)) return false;
          const 施术者位移受限强度 = Math.max(
            0,
            ...Object.values(attacker?.状态效果 || {}).map(状态 => Number(状态?.战斗效果?.lock_level || 0)),
          );
          const 施术者位移受限 = 施术者位移受限强度 > 0;
          const 施术者位移压制率 = 施术者位移受限
            ? Math.min(0.45, Math.max(0.18, 0.18 + (施术者位移受限强度 - 1) * 0.08))
            : 0;
          const 位移效果受限 = 施术者位移受限 && ['自身', '目标', '自身与目标'].includes(位移对象);
          const 有效位移强度 = 位移效果受限
            ? Math.max(0.02, Number((位移强度 * (1 - 施术者位移压制率)).toFixed(4)))
            : 位移强度;
          const 位移压制文本 = 位移效果受限 ? `，效果压低${Math.round(施术者位移压制率 * 100)}%` : '';
          const 受限近身反击风险 = 位移效果受限
            ? Math.max(近身反击风险, Math.min(0.3, 近身反击风险 + 施术者位移压制率 * 0.12))
            : 近身反击风险;
          const 目标列表 = resolveSkillEffectTargetCharacters(playerAction.skill, effect, attacker, defender, combatData);
          let 已生效 = false;
          if (位移对象 === '自身') {
            const 参照目标 = 目标列表.find(目标对象 => 目标对象 && 目标对象 !== attacker);
            const 战斗效果 = 构建友方位移效果(有效位移强度);
            putConditionWithUniqueKey(attacker, `位移:${位移类型}:自身`, {
              类型: 'buff',
              层数: 1,
              描述: `由[${skillName || '技能'}]形成${位移类型}收益${参照目标 ? `，参照${参照目标.name || '目标'}` : ''}${位移压制文本}`,
              duration: 持续回合,
              战斗效果: { ...战斗效果 },
            });
            if (参照目标 && 受限近身反击风险 > 0) {
              putConditionWithUniqueKey(参照目标, `位移:${位移类型}:反击窗口`, {
                类型: 'buff',
                层数: 1,
                描述: `由[${skillName || '技能'}]近身形成反击窗口${位移压制文本}`,
                duration: 持续回合,
                战斗效果: { ...createEmptyCombatEffectMap(), counter_attack_ratio: 受限近身反击风险 },
              });
            }
            if (是主原型效果(effect)) 登记主原型成立目标(attacker);
            result.desc += ` [位移执行] ${位移类型}转化为闪避/反应收益${位移压制文本}${受限近身反击风险 > 0 ? '，并带出反击风险' : ''}。`;
            return true;
          }
          if (位移类型 === '换位' || 位移对象 === '自身与目标') {
            const 交换目标 = 目标列表.find(目标对象 => 目标对象 && 目标对象 !== attacker);
            if (!交换目标) {
              result.desc += ` [位移执行] 换位缺少自身以外目标，未生效。`;
              return false;
            }
            if (String(effect?.影响方向 || '').trim() === '成功率' && 位移成功率 < 1 && Math.random() > 位移成功率) {
              result.desc += ` [位移执行] 换位扰动未成立。`;
              return false;
            }
            putConditionWithUniqueKey(attacker, `位移:换位`, {
              类型: 'buff',
              层数: 1,
              描述: `由[${skillName || '技能'}]扰乱与${交换目标.name || '目标'}的战斗节奏${位移压制文本}`,
              duration: 持续回合,
              战斗效果: 构建友方位移效果(有效位移强度),
            });
            putConditionWithUniqueKey(交换目标, `位移:换位`, {
              类型: 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]被${attacker.name || '施术者'}扰乱战斗节奏${位移压制文本}`,
              duration: 持续回合,
              战斗效果: 构建敌向位移效果(有效位移强度),
            });
            if (是主原型效果(effect)) {
              登记主原型成立目标(attacker);
              登记主原型成立目标(交换目标);
            }
            result.desc += ` [位移执行] 换位转化为双方反应/闪避变化${位移压制文本}。`;
            return true;
          }
          目标列表.forEach(目标对象 => {
            if (!目标对象) return;
            if (位移对象 === '目标' && String(effect?.影响方向 || '').trim() === '成功率' && 位移成功率 < 1 && Math.random() > 位移成功率) return;
            const 友方目标 = 是友方承载目标(目标对象);
            const 战斗效果 = 友方目标 || ['瞬移', '脱离'].includes(位移类型)
              ? 构建友方位移效果(有效位移强度)
              : 构建敌向位移效果(有效位移强度);
            if (!存在有效战斗效果(战斗效果)) return;
            putConditionWithUniqueKey(目标对象, `位移:${位移类型}`, {
              类型: 友方目标 ? 'buff' : 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]对${友方目标 ? '友方' : '目标'}形成${位移类型}战斗折算${位移压制文本}`,
              duration: 持续回合,
              战斗效果: { ...战斗效果 },
            });
            if (是主原型效果(effect)) 登记主原型成立目标(目标对象);
            已生效 = true;
          });
          if (已生效) result.desc += ` [位移执行] ${位移类型}按移动对象转化为${['自身', '友方', '友方单体', '友方群体', '召唤物', '分身'].includes(目标文本) ? '闪避/反应收益' : '反应/闪避压制'}${位移压制文本}。`;
          return 已生效;
        };

        let applyCopyEffect = effect => {
          void effect;
          return false;
        };

        const applyDelayBurstEffect = effect => {
          if (!effect || !(Number(effect?.延迟回合 || 0) > 0)) return false;
          const targetUnits = resolveDirectMechanismTargetList(effect);
          if (!targetUnits.length) return false;
          const 延迟效果 = deepClone(effect);
          delete 延迟效果.延迟回合;
          targetUnits.forEach(targetObj => {
            if (!targetObj) return;
            const 状态键 = putConditionWithUniqueKey(targetObj, `延迟效果:${skillName || '技能'}`, {
              类型: targetObj === attacker ? 'buff' : 'debuff',
              层数: 1,
              描述: `由[${skillName || '技能'}]登记的延迟效果`,
              duration: Math.max(1, Number(effect?.延迟回合 || 1)),
              延迟效果: true,
              结算效果: [延迟效果],
              面板修改比例: { str: 1, def: 1, agi: 1, sp_max: 1 },
              战斗效果: { ...createEmptyCombatEffectMap() },
            });
            result.desc += ` [延迟登记] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}登记[${状态键}]，${Math.max(1, Number(effect?.延迟回合 || 1))}回合后结算。`;
          });
          return true;
        };

        const directMechanismConsumerMap = {
          delay_burst: applyDelayBurstEffect,
          status_transfer: applyStatusTransferEffect,
          dot_detonate: applyDotDetonateEffect,
          shield_break: applyShieldBreakEffect,
          resource_drain: applyResourceTransferEffect,
          resource_refeed: applyResourceTransferEffect,
          ring_burst_gain: applyRingBurstEffect,
          mechanism_suppress: applyMechanismSuppressEffect,
          copy: effect => applyCopyEffect(effect),
          summon: applySummonEffect,
          damage_share: applyLifeLinkEffect,
          cost_share: 应用消耗分摊效果,
          cost_increase: applyCostIncreaseEffect,
        };

        const hasDirectDamageEffect = Number(pClash.威力倍率 || 0) > 0;
        const 已直接消费效果集合 = new Set();
        const runDirectMechanismConsumer = effect => {
          if (!effect) return false;
          if (已直接消费效果集合.has(effect)) return false;
          已直接消费效果集合.add(effect);
          const 正向资源变化 = effect?.原型 === '资源变化' && 读取战斗数值正负(effect?.数值) > 0;
          if (!正向资源变化 && 源动作被机制抹消(effect)) return false;
          const 写入状态生命周期事实 = (eventKind, targetObj, payload = {}) => {
            const stateName = String(payload.stateName || payload.状态名 || '').trim();
            if (!stateName) return null;
            return 写入战斗事件账本(combatData, {
              eventKind,
              round: Number(combatData?.回合 || 0),
              actorName: attacker?.name || attacker?.名称 || '',
              targetName: targetObj?.name || targetObj?.名称 || '',
              actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '',
              actionType: eventKind,
              sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '',
              sourceRound: Number(combatData?.回合 || 0),
              result: String(payload.result || eventKind).trim(),
              primaryOutcome: String(payload.primaryOutcome || eventKind).trim(),
              duration: Math.max(0, Number(payload.duration || 0)),
              calculationTrace: [
                ['stateName', stateName],
                ['stackMode', String(payload.stackMode || '').trim()],
                ['previousDuration', Math.max(0, Number(payload.previousDuration || 0))],
                ['nextDuration', Math.max(0, Number(payload.nextDuration || payload.duration || 0))],
              ],
              meta: {
                stateName,
                previousDuration: Math.max(0, Number(payload.previousDuration || 0)),
                nextDuration: Math.max(0, Number(payload.nextDuration || payload.duration || 0)),
                calculationTrace: [
                  ['stateName', stateName],
                  ['stackMode', String(payload.stackMode || '').trim()],
                  ['previousDuration', Math.max(0, Number(payload.previousDuration || 0))],
                  ['nextDuration', Math.max(0, Number(payload.nextDuration || payload.duration || 0))],
                ],
                stackMode: String(payload.stackMode || '').trim(),
                replaceReason: String(payload.replaceReason || '').trim(),
                removedStates: Array.isArray(payload.removedStates) ? payload.removedStates.map(item => String(item || '').trim()).filter(Boolean) : undefined,
              },
            });
          };
          const applyStandaloneStateEffect = stateEffect => {
            const 状态名 = String(stateEffect?.状态 || '').trim();
            if (!状态名) return false;
            const 状态目标文本 = String(stateEffect?.目标 || '').trim();
            const 状态目标友方 =
              状态目标文本 === '自身' ||
              状态目标文本 === '友方' ||
              状态目标文本 === '友方单体' ||
              状态目标文本 === '友方群体' ||
              状态目标文本 === '召唤物' ||
              状态目标文本 === '分身';
            const 状态视为增益 =
              状态目标友方 ||
              String(stateEffect?.特殊机制标识 || '无').includes('增益') ||
              stateEffect?.计算层效果?.无视异常 === true ||
              stateEffect?.计算层效果?.super_armor === true ||
              stateEffect?.计算层效果?.探查屏蔽 === true ||
              Number(stateEffect?.计算层效果?.min_hp_floor || 0) > 0 ||
              Number(stateEffect?.计算层效果?.hot_heal_ratio || 0) > 0;
            const targets = resolveSkillEffectTargetCharacters(
              playerAction.skill,
              stateEffect,
              attacker,
              defender,
              combatData,
            );
            const 可落地目标 = 过滤被时光回溯规避目标列表(targets).filter(targetObj => {
              if (!targetObj) return false;
              const 目标名 = String(targetObj?.name || targetObj?.名称 || '').trim();
              if (!目标名) return false;
              if (!hasDirectDamageEffect || 状态视为增益) return true;
              if (目标本次明确规避主效果(targetObj)) return false;
              return 主原型成立目标集合.has(目标名) || 主原型命中目标集合.has(目标名);
            });
            if (!可落地目标.length) return false;
            可落地目标.forEach(targetObj => {
              if (!targetObj) return;
              if (!targetObj.状态效果) targetObj.状态效果 = {};
              const friendly =
                targetObj === attacker ||
                ['自身', '友方', '友方单体', '友方群体', '召唤物', '分身'].includes(String(stateEffect?.目标 || '').trim());
              if (!friendly && 探查屏蔽阻断敌对锁定状态(targetObj, stateEffect)) {
                result.desc += ` [探查屏蔽] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}屏蔽了[${状态名}]锁定。`;
                return;
              }
              if (!friendly && 无视异常阻断负面或削减(targetObj, {
                原型: '状态施加',
                状态: 状态名,
                状态名称: 状态名,
                类型: 'debuff',
                计算层效果: stateEffect?.计算层效果 || {},
              })) {
                result.desc += ` [无视异常] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}免疫了[${状态名}]。`;
                return;
              }
              const 最终结果抹消规则 = 战斗机制抹消命中(targetObj, '最终结果', {
                原型: '状态施加',
                状态: 状态名,
                状态名称: 状态名,
                战斗效果: stateEffect?.计算层效果 || {},
              }, { 用途: '封锁' });
              if (最终结果抹消规则) {
                result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，[${状态名}]未能落地。`;
                return;
              }
              const 状态成功率审计 = 计算状态施加成功率审计(stateEffect, attacker, targetObj);
              const 驱动成功率 = 状态成功率审计.successRate;
              const 状态附着检定 = 驱动成功率 < 1 ? Math.random() : null;
              if (驱动成功率 < 1 && 状态附着检定 > 驱动成功率) {
                result.desc += ` [状态施加] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}抵住了[${状态名}]附着。`;
                写入战斗事件账本(combatData, {
                  eventKind: 'state_apply',
                  round: Number(combatData?.回合 || 0),
                  actorName: attacker?.name || attacker?.名称 || '',
                  targetName: targetObj?.name || targetObj?.名称 || '',
                  actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                  actionType: playerAction?.action_type || playerAction?.type || '',
                  sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                  sourceRound: Number(combatData?.回合 || 0),
                  result: 'resisted',
                  duration: Math.max(0, Number(stateEffect?.持续回合 || 0)),
                  driverAttr: String(读取状态施加默认驱动属性_战斗(stateEffect) || '').trim(),
                  meta: {
                    stateName: 状态名,
                    successRate: Number(驱动成功率 || 0),
                    roll: Number(状态附着检定 || 0),
                    successRateReason: 状态成功率审计.reason,
                    successRateBreakdown: 格式化状态附着成功率拆分(状态成功率审计, 状态附着检定, false),
                    reason: '状态附着抗性判定失败',
                  },
                });
                return;
              }
              const targetMaxHp = getCombatHpMaxValue(targetObj);
              const 状态护盾数值 = Number(stateEffect?.计算层效果?.shield_gain_bonus || 0);
              const 状态护盾值 = 状态护盾数值 > 0
                ? (/^[-+]?\d+(?:\.\d+)?%$/.test(String(stateEffect?.数值 ?? stateEffect?.护盾值 ?? '').trim()) || 状态护盾数值 <= 1
                  ? Math.max(1, Math.floor(targetMaxHp * 状态护盾数值))
                  : Math.floor(状态护盾数值))
                : 0;
              const 状态战斗效果 = { ...createEmptyCombatEffectMap(), ...(stateEffect?.计算层效果 || {}) };
              if (状态护盾值 > 0) 状态战斗效果.shield_gain_bonus = 0;
              if (String(stateEffect?.触发方式 || '').trim() === '遥控触发') {
                const 状态键 = 登记遥控状态效果(
                  targetObj,
                  { ...stateEffect, __施术者: attacker },
                  playerAction.skill?.name || playerAction.skill?.魂技名 || '技能',
                );
                if (状态键) {
                  if (是主原型效果(stateEffect)) 登记主原型成立目标(targetObj);
                  result.desc += ` [遥控登记] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}预置[${状态键}]。`;
                }
                return;
              }
              const oldState = targetObj.状态效果[状态名] && typeof targetObj.状态效果[状态名] === 'object'
                ? targetObj.状态效果[状态名]
                : null;
              if (
                oldState &&
                oldState.__本回合新附加 === true &&
                String(oldState.来源技能 || '').trim() === String(playerAction.skill?.name || playerAction.skill?.魂技名 || '').trim() &&
                String(oldState.来源角色 || '').trim() === String(attacker?.name || attacker?.名称 || '').trim()
              ) {
                return;
              }
              const 下一持续 = Math.max(Number(stateEffect?.持续回合 || 0), Number(oldState?.duration || 0));
              const 新状态条目 = {
                类型: friendly ? 'buff' : 'debuff',
                层数: 1,
                来源技能: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                来源角色: attacker?.name || attacker?.名称 || '',
                来源原型摘要: '状态施加',
                描述: `由[${playerAction.skill?.name || '技能'}]附加`,
                duration: 下一持续,
                驱动属性: String(读取状态施加默认驱动属性_战斗(stateEffect) || '').trim(),
                效果摘要: 构建状态结果效果摘要(状态战斗效果),
                shield_value: 状态护盾值,
                面板修改比例: { ...(stateEffect?.面板修改比例 || {}) },
                战斗效果: 状态战斗效果,
              };
              const 元素承伤修正 = 构建状态元素承伤修正(stateEffect);
              if (元素承伤修正) 新状态条目.元素承伤修正 = 元素承伤修正;
              const 持续移除命中 = 持续状态移除阻断状态附着(targetObj, 状态名, 新状态条目, attacker);
              if (持续移除命中) {
                result.desc += ` [持续状态移除] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}的[${状态名}]被[${持续移除命中.key}]拦截。`;
                return;
              }
              if (oldState) {
                const 旧持续 = Math.max(0, Number(oldState.duration || oldState.持续回合 || 0));
                const 新持续 = Math.max(0, Number(下一持续 || 0));
                const 旧强度 = Number(oldState.状态位阶 ?? oldState.位阶 ?? oldState.强度 ?? oldState.层数 ?? 0);
                const 新强度 = Number(stateEffect.状态位阶 ?? stateEffect.位阶 ?? stateEffect.强度 ?? 0);
                const 是高阶覆盖 = Number.isFinite(新强度) && Number.isFinite(旧强度) && 新强度 > 旧强度;
                写入状态生命周期事实('state_replace', targetObj, {
                  stateName: 状态名,
                  result: 是高阶覆盖 ? 'replace' : 'refresh',
                  primaryOutcome: 是高阶覆盖 ? 'state_replace' : 'state_refresh',
                  previousDuration: 旧持续,
                  nextDuration: 新持续,
                  duration: 新持续,
                  stackMode: 是高阶覆盖 ? 'replace' : 'refresh_duration',
                  replaceReason: 是高阶覆盖 ? 'higher_tier_replaced_lower_tier' : 'same_state_refresh_duration',
                });
              }
              targetObj.状态效果[状态名] = 新状态条目;
              targetObj.final = buildCombatFinalStats(targetObj);
              if (targetObj.召唤键) 同步召唤单位镜像(targetObj);
              新状态条目.__状态来源键 = 记录状态来源登记(combatData, {
                stateName: 状态名,
                targetName: targetObj?.name || targetObj?.名称 || '',
                sourceActorName: attacker?.name || attacker?.名称 || '',
                sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                sourceActionType: playerAction?.action_type || playerAction?.type || '',
                sourceRound: Number(combatData?.回合 || 0),
                duration: 下一持续,
                effectSummary: 构建状态结果效果摘要(新状态条目.战斗效果 || {}),
                driverAttr: String(读取状态施加默认驱动属性_战斗(stateEffect) || '').trim(),
              });
              写入战斗事件账本(combatData, {
                eventKind: 'state_apply',
                round: Number(combatData?.回合 || 0),
                actorName: attacker?.name || attacker?.名称 || '',
                targetName: targetObj?.name || targetObj?.名称 || '',
                actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                actionType: playerAction?.action_type || playerAction?.type || '',
                sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                sourceRound: Number(combatData?.回合 || 0),
                result: 'applied',
                applicationId: 新状态条目.__状态来源键,
                duration: 下一持续,
                effectSummary: 构建状态结果效果摘要(新状态条目.战斗效果 || {}),
                driverAttr: String(读取状态施加默认驱动属性_战斗(stateEffect) || '').trim(),
                meta: {
                  stateName: 状态名,
                  successRate: Number(驱动成功率 || 1),
                  roll: Number(状态附着检定 || 0),
                  successRateReason: 状态成功率审计.reason,
                  successRateBreakdown: 格式化状态附着成功率拆分(状态成功率审计, 状态附着检定, true),
                },
              });
              if (是主原型效果(stateEffect)) 登记主原型成立目标(targetObj);
              result.desc += ` [状态施加] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}获得[${状态名}]。`;
            });
            return true;
          };
          const applyStandaloneStatusRemoveEffect = removeEffect => {
            const targets = resolveSkillEffectTargetCharacters(
              playerAction.skill,
              removeEffect,
              attacker,
              defender,
              combatData,
            );
            const 可落地目标 = 过滤被时光回溯规避目标列表(targets);
          if (!可落地目标.length) return false;
          let removedAny = false;
          可落地目标.forEach(targetObj => {
              const removed = removeConditionsByPrototypeFilter(targetObj, removeEffect, attacker);
              if (removed.length > 0) {
                removedAny = true;
                removed.forEach(stateName => 写入状态生命周期事实('state_remove', targetObj, {
                  stateName,
                  result: 'removed',
                  primaryOutcome: 'state_remove',
                  removedStates: removed,
                  stackMode: 'cleanse_or_dispel',
                  replaceReason: 'state_remove_effect',
                }));
                if (是主原型效果(removeEffect)) 登记主原型成立目标(targetObj);
                result.desc += ` [状态移除] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}移除了[${removed.join('/')}].`;
              } else if (removed.__概率失败数 > 0) {
                result.desc += ` [状态移除] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}命中匹配状态，但驱动判定失败。`;
              } else {
                result.desc += ` [状态移除] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}没有匹配状态。`;
              }
            });
            登记持续原型运行壳(removeEffect, 可落地目标);
            return removedAny;
          };
          if (Number(effect?.延迟回合 || 0) > 0 && 战斗原型允许延迟回合(effect?.原型)) return applyDelayBurstEffect(effect);
          if (effect?.原型 === '状态施加') return applyStandaloneStateEffect(effect);
          if (effect?.原型 === '状态移除') return applyStandaloneStatusRemoveEffect(effect);
          const 执行并登记主原型目标 = 执行函数 => {
            const 生效量 = 执行函数();
            if (是主原型效果(effect) && (生效量 === true || Number(生效量 || 0) > 0)) {
              过滤被时光回溯规避目标列表(resolveSkillEffectTargetCharacters(
                playerAction.skill,
                effect,
                attacker,
                defender,
                combatData,
              )).forEach(登记主原型成立目标);
            }
            return 生效量;
          };
          if (effect?.原型 === '资源转移') return 执行并登记主原型目标(() => {
            const 生效量 = applyResourceTransferEffect(effect);
            登记持续原型运行壳(effect);
            return 生效量;
          });
          if (effect?.原型 === '资源变化' && Number(effect?.持续回合 || 0) > 0) return 执行并登记主原型目标(() => {
            const 持续数量 = applyResourceDurationEffect(effect);
            const 脉冲数量 = applyResourceChangePulseEffect(effect);
            return Number(持续数量 || 0) + Number(脉冲数量 || 0);
          });
          if (effect?.原型 === '资源变化' && 读取战斗数值正负(effect?.数值) < 0) return 执行并登记主原型目标(() => applyResourceBurnEffect(effect));
          if (effect?.原型 === '护盾变化' && 读取战斗数值正负(effect?.数值) < 0) return 执行并登记主原型目标(() => applyShieldBreakEffect(effect));
          if (effect?.原型 === '结算修正' && String(effect?.结算 || '').trim() === '持续伤害引爆') return 执行并登记主原型目标(() => applyDotDetonateEffect(effect));
          if (effect?.原型 === '结算修正' && String(effect?.结算 || '').trim() === '伤害分摊') return 执行并登记主原型目标(() => applyLifeLinkEffect(effect));
          if (effect?.原型 === '结算修正' && String(effect?.结算 || '').trim() === '消耗分摊') return 执行并登记主原型目标(() => 应用消耗分摊效果(effect));
          if (effect?.原型 === '状态转移') return 执行并登记主原型目标(() => {
            const 生效 = applyStatusTransferEffect(effect);
            登记持续原型运行壳(effect);
            return 生效;
          });
          if (effect?.原型 === '机制抹消') return 执行并登记主原型目标(() => applyMechanismSuppressEffect(effect));
          if (effect?.原型 === '位移执行') return applyMovementEffect(effect);
          if (effect?.原型 === '资源锁定') return applyResourceLockEffect(effect);
          if (effect?.原型 === '资源变化' && 读取战斗数值正负(effect?.数值) > 0) {
            const 资源文本 = String(effect?.资源 || '').trim();
            if (/生命|HP|hp/i.test(资源文本)) return applyImmediateRecoveryEffect(effect, 'hp', '生命');
            if (/体力|vit|sta/i.test(资源文本)) return applyImmediateRecoveryEffect(effect, 'vit', '体力');
            if (/精神|men/i.test(资源文本)) return applyImmediateRecoveryEffect(effect, 'men', '精神力');
            return applyImmediateRecoveryEffect(effect, 'sp', '魂力');
          }
          if (effect?.原型 === '时窗修正') return applyDotDurationAdjustEffect(effect);
          if (effect?.原型 === '状态施加' && String(effect?.状态 || '').trim() === '资源燃烧') return false;
          if (effect?.原型 === '召唤生成') return 执行并登记主原型目标(() => applySummonEffect(effect));
          const mechanism = String(effect?.原型 || '').trim();
          if (mechanism === '生命链接') return 执行并登记主原型目标(() => applyLifeLinkEffect(effect));
          const 运行时消费器 = String(读取战斗原型运行消费器(effect) || '').trim();
          const consumer = 运行时消费器 ? directMechanismConsumerMap[运行时消费器] : null;
          return consumer ? 执行并登记主原型目标(() => consumer(effect)) : false;
        };

        const 执行临时行动直接效果 = (行动者, 目标者, 行动, 效果列表 = []) => {
          const 可执行效果 = (Array.isArray(效果列表) ? 效果列表 : []).filter(effect => effect && typeof effect === 'object');
          if (!可执行效果.length) return false;
          const 原攻击者 = attacker;
          const 原防御者 = defender;
          const 原玩家行动 = playerAction;
          const 原攻击者最终属性 = attackerFinalStat;
          const 原防御者最终属性 = defenderFinalStat;
          attacker = 行动者;
          defender = 目标者;
          playerAction = 行动 || { type: '技能', action_type: '技能', skill: { _效果数组: 可执行效果 } };
          attackerFinalStat = 行动者?.final || 行动者 || {};
          defenderFinalStat = 目标者?.final || 目标者 || {};
          let 已生效 = false;
          try {
            可执行效果.forEach(effect => {
              if (runDirectMechanismConsumer(effect)) 已生效 = true;
            });
          } finally {
            attacker = 原攻击者;
            defender = 原防御者;
            playerAction = 原玩家行动;
            attackerFinalStat = 原攻击者最终属性;
            defenderFinalStat = 原防御者最终属性;
          }
          return 已生效;
        };

        applyCopyEffect = effect => {
          if (!effect) return false;
          const 复制目标上下文 = resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData, effect);
          const sourceObj = 复制目标上下文.primaryTarget || resolveSkillEffectTargetCharacter(playerAction.skill, effect, attacker, defender);
          const 目标友方集合 = 复制目标上下文.friendlySet || [];
          const 目标敌方集合 = 复制目标上下文.hostileSet || [];
          const 复制类型文本 = String(effect?.复制类型 || '复制技能').trim();
          const 复制类型 = /全部/.test(复制类型文本) ? '复制全部' : /属性/.test(复制类型文本) ? '复制属性' : '复制技能';
          const 事件链 = 读取事件链状态(playerAction || playerAction?.skill || {});
          if (/技能|全部/.test(复制类型)) {
            if (事件链.复制深度 >= 2) {
              result.desc += ` [复制截断] 复制链深度已达上限。`;
              return false;
            }
            事件链.复制深度 += 1;
          }
          const 复制模式 = ['即时镜像', '复刻友方'].includes(String(effect?.复制模式 || '即时镜像').trim())
            ? String(effect?.复制模式 || '即时镜像').trim()
            : '即时镜像';
          if (复制模式 === '复刻友方') {
            result.desc += ` [复制等待] 复刻友方需要在战斗外经剧情裁定。`;
            return false;
          }
          const 可用次数 = /技能|全部/.test(复制类型) ? Math.max(1, Math.floor(Number(effect?.可用次数 || 1))) : undefined;
          const 复制次数 = /技能|全部/.test(复制类型) ? Math.max(1, Math.floor(Number(effect?.保存上限 || 1))) : 99;
          const 复制保留回合 = Math.max(1, Math.floor(Number(effect?.保留回合 || effect?.持续回合 || 1)));
          if (!sourceObj) {
            result.desc += ` [复制失败] 没有可复制对象。`;
            return false;
          }
          const 来源名称 = sourceObj?.name || '目标';
          const 来源等级 = Number(sourceObj?.魂环年限 || sourceObj?.年限 || sourceObj?.等级 || sourceObj?.level || 0);
          const 自身等级 = Number(attacker?.魂环年限 || attacker?.年限 || attacker?.等级 || attacker?.level || 0);
          const 等级差 = Math.max(0, 来源等级 - 自身等级);
          const 读取削减比例 = value => {
            const text = String(value ?? '').trim();
            const n = Number(text.replace('%', ''));
            if (!Number.isFinite(n)) return 0.2;
            return Math.max(0, Math.min(0.95, text.includes('%') || n > 1 ? n / 100 : n));
          };
          const 削减比例 = /属性|全部/.test(复制类型) ? 读取削减比例(effect?.削减比例) : 0;
          const 复制驱动倍率 = /属性|全部/.test(复制类型) && String(effect?.影响方向 || '').trim() === '效果强度' && String(effect?.驱动属性 || '').trim()
            ? 计算原型驱动缩放系数(effect, attacker, attacker?.final || {}, sourceObj, sourceObj?.final || buildCombatFinalStats(sourceObj))
            : 1;
          const 复制倍率 = Number(限制战斗倍率((1 - 削减比例) * (Number.isFinite(复制驱动倍率) ? 复制驱动倍率 : 1), 0.05, 1.5).toFixed(3));
          const 来源为敌方 = 目标敌方集合.some(unit => unit && isCombatUnitIdentityMatch(unit, sourceObj?.name || sourceObj?.名称 || sourceObj));
          const 是否敌方复制 = 来源为敌方;
          const 目标状态效果 = sourceObj?.状态效果 && typeof sourceObj.状态效果 === 'object' ? Object.values(sourceObj.状态效果) : [];
          const 目标战斗效果 = 目标状态效果.map(状态 => 状态?.战斗效果 || {});
          const 目标被控制 = 目标战斗效果.some(效果 => 效果?.cannot_react || 效果?.skip_turn);
          const 锁定强度 = Math.max(0, ...目标战斗效果.map(效果 => Number(效果?.lock_level || 0)));
          const 接触成立 = /近战|接触|贴身|抓取/.test(`${String(pClash?.伤害类型 || '')}${String(playerAction?.action_type || '')}${String(skillName || '')}`);
          const 判定修正 = (目标被控制 ? -0.18 : 0) + Math.min(0.18, 锁定强度 * -0.04) + (接触成立 ? -0.08 : 0);
          const 强行判定失败概率 = Math.max(0.08, Math.min(0.8, 0.24 + 等级差 * 0.08 + 判定修正));
          let 已复制 = false;
          if (是否敌方复制 && 复制模式 === '即时镜像') {
            if (Math.random() < 强行判定失败概率) {
              result.desc += ` [复制失败] 即时镜像未过，损失动作。`;
              return false;
            }
            result.desc += ` [复制判定] 即时镜像通过。`;
          }
          if (/属性|全部/.test(复制类型)) {
            const sourceStats = sourceObj?.final || sourceObj?.属性 || sourceObj || {};
            const 属性快照 = {};
            [
              ['str', '力量'],
              ['def', '防御'],
              ['agi', '敏捷'],
              ['vit_max', '体力上限'],
              ['sp_max', '魂力上限'],
              ['men_max', '精神力上限'],
            ].forEach(([key, label]) => {
              const sourceValue = Number(sourceStats[key] ?? sourceStats[label] ?? sourceObj?.属性?.[label] ?? 0);
              if (sourceValue > 0) 属性快照[label] = Math.max(1, Math.round(sourceValue * 复制倍率));
            });
            if (Object.keys(属性快照).length) {
              const copiedKey = putConditionWithUniqueKey(attacker, `属性复制:${sourceObj?.name || '目标'}`, {
                类型: 'buff',
                层数: 1,
                描述: `由[${skillName || '技能'}]复制${sourceObj === attacker ? '自身' : sourceObj?.name || '目标'}的属性`,
                duration: 复制保留回合,
                属性快照,
                战斗效果: { ...createEmptyCombatEffectMap() },
              });
              result.desc += ` [属性复制] 获得${Object.keys(属性快照).join('/')}属性镜像，复制倍率${Math.round(复制倍率 * 100)}%，维持${复制保留回合}回合，进入[${copiedKey}]。`;
              已复制 = true;
            } else {
              result.desc += ` [属性复制] 未找到可复制属性。`;
            }
          }
          if (/技能|全部/.test(复制类型)) {
            const 当前技能键 = 生成技能复制键(playerAction?.skill || {});
            const 读取目标本次动作技能 = 目标对象 => {
              if (!目标对象) return null;
              if (isCombatUnitIdentityMatch(目标对象, defender?.name || defender?.名称 || defender)) return npcAction?.skill || null;
              if (isCombatUnitIdentityMatch(目标对象, attacker?.name || attacker?.名称 || attacker)) return playerAction?.skill || null;
              return 目标对象?.本次动作?.skill || 目标对象?.当前动作?.skill || null;
            };
            const 技能可复刻 = skill => {
              const 技能名 = String(skill?.name || skill?.魂技名 || skill?.技能名称 || '').trim();
              if (/^复刻·/.test(技能名)) return false;
              const 技能键 = 生成技能复制键(skill);
              if (!技能键 || 技能键 === 当前技能键 || 事件链.复制键集合.includes(技能键)) return false;
              if (技能含复制执行(skill)) return false;
              return true;
            };
            const 本次动作技能 = 复制模式 === '即时镜像' ? 读取目标本次动作技能(sourceObj) : null;
            const 预先复刻候选 = [];
            const 可复刻技能 = (本次动作技能
              ? [本次动作技能]
              : 预先复刻候选)
              .filter(技能可复刻)
              .slice(0, 复制次数);
            if (!可复刻技能.length) {
              result.desc += 本次动作技能
                ? ` [技能复刻] 目标本次动作不可复刻。`
                : ` [技能复刻] 未捕捉到目标本次可复刻动作。`;
              return 已复制;
            }
            可复刻技能.forEach(skill => {
              const 技能键 = 生成技能复制键(skill);
              if (技能键 && !事件链.复制键集合.includes(技能键)) 事件链.复制键集合.push(技能键);
            });
            const copiedKey = putConditionWithUniqueKey(attacker, `技能复刻:${sourceObj?.name || '目标'}`, {
              类型: 'buff',
              层数: 1,
              描述: `由[${skillName || '技能'}]复刻${sourceObj === attacker ? '自身' : sourceObj?.name || '目标'}的招式`,
              duration: 复制保留回合,
              战斗效果: { ...createEmptyCombatEffectMap() },
              复刻技能列表: 可复刻技能.map(skill => deepClonePlain(skill)),
              技能列表: Object.fromEntries(
                可复刻技能.map(skill => [
                  `技能_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 8)}`,
                  {
                    技能数据: deepClonePlain(skill),
                    ...(可用次数 !== undefined ? { 剩余次数: 可用次数 } : {}),
                  },
                ]),
              ),
            });
            result.desc += 可用次数 === undefined
              ? ` [技能复刻] 获得${可复刻技能.length}个复刻招式，维持${复制保留回合}回合，进入[${copiedKey}]。`
              : ` [技能复刻] 获得${可复刻技能.length}个复刻招式，剩余${可用次数}次，维持${复制保留回合}回合，进入[${copiedKey}]。`;
            已复制 = true;
          }
          if (!已复制 && !/技能|属性|全部/.test(复制类型)) result.desc += ` [复制失败] 复制类型无效。`;
          return 已复制;
        };


        const applyStealBuffEffect = effect => {
          if (!effect) return false;
          const stealCount = Math.max(1, Number(effect?.窃取数量 || 1));
          const sourceTargets = resolveSkillEffectTargetCharacters(
            playerAction.skill,
            effect,
            attacker,
            defender,
            combatData,
          );
          const 可窃取目标 = 过滤被时光回溯规避目标列表(sourceTargets);
          const stolenLogs = [];
          可窃取目标.forEach(sourceObj => {
            const stolen = [];
            for (let index = 0; index < stealCount; index += 1) {
              const candidate = pickTransferableCondition(sourceObj, ['buff']);
              if (!candidate) break;
              const removed = removeConditionWithSustain(sourceObj, candidate.key);
              if (!removed) break;
              removed.描述 = `由[${skillName || '技能'}]自${sourceObj === attacker ? '自身' : sourceObj.name || '目标'}窃取`;
              const copiedKey = putConditionWithUniqueKey(attacker, `${candidate.key}·窃取`, removed);
              stolen.push({ from: candidate.key, to: copiedKey });
            }
            if (stolen.length) {
              stolenLogs.push(`${sourceObj === attacker ? '自身' : sourceObj.name || '目标'}失去了[${stolen.map(item => item.from).join('/')}]，自身获得[${stolen.map(item => item.to).join('/')}]`);
            }
          });
          if (!stolenLogs.length) {
            result.desc += ` [窃取失败] 未找到可窃取的增益状态。`;
            return false;
          }
          result.desc += ` [窃取增益] ${stolenLogs.join('；')}。`;
          return true;
        };

        const applyStateExchangeEffect = effect => {
          if (!effect) return false;
          const 状态交换目标上下文 = resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData, effect);
          const targetObj = 状态交换目标上下文.primaryTarget;
          if (目标已被时光回溯规避(result, targetObj)) {
            result.desc += ` [时光回溯] ${targetObj?.name || '目标'}已被本回合回溯壳规避，本次状态交换不再落地。`;
            return false;
          }
          const 目标为敌方 = (状态交换目标上下文.hostileSet || []).some(unit =>
            unit && isCombatUnitIdentityMatch(unit, targetObj?.name || targetObj?.名称 || targetObj),
          );
          if (!targetObj || targetObj === attacker || !目标为敌方) {
            result.desc += ` [状态交换] 缺少有效交换目标。`;
            return false;
          }
          const 目标状态 = String(effect?.状态 || '任意负面').trim() || '任意负面';
          const ownCandidate = pickTransferableCondition(attacker, ['debuff'], 目标状态);
          const targetCandidate = pickTransferableCondition(targetObj, ['buff'], '任意增益');
          if (!ownCandidate || !targetCandidate) {
            result.desc += ` [状态交换] 双方没有形成可交换的状态组合。`;
            return false;
          }
          const 源动作抹消规则 = 读取源动作抹消规则(effect, { 消费: false });
          if (源动作抹消规则) {
            描述源动作抹消(源动作抹消规则);
            return false;
          }
          const ownSnapshot = removeConditionWithSustain(attacker, ownCandidate.key);
          const targetSnapshot = removeConditionWithSustain(targetObj, targetCandidate.key);
          if (!ownSnapshot || !targetSnapshot) {
            result.desc += ` [状态交换] 交换过程中状态已失效。`;
            return false;
          }
          ownSnapshot.描述 = `由[${skillName || '技能'}]自${attacker.name || '自身'}交换至${targetObj.name || '目标'}`;
          targetSnapshot.描述 = `由[${skillName || '技能'}]自${targetObj.name || '目标'}交换至${attacker.name || '自身'}`;
          const ownNewKey = putConditionWithUniqueKey(targetObj, ownCandidate.key, ownSnapshot);
          const targetNewKey = putConditionWithUniqueKey(attacker, targetCandidate.key, targetSnapshot);
          attacker.final = buildCombatFinalStats(attacker);
          targetObj.final = buildCombatFinalStats(targetObj);
          result.desc += ` [状态交换] 自身的[${ownCandidate.key}]与${targetObj.name || '目标'}的[${targetCandidate.key}]完成交换，现分别变为[${targetNewKey}]与[${ownNewKey}]。`;
          return true;
        };

        if (directHealEffect) {
          applyImmediateRecoveryEffect(directHealEffect, 'hp', '生命');
          if (selfMirrorEffect && !effectTargetsSelf(directHealEffect))
            applyImmediateRecoveryEffect(mirrorEffectToSelf(directHealEffect), 'hp', '生命');
        }

        if (directSpEffect) {
          applyImmediateRecoveryEffect(directSpEffect, 'sp', '魂力');
          if (selfMirrorEffect && !effectTargetsSelf(directSpEffect))
            applyImmediateRecoveryEffect(mirrorEffectToSelf(directSpEffect), 'sp', '魂力');
        } else if ((pState.计算层效果?.sp_gain_ratio || 0) > 0 && !能力共享自身禁用命中) {
          const selfResourceBlockRatio = 读取资源锁定比例(attacker, 'sp', '转化锁定');
          设置战斗延迟效果资源值(attacker, 'sp', Math.min(
            attackerFinalStat.sp_max || attacker.sp_max || 0,
            attacker.sp +
              Math.floor(
                (attackerFinalStat.sp_max || attacker.sp_max || 0) *
                  pState.计算层效果.sp_gain_ratio *
                  (1 - selfResourceBlockRatio),
              ),
          ));
        }
        if (directMenEffect) {
          applyImmediateRecoveryEffect(directMenEffect, 'men', '精神力');
          if (selfMirrorEffect && !effectTargetsSelf(directMenEffect))
            applyImmediateRecoveryEffect(mirrorEffectToSelf(directMenEffect), 'men', '精神力');
        } else if ((pState.计算层效果?.men_gain_ratio || 0) > 0 && !能力共享自身禁用命中) {
          const selfResourceBlockRatio = 读取资源锁定比例(attacker, 'men', '转化锁定');
          设置战斗延迟效果资源值(attacker, 'men', Math.min(
            attackerFinalStat.men_max || attacker.men_max || 0,
            attacker.men +
              Math.floor(
                (attackerFinalStat.men_max || attacker.men_max || 0) *
                  pState.计算层效果.men_gain_ratio *
                  (1 - selfResourceBlockRatio),
              ),
          ));
        }

          const 写入直接状态移除事实 = (targetObj, removed = [], reason = 'state_remove') => {
            (Array.isArray(removed) ? removed : []).map(item => String(item || '').trim()).filter(Boolean).forEach(stateName => {
              写入战斗事件账本(combatData, {
                eventKind: 'state_remove',
                round: Number(combatData?.回合 || 0),
                actorName: attacker?.name || attacker?.名称 || '',
                targetName: targetObj?.name || targetObj?.名称 || '',
                actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '',
                actionType: 'state_remove',
                sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || skillName || '',
                sourceRound: Number(combatData?.回合 || 0),
                result: 'removed',
                primaryOutcome: 'state_remove',
                meta: {
                  stateName,
                  removedStates: removed,
                  calculationTrace: [
                    ['stateName', stateName],
                    ['stackMode', 'cleanse_or_dispel'],
                    ['previousDuration', 0],
                    ['nextDuration', 0],
                  ],
                  stackMode: 'cleanse_or_dispel',
                  replaceReason: reason,
                },
              });
            });
          };

          if (directCleanseEffect && !源动作被机制抹消(directCleanseEffect)) {
            const cleanseTargets = 过滤被时光回溯规避目标列表(resolveSkillEffectTargetCharacters(
              playerAction.skill,
              directCleanseEffect,
              attacker,
              defender,
              combatData,
            ));
            cleanseTargets.forEach(cleanseTarget => {
              const removed = removeNegativeConditionsByCleanse(cleanseTarget, 读取状态移除数量(directCleanseEffect.数量 ?? 1));
              if (removed.length > 0) {
                写入直接状态移除事实(cleanseTarget, removed, 'direct_cleanse');
                if (是主原型效果(directCleanseEffect)) 登记主原型成立目标(cleanseTarget);
                result.desc += ` [净化生效] ${cleanseTarget === attacker ? '自身' : cleanseTarget.name}清除了[${removed.join('/')}]。`;
              } else
                result.desc += ` [净化生效] ${cleanseTarget === attacker ? '自身' : cleanseTarget.name}当前没有可清除的负面状态。`;
            });
            if (selfMirrorEffect && !effectTargetsSelf(directCleanseEffect)) {
              const removedSelf = removeNegativeConditionsByCleanse(
                attacker,
                读取状态移除数量(directCleanseEffect.数量 ?? 1),
              );
              if (removedSelf.length > 0) {
                写入直接状态移除事实(attacker, removedSelf, 'direct_cleanse_self_mirror');
                result.desc += ` [自身反馈] 自身同步清除了[${removedSelf.join('/')}]。`;
              }
            }
          }
          if (directDispelEffect && !源动作被机制抹消(directDispelEffect)) {
            const dispelTargets = 过滤被时光回溯规避目标列表(resolveSkillEffectTargetCharacters(
              playerAction.skill,
              directDispelEffect,
              attacker,
              defender,
              combatData,
            ));
            dispelTargets.forEach(dispelTarget => {
              const removed = removePositiveConditionsByDispel(dispelTarget, Number(directDispelEffect.驱散数量 || 1));
              if (removed.length > 0) {
                写入直接状态移除事实(dispelTarget, removed, 'direct_dispel');
                if (是主原型效果(directDispelEffect)) 登记主原型成立目标(dispelTarget);
                result.desc += ` [驱散生效] ${dispelTarget === attacker ? '自身' : dispelTarget.name}失去了[${removed.join('/')}]。`;
              } else
                result.desc += ` [驱散生效] ${dispelTarget === attacker ? '自身' : dispelTarget.name}当前没有可驱散的增益状态。`;
            });
            if (selfMirrorEffect && !effectTargetsSelf(directDispelEffect)) {
              const removedSelf = removePositiveConditionsByDispel(
                attacker,
                Number(directDispelEffect.驱散数量 || 1),
              );
              if (removedSelf.length > 0) {
                写入直接状态移除事实(attacker, removedSelf, 'direct_dispel_self_mirror');
                result.desc += ` [自身反馈] 自身同步失去了[${removedSelf.join('/')}]。`;
              }
            }
          }
          if (directStatusRemoveEffects.length) {
            directStatusRemoveEffects.forEach(effect => {
              if (源动作被机制抹消(effect)) return;
              const statusTargets = 过滤被时光回溯规避目标列表(resolveSkillEffectTargetCharacters(
                playerAction.skill,
                effect,
                attacker,
                defender,
                combatData,
              ));
              statusTargets.forEach(statusTarget => {
                const removed = removeConditionsByPrototypeFilter(statusTarget, effect, attacker);
                if (removed.length > 0) {
                  写入直接状态移除事实(statusTarget, removed, 'direct_status_remove');
                  if (是主原型效果(effect)) 登记主原型成立目标(statusTarget);
                  result.desc += ` [状态移除] ${statusTarget === attacker ? '自身' : statusTarget.name}移除了[${removed.join('/')}]。`;
                } else if (removed.__概率失败数 > 0)
                  result.desc += ` [状态移除] ${statusTarget === attacker ? '自身' : statusTarget.name}命中匹配状态，但驱动判定失败。`;
                else
                  result.desc += ` [状态移除] ${statusTarget === attacker ? '自身' : statusTarget.name}没有匹配状态。`;
              });
              登记持续原型运行壳(effect, statusTargets);
            });
          }
          if (directStealBuffEffect && !源动作被机制抹消(directStealBuffEffect)) {
            applyStealBuffEffect(directStealBuffEffect);
          }
          if (directShieldEffect && !源动作被机制抹消(directShieldEffect)) {
            applyShieldEffect(directShieldEffect);
            if (selfMirrorEffect && !effectTargetsSelf(directShieldEffect))
              applyShieldEffect(mirrorEffectToSelf(directShieldEffect));
          }
          if (directSummonEffect) {
            runDirectMechanismConsumer(directSummonEffect);
          }
          if (directMechanismSuppressEffect) {
            runDirectMechanismConsumer(directMechanismSuppressEffect);
          }
          if (directCopyEffect) {
            runDirectMechanismConsumer(directCopyEffect);
          }
          if (directGrantEffect) {
            const 授予源抹消规则 = 战斗机制抹消命中(attacker, '源动作', directGrantEffect, { 用途: '封锁' });
            if (授予源抹消规则) {
              result.desc += ` [机制抹消] ${attacker?.name || '施术者'}无法使用【${读取战斗机制抹消对象摘要(授予源抹消规则.抹消对象)}】，本次机制授予被阻断。`;
            } else {
            const grantTargets = 过滤被时光回溯规避目标列表(resolveSkillEffectTargetCharacters(
              playerAction.skill,
              directGrantEffect,
              attacker,
              defender,
              combatData,
            ));
            const grantEffects = (Array.isArray(directGrantEffect.授予效果) ? directGrantEffect.授予效果 : [])
              .filter(effect => effect && typeof effect === 'object');
            if (grantEffects.length > 0) {
              grantTargets.forEach(targetObj => {
                if (!targetObj) return;
                const 授予落地抹消规则 = 战斗机制抹消命中(targetObj, '最终结果', {
                  ...directGrantEffect,
                  原型: '机制授予',
                }, { 用途: '封锁' });
                if (授予落地抹消规则) {
                  result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}对【${读取战斗机制抹消对象摘要(授予落地抹消规则.抹消对象)}】存在封锁，本次授予未能落地。`;
                  return;
                }
                const 来源技能名 = String(playerAction.skill?.name || playerAction.skill?.魂技名 || '临时触发权').trim() || '临时触发权';
                const 授予触发条件 = String(directGrantEffect.触发条件 || '主动触发').trim() === '随下次行动触发' ? '随下次行动触发' : '主动触发';
                const 授予效果摘要 = grantEffects
                  .map(effect => 读取战斗机制抹消对象摘要({ 原型: effect?.原型, 状态: effect?.状态, 规则: effect?.规则, 资源: effect?.资源, 结算: effect?.结算 }))
                  .filter(Boolean);
                const stateName = putConditionWithUniqueKey(targetObj, `原型授予:${来源技能名}`, {
                  类型: 'buff',
                  层数: 1,
                  描述: `由[${playerAction.skill.name || '技能'}]预付成本授予`,
                  duration: 授予触发条件 === '随下次行动触发' ? 1 : Math.max(1, Number(directGrantEffect.持续回合 || directGrantEffect.持续 || 1)),
                  来源技能: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                  来源角色: attacker?.name || attacker?.名称 || '',
                  来源原型摘要: '机制授予',
                  授予效果原型摘要: 授予效果摘要,
                  效果授予状态: true,
                  触发条件: 授予触发条件,
                  可用次数: 授予触发条件 === '随下次行动触发' ? 1 : Math.max(1, Math.floor(Number(directGrantEffect.可用次数 || 1))),
                  授予效果: deepClone(grantEffects),
                  战斗效果: {
                    ...createEmptyCombatEffectMap(),
                    mechanism_grant: true,
                  },
                });
                result.desc += ` [原型授予] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}获得[${stateName}]。`;
              });
            }
            }
          }
          玩家临时技能已生效 = 判断临时技能真实落地(玩家临时技能快照);
          NPC临时技能已生效 = 判断NPC临时技能对抗结算落地(NPC临时技能快照);
          if (npcAction.skill) {
            const NPC临时技能效果 = (
              npcAction.skill.__机制授予主动技能 === true ||
              !!npcAction.skill.__复制效果键 ||
              !!npcAction.skill.__复制状态名
            ) ? getSkillEffects(npcAction.skill) : [];
            const NPC授予前置效果 = Array.isArray(npcAction.__授予前置效果列表) ? npcAction.__授予前置效果列表 : [];
            if (NPC临时技能效果.length || NPC授予前置效果.length) {
              NPC临时技能已生效 = 执行临时行动直接效果(defender, attacker, npcAction, [...NPC临时技能效果, ...NPC授予前置效果]) || NPC临时技能已生效;
            }
          }
          执行授予前置效果();
          directStateExchangeEffects.forEach(effect => {
            applyStateExchangeEffect(effect);
            登记持续原型运行壳(effect);
          });
          [
            directStatusTransferEffect,
            directDotDetonateEffect,
            directShieldBreakEffect,
            directRingBurstEffect,
            directResourceBurnEffect,
            directResourceLockEffect,
            directLifeLinkEffect,
            directDotExtendEffect,
            directDotCompressEffect,
            ...遥控状态施加效果列表,
            ...directPrototypeEffects,
          ]
            .filter(Boolean)
            .forEach(effect => {
              runDirectMechanismConsumer(effect);
            });

          const 直接消费状态施加集合 = new Set([
            directResourceBurnEffect,
          ].filter(Boolean));
          const 主要状态施加效果列表 = 独立状态施加效果列表().filter(effect => !直接消费状态施加集合.has(effect));
          const 附带状态施加效果列表 = 跟随状态施加效果列表().filter(effect => !直接消费状态施加集合.has(effect));
          const 目标主效果成立 = 目标对象 => {
            if (!目标对象 || 目标本次明确规避主效果(目标对象)) return false;
            const 目标名 = String(目标对象?.name || 目标对象?.名称 || '').trim();
            if (hasDirectDamageEffect) return !!目标名 && (主原型成立目标集合.has(目标名) || 主原型命中目标集合.has(目标名));
            return true;
          };
          if (
            pState.状态名称 &&
            pState.状态名称 !== '无' &&
            (Number(result.totalProjectedDamage || result.dmg || 0) > 0 || hostileTargetRedirectedToSelf || !hasDirectDamageEffect)
          ) {
            const skillSideEffects = getBattleSkillSideEffectList(playerAction.skill).filter(item =>
              ['回合结束时', '效果结束后'].includes(String(item?.触发时机 || '').trim()),
            );
            const stateTargetContext = getEffectTargetContext(pState);
            const stateTargets = stateTargetContext.targetSet;
            const stateTargetsFriendly = ['自身', '友方单体', '友方群体'].includes(stateTargetContext.targetKind);
            const 主状态需要命中成立 = !状态施加视为友方或增益(pState);
            stateTargets.forEach(targetObj => {
              const 目标标记名 = String(targetObj?.name || (targetObj === attacker ? '自身' : '') || '').trim();
              if (主状态需要命中成立 && !目标主效果成立(targetObj)) return;
              const 被时光回溯规避 = Array.isArray(result.targetResults) && result.targetResults.some(entry =>
                entry?.target &&
                targetObj &&
                isCombatUnitIdentityMatch(entry.target, targetObj) &&
                entry?.被时光回溯规避 === true,
              );
              if (被时光回溯规避) {
                result.desc += ` [时光回溯] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}已被本回合回溯壳规避，本次后续状态不再落地。`;
                return;
              }
              const 状态名 = String(pState.状态名称 || '').trim();
              if (
                directDotDetonateEffect &&
                状态名 &&
                引爆移除状态标记集.has(`${目标标记名}::${状态名}`)
              ) {
                result.desc += ` [持续引爆] ${targetObj === attacker ? '自身' : targetObj.name}的[${状态名}]已被引爆消耗，本回合不重复附着。`;
                return;
              }
              const targetFinalStat = targetObj?.final || buildCombatFinalStats(targetObj);
              const targetConditionEffects = targetObj.状态效果
                ? Object.values(targetObj.状态效果).map(c => c?.战斗效果 || {})
                : [];
              const selfRedirectedDebuff =
                hostileTargetRedirectedToSelf && targetObj === attacker && !stateTargetsFriendly;
              let isBuff =
                (!selfRedirectedDebuff && targetObj === attacker) ||
                stateTargetsFriendly ||
                String(pState.特殊机制标识 || '无').includes('增益') ||
                pState.计算层效果?.无视异常 === true ||
                pState.计算层效果?.super_armor === true ||
                pState.计算层效果?.探查屏蔽 === true ||
                Number(pState.计算层效果?.min_hp_floor || 0) > 0 ||
                Number(pState.计算层效果?.hot_heal_ratio || 0) > 0;
              if (!isBuff && 无视异常阻断负面或削减(targetObj, {
                原型: '状态施加',
                状态: pState.状态名称,
                状态名称: pState.状态名称,
                类型: 'debuff',
                计算层效果: pState.计算层效果 || {},
              })) {
                result.desc += ` [无视异常] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}免疫了[${pState.状态名称}]。`;
                return;
              }
              const 最终结果抹消规则 = 战斗机制抹消命中(targetObj, '最终结果', {
                原型: '状态施加',
                状态: pState.状态名称,
                状态名称: pState.状态名称,
                类型: isBuff ? 'buff' : 'debuff',
                战斗效果: pState.计算层效果 || {},
              }, { 用途: '封锁' });
              if (最终结果抹消规则) {
                result.desc += ` [机制抹消] ${targetObj === attacker ? '自身' : targetObj.name}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，本次状态未能附着。`;
                return;
              }
              const 状态成功率审计 = 计算状态施加成功率审计(pState, attacker, targetObj);
              const 驱动成功率 = 状态成功率审计.successRate;
              const 状态附着检定 = 驱动成功率 < 1 ? Math.random() : null;
              if (驱动成功率 < 1 && 状态附着检定 > 驱动成功率) {
                result.desc += ` [状态施加] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}抵住了[${pState.状态名称}]附着。`;
                写入战斗事件账本(combatData, {
                  eventKind: 'state_apply',
                  round: Number(combatData?.回合 || 0),
                  actorName: attacker?.name || attacker?.名称 || '',
                  targetName: targetObj?.name || targetObj?.名称 || '',
                  actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                  actionType: playerAction?.action_type || playerAction?.type || '',
                  sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                  sourceRound: Number(combatData?.回合 || 0),
                  result: 'resisted',
                  duration: Math.max(0, Number(pState?.持续回合 || 0)),
                  driverAttr: String(读取状态施加默认驱动属性_战斗(pState) || '').trim(),
                  meta: {
                    stateName: pState.状态名称,
                    successRate: Number(驱动成功率 || 0),
                    roll: Number(状态附着检定 || 0),
                    successRateReason: 状态成功率审计.reason,
                    successRateBreakdown: 格式化状态附着成功率拆分(状态成功率审计, 状态附着检定, false),
                    reason: '状态附着抗性判定失败',
                  },
                });
                return;
              }
              const supportScale = isBuff ? getSupportEffectScale(attacker, targetObj) : 1;
              let scaledMods = { ...(pState.面板修改比例 || {}) };
              let scaledDeltas = { ...(pState.面板固定修正 || {}) };
              ['str', 'def', 'agi', 'men_max', 'sp_max'].forEach(k => {
                if (scaledMods[k] !== undefined && scaledMods[k] !== 1.0) {
                  scaledMods[k] = 1 + (scaledMods[k] - 1) * supportScale;
                }
                if (scaledDeltas[k] !== undefined) {
                  scaledDeltas[k] = Number((Number(scaledDeltas[k] || 0) * supportScale).toFixed(4));
                }
              });
              let scaledCalc = isBuff
                ? scaleBattleSupportBuffCalc(pState.计算层效果 || {}, supportScale)
                : { ...(pState.计算层效果 || {}) };
              if (fusionProfile) {
                scaledMods = scaleFusionPanelModifierMap(scaledMods, fusionProfile);
                scaledCalc = scaleFusionCombatEffectMap(scaledCalc, fusionProfile);
              }

              let isImmune = false;
              const isControlLike =
                !isBuff &&
                (pState.计算层效果?.skip_turn === true ||
                  pState.计算层效果?.cannot_react === true ||
                  pState.计算层效果?.silence === true ||
                  pState.计算层效果?.skill_seal === true ||
                  pState.计算层效果?.blind === true ||
                  Number(pState.计算层效果?.lock_level || 0) > 0 ||
                  Number(pState.计算层效果?.random_target_rate || 0) > 0 ||
                  Number(pState.计算层效果?.cast_speed_penalty || 0) > 0);
              if (isControlLike) {
                const controlScale = 计算精神机制缩放系数(attacker, attackerFinalStat, targetObj, targetFinalStat, 1, {
                  下限: 0,
                  上限: 2,
                  压制指数: 1,
                });
                if (controlScale < 0.42) isImmune = true;
              }
              if (!isBuff && 探查屏蔽阻断敌对锁定状态(targetObj, pState)) {
                result.desc += ` [探查屏蔽] ${targetObj === attacker ? '自身' : targetObj.name}屏蔽了[${pState.状态名称}]锁定。`;
                return;
              }
              if (!isBuff && 目标本次明确规避主效果(targetObj)) {
                result.desc += ` [状态施加] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}已通过完整闪避避开本次[${pState.状态名称}]附着。`;
                return;
              }

              if (isImmune) {
                result.desc += ` 但${targetObj === attacker ? '自身' : targetObj.name}凭借绝对的属性碾压，强行豁免了[${pState.状态名称}]控制！`;
                return;
              }
              if (!targetObj.状态效果) targetObj.状态效果 = {};
              const 旧状态条目 = targetObj.状态效果[pState.状态名称] && typeof targetObj.状态效果[pState.状态名称] === 'object'
                ? targetObj.状态效果[pState.状态名称]
                : null;
              if (
                旧状态条目 &&
                旧状态条目.__本回合新附加 === true &&
                String(旧状态条目.来源技能 || '').trim() === String(playerAction.skill?.name || playerAction.skill?.魂技名 || '').trim() &&
                String(旧状态条目.来源角色 || '').trim() === String(attacker?.name || attacker?.名称 || '').trim()
              ) {
                return;
              }
              const 下一持续 = Math.max(Number(pState.持续回合 || 0), Number(旧状态条目?.duration || 0));
              const targetMaxHp = getCombatHpMaxValue(targetObj);
              const 状态护盾数值 = Number(scaledCalc?.shield_gain_bonus || 0);
              const 状态护盾值 = 状态护盾数值 > 0
                ? (/^[-+]?\d+(?:\.\d+)?%$/.test(String(pState?.数值 ?? pState?.护盾值 ?? '').trim()) || 状态护盾数值 <= 1
                  ? Math.max(1, Math.floor(targetMaxHp * 状态护盾数值))
                  : Math.floor(状态护盾数值))
                : 0;
              if (状态护盾值 > 0) scaledCalc.shield_gain_bonus = 0;
              清理非生命流失状态伤害字段(pState.状态名称, scaledCalc);
              const 新状态条目 = {
                类型: isBuff ? 'buff' : 'debuff',
                状态名称: pState.状态名称,
                __本回合新附加: true,
                层数: 1,
                来源技能: playerAction.skill.name || playerAction.skill.魂技名 || '',
                来源角色: attacker.name || attacker.名称 || '',
                来源原型摘要: String(pState.原型 || '状态施加'),
                描述: `由[${playerAction.skill.name}]附加`,
                duration: 下一持续,
                驱动属性: String(读取状态施加默认驱动属性_战斗(pState) || '').trim(),
                效果摘要: 构建状态结果效果摘要(scaledCalc),
                副作用列表: skillSideEffects.filter(item => {
                  const boundState = String(item?.关联状态 || '').trim();
                  return !boundState || boundState === pState.状态名称;
                }),
                shield_value: 状态护盾值,
                抹消规则: Array.isArray(pState.抹消规则) ? deepClone(pState.抹消规则) : [],
                面板修改比例: scaledMods,
                面板固定修正: scaledDeltas,
                战斗效果: {
                  ...createEmptyCombatEffectMap(),
                  silence: scaledCalc?.silence ?? false,
                  disarm: scaledCalc?.disarm ?? false,
                  blind: scaledCalc?.blind ?? false,
                  invincible: scaledCalc?.invincible ?? false,
                  无视异常: scaledCalc?.无视异常 ?? false,
                  skill_seal: scaledCalc?.skill_seal ?? false,
                  探查屏蔽: scaledCalc?.探查屏蔽 ?? false,
                  counter_attack_ratio: scaledCalc?.counter_attack_ratio ?? 0,
                  damage_reflect_ratio: scaledCalc?.damage_reflect_ratio ?? 0,
                  damage_transfer_ratio: scaledCalc?.damage_transfer_ratio ?? 0,
                  damage_transfer_target: scaledCalc?.damage_transfer_target ?? '',
                  damage_absorb_ratio: scaledCalc?.damage_absorb_ratio ?? 0,
                  吸收来源: scaledCalc?.吸收来源 ?? '',
                  吸收资源: scaledCalc?.吸收资源 ?? '',
                  吸收转化效果: scaledCalc?.吸收转化效果 ?? '',
                  伤害吸收增幅上限: scaledCalc?.伤害吸收增幅上限 ?? 0,
                  damage_share_ratio: scaledCalc?.damage_share_ratio ?? 0,
                  cost_share_ratio: scaledCalc?.cost_share_ratio ?? 0,
                  damage_reduction: scaledCalc?.damage_reduction ?? 0,
                  block_count: scaledCalc?.block_count ?? 0,
                  super_armor: scaledCalc?.super_armor ?? false,
                  death_save_count: scaledCalc?.death_save_count ?? 0,
                  revive_count: scaledCalc?.revive_count ?? 0,
                  revive_heal_ratio: scaledCalc?.revive_heal_ratio ?? 0,
                  hit_bonus: isBuff ? (scaledCalc?.hit_bonus ?? 0) : 0,
                  dodge_penalty: isBuff ? 0 : (scaledCalc?.dodge_penalty ?? 0),
                  dodge_bonus: isBuff ? (scaledCalc?.dodge_bonus ?? 0) : 0,
                  lock_level: isBuff ? 0 : (scaledCalc?.lock_level ?? 0),
                  skip_turn: scaledCalc?.skip_turn ?? false,
                  cannot_react: scaledCalc?.cannot_react ?? false,
                  dot_damage: scaledCalc?.dot_damage ?? pState.持续真伤dot,
                  dot_damage_ratio: scaledCalc?.dot_damage_ratio ?? 0,
                  armor_pen: scaledCalc?.armor_pen ?? 0,
                  reaction_bonus: scaledCalc?.reaction_bonus ?? 0,
                  reaction_penalty: scaledCalc?.reaction_penalty ?? 0,
                  attacker_speed_bonus: scaledCalc?.attacker_speed_bonus ?? 0,
                  cast_speed_bonus: scaledCalc?.cast_speed_bonus ?? 0,
                  cast_speed_penalty: scaledCalc?.cast_speed_penalty ?? 0,
                  hit_penalty: scaledCalc?.hit_penalty ?? 0,
                  interrupt_bonus: scaledCalc?.interrupt_bonus ?? 0,
                  final_damage_mult: scaledCalc?.final_damage_mult ?? 1.0,
                  received_damage_mult: scaledCalc?.received_damage_mult ?? 1.0,
                  defense_strip: isBuff ? 0 : (scaledCalc?.defense_strip ?? 0),
                  spirit_resist_strip: isBuff ? 0 : (scaledCalc?.spirit_resist_strip ?? 0),
                  final_damage_bonus: scaledCalc?.final_damage_bonus ?? 0,
                  final_heal_mult: scaledCalc?.final_heal_mult ?? 1.0,
                  final_heal_bonus: scaledCalc?.final_heal_bonus ?? 0,
                  shield_gain_mult: scaledCalc?.shield_gain_mult ?? 1.0,
                  shield_gain_bonus: scaledCalc?.shield_gain_bonus ?? 0,
                  vit_gain_ratio: scaledCalc?.vit_gain_ratio ?? 0,
                  sp_gain_ratio: scaledCalc?.sp_gain_ratio ?? 0,
                  men_gain_ratio: scaledCalc?.men_gain_ratio ?? 0,
                  heal_block_ratio: scaledCalc?.heal_block_ratio ?? 0,
                  hot_heal_ratio: scaledCalc?.hot_heal_ratio ?? 0,
                  cost_ratio: scaledCalc?.cost_ratio ?? 1.0,
                  cost_delta: scaledCalc?.cost_delta ?? 0,
                  cost_delta_ratio: scaledCalc?.cost_delta_ratio ?? 0,
                  windup_ratio: scaledCalc?.windup_ratio ?? 1.0,
                  windup_delta: scaledCalc?.windup_delta ?? 0,
                  heal_inversion_ratio: scaledCalc?.heal_inversion_ratio ?? 0,
                  random_target_rate: scaledCalc?.random_target_rate ?? 0,
                  stealth_level: scaledCalc?.stealth_level ?? 0,
                  misfortune_backlash_ratio: scaledCalc?.misfortune_backlash_ratio ?? 0,
                  min_hp_floor: scaledCalc?.min_hp_floor ?? 0,
                  death_save_count: scaledCalc?.death_save_count ?? 0,
                  damage_share_count: scaledCalc?.damage_share_count ?? 0,
                  cost_share_count: scaledCalc?.cost_share_count ?? 0,
                  invincible_tier_threshold: scaledCalc?.invincible_tier_threshold ?? 0,
                  每日触发次数上限: scaledCalc?.每日触发次数上限 ?? 0,
                },
              };
              if (无视异常阻断负面或削减(targetObj, 新状态条目)) {
                result.desc += ` [无视异常] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}免疫了[${pState.状态名称}]。`;
                return;
              }
              const 元素承伤修正 = 构建状态元素承伤修正(pState);
              if (元素承伤修正) 新状态条目.元素承伤修正 = 元素承伤修正;
              const 持续移除命中 = 持续状态移除阻断状态附着(targetObj, pState.状态名称, 新状态条目, attacker);
              if (持续移除命中) {
                result.desc += ` [持续状态移除] ${targetObj === attacker ? '自身' : targetObj.name || '目标'}的[${pState.状态名称}]被[${持续移除命中.key}]拦截。`;
                return;
              }
              targetObj.状态效果[pState.状态名称] = 新状态条目;
              targetObj.final = buildCombatFinalStats(targetObj);
              if (targetObj.召唤键) 同步召唤单位镜像(targetObj);
              新状态条目.__状态来源键 = 记录状态来源登记(combatData, {
                stateName: pState.状态名称,
                targetName: targetObj?.name || targetObj?.名称 || '',
                sourceActorName: attacker?.name || attacker?.名称 || '',
                sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                sourceActionType: playerAction?.action_type || playerAction?.type || '',
                sourceRound: Number(combatData?.回合 || 0),
                duration: 下一持续,
                effectSummary: 构建状态结果效果摘要(新状态条目.战斗效果 || {}),
                driverAttr: String(读取状态施加默认驱动属性_战斗(pState) || '').trim(),
              });
              写入战斗事件账本(combatData, {
                eventKind: 'state_apply',
                round: Number(combatData?.回合 || 0),
                actorName: attacker?.name || attacker?.名称 || '',
                targetName: targetObj?.name || targetObj?.名称 || '',
                actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                actionType: playerAction?.action_type || playerAction?.type || '',
                sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                sourceRound: Number(combatData?.回合 || 0),
                result: 'applied',
                applicationId: 新状态条目.__状态来源键,
                duration: 下一持续,
                effectSummary: 构建状态结果效果摘要(新状态条目.战斗效果 || {}),
                driverAttr: String(读取状态施加默认驱动属性_战斗(pState) || '').trim(),
                meta: {
                  stateName: pState.状态名称,
                  successRate: Number(驱动成功率 || 1),
                  roll: Number(状态附着检定 || 0),
                  successRateReason: 状态成功率审计.reason,
                  successRateBreakdown: 格式化状态附着成功率拆分(状态成功率审计, 状态附着检定, true),
                },
              });
              if (是主原型效果(pState)) 登记主原型成立目标(targetObj);
              if (directTauntEffect && targetObj !== attacker) {
                targetObj.状态效果[pState.状态名称].强制目标名 = attacker.name || '';
              }
              if (directGuardEffect) {
                targetObj.状态效果[pState.状态名称].护卫者名 = attacker.name || '';
                const 护卫次数 = Math.max(0, Number(directGuardEffect.护卫次数 ?? directGuardEffect.guard_count ?? 0));
                if (护卫次数 > 0) targetObj.状态效果[pState.状态名称].护卫剩余次数 = 护卫次数;
              }
              let targetNameStr = targetObj === attacker ? '自身' : (targetObj?.name || targetObj?.名称 || '目标');
              result.desc += ` 并对${targetNameStr}施加了[${pState.状态名称}]状态！`;
              if (selfMirrorEffect && targetObj !== attacker) {
                if (!attacker.状态效果) attacker.状态效果 = {};
                const mirrorKey = `${pState.状态名称}·自返`;
                attacker.状态效果[mirrorKey] = deepClone(targetObj.状态效果[pState.状态名称]);
                attacker.状态效果[mirrorKey].描述 = `由[${playerAction.skill.name}]同步反馈`;
                result.desc += ` [自身反馈] 施术者同步获得了[${mirrorKey}]效果。`;
              }
            });
          }

          const 落地额外状态施加 = (状态施加效果, 选项 = {}) => {
            const 状态名 = String(状态施加效果?.状态 || '').trim();
            if (!状态名 || 状态名 === '无') return false;
            const 状态目标上下文 = getEffectTargetContext(状态施加效果);
            const 需要命中成立 = !状态施加视为友方或增益(状态施加效果);
            const 状态目标列表 = 过滤被时光回溯规避目标列表(状态目标上下文.targetSet).filter(目标对象 => {
              if (!目标对象) return false;
              const 目标名 = String(目标对象?.name || 目标对象?.名称 || '').trim();
              if (!目标名) return false;
              if (目标本次明确规避主效果(目标对象)) return false;
              if (选项.需要主原型成立 || (需要命中成立 && hasDirectDamageEffect)) return 主原型成立目标集合.has(目标名) || 主原型命中目标集合.has(目标名);
              return true;
            });
            if (!状态目标列表.length) return false;
            const 状态目标友方 = ['自身', '友方单体', '友方群体'].includes(状态目标上下文.targetKind);
            let 已落地 = false;
            状态目标列表.forEach(目标对象 => {
              const 目标名 = String(目标对象?.name || 目标对象?.名称 || '目标').trim();
              const 是增益 =
                目标对象 === attacker ||
                状态目标友方 ||
                String(状态施加效果.特殊机制标识 || '无').includes('增益') ||
                状态施加效果.计算层效果?.无视异常 === true ||
                状态施加效果.计算层效果?.super_armor === true ||
                状态施加效果.计算层效果?.探查屏蔽 === true ||
                Number(状态施加效果.计算层效果?.min_hp_floor || 0) > 0 ||
                Number(状态施加效果.计算层效果?.hot_heal_ratio || 0) > 0;
              if (!是增益 && 探查屏蔽阻断敌对锁定状态(目标对象, 状态施加效果)) {
                result.desc += ` [探查屏蔽] ${目标名}屏蔽了[${状态名}]锁定。`;
                return;
              }
              if (!是增益 && 无视异常阻断负面或削减(目标对象, {
                原型: '状态施加',
                状态: 状态名,
                状态名称: 状态名,
                类型: 'debuff',
                计算层效果: 状态施加效果.计算层效果 || {},
              })) {
                result.desc += ` [无视异常] ${目标名}免疫了[${状态名}]。`;
                return;
              }
              const 最终结果抹消规则 = 战斗机制抹消命中(目标对象, '最终结果', {
                原型: '状态施加',
                状态: 状态名,
                状态名称: 状态名,
                类型: 是增益 ? 'buff' : 'debuff',
                战斗效果: 状态施加效果.计算层效果 || {},
              }, { 用途: '封锁' });
              if (最终结果抹消规则) {
                result.desc += ` [机制抹消] ${目标名}对【${读取战斗机制抹消对象摘要(最终结果抹消规则.抹消对象)}】存在封锁，本次状态未能附着。`;
                return;
              }
              const 状态成功率审计 = 计算状态施加成功率审计(状态施加效果, attacker, 目标对象);
              const 驱动成功率 = 状态成功率审计.successRate;
              const 状态附着检定 = 驱动成功率 < 1 ? Math.random() : null;
              if (驱动成功率 < 1 && 状态附着检定 > 驱动成功率) {
                result.desc += ` [状态施加] ${目标名}抵住了[${状态名}]附着。`;
                写入战斗事件账本(combatData, {
                  eventKind: 'state_apply',
                  round: Number(combatData?.回合 || 0),
                  actorName: attacker?.name || attacker?.名称 || '',
                  targetName: 目标对象?.name || 目标对象?.名称 || '',
                  actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                  actionType: playerAction?.action_type || playerAction?.type || '',
                  sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                  sourceRound: Number(combatData?.回合 || 0),
                  result: 'resisted',
                  duration: Math.max(0, Number(状态施加效果?.持续回合 || 0)),
                  driverAttr: String(读取状态施加默认驱动属性_战斗(状态施加效果) || '').trim(),
                  meta: {
                    stateName: 状态名,
                    successRate: Number(驱动成功率 || 0),
                    roll: Number(状态附着检定 || 0),
                    successRateReason: 状态成功率审计.reason,
                    successRateBreakdown: 格式化状态附着成功率拆分(状态成功率审计, 状态附着检定, false),
                    reason: '状态附着抗性判定失败',
                  },
                });
                return;
              }
              const 目标终值 = 目标对象?.final || buildCombatFinalStats(目标对象);
              const 目标战斗效果列表 = 目标对象.状态效果
                ? Object.values(目标对象.状态效果).map(状态 => 状态?.战斗效果 || {})
                : [];
              const 是控制类 =
                !是增益 &&
                (状态施加效果.计算层效果?.skip_turn === true ||
                  状态施加效果.计算层效果?.cannot_react === true ||
                  状态施加效果.计算层效果?.silence === true ||
                  状态施加效果.计算层效果?.skill_seal === true ||
                  状态施加效果.计算层效果?.blind === true ||
                  Number(状态施加效果.计算层效果?.lock_level || 0) > 0 ||
                  Number(状态施加效果.计算层效果?.random_target_rate || 0) > 0 ||
                  Number(状态施加效果.计算层效果?.cast_speed_penalty || 0) > 0);
              const 控制缩放 = 是控制类
                ? 计算精神机制缩放系数(attacker, attackerFinalStat, 目标对象, 目标终值, 1, {
                    下限: 0,
                    上限: 2,
                    压制指数: 1,
                  })
                : 1;
              const 免疫等级阈值 = Math.max(
                0,
                ...目标战斗效果列表.map(战斗效果 => Number(战斗效果?.invincible_tier_threshold || 0)),
              );
              if ((是控制类 && 控制缩放 < 0.42) || (免疫等级阈值 > 0 && getCombatUnitTierNumber(attacker) < 免疫等级阈值)) {
                result.desc += ` 但${目标名}凭借绝对的属性碾压，强行豁免了[${状态名}]控制！`;
                return;
              }
              if (!目标对象.状态效果) 目标对象.状态效果 = {};
              const 原状态 = 目标对象.状态效果[状态名] && typeof 目标对象.状态效果[状态名] === 'object'
                ? 目标对象.状态效果[状态名]
                : null;
              const 下一持续 = Math.max(Number(状态施加效果.持续回合 || 0), Number(原状态?.duration || 0));
              const 状态战斗效果 = mergeCombatEffectMaps(createEmptyCombatEffectMap(), 状态施加效果.计算层效果 || {});
              清理非生命流失状态伤害字段(状态名, 状态战斗效果);
              const 状态护盾数值 = Number(状态战斗效果.shield_gain_bonus || 0);
              const 状态护盾值 = 状态护盾数值 > 0
                ? (/^[-+]?\d+(?:\.\d+)?%$/.test(String(状态施加效果?.数值 ?? 状态施加效果?.护盾值 ?? '').trim()) || 状态护盾数值 <= 1
                  ? Math.max(1, Math.floor(getCombatHpMaxValue(目标对象) * 状态护盾数值))
                  : Math.floor(状态护盾数值))
                : 0;
              if (状态护盾值 > 0) 状态战斗效果.shield_gain_bonus = 0;
              const 旧状态条目 = 目标对象.状态效果[状态名] && typeof 目标对象.状态效果[状态名] === 'object'
                ? 目标对象.状态效果[状态名]
                : null;
              if (
                旧状态条目 &&
                旧状态条目.__本回合新附加 === true &&
                String(旧状态条目.来源技能 || '').trim() === String(playerAction.skill?.name || playerAction.skill?.魂技名 || '').trim() &&
                String(旧状态条目.来源角色 || '').trim() === String(attacker?.name || attacker?.名称 || '').trim()
              ) {
                return;
              }
              const 新状态条目 = {
                类型: 是增益 ? 'buff' : 'debuff',
                状态名称: 状态名,
                __本回合新附加: true,
                层数: 1,
                来源技能: playerAction.skill.name || playerAction.skill.魂技名 || '',
                来源角色: attacker.name || attacker.名称 || '',
                来源原型摘要: String(状态施加效果.原型 || '状态施加'),
                描述: `由[${playerAction.skill.name}]附加`,
                duration: 下一持续,
                驱动属性: String(读取状态施加默认驱动属性_战斗(状态施加效果) || '').trim(),
                效果摘要: 构建状态结果效果摘要(状态战斗效果),
                shield_value: 状态护盾值,
                面板修改比例: { ...(状态施加效果.面板修改比例 || {}) },
                面板固定修正: { ...(状态施加效果.面板固定修正 || {}) },
                战斗效果: 状态战斗效果,
              };
              if (无视异常阻断负面或削减(目标对象, 新状态条目)) {
                result.desc += ` [无视异常] ${目标名}免疫了[${状态名}]。`;
                return;
              }
              const 元素承伤修正 = 构建状态元素承伤修正(状态施加效果);
              if (元素承伤修正) 新状态条目.元素承伤修正 = 元素承伤修正;
              const 持续移除命中 = 持续状态移除阻断状态附着(目标对象, 状态名, 新状态条目, attacker);
              if (持续移除命中) {
                result.desc += ` [持续状态移除] ${目标对象 === attacker ? '自身' : 目标名}的[${状态名}]被[${持续移除命中.key}]拦截。`;
                return;
              }
              目标对象.状态效果[状态名] = 新状态条目;
              目标对象.final = buildCombatFinalStats(目标对象);
              if (目标对象.召唤键) 同步召唤单位镜像(目标对象);
              新状态条目.__状态来源键 = 记录状态来源登记(combatData, {
                stateName: 状态名,
                targetName: 目标对象?.name || 目标对象?.名称 || '',
                sourceActorName: attacker?.name || attacker?.名称 || '',
                sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                sourceActionType: playerAction?.action_type || playerAction?.type || '',
                sourceRound: Number(combatData?.回合 || 0),
                duration: 下一持续,
                effectSummary: 构建状态结果效果摘要(新状态条目.战斗效果 || {}),
                driverAttr: String(读取状态施加默认驱动属性_战斗(状态施加效果) || '').trim(),
              });
              写入战斗事件账本(combatData, {
                eventKind: 'state_apply',
                round: Number(combatData?.回合 || 0),
                actorName: attacker?.name || attacker?.名称 || '',
                targetName: 目标对象?.name || 目标对象?.名称 || '',
                actionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                actionType: playerAction?.action_type || playerAction?.type || '',
                sourceActionName: playerAction.skill?.name || playerAction.skill?.魂技名 || '',
                sourceRound: Number(combatData?.回合 || 0),
                result: 'applied',
                applicationId: 新状态条目.__状态来源键,
                duration: 下一持续,
                effectSummary: 构建状态结果效果摘要(新状态条目.战斗效果 || {}),
                driverAttr: String(读取状态施加默认驱动属性_战斗(状态施加效果) || '').trim(),
                meta: {
                  stateName: 状态名,
                  successRate: Number(驱动成功率 || 1),
                  roll: Number(状态附着检定 || 0),
                  successRateReason: 状态成功率审计.reason,
                  successRateBreakdown: 格式化状态附着成功率拆分(状态成功率审计, 状态附着检定, true),
                },
              });
              result.desc += ` 并对${目标对象 === attacker ? '自身' : 目标名}施加了[${状态名}]状态！`;
              已落地 = true;
            });
            return 已落地;
          };
          主要状态施加效果列表.slice(1).forEach(状态施加效果 => 落地额外状态施加(状态施加效果));
          附带状态施加效果列表.forEach(状态施加效果 => 落地额外状态施加(状态施加效果, { 需要主原型成立: true }));

          const 本次命中成立 =
            Number(result.totalProjectedDamage || result.dmg || 0) > 0 ||
            (Array.isArray(result.targetResults) && result.targetResults.some(entry => Number(entry?.damage || 0) > 0)) ||
            (pState.状态名称 && pState.状态名称 !== '无' && String(result.desc || '').includes(`施加了[${pState.状态名称}]`));
          const 本次效果生效成立 = 本次命中成立 || !hasDirectDamageEffect || targetsFriendlySkill;
          const immediateSideEffects = getBattleSkillSideEffectList(playerAction.skill).filter(item => {
            const 触发时机 = String(item?.触发时机 || '').trim();
            if (触发时机 === '效果生效后') return 本次效果生效成立;
            if (触发时机 === '命中后') return 本次命中成立;
            return false;
          });
          if (immediateSideEffects.length > 0) {
            const sideEffectTargetContext = resolveSkillTargetContext(playerAction.skill, attacker, defender, combatData);
            const sideEffectTargets = Array.isArray(sideEffectTargetContext?.targetSet)
              ? sideEffectTargetContext.targetSet.filter(Boolean)
              : [defender].filter(Boolean);
            const sideEffectLogs = [];
            const 副作用落地选项 = {
              允许食物增幅副作用叠加: 允许食物增幅副作用叠加(playerAction.skill),
            };
            immediateSideEffects.forEach(item => {
              resolveBattleSideEffectTargets(item, attacker, sideEffectTargets).forEach(targetChar => {
                const 副作用目标名 = String(targetChar?.name || targetChar?.名称 || '').trim();
                const 副作用目标文本 = String(item?.目标 || '').trim();
                const 副作用友方目标 =
                  副作用目标文本 === '自身' ||
                  副作用目标文本 === '友方' ||
                  副作用目标文本 === '友方单体' ||
                  副作用目标文本 === '友方群体' ||
                  targetChar === attacker;
                if (
                  !副作用友方目标 &&
                  副作用目标名 &&
                  (目标本次明确规避主效果(targetChar) ||
                    (hasDirectDamageEffect &&
                      !主原型成立目标集合.has(副作用目标名) &&
                      !主原型命中目标集合.has(副作用目标名)))
                ) return;
                const 被时光回溯规避 = Array.isArray(result.targetResults) && result.targetResults.some(entry =>
                  entry?.target &&
                  targetChar &&
                  isCombatUnitIdentityMatch(entry.target, targetChar) &&
                  entry?.被时光回溯规避 === true,
                );
                if (被时光回溯规避) return;
                applyBattleSideEffectState(targetChar, item, playerAction.skill.name || '技能', sideEffectLogs, combatData, 副作用落地选项);
              });
            });
            if (sideEffectLogs.length > 0) {
              result.desc += ` ${sideEffectLogs.join(' ')}`;
            }
          }

          if (玩家临时技能已生效 && 玩家临时技能使用权?.提交) result.desc += 玩家临时技能使用权.提交();
          if (NPC临时技能已生效 && NPC临时技能使用权?.提交) result.desc += NPC临时技能使用权.提交();
          result.interrupt_bonus = attackerInterruptBonus;
          return result;
        }

        function buildStrategicCandidates(
          defender,
          attacker,
          combatData,
          playerAction,
          ratio,
          availableSkills,
          allyTeam,
          makeNpcAction,
        ) {
          const combatType = combatData.战斗类型 || '突发遭遇';
          const isDeadlyFight = combatType === '死战' || combatType === '突发遭遇';
          const 原始等级差 = (attacker.lv || 1) - (defender.lv || 1);
          const playerPower = Number(getPrimaryDamageEffect(playerAction.skill)?.威力倍率 || 0) || 0;
          const isChargingHighThreat =
            !!attacker.蓄力技能 ||
            playerAction.action_type === '蓄力挨打' ||
            ((playerAction.cast_time || 0) >= 20 && playerPower >= 120);
          const 实力压制威胁 = 计算实力压制威胁(attacker, defender, combatData);
          const lvDiff = 实力压制威胁.已识别 ? 原始等级差 : 0;
          const 开局绝境窗口 = isDeadlyFight && 实力压制威胁.是严重威胁;
          const activeBuffs = Object.keys(defender.状态效果 || {});
          const 己方团队 = dedupeCombatTargetList([defender, ...((allyTeam || []).filter(Boolean))]).filter(isCombatUnitAlive);
          const 护援目标 = 选择护援队友(defender, 己方团队, attacker, {
            isChargingHighThreat,
            实力压制威胁,
          });
          const behaviorState = buildBattleStateContext(defender, attacker, combatData, {
            combatType,
            isDeadlyFight,
            ratio,
            isChargingHighThreat,
            实力压制威胁,
            开局绝境窗口,
            playerPower,
            allyTeam: 己方团队,
            alliesCount: 己方团队.length,
            团队护援压力: Number(护援目标?.压力 || 0),
            团队护援目标名: String(护援目标?.队友?.name || ''),
          });
          behaviorState.局势评估 = 评估战斗行为局势(defender, attacker, behaviorState);

          const strategicBranches = [];

          if (defender.武魂融合技) {
            Object.entries(defender.武魂融合技).forEach(([fusionName, fusionSkill]) => {
              if (!isFusionSkillAvailable(defender, fusionSkill, allyTeam)) return;

              let weight = 0;
              if (isDeadlyFight) {
                if (lvDiff >= 5) weight += 开局绝境窗口 ? 120 : 80;
                if (开局绝境窗口) weight += 80;
                if (isChargingHighThreat) weight += 60;
                if (getCombatHpRatio(defender) < 0.5) weight += 30;
              } else if (getCombatHpRatio(defender) < 0.4 || isChargingHighThreat) {
                weight += 50;
              }

              strategicBranches.push({
                name: '武魂融合技',
                weight: adjustBehaviorWeight('武魂融合技', weight, defender, attacker, behaviorState),
                __预览技能: buildFusionCombatSkill(fusionSkill, fusionName, defender),
                build() {
                  const skill = buildFusionCombatSkill(fusionSkill, fusionName, defender);
                  skill.name = `武魂融合技·${skill.name}`;
                  return makeNpcAction(
                    '武魂融合技',
                    `[绝地反击] 面对${lvDiff >= 5 ? '不可战胜的强敌' : '巨大的压力'}，${buildFusionCastNarration(fusionSkill, defender.name || 'NPC')}`,
                    skill,
                  );
                },
              });
            });
          }

          const trueBodySkill = availableSkills.find(skill => {
            const stateName = getPrimaryStateName(skill) || '';
            return /真身/.test(skill.name || '') || /真身/.test(stateName);
          });
          if (trueBodySkill) {
            const 真身记忆 = ensureActorDecisionMemory(defender);
            const trueBodyState = getPrimaryStateName(trueBodySkill) || '无';
            const 已开启真身 = activeBuffs.some(状态名 => {
              const 文本 = String(状态名 || '');
              return (
                /真身|第七魂环/.test(文本) ||
                (trueBodyState !== '无' && (文本.includes(trueBodyState) || trueBodyState.includes(文本)))
              );
            });
            const 近期已开真身 =
              Number(真身记忆.recent_actions?.开启真身 || 0) > 0 ||
              Number(真身记忆.recent_actions?.[trueBodySkill.name || trueBodySkill.技能名称 || ''] || 0) > 0;
            if (!已开启真身 && !近期已开真身) {
              let weight = 8;
              const 是早期稳态真身 =
                Number(behaviorState.round || 0) <= 4 &&
                Math.abs(原始等级差) <= 4 &&
                getCombatHpRatio(defender) > 0.85 &&
                !开局绝境窗口 &&
                !isChargingHighThreat;
              const 真身战况推力 = Math.max(
                开局绝境窗口 ? 1 : 0,
                isChargingHighThreat ? 0.82 : 0,
                getCombatHpRatio(defender) < 0.35 ? 0.9 : getCombatHpRatio(defender) < 0.55 ? 0.55 : 0,
                Math.min(0.25, Number(behaviorState.round || 0) / 12),
              );
              if (真身战况推力 > 0.35 && isDeadlyFight) {
                if (lvDiff >= 0) weight += 开局绝境窗口 ? 95 : 45;
                if (开局绝境窗口) weight += 70;
                if (isChargingHighThreat) weight += 60;
                if (Number(behaviorState.round || 0) >= 3) weight += 25;
              } else {
                weight += Math.floor(真身战况推力 * 50);
              }
              weight = 按资源压力调整权重(weight, trueBodySkill, defender, {
                是真身: true,
                战况推力: 真身战况推力,
                回合: behaviorState.round,
              });
              if (是早期稳态真身)
                weight = Math.min(weight, 4);

              strategicBranches.push({
                name: '开启真身',
                weight: adjustBehaviorWeight('开启真身', weight, defender, attacker, behaviorState),
                __早期低概率: 是早期稳态真身,
                build() {
                  const sustainConfig = resolveActionSustainConfig(defender, '开启真身', trueBodySkill, trueBodyState);
                  applyStateToCharacter(defender, getPrimaryStateEffect(trueBodySkill), trueBodySkill.name, true);
                  if (sustainConfig) registerSustainEffect(defender, `truebody:${trueBodySkill.name}`, sustainConfig);
                  defender.状态效果 = defender.状态效果 || {};
                  defender.状态效果[`第七魂环真身:${trueBodySkill.name}`] = {
                    名称: `第七魂环真身:${trueBodySkill.name}`,
                    来源: trueBodySkill.name,
                    持续回合: Math.max(2, Number(getPrimaryStateEffect(trueBodySkill)?.持续回合 || 3)),
                    描述: '武魂真身已开启，近期不会重复释放同一真身。',
                  };
                  return makeNpcAction(
                    '开启真身',
                    `[质变底牌] NPC仰天长啸，第七魂环闪耀，直接释放了[${trueBodySkill.name}]！`,
                    trueBodySkill,
                  );
                },
              });
            }
          }

          const lifeFireSkill = defender.血脉之力?.技能?.['点燃生命之火'];
          if (
            isDeadlyFight &&
            lifeFireSkill &&
            !defender.血脉之力?.生命之火 &&
            behaviorState.isDesperateNoEscape
          ) {
            let weight = 0;
            if (lvDiff >= 10 && getCombatHpRatio(defender) < 0.8) weight += 60;
            else if (getCombatHpRatio(defender) < 0.3) weight += 80;
            if (isChargingHighThreat) weight += 25;

            let fireLog = `[绝命搏杀] 面对${lvDiff >= 10 ? '令人绝望的实力差距' : '生死绝境'}，NPC做出了极其疯狂的举动！强行点燃了生命之火做殊死一搏！`;
            if (!behaviorState.canFlee) {
              fireLog = `[困兽犹斗] 被迫背水一战，已经退无可退！NPC被逼入绝境，发出一声绝望的狂吼，强行点燃了生命之火，誓要同归于尽！`;
            }
            strategicBranches.push({
              name: '点燃生命之火',
              weight: adjustBehaviorWeight('点燃生命之火', weight, defender, attacker, behaviorState),
              build() {
                const sustainConfig = resolveActionSustainConfig(defender, '点燃生命之火', lifeFireSkill, '');
                defender.血脉之力.生命之火 = true;
                if (sustainConfig)
                  registerSustainEffect(defender, `life_fire:${lifeFireSkill.name || '点燃生命之火'}`, sustainConfig);
                return makeNpcAction('点燃生命之火', fireLog, lifeFireSkill);
              },
            });
          }

          const hasDomain =
            (defender.当前领域 || '无') === '无' && (defender.装备?.斗铠?.等级 >= 3 || defender.men_max >= 30000);
          if (hasDomain) {
            let weight = 0;
            if (getCombatHpRatio(defender) >= 0.9) weight += 40;
            if (getCombatHpRatio(defender) < 0.5) weight += 80;
            if (isChargingHighThreat) weight += 30;

            strategicBranches.push({
              name: '展开领域',
              weight: adjustBehaviorWeight('展开领域', weight, defender, attacker, behaviorState),
              build() {
                const actionType = defender.装备?.斗铠?.等级 >= 3 ? '展开斗铠领域' : '展开精神领域';
                const sustainConfig = resolveActionSustainConfig(defender, actionType, null, '');
                defender.当前领域 =
                  actionType === '展开斗铠领域'
                    ? defender.装备?.斗铠?.等级 >= 4
                      ? '【四字斗铠领域】全开(未定)'
                      : '【三字斗铠领域】全开(未定)'
                    : '【精神领域】全开';
                if (sustainConfig) registerSustainEffect(defender, `domain:${actionType}`, sustainConfig);
                return makeNpcAction(actionType, `[主场展开] NPC不愿陷入被动，果断张开领域法则抢占先机！`);
              },
            });
          }

          let bestSpirit = null;
          let maxSpiritLv = 0;
          取角色武魂条目_战斗(defender).forEach(([, sp]) => {
            取武魂魂灵条目_战斗(sp).forEach(([, ss]) => {
              if (ss.状态 === '活跃' && ss.战力面板?.对标等级 > maxSpiritLv) {
                maxSpiritLv = ss.战力面板.对标等级;
                bestSpirit = ss;
              }
            });
          });
          if (bestSpirit && defender.men >= 500) {
            let weight = 0;
            const playerLv = attacker.lv || 1;
              if (maxSpiritLv >= playerLv - 15) {
                weight += 20;
                if (maxSpiritLv >= playerLv) weight += 40;
                if (getCombatHpRatio(defender) >= 0.9) weight += 30;
              }
            strategicBranches.push({
              name: '召唤魂灵',
              weight: adjustBehaviorWeight('召唤魂灵', weight, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '召唤魂灵',
                  `[战术召唤] NPC消耗精神力，将【${bestSpirit.表象名称}】实体化召唤至战场！(对标等级: ${formatBattleCultivationLevelText(maxSpiritLv, '0')})`,
                );
              },
            });
          }

          const hasArmor = defender.装备?.斗铠?.等级 > 0 && defender.装备?.斗铠?.装备状态 !== '已装备';
          const hasMech = 战斗机甲可召唤(defender);
          if (hasArmor || hasMech) {
            let armorLv = defender.装备?.斗铠?.等级 || 0;
            let isRejected = defender.装备?.斗铠?._已排异 || false;
            let minQ = Infinity;
            let pCount = 0;

            if (hasArmor && defender.装备?.斗铠?.parts) {
              Object.values(defender.装备.斗铠.parts).forEach(part => {
                if (part.状态 !== '未打造' && part.状态 !== '重创') {
                  if (part.品质系数 < minQ) minQ = part.品质系数;
                  pCount++;
                }
              });
            }

            let armorCast = Math.max(0, 20 - armorLv * 5);
            if (armorLv === 1 && !isRejected && minQ > 1.2 && pCount > 0) armorCast = Math.max(0, armorCast - 5);
            const mechLv = defender.装备?.机甲?.等级 || '无';
            const mechCast = mechLv === '红级' ? 0 : 50;
            const equipCastTime = hasArmor ? armorCast : mechCast;

            let weight = 0;
            let decisionLog = '';
            if (equipCastTime === 0) weight += 500;
            if (combatType === '死战' && getCombatHpRatio(defender) >= 0.95) {
              weight += 200;
              decisionLog = '面临生死决战，NPC不敢托大，开局便试图引动装备护体！';
            }
            if (playerPower >= 150) {
              weight += 80;
              decisionLog = '感受到对方攻击中蕴含的毁灭性波动，NPC深知肉体无法硬抗，强行尝试穿戴装备！';
            }
            if ((playerAction.cast_time || 0) > equipCastTime || ratio >= 1.2) {
              weight += 60;
              if (!decisionLog) decisionLog = 'NPC察觉到对方前摇破绽，从容地开始释放装备。';
            }
            if (getCombatHpRatio(defender) < 0.4) {
              weight += 50;
              if (!decisionLog) decisionLog = '身受重创的NPC陷入绝境，拼着被打断的风险也要强行穿戴装备！';
            }

            weight -= equipCastTime;
            const 装备必要窗口 =
              combatType === '死战' ||
              playerPower >= 150 ||
              isChargingHighThreat ||
              getCombatHpRatio(defender) < 0.45 ||
              behaviorState?.threatProfile?.severeDamage === true ||
              behaviorState?.threatProfile?.lethalRisk === true;
            if (!装备必要窗口) {
              const 常规上限 = equipCastTime === 0 ? 18 : 8;
              weight = Math.min(weight, 常规上限);
              if (!decisionLog) decisionLog = 'NPC保留装备节奏，只在无压力窗口试探性强化防御。';
            } else if (equipCastTime === 0) {
              weight = Math.min(weight, 120);
            }
            strategicBranches.push({
              name: '穿戴装备',
              weight: adjustBehaviorWeight('穿戴装备', weight, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '穿戴装备',
                  `[战术决断] ${decisionLog || 'NPC准备强化自身防御强度。'} (前摇: ${equipCastTime})`,
                  {
                    name: hasArmor ? '斗铠附体' : '召唤机甲',
                    技能分类: '辅助',
                    前摇: equipCastTime,
                  },
                  { equip_target: hasArmor ? 'armor' : 'mech' },
                );
              },
            });
          }

          const hasMechOrArmor = 战斗机甲已装备(defender) || defender.装备?.斗铠?.装备状态 === '已装备';
          if (
            behaviorState.canFlee &&
            combatType !== '擂台切磋' &&
            getCombatHpRatio(defender) < 0.15 &&
            !hasMechOrArmor
          ) {
            strategicBranches.push({
              name: '亡命奔逃',
              weight: adjustBehaviorWeight('亡命奔逃', 50, defender, attacker, behaviorState),
              build() {
                return makeNpcAction('亡命奔逃', `[濒死溃逃] NPC身受重创，丧失了战意，放弃防守转身亡命奔逃！`);
              },
            });
          } else if (!behaviorState.canFlee && getCombatHpRatio(defender) < 0.15) {
            // 如果濒死但无法逃跑，给肉体兜底和死斗加一点补救权重
            behaviorState.isDesperateNoEscape = true;
          }

          strategicBranches.forEach(候选 => {
            if (!候选 || 候选.__预览技能) return;
            if (候选.name === '开启真身') 候选.__预览技能 = trueBodySkill || null;
            else if (候选.name === '点燃生命之火') 候选.__预览技能 = lifeFireSkill || null;
            else if (候选.name === '穿戴装备') 候选.__预览技能 = normalizeSkillData({
              name: hasArmor ? '斗铠附体' : '召唤机甲',
              技能分类: '辅助',
              消耗: '无',
              前摇: hasArmor ? Math.max(0, 20 - Number(defender.装备?.斗铠?.等级 || 0) * 5) : 50,
            }, hasArmor ? '斗铠附体' : '召唤机甲');
          });

          return { strategicBranches, behaviorState, combatType, activeBuffs };
        }

        function buildNpcSkillCandidateContext(
          defender,
          attacker,
          playerAction,
          availableSkills,
          behaviorState,
          activeBuffs,
          isLowHealth,
          选项 = {},
        ) {
          const 主动回合 = 选项.主动回合 === true;
          const 防御者系别 = 读取规划单位系别(defender);
          const 攻击者系别 = 读取规划单位系别(attacker);
          const attackerSpeed = attacker.agi - (playerAction.cast_time || 0) * 10;
          const isChargingHighThreat = !!behaviorState?.isChargingHighThreat;
          const threatProfile = estimateIncomingActionThreat(attacker, defender, playerAction, behaviorState?.combatData);
          const 读取规划同队单位 = unit => {
            const combatData = behaviorState?.combatData || {};
            const 玩家侧 = dedupeCombatTargetList((combatData?.参战者?.team_player || []).filter(Boolean)).filter(isCombatUnitAlive);
            const 敌方侧 = dedupeCombatTargetList((combatData?.参战者?.team_enemy || []).filter(Boolean)).filter(isCombatUnitAlive);
            if (玩家侧.some(member => isCombatUnitIdentityMatch(member, unit?.name || unit))) return 玩家侧;
            if (敌方侧.some(member => isCombatUnitIdentityMatch(member, unit?.name || unit))) return 敌方侧;
            return [unit].filter(Boolean);
          };
          const 读取状态移除规划目标组 = effect => {
            const 目标文本 = String(effect?.目标 || '').trim();
            const 状态文本 = String(effect?.状态 || '').trim();
            const 友方列表 = 读取规划同队单位(defender);
            const 敌方列表 = 读取规划同队单位(attacker);
            if (目标文本.includes('自身')) return [{ 目标立场: '友方', 目标列表: [defender] }];
            if (目标文本.includes('群体')) {
              if (状态文本 === '任意负面') return [{ 目标立场: '友方', 目标列表: 友方列表 }];
              if (状态文本 === '任意增益') return [{ 目标立场: '敌方', 目标列表: 敌方列表 }];
              return [
                { 目标立场: '友方', 目标列表: 友方列表 },
                { 目标立场: '敌方', 目标列表: 敌方列表 },
              ];
            }
            return [{ 目标立场: '敌方', 目标列表: [attacker] }];
          };
          const 状态依赖技能当前可执行 = skill => {
            const effects = getSkillEffects(skill, { 行为规划: true });
            if (effects.some(effect => String(effect?.原型 || '').trim() === '成长修正')) return false;
            const hasState = (unit, effect = {}) => countConditionsByPrototypeFilter(unit, effect) > 0;
            return effects.every(effect => {
              const 原型名 = String(effect?.原型 || '').trim();
              if (原型名 === '状态移除') {
                return 读取状态移除规划目标组(effect).some(group =>
                  (group.目标列表 || []).some(target => hasState(target, effect)),
                );
              }
              if (原型名 === '状态交换') {
                return true;
              }
              if (原型名 === '状态转移') {
                const 来源 = String(effect?.来源 || '自身').trim();
                const 去向 = String(effect?.去向 || '目标').trim();
                if (!['自身', '目标', '友方', '敌方'].includes(来源) || !['自身', '目标', '友方', '敌方'].includes(去向)) return false;
                const 端点列表 = 端点 => {
                  if (端点 === '自身') return [defender].filter(Boolean);
                  if (端点 === '目标') return 来源 === 去向 ? 读取规划同队单位(attacker) : [attacker].filter(Boolean);
                  if (端点 === '友方') return 友方列表;
                  if (端点 === '敌方') return 敌方列表;
                  return [];
                };
                const 来源列表 = 端点列表(来源);
                const 去向列表 = 端点列表(去向);
                return 来源列表.some(source =>
                  hasState(source, effect) &&
                  去向列表.some(receiver => receiver && !isCombatUnitIdentityMatch(receiver, source?.name || source)),
                );
              }
              return true;
            });
          };
          const 反高速窗口 = 评估反高速应对窗口(defender, attacker, playerAction, behaviorState?.combatData);
          const validSkills = availableSkills.filter(skill => {
            if (单位已有同名维持技能(defender, skill)) return false;
            const castTime = getSkillCastTime(skill);
            const npcSpeed = defender.agi - castTime * 10;
            const cost = parseSkillCostForChar(skill, defender, {
              actor: defender,
              caster: defender,
              attacker: defender,
              target: attacker,
              defender: attacker,
              action: { action_type: '释放魂技', type: '技能', skill },
              skill,
              combatData: behaviorState?.combatData,
              当前行动: '释放魂技',
            });
            const 反高速可应对 = 反高速窗口.成立 && (castTime <= 12 || /群体|全场/.test(String(getSkillTarget(skill) || '')));
            return cost.canCast && 状态依赖技能当前可执行(skill) && (
              主动回合 ||
              npcSpeed > Math.max(1, attackerSpeed) * 0.8 ||
              反高速可应对
            );
          });

          function pickSkillWithWeight(skills) {
            const weighted = (skills || []).map(skill => {
              const 规划技能 = 构建行为规划技能(skill, attacker, defender, behaviorState?.combatData);
              const stateName = getPrimaryStateName(规划技能) || '无';
              const skillType = getSkillType(skill);
              if (stateName !== '无' && ['辅助', '防御'].includes(skillType) && activeBuffs.includes(stateName)) {
                return {
                  skill,
                  规划技能,
                  weight: 6,
                  name: skill.name || '未命名技能',
                  build() {
                    return skill;
                  },
                };
              }

              let weight = 10;
              if (skill.__机制授予主动技能 === true) weight += 55;
              const skillType2 = getSkillType(skill);
              const skillCastTime = getSkillCastTime(skill);
              const 原始技能威力 = Number(getPrimaryDamageEffect(规划技能)?.威力倍率 || 0) || 0;
              const 技能效果列表 = getSkillEffects(规划技能, { 行为规划: true });
              const 状态交换效果列表 = 技能效果列表.filter(effect => String(effect?.原型 || '').trim() === '状态交换');
              const 状态交换规划收益 = 状态交换效果列表.reduce(
                (总和, effect) => 总和 + Math.max(0, 估算状态交换释放权重(effect, defender, attacker, behaviorState)),
                0,
              );
              if (状态交换效果列表.length && 状态交换规划收益 <= 0 && 技能效果列表.every(effect => String(effect?.原型 || '').trim() === '状态交换')) {
                return {
                  skill,
                  规划技能,
                  weight: 0,
                  name: skill.name || '未命名技能',
                  build() {
                    return skill;
                  },
                };
              }
              const 结算修正效果列表 = 技能效果列表.filter(effect => String(effect?.原型 || '').trim() === '结算修正');
              const skillPower = 原始技能威力;
              const 当前技能元素集合 = new Set(normalizeBattleSkillAttributeTokens([
                ...(Array.isArray(规划技能?.附带属性) ? 规划技能.附带属性 : [规划技能?.附带属性]),
                getBattleSkillAttributeSummary(规划技能 || {}).显示元素,
                ...((getBattleSkillElementStructure(规划技能 || {}).核心元素) || []),
                ...((getBattleSkillElementStructure(规划技能 || {}).驱动元素) || []),
                ...((getBattleSkillElementStructure(规划技能 || {}).触发元素) || []),
              ]));
              const 结算修正元素匹配 = effect => {
                const 限定元素列表 = normalizeBattleSkillAttributeTokens(effect?.限定元素);
                if (!限定元素列表.length) return 1;
                return 限定元素列表.some(元素 => 当前技能元素集合.has(元素)) ? 1 : 0;
              };
              const 结算修正强度 = (结算名, 方向 = '任意') =>
                结算修正效果列表.reduce((总和, effect) => {
                  if (String(effect?.结算 || '').trim() !== 结算名) return 总和;
                  if (!结算修正元素匹配(effect)) return 总和;
                  let 数值 = 读取战斗数值正负(effect?.数值);
                  if (!读取战斗数值是否百分比(effect?.数值)) {
                    if (结算名 === '消耗') {
                      const 总消耗 = Math.max(1, Number(cost.reqSp || 0) + Number(cost.reqVit || 0) + Number(cost.reqMen || 0));
                      数值 = 数值 / 总消耗;
                    } else if (结算名 === '前摇') {
                      数值 = 数值 / Math.max(1, Number(skillCastTime || skill?.前摇 || 1));
                    }
                  }
                  if (方向 === '正向' && 数值 <= 0) return 总和;
                  if (方向 === '负向' && 数值 >= 0) return 总和;
                  return 总和 + Math.max(0, Math.abs(数值));
                }, 0);
              const 有未匹配限定结算修正 = 结算修正效果列表.some(effect => effect?.限定元素 !== undefined && !结算修正元素匹配(effect));
              const 有结算修正 = 结算名 => 结算修正效果列表.some(effect => String(effect?.结算 || '').trim() === 结算名);
              const 伤害预结算 = estimateIncomingActionThreat(
                defender,
                attacker,
                { action_type: '技能', type: '技能', cast_time: skillCastTime, skill: 规划技能 },
                behaviorState?.combatData,
              );
              const 预结算伤害比例 = Number(伤害预结算.projectedDamageRatio || 0);
              if (isSupportLikeSkill(skill)) {
                const targetForCost = String(getSkillTarget(skill)).includes('敌方') ? attacker : defender;
                skill.__targetForSupportCost = targetForCost;
              }
              const cost = parseSkillCostForChar(skill, defender, {
                actor: defender,
                caster: defender,
                attacker: defender,
                target: String(getSkillTarget(skill)).includes('敌方') ? attacker : defender,
                defender: String(getSkillTarget(skill)).includes('敌方') ? attacker : defender,
                action: { action_type: '释放魂技', type: '技能', skill },
                skill,
                combatData: behaviorState?.combatData,
                当前行动: '释放魂技',
              });
              delete skill.__targetForSupportCost;
              const spRatio = cost.reqSp > 0 ? cost.reqSp / Math.max(1, defender.sp) : 0;
              const vitRatio = cost.reqVit > 0 ? cost.reqVit / Math.max(1, defender.sta || defender.体力 || 1) : 0;
              const 副作用自损风险 = getBattleSkillSideEffectList(skill).reduce((总和, 条目) => {
                return 总和 + estimateBattleSideEffectRisk(条目, defender);
              }, 0);

              if (防御者系别 === '强攻系') {
                if (skillType2 === '输出') weight += 40 + Math.floor(skillPower / 10);
                if (skillType2 === '防御') weight += 20;
              } else if (防御者系别 === '敏攻系') {
                if (skillType2 === '输出' && skillCastTime <= 15) weight += 60;
                if (skillType2 === '输出' && skillCastTime > 20) weight -= 30;
              } else if (防御者系别 === '防御系') {
                if (skillType2 === '防御') weight += 80;
                if (skillType2 === '输出') weight += 10;
              } else if (防御者系别 === '控制系') {
                if (skillType2 === '控制') weight += 80;
              } else if (['辅助系', '治疗系', '食物系', '召唤系'].includes(防御者系别)) {
                if (skillType2 === '辅助') weight += 24;
                if (skillType2 === '防御') weight += 18;
                if (skillType2 === '控制') weight += 12;
                if (skillType2 === '输出') weight -= 12;
              }

              const isUltimate =
                skillPower >= 200 || 伤害预结算.severeDamage === true || /真身|第八魂技|第九魂技|武魂融合技|生命之火/.test(skill.name || '');
              if (isUltimate) {
                const enemyHpRatio = getCombatHpRatio(attacker);
                const myHpRatio = getCombatHpRatio(defender);
                if (behaviorState.combatType === '擂台切磋') weight -= 30;
                else {
                  if (防御者系别 === '强攻系' && enemyHpRatio < 0.65) weight += 30;
                  if (伤害预结算.lethalRisk === true) weight += 80;
                  else if (enemyHpRatio < 0.4) weight += 60;
                  if (myHpRatio < 0.3) weight += 50;
                  if (enemyHpRatio > 0.8 && myHpRatio > 0.8) weight -= 20;
                }
              }
              if (伤害预结算.targetHitsDefender) {
                if (伤害预结算.lethalRisk === true) weight += 70;
                else if (伤害预结算.severeDamage === true) weight += 42;
                else if (伤害预结算.moderateDamage === true) weight += 24;
                else if (预结算伤害比例 > 0) weight += Math.min(18, Math.floor(预结算伤害比例 * 70));
              }

              weight -= Math.floor(skillCastTime / 5);
              if (!isLowHealth) {
                if (spRatio > 0.5 || vitRatio > 0.5) weight = Math.floor(weight * 0.3);
                else if (spRatio > 0.3 || vitRatio > 0.3) weight = Math.floor(weight * 0.7);
              } else if (vitRatio > 0.5) {
                weight = Math.floor(weight * 0.5);
              }
              if (副作用自损风险 > 0) {
                weight -= 副作用自损风险 >= 3 ? 120 : Math.ceil(副作用自损风险 * (isLowHealth ? 70 : 42));
              }

              const summary = deriveBattleSummaryFromEffects(规划技能);
              const mainType = inferMainTypeFromEffects(规划技能) || '无';
              const 来源类别 = getBattleSkillSourceCategory(规划技能);
              const 战斗目标类型 = inferSkillPrimaryTargetKind(规划技能);
              const enemyHpRatio = getCombatHpRatio(attacker);
              const selfHpRatio = getCombatHpRatio(defender);
              const enemySpRatio = Math.max(0, Number(attacker.sp || 0)) / Math.max(1, Number(attacker.sp_max || 1));
              const enemyMenRatio = Math.max(0, Number(attacker.men || 0)) / Math.max(1, Number(attacker.men_max || 1));
              const fieldActive = Object.keys(defender.状态效果 || {}).some(k => /领域|场地|结界|召唤/.test(k));
              const enemySnapshot = buildConditionTacticalSnapshot(attacker);
              const selfSnapshot = buildConditionTacticalSnapshot(defender);
              const allyCount = behaviorState.alliesCount || 1;
              const selfSpRatio = Math.max(0, Number(defender.sp || 0)) / Math.max(1, Number(defender.sp_max || 1));
              const selfMenRatio = Math.max(0, Number(defender.men || 0)) / Math.max(1, Number(defender.men_max || 1));
              const 技能规划收益 = 评估技能规划净收益(规划技能, {
                actor: defender,
                primaryTarget: attacker,
                behaviorState: { ...behaviorState, threatProfile: 伤害预结算, observedTargetAction: playerAction },
              });
              const 规划净收益 = Number(技能规划收益.净收益 || 0);
              const 是全场规划 = 战斗目标类型 === '全场' || summary.目标规模 === '全场';
              const 对手攻击压制 =
                ['强攻系', '敏攻系'].includes(攻击者系别) &&
                (threatProfile.incomingAttackIntent === true ||
                  Number(behaviorState.round || 0) >= 3 ||
                  getCombatHpRatio(defender) < 0.82);
              const hasFriendlyGrantable = skillCanGrantFriendlyMechanism(规划技能);
              const hasHostileSemantic = skillTargetsEnemySide(规划技能) && 技能包含运行语义(规划技能, '敌对');
              const 是团队保护 = 是团队保护技能(规划技能, { summary });
              const 是保命技能 = 技能具有保命倾向(规划技能, { summary });
              const 是规则压制 = 技能具有规则压制倾向(规划技能, { summary, mainType });
              const hasAntiHeal = isBattleSkillAntiHealProfile(规划技能, { summary });
              const defenseNature = String(summary.防御性质 || '无');
              const hasSharedVision = isBattleSkillSharedVisionProfile(规划技能);
              const hasCounter = defenseNature === '反制';
              const hasBlock = defenseNature === '免伤';
              const hasShield = defenseNature === '护盾';
              const hasDeathSave = defenseNature === '免死';
              const hasRevive = summary.回复性质 === '复活机制' || hasBattleSkillRuntimeConsumer(规划技能, ['revive']);
              const hasClone = defenseNature === '分身';
              const hasInvincible = defenseNature === '无敌';
              const hasReflect = defenseNature === '反射';
              const hasDamageShare = defenseNature === '分摊';
              const hasSkillSeal = isBattleSkillSealProfile(规划技能);
              const hasHealInvert = isBattleSkillHealInvertProfile(规划技能);
              const hasDotDetonate = isBattleSkillDotDetonateProfile(规划技能);
              const hasShieldBreak = isBattleSkillShieldBreakProfile(规划技能);
              const hasResourceDrain = isBattleSkillResourceDrainProfile(规划技能);
              const hasResourceRefeed = isBattleSkillResourceRefeedProfile(规划技能);
              const hasMechanismSuppress = isBattleSkillMechanismSuppressProfile(规划技能);
              const suppressTargets = hasMechanismSuppress ? 读取战斗技能抹消战术标签(规划技能) : [];
              const hasExecute = isBattleSkillExecuteProfile(规划技能, { summary });
              const hasResourceRecover = getSkillEffects(规划技能, { 行为规划: true }).some(effect =>
                isBattleRecoverEffect(effect, ['sp', 'men']),
              ) || hasResourceRefeed;
              const hasHeal = 技能效果列表.some(effect => isBattleRecoverEffect(effect, ['vit']));
              const hasDotPressure = isBattleSkillDotPressureProfile(规划技能);
              const hasDelayBurst = hasBattleSkillRuntimeConsumer(规划技能, ['delay_burst']);
              const hasVolatile = hasBattleSkillRuntimeConsumer(规划技能, ['self_random_variance']);
              const hasReflectiveConvert = hasBattleSkillRuntimeConsumer(规划技能, ['damage_to_heal', 'heal_to_damage']);
              const 有复制机制 = hasBattleSkillRuntimeConsumer(规划技能, ['copy']);
              const 有炸环 = hasBattleSkillRuntimeConsumer(规划技能, ['ring_burst_gain']);
              const 时光回溯效果列表 = 读取技能时光回溯效果列表(规划技能, {
                actor: defender,
                caster: defender,
                attacker: defender,
                target: attacker,
                defender: attacker,
                action: { action_type: '释放魂技', type: '技能', skill: 规划技能 },
                skill: 规划技能,
                combatData: behaviorState?.combatData,
                当前行动: '释放魂技',
              });
              const 有主动时光回溯 = 时光回溯效果列表.some(effect => String(effect?.发动方式 || '').trim() === '主动');
              const 有被动时光回溯 = 时光回溯效果列表.some(effect => String(effect?.发动方式 || '被动').trim() !== '主动');
              const 有时光回溯 = 有主动时光回溯 || 有被动时光回溯;
              const 有气运控制 = hasBattleSkillRuntimeConsumer(规划技能, ['luck_interference', 'misfortune_backlash']);
              const 有元素封禁 = hasBattleSkillRuntimeConsumer(规划技能, ['element_seal']);
              const penetrationValue = Number(getPrimaryDamageEffect(规划技能)?.防御穿透 || 0);
              const 结算增伤强度 = 结算修正强度('造成伤害', '正向');
              const 结算易伤强度 = 结算修正强度('受到伤害', '正向');
              const 结算承伤下调强度 = 结算修正强度('受到伤害', '负向');
              const 结算反伤强度 = 结算修正强度('反伤');
              const 结算转移强度 = 结算修正强度('伤害转移');
              const 结算分摊强度 = 结算修正强度('伤害分摊');
              const 结算消耗下调强度 = 结算修正强度('消耗', '负向');
              const 结算消耗上调强度 = 结算修正强度('消耗', '正向');
              const 结算前摇下调强度 = 结算修正强度('前摇', '负向');
              const 结算前摇上调强度 = 结算修正强度('前摇', '正向');
              const 结算防御穿透强度 = 结算修正强度('防御穿透');
              const 结算伤害吸收强度 = 结算修正强度('伤害吸收');
              const 结算反击强度 = 结算修正强度('反击');
              const 结算技能效果强度 = 结算修正强度('技能效果');
              const 结算治疗强度 = 结算修正强度('治疗', '正向');
              const actorMemory = ensureActorDecisionMemory(defender);
              const counterPenalty = getActorSkillCounterPenalty(defender, skill.name || skill.技能名称 || '');
              const 技能显示名 = String(skill.name || skill.魂技名 || skill.技能名称 || '').trim();
              const 近期同技次数 = 读取归一行为记忆值_V1(actorMemory.recent_actions || {}, [技能显示名], 0);
              const 重复状态衰减 = 评估敌对重复状态收益衰减(skill, attacker, {
                projectedDamageRatio: 预结算伤害比例,
                recentSameActionCount: 近期同技次数,
              });
              const 状态名称 = String(getPrimaryStateName(规划技能) || skill.name || skill.技能名称 || '').trim();
              const 是否真身技能 = /真身/.test(String(skill.name || skill.技能名称 || 状态名称 || ''));
              const 已有同类增益 =
                !!状态名称 &&
                Object.keys(defender.状态效果 || {}).some(现有状态名 =>
                  String(现有状态名 || '').includes(状态名称) || 状态名称.includes(String(现有状态名 || '')),
                );
              const 是友方增益目标 = ['自身', '友方单体', '友方群体'].includes(战斗目标类型);
              const 是核心自我增益 =
                是友方增益目标 &&
                !已有同类增益 &&
                (mainType === '增益类' ||
                  summary.防御性质 !== '无' ||
                  hasReflect ||
                  hasCounter ||
                  hasBlock ||
                  hasShield ||
                  /真身|霸体|速度提升|威力增幅|伤害反射|增幅|强化/.test(String(skill.name || skill.技能名称 || '')));
              const 近期受挫次数 = Object.values(actorMemory.countered_skills || {}).reduce(
                (总数, 次数) => 总数 + Math.max(0, Number(次数 || 0)),
                0,
              );
              const 久攻不下 =
                (Number(behaviorState.round || 0) >= 3 && enemyHpRatio > 0.86) ||
                (Number(behaviorState.round || 0) >= 2 && 近期受挫次数 >= 2) ||
                (Number(behaviorState.round || 0) >= 5 && enemyHpRatio > 0.72);
              const 状态移除释放权重 = 技能效果列表
                .filter(effect => String(effect?.原型 || '').trim() === '状态移除')
                .reduce((总和, effect) => {
                  return 总和 + 读取状态移除规划目标组(effect).reduce((组总分, group) => 组总分 + 估算状态移除目标列表释放权重(effect, defender, group.目标列表, {
                    目标立场: group.目标立场,
                    高威胁窗口: isChargingHighThreat || threatProfile.severeControl === true,
                    久攻不下,
                  }), 0);
                }, 0);
              const 低耗开局增益 =
                Number(behaviorState.round || 0) <= 2 &&
                cost.reqSp <= Math.max(1, Number(defender.sp || 0)) * 0.18 &&
                cost.reqVit <= Math.max(1, Number(defender.sta || defender.体力 || 1)) * 0.18;
              const 恢复真实需求 =
                (hasHeal && selfHpRatio < 0.68) ||
                (hasResourceRecover && (selfSpRatio < 0.58 || selfMenRatio < 0.58)) ||
                selfSnapshot.hasBadCondition;
              const 反射真实需求 =
                hasReflect &&
                threatProfile.targetHitsDefender === true &&
                threatProfile.incomingAttackIntent === true &&
                (threatProfile.projectedDamageRatio >= 0.08 ||
                  threatProfile.moderateDamage === true ||
                  threatProfile.severeDamage === true ||
                  threatProfile.lethalRisk === true);
              const 防护真实需求 =
                selfHpRatio < 0.72 ||
                isChargingHighThreat ||
                (threatProfile.targetHitsDefender && (!hasReflect || 反射真实需求)) ||
                behaviorState.实力压制威胁?.是威胁 === true ||
                Number(behaviorState.团队护援压力 || 0) >= 45;
              const 增益真实需求 =
                已有同类增益 !== true &&
                (恢复真实需求 ||
                  防护真实需求 ||
                  反射真实需求 ||
                  久攻不下 ||
                  是规则压制 ||
                  (是团队保护 && allyCount > 1 && Number(behaviorState.团队护援压力 || 0) >= 30) ||
                  (是核心自我增益 && 低耗开局增益 && !是否真身技能));
              const 满状态低压 =
                selfHpRatio > 0.86 &&
                selfSpRatio > 0.68 &&
                selfMenRatio > 0.68 &&
                !isChargingHighThreat &&
                !(threatProfile.targetHitsDefender && threatProfile.incomingAttackIntent) &&
                !久攻不下;
              const 真身战况推力 = Math.max(
                behaviorState.开局绝境窗口 === true ? 1 : 0,
                threatProfile.targetHitsDefender && threatProfile.lethalRisk ? 1 : 0,
                threatProfile.targetHitsDefender && (threatProfile.severeDamage || threatProfile.severeControl) ? 0.82 : 0,
                selfHpRatio < 0.35 ? 0.9 : selfHpRatio < 0.55 ? 0.55 : 0,
                久攻不下 ? 0.65 : 0,
                Math.min(0.25, Number(behaviorState.round || 0) / 12),
              );
              const 终局爆发修正强度 = 结算增伤强度 + 结算易伤强度 + 结算防御穿透强度 + 结算修正强度('持续伤害引爆') + (有炸环 ? 0.6 : 0);
              if (终局爆发修正强度 > 0) {
                if (enemyHpRatio < 0.38 || 伤害预结算.lethalRisk || 久攻不下) weight += 18 + Math.floor(终局爆发修正强度 * 70);
                else if (Number(behaviorState.round || 0) <= 1 && skillPower < 140) weight -= 14;
                if (enemySnapshot.hasDefenseBuffed || enemySnapshot.hasShielded) weight += Math.floor(结算防御穿透强度 * 55);
              }
              if (结算消耗下调强度 > 0 || 结算前摇下调强度 > 0) {
                const 准备窗口 = Number(behaviorState.round || 0) <= 2 || selfSpRatio < 0.55 || selfMenRatio < 0.55 || skillCastTime >= 18;
                if (准备窗口) weight += 12 + Math.floor((结算消耗下调强度 + 结算前摇下调强度) * 52);
                else weight += 4;
                if (结算前摇下调强度 > 0 && (isChargingHighThreat || skillCastTime >= 18)) weight += 16;
              }
              if (结算消耗上调强度 > 0 || 结算前摇上调强度 > 0) {
                if (isChargingHighThreat || enemySpRatio < 0.45 || enemyMenRatio < 0.45) weight += 16 + Math.floor((结算消耗上调强度 + 结算前摇上调强度) * 58);
                else weight += 6;
              }
              if (结算承伤下调强度 > 0 || 结算反伤强度 > 0 || 结算转移强度 > 0 || 结算分摊强度 > 0 || 结算反击强度 > 0) {
                if (防护真实需求) weight += 14 + Math.floor((结算承伤下调强度 + 结算反伤强度 + 结算转移强度 + 结算分摊强度 + 结算反击强度) * 64);
                else weight -= 满状态低压 ? 18 : 4;
                if (结算分摊强度 > 0 && allyCount <= 1) weight -= 28;
                if ((结算反伤强度 > 0 || 结算转移强度 > 0 || 结算反击强度 > 0) && threatProfile.incomingAttackIntent) weight += 18;
              }
              if (结算治疗强度 > 0 && (hasHeal || summary.回复性质 !== '无')) {
                weight += selfHpRatio < 0.55 ? 22 + Math.floor(结算治疗强度 * 52) : -10;
              }
              if (结算伤害吸收强度 > 0) {
                const 吸收资源列表 = 结算修正效果列表
                  .filter(effect => String(effect?.结算 || '').trim() === '伤害吸收' && 结算修正元素匹配(effect))
                  .map(effect => String(effect?.吸收资源 || '生命').trim() || '生命');
                const 需要吸收 =
                  吸收资源列表.includes('生命') && selfHpRatio < 0.68 ||
                  吸收资源列表.includes('体力') && selfHpRatio < 0.72 ||
                  吸收资源列表.includes('魂力') && selfSpRatio < 0.62 ||
                  吸收资源列表.includes('精神力') && selfMenRatio < 0.62;
                weight += 需要吸收 ? 14 + Math.floor(结算伤害吸收强度 * 48) : 4;
              }
              if (结算技能效果强度 > 0) {
                const 可识别增幅入口 = mainType !== '无' || skillPower > 0 || summary.控制强度 !== '无' || summary.回复性质 !== '无';
                weight += 可识别增幅入口 ? Math.floor(结算技能效果强度 * 36) : -16;
              }
              if (有未匹配限定结算修正) weight -= 18;
              if (规划净收益 > 0) {
                weight += Math.min(120, Math.floor(规划净收益 * (是全场规划 ? 0.42 : 0.28)));
              } else if (规划净收益 < 0) {
                weight -= Math.min(180, Math.ceil(Math.abs(规划净收益) * (是全场规划 ? 0.7 : 0.45)));
              } else if (是全场规划) {
                weight -= 24;
              }

              if (['控制类', '削弱类'].includes(mainType)) weight += 15;
              if (mainType === '增益类') {
                if (增益真实需求) weight += Number(behaviorState.round || 0) <= 2 ? 12 : 6;
                else weight -= 满状态低压 ? 32 : 18;
              }
              if (是核心自我增益) {
                if (低耗开局增益 && 增益真实需求) weight += 16;
                if (久攻不下) weight += 24;
                if (!增益真实需求 && !是否真身技能) weight -= 22;
                if (是否真身技能) weight += Math.floor(真身战况推力 * 72) - 48;
                if (selfSnapshot.hasDefenseBuffed && summary.防御性质 !== '无') weight -= 24;
                if (hasReflect && 满状态低压 && !久攻不下) weight -= 28;
                if (hasReflect && !反射真实需求) weight -= 46;
                if (hasReflect && 反射真实需求)
                  weight += threatProfile.severeDamage || threatProfile.lethalRisk ? 34 : threatProfile.moderateDamage ? 22 : 12;
                if (
                  防御者系别 === '强攻系' &&
                  summary.防御性质 !== '无' &&
                  selfHpRatio > 0.72 &&
                  !(threatProfile.targetHitsDefender && threatProfile.incomingAttackIntent) &&
                  !isChargingHighThreat
                )
                  weight -= 80;
              }
              if (
                防御者系别 === '强攻系' &&
                selfHpRatio > 0.55 &&
                !isChargingHighThreat &&
                是友方增益目标 &&
                mainType !== '伤害类' &&
                !是否真身技能 &&
                !(threatProfile.lethalRisk || threatProfile.severeDamage || threatProfile.severeControl)
              )
                weight -= summary.防御性质 !== '无' ? 120 : 60;
              if (久攻不下 && mainType === '伤害类' && (penetrationValue >= 15 || hasShieldBreak || skillPower >= 160 || 伤害预结算.moderateDamage === true)) weight += 32;
              if (久攻不下 && 是规则压制) weight += 18;
              if (mainType === '特殊规则类') weight -= 5;
              if (是保命技能 && (selfHpRatio < 0.55 || isChargingHighThreat)) weight += 12;
              if (是团队保护 && allyCount > 1) weight += 10;
              if (是团队保护 && allyCount > 1) {
                const 护援压力 = Math.max(0, Number(behaviorState.团队护援压力 || 0));
                weight += 22 + Math.floor(护援压力 * 0.65);
                if (inferSkillPrimaryTargetKind(规划技能) !== '自身') weight += 18;
                if (防御者系别 === '防御系') weight += 18;
              }
              if (是规则压制 && (isChargingHighThreat || enemySnapshot.hasShielded || enemySnapshot.hasHealingTrend))
                weight += 10;
              if (来源类别 === '武魂融合技') {
                if (isChargingHighThreat || enemyHpRatio < 0.42 || selfHpRatio < 0.42) weight += 36;
                else weight -= 18;
              } else if (来源类别 === '自创魂技') {
                if (是规则压制 || hasHostileSemantic) weight += 12;
                if (hasFriendlyGrantable && allyCount > 1) weight += 8;
              }

              if (summary.控制强度 === '硬控' && isChargingHighThreat) weight += 60;
              else if (summary.控制强度 === '软控' && isChargingHighThreat) weight += 35;

              if (
                mainType === '伤害类' &&
                summary.目标规模 === '单体' &&
                summary.爆发级别 === '高' &&
                (enemyHpRatio < 0.35 || (防御者系别 === '强攻系' && enemyHpRatio < 0.65))
              )
                weight += 防御者系别 === '强攻系' && enemyHpRatio < 0.65 ? 38 : 50;
              if (
                防御者系别 === '强攻系' &&
                mainType === '伤害类' &&
                enemyHpRatio < 0.72 &&
                !enemySnapshot.hasShielded &&
                !enemySnapshot.hasDefenseBuffed
              )
                weight += Math.min(42, Math.floor(skillPower / 8));
              if (mainType === '伤害类' && summary.目标规模 === '群体' && behaviorState.round <= 1) weight += 25;

              if (summary.防御性质 !== '无' && selfHpRatio < 0.5) weight += 30;
              if (summary.防御性质 === '免死' && selfHpRatio < 0.35) weight += 40;
              if (hasClone && (behaviorState.round <= 2 || selfHpRatio < 0.65)) weight += 25;

              if (summary.回复性质 !== '无' && (selfHpRatio < 0.6 || selfSnapshot.hasBadCondition)) weight += 35;
              if (summary.回复性质 === '净化' && selfSnapshot.hasBadCondition) weight += 60;
              if (状态移除释放权重 > 0) weight += Math.min(140, Math.round(状态移除释放权重 * 0.85));
              if (
                对手攻击压制 &&
                ['辅助系', '治疗系', '食物系', '召唤系'].includes(防御者系别) &&
                (mainType === '增益类' || hasFriendlyGrantable || hasResourceRecover) &&
                selfHpRatio > 0.45
              ) {
                weight -= hasResourceRecover && (selfSpRatio < 0.28 || selfMenRatio < 0.28) ? 12 : 30;
              }

              if (summary.协同性 === '高' && (behaviorState.alliesCount || 1) > 1) weight += 20;
              else if (summary.协同性 === '中' && (behaviorState.alliesCount || 1) > 1) weight += 10;
              if (hasFriendlyGrantable && allyCount > 1) {
                weight += summary.目标规模 === '单体' ? 8 : 18;
                if (selfHpRatio < 0.55 || behaviorState.round <= 2) weight += 10;
              }
              if (hasHostileSemantic && summary.控制强度 !== '无' && isChargingHighThreat) weight += 10;

              if (summary.生效方式 === '持续') weight += fieldActive ? -30 : 20;
              if (summary.生效方式 === '延迟') {
                if (behaviorState.round <= 1) weight += 15;
                else if (enemyHpRatio < 0.3) weight -= 10;
              }
              if (summary.生效方式 === '触发') weight += 5;

              if (summary.爆发级别 === '高' && enemyHpRatio < 0.4) weight += 20;
              if (summary.持续性 === '长' && behaviorState.round <= 2) weight += 15;
              if (summary.持续性 === '中' && behaviorState.combatType !== '擂台切磋') weight += 10;

              if (hasAntiHeal) {
                if (enemySnapshot.hasAntiHeal) weight -= 35;
                else if (['辅助系', '治疗系', '食物系', '召唤系'].includes(攻击者系别) || enemySnapshot.hasHealingTrend)
                  weight += 45;
                else if (enemyHpRatio > 0.55) weight += 20;
              }
              if (hasSharedVision && allyCount > 1) {
                if (selfSnapshot.hasSharedVision) weight -= 30;
                else weight += 30;
              }
              if (hasCounter && (isChargingHighThreat || selfHpRatio < 0.55))
                weight += selfSnapshot.hasReactiveDefense ? 8 : 22;
              if (hasBlock && (isChargingHighThreat || selfHpRatio < 0.5))
                weight += selfSnapshot.hasReactiveDefense ? 10 : 28;
              if (hasShield && selfHpRatio < 0.55) weight += selfSnapshot.hasShielded ? 6 : 24;
              if (hasDeathSave && selfHpRatio < 0.35) weight += 55;
              if (hasInvincible && (isChargingHighThreat || selfHpRatio < 0.28)) weight += 85;
              if (hasReflect) {
                if (反射真实需求) weight += isChargingHighThreat ? 28 : 14;
                else weight -= 18;
              }
              if (hasDamageShare && allyCount > 1 && selfHpRatio < 0.45) weight += 28;
              if (hasClone && (selfHpRatio < 0.55 || behaviorState.round <= 2)) weight += 26;
              if (hasRevive && selfHpRatio < 0.3) weight += 62;
              if (hasExecute && enemyHpRatio < 0.35) weight += 40;
              if (hasSkillSeal && isChargingHighThreat) weight += 58;
              if (hasHealInvert && (enemySnapshot.hasHealingTrend || ['辅助系', '治疗系', '食物系', '召唤系'].includes(攻击者系别)))
                weight += 42;
              if (hasDotDetonate && enemySnapshot.debuffCount > 0) weight += 38 + enemySnapshot.debuffCount * 6;
              if (hasShieldBreak && enemySnapshot.hasShielded) weight += 44;
              if (hasResourceDrain) {
                if (enemySpRatio > 0.45 || enemyMenRatio > 0.45) weight += 28;
                if (selfSpRatio < 0.5 || selfMenRatio < 0.5) weight += 12;
                if (isChargingHighThreat) weight += 10;
              }
              if (hasMechanismSuppress) {
            if ((suppressTargets.includes('复活机制') || suppressTargets.includes('回复机制')) && enemySnapshot.hasHealingTrend) weight += 26;
                if ((suppressTargets.includes('护盾') || suppressTargets.includes('防御机制')) && enemySnapshot.hasShielded) weight += 24;
                if ((suppressTargets.includes('隐身') || suppressTargets.includes('增益')) && enemySnapshot.hasStealthed) weight += 24;
                if ((suppressTargets.includes('增益') || suppressTargets.includes('特殊规则')) && enemySnapshot.buffCount > 0)
                  weight += 14 + enemySnapshot.buffCount * 4;
                if (suppressTargets.includes('防御机制') && enemySnapshot.hasReactiveDefense) weight += 20;
              }
              if (有复制机制) {
                if (enemySnapshot.buffCount > 0 || enemyHpRatio < 0.72 || 久攻不下) weight += 16;
                if (behaviorState.round <= 2 && selfHpRatio > 0.7) weight += 8;
              }
              if (有炸环) {
                if (behaviorState.isDesperateNoEscape || selfHpRatio < 0.4 || enemyHpRatio < 0.38 || 久攻不下) weight += 34;
                else weight -= 22;
              }
              if (有主动时光回溯) {
                const 主动压制估值 = 时光回溯效果列表
                  .filter(effect => String(effect?.发动方式 || '').trim() === '主动')
                  .reduce((总和, effect) => {
                    const 目标上下文 = resolveTimeRewindTargetContext(skill, defender, attacker, behaviorState?.combatData, effect);
                    const 压制目标 = (目标上下文.targetSet || []).filter(targetObj =>
                      targetObj && !isCombatUnitIdentityMatch(targetObj, defender?.name || defender),
                    );
                    const 敌方压制价值 = 压制目标.some(targetObj => isCombatUnitIdentityMatch(targetObj, attacker?.name || attacker)) ? 18 : 0;
                    const 数量价值 = Math.min(24, Math.max(0, 压制目标.length - 1) * 8);
                    const 全场误伤惩罚 = String(effect?.目标 || '').trim() === '全场' && allyCount > 1 ? Math.min(28, allyCount * 8) : 0;
                    return 总和 + 敌方压制价值 + 数量价值 - 全场误伤惩罚;
                  }, 0);
                if (behaviorState.round <= 1) weight += 12;
                if (isChargingHighThreat || dAgi > aAgi || threatProfile.incomingAttackIntent) weight += 18;
                weight += 主动压制估值;
                if (selfSpRatio < 0.25 || selfMenRatio < 0.25) weight -= 18;
              }
              if (有被动时光回溯) {
                const 已错过预结算窗口 = threatProfile.targetHitsDefender && threatProfile.incomingAttackIntent && behaviorState?.当前阶段 === '正式落地后';
                if (已错过预结算窗口) {
                  weight = 0;
                } else {
                  const 被动保护估值 = 时光回溯效果列表
                    .filter(effect => String(effect?.发动方式 || '被动').trim() !== '主动')
                    .reduce((总和, effect) => {
                      const 目标上下文 = resolveTimeRewindTargetContext(skill, defender, attacker, behaviorState?.combatData, effect);
                      const 保护目标 = (目标上下文.targetSet || []).filter(Boolean);
                      if (!保护目标.length) return 总和 - 12;
                      const 目标估值 = 保护目标.reduce((目标总分, targetObj) => {
                        const 已有未触发时光回溯壳 = !!查找时光回溯状态条目(targetObj);
                        const 关键单位 = targetObj.type === '辅助系' || targetObj.type === '治疗系' || targetObj.type === '控制系';
                        const 本回合态价值 =
                          (targetObj.蓄力技能 ? 1 : 0) +
                          (Object.values(targetObj.__技能限制运行态 || {}).some(限制态 => Number(限制态?.已用次数 || 0) > 0) ? 1 : 0);
                        const 消耗可承受 = 计算时光回溯专属消耗(skill, targetObj, effect).canCast !== false;
                        let 分值 = 0;
                        if (已有未触发时光回溯壳) 分值 -= 36;
                        if (!消耗可承受) 分值 -= 28;
                        if (关键单位) 分值 += 14;
                        if (本回合态价值 > 0) 分值 += 12 * 本回合态价值;
                        return 目标总分 + 分值;
                      }, 0);
                      return 总和 + Math.min(60, Math.max(-54, 目标估值));
                    }, 0);
                  if (!threatProfile.targetHitsDefender || !threatProfile.incomingAttackIntent) weight -= 36;
                  else if (threatProfile.lethalRisk === true) weight += 72;
                  else if (threatProfile.severeDamage === true || threatProfile.severeControl === true) weight += 52;
                  else if (threatProfile.moderateDamage === true) weight += 28;
                  else weight += 12;
                  weight += 被动保护估值;
                }
              }
              if (有气运控制) {
                if (isChargingHighThreat || enemySnapshot.hasReactiveDefense || enemySnapshot.hasShielded || 久攻不下) weight += 18;
              }
              if (有元素封禁) {
                if (isChargingHighThreat || /元素/.test(攻击者系别) || enemySnapshot.buffCount > 0) weight += 24;
              }
              if (hasResourceRecover && (selfSpRatio < 0.4 || selfMenRatio < 0.4))
                weight += selfSnapshot.hasHealingTrend ? 10 : 30;
              if (hasHeal && selfHpRatio < 0.45) weight += selfSnapshot.hasHealingTrend ? 8 : 28;
              if (enemySnapshot.hasShielded && (penetrationValue >= 20 || skillPower >= 180 || 伤害预结算.severeDamage === true)) weight += 24;
              if (enemySnapshot.hasDefenseBuffed && penetrationValue >= 15) weight += 18;
              if (enemySnapshot.isLockedOrControlled) {
                if (summary.爆发级别 === '高') weight += 18;
                if (hasDelayBurst || skillCastTime >= 18) weight += 12;
                if (hasExecute && enemyHpRatio < 0.45) weight += 15;
                if (summary.控制强度 === '软控') weight -= 20;
                else if (summary.控制强度 === '硬控' && !isChargingHighThreat) weight -= 8;
              }
              if (enemySnapshot.debuffCount > 0 && hasDotPressure) weight += 10;
              if (selfSnapshot.hasReactiveDefense && (hasCounter || hasBlock || hasShield || hasDeathSave))
                weight -= 12;
              if (hasVolatile) {
                if (behaviorState.isDesperateNoEscape || selfHpRatio < 0.35 || enemyHpRatio < 0.35) weight += 12;
                else weight -= 10;
              }
              if (hasReflectiveConvert) {
                if (enemySnapshot.buffCount > 0 || selfHpRatio < 0.5 || enemyHpRatio < 0.5) weight += 15;
              }
              if (threatProfile.targetHitsDefender) {
                if (threatProfile.severeDamage || threatProfile.lethalRisk) {
                  if (hasInvincible) weight += threatProfile.bypassesInvincible ? 0 : 110;
                  if (hasDeathSave) weight += threatProfile.lethalRisk ? 38 : 12;
                  if (hasRevive) weight += 70;
                  if (hasClone) weight += 66;
                  if (hasDamageShare && allyCount > 1) weight += 58;
                  if (hasShield) weight += 36;
                  if (hasBlock) weight += threatProfile.lethalRisk ? 78 : 52;
                } else if (threatProfile.moderateDamage) {
                  if (hasShield || hasCounter) weight += 18;
                  if (hasBlock) weight += 26;
                  if (hasReflect && 反射真实需求) weight += 12;
                }
                if (threatProfile.severeControl) {
                  if (hasSharedVision || hasClone || hasInvincible) weight += 12;
                  if (skillType2 === '控制') weight -= 8;
                }
              }
              if (Number(actorMemory.focus_ttl || 0) > 0) {
                if (actorMemory.focus_reason === 'control_window') {
                  if (summary.爆发级别 === '高' || hasExecute) weight += 16;
                  if (summary.控制强度 !== '无') weight -= 10;
                } else if (
                  actorMemory.focus_reason === 'shared_vision_focus' &&
                  summary.目标规模 === '单体' &&
                  summary.爆发级别 !== '无'
                )
                  weight += 18;
                else if (
                  actorMemory.focus_reason === 'dot_pressure' &&
                  (hasDotPressure || hasExecute || summary.爆发级别 === '高')
                )
                  weight += 14;
                else if (
                  actorMemory.focus_reason === 'anti_heal_window' &&
                  (hasDotPressure || hasExecute || summary.爆发级别 !== '无')
                )
                  weight += 10;
                else if (
                  actorMemory.focus_reason === 'armor_break_window' &&
                  (penetrationValue >= 15 || summary.爆发级别 === '高')
                )
                  weight += 10;
                else if (actorMemory.focus_reason === 'finisher' && (hasExecute || summary.爆发级别 !== '无'))
                  weight += 18;
              }
              if (counterPenalty > 0) weight -= counterPenalty;
              if (重复状态衰减.惩罚 > 0) weight -= 重复状态衰减.惩罚;

              if (summary.保留倾向 >= 70 && !(isChargingHighThreat || enemyHpRatio < 0.35 || selfHpRatio < 0.35))
                weight -= 30;
              else if (summary.保留倾向 >= 40 && !(isChargingHighThreat || enemyHpRatio < 0.45 || selfHpRatio < 0.45))
                weight -= 15;

              let 局势系数 = 1;
              if (是否真身技能) 局势系数 *= 读取局势倾向系数(behaviorState, '真身');
              if (mainType === '伤害类' || skillType2 === '输出') 局势系数 *= 读取局势倾向系数(behaviorState, '输出');
              if (mainType === '增益类' || hasFriendlyGrantable) 局势系数 *= 读取局势倾向系数(behaviorState, '辅助');
              if (summary.回复性质 !== '无' || hasHeal || hasResourceRecover) 局势系数 *= 读取局势倾向系数(behaviorState, '恢复');
              if (summary.防御性质 !== '无' || 是保命技能) 局势系数 *= 读取局势倾向系数(behaviorState, '防御');
              if (summary.控制强度 !== '无' || skillType2 === '控制') 局势系数 *= 读取局势倾向系数(behaviorState, '控制');
              if (hasShieldBreak || hasMechanismSuppress || 是规则压制 || penetrationValue >= 15)
                局势系数 *= 读取局势倾向系数(behaviorState, '破局');
              const 技能消耗压力 = 计算技能消耗压力(skill, defender);
              if (技能消耗压力.综合压力 >= 0.32) 局势系数 *= 读取局势倾向系数(behaviorState, '高耗');
              weight = Math.floor(weight * 限制行为概率(局势系数, 0.18, 2.4));

              if (是否真身技能) {
                if (已有同类增益) weight -= 90;
                weight = 按资源压力调整权重(weight, skill, defender, {
                  是真身: true,
                  战况推力: 真身战况推力,
                  回合: behaviorState.round,
                });
                if (
                  Number(behaviorState.round || 0) <= 4 &&
                  Math.abs(Number((attacker?.lv || 1) - (defender?.lv || 1))) <= 4 &&
                  !behaviorState.开局绝境窗口 &&
                  !isChargingHighThreat &&
                  selfHpRatio > 0.85
                )
                  weight = Math.min(Math.floor(weight * 0.12), 8);
              } else {
                const 严重威胁窗口 =
                  threatProfile.targetHitsDefender &&
                  (threatProfile.severeDamage || threatProfile.lethalRisk || threatProfile.severeControl);
                const 开局绝境窗口 = behaviorState.开局绝境窗口 === true;
                const 终结窗口 = enemyHpRatio < 0.35;
                const 破局窗口 =
                  久攻不下 &&
                  (penetrationValue >= 15 || hasShieldBreak || hasMechanismSuppress || 是规则压制 || skillPower >= 180 || 伤害预结算.severeDamage === true);
                const 自救窗口 = selfHpRatio < 0.45 || 严重威胁窗口;
                const 高耗基础窗口 =
                  开局绝境窗口 ||
                  自救窗口 ||
                  终结窗口 ||
                  破局窗口 ||
                  (hasResourceRecover && (selfSpRatio < 0.35 || selfMenRatio < 0.35)) ||
                  (是团队保护 && allyCount > 1 && Number(behaviorState.团队护援压力 || 0) >= 60);
                const 高耗战况推力 = Math.max(
                  开局绝境窗口 ? 1 : 0,
                  自救窗口 ? 0.82 : 0,
                  终结窗口 ? 0.7 : 0,
                  破局窗口 ? 0.72 : 0,
                  hasResourceRecover && (selfSpRatio < 0.35 || selfMenRatio < 0.35) ? 0.62 : 0,
                  是团队保护 && allyCount > 1 ? Math.min(0.8, Number(behaviorState.团队护援压力 || 0) / 100) : 0,
                  Math.min(0.7, Number(behaviorState.round || 0) / 7),
                  高耗基础窗口 ? 0.35 : 0,
                );
                weight = 按资源压力调整权重(weight, skill, defender, {
                  战况推力: 高耗战况推力,
                  回合: behaviorState.round,
                });
              }

              return {
                skill,
                规划技能,
                weight: Math.max(0, weight),
                name: skill.name || '未命名技能',
                重复状态衰减,
                build() {
                  return skill;
                },
              };
            });

            const 去重技能表 = new Map();
            weighted.forEach(项目 => {
              const 技能名 = String(项目?.skill?.name || 项目?.skill?.技能名称 || 项目?.name || '').trim();
            const 键 = 技能名;
              if (!键.trim()) return;
              const 旧项 = 去重技能表.get(键);
              if (!旧项 || Number(项目.weight || 0) > Number(旧项.weight || 0)) 去重技能表.set(键, 项目);
            });
            const 去重权重列表 = [...去重技能表.values()];
            const picked = rollBranchByPriority(去重权重列表, '行为预演/技能选择');
            if (!picked.option) return { skill: null, trace: picked.trace };
            return { skill: picked.option.build(), trace: picked.trace, weighted: 去重权重列表 };
          }

          const defSkills = validSkills.filter(skill => {
            const 规划技能 = 构建行为规划技能(skill, attacker);
            const skillType = getSkillType(skill);
            const mainType = inferMainTypeFromEffects(规划技能);
            const summary = deriveBattleSummaryFromEffects(规划技能);
            const 来源类别 = getBattleSkillSourceCategory(规划技能);
            return isBattleSkillDefensiveProfile(规划技能, { skillType, mainType, summary, 来源类别 });
          });
          const atkSkills = validSkills.filter(skill => {
            const 规划技能 = 构建行为规划技能(skill, attacker);
            const skillType = getSkillType(skill);
            const mainType = inferMainTypeFromEffects(规划技能);
            const summary = deriveBattleSummaryFromEffects(规划技能);
            return isBattleSkillOffensiveProfile(规划技能, { skillType, mainType, summary });
          });
          const controlSkills = validSkills.filter(skill => {
            const 规划技能 = 构建行为规划技能(skill, attacker);
            const mainType = inferMainTypeFromEffects(规划技能);
            const calc = getPrimaryStateCalc(规划技能);
            const flags = getPrimaryStateFlags(规划技能);
            const summary = deriveBattleSummaryFromEffects(规划技能);
            return isBattleSkillControlProfile(规划技能, { mainType, calc, flags, summary });
          });

          const 全技能评分 = pickSkillWithWeight(validSkills);
          const 评分技能列表 = Array.isArray(全技能评分.weighted) ? 全技能评分.weighted : [];
          const 按画像取技能 = (筛选函数, 兜底技能 = null) => {
            const 候选列表 = 评分技能列表
              .filter(项目 => 项目?.skill && Number(项目.weight || 0) > 0 && 筛选函数(项目.规划技能 || 项目.skill, 项目.skill))
              .sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0));
            return 候选列表[0]?.skill || 兜底技能 || null;
          };

          const defSkill = 按画像取技能((规划技能, 原技能) => {
            const skillType = getSkillType(原技能);
            const mainType = inferMainTypeFromEffects(规划技能);
            const summary = deriveBattleSummaryFromEffects(规划技能);
            const 来源类别 = getBattleSkillSourceCategory(规划技能);
            return isBattleSkillDefensiveProfile(规划技能, { skillType, mainType, summary, 来源类别 });
          });
          const atkSkill = 按画像取技能((规划技能, 原技能) => {
            const skillType = getSkillType(原技能);
            const mainType = inferMainTypeFromEffects(规划技能);
            const summary = deriveBattleSummaryFromEffects(规划技能);
            return isBattleSkillOffensiveProfile(规划技能, { skillType, mainType, summary });
          });
          const rangePressureSkill = 选取范围压制技能(
            [
              ...评分技能列表
                .filter(项目 => 项目?.skill && Number(项目.weight || 0) > 0)
                .map(项目 => 项目.skill),
              ...(反高速窗口.成立 ? validSkills : []),
            ],
          );
          const quickClashSkill = 评分技能列表
            .filter(项目 => 项目?.skill && Number(项目.weight || 0) > 0 && getSkillCastTime(项目.skill) <= 12 && getPrimaryDamageEffect(项目.skill))
            .sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0))[0]?.skill ||
            (反高速窗口.成立
              ? validSkills
                  .filter(skill => getSkillCastTime(skill) <= 12 && getPrimaryDamageEffect(skill))
                  .sort((左, 右) => Number(getPrimaryDamageEffect(右)?.威力倍率 || 0) - Number(getPrimaryDamageEffect(左)?.威力倍率 || 0))[0]
              : null) ||
            null;
          const hardControlSkill = 按画像取技能(
            规划技能 => {
              const mainType = inferMainTypeFromEffects(规划技能);
              const calc = getPrimaryStateCalc(规划技能);
              const flags = getPrimaryStateFlags(规划技能);
              const summary = deriveBattleSummaryFromEffects(规划技能);
              return isBattleSkillControlProfile(规划技能, { mainType, calc, flags, summary });
            },
            controlSkills[0] || null,
          );
          const antiHealSkill = 按画像取技能(规划技能 => isBattleSkillAntiHealProfile(规划技能));
          const pierceSkill = 按画像取技能((规划技能, 原技能) => {
              const dmg = getPrimaryDamageEffect(规划技能);
              return Number(dmg?.防御穿透 || 0) >= 15 || /破甲|穿透|粉碎/.test(String(原技能?.name || ''));
          });
          const lowCostAtkSkill = 按画像取技能(
            (规划技能, 原技能) => {
              const skillType = getSkillType(原技能);
              const mainType = inferMainTypeFromEffects(规划技能);
              const summary = deriveBattleSummaryFromEffects(规划技能);
              if (!isBattleSkillOffensiveProfile(规划技能, { skillType, mainType, summary })) return false;
              const cost = parseSkillCostForChar(原技能, defender, {
                actor: defender,
                caster: defender,
                attacker: defender,
                target: attacker,
                defender: attacker,
                action: { action_type: '释放魂技', type: '技能', skill: 原技能 },
                skill: 原技能,
                combatData: behaviorState?.combatData,
                当前行动: '释放魂技',
              });
              const 魂力压力 = cost.reqSp > 0 ? cost.reqSp / Math.max(1, Number(defender.sp || 0)) : 0;
              const 体力压力 = cost.reqVit > 0 ? cost.reqVit / Math.max(1, Number(defender.sta || defender.体力 || 1)) : 0;
              const 精神压力 = cost.reqMen > 0 ? cost.reqMen / Math.max(1, Number(defender.men || 0)) : 0;
              return cost.canCast && Math.max(魂力压力, 体力压力, 精神压力) <= 0.28;
            },
            atkSkill,
          );
          const executeSkill = 按画像取技能(规划技能 => {
              const dmg = getPrimaryDamageEffect(规划技能);
              return isBattleSkillExecuteProfile(规划技能, { damage: dmg, summary: deriveBattleSummaryFromEffects(规划技能) });
          });
          const 自身血量比 = getCombatHpRatio(defender);
          const 自身魂力比 = Math.max(0, Number(defender.sp || 0)) / Math.max(1, Number(defender.sp_max || 1));
          const 自身精神比 = Math.max(0, Number(defender.men || 0)) / Math.max(1, Number(defender.men_max || 1));
          const 自身状态快照 = buildConditionTacticalSnapshot(defender);
          const 友方规划列表 = 读取规划同队单位(defender).filter(unit => unit && isCombatUnitAlive(unit));
          const 友方需要生命恢复 = 友方规划列表.some(unit => getCombatHpRatio(unit) < 0.72);
          const 友方需要魂力支援 = 友方规划列表.some(unit => Math.max(0, Number(unit.sp || 0)) / Math.max(1, Number(unit.sp_max || 1)) < 0.62);
          const 友方需要精神支援 = 友方规划列表.some(unit => Math.max(0, Number(unit.men || 0)) / Math.max(1, Number(unit.men_max || 1)) < 0.62);
          const 友方需要净化 = 友方规划列表.some(unit => buildConditionTacticalSnapshot(unit).hasBadCondition);
          const recoverSkill = 按画像取技能(规划技能 =>
            getSkillEffects(规划技能).some(effect => {
              if (isBattleRecoverEffect(effect, ['vit'])) return 友方需要生命恢复 || 自身血量比 < 0.72;
              if (isBattleRecoverEffect(effect, ['sp'])) return 友方需要魂力支援 || 自身魂力比 < 0.62;
              if (isBattleRecoverEffect(effect, ['men'])) return 友方需要精神支援 || 自身精神比 < 0.62;
              return (友方需要净化 || 自身状态快照.hasBadCondition) && isBattleRecoverEffect(effect);
            }),
          );
          const teamSupportSkill = 按画像取技能((规划技能, 原技能) => {
                const skillType = getSkillType(原技能);
                const summary = deriveBattleSummaryFromEffects(规划技能);
                const 来源类别 = getBattleSkillSourceCategory(规划技能);
                return isBattleSkillTeamSupportProfile(规划技能, { skillType, summary, 来源类别 });
          });
          const teamProtectSkill = 按画像取技能(规划技能 => 是团队保护技能(规划技能, { summary: deriveBattleSummaryFromEffects(规划技能) }));
          const invincibleSkill = 按画像取技能(规划技能 => deriveBattleSummaryFromEffects(规划技能).防御性质 === '无敌');
          const shieldBreakSkill = 按画像取技能(规划技能 => isBattleSkillShieldBreakProfile(规划技能));
          const dotDetonateSkill = 按画像取技能(规划技能 => isBattleSkillDotDetonateProfile(规划技能));
          const skillSealSkill = 按画像取技能(规划技能 => isBattleSkillSealProfile(规划技能));
          const healInvertSkill = 按画像取技能(规划技能 => isBattleSkillHealInvertProfile(规划技能));
          const statusTransferSkill = 按画像取技能(规划技能 => isBattleSkillTransferProfile(规划技能));
          const reactiveDefenseSkill =
            按画像取技能(规划技能 => isBattleSkillReactiveDefenseProfile(规划技能, { summary: deriveBattleSummaryFromEffects(规划技能) })) ||
            defSkill ||
            null;
          const npcAtkPower = Number(getPrimaryDamageEffect(atkSkill)?.威力倍率 || 0);
          const skillTraceLog = 全技能评分.trace || '';

          return {
            defSkill,
            atkSkill,
            rangePressureSkill,
            quickClashSkill,
            hardControlSkill,
            antiHealSkill,
            pierceSkill,
            lowCostAtkSkill,
            executeSkill,
            recoverSkill,
            teamSupportSkill,
            teamProtectSkill,
            invincibleSkill,
            shieldBreakSkill,
            dotDetonateSkill,
            skillSealSkill,
            healInvertSkill,
            statusTransferSkill,
            reactiveDefenseSkill,
            npcAtkPower,
            skillTraceLog,
            threatProfile,
            评分技能列表,
          };
        }

        function buildTacticalCandidates(
          defender,
          attacker,
          playerAction,
          behaviorState,
          skillContext,
          makeNpcAction,
          isSupport,
          isLowHealth,
        ) {
          const {
            defSkill,
            atkSkill,
            rangePressureSkill,
            quickClashSkill,
            hardControlSkill,
            antiHealSkill,
            pierceSkill,
            lowCostAtkSkill,
            executeSkill,
            recoverSkill,
            teamSupportSkill,
            teamProtectSkill,
            invincibleSkill,
            shieldBreakSkill,
            dotDetonateSkill,
            skillSealSkill,
            healInvertSkill,
            statusTransferSkill,
            reactiveDefenseSkill,
            npcAtkPower,
            threatProfile,
          } = skillContext;
          const playerPower = behaviorState.playerPower || 0;
          const 防御者系别 = 读取规划单位系别(defender);
          const 攻击者系别 = 读取规划单位系别(attacker);
          const isChargingHighThreat = !!behaviorState.isChargingHighThreat;
          const enemySnapshot = buildConditionTacticalSnapshot(attacker);
          const selfSnapshot = buildConditionTacticalSnapshot(defender);
          const enemyHpRatio = getCombatHpRatio(attacker);
          const selfHpRatio = getCombatHpRatio(defender);
          const selfSpRatio = Math.max(0, Number(defender.sp || 0)) / Math.max(1, Number(defender.sp_max || 1));
          const selfMenRatio = Math.max(0, Number(defender.men || 0)) / Math.max(1, Number(defender.men_max || 1));
          const allyCount = behaviorState.alliesCount || 1;
          const lethalThreat = !!threatProfile?.lethalRisk;
          const severeThreat = !!threatProfile?.severeDamage;
          const severeControlThreat = !!threatProfile?.severeControl;
          const 主动威胁分层 = 计算行为威胁分层(threatProfile || {});
          const 来袭技能文本 = JSON.stringify(playerAction?.skill || {});
          const 来袭含控制减益 = severeControlThreat ||
            /状态施加|debuff|控制|眩晕|麻痹|僵直|混乱|沉默|封技|失控|无法反应|致盲|锁定|位移限制|迟缓|中毒|流血|防御剥夺|精神抗性剥夺|cast_speed_penalty|dodge_penalty|reaction_penalty/.test(来袭技能文本);
          const 试探阶段 = Number(behaviorState?.combatData?.回合 || behaviorState?.combatData?.round || 1) <= 1 &&
            !lethalThreat &&
            !severeThreat;
          const reactiveThreat =
            lethalThreat || severeThreat || severeControlThreat || (!!threatProfile?.moderateDamage && selfHpRatio < 0.65);

          let isLockedBySpirit = false;
          if ((playerAction.cast_time || 0) > 0) {
            const pState = getPrimaryStateEffect(playerAction.skill) || {};
            const pCalc = getPrimaryStateCalc(playerAction.skill);
            const hasStructuredLock = Number(pCalc.lock_level || 0) > 0;
            const hasCompatFlagLock =
              getPrimaryStateFlags(playerAction.skill).includes('锁定') ||
              getPrimaryStateFlags(playerAction.skill).includes('威压');
            if (hasStructuredLock || hasCompatFlagLock) {
              if (attacker.men_max > defender.men_max) isLockedBySpirit = true;
            }
          }

          const tacticalBranches = [];
          const 标记候选预览技能 = 候选 => {
            if (!候选 || 候选.__预览技能) return 候选;
            const 技能表 = {
              断疗压制: antiHealSkill,
              破防强攻: pierceSkill,
              斩盾: shieldBreakSkill,
              持续引爆: dotDetonateSkill,
              封技断流: skillSealSkill,
              逆疗封锁: healInvertSkill,
              移祸转压: statusTransferSkill,
              乘胜追击: executeSkill,
              稳态回气: recoverSkill,
              战术协同: teamSupportSkill,
              借力守势: reactiveDefenseSkill,
              协同点杀: atkSkill,
              连段爆发: atkSkill,
              压血收束: executeSkill,
              强攻压制: atkSkill,
              敏攻截断: hardControlSkill || skillSealSkill || rangePressureSkill,
              范围压制: rangePressureSkill,
              短前摇对轰: quickClashSkill,
              游击收割: executeSkill && executeCastTime <= 18 ? executeSkill : atkSkill,
              坚壁反制: reactiveDefenseSkill,
              连锁控制: hardControlSkill,
              统筹增援: teamSupportSkill,
              强势对轰: atkSkill,
              危机自保: defSkill || reactiveDefenseSkill,
              护援队友: teamProtectSkill || 建立普防护援技能(),
            };
            候选.__预览技能 = 技能表[候选.name] || null;
            return 候选;
          };

          if (reactiveThreat) {
            const emergencyDefenseSkill =
              (!threatProfile?.bypassesInvincible && invincibleSkill) ||
              reactiveDefenseSkill ||
              defSkill ||
              recoverSkill ||
              null;
            if (emergencyDefenseSkill) {
              tacticalBranches.push({
                name: '极限保命',
                weight: adjustBehaviorWeight(
                  '危机自保',
                  lethalThreat ? 155 : severeThreat ? 132 : 108,
                  defender,
                  attacker,
                  behaviorState,
                ),
                __预览技能: emergencyDefenseSkill,
                build() {
                  const severityText = lethalThreat ? '已逼近致命线' : severeControlThreat ? '可能被一举封死节奏' : '将承受重创';
                  return makeNpcAction(
                    '危机自保',
                    `[反应判定成功] NPC瞬间判断自己${severityText}，立即交出[${emergencyDefenseSkill.name}]进行极限保命！`,
                    emergencyDefenseSkill,
                  );
                },
              });
            }
          }

          const atkSummary = atkSkill ? deriveBattleSummaryFromEffects(atkSkill) : createEmptyBattleSummary();
          const executeSummary = executeSkill
            ? deriveBattleSummaryFromEffects(executeSkill)
            : createEmptyBattleSummary();
          const atkCastTime = getSkillCastTime(atkSkill);
          const executeCastTime = getSkillCastTime(executeSkill);
          const 反高速窗口 = 评估反高速应对窗口(defender, attacker, playerAction, behaviorState?.combatData || {});
          if (防御者系别 === '敏攻系' && rangePressureSkill && Number(playerAction?.cast_time || 0) >= 8) {
            const 技能名 = String(rangePressureSkill.name || rangePressureSkill.魂技名 || rangePressureSkill.技能名称 || '').trim();
            const 近期次数 = 读取归一行为记忆值_V1(ensureActorDecisionMemory(defender).recent_actions || {}, [技能名], 0);
            const 重复状态衰减 = 评估敌对重复状态收益衰减(rangePressureSkill, attacker, {
              projectedDamageRatio: Number(threatProfile?.projectedDamageRatio || 0),
              recentSameActionCount: 近期次数,
            });
            if (Number(重复状态衰减.惩罚 || 0) < 80) {
              tacticalBranches.push({
                name: '范围压制',
                weight: adjustBehaviorWeight('范围压制', 128 - Math.floor(Number(重复状态衰减.惩罚 || 0) * 0.6), defender, attacker, behaviorState),
                __预览技能: rangePressureSkill,
                build() {
                  return makeNpcAction(
                    '范围压制',
                    `[范围压制] 敏攻系判断对手前摇窗口已经暴露，改用[${rangePressureSkill.name}]压缩其身位。`,
                    rangePressureSkill,
                  );
                },
              });
            }
          }

          if (反高速窗口.成立 && rangePressureSkill) {
            tacticalBranches.push({
              name: '范围压制',
              weight: adjustBehaviorWeight('范围压制', 68 + Math.round(反高速窗口.强度 * 52), defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '范围压制',
                  `[范围压制] NPC判断对手速度优势明显，改用[${rangePressureSkill.name}]压缩其闪避空间。`,
                  rangePressureSkill,
                );
              },
            });
          }
          if (反高速窗口.成立 && quickClashSkill && quickClashSkill !== rangePressureSkill) {
            tacticalBranches.push({
              name: '短前摇对轰',
              weight: adjustBehaviorWeight('短前摇对轰', 58 + Math.round(反高速窗口.强度 * 44), defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '短前摇对轰',
                  `[短前摇对轰] NPC放弃追逐身位，改以[${quickClashSkill.name}]抢在高速切入窗口正面截击。`,
                  quickClashSkill,
                );
              },
            });
          }

          if (enemySnapshot.hasHealingTrend && antiHealSkill && !enemySnapshot.hasAntiHeal) {
            tacticalBranches.push({
              name: '断疗压制',
              weight: adjustBehaviorWeight('断疗压制', 95, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '断疗压制',
                  `[战术封锁] NPC敏锐察觉到对手恢复节奏已成，立刻以[${antiHealSkill.name}]强行断疗压制！`,
                  antiHealSkill,
                );
              },
            });
          }
          if ((enemySnapshot.hasShielded || enemySnapshot.hasDefenseBuffed) && pierceSkill) {
            tacticalBranches.push({
              name: '破防强攻',
              weight: adjustBehaviorWeight(
                '破防强攻',
                enemySnapshot.hasShielded ? 90 : 75,
                defender,
                attacker,
                behaviorState,
              ),
              build() {
                return makeNpcAction(
                  '破防强攻',
                  `[破防识别] NPC判断常规打击难以奏效，改以[${pierceSkill.name}]强行撕开防御层！`,
                  pierceSkill,
                );
              },
            });
          }
          if (enemySnapshot.hasShielded && shieldBreakSkill) {
            tacticalBranches.push({
              name: '斩盾',
              weight: adjustBehaviorWeight('破防强攻', 104, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '破防强攻',
                  `[护盾识别] NPC判断常规伤害会被护盾吞掉，直接以[${shieldBreakSkill.name}]斩碎护盾层！`,
                  shieldBreakSkill,
                );
              },
            });
          }
          if (enemySnapshot.debuffCount > 0 && dotDetonateSkill) {
            tacticalBranches.push({
              name: '持续引爆',
              weight: adjustBehaviorWeight('乘胜追击', enemyHpRatio < 0.5 ? 98 : 78, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '乘胜追击',
                  `[引爆窗口] NPC察觉对手身上已叠起持续伤害，立刻以[${dotDetonateSkill.name}]引爆全部压血。`,
                  dotDetonateSkill,
                );
              },
            });
          }
          if ((attacker?.蓄力技能 || isChargingHighThreat || (playerAction.cast_time || 0) >= 15) && skillSealSkill) {
            tacticalBranches.push({
              name: '封技断流',
              weight: adjustBehaviorWeight('危机自保', 112, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '危机自保',
                  `[术路封锁] NPC判断对手技能前摇已成，立即以[${skillSealSkill.name}]封死其后续术路。`,
                  skillSealSkill,
                );
              },
            });
          }
          if (enemySnapshot.hasHealingTrend && healInvertSkill && !enemySnapshot.hasAntiHeal) {
            tacticalBranches.push({
              name: '逆疗封锁',
              weight: adjustBehaviorWeight('断疗压制', 100, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '断疗压制',
                  `[逆疗反制] NPC识别到对手恢复链成型，果断用[${healInvertSkill.name}]把治疗窗口反拧成伤害。`,
                  healInvertSkill,
                );
              },
            });
          }
          if (selfSnapshot.hasBadCondition && statusTransferSkill) {
            tacticalBranches.push({
              name: '移祸转压',
              weight: adjustBehaviorWeight('危机自保', 76, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '危机自保',
                  `[移祸脱身] NPC不愿继续背着异常状态硬拼，转而用[${statusTransferSkill.name}]把压力甩回对手。`,
                  statusTransferSkill,
                );
              },
            });
          }
          if ((enemySnapshot.isLockedOrControlled || enemyHpRatio < 0.4) && executeSkill) {
            tacticalBranches.push({
              name: '乘胜追击',
              weight: adjustBehaviorWeight(
                '乘胜追击',
                enemyHpRatio < 0.35 ? 105 : 80,
                defender,
                attacker,
                behaviorState,
              ),
              build() {
                return makeNpcAction(
                  '乘胜追击',
                  `[收割窗口] NPC看准对手露出的破绽，立刻释放[${executeSkill.name}]试图完成收割！`,
                  executeSkill,
                );
              },
            });
          }
          if (
            (selfSpRatio < 0.35 || selfMenRatio < 0.35 || selfHpRatio < 0.45) &&
            recoverSkill &&
            !(selfSnapshot.hasHealingTrend && selfHpRatio > 0.55 && selfSpRatio > 0.45 && selfMenRatio > 0.45)
          ) {
            const 强攻压制下续航价值 =
              ['强攻系', '敏攻系'].includes(攻击者系别) &&
              ['辅助系', '治疗系', '食物系', '召唤系'].includes(防御者系别) &&
              selfHpRatio > 0.45
                ? 0.62
                : 1;
            tacticalBranches.push({
              name: '稳态回气',
              weight: adjustBehaviorWeight('稳态回气', 72 * 强攻压制下续航价值, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '稳态回气',
                  `[调整节奏] NPC判断硬拼将迅速透支，转而以[${recoverSkill.name}]修补状态链。`,
                  recoverSkill,
                );
              },
            });
          }
          const 护援目标 = 选择护援队友(defender, behaviorState.allyTeam || [], attacker, behaviorState);
          const 护援压力 = Math.max(Number(behaviorState.团队护援压力 || 0), Number(护援目标?.压力 || 0));
          const 自身已筑防 =
            selfSnapshot.hasShielded ||
            selfSnapshot.hasDefenseBuffed ||
            selfSnapshot.hasReactiveDefense ||
            selfHpRatio >= 0.72;
          if (防御者系别 === '防御系' && allyCount > 1 && 护援目标 && 护援压力 >= 22) {
            if (teamProtectSkill) {
              tacticalBranches.push({
                name: '护援队友',
                weight: adjustBehaviorWeight(
                  '护援队友',
                  92 + Math.min(55, 护援压力),
                  defender,
                  attacker,
                  behaviorState,
                ),
                build() {
                  return makeNpcAction(
                    '护援队友',
                    `[护援判断] 防御系NPC判断${护援目标.队友.name || '队友'}承压更高，优先用[${teamProtectSkill.name}]替队友稳住阵线。`,
                    teamProtectSkill,
                  );
                },
              });
            }
            if (自身已筑防) {
              tacticalBranches.push({
                name: '护援队友',
                weight: adjustBehaviorWeight(
                  '护援队友',
                  82 + Math.min(48, 护援压力),
                  defender,
                  attacker,
                  behaviorState,
                ),
                build() {
                  return makeNpcAction(
                    '防御',
                    `[护援判断] 防御系NPC已具备承伤条件，直接以普防卡位保护${护援目标.队友.name || '队友'}。`,
                    建立普防护援技能(),
                  );
                },
              });
            }
          }
          if (allyCount > 1 && teamSupportSkill && !selfSnapshot.hasSharedVision) {
            tacticalBranches.push({
              name: '战术协同',
              weight: adjustBehaviorWeight('战术协同', 68, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '战术协同',
                  `[协同铺垫] NPC选择用[${teamSupportSkill.name}]为己方建立更高效的集火与联动节奏。`,
                  teamSupportSkill,
                );
              },
            });
          }
          if (
            (isChargingHighThreat || playerPower >= 220 || selfHpRatio < 0.45) &&
            reactiveDefenseSkill &&
            !selfSnapshot.hasReactiveDefense
          ) {
            tacticalBranches.push({
              name: '借力守势',
              weight: adjustBehaviorWeight('借力守势', 88, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '借力守势',
                  `[借势防御] NPC放弃盲目硬拼，转而用[${reactiveDefenseSkill.name}]等待对手失衡反噬。`,
                  reactiveDefenseSkill,
                );
              },
            });
          }

          if (
            selfSnapshot.hasSharedVision &&
            atkSkill &&
            atkSummary.目标规模 === '单体' &&
            atkSummary.爆发级别 !== '无'
          ) {
            tacticalBranches.push({
              name: '协同点杀',
              weight: adjustBehaviorWeight('协同点杀', enemyHpRatio < 0.5 ? 86 : 70, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '协同点杀',
                  `[视野锁头] NPC借助共享视野完成目标校准，立刻以[${atkSkill.name}]集中打穿要害！`,
                  atkSkill,
                );
              },
            });
          }

          if (enemySnapshot.isLockedOrControlled && atkSkill && (atkSummary.爆发级别 === '高' || atkCastTime >= 18)) {
            tacticalBranches.push({
              name: '连段爆发',
              weight: adjustBehaviorWeight('连段爆发', 82, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '连段爆发',
                  `[顺势爆发] NPC看准对手已被压制，果断接上[${atkSkill.name}]扩大伤害窗口！`,
                  atkSkill,
                );
              },
            });
          }

          if (enemySnapshot.hasDotPressure && executeSkill) {
            tacticalBranches.push({
              name: '压血收束',
              weight: adjustBehaviorWeight(
                '压血收束',
                enemyHpRatio < 0.45 ? 88 : 68,
                defender,
                attacker,
                behaviorState,
              ),
              build() {
                return makeNpcAction(
                  '压血收束',
                  `[持续压杀] NPC判断对手已被持续伤害拖入危险区，立刻以[${executeSkill.name}]完成收束。`,
                  executeSkill,
                );
              },
            });
          }

          if (防御者系别 === '强攻系' && atkSkill && selfHpRatio > 0.4 && playerPower <= npcAtkPower * 1.7) {
            tacticalBranches.push({
              name: '强攻压制',
              weight: adjustBehaviorWeight('强攻压制', enemyHpRatio < 0.5 ? 84 : 72, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '强攻压制',
                  `[正面压制] 身为强攻系，NPC更倾向以[${atkSkill.name}]持续压迫对手，不给其调整空间。`,
                  atkSkill,
                );
              },
            });
          }

          if (防御者系别 === '敏攻系') {
            const 敏攻截断候选技能 =
              (反高速窗口.成立 && (hardControlSkill || skillSealSkill || rangePressureSkill)) ||
              ((threatProfile.incomingAttackIntent === true || isChargingHighThreat) && (hardControlSkill || skillSealSkill || rangePressureSkill)) ||
              null;
            const 敏攻截断技能 = 敏攻截断候选技能 && (() => {
              const 技能 = 敏攻截断候选技能;
              const 目标文本 = String(getSkillTarget(技能) || '').trim();
              if (/群体|全场/.test(目标文本)) return true;
              if (isBattleSkillSealProfile(技能)) return true;
              if (hasBattleSkillRuntimeConsumer(技能, ['interrupt', 'hard_control', 'skill_seal'])) return true;
              return getSkillEffects(技能, { 行为规划: true }).some(effect => {
                const 原型 = String(effect?.原型 || '').trim();
                if (原型 === '资源锁定' || 原型 === '规则改写' || 原型 === '决策干扰' || 原型 === '判定修正' || 原型 === '时窗修正') return true;
                if (原型 !== '状态施加') return false;
                const 战斗效果 = effect?.计算层效果 || {};
                return 战斗效果.skip_turn === true ||
                  战斗效果.cannot_react === true ||
                  战斗效果.silence === true ||
                  战斗效果.skill_seal === true ||
                  Number(战斗效果.lock_level || 0) > 0 ||
                  Number(战斗效果.dodge_penalty || 0) > 0 ||
                  Number(战斗效果.cast_speed_penalty || 0) > 0;
              });
            })()
              ? 敏攻截断候选技能
              : null;
            if (敏攻截断技能) {
              tacticalBranches.push({
                name: '敏攻截断',
                weight: adjustBehaviorWeight(
                  '连锁控制',
                  反高速窗口.成立
                    ? 84 + Math.round(反高速窗口.强度 * 34)
                    : threatProfile.incomingAttackIntent === true
                      ? 76
                      : 62,
                  defender,
                  attacker,
                  behaviorState,
                ),
                build() {
                  return makeNpcAction(
                    '敏攻截断',
                    `[敏攻截断] 敏攻系NPC优先以[${敏攻截断技能.name}]截断对手节奏，再寻找下一次切入窗口。`,
                    敏攻截断技能,
                  );
                },
              });
            }
          }

          if (防御者系别 === '敏攻系' && (executeSkill || atkSkill)) {
            const agileStrike = executeSkill && executeCastTime <= 18 ? executeSkill : atkSkill;
            if (agileStrike) {
              tacticalBranches.push({
                name: '游击收割',
                weight: adjustBehaviorWeight(
                  '游击收割',
                  enemyHpRatio < 0.5 ? 90 : 70,
                  defender,
                  attacker,
                  behaviorState,
                ),
                build() {
                  return makeNpcAction(
                    '游击收割',
                    `[游击切入] 敏攻系NPC抓住节奏缝隙，以[${agileStrike.name}]高速切入撕开战局。`,
                    agileStrike,
                  );
                },
              });
            }
          }

          if (防御者系别 === '防御系' && reactiveDefenseSkill && !selfSnapshot.hasReactiveDefense) {
            tacticalBranches.push({
              name: '坚壁反制',
              weight: adjustBehaviorWeight(
                '坚壁反制',
                isChargingHighThreat || playerPower >= 220 ? 92 : 74,
                defender,
                attacker,
                behaviorState,
              ),
              build() {
                return makeNpcAction(
                  '坚壁反制',
                  `[以守待攻] 防御系NPC更擅长把战局拖入自己的节奏，先用[${reactiveDefenseSkill.name}]筑起反制层。`,
                  reactiveDefenseSkill,
                );
              },
            });
          }

          if (防御者系别 === '控制系' && hardControlSkill && !enemySnapshot.isLockedOrControlled) {
            tacticalBranches.push({
              name: '连锁控制',
              weight: adjustBehaviorWeight('连锁控制', 88, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '连锁控制',
                  `[节奏封锁] 控制系NPC优先争夺节奏，试图用[${hardControlSkill.name}]建立状态链优势。`,
                  hardControlSkill,
                );
              },
            });
          }

          if (
            ['辅助系', '治疗系', '食物系', '召唤系'].includes(防御者系别) &&
            allyCount > 1 &&
            teamSupportSkill &&
            !selfSnapshot.hasSharedVision
          ) {
            tacticalBranches.push({
              name: '统筹增援',
              weight: adjustBehaviorWeight('统筹增援', 82, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '统筹增援',
                  `[团队统筹] 辅助型NPC更重视整体节奏，优先用[${teamSupportSkill.name}]为己方建立协同优势。`,
                  teamSupportSkill,
                );
              },
            });
          }
          if (['辅助系', '治疗系', '食物系'].includes(防御者系别)) {
            const 支援补位技能 = recoverSkill || teamProtectSkill || teamSupportSkill;
            if (支援补位技能) {
              const 支援急迫度 =
                recoverSkill && (selfHpRatio < 0.72 || selfSnapshot.hasBadCondition)
                  ? 120
                  : teamProtectSkill && Number(behaviorState.团队护援压力 || 0) >= 35
                    ? 108
                    : 92;
              tacticalBranches.push({
                name: '支援补位',
                weight: adjustBehaviorWeight('战术协同', 支援急迫度, defender, attacker, behaviorState),
                build() {
                  return makeNpcAction(
                    '支援补位',
                    `[支援判断] ${防御者系别 || '辅助型'}NPC优先履行团队职责，释放[${支援补位技能.name}]补齐治疗/护援/增益窗口。`,
                    支援补位技能,
                  );
                },
              });
            }
          }

          if (isLockedBySpirit && defSkill) {
            tacticalBranches.push({
              name: '危机自保',
              weight: adjustBehaviorWeight('危机自保', 120, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '危机自保',
                  `[气机锁定] NPC被玩家恐怖的精神力死死锁定，不敢轻举妄动，只能全力转为防御！`,
                  defSkill,
                );
              },
            });
          } else if (isChargingHighThreat) {
            if (hardControlSkill) {
              tacticalBranches.push({
                name: '控制截断',
                weight: adjustBehaviorWeight('控制截断', 120, defender, attacker, behaviorState),
                build() {
                  return makeNpcAction(
                    '控制截断',
                    `[破绽捕捉] NPC察觉到玩家正在蓄力，果断释放[${hardControlSkill.name}]试图打断！`,
                    hardControlSkill,
                  );
                },
              });
            }
            if (atkSkill) {
              tacticalBranches.push({
                name: '强势对轰',
                weight: adjustBehaviorWeight('强势对轰', 90, defender, attacker, behaviorState),
                build() {
                  return makeNpcAction(
                    '强势对轰',
                    `[破绽捕捉] NPC见玩家露出极大的前摇破绽，立刻释放[${atkSkill.name}]企图趁机重创玩家！`,
                    atkSkill,
                  );
                },
              });
            }
            tacticalBranches.push({
              name: '伺机闪避',
              weight: adjustBehaviorWeight('伺机闪避', 60, defender, attacker, behaviorState),
              build() {
                return makeNpcAction(
                  '伺机闪避',
                  `[致命预警] NPC察觉到毁灭性的蓄力波动，但无力打断，只能全力游走试图拉开距离。`,
                );
              },
            });
          } else if (isLowHealth || playerPower >= 250 || (isSupport && 主动威胁分层.中威胁)) {
            if (defSkill) {
              tacticalBranches.push({
                name: '危机自保',
                weight: adjustBehaviorWeight('危机自保', 主动威胁分层.低威胁 ? 22 : 60, defender, attacker, behaviorState),
                build() {
                  return makeNpcAction(
                    '危机自保',
                    `[谨慎应对] NPC判断局势不利，选择以[${defSkill.name}]稳住阵脚。`,
                    defSkill,
                  );
                },
              });
            }
            if (atkSkill) {
              let clashWeight = playerPower > npcAtkPower * 1.5 ? 20 : 55;
              tacticalBranches.push({
                name: '强势对轰',
                weight: adjustBehaviorWeight('强势对轰', clashWeight, defender, attacker, behaviorState),
                build() {
                  return makeNpcAction(
                    '强势对轰',
                    playerPower > npcAtkPower * 1.5
                      ? `[孤注一掷] 尽管身处劣势，NPC仍狠下心正面搏杀！`
                      : `[强势对轰] NPC不甘示弱，释放[${atkSkill.name}]正面迎击！`,
                    atkSkill,
                  );
                },
              });
            }
            tacticalBranches.push({
              name: '伺机闪避',
              weight: adjustBehaviorWeight('伺机闪避', 主动威胁分层.低威胁 ? 12 : 85, defender, attacker, behaviorState),
              build() {
                return makeNpcAction('伺机闪避', `[劣势拉扯] NPC判断继续硬拼代价过高，选择拉开步点重排节奏。`);
              },
            });
          } else {
            if (atkSkill) {
              tacticalBranches.push({
                name: '强势对轰',
                weight: adjustBehaviorWeight('强势对轰', 75, defender, attacker, behaviorState),
                build() {
                  return makeNpcAction(
                    '强势对轰',
                    `[主动进攻] NPC抓住节奏，释放[${atkSkill.name}]尝试夺回主动权！`,
                    atkSkill,
                  );
                },
              });
            }
            if (defSkill) {
              tacticalBranches.push({
                name: '危机自保',
                weight: adjustBehaviorWeight('危机自保', 主动威胁分层.低威胁 ? 12 : 45, defender, attacker, behaviorState),
                build() {
                  return makeNpcAction('危机自保', `[稳扎稳打] NPC选择先用[${defSkill.name}]保护自身。`, defSkill);
                },
              });
            }
            tacticalBranches.push({
              name: '伺机闪避',
              weight: adjustBehaviorWeight('伺机闪避', 主动威胁分层.低威胁 ? 6 : 30, defender, attacker, behaviorState),
              build() {
                return makeNpcAction('伺机闪避', `[战术拉扯] NPC利用身法周旋，试图重排节奏。`);
              },
            });
          }

          const 有非兜底实技候选 = tacticalBranches.some(候选 => {
            标记候选预览技能(候选);
            return 候选?.__预览技能 || 候选?.skill;
          });
          if (reactiveThreat || isLowHealth || playerPower >= 250 || !有非兜底实技候选) {
            tacticalBranches.push({
              name: '肉体兜底',
              weight: adjustBehaviorWeight('肉体兜底', 主动威胁分层.低威胁 ? 4 : 20, defender, attacker, behaviorState),
              build() {
                return makeNpcAction('肉体兜底', `[从容应对] NPC无合适魂技，收缩防御，准备肉体硬抗。`, null, {
                  def_mult: 1.2,
                });
              },
            });
          }

          const 去重战术表 = new Map();
          tacticalBranches.forEach(候选 => {
            if (!候选) return;
            标记候选预览技能(候选);
            const 预览技能 = 候选.__预览技能 || 候选.skill || null;
            const 技能名 = String(预览技能?.name || 预览技能?.技能名称 || 候选.name || '').trim();
            const 去重键 = 技能名 || `动作:${候选.name || ''}`;
            const 旧候选 = 去重战术表.get(去重键);
            if (!旧候选 || Number(候选.weight || 0) > Number(旧候选.weight || 0)) 去重战术表.set(去重键, 候选);
          });

          const 去重战术列表 = [...去重战术表.values()];
          if (behaviorState?.主动回合 === true) {
            const 有实技候选 = 去重战术列表.some(候选 => {
              const 名称 = String(候选?.name || '').trim();
              return 候选?.__预览技能 && !['伺机闪避', '肉体兜底'].includes(名称) && Number(候选.weight || 0) >= 32;
            });
            if (有实技候选) {
              去重战术列表.forEach(候选 => {
                const 名称 = String(候选?.name || '').trim();
                if (名称 === '肉体兜底') 候选.weight = reactiveThreat ? Math.min(Number(候选.weight || 0), 12) : 0;
                if (名称 === '伺机闪避') 候选.weight = reactiveThreat ? Math.min(Number(候选.weight || 0), 18) : 0;
              });
            } else if (主动威胁分层.低威胁) {
              去重战术列表.forEach(候选 => {
                const 名称 = String(候选?.name || '').trim();
                if (名称 === '肉体兜底') 候选.weight = Math.min(Number(候选.weight || 0), 4);
                if (名称 === '伺机闪避') 候选.weight = Math.min(Number(候选.weight || 0), 8);
              });
            }
          }
          const 最优规避或截断 = 去重战术列表
            .filter(候选 => /伺机闪避|闪避|位移规避|敏攻截断|控制截断|短前摇对轰/.test(`${候选?.name || ''}`))
            .sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0))[0];
          if (最优规避或截断 && (试探阶段 || 来袭含控制减益)) {
            去重战术列表.forEach(候选 => {
              const 名称 = String(候选?.name || '').trim();
              if (!/肉体兜底|承伤硬抗/.test(名称)) return;
              const 优势 = Number(最优规避或截断.weight || 0) - Number(候选.weight || 0);
              if (来袭含控制减益 || 优势 >= 24) {
                候选.weight = 0;
                候选.__主动规划审计 = {
                  ...(候选.__主动规划审计 || {}),
                  选择原因: 来袭含控制减益
                    ? '来袭包含控制或减益，通用硬抗不能替代规避/截断判定'
                    : '试探阶段规避收益显著更高，通用硬抗退出战术池',
                };
              }
            });
          }
          return 去重战术列表;
        }

      // ==========================================
      // 📍 NPC 决策逻辑 (真实读取版)
        // ==========================================
        function determineNpcAction(combatData, playerAction, ratio) {
          hydrateCombatData(combatData);
          let defender = combatData.参战者.team_enemy?.[0];
          let attacker = combatData.参战者.team_player?.[0];

          function makeNpcAction(type, log, skill = null, extra = {}) {
            const normalizedSkill = skill ? normalizeSkillData(skill, skill.name || skill.技能名称 || type) : null;
            if (normalizedSkill) 写入战斗来源类别上下文(normalizedSkill, 读取战斗来源类别上下文(skill, 规范化战斗来源类别(type, '魂技')));
            return Object.assign(
              {
                type,
                log: replaceBattleReportGenericNames(log, { player: attacker, enemy: defender }),
                skill: normalizedSkill,
                def_mult: 1.0,
              },
              extra,
            );
          }

          if (ratio < 0.5) {
            return makeNpcAction(
              '无法反应',
              '[速度碾压] NPC根本无法看清攻击轨迹，来不及做出任何反应！本回合防御力减半。',
              null,
              { def_mult: 0.5 },
            );
          }

          const combatType = combatData.战斗类型 || '突发遭遇';
          const isDeadlyFight = combatType === '死战' || combatType === '突发遭遇';
          const lvDiff = (attacker.lv || 1) - (defender.lv || 1);
          const playerPower = Number(getPrimaryDamageEffect(playerAction.skill)?.威力倍率 || 0) || 0;
          const isChargingHighThreat =
            !!attacker.蓄力技能 ||
            playerAction.action_type === '蓄力挨打' ||
            ((playerAction.cast_time || 0) >= 20 && playerPower >= 120);
          const 防御者系别 = 读取规划单位系别(defender);
          const isSupport = ['辅助系', '治疗系', '食物系', '召唤系'].includes(防御者系别);
          const isLowHealth = getCombatHpRatio(defender) < 0.3;
          const activeBuffs = Object.keys(defender.状态效果 || {});
          const allyTeam = 读取战斗主队单位列表(combatData, '敌方').filter(unit => unit.name !== defender.name);
          const availableSkills = collectCombatSkills(defender, allyTeam);
          const strategicContext = buildStrategicCandidates(
            defender,
            attacker,
            combatData,
            playerAction,
            ratio,
            availableSkills,
            allyTeam,
            makeNpcAction,
          );
          const behaviorState = strategicContext.behaviorState;
          behaviorState.规划上下文 = 构建规划上下文(defender, attacker, combatData, behaviorState);
          behaviorState.规划上下文.锁定敌对目标 = attacker;
          behaviorState.锁定敌对目标 = attacker;
          const strategicAction = chooseAndBuildActorAction(
            defender,
            attacker,
            behaviorState,
            strategicContext.strategicBranches,
            '行为预演/战略阶段',
          );
          if (strategicAction) return strategicAction;
          const skillContext = buildNpcSkillCandidateContext(
            defender,
            attacker,
            playerAction,
            availableSkills,
            behaviorState,
            activeBuffs,
            isLowHealth,
          );
          const tacticalBranches = buildTacticalCandidates(
            defender,
            attacker,
            playerAction,
            behaviorState,
            skillContext,
            makeNpcAction,
            isSupport,
            isLowHealth,
          );
          const 玩家技能 = playerAction?.skill || null;
          const 玩家目标文本 = String(getSkillTarget(玩家技能 || {}) || '').trim();
          const 玩家范围控制来袭 =
            !!玩家技能 &&
            (
              isBattleSkillControlProfile(玩家技能) ||
              /群体|全场/.test(玩家目标文本) ||
              hasBattleSkillRuntimeConsumer(玩家技能, ['interrupt', 'hard_control', 'skill_seal'])
            );
          const 敌方具备真截断 =
            !!(skillContext.hardControlSkill || skillContext.skillSealSkill) &&
            判定技能具备真实截断资格_V1(skillContext.hardControlSkill || skillContext.skillSealSkill);
          if (玩家范围控制来袭 && !敌方具备真截断) {
            tacticalBranches.forEach(候选 => {
              if (!候选) return;
              if (候选.name === '强势对轰') 候选.weight = Math.max(0, Number(候选.weight || 0) - 58);
              if (候选.name === '伺机闪避') 候选.weight = Number(候选.weight || 0) + 36;
              if (候选.name === '危机自保') 候选.weight = Number(候选.weight || 0) + 22;
            });
          }

          const tacticalAction = chooseAndBuildActorAction(
            defender,
            attacker,
            behaviorState,
            tacticalBranches,
            '常规战术阶段',
            `${skillContext.skillTraceLog ? skillContext.skillTraceLog + ' | ' : ''}`,
          );
          if (tacticalAction) return tacticalAction;
          const tacticalChoice = chooseActorActionByCandidates(
            defender,
            attacker,
            behaviorState,
            tacticalBranches,
            '常规战术阶段',
          );
          return makeNpcAction(
            '肉体兜底',
            `${skillContext.skillTraceLog ? skillContext.skillTraceLog + ' | ' : ''}${tacticalChoice.trace ? tacticalChoice.trace + ' ' : ''}[从容应对] NPC无合适魂技，收缩防御，准备肉体硬抗。`,
            null,
            { def_mult: 1.2 },
          );
        }

        // ==========================================
        // 📍 5. 高阶机制伤害修饰与炸膛判定
        // ==========================================
        function applyHighTierMechanics(attackerChar, defenderChar, playerAction, baseResult) {
          let result = { ...baseResult };
          refreshSettleResultProjectedDamage(result);
          const primaryTargetEntry = summarizeProjectedDamageEntries(result.targetResults).primaryEntry;
          const primaryTarget = primaryTargetEntry?.target || defenderChar || null;
          let attackerStats = attackerChar.属性 || attackerChar;
          let defenderStats = primaryTarget?.属性 || primaryTarget || defenderChar?.属性 || defenderChar || {};
          const unlockedAttributeSet = new Set(collectBattleUnlockedAttributeTokens(attackerChar));
          const rewriteTargetResultsDamage = mapper => {
            if (!Array.isArray(result.targetResults) || !result.targetResults.length) return;
            result.targetResults = result.targetResults.map((entry, index) => ({
              ...entry,
              damage: Math.max(0, Math.floor(Number(mapper(entry, index) ?? entry.damage ?? 0))),
            }));
            refreshSettleResultProjectedDamage(result);
          };

          const skillElementLabel = getBattleSkillDisplayElement(playerAction.skill || {});
          let isHoly = skillElementLabel === '神圣' || skillElementLabel === '光明' || skillElementLabel === '生命';
          if (isHoly) {
            let doubledTargets = 0;
            rewriteTargetResultsDamage(entry => {
              const targetStats = entry?.target?.属性 || entry?.target || {};
              if (targetStats.is_evil) {
                doubledTargets += 1;
                return Number(entry?.damage || 0) * 2;
              }
              return Number(entry?.damage || 0);
            });
            if (doubledTargets > 0) {
              const targetSuffix = doubledTargets > 1 ? `，共压制 ${doubledTargets} 个邪属性目标` : '';
              result.desc += ` [神圣克制] 纯粹的光明之力如同沸水泼雪，无视等级压制，对邪魂师造成双倍真实伤害${targetSuffix}！`;
            }
          }

          if (playerAction.action_type === '多元素融合') {
            const fusionElements = sortBattleFusionElements(
              playerAction.fusionElements || playerAction.skill?.附带属性 || [],
            );
            const fusionSemantics = resolveBattleFusionSemantics(fusionElements);
            const missingFusionPermissions = fusionElements.filter(token => !unlockedAttributeSet.has(token));
            const gatedFusionSemantics = missingFusionPermissions.length
              ? {
                  ...fusionSemantics,
                  multiplier: 1,
                  failAdjust: 0,
                  derivedEffects: [],
                  summary: `${fusionSemantics.summary} 但当前缺少调用权：${missingFusionPermissions.join('/')}，只能按普通并行融合处理。`,
                }
              : fusionSemantics;
            const fusionPattern = String(
              gatedFusionSemantics.pattern || playerAction.fusionPattern || buildBattleFusionPattern(fusionElements),
            );
            const fusionLabel = fusionElements.length ? fusionPattern : `${fusionElements.length || 2}种元素`;
            let elementCount = fusionElements.length || 2;
            let failRate = 0;
            let baseFailRate = (elementCount - 1) * 35 + Number(gatedFusionSemantics.failAdjust || 0);
            let menAdvantage = (attackerStats.men / defenderStats.men_max) * 15;
            failRate = Math.max(5, baseFailRate - menAdvantage);

            let roll = Math.floor(Math.random() * 100) + 1;
            if (fusionElements.length) {
              result.desc += ` [组合语义] ${gatedFusionSemantics.pattern}：${gatedFusionSemantics.summary}`;
              if (Array.isArray(gatedFusionSemantics.derivedEffects) && gatedFusionSemantics.derivedEffects.length) {
                result.desc += ` [衍生能力] ${gatedFusionSemantics.derivedEffects.join(' / ')}`;
              }
            }

            if (roll <= failRate) {
              rewriteTargetResultsDamage(() => 0);
              result.backlash_dmg = Math.floor(getCombatHpMaxValue(attacker) * 0.3);
              result.desc += ` [元素炸膛] 精神力失控！${fusionLabel}在手中轰然引爆，遭到极致反噬！(Roll: ${roll} <= 炸膛率: ${Math.floor(failRate)}%)`;
            } else {
              let multiplier = Math.pow(1.5, elementCount - 1) * Number(gatedFusionSemantics.multiplier || 1);
              if (playerAction.is_charged) {
                multiplier *= 1.5;
                result.desc += ` [极致蓄力] 元素被压缩到极致！`;
              }
              rewriteTargetResultsDamage(entry => Number(entry?.damage || 0) * multiplier);
              result.desc += ` [元素共鸣] ${fusionLabel}完美融合！威力呈指数级暴涨！(威力倍率: x${multiplier.toFixed(2)})`;
            }
          }

          return refreshSettleResultProjectedDamage(result);
        }

        // ==========================================
        // 📍 6. 辅助区：意图解析与NPC决策 (真实读取版)
        // ==========================================
        function parseSerializedPlayerActionQueue(playerInput) {
          const source = String(playerInput || '');
          const match = source.match(/\[动作队列\]([\s\S]*?)\[\/动作队列\]/i);
          if (!match) return [];
          try {
            const parsed = JSON.parse(match[1]);
            return Array.isArray(parsed) ? parsed.filter(item => item && typeof item === 'object') : [];
          } catch (error) {
            console.warn('battle action queue parse failed', error);
            return [];
          }
        }

        function 读取序列化批量目标(entry = {}) {
          const 名称列表 = [];
          const 对象列表 = [];
          [
            entry.__批量目标列表,
            entry.__批量目标名称列表,
            entry.目标列表,
            entry.目标名称列表,
          ].forEach(候选 => {
            if (Array.isArray(候选)) {
              候选.forEach(项目 => {
                if (!项目) return;
                if (typeof 项目 === 'object') 对象列表.push(项目);
                else 名称列表.push(String(项目 || '').trim());
              });
            } else if (typeof 候选 === 'string') {
              候选
                .split(/[、,，;；\s]+/)
                .map(项目 => 项目.trim())
                .filter(Boolean)
                .forEach(项目 => 名称列表.push(项目));
            }
          });
          return {
            名称列表: [...new Set(名称列表.filter(Boolean))],
            对象列表: dedupeCombatTargetList(对象列表),
          };
        }

        function buildPlayerActionFromSerializedEntry(entry, ownerName = '', ownerChar = null) {
          if (!entry || typeof entry !== 'object') return null;
          const expectedOwner = String(ownerName || '').trim();
          const serializedOwner = String(entry.actor_name || entry.__行动者名 || entry.行动者 || '').trim();
          if (serializedOwner && expectedOwner && serializedOwner !== expectedOwner) return null;
          const actionType = String(entry.action_type || entry.type || entry.skill?.技能分类 || '常规攻击').trim() || '常规攻击';
          const rawSkill = entry.skill && typeof entry.skill === 'object' ? entry.skill : { name: actionType };
          const skillName = String(
            rawSkill.魂技名 || rawSkill.name || rawSkill.技能名称 || actionType,
          ).trim() || actionType;
          const 来源类别 = 规范化战斗来源类别(entry.category || entry.source_detail || entry.action_type, '魂技');
          const 来源明细 = entry.source_detail || entry.category || entry.action_type || '魂技';
          const 魂环路径 = Array.isArray(entry.__魂环路径)
            ? entry.__魂环路径.map(片段 => String(片段))
            : Array.isArray(rawSkill.__魂环路径)
              ? rawSkill.__魂环路径.map(片段 => String(片段))
              : [];
          const 魂技槽位 = String(entry.__魂技槽位 || rawSkill.__魂技槽位 || '').trim();
          const 当前槽位技能 = 魂环路径.length ? 按魂环路径读取当前角色魂技_战斗(ownerChar, 魂环路径, 魂技槽位) : null;
          const executionSkill = 当前槽位技能?.技能 || rawSkill;
          const executionSkillName = String(
            executionSkill.魂技名 || executionSkill.name || executionSkill.技能名称 || 当前槽位技能?.槽位 || skillName,
          ).trim() || skillName;
          const actionCastTime = 当前槽位技能
            ? (Number(executionSkill.前摇 ?? 10) || 10)
            : (Number(entry.前摇 ?? executionSkill.前摇 ?? rawSkill.前摇 ?? 10) || 10);
          const nextAction = {
            action_type: actionType,
            cast_time: actionCastTime,
            is_charged: entry.is_charged === true,
            前摇已结算: entry.前摇已结算 === true,
            category: 来源类别,
            source_detail: 来源明细,
            skill: 写入战斗来源类别上下文(normalizeSkillData(executionSkill, executionSkillName), {
              来源类别,
              来源明细,
              魂环路径,
              魂技槽位: 当前槽位技能?.槽位 || 魂技槽位,
            }),
          };
          if (expectedOwner) nextAction.actor_name = expectedOwner;
          if (魂环路径.length) nextAction.skill.__魂环路径 = [...魂环路径];
          if (当前槽位技能?.槽位 || 魂技槽位) nextAction.skill.__魂技槽位 = 当前槽位技能?.槽位 || 魂技槽位;
          if (entry.equip_target) nextAction.equip_target = String(entry.equip_target || '').trim();
          if (entry.target_name) nextAction.target_name = String(entry.target_name || '').trim();
          if (entry.物品名 || entry.skill?.__物品名) nextAction.物品名 = String(entry.物品名 || entry.skill.__物品名 || '').trim();
          if (entry.食用目标) nextAction.食用目标 = String(entry.食用目标 || '').trim();
          if (entry.造物处理) nextAction.造物处理 = String(entry.造物处理 || '').trim();
          if (entry.物品接收者) nextAction.物品接收者 = String(entry.物品接收者 || '').trim();
          if (entry.立即使用 === true) nextAction.立即使用 = true;
          if (entry.立即食用 === true || entry.即食 === true) nextAction.立即食用 = true;
          if (entry.heal_ratio !== undefined) nextAction.heal_ratio = Number(entry.heal_ratio || 0) || 0;
          const 批量目标 = 读取序列化批量目标(entry);
          if (批量目标.对象列表.length) nextAction.__批量目标列表 = 批量目标.对象列表;
          if (批量目标.名称列表.length) nextAction.__批量目标名称列表 = 批量目标.名称列表;
          if (Array.isArray(entry.__炸环选择路径列表)) {
            nextAction.__炸环选择路径列表 = entry.__炸环选择路径列表
              .filter(路径 => Array.isArray(路径) && 路径.length)
              .map(路径 => 路径.map(片段 => String(片段)));
            nextAction.skill.__炸环选择路径列表 = nextAction.__炸环选择路径列表.map(路径 => [...路径]);
          }
          if (entry.__批量消耗模式) {
            nextAction.__批量消耗模式 = String(entry.__批量消耗模式 || '').trim();
          }
          if (Array.isArray(entry.fusionElements)) {
            nextAction.fusionElements = normalizeBattleSkillAttributeTokens(entry.fusionElements);
          }
          if (entry.fusionPattern) nextAction.fusionPattern = String(entry.fusionPattern || '').trim();
          return nextAction;
        }

        function 序列化动作属于当前角色(entry = {}, charData = {}, alliedTeam = [], options = {}) {
          const strict = options?.strict === true;
          const actionType = String(entry?.action_type || entry?.type || '').trim();
          if (!['释放魂技', '武魂融合技', '使用物品'].includes(actionType)) return true;
          if (actionType === '使用物品') {
            const itemName = String(entry?.物品名 || entry?.skill?.__物品名 || entry?.skill?.name || entry?.skill?.魂技名 || '').trim();
            if (itemName && charData?.背包 && Object.prototype.hasOwnProperty.call(charData.背包, itemName)) return true;
            const alliedHasItem = (Array.isArray(alliedTeam) ? alliedTeam : []).some(unit => unit?.背包 && Object.prototype.hasOwnProperty.call(unit.背包, itemName));
            return alliedHasItem ? false : !strict;
          }
          const skillName = String(entry?.skill?.魂技名 || entry?.skill?.name || entry?.skill?.技能名称 || '').trim();
          if (!skillName) return !strict;
          const 魂环路径 = Array.isArray(entry?.__魂环路径)
            ? entry.__魂环路径.map(片段 => String(片段))
            : Array.isArray(entry?.skill?.__魂环路径)
              ? entry.skill.__魂环路径.map(片段 => String(片段))
              : [];
          const 魂技槽位 = String(entry?.__魂技槽位 || entry?.skill?.__魂技槽位 || '').trim();
          if (魂环路径.length) {
            const 魂环 = 按路径读取对象_V1(charData, 魂环路径);
            if (!魂环 || typeof 魂环 !== 'object') {
              const 友方命中 = (Array.isArray(alliedTeam) ? alliedTeam : []).some(队友 => {
                const 友方魂环 = 按路径读取对象_V1(队友, 魂环路径);
                return !!(友方魂环 && typeof 友方魂环 === 'object');
              });
              return 友方命中 ? false : !strict;
            }
            if (魂技槽位) {
              const 槽位技能 = 魂环[魂技槽位];
              if (槽位技能 && typeof 槽位技能 === 'object') return true;
              const 友方槽位命中 = (Array.isArray(alliedTeam) ? alliedTeam : []).some(队友 => {
                const 友方魂环 = 按路径读取对象_V1(队友, 魂环路径);
                return !!(友方魂环 && typeof 友方魂环 === 'object' && 友方魂环[魂技槽位] && typeof 友方魂环[魂技槽位] === 'object');
              });
              return 友方槽位命中 ? false : !strict;
            }
            return 取魂环魂技条目_战斗(魂环).some(([槽位名, 槽位技能]) =>
              String(槽位技能?.魂技名 || 槽位技能?.name || 槽位技能?.技能名称 || 槽位名).trim() === skillName
            );
          }
          if (/^第\d+魂技(?:_2)?$/.test(skillName)) return !strict;
          const 当前角色命中 = collectCombatSkills(charData, alliedTeam).some(skill =>
            String(skill?.name || skill?.魂技名 || skill?.技能名称 || '').trim() === skillName
          );
          if (当前角色命中) return true;
          const 友方命中 = (Array.isArray(alliedTeam) ? alliedTeam : []).some(队友 =>
            collectCombatSkills(队友, []).some(skill => String(skill?.name || skill?.魂技名 || skill?.技能名称 || '').trim() === skillName)
          );
          return 友方命中 ? false : !strict;
        }

        function 序列化动作确属他人(entry = {}, charData = {}, alliedTeam = []) {
          return !序列化动作属于当前角色(entry, charData, alliedTeam, { strict: true });
        }

        function 解析玩家输入魂技槽位(playerInput = '', charData = {}) {
          const 输入 = String(playerInput || '');
          const 数字映射 = { 一: 1, 二: 2, 三: 3, 四: 4, 五: 5, 六: 6, 七: 7, 八: 8, 九: 9, 十: 10 };
          const 命中 = 输入.match(/第\s*(\d+|[一二三四五六七八九十])\s*魂技/);
          if (!命中) return null;
          const 序号 = Number(命中[1]) || 数字映射[命中[1]] || 0;
          if (!(序号 > 0)) return null;
          const 槽位 = `第${序号}魂技`;
          for (const [spiritName, spirit] of 取角色武魂条目_战斗(charData)) {
            for (const [soulSpiritName, soulSpirit] of 取武魂魂灵条目_战斗(spirit)) {
              for (const [ringIndex, ring] of 取魂灵魂环条目_战斗(soulSpirit)) {
                const skill = ring?.[槽位];
                if (skill && typeof skill === 'object' && !Array.isArray(skill)) {
                  const name = String(skill.魂技名 || skill.name || 槽位).trim();
                  const runtimeSkill = normalizeSkillData(skill, name);
                  写入战斗来源类别上下文(runtimeSkill, {
                    来源类别: '魂技',
                    来源明细: spirit?.表象名称 || spiritName || soulSpiritName || `第${ringIndex}魂环`,
                    魂环路径: [spiritName, soulSpiritName, ringIndex],
                    魂技槽位: 槽位,
                  });
                  return runtimeSkill;
                }
              }
            }
            for (const [ringIndex, ring] of 取武魂直接魂环条目_战斗(spirit)) {
              const skill = ring?.[槽位];
              if (skill && typeof skill === 'object' && !Array.isArray(skill)) {
                const name = String(skill.魂技名 || skill.name || 槽位).trim();
                const runtimeSkill = normalizeSkillData(skill, name);
                写入战斗来源类别上下文(runtimeSkill, {
                  来源类别: '魂技',
                  来源明细: spirit?.表象名称 || spiritName || `第${ringIndex}魂环`,
                  魂环路径: [spiritName, ringIndex],
                  魂技槽位: 槽位,
                });
                return runtimeSkill;
              }
            }
          }
          return null;
        }

        function 动作允许同名连放(单位 = {}, 动作 = {}) {
          const 技能 = 动作?.skill && typeof 动作.skill === 'object' ? 动作.skill : {};
          if (String(单位?.type || 单位?.系别 || '').trim() === '食物系') return true;
          const 技能文本 = `${动作?.action_type || ''} ${技能?.name || ''} ${技能?.魂技名 || ''} ${JSON.stringify(技能?._效果数组 || [])}`;
          if (/食物/.test(技能文本)) return true;
          const 战斗目标类型 = inferSkillPrimaryTargetKind(技能);
          if (!['自身', '友方单体', '友方群体'].includes(战斗目标类型)) return true;
          return !isSupportLikeSkill(技能);
        }

        function 读取动作去重目标键(动作 = {}) {
          const 批量名称 = 读取动作批量目标原始列表(动作)
            .map(项目 => (typeof 项目 === 'object' ? String(项目?.name || 项目?.名称 || '') : String(项目 || '')))
            .map(名称 => 名称.trim())
            .filter(Boolean);
          if (批量名称.length) return 批量名称.sort().join('/');
          return String(动作.target_name || 动作.skill?.目标 || '默认目标').trim() || '默认目标';
        }

        function 去重动作队列友方辅助(单位, 动作) {
          if (!动作 || typeof 动作 !== 'object') return 动作;
          const 原队列 = [...(Array.isArray(动作.pre_actions) ? 动作.pre_actions : []), 动作];
          const 已见 = new Set();
          const 保留 = [];
          原队列.forEach(候选动作 => {
            if (!候选动作 || typeof 候选动作 !== 'object') return;
            if (动作允许同名连放(单位, 候选动作)) {
              保留.push(候选动作);
              return;
            }
            const 技能名 = String(候选动作.skill?.name || 候选动作.skill?.魂技名 || 候选动作.action_type || '').trim();
            const 去重键 = `${技能名}:${读取动作去重目标键(候选动作)}`;
            if (已见.has(去重键)) return;
            已见.add(去重键);
            保留.push(候选动作);
          });
          if (!保留.length) {
            return {
              action_type: '观察',
              cast_time: 1,
              skill: normalizeSkillData({ name: '观察', 技能分类: '辅助', 消耗: '无', 前摇: 1 }, '观察'),
            };
          }
          const 主动作 = 保留[保留.length - 1];
          主动作.pre_actions = 保留.slice(0, -1);
          return 主动作;
        }

        function parsePlayerIntent(playerInput, combatDataOverride = null) {
          let combatData = combatDataOverride || window.BattleUIBridge?.getMVU('world.战斗');
          hydrateCombatData(combatData);
          let attacker = combatData.参战者.team_player?.[0];
          const preferredPlayerName = String(window.BattleUIBridge?.getMVU('sys.玩家名') || attacker?.name || '').trim();
          const mvuCharData =
            (attacker?.name ? window.BattleUIBridge?.getMVU('char.' + attacker.name) : null) ||
            (preferredPlayerName && preferredPlayerName === String(attacker?.name || '').trim() ? window.BattleUIBridge?.getMVU('char.' + preferredPlayerName) : null);
          let charData = combatDataOverride
            ? deepClonePlain(mvuCharData || attacker)
            : mvuCharData;
          bindCombatParticipant(charData);

          let action = {
            action_type: '常规攻击',
            cast_time: 10,
            is_charged: false,
            skill: normalizeSkillData(
              {
                name: '普通攻击',
                技能分类: '输出',
                消耗: '无',
                前摇: 10,
                _效果数组: [
                  { 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 },
                ],
              },
              '普通攻击',
            ),
          };
          if (!charData) return action;

          const serializedQueue = parseSerializedPlayerActionQueue(playerInput);
          if (serializedQueue.length) {
            const attackerName = String(attacker?.name || attacker?.名称 || '').trim();
            const 玩家同队单位 = 读取战斗主队单位列表(combatData, '玩家').filter(unit => unit.name !== charData.name);
            const hasForeignOwner = serializedQueue.some(entry => {
              const owner = String(entry?.actor_name || entry?.__行动者名 || entry?.行动者 || '').trim();
              const actionType = String(entry?.action_type || entry?.type || '').trim();
              if (['释放魂技', '武魂融合技', '使用物品'].includes(actionType) && !owner) return true;
              return owner && attackerName && owner !== attackerName;
            });
            const hasForeignSkill = serializedQueue.some(entry => 序列化动作确属他人(entry, charData, 玩家同队单位));
            const queueActions = hasForeignOwner
              ? []
              : hasForeignSkill
                ? []
                : serializedQueue.map(entry => buildPlayerActionFromSerializedEntry(entry, attackerName, charData)).filter(Boolean);
            if (hasForeignOwner || hasForeignSkill) {
              const 队列行动者列表 = [...new Set(serializedQueue
                .map(entry => String(entry?.actor_name || entry?.__行动者名 || entry?.行动者 || '').trim() || '旧动作队列')
                .filter(Boolean))];
              return {
                action_type: '施法失败',
                cast_time: 0,
                skill: normalizeSkillData({ name: '动作未确认', 技能分类: '辅助', 消耗: '无', 前摇: 0, _效果数组: [] }, '动作未确认'),
                __解析失败日志: `[动作取消] 动作队列属于${队列行动者列表.join('、') || '其他角色'}，当前行动者是${attackerName || '当前参战者'}，已取消本次释放，请重新选择魂技。`,
              };
            }
            if (queueActions.length) {
              const mainAction = queueActions[queueActions.length - 1];
              const preActions = queueActions.slice(0, -1);
              if (preActions.length) mainAction.pre_actions = preActions;
              return 去重动作队列友方辅助(attacker, mainAction);
            }
          }

          if (/收回召唤|召回召唤|收回.*召唤|召回/.test(playerInput)) {
            action.action_type = '收回召唤';
            action.cast_time = 8;
            action.skill = normalizeSkillData({ name: '收回召唤', 技能分类: '辅助', 消耗: '无', 前摇: 8 }, '收回召唤');
            return action;
          }

          if (/防御/.test(playerInput) && !/普通攻击|攻击|伤害/.test(playerInput)) {
            action.action_type = '防御';
            action.cast_time = 10;
            action.skill = normalizeSkillData({ name: '防御', 技能分类: '防御', 消耗: '无', 前摇: 10 }, '防御');
            return action;
          }
          if (/闪避/.test(playerInput)) {
            action.action_type = '闪避';
            action.cast_time = 12;
            action.skill = normalizeSkillData({ name: '闪避', 技能分类: '防御', 消耗: '体力:5%', 前摇: 12 }, '闪避');
            return action;
          }
          if (/撤离|逃跑|逃走|脱离/.test(playerInput)) {
            action.action_type = '撤离';
            action.cast_time = 20;
            action.skill = normalizeSkillData({ name: '撤离', 技能分类: '辅助', 消耗: '无', 前摇: 20 }, '撤离');
            return action;
          }

          let matchedSkill = null;
          let matchedSkillName = '';
          // 为了支持多重施法，我们需要找出所有被提及的技能。但为了保持单技能模式兜底，我们先选出最主要的那个。
          // TODO: 后续可以升级为返回技能数组，这里先保留主技能逻辑，把时间累计放进 pre_actions 处理中
          const 玩家同队单位 = 读取战斗主队单位列表(combatData, '玩家').filter(unit => unit.name !== charData.name);
          let directSkills = collectCombatSkills(charData, 玩家同队单位);
          directSkills.forEach(skill => {
            const 完整技能名 = String(skill.name || '').trim();
            const 去融合前缀名 = 完整技能名.replace(/^武魂融合技·/, '');
            const 主体技能名 = 去融合前缀名.replace(/·[^·\s]+$/, '');
            const 命中主体技能 = !!(主体技能名 && playerInput.includes(主体技能名));
            if ((完整技能名 && playerInput.includes(完整技能名)) || (去融合前缀名 && playerInput.includes(去融合前缀名))) {
              matchedSkill = skill;
              matchedSkillName = 完整技能名 || 去融合前缀名;
              return;
            }
            if (命中主体技能 && !matchedSkill) {
              matchedSkill = skill;
              matchedSkillName = 完整技能名 || 主体技能名;
            }
          });
          if (!matchedSkill) {
            const slotSkill = 解析玩家输入魂技槽位(playerInput, charData);
            if (slotSkill) {
              matchedSkill = slotSkill;
              matchedSkillName = String(slotSkill.name || slotSkill.魂技名 || slotSkill.__魂技槽位 || '魂技').trim();
            }
          }

          if (!matchedSkill && /触发|发动|引爆|引动|激活/.test(playerInput)) {
            action.action_type = '触发状态';
            action.cast_time = 5;
            action.skill = normalizeSkillData({ name: '触发状态', 技能分类: '辅助', 消耗: '无', 前摇: 5, _效果数组: [] }, '触发状态');
            action.玩家输入 = String(playerInput || '');
            return action;
          }

          if (matchedSkill) {
            action.action_type = matchedSkillName.includes('武魂融合技') ? '武魂融合技' : '释放魂技';
            action.cast_time = getSkillCastTime(matchedSkill) || 10;
            action.skill = normalizeSkillData(matchedSkill, matchedSkillName);
            action.玩家输入 = String(playerInput || '');
            if (/食用|即食|吃下|喂给|直接吃/.test(playerInput)) action.立即食用 = true;
            if (
              /全队|全体|所有人|所有队友|全部队友/.test(playerInput) &&
              isSupportLikeSkill(action.skill) &&
              inferSkillPrimaryTargetKind(action.skill) === '友方单体'
            ) {
              action.__批量目标名称列表 = ['全队'];
              action.__批量消耗模式 = '单体批量';
              action.__连放来源 = '玩家声明批量辅助';
            }
          }

          // 💥 [核心改造] 副动作(Pre-Actions)多维复合解析引擎
          // 把玩家提及的额外操作统一压入 pre_actions 数组。之后会在外层判断总 cast_time。
          action.pre_actions = [];

          // 1. 生命之火 (通常认为是极速瞬间爆发)
          if (playerInput.includes('生命之火')) {
            let lifeFireSkillData = charData.血脉之力?.技能?.['点燃生命之火'];
            if (lifeFireSkillData) {
              action.pre_actions.push({
                action_type: '点燃生命之火',
                skill: normalizeSkillData(lifeFireSkillData, '点燃生命之火'),
                cast_time: 5, // 给一点微小的基础耗时，防止无限套娃
              });
            }
          }

          // 2. 穿戴斗铠 (极其吃前摇)
          if (
            playerInput.includes('斗铠') &&
            (playerInput.includes('穿') || playerInput.includes('释放') || playerInput.includes('附体'))
          ) {
            let armorLv = charData.装备?.斗铠?.等级 || 1;
            let isRejected = charData.装备?.斗铠?._已排异 || false;
            let minQ = Infinity;
            let pCount = 0;
            if (charData.装备?.斗铠?.parts) {
              Object.values(charData.装备.斗铠.parts).forEach(p => {
                if (p.状态 !== '未打造' && p.状态 !== '重创') {
                  if (p.品质系数 < minQ) minQ = p.品质系数;
                  pCount++;
                }
              });
            }

            let armorCast = Math.max(0, 20 - armorLv * 5);
            if (armorLv === 1 && !isRejected && minQ > 1.2 && pCount > 0) {
              armorCast = Math.max(0, armorCast - 5);
            }

            // 不再硬性判断是否瞬间，统统塞进去，外层的 40上限 机制会教他做人
            action.pre_actions.push({
              action_type: '穿戴装备',
              equip_target: 'armor',
              cast_time: armorCast,
              skill: normalizeSkillData({
                name: armorCast <= 0 ? '斗铠瞬间附体' : '斗铠附体读条',
                技能分类: '辅助',
                消耗: '无',
              }),
            });
            playerInput = playerInput.replace(/斗铠/g, '已解析的斗铠');
          }

          // 3. 展开各类领域 (通常较快)
          if (playerInput.includes('斗铠领域')) {
            action.pre_actions.push({
              action_type: '展开斗铠领域',
              cast_time: 0,
              skill: normalizeSkillData({ name: '展开斗铠领域', 技能分类: '辅助', 消耗: '无' }),
            });
          } else if (playerInput.includes('精神领域')) {
            action.pre_actions.push({
              action_type: '展开精神领域',
              cast_time: 0,
              skill: normalizeSkillData({ name: '展开精神领域', 技能分类: '辅助', 消耗: '无' }),
            });
          } else if (playerInput.includes('武魂领域')) {
            action.pre_actions.push({
              action_type: '展开武魂领域',
              cast_time: 0,
              skill: normalizeSkillData({ name: '展开武魂领域', 技能分类: '辅助', 消耗: '无' }),
            });
          }

          // 4. 武魂融合技判定 (这是一个巨大的主动作，会覆盖掉普通的释放魂技)
          if (playerInput.includes('武魂融合技')) {
            let hasFusion = false;
            let 融合失败原因 = '武魂融合技条件不足';
            Object.entries(charData.武魂融合技 || {}).forEach(([fusionName, fusionSkill]) => {
              const 融合可用性 = 获取融合技能可用性(charData, fusionSkill, 玩家同队单位);
              if (!融合可用性.可用) {
                融合失败原因 = 融合可用性.原因 || 融合失败原因;
                return;
              }
              hasFusion = true;
              action.action_type = '武魂融合技';
              action.skill = buildFusionCombatSkill(fusionSkill, fusionName, charData);
              action.skill.name = `武魂融合技·${action.skill.name}`;
              action.cast_time = getSkillCastTime(action.skill) || 30;
            });
            if (!hasFusion) {
              action.action_type = '施法失败';
              action.cast_time = 0;
              action.log = `[融合失败] ${融合失败原因}`;
            }
          } else if (!matchedSkill) {
            // 如果没有任何武魂融合技、也没有匹配到具体魂技，但是匹配到了一些其它的特殊主动作
            if (playerInput.includes('多元素融合')) {
              action.action_type = '多元素融合';
              action.fusionElements = extractBattleFusionElementsFromText(playerInput);
              action.fusionPattern = buildBattleFusionPattern(action.fusionElements);
              if (playerInput.includes('蓄力')) action.is_charged = true;
              action.skill = normalizeSkillData({
                name: '多元素融合',
                技能分类: '输出',
                消耗: '无',
                附带属性: action.fusionElements || [],
              });
            } else if (playerInput.includes('五行剥离')) {
              action.action_type = '五行剥离';
              action.cast_time = 18;
              action.skill = normalizeSkillData({
                name: '五行剥离',
                技能分类: '控制',
                消耗: '魂力:18% 精神力:25%',
                cast_time: 18,
              });
            } else if (playerInput.includes('五行遁法')) {
              action.action_type = '五行遁法';
              action.cast_time = 12;
              action.skill = normalizeSkillData({
                name: '五行遁法',
                技能分类: '辅助',
                消耗: '魂力:12% 精神力:18% 维持:精神力:8%',
                cast_time: 12,
              });
            } else if (playerInput.includes('吸血反哺')) {
              action.action_type = '吸血反哺';
              action.heal_ratio = 0.3;
            } else if (playerInput.includes('机甲') && (playerInput.includes('召唤') || playerInput.includes('进入'))) {
              // 普通机甲同理，必须作为主动作读条
              action.action_type = '穿戴装备';
              let mechLv = charData.装备?.机甲?.等级 || '黄级';
              action.cast_time = mechLv === '红级' ? 0 : 50;
              action.equip_target = 'mech';
              action.skill = normalizeSkillData({ name: '召唤机甲', 技能分类: '辅助', 消耗: '无' });
            }
          }

          return 去重动作队列友方辅助(attacker, action);
        }

        // ==========================================
        // 📍 团战前置：行动轴与索敌逻辑
        // ==========================================

        // 1. 生成全局行动轴
        function generateActionQueue(combatData) {
          确保召唤单位表(combatData);
          确保队伍临时意图(combatData);
          let allFighters = [];

          读取战斗主队单位列表(combatData, '玩家').forEach(p => {
            bindCombatParticipant(p);
            syncCombatActionState(p);
            if (isCombatUnitAbleToFight(p)) allFighters.push({ char: p, side: 'player' });
          });
          读取战斗主队单位列表(combatData, '敌方').forEach(e => {
            bindCombatParticipant(e);
            syncCombatActionState(e);
            if (isCombatUnitAbleToFight(e)) allFighters.push({ char: e, side: 'enemy' });
          });
          读取召唤单位列表(combatData, { 行动模式: '自主行动' }).forEach(单位 => {
            bindCombatParticipant(单位);
            if (isCombatUnitAbleToFight(单位)) allFighters.push({ char: 单位, side: 单位.阵营 === '敌方' ? 'enemy' : 'player' });
          });

          let typePriority = {
            辅助系: 1,
            控制系: 2,
            敏攻系: 2,
            强攻系: 2,
            精神系: 2,
            元素系: 2,
            防御系: 3,
            治疗系: 3,
            食物系: 3,
          };

          allFighters.sort((a, b) => {
            let pA = typePriority[a.char.type] || 4;
            let pB = typePriority[b.char.type] || 4;
            if (pA !== pB) return pA - pB;

            let agiA = a.char.agi;
            let agiB = b.char.agi;
            return agiB - agiA;
          });

          return allFighters;
        }

        function 刷新队伍行动轴意图(entry = {}, targets = {}, action = null, battleState = {}) {
          const combatData = battleState?.combatData || {};
          const actor = entry?.char;
          if (!actor || !combatData) return;
          const 阵营 = entry.side === 'enemy' ? '敌方' : '玩家';
          const 意图表 = 确保队伍临时意图(combatData);
          const 当前 = 意图表[阵营] || {};
          const skill = action?.skill || null;
          const target = targets?.enemyTarget || targets?.allyTarget || null;
          const 规划上下文 = 构建规划上下文(actor, target, combatData, { combatData });
          if (targets?.enemyTarget && skill && !技能偏向友方目标(skill)) {
            const 目标名 = 读取规划单位名称(targets.enemyTarget);
            if (目标名) {
              当前.集火目标 = 当前.集火目标 || 目标名;
              当前.集火计数 = Number(当前.集火计数 || 0) + (当前.集火目标 === 目标名 ? 1 : 0);
              当前.集火理由 = 读取规划目标优先项(规划上下文, targets.enemyTarget, '敌方').理由;
            }
          }
          if (targets?.allyTarget && skill && 技能偏向友方目标(skill)) {
            const 目标名 = 读取规划单位名称(targets.allyTarget);
            if (目标名) {
              当前.保护目标 = 当前.保护目标 || 目标名;
              当前.保护计数 = Number(当前.保护计数 || 0) + (当前.保护目标 === 目标名 ? 1 : 0);
              当前.保护理由 = 读取规划目标优先项(规划上下文, targets.allyTarget, '友方').理由;
            }
          }
          意图表[阵营] = 当前;
        }

        // 2. D100 索敌逻辑
        function findTarget(attackerChar, enemyTeam, combatData = {}) {
          bindCombatParticipant(attackerChar);
          (enemyTeam || []).forEach(bindCombatParticipant);
          let validTargets = enemyTeam.filter(t => isCombatUnitAbleToFight(t));
          if (validTargets.length === 0) return null;
          const 规划上下文 = 构建规划上下文(attackerChar, validTargets[0] || null, combatData, { combatData });
          const forcedTauntTargetName = getForcedTauntTargetName(attackerChar);
          if (forcedTauntTargetName) {
            const forcedTarget = validTargets.find(target => isCombatUnitIdentityMatch(target, forcedTauntTargetName));
            if (forcedTarget) {
              记录行动闭环审计(combatData, '索敌规划', {
                行动者: attackerChar?.name || attackerChar?.名称 || '',
                目标: forcedTarget?.name || forcedTarget?.名称 || '',
                战略意图: 规划上下文?.战略意图?.主意图 || '',
                目标理由: ['嘲讽强制目标'],
                候选来源: '嘲讽强制索敌',
                原始净收益: 100,
                干扰强度: 读取索敌干扰强度(attackerChar),
                最终权重: 100,
                选择原因: '嘲讽目标强制命中',
              });
              return forcedTarget;
            }
          }
          const lockableTargets = validTargets.filter(target => 可以锁定隐匿目标(attackerChar, target, null));
          if (lockableTargets.length > 0) validTargets = lockableTargets;
          const 干扰强度 = 读取索敌干扰强度(attackerChar);
          const 错误队伍 = 读取索敌错误队伍(attackerChar, validTargets, null, combatData);
          if (validTargets.length === 1 && !(干扰强度 > 0 && 错误队伍.length)) {
            const 单目标 = validTargets[0];
            const 优先项 = 读取规划目标优先项(规划上下文, 单目标, '敌方');
            记录目标权重探针(combatData, {
              round: Number(combatData?.回合 || 0),
              actor: attackerChar?.name || attackerChar?.名称 || '',
              targetPoolSide: 'hostile',
              targetType: '敌方单体',
              selectedTarget: 单目标?.name || 单目标?.名称 || '',
              source: 'findTarget.single',
              candidates: [{ target: 单目标?.name || 单目标?.名称 || '', baseWeight: Number(优先项.优先级 || 0), finalWeight: Number(优先项.优先级 || 0), tagReasons: 优先项.理由 || [], poolSide: 'hostile' }],
            });
            记录行动闭环审计(combatData, '索敌规划', {
              行动者: attackerChar?.name || attackerChar?.名称 || '',
              目标: 单目标?.name || 单目标?.名称 || '',
              战略意图: 规划上下文?.战略意图?.主意图 || '',
              目标理由: 优先项.理由 || [],
              候选来源: '索敌单目标',
              原始净收益: Number(优先项.优先级 || 0),
              干扰强度,
              最终权重: Number(优先项.优先级 || 1),
              选择原因: '唯一有效敌方目标',
            });
            return 单目标;
          }

          let totalWeight = 0;
          const 攻击者系别 = 读取规划单位系别(attackerChar);
          let weightedTargets = validTargets.map(target => {
            let weight = 10;
            const 目标系别 = 读取规划单位系别(target);
            let isSupport = ['辅助系', '治疗系', '食物系', '召唤系', '控制系'].includes(目标系别);
            let isTank = ['防御系', '强攻系'].includes(目标系别);
            let hpRatio = getCombatHpRatio(target);
            if (攻击者系别 === '敏攻系') {
              if (isSupport) weight += 60;
              if (hpRatio < 0.3) weight += 30;
            } else if (攻击者系别 === '强攻系') {
              if (isTank) weight += 50;
              if (hpRatio < 0.5) weight += 40;
            } else if (攻击者系别 === '控制系') {
              if (isTank) weight += 40;
              if (isSupport) weight += 40;
            } else {
              if (hpRatio < 0.5) weight += 50;
            }
            const 优先项 = 读取规划目标优先项(规划上下文, target, '敌方');
            weight += Math.round(Number(优先项.优先级 || 0) * 0.35);
            totalWeight += weight;
            return { target: target, weight: weight, __目标理由: 优先项.理由 };
          });
          weightedTargets = 扰动索敌候选列表(污染索敌候选池(attackerChar, weightedTargets, 错误队伍, 干扰强度), 干扰强度);
          totalWeight = weightedTargets.reduce((总和, 项) => 总和 + Number(项.weight || 0), 0);

          let roll = Math.random() * totalWeight;
          for (let wt of weightedTargets) {
            roll -= wt.weight;
            if (roll <= 0) {
              记录目标权重探针(combatData, {
                round: Number(combatData?.回合 || 0),
                actor: attackerChar?.name || attackerChar?.名称 || '',
                targetPoolSide: 'hostile',
                targetType: '敌方单体',
                selectedTarget: wt.target?.name || wt.target?.名称 || '',
                source: 'findTarget.roll',
                candidates: weightedTargets.map(item => ({ target: item.target?.name || item.target?.名称 || '', baseWeight: Number(item.weight || 0), finalWeight: Number(item.weight || 0), tagReasons: item.__目标理由 || [], poolSide: item.__索敌污染 ? 'polluted' : 'hostile' })),
              });
              记录行动闭环审计(combatData, '索敌规划', {
                行动者: attackerChar?.name || attackerChar?.名称 || '',
                目标: wt.target?.name || wt.target?.名称 || '',
                战略意图: 规划上下文?.战略意图?.主意图 || '',
                目标理由: wt.__目标理由 || [],
                候选来源: wt.__索敌污染 ? '索敌污染池' : '索敌目标优先表',
                原始净收益: Number(wt.weight || 0),
                干扰强度,
                最终权重: Number(wt.weight || 0),
                选择原因: wt.__索敌污染 ? '索敌干扰污染命中' : '目标优先表命中',
              });
              return wt.target;
            }
          }
          const 兜底目标 = weightedTargets[0];
          记录目标权重探针(combatData, {
            round: Number(combatData?.回合 || 0),
            actor: attackerChar?.name || attackerChar?.名称 || '',
            targetPoolSide: 'hostile',
            targetType: '敌方单体',
            selectedTarget: 兜底目标?.target?.name || 兜底目标?.target?.名称 || '',
            source: 'findTarget.fallback',
            candidates: weightedTargets.map(item => ({ target: item.target?.name || item.target?.名称 || '', baseWeight: Number(item.weight || 0), finalWeight: Number(item.weight || 0), tagReasons: item.__目标理由 || [], poolSide: item.__索敌污染 ? 'polluted' : 'hostile' })),
          });
          记录行动闭环审计(combatData, '索敌规划', {
            行动者: attackerChar?.name || attackerChar?.名称 || '',
            目标: 兜底目标?.target?.name || 兜底目标?.target?.名称 || '',
            战略意图: 规划上下文?.战略意图?.主意图 || '',
            目标理由: 兜底目标?.__目标理由 || [],
            候选来源: '索敌兜底',
            原始净收益: Number(兜底目标?.weight || 0),
            干扰强度,
            最终权重: Number(兜底目标?.weight || 0),
            选择原因: '权重兜底目标',
          });
          return 兜底目标.target;
        }

        function findAllyTarget(actorChar, allyTeam, combatData = {}) {
          if (!allyTeam || allyTeam.length === 0) return actorChar;
          let validAllies = allyTeam.filter(t => isCombatUnitAlive(t));
          if (validAllies.length === 0) return actorChar;
          const 规划上下文 = 构建规划上下文(actorChar, actorChar, combatData, { combatData });
          const 候选 = validAllies.map(ally => {
            bindCombatParticipant(ally);
            const 优先项 = 读取规划目标优先项(规划上下文, ally, '友方');
            const weight = Math.max(1, Math.round(Number(优先项.优先级 || 0) + (1 - getCombatHpRatio(ally)) * 75));
            return { target: ally, weight, __目标理由: 优先项.理由 };
          });
          const 干扰强度 = 读取索敌干扰强度(actorChar);
          const picked = chooseWeightedOption(扰动索敌候选列表(候选, 干扰强度));
          const targetAlly = picked?.target || actorChar;
          记录目标权重探针(combatData, {
            round: Number(combatData?.回合 || 0),
            actor: actorChar?.name || actorChar?.名称 || '',
            targetPoolSide: 'allied',
            targetType: '友方单体',
            selectedTarget: targetAlly?.name || targetAlly?.名称 || '',
            source: 'findAllyTarget.roll',
            candidates: 候选.map(item => ({ target: item.target?.name || item.target?.名称 || '', baseWeight: Number(item.weight || 0), finalWeight: Number(item.weight || 0), tagReasons: item.__目标理由 || [], poolSide: 'allied' })),
          });
          记录行动闭环审计(combatData, '辅助目标规划', {
            行动者: actorChar?.name || actorChar?.名称 || '',
            目标: targetAlly?.name || targetAlly?.名称 || '',
            战略意图: 规划上下文?.战略意图?.主意图 || '',
            目标理由: picked?.__目标理由 || [],
            候选来源: '友方目标优先表',
            原始净收益: Number(picked?.weight || 0),
            干扰强度,
            最终权重: Number(picked?.weight || 0),
            选择原因: '友方目标优先表命中',
          });
          return targetAlly;
        }

        function scoreEnemyTargetForSkill(attackerChar, target, skill, combatData = null) {
          bindCombatParticipant(attackerChar);
          bindCombatParticipant(target);
          const 规划上下文 = combatData ? 构建规划上下文(attackerChar, target, combatData, { combatData }) : null;
          const 规划技能 = 构建行为规划技能(skill, target, attackerChar, combatData);
          const snapshot = buildConditionTacticalSnapshot(target);
          const summary = deriveBattleSummaryFromEffects(规划技能);
          const mainType = inferMainTypeFromEffects(规划技能) || '无';
          const 是规则压制 = 技能具有规则压制倾向(规划技能, { summary, mainType });
          const 来源类别 = getBattleSkillSourceCategory(规划技能);
          const dmg = getPrimaryDamageEffect(规划技能);
          const hpRatio = getCombatHpRatio(target);
          const spRatio = Math.max(0, Number(target.sp || 0)) / Math.max(1, Number(target.sp_max || 1));
          const menRatio = Math.max(0, Number(target.men || 0)) / Math.max(1, Number(target.men_max || 1));
          const power = Number(dmg?.威力倍率 || 0);
          const penetration = Number(dmg?.防御穿透 || 0);
          const 伤害预结算 = estimateIncomingActionThreat(
            attackerChar,
            target,
            { action_type: '技能', type: '技能', cast_time: getSkillCastTime(skill), skill: 规划技能 },
            null,
          );
          const 预结算伤害比例 = Number(伤害预结算.projectedDamageRatio || 0);
          const 攻击者系别 = 读取规划单位系别(attackerChar);
          const 目标系别 = 读取规划单位系别(target);
          const isSupport = ['辅助系', '治疗系', '食物系', '召唤系', '控制系'].includes(目标系别);
          const isTank = ['防御系', '强攻系'].includes(目标系别);
          let weight = 10;

          if (攻击者系别 === '敏攻系') {
            if (isSupport) weight += 45;
            if (hpRatio < 0.4) weight += 30;
          } else if (攻击者系别 === '强攻系') {
            if (isTank) weight += 20;
            if (hpRatio < 0.5) weight += 25;
          } else if (攻击者系别 === '控制系') {
            if (isSupport) weight += 35;
            if (!snapshot.isLockedOrControlled) weight += 15;
          } else {
            if (hpRatio < 0.5) weight += 25;
          }

          if (isBattleSkillAntiHealProfile(规划技能, { summary })) {
            if (snapshot.hasAntiHeal) weight -= 40;
            if (snapshot.hasHealingTrend || isSupport) weight += 70;
          }
          if (isBattleSkillExecuteProfile(规划技能, { summary, damage: dmg })) {
            if (hpRatio < 0.35) weight += 80;
            else if (hpRatio < 0.55) weight += 25;
            if (snapshot.isLockedOrControlled) weight += 20;
            if (snapshot.hasDotPressure) weight += 15;
          }
          if (伤害预结算.targetHitsDefender) {
            if (伤害预结算.lethalRisk === true) weight += 90;
            else if (伤害预结算.severeDamage === true) weight += 52;
            else if (伤害预结算.moderateDamage === true) weight += 30;
            else if (预结算伤害比例 > 0) weight += Math.min(22, Math.floor(预结算伤害比例 * 80));
          }
          if (isBattleSkillShieldBreakProfile(规划技能, { damage: dmg })) {
            if (snapshot.hasShielded) weight += 60;
            if (snapshot.hasDefenseBuffed || isTank) weight += 35;
          }
          if (summary.控制强度 !== '无') {
            if (!snapshot.isLockedOrControlled) weight += 30;
            else weight -= 25;
            if (target.蓄力技能) weight += 35;
          }
          if (isBattleSkillDotPressureProfile(规划技能)) {
            if (hpRatio > 0.55) weight += 20;
            if (snapshot.debuffCount > 0) weight += 15;
            if (hpRatio < 0.25) weight -= 15;
          }
          if (isBattleSkillDotDetonateProfile(规划技能) && snapshot.debuffCount > 0) {
            weight += 40 + snapshot.debuffCount * 6;
          }
          if (isBattleSkillRevealProfile(规划技能) && snapshot.hasStealthed) weight += 75;
          if (isBattleSkillBuffStealProfile(规划技能) && snapshot.buffCount > 0) weight += 60 + snapshot.buffCount * 8;
          getSkillEffects(规划技能, { 行为规划: true })
            .filter(effect => String(effect?.原型 || '').trim() === '状态移除' && !String(effect?.目标 || '').trim().includes('自身'))
            .forEach(effect => {
              weight += Math.min(120, Math.round(估算状态移除释放权重(effect, attackerChar, target, { 目标立场: '敌方' }) * 0.7));
            });
          if (isBattleSkillHealInvertProfile(规划技能) && snapshot.hasHealingTrend) weight += 48;
          if (isBattleSkillSealProfile(规划技能) && target.蓄力技能) weight += 65;
          if (isBattleSkillTauntProfile(规划技能)) {
            const offenseScore = Math.max(Number(target.str || 0), Number(target.men_max || 0));
            if (isSupport) weight += 20;
            weight += Math.min(45, Math.floor(offenseScore / 250));
          }
          if (isBattleSkillResourceDrainProfile(规划技能)) {
            if (spRatio > 0.45 || menRatio > 0.45) weight += 45;
            if (isSupport || target.蓄力技能) weight += 18;
          }
          if (isBattleSkillMechanismSuppressProfile(规划技能)) {
            const suppressTargets = 读取战斗技能抹消战术标签(规划技能);
            if ((suppressTargets.includes('复活机制') || suppressTargets.includes('回复机制')) && snapshot.hasHealingTrend) weight += 58;
            if ((suppressTargets.includes('护盾') || suppressTargets.includes('防御机制')) && snapshot.hasShielded) weight += 52;
            if ((suppressTargets.includes('隐身') || suppressTargets.includes('增益')) && snapshot.hasStealthed) weight += 62;
            if ((suppressTargets.includes('增益') || suppressTargets.includes('特殊规则')) && snapshot.buffCount > 0)
              weight += 36 + snapshot.buffCount * 6;
            if (suppressTargets.includes('防御机制') && snapshot.hasReactiveDefense) weight += 38;
          }
          if (是规则压制) {
            if (snapshot.hasShielded || snapshot.hasHealingTrend || target.蓄力技能) weight += 12;
          }
          if (来源类别 === '自创魂技') {
            if (snapshot.hasShielded || snapshot.hasHealingTrend || snapshot.hasStealthed) weight += 10;
          } else if (来源类别 === '武魂融合技') {
            if (hpRatio < 0.45 || snapshot.isLockedOrControlled) weight += 18;
          }
          if (
            summary.目标规模 === '单体' &&
            snapshot.hasStealthed &&
            !可以锁定隐匿目标(attackerChar, target, skill)
          ) {
            weight = Math.max(1, Math.floor(weight * 0.18));
          }
          if (规划上下文) {
            const 优先项 = 读取规划目标优先项(规划上下文, target, '敌方');
            weight += Math.round(Number(优先项.优先级 || 0) * 0.24);
          }
          return Math.max(1, weight);
        }

        function chooseEnemyTargetForSkill(attackerChar, enemyTeam, skill, fallbackTarget = null, combatData = {}) {
          const validTargets = (enemyTeam || []).filter(target => target && isCombatUnitAbleToFight(target));
          if (validTargets.length === 0) return fallbackTarget || null;
          const 记录技能索敌 = (选中目标, 来源 = '', 理由 = []) => {
            const 规划上下文 = 构建规划上下文(attackerChar, 选中目标, combatData, { combatData });
            const 优先项 = 读取规划目标优先项(规划上下文, 选中目标, '敌方');
            const 干扰强度 = 读取索敌干扰强度(attackerChar, skill);
            记录目标权重探针(combatData, {
              round: Number(combatData?.回合 || 0),
              actor: attackerChar?.name || attackerChar?.名称 || '',
              targetPoolSide: 'hostile',
              targetType: inferSkillPrimaryTargetKind(skill),
              selectedTarget: 选中目标?.name || 选中目标?.名称 || '',
              source: 来源 || 'chooseEnemyTargetForSkill',
              candidates: validTargets.map(targetObj => ({
                target: targetObj?.name || targetObj?.名称 || '',
                baseWeight: Number(读取规划目标优先项(规划上下文, targetObj, '敌方')?.优先级 || 0),
                finalWeight: Number(scoreEnemyTargetForSkill(attackerChar, targetObj, skill, combatData) || 0),
                tagReasons: 读取规划目标优先项(规划上下文, targetObj, '敌方')?.理由 || [],
                poolSide: 'hostile',
              })),
            });
            记录行动闭环审计(combatData, '索敌规划', {
              行动者: attackerChar?.name || attackerChar?.名称 || '',
              目标: 选中目标?.name || 选中目标?.名称 || '',
              技能: skill?.name || skill?.魂技名 || '',
              战略意图: 规划上下文?.战略意图?.主意图 || '',
              目标理由: 理由.length ? 理由 : 优先项.理由 || [],
              候选来源: 来源,
              原始净收益: Number(优先项.优先级 || 0),
              干扰强度,
              最终权重: Number(优先项.优先级 || 1),
              选择原因: 来源 || '技能目标闭环命中',
            });
          };
          const forcedTauntTargetName = getForcedTauntTargetName(attackerChar);
          if (forcedTauntTargetName) {
            const forcedTarget = validTargets.find(target => isCombatUnitIdentityMatch(target, forcedTauntTargetName));
            if (forcedTarget) {
              记录技能索敌(forcedTarget, '技能嘲讽强制目标', ['嘲讽强制目标']);
              return forcedTarget;
            }
          }
          const focusTarget = getActorFocusedTarget(attackerChar, validTargets);
          if (!skill) {
            const 选中目标 = focusTarget || fallbackTarget || validTargets[0];
            记录技能索敌(选中目标, focusTarget ? '关注目标闭环' : '无技能目标闭环', focusTarget ? ['延续关注目标'] : []);
            return 选中目标;
          }
          const 规划技能 = 构建行为规划技能(skill, focusTarget || fallbackTarget || validTargets[0], attackerChar, combatData);
          const summary = deriveBattleSummaryFromEffects(规划技能);
          const effectiveTargets = summary.目标规模 !== '单体'
            ? validTargets
            : (() => {
                const visibleTargets = validTargets.filter(target => 可以锁定隐匿目标(attackerChar, target, skill));
                return visibleTargets.length > 0 ? visibleTargets : [];
              })();
          if (!effectiveTargets.length) {
            记录技能索敌(fallbackTarget, '无可锁定目标兜底', ['目标不可锁定']);
            return fallbackTarget || null;
          }
          const memory = ensureActorDecisionMemory(attackerChar);
          const effectiveFocusTarget =
            focusTarget && effectiveTargets.some(target => target.name === focusTarget.name) ? focusTarget : null;
          if (effectiveFocusTarget && memory.focus_reason === 'shared_vision_focus' && summary.目标规模 === '单体') {
            记录技能索敌(effectiveFocusTarget, '共享视野锁定', ['延续共享视野焦点']);
            return effectiveFocusTarget;
          }
          if (effectiveTargets.length === 1) {
            记录技能索敌(effectiveTargets[0], '技能单目标闭环', ['唯一有效技能目标']);
            return effectiveTargets[0];
          }
          const 目标候选 = effectiveTargets.map(target => {
              let weight = scoreEnemyTargetForSkill(attackerChar, target, skill, combatData);
              if (focusTarget && target.name === focusTarget.name) {
                weight += 18 + Number(memory.focus_ttl || 0) * 6;
                if (memory.focus_reason === 'control_window') {
                if (summary.爆发级别 === '高' || isBattleSkillExecuteProfile(规划技能, { summary, damage: getPrimaryDamageEffect(规划技能) })) weight += 18;
                if (summary.控制强度 !== '无') weight -= 12;
              } else if (memory.focus_reason === 'anti_heal_window') {
                  if (isBattleSkillDotPressureProfile(规划技能) || isBattleSkillExecuteProfile(规划技能, { summary, damage: getPrimaryDamageEffect(规划技能) }) || summary.爆发级别 !== '无') weight += 12;
              } else if (memory.focus_reason === 'armor_break_window') {
                  if (Number(getPrimaryDamageEffect(规划技能)?.防御穿透 || 0) >= 15 || summary.爆发级别 === '高')
                    weight += 14;
              } else if (memory.focus_reason === 'shared_vision_focus') {
                  if (summary.目标规模 === '单体') weight += 18;
              } else if (memory.focus_reason === 'dot_pressure') {
                  if (isBattleSkillExecuteProfile(规划技能, { summary, damage: getPrimaryDamageEffect(规划技能) }) || isBattleSkillDotPressureProfile(规划技能) || summary.爆发级别 === '高') weight += 16;
              } else if (memory.focus_reason === 'finisher') {
                  if (isBattleSkillExecuteProfile(规划技能, { summary, damage: getPrimaryDamageEffect(规划技能) }) || summary.爆发级别 !== '无') weight += 22;
              }
              }
              return { target, weight: Math.max(1, weight) };
            });
          const 干扰强度 = 读取索敌干扰强度(attackerChar, skill);
          const 污染后候选 = 污染索敌候选池(
            attackerChar,
            目标候选,
            读取索敌错误队伍(attackerChar, effectiveTargets, skill, combatData),
            干扰强度,
          );
          const picked = chooseWeightedOption(扰动索敌候选列表(污染后候选, 干扰强度));
          const 选中目标 = picked?.target || effectiveFocusTarget || fallbackTarget || effectiveTargets[0];
          const 规划上下文 = 构建规划上下文(attackerChar, 选中目标, combatData, { combatData });
          const 优先项 = 读取规划目标优先项(规划上下文, 选中目标, '敌方');
          记录行动闭环审计(combatData, '索敌规划', {
            行动者: attackerChar?.name || attackerChar?.名称 || '',
            目标: 选中目标?.name || 选中目标?.名称 || '',
            技能: skill?.name || skill?.魂技名 || '',
            战略意图: 规划上下文?.战略意图?.主意图 || '',
            目标理由: picked?.__目标理由 || 优先项.理由 || [],
            候选来源: picked?.__索敌污染 ? '技能目标污染池' : '技能目标优先表',
            原始净收益: Number(picked?.weight || 0),
            干扰强度,
            最终权重: Number(picked?.weight || 0),
            选择原因: picked?.__索敌污染 ? '索敌干扰污染命中' : '技能目标闭环命中',
          });
          return 选中目标;
        }

        function scoreAllyTargetForSkill(actorChar, ally, skill, combatData = null) {
          bindCombatParticipant(actorChar);
          bindCombatParticipant(ally);
          const 规划上下文 = combatData ? 构建规划上下文(actorChar, ally, combatData, { combatData }) : null;
          const snapshot = buildConditionTacticalSnapshot(ally);
          const 规划技能 = 构建行为规划技能(skill, ally, actorChar, combatData);
          const summary = deriveBattleSummaryFromEffects(规划技能);
          const 来源类别 = getBattleSkillSourceCategory(规划技能);
          const hpRatio = getCombatHpRatio(ally);
          const spRatio = Math.max(0, Number(ally.sp || 0)) / Math.max(1, Number(ally.sp_max || 1));
          const menRatio = Math.max(0, Number(ally.men || 0)) / Math.max(1, Number(ally.men_max || 1));
          const attackScore = Math.max(Number(ally.str || 0), Number(ally.men_max || 0));
          let weight = ally.name === actorChar.name ? 12 : 10;
          if (
            getSkillEffects(规划技能, { 行为规划: true }).some(effect => isBattleRecoverEffect(effect, ['vit'])) ||
            ['护盾', '免死', '护卫', '减伤', '免伤', '霸体', '无敌', '分摊', '反射'].includes(summary.防御性质)
          )
            weight += Math.floor((1 - hpRatio) * 120) + (snapshot.hasBadCondition ? 15 : 0);
          getSkillEffects(规划技能, { 行为规划: true })
            .filter(effect => String(effect?.原型 || '').trim() === '状态移除' && String(effect?.目标 || '').trim().includes('自身'))
            .forEach(effect => {
              weight += Math.min(120, Math.round(估算状态移除释放权重(effect, actorChar, ally, { 目标立场: '友方' }) * 0.72));
            });
          if (getSkillEffects(规划技能, { 行为规划: true }).some(effect => isBattleRecoverEffect(effect, ['sp', 'men'])))
            weight += Math.floor((1 - spRatio) * 40) + Math.floor((1 - menRatio) * 40);
          if (isBattleSkillResourceRefeedProfile(规划技能))
            weight += Math.floor((1 - spRatio) * 55) + Math.floor((1 - menRatio) * 55);
          if (summary.协同性 === '高') weight += Math.min(40, Math.floor(attackScore / 500));
          if (summary.防御性质 === '护卫' && ally.name !== actorChar.name) weight += Math.floor((1 - hpRatio) * 120) + 36;
          if (是团队保护技能(规划技能, { summary }) && ally.name !== actorChar.name) {
            weight += Math.floor((1 - hpRatio) * 80) + 18;
            if (['辅助系', '治疗系', '食物系', '召唤系', '控制系'].includes(读取规划单位系别(ally))) weight += 22;
            if (snapshot.isLockedOrControlled || snapshot.hasBadCondition) weight += 16;
            if (snapshot.hasShielded || snapshot.hasReactiveDefense) weight -= 12;
          }
          if (isBattleSkillTauntProfile(规划技能) && ally.name === actorChar.name) weight += 35;
          if (summary.防御性质 === '分身' || hasBattleSkillRuntimeConsumer(规划技能, ['stealth'])) {
            if (ally.name === actorChar.name) weight += 18;
            if (hpRatio < 0.5) weight += 20;
          }
          if (来源类别 === '武魂融合技' && ally.name !== actorChar.name) weight += 12;
          if (['辅助系', '治疗系', '食物系', '召唤系', '控制系'].includes(读取规划单位系别(ally))) weight += 10;
          if (规划上下文) {
            const 优先项 = 读取规划目标优先项(规划上下文, ally, '友方');
            weight += Math.round(Number(优先项.优先级 || 0) * 0.28);
          }
          return Math.max(1, weight);
        }

        function chooseAllyTargetForSkill(actorChar, allyTeam, skill, fallbackTarget = null, combatData = {}) {
          const validAllies = (allyTeam || []).filter(target => target && isCombatUnitAlive(target));
          if (validAllies.length === 0) return actorChar;
          if (!skill) {
            const 选中目标 = fallbackTarget || validAllies[0] || actorChar;
            const 规划上下文 = 构建规划上下文(actorChar, 选中目标, combatData, { combatData });
            const 优先项 = 读取规划目标优先项(规划上下文, 选中目标, '友方');
            记录行动闭环审计(combatData, '辅助目标规划', {
              行动者: actorChar?.name || actorChar?.名称 || '',
              目标: 选中目标?.name || 选中目标?.名称 || '',
              战略意图: 规划上下文?.战略意图?.主意图 || '',
              目标理由: 优先项.理由 || [],
              候选来源: '无技能友方目标闭环',
              原始净收益: Number(优先项.优先级 || 0),
              干扰强度: 读取索敌干扰强度(actorChar),
              最终权重: Number(优先项.优先级 || 1),
              选择原因: '友方目标兜底仍消费目标优先表',
            });
            return 选中目标;
          }
          const 正常候选 = validAllies
            .map(target => ({ target, weight: scoreAllyTargetForSkill(actorChar, target, skill, combatData) }))
            .sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0));
          const picked = 正常候选[0] || null;
          const 选中目标 = picked?.target || fallbackTarget || validAllies[0] || actorChar;
          const 规划上下文 = 构建规划上下文(actorChar, 选中目标, combatData, { combatData });
          const 优先项 = 读取规划目标优先项(规划上下文, 选中目标, '友方');
          记录行动闭环审计(combatData, '辅助目标规划', {
            行动者: actorChar?.name || actorChar?.名称 || '',
            目标: 选中目标?.name || 选中目标?.名称 || '',
            技能: skill?.name || skill?.魂技名 || '',
            战略意图: 规划上下文?.战略意图?.主意图 || '',
            目标理由: picked?.__目标理由 || 优先项.理由 || [],
            候选来源: '辅助目标优先表',
            原始净收益: Number(picked?.weight || 0),
            干扰强度: 0,
            最终权重: Number(picked?.weight || 0),
            选择原因: '辅助目标闭环命中',
          });
          return 选中目标;
        }

        function 准备自动批量辅助动作(actorChar, action, allyTeam = [], battleState = null) {
          if (!actorChar || !action?.skill || !isSupportLikeSkill(action.skill)) return action;
          if (inferSkillPrimaryTargetKind(action.skill) !== '友方单体') return action;
          if (String(actorChar?.type || '').trim() === '食物系') return action;
          const 系别 = String(actorChar?.type || actorChar?.系别 || '').trim();
          const 允许批量 =
            ['防御系', '辅助系', '治疗系'].includes(系别) ||
            是团队保护技能(action.skill) ||
            getSkillEffects(action.skill, { 行为规划: true }).some(effect => isBattleRecoverEffect(effect));
          if (!允许批量) return action;
          const 候选队伍 = dedupeCombatTargetList([actorChar, ...(Array.isArray(allyTeam) ? allyTeam : [])])
            .filter(target => target && isCombatUnitAlive(target));
          if (候选队伍.length <= 1) return action;
          const 排序队伍 = [...候选队伍].sort(
            (左, 右) =>
              scoreAllyTargetForSkill(actorChar, 右, action.skill, battleState?.combatData) -
              scoreAllyTargetForSkill(actorChar, 左, action.skill, battleState?.combatData),
          );
          const 最大数量 = Math.min(排序队伍.length, 系别 === '防御系' ? 4 : 3);
          for (let 数量 = 最大数量; 数量 >= 2; 数量 -= 1) {
            const 批量目标列表 = 排序队伍.slice(0, 数量);
            const 批量试算技能 = {
              ...action.skill,
              __批量目标列表: 批量目标列表,
              __批量消耗模式: '单体批量',
            };
            const 扣费 = parseSkillCostForChar(批量试算技能, actorChar);
            if (!扣费.canCast) continue;
            action.__批量目标列表 = 批量目标列表;
            action.__批量消耗模式 = '单体批量';
            action.__连放来源 = 'NPC批量辅助';
            action.target_name = 批量目标列表[0]?.name || 批量目标列表[0]?.名称 || action.target_name || null;
            action.decision_log = `${action.decision_log || ''} [批量辅助] ${actorChar.name || 'NPC'}将[${action.skill.name || action.action_type}]同时覆盖${批量目标列表.map(单位 => 单位.name || 单位.名称 || '队友').join('、')}。`;
            break;
          }
          return action;
        }

        function 读取召唤物继承敌对目标(actorEntry = {}, enemyTeam = [], combatData = {}) {
          const actor = actorEntry?.char;
          if (!actor?.召唤键) return null;
          const validEnemies = (enemyTeam || []).filter(unit => unit && isCombatUnitAbleToFight(unit));
          if (!validEnemies.length) return null;
          const selfFocus = getActorFocusedTarget(actor, validEnemies);
          if (selfFocus) return selfFocus;
          const hostName = String(actor?.宿主名 || actor?.__宿主?.name || actor?.__宿主?.名称 || '').trim();
          const allySide = actorEntry.side === 'enemy' ? '敌方' : '玩家';
          const host = actor.__宿主 || 读取战斗主队单位列表(combatData, allySide).find(unit => isCombatUnitIdentityMatch(unit, hostName));
          const hostFocus = getActorFocusedTarget(host, validEnemies);
          if (hostFocus) return hostFocus;
          const teamIntent = 确保队伍临时意图(combatData)[allySide] || {};
          const focusName = String(teamIntent.集火目标 || '').trim();
          if (!focusName) return null;
          return validEnemies.find(unit => isCombatUnitIdentityMatch(unit, focusName)) || null;
        }

        function chooseTargetForActor(actorEntry, battleState) {
          if (!actorEntry || !battleState?.combatData) return null;
          const enemyTeam =
            actorEntry.side === 'player'
              ? 读取战斗阵营单位列表(battleState.combatData, '敌方')
              : 读取战斗阵营单位列表(battleState.combatData, '玩家');
          const allyTeam =
            actorEntry.side === 'player'
              ? 读取战斗阵营单位列表(battleState.combatData, '玩家')
              : 读取战斗阵营单位列表(battleState.combatData, '敌方');
          const declaredTargetName = String(actorEntry.__declaredTargetName || '').trim();
          const declaredEnemyTarget = declaredTargetName
            ? enemyTeam.find(unit => isCombatUnitIdentityMatch(unit, declaredTargetName) && isCombatUnitAbleToFight(unit))
            : null;
          const declaredAllyTarget = declaredTargetName
            ? allyTeam.find(unit => isCombatUnitIdentityMatch(unit, declaredTargetName) && isCombatUnitAbleToFight(unit))
            : null;
          const inheritedSummonTarget = 读取召唤物继承敌对目标(actorEntry, enemyTeam, battleState.combatData);
          if (inheritedSummonTarget) setActorFocusTarget(actorEntry.char, inheritedSummonTarget, 'shared_vision_focus', 1);
          const focusEnemy = declaredEnemyTarget || inheritedSummonTarget || getActorFocusedTarget(actorEntry.char, enemyTeam);
          return {
            enemyTarget: focusEnemy || findTarget(actorEntry.char, enemyTeam, battleState.combatData),
            allyTarget: declaredAllyTarget || findAllyTarget(actorEntry.char, allyTeam, battleState.combatData),
          };
        }

        function buildAutoActionForActor(actorEntry, targets, battleState) {
          const actor = actorEntry?.char;
          if (!actor) return null;
          bindCombatParticipant(actor);
          const 行动者系别 = 读取规划单位系别(actor);
          const enemyTarget = targets?.enemyTarget;
          const allyTarget = targets?.allyTarget;
          if (enemyTarget) bindCombatParticipant(enemyTarget);
          if (allyTarget) bindCombatParticipant(allyTarget);
          if (actor.召唤键) {
            const 技能 = 选择召唤攻击技能(actor, enemyTarget);
            记录召唤物规划审计(battleState.combatData, actor, enemyTarget, 技能, '行动轴召唤物基础动作候选');
            return {
              action_type: '召唤自主行动',
              cast_time: 技能?.前摇 || 10,
              skill: 技能,
              source: 'summon',
              decision_log: `[召唤自主行动] ${actor.name || '召唤物'}自行参战。`,
              def_mult: 1,
            };
          }

          const allyTeam =
            actorEntry.side === 'player'
              ? 读取战斗阵营单位列表(battleState.combatData, '玩家').filter(unit => unit.name !== actor.name)
              : 读取战斗阵营单位列表(battleState.combatData, '敌方').filter(unit => unit.name !== actor.name);

          const makeActorAction = (type, log, skill = null, extra = {}) =>
            {
              const normalizedSkill = skill ? normalizeSkillData(skill, skill.name || skill.技能名称 || type) : null;
              if (normalizedSkill) 写入战斗来源类别上下文(normalizedSkill, 读取战斗来源类别上下文(skill, 规范化战斗来源类别(type, '魂技')));
              return Object.assign(
                {
                  type,
                  log,
                  skill: normalizedSkill,
                  def_mult: 1.0,
                },
                extra,
              );
            };

          const observedTargetAction = battleState?.observedTargetAction || enemyTarget?.蓄力技能 || {
            action_type: '常规攻击',
            cast_time: 10,
            skill: normalizeSkillData(
              {
                name: '普通攻击',
                技能分类: '输出',
                消耗: '无',
                前摇: 10,
                _效果数组: [
                  { 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 },
                ],
              },
              '普通攻击',
            ),
          };

          const ratio = enemyTarget?.蓄力技能
            ? calculateReactionRatio(enemyTarget, actor, observedTargetAction, {
                战斗类型: battleState.combatData.战斗类型 || '突发遭遇',
                先攻: actor.name,
                参战者: { team_player: [enemyTarget], team_enemy: [actor] },
              })
            : 1;

          const availableSkills = collectCombatSkills(actor, allyTeam);
          const strategicContext = buildStrategicCandidates(
            actor,
            enemyTarget,
            battleState.combatData,
            observedTargetAction,
            ratio,
            availableSkills,
            allyTeam,
            makeActorAction,
          );
          strategicContext.behaviorState.规划上下文 = 构建规划上下文(actor, enemyTarget, battleState.combatData, strategicContext.behaviorState);
          if (battleState?.observedTargetAction && enemyTarget) {
            strategicContext.behaviorState.规划上下文.锁定敌对目标 = enemyTarget;
            strategicContext.behaviorState.锁定敌对目标 = enemyTarget;
          }
          strategicContext.behaviorState.战局画像 = strategicContext.behaviorState.规划上下文.战局画像;
          strategicContext.behaviorState.目标优先表 = strategicContext.behaviorState.规划上下文.目标优先表;
          strategicContext.behaviorState.战略意图 = strategicContext.behaviorState.规划上下文.战略意图;
          strategicContext.behaviorState.主动回合 = true;
          const isSupport = ['辅助系', '治疗系', '食物系', '召唤系'].includes(行动者系别);
          const isLowHealth = getCombatHpRatio(actor) < 0.3;
          const skillContext = buildNpcSkillCandidateContext(
            actor,
            enemyTarget,
            observedTargetAction,
            availableSkills,
            strategicContext.behaviorState,
            strategicContext.activeBuffs,
            isLowHealth,
            { 主动回合: true },
          );
          strategicContext.behaviorState.skillContext = skillContext;
          const 是输出主职 = ['强攻系', '敏攻系', '元素系', '精神系'].includes(行动者系别);
          const 高价值主动技能 =
            (skillContext.评分技能列表 || []).some(项目 => 项目?.skill && Number(项目.weight || 0) >= 20) ||
            (是输出主职 && !!(skillContext.atkSkill || skillContext.lowCostAtkSkill || skillContext.pierceSkill)) ||
            (isSupport && !!(skillContext.recoverSkill || skillContext.teamSupportSkill || skillContext.teamProtectSkill));
          const 必要战略窗口 =
            strategicContext.behaviorState.isChargingHighThreat ||
            getCombatHpRatio(actor) < 0.45 ||
            strategicContext.behaviorState?.threatProfile?.lethalRisk === true ||
            strategicContext.behaviorState?.threatProfile?.severeDamage === true;
          if (高价值主动技能 && !必要战略窗口) {
            strategicContext.strategicBranches.forEach(候选 => {
              if (!候选) return;
              if (候选.name === '穿戴装备') 候选.weight = 0;
              else if (候选.name === '开启真身') 候选.weight = Math.min(Number(候选.weight || 0), 4);
              else if (候选.name === '展开领域') 候选.weight = Math.min(Number(候选.weight || 0), 8);
            });
          }

          const convertDecisionToTurnAction = decisionAction => {
            if (!decisionAction) return null;
            const 原始规划意图 = strategicContext.behaviorState?.战略意图?.主意图 || strategicContext.behaviorState?.规划上下文?.战略意图?.主意图 || '';
            const 是支援动作 = decisionAction.skill && 技能偏向友方目标(decisionAction.skill);
            const 规划显示意图 = 原始规划意图 === '击杀'
              ? '压制收束'
              : 原始规划意图 === '终局爆发'
                ? '终局压制'
                : (/治疗|保核/.test(原始规划意图) && !是支援动作 ? '压制续航' : 原始规划意图 || '战术行动');
            const neutralSkill = normalizeSkillData(
              {
                name: decisionAction.type || '战术动作',
                技能分类: '辅助',
                消耗: '无',
                前摇: 10,
                _效果数组: [],
              },
              decisionAction.type || '战术动作',
            );

          const 回合动作 = {
            action_type: decisionAction.type || '释放技能',
            cast_time: getSkillCastTime(decisionAction.skill || neutralSkill) || 10,
            skill: decisionAction.skill || neutralSkill,
              source: 'auto_actor',
              decision_log: replaceBattleReportGenericNames(
                `[规划] ${规划显示意图}。 ${decisionAction.log || ''}`.trim(),
                { player: enemyTarget, enemy: actor },
              ),
              def_mult: decisionAction.def_mult || 1.0,
            };
            回合动作.__行动规划审计 = {
              战略意图: strategicContext.behaviorState?.规划上下文?.战略意图?.主意图 || '',
              目标理由: strategicContext.behaviorState?.规划上下文?.目标优先表?.敌方?.[0]?.理由 || [],
              候选来源: decisionAction.type || '主动规划动作',
            };
            const 最终动作 = 准备自动批量辅助动作(actor, 回合动作, [actor, ...allyTeam], battleState);
            const 回填动作名 = normalizeBattleActionDisplayName(
              最终动作?.skill?.name ||
              最终动作?.skill?.魂技名 ||
              最终动作?.type ||
              decisionAction.skill?.name ||
              decisionAction.skill?.魂技名 ||
              decisionAction.type ||
              '',
            );
            const 回填候选名 = normalizeBattleActionDisplayName(
              decisionAction.skill?.name ||
              decisionAction.skill?.魂技名 ||
              decisionAction.type ||
              '',
            );
            const 动作确有覆写 = !!(回填动作名 && 回填候选名 && 回填动作名 !== 回填候选名);
            回填行动闭环审计最终动作(battleState.combatData, actor, {
              round: Number(battleState?.combatData?.回合 || 0),
              finalResolvedActionName: 回填动作名,
              hitCandidateName: 回填候选名,
              actionOverrideSource: 动作确有覆写 ? '主动规划终态回填' : '',
              目标语义: String(最终动作?.skill?.目标 || 最终动作?.skill?.target || '').trim(),
              承载方式: String(最终动作?.skill?.承载方式 || '').trim(),
              目标: String(最终动作?.target_name || enemyTarget?.name || '').trim(),
            });
            return 最终动作;
          };
          const 是职责支援技能 = 技能 => {
            if (!技能) return false;
            if (isSupportLikeSkill(技能)) return true;
            return getSkillEffects(技能, { 行为规划: true }).some(effect => {
              const 原型 = String(effect?.原型 || '').trim();
              const 结算 = String(effect?.结算 || '').trim();
              const 数值 = 读取战斗数值正负(effect?.数值);
              if (原型 === '资源变化') return 数值 > 0;
              if (['护盾变化', '规则防御', '机制授予', '状态移除'].includes(原型)) return true;
              if (原型 === '状态施加') {
                const 战斗效果 = effect?.计算层效果 || {};
                return Number(战斗效果.shield_gain_bonus || 0) > 0 ||
                  战斗效果.super_armor === true ||
                  战斗效果.invincible === true ||
                  Number(战斗效果.damage_reduction || 0) > 0 ||
                  Number(战斗效果.damage_share_ratio || 0) > 0 ||
                  Number(战斗效果.hot_heal_ratio || 0) > 0;
              }
              if (['属性修正', '判定修正'].includes(原型)) return 数值 > 0;
              if (原型 === '结算修正') {
                if (['消耗', '前摇'].includes(结算)) return 数值 < 0;
                return ['治疗', '受到伤害', '伤害吸收', '伤害分摊', '消耗分摊', '技能效果'].includes(结算);
              }
              return false;
            });
          };
          const 技能具备伤害原型 = 技能 => getSkillEffects(技能, { 行为规划: true })
            .some(effect => String(effect?.原型 || '').trim() === '伤害结算' && (
              Number(effect?.威力倍率 || effect?.威力 || effect?.基础倍率 || 0) > 0 ||
              Number(effect?.段数 || 0) > 0 ||
              /伤害/.test(String(effect?.伤害类型 || effect?.结算 || ''))
            ));
          const 支援技能有新收益 = 技能 => {
            if (!技能) return false;
            const 技能名 = String(技能.name || 技能.魂技名 || 技能.技能名称 || '').trim();
            const 近期次数 = Number(ensureActorDecisionMemory(actor).recent_actions?.[技能名] || 0);
            const 友方候选 = dedupeCombatTargetList([actor, ...allyTeam]).filter(unit => unit && isCombatUnitAlive(unit));
            const 目标 = [...友方候选]
              .sort((左, 右) =>
                scoreAllyTargetForSkill(actor, 右, 技能, battleState.combatData) -
                  scoreAllyTargetForSkill(actor, 左, 技能, battleState.combatData) ||
                getCombatHpRatio(左) - getCombatHpRatio(右),
              )[0] || allyTarget || actor;
            const 规划技能 = 构建行为规划技能(技能, 目标, actor, battleState.combatData);
            const 效果列表 = getSkillEffects(规划技能, { 行为规划: true });
            const 目标血量 = getCombatHpRatio(目标);
            const 最低友方血量 = 友方候选.reduce((最低, unit) => Math.min(最低, getCombatHpRatio(unit)), 1);
            const 自身资源 = 读取规划综合资源比例_V1(actor);
            const 来袭 = strategicContext.behaviorState?.threatProfile || {};
            const 是保护效果 = effect => {
              const 原型 = String(effect?.原型 || '').trim();
              const 结算 = String(effect?.结算 || '').trim();
              const 战斗效果 = effect?.计算层效果 || {};
              if (['护盾变化', '规则防御'].includes(原型)) return true;
              if (原型 === '状态施加') {
                return Number(战斗效果.shield_gain_bonus || 0) > 0 ||
                  战斗效果.super_armor === true ||
                  战斗效果.invincible === true ||
                  Number(战斗效果.damage_reduction || 0) > 0 ||
                  Number(战斗效果.damage_share_ratio || 0) > 0;
              }
              return 原型 === '结算修正' && 结算 === '伤害分摊';
            };
            const 有急救 = (目标血量 < 0.72 || 最低友方血量 < 0.72) &&
              效果列表.some(effect => isBattleRecoverEffect(effect) || String(effect?.原型 || '').trim() === '资源变化');
            const 有保护 = (来袭.lethalRisk === true || 来袭.severeDamage === true || getCombatHpRatio(actor) < 0.45 || 目标血量 < 0.72 || 最低友方血量 < 0.72) &&
              效果列表.some(是保护效果);
            const 有异常处理 = 效果列表.some(effect => ['状态移除', '机制授予'].includes(String(effect?.原型 || '').trim()));
            const 有资源窗口 = 自身资源 < 0.62 && 效果列表.some(effect => String(effect?.原型 || '').trim() === '资源变化' && 读取战斗数值正负(effect?.数值) > 0);
            const 目标已有同名维持 = 友方候选.some(unit => 单位已有同名维持技能(unit, 技能));
            if (目标已有同名维持 && !有急救 && !有保护 && !有异常处理) return false;
            if (近期次数 >= 2 && !有急救 && !有保护 && !有资源窗口) return false;
            return 有急救 || 有保护 || 有异常处理 || 有资源窗口;
          };
          const 存在有效伤害魂技 = (availableSkills || []).some(技能 => 技能具备伤害原型(newSkillData(技能)));
          const 支援无伤害战术窗口 =
            ['辅助系', '治疗系', '食物系'].includes(行动者系别) &&
            !存在有效伤害魂技;
          const 评分职责候选 = ['辅助系', '治疗系', '食物系'].includes(行动者系别)
            ? (skillContext.评分技能列表 || [])
                .filter(项目 => 项目?.skill && 是职责支援技能(项目.规划技能 || 项目.skill) && 支援技能有新收益(项目.skill))
                .map(项目 => {
                  const 技能名 = String(项目.skill.name || 项目.skill.技能名称 || '').trim();
                  const 近期次数 = Number(ensureActorDecisionMemory(actor).recent_actions?.[技能名] || 0);
                  const 仍有承压友方 = dedupeCombatTargetList([actor, ...allyTeam])
                    .some(unit => unit && isCombatUnitAlive(unit) && getCombatHpRatio(unit) < 0.72);
                  return {
                    skill: 项目.skill,
                    weight: Math.max(1, Number(项目.weight || 0) + 28 - (仍有承压友方 ? 0 : 近期次数 * 26)),
                  };
                })
            : [];
          const 技能评分查找 = new Map((skillContext.评分技能列表 || []).map(项目 => [
            String(项目?.skill?.name || 项目?.skill?.魂技名 || 项目?.skill?.技能名称 || '').trim(),
            Number(项目?.weight || 0),
          ]));
          const 直接职责候选 = ['辅助系', '治疗系', '食物系'].includes(行动者系别)
            ? (availableSkills || [])
                .map(skill => newSkillData(skill))
                .filter(skill => skill && 是职责支援技能(skill) && 支援技能有新收益(skill))
                .map(skill => {
                  const 技能名 = String(skill.name || skill.魂技名 || skill.技能名称 || '').trim();
                  return {
                    skill,
                    weight: Math.max(1, Number(技能评分查找.get(技能名) || 0) + scoreAllyTargetForSkill(actor, allyTarget || actor, skill, battleState.combatData) + 18),
                  };
                })
            : [];
          const 主动职责候选 = [...评分职责候选, ...直接职责候选]
            .filter((项目, index, 列表) => {
              const 技能名 = String(项目?.skill?.name || 项目?.skill?.魂技名 || 项目?.skill?.技能名称 || '').trim();
              return 技能名 && 列表.findIndex(候选 => String(候选?.skill?.name || 候选?.skill?.魂技名 || 候选?.skill?.技能名称 || '').trim() === 技能名) === index;
            });
          const 主动职责技能 = [...主动职责候选].sort((左, 右) => Number(右.weight || 0) - Number(左.weight || 0))[0]?.skill || (
            ['辅助系', '治疗系', '食物系'].includes(行动者系别) &&
            (skillContext.recoverSkill || skillContext.teamProtectSkill || skillContext.teamSupportSkill)
          );
          const 支援职责可承压触发 =
            ['辅助系', '治疗系', '食物系'].includes(行动者系别) &&
            是职责支援技能(主动职责技能) &&
            支援技能有新收益(主动职责技能) &&
            strategicContext.behaviorState?.threatProfile?.lethalRisk !== true;
          if (主动职责技能 && 支援技能有新收益(主动职责技能) && (!必要战略窗口 || 支援职责可承压触发)) {
            recordActorActionMemory(actor, 主动职责技能.name || 主动职责技能.技能名称 || '支援补位');
            return convertDecisionToTurnAction(
              makeActorAction(
                '支援补位',
                `[职责优先] ${行动者系别 || '支援型'}NPC优先完成团队支援职责，直接以[${主动职责技能.name}]稳住局面。`,
                主动职责技能,
              ),
            );
          }
          const 是低效纯直伤技能 = 技能 => {
            if (!技能 || !enemyTarget) return false;
            const 效果列表 = getSkillEffects(技能, { 行为规划: true });
            if (!效果列表.some(effect => String(effect?.原型 || '').trim() === '伤害结算')) return false;
            const 有战术副价值 = 效果列表.some(effect => {
              const 原型 = String(effect?.原型 || '').trim();
              const 结算 = String(effect?.结算 || '').trim();
              if (原型 === '伤害结算') return Number(effect?.防御穿透 || 0) >= 20;
              if (['状态施加', '判定修正', '属性修正', '状态移除', '资源锁定', '规则改写', '决策干扰', '时窗修正'].includes(原型)) return true;
              if (原型 === '结算修正') return ['防御穿透', '防御剥夺', '精神抗性剥夺', '持续伤害引爆', '消耗', '前摇'].includes(结算);
              return false;
            });
            if (有战术副价值) return false;
            const 规划技能 = normalizeSkillData(技能, 技能.name || 技能.魂技名 || 技能.技能名称 || '技能');
            const 威胁 = estimateIncomingActionThreat(actor, enemyTarget, {
              action_type: '技能',
              type: '技能',
              cast_time: getSkillCastTime(规划技能),
              skill: 规划技能,
            }, battleState.combatData || {});
            return Number(威胁.projectedDamageRatio || 0) < 0.015;
          };
          const 近期被闪牵制次数 = Math.max(0, Number(ensureActorDecisionMemory(actor).dodge_whiff_count || 0));
          const 闪避克制次数 = Math.max(读取闪避克制记忆(actor, enemyTarget), 近期被闪牵制次数);
          const 目标敏捷压制 =
            !!enemyTarget &&
            (
              读取规划单位系别(enemyTarget) === '敏攻系' ||
              Number(enemyTarget?.final?.agi ?? enemyTarget?.agi ?? 0) > Math.max(1, Number(actor?.final?.agi ?? actor?.agi ?? 0)) * 1.18
            );
          const 目标近期拉扯 =
            !!enemyTarget &&
            (
              Number(enemyTarget?.__本回合闪避成功次数 || 0) > 0 ||
              /闪避|游击|拉开|脱离|瞬移/.test(String(ensureActorDecisionMemory(enemyTarget).last_action || ''))
            );
          const 目标敏攻拉扯 =
            !!enemyTarget &&
            (
              闪避克制次数 > 0 ||
              (目标敏捷压制 && 目标近期拉扯) ||
              (目标敏捷压制 && Number(enemyTarget?.__本回合闪避成功次数 || 0) > 0)
            );
          const 反敏攻牵制成立 = 目标敏捷压制 && 闪避克制次数 > 0 || 目标敏攻拉扯;
          const 稳定反敏攻牵制 = !!enemyTarget && (目标敏捷压制 || 闪避克制次数 >= 2 || (目标敏攻拉扯 && 闪避克制次数 > 0));
          const 技能具备反敏攻价值 = 技能 => {
            if (!技能) return false;
            if (isBattleSkillControlProfile(技能)) return true;
            if (hasBattleSkillRuntimeConsumer(技能, ['interrupt', 'hard_control', 'skill_seal', 'self_shift', 'disengage_shift', 'position_exchange', 'pursuit_shift'])) return true;
            const 目标文本 = String(getSkillTarget(技能) || '').trim();
            if (/群体|全场/.test(目标文本)) return true;
            return getSkillEffects(技能, { 行为规划: true }).some(effect => {
              const 原型 = String(effect?.原型 || '').trim();
              const 战斗效果 = effect?.计算层效果 || {};
              if (['资源锁定', '规则改写', '决策干扰', '判定修正', '时窗修正'].includes(原型)) return true;
              if (原型 === '状态施加') {
                return Number(战斗效果.hit_bonus || 0) > 0 ||
                  Number(战斗效果.lock_level || 0) > 0 ||
                  Number(战斗效果.dodge_penalty || 0) > 0 ||
                  Number(战斗效果.cast_speed_penalty || 0) > 0 ||
                  战斗效果.skip_turn === true ||
                  战斗效果.cannot_react === true ||
                  战斗效果.silence === true ||
                  战斗效果.skill_seal === true ||
                  战斗效果.blind === true;
              }
              return false;
            });
          };
          const 技能属于明确反敏攻技能 = 技能 => {
            if (!技能) return false;
            if (isBattleSkillControlProfile(技能)) return true;
            if (isBattleSkillReactiveDefenseProfile(技能) || isBattleSkillDefensiveProfile(技能)) return true;
            if (hasBattleSkillRuntimeConsumer(技能, ['interrupt', 'hard_control', 'skill_seal', 'self_shift', 'disengage_shift', 'position_exchange', 'pursuit_shift'])) return true;
            return /群体|全场/.test(String(getSkillTarget(技能) || '').trim());
          };
          const 技能属于敏攻陷阱动作 = 技能 => {
            if (!技能) return /普通攻击|常规攻击/.test(String(技能 || ''));
            if (!技能具备伤害原型(技能)) return false;
            if (技能具备反敏攻价值(技能)) return false;
            const 目标文本 = String(getSkillTarget(技能) || '').trim();
            if (/群体|全场/.test(目标文本)) return false;
            return true;
          };
          const 动作属于敏攻陷阱动作 = 动作 => {
            const 动作名 = normalizeBattleActionDisplayName(动作?.skill?.name || 动作?.skill?.魂技名 || 动作?.type || 动作?.action_type || '');
            const 动作类型 = String(动作?.type || 动作?.action_type || '').trim();
            if (/短前摇对轰|强势对轰|主动压迫/.test(动作类型)) {
              const 目标文本 = String(getSkillTarget(动作?.skill || {}) || '').trim();
              const 技能分类 = String(动作?.skill?.技能分类 || '').trim();
              if (
                !技能属于明确反敏攻技能(动作?.skill || null) ||
                (
                  /输出|强攻|敏攻|破防/.test(技能分类) &&
                  !/群体|全场/.test(目标文本) &&
                  技能具备伤害原型(动作?.skill || null)
                )
              ) return true;
            }
            if (/普通攻击|常规攻击|主动压迫|低风险/.test(动作名)) return true;
            return 技能属于敏攻陷阱动作(动作?.skill || null);
          };
          const 动作属于被闪后禁复读动作 = 动作 => {
            if (!enemyTarget || !目标敏攻拉扯) return false;
            const 技能 = 动作?.skill || null;
            const 动作名 = normalizeBattleActionDisplayName(技能?.name || 技能?.魂技名 || 技能?.技能名称 || 动作?.type || 动作?.action_type || '');
            if (!动作名) return false;
            if (读取闪避克制记忆(actor, enemyTarget) <= 0) return false;
            const 目标文本 = String(getSkillTarget(技能 || {}) || '').trim();
            const 友方动作 = 技能偏向友方目标(技能 || {}, 动作);
            if (友方动作 || /群体|全场/.test(目标文本)) return false;
            if (isBattleSkillReactiveDefenseProfile(技能) || isBattleSkillDefensiveProfile(技能)) return false;
            if (hasBattleSkillRuntimeConsumer(技能, ['interrupt', 'hard_control', 'skill_seal', 'self_shift', 'disengage_shift', 'position_exchange', 'pursuit_shift'])) return false;
            const 允许硬控直压 = getSkillEffects(技能, { 行为规划: true }).some(effect => {
              if (String(effect?.原型 || '').trim() !== '状态施加') return false;
              const 战斗效果 = effect?.计算层效果 || {};
              return 战斗效果.skip_turn === true ||
                战斗效果.cannot_react === true ||
                战斗效果.silence === true ||
                战斗效果.skill_seal === true ||
                Number(战斗效果.lock_level || 0) >= 2;
            });
            if (允许硬控直压) return false;
            return 技能具备伤害原型(技能) || isBattleSkillControlProfile(技能);
          };
          const 选择反敏攻技能 = () => {
            const 明确反敏攻首选 = [
              skillContext.controlSkill,
              skillContext.interruptSkill,
              选取范围压制技能(availableSkills),
              skillContext.reactiveDefenseSkill,
              skillContext.defSkill,
            ]
              .map(skill => newSkillData(skill))
              .filter(Boolean)
              .filter((技能, index, 列表) => {
                const 名称 = String(技能.name || 技能.魂技名 || 技能.技能名称 || '').trim();
                return 名称 && 列表.findIndex(候选 => String(候选?.name || 候选?.魂技名 || 候选?.技能名称 || '').trim() === 名称) === index;
              })
              .filter(技能 => !技能属于敏攻陷阱动作(技能))
              .filter(技能 => 技能属于明确反敏攻技能(技能))
              .filter(技能 => parseSkillCostForChar(技能, actor, {
                actor,
                caster: actor,
                target: enemyTarget,
                defender: enemyTarget,
                skill: 技能,
                combatData: battleState.combatData,
                当前行动: '反敏攻调整',
              }).canCast !== false)
              .sort((左, 右) => {
                const 左名 = String(左.name || 左.魂技名 || 左.技能名称 || '').trim();
                const 右名 = String(右.name || 右.魂技名 || 右.技能名称 || '').trim();
                return Number(技能评分查找.get(右名) || 0) - Number(技能评分查找.get(左名) || 0);
              })[0] || null;
            if (明确反敏攻首选) return 明确反敏攻首选;
            return null;
          };
          if (稳定反敏攻牵制) {
            const 入口反敏攻技能 = 选择反敏攻技能();
            if (入口反敏攻技能 && !技能属于敏攻陷阱动作(入口反敏攻技能) && 技能属于明确反敏攻技能(入口反敏攻技能)) {
              recordActorActionMemory(actor, 入口反敏攻技能.name || 入口反敏攻技能.技能名称 || '反敏攻压制');
              return convertDecisionToTurnAction(
                makeActorAction(
                  '反敏攻压制',
                  `[反敏攻调整] ${actor.name || '行动者'}先手压缩高敏目标的拉扯窗口，直接改以[${入口反敏攻技能.name || '反制动作'}]处理对局。`,
                  入口反敏攻技能,
                ),
              );
            }
          }
          const 对手长前摇主动窗口 = Math.max(
            Number(observedTargetAction?.cast_time || 0),
            getSkillCastTime(observedTargetAction?.skill || observedTargetAction || {}),
          ) >= 10;
          const 敏攻长前摇压制技能 = 读取规划单位系别(actor) === '敏攻系' && 对手长前摇主动窗口
            ? (availableSkills || [])
                .map(skill => newSkillData(skill))
                .filter(Boolean)
                .filter(技能 => {
                  const 目标文本 = String(getSkillTarget(技能) || '').trim();
                  return /群体|全场/.test(目标文本) && 技能具备反敏攻价值(技能);
                })
                .filter(技能 => parseSkillCostForChar(技能, actor, {
                  actor,
                  caster: actor,
                  target: enemyTarget,
                  defender: enemyTarget,
                  skill: 技能,
                  combatData: battleState.combatData,
                  当前行动: '长前摇压制',
                }).canCast !== false)
                .sort((左, 右) => {
                  const 左名 = String(左.name || 左.魂技名 || 左.技能名称 || '').trim();
                  const 右名 = String(右.name || 右.魂技名 || 右.技能名称 || '').trim();
                  return Number(技能评分查找.get(右名) || 0) - Number(技能评分查找.get(左名) || 0);
                })[0]
            : null;
          if (敏攻长前摇压制技能) {
            recordActorActionMemory(actor, 敏攻长前摇压制技能.name || 敏攻长前摇压制技能.技能名称 || '范围压制');
            return convertDecisionToTurnAction(
              makeActorAction(
                '范围压制',
                `[主动规划] ${actor.name || '行动者'}捕捉到对手长前摇窗口，改以[${敏攻长前摇压制技能.name || '范围压制'}]截断节奏。`,
                敏攻长前摇压制技能,
              ),
            );
          }
          const strategicAction = chooseAndBuildActorAction(
            actor,
            enemyTarget,
            strategicContext.behaviorState,
            strategicContext.strategicBranches,
            '行为预演/主动战略阶段',
          );
          if (
            strategicAction &&
            (!稳定反敏攻牵制 || (!动作属于敏攻陷阱动作(strategicAction) && !动作属于被闪后禁复读动作(strategicAction)))
          ) return convertDecisionToTurnAction(strategicAction);

          const tacticalBranches = buildTacticalCandidates(
            actor,
            enemyTarget,
            observedTargetAction,
            strategicContext.behaviorState,
            skillContext,
            makeActorAction,
            isSupport,
            isLowHealth,
          );
          if (支援无伤害战术窗口 && !(主动职责技能 && 支援技能有新收益(主动职责技能))) {
            const 来袭 = strategicContext.behaviorState?.threatProfile || {};
            tacticalBranches.push({
              name: '普通攻击',
              weight: 来袭.lethalRisk === true || 来袭.severeDamage === true ? 18 : 44,
              build: () => makeActorAction(
                '常规攻击',
                '[无伤害魂技] 当前没有有效伤害魂技，改用普通攻击试探并争取空间。',
                normalizeSkillData({
                  name: '普通攻击',
                  技能分类: '输出',
                  消耗: '无',
                  前摇: 10,
                  _效果数组: [
                    { 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 },
                  ],
                }, '普通攻击'),
              ),
            });
            tacticalBranches.forEach(候选 => {
              if (!候选) return;
              if (候选.name === '伺机闪避') 候选.weight = Number(候选.weight || 0) + (来袭.lethalRisk === true || 来袭.severeDamage === true ? 54 : 18);
              if (候选.name === '肉体兜底') 候选.weight = Number(候选.weight || 0) + (来袭.lethalRisk === true || 来袭.severeDamage === true ? 42 : 14);
            });
          }
          const tacticalAction = chooseAndBuildActorAction(
            actor,
            enemyTarget,
            strategicContext.behaviorState,
            tacticalBranches,
            'ACTIVE_TACTICAL_STAGE',
            `${skillContext.skillTraceLog ? skillContext.skillTraceLog + ' | ' : ''}`,
          );
          const 非必要兜底动作 =
            tacticalAction &&
            ['肉体兜底', '伺机闪避'].includes(String(tacticalAction.type || '').trim()) &&
            strategicContext.behaviorState?.threatProfile?.lethalRisk !== true &&
            (isSupport || !必要战略窗口 || Number(ensureActorDecisionMemory(actor).recent_actions?.[String(tacticalAction.type || '').trim()] || 0) > 0);
          const 低效直伤动作 = tacticalAction?.skill && 是低效纯直伤技能(tacticalAction.skill) && !必要战略窗口;
          if (
            tacticalAction &&
            !非必要兜底动作 &&
            !低效直伤动作 &&
            (!稳定反敏攻牵制 || (!动作属于敏攻陷阱动作(tacticalAction) && !动作属于被闪后禁复读动作(tacticalAction)))
          ) return convertDecisionToTurnAction(tacticalAction);

          const 回落候选 =
            行动者系别 === '强攻系'
              ? [
                  skillContext.lowCostAtkSkill,
                  skillContext.pierceSkill,
                  skillContext.atkSkill,
                  isLowHealth ? skillContext.recoverSkill : null,
                  isLowHealth ? skillContext.reactiveDefenseSkill : null,
                  isLowHealth ? skillContext.defSkill : null,
                ]
              : ['辅助系', '治疗系', '食物系', '召唤系'].includes(行动者系别)
                ? [
                    skillContext.recoverSkill,
                    skillContext.teamSupportSkill,
                    skillContext.lowCostAtkSkill,
                    skillContext.reactiveDefenseSkill,
                    skillContext.defSkill,
                    skillContext.atkSkill,
                  ]
                : [
                    skillContext.lowCostAtkSkill,
                    skillContext.hardControlSkill,
                    skillContext.rangePressureSkill,
                    skillContext.pierceSkill,
                    skillContext.recoverSkill,
                    skillContext.reactiveDefenseSkill,
                  skillContext.defSkill,
                  skillContext.atkSkill,
                ];
          const 评分技能权重 = new Map((skillContext.评分技能列表 || []).map(项目 => [
            String(项目?.skill?.name || 项目?.skill?.魂技名 || 项目?.skill?.技能名称 || '').trim(),
            Number(项目?.weight || 0),
          ]));
          const 对手长前摇窗口 = Math.max(
            Number(observedTargetAction?.cast_time || 0),
            getSkillCastTime(observedTargetAction?.skill || observedTargetAction || {}),
          ) >= 10;
          const 回落候选技能 = [...回落候选, ...(skillContext.评分技能列表 || []).map(项目 => 项目?.skill)]
            .filter(Boolean)
            .filter((技能, index, 列表) => {
              const 名称 = String(技能.name || 技能.魂技名 || 技能.技能名称 || '').trim();
              return 名称 && 列表.findIndex(候选 => String(候选?.name || 候选?.魂技名 || 候选?.技能名称 || '').trim() === 名称) === index;
            })
            .filter(技能 => !(是低效纯直伤技能(技能) && !必要战略窗口))
            .filter(技能 => !(稳定反敏攻牵制 && 技能属于敏攻陷阱动作(技能)))
            .map((技能, index) => {
              const 名称 = String(技能.name || 技能.魂技名 || 技能.技能名称 || '').trim();
              const 近期次数 = Number(ensureActorDecisionMemory(actor).recent_actions?.[名称] || 0);
              const 效果列表 = getSkillEffects(技能, { 行为规划: true });
              const 是防护承压 = 效果列表.some(effect => ['护盾变化', '规则防御'].includes(String(effect?.原型 || '').trim()) || String(effect?.结算 || '').trim() === '伤害分摊');
              const 被闪惩罚 = getActorSkillDodgeWhiffPenalty(actor, 名称, enemyTarget);
              const 目标文本 = String(getSkillTarget(技能) || '').trim();
              const 是单体前压 = 技能具备伤害原型(技能) && !/群体|全场/.test(目标文本);
              const 是范围控制 = /群体|全场/.test(目标文本) && (
                isBattleSkillControlProfile(技能) ||
                getSkillEffects(技能, { 行为规划: true }).some(effect => String(effect?.原型 || '').trim() === '状态施加')
              );
              const 基础权重 = Number(评分技能权重.get(名称) || 0) + Math.max(0, 24 - index * 3);
              return {
                技能,
                权重: 基础权重 -
                  近期次数 * (是防护承压 && !必要战略窗口 ? 42 : 26) -
                  被闪惩罚 -
                  ((稳定反敏攻牵制 && 是单体前压) ? 18 + 闪避克制次数 * 8 : 0) +
                  (读取规划单位系别(actor) === '敏攻系' && 是范围控制 ? 96 : 0) +
                  (读取规划单位系别(actor) === '敏攻系' && 对手长前摇窗口 && 是范围控制 ? 180 : 0),
              };
            })
            .sort((左, 右) => 右.权重 - 左.权重);
          const 回落技能 = 回落候选技能[0]?.技能 || null;
          const 反敏攻技能 = 稳定反敏攻牵制 ? 选择反敏攻技能() : null;
          if (稳定反敏攻牵制 && 反敏攻技能) {
            recordActorActionMemory(actor, 反敏攻技能.name || 反敏攻技能.技能名称 || '反敏攻压制');
            recordActorActionMemory(actor, '反敏攻压制');
            return convertDecisionToTurnAction(
              makeActorAction(
                '反敏攻压制',
                `[反敏攻调整] ${actor.name || '行动者'}判断正面硬追会继续送出闪避窗口，改以[${反敏攻技能.name || '反制动作'}]压缩对手拉扯空间。`,
                反敏攻技能,
              ),
            );
          }
          if (稳定反敏攻牵制 && 回落技能) {
            recordActorActionMemory(actor, 回落技能.name || 回落技能.技能名称 || '主动轮换');
            return convertDecisionToTurnAction(
              makeActorAction(
                '主动轮换',
                `[反敏攻调整] ${actor.name || '行动者'}没有专用反制动作，改以[${回落技能.name || '战术技能'}]继续试探，不再空转收招。`,
                回落技能,
              ),
            );
          }
          if (回落技能) {
            recordActorActionMemory(actor, 回落技能.name || 回落技能.技能名称 || '稳态调整');
            return convertDecisionToTurnAction(
              makeActorAction(
                '稳态调整',
                `[稳态调整] 无明确优势动作，转为低风险的[${回落技能.name || '战术技能'}]调整节奏。`,
                回落技能,
              ),
            );
          }
          const 支援轮换技能 = isSupport
            ? (availableSkills || [])
                .map(skill => newSkillData(skill))
                .filter(skill => skill && 是职责支援技能(skill) && 支援技能有新收益(skill))
                .filter(skill => parseSkillCostForChar(skill, actor, {
                  actor,
                  caster: actor,
                  target: allyTarget || actor,
                  defender: enemyTarget,
                  skill,
                  combatData: battleState.combatData,
                  当前行动: '支援轮换',
                }).canCast !== false)
                .sort((左, 右) => {
                  const 左名 = String(左.name || 左.魂技名 || '').trim();
                  const 右名 = String(右.name || 右.魂技名 || '').trim();
                  const 记忆 = ensureActorDecisionMemory(actor);
                  return Number(记忆.recent_actions?.[左名] || 0) - Number(记忆.recent_actions?.[右名] || 0) || getSkillCastTime(左) - getSkillCastTime(右);
                })[0]
            : null;
          const 任意轮换技能 = isSupport && !支援轮换技能
            ? (availableSkills || [])
                .map(skill => newSkillData(skill))
                .filter(skill => skill && (技能具备伤害原型(skill) || 支援技能有新收益(skill)))
                .filter(skill => parseSkillCostForChar(skill, actor, {
                  actor,
                  caster: actor,
                  target: allyTarget || actor,
                  defender: enemyTarget,
                  skill,
                  combatData: battleState.combatData,
                  当前行动: '支援轮换',
                }).canCast !== false)
                .sort((左, 右) => {
                  const 记忆 = ensureActorDecisionMemory(actor);
                  const 左名 = String(左.name || 左.魂技名 || '').trim();
                  const 右名 = String(右.name || 右.魂技名 || '').trim();
                  return Number(记忆.recent_actions?.[左名] || 0) - Number(记忆.recent_actions?.[右名] || 0) || getSkillCastTime(左) - getSkillCastTime(右);
                })[0]
            : null;
          const 实际轮换技能 = 支援轮换技能 || 任意轮换技能;
          if (实际轮换技能 && strategicContext.behaviorState?.threatProfile?.lethalRisk !== true) {
            recordActorActionMemory(actor, 实际轮换技能.name || 实际轮换技能.技能名称 || '支援轮换');
            return convertDecisionToTurnAction(
              makeActorAction(
                '支援轮换',
                `[支援轮换] 暂无急救窗口，改用[${实际轮换技能.name || '支援技能'}]轮换维持团队收益。`,
                实际轮换技能,
              ),
            );
          }
          const 目标动作非敌对 =
            enemyTarget &&
            !判定单挑动作敌对(observedTargetAction, enemyTarget, actor, battleState.combatData);
          if (目标动作非敌对 && strategicContext.behaviorState?.threatProfile?.lethalRisk !== true) {
            const 行动记忆 = ensureActorDecisionMemory(actor);
            const 可用敌对技能 = (availableSkills || [])
              .map(skill => newSkillData(skill))
              .filter(skill => skill && (技能具备伤害原型(skill) || getSkillEffects(skill, { 行为规划: true }).some(effect => {
                const 原型 = String(effect?.原型 || '').trim();
                const 目标文本 = String(effect?.目标 || skill?.目标 || '').trim();
                return ['状态施加', '资源锁定', '规则改写', '决策干扰', '判定修正'].includes(原型) &&
                  !['自身', '友方', '友方单体', '友方群体'].includes(目标文本);
              })))
              .filter(skill => parseSkillCostForChar(skill, actor, {
                actor,
                caster: actor,
                target: enemyTarget,
                defender: enemyTarget,
                skill,
                combatData: battleState.combatData,
                当前行动: '主动压迫',
              }).canCast !== false);
            const 无有效敌对技能 =
              !skillContext.lowCostAtkSkill &&
              !skillContext.atkSkill &&
              !skillContext.pierceSkill &&
              !skillContext.controlSkill &&
              !skillContext.interruptSkill &&
              !可用敌对技能.length;
            if (无有效敌对技能) {
              recordActorActionMemory(actor, '战术观察');
              return convertDecisionToTurnAction(
                makeActorAction(
                  '战术观察',
                  `[战术观察] ${actor.name || '行动者'}未找到合适敌对技能，保持距离观察，不以普通攻击硬追。`,
                  null,
                ),
              );
            }
            const 压迫候选列表 = [
              反敏攻技能,
              skillContext.hardControlSkill,
              skillContext.rangePressureSkill,
              skillContext.controlSkill,
              skillContext.interruptSkill,
              ...可用敌对技能,
            ]
              .filter(Boolean)
              .filter((技能, index, 列表) => {
                const 名称 = String(技能?.name || 技能?.魂技名 || 技能?.技能名称 || '').trim();
                return 名称 && 列表.findIndex(候选 => String(候选?.name || 候选?.魂技名 || 候选?.技能名称 || '').trim() === 名称) === index;
              })
              .map(技能 => {
                const 名称 = String(技能?.name || 技能?.魂技名 || 技能?.技能名称 || '').trim();
                const 近期次数 = 读取归一行为记忆值_V1(行动记忆?.recent_actions || {}, [名称], 0);
                const 衰减 = 评估敌对重复状态收益衰减(技能, enemyTarget, {
                  projectedDamageRatio: 0,
                  recentSameActionCount: 近期次数,
                });
                return { 技能, 名称, 近期次数, 衰减 };
              });
            const 可替代压迫候选 = 压迫候选列表.filter(项目 => Number(项目?.衰减?.惩罚 || 0) < 80);
            const 压迫候选命中 = (可替代压迫候选[0] || 压迫候选列表[0]) || null;
            const 压迫技能 = 压迫候选命中?.技能 || normalizeSkillData(
              {
                name: '普通攻击',
                技能分类: '输出',
                消耗: '无',
                前摇: 10,
                _效果数组: [
                  { 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 },
                ],
              },
              '普通攻击',
            );
            const 压迫重复状态衰减 = 压迫候选命中?.衰减 || { 惩罚: 0, 状态列表: [] };
            const 压迫技能名 = String(压迫技能?.name || 压迫技能?.魂技名 || 压迫技能?.技能名称 || '').trim();
            const 同技压迫次数 = 读取归一行为记忆值_V1(行动记忆?.recent_actions || {}, [压迫技能名], 0);
            const 主动压迫次数 = 读取归一行为记忆值_V1(行动记忆?.recent_actions || {}, ['主动压迫', '反敏攻压制'], 0);
            const 压迫属于真实反敏攻 = !!压迫技能 && (
              hasBattleSkillRuntimeConsumer(压迫技能, ['interrupt', 'hard_control', 'skill_seal', 'self_shift', 'disengage_shift', 'position_exchange', 'pursuit_shift']) ||
              /群体|全场/.test(String(getSkillTarget(压迫技能) || '').trim()) ||
              判定技能具备真实截断资格_V1(压迫技能)
            );
            const 连续前压应转守 =
              !目标敏攻拉扯 &&
              (
                (同技压迫次数 >= 1 && (!压迫属于真实反敏攻 || Number(压迫重复状态衰减.惩罚 || 0) >= 80)) ||
                主动压迫次数 >= 2
              );
            const 需要轮换压迫 =
              (
                目标敏攻拉扯 &&
                (
                  !反敏攻技能 ||
                  动作属于敏攻陷阱动作({ type: '主动压迫', action_type: '主动压迫', skill: 压迫技能 })
                )
              ) || 连续前压应转守;
            const 轮换反敏攻技能 = 目标敏攻拉扯
              ? (availableSkills || [])
                  .map(skill => newSkillData(skill))
                  .filter(skill => skill && skill !== 压迫技能 && 技能具备反敏攻价值(skill) && !技能属于敏攻陷阱动作(skill))
                  .filter(skill => parseSkillCostForChar(skill, actor, {
                    actor,
                    caster: actor,
                    target: enemyTarget,
                    defender: enemyTarget,
                    skill,
                    combatData: battleState.combatData,
                    当前行动: '反敏攻轮换',
                  }).canCast !== false)
                  .sort((左, 右) => Number(技能评分查找.get(String(右.name || 右.魂技名 || 右.技能名称 || '').trim()) || 0) - Number(技能评分查找.get(String(左.name || 左.魂技名 || 左.技能名称 || '').trim()) || 0))[0]
              : null;
            const 轮换压迫技能 = 需要轮换压迫
              ? (
                  轮换反敏攻技能 ||
                  压迫候选列表.find(项目 => 项目?.技能 && 项目?.技能 !== 压迫技能 && Number(项目?.衰减?.惩罚 || 0) < 80)?.技能 ||
                  (目标敏攻拉扯 ? 压迫技能 : normalizeSkillData(
                    {
                      name: '普通攻击',
                      技能分类: '输出',
                      消耗: '无',
                      前摇: 10,
                      _效果数组: [
                        { 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 },
                      ],
                    },
                    '普通攻击',
                  ))
                )
              : 压迫技能;
            const 实际压迫技能 = 轮换压迫技能 || 压迫技能;
            recordActorActionMemory(actor, 实际压迫技能.name || 实际压迫技能.技能名称 || (目标敏攻拉扯 ? '反敏攻压制' : '主动压迫'), { target: enemyTarget });
            recordActorActionMemory(actor, 目标敏攻拉扯 ? '反敏攻压制' : '主动压迫', { target: enemyTarget });
            return convertDecisionToTurnAction(
              makeActorAction(
                目标敏攻拉扯 ? '反敏攻压制' : '主动压迫',
                需要轮换压迫
                  ? `[主动轮换] ${actor.name || '行动者'}不再空转收招，改以[${实际压迫技能.name || '普通攻击'}]重新试探对手反应。`
                  : 目标敏攻拉扯
                    ? `[反敏攻调整] ${actor.name || '行动者'}识别到前压被闪避牵制，改以[${实际压迫技能.name || '战术动作'}]处理拉扯窗口。`
                    : `[主动压迫] ${actor.name || '行动者'}判断对手没有主动进攻，改以[${实际压迫技能.name || '普通攻击'}]前压争夺节奏。`,
                实际压迫技能,
              ),
            );
          }
          if (isSupport && strategicContext.behaviorState?.threatProfile?.lethalRisk !== true) {
            recordActorActionMemory(actor, '战术待机');
            return convertDecisionToTurnAction(
              makeActorAction(
                '战术待机',
                `[战术待机] 暂无新的有效支援窗口，维持既有节奏并观察战局。`,
                null,
              ),
            );
          }
          return convertDecisionToTurnAction(
            makeActorAction('肉体兜底', '[承压守势] 无合适魂技，收缩防御，准备承伤硬抗。', null, {
              def_mult: 1.2,
            }),
          );
        }

        function createActorTurnCombatData(actorEntry, target, battleState) {
          const actor = actorEntry.char;
          const actorAllies =
            actorEntry.side === 'player'
              ? 读取战斗主队单位列表(battleState.combatData, '玩家').filter(unit => unit.name !== actor.name)
              : 读取战斗主队单位列表(battleState.combatData, '敌方').filter(unit => unit.name !== actor.name);
          const targetAllies =
            actorEntry.side === 'player'
              ? 读取战斗主队单位列表(battleState.combatData, '敌方').filter(unit => unit.name !== target.name)
              : 读取战斗主队单位列表(battleState.combatData, '玩家').filter(unit => unit.name !== target.name);

          return {
            战斗类型: battleState.combatData.战斗类型 || '突发遭遇',
            回合: battleState.combatData.回合,
            __规则改写运行态: battleState.combatData.__规则改写运行态,
            __父级战斗数据: battleState.combatData,
            先攻: actor.name,
            参战者: {
              team_player: [actor, ...actorAllies],
              team_enemy: [target, ...targetAllies],
            },
          };
        }

        function isActorHardControlled(char) {
          if (!char?.状态效果) return false;
          return Object.values(char.状态效果).some(cond => cond?.战斗效果?.skip_turn === true);
        }

        function runActorTurn(actorEntry, battleState) {
          if (!actorEntry || !battleState?.combatData) return null;
          const actor = actorEntry.char;
          bindCombatParticipant(actor);
          syncCombatActionState(actor);
          if (!isCombatUnitAlive(actor)) {
            return { actor: actor.name, side: actorEntry.side, skipped: true, reason: '已失去战斗力' };
          }
          if (!isCombatUnitAbleToFight(actor)) {
            return {
              actor: actor.name,
              side: actorEntry.side,
              skipped: true,
              reason: '体力耗尽',
              log: `[团战执行] ${actor.name}体力归零，已陷入昏迷，本回合无法行动。`,
            };
          }

          if (isActorHardControlled(actor)) {
            return {
              actor: actor.name,
              side: actorEntry.side,
              skipped: true,
              reason: '处于硬控状态',
              log: `[团战执行] ${actor.name}处于硬控状态，本回合无法行动。`,
            };
          }

          const targets = chooseTargetForActor(actorEntry, battleState);
          const declaredRound = Number(actorEntry.__declaredRound || 0);
          const currentRound = Number(battleState.combatData?.回合 || battleState.round || 0);
          const declaredTargetName = String(actorEntry.__declaredTargetName || '').trim();
          if (declaredRound === currentRound && declaredTargetName) {
            const declaredTarget = [
              ...读取战斗阵营单位列表(battleState.combatData, actorEntry.side === 'player' ? '敌方' : '玩家'),
              ...读取战斗阵营单位列表(battleState.combatData, actorEntry.side === 'player' ? '玩家' : '敌方'),
            ].find(unit => isCombatUnitIdentityMatch(unit, declaredTargetName) && isCombatUnitAlive(unit));
            if (!declaredTarget) {
              const actionName = normalizeBattleActionDisplayName(actorEntry.__declaredActionName || actorEntry.__declaredAction?.skill?.name || actorEntry.__declaredAction?.skill?.魂技名 || actorEntry.__declaredAction?.action_type || '行动');
              const initialIntent = 查找行动轴初始意图节点(battleState.combatData, {
                round: currentRound,
                actorName: actor.name || actor.名称 || '',
                targetName: declaredTargetName,
                actionName,
              });
              const blockedEvent = 写入战斗事件账本(battleState.combatData, {
                eventKind: 'blocked_action',
                round: currentRound,
                actorName: actor.name || actor.名称 || '',
                actorSide: actorEntry.side === 'enemy' ? 'enemy' : 'player',
                targetName: declaredTargetName,
                targetSide: actorEntry.side === 'enemy' ? 'player' : 'enemy',
                actionName,
                initialActionName: actionName,
                finalActionName: actionName,
                actionType: 'target_lost',
                parentNodeId: initialIntent?.nodeId || '',
                sourceNodeId: initialIntent?.nodeId || '',
                result: 'blocked',
                failReason: 'target_lost',
                failureReason: 'target_lost',
                meta: {
                  source: 'declared_target_lost',
                  reasonCode: 'TARGET_LOST',
                  reasonText: '预声明目标已失去有效落点',
                  declaredTargetName,
                  declaredActionName: actionName,
                },
              });
              return {
                actor: actor.name,
                side: actorEntry.side,
                target: declaredTargetName,
                skipped: true,
                reason: '预声明目标已失效',
                action: actorEntry.__declaredAction || null,
                blockedEvent,
                log: `[目标丢失] ${actor.name}原本锁定${declaredTargetName}，但该目标已失去有效落点，本次行动取消。`,
              };
            }
            if (declaredTarget && targets) {
              if (读取规划单位阵营(declaredTarget, battleState.combatData) === 读取规划单位阵营(actor, battleState.combatData)) targets.allyTarget = declaredTarget;
              else targets.enemyTarget = declaredTarget;
            }
          }
          if (!targets || !targets.enemyTarget) {
            return { actor: actor.name, side: actorEntry.side, skipped: true, reason: '无可用目标' };
          }
          bindCombatParticipant(targets.enemyTarget);
          bindCombatParticipant(targets.allyTarget);

          let action = null;
          let actionLog = '';
          const turnExtraPatchOps = [];
          if (actor.蓄力技能) {
            action = actor.蓄力技能;
            if ((action.cast_time || 0) <= 40) {
              actionLog += `[团战执行] ${actor.name}完成蓄力，释放[${action.skill?.name || action.action_type}]！ `;
              actor.蓄力技能 = null;
            } else {
              action.cast_time -= 30;
              return {
                actor: actor.name,
                side: actorEntry.side,
                target: targets.enemyTarget.name,
                charging: true,
                action,
                log: `[团战执行] ${actor.name}继续为[${action.skill?.name || action.action_type}]蓄力，剩余前摇:${action.cast_time}。`,
              };
            }
          } else {
            action = declaredRound === currentRound && actorEntry.__declaredAction
              ? actorEntry.__declaredAction
              : buildAutoActionForActor(actorEntry, targets, battleState);
            if (!(declaredRound === currentRound && actorEntry.__declaredAction)) {
              刷新队伍行动轴意图(actorEntry, targets, action, battleState);
            }
            action = 去重动作队列友方辅助(actor, action);
            套用动作队列实际前摇(actor, action, targets.enemyTarget, battleState.combatData);

            let totalTimeCost = 0;
            let validPreActions = [];
            let carryOverAction = null;

            if (action.pre_actions && action.pre_actions.length > 0) {
              for (let i = 0; i < action.pre_actions.length; i++) {
                let pa = action.pre_actions[i];
                if (totalTimeCost + pa.cast_time <= 40) {
                  totalTimeCost += pa.cast_time;
                  validPreActions.push(pa);
                } else {
                  carryOverAction = pa;
                  carryOverAction.cast_time -= Math.max(0, 40 - totalTimeCost);
                  break;
                }
              }
            }

            if (!carryOverAction) {
              if (totalTimeCost + (action.cast_time || 0) <= 40) {
                totalTimeCost += action.cast_time || 0;
              } else {
                carryOverAction = action;
                carryOverAction.cast_time -= Math.max(0, 40 - totalTimeCost);
              }
            }

            validPreActions.forEach(preAct => {
              const preCostLog = applyActionCost(actor, preAct, targets.enemyTarget, battleState.combatData);
              if (preCostLog) actionLog += preCostLog + ' ';
              if (preAct.action_type === '穿戴装备') {
                应用战斗装备变化(actor, () => {
                  const 装备槽 = ensureBattleEquipmentSlot(actor, preAct.equip_target);
                  if (装备槽) 装备槽.装备状态 = '已装备';
                });
                actionLog += `[连招生效] ${actor.name}趁隙穿戴了${preAct.equip_target === 'armor' ? '斗铠' : '机甲'}！`;
              } else {
                const 前置结算 = 执行连放前置动作结算(actor, targets.enemyTarget, preAct, createActorTurnCombatData(actorEntry, targets.enemyTarget, battleState));
                if (前置结算.log) actionLog += `${replaceBattleReportGenericNames(前置结算.log, { player: actor, enemy: targets.enemyTarget })} `;
                if (Array.isArray(前置结算.extraPatchOps) && 前置结算.extraPatchOps.length)
                  turnExtraPatchOps.push(...前置结算.extraPatchOps);
              }
              清理动作运行态字段(preAct);
            });
            action.pre_actions = validPreActions;

            if (carryOverAction) {
              actor.蓄力技能 = carryOverAction;
              return {
                actor: actor.name,
                side: actorEntry.side,
                target: targets.enemyTarget.name,
                charging: true,
                action: actor.蓄力技能,
                log: replaceBattleReportGenericNames(`[团战执行/转蓄力] 连招耗时过长，${actor.name}进入蓄力状态准备[${carryOverAction.skill?.name || carryOverAction.action_type}]，剩余前摇:${carryOverAction.cast_time}。`, {
                  player: actor,
                  enemy: targets.enemyTarget,
                }),
              };
            }

            if (action.action_type !== '施法失败') {
              const costLog = applyActionCost(actor, action, targets.enemyTarget, battleState.combatData);
              if (action.decision_log) actionLog += action.decision_log + ' ';
              if (costLog) actionLog += costLog + ' ';
            }

            if (action.action_type === '收回召唤') {
              const 收回日志 = 主动收回召唤单位(battleState.combatData, actor, action.target_name || '');
              清理动作运行态字段(action);
              return {
                actor: actor.name,
                side: actorEntry.side,
                target: actor.name,
                action,
                reactionAction: null,
                settleResult: { desc: 收回日志 },
                log: `${actionLog}[团战执行] ${actor.name}收回召唤。 ${收回日志}`.trim(),
                extraPatchOps: turnExtraPatchOps,
                actorVit: getCombatHpValue(actor),
                targetVit: getCombatHpValue(actor),
              };
            }
          }

          let finalTarget = targets.enemyTarget;
          const skillTargetKind = inferSkillPrimaryTargetKind(action?.skill || {});
          const targetsFriendlyTeam = ['自身', '友方单体', '友方群体'].includes(skillTargetKind);
          const enemyTeam =
            actorEntry.side === 'player'
              ? 读取战斗阵营单位列表(battleState.combatData, '敌方')
              : 读取战斗阵营单位列表(battleState.combatData, '玩家');
          const allyTeam =
            actorEntry.side === 'player'
              ? [actor, ...读取战斗阵营单位列表(battleState.combatData, '玩家').filter(unit => unit.name !== actor.name)]
              : [actor, ...读取战斗阵营单位列表(battleState.combatData, '敌方').filter(unit => unit.name !== actor.name)];
          if (targetsFriendlyTeam && skillTargetKind !== '自身') {
            finalTarget = chooseAllyTargetForSkill(actor, allyTeam, action?.skill, targets.allyTarget || actor, battleState.combatData);
          } else if (skillTargetKind === '自身') {
            finalTarget = actor;
          } else if (battleState?.observedTargetAction && targets?.enemyTarget) {
            finalTarget = targets.enemyTarget;
          } else {
            finalTarget = chooseEnemyTargetForSkill(actor, enemyTeam, action?.skill, targets.enemyTarget, battleState.combatData);
          }
          if (!finalTarget) finalTarget = targets.enemyTarget || actor;
          if (!targetsFriendlyTeam) {
            const guardRedirectTarget = resolveGuardRedirectTarget(finalTarget, enemyTeam);
            if (guardRedirectTarget && guardRedirectTarget.name !== finalTarget.name) {
              actionLog += `[护卫拦截] ${guardRedirectTarget.name}挺身挡在${finalTarget.name}身前，接下本次攻击。 `;
              finalTarget = guardRedirectTarget;
            }
          }
          action.target_name = finalTarget?.name || action.target_name || null;
          if (action?.skill && action.target_name) action.skill.__指定目标名 = action.target_name;
          const actionStartEvent = 写入战斗事件账本(battleState.combatData, {
            eventKind: 'action_start',
            round: Number(battleState.combatData?.回合 || 0),
            actorName: actor?.name || actor?.名称 || '',
            actorSide: actorEntry.side === 'enemy' ? 'enemy' : 'player',
            targetName: finalTarget?.name || finalTarget?.名称 || '',
            targetSide: actorEntry.side === 'enemy' ? (targetsFriendlyTeam ? 'enemy' : 'player') : (targetsFriendlyTeam ? 'player' : 'enemy'),
            actionName: action?.skill?.name || action?.skill?.魂技名 || action?.action_type || '行动',
            initialActionName: normalizeBattleActionDisplayName(actorEntry.__declaredActionName || action?.skill?.name || action?.skill?.魂技名 || action?.action_type || '行动'),
            finalActionName: normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.action_type || '行动'),
            actionType: actor.召唤键 ? (action?.action_type || '召唤自主行动') : (action?.action_type || action?.type || '自动行动'),
            targetScope: 推断战斗行动目标范围(action) || 'single',
            result: 'declared',
            meta: {
              source: actor.召唤键 ? 'summon' : 'auto_actor',
              summonHostName: actor.召唤键 ? (actor?.宿主名 || actor?.__宿主?.name || actor?.__宿主?.名称 || '') : '',
              summonType: actor.召唤键 ? (actor?.类型 || '') : '',
              summonMode: actor.召唤键 ? (actor?.行动模式 || '') : '',
            },
          });

          const actorTurnCombatData = createActorTurnCombatData(actorEntry, finalTarget, battleState);
          const ratio = calculateReactionRatio(actor, finalTarget, action, actorTurnCombatData);
          let reactionAction = { type: '无法反应', log: '无', skill: null, def_mult: 1.0 };
          if (finalTarget === actor || targetsFriendlyTeam) {
            reactionAction.log = finalTarget === actor
              ? `[行动完成] ${actor.name}完成【${action.skill?.name || action.action_type || '行动'}】。`
              : `[配合] ${actor.name}将【${action.skill?.name || action.action_type || '行动'}】交给${finalTarget.name}承接。`;
          } else {
            reactionAction = finalTarget.is_controlled
              ? { type: '无法反应', log: `${finalTarget.name}处于被控状态，无法动作。`, skill: null, def_mult: 1.0 }
              : determineNpcAction(actorTurnCombatData, action, ratio);
          }

          const 行为链结果 = finalTarget === actor || targetsFriendlyTeam ? null : 自动行为链再判定(actor, finalTarget, action, reactionAction, actorTurnCombatData);
          if (finalTarget !== actor && !targetsFriendlyTeam && actionStartEvent) {
            const reactionType = String(reactionAction?.type || reactionAction?.action_type || '').trim();
            const reactionSkillName = normalizeBattleActionDisplayName(reactionAction?.skill?.name || reactionAction?.skill?.魂技名 || reactionType || '应招');
            let reactionEventKind = 'pass';
            let reactionResult = 'failed';
            if (/伺机闪避|闪避|dodge/i.test(reactionType + reactionSkillName)) {
              reactionEventKind = 'dodge';
              reactionResult = /成功|evaded|dodge_success/i.test(String(reactionAction?.log || reactionAction?.result || '')) ? 'evaded' : 'attempted';
            } else if (/防御|承伤硬抗|硬抗|偏转|收招转防|借力守势|坚壁|guard|defend/i.test(reactionType + reactionSkillName)) {
              reactionEventKind = 'defend';
              reactionResult = 'guarded';
            } else if (/无法反应|来不及|被控|控制|抢招失败|no reaction/i.test(reactionType + String(reactionAction?.log || ''))) {
              reactionEventKind = 'pass';
              reactionResult = 'reaction_failed';
            }
            写入战斗事件账本(battleState.combatData, {
              eventKind: reactionEventKind,
              round: Number(battleState.combatData?.回合 || 0),
              actorName: finalTarget?.name || finalTarget?.名称 || '',
              targetName: actor?.name || actor?.名称 || '',
              actionName: reactionSkillName,
              actionType: 'reaction',
              sourceActionName: actionStartEvent.actionName || action?.skill?.name || action?.skill?.魂技名 || action?.action_type || '行动',
              sourceActionId: actionStartEvent.actionId || '',
              parentNodeId: actionStartEvent.chainNodeId || '',
              sourceNodeId: actionStartEvent.chainNodeId || '',
              result: reactionResult,
              meta: {
                source: actor.召唤键 ? 'summon_target_reaction' : 'auto_actor_target_reaction',
                reactionType,
                reactionLog: String(reactionAction?.log || '').trim(),
              },
            });
          }
          const settleResult = executeClash(action, reactionAction, actorTurnCombatData);
          const 目标反应日志 = replaceBattleReportGenericNames(
            `${行为链结果?.日志 ? 行为链结果.日志 + ' ' : ''}${reactionAction.log} ${settleResult.desc}`,
            { player: actor, enemy: finalTarget },
          );
          let turnLog =
            `${actionLog}[团战执行] ${actor.name}以[${action?.skill?.name || action?.action_type || '未知动作'}]指向[${finalTarget.name}]。 ${目标反应日志}`.trim();

          const damagePackage = applyResolvedDamagePackage(actor, action, settleResult, {
            primaryTarget: finalTarget,
            combatData: actorTurnCombatData,
          });
          let appliedDamage = damagePackage.primaryAppliedDamage;
          if (damagePackage.log) turnLog += ` ${damagePackage.log}`;
          const 行为防反日志 = 执行行为防反结算(
            actor,
            finalTarget,
            action,
            reactionAction,
            settleResult,
            damagePackage,
            actorTurnCombatData,
          );
          if (行为防反日志) turnLog += ` ${行为防反日志}`;
          const fusionAftermathLog = applyFusionActionAftermath(actor, action, actorTurnCombatData);
          if (fusionAftermathLog) turnLog += ` ${fusionAftermathLog}`;
          const 召唤协同日志 = actor.召唤键 ? '' : 执行协同召唤追击(actor, finalTarget, appliedDamage, battleState.combatData);
          if (召唤协同日志) turnLog += ` ${召唤协同日志}`;
          const rootEventLedger = 确保战斗事件账本(battleState.combatData);
          const hasStructuredSettlement = rootEventLedger.some(event => {
            const kind = String(event?.eventKind || '').trim();
            if (!['hit_result', 'state_apply', 'resource_change', 'create', 'summon_create', 'summon_assist', 'shield_create', 'blocked_action', 'failed_action', 'target_fail'].includes(kind)) return false;
            return String(event?.sourceActionId || event?.actionId || '').trim() === String(actionStartEvent?.actionId || '').trim() ||
              String(event?.sourceNodeId || event?.parentNodeId || '').trim() === String(actionStartEvent?.chainNodeId || '').trim();
          });
          if (!hasStructuredSettlement && actionStartEvent?.actionId) {
            const finalActionName = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.action_type || action?.type || '行动');
            const initialActionName = normalizeBattleActionDisplayName(action?.__原定技能名 || action?.__原定动作名 || actionStartEvent.actionName || finalActionName);
            const actionText = `${finalActionName} ${action?.action_type || ''} ${action?.type || ''}`;
            const isStanceFallback = 判定守势待机动作(actionText);
            const fallbackReason = isStanceFallback
              ? `${actor?.name || actor?.名称 || '行动者'}的【${finalActionName || '行动'}】稳住身位`
              : `${actor?.name || actor?.名称 || '行动者'}的【${finalActionName || '行动'}】未形成结算事实，战局链路已截断`;
            写入战斗事件账本(battleState.combatData, {
              eventKind: 'blocked_action',
              round: Number(battleState.combatData?.回合 || 0),
              actorName: actor?.name || actor?.名称 || '',
              targetName: finalTarget?.name || finalTarget?.名称 || '',
              actionName: finalActionName,
              initialActionName,
              finalActionName,
              discardedActionName: initialActionName && initialActionName !== finalActionName ? initialActionName : '',
              actionType: action?.action_type || action?.type || 'auto_actor_no_effect',
              actionId: actionStartEvent.actionId,
              sourceActionId: actionStartEvent.actionId,
              sourceActionName: actionStartEvent.actionName || initialActionName,
              parentNodeId: actionStartEvent.chainNodeId || '',
              sourceNodeId: actionStartEvent.chainNodeId || '',
              result: isStanceFallback ? 'no_effect' : 'interrupted',
              failReason: fallbackReason,
              meta: {
                source: actor.召唤键 ? 'summon' : 'auto_actor',
                reasonCode: isStanceFallback ? 'NO_EFFECTIVE_OPENING' : 'NO_STRUCTURED_SETTLEMENT',
                reasonText: isStanceFallback ? '守势调整' : '敌对动作缺少结构化结算事实',
                failureReason: isStanceFallback ? 'no_effective_opening' : 'no_structured_settlement',
                primaryOutcome: isStanceFallback ? 'no_effect' : 'interrupted',
              },
            });
          }
          清理动作运行态字段(action);

          if (reactionAction.type === '穿戴装备') {
            const actorStateCalc = getPrimaryStateCalc(action.skill);
            const actorInterruptChance = 计算行动打断概率(action.skill, finalTarget, settleResult);
            const isTargetInterrupted =
              appliedDamage / getCombatHpMaxValue(finalTarget) >= 0.15 ||
              actorStateCalc.skip_turn === true ||
              getPrimaryStateFlags(action.skill).includes('硬控') ||
              (actorInterruptChance > 0 && Math.random() <= actorInterruptChance);
            if (!isTargetInterrupted) {
              应用战斗装备变化(finalTarget, () => {
                const 装备槽 = ensureBattleEquipmentSlot(finalTarget, reactionAction.equip_target || reactionAction.skill?.equip_target);
                if (装备槽) 装备槽.装备状态 = '已装备';
              });
              turnLog += ` [装备生效] ${finalTarget.name}成功完成装备穿戴。`;
            } else {
              turnLog += ` [穿戴失败] ${finalTarget.name}的装备穿戴被强行打断。`;
            }
          }

          if (
            getCombatHpRatio(finalTarget) < 0.1 &&
            finalTarget !== actor &&
            !targetsFriendlyTeam
          ) {
            let hasMech = 战斗机甲已装备(finalTarget);
            let hasArmor = finalTarget.装备?.斗铠?.装备状态 === '已装备';
            if (hasMech || hasArmor) {
              设置战斗血量值(finalTarget, Math.floor(getCombatHpMaxValue(finalTarget) * 0.1));
              turnLog += ` [装备护主] ${finalTarget.name}触发装备护主，强制锁血至 10%。${applyArmorDamage(finalTarget)}`;
            }
          }

          const focusUpdate = updateActorFocusFromAction(actor, action, finalTarget, targets.enemyTarget, settleResult);
          if (focusUpdate?.target) {
            broadcastActorFocusToTeam(actorEntry, battleState, focusUpdate.target, focusUpdate.reason, focusUpdate.ttl);
            const focusLabelMap = {
              control_window: '控制追击',
              anti_heal_window: '断疗压杀',
              armor_break_window: '破防追击',
              dot_pressure: '持续压血',
              finisher: '收割',
              shared_vision_focus: '集火',
            };
            turnLog += ` [战术焦点] ${actor.name}将${focusUpdate.target.name}锁为后续${focusLabelMap[focusUpdate.reason] || '追击'}目标。`;
          }
          if (evaluateActionCountered(action, finalTarget, reactionAction, settleResult, skillTargetKind)) {
            recordActorSkillCountered(
              actor,
              action?.skill?.name || action?.action_type || '',
              reactionAction?.type || settleResult?.desc || '',
            );
            turnLog += ` [战术复盘] ${actor.name}意识到[${action?.skill?.name || action?.action_type || '该招式'}]再次被有效克制，短期内会降低其使用倾向。`;
          }

          return {
            actor: actor.name,
            side: actorEntry.side,
            target: finalTarget.name,
            action,
            reactionAction,
            settleResult,
            log: turnLog,
            extraPatchOps: [
              ...turnExtraPatchOps,
              ...(Array.isArray(settleResult?.extraPatchOps) ? settleResult.extraPatchOps : []),
            ],
            actorVit: getCombatHpValue(actor),
            targetVit: getCombatHpValue(finalTarget),
          };
        }

        function getTeamLivingCount(team) {
          return (team || []).filter(unit => {
            bindCombatParticipant(unit);
            syncCombatActionState(unit);
            return isCombatUnitAbleToFight(unit);
          }).length;
        }

        function 记录团战行动轴声明(queue = [], combatData = {}, logs = []) {
          const 声明列表 = (Array.isArray(queue) ? queue : [])
            .filter(entry => entry?.char && isCombatUnitAbleToFight(entry.char))
            .map(entry => {
              const targets = chooseTargetForActor(entry, { combatData });
              const action = buildAutoActionForActor(entry, targets, { combatData });
              刷新队伍行动轴意图(entry, targets, action, { combatData });
              const target = targets?.enemyTarget || targets?.allyTarget || null;
              const 前摇 = Number(action?.cast_time ?? action?.skill?.前摇 ?? 10) || 10;
              const 分桶 = `${Math.floor(Math.min(39, Math.max(0, 前摇)) / 10) * 10}-${Math.floor(Math.min(39, Math.max(0, 前摇)) / 10) * 10 + 9}`;
              写入行动轴初始意图节点(combatData, entry, target, action, 分桶);
              entry.__declaredRound = Number(combatData?.回合 || 0);
              entry.__declaredAction = action;
              entry.__declaredActionName = normalizeBattleActionDisplayName(action?.skill?.name || action?.skill?.魂技名 || action?.action_type || action?.type || '行动');
              entry.__declaredTargetName = String(target?.name || target?.名称 || '').trim();
              entry.__declaredTimingBucket = 分桶;
              return {
                行动者: entry.char,
                目标: target,
                动作: action,
                前摇,
                分桶,
              };
            });
          const 来袭计数 = new Map();
          声明列表.forEach(声明 => {
            const 目标名 = String(声明.目标?.name || 声明.目标?.名称 || '').trim();
            if (!目标名) return;
            const key = `${目标名}::${声明.分桶}`;
            来袭计数.set(key, (来袭计数.get(key) || 0) + 1);
          });
          声明列表.forEach(声明 => {
            if (!声明.目标) return;
            const key = `${声明.目标.name || 声明.目标.名称}::${声明.分桶}`;
            const 数量 = Math.max(1, Number(来袭计数.get(key) || 1));
            if (数量 <= 1) return;
            声明.目标.__本回合防御承压池 = 声明.目标.__本回合防御承压池 || {};
            声明.目标.__本回合防御承压池[声明.分桶] = 数量;
          });
          if (声明列表.length) {
            logs.push(`[行动轴声明] ${声明列表.map(声明 => `${声明.行动者.name || '单位'}->${声明.目标?.name || '无目标'}(${声明.前摇})`).join('；')}`);
          }
          const 队伍意图 = 确保队伍临时意图(combatData);
          const 意图摘要 = ['玩家', '敌方']
            .map(阵营 => {
              const 项 = 队伍意图[阵营] || {};
              const 片段 = [];
              if (项.集火目标) 片段.push(`${阵营}集火:${项.集火目标}x${Number(项.集火计数 || 0)}`);
              if (项.保护目标) 片段.push(`${阵营}保护:${项.保护目标}x${Number(项.保护计数 || 0)}`);
              return 片段.join('，');
            })
            .filter(Boolean);
          if (意图摘要.length) logs.push(`[队伍意图] ${意图摘要.join('；')}`);
          const 多威胁 = Array.from(来袭计数.entries()).filter(([, 数量]) => 数量 > 1);
          if (多威胁.length) {
            logs.push(`[来袭窗口] ${多威胁.map(([key, 数量]) => {
              const [目标名, 分桶] = key.split('::');
              return `${目标名}@${分桶}:${数量}重威胁`;
            }).join('；')}`);
          }
        }

        function settleTeamRoundEnd(combatData, logs) {
          const allUnits = [
            ...读取战斗主队单位列表(combatData, '玩家'),
            ...读取战斗主队单位列表(combatData, '敌方'),
            ...读取召唤单位列表(combatData),
          ];

          allUnits.forEach(unit => {
            bindCombatParticipant(unit);
            syncCombatActionState(unit);
            if (!isCombatUnitAlive(unit)) return;

            const sustainResult = settleSustainEffectsAtRoundEnd(unit, unit.name || '未知单位', combatData);
            const conditionResult = settleConditionsAtRoundEnd(unit, unit.name || '未知单位', combatData);
            syncCombatActionState(unit);
            if (sustainResult.log) logs.push(`[团战回合尾] ${sustainResult.log}`);
            if (conditionResult.log) logs.push(`[团战回合尾] ${conditionResult.log}`);
          });
          const 规则改写回合尾日志 = 递减战斗规则改写运行态(combatData);
          if (规则改写回合尾日志) logs.push(`[团战回合尾] ${规则改写回合尾日志}`);
        }

        function runTeamBattleSimulation(combatData, maxRounds = 3) {
          hydrateCombatData(combatData);
          确保召唤单位表(combatData);
          if (isSoulTowerCombatTypeValue(combatData?.战斗类型 || '')) {
            const rosterCheck = validateSoulTowerCombatRoster(combatData);
            if (!rosterCheck.ok) {
              return {
                rounds: 0,
                roundStart: Number(combatData?.回合 || 0),
                roundEnd: Number(combatData?.回合 || 0),
                winner: 'unfinished',
                playerAlive: getTeamLivingCount(读取战斗主队单位列表(combatData, '玩家')),
                enemyAlive: getTeamLivingCount(读取战斗主队单位列表(combatData, '敌方')),
                logs: [`[魂灵塔资格驳回] ${rosterCheck.message}`],
                extraPatchOps: [],
              };
            }
          }
          let logs = [];
          let extraPatchOps = [];
          let rounds = 0;
          const startingRound = Number(combatData.回合 || 0);

          while (rounds < maxRounds) {
            rounds++;
            const currentRound = startingRound + rounds;
            combatData.回合 = currentRound;
            清理本回合多威胁运行态(combatData);
            logs.push(`[团战第${currentRound}回合开始]`);
            记录战斗上下文时光回溯快照(combatData);
            const 召唤回合开始日志 = 执行召唤回合开始(combatData);
            if (召唤回合开始日志) logs.push(召唤回合开始日志);

            const queue = generateActionQueue(combatData);
            记录团战行动轴声明(queue, combatData, logs);
            for (const actorEntry of queue) {
              const teamPlayerAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '玩家'));
              const teamEnemyAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '敌方'));
              if (teamPlayerAlive <= 0 || teamEnemyAlive <= 0) break;

              const turnResult = runActorTurn(actorEntry, { combatData, round: currentRound, logs });
              if (turnResult?.log) logs.push(turnResult.log);
              if (Array.isArray(turnResult?.extraPatchOps) && turnResult.extraPatchOps.length)
                extraPatchOps.push(...turnResult.extraPatchOps);
            }

            settleTeamRoundEnd(combatData, logs);

            const teamPlayerAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '玩家'));
            const teamEnemyAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '敌方'));
            logs.push(`[团战回合总结] 我方存活:${teamPlayerAlive} 敌方存活:${teamEnemyAlive}`);

            if (teamPlayerAlive <= 0 || teamEnemyAlive <= 0) {
              break;
            }
          }

          const finalPlayerAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '玩家'));
          const finalEnemyAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '敌方'));
          const winner = finalEnemyAlive <= 0 ? 'player' : finalPlayerAlive <= 0 ? 'enemy' : 'unfinished';

          // 如果是团战模拟结束且是虚拟环境死亡，修正战利品结算与强制锁血弹出
          const combatType = combatData.战斗类型 || '突发遭遇';
          if (combatType === '升灵台虚拟战斗' || combatType === '魂灵塔冲塔') {
            if (winner === 'enemy') {
              读取战斗主队单位列表(combatData, '玩家').forEach(p => {
                if (getCombatHpValue(p) <= 0) 设置战斗血量值(p, 1);
              });
              logs.push(`[虚拟战败保护] 玩家方全员战败，触发安全协议，强制弹出并锁定HP为 1！`);
            }
          }

          return {
            rounds,
            roundStart: startingRound + 1,
            roundEnd: Number(combatData.回合 || startingRound),
            winner,
            playerAlive: finalPlayerAlive,
            enemyAlive: finalEnemyAlive,
            logs,
            extraPatchOps,
          };
        }

        function runTeamBattleRound(combatData) {
          hydrateCombatData(combatData);
          确保召唤单位表(combatData);
          if (isSoulTowerCombatTypeValue(combatData?.战斗类型 || '')) {
            const rosterCheck = validateSoulTowerCombatRoster(combatData);
            if (!rosterCheck.ok) {
              return {
                rounds: 0,
                roundStart: Number(combatData?.回合 || 0),
                roundEnd: Number(combatData?.回合 || 0),
                winner: 'unfinished',
                playerAlive: getTeamLivingCount(读取战斗主队单位列表(combatData, '玩家')),
                enemyAlive: getTeamLivingCount(读取战斗主队单位列表(combatData, '敌方')),
                logs: [`[魂灵塔资格驳回] ${rosterCheck.message}`],
                extraPatchOps: [],
              };
            }
          }
          const currentRound = Number(combatData.回合 || 0) + 1;
          combatData.回合 = currentRound;
          清理本回合多威胁运行态(combatData);
          let logs = [`[团战第${currentRound}回合开始]`];
          let extraPatchOps = [];
          记录战斗上下文时光回溯快照(combatData);
          const 召唤回合开始日志 = 执行召唤回合开始(combatData);
          if (召唤回合开始日志) logs.push(召唤回合开始日志);

          const queue = generateActionQueue(combatData);
          记录团战行动轴声明(queue, combatData, logs);
          for (const actorEntry of queue) {
            const teamPlayerAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '玩家'));
            const teamEnemyAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '敌方'));
            if (teamPlayerAlive <= 0 || teamEnemyAlive <= 0) break;

            const turnResult = runActorTurn(actorEntry, { combatData, round: currentRound, logs });
            if (turnResult?.log) logs.push(turnResult.log);
            if (Array.isArray(turnResult?.extraPatchOps) && turnResult.extraPatchOps.length)
              extraPatchOps.push(...turnResult.extraPatchOps);
          }

          settleTeamRoundEnd(combatData, logs);

          const teamPlayerAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '玩家'));
          const teamEnemyAlive = getTeamLivingCount(读取战斗主队单位列表(combatData, '敌方'));
          const winner = teamEnemyAlive <= 0 ? 'player' : teamPlayerAlive <= 0 ? 'enemy' : 'unfinished';
          logs.push(`[团战回合总结] 我方存活:${teamPlayerAlive} 敌方存活:${teamEnemyAlive}`);

          return {
            rounds: 1,
            roundStart: currentRound,
            roundEnd: currentRound,
            winner,
            playerAlive: teamPlayerAlive,
            enemyAlive: teamEnemyAlive,
            logs,
            extraPatchOps,
          };
        }

        function ui_executeBattleFlow(combatData, options = {}) {
          if (!combatData) {
            return {
              mode: options.mode || 'single_round',
              roundsExecuted: 0,
              winner: 'unfinished',
              logs: ['[UI执行] 未提供 combatData。'],
              snapshot: null,
            };
          }

          const mode = options.mode === 'multi_round' ? 'multi_round' : 'single_round';
          const rounds = Math.max(1, Number(options.rounds || 1));
          const result =
            mode === 'multi_round' ? runTeamBattleSimulation(combatData, rounds) : runTeamBattleRound(combatData);
          const extraPatchOps = Array.isArray(result.extraPatchOps) ? result.extraPatchOps : [];
          const mvuUpdate =
            window.BattleUIBridge?.persistCombatData?.(combatData, {
              analysis:
                'Frontend team battle arbitration already produced the exact combat result. Apply the following JSONPatch exactly as given.',
              extraPatchOps,
              syncHpRecoveryOnly: true,
            }) || null;

          return {
            mode,
            roundsRequested: mode === 'multi_round' ? rounds : 1,
            roundsExecuted: result.rounds || 0,
            roundStart: result.roundStart,
            roundEnd: result.roundEnd,
            winner: result.winner || 'unfinished',
            playerAlive: result.playerAlive,
            enemyAlive: result.enemyAlive,
            extraPatchOps,
            logs: result.logs || [],
            snapshot: ui_getBattleSnapshot(combatData),
            mvuUpdate,
          };
        }

        // ==========================================
        // 📍 UI 适配器层 (对外暴露接口)
        // ==========================================

        function ui_getBattleSnapshot(combatData) {
          if (!combatData) return null;
          hydrateCombatData(combatData);
          确保召唤单位表(combatData);

          const buildUnitSnapshot = char => {
            if (!char) return null;
            syncCombatActionState(char);
            return {
              name: char.name || '未知',
              lv: char.lv || 1,
              lv_label: formatBattleCultivationLevelText(char.lv || 1, '1'),
              type: char.type || '未知系',
              hp: getCombatHpValue(char),
              hp_max: getCombatHpMaxValue(char),
              HP: getCombatHpValue(char),
              HP上限: getCombatHpMaxValue(char),
              vit: getCombatStaminaValue(char),
              vit_max: getCombatStaminaMaxValue(char),
              sta: getCombatStaminaValue(char),
              sta_max: getCombatStaminaMaxValue(char),
              体力: getCombatStaminaValue(char),
              体力上限: getCombatStaminaMaxValue(char),
              sp: char.sp || 0,
              sp_max: char.sp_max || 1,
              魂力: char.sp || 0,
              魂力上限: char.sp_max || 1,
              men: char.men || 0,
              men_max: char.men_max || 1,
              精神力: char.men || 0,
              精神力上限: char.men_max || 1,
              召唤键: char.召唤键 || '',
              单位性质: char.单位性质 || '',
              类型: char.类型 || char.type || '',
              年限: char.年限 || 0,
              标准物种: char.标准物种 || '',
              具体物种: char.具体物种 || '',
              行动模式: char.行动模式 || '',
              宿主名: char.宿主名 || '',
              精神负载: char.精神负载 || 0,
              稳定状态: char.召唤键 ? 读取召唤稳定状态(char) : '',
              当前领域: char.当前领域 || '无',
              状态效果: Object.entries(char.状态效果 || {}).map(([name, cond]) => ({
                name,
                type: cond.类型 || 'buff',
                duration: cond.duration || 0,
                desc: cond.描述 || '',
                skip_turn: cond.战斗效果?.skip_turn || false,
                dot: cond.战斗效果?.dot_damage || 0,
              })),
              sustains: Object.keys(char.持续效果 || {}),
              isCharging: !!char.蓄力技能,
              chargingCastTime: char.蓄力技能?.cast_time || 0,
            };
          };

          return {
            round: Number(combatData.回合 || 0),
            战斗类型: combatData.战斗类型 || '突发遭遇',
            floor: Number(combatData.floor || 0),
            大关卡: Number(combatData.大关卡 || 0),
            大关标签: combatData.大关标签 || '',
            先攻: combatData.先攻 || '无',
            team_player: (combatData.参战者.team_player || []).map(buildUnitSnapshot).filter(Boolean),
            team_enemy: (combatData.参战者.team_enemy || []).map(buildUnitSnapshot).filter(Boolean),
            summons: 读取召唤单位列表(combatData).filter(单位 => !召唤单位是分身(单位)).map(buildUnitSnapshot).filter(Boolean),
          };
        }

        function 获取同队可行动单位(战斗数据, 角色数据) {
          const 玩家队伍 = (Array.isArray(战斗数据?.参战者?.team_player) ? 战斗数据.参战者.team_player : []).filter(Boolean);
          const 敌方队伍 = (Array.isArray(战斗数据?.参战者?.team_enemy) ? 战斗数据.参战者.team_enemy : []).filter(Boolean);
          const 角色标识 = String(角色数据?.name || 角色数据?.名称 || 角色数据?.charKey || 角色数据?.char_key || 角色数据?.key || '').trim();
          const 角色在玩家队 = 玩家队伍.some(unit => isCombatUnitIdentityMatch(unit, 角色标识));
          const 角色在敌方队 = 敌方队伍.some(unit => isCombatUnitIdentityMatch(unit, 角色标识));
          const 所在队伍 = 角色在玩家队 ? 玩家队伍 : 角色在敌方队 ? 敌方队伍 : 玩家队伍;
          const 已见 = new Set();
          return 所在队伍.filter(unit => {
            if (!unit || (角色标识 && isCombatUnitIdentityMatch(unit, 角色标识))) return false;
            const 单位标识 = String(unit?.name || unit?.名称 || unit?.charKey || unit?.char_key || unit?.key || '').trim();
            if (单位标识 && 已见.has(单位标识)) return false;
            if (单位标识) 已见.add(单位标识);
            return isCombatUnitAbleToFight(unit);
          });
        }

        function ui_getAvailableActions(charData, combatData) {
          if (!charData) return [];
          bindCombatParticipant(charData);
          确保召唤单位表(combatData || {});
          const 当前tick = Number(window.BattleUIBridge?.getMVU('world.时间.tick') || 0);
          const 当前角色名 = String(charData?.name || charData?.名称 || '').trim();
          const allyTeam = 获取同队可行动单位(combatData, charData);
          const 读取UI单位标识 = 单位 =>
            String(单位?.name || 单位?.名称 || 单位?.charKey || 单位?.char_key || 单位?.key || '').trim();
          const UI单位相同 = (左, 右) => {
            if (!左 || !右) return false;
            const 左标识 = 读取UI单位标识(左);
            const 右标识 = 读取UI单位标识(右);
            return 左 === 右 || (!!左标识 && !!右标识 && 左标识 === 右标识);
          };
          const 去重UI存活队伍 = 队伍 => {
            const 已见 = new Set();
            return (Array.isArray(队伍) ? 队伍 : [])
              .filter(Boolean)
              .filter(单位 => {
                const 标识 = 读取UI单位标识(单位);
                if (标识 && 已见.has(标识)) return false;
                if (标识) 已见.add(标识);
                return isCombatUnitAlive(单位);
              });
          };
          const 读取UI辅助消耗上下文 = skill => {
            if (!skill || !isSupportLikeSkill(skill)) return { 目标: null, 目标数量: 1, 目标列表: [], 批量模式: '' };
            const 目标文本 = String(getSkillTarget(skill) || '').trim();
            if (!目标文本 || /自身/.test(目标文本)) return { 目标: charData, 目标数量: 1, 目标列表: [charData], 批量模式: '' };
            const 玩家队伍 = 读取战斗主队单位列表(combatData, '玩家');
            const 敌方队伍 = 读取战斗主队单位列表(combatData, '敌方');
            const 在玩家队 = 玩家队伍.some(单位 => UI单位相同(单位, charData));
            const 友方队伍 = 去重UI存活队伍([charData, ...(在玩家队 ? 玩家队伍 : 敌方队伍), ...allyTeam]);
            const 对方队伍 = 去重UI存活队伍(在玩家队 ? 敌方队伍 : 玩家队伍);
            const 取最高魂力目标 = 队伍 =>
              [...队伍].sort((左, 右) => Number(右?.sp_max || 右?.属性?.魂力上限 || 0) - Number(左?.sp_max || 左?.属性?.魂力上限 || 0))[0] ||
              null;
            if (/己方|友方/.test(目标文本)) {
              if (/群体/.test(目标文本) && !是否七九辅助单体语义(charData, skill)) {
                return { 目标: 取最高魂力目标(友方队伍) || charData, 目标数量: Math.max(1, 友方队伍.length), 目标列表: 友方队伍, 批量模式: '原生群体' };
              }
              return { 目标: 取最高魂力目标(友方队伍) || charData, 目标数量: 1, 目标列表: [], 批量模式: '' };
            }
            if (/敌/.test(目标文本)) {
              if (/群体/.test(目标文本)) return { 目标: 取最高魂力目标(对方队伍), 目标数量: Math.max(1, 对方队伍.length), 目标列表: 对方队伍, 批量模式: '原生群体' };
              return { 目标: combatData?.参战者?.team_enemy?.[0] || 取最高魂力目标(对方队伍), 目标数量: 1, 目标列表: [], 批量模式: '' };
            }
            return { 目标: charData, 目标数量: 1, 目标列表: [charData], 批量模式: '' };
          };
          const 读取UI经验目标 = skill => {
            const 战斗目标类型 = inferSkillPrimaryTargetKind(skill);
            if (['自身', '友方单体', '友方群体'].includes(战斗目标类型)) return charData;
            return combatData?.参战者?.team_enemy?.[0] || null;
          };
          const availableSkills = collectUnifiedSkillEntries(charData, allyTeam, {
            includePassive: false,
            includeActive: true,
            includeUnavailableFusion: true,
          });

          const actions = [];
          actions.push(
            {
              id: 'basic_attack',
              type: 'tactical',
              action_type: '常规攻击',
              name: '普通攻击',
              category: '战术',
              cast_time: 10,
              cost_text: '无',
              enabled: true,
              reason: '',
              raw_skill: normalizeSkillData(
                {
                  name: '普通攻击',
                  技能分类: '输出',
                  消耗: '无',
                  前摇: 10,
                  _效果数组: [
                    { 原型: '伤害结算', 目标: '单体', 威力倍率: 50, 伤害类型: '近身攻击', 防御穿透: 0 },
                  ],
                },
                '普通攻击',
              ),
            },
            {
              id: 'guard',
              type: 'tactical',
              action_type: '防御',
              name: '防御',
              category: '战术',
              cast_time: 10,
              cost_text: '无',
              enabled: true,
              reason: '',
              raw_skill: normalizeSkillData({ name: '防御', 技能分类: '防御', 消耗: '无', 前摇: 10 }, '防御'),
            },
            {
              id: 'evade',
              type: 'tactical',
              action_type: '闪避',
              name: '闪避',
              category: '战术',
              cast_time: 12,
              cost_text: '体力:5%',
              enabled: parseSkillCostForChar(normalizeSkillData({ name: '闪避', 技能分类: '防御', 消耗: '体力:5%', 前摇: 12 }, '闪避'), charData).canCast,
              reason: '',
              raw_skill: normalizeSkillData({ name: '闪避', 技能分类: '防御', 消耗: '体力:5%', 前摇: 12 }, '闪避'),
            },
          );

          availableSkills.forEach(skill => {
            let costParsed = null;
            try {
              if (skill && isSupportLikeSkill(skill)) {
                const 辅助消耗上下文 = 读取UI辅助消耗上下文(skill);
                if (辅助消耗上下文.目标) skill.__targetForSupportCost = 辅助消耗上下文.目标;
                skill.__辅助目标数量 = Math.max(1, Math.floor(Number(辅助消耗上下文.目标数量 || 1)));
                if (Array.isArray(辅助消耗上下文.目标列表) && 辅助消耗上下文.目标列表.length > 0) {
                  skill.__批量目标列表 = 辅助消耗上下文.目标列表;
                  skill.__批量消耗模式 = 辅助消耗上下文.批量模式 || '';
                }
              }
              const 预览目标 = 读取UI经验目标(skill);
              const 来源类别 = getBattleSkillSourceCategory(skill);
              costParsed = parseSkillCostForChar(skill, charData, {
                actor: charData,
                caster: charData,
                attacker: charData,
                target: 预览目标,
                defender: 预览目标,
                action: { action_type: 来源类别 === '武魂融合技' ? '武魂融合技' : '释放魂技', type: '技能', skill, category: 来源类别 },
                skill,
                combatData,
                当前行动: 来源类别 === '武魂融合技' ? '武魂融合技' : '释放魂技',
              });
            } finally {
              if (skill && isSupportLikeSkill(skill)) {
                delete skill.__targetForSupportCost;
                delete skill.__辅助目标数量;
                delete skill.__批量目标列表;
                delete skill.__批量消耗模式;
              }
            }
            const 来源上下文 = 读取战斗来源类别上下文(skill, '魂技');
            const 来源类别 = 来源上下文.来源类别;
            const 百分比启动检查 = 来源类别 === '武魂融合技'
              ? { 可用: true, 原因: '' }
              : 检查普通魂技百分比启动消耗(skill, charData, {
                  action_type: '释放魂技',
                  category: 来源类别,
                  source_detail: 来源上下文.来源明细,
                });
            const 来源键 = [
              来源类别,
              ...(Array.isArray(来源上下文.魂环路径) ? 来源上下文.魂环路径 : []),
              来源上下文.魂技槽位,
              skill.name,
            ].map(片段 => String(片段 || '').replace(/[^\w\u4e00-\u9fa5-]+/g, '_')).filter(Boolean).join('_');
            const 技能动作 = {
              id: `skill_${来源键 || skill.name}`,
              type: 'skill',
              action_type: 来源类别 === '武魂融合技' ? '武魂融合技' : '释放魂技',
              name: skill.name,
              category: 来源类别,
              source_detail: 来源上下文.来源明细,
              semantic_role: getSkillType(skill) || '输出',
              tags: 读取技能原型标签(skill),
              cast_time: getSkillCastTime(skill),
              cost_text: getFusionSkillDisplayCostText(skill),
              enabled: 百分比启动检查.可用 && costParsed.canCast,
              reason: !百分比启动检查.可用 ? 百分比启动检查.原因 : costParsed.canCast ? '' : (costParsed.failureReason || '状态不足'),
              raw_skill: skill,
            };
            actions.push(技能动作);
          });

          Object.entries(charData?.背包 && typeof charData.背包 === 'object' ? charData.背包 : {}).forEach(([物品名, 背包状态]) => {
            if (!物品名 || !背包状态 || typeof 背包状态 !== 'object') return;
            const 物品数据 = 读取战斗背包物品数据(物品名, 背包状态);
            if (判断战斗可装配魂导器(物品名, 物品数据)) return;
            if (!Array.isArray(物品数据?.使用效果) || !物品数据.使用效果.length) return;
            const 校验结果 = 校验战斗背包物品可用(物品名, 物品数据, 当前角色名, 当前tick);
            const 物品技能 = 构建战斗背包技能(物品名, 物品数据);
            const 技能动作 = {
              id: `item_${物品名}`,
              type: 'item',
              action_type: '使用物品',
              name: 物品名,
              category: '物品',
              source_detail: '背包',
              semantic_role: getSkillType(物品技能) || '辅助',
              tags: ['物品', ...(读取技能原型标签(物品技能) || [])],
              cast_time: getSkillCastTime(物品技能),
              cost_text: `数量:${Math.max(0, Number(物品数据?.数量 || 0))}`,
              enabled: 校验结果.可用,
              reason: 校验结果.可用 ? '' : 校验结果.原因,
              raw_skill: 物品技能,
              物品名,
            };
            actions.push(技能动作);
          });

          if (charData.装备?.斗铠?.等级 > 0 && charData.装备?.斗铠?.装备状态 !== '已装备') {
            const 斗铠等级 = Number(charData.装备?.斗铠?.等级 || 1);
            const 已排异 = charData.装备?.斗铠?._已排异 || false;
            let 最低品质 = Infinity;
            let 部件数量 = 0;
            Object.values(charData.装备?.斗铠?.parts || {}).forEach(部件 => {
              if (部件?.状态 !== '未打造' && 部件?.状态 !== '重创') {
                if (Number(部件?.品质系数 || 0) < 最低品质) 最低品质 = Number(部件?.品质系数 || 0);
                部件数量++;
              }
            });
            let 斗铠前摇 = Math.max(0, 20 - 斗铠等级 * 5);
            if (斗铠等级 === 1 && !已排异 && 最低品质 > 1.2 && 部件数量 > 0) 斗铠前摇 = Math.max(0, 斗铠前摇 - 5);
            const 斗铠动作 = {
              id: 'equip_armor',
              type: 'equip',
              action_type: '穿戴装备',
              name: '斗铠附体',
              category: '特殊动作',
              cast_time: 斗铠前摇,
              cost_text: '无',
              enabled: true,
              reason: '',
              equip_target: 'armor',
              raw_skill: normalizeSkillData({
                name: 斗铠前摇 <= 0 ? '斗铠瞬间附体' : '斗铠附体读条',
                技能分类: '辅助',
                消耗: '无',
                前摇: 斗铠前摇,
              }),
            };
            actions.push(斗铠动作);
          }

          if (
            charData.装备?.机甲?.等级 &&
            charData.装备.机甲.等级 !== '无' &&
            charData.装备?.机甲?.装备状态 !== '已装备' &&
            charData.装备?.机甲?.状态 !== '重创'
          ) {
            const 机甲前摇 = charData.装备?.机甲?.等级 === '红级' ? 0 : 50;
            const 机甲动作 = {
              id: 'equip_mech',
              type: 'equip',
              action_type: '穿戴装备',
              name: '召唤机甲',
              category: '特殊动作',
              cast_time: 机甲前摇,
              cost_text: '无',
              enabled: true,
              reason: '',
              equip_target: 'mech',
              raw_skill: normalizeSkillData({ name: '召唤机甲', 技能分类: '辅助', 消耗: '无', 前摇: 机甲前摇 }),
            };
            actions.push(机甲动作);
          }

          if (charData.血脉之力?.技能?.['点燃生命之火'] && !charData.血脉之力?.生命之火) {
            const 生命之火技能 = normalizeSkillData(charData.血脉之力?.技能?.['点燃生命之火'], '点燃生命之火');
            const 生命之火消耗 = parseSkillCostForChar(生命之火技能, charData);
            const 生命之火动作 = {
              id: 'special_lifefire',
              type: 'special',
              action_type: '点燃生命之火',
              name: '点燃生命之火',
              category: '特殊动作',
              cast_time: 5,
              cost_text: getSkillCostText(生命之火技能),
              enabled: 生命之火消耗.canCast,
              reason: 生命之火消耗.canCast ? '' : (生命之火消耗.failureReason || '状态不足'),
              raw_skill: 生命之火技能,
            };
            actions.push(生命之火动作);
          }

          if (hasBattleUnlockedAttributeSet(charData, ['金', '木', '水', '火', '土'])) {
            const wuxingStripSkill = normalizeSkillData({
              name: '五行剥离',
              技能分类: '控制',
              消耗: '魂力:18% 精神力:25%',
              cast_time: 18,
            });
            const wuxingEscapeSkill = normalizeSkillData({
              name: '五行遁法',
              技能分类: '辅助',
              消耗: '魂力:12% 精神力:18% 维持:精神力:8%',
              cast_time: 12,
            });
            const stripCostParsed = parseSkillCostForChar(wuxingStripSkill, charData);
            const escapeCostParsed = parseSkillCostForChar(wuxingEscapeSkill, charData);
            actions.push({
              id: 'special_wuxing_strip',
              type: 'special',
              action_type: '五行剥离',
              name: '五行剥离',
              category: '纯操控',
              semantic_role: '控制',
              cast_time: getSkillCastTime(wuxingStripSkill),
              cost_text: getSkillCostText(wuxingStripSkill),
              enabled: stripCostParsed.canCast,
              reason: stripCostParsed.canCast ? '' : '状态不足',
              raw_skill: wuxingStripSkill,
            });
            actions.push({
              id: 'special_wuxing_escape',
              type: 'special',
              action_type: '五行遁法',
              name: '五行遁法',
              category: '纯操控',
              semantic_role: '辅助',
              cast_time: getSkillCastTime(wuxingEscapeSkill),
              cost_text: getSkillCostText(wuxingEscapeSkill),
              enabled: escapeCostParsed.canCast,
              reason: escapeCostParsed.canCast ? '' : '状态不足',
              raw_skill: wuxingEscapeSkill,
            });
          }

          const 可收回召唤 = 读取召唤单位列表(combatData, { 宿主: charData })
            .filter(单位 => isCombatUnitAlive(单位) && !召唤单位是分身(单位));
          if (可收回召唤.length > 1) {
            actions.push({
              id: 'summon_recall_all',
              type: 'summon',
              action_type: '收回召唤',
              name: '收回全部',
              category: '召唤',
              cast_time: 8,
              cost_text: '无',
              enabled: true,
              reason: '',
              raw_skill: normalizeSkillData({ name: '收回召唤', 技能分类: '辅助', 消耗: '无', 前摇: 8 }, '收回召唤'),
            });
          }
          可收回召唤.forEach(单位 => {
            actions.push({
              id: `summon_recall_${单位.召唤键 || 单位.name || actions.length}`,
              type: 'summon',
              action_type: '收回召唤',
              name: `收回·${单位.name || 单位.名称 || '召唤物'}`,
              category: '召唤',
              cast_time: 8,
              cost_text: '无',
              enabled: true,
              reason: '',
              target_name: 单位.召唤键 || 单位.name || 单位.名称 || '',
              raw_skill: normalizeSkillData({ name: '收回召唤', 技能分类: '辅助', 消耗: '无', 前摇: 8 }, '收回召唤'),
            });
          });

          actions.push({
            id: 'action_flee',
            type: 'tactical',
            action_type: '撤离',
            name: '亡命奔逃',
            category: '特殊动作',
            cast_time: 20,
            cost_text: '无',
            enabled: true,
            reason: '',
            raw_skill: normalizeSkillData({ name: '撤离', 技能分类: '辅助', 消耗: '无', 前摇: 20 }, '撤离'),
          });

          actions.forEach(动作 => 套用动作实际前摇(charData, 动作, 读取UI经验目标(动作.raw_skill || 动作.skill || {}), combatData));
          if (当前角色名) actions.forEach(action => {
            if (action && typeof action === 'object' && !String(action.actor_name || action.__行动者名 || '').trim()) {
              action.actor_name = 当前角色名;
            }
          });
          return actions;
        }

        function toUiNumber(value, fallback = 0) {
          const parsed = Number(value);
          return Number.isFinite(parsed) ? parsed : fallback;
        }

        function htmlEscapeText(value) {
          return String(value ?? '')
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#39;');
        }

        function 显示战斗提示(消息, 类型 = 'error') {
          const 文本 = String(消息 || '').trim();
          if (!文本) return;
          const 窗口列表 = [];
          try { 窗口列表.push(window); } catch (错误) {}
          try {
            if (window.parent && window.parent !== window) 窗口列表.push(window.parent);
          } catch (错误) {}
          for (const 目标窗口 of 窗口列表) {
            try {
              if (目标窗口.MVU_Toast && typeof 目标窗口.MVU_Toast.show === 'function') {
                目标窗口.MVU_Toast.show(文本, 类型);
                return;
              }
              const 提示器 = 目标窗口.toastr;
              if (提示器 && typeof 提示器[类型] === 'function') {
                提示器[类型](文本);
                return;
              }
            } catch (错误) {}
          }
          if (类型 === 'error') console.warn(文本);
          else console.info(文本);
        }

        function flattenUiCombatant(unit) {
          const safeUnit = unit && typeof unit === 'object' ? unit : {};
          const stat = safeUnit.属性 && typeof safeUnit.属性 === 'object' ? safeUnit.属性 : {};
          const status = safeUnit.状态 && typeof safeUnit.状态 === 'object' ? safeUnit.状态 : {};
          const merged = { ...stat, ...safeUnit };
          COMBAT_STAT_KEYS.forEach(key => {
            if (stat[key] !== undefined) merged[key] = stat[key];
          });
          merged.name = safeUnit.name || stat.name || safeUnit.base?.name || '未知';
          merged.lv = toUiNumber(merged.lv ?? merged.等级, toUiNumber(stat.等级, 0));
          merged.type = safeUnit.type || safeUnit.系别 || merged.type || merged.系别 || '未知系';
          merged.hp_max = Math.max(
            1,
            toUiNumber(merged.hp_max ?? merged.HP上限, toUiNumber(stat.HP上限, 1)),
          );
          merged.hp = Math.max(
            0,
            toUiNumber(merged.hp ?? merged.HP, toUiNumber(stat.HP, merged.hp_max)),
          );
          merged.vit_max = Math.max(1, toUiNumber(merged.vit_max ?? merged.体力上限, toUiNumber(stat.体力上限, 1)));
          merged.vit = Math.max(0, toUiNumber(merged.vit ?? merged.体力, toUiNumber(stat.体力, merged.vit_max)));
          merged.sta_max = Math.max(1, toUiNumber(merged.sta_max ?? merged.体力上限, toUiNumber(stat.体力上限, 1)));
          merged.sta = Math.max(0, toUiNumber(merged.sta ?? merged.体力, toUiNumber(stat.体力, merged.sta_max)));
          merged.sp_max = Math.max(1, toUiNumber(merged.sp_max ?? merged.魂力上限, toUiNumber(stat.魂力上限, 1)));
          merged.sp = Math.max(0, toUiNumber(merged.sp ?? merged.魂力, toUiNumber(stat.魂力, merged.sp_max)));
          merged.men_max = Math.max(
            1,
            toUiNumber(merged.men_max ?? merged.精神力上限, toUiNumber(stat.精神力上限, 1)),
          );
          merged.men = Math.max(0, toUiNumber(merged.men ?? merged.精神力, toUiNumber(stat.精神力, merged.men_max)));
          merged.str = toUiNumber(merged.str ?? merged.力量, toUiNumber(stat.力量, 0));
          merged.def = toUiNumber(merged.def ?? merged.防御, toUiNumber(stat.防御, 0));
          merged.agi = toUiNumber(merged.agi ?? merged.敏捷, toUiNumber(stat.敏捷, 0));
          merged.状态效果 = merged.状态效果 || stat.状态效果 || {};
          merged.持续效果 = merged.持续效果 || {};
          merged.isSummon = !!safeUnit.召唤键;
          merged.召唤键 = safeUnit.召唤键 || '';
          merged.行动模式 = safeUnit.行动模式 || '';
          merged.宿主名 = safeUnit.宿主名 || '';
          merged.稳定状态 = safeUnit.稳定状态 || (safeUnit.__禁用召唤技能 ? '受限' : safeUnit.__精神压缩 < 1 ? '超载' : '稳定');
          merged.alive = status.存活 !== false && safeUnit.alive !== false && merged.hp > 0;
          return merged;
        }

        function 获取可见装备标签(unit = {}) {
          const 装备 = unit?.装备 && typeof unit.装备 === 'object' ? unit.装备 : {};
          const 标签 = [];
          const 机甲 = 装备.机甲 && typeof 装备.机甲 === 'object' ? 装备.机甲 : null;
          const 斗铠 = 装备.斗铠 && typeof 装备.斗铠 === 'object' ? 装备.斗铠 : null;
          const 防具 = 装备.防具 && typeof 装备.防具 === 'object' ? 装备.防具 : null;
          if (机甲 && String(机甲.装备状态 || '').trim() === '已装备' && String(机甲.等级 || '无').trim() !== '无' && String(机甲.状态 || '') !== '重创') {
            标签.push(`${String(机甲.等级 || '').trim()}机甲`);
          }
          if (斗铠 && String(斗铠.装备状态 || '').trim() === '已装备' && Number(斗铠.等级 || 0) > 0) {
            标签.push(`${Number(斗铠.等级 || 0)}字斗铠`);
          }
          if (防具 && String(防具.装备状态 || '').trim() === '已装备') {
            const 防具名 = String(防具.名称 || 防具.name || '防具').trim();
            if (防具名 && 防具名 !== '无') 标签.push(防具名);
          }
          return 标签;
        }

        function setUiText(id, value) {
          const node = byId(id);
          if (node) node.textContent = String(value ?? '');
        }

        function 设置界面文本显隐(id, value, 是否显示 = true) {
          const 节点 = byId(id);
          if (!节点) return;
          节点.hidden = !是否显示;
          节点.textContent = 是否显示 ? String(value ?? '') : '';
        }

        function setUiBar(id, value, max) {
          const node = byId(id);
          if (!node) return;
          const ratio = Math.max(0, Math.min(100, (toUiNumber(value, 0) / Math.max(1, toUiNumber(max, 1))) * 100));
          node.style.width = `${ratio}%`;
          node.classList.toggle('is-critical', ratio <= 30 && /hp/.test(node.className));
        }

        function 读取战斗资源比例(单位 = {}, 当前键 = '', 上限键 = '') {
          const 当前值 = Math.max(0, toUiNumber(单位?.[当前键], 0));
          const 上限值 = Math.max(1, toUiNumber(单位?.[上限键], 1));
          return Math.max(0, Math.min(1, 当前值 / 上限值));
        }

        function 推入战斗资源警报(警报列表, 单位, 配置) {
          const 单位名 = String(单位?.name || '').trim();
          if (!单位名) return;
          const 比例 = 读取战斗资源比例(单位, 配置.当前键, 配置.上限键);
          if (比例 > 配置.阈值) return;
          const 百分比 = Math.max(0, Math.min(100, Math.floor(比例 * 100)));
          警报列表.push({
            id: `${配置.名称}:${单位名}`,
            类型: 配置.类型,
            优先级: 配置.优先级,
            签名: `${配置.名称}:${单位名}`,
            百分比,
            文本: `${配置.前缀} ${单位名} ${配置.短名} ${百分比}%`,
          });
        }

        function 读取敌方高危状态列表(单位 = {}) {
          const 状态效果 = 单位?.状态效果 && typeof 单位.状态效果 === 'object' ? 单位.状态效果 : {};
          const 状态名列表 = Object.entries(状态效果)
            .filter(([名称, 状态]) => {
              const 文本 = `${名称} ${状态?.类型 || ''} ${状态?.状态名称 || ''} ${状态?.描述 || ''} ${状态?.战斗效果?.标签 || ''}`;
              return /狂暴|蓄力|爆发|高危|危险|处决|必杀/.test(文本);
            })
            .map(([名称]) => String(名称 || '').trim())
            .filter(Boolean);
          if (单位?.蓄力技能 || Number(单位?.蓄力剩余 ?? 单位?.cast_time_left ?? 0) > 0) 状态名列表.unshift('蓄力');
          return Array.from(new Set(状态名列表)).slice(0, 2);
        }

        function 计算战斗智能警报(combatData, 上次警报列表 = []) {
          const 上次警报表 = new Map((Array.isArray(上次警报列表) ? 上次警报列表 : []).map(警报 => [警报.id, 警报]));
          const 当前时间 = Date.now();
          const 警报列表 = [];
          const 我方单位列表 = [
            ...(combatData?.参战者?.team_player || []),
            ...读取召唤单位列表(combatData, { 阵营: '玩家' }),
          ].map(flattenUiCombatant);
          我方单位列表.forEach(单位 => {
            推入战斗资源警报(警报列表, 单位, { 名称: '生命危险', 短名: '生命', 当前键: 'hp', 上限键: 'hp_max', 阈值: 0.3, 类型: 'danger', 优先级: 10, 前缀: '[战术警报]' });
            推入战斗资源警报(警报列表, 单位, { 名称: '魂力警戒', 短名: '魂力', 当前键: 'sp', 上限键: 'sp_max', 阈值: 0.3, 类型: 'warning', 优先级: 7, 前缀: '[资源警戒]' });
            推入战斗资源警报(警报列表, 单位, { 名称: '精神警戒', 短名: '精神', 当前键: 'men', 上限键: 'men_max', 阈值: 0.25, 类型: 'warning', 优先级: 6, 前缀: '[资源警戒]' });
            推入战斗资源警报(警报列表, 单位, { 名称: '体力警戒', 短名: '体力', 当前键: 'sta', 上限键: 'sta_max', 阈值: 0.2, 类型: 'warning', 优先级: 5, 前缀: '[资源警戒]' });
          });

          const 目标敌方名称 = String(window.BattleUI?.state?.selectedAction?.target_name || '').trim();
          const 当前敌方 = (combatData?.参战者?.team_enemy || []).map(flattenUiCombatant).find(单位 => !目标敌方名称 || 单位.name === 目标敌方名称);
          读取敌方高危状态列表(当前敌方 || {}).forEach(状态名 => {
            const 单位名 = String(当前敌方?.name || '敌方').trim();
            警报列表.push({
              id: `敌方高危:${单位名}:${状态名}`,
              类型: 'danger',
              优先级: 8,
              签名: `敌方高危:${单位名}:${状态名}`,
              文本: `[高危目标] ${单位名} ${状态名}`,
            });
          });

          return 警报列表
            .sort((左, 右) => 右.优先级 - 左.优先级)
            .map(警报 => {
              const 上次 = 上次警报表.get(警报.id);
              const 低值恶化 = Number.isFinite(Number(警报.百分比)) && Number(警报.百分比) < Number(上次?.百分比 ?? 警报.百分比);
              const 延续旧警报 = 上次 && 上次.签名 === 警报.签名 && !低值恶化;
              const 首次时间 = 延续旧警报 ? Number(上次.首次时间 || 当前时间) : 当前时间;
              return {
                ...警报,
                首次时间,
                已确认: 当前时间 - 首次时间 >= 3000,
              };
            });
        }

        function 渲染战斗智能警报(警报列表 = []) {
          const node = byId('ui-smart-alert-layer');
          if (!node) return;
          const 状态 = window.BattleUI?.state || {};
          if (状态.智能警报弱化计时器) {
            clearTimeout(状态.智能警报弱化计时器);
            状态.智能警报弱化计时器 = null;
          }
          if (!Array.isArray(警报列表) || !警报列表.length) {
            node.innerHTML = '';
            return;
          }
          const 当前时间 = Date.now();
          const 需要折叠 = 警报列表.length > 3;
          const 显示列表 = 警报列表.slice(0, 需要折叠 ? 2 : 3);
          const 剩余数量 = Math.max(0, 警报列表.length - 显示列表.length);
          const 卡片列表 = [
            ...显示列表.map(警报 => {
              const 类型类 = 警报.类型 === 'danger' ? 'battle-alert-card--danger' : 'battle-alert-card--warning';
              const 状态类 = 警报.已确认 ? ' is-acknowledged' : ' is-new';
              return `<div class="battle-alert-card ${类型类}${状态类}"><span class="battle-alert-text">${htmlEscapeText(警报.文本)}</span></div>`;
            }),
            剩余数量 > 0
              ? `<div class="battle-alert-card battle-alert-card--system ${警报列表.slice(显示列表.length).every(警报 => 警报.已确认) ? 'is-acknowledged' : 'is-new'}"><span class="battle-alert-text">另有 ${剩余数量} 项异常</span></div>`
              : '',
          ].filter(Boolean);
          node.innerHTML = 卡片列表.join('');
          const 下次弱化延迟 = Math.min(
            ...警报列表
              .filter(警报 => !警报.已确认)
              .map(警报 => Math.max(0, 3000 - (当前时间 - Number(警报.首次时间 || 当前时间)))),
          );
          if (Number.isFinite(下次弱化延迟)) {
            状态.智能警报弱化计时器 = setTimeout(() => {
              const 下次时间 = Date.now();
              const 当前警报列表 = Array.isArray(window.BattleUI?.state?.智能警报列表) ? window.BattleUI.state.智能警报列表 : [];
              当前警报列表.forEach(警报 => {
                if (下次时间 - Number(警报.首次时间 || 下次时间) >= 3000) 警报.已确认 = true;
              });
              window.BattleUI.state.警报确认表 = 当前警报列表.reduce((表, 警报) => {
                if (警报.已确认) 表[警报.id] = 警报.首次时间;
                return 表;
              }, {});
              渲染战斗智能警报(当前警报列表);
            }, 下次弱化延迟 + 32);
          }
        }

        function renderUiStats(containerId, unit, options = {}) {
          const node = byId(containerId);
          if (!node) return;
          const stats = [
            ['力', Math.round(Number(unit.str || 0))],
            ['防', Math.round(Number(unit.def || 0))],
            ['速', Math.round(Number(unit.agi || 0))],
          ];
          node.innerHTML = stats
            .map(([label, value]) => `<div class="stat-item"><div class="stat-label">${htmlEscapeText(label)}</div><div class="stat-value">${htmlEscapeText(value)}</div></div>`)
            .join('');
        }

        function renderUiBuffs(containerId, unit) {
          const node = byId(containerId);
          if (!node) return;
          const 状态效果 = Object.entries(unit.状态效果 || {}).slice(0, 8);
          const sustains = Object.keys(unit.持续效果 || {}).slice(0, 4);
          const 装备标签 = 获取可见装备标签(unit);
          const chips = [
            ...装备标签.map(name => `<span class="tag-chip equip">${htmlEscapeText(name)}</span>`),
            ...状态效果.map(([name, condition]) => {
              const typeText = String(condition?.类型 || condition?.type || '').toLowerCase();
              const kind = /debuff|负面|伤|弱/.test(typeText) ? 'debuff' : 'buff';
              return `<span class="tag-chip ${kind}">${htmlEscapeText(name)}</span>`;
            }),
            ...sustains.map(name => `<span class="tag-chip sustain">${htmlEscapeText(name)}</span>`),
          ];
          node.innerHTML = chips.join('');
        }

        function renderUiCombatant(prefix, unit) {
          const safeUnit = flattenUiCombatant(unit);
          const 徽标文本 = 读取战斗单位徽标文本(safeUnit);
          设置界面文本显隐(`ui-${prefix}-lv`, 徽标文本, !!徽标文本);
          setUiText(`ui-${prefix}-name`, safeUnit.name || (prefix === 'player' ? '玩家' : '对手'));
          setUiText(`ui-${prefix}-type`, safeUnit.type || '未知系');
          setUiText(`ui-${prefix}-hp-text`, `${Math.round(safeUnit.hp)} / ${Math.round(safeUnit.hp_max)}`);
          setUiText(`ui-${prefix}-sta-text`, `${Math.round(safeUnit.sta)} / ${Math.round(safeUnit.sta_max)}`);
          setUiText(`ui-${prefix}-sp-text`, `${Math.round(safeUnit.sp)} / ${Math.round(safeUnit.sp_max)}`);
          setUiText(`ui-${prefix}-men-text`, `${Math.round(safeUnit.men)} / ${Math.round(safeUnit.men_max)}`);
          setUiBar(`ui-${prefix}-hp-bar`, safeUnit.hp, safeUnit.hp_max);
          setUiBar(`ui-${prefix}-sta-bar`, safeUnit.sta, safeUnit.sta_max);
          setUiBar(`ui-${prefix}-sp-bar`, safeUnit.sp, safeUnit.sp_max);
          setUiBar(`ui-${prefix}-men-bar`, safeUnit.men, safeUnit.men_max);
          renderUiStats(`ui-${prefix}-stats`, safeUnit);
          renderUiBuffs(`ui-${prefix}-buffs`, safeUnit);
          return safeUnit;
        }

        function 选择战斗动作目标(目标名 = '', 动作来源 = null) {
          const 状态 = window.BattleUI?.state || {};
          const 动作 = 动作来源 || 状态.selectedAction || null;
          const 安全目标名 = String(目标名 || '').trim();
          if (!动作 || !安全目标名) return false;
          const 候选名称列表 = 读取动作目标候选(动作, 状态).map(读取战斗单位名).filter(Boolean);
          if (!候选名称列表.includes(安全目标名)) return false;
          动作.target_name = 安全目标名;
          if (动作是造物承载(动作)) {
            动作.物品接收者 = 安全目标名;
            写入造物动作选择(动作, 状态);
          } else {
            写入普通动作目标(动作, 状态);
          }
          刷新战斗意图输出(动作);
          return true;
        }

        function 同步主战斗面板目标态(面板前缀, 单位名 = '', 目标名称集合 = new Set(), 当前目标名 = '') {
          const 面板 = byId(`ui-${面板前缀}-panel`);
          const 安全单位名 = String(单位名 || '').trim();
          if (!面板) return;
          const 可作为目标 = !!(安全单位名 && 目标名称集合.has(安全单位名));
          面板.classList.toggle('is-targetable', 可作为目标);
          面板.classList.toggle('is-current-target', 可作为目标 && 安全单位名 === 当前目标名);
          if (可作为目标) {
            面板.setAttribute('data-target-name', 安全单位名);
            面板.setAttribute('role', 'button');
            面板.setAttribute('aria-pressed', 安全单位名 === 当前目标名 ? 'true' : 'false');
            面板.setAttribute('tabindex', '0');
          } else {
            面板.removeAttribute('data-target-name');
            面板.removeAttribute('role');
            面板.removeAttribute('aria-pressed');
            面板.removeAttribute('tabindex');
          }
          if (!面板.__battleTargetPanelBound) {
            面板.addEventListener('click', () => {
              const 目标名 = String(面板.getAttribute('data-target-name') || '').trim();
              if (目标名) 选择战斗动作目标(目标名);
            });
            面板.addEventListener('keydown', event => {
              if (event.key !== 'Enter' && event.key !== ' ') return;
              const 目标名 = String(面板.getAttribute('data-target-name') || '').trim();
              if (!目标名) return;
              event.preventDefault();
              选择战斗动作目标(目标名);
            });
            面板.__battleTargetPanelBound = true;
          }
        }

        function 同步战斗队伍展开按钮(阵营, 单位数量, 已展开) {
          const 按钮 = byId(`ui-${阵营}-team-toggle`);
          if (!按钮) return;
          按钮.hidden = 单位数量 <= 0;
          按钮.disabled = 单位数量 <= 0;
          按钮.textContent = 已展开 ? '▴' : '▾';
          按钮.setAttribute('aria-expanded', 已展开 ? 'true' : 'false');
          按钮.setAttribute('title', 已展开 ? '收起队列' : '展开队列');
          if (按钮.__battleTeamToggleBound) return;
          按钮.addEventListener('click', event => {
            event.preventDefault();
            event.stopPropagation();
            const 状态 = window.BattleUI?.state || {};
            if (!状态.队伍展开状态 || typeof 状态.队伍展开状态 !== 'object') 状态.队伍展开状态 = {};
            状态.队伍展开状态[阵营] = 按钮.getAttribute('aria-expanded') !== 'true';
            同步战斗目标显示(状态.selectedAction || null);
          });
          按钮.__battleTeamToggleBound = true;
        }

        function renderUiTeam(容器ID, 单位来源列表, 激活名称 = '', 选项 = {}) {
          const node = byId(容器ID);
          if (!node) return;
          const 阵营 = String(选项.阵营 || '').trim();
          const 目标名称集合 = new Set((Array.isArray(选项.targetNames) ? 选项.targetNames : []).map(name => String(name || '').trim()).filter(Boolean));
          const 当前目标名 = String(选项.currentTargetName || '').trim();
          const 单位列表 = (Array.isArray(单位来源列表) ? 单位来源列表 : []).map(flattenUiCombatant);
          const 展开状态表 = window.BattleUI?.state?.队伍展开状态 || {};
          const 有手动展开状态 = 阵营 && typeof 展开状态表[阵营] === 'boolean';
          const 已展开 = 单位列表.length > 0 && (有手动展开状态 ? 展开状态表[阵营] : 单位列表.length > 1);
          node.hidden = !已展开;
          node.parentElement?.classList?.toggle('has-team-open', 已展开);
          if (阵营) 同步战斗队伍展开按钮(阵营, 单位列表.length, 已展开);
          node.innerHTML = 单位列表
            .map(单位 => {
              const 激活类 = 单位.name === 激活名称 ? ' active' : '';
              const 召唤类 = 单位.isSummon ? ' summon' : '';
              const 可选目标类 = 目标名称集合.has(单位.name) ? ' is-targetable' : '';
              const 当前目标类 = 单位.name === 当前目标名 ? ' is-current-target' : '';
              const 血量比例 = Math.max(0, Math.min(100, (单位.hp / Math.max(1, 单位.hp_max)) * 100));
              const 装备标签 = 获取可见装备标签(单位).join(' · ');
              const 信息片段 = 单位.isSummon
                ? [单位.行动模式 || '召唤', 单位.稳定状态 || '稳定']
                : [装备标签];
              const 信息HTML = 信息片段.filter(Boolean).length ? `<div class="side-meta">${htmlEscapeText(信息片段.filter(Boolean).join(' · '))}</div>` : '';
              const 目标属性 = 目标名称集合.has(单位.name)
                ? ` data-target-name="${htmlEscapeText(单位.name)}" aria-pressed="${单位.name === 当前目标名 ? 'true' : 'false'}"`
                : '';
              const 目标标记 = 单位.name === 当前目标名 ? '<span class="side-target-mark" aria-hidden="true">·</span>' : '';
              return `<button class="side-card${激活类}${召唤类}${可选目标类}${当前目标类}" type="button"${目标属性}><div class="side-head"><div class="side-name">${htmlEscapeText(单位.name)}</div>${目标标记}</div>${信息HTML}<div class="side-mini-bar"><div class="side-mini-fill" style="width:${血量比例}%"></div></div></button>`;
            })
            .join('');
          node.querySelectorAll('[data-target-name]').forEach(button => {
            if (button.__battleTargetBound) return;
            button.addEventListener('click', event => {
              event.stopPropagation();
              const 状态 = window.BattleUI?.state || {};
              const 动作 = 状态.selectedAction || null;
              if (!动作) return;
              const 目标名 = String(button.getAttribute('data-target-name') || '').trim();
              选择战斗动作目标(目标名, 动作);
            });
            button.addEventListener('keydown', event => {
              if (event.key !== 'Enter' && event.key !== ' ') return;
              event.stopPropagation();
            });
            button.__battleTargetBound = true;
          });
        }

        function 同步战斗目标显示(动作来源 = null) {
          const state = window.BattleUI?.state || {};
          const combatData = state.combatData || {};
          if (!combatData || !combatData.参战者) {
            renderUiTargetControls(动作来源 || state.selectedAction || null);
            return;
          }
          const 当前动作 = 动作来源 || state.selectedAction || null;
          const 目标候选列表 = 读取动作目标候选(当前动作 || {}, state);
          const 目标候选名称 = 目标候选列表.map(读取战斗单位名).filter(Boolean);
          const 目标名称集合 = new Set(目标候选名称);
          const 当前目标名 = String(当前动作 && 当前动作.target_name ? 当前动作.target_name : '').trim();
          同步主战斗面板目标态('player', state.player?.name || '', 目标名称集合, 当前目标名);
          同步主战斗面板目标态('enemy', state.enemy?.name || '', 目标名称集合, 当前目标名);
          const playerTeam = [
            ...(combatData.参战者.team_player || []),
            ...读取召唤单位列表(combatData, { 阵营: '玩家' }),
          ];
          const enemyTeam = [
            ...(combatData.参战者.team_enemy || []),
            ...读取召唤单位列表(combatData, { 阵营: '敌方' }),
          ];
          renderUiTeam('ui-team-player', playerTeam, state.player?.name || '', {
            阵营: 'player',
            currentTargetName: 当前目标名,
            targetNames: 目标候选名称,
          });
          renderUiTeam('ui-team-enemy', enemyTeam, state.enemy?.name || '', {
            阵营: 'enemy',
            currentTargetName: 当前目标名,
            targetNames: 目标候选名称,
          });
          renderUiTargetControls(当前动作);
        }

        function renderUiSummonQueue(combatData) {
          const node = byId('ui-summon-queue');
          if (!node) return;
          确保召唤单位表(combatData || {});
          const 召唤列表 = 读取召唤单位列表(combatData)
            .filter(单位 => !召唤单位是分身(单位))
            .map(flattenUiCombatant);
          node.hidden = 召唤列表.length === 0;
          if (!召唤列表.length) {
            node.innerHTML = '';
            return;
          }
          node.innerHTML = 召唤列表
            .map(单位 => {
              const hpRatio = Math.max(0, Math.min(100, (单位.hp / Math.max(1, 单位.hp_max)) * 100));
              const 状态类 = String(单位.稳定状态 || '稳定') === '稳定' ? 'stable' : 'strained';
              const 基础属性 = `力${Math.round(Number(单位.str || 0))} 防${Math.round(Number(单位.def || 0))} 敏${Math.round(Number(单位.agi || 0))}`;
              const 基础动作 = (Array.isArray(单位.技能列表) ? 单位.技能列表 : [])
                .map(技能 => String(技能?.name || 技能?.魂技名 || '').trim())
                .filter(Boolean)
                .slice(0, 3)
                .join(' / ') || '普通攻击';
              return `
                <div class="summon-card ${状态类}">
                  <div class="summon-card-head">
                    <b>${htmlEscapeText(单位.name || '召唤物')}</b>
                    <button class="summon-recall-btn" type="button" data-summon-key="${htmlEscapeText(单位.召唤键 || 单位.name || '')}">收回</button>
                  </div>
                  <div class="summon-card-meta">${htmlEscapeText(单位.宿主名 || '宿主')} · ${htmlEscapeText(单位.行动模式 || '召唤')} · ${htmlEscapeText(单位.稳定状态 || '稳定')}</div>
                  <div class="summon-card-meta">${htmlEscapeText(基础属性)} · ${htmlEscapeText(基础动作)}</div>
                  <div class="summon-hp"><span style="width:${hpRatio}%"></span></div>
                  <div class="summon-card-foot">${Math.round(单位.hp)} / ${Math.round(单位.hp_max)}</div>
                </div>
              `;
            })
            .join('');
          node.querySelectorAll('[data-summon-key]').forEach(button => {
            button.addEventListener('click', () => {
              const state = window.BattleUI?.state || {};
              const 召唤键 = String(button.getAttribute('data-summon-key') || '').trim();
              const action = (state.availableActions || []).find(item => item.action_type === '收回召唤' && String(item.target_name || '') === 召唤键) || {
                id: `summon_recall_${召唤键}`,
                type: 'summon',
                action_type: '收回召唤',
                name: '收回召唤',
                category: '召唤',
                cast_time: 8,
                cost_text: '无',
                enabled: true,
                target_name: 召唤键,
                raw_skill: normalizeSkillData({ name: '收回召唤', 技能分类: '辅助', 消耗: '无', 前摇: 8 }, '收回召唤'),
              };
              state.selectedAction = action;
              state.selectedSkillActions = [action];
              刷新战斗意图输出(action);
              renderUiActionGrid(state.availableActions || [], state.activeCategory || '全部');
            });
          });
        }

        function findUiSkillCost(skill = {}) {
          if (skill?.__fusion_display_cost_text) return String(skill.__fusion_display_cost_text);
          const direct = skill.消耗 || skill.cost || skill.cost_text || '';
          return direct ? String(direct) : '';
        }

        function findUiSkillCastTime(skill = {}) {
          const direct = toUiNumber(skill.前摇 ?? skill['前摇'], NaN);
          if (Number.isFinite(direct)) return direct;
          return 10;
        }

        function pushUiSkillAction(actions, skill, fallbackName, source, 魂环路径 = null, 魂技槽位 = '') {
          if (!skill || typeof skill !== 'object') return;
          const name = String(skill.魂技名 || skill.name || fallbackName || '').trim();
          if (!name) return;
          const runtimeSkill = normalizeSkillData(skill, name);
          if (Array.isArray(魂环路径) && 魂环路径.length) runtimeSkill.__魂环路径 = [...魂环路径];
          const 技能槽位 = String(魂技槽位 || fallbackName || name).trim();
          if (技能槽位) runtimeSkill.__魂技槽位 = 技能槽位;
          const 来源明细 = String(source || '魂技').trim() || '魂技';
          const 来源类别 = 规范化战斗来源类别(来源明细, '魂技');
          写入战斗来源类别上下文(runtimeSkill, { 来源类别, 来源明细, 魂环路径, 魂技槽位: 技能槽位 });
          const 来源键 = [
            来源类别,
            ...(Array.isArray(魂环路径) ? 魂环路径 : []),
            技能槽位,
            name,
            actions.length,
          ].map(片段 => String(片段 || '').replace(/[^\w\u4e00-\u9fa5-]+/g, '_')).filter(Boolean).join('_');
          actions.push({
            id: `skill_${来源键 || actions.length}`,
            type: 'skill',
            action_type: '释放魂技',
            name,
            category: 来源类别,
            source_detail: 来源明细,
            cast_time: findUiSkillCastTime(skill),
            cost_text: findUiSkillCost(skill),
            enabled: true,
            reason: '',
            raw_skill: runtimeSkill,
            skill: runtimeSkill,
          });
        }

        function collectUiSkillActions(charData) {
          const actions = [];
          const char = charData && typeof charData === 'object' ? charData : {};
          取角色武魂条目_战斗(char).forEach(([spiritName, spirit]) => {
            取武魂魂灵条目_战斗(spirit).forEach(([soulSpiritName, soulSpirit]) => {
              取魂灵魂环条目_战斗(soulSpirit).forEach(([ringIndex, ring]) => {
                取魂环魂技条目_战斗(ring).forEach(([skillName, skill]) => {
                  pushUiSkillAction(
                    actions,
                    skill,
                    skillName,
                    spirit?.表象名称 || spiritName || soulSpiritName || `第${ringIndex}魂环`,
                    [spiritName, soulSpiritName, ringIndex],
                    skillName,
                  );
                });
              });
            });
            取武魂直接魂环条目_战斗(spirit).forEach(([ringIndex, ring]) => {
              取魂环魂技条目_战斗(ring).forEach(([skillName, skill]) => {
                pushUiSkillAction(actions, skill, skillName, spirit?.表象名称 || spiritName || `第${ringIndex}魂环`, [spiritName, ringIndex], skillName);
              });
            });
          });
          Object.entries(char.自创魂技 || {}).forEach(([name, skill]) => pushUiSkillAction(actions, skill, name, '自创魂技'));
          Object.entries(char.武魂融合技 || {}).forEach(([name, fusion]) => {
            pushUiSkillAction(actions, buildFusionCombatSkill(fusion, name, char), `武魂融合技·${name}`, '武魂融合技');
          });
          actions.push(
            { id: 'basic_attack', type: 'tactical', action_type: '常规攻击', name: '普通攻击', category: '战术', cast_time: 10, cost_text: '无', enabled: true, skill: { name: '普通攻击' } },
            { id: 'guard', type: 'tactical', action_type: '防御', name: '防御', category: '战术', cast_time: 10, cost_text: '无', enabled: true, skill: { name: '防御' } },
            { id: 'evade', type: 'tactical', action_type: '闪避', name: '闪避', category: '战术', cast_time: 12, cost_text: '体力', enabled: true, skill: { name: '闪避' } },
            { id: 'flee', type: 'tactical', action_type: '撤离', name: '撤离', category: '战术', cast_time: 20, cost_text: '无', enabled: true, skill: { name: '撤离' } },
          );
          return actions;
        }

        root.BattleUIBridge = Object.assign(root.BattleUIBridge || {}, {
          getBattleSubmitMode() {
            return 读取战斗提交模式();
          },
          setBattleSubmitMode(mode) {
            const 下个模式 = 写入战斗提交模式(mode);
            同步战斗提交模式控件();
            try {
              root.dispatchEvent(new CustomEvent('battle-submit-mode-changed', { detail: { mode: 下个模式 } }));
            } catch (error) {}
            return 下个模式;
          },
          executeBattleFlow(combatData, options = {}) {
            return ui_executeBattleFlow(combatData, options);
          },
          getBattleSnapshot(combatData) {
            return ui_getBattleSnapshot(combatData);
          },
          getAvailableActions(charData, combatData) {
            return ui_getAvailableActions(charData, combatData);
          },
        });

        function getUiCombatData() {
          const combatData = window.BattleUIBridge?.getMVU('world.战斗');
          const 参战者 = combatData?.参战者;
          if (
            combatData &&
            typeof combatData === 'object' &&
            Array.isArray(参战者?.team_player) &&
            Array.isArray(参战者?.team_enemy)
          ) return deepClonePlain(combatData);
          return deepClonePlain(_options.combatData || {});
        }

        function renderUiChips(combatData, player, enemy) {
          const node = byId('ui-combat-chips');
          if (!node) return;
          const chips = [
            ['回合', Number(combatData.回合 || 0)],
            ['战斗', combatData.战斗类型 || '战斗'],
            ['阶段', combatData.阶段 || 战斗阶段枚举_V1.宣告],
            ['指向', `${player.name || '玩家'} -> ${enemy.name || '对手'}`],
          ];
          node.innerHTML = chips
            .map(([标签, 内容]) => `<span class="intent-pill"><span>${htmlEscapeText(标签)}</span><b>${htmlEscapeText(内容)}</b></span>`)
            .join('');
        }

        function 构建战斗下拉控件(配置 = {}) {
          const 控件类 = String(配置.类名 || '').trim();
          const 标签 = String(配置.标签 || '').trim();
          const 当前值 = String(配置.当前值 || '').trim();
          const 占位 = String(配置.占位 || '未定').trim();
          const 属性名 = String(配置.属性名 || 'value').trim();
          const 选项列表 = Array.isArray(配置.选项列表) ? 配置.选项列表 : [];
          const 当前项 = 选项列表.find(项 => String(项?.值 ?? 项 ?? '').trim() === 当前值) || null;
          const 当前文本 = String((当前项?.文本 ?? 当前值) || 占位).trim();
          const 选项HTML = 选项列表.length
            ? 选项列表
                .map(项 => {
                  const 值 = String(项?.值 ?? 项 ?? '').trim();
                  const 文本 = String(项?.文本 ?? 值).trim();
                  const 激活 = 值 === 当前值 ? ' active' : '';
                  return `<button class="battle-holo-option${激活}" type="button" data-${属性名}="${htmlEscapeText(值)}" role="option" aria-selected="${激活 ? 'true' : 'false'}">${htmlEscapeText(文本)}</button>`;
                })
                .join('')
            : `<span class="battle-holo-empty">无可选项</span>`;
          return `
            <div class="battle-terminal-row${控件类 ? ` ${htmlEscapeText(控件类)}` : ''}">
              <span class="battle-terminal-label">${htmlEscapeText(标签)}</span>
              <div class="battle-holo-select" data-battle-select="${htmlEscapeText(属性名)}">
                <button class="battle-holo-trigger" type="button" data-dropdown-trigger aria-haspopup="listbox" aria-expanded="false">
                  <span data-dropdown-value>${htmlEscapeText(当前文本)}</span><i aria-hidden="true">▾</i>
                </button>
                <div class="battle-holo-menu" role="listbox">${选项HTML}</div>
              </div>
            </div>
          `;
        }

        function 渲染动作摘要(action = null) {
          const node = byId('ui-action-summary');
          if (!node) return;
          const state = window.BattleUI?.state || {};
          const 当前动作 = action || state.selectedAction || null;
          if (!当前动作) {
            node.innerHTML = '<div class="battle-terminal-current"><span>当前动作</span><b>待选择</b></div>';
            return;
          }
          const 动作名 = String(当前动作.name || 当前动作.action_type || '行动').trim();
          const 目标名 = String(当前动作.target_name || 当前动作.物品接收者 || resolveIntentTargetNameFromAction(当前动作, state.combatData) || '自身').trim();
          const 消耗文本 = String(当前动作.cost_text || '').trim() || '无耗';
          const 前摇文本 = Number(当前动作.cast_time || 0) ? `${Number(当前动作.cast_time || 0)}前摇` : '即时';
          node.innerHTML = `
            <div class="battle-terminal-current">
              <span>当前动作</span>
              <b>[ ${htmlEscapeText(动作名)} ]</b>
              <em>消耗: ${htmlEscapeText(消耗文本)} | ${htmlEscapeText(前摇文本)}</em>
            </div>
          `;
        }

        function renderUiActionFilters(actions, activeCategory) {
          const node = byId('ui-action-filters');
          if (!node) return;
          const categoryOrder = ['魂技', '自创魂技', '武魂融合技', '血脉技能', '召唤', '战术', '特殊动作', '纯操控'];
          const categories = ['全部', ...Array.from(new Set(actions.map(action => action.category || '战术')))]
            .sort((left, right) => {
              if (left === '全部') return -1;
              if (right === '全部') return 1;
              const leftIndex = categoryOrder.includes(left) ? categoryOrder.indexOf(left) : Number.MAX_SAFE_INTEGER;
              const rightIndex = categoryOrder.includes(right) ? categoryOrder.indexOf(right) : Number.MAX_SAFE_INTEGER;
              if (leftIndex !== rightIndex) return leftIndex - rightIndex;
              return String(left).localeCompare(String(right), 'zh-Hans-CN');
            });
          node.innerHTML = categories
            .map(category => `<button class="filter-btn${category === activeCategory ? ' active' : ''}" type="button" data-category="${htmlEscapeText(category)}">${htmlEscapeText(category)}</button>`)
            .join('');
          node.querySelectorAll('[data-category]').forEach(button => {
            button.addEventListener('click', () => {
              const state = window.BattleUI?.state || {};
              state.activeCategory = button.dataset.category || '全部';
              renderUiActionGrid(state.availableActions || [], state.activeCategory);
              renderUiActionFilters(state.availableActions || [], state.activeCategory);
            });
          });
        }

        function 获取动作炸环机制效果(action = {}) {
          const 技能 = action?.raw_skill || action?.skill;
          if (!技能 || typeof 技能 !== 'object') return null;
          const 效果列表 = Array.isArray(技能._效果数组) ? 技能._效果数组 : [];
          return 效果列表.find(效果 => 效果 && String(效果.原型 || '').trim() === '炸环') || null;
        }

        function 格式化魂环标签_V1(魂环项 = {}, 索引 = 0) {
          const 魂环键文本 = String(魂环项?.魂环键 || `第${索引 + 1}魂环`).trim();
          const 年限文本 = Math.max(0, Number(魂环项?.年限 || 0));
          const 档位文本 = String(魂环项?.档位类型 || 判定魂环档位类型_V1(年限文本));
          return `${魂环键文本}(${档位文本}/${年限文本}年)`;
        }

        function 构建炸环候选列表_V1(action = {}, state = {}) {
          const 玩家角色数据 = state?.玩家角色数据 || {};
          const 技能 = action?.raw_skill || action?.skill || {};
          const 当前路径 = Array.isArray(技能.__魂环路径) ? 技能.__魂环路径 : [];
          const 候选列表 = 收集角色可炸魂环列表_V1(玩家角色数据)
            .filter(项 => !读取魂环路径恢复标记_V1(玩家角色数据, 项.路径).恢复中)
            .map(项 => ({ ...项, 当前技能魂环: 路径相同_V1(项.路径, 当前路径) }));
          候选列表.sort((左, 右) => {
            if (左.当前技能魂环 && !右.当前技能魂环) return -1;
            if (!左.当前技能魂环 && 右.当前技能魂环) return 1;
            return Number(右.年限 || 0) - Number(左.年限 || 0);
          });
          return 候选列表;
        }

        function 打开炸环选择对话_V1(action = {}, state = {}) {
          const 炸环效果 = 获取动作炸环机制效果(action);
          if (!炸环效果) return true;
          const 候选列表 = 构建炸环候选列表_V1(action, state);
          if (!候选列表.length) {
            显示战斗提示('当前没有可炸的魂环。');
            return false;
          }
          const 默认已选路径 = Array.isArray(action?.skill?.__炸环选择路径列表)
            ? action.skill.__炸环选择路径列表
            : [];
          const 默认索引集合 = new Set();
          候选列表.forEach((项, 索引) => {
            if (项.当前技能魂环) 默认索引集合.add(索引 + 1);
            if (默认已选路径.some(路径 => 路径相同_V1(路径, 项.路径))) 默认索引集合.add(索引 + 1);
          });
          if (!默认索引集合.size) 默认索引集合.add(1);
          const 默认输入 = Array.from(默认索引集合).sort((a, b) => a - b).join(',');
          const 列表文本 = 候选列表
            .map((项, 索引) => `${索引 + 1}. ${格式化魂环标签_V1(项, 索引)}${项.当前技能魂环 ? ' [当前技能魂环]' : ''}`)
            .join('\n');
          const 输入文本 = window.prompt(`选择要炸掉的魂环（可多选，用逗号分隔）:\n${列表文本}`, 默认输入);
          if (输入文本 === null) return false;
          const 选择索引 = String(输入文本 || '')
            .split(/[，,、\s]+/)
            .map(片段 => Number(片段))
            .filter(数值 => Number.isFinite(数值) && 数值 >= 1 && 数值 <= 候选列表.length);
          const 去重索引 = Array.from(new Set(选择索引));
          if (!去重索引.length) {
            显示战斗提示('未选择有效魂环，本次炸环取消。');
            return false;
          }
          const 选择路径列表 = 去重索引.map(索引 => 候选列表[索引 - 1]?.路径).filter(路径 => Array.isArray(路径));
          if (!Array.isArray(action.skill.__魂环路径) || !action.skill.__魂环路径.length) {
            显示战斗提示('当前技能未绑定魂环路径，无法执行炸环。');
            return false;
          }
          if (!选择路径列表.some(路径 => 路径相同_V1(路径, action.skill.__魂环路径))) {
            选择路径列表.unshift([...action.skill.__魂环路径]);
          }
          action.__炸环选择路径列表 = 选择路径列表.map(路径 => [...路径]);
          action.skill.__炸环选择路径列表 = action.__炸环选择路径列表.map(路径 => [...路径]);
          return true;
        }

        function 读取动作技能描述文本(动作 = {}) {
          const 技能 = 动作?.raw_skill || 动作?.skill || {};
          const 读取文本 = (...候选列表) => 候选列表.map(值 => String(值 ?? '').trim()).find(Boolean) || '';
          const 描述行 = [];
          const 添加描述 = (标签, 文本) => {
            const 内容 = String(文本 || '').trim();
            if (!内容 || 描述行.some(行 => 行.内容 === 内容)) return;
            描述行.push({ 标签, 内容 });
          };
          添加描述('画面', 读取文本(技能.画面描述, 技能.visualDesc));
          添加描述('效果', 读取文本(技能.效果描述, 技能.effectDesc, 技能.描述, 技能.效果));
          const 效果列表 = Array.isArray(技能._效果数组)
            ? 技能._效果数组
            : Array.isArray(技能['使用效果'])
              ? 技能['使用效果']
              : [];
          if (!描述行.length && 效果列表.length) {
            const 效果摘要 = 效果列表
              .map(效果 => {
                if (!效果 || typeof 效果 !== 'object') return '';
                const 原型 = 读取文本(效果.原型, 效果.type);
                const 目标 = 读取文本(效果.目标, 效果.target);
                const 数值 = 读取文本(效果.数值, 效果.威力倍率, 效果.状态, 效果.结算);
                if (!原型 && Array.isArray(效果.使用效果)) {
                  return 效果.使用效果.map(子效果 => 读取文本(子效果 && 子效果.原型)).filter(Boolean).join(' / ');
                }
                return [原型, 目标, 数值].filter(Boolean).join(' · ');
              })
              .filter(Boolean)
              .slice(0, 4)
              .join('；');
            添加描述('效果', 效果摘要);
          }
          if (!描述行.length) 添加描述('动作', 动作.reason || 动作.action_type || 动作.category || '无额外效果说明');
          return 描述行;
        }

        function 构建战斗魂技展示数据(动作 = {}) {
          const 技能 = 动作?.raw_skill || 动作?.skill || {};
          const 名称 = String(动作.name || 技能.name || 技能.魂技名 || 动作.action_type || '行动').trim();
          const 分类文本 = String(动作.category || 技能.技能分类 || '战术').trim() || '战术';
          const 类型文案 = 动作.action_type === '释放魂技' ? '魂技' : 分类文本;
          const 来源明细 = String(动作.source_detail || '').trim();
          const 标签集合 = new Set();
          [类型文案, 来源明细 && 来源明细 !== 类型文案 && 来源明细 !== 分类文本 ? 来源明细 : '', ...(Array.isArray(动作.tags) ? 动作.tags : [])]
            .map(值 => String(值 || '').trim())
            .filter(Boolean)
            .forEach(值 => 标签集合.add(值));
          const 消耗原文 = String(动作.cost_text || findUiSkillCost(技能) || '').trim();
          const 消耗文本 = 消耗原文 && 消耗原文 !== '无' ? 消耗原文 : '无耗';
          const 前摇数值 = Number(动作.cast_time ?? findUiSkillCastTime(技能) ?? 0) || 0;
          const 前摇文本 = 前摇数值 ? `${前摇数值}` : '即时';
          const 动作类型 = String(动作.action_type || '').trim();
          const 目标文本 = 动作是造物承载(动作)
            ? '造物'
            : 动作类型 === '撤离'
              ? '撤离'
              : ['防御', '闪避', '穿戴装备', '收回召唤'].includes(动作类型)
                ? '自身'
                : inferSkillPrimaryTargetKind(技能 || {});
          const 描述行 = 读取动作技能描述文本(动作);
          const 短效果摘要 = String(描述行[0]?.内容 || 动作.reason || 动作.action_type || 类型文案 || '待解析').trim();
          return {
            名称,
            类型文案,
            标签列表: Array.from(标签集合).slice(0, 5),
            消耗文本,
            前摇文本,
            目标文本,
            短效果摘要,
            描述行,
          };
        }

        function 构建动作悬浮效果Html(动作 = {}) {
          const 展示 = 构建战斗魂技展示数据(动作);
          const 标签Html = 展示.标签列表.length
            ? `<div class="battle-ring-tooltip-tags">${展示.标签列表.map(标签 => `<span class="battle-ring-tooltip-chip">${htmlEscapeText(标签)}</span>`).join('')}</div>`
            : '';
          const 显示摘要 = 展示.短效果摘要 && !展示.描述行.some(行 => 行.内容 === 展示.短效果摘要);
          const 描述Html = 展示.描述行
            .map(行 => `<div class="battle-ring-tooltip-copy"><em>${htmlEscapeText(行.标签)}</em><span>${htmlEscapeText(行.内容)}</span></div>`)
            .join('');
          return `
            <div class="battle-ring-tooltip-title">
              <b>${htmlEscapeText(展示.名称)}</b>
              <span>${htmlEscapeText(展示.类型文案)}</span>
            </div>
            ${显示摘要 ? `<div class="battle-ring-tooltip-desc">${htmlEscapeText(展示.短效果摘要)}</div>` : ''}
            <div class="battle-ring-tooltip-meta">
              <span class="battle-ring-tooltip-meta-row"><em>消耗</em><strong>${htmlEscapeText(展示.消耗文本)}</strong></span>
              <span class="battle-ring-tooltip-meta-row"><em>前摇</em><strong>${htmlEscapeText(展示.前摇文本)}</strong></span>
              <span class="battle-ring-tooltip-meta-row"><em>目标</em><strong>${htmlEscapeText(展示.目标文本)}</strong></span>
            </div>
            ${描述Html}
            ${标签Html}
          `;
        }

        function 读取战斗单位名(unit = {}) {
          return String(unit?.name || unit?.名称 || unit?.charKey || unit?.char_key || unit?.key || '').trim();
        }

        function 读取目标控制节点() {
          let node = byId('ui-target-controls');
          if (node) return node;
          const grid = byId('ui-action-grid');
          if (!grid || !grid.parentElement) return null;
          node = document.createElement('div');
          node.id = 'ui-target-controls';
          node.className = 'battle-target-controls';
          node.hidden = true;
          grid.insertAdjacentElement('afterend', node);
          return node;
        }

        function 读取技能悬浮节点() {
          return byId('ui-skill-tooltip');
        }

        function 关闭技能悬浮() {
          const 节点 = 读取技能悬浮节点();
          if (!节点) return;
          节点.hidden = true;
          节点.classList.remove('show');
          节点.innerHTML = '';
          节点.style.left = '';
          节点.style.top = '';
          节点.style.width = '';
          节点.style.maxHeight = '';
          节点.style.overflow = '';
        }

        function 定位技能悬浮(触发节点) {
          const 悬浮节点 = 读取技能悬浮节点();
          if (!悬浮节点 || !触发节点 || typeof 触发节点.getBoundingClientRect !== 'function') return;
          const 容器矩形 = wrapperElement.getBoundingClientRect();
          const 触发矩形 = 触发节点.getBoundingClientRect();
          悬浮节点.hidden = false;
          悬浮节点.classList.add('show');
          const 宽度 = Math.min(420, Math.max(220, 容器矩形.width - 24));
          悬浮节点.style.width = `${宽度}px`;
          悬浮节点.style.maxHeight = '';
          悬浮节点.style.overflow = 'visible';
          const 可用高度 = Math.max(160, 容器矩形.height - 24);
          let 实际高度 = Math.max(悬浮节点.offsetHeight || 160, 120);
          if (实际高度 > 可用高度) {
            实际高度 = 可用高度;
            悬浮节点.style.maxHeight = `${Math.round(可用高度)}px`;
            悬浮节点.style.overflow = 'hidden auto';
          }
          let 左 = 触发矩形.left - 容器矩形.left;
          let 上 = 触发矩形.bottom - 容器矩形.top + 8;
          if (左 + 宽度 > 容器矩形.width - 12) 左 = 容器矩形.width - 宽度 - 12;
          if (左 < 12) 左 = 12;
          if (上 + 实际高度 > 容器矩形.height - 12) 上 = 触发矩形.top - 容器矩形.top - 实际高度 - 8;
          if (上 < 12) 上 = 12;
          悬浮节点.style.left = `${Math.round(左)}px`;
          悬浮节点.style.top = `${Math.round(上)}px`;
        }

        function 显示技能悬浮(触发节点, action = {}) {
          const 悬浮节点 = 读取技能悬浮节点();
          if (!悬浮节点) return;
          悬浮节点.innerHTML = 构建动作悬浮效果Html(action);
          定位技能悬浮(触发节点);
        }

        function 动作是造物承载(action = {}) {
          const skill = action?.raw_skill || action?.skill || {};
          return String(skill?.承载方式 || '').trim() === '造物承载';
        }

        function 读取动作目标候选(action = {}, state = {}) {
          const combatData = state.combatData || {};
          const playerTeam = (combatData?.参战者?.team_player || []).filter(unit => unit && isCombatUnitAlive(unit));
          const enemyTeam = (combatData?.参战者?.team_enemy || []).filter(unit => unit && isCombatUnitAlive(unit));
          const skill = action?.raw_skill || action?.skill || {};
          if (['防御', '闪避', '撤离', '穿戴装备', '收回召唤'].includes(String(action?.action_type || ''))) return [];
          if (动作是造物承载(action)) {
            const mode = String(action.造物处理 || '生成到自己背包');
            if (mode === '生成给友方') return playerTeam;
            if (mode === '立即给目标使用') return [...playerTeam, ...enemyTeam];
            return [];
          }
          const targetKind = inferSkillPrimaryTargetKind(skill);
          if (targetKind === '自身') return playerTeam.slice(0, 1);
          if (targetKind === '友方单体') return playerTeam;
          if (targetKind === '敌方单体') return enemyTeam;
          return [];
        }

        function 写入普通动作目标(action = {}, state = {}) {
          if (!action || typeof action !== 'object') return;
          if (动作是造物承载(action)) return;
          const candidates = 读取动作目标候选(action, state);
          if (!candidates.length) {
            const 动作类型 = String(action?.action_type || '').trim();
            if (动作类型 === '收回召唤' && String(action.target_name || '').trim()) return;
            delete action.target_name;
            return;
          }
          const current = String(action.target_name || '').trim();
          const matched = candidates.find(unit => 读取战斗单位名(unit) === current);
          action.target_name = 读取战斗单位名(matched || candidates[0]);
        }

        function 写入造物动作选择(action = {}, state = {}) {
          if (!action || typeof action !== 'object' || !动作是造物承载(action)) return;
          const combatData = state.combatData || {};
          const player = combatData?.参战者?.team_player?.[0] || state.player || {};
          const playerName = 读取战斗单位名(player);
          const mode = String(action.造物处理 || '生成到自己背包').trim() || '生成到自己背包';
          action.造物处理 = mode;
          action.立即使用 = mode === '立即自用' || mode === '立即给目标使用';
          if (action.立即使用) action.即食 = true;
          else {
            delete action.即食;
            delete action.立即食用;
          }
          if (mode === '生成到自己背包' || mode === '立即自用') {
            action.物品接收者 = playerName;
            action.target_name = mode === '立即自用' ? playerName : '';
            if (mode === '立即自用') action.食用目标 = playerName;
            else delete action.食用目标;
            return;
          }
          const candidates = 读取动作目标候选(action, state);
          const current = String(action.target_name || action.物品接收者 || '').trim();
          const matched = candidates.find(unit => 读取战斗单位名(unit) === current);
          const targetName = 读取战斗单位名(matched || candidates[0] || player);
          action.target_name = targetName;
          action.物品接收者 = targetName;
          if (mode === '立即给目标使用') action.食用目标 = targetName;
          else delete action.食用目标;
        }

        function 刷新战斗意图输出(action = null) {
          const state = window.BattleUI?.state || {};
          const output = byId('ui-intent-output');
          if (output) output.value = buildIntentText(action ? [action] : undefined);
          渲染动作摘要(action || state.selectedAction || null);
          同步战斗目标显示(action || state.selectedAction || null);
        }

        function renderUiTargetControls(action = null) {
          const node = 读取目标控制节点();
          if (!node) return;
          const state = window.BattleUI?.state || {};
          if (!action) {
            node.hidden = true;
            node.innerHTML = '';
            return;
          }
          if (动作是造物承载(action)) {
            写入造物动作选择(action, state);
            const mode = String(action.造物处理 || '生成到自己背包');
            const modes = ['生成到自己背包', '生成给友方', '立即自用', '立即给目标使用'];
            const candidates = 读取动作目标候选(action, state);
            const 当前目标 = String(action.target_name || action.物品接收者 || '').trim();
            const 目标选项 = candidates.map(unit => 读取战斗单位名(unit)).filter(Boolean).map(name => ({ 值: name, 文本: name }));
            const targetHtml = 目标选项.length
              ? 构建战斗下拉控件({
                  标签: '施放目标',
                  当前值: 当前目标,
                  占位: '自身',
                  属性名: 'target-name',
                  选项列表: 目标选项,
                })
              : '';
            node.hidden = false;
            node.innerHTML = `
              ${targetHtml}
              ${构建战斗下拉控件({
                标签: '施放方式',
                当前值: mode,
                属性名: 'construct-mode',
                选项列表: modes.map(item => ({ 值: item, 文本: item })),
              })}
            `;
            node.querySelectorAll('[data-construct-mode]').forEach(button => {
              button.addEventListener('click', () => {
                action.造物处理 = button.getAttribute('data-construct-mode') || '生成到自己背包';
                写入造物动作选择(action, state);
                刷新战斗意图输出(action);
              });
            });
            node.querySelectorAll('[data-target-name]').forEach(button => {
              button.addEventListener('click', () => {
                选择战斗动作目标(button.getAttribute('data-target-name') || '', action);
              });
            });
            return;
          }
          写入普通动作目标(action, state);
          const candidates = 读取动作目标候选(action, state);
          if (!candidates.length) {
            node.hidden = true;
            node.innerHTML = '';
            return;
          }
          const 当前目标 = String(action.target_name || '').trim();
          const 目标选项 = candidates.map(unit => 读取战斗单位名(unit)).filter(Boolean).map(name => ({ 值: name, 文本: name }));
          node.hidden = false;
          node.innerHTML = 构建战斗下拉控件({
            标签: '施放目标',
            当前值: 当前目标,
            属性名: 'target-name',
            选项列表: 目标选项,
          });
          node.querySelectorAll('[data-target-name]').forEach(button => {
            button.addEventListener('click', () => {
              选择战斗动作目标(button.getAttribute('data-target-name') || '', action);
            });
          });
        }

        function 关闭战斗下拉控件(例外节点 = null) {
          wrapperElement.querySelectorAll('.battle-holo-select.is-open').forEach(节点 => {
            if (例外节点 && 节点 === 例外节点) return;
            节点.classList.remove('is-open');
            节点.querySelector('[data-dropdown-trigger]')?.setAttribute('aria-expanded', 'false');
          });
        }

        function 同步战斗下拉文本(根节点, 当前值 = '', 属性名 = 'value') {
          if (!根节点) return;
          const 安全当前值 = String(当前值 || '').trim();
          const 选项列表 = Array.from(根节点.querySelectorAll(`[data-${属性名}]`));
          const 命中选项 = 选项列表.find(选项 => String(选项.getAttribute(`data-${属性名}`) || '').trim() === 安全当前值) || 选项列表[0] || null;
          选项列表.forEach(选项 => {
            const 命中 = 选项 === 命中选项;
            选项.classList.toggle('active', 命中);
            选项.setAttribute('aria-selected', 命中 ? 'true' : 'false');
          });
          const 文本节点 = 根节点.querySelector('[data-dropdown-value]');
          if (文本节点 && 命中选项) 文本节点.textContent = 命中选项.textContent || 安全当前值;
        }

        function 初始化战斗下拉控件() {
          if (wrapperElement.__battleDropdownBound) return;
          wrapperElement.addEventListener('click', event => {
            const 触发器 = event.target?.closest?.('[data-dropdown-trigger]');
            if (触发器 && wrapperElement.contains(触发器)) {
              const 下拉根 = 触发器.closest('.battle-holo-select');
              if (!下拉根) return;
              const 将展开 = !下拉根.classList.contains('is-open');
              关闭战斗下拉控件(下拉根);
              下拉根.classList.toggle('is-open', 将展开);
              触发器.setAttribute('aria-expanded', 将展开 ? 'true' : 'false');
              event.preventDefault();
              event.stopPropagation();
              return;
            }
            const 选项 = event.target?.closest?.('.battle-holo-option');
            if (选项 && wrapperElement.contains(选项)) {
              关闭战斗下拉控件();
              return;
            }
            if (!event.target?.closest?.('.battle-holo-select')) 关闭战斗下拉控件();
          });
          wrapperElement.addEventListener('keydown', event => {
            if (event.key === 'Escape') 关闭战斗下拉控件();
          });
          wrapperElement.__battleDropdownBound = true;
        }

        function renderUiActionGrid(actions, activeCategory = '全部') {
          const node = byId('ui-action-grid');
          if (!node) return;
          const state = window.BattleUI?.state || {};
          const selectedId = state.selectedAction?.id || '';
          const filtered = activeCategory && activeCategory !== '全部'
            ? actions.filter(action => (action.category || '战术') === activeCategory)
            : actions;
          node.innerHTML = filtered
            .map(action => {
              const selected = action.id === selectedId ? ' is-selected' : '';
              const disabled = action.enabled === false ? ' disabled' : '';
              const 展示 = 构建战斗魂技展示数据(action);
              const 卡片标签 = 展示.标签列表[0] || 展示.类型文案;
              return `
                <button class="action-btn${selected} battle-skill-card" type="button" data-action-id="${htmlEscapeText(action.id)}"${disabled}>
                  <span class="battle-skill-head">
                    <span class="battle-skill-name">${htmlEscapeText(展示.名称)}</span>
                    <span class="battle-skill-tag">${htmlEscapeText(卡片标签)}</span>
                  </span>
                  <span class="battle-skill-meta">
                    <span><em>消耗</em><b>${htmlEscapeText(展示.消耗文本)}</b></span>
                    <span><em>前摇</em><b>${htmlEscapeText(展示.前摇文本)}</b></span>
                    <span><em>目标</em><b>${htmlEscapeText(展示.目标文本)}</b></span>
                  </span>
                </button>
              `;
            })
            .join('');
          if (!node.__battleTooltipScrollBound) {
            node.addEventListener('scroll', 关闭技能悬浮, { passive: true });
            node.__battleTooltipScrollBound = true;
          }
          node.querySelectorAll('[data-action-id]').forEach(button => {
            button.addEventListener('click', () => {
              const state = window.BattleUI?.state || {};
              const action = (state.availableActions || []).find(item => item.id === button.dataset.actionId);
              if (!action || action.enabled === false) return;
              关闭技能悬浮();
              if (!打开炸环选择对话_V1(action, state)) return;
              state.selectedAction = action;
              state.selectedSkillActions = [action];
              if (动作是造物承载(action)) 写入造物动作选择(action, state);
              else 写入普通动作目标(action, state);
              清空战斗预演面板();
              刷新战斗意图输出(action);
              renderUiActionGrid(state.availableActions || [], state.activeCategory || '全部');
            });
            button.addEventListener('mouseenter', () => {
              const state = window.BattleUI?.state || {};
              const action = (state.availableActions || []).find(item => item.id === button.dataset.actionId);
              if (!action) return;
              显示技能悬浮(button, action);
            });
            button.addEventListener('focus', () => {
              const state = window.BattleUI?.state || {};
              const action = (state.availableActions || []).find(item => item.id === button.dataset.actionId);
              if (!action) return;
              显示技能悬浮(button, action);
            });
            button.addEventListener('mouseleave', () => {
              const 悬浮节点 = 读取技能悬浮节点();
              if (!悬浮节点) return;
              if (!悬浮节点.matches(':hover')) 关闭技能悬浮();
            });
            button.addEventListener('blur', 关闭技能悬浮);
          });
          同步战斗目标显示(state.selectedAction || null);
        }

        function 读取界面自动续推设置(state = window.BattleUI?.state || {}) {
          const 设置 = 规范化自动续推设置(state.autoContinueConfig || {}, 'multi_round');
          return state.currentMode === 'multi_round' ? 设置 : { ...设置, maxRounds: 1 };
        }

        function 写入界面自动续推设置(补丁 = {}) {
          const state = window.BattleUI?.state;
          if (!state) return;
          state.autoContinueConfig = 规范化自动续推设置({ ...(state.autoContinueConfig || {}), ...补丁 }, 'multi_round');
          渲染自动续推设置控件();
        }

        function 渲染自动续推设置控件() {
          const state = window.BattleUI?.state;
          if (!state) return;
          const panel = byId('ui-auto-continue-settings');
          const maxInput = byId('ui-auto-round-max');
          const thresholdInput = byId('ui-auto-stop-threshold');
          const chanceInput = byId('ui-auto-continue-chance');
          if (!panel && !maxInput && !thresholdInput && !chanceInput) return;
          const 设置 = 规范化自动续推设置(state.autoContinueConfig || {}, 'multi_round');
          state.autoContinueConfig = 设置;
          const 同步输入 = (input, key, patchKey) => {
            if (!input) return;
            if (!input.__battleAutoContinueBound) {
              const 提交 = () => {
                const 原文 = String(input.value || '').trim();
                if (!原文) {
                  渲染自动续推设置控件();
                  return;
                }
                写入界面自动续推设置({ [patchKey]: 原文 });
              };
              input.addEventListener('input', 提交);
              input.addEventListener('change', 提交);
              input.__battleAutoContinueBound = true;
            }
            const 显示值 = String(Math.round(设置[key]));
            if (root.document?.activeElement !== input && input.value !== 显示值) input.value = 显示值;
          };
          同步输入(maxInput, 'maxRounds', 'maxRounds');
          同步输入(thresholdInput, 'stopDamagePercent', 'stopDamagePercent');
          同步输入(chanceInput, 'continueChancePercent', 'continueChancePercent');
          if (panel) {
            const 连续模式 = state.currentMode === 'multi_round';
            panel.hidden = !连续模式;
            panel.classList.toggle('is-single-round', !连续模式);
          }
        }

        function setUiBattleMode(mode) {
          const normalized = mode === 'multi_round' ? 'multi_round' : 'single_round';
          if (window.BattleUI && window.BattleUI.state) {
            window.BattleUI.state.currentMode = normalized;
          }
          document.querySelectorAll('#ui-mode-group [data-mode]').forEach(btn => {
            btn.classList.toggle('active', btn.dataset.mode === normalized);
            btn.setAttribute('aria-selected', btn.dataset.mode === normalized ? 'true' : 'false');
          });
          同步战斗下拉文本(byId('ui-mode-group'), normalized, 'mode');
          渲染动作摘要(window.BattleUI?.state?.selectedAction || null);
          渲染自动续推设置控件();
        }

        function setUiIntentMode(mode) {
          const normalized = String(mode || '点到为止').trim() || '点到为止';
          if (window.BattleUI && window.BattleUI.state) window.BattleUI.state.currentIntentMode = normalized;
          const input = byId('ui-intent-mode');
          if (input && input.value !== normalized) input.value = normalized;
          同步战斗下拉文本(byId('ui-intent-mode-select'), normalized, 'intent-mode');
          渲染动作摘要(window.BattleUI?.state?.selectedAction || null);
        }

        async function initBattleUiFromMvu() {
          syncFromBattleEngine();
        }

        function syncFromBattleEngine() {
          const combatData = getUiCombatData();
          if (!combatData || !combatData.参战者) return;
          hydrateCombatData(combatData);
          确保召唤单位表(combatData);
          const player = renderUiCombatant('player', combatData.参战者.team_player?.[0]);
          const enemy = renderUiCombatant('enemy', combatData.参战者.team_enemy?.[0]);
          renderUiChips(combatData, player, enemy);

          const rawCharData =
            window.BattleUIBridge?.getMVU(`char.${player.name}`) ||
            combatData.参战者.team_player?.[0];
          const charData = rawCharData && typeof rawCharData === 'object' && !Array.isArray(rawCharData)
            ? { ...rawCharData, name: String(rawCharData.name || rawCharData.名称 || player.name || '').trim() || player.name }
            : rawCharData;
          const availableActions = ui_getAvailableActions(charData, combatData);
          const previousState = window.BattleUI?.state || {};
          const activeCategory = previousState.activeCategory || '全部';
          const currentIntentMode = previousState.currentIntentMode || combatData.战斗意图 || '点到为止';
          const autoContinueConfig = 规范化自动续推设置(previousState.autoContinueConfig || {}, 'multi_round');
          const pendingTowerSettlement = normalizeSoulTowerPendingSettlement(combatData.魂灵塔待结算);
          const selectedAction =
            (previousState.selectedAction &&
              availableActions.find(action => action.id === previousState.selectedAction.id && action.enabled !== false)) ||
            availableActions.find(action => action.enabled !== false) ||
            availableActions[0] ||
            null;
          window.BattleUI = Object.assign(window.BattleUI || {}, {
            state: {
              ...previousState,
              combatData,
              player,
              enemy,
              玩家角色数据: charData,
              availableActions,
              activeCategory,
              selectedAction,
              selectedSkillActions: selectedAction ? [selectedAction] : [],
              selectedPreActions: [],
              currentMode: previousState.currentMode || 'single_round',
              currentIntentMode,
              autoContinueConfig,
              pendingTowerSettlement,
              activeBattleRecordTab: previousState.activeBattleRecordTab === 'preview' ? 'preview' : 'actual',
              battleRecordCollapsed: previousState.battleRecordCollapsed !== false,
            },
          });
          setUiBattleMode(window.BattleUI.state.currentMode);
          setUiIntentMode(window.BattleUI.state.currentIntentMode);
          渲染自动续推设置控件();
          renderUiActionFilters(availableActions, activeCategory);
          renderUiActionGrid(availableActions, activeCategory);
          renderUiSummonQueue(combatData);
          渲染战斗记录面板();
          const output = byId('ui-intent-output');
          if (selectedAction) {
            if (动作是造物承载(selectedAction)) 写入造物动作选择(selectedAction, window.BattleUI.state);
            else 写入普通动作目标(selectedAction, window.BattleUI.state);
            if (output) output.value = buildIntentText([selectedAction]);
            同步战斗目标显示(selectedAction);
          }
          渲染动作摘要(selectedAction);
          renderSoulTowerSettlementPanel(pendingTowerSettlement);
          const 智能警报列表 = 计算战斗智能警报(combatData, previousState.智能警报列表 || []);
          window.BattleUI.state.智能警报列表 = 智能警报列表;
          window.BattleUI.state.警报确认表 = 智能警报列表.reduce((表, 警报) => {
            if (警报.已确认) 表[警报.id] = 警报.首次时间;
            return 表;
          }, {});
          渲染战斗智能警报(智能警报列表);
        }

        component.syncFromBattleEngine = syncFromBattleEngine;

        function buildIntentSerializedSkill(skill = {}) {
          const 来源 = skill && typeof skill === 'object' ? skill : {};
          const serialized = {};
          [
            '魂技名',
            '画面描述',
            '效果描述',
            '技能分类',
            '目标',
            '承载方式',
            '消耗',
            '前摇',
            '附带属性',
            '使用条件',
            '战斗摘要',
            '战斗语义',
            '技能掌控度',
            '_效果数组',
            '副作用列表',
            '产物描述',
          ].forEach(key => {
            if (来源[key] !== undefined) serialized[key] = deepClonePlain(来源[key]);
          });
          if (!serialized.魂技名 && 来源.name) serialized.魂技名 = 来源.name;
          if (String(来源.__物品名 || '').trim()) serialized.__物品名 = String(来源.__物品名 || '').trim();
          if (Array.isArray(来源.__魂环路径) && 来源.__魂环路径.length) serialized.__魂环路径 = 来源.__魂环路径.map(片段 => String(片段));
          if (String(来源.__魂技槽位 || '').trim()) serialized.__魂技槽位 = String(来源.__魂技槽位 || '').trim();
          return serialized;
        }

        function buildSerializedEntryFromAction(action) {
          if (!action) return null;
          const skill = action.raw_skill || action.skill || {};
          const type = action.action_type || action.type || getSkillType(skill) || '输出';
          const name = action.name || skill.name || '';
          const resolvedTargetName = resolveIntentTargetNameFromAction(action, window.BattleUI?.state?.combatData || {});
          const 来源上下文 = 读取战斗来源类别上下文(skill, action.category || '魂技');
          const 魂环路径 = Array.isArray(skill?.__魂环路径) ? skill.__魂环路径.map(片段 => String(片段)) : [];
          const 魂技槽位 = String(skill?.__魂技槽位 || '').trim();
          const actionObj = {
            type,
            action_type: type,
            category: action.category || 来源上下文.来源类别,
            source_detail: action.source_detail || 来源上下文.来源明细,
            skill: buildIntentSerializedSkill(skill),
            前摇: Number(action.cast_time ?? skill.前摇 ?? 0) || 0,
            target_name: resolvedTargetName,
            前摇已结算: action.前摇已结算 === true,
          };
          if (魂环路径.length) actionObj.__魂环路径 = 魂环路径;
          if (魂技槽位) actionObj.__魂技槽位 = 魂技槽位;
          const ownerName = String(action.actor_name || action.__行动者名 || window.BattleUI?.state?.player?.name || '').trim();
          if (ownerName) actionObj.actor_name = ownerName;
          if (type === '穿戴装备') actionObj.equip_target = /机甲/.test(name) ? 'mech' : 'armor';
          if (type === '吸血反哺') actionObj.heal_ratio = action.heal_ratio || 0.3;
          if (action.物品名 || skill.__物品名) actionObj.物品名 = String(action.物品名 || skill.__物品名 || '').trim();
          if (action.造物处理) actionObj.造物处理 = String(action.造物处理 || '').trim();
          if (action.物品接收者) actionObj.物品接收者 = String(action.物品接收者 || '').trim();
          if (action.立即使用 === true) actionObj.立即使用 = true;
          if (action.即食 === true) actionObj.即食 = true;
          if (action.食用目标) actionObj.食用目标 = String(action.食用目标 || '').trim();
          if (type === '多元素融合') {
            actionObj.fusionElements = normalizeBattleSkillAttributeTokens(action.fusionElements || []);
            actionObj.fusionPattern = String(
              action.fusionPattern || buildBattleFusionPattern(actionObj.fusionElements),
            );
          }
          const 炸环选择路径列表 =
            (Array.isArray(action?.__炸环选择路径列表) && action.__炸环选择路径列表) ||
            (Array.isArray(skill?.__炸环选择路径列表) && skill.__炸环选择路径列表) ||
            [];
          if (炸环选择路径列表.length) {
            actionObj.__炸环选择路径列表 = 炸环选择路径列表
              .filter(路径 => Array.isArray(路径) && 路径.length)
              .map(路径 => 路径.map(片段 => String(片段)));
          }
          if (action.is_charged) actionObj.is_charged = true;
          return actionObj;
        }

        function buildIntentText(actions) {
          const state = window.BattleUI?.state || {};
          const fallbackActions = [
            ...(state.selectedPreActions || []),
            state.selectedSkillActions?.[state.selectedSkillActions.length - 1] || state.selectedAction,
          ].filter(Boolean);
          const sourceActions = Array.isArray(actions) && actions.length ? actions : fallbackActions;
          const queue = sourceActions.map(buildSerializedEntryFromAction).filter(Boolean);
          const parts = [];
          if (queue.length) {
            parts.push(queue.map(action => action.skill?.魂技名 || action.skill?.name || action.type).filter(Boolean).join('，'));
            parts.push(`[动作队列]${JSON.stringify(queue)}[/动作队列]`);
          }
          const target =
            queue.find(action => String(action?.target_name || '').trim())?.target_name ||
            resolveIntentTargetNameFromAction(sourceActions[0], state.combatData);
          if (target) parts.push(`[目标]${target}[/目标]`);
          return parts.join('\n');
        }

        function 读取可提交战斗动作队列(state = {}) {
          const mainAction = state.selectedSkillActions?.[state.selectedSkillActions.length - 1] || state.selectedAction;
          if (mainAction && mainAction.enabled === false) return [];
          return [
            ...(Array.isArray(state.selectedPreActions) ? state.selectedPreActions : []).filter(action => action && action.enabled !== false),
            mainAction,
          ].filter(Boolean);
        }

        function 读取战斗记录面板节点() {
          return 读取战斗记录终端节点()?.querySelector('#ui-battle-preview-panel') || null;
        }

        function 设置战斗记录展开状态(展开) {
          const state = window.BattleUI?.state || {};
          state.battleRecordCollapsed = 展开 === true ? false : true;
          const terminal = 读取战斗记录终端节点();
          if (!terminal) return;
          const collapsed = state.battleRecordCollapsed !== false;
          terminal.classList.toggle('battle-record-terminal--collapsed', collapsed);
          terminal.querySelector('.battle-record-body')?.toggleAttribute('hidden', collapsed);
          const toggle = terminal.querySelector('#ui-battle-record-toggle');
          if (toggle) {
            toggle.setAttribute('aria-expanded', collapsed ? 'false' : 'true');
            toggle.textContent = collapsed ? '记录' : '隐藏';
          }
          if (component.syncRecordPortalPosition) component.syncRecordPortalPosition();
        }

        function 同步战斗记录展开状态() {
          const state = window.BattleUI?.state || {};
          设置战斗记录展开状态(state.battleRecordCollapsed === false);
        }

        function 读取战斗记录页签() {
          const state = window.BattleUI?.state || {};
          return state.activeBattleRecordTab === 'preview' ? 'preview' : 'actual';
        }

        function 设置战斗记录页签(tab = 'actual') {
          const activeTab = tab === 'preview' ? 'preview' : 'actual';
          if (window.BattleUI?.state) window.BattleUI.state.activeBattleRecordTab = activeTab;
          读取战斗记录终端节点()?.querySelectorAll('[data-battle-record-tab]').forEach(button => {
            const active = button.getAttribute('data-battle-record-tab') === activeTab;
            button.classList.toggle('active', active);
            button.setAttribute('aria-selected', active ? 'true' : 'false');
          });
          设置战斗记录展开状态(true);
          渲染战斗记录面板();
        }

        function 清空战斗预演面板() {
          if (window.BattleUI?.state) window.BattleUI.state.previewResult = null;
          渲染战斗记录面板();
        }

        function 读取轨迹类型(trace = {}) {
          return String(trace?.type || trace?.类型 || '').trim();
        }

        function 判定轨迹是目标规划(轨迹 = {}) {
          return /索敌规划|辅助目标规划/.test(读取轨迹类型(轨迹));
        }

        function 判定轨迹是战术动作(轨迹 = {}) {
          return /战术确立|主动规划|应招审计|再判定审计|换招审计/.test(读取轨迹类型(轨迹));
        }

        function 判定轨迹是行为链校验(轨迹 = {}) {
          return /应招审计|再判定审计|换招审计/.test(读取轨迹类型(轨迹));
        }

        function 归一判定轨迹(trace = {}) {
          const 类型 = 读取轨迹类型(trace);
          return {
            ...trace,
            类型,
            行动者: String(trace?.actor || trace?.行动者 || '').trim(),
            目标: String(trace?.target || trace?.目标 || '').trim(),
            技能: normalizeBattleActionDisplayName(trace?.skill || trace?.技能 || ''),
            回合: Number(trace?.round || trace?.回合 || 0),
            最终权重: Number(trace?.最终权重 || trace?.weight || 0),
            选择原因: String(trace?.选择原因 || trace?.reason || '').trim(),
            候选来源: String(trace?.候选来源 || trace?.source || '').trim(),
            resolutionType: String(trace?.resolutionType || trace?.决断类型 || '').trim(),
            skipReason: String(trace?.skipReason || trace?.静默原因 || '').trim(),
            roundPhase: String(trace?.roundPhase || trace?.阶段 || '').trim(),
            sourceActionName: normalizeBattleActionDisplayName(trace?.sourceActionName || trace?.反击动作来源 || ''),
            actionId: String(trace?.actionId || trace?.动作ID || '').trim(),
            sourceActionId: String(trace?.sourceActionId || trace?.来源动作ID || '').trim(),
            sourceActionType: String(trace?.sourceActionType || trace?.反击动作类型 || '').trim(),
            failReason: String(trace?.failReason || trace?.失败原因 || '').trim(),
            displayTargetName: String(trace?.displayTargetName || trace?.展示目标名 || '').trim(),
            目标语义: String(trace?.targetSemantics || trace?.目标语义 || trace?.目标类型 || '').trim(),
            承载方式: String(trace?.carryMode || trace?.承载方式 || '').trim(),
            hitCandidateName: normalizeBattleActionDisplayName(trace?.hitCandidateName || trace?.命中候选名 || ''),
            finalResolvedActionName: normalizeBattleActionDisplayName(trace?.finalResolvedActionName || trace?.最终落地动作 || ''),
            actionOverrideSource: String(trace?.actionOverrideSource || trace?.动作覆写来源 || '').trim(),
            战术确立来源: String(trace?.playerLockedSource || trace?.战术确立来源 || '').trim(),
          };
        }

        function 修正支援类判定目标名(轨迹 = {}, fallbackActor = '') {
          const target = String(轨迹?.displayTargetName || 轨迹?.目标 || '').trim();
          const actorName = String(轨迹?.行动者 || 轨迹?.actor || fallbackActor || '').trim();
          const intent = String(轨迹?.战略意图 || 轨迹?.intent || '').trim();
          const semantics = String(轨迹?.目标语义 || 轨迹?.targetSemantics || '').trim();
          if (!/治疗|保核|防守|驱散|辅助|支援/.test(`${intent} ${semantics}`)) return target;
          if (!target || (actorName && target !== actorName && !/我方|友方|自身|全体|前排|后排/.test(target))) return actorName || '自身';
          return target;
        }

        function 读取判定决断类型(轨迹 = {}) {
          const explicit = String(轨迹?.resolutionType || '').trim();
          if (explicit) return explicit;
          const 类型 = 读取轨迹类型(轨迹);
          const 原因 = String(轨迹?.选择原因 || '').trim();
          if (!/再判定审计|换招审计/.test(类型)) {
            if (轨迹?.动作校正?.发生校正 || (轨迹?.actionOverrideSource && 轨迹?.finalResolvedActionName)) return 'switch_action';
            return '';
          }
          const 再判定命中项 = 读取判定流程候选列表(轨迹, 0)
            .find(item => ['EXECUTED', 'LOCKED', 'SELECTED'].includes(读取候选状态(item, 轨迹))) || null;
          if (轨迹?.动作校正?.发生校正) return 'switch_action';
          if (再判定命中项) {
            if (读取候选名称(再判定命中项) === '放弃再判定') return 'keep_original';
            return 'switch_action';
          }
          if (/放弃再判定|维持原案|维持原定|未发现更优解/.test(原因)) return 'keep_original';
          if (/再判定/.test(类型)) return '';
          return '';
        }

        function 判定轨迹是静默辅助规划(轨迹 = {}) {
          const 类型 = 读取轨迹类型(轨迹);
          if (!/辅助目标规划/.test(类型)) return false;
          const actor = String(轨迹?.行动者 || '').trim();
          const target = String(轨迹?.目标 || '').trim();
          const 理由文本 = [
            轨迹?.选择原因,
            ...(Array.isArray(轨迹?.目标理由) ? 轨迹.目标理由 : []),
            ...(Array.isArray(轨迹?.前瞻理由) ? 轨迹.前瞻理由 : []),
            ...(Array.isArray(轨迹?.职责理由) ? 轨迹.职责理由 : []),
          ].join(' ');
          if (轨迹?.skipReason) return true;
          if (actor && target && isSameBattleReportName(actor, target) && /常规观察|观察|友方目标优先表命中/.test(理由文本)) return true;
          if (!String(轨迹?.技能 || '').trim() && 读取判定流程候选列表(轨迹, 0).length <= 0) return true;
          return false;
        }

        function 判定轨迹属于中间推演(轨迹 = {}) {
          if (判定轨迹是静默辅助规划(轨迹)) return true;
          if (!/主动规划/.test(读取轨迹类型(轨迹))) return false;
          if (轨迹?.动作校正?.发生校正) return false;
          const 技能 = normalizeBattleActionDisplayName(轨迹?.技能 || '');
          const 原因 = String(轨迹?.选择原因 || '').trim();
          const 候选数 = 读取判定流程候选列表(轨迹, 0).length;
          const 行动者 = String(轨迹?.行动者 || '').trim();
          const 回合 = Number(轨迹?.回合 || 轨迹?.round || 0);
          const 是真实后续主规划 =
            !!行动者 &&
            回合 >= 2 &&
            (
              候选数 > 0 ||
              /收招转防|战术观察|守势维持|伺机闪避|承伤硬抗|危机自保|防御|反敏攻压制|支援轮换|支援补位|稳态调整/.test(原因)
            );
          if (是真实后续主规划) return false;
          return (
            !技能 ||
            /无可用候选|未形成有效出手机会|当前未形成有效出手机会|常规方案收益过低|守势观察/.test(原因)
          );
        }

        function 读取目标规划理由列表(轨迹 = {}) {
          return [
            ...(Array.isArray(轨迹.目标理由) ? 轨迹.目标理由 : []),
            ...(Array.isArray(轨迹.前瞻理由) ? 轨迹.前瞻理由 : []),
            ...(Array.isArray(轨迹.职责理由) ? 轨迹.职责理由 : []),
          ]
            .map(item => 清洗判定侧写理由(item, 轨迹))
            .filter(Boolean)
            .filter((item, index, arr) => arr.indexOf(item) === index)
            .slice(0, 3);
        }

        function 判定目标规划为支援检索(轨迹 = {}) {
          const 类型 = 读取轨迹类型(轨迹);
          const 来源 = String(轨迹.候选来源 || '').trim();
          return /辅助目标规划|友方目标|辅助目标/.test(`${类型} ${来源}`) || 判定侧写是友方支援动作(轨迹);
        }

        function 构建目标规划局势文本(轨迹 = {}) {
          const 文本 = [
            ...读取目标规划理由列表(轨迹),
          ].join('；');
          if (判定目标规划为支援检索(轨迹)) {
            if (/资源|药剂|物品|恢复|回复/.test(文本)) return '己方资源需要重新调度，准备确认支援落点';
            if (/治疗|续航|保核|护盾|防护|护援|庇护/.test(文本)) return '己方防线承压，准备优先照看关键队友';
            return '正在核对己方支援落点';
          }
          if (/嘲讽强制目标/.test(文本)) return '受到嘲讽牵制，只能先盯住当前目标';
          if (/唯一有效敌方目标/.test(文本)) return '当前威胁已经明确，准备直接向其施压';
          if (/后排.*续航核心|治疗核心|续航核心/.test(文本)) return '对方后排续航暴露，准备先压住关键目标';
          if (/控制优先|控制|封技|打断/.test(文本)) return '对方节奏露出空当，准备先手打断';
          if (/蓄力高威胁|常规威胁|稳定威胁/.test(文本)) return '对方威胁持续抬升，准备先手压住攻势';
          return '正在按威胁与站位重新筛选目标';
        }

        function 构建目标规划检索文本(轨迹 = {}) {
          const 理由 = 读取目标规划理由列表(轨迹);
          if (理由.length) {
            const 前缀 = 判定目标规划为支援检索(轨迹) ? '优先核对' : '优先检查';
            return `${前缀}${理由.join('、')}`;
          }
          return 判定目标规划为支援检索(轨迹)
            ? '按当前战线压力核对己方支援对象'
            : '按威胁、距离与职责重新筛选对手';
        }

        function 构建目标规划决断文本(轨迹 = {}) {
          const target = String(轨迹.displayTargetName || 轨迹.目标 || '').trim();
          if (!target) return 判定目标规划为支援检索(轨迹) ? '暂未确认合适的支援对象。' : '暂未锁定合适的出手对象。';
          if (判定目标规划为支援检索(轨迹)) return `确认支援对象【${target}】。`;
          if (/嘲讽|TAUNT/i.test(String(轨迹.reasonCode || 轨迹.targetRuleCode || 轨迹.目标规则码 || '').trim())) return `被迫盯住对手【${target}】。`;
          return `锁定对手【${target}】。`;
        }

        function 构建判定侧写归一理由键(轨迹 = {}) {
          const normalized = 归一判定轨迹(轨迹);
          if (判定轨迹是目标规划(normalized)) {
            const 目标 = String(normalized.displayTargetName || normalized.目标 || '').trim();
            const 理由 = 读取目标规划理由列表(normalized)
              .filter(Boolean)
              .join('|');
            return `${读取轨迹类型(normalized)}::${目标}::${理由 || 'target-scan'}`;
          }
          const 目标 = String(normalized.displayTargetName || normalized.目标 || '').trim();
          return `${读取轨迹类型(normalized)}::${目标}::${读取判定流程过滤文本(normalized)}`;
        }

        function 判定轨迹静默目标遍历详情(轨迹 = {}) {
          const 文本 = [
            ...读取目标规划理由列表(轨迹),
          ].join('；');
          return /唯一有效敌方目标|唯一有效友方目标/.test(文本);
        }

        function 构建中间推演摘要(轨迹 = {}) {
          const normalized = 归一判定轨迹(轨迹);
          const target = String(normalized.displayTargetName || normalized.目标 || '').trim();
          const 过滤文本 = 读取判定流程过滤文本(normalized);
          if (判定轨迹是目标规划(normalized)) {
            return target ? `检索【${target}】后，未进入最终决断。` : '检索阶段未形成最终目标。';
          }
          if (target && /收益过低|未形成/.test(过滤文本)) return `评估【${target}】时，${过滤文本}。`;
          if (target) return `围绕【${target}】推演后，${过滤文本}。`;
          return `${过滤文本}。`;
        }

        function 读取候选名称(候选 = {}) {
          return normalizeBattleActionDisplayName(
            候选?.名称 ||
            候选?.skill?.name ||
            候选?.skill?.魂技名 ||
            候选?.__预览技能?.name ||
            候选?.__预览技能?.魂技名 ||
            候选?.name ||
            '',
          );
        }

        function 读取候选权重(候选 = {}) {
          return Number(候选?.权重 ?? 候选?.weight ?? 0);
        }

        function 读取候选状态(候选 = {}, 轨迹 = {}) {
          const explicit = String(候选?.candidateStatus || 候选?.状态 || 候选?.status || '').trim().toUpperCase();
          if (['SCORED', 'SAMPLED', 'LOCKED', 'EXECUTED', 'REJECTED', 'FILTERED', 'SHADOWED', 'ABORTED', 'SELECTED'].includes(explicit)) return explicit;
          return '';
        }

        function 读取候选否决码(候选 = {}, 轨迹 = {}) {
          const raw = String(候选?.rejectionCode || 候选?.否决码 || 候选?.reasonCode || '').trim();
          if (raw) return raw;
          const status = 读取候选状态(候选, 轨迹);
          if (status === 'FILTERED') return 'FILTERED_BY_SCORE';
          if (status === 'REJECTED') return 'LOWER_PRIORITY';
          return '';
        }

        function 映射候选否决原因(code = '') {
          const key = String(code || '').trim().toUpperCase();
          const map = {
            OUT_OF_MANA: '魂力不足',
            COOLDOWN: '冷却未转好',
            TARGET_INVALID: '目标非法',
            INTENT_TARGET_MISMATCH: '意图与目标不匹配',
            RANGE_INVALID: '距离不满足',
            CAP_REACHED: '达到上限',
            CONTROL_GAP: '无法限制后续反扑',
            RESOURCE_PRESSURE: '资源压力过高',
            PROTECT_ALLY_GAP: '掩护价值不足',
            LETHAL_GAP: '终结收益不足',
            DIRECT_PRESSURE_GAP: '当前压制收益不足',
            LOWER_PRIORITY: '优先级不足',
            REPLACED_BY_MANUAL_ACTION: '被手动动作覆盖',
            FILTERED_BY_SCORE: '评分未达有效阈值',
          };
          return map[key] || (key ? `未知规则限制(CODE: ${key})` : '');
        }

        function 包裹判定动作名称(名称 = '') {
          const text = normalizeBattleActionDisplayName(名称);
          return text ? `【${text.replace(/^【|】$/g, '')}】` : '';
        }

        function 判定无反应动作名(名称 = '') {
          return /^(?:无暇反应|无法反应|无反应|来不及反应)$/i.test(normalizeBattleActionDisplayName(名称));
        }

        function 判定可显示为原定反应动作(名称 = '') {
          const actionName = normalizeBattleActionDisplayName(名称);
          if (!actionName || 判定无反应动作名(actionName)) return false;
          return /^(?:敌方截击|敌方压迫|承伤硬抗|肉体兜底|伺机闪避|闪避|防御|偏转|收招转防|借力守势|坚壁|抢落点|压招|短前摇对轰)$/.test(actionName);
        }

        function 格式化判定候选名称(名称 = '') {
          return 包裹判定动作名称(名称);
        }

        function 格式化玩家判定结果(value = '') {
          const raw = String(value || '').trim();
          if (!raw) return '';
          const mapped = 映射玩家态Outcome文本(raw);
          if (mapped) return mapped;
          if (/counter_secondary_reaction/i.test(raw)) return '反防反应';
          if (/counter_reaction/i.test(raw)) return '防反应对';
          if (/replanned/i.test(raw)) return '动作调整完成';
          if (/guarded[_\s-]+hit|hit[_\s-]+guarded/i.test(raw)) return '防守后承击';
          if (/^(dodged|evaded|dodge_success|miss)$/i.test(raw)) return '规避成功';
          if (/^(failed|fail|reaction_failed)$/i.test(raw)) return '未能截断，转为承击';
          if (/^(attempted)$/i.test(raw)) return '尝试应对';
          if (/^(guarded|blocked|defended)$/i.test(raw)) return '防守成立';
          if (/^(hit|damaged|success|applied|opened)$/i.test(raw)) return '判定成功';
          if (/^(resisted|immune|immunity)$/i.test(raw)) return /immune/i.test(raw) ? '免疫' : '抵抗成功';
          if (/^(critical)$/i.test(raw)) return '关键事件';
          return raw.replace(/_/g, ' ');
        }

        function 映射玩家态Outcome文本(value = '') {
          const raw = String(value || '').trim();
          if (!raw) return '';
          const key = raw.toLowerCase().replace(/[\s-]+/g, '_');
          const map = {
            no_effect: '未产生结算变化',
            dodged: '规避成功',
            evaded: '规避成功',
            dodge_success: '规避成功',
            interrupted: '动作截断',
            reaction_failed: '应对失败（未能截断，转为承击）',
            failed: '应对失败（未能截断，转为承击）',
            fail: '应对失败（未能截断，转为承击）',
            state_applied: '状态生效',
            control_applied: '状态生效',
            state_resisted: '状态化解',
            control_resisted: '状态化解',
            state_immune: '状态免疫',
            control_immune: '状态免疫',
            guarded: '防守成立',
            blocked: '防守成立',
            full_hit: '正面命中',
            graze: '擦伤',
            critical: '关键事件',
            action_committed: '动作成立',
            resource_recovered: '资源回稳',
            cap_reached: '达到上限',
            no_valid_window: '暂未展开',
          };
          return map[key] || '';
        }

        function 格式化反应判定结果(dto = {}) {
          const raw = String(dto.result || dto.outcome || '').trim();
          const key = raw.toLowerCase().replace(/[\s-]+/g, '_');
          const actionName = normalizeBattleActionDisplayName(dto.finalReactionName || '');
          if (key === 'no_effect') {
            if (/伺机闪避|闪避|规避/.test(actionName)) return '规避未能生效';
            if (/承伤硬抗|肉体兜底|防御|坚壁|偏转|收招转防|借力守势/.test(actionName)) return '防守未能改变承击结果';
            if (/敌方压迫|压招|抢落点|短前摇对轰|范围压制|反压/.test(actionName)) return '未能截断来袭';
            return '未能改变本次应对结果';
          }
          return 映射玩家态Outcome文本(raw) || 格式化玩家判定结果(raw);
        }

        function 格式化玩家反应类型(value = '') {
          const raw = String(value || '').trim();
          if (!raw) return '';
          const map = {
            counter_secondary_reaction: '反防反应',
            counter_reaction: '防反应对',
            agile_interrupt: '敏攻截断',
            dodge: '闪避应对',
            guard: '防御应对',
            parry: '偏转应对',
            no_reaction: '节奏受压',
            无法反应: '节奏受压',
          };
          return map[raw] || raw.replace(/_/g, ' ');
        }

        function 清洗玩家可见判定文本(line = '') {
          return String(line || '')
            .replace(/\b(counter_secondary_reaction|counter_reaction|guarded_hit|hit_guarded|replanned|reaction_failed|failed|dodged|evaded|dodge_success|guarded|blocked|defended|critical|no[_\s-]+effect|interrupted|state_applied|state_resisted|state_immune)\b/gi, match => 格式化玩家判定结果(match));
        }

        function 转义正则文本(text = '') {
          return String(text || '').replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
        }

        function 渲染判定侧写HTML(line = '', 轨迹 = {}) {
          const safeLine = 清洗玩家可见判定文本(line);
          let html = htmlEscapeText(safeLine).replace(/\r?\n/g, '<br>');
          const entities = [
            { className: 'battle-preview-trace-actor', value: 轨迹.行动者 },
            { className: 'battle-preview-trace-target', value: 轨迹.目标 || 轨迹.实际目标 },
          ]
            .filter(item => String(item.value || '').trim())
            .sort((a, b) => String(b.value).length - String(a.value).length);
          entities.forEach(item => {
            html = html.replace(new RegExp(转义正则文本(htmlEscapeText(item.value)), 'g'), `<span class="${item.className}">${htmlEscapeText(item.value)}</span>`);
          });
          html = html.replace(/【([^】]+)】/g, '<span class="battle-preview-trace-skill">【$1】</span>');
          html = html.replace(/^(├─|└─)/, '<span class="battle-preview-trace-muted">$1</span>');
          if (/最终|确认|转入|执行|完成|造成|召出|命中|落地/.test(safeLine)) html = `<span class="battle-preview-trace-decision">${html}</span>`;
          return html;
        }

        function 构建CandidatePoolDTO(trace = {}) {
          return 读取判定流程候选列表(trace, 4).map(item => ({
            name: 读取候选名称(item),
            score: Math.round(读取候选权重(item)),
            status: 读取候选状态(item, trace || {}),
            explicitStatus: String(item?.candidateStatus || item?.状态 || item?.status || '').trim().toUpperCase(),
            rejectionCode: 读取候选否决码(item, trace || {}),
            repeatCount: Number(item?.repeatCount || 0),
            repeatCategory: String(item?.repeatCategory || ''),
            repeatDecayMultiplier: Number(item?.repeatDecayMultiplier || 1),
            repeatDecayReasonCode: String(item?.repeatDecayReasonCode || ''),
            repeatExceptionCode: String(item?.repeatExceptionCode || ''),
            scoreBeforeRepeatDecay: Number(item?.scoreBeforeRepeatDecay || 0),
            scoreAfterRepeatDecay: Number(item?.scoreAfterRepeatDecay || 0),
            effectTags: Array.isArray(item?.effectTags) ? item.effectTags.map(tag => String(tag || '').trim()).filter(Boolean) : [],
            rejectedByEffectGap: String(item?.rejectedByEffectGap || '').trim(),
          }));
        }

        function 构建NarrativeDecisionDTO(trace = {}, options = {}) {
          const sourceTrace = trace && typeof trace === 'object' ? trace : {};
          补写NarrativeDecisionSidecar(sourceTrace);
          const sidecar = sourceTrace.__narrativeDecisionSidecar && typeof sourceTrace.__narrativeDecisionSidecar === 'object'
            ? sourceTrace.__narrativeDecisionSidecar
            : {};
          const normalized = 归一判定轨迹(sourceTrace);
          const candidates = 构建CandidatePoolDTO(normalized);
          const finalActionName = normalizeBattleActionDisplayName(normalized.finalResolvedActionName || normalized.技能 || normalized.hitCandidateName || '');
          const selected = candidates.find(item => ['EXECUTED', 'LOCKED', 'SELECTED'].includes(item.explicitStatus || '')) || null;
          const rejectedAlternatives = candidates
            .filter(item => item.name && item.name !== finalActionName)
            .sort((a, b) => Number(b.score || 0) - Number(a.score || 0))
            .slice(0, 2)
            .map(item => ({
              actionName: item.name,
              score: Number(item.score || 0),
              reasonCode: item.rejectionCode || '',
              reasonText: 映射候选否决原因(item.rejectionCode) || '',
              effectTags: item.effectTags,
              rejectedByEffectGap: item.rejectedByEffectGap,
            }));
          const narrationTrustLevel = String(sidecar.narrationTrustLevel || '').trim() || 'FACT_ONLY';
          const formulaTrustLevel = String(sidecar.formulaTrustLevel || '').trim() || 'MISSING_OPERAND';
          const fatalCodes = Array.isArray(sidecar.fatalCodes) ? sidecar.fatalCodes.slice() : [];
          const dominantReason = String(sidecar.dominantReason || '').trim();
          return {
            __dtoKind: 'NarrativeDecisionDTO',
            actorName: String(normalized.行动者 || options.actorName || '行动者').trim(),
            targetName: String(normalized.displayTargetName || normalized.目标 || options.targetName || '目标').trim(),
            finalActionName,
            selectedCandidateName: selected?.name || finalActionName,
            rejectedAlternatives,
            dominantReason,
            tieBreakReason: dominantReason === 'TIE_BREAK' ? '战术重心破平' : '',
            narrationTrustLevel,
            formulaTrustLevel,
            fatalCodes,
          };
        }

        function formatTacticalNarration(dto = {}) {
          if (!dto || dto.__dtoKind !== 'NarrativeDecisionDTO') return '';
          if (!dto || dto.narrationTrustLevel !== 'TRUSTED') return '';
          const alt = dto.rejectedAlternatives?.[0] || null;
          if (!alt?.actionName || !String(alt?.reasonCode || alt?.rejectedByEffectGap || '').trim()) return '';
          const actorName = dto.actorName || '行动者';
          const targetName = dto.targetName || '目标';
          const finalActionName = dto.finalActionName || '当前动作';
          const altText = alt?.actionName ? `【${alt.actionName}】` : '另一套方案';
          const altReasonCode = String(alt?.reasonCode || '').trim().toUpperCase();
          const 取舍句 = (() => {
            if (!alt?.actionName) return '';
            if (dto.dominantReason === 'CONTROL') {
              return altReasonCode === 'CONTROL_GAP' || alt?.rejectedByEffectGap === 'CONTROL_RESTRICTION_GAP'
                ? `${altText}的直接收益接近，却无法限制${targetName}后续反扑；`
                : '';
            }
            if (dto.dominantReason === 'RESOURCE_PRESSURE') {
              return altReasonCode === 'RESOURCE_PRESSURE'
                ? `${altText}压制更重，却会拖空后续魂力；`
                : '';
            }
            if (dto.dominantReason === 'PROTECT_ALLY') {
              return altReasonCode === 'PROTECT_ALLY_GAP'
                ? `${altText}难以及时补上己方防线缺口；`
                : '';
            }
            if (dto.dominantReason === 'LETHAL') {
              return altReasonCode === 'LETHAL_GAP'
                ? `${altText}仍留余地，难以把握眼前终结窗口；`
                : '';
            }
            if (dto.dominantReason === 'DIRECT_PRESSURE') {
              return altReasonCode === 'DIRECT_PRESSURE_GAP'
                ? `${altText}只能牵制一时，正面推进力度不足；`
                : '';
            }
            return '';
          })();
          if (dto.dominantReason === 'TIE_BREAK') {
            return `战术侧写：局势焦灼之下，${altText}与【${finalActionName}】收益相近，${actorName}最终按战术重心抢出【${finalActionName}】，把压力压向${targetName}。`;
          }
          if (!取舍句) return '';
          if (dto.dominantReason === 'RESOURCE_PRESSURE') {
            return `战术侧写：${取舍句}${actorName}转以【${finalActionName}】稳住局面，避免下一轮断档。`;
          }
          if (dto.dominantReason === 'PROTECT_ALLY') {
            return `战术侧写：${actorName}捕捉到战线缺口，${取舍句}改以【${finalActionName}】介入局面，为己方争回喘息空间。`;
          }
          if (dto.dominantReason === 'LETHAL') {
            return `战术侧写：察觉${targetName}已露颓势，${取舍句}${actorName}顺势施展【${finalActionName}】逼向终结。`;
          }
          if (dto.dominantReason === 'CONTROL') {
            return `战术侧写：${取舍句}${actorName}因此改以【${finalActionName}】封锁退路，试图接管节奏。`;
          }
          if (dto.dominantReason === 'DIRECT_PRESSURE') {
            return `战术侧写：${取舍句}${actorName}改以【${finalActionName}】压住${targetName}的身位，稳稳推进战线。`;
          }
          return '';
        }

        function 渲染判定候选对比HTML(轨迹 = {}) {
          if (判定轨迹是目标规划(轨迹) || 读取轨迹类型(轨迹) === '防反机制') return '';
          if (轨迹.动作校正?.发生校正) return '';
          if (/主动规划|战术确立/.test(读取轨迹类型(轨迹))) {
            const dto = 构建NarrativeDecisionDTO(轨迹);
            if (!dto || dto.narrationTrustLevel !== 'TRUSTED') return '';
          }
          const 最终动作 = normalizeBattleActionDisplayName(轨迹.finalResolvedActionName || 轨迹.实际技能 || 轨迹.技能 || '');
          const 覆写来源 = String(轨迹.actionOverrideSource || '').trim();
          const candidates = 读取判定流程候选列表(轨迹, 3);
          const 候选包含最终动作 = !!(最终动作 && candidates.some(item => 读取候选名称(item) === 最终动作));
          const 最终为非敌对动作 =
            判定侧写是自我应对动作({ ...轨迹, 技能: 最终动作, 实际技能: 最终动作 }) ||
            /战术观察|待机|守势维持|守势对峙/.test(最终动作);
          if (
            最终为非敌对动作 &&
            (覆写来源 || !候选包含最终动作)
          ) return '';
          if (candidates.length < 2) return '';
          const 命中技能 = normalizeBattleActionDisplayName(轨迹.hitCandidateName || 轨迹.技能 || 轨迹.实际技能 || 轨迹.动作校正?.实际技能 || '');
          const 命中标签原文 = String(轨迹.最终命中标签 || '').trim();
          const 是再判定策略候选 = name => /抢落点|压招|偏转|放弃再判定|换招/.test(normalizeBattleActionDisplayName(name || ''));
          const 命中标签 = 命中标签原文 === '最终命中'
            ? (是再判定策略候选(命中技能) && 命中技能 !== 最终动作 ? '复核命中' : 命中标签原文)
            : '';
          return `<div class="battle-preview-trace-candidate"><div class="battle-preview-trace-candidate-head">候选取舍</div>${candidates.map(item => {
            const name = 读取候选名称(item);
            const status = 读取候选状态(item, 轨迹);
            const isChosen = ['EXECUTED', 'LOCKED', 'SELECTED'].includes(status);
            const label = isChosen
              ? `${包裹判定动作名称(name)} 已锁定`
              : `${包裹判定动作名称(name)} 未成为本轮主轴`;
            return `<div class="battle-preview-trace-candidate-line"><span class="battle-preview-trace-candidate-label">${渲染判定侧写HTML(label, 轨迹)}</span>${isChosen && 命中标签 ? ` <span class="battle-preview-trace-hit-tag">${htmlEscapeText(命中标签)}</span>` : ''}</div>`;
          }).join('')}</div>`;
        }

        function 读取判定流程候选列表(轨迹 = {}, limit = 5) {
          const sorted = (Array.isArray(轨迹.候选排序结果) ? 轨迹.候选排序结果 : [])
            .slice()
            .filter(候选 => 读取候选名称(候选))
            .sort((左, 右) => 读取候选权重(右) - 读取候选权重(左));
          if (Number(limit) === 0) return sorted;
          return sorted.slice(0, Math.max(1, Number(limit || 5)));
        }

        function 格式化审计证据值(value) {
          if (value == null || value === '') return '';
          if (typeof value === 'string') return value;
          try { return JSON.stringify(value, null, 2); }
          catch (_) { return String(value); }
        }

        function 补齐判定轨迹回合(轨迹 = {}, 已有码条目 = [], 向后条目 = []) {
          const 既有回合 = Number(轨迹?.回合 || 轨迹?.round || 轨迹?.实际回合 || 0);
          if (既有回合 > 0) return 既有回合;
          const actor = String(轨迹?.行动者 || '').trim();
          if (!actor) return 0;
          const 近邻池 = [...已有码条目].reverse().concat(向后条目);
          const 近邻 = 近邻池.find(item => {
            const next = 归一判定轨迹(item || {});
            return String(next?.行动者 || '').trim() === actor && Number(next?.回合 || next?.round || next?.实际回合 || 0) > 0;
          });
          return Number(近邻?.回合 || 近邻?.round || 近邻?.实际回合 || 0);
        }

        function 构建判定流程归并键(轨迹 = {}) {
          const normalized = 归一判定轨迹(轨迹);
          const actor = String(normalized.行动者 || '').trim();
          const round = Number(normalized.回合 || 0);
          const 类型 = 读取轨迹类型(normalized);
          if (!actor || !类型) return '';
          if (类型 === '战术确立') return `${round}::${actor}::locked`;
          if (/主动规划/.test(类型)) return `${round}::${actor}::active`;
          if (/应招审计/.test(类型)) return `${round}::${actor}::reaction`;
          if (/再判定审计/.test(类型)) return `${round}::${actor}::reeval`;
          if (/换招审计/.test(类型)) return `${round}::${actor}::switch`;
          if (判定轨迹是目标规划(normalized)) return `${round}::${actor}::${/辅助目标规划/.test(类型) ? 'ally-target' : 'enemy-target'}`;
          return `${round}::${actor}::${类型}`;
        }

        function 读取判定流程分段键(轨迹 = {}) {
          const normalized = 归一判定轨迹(轨迹);
          const 类型 = 读取轨迹类型(normalized);
          if (类型 === '防反机制') return 'counter';
          if (判定轨迹是目标规划(normalized) || /战术确立|主动规划/.test(类型)) return 'decision';
          if (/应招审计|再判定审计|换招审计/.test(类型)) return 'reaction';
          return 'decision';
        }

        function 补写NarrativeDecisionSidecar(轨迹 = {}) {
          if (!轨迹 || typeof 轨迹 !== 'object') return;
          const normalized = 归一判定轨迹(轨迹);
          if (!/主动规划|战术确立/.test(读取轨迹类型(normalized))) return;
          const candidates = 读取判定流程候选列表(normalized, 0);
          const finalActionName = normalizeBattleActionDisplayName(normalized.finalResolvedActionName || normalized.技能 || normalized.hitCandidateName || '');
          const fatalCodes = [];
          if (!finalActionName || !String(normalized.行动者 || '').trim()) fatalCodes.push('INVALID_ACTION_FACT');
          if (finalActionName && candidates.length && !candidates.some(item => 读取候选名称(item) === finalActionName)) fatalCodes.push('CANDIDATE_FINAL_MISMATCH');
          if (candidates.some(item => !String(item?.candidateStatus || item?.状态 || item?.status || '').trim())) fatalCodes.push('NARRATION_CANDIDATE_STATUS_MISSING');
          if (candidates.some(item => ['SELECTED', 'LOCKED', 'EXECUTED'].includes(读取候选状态(item, normalized)) && 读取候选否决码(item, normalized))) fatalCodes.push('CANDIDATE_SELECTED_HAS_REJECTION_CODE');
          if (/回填|最终落点|执行声明|终态回填|系统/i.test(String(normalized.actionOverrideSource || ''))) fatalCodes.push('CANDIDATE_FINAL_MISMATCH');
          const terms = [
            Number(normalized.原始权重 || normalized.原始净收益 || 0),
            Number(normalized.战术修正 || 0),
            Number(normalized.目标修正 || 0) + Number(normalized.目标价值修正 || 0),
            Number(normalized.资源修正 || 0),
            Number(normalized.团队意图修正 || 0),
            Number(normalized.二回合前瞻修正 || 0),
            Number(normalized.职责修正 || 0),
          ].filter(value => Number.isFinite(value) && Math.abs(value) >= 1);
          const finalWeight = Number(normalized.最终权重 || 0);
          let formulaTrustLevel = 'TRUSTED';
          if (!Number.isFinite(finalWeight) || (terms.length < 2 && Math.abs(finalWeight) >= 1)) {
            formulaTrustLevel = 'MISSING_OPERAND';
            fatalCodes.push('FATAL_WEIGHT_FORMULA_MISSING_OPERAND');
          } else if (Math.abs(Math.round(finalWeight - terms.reduce((sum, value) => sum + value, 0))) > 1) {
            formulaTrustLevel = 'HIDDEN_UNBALANCED';
            fatalCodes.push('FATAL_WEIGHT_FORMULA_UNBALANCED');
          }
          const candidateDtos = candidates.map(item => ({
            name: 读取候选名称(item),
            score: Number(读取候选权重(item) || 0),
            status: 读取候选状态(item, normalized),
            rejectionCode: 读取候选否决码(item, normalized),
            effectTags: Array.isArray(item?.effectTags) ? item.effectTags.map(tag => String(tag || '').trim()).filter(Boolean) : [],
            rejectedByEffectGap: String(item?.rejectedByEffectGap || '').trim(),
            candidateSource: String(item?.candidateSource || item?.候选来源 || '').trim(),
          }));
          const selected = candidateDtos.find(item => ['EXECUTED', 'LOCKED', 'SELECTED'].includes(item.status)) || null;
          if (!selected && candidates.length) fatalCodes.push('CANDIDATE_SELECTED_MISSING');
          if (selected?.name && finalActionName && selected.name !== finalActionName) fatalCodes.push('CANDIDATE_SELECTED_FINAL_MISMATCH');
          const rejectedAlternatives = candidateDtos
            .filter(item => item.name && item.name !== finalActionName)
            .sort((a, b) => Number(b.score || 0) - Number(a.score || 0))
            .slice(0, 2);
          const traceSource = String(normalized.候选来源 || normalized.source || '').trim();
          const syntheticCandidateChain = /结构化动作事实/.test(traceSource) ||
            candidateDtos.some(item => /SYNTHETIC_BASELINE|EXECUTED_ACTION_FACT/.test(item.candidateSource));
          if (syntheticCandidateChain) fatalCodes.push('NARRATION_SYNTHETIC_CANDIDATE_CHAIN');
          const finalScore = Number(normalized.最终权重 || selected?.score || 0);
          const tieCandidate = rejectedAlternatives.find(item => Math.abs(Number(item.score || 0) - finalScore) <= 1 && finalScore > 0);
          const selectedEffectTags = Array.isArray(selected?.effectTags) ? selected.effectTags : [];
          const topAlternativeEffectTags = Array.isArray(rejectedAlternatives[0]?.effectTags) ? rejectedAlternatives[0].effectTags : [];
          const topAlternativeReasonCode = String(rejectedAlternatives[0]?.rejectionCode || '').trim().toUpperCase();
          const targetBreakdown = normalized.目标价值分解 && typeof normalized.目标价值分解 === 'object' ? normalized.目标价值分解 : {};
          const controlDominates = selectedEffectTags.includes('CONTROL_RESTRICTION') &&
            !topAlternativeEffectTags.includes('CONTROL_RESTRICTION') &&
            (topAlternativeReasonCode === 'CONTROL_GAP' || rejectedAlternatives[0]?.rejectedByEffectGap === 'CONTROL_RESTRICTION_GAP');
          const lethalDominates = selectedEffectTags.some(tag => /^(?:LETHAL|EXECUTE|FINISH|KILL_WINDOW)$/.test(tag)) ||
            topAlternativeReasonCode === 'LETHAL_GAP' ||
            Number(targetBreakdown.lethalWindow || targetBreakdown.killWindow || targetBreakdown.finishWindow || 0) > 0;
          const resourceDominates = Number(normalized.资源修正 || 0) <= -12 ||
            topAlternativeReasonCode === 'RESOURCE_PRESSURE' ||
            selectedEffectTags.includes('RESOURCE_STABLE');
          const protectDominates = selectedEffectTags.some(tag => /^(?:PROTECT_ALLY|EMERGENCY_HEAL|GUARD_ALLY|ALLY_COVER)$/.test(tag)) ||
            topAlternativeReasonCode === 'PROTECT_ALLY_GAP' ||
            Number(normalized.职责修正 || 0) > 0;
          const directPressureDominates = selectedEffectTags.includes('DIRECT_DAMAGE') &&
            (topAlternativeReasonCode === 'DIRECT_PRESSURE_GAP' || rejectedAlternatives[0]?.rejectedByEffectGap === 'DIRECT_PRESSURE_GAP');
          let dominantReason = 'DIRECT_PRESSURE';
          if (tieCandidate) dominantReason = 'TIE_BREAK';
          else if (controlDominates) dominantReason = 'CONTROL';
          else if (lethalDominates) dominantReason = 'LETHAL';
          else if (resourceDominates) dominantReason = 'RESOURCE_PRESSURE';
          else if (protectDominates) dominantReason = 'PROTECT_ALLY';
          const missingAlternative = !rejectedAlternatives.length || !rejectedAlternatives[0].rejectionCode;
          if (missingAlternative) fatalCodes.push('NARRATION_MISSING_ALTERNATIVE_REASON');
          const reactionAlternativePollution = rejectedAlternatives.some(item => 判定可显示为原定反应动作(item.name) || 判定守势待机动作(item.name));
          if (reactionAlternativePollution) fatalCodes.push('NARRATION_REACTION_ALTERNATIVE_POLLUTION');
          const genericAlternativeReason = !rejectedAlternatives[0]?.rejectionCode ||
            /^(?:LOWER_PRIORITY|FILTERED_BY_SCORE)$/.test(String(rejectedAlternatives[0]?.rejectionCode || '').trim().toUpperCase()) ||
            (!rejectedAlternatives[0]?.rejectedByEffectGap && dominantReason !== 'TIE_BREAK');
          if (genericAlternativeReason) fatalCodes.push('NARRATION_GENERIC_ALTERNATIVE_REASON');
          if (dominantReason === 'DIRECT_PRESSURE' && !directPressureDominates) fatalCodes.push('NARRATION_DIRECT_PRESSURE_UNEXPLAINED');
          if (dominantReason === 'CONTROL' && !controlDominates) fatalCodes.push('NARRATION_UNVERIFIED_CONTROL_GAP');
          const finalTrustLevel = fatalCodes.includes('INVALID_ACTION_FACT')
            ? 'INVALID'
            : (fatalCodes.length || formulaTrustLevel !== 'TRUSTED')
              ? 'FACT_ONLY'
              : 'TRUSTED';
          const uniqueFatalCodes = fatalCodes.filter((code, index, arr) => arr.indexOf(code) === index);
          轨迹.__narrativeDecisionSidecar = {
            narrationTrustLevel: finalTrustLevel,
            formulaTrustLevel,
            fatalCodes: uniqueFatalCodes,
            dominantReason: finalTrustLevel === 'TRUSTED' ? dominantReason : '',
          };
        }

        function 构建判定流程展示数据(traceList = [], logs = [], context = {}) {
          const rawList = Array.isArray(traceList) ? traceList : [];
          rawList.forEach(trace => {
            if (!trace || typeof trace !== 'object') return;
            const type = 读取轨迹类型(trace);
            if (!/主动规划|战术确立/.test(type)) return;
            补写NarrativeDecisionSidecar(trace);
          });
          const 事件账本 = Array.isArray(context?.eventLedger)
            ? context.eventLedger
            : (Array.isArray(context?.combatData?.__battleEventLedger) ? context.combatData.__battleEventLedger : []);
          const 账本动作落地键集合 = new Set();
          const 账本行动者回合集合 = new Set();
          const 公开战报动作键集合 = new Set();
          const 账本落地事件类型 = new Set(['hit_result', 'state_apply', 'state_tick', 'resource_change', 'defend', 'dodge', 'pass', 'counter', 'create', 'summon_create', 'summon_assist', 'shield_create', 'blocked_action', 'failed_action', 'target_fail']);
          (Array.isArray(事件账本) ? 事件账本 : []).forEach(event => {
            if (!event || typeof event !== 'object') return;
            if (!账本落地事件类型.has(String(event.eventKind || '').trim())) return;
            const round = Number(event.round || event.sourceRound || 0);
            const actor = String(event.actorName || event.actor || '').trim();
            if (!(round > 0) || !actor) return;
            账本行动者回合集合.add(`${round}::${actor}`);
            [
              event.actionName,
              event.sourceActionName,
              event.meta?.actionName,
              event.meta?.sourceActionName,
            ].map(name => normalizeBattleActionDisplayName(name || '')).filter(Boolean).forEach(action => {
              账本动作落地键集合.add(`${round}::${actor}::${action}`);
            });
          });
          const 公开战报行 = Array.isArray(context?.publicBattleLines)
            ? context.publicBattleLines
            : (事件账本.length ? 构建事件账本公开战报Block文本行(事件账本, 24, context) : []);
          公开战报行.forEach(line => {
            const text = String(line || '').trim();
            const round = Number(text.match(/^第(\d+)回合：/)?.[1] || 0);
            if (!(round > 0)) return;
            const body = text.replace(/^第\d+回合：/, '');
            for (const match of body.matchAll(/([^，。；\s]+?)施展【([^】]+)】/g)) {
              const actor = String(match[1] || '').trim();
              const action = normalizeBattleActionDisplayName(match[2] || '');
              if (actor && action) 公开战报动作键集合.add(`${round}::${actor}::${action}`);
            }
            for (const match of body.matchAll(/随后([^，。；\s]+?)抓住[^，。；]+，以【([^】]+)】进行反击/g)) {
              const actor = String(match[1] || '').trim();
              const action = normalizeBattleActionDisplayName(match[2] || '');
              if (actor && action) 公开战报动作键集合.add(`${round}::${actor}::${action}`);
            }
            for (const match of body.matchAll(/([^，。；\s]+?)的【([^】]+)】本回合对抗结束/g)) {
              const actor = String(match[1] || '').trim();
              const action = normalizeBattleActionDisplayName(match[2] || '');
              if (actor && action) 公开战报动作键集合.add(`${round}::${actor}::${action}`);
            }
          });
          const 判定轨迹有账本落地 = trace => {
            const normalized = 归一判定轨迹(trace || {});
            const round = Number(normalized.回合 || normalized.round || 0);
            const actor = String(normalized.行动者 || '').trim();
            const action = normalizeBattleActionDisplayName(normalized.finalResolvedActionName || normalized.技能 || '');
            if (!(round > 0) || !actor) return true;
            if (!事件账本.length) return true;
            if (normalized.actionId) {
              return (Array.isArray(事件账本) ? 事件账本 : []).some(event =>
                String(event?.actionId || '') === normalized.actionId ||
                String(event?.sourceActionId || '') === normalized.actionId
              );
            }
            if (action && 公开战报动作键集合.size && 公开战报动作键集合.has(`${round}::${actor}::${action}`)) return true;
            if (action && 公开战报动作键集合.size && !公开战报动作键集合.has(`${round}::${actor}::${action}`)) return false;
            if (action && 账本动作落地键集合.has(`${round}::${actor}::${action}`)) return true;
            if (!action && 账本行动者回合集合.has(`${round}::${actor}`)) return true;
            return false;
          };
          const 绑定判定轨迹到账本动作 = trace => {
            const normalized = 归一判定轨迹(trace || {});
            const originalAction = normalizeBattleActionDisplayName(normalized.技能 || '');
            const finalAction = normalizeBattleActionDisplayName(normalized.finalResolvedActionName || '');
            const resolved = finalAction && finalAction !== originalAction
              ? {
                  ...normalized,
                  技能: finalAction,
                  审计技能: originalAction,
                  实际技能: finalAction,
                  实际执行动作: finalAction,
                  actionOverrideSource: String(normalized.actionOverrideSource || '最终落点').trim(),
                }
              : normalized;
            if (resolved.actionId) return resolved;
            const round = Number(normalized.回合 || normalized.round || 0);
            const actor = String(normalized.行动者 || '').trim();
            const action = normalizeBattleActionDisplayName(resolved.finalResolvedActionName || resolved.技能 || '');
            if (!(round > 0) || !actor || !action || !Array.isArray(事件账本) || !事件账本.length) return resolved;
            const event = 事件账本.find(item =>
              item &&
              Number(item.round || item.sourceRound || 0) === round &&
              isSameBattleReportName(String(item.actorName || '').trim(), actor) &&
              [item.actionName, item.sourceActionName, item.meta?.actionName, item.meta?.sourceActionName]
                .map(name => normalizeBattleActionDisplayName(name || ''))
                .includes(action)
            );
            if (!event) return resolved;
            return {
              ...resolved,
              actionId: String(event.actionId || event.sourceActionId || '').trim(),
              sourceActionId: String(event.sourceActionId || '').trim(),
            };
          };
          const 判定轨迹需要账本门禁 = trace => {
            const normalized = 归一判定轨迹(trace || {});
            const 类型 = 读取轨迹类型(normalized);
            if (!/主动规划|应招审计|再判定审计|换招审计/.test(类型)) return false;
            if (判定侧写是自我应对动作(normalized)) return false;
            const targetSemantics = String(normalized.目标语义 || '').trim();
            const action = normalizeBattleActionDisplayName(normalized.finalResolvedActionName || normalized.技能 || '');
            const target = String(normalized.目标 || normalized.displayTargetName || '').trim();
            return /敌方|hostile/.test(targetSemantics) || (!!target && !/收招转防|承伤硬抗|肉体兜底|伺机闪避|防御|观察|待机/.test(action));
          };
          const rows = [];
          const 玩家侧名称集合 = new Set(
            [
              ...(Array.isArray(context?.combatData?.参战者?.team_player) ? context.combatData.参战者.team_player : []),
              ...(Array.isArray(context?.units) ? context.units.filter(unit => unit?.阵营 === '玩家') : []),
            ]
              .map(unit => String(unit?.name || unit?.名称 || '').trim())
              .filter(Boolean)
          );
          const 轨迹展示优先级 = 轨迹 => {
            const 类型 = 读取轨迹类型(轨迹);
            let score = 0;
            if (判定轨迹是目标规划(轨迹)) score += 10;
            if (类型 === '战术确立') score += 28;
            if (类型 === '主动规划') score += 30;
            if (类型 === '应招审计') score += 36;
            if (类型 === '换招审计') score += 38;
            if (类型 === '再判定审计') score += 34;
            if (轨迹?.动作校正?.发生校正) score += 8;
            if (String(轨迹?.技能 || '').trim()) score += 8;
            if (Number(轨迹?.最终权重 || 0) > 0) score += 4;
            return score;
          };
          const 归并附注 = (载体, 轨迹, kind = '中间推演列表') => {
            if (!载体 || !轨迹) return;
            if (!Array.isArray(载体[kind])) 载体[kind] = [];
            const normalized = 归一判定轨迹(轨迹);
            normalized.normalizedReasonKey ||= 构建判定侧写归一理由键(normalized);
            const 已存在 = 载体[kind].some(item => {
              const prev = 归一判定轨迹(item);
              const prevTarget = String(prev.displayTargetName || prev.目标 || '').trim();
              const nextTarget = String(normalized.displayTargetName || normalized.目标 || '').trim();
              return 读取轨迹类型(prev) === 读取轨迹类型(normalized) &&
                String(prev.行动者 || '').trim() === String(normalized.行动者 || '').trim() &&
                prevTarget === nextTarget &&
                String(prev.normalizedReasonKey || 构建判定侧写归一理由键(prev)) === String(normalized.normalizedReasonKey || '');
            });
            if (!已存在) 载体[kind].push(normalized);
          };
          const 标记最终命中标签 = 轨迹 => {
            if (!轨迹 || 判定轨迹是目标规划(轨迹)) return '';
            const 决断类型 = 读取判定决断类型(轨迹);
            if (决断类型 === 'keep_original') return '再判定维持';
            if (决断类型 === 'switch_action' || 轨迹?.动作校正?.发生校正) return '最终改判';
            return '最终命中';
          };
          for (let index = 0; index < rawList.length; index += 1) {
            const current = 归一判定轨迹(rawList[index] || {});
            const 推断回合 = 补齐判定轨迹回合(current, rawList.slice(0, index), rawList.slice(index + 1));
            if (推断回合 > 0 && !(Number(current.回合 || current.round || current.实际回合 || 0) > 0)) current.回合 = 推断回合;
            const currentNoCandidate =
              /无可用候选/.test(String(current.选择原因 || '')) &&
              !String(current.技能 || '').trim() &&
              读取判定流程候选列表(current, 1).length === 0;
            const handoffIndex = currentNoCandidate
              ? rawList.findIndex((item, itemIndex) => {
                  const next = 归一判定轨迹(item || {});
                  const nextRound = 补齐判定轨迹回合(next, rawList.slice(0, itemIndex), rawList.slice(itemIndex + 1));
                  if (nextRound > 0 && !(Number(next.回合 || next.round || next.实际回合 || 0) > 0)) next.回合 = nextRound;
                  return itemIndex > index &&
                    itemIndex <= index + 8 &&
                    next.行动者 === current.行动者 &&
                    Number(next.回合 || 0) === Number(current.回合 || 0) &&
                    (String(next.技能 || '').trim() || 读取判定流程候选列表(next, 1).length > 0);
                })
              : -1;
            if (handoffIndex > index) {
              const to = 归一判定轨迹(rawList[handoffIndex] || {});
              const toRound = 补齐判定轨迹回合(to, rawList.slice(0, handoffIndex), rawList.slice(handoffIndex + 1));
              if (toRound > 0 && !(Number(to.回合 || to.round || to.实际回合 || 0) > 0)) to.回合 = toRound;
              current.phaseBucket = 读取判定流程分段键(current);
              current.kind = current.phaseBucket;
              current.normalizedReasonKey = 构建判定侧写归一理由键(current);
              to.phaseBucket = 读取判定流程分段键(to);
              to.kind = to.phaseBucket;
              to.normalizedReasonKey = 构建判定侧写归一理由键(to);
              rows.push({ type: 'handoff', from: current, to });
              continue;
            }
            const 展示轨迹 = 判定轨迹是战术动作(current) ? 绑定判定轨迹到账本动作(current) : current;
            展示轨迹.phaseBucket = 读取判定流程分段键(展示轨迹);
            展示轨迹.kind = 展示轨迹.phaseBucket;
            展示轨迹.normalizedReasonKey = 构建判定侧写归一理由键(展示轨迹);
            rows.push({
              type: 'trace',
              trace: 展示轨迹,
            });
          }
          for (let index = rows.length - 1; index >= 0; index -= 1) {
            const item = rows[index];
            const trace = item?.type === 'handoff' ? (item.to || item.from || {}) : item?.trace;
            let round = Number(trace?.回合 || trace?.round || 0);
            if (!(round > 0)) {
              const actor = String(trace?.行动者 || '').trim();
              const nearest = rows.slice(index + 1).find(next => {
                const nextTrace = next?.type === 'handoff' ? (next.to || next.from || {}) : next?.trace;
                return nextTrace && String(nextTrace.行动者 || '').trim() === actor && Number(nextTrace.回合 || nextTrace.round || 0) > 0;
              });
              round = Number((nearest?.type === 'handoff' ? (nearest.to || nearest.from || {}) : nearest?.trace)?.回合 || 0);
            }
            if (round > 0) {
              if (item.type === 'handoff') {
                if (item.from) item.from.回合 = Number(item.from.回合 || round);
                if (item.to) item.to.回合 = Number(item.to.回合 || round);
              } else if (item.trace) {
                item.trace.回合 = Number(item.trace.回合 || round);
              }
            }
          }
          const best = new Map();
          const fallback = new Map();
          const extras = new Map();
          const otherRows = [];
          rows.forEach((item, index) => {
            if (item?.type !== 'trace') {
              otherRows.push({ ...item, __index: index });
              return;
            }
            const trace = 归一判定轨迹(item.trace || {});
            if (判定轨迹是静默辅助规划(trace)) return;
            if (事件账本.length && 判定轨迹是战术动作(trace) && !判定轨迹有账本落地(trace)) return;
            const key = 构建判定流程归并键(trace);
            const rank = 轨迹展示优先级(trace) * 1000 + index;
            const store = extras.get(key) || { 中间推演列表: [], 目标遍历详情: [] };
            const isIntermediate = 判定轨迹属于中间推演(trace);
            if (isIntermediate) {
              const detailKind = /主动规划/.test(读取轨迹类型(trace)) && String(trace.目标 || trace.displayTargetName || '').trim()
                ? '目标遍历详情'
                : '中间推演列表';
              归并附注(store, trace, detailKind);
              extras.set(key, store);
              const prevFallback = fallback.get(key);
              const 保护后续主规划 =
                /主动规划/.test(读取轨迹类型(trace)) &&
                Number(trace.回合 || trace.round || 0) >= 2;
              if (!prevFallback || rank >= prevFallback.rank || 保护后续主规划) {
                fallback.set(key, { item: { ...item, trace }, rank, index });
              }
              return;
            }
            const prev = best.get(key);
            if (!prev || rank >= prev.rank) {
              if (prev?.item?.trace) {
                归并附注(store, 归一判定轨迹(prev.item.trace), '中间推演列表');
                extras.set(key, store);
              }
              best.set(key, { item: { ...item, trace }, rank, index });
            } else {
              归并附注(store, trace, '中间推演列表');
              extras.set(key, store);
            }
          });
          const mergedTraceRows = [];
          const allKeys = new Set([...best.keys(), ...fallback.keys()]);
          [...allKeys].forEach(key => {
            const entry = best.get(key) || fallback.get(key);
            if (!entry?.item?.trace) return;
            const trace = 归一判定轨迹(entry.item.trace);
            const extra = extras.get(key);
            if (extra?.中间推演列表?.length) trace.中间推演列表 = extra.中间推演列表;
            if (extra?.目标遍历详情?.length && !判定轨迹静默目标遍历详情(trace)) trace.目标遍历详情 = extra.目标遍历详情;
            trace.最终命中标签 = 标记最终命中标签(trace);
            mergedTraceRows.push({ type: 'trace', trace, __index: entry.index });
          });
          const 主动规划主卡保留 = new Map();
          mergedTraceRows.forEach(item => {
            const trace = 归一判定轨迹(item?.trace || {});
            if (读取轨迹类型(trace) !== '主动规划') return;
            const actor = String(trace.行动者 || '').trim();
            const round = Number(trace.回合 || trace.round || 0);
            if (!actor || !(round > 0)) return;
            const key = `${round}::${actor}`;
            const action = normalizeBattleActionDisplayName(trace.finalResolvedActionName || trace.技能 || '');
            const reason = String(trace.选择原因 || '').trim();
            const score =
              (Number(trace.最终权重 || 0) > 0 ? 1000 : 0) +
              (action ? 120 : 0) +
              (/守势观察|未形成有效出手机会/.test(reason) ? 0 : 40) +
              Number(item?.__index || 0);
            const prev = 主动规划主卡保留.get(key);
            if (!prev || score >= prev.score) 主动规划主卡保留.set(key, { item, score });
          });
          const 主动规划附注 = new Set();
          mergedTraceRows.forEach(item => {
            const trace = 归一判定轨迹(item?.trace || {});
            if (读取轨迹类型(trace) !== '主动规划') return;
            const actor = String(trace.行动者 || '').trim();
            const round = Number(trace.回合 || trace.round || 0);
            if (!actor || !(round > 0)) return;
            const key = `${round}::${actor}`;
            const keep = 主动规划主卡保留.get(key)?.item;
            if (!keep || keep === item) return;
            const targetTrace = keep.trace || {};
            targetTrace.中间推演列表 ||= [];
            const 已存在 = targetTrace.中间推演列表.some(extra => {
              const prev = 归一判定轨迹(extra);
              return 读取轨迹类型(prev) === '主动规划' &&
                String(prev.行动者 || '').trim() === actor &&
                normalizeBattleActionDisplayName(prev.finalResolvedActionName || prev.技能 || '') ===
                  normalizeBattleActionDisplayName(trace.finalResolvedActionName || trace.技能 || '') &&
                String(prev.选择原因 || '').trim() === String(trace.选择原因 || '').trim();
            });
            if (!已存在) targetTrace.中间推演列表.push(trace);
            主动规划附注.add(item);
          });
          const 自保主规划键集合 = new Set(
            mergedTraceRows
              .map(item => 归一判定轨迹(item?.trace || {}))
              .filter(trace => 读取轨迹类型(trace) === '主动规划' && 判定侧写是自我应对动作(trace))
              .map(trace => `${Number(trace.回合 || trace.round || 0)}::${String(trace.行动者 || '').trim()}`)
          );
          const allRows = [...mergedTraceRows, ...otherRows]
            .filter(item => {
              if (主动规划附注.has(item)) return false;
              if (item?.type !== 'trace') return true;
              const trace = 归一判定轨迹(item.trace || {});
              if (!判定轨迹是目标规划(trace)) return true;
              return !自保主规划键集合.has(`${Number(trace.回合 || trace.round || 0)}::${String(trace.行动者 || '').trim()}`);
            })
            .sort((左, 右) =>
              Number((左?.trace || 左?.to || 左?.from || {}).回合 || 0) - Number((右?.trace || 右?.to || 右?.from || {}).回合 || 0) ||
              Number(左?.__index || 0) - Number(右?.__index || 0));
          const visibleRoundSet = new Set();
          公开战报行
            .map(line => Number(String(line || '').match(/^第(\d+)回合：/)?.[1] || 0))
            .filter(round => round > 0)
            .forEach(round => visibleRoundSet.add(round));
          (Array.isArray(事件账本) ? 事件账本 : []).forEach(event => {
            const round = Number(event?.round || event?.sourceRound || 0);
            if (!(round > 0)) return;
            const kind = String(event?.eventKind || '').trim();
            const action = normalizeBattleActionDisplayName(event?.finalActionName || event?.actionName || event?.sourceActionName || '');
            const result = String(event?.result || event?.primaryOutcome || event?.meta?.primaryOutcome || '').trim();
            const reason = String(event?.failReason || event?.failureReason || event?.meta?.failureReason || event?.reasonCode || event?.meta?.reasonCode || '').trim();
            const 是行动轴事件 = [
              'action_start', 'pass', 'defend', 'dodge', 'blocked_action', 'failed_action', 'target_fail',
              'hit_result', 'state_apply', 'state_tick', 'resource_change', 'create', 'summon_create', 'shield_create',
            ].includes(kind);
            if (!是行动轴事件) return;
            if (判定公开战报事件是内部兜底(event) && !/战术待机|待机|观察|防御|收招转防|守势|pass|observe|defend|stance/i.test(`${action} ${result} ${reason}`)) return;
            visibleRoundSet.add(round);
          });
          const visibleRows = visibleRoundSet.size
            ? allRows.filter(item => {
                const trace = item?.trace || item?.to || item?.from || {};
                const round = Number(trace?.回合 || trace?.round || 0);
                return !(round > 0) || visibleRoundSet.has(round);
              })
            : allRows;
          const groupedRows = new Map();
          const 玩家手选主卡 = [];
          if (玩家侧名称集合.size) {
            const 已有主卡键 = new Set(
              visibleRows
                .filter(item => item?.type === 'trace')
                .map(item => 归一判定轨迹(item.trace || {}))
                .filter(trace => /战术确立|主动规划/.test(读取轨迹类型(trace)))
                .map(trace => `${Number(trace.回合 || trace.round || 0)}::${String(trace.行动者 || '').trim()}`)
            );
            const 账本手选动作 = (Array.isArray(事件账本) ? 事件账本 : [])
              .filter(event =>
                event &&
                String(event.eventKind || '').trim() === 'action_start' &&
                Number(event.round || event.sourceRound || 0) > 0 &&
                玩家侧名称集合.has(String(event.actorName || event.actor || '').trim())
              )
              .sort((left, right) =>
                Number(left.round || left.sourceRound || 0) - Number(right.round || right.sourceRound || 0) ||
                String(left.actionId || '').localeCompare(String(right.actionId || ''), 'zh-Hans-CN'));
            账本手选动作.forEach((event, index) => {
                const actor = String(event.actorName || event.actor || '').trim();
                const round = Number(event.round || event.sourceRound || 0);
                const key = `${round}::${actor}`;
                if (已有主卡键.has(key)) return;
                const actionName = normalizeBattleActionDisplayName(event.actionName || event.finalActionName || event.meta?.actionName || event.meta?.finalActionName || '');
                if (!actionName || /未完成动作|行动失败|系统反击|承伤硬抗|肉体兜底|伺机闪避/.test(actionName)) return;
                const sameRoundTrace = visibleRows
                  .map(row => row?.type === 'trace' ? 归一判定轨迹(row.trace || {}) : null)
                  .find(trace =>
                    trace &&
                    Number(trace.回合 || trace.round || 0) === round &&
                    isSameBattleReportName(String(trace.行动者 || '').trim(), actor)
                  );
                const targetNameRaw = String(event.targetName || sameRoundTrace?.displayTargetName || sameRoundTrace?.目标 || sameRoundTrace?.实际目标 || '').trim();
                const targetSemantics = String(sameRoundTrace?.目标语义 || event.targetPoolSide || '').trim();
                const carryMode = String(sameRoundTrace?.承载方式 || event.actionType || '').trim();
                const ledgerTargetSide = String(event.targetPoolSide || event.targetSide || '').trim();
                const actionType = String(event.actionType || '').trim();
                const nonHostileTarget =
                  判定侧写是造物或自用动作(sameRoundTrace || {}) ||
                  判定侧写是友方支援动作(sameRoundTrace || {}) ||
                  (/自身|友方单体|友方群体|造物承载/.test(targetSemantics)) ||
                  /self|ally|support|create|summon|shield/i.test(`${ledgerTargetSide}|${actionType}`) ||
                  /造物|物品|药剂|食物|牛肉干|护壁|鼓舞/.test(actionName);
                const targetName = nonHostileTarget ? '' : targetNameRaw;
                玩家手选主卡.push({
                  type: 'trace',
                  trace: 归一判定轨迹({
                    type: '战术确立',
                    actor,
                    target: targetName,
                    skill: actionName,
                    round,
                    选择原因: '玩家已手选本轮主轴动作',
                    候选来源: '玩家手选动作',
                    finalResolvedActionName: actionName,
                    displayTargetName: targetName,
                    目标语义: targetSemantics,
                    承载方式: carryMode,
                    actionId: String(event.actionId || '').trim(),
                    sourceActionId: String(event.sourceActionId || '').trim(),
                    playerLockedSource: 'eventLedger',
                  }),
                  __index: -1000 + index,
                });
                已有主卡键.add(key);
              });
          }
          [...玩家手选主卡, ...visibleRows].forEach(item => {
            const round = Number((item?.trace || item?.to || item?.from || {}).回合 || 0);
            const key = round > 0 ? `round:${round}` : `raw:${groupedRows.size}`;
            if (!groupedRows.has(key)) groupedRows.set(key, []);
            groupedRows.get(key).push(item);
          });
          return [...groupedRows.entries()]
            .sort((左, 右) => {
              const 左回合 = Number(String(左[0]).replace(/^round:/, '') || 0);
              const 右回合 = Number(String(右[0]).replace(/^round:/, '') || 0);
              return 左回合 - 右回合;
            })
            .flatMap(([, rows]) => {
              if (rows.length <= 10) return rows;
              const lockedRows = rows.filter(item => {
                if (item?.type !== 'trace') return false;
                return 读取轨迹类型(归一判定轨迹(item.trace || {})) === '战术确立';
              });
              const lockedSet = new Set(lockedRows);
              const tailRows = rows.filter(item => !lockedSet.has(item)).slice(-(10 - Math.min(lockedRows.length, 10)));
              return [...lockedRows.slice(0, 10), ...tailRows];
            })
            .map(item => {
              if ('__index' in item) delete item.__index;
              return item;
            });
        }

        function 读取结算轨迹值(traceRows = [], key = '') {
          const row = (Array.isArray(traceRows) ? traceRows : []).find(item => String(item?.key || '').trim() === key);
          return row?.value;
        }

        function 因果链节点默认可见(node = {}) {
          const kind = String(node?.nodeKind || '').trim();
          if (kind !== 'counter_window') return true;
          const result = String(node?.result || '').trim();
          const reasonCode = String(node?.reasonCode || '').trim();
          const outcome = String(node?.primaryOutcome || '').trim();
          const failureReason = String(node?.failureReason || node?.reasonText || '').trim();
          if (result === 'opened' || reasonCode === 'COUNTER_WINDOW_OPENED') return true;
          if (reasonCode === 'COUNTER_WINDOW_MISSED' || /missed|错失/i.test(`${result} ${outcome} ${failureReason}`)) return true;
          if (reasonCode === 'NO_EFFECTIVE_OPENING' || outcome === 'no_valid_window' || /no_valid|未形成稳定|未满足门槛/.test(failureReason)) return false;
          return /missed|错失|失败|fail/i.test(`${result} ${reasonCode} ${outcome} ${failureReason}`);
        }

        function 构建因果链行动区块条目(resolutionTrace = [], decisionTrace = []) {
          const nodes = (Array.isArray(resolutionTrace) ? resolutionTrace : [])
            .filter(item => item && typeof item === 'object');
          if (!nodes.length) return [];
          const behaviorTraceByActorRound = new Map();
          (Array.isArray(decisionTrace) ? decisionTrace : [])
            .map(item => 归一判定轨迹(item || {}))
            .filter(item => item && /战术确立|主动规划|应招审计|再判定审计|换招审计/.test(读取轨迹类型(item)))
            .forEach(item => {
              const key = `${Number(item.回合 || 0)}::${String(item.行动者 || '').trim()}`;
              if (!String(item.行动者 || '').trim() || !(Number(item.回合 || 0) > 0)) return;
              if (!behaviorTraceByActorRound.has(key)) behaviorTraceByActorRound.set(key, []);
              behaviorTraceByActorRound.get(key).push(item);
            });
          const nodeById = new Map(nodes.map(node => [String(node?.nodeId || '').trim(), node]).filter(([id]) => id));
          const byParent = new Map();
          nodes.forEach(node => {
            const parent = String(node.parentNodeId || '').trim();
            if (!parent) return;
            if (!byParent.has(parent)) byParent.set(parent, []);
            byParent.get(parent).push(node);
          });
          const initialIntentByActorRound = new Map();
          nodes
            .filter(node => String(node.nodeKind || '').trim() === 'initial_intent')
            .forEach(node => {
              const key = `${Number(node.round || 0)}::${String(node.actorName || '').trim()}`;
              if (!initialIntentByActorRound.has(key)) initialIntentByActorRound.set(key, []);
              initialIntentByActorRound.get(key).push(node);
            });
          const actionRoots = nodes.filter(node => String(node.nodeKind || '') === 'action_decision' && String(node.finalActionName || '').trim());
          const blocks = actionRoots.map(root => {
            const children = byParent.get(String(root.nodeId || '').trim()) || [];
            const resultChildren = children.filter(node => ['target_branch', 'replan_decision', 'reaction_window', 'reaction_decision', 'hit_check', 'state_check', 'damage_settlement', 'state_settlement', 'counter_action', 'counter_window', 'summon_assist', 'final_result'].includes(String(node.nodeKind || '')) && 因果链节点默认可见(node));
            const intentCandidates = initialIntentByActorRound.get(`${Number(root.round || 0)}::${String(root.actorName || '').trim()}`) || [];
            const rootTarget = String(root.targetName || '').trim();
            const rootInitial = normalizeBattleActionDisplayName(root.initialActionName || root.finalActionName || '');
            const initialIntent = intentCandidates.find(node =>
              normalizeBattleActionDisplayName(node.initialActionName || '') === rootInitial &&
              (!rootTarget || !String(node.targetName || '').trim() || String(node.targetName || '').trim() === rootTarget)
            ) || intentCandidates.find(node => !rootTarget || !String(node.targetName || '').trim() || String(node.targetName || '').trim() === rootTarget) || intentCandidates[0] || null;
            return {
              type: 'resolution_action_block',
              round: Number(root.round || 0),
              回合: Number(root.round || 0),
              roundPhase: 'action_chain',
              root,
              initialIntent,
              children: resultChildren,
              behaviorTraces: behaviorTraceByActorRound.get(`${Number(root.round || 0)}::${String(root.actorName || '').trim()}`) || [],
              byParent,
              nodeById,
              fromResolutionTrace: true,
            };
          }).filter(item => item.root);
          const coveredNodeIds = new Set(blocks.flatMap(item => [item.root, ...读取因果链全量子节点(item)].map(node => String(node?.nodeId || '').trim()).filter(Boolean)));
          nodes
            .filter(node => String(node.nodeKind || '').trim() === 'final_result' && String(node.primaryOutcome || '').trim() === 'summon_created' && !coveredNodeIds.has(String(node.nodeId || '').trim()))
            .forEach(node => {
              const sourceAction = normalizeBattleActionDisplayName(读取结算轨迹值(node.calculationTrace, 'sourceAction') || node.finalActionName || node.actionName || '召唤');
              const summonName = String(读取结算轨迹值(node.calculationTrace, 'summonName') || node.summonName || node.targetName || '').trim();
              blocks.push({
                type: 'resolution_action_block',
                round: Number(node.round || 0),
                回合: Number(node.round || 0),
                roundPhase: 'action_chain',
                root: {
                  nodeId: `presentation:${node.nodeId || summonName || sourceAction}`,
                  round: Number(node.round || 0),
                  nodeKind: 'action_decision',
                  actorName: String(node.actorName || 读取结算轨迹值(node.calculationTrace, 'actor') || '').trim(),
                  targetName: summonName,
                  finalActionName: sourceAction,
                  source: 'summon_create',
                },
                children: [node],
                behaviorTraces: behaviorTraceByActorRound.get(`${Number(node.round || 0)}::${String(node.actorName || 读取结算轨迹值(node.calculationTrace, 'actor') || '').trim()}`) || [],
                byParent: new Map([[`presentation:${node.nodeId || summonName || sourceAction}`, [node]]]),
                nodeById: new Map([[String(node?.nodeId || '').trim(), node]].filter(([id]) => id)),
                fromResolutionTrace: true,
              });
            });
          const blockKey = item => [
            Number(item?.round || item?.回合 || 0),
            String(item?.root?.actorName || '').trim(),
            normalizeBattleActionDisplayName(item?.root?.finalActionName || item?.root?.actionName || ''),
            String(item?.root?.targetName || '').trim(),
          ].join('::');
          const blockDetailScore = item => 读取因果链全量子节点(item)
            .filter(node => !['action_decision'].includes(String(node?.nodeKind || '').trim()))
            .length;
          const bestByKey = new Map();
          blocks.forEach(item => {
            const key = blockKey(item);
            const prev = bestByKey.get(key);
            if (!prev || blockDetailScore(item) > blockDetailScore(prev)) bestByKey.set(key, item);
          });
          return Array.from(bestByKey.values());
        }

        function 构建因果链回合末聚合条目(resolutionTrace = []) {
          return (Array.isArray(resolutionTrace) ? resolutionTrace : [])
            .filter(node => node && typeof node === 'object' && String(node.nodeKind || '').trim() === 'aggregation')
            .map(node => {
              const aggregateKind = String(node.aggregateKind || node.primaryOutcome || 'state_tick').trim();
              const stateName = String(node.stateName || 读取结算轨迹值(node.calculationTrace, 'stateName') || '持续状态').trim();
              const total = Math.max(0, Number(读取结算轨迹值(node.calculationTrace, 'totalAmount') || 0));
              const childCount = Math.max(0, Number(读取结算轨迹值(node.calculationTrace, 'childCount') || (Array.isArray(node.childNodeIds) ? node.childNodeIds.length : 0)));
              const isHeal = aggregateKind === 'heal_tick';
              const text = `【${stateName}】持续结算，${childCount} 个目标共${isHeal ? '恢复' : '损失'} ${Math.round(total)} 点`;
              return {
                type: 'settlement_aggregation',
                round: Number(node.round || 0),
                回合: Number(node.round || 0),
                roundPhase: 'turn_end',
                kind: 'status_tick_aggregation',
                aggregateKind,
                stateName,
                childNodeIds: Array.isArray(node.childNodeIds) ? node.childNodeIds : [],
                ledgerEventIds: Array.isArray(node.ledgerEventIds) ? node.ledgerEventIds : [],
                sourceNodeIds: Array.isArray(node.childNodeIds) ? node.childNodeIds : [],
                sourceEventIds: Array.isArray(node.ledgerEventIds) ? node.ledgerEventIds : [],
                text,
                children: [],
                fromResolutionTrace: true,
              };
            });
        }

        function 读取因果链全量子节点(条目 = {}) {
          const result = [];
          const byParent = 条目?.byParent instanceof Map ? 条目.byParent : new Map();
          const visit = node => {
            if (!node || typeof node !== 'object') return;
            result.push(node);
            (byParent.get(String(node.nodeId || '').trim()) || []).forEach(visit);
          };
          (Array.isArray(条目?.children) ? 条目.children : []).forEach(visit);
          return result;
        }

        function 计算因果链展示权重(条目 = {}) {
          const root = 条目?.root || {};
          const finalAction = normalizeBattleActionDisplayName(root.finalActionName || root.actionName || '');
          const initialAction = normalizeBattleActionDisplayName(root.initialActionName || '');
          const discardedAction = normalizeBattleActionDisplayName(root.discardedActionName || (initialAction && initialAction !== finalAction ? initialAction : ''));
          const nodes = 读取因果链全量子节点(条目);
          let score = /普通攻击/.test(finalAction) ? 1 : /魂技|真身|融合|爆发/.test(finalAction) ? 6 : 3;
          let childEscalated = false;
          const reasons = [];
          const actorSide = 标准化战斗阵营侧(root.actorSide || '');
          const targetSide = 标准化战斗阵营侧(root.targetSide || '');
          const rootSource = String(root.source || root.meta?.source || '').trim();
          const rootActorName = String(root.actorName || '').trim();
          const rootIsSummonAction = rootSource === 'summon' || /召唤/.test(String(root.actionType || root.sourceType || ''));
          const 节点是否玩家受击事实 = node => {
            const kind = String(node?.nodeKind || '').trim();
            if (标准化战斗阵营侧(node?.targetSide || '') !== 'player') return false;
            const nodeTargetName = String(node?.targetName || '').trim();
            if (rootIsSummonAction && rootActorName && isSameBattleReportName(nodeTargetName, rootActorName)) return false;
            const damage = Number(读取结算轨迹值(node?.calculationTrace, 'finalDamage') || 读取结算轨迹值(node?.calculationTrace, 'damage') || 0);
            if (['damage_settlement', 'counter_action'].includes(kind)) return damage > 0;
            if (kind === 'state_settlement') {
              const stateName = String(读取结算轨迹值(node?.calculationTrace, 'stateName') || node?.stateName || '').trim();
              return !!stateName && !/immune|resist|免疫|抵抗|豁免/.test(String(node?.result || node?.primaryOutcome || ''));
            }
            return false;
          };
          if (actorSide === 'player' || root.isPlayerAction === true) {
            score += 4;
            reasons.push(rootIsSummonAction ? '召唤行动' : '玩家行动');
          }
          if (actorSide !== 'player' && targetSide === 'player') {
            score += 8;
            childEscalated = true;
            reasons.push('玩家受击');
          }
          if (discardedAction && discardedAction !== finalAction) {
            score += 7;
            childEscalated = true;
            reasons.push('变招');
          }
          nodes.forEach(node => {
            const kind = String(node?.nodeKind || '').trim();
            const outcome = String(node?.primaryOutcome || '').trim();
            const stateName = String(读取结算轨迹值(node?.calculationTrace, 'stateName') || node?.stateName || '').trim();
            const damage = Number(读取结算轨迹值(node?.calculationTrace, 'finalDamage') || 读取结算轨迹值(node?.calculationTrace, 'damage') || 0);
            if (节点是否玩家受击事实(node)) {
              score += 8;
              childEscalated = true;
              reasons.push('玩家受击');
            }
            if (kind === 'counter_action') {
              score += 9;
              childEscalated = true;
              reasons.push(Number(node?.counterDepth || 0) >= 2 ? '反防反' : '防反');
            } else if (kind === 'summon_assist') {
              score += 7;
              childEscalated = true;
              reasons.push('召唤协同');
            } else if (kind === 'state_settlement') {
              const control = /控制|眩晕|禁锢|束缚|位移限制|减速|迟缓|沉默|封锁/.test(stateName);
              score += control ? 7 : 4;
              if (control) childEscalated = true;
              reasons.push(stateName ? `状态:${stateName}` : '状态');
            } else if (kind === 'damage_settlement') {
              if (damage >= 100) {
                score += 6;
                childEscalated = true;
                reasons.push('重创');
              } else if (damage > 0) score += 2;
              if (/kill|fatal|濒危|击杀|免死/.test(outcome)) {
                score += 10;
                childEscalated = true;
                reasons.push('致命');
              }
            } else if (kind === 'counter_window' && /missed|错失|失败/.test(String(node?.reasonCode || node?.result || ''))) {
              score += 4;
              reasons.push('窗口错失');
            }
          });
          const eventWeight = score >= 16 ? 'critical' : score >= 9 ? 'high' : score >= 4 ? 'medium' : 'low';
          return {
            score,
            eventWeight,
            childEscalated,
            collapse: eventWeight === 'low' && !childEscalated,
            reasons: [...new Set(reasons)].slice(0, 3),
          };
        }
        function 渲染因果链行动区块(条目 = {}) {
          const root = 条目?.root || {};
          const 构建ActionChainDTO = actionRoot => {
            const initial = normalizeBattleActionDisplayName(actionRoot.initialActionName || '');
            const final = normalizeBattleActionDisplayName(actionRoot.finalActionName || actionRoot.actionName || '行动');
            return {
              actorName: String(actionRoot.actorName || '行动者').trim(),
              targetName: String(actionRoot.targetName || '').trim(),
              initialActionName: initial,
              finalActionName: final,
              discardedActionName: normalizeBattleActionDisplayName(actionRoot.discardedActionName || (initial && initial !== final ? initial : '')),
              actionStatus: String(actionRoot.actionStatus || actionRoot.meta?.actionStatus || '').trim(),
            };
          };
          const actionDTO = 构建ActionChainDTO(root);
          const initialIntentNode = 条目?.initialIntent || null;
          const children = Array.isArray(条目?.children) ? 条目.children : [];
          const behaviorTraces = Array.isArray(条目?.behaviorTraces) ? 条目.behaviorTraces : [];
          const byParent = 条目?.byParent instanceof Map ? 条目.byParent : new Map();
          const nodeById = 条目?.nodeById instanceof Map ? 条目.nodeById : new Map([[String(root?.nodeId || '').trim(), root], ...children.map(node => [String(node?.nodeId || '').trim(), node])].filter(([id]) => id));
          const actor = actionDTO.actorName;
          const target = actionDTO.targetName;
          const initialAction = actionDTO.initialActionName;
          const finalAction = actionDTO.finalActionName;
          const discardedAction = actionDTO.discardedActionName;
          const lines = [];
          const detailLines = [];
          const renderedSettlementKeys = new Set();
          const renderedReplanKeys = new Set();
          const pushTimelineLine = (text, kind = 'detail', options = {}) => {
            const lineText = String(text || '').trimEnd();
            if (!lineText) return;
            const item = { text: lineText, kind: String(kind || 'detail').trim() || 'detail' };
            if (options && options.detail === true) detailLines.push(item);
            else lines.push(item);
          };
          const pushDetailLine = (text, kind = 'detail') => pushTimelineLine(text, kind, { detail: true });
          const mapTimelineKindFromNode = nodeKind => {
            const kind = String(nodeKind || '').trim();
            if (kind === 'initial_intent' || kind === 'replan_decision') return 'intent';
            if (kind === 'reaction_window' || kind === 'reaction_decision') return 'reaction';
            if (kind === 'hit_check' || kind === 'state_check') return 'check';
            if (kind === 'damage_settlement' || kind === 'state_settlement' || kind === 'final_result' || kind === 'summon_assist') return 'settlement';
            if (kind === 'counter_window' || kind === 'counter_action') return 'counter';
            if (kind === 'target_branch') return 'branch';
            return 'detail';
          };
          if (initialIntentNode) {
            const plannedAction = normalizeBattleActionDisplayName(initialIntentNode.initialActionName || '行动');
            const plannedTarget = String(initialIntentNode.targetName || target || '').trim();
            const timingBucket = String(读取结算轨迹值(initialIntentNode.calculationTrace, 'timingBucket') || '').trim();
            pushTimelineLine(`├─ 初始意图：【${plannedAction}】${plannedTarget ? ` 指向${plannedTarget}` : ''}${timingBucket ? `（行动窗口 ${timingBucket}）` : ''}`, 'intent');
          }
          const 映射行动链变招原因短语 = node => {
            const code = String(node?.replanReasonCode || node?.reasonCode || '').trim().toUpperCase();
            const text = String(node?.replanReasonText || node?.reasonText || '').trim();
            if (code === 'INTERRUPTED_BY_SPEED') return '被对手抢先压制';
            if (code === 'TARGET_REPOSITIONED' || code === 'TARGET_LOST') return '原目标失去有效落点';
            if (code === 'RESOURCE_INSUFFICIENT') return '资源不足';
            if (code === 'CONTROLLED') return '状态限制';
            if (code === 'OUT_OF_RANGE') return '距离不满足';
            if (code === 'NO_VALID_TARGET') return '缺少合法目标';
            if (code === 'NO_EFFECTIVE_OPENING') return '没有合适出手窗口';
            if (code === 'TACTICAL_DISADVANTAGE' && /抢先压制|先手压制|不宜继续前压|有效出手窗口|有效出手机会|窗口/.test(text)) {
              return text.replace(/后调整动作|调整动作/g, '').replace(/[，。；\s]+$/g, '').trim();
            }
            return '';
          };
          if (discardedAction && discardedAction !== finalAction) {
            const reason = 映射行动链变招原因短语(root);
            renderedReplanKeys.add(`${discardedAction}|${finalAction}|${reason}`);
            pushTimelineLine(`├─ 原计划【${discardedAction}】${reason ? `，因${reason}` : ''}改为【${finalAction}】`, 'intent');
          } else {
            pushTimelineLine(`├─ 最终动作：【${finalAction}】${target ? ` 指向${target}` : ''}`, 'detail');
          }
          const 判定候选链不可信 = trace => {
            const candidates = 构建CandidatePoolDTO(trace);
            const finalActionName = normalizeBattleActionDisplayName(trace?.finalResolvedActionName || trace?.最终动作 || trace?.技能 || trace?.finalActionName || '');
            const finalMissingFromPool = !!(finalActionName && candidates.length && !candidates.some(item => item.name === finalActionName));
            const selectedWithRejectCode = candidates.some(item => ['SELECTED', 'LOCKED', 'EXECUTED'].includes(item.status) && item.rejectionCode);
            const explicitStatusMissing = 读取判定流程候选列表(trace, 0).some(item => !String(item?.candidateStatus || item?.状态 || item?.status || '').trim());
            const backfillSource = /回填|最终落点|执行声明|终态回填|系统/i.test(String(trace?.actionOverrideSource || trace?.动作覆写来源 || ''));
            return finalMissingFromPool || selectedWithRejectCode || explicitStatusMissing || backfillSource;
          };
          const 读取权重公式信任状态 = trace => {
            const normalized = 归一判定轨迹(trace || {});
            const pieces = [
              Number(normalized.原始权重 || normalized.原始净收益 || 0),
              Number(normalized.战术修正 || 0),
              Number(normalized.目标修正 || 0) + Number(normalized.目标价值修正 || 0),
              Number(normalized.资源修正 || 0),
              Number(normalized.团队意图修正 || 0),
              Number(normalized.二回合前瞻修正 || 0),
              Number(normalized.职责修正 || 0),
            ].filter(value => Number.isFinite(value) && Math.abs(value) >= 1);
            const finalWeight = Number(normalized.最终权重 || 0);
            if (!Number.isFinite(finalWeight)) return { level: 'MISSING_OPERAND', diff: 0 };
            if (pieces.length < 2 && Math.abs(finalWeight) >= 1) return { level: 'MISSING_OPERAND', diff: 0 };
            const diff = Math.round(finalWeight - pieces.reduce((sum, value) => sum + value, 0));
            return { level: Math.abs(diff) > 1 ? 'HIDDEN_UNBALANCED' : 'TRUSTED', diff };
          };
          const 判定战术侧写可信 = trace => {
            if (!trace || /再判定审计|换招审计|应招审计/.test(读取轨迹类型(trace))) return false;
            if (判定候选链不可信(trace)) return false;
            return true;
          };
          const 构建行为演算明细行 = trace => {
            const normalized = 归一判定轨迹(trace || {});
            if (!/再判定审计|换招审计|应招审计/.test(读取轨迹类型(normalized))) return formatTacticalNarration(构建NarrativeDecisionDTO(normalized));
            const dto = /再判定审计/.test(读取轨迹类型(normalized))
              ? 构建ReplanAuditDTO(normalized)
              : 构建ActivePlanDTO(normalized);
            const label = dto.auditKind === 'replan'
              ? '再判定'
              : (/应招审计/.test(dto.auditType || '') ? '反应判定' : '主动规划');
            const parts = [`${label}：${dto.actorName || actor}`];
            if (dto.auditKind === 'replan') {
              if (dto.sourceActionName) parts.push(`应对【${dto.sourceActorName ? `${dto.sourceActorName}的` : ''}${dto.sourceActionName}】`);
              if (dto.replanActionName) parts.push(`选择【${dto.replanActionName}】`);
            } else {
              if (dto.finalActionName) parts.push(`执行【${dto.finalActionName}】`);
              if (dto.targetName) parts.push(`目标【${dto.targetName}】`);
            }
            return parts.join('，');
          };
          const 归一玩家态意图标签 = (intent = '', dto = {}) => {
            const raw = String(intent || '').trim();
            const actorName = String(dto?.actorName || actor || '').trim();
            const targetName = String(dto?.targetName || '').trim();
            if (actorName && targetName && actorName !== targetName && !/我方|友方|自身|全体|前排|后排/.test(targetName)) {
              if (/保核/.test(raw)) return '护核压制';
              if (/防守/.test(raw)) return '守势反击';
              if (/治疗|驱散|辅助|支援/.test(raw)) return '支援转压制';
            }
            return raw;
          };
          const 构建ActivePlanDTO = trace => {
            const normalized = 归一判定轨迹(trace || {});
            const selectedCandidate = 读取判定流程候选列表(normalized, 0).find(item => ['EXECUTED', 'LOCKED', 'SELECTED'].includes(读取候选状态(item, normalized))) || null;
            const targetName = target || 修正支援类判定目标名(normalized, actor);
            const dto = {
              auditKind: 'active',
              auditType: 读取轨迹类型(normalized),
              actorName: String(normalized.行动者 || '').trim(),
              intent: String(normalized.战略意图 || '').trim(),
              hitCandidateName: normalizeBattleActionDisplayName(读取候选名称(selectedCandidate || {}) || normalized.selectedCandidateName || ''),
              finalActionName: normalizeBattleActionDisplayName(normalized.finalResolvedActionName || normalized.技能 || normalized.hitCandidateName || '行动'),
              targetName,
            };
            dto.intent = 归一玩家态意图标签(dto.intent, dto);
            return dto;
          };
          const 构建ReplanAuditDTO = trace => {
            const normalized = 归一判定轨迹(trace || {});
            const selectedCandidate = 读取判定流程候选列表(normalized, 0).find(item => ['EXECUTED', 'LOCKED', 'SELECTED'].includes(读取候选状态(item, normalized))) || null;
            const hit = normalizeBattleActionDisplayName(读取候选名称(selectedCandidate || {}) || normalized.selectedCandidateName || '');
            return {
              auditKind: 'replan',
              auditType: 读取轨迹类型(normalized),
              actorName: String(normalized.reactionActorName || normalized.行动者 || '').trim(),
              intent: String(normalized.战略意图 || '').trim(),
              sourceActorName: String(normalized.sourceActorName || normalized.来源行动者 || '').trim(),
              sourceActionName: normalizeBattleActionDisplayName(normalized.sourceActionName || normalized.来源动作 || ''),
              replanActionName: normalizeBattleActionDisplayName(normalized.replanActionName || hit || normalized.finalResolvedActionName || normalized.技能 || ''),
            };
          };
          const 构建候选池明细行 = trace => {
            return '';
          };
          const 构建再判定定义行 = trace => {
            if (!/再判定审计/.test(读取轨迹类型(trace))) return '';
            const names = 读取判定流程候选列表(trace, 0).map(读取候选名称).filter(Boolean);
            const definitions = [];
            if (names.some(name => /抢落点/.test(name))) definitions.push('抢落点：争夺动作落点或先后窗口');
            if (names.some(name => /压招/.test(name))) definitions.push('压招：用当前动作压过对方动作');
            if (names.some(name => /偏转/.test(name))) definitions.push('偏转：降低或改变来袭效果');
            if (names.some(name => /伺机闪避/.test(name))) definitions.push('伺机闪避：规避并保留反击/再判定窗口');
            if (names.some(name => /^闪避$/.test(name))) definitions.push('闪避：只规避当前攻击');
            return definitions.length ? `动作定义：${definitions.join('；')}` : '';
          };
          const 构建权重拆分明细行 = trace => {
            return '';
          };
          const 构建取舍明细行 = trace => {
            return '';
          };
          const usedBehaviorKeys = new Set();
          let primaryTacticalNarrationRendered = false;
          const 查找当前主动叙事轨迹 = () => {
            const traces = Array.isArray(behaviorTraces) ? behaviorTraces : [];
            const finalName = normalizeBattleActionDisplayName(finalAction || '');
            const initialName = normalizeBattleActionDisplayName(initialAction || '');
            return traces.find(trace => {
              if (!/主动规划|战术确立/.test(读取轨迹类型(trace))) return false;
              const names = [
                trace?.finalResolvedActionName,
                trace?.实际技能,
                trace?.技能,
              ].map(name => normalizeBattleActionDisplayName(name || '')).filter(Boolean);
              if (finalName && names.includes(finalName)) return true;
              if (initialName && names.includes(initialName)) return true;
              return false;
            }) || null;
          };
          const 主动叙事轨迹 = 查找当前主动叙事轨迹();
          if (主动叙事轨迹) {
            const narration = formatTacticalNarration(构建NarrativeDecisionDTO(主动叙事轨迹));
            if (narration) {
              pushDetailLine(`├─ ${narration}`, 'intent');
              primaryTacticalNarrationRendered = true;
              usedBehaviorKeys.add(`${读取轨迹类型(主动叙事轨迹)}::${normalizeBattleActionDisplayName(主动叙事轨迹.finalResolvedActionName || 主动叙事轨迹.技能 || '')}::${String(主动叙事轨迹.目标 || '').trim()}`);
            }
          }
          const 行为明细匹配当前动作 = trace => {
            const type = 读取轨迹类型(trace);
            if (!/战术确立|主动规划|应招审计|再判定审计|换招审计/.test(type)) return false;
            const traceTarget = String(trace?.目标 || trace?.displayTargetName || '').trim();
            if (!/再判定审计|换招审计/.test(type) && target && traceTarget && !isSameBattleReportName(traceTarget, target)) return false;
            const actionNames = [
              trace?.finalResolvedActionName,
              trace?.实际技能,
              trace?.技能,
              trace?.hitCandidateName,
            ].map(name => normalizeBattleActionDisplayName(name || '')).filter(Boolean);
            const candidates = 读取判定流程候选列表(trace, 0).map(读取候选名称).filter(Boolean);
            const sourceActionName = normalizeBattleActionDisplayName(trace?.sourceActionName || trace?.来源动作 || '');
            const matchesAction = /再判定审计/.test(type)
              ? (!!sourceActionName && (!finalAction || sourceActionName === finalAction))
              : (
                  !finalAction ||
                  actionNames.some(name => name === finalAction) ||
                  (!/主动规划|战术确立/.test(type) && candidates.some(name => name === finalAction)) ||
                  (/换招审计/.test(type) && candidates.some(name => /换招/.test(name)))
                );
            if (!matchesAction) return false;
            const reason = String(trace?.选择原因 || trace?.reason || '').trim();
            const hasPositiveCandidate = Number(trace?.最终权重 || 0) > 0 || 读取判定流程候选列表(trace, 0).some(item => 读取候选权重(item) > 0);
            if (/未形成有效出手机会|当前未形成有效出手机会/.test(reason) && !hasPositiveCandidate) return false;
            return true;
          };
          behaviorTraces
            .filter(行为明细匹配当前动作)
            .slice(0, 4)
            .forEach(trace => {
              if (primaryTacticalNarrationRendered && /战术确立|主动规划/.test(读取轨迹类型(trace))) return;
              const key = `${读取轨迹类型(trace)}::${normalizeBattleActionDisplayName(trace.finalResolvedActionName || trace.技能 || '')}::${String(trace.目标 || '').trim()}`;
              if (usedBehaviorKeys.has(key)) return;
              usedBehaviorKeys.add(key);
              [构建行为演算明细行(trace), 构建候选池明细行(trace), 构建再判定定义行(trace), 构建权重拆分明细行(trace), 构建取舍明细行(trace)]
                .filter(Boolean)
                .forEach(line => pushDetailLine(`├─ ${line}`, 'intent'));
            });
          const formatCalcNumber = value => {
            const number = Number(value);
            if (!Number.isFinite(number)) return '';
            return Math.abs(number - Math.round(number)) < 0.01 ? String(Math.round(number)) : number.toFixed(2).replace(/0+$/, '').replace(/\.$/, '');
          };
          const 构建基础伤害代入式 = values => {
            const text = String(values?.formulaText || '').trim();
            const skillPower = Number(values?.skillPower || 0);
            const attackValue = Number(values?.attackValue || 0);
            const defenseValue = Number(values?.defenseValue || 0);
            const baseDamage = Number(values?.baseDamage || 0);
            if (!(skillPower > 0) || !(baseDamage > 0)) return '';
            const terms = [formatCalcNumber(skillPower)];
            if (attackValue > 0 && defenseValue > 0) terms.push(`(${formatCalcNumber(attackValue)}/${formatCalcNumber(defenseValue)})`);
            if (/魂力驱动/.test(text) && Number.isFinite(Number(values?.soulDriveScale))) terms.push(formatCalcNumber(values.soulDriveScale));
            if (/精神驱动/.test(text) && Number.isFinite(Number(values?.spiritDriveScale))) terms.push(formatCalcNumber(values.spiritDriveScale));
            if (/定位/.test(text) && Number.isFinite(Number(values?.positionDamageScale))) terms.push(formatCalcNumber(values.positionDamageScale));
            if (/近身系数/.test(text) && Number.isFinite(Number(values?.meleeContactScale))) terms.push(formatCalcNumber(values.meleeContactScale));
            if (/消耗加成/.test(text) && Number.isFinite(Number(values?.costDamageScale))) terms.push(formatCalcNumber(values.costDamageScale));
            const fusionDamageMult = Number(values?.fusionDamageMult || 1);
            if (Number.isFinite(fusionDamageMult) && Math.abs(fusionDamageMult - 1) >= 0.0001) terms.push(formatCalcNumber(fusionDamageMult));
            return terms.length >= 2 ? `代入：${terms.join('×')}=${formatCalcNumber(baseDamage)}` : '';
          };
          const 构建伤害计算明细行 = child => {
            const 读取 = key => 读取结算轨迹值(child.calculationTrace, key);
            const parts = [];
            const incoming = Number(读取('incomingDamage') || 0);
            const reactive = Number(读取('reactiveDamage') || 0);
            const threshold = Number(读取('defenseThreshold') || 0);
            const finalDamage = Number(读取('finalDamage') || 0);
            const segmentIndex = Number(读取('segmentIndex') || 0);
            const segmentCount = Number(读取('segmentCount') || 0);
            if (segmentCount > 1 && segmentIndex > 0) parts.push(`第${Math.round(segmentIndex)}/${Math.round(segmentCount)}段`);
            if (incoming > 0) parts.push(`入参${formatCalcNumber(incoming)}`);
            if (reactive > 0 && Math.round(reactive) !== Math.round(incoming)) parts.push(`反应后${formatCalcNumber(reactive)}`);
            if (threshold > 0) parts.push(`破防阈值${formatCalcNumber(threshold)}`);
            [
              ['actualDefense', '有效防御', 0],
              ['soulDriveScale', '魂力驱动', 1],
              ['spiritDriveScale', '精神驱动', 1],
              ['positionDamageScale', '定位', 1],
              ['costDamageScale', '消耗', 1],
              ['fluctuation', '波动', 1],
              ['grazeMultiplier', '擦伤', 1],
              ['damageReduction', '减伤', 0],
              ['receivedDamageMult', '承伤', 1],
              ['elementDamageMult', '元素承伤', 1],
              ['finalDamageMult', '最终倍率', 1],
              ['finalDamageBonus', '最终加值', 0],
              ['activeReactionShield', '反应护盾', 0],
            ].forEach(([key, label, neutral]) => {
              const value = Number(读取(key));
              if (!Number.isFinite(value)) return;
              if (Math.abs(value - neutral) < 0.0001) return;
              parts.push(`${label}${formatCalcNumber(value)}`);
            });
            if (finalDamage > 0) parts.push(`最终${formatCalcNumber(finalDamage)}`);
            return parts.length >= 2 ? `计算明细：${parts.join(' -> ')}` : '';
          };
          const 构建伤害基础公式行 = child => {
            const 读取 = key => 读取结算轨迹值(child.calculationTrace, key);
            const formulaText = String(读取('baseFormulaText') || '').trim();
            const damageType = String(读取('damageType') || '').trim();
            const skillPower = Number(读取('skillPower') || 0);
            const attackValue = Number(读取('attackValue') || 0);
            const defenseValue = Number(读取('defenseValue') || 0);
            const baseDamage = Number(读取('baseDamage') || 0);
            if (!formulaText || !(skillPower > 0) || !(baseDamage > 0)) return '';
            const params = [`威力${formatCalcNumber(skillPower)}`];
            if (attackValue > 0) params.push(`攻势${formatCalcNumber(attackValue)}`);
            if (defenseValue > 0) params.push(`防守${formatCalcNumber(defenseValue)}`);
            const substitution = 构建基础伤害代入式({
              formulaText,
              skillPower,
              attackValue,
              defenseValue,
              baseDamage,
              soulDriveScale: Number(读取('soulDriveScale') || 0),
              spiritDriveScale: Number(读取('spiritDriveScale') || 0),
              positionDamageScale: Number(读取('positionDamageScale') || 0),
              meleeContactScale: Number(读取('meleeContactScale') || 0),
              costDamageScale: Number(读取('costDamageScale') || 0),
              fusionDamageMult: Number(读取('fusionDamageMult') || 1),
            });
            return `基础公式：${damageType ? `【${damageType}】` : ''}${formulaText}，${params.join('，')}，得出${formatCalcNumber(baseDamage)}${substitution ? `；${substitution}` : ''}`;
          };
          const 构建攻防比审计行 = child => {
            const attackValue = Number(读取结算轨迹值(child.calculationTrace, 'attackValue') || 0);
            const defenseValue = Number(读取结算轨迹值(child.calculationTrace, 'defenseValue') || 0);
            if (!(attackValue > 0) || !(defenseValue > 0)) return '';
            const ratio = attackValue / defenseValue;
            if (!Number.isFinite(ratio) || (ratio >= 0.1 && ratio <= 5)) return '';
            return `攻防比审计：攻势 ${formatCalcNumber(attackValue)} / 防守 ${formatCalcNumber(defenseValue)} = ${formatCalcNumber(ratio)}，超出观察阈值 0.1-5；未启用 clamp`;
          };
          const 构建伤害路径行 = child => {
            const 读取 = key => 读取结算轨迹值(child.calculationTrace, key);
            const incoming = Number(读取('incomingDamage') || 0);
            const threshold = Number(读取('defenseThreshold') || 0);
            const finalDamage = Number(读取('finalDamage') || 0);
            if (!(incoming > 0) || !(finalDamage > 0)) return '';
            const parts = [`入参段伤害 ${formatCalcNumber(incoming)}`];
            if (threshold > 0) parts.push(`破防阈值 ${formatCalcNumber(threshold)}`);
            parts.push(`最终扣血 ${formatCalcNumber(finalDamage)}`);
            return `伤害路径：${parts.join(' -> ')}`;
          };
          const 构建闪避判定行 = child => {
            const 读取 = key => 读取结算轨迹值(child.calculationTrace, key);
            const dodgeRate = Number(读取('dodgeRate'));
            const dodgeRoll = Number(读取('dodgeRoll'));
            const failureReason = String(读取('failureReason') || child.failureReason || '').trim();
            const parts = [];
            if (Number.isFinite(dodgeRate) && Number.isFinite(dodgeRoll)) {
              parts.push(`闪避率${formatCalcNumber(dodgeRate)}`);
              parts.push(`投点${formatCalcNumber(dodgeRoll)}`);
              parts.push(dodgeRoll <= dodgeRate ? '投点≤闪避率，规避成功' : '投点>闪避率，规避失败');
            } else if (/dodged|evaded|miss/i.test(failureReason) || child.primaryOutcome === 'miss') parts.push('目标完成规避，落点失效');
            if (failureReason && !/dodged|evaded|miss/i.test(failureReason)) parts.push(`原因${failureReason}`);
            return parts.length ? `闪避判定：${parts.join(' -> ')}` : '';
          };
          const 查找反应窗口伤害结算节点 = reactionNode => {
            const windowId = String(reactionNode?.parentNodeId || '').trim();
            if (!windowId) return null;
            const windowNode = nodeById.get(windowId) || null;
            const rootId = String(windowNode?.parentNodeId || '').trim();
            const windowChildren = byParent.get(windowId) || [];
            const rootChildren = rootId ? (byParent.get(rootId) || []) : [];
            const hitNode = windowChildren.find(node => String(node.nodeKind || '').trim() === 'hit_check') ||
              rootChildren.find(node => String(node.nodeKind || '').trim() === 'hit_check');
            if (!hitNode) return null;
            return (byParent.get(String(hitNode.nodeId || '').trim()) || []).find(node => String(node.nodeKind || '').trim() === 'damage_settlement') || null;
          };
          const 构建防御反应参数行 = reactionNode => {
            const action = normalizeBattleActionDisplayName(reactionNode?.finalActionName || reactionNode?.actionName || reactionNode?.initialActionName || '');
            if (!/承伤硬抗|肉体兜底|防御|偏转|收招转防|借力守势|坚壁/.test(action)) return '';
            const settlement = 查找反应窗口伤害结算节点(reactionNode);
            if (!settlement) return '';
            const 读取 = key => 读取结算轨迹值(settlement.calculationTrace, key);
            const finalDamage = Number(读取('finalDamage') || 0);
            const actualDefense = Number(读取('actualDefense'));
            const threshold = Number(读取('defenseThreshold'));
            const damageReduction = Number(读取('damageReduction'));
            const activeShield = Number(读取('activeReactionShield'));
            const parts = [];
            if (Number.isFinite(actualDefense) && actualDefense > 0) parts.push(`有效防御${formatCalcNumber(actualDefense)}`);
            if (Number.isFinite(threshold) && threshold > 0) parts.push(`破防阈值${formatCalcNumber(threshold)}`);
            if (Number.isFinite(damageReduction) && damageReduction > 0) parts.push(`减伤${formatCalcNumber(damageReduction * 100)}%`);
            if (Number.isFinite(activeShield) && activeShield > 0) parts.push(`反应护盾${formatCalcNumber(activeShield)}`);
            if (finalDamage >= 0) parts.push(`最终承伤${formatCalcNumber(finalDamage)}`);
            return parts.length >= 2 ? `防御判定：${parts.join(' -> ')}` : '';
          };
          const 查找反应来源动作节点 = reactionNode => {
            const directSourceId = String(reactionNode?.sourceNodeId || '').trim();
            if (directSourceId && nodeById.has(directSourceId)) return nodeById.get(directSourceId);
            const windowNode = nodeById.get(String(reactionNode?.parentNodeId || '').trim()) || null;
            const windowSourceId = String(windowNode?.sourceNodeId || '').trim();
            if (windowSourceId && nodeById.has(windowSourceId)) return nodeById.get(windowSourceId);
            const windowParentId = String(windowNode?.parentNodeId || '').trim();
            if (windowParentId && nodeById.has(windowParentId)) return nodeById.get(windowParentId);
            return null;
          };
          const 构建反应来源动作短语 = reactionNode => {
            const sourceNode = 查找反应来源动作节点(reactionNode);
            const sourceActionName = normalizeBattleActionDisplayName(
              读取结算轨迹值(reactionNode?.calculationTrace, 'sourceAction') ||
              reactionNode?.sourceActionName ||
              sourceNode?.finalActionName ||
              sourceNode?.actionName ||
              sourceNode?.initialActionName ||
              '',
            );
            const sourceActorName = String(
              读取结算轨迹值(reactionNode?.calculationTrace, 'sourceActorName') ||
              sourceNode?.actorName ||
              reactionNode?.targetName ||
              '',
            ).trim();
            if (sourceActionName && sourceActorName) return `【${sourceActorName}的${sourceActionName}】`;
            if (sourceActionName) return `【${sourceActionName}】`;
            return '来源未绑定的动作';
          };
          const 构建ReactionAuditDTO = reactionNode => {
            const 读取 = key => 读取结算轨迹值(reactionNode.calculationTrace, key);
            const sourceNode = 查找反应来源动作节点(reactionNode);
            const rawResult = String(读取('result') || reactionNode.result || '').trim();
            const outcome = String(reactionNode.primaryOutcome || '').trim();
            const result = /attempted|尝试/.test(rawResult) && outcome && !/reaction_failed|reaction_window_opened/.test(outcome)
              ? outcome
              : rawResult;
            return {
              sourceActionName: normalizeBattleActionDisplayName(
                读取('sourceAction') ||
                reactionNode.sourceActionName ||
                sourceNode?.finalActionName ||
                sourceNode?.actionName ||
                sourceNode?.initialActionName ||
                '',
              ),
              sourceActorName: String(读取('sourceActorName') || sourceNode?.actorName || reactionNode.targetName || '').trim(),
              reactionActorName: String(读取('reactionActorName') || reactionNode.actorName || '').trim(),
              initialReactionName: normalizeBattleActionDisplayName(读取('initialReaction') || reactionNode.initialActionName || ''),
              finalReactionName: normalizeBattleActionDisplayName(读取('finalReaction') || reactionNode.finalActionName || ''),
              reactionKind: String(读取('reactionKind') || '').trim(),
              result,
              outcome,
            };
          };
          const 构建StateCheckDTO = stateNode => {
            const meta = stateNode?.meta && typeof stateNode.meta === 'object' ? stateNode.meta : {};
            const rateRaw = 读取结算轨迹值(stateNode.calculationTrace, 'successRate') ?? stateNode.successRate ?? meta.successRate;
            const rollRaw = 读取结算轨迹值(stateNode.calculationTrace, 'roll') ?? stateNode.roll ?? meta.roll;
            const successRate = Number(rateRaw);
            const roll = Number(rollRaw);
            const result = String(stateNode.result || '').trim();
            return {
              stateName: String(读取结算轨迹值(stateNode.calculationTrace, 'stateName') || stateNode.stateName || '').trim(),
              successRateBreakdown: String(读取结算轨迹值(stateNode.calculationTrace, 'successRateBreakdown') || stateNode.successRateBreakdown || meta.successRateBreakdown || meta.stateSuccessRateBreakdown || '附着成功率：缺少 successRateBreakdown / successRateReason，无法生成计算式').trim(),
              successRate,
              roll,
              result,
              primaryOutcome: String(stateNode.primaryOutcome || '').trim(),
            };
          };
          const 构建反应决策上下文行 = reactionNode => {
            const dto = 构建ReactionAuditDTO(reactionNode);
            const outcomeText = 格式化反应判定结果(dto);
            const parts = [];
            if (dto.reactionActorName) {
              const sourceText = dto.sourceActionName
                ? (dto.sourceActorName ? `【${dto.sourceActorName}的${dto.sourceActionName}】` : `【${dto.sourceActionName}】`)
                : '来袭动作';
              parts.push(`${dto.reactionActorName}应对${sourceText}`);
            }
            else if (dto.sourceActionName) parts.push(`应对来源【${dto.sourceActionName}】`);
            const finalNoReaction = 判定无反应动作名(dto.finalReactionName);
            if (dto.initialReactionName && dto.finalReactionName && dto.initialReactionName !== dto.finalReactionName) {
              if (finalNoReaction) {
                parts.push(判定可显示为原定反应动作(dto.initialReactionName)
                  ? `原定【${dto.initialReactionName}】被压住，未能展开`
                  : '节奏被压住，未能展开应对');
              } else {
                parts.push(`原定【${dto.initialReactionName}】 -> 改为【${dto.finalReactionName}】`);
              }
            } else if (dto.finalReactionName) {
              parts.push(finalNoReaction ? '未能作出有效反应' : `选择【${dto.finalReactionName}】`);
            }
            const readableKind = 格式化玩家反应类型(dto.reactionKind);
            const readableResult = outcomeText || 格式化玩家判定结果(dto.result);
            if (!finalNoReaction && readableKind) parts.push(`类型：${readableKind}`);
            if (readableResult) parts.push(`结果：${readableResult}`);
            return parts.length >= 2 ? `反应判定：${parts.join('，')}` : '';
          };
          const 构建反应决策参数行 = reactionNode => {
            const 读取 = key => 读取结算轨迹值(reactionNode.calculationTrace, key);
            const parts = [];
            const ratio = Number(读取('reactionRatio'));
            const sourceActorName = String(读取('sourceActorName') || reactionNode.targetName || '').trim();
            const reactionActorName = String(读取('reactionActorName') || reactionNode.actorName || '').trim();
            const castTimeGap = Number(读取('castTimeGap'));
            const attackerCastTime = Number(读取('attackerCastTime'));
            const reactorCastTime = Number(读取('reactorCastTime'));
            const threatScore = Number(读取('threatScore'));
            const attackerSpeed = Number(读取('sourceActionSpeed') || 读取('attackerSpeed'));
            const defenderReaction = Number(读取('reactionValue') || 读取('defenderReaction'));
            const attackerAgility = Number(读取('sourceAgility') || 读取('attackerAgility'));
            const defenderAgility = Number(读取('reactionAgility') || 读取('defenderAgility'));
            const defenderMentalMax = Number(读取('reactionMental') || 读取('defenderMentalMax'));
            const castPenalty = Number(读取('castPenalty'));
            const attackerSpeedBonus = Number(读取('attackerSpeedBonus'));
            const defenderReactionBonus = Number(读取('defenderReactionBonus'));
            const defenderReactionPenalty = Number(读取('defenderReactionPenalty'));
            const defenderAgilityMult = Number(读取('defenderAgilityMult'));
            const maintainReactionPenalty = Number(读取('maintainReactionPenalty'));
            const defenderAgilityComponent = Number(读取('defenderAgilityComponent'));
            const defenderMentalComponent = Number(读取('defenderMentalComponent'));
            const reactionBaseBeforeMultiplier = Number(读取('reactionBaseBeforeMultiplier'));
            const maintainReactionMultiplier = Number(读取('maintainReactionMultiplier'));
            const reactionBudgetMultiplier = Number(读取('reactionBudgetMultiplier'));
            const experienceReactionMultiplier = Number(读取('experienceReactionMultiplier'));
            const ratioPostMultiplier = Number(读取('ratioPostMultiplier'));
            const firstStrikePenalty = 读取('firstStrikePenalty') === true || 读取('firstStrikePenalty') === 'true';
            if (Number.isFinite(ratio) && ratio > 0) {
              if (!(Number.isFinite(attackerSpeed) && attackerSpeed > 0 && Number.isFinite(defenderReaction) && defenderReaction > 0)) {
                return '';
              }
              const ratioFormula = [
                `${reactionActorName ? `${reactionActorName}反应` : '应招方反应'}${formatCalcNumber(defenderReaction)} / ${sourceActorName ? `${sourceActorName}攻速` : '攻方速度'}${formatCalcNumber(attackerSpeed)}`,
              ];
              if (Number.isFinite(ratioPostMultiplier) && Math.abs(ratioPostMultiplier - 1) >= 0.0001) {
                ratioFormula.push(`${firstStrikePenalty ? '先攻压制' : '比值修正'}${formatCalcNumber(ratioPostMultiplier)}`);
              }
              parts.push(`反应比值 = ${ratioFormula.join(' × ')} = ${formatCalcNumber(ratio)}`);
              const reactionBaseParts = [];
              if (Number.isFinite(defenderAgility) && defenderAgility > 0) {
                const agilityText = Number.isFinite(defenderAgilityMult) && Math.abs(defenderAgilityMult - 1) >= 0.0001
                  ? `敏捷${formatCalcNumber(defenderAgility)}×${formatCalcNumber(defenderAgilityMult)}`
                  : `敏捷${formatCalcNumber(defenderAgility)}`;
                reactionBaseParts.push(agilityText);
              } else if (Number.isFinite(defenderAgilityComponent) && defenderAgilityComponent > 0) {
                reactionBaseParts.push(`敏捷项${formatCalcNumber(defenderAgilityComponent)}`);
              }
              if (Number.isFinite(defenderMentalMax) && defenderMentalMax > 0) reactionBaseParts.push(`精神${formatCalcNumber(defenderMentalMax)}`);
              else if (Number.isFinite(defenderMentalComponent) && defenderMentalComponent > 0) reactionBaseParts.push(`精神项${formatCalcNumber(defenderMentalComponent)}`);
              if (Number.isFinite(defenderReactionBonus) && defenderReactionBonus > 0) reactionBaseParts.push(`反应加值${formatCalcNumber(defenderReactionBonus)}`);
              if (Number.isFinite(defenderReactionPenalty) && defenderReactionPenalty > 0) reactionBaseParts.push(`反应惩罚-${formatCalcNumber(defenderReactionPenalty)}`);
              const reactionMultiplierParts = [];
              if (Number.isFinite(maintainReactionMultiplier) && Math.abs(maintainReactionMultiplier - 1) >= 0.0001) {
                reactionMultiplierParts.push(`维持修正${formatCalcNumber(maintainReactionMultiplier)}`);
              } else if (Number.isFinite(maintainReactionPenalty) && maintainReactionPenalty > 0) {
                reactionMultiplierParts.push(`维持修正${formatCalcNumber(1 - Math.min(0.25, Math.max(0, maintainReactionPenalty)))}`);
              }
              if (Number.isFinite(reactionBudgetMultiplier) && Math.abs(reactionBudgetMultiplier - 1) >= 0.0001) reactionMultiplierParts.push(`反应预算${formatCalcNumber(reactionBudgetMultiplier)}`);
              if (Number.isFinite(experienceReactionMultiplier) && Math.abs(experienceReactionMultiplier - 1) >= 0.0001) reactionMultiplierParts.push(`经验修正${formatCalcNumber(experienceReactionMultiplier)}`);
              if (reactionBaseParts.length) {
                const baseText = reactionBaseParts.join(' + ').replace(/\+ 反应惩罚-/g, '- 反应惩罚');
                const baseFormula = Number.isFinite(reactionBaseBeforeMultiplier) && Math.abs(reactionBaseBeforeMultiplier - defenderReaction) >= 0.01
                  ? `(${baseText})`
                  : baseText;
                const multiplierText = reactionMultiplierParts.length ? ` × ${reactionMultiplierParts.join(' × ')}` : '';
                parts.push(`防守反应来源：${baseFormula}${multiplierText} = ${formatCalcNumber(defenderReaction)}`);
              }
              const speedParts = [];
              if (Number.isFinite(attackerAgility) && attackerAgility > 0) speedParts.push(`敏捷${formatCalcNumber(attackerAgility)}`);
              if (Number.isFinite(castPenalty) && castPenalty > 0) speedParts.push(`前摇惩罚${formatCalcNumber(castPenalty * 100)}%`);
              if (Number.isFinite(attackerSpeedBonus) && attackerSpeedBonus > 0) speedParts.push(`速度加值${formatCalcNumber(attackerSpeedBonus)}`);
              if (speedParts.length) parts.push(`攻方速度来源：${speedParts.join('，')}`);
            }
            [
              [castTimeGap, '前摇差'],
              [attackerCastTime, '攻方前摇'],
              [reactorCastTime, '应招前摇'],
              [threatScore, '威胁评分'],
            ].forEach(([value, label]) => {
              if (!Number.isFinite(value) || Math.abs(value) < 0.0001) return;
              parts.push(`${label}${formatCalcNumber(value)}`);
            });
            return parts.length ? `反应依据：${parts.join(' -> ')}` : '';
          };
          const 构建子级树前缀 = (parentPrefix = '├─', isLast = false) => {
            const raw = String(parentPrefix || '├─');
            let stem = raw;
            if (/[├└]─$/.test(stem)) stem = stem.slice(0, -2);
            if (/└─$/.test(raw)) stem += '│ ';
            else stem += '│ ';
            return `${stem}${isLast ? '└─' : '├─'}`;
          };
          const 筛选可见因果子节点 = nodes => {
            const visible = [];
            const reactionIndexByKey = new Map();
            (Array.isArray(nodes) ? nodes : []).forEach(node => {
              if (String(node?.nodeKind || '').trim() !== 'reaction_decision') {
                visible.push(node);
                return;
              }
              const action = normalizeBattleActionDisplayName(node.finalActionName || node.actionName || node.initialActionName || '行动');
              const sourceAction = String(读取结算轨迹值(node.calculationTrace, 'sourceAction') || node.sourceAction || '').trim();
              const key = [
                String(node.actorName || '').trim(),
                String(node.targetName || '').trim(),
                action,
                sourceAction,
              ].join('|');
              const score = (读取结算轨迹值(node.calculationTrace, 'initialReaction') ? 4 : 0)
                + (读取结算轨迹值(node.calculationTrace, 'reactionRatio') ? 2 : 0)
                + (!/attempted|尝试/.test(String(node.result || '').trim()) ? 3 : 0)
                + (Array.isArray(node.calculationTrace) ? Math.min(6, node.calculationTrace.length) : 0);
              const existingIndex = reactionIndexByKey.get(key);
              if (existingIndex === undefined) {
                reactionIndexByKey.set(key, visible.length);
                visible.push({ ...node, __visibleScore: score });
              } else {
                const existing = visible[existingIndex] || {};
                const mergedTrace = [];
                const seenTraceKeys = new Set();
                [...(Array.isArray(existing.calculationTrace) ? existing.calculationTrace : []), ...(Array.isArray(node.calculationTrace) ? node.calculationTrace : [])].forEach(item => {
                  const traceKey = `${String(item?.key || '').trim()}|${String(item?.label || '').trim()}`;
                  if (!traceKey.trim() || seenTraceKeys.has(traceKey)) return;
                  seenTraceKeys.add(traceKey);
                  mergedTrace.push(item);
                });
                const existingResult = String(existing.result || '').trim();
                const nextResult = String(node.result || '').trim();
                const result = (!nextResult || /attempted|尝试/.test(nextResult)) ? existingResult : nextResult;
                visible[existingIndex] = {
                  ...(score > Number(existing.__visibleScore || 0) ? node : existing),
                  result: result || existingResult || nextResult,
                  primaryOutcome: /attempted|尝试/.test(String(node.primaryOutcome || '').trim()) ? existing.primaryOutcome : (node.primaryOutcome || existing.primaryOutcome),
                  calculationTrace: mergedTrace,
                  __visibleScore: Math.max(score, Number(existing.__visibleScore || 0)),
                };
              }
            });
            return visible;
          };
          const 读取投影去重键 = child => {
            const kind = String(child?.nodeKind || '').trim();
            if (!['state_settlement', 'damage_settlement', 'final_result'].includes(kind)) return '';
            const ledgerIds = Array.isArray(child?.ledgerEventIds) ? child.ledgerEventIds.map(id => String(id || '').trim()).filter(Boolean) : [];
            if (ledgerIds.length) return `${kind}|${ledgerIds.join(',')}`;
            if (kind === 'state_settlement') {
              const stateName = String(读取结算轨迹值(child?.calculationTrace, 'stateName') || child?.stateName || '').trim();
              const targetName = String(child?.targetName || target || '').trim();
              const sourceAction = normalizeBattleActionDisplayName(读取结算轨迹值(child?.calculationTrace, 'sourceAction') || child?.sourceActionName || finalAction || '');
              if (stateName && targetName) return `${kind}|${sourceAction}|${targetName}|${stateName}`;
            }
            const nodeId = String(child?.nodeId || '').trim();
            return nodeId ? `${kind}|${nodeId}` : '';
          };
          const pushChildLine = (child, prefix = '├─') => {
            const kind = String(child.nodeKind || '').trim();
            const projectionKey = 读取投影去重键(child);
            if (projectionKey && renderedSettlementKeys.has(projectionKey)) return;
            if (projectionKey) renderedSettlementKeys.add(projectionKey);
            const timelineKind = mapTimelineKindFromNode(kind);
            const pushChildTimelineLine = text => pushTimelineLine(text, timelineKind);
            const childActor = String(child.actorName || '').trim();
            const childTarget = String(child.targetName || '').trim();
            const childAction = normalizeBattleActionDisplayName(child.finalActionName || child.actionName || child.initialActionName || '行动');
            if (kind === 'replan_decision') {
              const discarded = normalizeBattleActionDisplayName(child.discardedActionName || child.initialActionName || '原计划');
              const reason = 映射行动链变招原因短语(child);
              const replanKey = `${discarded}|${childAction}|${reason}`;
              if (renderedReplanKeys.has(replanKey)) return;
              renderedReplanKeys.add(replanKey);
              pushChildTimelineLine(`${prefix} 原计划【${discarded}】${reason ? `，因${reason}` : ''}改为【${childAction}】`);
            } else if (kind === 'reaction_window') {
              pushChildTimelineLine(`${prefix} 反应窗口：${childActor || childTarget || '目标'}捕捉到${构建反应来源动作短语(child)}`);
              筛选可见因果子节点(byParent.get(String(child.nodeId || '').trim()) || [])
                .filter(node => ['reaction_decision', 'hit_check', 'state_check', 'damage_settlement', 'state_settlement', 'final_result'].includes(String(node.nodeKind || '')))
                .forEach((node, index, list) => pushChildLine(node, 构建子级树前缀(prefix, index === list.length - 1)));
            } else if (kind === 'reaction_decision') {
              const reactionSubject = childActor || childTarget || '目标';
              if (/无暇反应|无法反应|无反应|来不及反应/.test(childAction)) {
                pushChildTimelineLine(`${prefix} 反应：${reactionSubject}错过应对窗口`);
              } else {
                pushChildTimelineLine(`${prefix} 反应：${reactionSubject}以【${childAction}】应对`);
              }
              const contextLine = 构建反应决策上下文行(child);
              if (contextLine) pushDetailLine(`${构建子级树前缀(prefix, false)} ${contextLine}`, timelineKind);
              const reactionCalcLine = 构建反应决策参数行(child);
              if (reactionCalcLine) pushDetailLine(`${构建子级树前缀(prefix, false)} ${reactionCalcLine}`, timelineKind);
              const defenseLine = 构建防御反应参数行(child);
              if (defenseLine) pushDetailLine(`${构建子级树前缀(prefix, false)} ${defenseLine}`, timelineKind);
            } else if (kind === 'hit_check') {
              const result = String(child.result || '').trim();
              const missed = /miss|evade|dodge|未命中|闪避/.test(result) || child.primaryOutcome === 'miss';
              pushChildTimelineLine(missed
                ? `${prefix} 命中检定：${actor}的【${finalAction}】未能命中${childTarget || target || '目标'}`
                : `${prefix} 命中检定：${actor}的【${finalAction}】锁定${childTarget || target || '目标'}，攻势落定`);
              const dodgeLine = 构建闪避判定行(child);
              if (dodgeLine) pushDetailLine(`${构建子级树前缀(prefix, false)} ${dodgeLine}`, timelineKind);
              (byParent.get(String(child.nodeId || '').trim()) || [])
                .filter(node => ['damage_settlement', 'state_settlement', 'final_result'].includes(String(node.nodeKind || '')))
                .forEach((node, index, list) => pushChildLine(node, 构建子级树前缀(prefix, index === list.length - 1)));
            } else if (kind === 'state_check') {
              const stateDTO = 构建StateCheckDTO(child);
              const immune = /immune|immunity|免疫|无视异常/.test(stateDTO.result) || /immune/.test(stateDTO.primaryOutcome);
              const resisted = /resist|resisted|抵抗|豁免|未附着/.test(stateDTO.result) || /resisted/.test(stateDTO.primaryOutcome);
              if (stateDTO.stateName) pushChildTimelineLine(immune
                ? `${prefix} 状态检定：${childTarget || target || '目标'}免疫【${stateDTO.stateName}】附着`
                : resisted
                ? `${prefix} 状态检定：${childTarget || target || '目标'}抵住【${stateDTO.stateName}】附着`
                : `${prefix} 状态检定：【${stateDTO.stateName}】锁定${childTarget || target || '目标'}，开始附着`);
              if (stateDTO.successRateBreakdown) {
                pushDetailLine(`${构建子级树前缀(prefix, false)} ${stateDTO.successRateBreakdown}`, timelineKind);
              }
              (byParent.get(String(child.nodeId || '').trim()) || [])
                .filter(node => ['state_settlement', 'final_result'].includes(String(node.nodeKind || '')))
                .forEach((node, index, list) => pushChildLine(node, 构建子级树前缀(prefix, index === list.length - 1)));
            } else if (kind === 'damage_settlement') {
              const finalDamage = Number(读取结算轨迹值(child.calculationTrace, 'finalDamage') || 0);
              const result = String(child.result || '').trim();
              if (/miss|evade|dodge|未命中|闪避/.test(result) || child.primaryOutcome === 'miss') pushChildTimelineLine(`${prefix} 伤害结算：${childTarget || target || '目标'}未受到实质伤害`);
              else pushChildTimelineLine(`${prefix} 伤害结算：${childTarget || target || '目标'}${finalDamage > 0 ? `受到 ${Math.round(finalDamage)} 点伤害` : '未受到实质伤害'}`);
              const calcLine = 构建伤害计算明细行(child);
              const formulaLine = 构建伤害基础公式行(child);
              const ratioAuditLine = 构建攻防比审计行(child);
              const pathLine = 构建伤害路径行(child);
              [formulaLine, ratioAuditLine, pathLine, calcLine]
                .filter(Boolean)
                .forEach((line, index, list) => pushDetailLine(`${构建子级树前缀(prefix, index === list.length - 1)} ${line}`, timelineKind));
              (byParent.get(String(child.nodeId || '').trim()) || [])
                .filter(node => String(node.nodeKind || '') === 'summon_assist')
                .forEach((node, index, list) => pushChildLine(node, 构建子级树前缀(prefix, index === list.length - 1)));
            } else if (kind === 'state_settlement') {
              const stateName = String(读取结算轨迹值(child.calculationTrace, 'stateName') || child.stateName || '').trim();
              const duration = Math.max(0, Number(读取结算轨迹值(child.calculationTrace, 'duration') || child.duration || 0));
              const immune = /immune|immunity|免疫|无视异常/.test(String(child.result || '')) || /immune/.test(String(child.primaryOutcome || ''));
              if (stateName) pushChildTimelineLine(`${prefix} 状态结算：${childTarget || target || '目标'}${immune ? '免疫' : (/resist|resisted|抵抗|豁免/.test(String(child.result || '')) ? '抵住' : '陷入')}【${stateName}】${duration > 0 && !immune ? `，持续${duration}回合` : ''}`);
            } else if (kind === 'counter_action') {
              const damage = Number(读取结算轨迹值(child.calculationTrace, 'finalDamage') || 读取结算轨迹值(child.calculationTrace, 'damage') || 0);
              const counterLabel = Number(child.counterDepth || 0) >= 2 ? '反防反分支' : '防反分支';
              pushChildTimelineLine(`${prefix} ${counterLabel}：${childActor || '防守方'}以【${childAction}】反击${childTarget || actor}${damage > 0 ? `，造成 ${Math.round(damage)} 点伤害` : ''}`);
              const formulaLine = 构建伤害基础公式行(child);
              const ratioAuditLine = 构建攻防比审计行(child);
              const pathLine = 构建伤害路径行(child);
              const calcLine = 构建伤害计算明细行(child);
              const counterCalcLines = [formulaLine, ratioAuditLine, pathLine, calcLine].filter(Boolean);
              筛选可见因果子节点(byParent.get(String(child.nodeId || '').trim()) || [])
                .filter(node => ['reaction_window', 'reaction_decision'].includes(String(node.nodeKind || '')))
                .forEach((node, index, list) => pushChildLine(node, 构建子级树前缀(prefix, !counterCalcLines.length && index === list.length - 1)));
              counterCalcLines
                .forEach((line, index, list) => pushDetailLine(`${构建子级树前缀(prefix, index === list.length - 1)} ${line}`, timelineKind));
              (byParent.get(String(child.nodeId || '').trim()) || [])
                .filter(node => ['target_branch', 'counter_window'].includes(String(node.nodeKind || '')) && 因果链节点默认可见(node))
                .forEach((node, index, list) => pushChildLine(node, 构建子级树前缀(prefix, index === list.length - 1)));
            } else if (kind === 'summon_assist') {
              const damage = Number(读取结算轨迹值(child.calculationTrace, 'damage') || 读取结算轨迹值(child.calculationTrace, 'finalDamage') || 0);
              pushChildTimelineLine(`${prefix} 召唤协同追击：${childActor || '召唤物'}以【${childAction}】承接攻势${childTarget ? `追击${childTarget}` : ''}${damage > 0 ? `，造成 ${Math.round(damage)} 点伤害` : ''}`);
            } else if (kind === 'final_result' && String(child.primaryOutcome || '').trim() === 'summon_created') {
              const summonName = String(读取结算轨迹值(child.calculationTrace, 'summonName') || child.summonName || childTarget || '').trim();
              const summonType = String(读取结算轨迹值(child.calculationTrace, 'summonType') || child.summonType || '').trim();
              const summonMode = String(读取结算轨迹值(child.calculationTrace, 'summonMode') || child.summonMode || '').trim();
              const mentalLoad = Number(读取结算轨迹值(child.calculationTrace, 'mentalLoad') || child.mentalLoad || 0);
              pushChildTimelineLine(`${prefix} 召唤生成：召出${summonType || '召唤物'}【${summonName || '召唤物'}】${summonMode ? `，行动模式：${summonMode}` : ''}${mentalLoad > 0 ? `，精神负载 ${Math.round(mentalLoad)}` : ''}`);
            } else if (kind === 'final_result') {
              const outcome = String(child.primaryOutcome || '').trim();
              const rawReason = String(child.failureReason || child.failReason || child.reasonText || 读取结算轨迹值(child.calculationTrace, 'failureReason') || 读取结算轨迹值(child.calculationTrace, 'failReason') || '').trim();
              let reason = 映射玩家可见失败原因(child.reasonCode || child.meta?.reasonCode || 读取结算轨迹值(child.calculationTrace, 'reasonCode') || '', rawReason) ||
                (outcome === 'interrupted' ? '被对手截断' : '战机已经消散');
              reason = String(reason || '')
                .replace(/，?未能形成有效出手/g, '')
                .replace(/，?未能完成出手/g, '')
                .replace(/，?未能展开/g, '')
                .replace(/[，。；\s]+$/g, '')
                .trim();
              if (/interrupted|blocked|failed|no_effect/.test(`${outcome} ${child.result || ''}`)) {
                const actionText = `${childAction} ${child.actionType || ''} ${child.type || ''}`;
                if (判定守势待机动作(actionText) && /no_effect|no_valid_window|NO_EFFECTIVE_OPENING|未形成主动结算效果|没有形成主动结算效果|稳住身位|观察战局|no_effective_opening/i.test(`${outcome} ${child.result || ''} ${rawReason} ${child.reasonCode || child.meta?.reasonCode || ''}`)) {
                  pushChildTimelineLine(`${prefix} 守势调整：${构建守势待机短句(childActor || actor, actionText)}`);
                } else {
                  pushChildTimelineLine(`${prefix} ${childActor || actor}的【${childAction}】${reason ? `因${reason}，` : ''}未能完成出手`);
                }
              }
            } else if (kind === 'counter_window') {
              const opened = String(child.result || '').trim() === 'opened' || child.reasonCode === 'COUNTER_WINDOW_OPENED';
              const counterWindowLabel = Number(child.counterDepth || 0) >= 2 ? '反防反窗口' : '防反窗口';
              pushChildTimelineLine(opened
                ? `${prefix} ${counterWindowLabel}：${childActor || '防守方'}捕捉到${childTarget || actor || '目标'}的破绽`
                : `${prefix} ${counterWindowLabel}：${childActor || '防守方'}窥见破绽，但错失反打时机，未能反打`);
              (byParent.get(String(child.nodeId || '').trim()) || [])
                .filter(node => ['counter_action', 'damage_settlement', 'state_settlement', 'final_result'].includes(String(node.nodeKind || '')))
                .forEach((node, index, list) => pushChildLine(node, 构建子级树前缀(prefix, index === list.length - 1)));
            }
          };
          children.forEach(child => {
            const kind = String(child.nodeKind || '').trim();
            if (kind === 'target_branch') {
              const branchTarget = String(child.targetName || '目标').trim();
              pushTimelineLine(`├─ 目标分支：${branchTarget}`, 'branch');
              const branchChildren = 筛选可见因果子节点(byParent.get(String(child.nodeId || '').trim()) || []);
              branchChildren.filter(node => ['replan_decision', 'reaction_window', 'reaction_decision', 'hit_check', 'state_check', 'damage_settlement', 'state_settlement', 'counter_action', 'counter_window', 'summon_assist', 'final_result'].includes(String(node.nodeKind || '')) && 因果链节点默认可见(node)).forEach((node, index, list) => pushChildLine(node, 构建子级树前缀('├─', index === list.length - 1)));
              return;
            }
            pushChildLine(child);
          });
          if (lines.length) lines[lines.length - 1].text = String(lines[lines.length - 1].text || '').replace(/^├─/, '└─');
          const actionPrefix = String(root.source || '').trim() === 'summon' ? '召唤自主行动' : '执行';
          const title = `[行动链] ${actor} ${actionPrefix}【${finalAction}】${target ? ` -> ${target}` : ''}`;
          const 展示权重 = 计算因果链展示权重(条目);
          const summary = 展示权重.reasons.length ? `${title}｜${展示权重.reasons.join('、')}` : title;
          const treeHtml = `<div class="battle-preview-trace-tree battle-preview-trace-timeline">${lines.map(line => {
            const kind = String(line?.kind || 'detail').trim() || 'detail';
            return `<div class="battle-preview-trace-node battle-preview-trace-node--${htmlEscapeText(kind)}" data-trace-node-kind="${htmlEscapeText(kind)}"><span>${渲染判定侧写HTML(line?.text || '')}</span></div>`;
          }).join('\n')}</div>`;
          const detailHtml = detailLines.length ? `
            <details class="battle-preview-trace-detail-fold">
              <summary>展开判定明细</summary>
              <div class="battle-preview-trace-tree battle-preview-trace-timeline battle-preview-trace-detail-tree">${detailLines.map(line => {
                const kind = String(line?.kind || 'detail').trim() || 'detail';
                return `<div class="battle-preview-trace-node battle-preview-trace-node--${htmlEscapeText(kind)} battle-preview-trace-node--detail" data-trace-node-kind="${htmlEscapeText(kind)}"><span>${渲染判定侧写HTML(line?.text || '')}</span></div>`;
              }).join('\n')}</div>
            </details>
          ` : '';
          return `
            <details class="battle-preview-trace-row battle-preview-trace-card battle-preview-trace-card--action-chain battle-preview-action-chain-fold" data-smart-collapse="1" data-event-weight="${htmlEscapeText(展示权重.eventWeight)}"${展示权重.childEscalated ? ' data-child-escalated="1"' : ''}${展示权重.collapse ? '' : ' open'}>
              <summary class="battle-preview-trace-title"><b>${htmlEscapeText(summary)}</b></summary>
              ${treeHtml}
              ${detailHtml}
            </details>
          `;
        }
        function 格式化结算数值(value) {
          const number = Number(value);
          if (!Number.isFinite(number)) return '';
          return Math.abs(number - Math.round(number)) < 0.01 ? String(Math.round(number)) : number.toFixed(2).replace(/0+$/, '').replace(/\.$/, '');
        }

        function 构建基础伤害代入式(values = {}) {
          const text = String(values.formulaText || '').trim();
          const skillPower = Number(values.skillPower || 0);
          const attackValue = Number(values.attackValue || 0);
          const defenseValue = Number(values.defenseValue || 0);
          const baseDamage = Number(values.baseDamage || 0);
          if (!(skillPower > 0) || !(baseDamage > 0)) return '';
          const terms = [格式化结算数值(skillPower)];
          if (attackValue > 0 && defenseValue > 0) terms.push(`(${格式化结算数值(attackValue)}/${格式化结算数值(defenseValue)})`);
          if (/魂力驱动/.test(text) && Number.isFinite(Number(values.soulDriveScale))) terms.push(格式化结算数值(values.soulDriveScale));
          if (/精神驱动/.test(text) && Number.isFinite(Number(values.spiritDriveScale))) terms.push(格式化结算数值(values.spiritDriveScale));
          if (/定位/.test(text) && Number.isFinite(Number(values.positionDamageScale))) terms.push(格式化结算数值(values.positionDamageScale));
          if (/近身系数/.test(text) && Number.isFinite(Number(values.meleeContactScale))) terms.push(格式化结算数值(values.meleeContactScale));
          if (/消耗加成/.test(text) && Number.isFinite(Number(values.costDamageScale))) terms.push(格式化结算数值(values.costDamageScale));
          const fusionDamageMult = Number(values.fusionDamageMult || 1);
          if (Number.isFinite(fusionDamageMult) && Math.abs(fusionDamageMult - 1) >= 0.0001) terms.push(格式化结算数值(fusionDamageMult));
          return terms.length >= 2 ? `代入：${terms.join('×')}=${格式化结算数值(baseDamage)}` : '';
        }

        function 构建结算链事件明细(event = {}) {
          if (String(event?.eventKind || '').trim() !== 'hit_result') return [];
          const meta = event?.meta && typeof event.meta === 'object' ? event.meta : {};
          const result = String(event?.result || '').trim();
          const incoming = Math.max(0, Number(meta.incomingDamage || 0));
          const reactive = Math.max(0, Number(meta.reactiveDamage || 0));
          const threshold = Math.max(0, Number(meta.defenseThreshold || 0));
          const finalDamage = Math.max(0, Number(meta.damage || event.damage || 0));
          const segmentIndex = Math.max(0, Number(meta.segmentIndex || 0));
          const segmentCount = Math.max(0, Number(meta.segmentCount || 0));
          const lines = [];
          if (/miss|evade|dodge|未命中|闪避/.test(result)) {
            const dodgeRate = Number(meta.dodgeRate);
            const dodgeRoll = Number(meta.dodgeRoll);
            const parts = ['命中被规避'];
            if (Number.isFinite(dodgeRate) && dodgeRate > 0) parts.push(`闪避率 ${格式化结算数值(dodgeRate)}`);
            if (Number.isFinite(dodgeRoll) && dodgeRoll > 0) parts.push(`投点 ${格式化结算数值(dodgeRoll)}`);
            lines.push(`检定链：${parts.join(' -> ')}`);
            return lines;
          }
          const formulaTrace = meta.formulaTrace && typeof meta.formulaTrace === 'object' ? meta.formulaTrace : meta;
          const baseFormulaText = String(formulaTrace.formulaText || meta.baseFormulaText || '').trim();
          const damageType = String(formulaTrace.damageType || meta.damageType || '').trim();
          const skillPower = Number(formulaTrace.skillPower || meta.skillPower || 0);
          const attackValue = Number(formulaTrace.attackValue || meta.formulaAttackValue || 0);
          const defenseValue = Number(formulaTrace.defenseValue || meta.formulaDefenseValue || 0);
          const baseDamage = Number(formulaTrace.baseDamage || meta.baseFormulaDamage || 0);
          if (baseFormulaText && skillPower > 0 && baseDamage > 0) {
            const params = [`威力 ${格式化结算数值(skillPower)}`];
            if (attackValue > 0) params.push(`攻势 ${格式化结算数值(attackValue)}`);
            if (defenseValue > 0 && defenseValue !== 1) params.push(`防守 ${格式化结算数值(defenseValue)}`);
            const substitution = 构建基础伤害代入式({
              formulaText: baseFormulaText,
              skillPower,
              attackValue,
              defenseValue,
              baseDamage,
              soulDriveScale: Number(meta.soulDriveScale || formulaTrace.soulDriveScale || 0),
              spiritDriveScale: Number(meta.spiritDriveScale || formulaTrace.spiritDriveScale || 0),
              positionDamageScale: Number(meta.positionDamageScale || formulaTrace.positionDamageScale || 0),
              meleeContactScale: Number(meta.meleeContactScale || formulaTrace.meleeContactScale || 0),
              costDamageScale: Number(meta.costDamageScale || formulaTrace.costDamageScale || 0),
              fusionDamageMult: Number(meta.fusionDamageMult || formulaTrace.fusionDamageMult || 1),
            });
            lines.push(`基础公式：${damageType ? `【${damageType}】` : ''}${baseFormulaText}，${params.join('，')}，得出 ${格式化结算数值(baseDamage)}${substitution ? `；${substitution}` : ''}`);
          }
          const damagePath = [];
          if (segmentCount > 1 && segmentIndex > 0) damagePath.push(`第${格式化结算数值(segmentIndex)}/${格式化结算数值(segmentCount)}段`);
          if (incoming > 0) damagePath.push(`入参段伤害 ${格式化结算数值(incoming)}`);
          if (reactive > 0 && Math.round(reactive) !== Math.round(incoming)) damagePath.push(`反应/元素后 ${格式化结算数值(reactive)}`);
          if (threshold > 0) damagePath.push(`破防阈值 ${格式化结算数值(threshold)}`);
          if (finalDamage > 0) damagePath.push(`最终扣血 ${格式化结算数值(finalDamage)}`);
          if (damagePath.length >= 2) lines.push(`伤害路径：${damagePath.join(' -> ')}`);

          const attackDefenseParts = [];
          const actualDefense = Number(meta.actualDefense);
          if (Number.isFinite(actualDefense) && actualDefense > 0) attackDefenseParts.push(`有效防御 ${格式化结算数值(actualDefense)}`);
          if (Number(meta.defenseStrip || 0) > 0) attackDefenseParts.push(`防御剥夺 ${格式化结算数值(Number(meta.defenseStrip) * 100)}%`);
          if (Number(meta.spiritResistStrip || 0) > 0) attackDefenseParts.push(`精神抗性剥夺 ${格式化结算数值(Number(meta.spiritResistStrip) * 100)}%`);
          if (threshold > 0) attackDefenseParts.push(segmentCount > 1 ? '破防阈值=防御×0.008÷√段数' : '破防阈值=防御×0.008');
          if (attackDefenseParts.length) lines.push(`攻防参数：${attackDefenseParts.join('，')}`);

          const multiplierParts = [
            ['soulDriveScale', '魂力驱动', 1, 'x'],
            ['spiritDriveScale', '精神驱动', 1, 'x'],
            ['positionDamageScale', '定位', 1, 'x'],
            ['costDamageScale', '消耗', 1, 'x'],
            ['fluctuation', '波动', 1, 'x'],
            ['damageReduction', '减伤', 0, '-'],
            ['jadeHandReduction', '玄玉手', 0, '-'],
            ['receivedDamageMult', '承伤', 1, 'x'],
            ['elementDamageMult', '元素承伤', 1, 'x'],
            ['finalDamageMult', '最终倍率', 1, 'x'],
            ['finalDamageBonus', '最终加值', 0, '+'],
            ['activeReactionShield', '反应护盾', 0, '-'],
          ].map(([key, label, neutral, prefix]) => {
            const value = Number(meta[key]);
            if (!Number.isFinite(value) || Math.abs(value - neutral) < 0.0001) return '';
            const displayValue = 格式化结算数值(value);
            if (String(displayValue) === String(格式化结算数值(neutral))) return '';
            if (prefix === 'x') return `${label}×${格式化结算数值(value)}`;
            if (prefix === '-') return `${label}-${格式化结算数值(key.includes('Reduction') ? value * 100 : value)}${key.includes('Reduction') ? '%' : ''}`;
            return `${label}+${格式化结算数值(value)}`;
          }).filter(Boolean);
          if (multiplierParts.length) lines.push(`倍率修正：${multiplierParts.join('，')}`);

          const breakType = String(meta.breakType || '').trim();
          if ((/graze|chip|擦伤/.test(result) || breakType) && reactive > 0 && threshold > 0) {
            const ratio = reactive / Math.max(1, threshold);
            if (ratio >= 0.22) {
              const grazeScale = 0.32 + Math.min(0.46, ratio * 0.48);
              lines.push(`擦伤公式：破防接近度 ${格式化结算数值(ratio)}，韧性削伤=floor(${格式化结算数值(reactive)}×${格式化结算数值(grazeScale)})=${格式化结算数值(finalDamage)}`);
            } else {
              lines.push(`擦伤公式：破防接近度 ${格式化结算数值(ratio)} < 0.22，仅保留 1 点强制伤害`);
            }
          } else if (reactive > 0 && threshold > 0) {
            lines.push(`破防判定：${格式化结算数值(reactive)} ≥ ${格式化结算数值(threshold)}，进入直接伤害结算`);
          }
          return lines;
        }

        function 格式化结算链事件文本(event = {}) {
          const kind = String(event?.eventKind || '').trim();
          const actor = String(event?.actorName || '行动者').trim();
          const target = String(event?.targetName || '目标').trim();
          const action = normalizeBattleActionDisplayName(event?.actionName || event?.sourceActionName || '') || '行动';
          const stateName = 读取事件账本状态名(event);
          const amount = Math.max(0, 读取事件账本数值(event, 'damage') || 读取事件账本数值(event, 'amount'));
          const result = String(event?.result || '').trim();
          const damageDetailText = () => {
            const firstDetail = 构建结算链事件明细(event).find(line => /^伤害路径：/.test(line));
            return firstDetail ? `（计算：${firstDetail.replace(/^伤害路径：/, '')}）` : '';
          };
          const stateDetailText = () => {
            const explicitBreakdown = String(event?.meta?.successRateBreakdown || event?.meta?.stateSuccessRateBreakdown || '').trim();
            if (explicitBreakdown) return `（计算：${explicitBreakdown}）`;
            return '（计算：缺少 successRateBreakdown / successRateReason，无法生成附着成功率计算式）';
          };
           if (kind === 'hit_result') {
             if (/miss|evade|dodge|未命中|闪避/.test(result)) return `${actor}的【${action}】未能命中${target}。`;
             if (/graze|chip|擦伤/.test(result)) return `${actor}的【${action}】擦过${target}，造成 ${amount} 点擦伤${damageDetailText()}。`;
             return amount > 0
               ? `${actor}的【${action}】命中${target}，造成 ${amount} 点伤害${damageDetailText()}。`
               : `${actor}的【${action}】落到${target}身上，但未造成实质伤害。`;
           }
           if (kind === 'dodge') {
             const sourceAction = normalizeBattleActionDisplayName(event?.sourceActionName || '');
             if (/evaded|miss|dodge_success|闪避成功|未命中/.test(result) && sourceAction) {
               return `${target}的【${sourceAction}】未能命中${actor}。`;
             }
             return `${actor}以【${action}】调整身位，尝试避开${target}的攻势。`;
           }
           if (kind === 'state_apply') {
            const duration = Math.max(0, Number(event?.duration || 0));
            if (/immune|immunity|免疫|无视异常/.test(result)) {
              return `${target}免疫了${actor}的【${action}】附带的【${stateName || '状态'}】${stateDetailText()}。`;
            }
            if (/resist|resisted|抵抗|豁免/.test(result)) {
              return `${target}抵住了${actor}的【${action}】附带的【${stateName || '状态'}】${stateDetailText()}。`;
            }
            return `${actor}的【${action}】令${target}陷入【${stateName || '状态'}】${duration > 0 ? `（持续${duration}回合）` : ''}${stateDetailText()}。`;
          }
          if (kind === 'counter') {
            const failReason = 映射玩家可见失败原因(event?.reasonCode || event?.meta?.reasonCode || '', event?.failReason || event?.failureReason || event?.meta?.failureReason || '');
            if (result === 'fail') return `${actor}尝试以【${action}】反击${target}，但${failReason || '未能完成反打'}。`;
            return amount > 0
              ? `${actor}以【${action}】反击${target}，造成 ${amount} 点伤害。`
              : `${actor}以【${action}】反击${target}，但未造成实质伤害。`;
          }
          if (kind === 'summon_assist') {
            const host = String(event?.meta?.hostName || event?.meta?.summonHostName || '宿主').trim();
            const hostAction = normalizeBattleActionDisplayName(event?.meta?.hostActionName || '有效攻势');
            const hostDamage = Math.max(0, Number(event?.meta?.hostDamage || 0));
            const assistDamage = Math.max(0, 读取事件账本数值(event, 'damage'));
            return `${actor}承接${host}的【${hostAction}】协同追击${target}${assistDamage > 0 ? `，造成 ${assistDamage} 点伤害` : ''}${hostDamage > 0 ? `（触发源伤害 ${hostDamage}）` : ''}。`;
          }
          if (kind === 'blocked_action' || kind === 'failed_action' || kind === 'target_fail') {
            const reason = 映射玩家可见失败原因(event?.reasonCode || event?.meta?.reasonCode || '', event?.failReason || event?.failureReason || event?.meta?.reasonText || event?.meta?.failureReason || '') || '被对手压住节奏';
            return `${actor}的【${action}】动作受阻：${reason}。`;
          }
          if (kind === 'create') return `${actor}完成【${action}】，生成造物。`;
          if (kind === 'summon_create') {
            const summonName = String(event?.meta?.summonName || '召唤物').trim();
            const summonType = String(event?.meta?.summonType || '召唤单位').trim();
            const summonMode = String(event?.meta?.summonMode || '协同攻击').trim();
            const mentalLoad = Math.max(0, Math.round(Number(event?.meta?.mentalLoad || 0)));
            return `${actor}完成【${action}】，召出${summonType}【${summonName}】，行动模式：${summonMode}${mentalLoad > 0 ? `，精神负载 ${mentalLoad}` : ''}，下回合起纳入行动轴。`;
          }
          if (kind === 'shield_create') return `${actor}完成【${action}】，生成护盾${amount > 0 ? ` ${amount} 点` : ''}。`;
          if (kind === 'resource_change') {
            const resource = String(event?.meta?.resource || '资源').trim();
            const delta = Math.round(Number(event?.meta?.delta || 0));
            const targetLabel = target || actor || '目标';
            if (!delta) return '';
            return `${targetLabel}的${resource}${delta > 0 ? '恢复' : '消耗'} ${Math.abs(delta)} 点。`;
          }
          return '';
        }

        function 构建回合末状态聚合侧写条目(eventLedger = []) {
          const groups = new Map();
          (Array.isArray(eventLedger) ? eventLedger : [])
            .filter(event => event && String(event.eventKind || '').trim() === 'state_tick')
            .forEach(event => {
              const round = Math.max(0, Number(event.round || event.sourceRound || 0));
              const stateName = 读取事件账本状态名(event);
              const aggregateKind = 读取状态Tick聚合种类(event);
              const resource = String(event?.meta?.resource || '生命值').trim();
              if (!(round > 0) || !stateName) return;
              const key = `${round}|${aggregateKind}|${stateName}|${resource}`;
              if (!groups.has(key)) groups.set(key, { round, aggregateKind, stateName, resource, events: [] });
              groups.get(key).events.push(event);
            });
          return [...groups.values()]
            .filter(group => group.events.length >= 2)
            .map(group => {
              const isHeal = group.aggregateKind === 'heal_tick';
              const total = group.events.reduce((sum, event) => sum + Math.max(0, 读取事件账本数值(event, 'amount')), 0);
              const children = group.events.map(event => {
                const target = String(event.targetName || '目标').trim();
                const amount = Math.max(0, 读取事件账本数值(event, 'amount'));
                return `${target}${isHeal ? '恢复' : '损失'} ${amount} 点${group.resource}`;
              });
              return {
                type: 'settlement_aggregation',
                round: group.round,
                回合: group.round,
                roundPhase: 'turn_end',
                kind: 'status_tick_aggregation',
                aggregateKind: group.aggregateKind,
                stateName: group.stateName,
                childNodeIds: group.events.map(event => String(event.chainNodeId || '').trim()).filter(Boolean),
                ledgerEventIds: group.events.map(event => String(event.eventId || '').trim()).filter(Boolean),
                sourceNodeIds: group.events.map(event => String(event.chainNodeId || '').trim()).filter(Boolean),
                sourceEventIds: group.events.map(event => String(event.eventId || '').trim()).filter(Boolean),
                text: `【${group.stateName}】在 ${group.events.length} 个目标身上结算，合计${isHeal ? '恢复' : '造成'} ${Math.round(total)} 点${group.resource}`,
                children,
                fromEventLedger: true,
              };
            });
        }
        function 构建事件账本结算链侧写条目(eventLedger = []) {
          const seen = new Set();
          return (Array.isArray(eventLedger) ? eventLedger : [])
            .map(event => {
              const kind = String(event?.eventKind || '').trim();
              if (!['hit_result', 'state_apply', 'resource_change', 'counter', 'create', 'summon_create', 'summon_assist', 'shield_create', 'blocked_action', 'failed_action', 'target_fail', 'dodge'].includes(kind)) return null;
              const text = 格式化结算链事件文本(event);
              if (!text) return null;
              const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
              const key = [
                round,
                text,
              ].join('|');
              if (seen.has(key)) return null;
              seen.add(key);
              return {
                type: 'settlement',
                round,
                回合: round,
                text,
                details: 构建结算链事件明细(event),
                roundPhase: kind === 'state_tick' ? 'turn_end' : 'action_result',
                kind,
                sourceEventId: String(event?.eventId || '').trim(),
                sourceNodeId: String(event?.chainNodeId || '').trim(),
                ledgerEventIds: [String(event?.eventId || '').trim()].filter(Boolean),
                fromEventLedger: true,
              };
            })
            .filter(Boolean);
        }

        function 构建结算链侧写条目(logs = [], context = {}) {
          const ledger = context?.eventLedger || context?.combatData?.__battleEventLedger || [];
          const eventItems = 构建事件账本结算链侧写条目(ledger);
          const aggregationItems = 构建回合末状态聚合侧写条目(ledger);
          const eventLines = 构建事件账本公开战报Block文本行(ledger, 24, context);
          const rawLines = eventLines.filter(line => /随后受【/.test(String(line || '')));
          const lines = aggregationItems.length ? [] : ((typeof Formatter !== 'undefined' && Formatter.filterMechanicalDuplicates) ? Formatter.filterMechanicalDuplicates(rawLines) : rawLines.filter(line => !/^第\d+回合：结果判定：/.test(String(line || '').trim())));
          const tickItems = lines.map(line => {
            const match = String(line || '').match(/^(?:第(\d+)回合[：:]|[【\[]第(\d+)回合[】\]])\s*(.+)$/);
            const round = Number(match?.[1] || match?.[2] || 0);
            const text = String(match?.[3] || line || '').trim();
            return {
              type: 'settlement',
              round,
              回合: round,
              text,
              roundPhase: /随后受【/.test(text) ? 'turn_end' : 'action_result',
              kind: /随后受【/.test(text) ? 'status_tick' : 'settlement',
            };
          }).filter(item => item.text);
          return [...eventItems, ...aggregationItems, ...tickItems];
        }

        function 读取判定流程输入文本(轨迹 = {}) {
          const 来源 = String(轨迹.候选来源 || '').trim();
          const 类型 = 读取轨迹类型(轨迹);
          if (类型 === '战术确立') return '玩家手选动作';
          if (/主动战术|主动战略|主动/.test(来源) || 类型 === '主动规划') return '自动续推/主动规划';
          if (/应招/.test(来源) || 类型 === '应招审计') return '遭遇攻击，触发应招判定';
          if (/再判定/.test(来源) || 类型 === '再判定审计') return '行为链再判定';
          if (/换招/.test(来源) || 类型 === '换招审计') return '动作受阻，触发换招审计';
          return 来源 || 类型 || '系统判定';
        }

        function 读取过滤证据字段(轨迹 = {}) {
          const 候选 = 读取判定流程候选列表(轨迹, 0);
          const 技能名 = normalizeBattleActionDisplayName(轨迹.技能 || 轨迹.实际技能 || 轨迹.动作校正?.实际技能 || '');
          const 命中候选 = 技能名 ? 候选.find(item => 读取候选名称(item) === 技能名) : null;
          const 读取候选码 = item => [
            item?.reasonCode,
            item?.rejectionCode,
            item?.filterCode,
            item?.statusCode,
            item?.审计?.reasonCode,
            item?.审计?.rejectionCode,
            item?.审计?.filterCode,
            item?.审计?.statusCode,
          ].map(code => String(code || '').trim()).filter(Boolean);
          const 候选资源阻断 = item =>
            item?.资源可行 === false ||
            item?.审计?.资源可行 === false ||
            item?.可释放 === false ||
            item?.审计?.可释放 === false ||
            Number(item?.资源修正 ?? item?.审计?.资源修正 ?? 0) <= -30 ||
            读取候选码(item).some(code => /OUT_OF_MANA|RESOURCE|COST|CAST_RESOURCE|资源不可释放|资源不足/.test(code));
          const 候选冷却阻断 = item =>
            Number(item?.cooldownRemaining ?? item?.冷却剩余 ?? item?.审计?.cooldownRemaining ?? item?.审计?.冷却剩余 ?? 0) > 0 ||
            读取候选码(item).some(code => /COOLDOWN|CD|冷却/.test(code));
          const 候选状态阻断 = item =>
            item?.状态限制 === true ||
            item?.审计?.状态限制 === true ||
            读取候选码(item).some(code => /STATE_LOCK|CONTROLLED|SILENCED|DISARMED|STUNNED|HARD_CC|状态限制|无法行动|沉默|封技|缴械|硬控/.test(code));
          const 有资源阻断 = Number(轨迹.资源修正 || 0) <= -30 ||
            (命中候选 ? 候选资源阻断(命中候选) : (候选.length > 0 && 候选.every(候选资源阻断)));
          return {
            候选数: 候选.length,
            资源或释放条件不足: 有资源阻断,
            冷却证据: 命中候选 ? 候选冷却阻断(命中候选) : (候选.length > 0 && 候选.every(候选冷却阻断)),
            状态限制证据: 命中候选 ? 候选状态阻断(命中候选) : (候选.length > 0 && 候选.every(候选状态阻断)),
            目标条件不成立: !String(轨迹.目标 || '').trim() && !判定轨迹是目标规划(轨迹),
            无候选命中: 候选.length > 0 && !String(轨迹.技能 || '').trim(),
          };
        }

        function 读取判定流程过滤文本(轨迹 = {}) {
          const 类型 = 读取轨迹类型(轨迹);
          if (类型 === '战术确立') return '本轮主轴动作已确认并接入行动链';
          const 证据 = 读取过滤证据字段(轨迹);
          const 失败 = [];
          if (证据.资源或释放条件不足) 失败.push('资源或释放条件不足');
          if (证据.冷却证据) 失败.push('技能仍在冷却中');
          if (证据.状态限制证据) 失败.push('状态限制导致动作不可用');
          if (证据.目标条件不成立) 失败.push('目标条件不成立');
          if (失败.length) return 失败.join('；');
          if (/应招审计/.test(类型)) {
            if (证据.候选数 <= 0) return '当前未能捕捉到有效的规避或招架窗口';
            if (证据.无候选命中) return '当前未能形成有效的规避或招架方案';
            return '当前仍有规避或招架余地';
          }
          if (证据.候选数 <= 0) return '当前没有形成明确的出手机会';
          if (证据.无候选命中) return '常规方案收益过低，被暂时否决';
          return '当前仍具备出手条件';
        }

        function 读取判定流程无动作原因(轨迹 = {}) {
          const 证据 = 读取过滤证据字段(轨迹);
          const 原因 = [];
          if (证据.目标条件不成立) 原因.push('目标条件不成立');
          if (证据.资源或释放条件不足) 原因.push('资源或释放条件不足');
          if (证据.冷却证据) 原因.push('技能仍在冷却中');
          if (证据.状态限制证据) 原因.push('状态限制导致动作不可用');
          if (证据.候选数 <= 0) 原因.push('当前层未生成有效动作候选');
          if (!原因.length) 原因.push('候选因权重过低或条件不符未被采用');
          return 原因.join(' / ');
        }

        function 清洗主卡战术文本(raw = '') {
          return String(raw || '')
            .replace(/\s+/g, ' ')
            .trim();
        }

        function 判定侧写是敌方动作(轨迹 = {}) {
          const 类型 = 读取轨迹类型(轨迹);
          const 来源 = String(轨迹.候选来源 || '').trim();
          if (/辅助目标规划|友方目标|辅助目标/.test(`${类型} ${来源}`)) return false;
          const 技能 = normalizeBattleActionDisplayName(轨迹.finalResolvedActionName || 轨迹.技能 || '');
          const 命中候选名 = normalizeBattleActionDisplayName(轨迹.hitCandidateName || '');
          const 覆写来源 = String(轨迹.actionOverrideSource || '').trim();
          const 目标语义 = 读取轨迹目标语义(轨迹);
          const 分类 = 轨迹.效果原型分类 || {};
          const actor = String(轨迹.行动者 || '').trim();
          const target = String(轨迹.目标 || 轨迹.实际目标 || '').trim();
          if ((actor && target && isSameBattleReportName(actor, target)) || /自身|友方单体|友方群体/.test(目标语义)) return false;
          if (String(分类?.目标阵营 || '').trim() === '友方') return false;
          if (分类?.是否资源恢复 === true) return false;
          if (['治疗', '防护', '资源'].includes(String(分类?.主要效果类型 || '').trim())) return false;
          const 支援动作 = /治疗|防护|保核|友方|自身|资源恢复|回复|恢复|护援|护盾|庇护/.test(技能)
            && !/攻击|普通攻击|伤害|控制|封技|打断|限制|中毒|削弱|破防|收割|压制/.test(技能);
          return !!轨迹.目标 && !支援动作;
        }

        function 判定侧写是造物或自用动作(轨迹 = {}) {
          const 技能 = normalizeBattleActionDisplayName(轨迹.技能 || 轨迹.实际技能 || 轨迹.finalResolvedActionName || '');
          const 目标语义 = 读取轨迹目标语义(轨迹);
          const 承载方式 = String(轨迹.承载方式 || '').trim();
          const 分类 = 轨迹.效果原型分类 || {};
          const 文本 = `${技能} ${目标语义} ${承载方式} ${String(分类?.主要效果类型 || '')} ${String(分类?.目标阵营 || '')}`;
          if ((/造物|物品|药剂|食物/.test(文本) || /造物承载|物品使用/.test(承载方式)) && !/攻击|伤害|削弱|控制|封技|打断/.test(文本)) return true;
          if (String(分类?.目标阵营 || '').trim() === '友方' && 分类?.是否资源恢复 === true) return true;
          if (/食物|药剂|物品|造物/.test(技能) && ['资源', '治疗', '防护'].includes(String(分类?.主要效果类型 || '').trim())) return true;
          return false;
        }

        function 读取守势转入原因类别(轨迹 = {}) {
          const 技能 = normalizeBattleActionDisplayName(轨迹.finalResolvedActionName || 轨迹.实际技能 || 轨迹.技能 || '');
          const 文本 = [
            技能,
            String(轨迹.hitCandidateName || ''),
            String(轨迹.承载方式 || ''),
            String(轨迹.resolutionType || ''),
            String(轨迹.skipReason || ''),
          ].join(' ');
          const 候选文本 = 读取判定流程候选列表(轨迹, 0)
            .map(item => `${读取候选名称(item)}:${Math.round(读取候选权重(item))}`)
            .join('；');
          const 压制计数 = Number(轨迹?.threatProfile?.连续压制次数 || 轨迹?.连续压制次数 || 0);
          const 落空计数 = Number(轨迹?.threatProfile?.闪避克制次数 || 轨迹?.闪避克制次数 || 0);
          if (/压制|逼抢|贴身|强压|来袭|敌方攻势|承压|保命|保核|高威胁|当前不宜继续前压/.test(`${文本} ${候选文本}`) && (压制计数 > 0 || /承伤硬抗|坚壁反制|借力守势|伺机闪避|危机自保/.test(技能))) {
            return 'pressure';
          }
          if (/收招转防|战术观察|待机|守势维持|守势对峙/.test(技能) && (/窗口|未形成有效出手机会|收益过低|无可用候选|放弃继续执行|重校|动作受阻|改以/.test(`${文本} ${候选文本}`) || 落空计数 > 0)) {
            return 'window_lost';
          }
          if (/伺机闪避|闪避/.test(技能) && /窗口|重排|拉开|观察/.test(`${文本} ${候选文本}`)) return 'window_lost';
          return 'pressure';
        }

        function 判定侧写是自我应对动作(轨迹 = {}) {
          const 技能 = normalizeBattleActionDisplayName(轨迹.技能 || 轨迹.实际技能 || 轨迹.动作校正?.实际技能 || '');
          const actor = String(轨迹.行动者 || '').trim();
          const target = String(轨迹.目标 || 轨迹.实际目标 || '').trim();
          return /伺机闪避|闪避|承伤硬抗|肉体兜底|硬抗|防御|危机自保|坚壁反制|收招转防|借力守势|撤离|战术观察|待机|守势维持|守势对峙/.test(技能) ||
            (!!actor && !!target && isSameBattleReportName(actor, target));
        }

        function 构建侧写目标文本(轨迹 = {}) {
          if (读取轨迹类型(轨迹) === '战术确立') {
            const targetName = String(轨迹.displayTargetName || 轨迹.目标 || 轨迹.实际目标 || '').trim();
            return targetName ? `当前指向：【${targetName}】。` : '本轮不预设固定目标。';
          }
          const 技能 = normalizeBattleActionDisplayName(轨迹.技能 || 轨迹.实际技能 || 轨迹.动作校正?.实际技能 || '');
          const actor = String(轨迹.行动者 || '').trim();
          const target = String(轨迹.displayTargetName || 轨迹.目标 || 轨迹.实际目标 || '').trim();
          const 目标语义 = 读取轨迹目标语义(轨迹);
          if (判定侧写是造物或自用动作(轨迹)) {
            return '作用目标：造物承载。';
          }
          if ((actor && target && isSameBattleReportName(actor, target)) || /自身/.test(目标语义)) {
            if (判定侧写是自我应对动作(轨迹)) return actor ? `作用目标：自身态势（${actor}）。` : '作用目标：自身态势。';
            return actor ? `施法对象：自身（${actor}）。` : '施法对象：自身。';
          }
          if (/友方单体|友方群体/.test(目标语义)) return target ? `支援对象：【${target}】。` : '支援对象：己方。';
          if (判定侧写是自我应对动作(轨迹)) {
            if (/伺机闪避|闪避/.test(技能)) return '不锁定敌方目标，优先处理当前攻势并重排身位。';
            if (/撤离/.test(技能)) return '不继续锁定目标，优先寻找脱离战场的窗口。';
            if (/战术观察|待机|守势维持|守势对峙/.test(技能)) return '不锁定敌方目标，优先观察局势并保持距离。';
            return '不锁定敌方目标，优先稳住自身防线。';
          }
          return target ? `当前指向：【${target}】。` : '当前没有明确目标。';
        }

        function 读取轨迹目标语义(轨迹 = {}) {
          return String(
            轨迹.目标语义 ||
            轨迹.目标类型 ||
            轨迹.目标范围 ||
            轨迹.skill?.目标 ||
            轨迹.skill?.target ||
            轨迹.技能数据?.目标 ||
            '',
          ).trim();
        }

        function 判定侧写是友方支援动作(轨迹 = {}) {
          const 类型 = 读取轨迹类型(轨迹);
          const 来源 = String(轨迹.候选来源 || '').trim();
          const 技能 = normalizeBattleActionDisplayName(轨迹.技能 || 轨迹.实际技能 || 轨迹.动作校正?.实际技能 || '');
          const actor = String(轨迹.行动者 || '').trim();
          const target = String(轨迹.目标 || 轨迹.实际目标 || '').trim();
          const 目标语义 = 读取轨迹目标语义(轨迹);
          const 承载方式 = String(轨迹.承载方式 || '').trim();
          const 分类 = 轨迹.效果原型分类 || {};
          const 文本 = `${类型} ${来源} ${技能} ${目标语义} ${承载方式} ${String(分类?.主要效果类型 || '')} ${String(分类?.目标阵营 || '')}`;
          if (判定侧写是造物或自用动作(轨迹)) return true;
          if (actor && target && isSameBattleReportName(actor, target) && !isBattleTacticalFallbackAction(技能)) return true;
          if (/自身|友方单体|友方群体/.test(目标语义)) return true;
          if (/辅助目标规划|友方目标|辅助目标/.test(`${类型} ${来源}`)) return true;
          if (/治疗|防护|保核|友方|自身|资源恢复|回复|恢复|护援|护盾|庇护|造物|药剂|物品|造物承载|物品使用/.test(文本) &&
              !/攻击|普通攻击|伤害|控制|封技|打断|限制|中毒|削弱|破防|收割/.test(技能)) return true;
          return false;
        }

        function 转换友方侧写理由(text = '') {
          const raw = String(text || '').trim();
          if (!raw) return '';
          if (/魂力|体力|精神力|资源|保留|药剂|物品/.test(raw)) return '调整资源节奏';
          if (/治疗|续航|保核|核心|护援|护盾|防护|防御|稳态/.test(raw)) return '巩固防线';
          if (/安全距离|规避|闪避|身位|移动路线|范围压制窗口/.test(raw)) return '规避后续伤害';
          if (/压制|收割|击杀|斩杀|破防|集火|控制收益|终局压制/.test(raw)) return '巩固防线';
          return raw;
        }

        function 转译判定理由标签(text = '') {
          const raw = String(text || '').trim();
          if (!raw) return '';
          const map = [
            [/控制收益/g, '准备先手控住对手'],
            [/压制收束窗口|压制收束/g, '试图封死敌方退路'],
            [/终局压制窗口|终局压制/g, '试图一口气压垮对手'],
            [/集火压制/g, '集中火力压制对手'],
            [/压制敌方续航/g, '压制敌方续航'],
            [/控制压制/g, '尝试压住对手行动'],
            [/压制试探/g, '一边试探一边施压'],
            [/战术试探/g, '先试探对方反应'],
            [/资源保留/g, '保留后续出手空间'],
            [/调整资源节奏/g, '调整资源节奏'],
            [/巩固防线/g, '巩固防线'],
            [/规避后续伤害/g, '规避后续伤害'],
            [/支援检索/g, '先确认支援落点'],
            [/常规威胁/g, '对方仍具稳定威胁'],
            [/后排\/续航核心/g, '后排续航核心'],
            [/控制优先/g, '控制优先'],
            [/唯一有效敌方目标/g, '眼下只有这一个有效敌方目标'],
            [/范围压制窗口打开/g, '当前有继续施压的空间'],
            [/安全距离仍可维持/g, '仍能维持安全距离'],
            [/友方目标优先表命中/g, '优先确认友方落点'],
            [/常规观察/g, '先保持观察'],
            [/企图掌控局势/g, '准备抢住主动'],
          ];
          return map.reduce((value, [pattern, replacement]) => value.replace(pattern, replacement), raw);
        }

        function 清洗判定侧写理由(raw = '', 轨迹 = {}) {
          let text = String(raw || '').trim();
          if (!text) return '';
          const 敌方动作 = 判定侧写是敌方动作(轨迹);
          const 友方支援 = 判定侧写是友方支援动作(轨迹);
          text = text.replace(/\s*[+-]\d+(?:\.\d+)?\s*$/, '').trim();
          text = text.replace(/^目标:([^()]+)\(([^)]*)\)$/u, '目标$1：$2');
          text = text.replace(/^意图:/, '');
          if (友方支援) return 转换友方侧写理由(text);
          if (/压制敌方续航点/.test(text)) return '压制敌方续航';
          if (/治疗救场价值|压治疗核心|阻止治疗救场|治疗核心|治疗节奏/.test(text)) return '压制敌方续航';
          if (/击杀|斩杀|收割窗口|可斩杀/.test(text)) return '压制收束窗口';
          if (/终局爆发/.test(text)) return '终局压制窗口';
          if (/^治疗$|^保核$|^驱散$|治疗后资源|治疗后拖回合/.test(text)) return 敌方动作 ? '压制敌方支援链' : '友方支援窗口';
          if (/^控制$/.test(text)) return '控制收益';
          if (/^集火$/.test(text)) return '集火压制';
          if (/^保留资源$/.test(text)) return '资源保留';
          return 转译判定理由标签(text);
        }

        function 组合侧写局势文本(pieces = [], 轨迹 = {}) {
          const list = (Array.isArray(pieces) ? pieces : [])
            .map(item => String(item || '').trim())
            .filter(Boolean)
            .filter((item, index, arr) => arr.indexOf(item) === index)
            .slice(0, 4);
          if (!list.length) return '';
          const 主意图 = 转译判定理由标签(list[0]);
          const 理由 = list.slice(1).map(转译判定理由标签);
          if (!理由.length) return 主意图;
          const 左因 = 理由[0];
          const 右因 = 理由[1] || '';
          const 结果 = 主意图 || (判定侧写是友方支援动作(轨迹) ? '优先巩固防线' : '据此调整行动');
          return 右因
            ? `因${左因}，且${右因}，因此${结果}`
            : `因${左因}，因此${结果}`;
        }

        function 读取判定流程局势文本(轨迹 = {}) {
          if (判定轨迹是目标规划(轨迹)) return 构建目标规划局势文本(轨迹);
          const 规范战略 = 读取侧写战略标签(轨迹);
          const pieces = [
            规范战略 || '',
            ...(Array.isArray(轨迹.目标理由) ? 轨迹.目标理由.slice(0, 2) : []),
            ...(Array.isArray(轨迹.前瞻理由) ? 轨迹.前瞻理由.slice(0, 2) : []),
            ...(Array.isArray(轨迹.职责理由) ? 轨迹.职责理由.slice(0, 2) : []),
          ].map(item => 清洗判定侧写理由(item, 轨迹)).filter(Boolean);
          if (pieces.length) return 组合侧写局势文本(pieces, 轨迹);
          const 分类 = 轨迹.效果原型分类 || {};
          if (分类 && typeof 分类 === 'object') {
            const tags = Object.entries(分类)
              .filter(([, value]) => value === true || Number(value || 0) > 0)
              .map(([key]) => key)
              .slice(0, 4);
            if (tags.length) return `效果画像:${tags.join('、')}`;
          }
          return '按当前威胁、资源与目标优先级评估';
        }

        function 读取侧写战略标签(轨迹 = {}) {
          const 原始 = 转译判定理由标签(String(轨迹.战略意图 || '').trim());
          const 类型 = 读取轨迹类型(轨迹);
          const 来源 = String(轨迹.候选来源 || '').trim();
          const 技能 = normalizeBattleActionDisplayName(轨迹.技能 || '');
          const 目标理由文本 = [
            ...(Array.isArray(轨迹.目标理由) ? 轨迹.目标理由 : []),
            ...(Array.isArray(轨迹.前瞻理由) ? 轨迹.前瞻理由 : []),
            ...(Array.isArray(轨迹.职责理由) ? 轨迹.职责理由 : []),
          ].join('；');
          const 候选文本 = `${技能} ${目标理由文本}`;
          const 目标语义 = 读取轨迹目标语义(轨迹);
          const 分类 = 轨迹.效果原型分类 || {};
          const 动作画像文本 = `${技能} ${目标语义} ${String(分类?.主要效果类型 || '')} ${String(分类?.目标阵营 || '')}`;
          if (/造物|物品|药剂|食物/.test(动作画像文本) && !/攻击|伤害|削弱|控制|封技|打断/.test(动作画像文本)) {
            return '完成造物承载';
          }
          if (判定侧写是友方支援动作(轨迹)) {
            if (/资源|保留|药剂|物品/.test(`${原始} ${候选文本}`)) return '调整资源节奏';
            if (/闪避|身位|撤离|规避/.test(`${原始} ${候选文本}`)) return '规避后续伤害';
            return 转换友方侧写理由(原始) || '巩固防线';
          }
          if (/辅助目标规划|友方目标|辅助目标/.test(`${类型} ${来源}`)) {
            if (/治疗|保核|驱散|保留资源/.test(原始)) return '支援检索';
            return 原始 || '支援检索';
          }
          if (/敌方压迫|压招|压制|抢节奏/.test(候选文本) && !/沉默|封技|眩晕|控制链|锁定/.test(候选文本)) return '反制压制';
          if (/敌方截击|截击|贴身逼抢|抢落点|卡位|逼位/.test(候选文本) && !/沉默|封技|眩晕|控制链|锁定/.test(候选文本)) return '逼位卡位';
          if (/收招转防|战术观察|待机|守势维持|守势对峙/.test(技能)) return '守势调整';
          const 是敌方动作 = 判定侧写是敌方动作(轨迹);
          if (原始 === '击杀') return '压制收束';
          if ((原始 === '治疗' || 原始 === '保核') && 是敌方动作) {
            if (/控制|封技|打断|限制|中毒|削弱/.test(候选文本) && !/敌方压迫|敌方截击|贴身逼抢|抢落点|卡位/.test(候选文本)) return '控制';
            if (/普通攻击|攻击|伤害|破防|收割|集火|后排|续航核心|控制优先/.test(候选文本)) return '压制试探';
            return '战术试探';
          }
          if (原始 === '终局爆发') return '终局压制';
          const 转译后 = 转译判定理由标签(原始);
          if (/准备先手控住对手|准备抢住主动/.test(转译后)) {
            if (/后排.*续航核心|治疗核心|续航核心/.test(候选文本)) return '准备压住对方后排续航';
            if (/控制优先|控制|封技|打断/.test(候选文本)) return '准备先手打断对方节奏';
            if (/唯一有效敌方目标|对方仍具稳定威胁|常规威胁|蓄力高威胁/.test(候选文本)) return '准备直接向当前威胁施压';
          }
          return 转译后;
        }

        function 构建战略侧写文本(轨迹 = {}) {
          if (判定侧写是友方支援动作(轨迹)) return `${读取判定流程局势文本(轨迹)}。`;
          if (读取轨迹类型(轨迹) === '战术确立') {
            const 动作原名 = normalizeBattleActionDisplayName(轨迹.finalResolvedActionName || 轨迹.技能 || '当前动作');
            const 动作 = 包裹判定动作名称(动作原名);
            const 目标 = String(轨迹.displayTargetName || 轨迹.目标 || '').trim();
            const 非敌对 = 判定侧写是造物或自用动作(轨迹) || 判定侧写是友方支援动作(轨迹) || /造物|物品|药剂|食物|牛肉干|护壁|鼓舞/.test(动作原名);
            return `${轨迹.行动者 || '该单位'}已锁定${动作}${目标 && !非敌对 ? `，当前指向【${目标}】` : ''}，作为本轮主轴。`;
          }
          if (判定侧写是自我应对动作(轨迹)) {
            const 技能 = normalizeBattleActionDisplayName(轨迹.技能 || 轨迹.实际技能 || 轨迹.动作校正?.实际技能 || '');
            const 原因类别 = 读取守势转入原因类别(轨迹);
            if (/伺机闪避|闪避/.test(技能)) {
              return 原因类别 === 'window_lost'
                ? '原定前压窗口已经松动，先拉开身位重排节奏。'
                : '对方攻势逼近身前，先拉开身位寻找反击窗口。';
            }
            if (/撤离/.test(技能)) return '继续缠斗风险上升，优先寻找脱离窗口。';
            return 原因类别 === 'window_lost'
              ? '原定高价值动作窗口已经消散，先收住节奏稳住自身防线。'
              : '敌方压迫仍在抬升，优先收紧防线承住这一轮冲击。';
          }
          const 目标 = String(轨迹.目标 || '').trim();
          const 战略 = 读取侧写战略标签(轨迹);
          if (战略 && 目标) return `当前动作指向【${目标}】。`;
          if (战略) return `${读取判定流程局势文本(轨迹)}。`;
          return `${读取判定流程局势文本(轨迹)}。`;
        }

        function 构建动作决断文本(轨迹 = {}) {
          const 技能 = normalizeBattleActionDisplayName(轨迹.finalResolvedActionName || 轨迹.技能 || '');
          const 命中候选名 = normalizeBattleActionDisplayName(轨迹.hitCandidateName || '');
          const 覆写来源 = String(轨迹.actionOverrideSource || '').trim();
          const 行动者 = String(轨迹.行动者 || '行动者').trim();
          const 原始目标 = String(轨迹.目标 || 轨迹.实际目标 || '').trim();
          const 目标 = 原始目标 && !isSameBattleReportName(行动者, 原始目标) ? 原始目标 : '';
          const 类型 = 读取轨迹类型(轨迹);
          const 目标语义 = 读取轨迹目标语义(轨迹);
          const 分类 = 轨迹.效果原型分类 || {};
          const 文本 = `${技能} ${目标语义} ${String(轨迹.承载方式 || '')} ${String(分类?.主要效果类型 || '')} ${String(分类?.目标阵营 || '')}`;
          if (类型 === '战术确立') {
            const 非敌对 = 判定侧写是造物或自用动作(轨迹) || 判定侧写是友方支援动作(轨迹) || /造物|物品|药剂|食物|牛肉干|护壁|鼓舞/.test(技能);
            return `${行动者}锁定${包裹判定动作名称(技能 || '当前动作')}${目标 && !非敌对 ? `指向${目标}` : ''}，并将其作为本轮主轴。`;
          }
          if (判定轨迹是行为链校验(轨迹)) {
            const 动作名 = 包裹判定动作名称(技能);
            if (/应招审计/.test(类型)) {
              if (/伺机闪避|闪避/.test(技能)) return `${行动者}对照眼前攻势，确认留给自己的避让空间仍然足够，尝试以${动作名}规避。`;
              if (/防御|危机自保|承伤硬抗|坚壁反制|收招转防|借力守势/.test(技能)) return `${行动者}对照眼前攻势，确认以${动作名}承住这一轮冲击。`;
              return `${行动者}校验当前攻势${动作名 ? `，应对动作指向${动作名}` : ''}。`;
            }
            if (/再判定审计/.test(类型)) {
              if (/战术观察|待机|守势维持|守势对峙/.test(技能)) return `${行动者}复核后暂不继续前压，先观察战局。`;
              if (/防御|危机自保|承伤硬抗|坚壁反制|收招转防|借力守势/.test(技能)) return `${行动者}复核后确认转入${动作名}，稳住自身节奏。`;
              return `${行动者}复核后确认${动作名 || '当前动作'}作为实际落点。`;
            }
            if (/换招审计/.test(类型)) {
              if (/战术观察|待机|守势维持|守势对峙/.test(技能)) return `${行动者}受阻后没有继续出手，先观察战局。`;
              if (/防御|危机自保|承伤硬抗|坚壁反制|收招转防|借力守势/.test(技能)) return `${行动者}受阻后改以${动作名}稳住局面。`;
              return `${行动者}复核受阻后的动作调整，最终改为${动作名 || '当前动作'}。`;
            }
          }
          if (轨迹.动作校正?.发生校正) {
            const 实际技能 = normalizeBattleActionDisplayName(轨迹.动作校正.实际技能 || '');
            if (/伺机闪避|闪避/.test(实际技能)) return `${行动者}最终改以${包裹判定动作名称(实际技能)}处理当前攻势。`;
            if (/收招转防/.test(实际技能)) return 读取守势转入原因类别(轨迹) === 'window_lost'
              ? `${行动者}察觉原定窗口已经消散，收住前压节奏转入守势。`
              : `${行动者}顶住对方压迫，收住前压节奏转入守势稳住防线。`;
            if (/防御|危机自保|承伤硬抗|坚壁反制|借力守势/.test(实际技能)) return 读取守势转入原因类别(轨迹) === 'window_lost'
              ? `${行动者}放弃继续推进原案，最终改以${包裹判定动作名称(实际技能)}稳住自身节奏。`
              : `${行动者}最终改以${包裹判定动作名称(实际技能)}扛住当前压力。`;
            if (/撤离/.test(实际技能)) return `${行动者}最终改以${包裹判定动作名称(实际技能)}寻找脱离窗口。`;
            if (/战术观察/.test(实际技能)) return `${行动者}暂不贸然追击，先拉开身位观察战局。`;
            if (/待机|守势维持|守势对峙/.test(实际技能)) return `${行动者}暂不贸然出手，先稳住节奏观察战局。`;
            return `${行动者}最终改以${包裹判定动作名称(实际技能)}${目标 ? `应对${目标}` : '应对战局'}。`;
          }
          if (/伺机闪避|闪避/.test(技能)) return `${行动者}最终以${包裹判定动作名称(技能)}处理当前攻势，重排身位。`;
          if (/收招转防/.test(技能)) return 读取守势转入原因类别(轨迹) === 'window_lost'
            ? `${行动者}察觉战机已经溜走，收住前压节奏转入守势。`
            : `${行动者}收住前压节奏，转入守势稳住防线。`;
          if (/防御|危机自保|承伤硬抗|坚壁反制|借力守势/.test(技能)) return 读取守势转入原因类别(轨迹) === 'window_lost'
            ? `${行动者}最终选择${包裹判定动作名称(技能)}，主动收缩节奏保留后手。`
            : `${行动者}最终选择${包裹判定动作名称(技能)}稳住防线。`;
          if (/撤离/.test(技能)) return `${行动者}最终选择${包裹判定动作名称(技能)}寻找脱离战场的机会。`;
          if (/战术观察/.test(技能)) return `${行动者}暂不贸然追击，先拉开身位观察战局。`;
          if (/待机|守势维持|守势对峙/.test(技能)) return `${行动者}暂不贸然出手，先稳住节奏观察战局。`;
          if ((/造物|物品|药剂|食物/.test(文本) || /造物承载|物品使用/.test(String(轨迹.承载方式 || ''))) && !/攻击|伤害|削弱|控制|封技|打断/.test(文本)) {
            return `${行动者}最终以${包裹判定动作名称(技能)}完成造物承载。`;
          }
          if (判定侧写是友方支援动作(轨迹)) {
            const 支援动作名 = 包裹判定动作名称(技能);
            if (覆写来源 && 命中候选名 && 命中候选名 !== 技能) {
              return `${行动者}最终以${支援动作名}巩固自身节奏。`;
            }
            return `${行动者}最终以${支援动作名}巩固自身节奏。`;
          }
          if (/普通攻击|常规攻击/.test(技能)) return `${行动者}最终选择${包裹判定动作名称(技能)}${目标 ? `攻击${目标}` : '推进攻势'}。`;
          if (技能) return `${行动者}最终执行${包裹判定动作名称(技能)}${目标 ? `指向${目标}` : ''}。`;
          return `${行动者}本轮未形成有效出手机会，暂时转入守势观察。`;
        }

        function 构建轨迹侧写行(轨迹 = {}) {
          if (判定轨迹是目标规划(轨迹)) {
            return [
              `├─ ${读取判定流程局势文本(轨迹)}。`,
              `├─ ${构建目标规划检索文本(轨迹)}。`,
              `└─ ${构建目标规划决断文本(轨迹)}`,
            ].map(清洗主卡战术文本).filter(Boolean);
          }
          if (/主动规划|战术确立/.test(读取轨迹类型(轨迹))) {
            const dto = 构建NarrativeDecisionDTO(轨迹);
            const actorName = dto.actorName || 轨迹.行动者 || '行动者';
            const targetName = dto.targetName || 轨迹.目标 || '';
            const finalActionName = normalizeBattleActionDisplayName(dto.finalActionName || 轨迹.finalResolvedActionName || 轨迹.技能 || '');
            const actionText = 包裹判定动作名称(finalActionName || '当前动作');
            if (dto.narrationTrustLevel === 'INVALID') {
              return [
                '├─ 本段判定明细暂不可用。',
                `└─ ${actorName}本轮动作暂未确认。`,
              ].map(清洗主卡战术文本).filter(Boolean);
            }
            if (dto.narrationTrustLevel === 'TRUSTED') {
              const narration = formatTacticalNarration(dto);
              return [
                narration ? `├─ ${narration}` : '',
                `├─ ${targetName ? `当前指向：【${targetName}】。` : '当前没有明确目标。'}`,
                `└─ ${actorName}最终确认${actionText}${targetName ? `压向${targetName}` : ''}。`,
              ].map(清洗主卡战术文本).filter(Boolean);
            }
            return [
              `├─ ${targetName ? `当前指向：【${targetName}】。` : '当前没有明确目标。'}`,
              `└─ ${actorName}最终确认${actionText}${targetName ? `指向${targetName}` : ''}。`,
            ].map(清洗主卡战术文本).filter(Boolean);
          }
          return [
            `├─ ${/应招|再判定|换招/.test(读取轨迹类型(轨迹)) ? 读取判定流程输入文本(轨迹) : 构建战略侧写文本(轨迹)}`,
            `├─ ${构建侧写目标文本(轨迹)}`,
            `├─ ${读取判定流程过滤文本(轨迹)}。`,
            `└─ ${构建动作决断文本(轨迹)}`,
          ].map(清洗主卡战术文本).filter(Boolean);
        }

        function 构建轨迹标题(轨迹 = {}) {
          const 类型 = 读取轨迹类型(轨迹) || '判定';
          const 行动者 = 轨迹.行动者 || '行动者';
          if (判定轨迹是目标规划(轨迹)) return `[${类型}] 🎯 ${行动者} ${判定目标规划为支援检索(轨迹) ? '确认支援对象' : '锁定对手'}`;
          if (类型 === '战术确立') return `[${类型}] ⚔️ ${行动者} 锁定${包裹判定动作名称(normalizeBattleActionDisplayName(轨迹.finalResolvedActionName || 轨迹.技能 || '当前动作'))}`;
          const 技能 = normalizeBattleActionDisplayName(轨迹.finalResolvedActionName || 轨迹.实际技能 || 轨迹.技能 || '');
          if (判定轨迹是行为链校验(轨迹)) {
            const 标签 = /应招审计/.test(类型)
              ? '应招校验'
              : /再判定审计/.test(类型)
                ? '落点校验'
                : '变招校验';
            return `[${类型}] 🧭 ${行动者} ${标签}${技能 ? 包裹判定动作名称(技能) : ''}`;
          }
          if (/收招转防/.test(技能)) return `[${类型}] ⚔️ ${行动者} 转入守势`;
          if (/战术观察/.test(技能)) return `[${类型}] ⚔️ ${行动者} 观察战局`;
          if (/待机|守势维持|守势对峙/.test(技能)) return `[${类型}] ⚔️ ${行动者} 稳住节奏`;
          if (技能) return `[${类型}] ⚔️ ${行动者} 最终执行${包裹判定动作名称(技能)}`;
          return `[${类型}] ⚔️ ${行动者} 守势观察`;
        }

        function 构建候选证据列表(轨迹 = {}) {
          return 读取判定流程候选列表(轨迹, 0).map(item => ({
            候选名: 读取候选名称(item),
            权重: 读取候选权重(item),
            目标: String(item?.目标 || item?.target || '').trim(),
            资源可行: item?.资源可行 ?? item?.审计?.资源可行 ?? null,
            资源修正: Number(item?.资源修正 ?? item?.审计?.资源修正 ?? 0),
            审计: item?.审计 || {},
          }));
        }

        function 格式化预演审计明细(轨迹 = {}, 前置 = null) {
          if (读取轨迹类型(轨迹) === '防反机制') {
            return [
              '原始类型：防反机制',
              `行动者：${轨迹.行动者 || ''}`,
              `目标：${轨迹.目标 || ''}`,
              `结果：${轨迹.result || ''}`,
              `防反原始日志：${轨迹.rawText || ''}`,
            ].join('\n');
          }
          const sidecar = 轨迹?.__narrativeDecisionSidecar && typeof 轨迹.__narrativeDecisionSidecar === 'object'
            ? 轨迹.__narrativeDecisionSidecar
            : null;
          const narrativeAudit = sidecar ? {
            narrationTrustLevel: String(sidecar.narrationTrustLevel || ''),
            formulaTrustLevel: String(sidecar.formulaTrustLevel || ''),
            dominantReason: String(sidecar.dominantReason || ''),
            fatalCodes: Array.isArray(sidecar.fatalCodes) ? sidecar.fatalCodes.map(code => String(code || '')).filter(Boolean) : [],
          } : null;
          const 权重字段 = {
            原始权重: Number(轨迹.原始权重 || 0),
            战术修正: Number(轨迹.战术修正 || 0),
            团队意图修正: Number(轨迹.团队意图修正 || 0),
            repeatCount: Number(轨迹.repeatCount || 0),
            repeatDecayMultiplier: Number(轨迹.repeatDecayMultiplier || 1),
            repeatDecayReasonCode: String(轨迹.repeatDecayReasonCode || ''),
            repeatExceptionCode: String(轨迹.repeatExceptionCode || ''),
            scoreBeforeRepeatDecay: Number(轨迹.scoreBeforeRepeatDecay || 0),
            scoreAfterRepeatDecay: Number(轨迹.scoreAfterRepeatDecay || 0),
            资源修正: Number(轨迹.资源修正 || 0),
            最终权重: Number(轨迹.最终权重 || 0),
          };
          return [
            `原始类型：${读取轨迹类型(轨迹)}`,
            `行动者：${轨迹.行动者 || ''}`,
            `目标：${轨迹.目标 || ''}`,
            `回合：${Number(轨迹.回合 || 0)}`,
            `候选来源：${轨迹.候选来源 || ''}`,
            `原始选择原因：${轨迹.选择原因 || ''}`,
            前置 ? `流转前置类型：${读取轨迹类型(前置)}` : '',
            前置 ? `流转前置选择原因：${前置.选择原因 || ''}` : '',
            narrativeAudit ? `NarrativeDecisionDTO审计：\n${格式化审计证据值(narrativeAudit)}` : '',
            `原始权重字段：\n${格式化审计证据值(权重字段)}`,
            `完整候选池：\n${格式化审计证据值(构建候选证据列表(轨迹))}`,
            `过滤证据字段：\n${格式化审计证据值(读取过滤证据字段(轨迹))}`,
            轨迹.动作校正?.发生校正 ? `执行校对证据：\n${格式化审计证据值(轨迹.动作校正)}` : '',
          ].filter(Boolean).join('\n');
        }

        function 判定显示战斗开发态明细() {
          return window?.__LWCS_BATTLE_DEBUG_VISIBLE__ === true || window?.BattleUI?.state?.showBattleDebug === true;
        }

        function 渲染战斗开发态明细(内容 = '') {
          if (!判定显示战斗开发态明细()) return '';
          return `
              <details class="battle-preview-debug">
                <summary>展开底层调试明细 (开发模式)</summary>
                <pre><code>${htmlEscapeText(内容)}</code></pre>
              </details>
            `;
        }

        function 渲染NarrativeDecision开发态摘要(decisionTrace = []) {
          if (!判定显示战斗开发态明细()) return '';
          const lines = (Array.isArray(decisionTrace) ? decisionTrace : [])
            .filter(trace => /主动规划|战术确立/.test(读取轨迹类型(归一判定轨迹(trace || {}))))
            .map(trace => {
              补写NarrativeDecisionSidecar(trace);
              const normalized = 归一判定轨迹(trace || {});
              const sidecar = trace?.__narrativeDecisionSidecar && typeof trace.__narrativeDecisionSidecar === 'object'
                ? trace.__narrativeDecisionSidecar
                : null;
              if (!sidecar) return '';
              return `NarrativeDecisionDTO审计：\n${格式化审计证据值({
                actorName: normalized.行动者 || '',
                targetName: normalized.displayTargetName || normalized.目标 || '',
                finalActionName: normalizeBattleActionDisplayName(normalized.finalResolvedActionName || normalized.技能 || normalized.hitCandidateName || ''),
                narrationTrustLevel: String(sidecar.narrationTrustLevel || ''),
                formulaTrustLevel: String(sidecar.formulaTrustLevel || ''),
                dominantReason: String(sidecar.dominantReason || ''),
                fatalCodes: Array.isArray(sidecar.fatalCodes) ? sidecar.fatalCodes.map(code => String(code || '')).filter(Boolean) : [],
              })}`;
            })
            .filter(Boolean);
          return lines.length ? 渲染战斗开发态明细(lines.join('\n\n')) : '';
        }

        function 渲染判定流程卡片(轨迹 = {}, 前置 = null) {
          const normalized = 归一判定轨迹(轨迹);
          if (/主动规划|战术确立/.test(读取轨迹类型(normalized))) {
            构建NarrativeDecisionDTO(轨迹);
          }
          const 校正 = normalized.动作校正?.发生校正 ? normalized.动作校正 : null;
          const 中间推演列表 = Array.isArray(normalized.中间推演列表) ? normalized.中间推演列表 : [];
          const 目标遍历详情 = Array.isArray(normalized.目标遍历详情) ? normalized.目标遍历详情 : [];
          const cardClass = [
            'battle-preview-trace-row',
            'battle-preview-trace-card',
            判定轨迹是目标规划(normalized) ? 'battle-preview-trace-card--target' : 'battle-preview-trace-card--action',
            校正 ? 'battle-preview-trace-card--corrected' : '',
          ].filter(Boolean).join(' ');
          const 最终权重 = Math.round(Number(normalized.最终权重 || 0));
          const 零分紧凑 = /应招审计|再判定审计/.test(读取轨迹类型(normalized)) &&
            最终权重 === 0 &&
            !normalized.动作校正?.发生校正 &&
            (!normalized.中间推演列表 || normalized.中间推演列表.length === 0) &&
            (!normalized.目标遍历详情 || normalized.目标遍历详情.length === 0);
          const 校正HTML = 校正 ? `
              <div class="battle-preview-trace-correction">
                <b>${渲染判定侧写HTML(`原定倾向${包裹判定动作名称(校正.审计技能)}，改以${包裹判定动作名称(校正.实际技能)}应对。`, normalized)}</b>
              </div>
            ` : '';
          return `
            <div class="${cardClass}">
              <div class="battle-preview-trace-title">
                <b>${htmlEscapeText(构建轨迹标题(normalized))}</b>
              </div>
              ${零分紧凑 ? `
                <details class="battle-preview-trace-fold battle-preview-trace-fold--compact">
                  <summary>${htmlEscapeText(`${normalized.行动者 || '该单位'} ${/应招审计/.test(读取轨迹类型(normalized)) ? '校验当前攻势' : '复核当前动作'}（${/无法反应/.test(normalized.技能 || '') ? '节奏受压' : '暂未展开'}）`)}</summary>
                  <div class="battle-preview-trace-subtree">
                    ${构建轨迹侧写行(normalized).flatMap(line => String(line || '').split(/\r?\n/)).map(line => `<div class="battle-preview-trace-line">${渲染判定侧写HTML(line, normalized)}</div>`).join('')}
                  </div>
                </details>
              ` : `
                <div class="battle-preview-trace-tree">
                  ${构建轨迹侧写行(normalized).flatMap(line => String(line || '').split(/\r?\n/)).map(line => `<div class="battle-preview-trace-line">${渲染判定侧写HTML(line, normalized)}</div>`).join('')}
                </div>
              `}
              ${渲染判定候选对比HTML(normalized)}
              ${中间推演列表.length ? `
                <details class="battle-preview-trace-fold">
                  <summary>中间推演</summary>
                  <div class="battle-preview-trace-subtree">
                    ${中间推演列表.map(item => `<div class="battle-preview-trace-line">${渲染判定侧写HTML(`└─ ${构建中间推演摘要(item)}`, 归一判定轨迹(item))}</div>`).join('')}
                  </div>
                </details>
              ` : ''}
              ${目标遍历详情.length ? `
                <details class="battle-preview-trace-fold">
                  <summary>目标遍历详情</summary>
                  <div class="battle-preview-trace-subtree">
                    ${目标遍历详情.map(item => `<div class="battle-preview-trace-line">${渲染判定侧写HTML(`└─ ${构建中间推演摘要(item)}`, 归一判定轨迹(item))}</div>`).join('')}
                  </div>
                </details>
              ` : ''}
              ${校正HTML}
              ${渲染战斗开发态明细(格式化预演审计明细(normalized, 前置))}
            </div>
          `;
        }

        function 渲染流转侧写卡片(条目 = {}) {
          const from = 归一判定轨迹(条目.from || {});
          const to = 归一判定轨迹(条目.to || {});
          const 标题 = `[${读取轨迹类型(from) || '判定'}] 🔄 ${from.行动者 || '行动者'} 战术过渡`;
          const 明细 = [
            `流转前类型：${读取轨迹类型(from)}`,
            `流转前行动者：${from.行动者 || ''}`,
            `流转前回合：${Number(from.回合 || 0)}`,
            `流转前选择原因：${from.选择原因 || ''}`,
            `移交目标类型：${读取轨迹类型(to)}`,
            `移交目标候选来源：${to.候选来源 || ''}`,
            `移交目标技能：${to.技能 || ''}`,
          ].join('\n');
          return `
            <div class="battle-preview-trace-row battle-preview-trace-card battle-preview-trace-card--handoff">
              <div class="battle-preview-trace-title">
                <b>${htmlEscapeText(标题)}</b>
              </div>
              <div class="battle-preview-trace-tree">
                <span>${渲染判定侧写HTML('└─ 战略直觉暂未形成直接动作，转入常规战术博弈。', from)}</span>
              </div>
              ${渲染战斗开发态明细(明细)}
            </div>
          `;
        }

        function 渲染防反侧写卡片(轨迹 = {}) {
          const normalized = 归一判定轨迹(轨迹);
          const 错失 = String(normalized.result || '').trim() === 'fail';
          const 标题 = `[防反机制] ⚡ ${normalized.行动者 || '系统'} ${错失 ? '反击未成' : '完成反击'}`;
          const 反击动作 = normalizeBattleActionDisplayName(normalized.sourceActionName || '');
          const 显示动作 = 判定防反动作名缺失(反击动作) ? 'Action_Missing' : 反击动作;
          const 伤害 = Math.max(0, Number(normalized.damage || 0));
          const 原失败原因 = String(normalized.failReason || '').trim();
          const 概率片段 = String(原失败原因.match(/[（(]概率[:：]\s*\d+%[)）]/)?.[0] || '').trim();
          const 失败原因 = 原失败原因
            .replace(/[（(]概率[:：]\s*\d+%[)）]/g, '')
            .replace(/^未能完成反打$/g, '未形成稳定反打条件')
            .trim();
          const 展示失败原因 = 失败原因 || (概率片段 ? `未形成稳定反打条件${概率片段}` : '未形成稳定反打条件');
          const 目标 = String(normalized.目标 || normalized.displayTargetName || '').trim();
          return `
            <div class="battle-preview-trace-row battle-preview-trace-card battle-preview-trace-card--counter battle-preview-trace-subcard">
              <div class="battle-preview-trace-title">
                <b>${htmlEscapeText(标题)}</b>
              </div>
              <div class="battle-preview-trace-tree">
                <span>${渲染判定侧写HTML(`├─ ${错失 ? '攻防交换中短暂出现反打机会。' : '攻防交换中出现明确反击窗口。'}`, normalized)}</span>
                <span>${渲染判定侧写HTML(`└─ ${错失
                  ? `尝试以${包裹判定动作名称(显示动作)}反打，但${展示失败原因}。`
                  : (
                    伤害 > 0
                      ? `${normalized.行动者 || '反击方'}凭${包裹判定动作名称(显示动作)}抓住${目标 || '对手'}的破绽反击，造成了 ${伤害} 点伤害。`
                      : `${normalized.行动者 || '反击方'}凭${包裹判定动作名称(显示动作)}抓住${目标 || '对手'}的破绽反击，但未能造成实质伤害。`
                  )}`, normalized)}</span>
              </div>
              ${渲染战斗开发态明细(格式化预演审计明细(normalized))}
            </div>
          `;
        }

        function 渲染回合末聚合侧写卡片(条目 = {}) {
          const text = String(条目?.text || '').trim();
          if (!text) return '';
          const children = Array.isArray(条目?.children) ? 条目.children.map(item => String(item || '').trim()).filter(Boolean) : [];
          return `
            <details class="battle-preview-trace-row battle-preview-trace-card battle-preview-trace-card--settlement battle-preview-trace-subcard battle-preview-trace-card--aggregation" open>
              <summary class="battle-preview-trace-title">
                <b>${htmlEscapeText('[回合收束] 🩸 状态结算聚合')}</b>
              </summary>
              <div class="battle-preview-trace-tree">
                <span>${渲染判定侧写HTML(`├─ 🧾 摘要：${text}`)}</span>
                ${children.map((line, index) => `<span>${渲染判定侧写HTML(`${index === children.length - 1 ? '└─' : '├─'} ${line}`)}</span>`).join('\n')}
              </div>
            </details>
          `;
        }
        function 渲染结算链侧写卡片(条目 = {}) {
          const text = String(条目?.text || '').trim();
          if (!text) return '';
          const details = (Array.isArray(条目?.details) ? 条目.details : [])
            .map(line => String(line || '').trim())
            .filter(Boolean);
          const 标题 = String(条目?.kind || '') === 'status_tick' ? '[回合结算] 🩸 状态与环境影响' : '[结算链] 🧾 本回合结果';
          const detailHtml = details.length ? `
              <details class="battle-preview-trace-detail-fold">
                <summary>展开结算明细</summary>
                <div class="battle-preview-trace-tree battle-preview-trace-timeline battle-preview-trace-detail-tree">
                  ${details.map((line, index) => `<div class="battle-preview-trace-node battle-preview-trace-node--settlement battle-preview-trace-node--detail" data-trace-node-kind="settlement"><span>${渲染判定侧写HTML(`${index === details.length - 1 ? '└─' : '├─'} ${line}`)}</span></div>`).join('\n')}
                </div>
              </details>
          ` : '';
          return `
            <div class="battle-preview-trace-row battle-preview-trace-card battle-preview-trace-card--settlement ${String(条目?.kind || '') === 'status_tick' ? 'battle-preview-trace-subcard' : ''}">
              <div class="battle-preview-trace-title">
                <b>${htmlEscapeText(标题)}</b>
              </div>
              <div class="battle-preview-trace-tree">
                <span>${渲染判定侧写HTML(`└─ ${text}`)}</span>
              </div>
              ${detailHtml}
            </div>
          `;
        }

        function 格式化预演审计行(条目 = {}) {
          if (条目?.type === 'handoff') return 渲染流转侧写卡片(条目);
          if (条目?.type === 'resolution_action_block') return 渲染因果链行动区块(条目);
          if (条目?.type === 'settlement_aggregation') return 渲染回合末聚合侧写卡片(条目);
          if (条目?.type === 'settlement') return 渲染结算链侧写卡片(条目);
          const trace = 归一判定轨迹(条目?.trace || 条目 || {});
          if (读取轨迹类型(trace) === '防反机制') return 渲染防反侧写卡片(trace);
          return 渲染判定流程卡片(trace);
        }

        function 读取判定条目回合(条目 = {}, fallbackRound = 0) {
          if (条目?.type === 'settlement_aggregation' || 条目?.type === 'settlement' || 条目?.type === 'resolution_action_block') return Number(条目.round || 条目.回合 || fallbackRound || 0);
          const trace = 条目?.type === 'handoff' ? (条目.to || 条目.from || {}) : (条目.trace || 条目 || {});
          const round = Number(trace?.回合 || trace?.round || trace?.实际回合 || 0);
          return round > 0 ? round : Math.max(0, Number(fallbackRound || 0));
        }

        function 读取判定条目阶段排序值(条目 = {}) {
          const section = 读取判定条目分段键(条目);
          if (section === 'action_chain') return 5;
          if (section === 'decision') return 10;
          if (section === 'reaction') return 20;
          if (section === 'counter') return 30;
          if (section === 'action_result') return 40;
          if (section === 'turn_end') return 50;
          return 15;
        }

        function 读取判定条目分段键(条目 = {}) {
          if (条目?.type === 'resolution_action_block') return 'action_chain';
          if (条目?.type === 'settlement_aggregation') return 'turn_end';
          if (条目?.type === 'settlement') {
            return String(条目.roundPhase || '').trim() === 'turn_end' ? 'turn_end' : 'action_result';
          }
          const trace = 归一判定轨迹(条目?.type === 'handoff' ? (条目.to || 条目.from || {}) : (条目?.trace || 条目 || {}));
          return 读取判定流程分段键(trace);
        }

        function 渲染分回合判定流程(条目列表 = []) {
          const list = Array.isArray(条目列表) ? 条目列表 : [];
          if (!list.length) return '<div class="battle-preview-empty">无判定轨迹</div>';
          const 排序后列表 = list.slice().sort((左, 右) =>
            读取判定条目回合(左, 0) - 读取判定条目回合(右, 0) ||
            读取判定条目阶段排序值(左) - 读取判定条目阶段排序值(右),
          );
          const groups = [];
          let currentRound = 0;
          排序后列表.forEach(条目 => {
            const round = 读取判定条目回合(条目, currentRound);
            if (round > 0) currentRound = round;
            const key = round > 0 ? `round:${round}` : 'unassigned';
            let group = groups.find(item => item.key === key);
            if (!group) {
              group = { key, round, sections: new Map() };
              groups.push(group);
            }
            const sectionKey = 读取判定条目分段键(条目);
            const section = group.sections.get(sectionKey) || [];
            section.push(条目);
            group.sections.set(sectionKey, section);
          });
          const 顺序 = ['action_chain', 'decision', 'reaction', 'counter', 'action_result', 'turn_end'];
          const 分段标题 = {
            action_chain: '行动链',
            decision: '目标与行动',
            reaction: '应招与再判定',
            counter: '防反',
            action_result: '结算链',
            turn_end: '回合结算',
          };
          return groups.map(group => {
            const 有行动链主视图 = (group.sections.get('action_chain') || []).length > 0;
            const sectionHTML = 顺序.map(key => {
              if (有行动链主视图 && !['action_chain', 'turn_end'].includes(key)) return '';
              if (!有行动链主视图 && !['turn_end'].includes(key)) return '';
              const rows = group.sections.get(key) || [];
              if (!rows.length) return '';
              const cards = rows.map(格式化预演审计行).filter(html => String(html || '').trim());
              if (!cards.length) return '';
              return `
                <div class="battle-preview-trace-section">
                  <div class="battle-preview-trace-section-title">${htmlEscapeText(分段标题[key] || key)}</div>
                  ${cards.join('')}
                </div>
              `;
            }).filter(Boolean).join('');
            if (!sectionHTML) return '';
            return `
              <section class="battle-preview-trace-round">
                <div class="battle-preview-trace-round-title">${htmlEscapeText(group.round > 0 ? `第${group.round}回合` : '其他判定片段')}</div>
                ${sectionHTML}
              </section>
            `;
          }).filter(Boolean).join('');
        }

        function 构建战斗结果展示上下文(result = null) {
          const snapshot = result?.snapshot && typeof result.snapshot === 'object' ? result.snapshot : {};
          const teamPlayer = Array.isArray(snapshot.team_player) ? snapshot.team_player : [];
          const teamEnemy = Array.isArray(snapshot.team_enemy) ? snapshot.team_enemy : [];
          const sourceCombatData = result?.combatData && typeof result.combatData === 'object'
            ? result.combatData
            : result?.snapshot?.__父级战斗数据 && typeof result.snapshot.__父级战斗数据 === 'object'
              ? result.snapshot.__父级战斗数据
              : null;
          return {
            combatData: sourceCombatData || {
              参战者: {
                team_player: teamPlayer,
                team_enemy: teamEnemy,
              },
            },
            units: [...teamPlayer, ...teamEnemy],
          };
        }

        function 提取战斗结果战报Blocks(result = null) {
          const context = 构建战斗结果展示上下文(result);
          const ledger = result?.eventLedger || result?.combatData?.__battleEventLedger || [];
          const trace = Array.isArray(result?.resolutionTrace) ? result.resolutionTrace : (Array.isArray(result?.combatData?.__battleResolutionTrace) ? result.combatData.__battleResolutionTrace : []);
          const ledgerById = new Map((Array.isArray(ledger) ? ledger : []).map(event => [String(event?.eventId || '').trim(), event]).filter(([id]) => id));
          const traceById = new Map(trace.map(node => [String(node?.nodeId || '').trim(), node]).filter(([id]) => id));
          const 有防反来源 = (item = {}) => {
            const blocks = Array.isArray(item?.blocks) ? item.blocks : [];
            const eventIds = [];
            const nodeIds = [];
            blocks.forEach(block => {
              eventIds.push(String(block?.sourceEventId || '').trim());
              nodeIds.push(String(block?.sourceNodeId || '').trim());
              if (Array.isArray(block?.sourceEventIds)) block.sourceEventIds.forEach(id => eventIds.push(String(id || '').trim()));
              if (Array.isArray(block?.sourceNodeIds)) block.sourceNodeIds.forEach(id => nodeIds.push(String(id || '').trim()));
            });
            return eventIds.some(id => ['counter', 'counter_window'].includes(String(ledgerById.get(id)?.eventKind || '').trim())) ||
              nodeIds.some(id => ['counter_action', 'counter_window'].includes(String(traceById.get(id)?.nodeKind || '').trim()));
          };
          const sourceBlocks = Array.isArray(result?.publicReportBlocks) && result.publicReportBlocks.length
            ? result.publicReportBlocks
            : 构建事件账本公开战报Blocks(ledger, 10, context);
          return sourceBlocks
            .map(item => {
              const entry = 归一公开战报Block条目(item);
              if (!entry) return null;
              if (/反打/.test(entry.text) && !有防反来源(entry)) return null;
              return entry;
            })
            .filter(Boolean);
        }

        function 提取战斗结果战报行(result = null) {
          return 提取战斗结果战报Blocks(result).map(item => 序列化公开战报Blocks(item?.blocks)).filter(Boolean);
        }

        function 构建回合速览数据(result = null, context = {}) {
          const ledger = Array.isArray(result?.eventLedger) ? result.eventLedger : (Array.isArray(result?.combatData?.__battleEventLedger) ? result.combatData.__battleEventLedger : []);
          const rounds = new Map();
          const pushRound = round => {
            const key = Math.max(0, Number(round || 0));
            if (!rounds.has(key)) rounds.set(key, { round: key, playerHpDelta: 0, enemyHpDelta: 0, playerHpSourceEventIds: [], enemyHpSourceEventIds: [], resourceDeltas: [], highlights: [] });
            return rounds.get(key);
          };
          const pushHighlight = (round, text, weight = 1, source = {}) => {
            const clean = String(text || '').trim();
            if (!clean) return;
            const item = pushRound(round);
            const sourceEventId = String(source?.eventId || source?.sourceEventId || '').trim();
            const sourceNodeId = String(source?.chainNodeId || source?.nodeId || source?.sourceNodeId || '').trim();
            if (!item.highlights.some(entry => entry.text === clean)) {
              item.highlights.push({ text: clean, weight: Number(weight || 1), sourceEventId, sourceNodeId });
            }
          };
          const pushSourceId = (list = [], source = {}) => {
            const sourceEventId = String(source?.eventId || source?.sourceEventId || '').trim();
            if (sourceEventId && !list.includes(sourceEventId)) list.push(sourceEventId);
          };
          const pushHpDelta = (row, side = '', value = 0, source = {}) => {
            const amount = Math.round(Number(value || 0));
            if (!row || !amount) return;
            if (side === 'player') {
              row.playerHpDelta += amount;
              pushSourceId(row.playerHpSourceEventIds, source);
            } else if (side === 'enemy') {
              row.enemyHpDelta += amount;
              pushSourceId(row.enemyHpSourceEventIds, source);
            }
          };
          const pushResourceDelta = (round, actorName = '', resourceName = '', value = 0, source = {}) => {
            const actorText = String(actorName || '').trim();
            const resourceText = String(resourceName || '').trim();
            const amount = Math.round(Number(value || 0));
            if (!actorText || !resourceText || !amount) return;
            const item = pushRound(round);
            const key = `${actorText}|${resourceText}`;
            const existing = item.resourceDeltas.find(entry => entry.key === key);
            if (existing) {
              existing.value += amount;
              pushSourceId(existing.sourceEventIds, source);
            } else {
              const sourceEventIds = [];
              pushSourceId(sourceEventIds, source);
              item.resourceDeltas.push({ key, actorName: actorText, resourceName: resourceText, value: amount, sourceEventIds });
            }
          };
          ledger.forEach(event => {
            const round = Math.max(0, Number(event?.round || event?.sourceRound || 0));
            const kind = String(event?.eventKind || '').trim();
            const actor = String(event?.actorName || '').trim();
            const target = String(event?.targetName || '').trim();
            const action = normalizeBattleActionDisplayName(event?.finalActionName || event?.actionName || '');
            const result = String(event?.result || event?.primaryOutcome || event?.meta?.primaryOutcome || '').trim();
            const reason = String(event?.failReason || event?.failureReason || event?.meta?.failureReason || event?.reasonCode || event?.meta?.reasonCode || '').trim();
            if (判定公开战报事件是内部兜底(event) && !/战术待机|待机|观察|防御|收招转防|守势|pass|observe|defend|stance/i.test(`${action} ${result} ${reason}`)) return;
            const targetSide = 读取战报单位阵营(context, target);
            const row = pushRound(round);
            const damage = Math.max(0, 读取事件账本数值(event, 'damage') || 读取事件账本数值(event, 'amount'));
            if ((kind === 'hit_result' || kind === 'counter' || kind === 'state_tick') && damage > 0) {
              if (targetSide === 'player') pushHpDelta(row, 'player', -damage, event);
              else if (targetSide === 'enemy') pushHpDelta(row, 'enemy', -damage, event);
              if (kind === 'counter') pushHighlight(round, `${actor}防反命中${target}${damage ? `，${damage}伤害` : ''}`, 8, event);
              else if (damage >= 100 || /魂技|真身|融合|爆发/.test(action)) pushHighlight(round, `${actor}以【${action || '行动'}】重创${target}${damage ? `，${damage}伤害` : ''}`, /魂技|真身|融合|爆发/.test(action) || damage >= 160 ? 9 : 8, event);
            }            if (kind === 'action_cost') {
              const reqSp = Math.max(0, Number(event?.meta?.reqSp || 0));
              const reqVit = Math.max(0, Number(event?.meta?.reqVit || 0));
              const reqMen = Math.max(0, Number(event?.meta?.reqMen || 0));
              if (reqSp) pushResourceDelta(round, actor, '魂力', -reqSp, event);
              if (reqVit) pushResourceDelta(round, actor, '体力', -reqVit, event);
              if (reqMen) pushResourceDelta(round, actor, '精神力', -reqMen, event);
            } else if (kind === 'round_recover') {
              const resource = String(event?.meta?.resource || '').trim();
              const amount = Math.max(0, 读取事件账本数值(event, 'amount'));
              if (amount && resource) pushResourceDelta(round, actor, resource, amount, event);
            } else if (kind === 'state_tick') {
              const resource = String(event?.meta?.resource || '').trim();
              if (damage > 0 && resource && !/生命|HP|血/i.test(resource)) {
                const isHeal = /恢复|heal|hot/i.test(String(event?.result || ''));
                pushResourceDelta(round, target || actor, resource, isHeal ? damage : -damage, event);
              }
            } else if (kind === 'resource_change') {
              const resource = String(event?.meta?.resource || '').trim();
              const delta = Number(event?.meta?.delta || 0);
              if (resource && delta) pushResourceDelta(round, target || actor, resource, delta, event);
            }
          if (kind === 'state_apply' && 事件账本状态已附着(event)) {
              const stateName = 读取事件账本状态名(event);
              if (stateName) pushHighlight(round, `${target || actor}陷入【${stateName}】`, /控制|眩晕|禁锢|位移限制|迟缓|减速/.test(stateName) ? 7 : 4, event);
            } else if (kind === 'state_apply' && 事件账本状态被免疫(event)) {
              const stateName = 读取事件账本状态名(event);
              if (stateName) pushHighlight(round, `${target || actor}免疫【${stateName}】`, /控制|眩晕|禁锢|位移限制|迟缓|减速/.test(stateName) ? 6 : 3, event);
            } else if (kind === 'state_apply' && 事件账本状态被抵抗(event)) {
              const stateName = 读取事件账本状态名(event);
              if (stateName) pushHighlight(round, `${target || actor}抵住【${stateName}】`, /控制|眩晕|禁锢|位移限制|迟缓|减速/.test(stateName) ? 6 : 3, event);
            } else if (kind === 'summon_create') {
              const summonName = String(event?.summonName || event?.createdName || '').trim();
              pushHighlight(round, `${actor}召出${summonName ? `【${summonName}】` : '召唤物'}`, 7, event);
            } else if (kind === 'blocked_action' || kind === 'failed_action') {
              if (读取战报事件Outcome(event) === 'cap_reached') pushHighlight(round, `${actor}造物已达上限`, 5, event);
              else pushHighlight(round, `${actor}动作受阻`, 5, event);
            } else if (kind === 'defend') {
              pushHighlight(round, `${actor}转入防御`, 3, event);
            } else if (kind === 'pass') {
              pushHighlight(round, `${actor}暂缓出手`, 3, event);
            } else if (kind === 'dodge' && /evaded|dodged|闪避|规避/i.test(result)) {
              pushHighlight(round, `${actor}规避成功`, 4, event);
            } else if (kind === 'action_start' && 判定守势待机动作(action)) {
              pushHighlight(round, `${actor}暂缓出手`, 3, event);
            }
          });
          (Array.isArray(result?.resolutionTrace) ? result.resolutionTrace : [])
            .filter(node => node && typeof node === 'object' && String(node.nodeKind || '').trim() === 'action_decision')
            .forEach(node => {
              const round = Math.max(0, Number(node.round || 0));
              const actor = String(node.actorName || '').trim();
              const action = normalizeBattleActionDisplayName(node.finalActionName || node.actionName || '');
              if (!(round > 0) || !actor || !action) return;
              pushHighlight(round, `${actor}持续施压`, 2, { sourceNodeId: node.nodeId });
            });
          return [...rounds.values()]
            .filter(item => item.round > 0 && (item.playerHpDelta || item.enemyHpDelta || item.resourceDeltas.length || item.highlights.length))
            .sort((a, b) => a.round - b.round)
            .map(item => ({
              ...item,
              resourceDeltas: item.resourceDeltas
                .filter(entry => Math.round(Number(entry.value || 0)) !== 0)
                .slice(0, 4)
                .map(entry => ({ ...entry, value: Math.round(Number(entry.value || 0)), sourceEventIds: Array.isArray(entry.sourceEventIds) ? entry.sourceEventIds.slice(0, 8) : [] })),
              playerHpSourceEventIds: Array.isArray(item.playerHpSourceEventIds) ? item.playerHpSourceEventIds.slice(0, 12) : [],
              enemyHpSourceEventIds: Array.isArray(item.enemyHpSourceEventIds) ? item.enemyHpSourceEventIds.slice(0, 12) : [],
              highlights: item.highlights.sort((a, b) => b.weight - a.weight).slice(0, 3),
            }));
        }

        function 序列化回合速览行(rows = []) {
          const result = [];
          const formatDelta = value => value < 0 ? `${value} HP` : value > 0 ? `+${value} HP` : '0';
          (Array.isArray(rows) ? rows : []).forEach(item => {
            const parts = [`我方${formatDelta(Number(item?.playerHpDelta || 0))}`, `敌方${formatDelta(Number(item?.enemyHpDelta || 0))}`];
            const resourceParts = (Array.isArray(item?.resourceDeltas) ? item.resourceDeltas : [])
              .map(entry => `${entry.actorName || '单位'}${entry.resourceName || '资源'}${Number(entry.value || 0) > 0 ? '+' : ''}${Math.round(Number(entry.value || 0))}`)
              .filter(Boolean);
            const highlights = (Array.isArray(item?.highlights) ? item.highlights : []).map(entry => typeof entry === 'string' ? entry : entry?.text).filter(Boolean);
            result.push(`第${Number(item?.round || 0)}回合速览：${parts.join('，')}${resourceParts.length ? `；资源：${resourceParts.join('，')}` : ''}${highlights.length ? `；高光：${highlights.join('；')}` : ''}`);
          });
          return result;
        }

        function 渲染回合速览HTML(rows = []) {
          const list = Array.isArray(rows) ? rows : [];
          if (!list.length) return '';
          const formatDelta = value => value < 0 ? `${value} HP` : value > 0 ? `+${value} HP` : '0';
          const renderHpDelta = (label, value, ratioValue = 0, sourceEventIds = []) => {
            const delta = Number(value || 0);
            const className = `battle-round-dashboard-delta${delta < 0 ? ' is-loss' : delta > 0 ? ' is-gain' : ''}`;
            const ratio = Math.max(0, Math.min(100, Math.round(Number(ratioValue || 0))));
            const sourceAttr = (Array.isArray(sourceEventIds) ? sourceEventIds : []).map(id => String(id || '').trim()).filter(Boolean).slice(0, 12).join(',');
            return `<span class="${className}" data-hp-delta="${htmlEscapeText(String(delta))}" data-delta-ratio="${ratio}"${sourceAttr ? ` data-source-event-ids="${htmlEscapeText(sourceAttr)}"` : ''}><span class="battle-round-dashboard-delta-fill" style="width:${ratio}%"></span><span class="battle-round-dashboard-delta-text">${htmlEscapeText(`${label} ${formatDelta(delta)}`)}</span></span>`;
          };
          return `<section class="battle-round-dashboard" aria-label="回合速览">${list.map(item => {
            const playerDelta = Number(item?.playerHpDelta || 0);
            const enemyDelta = Number(item?.enemyHpDelta || 0);
            const maxHpDelta = Math.max(1, Math.abs(playerDelta), Math.abs(enemyDelta));
            const playerRatio = Math.round(Math.abs(playerDelta) / maxHpDelta * 100);
            const enemyRatio = Math.round(Math.abs(enemyDelta) / maxHpDelta * 100);
            const highlights = (Array.isArray(item?.highlights) ? item.highlights : [])
              .map(entry => typeof entry === 'string' ? { text: entry } : entry)
              .filter(entry => String(entry?.text || '').trim());
            const resourceDeltas = (Array.isArray(item?.resourceDeltas) ? item.resourceDeltas : []).filter(entry => Math.round(Number(entry?.value || 0)) !== 0);
            const resourceHtml = resourceDeltas.length ? `<div class="battle-round-dashboard-resources">${resourceDeltas.map(entry => {
              const sourceAttr = (Array.isArray(entry?.sourceEventIds) ? entry.sourceEventIds : []).map(id => String(id || '').trim()).filter(Boolean).slice(0, 8).join(',');
              return `<span${sourceAttr ? ` data-source-event-ids="${htmlEscapeText(sourceAttr)}"` : ''}>${htmlEscapeText(`${entry.actorName || '单位'} ${entry.resourceName || '资源'} ${Number(entry.value || 0) > 0 ? '+' : ''}${Math.round(Number(entry.value || 0))}`)}</span>`;
            }).join('')}</div>` : '';
            const statusLabel = (() => {
              const head = String(highlights[0]?.text || '').trim();
              if (/召出|召唤/.test(head)) return '召唤入场';
              if (/恢复|回稳|资源/.test(head)) return '资源回稳';
              if (/陷入|抵住|免疫/.test(head)) return '状态生效';
              if (/受阻|未能|截断/.test(head)) return '动作截断';
              if (/重创|伤害|防反/.test(head)) return '战果收束';
              return '回合结束';
            })();
            const highlightHtml = highlights.length ? `<div class="battle-round-dashboard-highlights">${highlights.map(entry => {
              const sourceEventId = String(entry?.sourceEventId || '').trim();
              const sourceNodeId = String(entry?.sourceNodeId || '').trim();
              const attrs = [
                sourceEventId ? `data-source-event-id="${htmlEscapeText(sourceEventId)}"` : '',
                sourceNodeId ? `data-source-node-id="${htmlEscapeText(sourceNodeId)}"` : '',
              ].filter(Boolean).join(' ');
              return `<span${attrs ? ` ${attrs}` : ''}>${htmlEscapeText(entry.text)}</span>`;
            }).join('')}</div>` : '';
            return `<div class="battle-round-dashboard-row"><div class="battle-round-dashboard-head"><span>第${Number(item?.round || 0)}回合</span><b>${htmlEscapeText(statusLabel)}</b></div><div class="battle-round-dashboard-bars">${renderHpDelta('我方', playerDelta, playerRatio, item?.playerHpSourceEventIds)}${renderHpDelta('敌方', enemyDelta, enemyRatio, item?.enemyHpSourceEventIds)}</div>${resourceHtml}${highlightHtml}</div>`;
          }).join('')}</section>`;
        }
        function 解码战斗预演HTML实体(text = '') {
          return String(text || '')
            .replace(/&nbsp;/g, ' ')
            .replace(/&lt;/g, '<')
            .replace(/&gt;/g, '>')
            .replace(/&quot;/g, '"')
            .replace(/&#39;/g, "'")
            .replace(/&amp;/g, '&');
        }

        function 提取战斗预演HTML可见文本(html = '') {
          const raw = String(html || '')
            .replace(/<details class="battle-preview-debug">[\s\S]*?<\/details>/gi, '')
            .replace(/<details class="battle-preview-trace-fold(?:\s+[^"]*)?">[\s\S]*?<\/details>/gi, '');
          if (!raw.trim()) return [];
          const withBreaks = raw
            .replace(/<div class="battle-preview-trace-round-title">/gi, '\n<div class="battle-preview-trace-round-title">')
            .replace(/<div class="battle-preview-trace-section-title">/gi, '\n<div class="battle-preview-trace-section-title">')
            .replace(/<div class="battle-preview-trace-title">/gi, '\n<div class="battle-preview-trace-title">')
            .replace(/<div class="battle-preview-trace-candidate-head">/gi, '\n<div class="battle-preview-trace-candidate-head">')
            .replace(/<div class="battle-preview-trace-candidate-line">/gi, '\n<div class="battle-preview-trace-candidate-line">')
            .replace(/<div class="battle-preview-trace-candidate-bar">/gi, '\n<div class="battle-preview-trace-candidate-bar">')
            .replace(/<\/(summary|p|div|section|details|pre|code|em|b)>/gi, '\n')
            .replace(/<(summary|p|div|section|details|pre|code|em|b)(?:\s[^>]*)?>/gi, '\n')
            .replace(/<br\s*\/?>/gi, '\n')
            .replace(/<[^>]+>/g, '');
          const rows = 解码战斗预演HTML实体(withBreaks)
            .split(/\r?\n+/)
            .map(line => /^[\s│]*[├└]─/.test(String(line || '')) ? String(line || '').replace(/[ \t]+$/g, '') : String(line || '').replace(/\s+/g, ' ').trim())
            .filter(Boolean);
          const sectionTitles = new Set(['行动链', '目标与行动', '应招与再判定', '防反', '结算链', '回合结算', '中间推演', '目标遍历详情']);
          const roundTitlePattern = /^第\d+回合$|^其他判定片段$/;
          const visibleRows = rows.filter((line, index) => {
            if (!sectionTitles.has(line)) return true;
            const next = rows[index + 1] || '';
            return Boolean(next) && !sectionTitles.has(next) && !roundTitlePattern.test(next);
          });
          const seenStateSettlement = new Set();
          let currentRound = '';
          return visibleRows.filter(line => {
            const roundMatch = String(line || '').match(/^第(\d+)回合$/);
            if (roundMatch) currentRound = roundMatch[1];
            const stateMatch = String(line || '').match(/状态结算：([^【\n]+?)(?:陷入|免疫|抵住)【([^】]+)】/);
            if (!stateMatch) return true;
            const key = `${currentRound || '0'}::${String(stateMatch[1] || '').trim()}::${String(stateMatch[2] || '').trim()}`;
            if (seenStateSettlement.has(key)) return false;
            seenStateSettlement.add(key);
            return true;
          });
        }

        function 过滤已在摘要出现的结算条目(条目列表 = [], 战报行 = []) {
          const rows = Array.isArray(条目列表) ? 条目列表 : [];
          const summaryLines = (Array.isArray(战报行) ? 战报行 : [])
            .map(line => String(line || '').trim())
            .filter(Boolean);
          const summaries = new Set(summaryLines);
          return rows.filter(item => {
            if (!item || item.type !== 'settlement') return true;
            const round = Number(item.round || item.回合 || 0);
            const text = String(item.text || '').trim();
            if (!text) return false;
            if (item.fromEventLedger === true) return true;
            if (String(item.kind || '') !== 'status_tick' && round > 0) {
              const roundPrefix = `第${round}回合：`;
              if (summaryLines.some(line => line.startsWith(roundPrefix))) return false;
            }
            const prefixed = round > 0 ? `第${round}回合：${text}` : text;
            return !summaries.has(prefixed) && !summaries.has(text);
          });
        }

        function 构建行动链补缺公开战报Blocks(审计条目 = [], 战报Blocks = []) {
          const existingRounds = new Set((Array.isArray(战报Blocks) ? 战报Blocks : [])
            .map(item => Number(String(序列化公开战报Blocks(item?.blocks || []) || '').match(/^第(\d+)回合：/)?.[1] || 0))
            .filter(round => round > 0));
          const supplemental = [];
          const 连续补缺动作 = new Map();
          (Array.isArray(审计条目) ? 审计条目 : [])
            .filter(item => item && item.type === 'resolution_action_block')
            .sort((left, right) => Number(left.round || left.回合 || 0) - Number(right.round || right.回合 || 0))
            .forEach(item => {
              const round = Number(item.round || item.回合 || 0);
              if (!(round > 0) || existingRounds.has(round)) return;
              const root = item.root || {};
              const actor = String(root.actorName || '').trim();
              const target = String(root.targetName || '').trim();
              const action = normalizeBattleActionDisplayName(root.finalActionName || root.actionName || '');
              if (!actor || !action) return;
              const targetText = target ? `朝${target}` : '在战场中';
              const repeatKey = `${actor}::${target}::${action}`;
              const previous = 连续补缺动作.get(repeatKey) || { round: 0, count: 0 };
              const repeatCount = previous.round === round - 1 ? previous.count + 1 : 0;
              连续补缺动作.clear();
              连续补缺动作.set(repeatKey, { round, count: repeatCount });
              const text = repeatCount >= 2
                ? `第${round}回合：${actor}仍以【${action}】压住${target || '战场'}，把连续攻势维持到底。`
                : repeatCount === 1
                  ? `第${round}回合：${actor}再度${targetText}催动【${action}】，延续上一轮的压迫。`
                  : `第${round}回合：${actor}${targetText}亮出【${action}】起手，交锋节奏随即被拉开。`;
              const nodeId = String(root.nodeId || '').trim();
              const textBlock = 构建公开战报文本块(text, {
                ...root,
                sourceNodeId: nodeId,
                sourceNodeIds: nodeId ? [nodeId] : [],
              });
              if (!textBlock) return;
              textBlock.projectionSource = 'action_chain_gap_ast';
              supplemental.push({ round, blocks: [textBlock], text, projectionSource: 'action_chain_gap_ast' });
              existingRounds.add(round);
            });
          return supplemental;
        }

        function 导出战斗记录可见文本(result = null, activeTab = 'preview') {
          if (!result) return '';
          const logs = Array.isArray(result.logs) ? result.logs : [];
          const context = 构建战斗结果展示上下文(result);
          context.eventLedger = result?.eventLedger || result?.combatData?.__battleEventLedger || [];
          const 原始战报Blocks = 提取战斗结果战报Blocks(result);
          const 原始战报 = 原始战报Blocks.map(item => 序列化公开战报Blocks(item?.blocks)).filter(Boolean);
          context.publicBattleLines = 原始战报;
          const 审计条目 = 过滤已在摘要出现的结算条目([
            ...构建因果链行动区块条目(Array.isArray(result.resolutionTrace) ? result.resolutionTrace : [], Array.isArray(result.decisionTrace) ? result.decisionTrace : []),
            ...构建判定流程展示数据(Array.isArray(result.decisionTrace) ? result.decisionTrace : [], logs, context),
            ...构建结算链侧写条目(logs, { ...context, eventLedger: result?.eventLedger || [] }),
          ], 原始战报);
          const 状态结算文本 = 审计条目
            .filter(item => item?.type === 'settlement' && String(item.kind || '') === 'status_tick')
            .map(item => `第${Number(item.round || item.回合 || 0)}回合：${String(item.text || '').trim()}`)
            .filter(Boolean);
          const 战报Blocks = 原始战报Blocks
            .filter(item => !状态结算文本.includes(序列化公开战报Blocks(item?.blocks)))
            .concat(构建行动链补缺公开战报Blocks(审计条目, 原始战报Blocks))
            .map(归一公开战报Block条目)
            .filter(Boolean)
            .sort((left, right) => Number(left?.round || 0) - Number(right?.round || 0));
          const 战报 = 战报Blocks.map(item => 序列化公开战报Blocks(item?.blocks)).filter(Boolean);
          const 流程HTML = 渲染分回合判定流程(审计条目);
          const 回合速览 = 构建回合速览数据(result, context);
          const 头部行 = [
            activeTab === 'preview' ? '预演结果' : '实战结果',
            格式化战斗模式显示文本(result.modeLabel, result.battleMode, result.mode),
            `推进${Math.max(0, Number(result.roundsExecuted || 0))}回合`,
          ].filter(Boolean);
          const 判定流程行 = ['判定流程', ...提取战斗预演HTML可见文本(流程HTML)];
          return [...头部行, ...序列化回合速览行(回合速览), ...战报, ...判定流程行].join('\n').trim();
        }
        root.__LWCS_BATTLE_EXPORT_VISIBLE_TEXT_IMPL__ = (result = null, activeTab = 'preview') =>
          导出战斗记录可见文本(result, activeTab);
        root.__LWCS_RENDER_BATTLE_REPORT_HTML_IMPL__ = (line = '', context = {}) =>
          渲染公开战报HTML(line, context);
        root.__LWCS_RENDER_BATTLE_REPORT_BLOCKS_HTML_IMPL__ = (blocks = [], context = {}) =>
          渲染公开战报BlocksHTML(blocks, context);
        root.__LWCS_BUILD_PUBLIC_REPORT_BLOCKS_IMPL__ = (eventLedger = [], limit = 8, context = {}) =>
          构建事件账本公开战报Blocks(eventLedger, limit, context);
        root.__LWCS_BUILD_BATTLE_LLM_SUMMARY_IMPL__ = (eventLedger = [], context = {}, options = {}) =>
          构建LLM战斗语义摘要(eventLedger, context, options);
        root.__LWCS_BUILD_BATTLE_ROUND_DASHBOARD_IMPL__ = (result = null, context = {}) =>
          构建回合速览数据(result, context);
        root.__LWCS_RENDER_BATTLE_ROUND_DASHBOARD_IMPL__ = (rows = []) =>
          渲染回合速览HTML(rows);
        root.__LWCS_RENDER_BATTLE_RESOLUTION_TRACE_HTML_IMPL__ = (trace = [], decisionTrace = []) =>
          `${渲染分回合判定流程([
            ...构建因果链行动区块条目(trace, Array.isArray(decisionTrace) ? decisionTrace : []),
            ...构建判定流程展示数据(Array.isArray(decisionTrace) ? decisionTrace : [], [], {}),
            ...构建因果链回合末聚合条目(trace),
          ])}${渲染NarrativeDecision开发态摘要(decisionTrace)}`;
        root.__LWCS_RENDER_BATTLE_DECISION_ROWS_HTML_IMPL__ = (rows = []) =>
          渲染分回合判定流程(Array.isArray(rows) ? rows : []);

        function 渲染战斗记录面板() {
          const node = 读取战斗记录面板节点();
          if (!node) return;
          const state = window.BattleUI?.state || {};
          同步战斗记录展开状态();
          const activeTab = 读取战斗记录页签();
          读取战斗记录终端节点()?.querySelectorAll('[data-battle-record-tab]').forEach(button => {
            const active = button.getAttribute('data-battle-record-tab') === activeTab;
            button.classList.toggle('active', active);
            button.setAttribute('aria-selected', active ? 'true' : 'false');
          });
          const result = activeTab === 'preview' ? state.previewResult : state.actualBattleResult;
          if (!result) {
            node.hidden = false;
            node.innerHTML = `
              <div class="battle-preview-head">
                <span>${activeTab === 'preview' ? '预演记录' : '实战记录'}</span>
                <b>暂无记录</b>
                <em>${activeTab === 'preview' ? '点击预演生成' : '点击结算生成'}</em>
              </div>
              <div class="battle-preview-empty">${activeTab === 'preview' ? '预演不会提交战斗，只用于查看预期战报和判定流程。' : '实战结算后会在这里保留本次战报。'}</div>
            `;
            return;
          }
          const logs = Array.isArray(result.logs) ? result.logs : [];
          const context = 构建战斗结果展示上下文(result);
          context.eventLedger = result?.eventLedger || result?.combatData?.__battleEventLedger || [];
          const 原始战报Blocks = 提取战斗结果战报Blocks(result);
          const 原始战报 = 原始战报Blocks.map(item => 序列化公开战报Blocks(item?.blocks)).filter(Boolean);
          context.publicBattleLines = 原始战报;
          const 审计条目 = 过滤已在摘要出现的结算条目([
            ...构建因果链行动区块条目(Array.isArray(result.resolutionTrace) ? result.resolutionTrace : [], Array.isArray(result.decisionTrace) ? result.decisionTrace : []),
            ...构建判定流程展示数据(Array.isArray(result.decisionTrace) ? result.decisionTrace : [], logs, context),
            ...构建结算链侧写条目(logs, { ...context, eventLedger: result?.eventLedger || [] }),
          ], 原始战报);
          const 状态结算文本 = 审计条目
            .filter(item => item?.type === 'settlement' && String(item.kind || '') === 'status_tick')
            .map(item => `第${Number(item.round || item.回合 || 0)}回合：${String(item.text || '').trim()}`)
            .filter(Boolean);
          const 战报Blocks = 原始战报Blocks
            .filter(item => !状态结算文本.includes(序列化公开战报Blocks(item?.blocks)))
            .concat(构建行动链补缺公开战报Blocks(审计条目, 原始战报Blocks))
            .map(归一公开战报Block条目)
            .filter(Boolean)
            .sort((left, right) => Number(left?.round || 0) - Number(right?.round || 0));
          const 战报 = 战报Blocks.map(item => 序列化公开战报Blocks(item?.blocks)).filter(Boolean);
          const 流程HTML = 渲染分回合判定流程(审计条目);
          const 回合速览 = 构建回合速览数据(result, context);
          const 战报展示上下文 = { ...context, combatData: context?.combatData || result?.combatData || {} };
          const 战报单位上下文 = 读取战报上下文单位(战报展示上下文);
          战报展示上下文.units = [
            ...战报单位上下文.playerUnits,
            ...战报单位上下文.enemyUnits,
            ...(Array.isArray(context?.units) ? context.units : []),
          ];
          node.hidden = false;
          node.innerHTML = `
            <div class="battle-preview-head">
              <span>${activeTab === 'preview' ? '预演结果' : '实战结果'}</span>
              <b>${htmlEscapeText(格式化战斗模式显示文本(result.modeLabel, result.battleMode, result.mode))}</b>
              <em>${htmlEscapeText(`推进${Math.max(0, Number(result.roundsExecuted || 0))}回合`)}</em>
            </div>
            ${渲染回合速览HTML(回合速览)}
            <div class="battle-preview-report">
              ${战报Blocks.length ? 战报Blocks.map(item => `<p>${渲染公开战报BlocksHTML(item.blocks, 战报展示上下文).html}</p>`).join('') : '<p>暂无战报</p>'}
            </div>
            <details class="battle-preview-trace">
              <summary>判定流程</summary>
              ${流程HTML}
            </details>
          `;
          node.querySelectorAll('[data-battle-report-skill]').forEach(button => {
            const skillName = String(button.getAttribute('data-skill-name') || '').trim();
            const actorName = String(button.getAttribute('data-actor-name') || '').trim();
            const skillSlot = String(button.getAttribute('data-skill-slot') || '').trim();
            const sourceContext = { ...context, combatData: context?.combatData || result?.combatData || {} };
            const actorUnit = actorName ? 查找战报上下文单位(sourceContext, actorName) : null;
            const resolvedSkill = actorUnit ? (按名称解析角色战斗魂技_战报(actorUnit, skillSlot || skillName) || 按名称解析角色战斗魂技_战报(actorUnit, skillName)) : null;
            if (!resolvedSkill) return;
            const action = {
              id: `report_hover_${String(actorName || 'unknown').replace(/[^\w\u4e00-\u9fa5-]+/g, '_')}_${String(skillName || '').replace(/[^\w\u4e00-\u9fa5-]+/g, '_')}`,
              type: 'skill',
              action_type: '释放魂技',
              name: normalizeBattleActionDisplayName(resolvedSkill?.name || resolvedSkill?.魂技名 || skillName),
              category: '魂技',
              source_detail: String(resolvedSkill?.__战斗来源明细 || resolvedSkill?.__战斗来源类别 || '魂技').trim() || '魂技',
              cast_time: findUiSkillCastTime(resolvedSkill),
              cost_text: findUiSkillCost(resolvedSkill),
              raw_skill: resolvedSkill,
              skill: resolvedSkill,
            };
            button.addEventListener('mouseenter', () => 显示技能悬浮(button, action));
            button.addEventListener('focus', () => 显示技能悬浮(button, action));
            button.addEventListener('mouseleave', () => {
              const 悬浮节点 = 读取技能悬浮节点();
              if (!悬浮节点) return;
              if (!悬浮节点.matches(':hover')) 关闭技能悬浮();
            });
            button.addEventListener('blur', 关闭技能悬浮);
          });
        }

        function 渲染战斗预演面板(result = null) {
          if (result && window.BattleUI?.state) window.BattleUI.state.previewResult = result;
          if (window.BattleUI?.state) window.BattleUI.state.activeBattleRecordTab = 'preview';
          设置战斗记录展开状态(true);
          渲染战斗记录面板();
        }

        function renderSoulTowerSettlementPanel(pendingSettlement = null) {
          const node = byId('ui-tower-settlement');
          const arbitrateBtn = byId('ui-arbitrate');
          const previewBtn = byId('ui-battle-preview');
          const intentModeInput = byId('ui-intent-mode');
          const modeControls = Array.from(document.querySelectorAll('#ui-mode-group [data-mode], #ui-mode-group [data-dropdown-trigger]'));
          if (!node) return;
          if (!pendingSettlement) {
            node.hidden = true;
            node.innerHTML = '';
            if (arbitrateBtn) arbitrateBtn.disabled = false;
            if (previewBtn) previewBtn.disabled = false;
            if (intentModeInput) intentModeInput.disabled = false;
            modeControls.forEach(btn => {
              btn.disabled = false;
            });
            return;
          }
          node.hidden = false;
          if (arbitrateBtn) arbitrateBtn.disabled = true;
          if (previewBtn) previewBtn.disabled = true;
          if (intentModeInput) intentModeInput.disabled = true;
          modeControls.forEach(btn => {
            btn.disabled = true;
          });
          const spiritText = buildSoulTowerDiscountSpiritDisplay(pendingSettlement.五折魂灵);
          const continueButton = pendingSettlement.可继续
            ? `<button class="ghost-btn tower-settlement-action" type="button" data-tower-settlement="continue">继续下一层</button>`
            : `<span class="tower-settlement-note">已抵达魂灵塔顶层，无法继续上冲。</span>`;
          node.innerHTML = `
            <div class="tower-settlement-card">
              <div class="tower-settlement-head">
                <b>魂灵塔通关待选择</b>
                <span>${htmlEscapeText(pendingSettlement.区域标签 || '魂灵塔')}</span>
              </div>
              <div class="tower-settlement-body">
                <span>当前可五折魂灵：${htmlEscapeText(spiritText)}</span>
                <span>选择结束将保留该资格；选择继续会立即放弃它并进入第${htmlEscapeText(pendingSettlement.下一层 || pendingSettlement.层数 + 1)}层。</span>
              </div>
              <div class="tower-settlement-actions">
                <button class="ghost-btn tower-settlement-action primary" type="button" data-tower-settlement="end">结束并保留资格</button>
                ${continueButton}
              </div>
            </div>
          `;
          node.querySelectorAll('[data-tower-settlement]').forEach(button => {
            button.addEventListener('click', () => {
              const action = button.getAttribute('data-tower-settlement') || 'end';
              window.BattleUI?.resolveSoulTowerSettlement?.(action);
            });
          });
        }

        function resolveSoulTowerSettlement(action = 'end') {
          const choice = action === 'continue' ? 'continue' : 'end';
          const state = window.BattleUI?.state || {};
          const combatData = deepClonePlain(getUiCombatData() || state.combatData || {});
          if (!combatData || !combatData.参战者) return { ok: false, reason: 'combat_missing' };
          hydrateCombatData(combatData);
          const pendingSettlement = normalizeSoulTowerPendingSettlement(combatData.魂灵塔待结算);
          if (!pendingSettlement) return { ok: false, reason: 'tower_settlement_missing' };
          const playerName = String(combatData?.参战者?.team_player?.[0]?.name || state.player?.name || '').trim();
          if (!playerName) return { ok: false, reason: 'player_missing' };
          const currentRecord = window.BattleUIBridge?.getMVU(`char.${playerName}.魂灵塔记录`) || {};
          const nextHighestFloor = Math.max(
            Math.floor(Number(currentRecord?.最高层 || 0)),
            Math.floor(Number(pendingSettlement.层数 || 0)),
          );
          const nextTowerRecord = {
            最高层: nextHighestFloor,
            当前五折魂灵: choice === 'end'
              ? pendingSettlement.五折魂灵
              : createEmptySoulTowerDiscountSpiritRecord(),
          };
          clearCombatAdjudicationHints(combatData);
          delete combatData.魂灵塔待结算;
          combatData.本次操作 = undefined;
          combatData.前端建议结果 = undefined;
          combatData.裁断约束 = undefined;
          combatData.建议终点HP区间 = undefined;
          combatData.前端推荐终点HP = undefined;
          combatData.预计HP伤害 = undefined;
          const extraPatchOps = [
            {
              op: 'replace',
              path: `/char/${escapeJsonPointerSegment(playerName)}/魂灵塔记录`,
              value: nextTowerRecord,
            },
          ];

          if (choice === 'continue' && pendingSettlement.可继续) {
            const nextFloor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(Number(pendingSettlement.下一层 || pendingSettlement.层数 + 1))));
            const nextMeta = getSoulTowerGateMeta(nextFloor);
            combatData.floor = nextFloor;
            combatData.回合 = 0;
            combatData.进行中 = true;
            combatData.裁断结果 = '';
            combatData.阶段 = 战斗阶段枚举_V1.宣告;
            combatData.大关卡 = nextMeta.gateIndex;
            combatData.大关标签 = nextMeta.gateLabel;
            combatData.关卡范围 = nextMeta.gateRangeLabel;
            combatData.关底战 = nextMeta.isGateBoss;
            combatData.环境 = 构建魂灵塔试炼地点(nextFloor);
            combatData.试炼状态 = 构建魂灵塔试炼地点(nextFloor);
            combatData.进行中 = false;
            if (combatData.参战者 && typeof combatData.参战者 === 'object') combatData.参战者.team_enemy = [];
            const 冲塔续层位置补丁 = 构建角色位置补丁(playerName, 构建魂灵塔试炼地点(nextFloor));
            if (冲塔续层位置补丁) extraPatchOps.push(冲塔续层位置补丁);
            extraPatchOps.push({
              op: 'replace',
              path: '/sys/系统播报',
              value: `[魂灵塔] 已放弃第${pendingSettlement.层数}层的五折资格，继续挑战第${nextFloor}层。`,
            });
          } else {
            combatData.进行中 = false;
            combatData.裁断结果 = `魂灵塔第${pendingSettlement.层数}层通关`;
            combatData.试炼状态 = '';
            const 冲塔结束位置补丁 = 构建角色位置补丁(playerName, 魂灵塔退出地点);
            if (冲塔结束位置补丁) extraPatchOps.push(冲塔结束位置补丁);
            extraPatchOps.push({
              op: 'replace',
              path: '/sys/系统播报',
              value: `[魂灵塔] 已结束本次冲塔，保留当前五折目标：${buildSoulTowerDiscountSpiritDisplay(pendingSettlement.五折魂灵)}。`,
            });
          }

          const detail =
            window.BattleUIBridge?.persistCombatData?.(combatData, {
              analysis:
                choice === 'continue'
                  ? 'Frontend soul tower settlement advanced to the next floor. Apply the patched battle state and latest tower record exactly as given.'
                  : 'Frontend soul tower settlement ended the run and preserved the latest discount target. Apply the patched tower record and close the battle context.',
              extraPatchOps,
              syncHpRecoveryOnly: false,
            }) || null;
          return {
            ok: true,
            action: choice,
            floor: pendingSettlement.层数,
            nextFloor: pendingSettlement.下一层,
            delivery: detail?.delivery || null,
          };
        }

        function 构建动作不可用战斗结果(reason = '', battleMode = '') {
          const text = String(reason || '当前动作不可用，请重新选择可执行动作').trim();
          const blocks = [{
            type: 'text',
            content: text,
            projectionSource: 'action_unavailable_ast',
          }];
          return {
            ok: false,
            mode: 'action_unavailable',
            battleMode,
            message: text,
            logs: [`[释放受限] ${text}`],
            decisionTrace: [],
            publicReport: text,
            publicReportBlocks: [{
              round: 0,
              blocks,
              text,
              projectionSource: 'action_unavailable_ast',
            }],
          };
        }

        function submitBattleIntent() {
          const state = window.BattleUI?.state || {};
          if (state.pendingTowerSettlement) {
            return {
              ok: false,
              mode: 'tower_pending_choice',
              message: '魂灵塔通关后需先选择结束或继续，当前不能直接再次结算。',
            };
          }
          const battleMode = state.currentMode === 'multi_round' ? 'multi_round' : 'single_round';
          const autoContinueConfig = 读取界面自动续推设置(state);
          state.combatData.战斗意图 = state.currentIntentMode || '点到为止';
          const queue = 读取可提交战斗动作队列(state);
          if (!queue.length) {
            const selected = state.selectedSkillActions?.[state.selectedSkillActions.length - 1] || state.selectedAction;
            const reason = String(selected?.reason || '当前动作不可用，请重新选择可执行动作').trim();
            显示战斗提示(reason, 'error');
            const result = 构建动作不可用战斗结果(reason, battleMode);
            window.__battleLastIntentText = '';
            if (window.BattleUI?.state) {
              window.BattleUI.state.actualBattleResult = result;
              window.BattleUI.state.activeBattleRecordTab = 'actual';
            }
            设置战斗记录展开状态(true);
            渲染战斗记录面板();
            return result;
          }
          const intentText = buildIntentText(queue);
          const output = byId('ui-intent-output');
          if (output) output.value = intentText;
          window.__battleLastIntentText = intentText;

          let result = { intentText, mode: 'intent_only', battleMode };
          try {
              window.dispatchEvent(new CustomEvent('battle-ui-intent-submit', { detail: { intentText, battleMode, intentMode: state.currentIntentMode || '点到为止', autoContinueConfig } }));
          } catch (error) {
            console.warn('battle-ui-intent-submit dispatch failed', error);
          }

          if (typeof onPlayerAttack === 'function') {
            try {
              const engineResult = onPlayerAttack(intentText, { mode: battleMode, intentMode: state.currentIntentMode || '点到为止', autoContinueConfig }) || {};
              if (typeof syncFromBattleEngine === 'function') syncFromBattleEngine();
              result = {
                ...engineResult,
                intentText,
                mode: engineResult.mode || 'engine_arbitrated',
                battleMode,
                aiRequest: engineResult.aiRequest || window.__lastBattleAIRequest || null,
              };
            } catch (error) {
              console.error('battle arbitration failed', error);
              result = { intentText, mode: 'engine_error', battleMode, error };
            }
          } else {
            result = { intentText, mode: 'engine_unavailable', battleMode, error: 'battle_engine_unavailable' };
          }

          try {
            if (window.BattleUI?.state) {
              window.BattleUI.state.actualBattleResult = result;
              window.BattleUI.state.activeBattleRecordTab = 'actual';
            }
            设置战斗记录展开状态(true);
            渲染战斗记录面板();
            window.dispatchEvent(new CustomEvent('battle-ui-submit-finished', { detail: result }));
          } catch (error) {
            console.warn('battle-ui-submit-finished dispatch failed', error);
          }
          return result;
        }

        function previewBattleIntent() {
          const state = window.BattleUI?.state || {};
          if (state.pendingTowerSettlement) return { ok: false, mode: 'tower_pending_choice' };
          const previewBtn = byId('ui-battle-preview');
          const battleMode = state.currentMode === 'multi_round' ? 'multi_round' : 'single_round';
          const autoContinueConfig = 读取界面自动续推设置(state);
          state.combatData.战斗意图 = state.currentIntentMode || '点到为止';
          const queue = 读取可提交战斗动作队列(state);
          if (!queue.length) {
            const selected = state.selectedSkillActions?.[state.selectedSkillActions.length - 1] || state.selectedAction;
            const reason = String(selected?.reason || '当前动作不可用，请重新选择可执行动作').trim();
            显示战斗提示(reason, 'error');
            const result = 构建动作不可用战斗结果(reason, battleMode);
            window.__battleLastIntentText = '';
            if (window.BattleUI?.state) window.BattleUI.state.previewResult = result;
            渲染战斗预演面板(result);
            return result;
          }
          const intentText = buildIntentText(queue);
          const output = byId('ui-intent-output');
          if (output) output.value = intentText;
          window.__battleLastIntentText = intentText;
          if (previewBtn) previewBtn.disabled = true;
          try {
            const result = onPlayerAttack(intentText, {
              mode: battleMode,
              intentMode: state.currentIntentMode || '点到为止',
              dryRun: true,
              combatData: state.combatData,
              autoContinueConfig,
            });
            window.BattleUI.state.previewResult = result;
            window.__LWCS_LAST_BATTLE_PREVIEW__ = result;
            渲染战斗预演面板(result);
            return result;
          } catch (error) {
            console.error('battle preview failed', error);
            const result = { preview: true, mode: 'engine_error', battleMode, error, logs: [`[预演失败] ${error?.message || error}`], decisionTrace: [] };
            window.BattleUI.state.previewResult = result;
            渲染战斗预演面板(result);
            return result;
          } finally {
            if (previewBtn) previewBtn.disabled = false;
          }
        }

        function bindUIEvents() {
          初始化战斗下拉控件();
          document.querySelectorAll('#ui-mode-group [data-mode]').forEach(btn => {
            if (btn.__battleModeBound) return;
            btn.addEventListener('click', () => {
              setUiBattleMode(btn.dataset.mode);
            });
            btn.__battleModeBound = true;
          });
          document.querySelectorAll('#ui-battle-submit-mode-group [data-submit-mode]').forEach(btn => {
            if (btn.__battleSubmitModeBound) return;
            btn.addEventListener('click', () => {
              window.BattleUIBridge?.setBattleSubmitMode?.(btn.dataset.submitMode || 'manual');
            });
            btn.__battleSubmitModeBound = true;
          });
          document.querySelectorAll('#ui-intent-mode-select [data-intent-mode]').forEach(btn => {
            if (btn.__battleIntentOptionBound) return;
            btn.addEventListener('click', () => {
              setUiIntentMode(btn.getAttribute('data-intent-mode') || '点到为止');
            });
            btn.__battleIntentOptionBound = true;
          });
          同步战斗提交模式控件();

          const intentModeInput = byId('ui-intent-mode');
          if (intentModeInput && !intentModeInput.__battleIntentBound) {
            intentModeInput.addEventListener('change', () => {
              setUiIntentMode(intentModeInput.value || '点到为止');
            });
            intentModeInput.__battleIntentBound = true;
          }

          const arbitrateBtn = byId('ui-arbitrate');
          if (arbitrateBtn && !arbitrateBtn.__battleSubmitBound) {
            arbitrateBtn.addEventListener('click', submitBattleIntent);
            arbitrateBtn.__battleSubmitBound = true;
          }
          const previewBtn = byId('ui-battle-preview');
          if (previewBtn && !previewBtn.__battlePreviewBound) {
            previewBtn.addEventListener('click', previewBattleIntent);
            previewBtn.__battlePreviewBound = true;
          }
          读取战斗记录终端节点()?.querySelectorAll('[data-battle-record-tab]').forEach(button => {
            if (button.__battleRecordTabBound) return;
            button.addEventListener('click', () => {
              设置战斗记录页签(button.getAttribute('data-battle-record-tab') || 'actual');
            });
            button.__battleRecordTabBound = true;
          });
          const recordToggle = 读取战斗记录终端节点()?.querySelector('#ui-battle-record-toggle');
          if (recordToggle && !recordToggle.__battleRecordToggleBound) {
            recordToggle.addEventListener('click', () => {
              const collapsed = window.BattleUI?.state?.battleRecordCollapsed !== false;
              设置战斗记录展开状态(collapsed);
            });
            recordToggle.__battleRecordToggleBound = true;
          }

          const closeBtn = byId('ui-battle-close');
          if (closeBtn && !closeBtn.__battleCloseBound) {
            closeBtn.addEventListener('click', () => {
              window.dispatchEvent(new CustomEvent('battle-ui-close-request', { detail: { source: 'battle_ui' } }));
            });
            closeBtn.__battleCloseBound = true;
          }
        }

        const originalInit = typeof initBattleUiFromMvu === 'function' ? initBattleUiFromMvu : null;
        if (originalInit) {
          initBattleUiFromMvu = async function () {
            await originalInit();
            bindUIEvents();
          window.BattleUI = Object.assign(window.BattleUI || {}, {
              buildIntentText,
              submitBattleIntent,
              previewBattleIntent,
              读取可提交战斗动作队列,
              resolveSoulTowerSettlement,
            });
          };
        }

        initBattleUiFromMvu();
      }
}

window.BattleUIComponent = BattleUIComponent;
window.mountBattleUI = function(containerElement, snapshot, options = {}) {
  return new BattleUIComponent(containerElement, snapshot, options);
};

// 调试钩子: 在浏览器控制台执行 __LWCS_DUMP_BATTLE__() 即可拿到上一次暗箱演算的完整快照与战报。
// 返回值: { 时间戳, 模式, 模式标签, 战斗类型, 回合数, 起始回合, 结束回合, 阶段, 参战者, 裁断结果, 压制战裁断, 战报, patchOps数 } 或 null。
window.__LWCS_DUMP_BATTLE__ = function () {
  return window.__LWCS_LAST_BATTLE_SNAPSHOT__ || null;
};

window.__LWCS_EXPORT_BATTLE_RECORD_VISIBLE_TEXT__ = function (result = null, activeTab = 'preview') {
  const impl = window.__LWCS_BATTLE_EXPORT_VISIBLE_TEXT_IMPL__;
  return typeof impl === 'function' ? impl(result, activeTab) : '';
};

window.__LWCS_RENDER_BATTLE_REPORT_HTML__ = function (line = '', context = {}) {
  try {
    const impl = window.__LWCS_RENDER_BATTLE_REPORT_HTML_IMPL__;
    return typeof impl === 'function' ? impl(line, context) : { html: String(line || ''), changed: false };
  } catch (_) {
    return { html: String(line || ''), changed: false };
  }
};

window.__LWCS_RENDER_BATTLE_REPORT_BLOCKS_HTML__ = function (blocks = [], context = {}) {
  try {
    const impl = window.__LWCS_RENDER_BATTLE_REPORT_BLOCKS_HTML_IMPL__;
    return typeof impl === 'function' ? impl(blocks, context) : { html: '', changed: false };
  } catch (_) {
    return { html: '', changed: false };
  }
};

window.__LWCS_BUILD_PUBLIC_REPORT_BLOCKS__ = function (eventLedger = [], limit = 8, context = {}) {
  try {
    const impl = window.__LWCS_BUILD_PUBLIC_REPORT_BLOCKS_IMPL__;
    return typeof impl === 'function' ? impl(eventLedger, limit, context) : [];
  } catch (_) {
    return [];
  }
};

window.__LWCS_BUILD_BATTLE_ROUND_DASHBOARD__ = function (result = null, context = {}) {
  try {
    const impl = window.__LWCS_BUILD_BATTLE_ROUND_DASHBOARD_IMPL__;
    return typeof impl === 'function' ? impl(result, context) : [];
  } catch (_) {
    return [];
  }
};

window.__LWCS_RENDER_BATTLE_ROUND_DASHBOARD__ = function (rows = []) {
  try {
    const impl = window.__LWCS_RENDER_BATTLE_ROUND_DASHBOARD_IMPL__;
    return typeof impl === 'function' ? impl(rows) : '';
  } catch (_) {
    return '';
  }
};

window.__LWCS_RENDER_BATTLE_RESOLUTION_TRACE_HTML__ = function (trace = [], decisionTrace = []) {
  try {
    const impl = window.__LWCS_RENDER_BATTLE_RESOLUTION_TRACE_HTML_IMPL__;
    return typeof impl === 'function' ? impl(trace, decisionTrace) : '';
  } catch (_) {
    return '';
  }
};

window.__LWCS_RENDER_BATTLE_DECISION_ROWS_HTML__ = function (rows = []) {
  try {
    const impl = window.__LWCS_RENDER_BATTLE_DECISION_ROWS_HTML_IMPL__;
    return typeof impl === 'function' ? impl(rows) : '';
  } catch (_) {
    return '';
  }
};

if (typeof 运行战斗回归夹具 === 'function') {
  window.__LWCS_RUN_BATTLE_REGRESSION_FIXTURE_BATCH__ = function (名称 = '') {
    return 运行战斗回归夹具(名称);
  };
}

if (typeof 生成战斗判定样本文本 === 'function') {
  window.__LWCS_GENERATE_BATTLE_DECISION_SAMPLES__ = function (数量 = 100) {
    return 生成战斗判定样本文本(数量);
  };
}

if (typeof 生成战斗判定样本结果 === 'function') {
  window.__LWCS_DEBUG_BATTLE_SAMPLE_RESULT__ = function (索引 = 1) {
    return 生成战斗判定样本结果(索引);
  };
}

if (typeof 生成可信战术侧写调试结果 === 'function') {
  window.__LWCS_DEBUG_BATTLE_TRUSTED_TACTICAL_NARRATION_RESULT__ = function () {
    return 生成可信战术侧写调试结果();
  };
}

if (typeof 生成直攻压制战术侧写调试结果 === 'function') {
  window.__LWCS_DEBUG_BATTLE_DIRECT_PRESSURE_TACTICAL_NARRATION_RESULT__ = function () {
    return 生成直攻压制战术侧写调试结果();
  };
}

if (typeof 生成斩杀战术侧写调试结果 === 'function') {
  window.__LWCS_DEBUG_BATTLE_LETHAL_TACTICAL_NARRATION_RESULT__ = function () {
    return 生成斩杀战术侧写调试结果();
  };
}

if (typeof 生成资源压力战术侧写调试结果 === 'function') {
  window.__LWCS_DEBUG_BATTLE_RESOURCE_PRESSURE_TACTICAL_NARRATION_RESULT__ = function () {
    return 生成资源压力战术侧写调试结果();
  };
}

if (typeof 生成保护协同战术侧写调试结果 === 'function') {
  window.__LWCS_DEBUG_BATTLE_PROTECT_ALLY_TACTICAL_NARRATION_RESULT__ = function () {
    return 生成保护协同战术侧写调试结果();
  };
}

if (typeof 生成同分破平战术侧写调试结果 === 'function') {
  window.__LWCS_DEBUG_BATTLE_TIE_BREAK_TACTICAL_NARRATION_RESULT__ = function () {
    return 生成同分破平战术侧写调试结果();
  };
}

})();
