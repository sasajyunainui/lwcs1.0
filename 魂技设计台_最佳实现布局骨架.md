# 魂技设计台最佳实现布局骨架

> 版本：2026-07-16
> 用途：作为 Vue 魂技设计台实现前的布局与交互骨架基线。
> 范围：只定义页面空间组织、区域顺序、信息层级和状态位置，不定义具体 CSS 实现。

## 1. 总体决策

魂技设计台的核心任务是：

> 让玩家在窄宿主中，以最少的来回滚动完成一个合法魂技，并随时知道当前正在编辑什么、哪里有问题、保存后会得到什么。

最终采用：

```text
顶部技能上下文
        ↓
四页任务导航
        ↓
当前页全宽编辑画布
        ↓
底部全局状态底座
```

固定原则：

- 保留四个页签：`基础`、`效果`、`消耗`、`描述`。
- 设计台只有一个滚动上下文。
- 当前页使用全宽编辑画布。
- 不使用左侧大纲。
- 不使用 60/40 双栏。
- 不使用右侧常驻大预览。
- 不使用页面内部独立滚动区域。
- 复杂结构通过摘要、关系标签、平铺句法和局部连接线表达。
- 底部状态底座只负责全局状态，不承担完整预览。

## 2. 843×608px 总骨架

以下线框按典型窄宿主表达。`[固定]` 表示随设计台保持可见，`[滚动]` 表示随当前页画布滚动。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ [固定] 技能上下文                                                             │
│  ← 技能设计台     [当前技能：第一魂技 / 酱香牛肉 ▾]     [重新读取] [保存设计] │
│  类型：自创魂技       当前状态：有未保存更改                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ [固定] 任务导航                                                               │
│  基础          效果          消耗          描述                                │
│  ─────         ─────         ─────         ─────                               │
│  当前页高亮    错误数显示    警告/错误数    完成/错误数                         │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│ [滚动] 当前页全宽编辑画布                                                     │
│                                                                              │
│  页面标题区                                                                  │
│  当前任务说明 / 当前页完成判断 / 局部状态                                     │
│                                                                              │
│  页面主体内容                                                                │
│  字段组、原型、账单或文本编辑区                                               │
│                                                                              │
│  页面底部安全留白                                                             │
│  防止最后一个控件被底部状态底座遮挡                                           │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│ [固定] 全局状态底座                                                           │
│  校验通过  │  复杂度预算 18.4 / 30.0  │  无警告  │  有未保存更改  │ 保存设计 │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 2.1 垂直层级

```text
技能上下文栏       识别当前技能、切换技能、读取、保存
任务导航栏         识别当前任务、切换页面、显示错误数量
页面标题区         说明当前页面目标和完成判断
页面编辑区         当前任务的唯一主要工作区
局部反馈区         当前字段、原型或账单的即时反馈
页面底部留白       给固定底座预留安全区域
全局状态底座       总 COST、预算、校验状态、保存入口
```

页面主体不再放一个跨页面复用的“大而全实时速览”。各页只在需要的位置放局部反馈。

## 3. 基础页骨架

基础页的任务是建立技能身份、承载方式和依赖关系。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 基础                                                                         │
│ 先确定技能是什么、以什么方式存在，以及哪些附加参数会因此出现。               │
│ 状态：未修改 / 有未保存更改 / 存在错误                                      │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│ 技能身份                                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ 魂技名称 *                                                                   │
│ [                                                                            ] │
│                                                                              │
│ 承载方式 *                                                                   │
│ [ 直接生效                                                   ▾ ]             │
│                                                                              │
│ 施放规则                                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ [ 开关 ] 设为被动技能       使用限制                                         │
│                            [ 无限制                              ▾ ]         │
│                                                                              │
│ 条件化载体参数（仅在相关承载方式下出现）                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ 数量                              有效时长                                   │
│ [                         ]       [                         ]                 │
│                                                                              │
│ 高级载体设置 [展开]                                                          │
│ └─ 融合对象、其他由 editor model 判定的条件字段                             │
│                                                                              │
│ 局部反馈                                                                     │
│ 载体切换后：已显示 2 个新字段；1 个字段已清理，请撤销或继续。                │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│ 校验通过 │ 复杂度预算 待评估 │ 有未保存更改 │ 保存设计                      │
└──────────────────────────────────────────────────────────────────────────────┘
```

布局规则：

- 技能名称单独占一行，作为首个视觉焦点。
- 承载方式单独占一行，避免与长标签挤在同一行。
- 短字段只有在宿主宽度足够时才并排。
- 依赖字段出现时使用明确的“因承载方式而显示”提示。
- 无效字段清理必须提供可理解的反馈和一次撤销。
- 不添加未经代码确认的“技能定位”等新业务字段。

## 4. 效果页骨架

效果页的任务是搭建主效果、条件分支、追加/替换效果和副作用。

### 4.1 常态：两个原型

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 效果                                                                         │
│ 先建立主效果，再按需要添加条件、追加或替换效果。                             │
│ [新增原型] [折叠全部] [仅展开错误]                      一次撤销：可用         │
├──────────────────────────────────────────────────────────────────────────────┤
│ 原型 1   主效果       [伤害结算 ▾]          [折叠] [复制] [删除]              │
│ 摘要：对目标造成一次伤害                                                     │
│                                                                              │
│ 目标                         数值                                             │
│ [ 单体 ▾ ]                   [ 120 ]                                          │
│                                                                              │
│ 如果 [添加条件]，那么 [添加满足后效果]                                       │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│ 原型 2   主效果       [状态施加 ▾]          [上移] [删除]                    │
│ 摘要：对目标施加灼烧                                                         │
│                                                                              │
│ 目标                         状态                                             │
│ [ 单体 ▾ ]                   [ 灼烧 ▾ ]                                       │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│ 局部反馈：当前原型产生的复杂度增量 +3.2                                      │
│ 局部文案：对目标造成伤害，并附加灼烧效果。                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ 校验通过 │ 复杂度预算 18.4 / 30.0 │ 保存设计                                  │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 复杂态：条件、追加和替换

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 原型 1   主效果       [资源变化 ▾]                                            │
│ 摘要：根据目标状态改变魂力                                                   │
│                                                                              │
│ 如果  [目标状态] [等于] [濒危]                                               │
│ 那么  [追加效果] [资源变化：魂力 +20%]                                       │
│ 否则  [替换效果] [资源变化：魂力 +5%]                                        │
│                                                                              │
│ 条件 2  [使用者状态] [包含] [狂暴]                                           │
│ 满足后 [追加效果] [属性修正：力量 +10%]                                      │
│                                                                              │
│ 嵌套效果                                                                       │
│ ├─ 追加：状态施加                                                            │
│ └─ 替换：资源变化                                                            │
│                                                                              │
│ [添加条件分支]  已达到 3 个分支上限                                          │
│                                                                              │
│ 局部错误：条件值不能为空                                                     │
│ 局部反馈：点击可定位到“条件 2 / 条件值”。                                   │
└──────────────────────────────────────────────────────────────────────────────┘
```

复杂效果表达规则：

- 使用中文句法，不在界面展示 `IF`、`THEN`、`DOM Tree`、`Inspector` 等开发术语。
- 条件最多显示为平铺的两行逻辑句。
- 嵌套关系使用单条连接线和关系标签，不使用多层完整边框。
- 原型默认显示摘要，当前原型、错误原型和定位目标自动展开。
- 每个原型最多 3 个条件分支。
- 嵌套效果最多 2 层。
- 达到限制时显示简短原因，新增按钮禁用。

## 5. 消耗页骨架

消耗页是唯一完整展示 COST 来源和算式的页面。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 消耗                                                                         │
│ 调整资源和时间参数，并核对复杂度预算的完整组成。                             │
│ 顶部摘要：总 COST 18.4 / 预算 30.0 / 结余 11.6                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ 资源代价                                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ 资源类型                  启动资源                  维持资源                   │
│ [ 魂力 ▾ ]                [ 120 ]                   [ 0 ]                      │
│                                                                              │
│ 时间与成长                                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ 施放前摇                  有效时长                  掌控度成长                 │
│ [ 2 tick ]                [ 3 回合 ]                [ 随等级提升 ▾ ]            │
│                                                                              │
│ 附带属性                                                                     │
│ [ 精神 ] [ 韧性 ] [ 破甲 ]                                                    │
│                                                                              │
│ COST 结算明细                                                                 │
│ ──────────────────────────────────────────────────────────────────────────── │
│ 来源                     计算说明                                      小计  │
│ 基础承载                 基础值 × 承载系数                              8.0   │
│ 效果 1                   主效果 + 条件分支                              6.4   │
│ 效果 2                   状态价值 × 持续修正                            5.2   │
│ 资源调整                 启动资源带来的减免                            -1.2   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ 总计                     18.4 / 预算 30.0                             结余 11.6 │
│                                                                              │
│ [来源项可点击，跳转到对应页签和字段]                                         │
├──────────────────────────────────────────────────────────────────────────────┤
│ 校验通过 │ 复杂度预算 18.4 / 30.0 │ 无警告 │ 保存设计                        │
└──────────────────────────────────────────────────────────────────────────────┘
```

预算超限态：

```text
│ 总计                     39.8 / 预算 30.0                         超出 9.8   │
│                                                                              │
│ 问题：效果 2 产生的复杂度最高。                                             │
│ [定位到效果 2]                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ 存在错误 │ 复杂度预算 39.8 / 30.0 │ 保存设计（禁用）                        │
└──────────────────────────────────────────────────────────────────────────────┘
```

注意：

- 具体算式和补偿规则必须来自编译结果，不由 UI 自行推断。
- 启动资源、维持资源、前摇和有效时长的字段归属必须由 editor model 唯一决定。
- 输入中间态不立即改写原始输入，也不在用户退格时立刻报错。
- 预算超限必须同时在账单总计、底部状态底座和具体来源项体现。

## 6. 描述页骨架

描述页只负责最终文本确认，不重复完整 COST 账单。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 描述                                                                         │
│ 将技能效果整理成玩家最终看到的画面描述和效果描述。                           │
├──────────────────────────────────────────────────────────────────────────────┤
│ 自动生成参考                                                                 │
│ ──────────────────────────────────────────────────────────────────────────── │
│ 对目标造成伤害，并根据目标状态追加对应效果。                                 │
│ [使用自动生成]                                                               │
│                                                                              │
│ 画面描述                                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ [                                                                            ] │
│ [                                                                            ] │
│                                                                              │
│ 效果描述                                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ [                                                                            ] │
│ [                                                                            ] │
│                                                                              │
│ 功法专属进度（仅相关类型显示）                                               │
│ ──────────────────────────────────────────────────────────────────────────── │
│ [字段由 editor model 条件显示]                                               │
│                                                                              │
│ 手动覆盖状态：当前内容已脱离自动生成结果                                     │
│ [恢复自动生成]                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ 校验通过 │ 复杂度预算 18.4 / 30.0 │ 有未保存更改 │ 保存设计                  │
└──────────────────────────────────────────────────────────────────────────────┘
```

文本规则：

- 自动生成内容是只读参考，不直接覆盖用户手动输入。
- 手动编辑后显示“已手动修改”。
- 恢复自动生成前必须说明将替换当前手动内容。
- 文本区域允许自然增高，但不得制造独立滚动容器。
- 描述页的主要视觉焦点是最终文本，不是装饰性卡片。

## 7. 响应式降级

### 宿主宽度不低于 960px

```text
顶部上下文
任务导航
全宽编辑画布
底部状态底座
```

字段组可以使用双列，但复杂条件和长文本仍使用全宽。

### 宿主宽度 620px 至 959px

```text
顶部上下文
任务导航
单列编辑画布
局部摘要插入当前区块
底部状态底座
```

短字段可以两列，长标签、条件行和账单自动改为单列。

### 宿主宽度低于 620px

```text
技能上下文
四页等宽导航
所有字段单列
关系句法分成多行
底部状态底座分两行
```

禁止出现横向滚动。固定底座必须允许换行或折叠次要信息。

## 8. 主题适配骨架

结构和布局完全一致，仅替换语义令牌：

| 主题 | 外壳气质 | 字体与边角 | 激活/关系表现 |
| --- | --- | --- | --- |
| 经典状态栏 | 青色全息边线、深蓝黑实体底板 | Noto Sans SC、小圆角 | 青色底边、直线关系 |
| 炫世唐门 | 技术终端、绿色细线 | Cascadia Code 中文回退、较小圆角 | 绿色选中线、硬朗关系线 |
| 金龙王 | 厚重装甲、暗红和金色边线 | 粗字重、直角或切角 | 金色焦点、暗红关系线 |
| 银龙王 | 冰白实体表面、浅青边线 | 轻字重、大圆角 | 冰白焦点、柔和边界 |
| 幻海蝶舞 | 青色主状态、粉色辅助状态 | 中文主题字体、柔和异形圆角 | 青/粉只用于激活和关系 |

主题适配禁止：

- 复制五套组件 DOM。
- 重新发明独立固定色板。
- 大面积渐变、玻璃透明和文字辉光。
- 用主题主色直接承载正文文字。
- 用颜色作为唯一状态提示。

## 9. 统一交互状态

```text
默认       实体控件表面，静默边线
悬停       边线和背景轻微变化，不改变尺寸
焦点       可见焦点环，返回触发控件
选中       明确底边或侧边标记
禁用       降低对比但保留可辨识边界，并说明原因
错误       字段级错误 + 页签错误数 + 底部问题状态
警告       字段级警告 + 底部警告状态
未保存     状态文字和小型标记，不依赖星号
保存中     禁止并发操作，显示正在保存
保存失败   保留草稿，显示重试和字段定位
```

## 10. 明确禁止恢复的旧结构

- 右侧固定大预览卡片。
- 60/40 编辑区与预览区。
- 左侧 20% 至 25% 大纲导航。
- 单页长画布承载四类任务。
- 顶部 COST 能量条或血条。
- 深层缩进树。
- 卡片套卡片。
- 原生 `select` 作为主要枚举控件。
- 裸 `X`、文本箭头和临时字符图标。
- 只在远端预览区显示错误。

## 11. 实现对照

现有代码中的以下边界继续保留：

- `SkillDesignerApp` 独占 `rawDraft`。
- `compileDraft()` 作为唯一编译入口。
- editor model 作为字段、控件、依赖和文案的唯一来源。
- `operationToken`、`previewToken`、草稿缓存和销毁清理机制。
- `Teleport` 组合框浮层。
- 现有 MVU 写回和剧情提交语义。

需要按本骨架重组：

- `SkillDesignerPreview` 的右侧常驻布局。
- workspace 的预览/编辑双栏结构。
- COST 预览的跨页职责。
- 主题令牌中的固定成功色、警告色和透明混合。
- 原型嵌套的视觉边框和缩进表达。

## 12. 统一美化方向

### 12.1 视觉关键词

最终观感不是“炫技面板”，而是：

```text
魂技卷轴的秩序感
+ 状态栏主题的材质
+ 数值编辑器的清晰度
+ 少量关系线和激活光
```

视觉焦点应该来自内容顺序，而不是来自大量发光、渐变或装饰卡片。

用户打开设计台时，应该先看到：

1. 当前技能名称和当前任务。
2. 当前页最重要的编辑目标。
3. 当前校验状态和总复杂度预算。
4. 下一步可以做什么。

### 12.2 表面层级

设计台只保留四个表面层级：

| 层级 | 用途 | 视觉表现 |
| --- | --- | --- |
| 外壳层 | 设计台整体背景、上下文栏 | 状态栏主题底板，实体高不透明度 |
| 编辑层 | 当前页内容和字段组 | 与外壳有轻微明度差，不使用玻璃感 |
| 控件层 | 输入、按钮、分段、标签 | 统一实体控件底色和实线边界 |
| 浮层 | Combobox、帮助说明、确认提示 | 复制当前主题语义令牌，明显高于编辑层 |

禁止出现：

- 外壳卡片套编辑卡片套字段卡片。
- 同一页面中大量相同强度的边框。
- 透明度过低导致背景穿透文字。
- 仅靠阴影区分层级。
- 内容区使用大面积渐变。

区块之间优先使用：

- 充足留白；
- 单条水平分割线；
- 标题和辅助说明的字号差；
- 左侧关系线；
- 当前状态标记。

### 12.3 字体层级

字体必须读取当前状态栏的 `--主题字体` 和 `--主题字重`，不引入与主题无关的 Google Font。

```text
设计台标题       18px，主题字重，正文主色
页面标题         16px，主题字重，正文主色
区块标题         15px，略高字重，主题主色或正文主色
字段标签         13px，正文主色或高对比弱色
输入内容         14px，正文主色
辅助说明         13px，必须达到可读对比度
错误/警告         13px，功能状态色 + 明确文字
账单数字         14-16px，使用等宽数字特性，不强制替换中文字体
```

排版要求：

- 正文行高约 1.45 至 1.6。
- 字距保持默认，不使用负字距。
- 不使用文字辉光制造层级。
- 长文本按自然换行，不截断重要内容。
- 标签不能用过浅的灰色。
- 金龙王的粗字重只用于标题、总计和关键数字，不用于所有正文。
- 银龙王的轻字重必须保留足够字面和对比度。

### 12.4 间距节奏

页面使用三档间距：

```text
紧凑间距：字段标签与控件、条件句内部
标准间距：同一字段组内的字段
分组间距：基础、效果、消耗、描述之间的主分隔
```

不使用每层都加内边距的方式表达嵌套。复杂结构越深，越应该减少边框和内缩，而不是继续增加卡片内边距。

### 12.5 控件外观

统一控件形态：

```text
高度：40-44px
背景：实体控件底色
边界：1px 实线
圆角：读取主题圆角
焦点：2px 可见焦点环
悬停：边线和底色轻微变化
禁用：保留轮廓，降低内容对比度
```

控件不允许：

- 原生 `select` 作为主要枚举控件；
- 只有一个裸字符的关闭按钮；
- 用 placeholder 代替标签；
- 点击后尺寸变化；
- hover 时整体缩放导致布局跳动；
- 只用主题高光表示错误。

控件具体视觉：

- `Combobox`：像实体输入框，不做胶囊化；右侧使用 Font Awesome 下拉图标。
- `MultiSelect`：已选项使用紧凑标签，删除按钮保持至少 40px 点击区。
- `Segmented`：整体是一个控件槽，选中项使用实色底边或内嵌色块。
- `Toggle`：保留原生 checkbox/switch 语义，并显示“启用/关闭”文字。
- `DurationInput`：日、时、分为同一组，不拆成三个独立视觉卡片。
- `Textarea`：实体底板、自然增高、无内部滚动。
- `HelpButton`：使用信息图标，不使用问号文本；浮层打开后支持 Escape 和焦点返回。

## 13. 四页的美化重点

### 13.1 基础页：像“技能身份登记”

视觉重点：

- 技能名称是首个最大字段。
- 承载方式是第二个明确决策点。
- 条件化参数出现时，用一条主题色提示线引导注意。
- 高级设置默认安静，不抢核心字段。

画面气质：

```text
上方是清楚的技能身份
中间是施放规则
下方是因承载方式出现的附加参数
每个区域之间有足够留白，不靠厚重卡片分组
```

### 13.2 效果页：像“平铺的魂技编排稿”

视觉重点：

- 当前正在编辑的原型使用一条主题关系线和轻微表面提亮。
- 折叠原型只显示编号、关系、原型名称和摘要。
- 条件使用“如果 / 那么 / 否则”的自然语言句法。
- 删除、复制、排序操作放在标题栏右侧，不和字段混在一起。
- 错误原型使用独立错误边线，并在摘要中显示错误数量。

画面气质：

```text
不是一摞卡片
而是一张有编号、有关系、有局部缩进的编排稿
用户先读懂效果链，再进入字段编辑
```

### 13.3 消耗页：像“透明的数值账单”

视觉重点：

- 总 COST、预算和差额使用最大数字层级。
- 资源输入在上，完整账单在下。
- 账单来源左对齐，公式居中，数字右对齐。
- 总计行通过上边线、字重和状态文字突出，不依赖大色块。
- 超限行只强调真正造成问题的来源项。

禁止：

- 血条、能量条、圆环图；
- 大面积红色背景；
- 公式使用过低对比度；
- 把示例数字误当成业务固定值。

### 13.4 描述页：像“最终技能卷面”

视觉重点：

- 自动生成参考区域视觉权重低于手动编辑区。
- 手动编辑区拥有最完整的阅读宽度和留白。
- 手动覆盖状态使用文字提示和小型状态标记。
- 保存前用户能看到最终实际保存的文本，而不是只看到系统生成结果。

画面气质：

```text
上方参考
中间编辑
下方确认
没有重复预览卡片，也没有大面积装饰性背景
```

## 14. 五套主题的具体美化方案

### 经典状态栏

- 外壳：深蓝黑实体底板，使用青色细边线建立方向。
- 控件：小圆角、实体填充、边界清楚。
- 标题：青色只用于页面标题、激活页签和关系线。
- 焦点：白色或高亮色焦点环，不能只用青色发光。
- 效果页：关系线使用直角和短线，表达清晰的科技秩序。
- 消耗页：账单横线使用青色弱化边线，不做霓虹发光。

### 炫世唐门

- 外壳：技术深蓝底，细绿色边线。
- 字体：优先使用主题等宽字体；中文字符使用状态栏已有中文回退字体。
- 控件：更小圆角和更硬朗的边界。
- 激活：绿色底边、左侧短标记或命令行式选中线。
- 效果页：关系标签可以使用短标签，但正文仍然使用自然中文。
- 消耗页：公式可使用等宽字体，表格线保持克制，不使用虚线铺满页面。

### 金龙王

- 外壳：暗红和象牙白为主体，金色只用于边线、标题焦点和关键关系。
- 控件：直角或轻切角，切角必须不影响文字和焦点环。
- 字体：标题和总计加重，普通字段保持可读字重。
- 激活：金色实线或金色底边，不使用暗红正文。
- 错误：使用厚重血红，但同时显示明确错误文案和图标。
- 效果页：关系线硬朗、短促，避免复杂装甲装饰。

### 银龙王

- 外壳：冰白和浅青实体表面，控件与背景必须有明确层次。
- 字体：轻字重只用于标题和辅助层，输入内容使用更稳定的中等字重。
- 控件：较大圆角，边线亮但不发光。
- 激活：冰白焦点和浅青底边。
- 错误：使用独立危险色，不能把错误做成低对比灰红。
- 消耗页：数字对齐优先，避免白色渐变干扰账单阅读。

### 幻海蝶舞

- 外壳：青色为主状态，粉色只作为辅助关系或危险提示。
- 控件：柔和异形圆角，但控件形状必须稳定，不能变成装饰性花瓣。
- 激活：青色表示当前任务，粉色表示辅助关系或警告。
- 效果页：关系线可以更柔和，但仍保持单线和明确起点。
- 描述页：粉色只用于手动覆盖、辅助状态或强调动作，不铺满文本背景。
- 禁止大面积青粉渐变和文字辉光。

## 15. 窄宿主与极窄状态补丁

### 15.1 620px 以下

```text
┌──────────────────────────────────────┐
│ 技能上下文 / 切换 / 保存               │
├──────────────────────────────────────┤
│ 基础 │ 效果 │ 消耗 │ 描述              │
├──────────────────────────────────────┤
│ 当前页标题与状态                      │
│                                      │
│ 标签                                  │
│ [控件                              ]   │
│                                      │
│ 如果 [条件类型]                       │
│   对象：[目标]                         │
│   比较：[等于]                         │
│   值：[输入                          ] │
│ 满足后：[追加效果]                    │
│                                      │
├──────────────────────────────────────┤
│ 校验状态  COST/预算                   │
│ [重新读取]              [保存设计]    │
└──────────────────────────────────────┘
```

关系线在此宽度只保留一条短左侧标记；不使用深层横向缩进。条件和分支改为上下文标题加扁平句法，所有输入保持整行可用宽度。

### 15.2 420px 与 375px

```text
┌──────────────────────────┐
│ 当前技能                  │
│ [切换技能              ]  │
│ [重新读取] [保存设计]     │
├──────────────────────────┤
│ 基础│效果│消耗│描述       │
├──────────────────────────┤
│ 页面标题                  │
│ 页面说明                  │
│                          │
│ 字段标签                  │
│ [控件                  ]  │
│                          │
│ 底部安全留白              │
├──────────────────────────┤
│ 状态：存在错误             │
│ COST 18.4 / 30.0          │
│ [保存设计                ] │
└──────────────────────────┘
```

底部底座固定为三行以内：第一行状态，第二行 COST/预算，第三行主操作。不得让保存按钮被文字挤出宿主。

## 16. 边缘状态线框

### 16.1 效果空状态、上限和嵌套上限

```text
效果 / 尚未建立原型
┌──────────────────────────────────────┐
│ 还没有效果原型                        │
│ 先添加一个主效果，之后再添加条件。    │
│ [新增原型]                            │
└──────────────────────────────────────┘

已达到原型上限
│ [新增原型 disabled]                   │
│ 原型数量已达到当前技能位上限。        │

已达到嵌套上限
│ [添加嵌套效果 disabled]               │
│ 嵌套最多支持 2 层。                    │
```

### 16.2 加载、保存失败、撤销

```text
加载中：当前页保留骨架占位，控件不可操作，关闭仍可用。
保存失败：字段区保留草稿；底座显示“保存失败”；首个错误页签自动打开。
撤销后：删除位置附近显示“已撤销上一次结构操作”，撤销按钮恢复 disabled。
重新读取：dirty 时先显示“重新读取确认”，确认后才替换草稿。
```

### 16.3 浮层

```text
触发按钮：[选择原型 ▾]
Teleport 到 body：
┌────────────────────────────┐
│ 搜索原型                    │
├────────────────────────────┤
│ 战斗内                      │
│ 伤害结算                    │
│ 目标当前生命值的伤害结算    │
└────────────────────────────┘
```

打开、搜索无结果、方向键选中和 Escape 返回触发按钮均必须可截图验收。

## 17. 逐页签核门槛

| 页面 | 设计冻结门槛 | 截图验收 |
| --- | --- | --- |
| 基础 | 确认承载方式、有效时长和融合字段唯一归属 | 843/620/400/375，默认、错误、切换清理 |
| 效果 | 三条件、追加/替换、两层嵌套的扁平句法 | 空状态、复杂状态、上限、删除撤销 |
| 消耗 | 账单来源来自编译结果，数字列使用 `tabular-nums` | 正常、警告、超限、窄屏、经典/金龙王 |
| 描述 | 自动文案来源和手动覆盖状态来自现有结构 | 自动/手动、恢复确认、1000 字文本 |

## 18. 美化验收图样

每个页面至少需要准备以下状态截图或夹具状态：

```text
默认空状态
正常已填状态
字段获得焦点
Combobox 打开
MultiSelect 有多个标签
复杂效果展开
错误字段
警告字段
未保存
正在保存
保存失败
恢复草稿
窄宽度单列
五套主题各一张代表图
```

验收时优先检查：

- 第一眼是否知道当前页和当前技能。
- 主字段是否比装饰更突出。
- 错误是否在字段附近出现。
- 底部状态底座是否遮挡内容。
- 主题变化是否只是材质变化，而不是换了一套布局。
- 文字是否被截断、重叠或压缩到不可读。
- 页面是否仍然像魂技设计台，而不是旧状态栏、后台控制台或通用表单。

## 19. 设计稿到代码映射

设计稿中的每个视觉区域只允许由一个 Vue 组件负责：

| 设计稿区域 | 唯一组件 |
| --- | --- |
| 技能上下文 | `SkillDesignerToolbar` |
| 任务导航 | `SkillDesignerTabs` |
| 页面标题区 | `SkillDesignerPageHeader` |
| 基础编辑区 | `SkillBasicPanel` |
| 效果编排区 | `SkillEffectPanel` |
| 消耗输入区 | `SkillCostPanel` |
| 消耗账单区 | `SkillCostLedger` |
| 描述编辑区 | `SkillDescriptionPanel` |
| 自动参考区 | `SkillDescriptionReference` |
| 全局状态底座 | `SkillDesignerStatusDock` |
| 组合框浮层 | `SkillCombobox` |
| 多选浮层 | `SkillMultiSelect` |

`SkillDesignerApp` 负责状态、页签切换、异步操作和区域编排；字段组件不得自行拼接跨区域布局。若某区域需要拆分，必须先更新本表，再修改模板。
