# 魂技设计台字段归属与页面映射

> 版本：2026-07-16
> 依据：`mvu_logic_bridge.js` 的 `创建技能设计台编辑模型`、草稿归一化、依赖清理和编译写回链路。
## 1. 页面唯一归属

| 页面 | 字段路径 | 控件 | 显示条件 | 业务用途 |
| --- | --- | --- | --- | --- |
| 基础 | `name` | `text` | 始终 | 技能正式名称 |
| 基础 | `deliveryForm` | `segmented` | 始终 | 承载方式；变化时清理造物字段 |
| 基础 | `使用限制周期` | `segmented` | 始终 | 使用限制周期 |
| 基础 | `使用限制次数` | `number` | 周期不是“无限制” | 使用次数上限 |
| 基础 | `启用被动` | `toggle` | 由 `技能设计台启用被动` 决定 | 被动状态 |
| 基础 | `被动触发` | `singleEnum` | 被动启用 | 被动触发时机 |
| 基础 | `被动触发周期` | `segmented` | 被动启用 | 被动次数周期 |
| 基础 | `被动触发次数` | `number` | 被动周期不是“无限制” | 被动次数上限 |
| 基础 | `constructItemCount` | `number` | `deliveryForm === 造物承载` | 造物数量 |
| 基础 | `constructDurationTick` | `duration` | `deliveryForm === 造物承载` | 造物有效时长；唯一的有效时长编辑入口 |
| 基础 | `constructDescription` | `text` | `deliveryForm === 造物承载` | 造物描述 |
| 基础 | `用法模式` | `segmented` | `previewMeta.scope === 武魂融合技` | 融合技能用法 |
| 基础 | `fusionMode` | `segmented` | `previewMeta.scope === 武魂融合技` | 自体/多人融合 |
| 基础 | `fusionPartnerKeys` | `multiEnum` | 融合模式为多人 | 合作角色 |
| 基础 | `fusionParticipants[*]` | `singleEnum`/`static` | 存在融合参与者 | 参与者角色与武魂 |
| 效果 | `prototypeEffects[*]` | `effectList` | 始终 | 一级原型、条件、追加/替换、嵌套效果 |
| 效果 | `prototypeEffects[*].条件分支[*]` | `conditionList` | 原型支持条件分支 | 条件与分支效果 |
| 效果 | `副作用列表[*]` | `effectList` | 始终 | 副作用 |
| 消耗 | `costType` | `singleEnum` | 始终 | 启动/维持资源类型 |
| 消耗 | `costValues[资源]` | `numberOrPercent` | 资源类型包含该资源 | 启动资源 |
| 消耗 | `sustainCostValues[资源]` | `numberOrPercent` | 资源类型包含该资源 | 维持资源 |
| 消耗 | `前摇` | `number` | 始终 | 正式技能前摇 tick；唯一的施放前摇编辑入口 |
| 消耗 | `启用技能掌控度` | `toggle` | 由技能掌控度判定决定 | 是否启用掌控度 |
| 消耗 | `技能掌控度中心等级` | `number` | 掌控度启用 | 掌控度中心等级 |
| 消耗 | `技能掌控度圆满等级` | `number` | 掌控度启用 | 掌控度圆满等级 |
| 消耗 | `attachedAttributes` | `multiEnum` | 始终 | 附带属性 |
| 消耗 | 编译结果 `budget` | `static` | 编译后 | 唯一 COST/预算来源 |
| 描述 | `visualDesc` | `textarea` | 始终 | 画面描述 |
| 描述 | `effectDesc` | `textarea` | 始终 | 效果描述 |
| 描述 | `artStage` | `singleEnum` | `scope === art` 且名称为“紫极魔瞳” | 真实存在的功法境界 |
| 描述 | `artLevel` | `number` | `scope === art` 且名称为“紫极魔瞳” | 真实存在的功法等级 |

## 2. 依赖与清理

- `deliveryForm` 改为非“造物承载”时，删除 `constructItemCount`、`constructDurationTick`、`constructDescription`。
- `使用限制周期 === 无限制` 时，删除 `使用限制次数`。
- 被动关闭时，删除 `被动触发`、`被动触发周期`、`被动触发次数`。
- 被动触发周期为“无限制”时，删除 `被动触发次数`。
- `costType` 变化时，只保留当前资源类型对应的 `costValues` 和 `sustainCostValues`。
- 掌控度关闭时，删除掌控度对象及其等级字段。
- 融合模式和合作角色变化时，通过现有参与者派生函数重建 `fusionParticipants`、`fusionPartner`、`fusionSourceSpirits`。
- 原型选择、条件类型、处理方式等依赖字段由 `applyDependentFieldChange` 清理失效值。

## 3. 编译与文案边界

- `compileDraft(rawDraft, { dryRun })` 是预览、校验和保存的唯一编译入口。
- UI 不计算 COST、预算减免或效果价值；消耗页只展示编译结果。
- 普通魂技、自创魂技的正式消耗由现有编译链写最终绝对值；武魂融合技/武魂真身继续保留运行时比例语义。
- 自动描述只能来自现有编译预览或现有业务翻译函数，不新增持久字段。
- “有效时长”不在消耗页重复编辑；“施放前摇”不在基础页重复编辑。
- 不新增“低消耗换 COST 减免”、旧字段兼容、迁移字段或未在当前模型存在的功法进度。
